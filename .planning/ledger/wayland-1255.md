---
issue: 1255
repo: FerroxLabs/wayland
kind: defect
title: "The accumulated-tool-result pass leaves one 130-byte stub per tool call, so carried bytes grow without bound and cross a 32k window at ~238 calls"
status: open
last_verified_commit: e58300bdf
criteria:
  - id: c1
    text: "The bytes bound_accumulated_tool_results leaves behind are bounded in the number of tool calls, or the prompt-cache cost of bounding them is measured and the tradeoff recorded as a decision"
    state: met
    evidence: "symbol:crates/wcore-agent/src/compact/micro.rs::fold_bounded_tool_rounds"
    owner: core
    note: "Filed 2026-08-30 by lane w2-window-arc while answering the adversarial refutation of FerroxLabs/wayland#1200. THE MEASUREMENT (unchanged, and still the right one): bound_accumulated_tool_results replaces each over-budget tool result with a stub and never re-mutates a stub -- that monotonicity is exactly what makes the pass prompt-cache safe -- so the bytes it leaves are `protected_tail + dropped x stub_len` with stub_len = 130, and the second term is unbounded in the number of tool calls. Driving the real pass with Some(32_768): n=20 -> 52,470B; n=100 -> 62,870B; n=500 -> 114,870B; n=2000 -> 309,870B = 77,467 tok against a 20,208-token ceiling; crossing at about 238 calls. CLOSED 2026-08-31 by lane f13-ctx-1255 at e58300bdf, ON THE FIRST DISJUNCT (the bytes ARE now bounded in the number of tool calls), with the second discharged alongside rather than instead. WHY NOT THE ARITHMETIC PIN. The disjunction reads `bounded ... OR the prompt-cache cost of BOUNDING them is measured and the tradeoff recorded as a decision`. Q-1255 in .planning/DECISIONS.md recorded the tradeoff but says in its own words that `the cost of that rewrite has not been measured`, so the second disjunct was not satisfied either -- the pin discharged neither arm as written. And c2 asks for the OPPOSITE POLARITY of the pin (the same call asserting the payload FITS at a session past the crossing), which no statement about the pass can satisfy however well measured. Discharging c1 by the pin would have left c2 permanently unmeetable while the ticket stayed nominally answered. So the behaviour changed. THE FIX: fold_bounded_tool_rounds, run at the END of the same pass. When the stub RESIDUE (the stub bytes outside the protected tail) exceeds total_budget_bytes, every foldable tool ROUND is elided down to one aggregate. A round is elidable only when an assistant message of nothing but ToolUse blocks is answered by the very next message, a user message of nothing but the matching ToolResult blocks, EVERY ONE ALREADY STUBBED -- so a fold can only ever remove bodies this pass had already emptied, never live tool output, and it removes ToolUse and ToolResult TOGETHER so it cannot orphan either. A message carrying text or thinking is never elided. WHY NOT A SMALLER CONSTANT: the ticket already says shortening bounded_result_stub moves the constant, not the O(n); the same is true of any scheme leaving k > 0 bytes per call, so the count of carried bodies -- not their size -- is what had to stop growing. MEASURED, ABSOLUTE, driving bound_accumulated_tool_results(.., Some(32_768)) on session_with_results(n, 50_000): n=240 (the computed crossing) 81,070B = 20,267 tok BEFORE -> 50,294B = 12,573 tok AFTER; n=500 114,870B -> 50,294B; n=2,000 309,870B = 77,467 tok -> 50,295B = 12,573 tok; n=20,000 -> 50,296B. The 2-byte spread from 500 to 20,000 calls is the decimal digits of the elided count in the aggregate, not a linear term. The ceiling the guard admits is 20,208 tokens in every one of those rows. THE PROMPT-CACHE COST, MEASURED RATHER THAN ESTIMATED. A fold rewrites history at the FRONT, so the provider's cached prefix is invalidated whole and the next turn re-reads the entire prompt. That is one uncached turn per fold, and the price is bought back by FREQUENCY: the residue grows one stub per dropped result and the fold fires at total_budget_bytes, so the interval is 30,832 / 130 = 237 dropped results on a 32,768-token window and 120,000 / 130 = 923 on the flat unknown-window constants. MEASURED on the turn-by-turn path (one round appended per turn, the real pass run every turn, 800 turns, window 32,768): 3 folds in 800 turns -- one prefix rewrite per 267 turns -- carried 62,124B at the end and never above 80,832B on any turn. Asserted, not just recorded, by a_fold_costs_one_prefix_rewrite_per_budget_of_stubs, so the stated price cannot drift from the code that charges it. BELOW the threshold the fold does nothing and the pass is byte-identical to before, which is why every pre-existing cache test still passes unchanged: the_ceiling_is_byte_stable_on_a_second_pass, the_ceiling_advances_in_epoch_sized_batches, a_large_window_leaves_the_pass_byte_identical_to_the_unknown_window_arm, and the four compact_tool_call_args monotonicity/determinism tests, all green with no edit. THE WRONG-REFUSAL HAZARD, WEIGHED ABOVE THE LEAK AND MEASURED IN THE SAME DIRECTION. bounded_result_stub ends `re-run the tool if you still need them` and says it on an Edit result as readily as on a Read: one standing invitation to re-execute a mutating tool per dropped result, so at 2,000 calls there were 1,999 of them. The fold REPLACES a run of those with a single aggregate that says the opposite -- `Do NOT re-run a tool that changed state; inspect the current state instead` -- and names how many of the elided calls were state-changing, so the count is in front of the model instead of inferred. Net effect on the hazard: the number of re-run invitations outside the protected tail falls from n-1 to 0. What the fold DOES cost the model is the tool NAMES and arguments of the elided rounds, whose results were already stubbed; the aggregate replaces that with a count and a state-changing count, and the running total is carried forward across later folds rather than restarting. Asserted by the_fold_tells_the_model_not_to_re_run_state_changing_tools. RED ARM (hetzner-dsm, worktree /root/w-f13/ctx-1255). cargo check -p wcore-agent --tests RC=0 BEFORE the mutation. M-1: `#[allow(unreachable_code)] return 0;` as the first statement of fold_bounded_tool_rounds -- it COMPILES (M1_CHECK_RC=0) and reddens FIVE tests including this criterion's own evidence, at the computed crossing count: `240 results: the pass left 81070 bytes = 20267 tokens, and the pre-flight guard admits only 20208` (micro.rs:2665). In the SAME run the_carried_payload_grows_by_one_stub_per_dropped_result and the_ceiling_is_byte_stable_on_a_second_pass both PASS -- the vacuity of the pre-existing pins demonstrated rather than argued (69 tests run: 64 passed, 5 failed, RED_RC=100). M-2: victims = only the user half of each elided round, orphaning the ToolUse -- it COMPILES and reddens exactly the_fold_never_orphans_a_tool_call and nothing else (69 run: 68 passed, 1 failed), because orphaning saves the identical bytes and every byte-bound test is blind to it. Both restored from git, touched, `git status --porcelain` empty, re-run: 69/69 green, and the whole wcore-agent + wcore-config suites 4,950/4,950. NOT CLAIMED. The fold only recognises the canonical round shape (an all-ToolUse assistant message answered by the very next all-ToolResult user message with matching ids). A provider or host that interleaves tool traffic differently gets the old stub arithmetic for those rounds; the bound then degrades gracefully rather than failing, but it is not universal. And cleared_count deliberately stays the epoch-quantized STUB batch, so a fold that fired on a call that created no new stub -- reachable only by lowering the budget or narrowing the window mid-session -- reclaims bytes without the engine emitting its info line. No file-cache generation bump is owed either way: a fold only ever removes bodies that are already stubs, and the diff-resend base such a body once backed was invalidated when the STUB was written, on a call that did report cleared_count > 0."
  - id: c2
    text: "A test drives the real bound_accumulated_tool_results(.., Some(32_768)) at a session length past the crossing point and asserts the carried payload fits input_ceiling_for_window(32_768); shown RED against today's 309,870 bytes / 77,467 tokens at 2,000 tool calls"
    state: met
    evidence: "test:crates/wcore-agent/src/compact/micro.rs::the_carried_payload_fits_the_window_however_long_the_session"
    owner: core
    note: "Filed 2026-08-30 by lane w2-window-arc. CLOSED 2026-08-31 by lane f13-ctx-1255 at e58300bdf. The evidence is the_carried_payload_fits_the_window_however_long_the_session, which RUNS the real bound_accumulated_tool_results(&mut msgs, &config, Some(32_768)) -- the arm BOTH production call sites take (AgentEngine::run_compaction step 0b and AgentEngine::compact_now, each passing Some(compaction_window_now())) -- at four session lengths PAST the crossing point: the crossing count itself, computed in the test as (admissible - MAX_TOOL_RESULT_BYTES).div_ceil(stub) + 2 = 240, then 500, 2,000 and 20,000. Three assertions per case and the SET is what grades it: (1) two non-vacuity preconditions -- the session must start over the window, AND the pre-fold arithmetic MAX_TOOL_RESULT_BYTES + (n-1) x stub must genuinely exceed the admissible bytes, so no case can silently fall back below the crossing; (2) carried / CHARS_PER_TOKEN <= input_ceiling_for_window(32_768), the criterion itself on the bytes production carries; (3) carried <= worst_case_carried_tool_result_bytes(Some(32_768)), so the predictor #1200 c2 was wrongly graded over must still be a true upper bound on the pass. A closing block answers `bounded, or merely smaller?`: the spread across 500 to 20,000 calls must be under ONE stub length, which no linear term can satisfy -- measured 2 bytes. SHOWN RED against today's behaviour, as the criterion requires: with the fold made a no-op (`#[allow(unreachable_code)] return 0;` first in fold_bounded_tool_rounds; cargo check -p wcore-agent --tests RC=0 both before and with the mutation) this test panics at micro.rs:2665 with `240 results: the pass left 81070 bytes = 20267 tokens, and the pre-flight guard admits only 20208`, and at 2,000 calls the unmutated figure is the ticket's own 309,870 bytes = 77,467 tokens. Restored, touched, tree clean, 69/69 green. The old pin the_carried_payload_grows_by_one_stub_per_dropped_result is KEPT, not deleted, and not inverted: it now measures the pure stub regime BELOW the fold threshold (n = 20/100/200), asserts a precondition that each case's residue really is under total_budget_bytes, and asserts the message count is untouched -- so it can only ever grade the monotone arithmetic it was written for, and it stays GREEN under the M-1 mutation, which is the demonstration that it could never have caught this."
---

`bound_accumulated_tool_results` replaces each over-budget tool result with a stub and never re-mutates one, so the bytes it leaves are `protected_tail + dropped_results x stub_len` with `stub_len` = 130. The second term grows without bound in the number of tool calls, so the ceiling FerroxLabs/wayland#1200 installed holds for short sessions and fails for long ones.

Measured on a 32,768-token window at `lane/f13-w2-window-arc`, driving the production pass:

| tool calls | carried bytes | carried tokens | ceiling (tokens) | fits? |
|---|---|---|---|---|
| 20 | 52,470 | 13,117 | 20,208 | yes |
| 100 | 62,870 | 15,717 | 20,208 | yes |
| 500 | 114,870 | 28,717 | 20,208 | no |
| 2,000 | 309,870 | 77,467 | 20,208 | no |

**Where.** crates/wcore-agent/src/compact/micro.rs — `bound_accumulated_tool_results`, its `candidates[..eligible]` stub loop, and `bounded_result_stub`. `is_stubbed_result` correctly keeps stubs out of `candidates` on later passes, which is why the residue only ever accumulates.

**Why it matters.** This is FerroxLabs/wayland#1150's reported symptom ("Absurd Input Token Size") surviving the fix that was supposed to remove it. #1200 moved the threshold from "any session" to "about 238 tool calls" on a 32k model; it did not remove it. A 2,000-tool-call session is ordinary for an agent CLI.

Criteria are the Acceptance section of the issue. The trade behind not fixing it in the filing lane is written out in `.planning/DECISIONS.md` under Q-1255.

**Closed 2026-08-31** (lane `f13-ctx-1255`, commit `e58300bdf`), on the FIRST arm of c1: the
carried payload is now bounded in the number of tool calls. Same call, same window, same fixture:

| tool calls | carried before | carried after | ceiling (tokens) | fits? |
|---|---|---|---|---|
| 240 (the crossing) | 81,070 B = 20,267 tok | 50,294 B = 12,573 tok | 20,208 | yes |
| 500 | 114,870 B = 28,717 tok | 50,294 B = 12,573 tok | 20,208 | yes |
| 2,000 | 309,870 B = 77,467 tok | 50,295 B = 12,573 tok | 20,208 | yes |
| 20,000 | — | 50,296 B = 12,574 tok | 20,208 | yes |

The 2-byte spread from 500 to 20,000 calls is the decimal digits of the elided count, not a term.

**The price, measured.** `fold_bounded_tool_rounds` elides already-stubbed tool rounds at the FRONT
of history, so each fold costs one uncached turn. It fires once per `total_budget_bytes / stub_len`
dropped results — 237 on a 32,768-token window, 923 on the flat fallback. Driving the pass
turn-by-turn for 800 turns on a 32,768-token window: **3 folds**, one prefix rewrite per 267 turns,
against a session that otherwise walks into the pre-flight abort at 238 calls and stops.

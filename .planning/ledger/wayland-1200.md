---
issue: 1200
repo: FerroxLabs/wayland
kind: defect
title: "The accumulated-tool-result ceiling is a window-independent constant, so it permits about 80,000 tokens on a 32,768-token model"
status: open
last_verified_commit: 6e404f28
criteria:
  - id: c1
    text: "The tool-result budget is derived from the resolved context window when one is known, with today's constants as the unknown fallback"
    state: met
    evidence: "symbol:crates/wcore-config/src/compact.rs::tool_result_bounds"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D17, found while verifying wayland#1150 — [Bug]: Absurd Input Token Size). Nothing has been done. The measured finding, verbatim: The c4 accumulated-tool-result ceiling is a window-independent constant, so at its shipped defaults it cannot keep the reporter's model inside its own context window. `total_budget_bytes = 120_000` bytes and `keep_recent = 4` (compact.rs:733-741) are absolute figures, never derived from the resolved context window. The four protected newest results are exempt 'however large', and each can carry up to `Tool::max_result_size()` = 50,000 chars (wcore-tools/src/lib.rs:529). Worst case carried tool-result payload = 120,000 + 4 × 50,000 = 320,000 bytes ≈ 80,000 tokens — about 2.4x the entire 32,768-token window the same release assumes for an unlisted model, and just under the 83,208 tokens the reporter measured. The ceiling's guarantee ('carried bytes stop growing with the session') is true and is what the evidence test measures; the guarantee a 32k user needs ('carried bytes fit the window') is neither claimed nor delivered, and nothing in the ledger says so. GRADED 2026-08-30 by lane w2-window-arc at 115cb4c6. tool_result_bounds(Option<usize>) derives BOTH terms from the resolved window when one is known and returns today's flat constants when it is None, which is what every caller with no window gets. bound_accumulated_tool_results takes the window and both engine call sites pass Some(compaction_window_now()) -- the same resolved window, learned narrowing included, that the pre-flight ceiling, the autocompact threshold and the emergency stop use. Sized against input_ceiling_for_window rather than the raw window because carried results are INPUT and the ceiling is the boundary that admits input; a bound the #255 guard aborts past is not a bound. RED ARM (hetzner-dsm): returning the flat constants from the Some arm reddens c2's test; restored, touched, green."
  - id: c2
    text: "A test asserts that on a 32,768-token window the worst-case carried payload (total_budget_bytes + keep_recent x max_result_size) fits the window; shown RED against today's 120,000 + 4 x 50,000"
    state: met
    evidence: "test:crates/wcore-agent/src/compact/micro.rs::the_pass_leaves_a_payload_the_window_can_admit"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D17). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below. GRADED 2026-08-30 by lane w2-window-arc at 115cb4c6. Asserts the ticket's own worst case -- total_budget_bytes + keep_recent x max_result_size -- fits a 32,768-token window, and fits the 20,208-token ceiling the guard actually admits. It carries the RED figure as a precondition: the unknown-window fallback is asserted to still be exactly 120,000 + 4 x 50,000 and to still overflow the window, so the test cannot go vacuous if the constants are ever changed underneath it. THE FIRST VERSION OF THE FIX DID NOT PASS THIS: sizing the budget against half the admissible bytes left 90,416 bytes = 22,604 tokens against a 20,208-token ceiling -- the reported state, merely smaller. The bound now reserves the un-lowerable protected-tail term (max(admissible/2, MAX_TOOL_RESULT_BYTES)) before spending the rest on the budget. Its control a_large_window_keeps_todays_flat_tool_result_constants pins the other direction, so a bound that shrank every window could not pass. REFUTED 2026-08-30 by this lane's independent verifier, and the refutation held. The evidence test named above asserted over CompactConfig::worst_case_carried_tool_result_bytes -- a pure arithmetic predictor with ZERO production callers. The property is delivered by the protected-tail byte-cap loop inside bound_accumulated_tool_results, and the verifier deleted that loop and left 4,876 tests green, this criterion's own evidence test included: the predictor returns the identical 80,832 in BOTH arms, so it is structurally blind to whether production honours it. Driving the real pass in both arms gave after=52,470B = 13,117 tok on HEAD against after=202,080B = 50,520 tok mutated, on a window whose guard admits 20,208. RE-CLOSED 2026-08-30 ON THE PRODUCTION PATH. The evidence is now the_pass_leaves_a_payload_the_window_can_admit, which RUNS bound_accumulated_tool_results(.., Some(window)) over 32,768 / 40,960 / 49,152 / 65,536 / 131,072 / 200,000 at 20 and 100 tool calls and asserts two things per case, the PAIR being what removes the blindness: (1) total_result_bytes / CHARS_PER_TOKEN <= input_ceiling_for_window(window), the criterion itself on the bytes production actually carries; and (2) carried <= worst_case_carried_tool_result_bytes(Some(window)), so the predictor must be a true UPPER BOUND on the pass it describes and can no longer drift away from it silently. Each case carries a non-vacuity precondition that the session starts over the window. Identity control: a_large_window_leaves_the_pass_byte_identical_to_the_unknown_window_arm asserts Some(200_000) is byte-identical to None over serde_json, and closes with the control that a 32,768-token window must carry STRICTLY LESS, so a pass that ignored the window entirely could not satisfy the identity. RED ARM (hetzner-dsm), the verifier's own M-V1 replayed rather than a new weaker one: the byte-cap loop replaced by `let protected = bounds.keep_recent.min(all.len()); let _ = bounds.protected_tail_bytes;`. It COMPILES -- cargo check -p wcore-agent --tests exit 0 -- and then reddens FIVE tests including the new production-path evidence at micro.rs:2190 with -- window 32768, 20 results: the pass left 202080 bytes = 50520 tokens, and the pre-flight guard admits only 20208 -- while the OLD predictor-only test the_worst_case_carried_tool_results_fit_a_32k_window PASSES in the same run, which is the vacuity demonstrated rather than argued (Summary: 39 tests run, 34 passed, 5 failed, RED_RC=100). Restored from origin, touched, re-run green. OPEN REMAINDER, NOT claimed by this criterion and filed as FerroxLabs/wayland#1255: the pass leaves one 130-byte stub per dropped result and never re-mutates one (that monotonicity is what makes it prompt-cache safe), so carried = protected_tail + dropped x 130 grows WITHOUT BOUND in the number of tool calls. Measured on a 32,768-token window: 52,470B at 20 calls, 62,870B at 100, 114,870B at 500, 309,870B = 77,467 tok at 2,000 -- 2.36x the whole window, which is the state this ticket reports, reached by a longer session instead of by a bigger budget. The ceiling is crossed at about 238 tool calls. That term is not in this criterion's arithmetic (total_budget_bytes + keep_recent x max_result_size), which is why c2 is graded met rather than re-opened, and it is pinned in-tree by the_carried_payload_grows_by_one_stub_per_dropped_result so it cannot be rediscovered as a surprise."
  - id: c3
    text: "The ledger entry for wayland#1150 c4 states WHICH guarantee is delivered -- carried bytes stop growing, or carried bytes fit the window -- rather than leaving a reader to assume the second"
    state: met
    evidence: "symbol:crates/wcore-config/src/compact.rs::worst_case_carried_tool_result_bytes"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D17). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below. GRADED 2026-08-30 by lane w2-window-arc at 115cb4c6. #1150 c4's ledger note now STATES which guarantee is delivered instead of leaving a reader to assume the stronger one, and the arithmetic behind the statement is a named function rather than a sentence: worst_case_carried_tool_result_bytes(window). Its doc records the NAMED GAP in the same place -- below a ceiling of about 12,500 tokens the single unconditionally-protected newest result is the binding term, it is the per-result ingestion cap in wcore_tools::Tool::max_result_size and is NOT window-derived, so nothing in wcore-config can close it. A window that small is unworkable by Q-1172c3 and is refused by the turn loop. AMENDED 2026-08-30. worst_case_carried_tool_result_bytes has no production caller and cannot acquire one -- it is a statement ABOUT the pass, not a step in it. That is legitimate for c3, whose criterion is about what the ledger STATES, but it is exactly what made c2 vacuous when c2 was graded over the same function. The predictor is now pinned to the pass it describes by the second assertion in the_pass_leaves_a_payload_the_window_can_admit (carried <= worst_case_carried_tool_result_bytes(Some(window)) at six windows and two session lengths), so a predictor that stops describing production reddens a test instead of quietly disagreeing with it. The NAMED GAP in its doc is also now measured rather than estimated: the gap is not only the sub-12,500-token ceiling the doc names, it is also the unbounded stub term (FerroxLabs/wayland#1255)."
---

The c4 accumulated-tool-result ceiling is a window-independent constant, so at its shipped defaults it cannot keep the reporter's model inside its own context window. `total_budget_bytes = 120_000` bytes and `keep_recent = 4` (compact.rs:733-741) are absolute figures, never derived from the resolved context window. The four protected newest results are exempt 'however large', and each can carry up to `Tool::max_result_size()` = 50,000 chars (wcore-tools/src/lib.rs:529). Worst case carried tool-result payload = 120,000 + 4 × 50,000 = 320,000 bytes ≈ 80,000 tokens — about 2.4x the entire 32,768-token window the same release assumes for an unlisted model, and just under the 83,208 tokens the reporter measured. The ceiling's guarantee ('carried bytes stop growing with the session') is true and is what the evidence test measures; the guarantee a 32k user needs ('carried bytes fit the window') is neither claimed nor delivered, and nothing in the ledger says so.

**Where.** crates/wcore-config/src/compact.rs:733-741 (default_tr_total_budget_bytes / default_tr_keep_recent) consumed by crates/wcore-agent/src/compact/micro.rs:647 bound_accumulated_tool_results

**Why it matters.** It shifts the entire load for small-window users back onto autocompact/emergency (c1), which is the loud, lossy path. A user on a 32k local model — the population this ticket is about — gets a 'ceiling' that permits more tool-result bytes than their model can accept, so the visible symptom (compact churn, or the endpoint truncating) persists after the fix. Deriving the budget from `known_context_window` when it is Some, with the current constants as the None fallback, would close it.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.

---
issue: 390
repo: FerroxLabs/wayland-core
kind: defect
title: "is_vcs_content_store arm 2 reads only <root>/.git, so a VENDORED gitfile's object store is VFS-readable (split from #244 c1)"
status: open
last_verified_commit: 972d1c17c
criteria:
  - id: c1
    text: "A VFS read of an object under a store named by a gitfile on a VENDORED checkout is refused, with that checkout`s own working tree still readable as the wrong-refusal control"
    state: met
    evidence: test:crates/wcore-tools/tests/vfs_nested_named_store_deny.rs::a_vendored_gitfile_named_store_is_refused_through_the_vfs
    owner: core
    note: "Arm 3 added to is_vcs_content_store: discover_nested_content_stores walks the root, and scan_control_dirs_in reads each nested control directory with EXACTLY the code that reads the root one. A VFS Read of <root>/vendor/pkg-git/objects/12/3456, named by the gitfile <root>/vendor/pkg/.git = `gitdir: ../pkg-git`, is now SecretDenied. Wrong-refusal controls in the same test stay green: the vendored checkout own working tree (vendor/pkg/src/lib.rs), the vendored gitdir HEAD, and an ordinary workspace file. RED ARM, verbatim: nested_stores_memoized body replaced with Vec::new(); cargo check -p wcore-tools --tests exit 0; test fails with `core#390 c1: the point-predicate must call the vendored gitfile store a content store`. Restored, blob identity verified equal to HEAD."
  - id: c2
    text: "The same holds for an objects/info/alternates borrow declared by a NESTED checkout, not only by the workspace root"
    state: met
    evidence: test:crates/wcore-tools/src/workspace_policy/tests.rs::every_store_the_scan_discovers_is_refused_whatever_it_is_named
    owner: core
    note: "CLOSED 2026-08-31 at 972d1c17c, and NOT by widening the list of store names. The two previous passes both stopped at the same wall: the arm-3 gate was store_shaped applied to the QUERY path, while the list it guards is built by StoreScan::push_store, which CANONICALIZES, so the two disagreed for every store whose resolved path carries no VCS_CONTENT_STORES component. Measured by the adversarial verifier driving this lane`s own pins: hidden=[odb-alt, odb-link], both VFS-admitted. WHAT LANDED: WorkspacePolicy::nested_walk_admits decides the gate from the SCAN`S OWN OUTPUT - a path is admitted when the last scan`s store list already covers it - with store_shaped kept only as the net for a store created or renamed SINCE that walk, and a cold-memo arm because a gate that is a function of an output cannot answer before there is one. A discovered store is a prefix of itself, so the class is impossible rather than enumerated: a fourth discovery route needs no new case. Costs ZERO filesystem probes; it reads the memo in hand and never revalidates it. GRADED by the same partition test, INVERTED not deleted exactly as its own failure message instructed: every_store_the_scan_discovers_is_refused_whatever_it_is_named still partitions discover_nested_content_stores` output by store_shaped, still asserts the hidden half NON-EMPTY (that non-emptiness is what proves the fix load-bearing rather than the fixture drifting), still asserts the visible half contains a store the LEXICAL arm cannot answer, and now asserts EVERY member of BOTH halves is refused - plus a FRESH policy asked about the hidden half FIRST while cold, so a gate that only happens to admit on a cold memo cannot pass. The two root-vs-nested pairs in vfs_nested_named_store_deny.rs are now SYMMETRY assertions (the_same_alternates_borrow_is_refused_from_either_declaring_control_dir, the_same_symlinked_store_leaf_is_refused_from_either_declaring_control_dir), each keeping its wrong-refusal control, and a_nested_alternates_borrow_named_nothing_store_like_is_refused is the inverted sibling. RED ARM, cargo check -p wcore-tools --tests exit 0 before any result was believed: nested_walk_admits reverted to store_shaped(canon) alone reddens TEN tests across FOUR files - every_store_the_scan_discovers_is_refused_whatever_it_is_named, a_nested_alternates_borrow_named_nothing_store_like_is_refused, both symmetry pairs, a_store_under_a_symlinked_control_dir_is_pruned_after_the_memo_is_warm and four vfs_guard_cost tests. Restored, sha256 verified equal to the pre-mutation copy. ALSO CLOSED HERE, the fail-OPEN the verifier found in the same function and no criterion graded: read_dir refusing a directory, and a DirEntry coming back Err, were both dropped silently, so a directory the walk could not list was not scanned, a nested control directory inside it was never found, and the store it names was never refused - a hole in a security predicate opened by an error nobody could see. StoreScan::opaque_dir treats what it cannot enumerate as a store and marks the scan INCOMPLETE so it is never memoised. Graded by a_directory_the_scan_cannot_enumerate_is_refused_not_skipped, whose first shape was VACUOUS on the build host (root bypasses DAC, so the --x fixture never failed) and was caught by its own red arm; it now asks the scan directly with a readable tree as the known-negative control, and runs the permission fixture only where it can bind. Red arm: dropping the opaque_dir call from the read_dir arm reddens it. RESIDUAL, filed as FerroxLabs/wayland-core#406 rather than hidden: a store that comes into being AFTER the walk at a path it never recorded and that store_shaped does not recognise is still missed, because closing that costs at least one probe on the REFUSE branch and c3 pins the ordinary-path guard at zero."
  - id: c3
    text: "Whatever caching the fix introduces is measured against #376`s complaint: the per-operation cost of is_vcs_content_store does not get worse than it is today, stated as a number"
    state: not-met
    evidence: test:crates/wcore-tools/tests/vfs_guard_cost.rs::a_gate_admitted_path_costs_one_probe_per_workspace_directory
    owner: core
    note: "STAYS not-met, re-measured on this tree at 972d1c17c AFTER c2 closed, and the number is unmoved: GATE COST: dirs=8 admitted=19 ordinary=3 | dirs=48 admitted=59 ordinary=3, a slope of exactly 1.000 filesystem probe per workspace directory on the gate-admitted path, with the gate-REFUSED control flat at 3 at both sizes. FerroxLabs/wayland-core#398 carries it. WHAT CHANGED is the attribution and it matters for how #398 can be closed: the previous pass measured that widening store_shaped to true would close c2 and simultaneously redden this test, and concluded c2 and c3 were not independently closeable. That conclusion was about the fix that was ASSUMED, not the one that landed. c2 closed with ZERO probes by making the gate a function of the scan`s own output, and the slope survived untouched - so the slope is store_shaped`s alone, and store_shaped now exists ONLY as the net for a store created or renamed since the last walk. WHAT DID MOVE, stated rather than waved past, because c3`s text is unqualified: the arm-3 walk now runs ONCE PER POLICY on the first guard, instead of on the first store-shaped path. The warm per-operation number c3 is about is UNCHANGED - one resolution, zero warm scans, three warm probes, pinned at both workspace sizes - but the COLD first guard now scans twice and costs 43 probes where it cost 12 on the vfs_guard_cost fixture (12 root scan + 31 walk, named as FIRST_GUARD_PROBES so the two tests that pin it cannot drift). an_ordinary_path_never_pays_for_the_nested_store_walk is renamed an_ordinary_path_pays_for_the_nested_store_walk_at_most_once and grades the steady state as a difference over N guards, which a one-off cannot inflate; its known-positive control is now that the ONE walk already paid for is the walk that refuses the vendored store, with NO further scan. Related change in the same direction: nested_stores_memoized no longer drops the memo above DENY_CACHE_MAX_DIRS, because with the gate reading that memo a policy that refuses to keep one re-walks the tree on EVERY guard rather than on the store-shaped ones alone - strictly cheaper in both shapes, at the price of the witness vector`s memory. The 16-vs-17 unit reconciliation this note previously ASSERTED is now IN THE TREE: witness_if_present`s doc quotes 17 total (16 statx + 1 openat) and names the unit, checkable with grep -c `16 filesystem syscalls` -> 0 against grep -c `17 total` -> 1. The verifier`s independently built middle arm returned 16 TOTAL where two other capture sets return 17, while base (8) and head (5) agree exactly across all three; that one-syscall disagreement is UNEXPLAINED and is recorded in vfs_guard_cost.rs`s header rather than reconciled away. Not grade-bearing: every set agrees the drop is TO 5."
  - id: c4
    text: "When the fix lands, grep_vcs_named_store_deny.rs`s `!is_vcs_content_store` assertion is INVERTED rather than deleted, so the two layers are re-tied"
    state: met
    evidence: test:crates/wcore-tools/tests/grep_vcs_named_store_deny.rs::grep_cannot_harvest_a_nested_gitfile_named_store
    owner: core
    note: "INVERTED, not deleted. The assertion was `!WorkspacePolicy::contained(&root).is_vcs_content_store(&store_file)` with the note `if the point-predicate has grown a nested arm, this note is stale`; it is now the positive, so Grep and the VFS are re-tied from both sides (vfs_nested_named_store_deny.rs carries the mirror assertion). It went RED on the unmodified test the moment arm 3 landed, which is how the inversion was found rather than chosen."
---

Split out of #244 c1 while closing #244 c3. #244 c1's text was the unqualified
"at any nested depth"; that has been rewritten to the scope that actually holds
and this issue carries the remainder, referenced from #244 c7.

Found by building the second-arm fixture for #244 c3, not reported by that
change's verifier. The Grep half of the same class IS closed in that change --
it is only the in-process VFS predicate that still misses this shape.

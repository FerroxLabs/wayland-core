---
issue: 398
repo: FerroxLabs/wayland-core
kind: defect
title: "Arm 3's lexical gate makes a guard on any path named objects/modules/store cost one syscall per workspace directory (split from #390 c3)"
status: open
last_verified_commit: 967bdf2fb
criteria:
  - id: c1
    text: "— The warm per-guard cost of a gate-admitted path is INDEPENDENT of the workspace's directory count. `vfs_guard_cost.rs::a_gate_admitted_path_costs_one_probe_per_workspace_directory` grades this as a slope and currently pins the slope at 1.0; when it is fixed that assertion is INVERTED rather than deleted, so the regression and its closure are graded by the same test."
    state: not-met
    evidence: test:crates/wcore-tools/tests/vfs_guard_cost.rs::the_post_walk_freshness_check_scales_with_checkouts_not_directories
    owner: core
    note: NOT MET AS WRITTEN: the test the criterion says to INVERT does not exist in this lineage. `grep -rl a_gate_admitted_path_costs_one_probe_per_workspace_directory --include=*.rs crates/` -> 0 hits, known-positive control green in the same session. Nor does the regression: the slope-1.0 tree is `875bf32cb` on `lane/f13-w3-vcs-residuals`, and `git merge-base --is-ancestor 875bf32cb HEAD` is FALSE. There is nothing here to invert, because there is nothing here that ever pinned 1.0.\nTHE PROPERTY IS MET, MEASURED, AND THIS LANE IS THE FIRST THAT COULD HAVE BROKEN IT. Closing core#406 c1 means the admit branch now asks whether arm 4`s memo is fresh, and the obvious way to ask - stamp every directory the walk descended - IS this ticket`s regression exactly. It was not taken. The witness set is the DECLARATION SITES the walk read (one gitfile, one commondir, one control directory, one objects/info/alternates), which is O(nested checkouts) and independent of the tree.\nMEASURED base-vs-HEAD on hetzner, base `ca15a48bf` vs HEAD `967bdf2fb` (lane/f13-s2-vcs-cost). Two DISTINCT binaries, identity proven by content and not by date: sha256 differs and `strings base_bin | grep -c nested_declarations_moved` = 0 against 5 for HEAD.\nCounted `GuardCounters`, warm probes per guard, two workspaces differing ONLY by ordinary empty directories:\n                                          8 dirs   48 dirs   slope\n  store-leaf-named `modules/vpc/main.tf`       4         4   0.000\n  ordinary path, no nested checkout            3         3   0.000\n  ordinary path, ONE vendored checkout         5         5   0.000\n  ordinary path, TWO vendored checkouts        7         7   0.000\nagainst the 1.000 this ticket reports. The third and fourth rows are the ones this lane added and they are the load-bearing ones: the freshness check`s price is LINEAR IN CHECKOUTS and FLAT IN DIRECTORIES, asserted as both in `the_post_walk_freshness_check_scales_with_checkouts_not_directories`. Its doc names its own red arm: witness the descended directories instead of the declaration sites and the two sizes diverge by one probe per extra directory.\nPROPOSED REPOINT, on the ISSUE: name `vfs_guard_cost.rs::a_store_named_path_costs_the_same_at_any_workspace_size` (the inverted form for the gate-admitted path) and `..::the_post_walk_freshness_check_scales_with_checkouts_not_directories` (the inverted form for the post-walk freshness check core#406 c1 introduced).
  - id: c2
    text: "— The refusals #390 bought stay bought: `vfs_nested_named_store_deny.rs` stays green in full, including the vendored-gitfile arm and the nested-alternates arm, so the cost fix is not a quiet revert."
    state: not-met
    evidence: test:crates/wcore-tools/tests/vfs_nested_store_deny.rs::a_vendored_gitfiles_object_store_is_refused
    owner: core
    note: NOT MET AS WRITTEN, for the pointer reason the previous grader recorded and for no other: the criterion names `vfs_nested_named_store_deny.rs` and `grep -rl vfs_nested_named_store_deny --include=*.rs crates/` returns 0 hits (known-positive control green in the same session). That spelling is `lane/f13-w3-vcs-residuals`; the file here is `vfs_nested_store_deny.rs`.\nTHE PROPERTY - `the refusals #390 bought stay bought, so the cost fix is not a quiet revert` - IS THE ONE THIS LANE HAD TO EARN, and it is verified with a mutation rather than by re-reading the code. `cargo nextest run -p wcore-tools --test vfs_nested_store_deny` -> 9 passed / 0 failed at HEAD (967bdf2fb), INCLUDING both arms the criterion names by test name: `a_vendored_gitfiles_object_store_is_refused` (the vendored-gitfile arm) and `a_nested_alternates_borrow_is_refused_whatever_its_target_is_named` (the nested-alternates arm). Full crate: 1874 passed / 0 failed, clippy `-D warnings` clean.\nRED ARM, compiled first so a red is behaviour and not a build break (`cargo check -p wcore-tools --tests` RC=0): neuter `nested_declarations_moved` to `return false` - base behaviour, the memo trusted forever - and EXACTLY TWO tests go red, both added by this lane. All seven pre-existing refusals in that file and every wrong-refusal control stay green. That is the discrimination this criterion asks for: a quiet revert would have reddened the two named arms, and it does not. Restored, `sha256sum -c` verified equal to the pre-mutation copy.\nPROPOSED REPOINT, on the ISSUE: `vfs_nested_store_deny.rs` (drop `named`).
  - id: c3
    text: "— The ordinary (gate-refused) path is unchanged at one resolution / zero warm scans / three warm probes, still pinned by `one_ordinary_path_guard_resolves_once_and_does_not_rescan`."
    state: met
    evidence: test:crates/wcore-tools/tests/vfs_guard_cost.rs::one_ordinary_path_guard_resolves_once_and_does_not_rescan
    owner: core
    note: MET, and re-measured here rather than carried forward, because this lane changed the code path the number prices. NOTE FOR THE NEXT GRADER: this criterion is the ORDINARY-PATH PROBE COUNT, not the differential-strace figure - the previous note under this id pasted the strace text, which belongs to the checkbox list`s c3 and is graded on core#394 c3 / core#396 c3.\n`vfs_guard_cost.rs::one_ordinary_path_guard_resolves_once_and_does_not_rescan` green at HEAD: `resolves` == N+1 over 50 guards (one resolution per guard, never two), `scans` stays at 1 (zero warm scans), and `probes - first_probes` == N*3 exactly (three warm probes). `nested_walk_count()` asserted == 1, so the whole-tree walk did not come back on the common path.\nMEASURED base-vs-HEAD on hetzner, base `ca15a48bf` vs HEAD `967bdf2fb` (lane/f13-s2-vcs-cost). Two DISTINCT binaries, identity proven by content and not by date: sha256 differs and `strings base_bin | grep -c nested_declarations_moved` = 0 against 5 for HEAD. All three figures are UNCHANGED from base, and that is the non-trivial part: closing core#406 c1 put a freshness check on precisely the branch an ordinary path takes. It costs nothing there because the witness set is the declaration sites of NESTED checkouts and an ordinary workspace has none, and because `<root>/.git`'s own declarations are deliberately excluded - arm 2 already revalidates that gitfile, its commondir and its alternates on every guard, and they ARE the three probes this criterion pins. Independently asserted as a number by `the_post_walk_freshness_check_scales_with_checkouts_not_directories`, which requires (3, 3) for the no-checkout arms at 8 and 48 workspace directories before it measures anything else.\nThe COLD one-off is unchanged from base too and is restated rather than hidden: the first guard costs 41 probes on the `vfs_guard_cost` fixture (17 for the arm-2 scan plus 24 for arm 4's one walk), asserted literally by the same test.
  - id: c4
    text: "— The figure is re-stated as a number for BOTH call shapes on the tree the fix lands on, base-vs-HEAD, by the two instruments named above (the counted slope and the differential `strace` per-traversed-directory figure), each with its known-positive control green."
    state: met
    evidence: test:crates/wcore-tools/tests/vfs_guard_cost.rs::a_store_named_path_costs_the_same_at_any_workspace_size
    owner: core
    note: "Both call shapes, base-vs-HEAD, each with its control:\n* counted slope (`GuardCounters`, platform-independent): gate-admitted path 4 probes at 8 dirs and 4 at 48 dirs, slope 0; gate-refused ordinary path 3 warm probes at both -- the control that shows the fixture is not the reason.\n* differential `strace` per traversed directory: base 35.009 -> HEAD 34.997, probe's known-positive control asserted green in every run.\nCold one-off stated separately: first guard 17 -> 41 probes on the `vfs_guard_cost` fixture, +6.0 syscalls/workspace directory, once per policy."
  - id: c5
    text: "— The `DENY_CACHE_MAX_DIRS` branch of `nested_stores_memoized` is graded by a test or the branch is removed; today no test reaches it and its cost behaviour is asserted only in a comment."
    state: blocked
    evidence: test:crates/wcore-tools/tests/vfs_guard_cost.rs::the_post_walk_freshness_check_scales_with_checkouts_not_directories
    owner: core
    note: BLOCKED, and split in two because the ticket carries two different c5s.\n(A) THE CRITERION AS TRANSCRIBED HERE - `the DENY_CACHE_MAX_DIRS branch of nested_stores_memoized is graded by a test or the branch is removed`. `nested_stores_memoized` does not exist on this lineage and this lane did not introduce it: arm 4 is a stamped memo with NO size-conditional caching branch, so the failure mode the criterion guards against (a workspace above the cap re-walking on every guard) is unreachable by construction. RECORDED AND STILL OPEN, unchanged by this lane: the UNRELATED `DENY_CACHE_MAX_DIRS` branch in core#1111's `deny_cache` (`workspace_policy.rs`, `(!dirs.is_empty() && dirs.len() <= DENY_CACHE_MAX_DIRS).then(...)`) is still reached by no test. WHAT WOULD UNBLOCK IT: that branch's false arm needs a workspace of 100,001 directories to reach on the production path, which is not acceptable in a suite that runs on macOS and Windows CI, and the alternatives - an env-var override on a security-relevant memo, or driving the predicate directly - are respectively a production change this lane has no mandate for and the vacuous grade the brief forbids. It needs an explicit decision on the ISSUE: make the cap injectable for tests, delete the cap, or move this to core#1111 where the memo lives.\n(B) THE CHECKBOX LIST'S c5 - `whether this is closed jointly with core#394 is decided explicitly and recorded` - IS DECIDED, AND THIS LANE IS THE FIRST TREE IN WHICH THE DECISION HAS TEETH. Answer: YES, one mechanism, and the coupling this ticket MEASURED (`mutating store_shaped to true reddens c1, c2 and one_ordinary_path_guard_resolves_once_and_does_not_rescan in the same run`) does not arise here because `store_shaped` is never introduced - a gate exists to stop an ordinary path paying for something, and arm 4 costs an ordinary path nothing. The decision was previously recorded against a tree where nothing tested it: core#394 was already closed and core#406 c1 was open, so no code had yet had to pay for freshness. This lane pays for it and the decision holds - the freshness check core#406 c1 needed is priced in NESTED CHECKOUTS (2 probes each) and not in DIRECTORIES (slope 0.000 across 8 vs 48), so core#394's closure and core#398's cost remain the same single design decision and neither was traded for the other. Measured in `the_post_walk_freshness_check_scales_with_checkouts_not_directories`.
---

Created 2026-08-31 to close a COVERAGE gap. It records no work as done.

`scripts/check-criteria-ledger.py` scopes every open `area:core` issue on
wayland and EVERY open issue on wayland-core. This issue was in scope from
the moment it was filed and had no ledger file, so
`scripts/check-release-readiness.py` -- which reads ledger files and nothing
else -- could not count it. CI runs the coverage gate with `--offline`, the
arm that would have reported the gap, so nothing said so for two days.

Criteria are transcribed from the issue body without edit. Where the body's
wording is loose it is LEFT loose rather than tightened here: sharpening a
criterion inside the ledger is how a criterion quietly becomes an easier
adjacent property. Whoever takes this restates it on the ISSUE first.

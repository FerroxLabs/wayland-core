---
issue: 383
repo: FerroxLabs/wayland-core
kind: defect
title: "is_project_secret still uses the weaker resolver, so a dangling symlink to a not-yet-existing project secret is not refused"
status: open
last_verified_commit: 3b860623
criteria:
  - id: c1
    text: "policy.is_project_secret(<in-root dangling symlink to a missing .env>) returns true"
    state: met
    evidence: symbol:crates/wcore-tools/src/workspace_policy.rs::is_project_secret_resolved
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D11, found while verifying wayland-core#356). Nothing has been done. The measured finding, verbatim: The same dangling-symlink escape #356 closed on `is_skill_source_path` and `is_repo_control_path` is still live on `is_project_secret`, because that predicate still uses the weaker `canon_for_scope` resolver. Measured on hetzner at origin/integ/f13 with a probe test: with `<root>/.env` absent and `<root>/notes.txt` a dangling symlink to it, `policy.is_project_secret(&notes.txt)` returns **false**, while the same `.env` named directly returns **true** and a link to an EXISTING `.env` returns **true**. Probe output verbatim: `PROBE live-link-to-existing-env: true` / `PROBE dangling-link-to-missing-env: false` / `PROBE same-under-canon_existing_ancestor-shape: true`. The read direction is closed (a secret that does not exist has nothing to leak, and a link to one that does exist canonicalizes and is caught); the residual is the WRITE direction — a Full-posture channel/remote session's SecretDenyFs would not refuse a write that lands as a not-yet-existing project secret through a dangling link. `is_project_secret`'s own doc says the Full deployment has no SandboxedFs wrapper to pre-canonicalize, so the raw path is what reaches the predicate there. CLOSED 2026-08-30 by lane sec-secrets on branch lane/f13-sec-secrets. `is_project_secret` now resolves through `canon_existing_ancestor`, the resolver #356 moved the other two predicates onto, and hands the resolved path to a lexical `is_project_secret_resolved`. The measured probe now answers the other way: with `<root>/.env` absent and `<root>/notes.txt` a dangling symlink to it, `policy.is_project_secret(&notes.txt)` returns TRUE. RED ARM: reverting that one call to `canon_for_scope` reddens `a_dangling_symlink_to_a_not_yet_existing_project_secret_is_refused` while the sibling vcs test stays GREEN in the same run, so the test grades this line and not its neighbours. CORRECTED 2026-08-30 after the verifier re-ran it: the arm reddens TWO tests, not one. `every_weak_resolver_site_states_which_resolver_and_why` (c3's naming guard) also fires, because the reverted line reintroduces a `canon_for_scope` site carrying no note. Re-measured on the fixed tree at line 981: `1834 tests run: 1832 passed, 2 failed`, exactly those two. Two independent instruments is stronger evidence than one, but the original 'and NOTHING else' was wrong as written."
  - id: c2
    text: "A test asserts all three arms measured here -- live link to an existing .env, dangling link to a missing .env, and the direct name -- shown RED against today's canon_for_scope"
    state: met
    evidence: test:crates/wcore-tools/src/workspace_policy/tests.rs::a_dangling_symlink_to_a_not_yet_existing_project_secret_is_refused
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D11). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below. CLOSED 2026-08-30 by lane sec-secrets on branch lane/f13-sec-secrets. The test asserts all three measured arms (direct name, live link to an EXISTING .env, dangling link to a MISSING .env) plus two controls that stop a blanket symlink refusal from passing it: a dangling link to an ordinary name stays writable, and the #667 carve-out for a secret OUTSIDE the root survives. Shown RED against today's `canon_for_scope` BEFORE the fix, on the unmodified tree: `panicked at crates/wcore-tools/src/workspace_policy/tests.rs:2481: a DANGLING symlink to a not-yet-existing project secret was not refused`. The two control arms above it passed in that same run, so the red was the defect and not a broken fixture."
  - id: c3
    text: "workspace_policy.rs no longer carries two resolvers with different escape properties, or every remaining site names which resolver it uses and why"
    state: met
    evidence: test:crates/wcore-tools/src/workspace_policy/tests.rs::every_weak_resolver_site_states_which_resolver_and_why
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D11). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below. CLOSED 2026-08-30 by lane sec-secrets on branch lane/f13-sec-secrets. Satisfied by the SECOND disjunct, deliberately: the two `SecretDenyFs` guard predicates (`is_project_secret`, `is_vcs_content_store`) moved onto `canon_existing_ancestor`, and every remaining `canon_for_scope` site now names the resolver and why -- :1688 `is_read_reachable` and :1694 `is_session_read_granted` are ADVISORY mirrors (the escalation prompt at path_boundary.rs:93) whose enforcing gate `SandboxedFs::contain_read` does its own dangling-link resolution through `landing_prefix`; :852 `is_session_write_granted` has NO production call site at all -- grepped, only two assertions in tests/path_write_grant_test.rs -- which is FerroxLabs/wayland-core#384's finding, so its note says that rather than calling it a mirror, and defers the resolver question to #384 c1's decision; and the three `$HOME` sites resolve a path that always exists, where the two resolvers cannot disagree. The naming is enforced by a test rather than asserted in prose, because a comment is not a guard. Its first shape was VACUOUS -- a fixed 12-line look-back found the note belonging to the neighbouring function -- and that was caught by red-arming it; the look-back is now scoped to the enclosing function and the red arm (delete one note) fails naming line 1890."
---

The same dangling-symlink escape #356 closed on `is_skill_source_path` and `is_repo_control_path` is still live on `is_project_secret`, because that predicate still uses the weaker `canon_for_scope` resolver. Measured on hetzner at origin/integ/f13 with a probe test: with `<root>/.env` absent and `<root>/notes.txt` a dangling symlink to it, `policy.is_project_secret(&notes.txt)` returns **false**, while the same `.env` named directly returns **true** and a link to an EXISTING `.env` returns **true**. Probe output verbatim: `PROBE live-link-to-existing-env: true` / `PROBE dangling-link-to-missing-env: false` / `PROBE same-under-canon_existing_ancestor-shape: true`. The read direction is closed (a secret that does not exist has nothing to leak, and a link to one that does exist canonicalizes and is caught); the residual is the WRITE direction — a Full-posture channel/remote session's SecretDenyFs would not refuse a write that lands as a not-yet-existing project secret through a dangling link. `is_project_secret`'s own doc says the Full deployment has no SandboxedFs wrapper to pre-canonicalize, so the raw path is what reaches the predicate there.

**Where.** crates/wcore-tools/src/workspace_policy.rs:903 (`is_project_secret`, `let canon = canon_for_scope(path);`), resolver at :2935; same resolver at :936 (`is_vcs_content_store`), :1688, :1694, :852. Guard call site: crates/wcore-tools/src/vfs.rs:2043 (`SecretDenyFs::guard`).

**Why it matters.** #356 was filed precisely because two resolvers with different escape properties in one file invite the next author to pick whichever is nearer, and the fix's own ledger note asserts the file now holds only one. It does not. The weaker one still guards a security refusal, and I measured the identical escape class still passing through it — so the class the ticket describes is narrowed to two predicates, not closed.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.

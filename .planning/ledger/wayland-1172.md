---
issue: 1172
repo: FerroxLabs/wayland
kind: defect
title: "Core cannot see a self-hosted endpoint's served context window: stock Ollama silently discards the system prompt while core reports 6% pressure"
status: closed
last_verified_commit: d1f27e6e
criteria:
  - id: c1
    text: "Core learns the window an endpoint actually serves, from the token counts already in its responses"
    state: met
    evidence: "symbol:crates/wcore-config/src/context_window.rs::ServedWindowTracker"
    owner: core
    note: "no probing and no address sniffing — the signal was already in the bytes we receive"
  - id: c2
    text: "The shortfall is named to the user, and says the HEAD of the prompt is what was lost"
    state: met
    evidence: "symbol:crates/wcore-config/src/context_window.rs::ServedWindowTracker"
    owner: core
    note: "Upgraded from the corpus test, which evidences DETECTION rather than the user-facing notice this criterion is about. engine.rs:15348-15351 is the emit_info site that names the shortfall and says the HEAD of the prompt is what was lost. SOFT SPOT CLOSED 2026-08-30 (round 3). The soft spot was real and the `file:` pointer had already rotted: after this lane edited engine.rs, line 15348 sits in the cost-reservation block, and the emit is now at engine.rs:16093. A line number cannot assert a sentence, so the evidence is a test. the_truncation_notice_says_the_head_of_the_prompt_is_what_was_lost asserts, on BOTH notice arms, that the text names the HEAD and says what was in it ("the system prompt and your task are what went") plus both figures - which is the criterion's own sentence, not an adjacent one: a user told only "context truncated" reasonably assumes the TAIL was trimmed and their task survived. RED ARM (hetzner-dsm, 2026-08-30, cargo nextest -p wcore-agent): replacing that clause with "Some of the conversation was dropped." fails it with the emitted text printed back. Restored, touched, green. INTEG MERGE 2026-08-30 (integ/f13), preserving the re-grade lane's finding: that lane had independently re-anchored this criterion under wayland#1198 after discovering the bare `file:` pointer had already rotted onto an unrelated FluxRouter web_search sources comment. Both arms' line numbers are now stale for a third reason -- this lane moved the notice text out of the engine body into a helper, so in the merged tree the sentence sits at engine.rs:1254-1262, not at either arm's figure. That is exactly why the evidence is a `test:` symbol and not a line: the test travels with the sentence. The wayland#1198 content-fragment requirement does not apply to a `test:` anchor."
  - id: c3
    text: "COMPENSATION: the learned window feeds the pre-flight guard and autocompact, so the truncation stops"
    state: met
    evidence: "test:crates/wcore-agent/src/engine.rs::an_unworkable_window_stops_the_run_and_the_refusal_reaches_the_user"
    owner: core
    note: "eb2f2635 added narrow_to_served_window / resolve_preflight_window / autocompact_threshold_now; both #255 guard call sites and the autocompact trigger route through them, so the guard, the trigger and the reported threshold cannot disagree. Narrowing is gated on CompactConfig::supports_compaction, so a 4,096 window is deliberately not narrowed onto. The spurious-compaction risk this wiring created is wayland-core#353. REFUTED 2026-08-29 by the 0.13.12 close-sweep, recorded verbatim: DOES NOT HOLD AS WRITTEN, and this is the substituted-property failure the brief warns about. The criterion is two clauses. Clause 1 -- 'the learned window feeds the pre-flight guard and autocompact' -- is TRUE and I verified the chokepointing: the #255 guard (engine.rs:13243) and the length-finish check (engine.rs:15089) both call resolve_preflight_window, and should_autocompact_now (engine.rs:18157) calls autocompact_threshold_now; all three route through narrow_to_served_window (engine.rs:8462). The named test passed. Clause 2 -- 'so the truncation stops' -- is FALSE in the exact configuration this ticket reports. narrow_to_served_window returns the window UNCHANGED unless CompactConfig::supports_compaction(served) (compact.rs:608). With MAX_RESERVE_FRACTION=0.55 and BASELINE_TURN_TOKENS=3,118, at the 4,096 slot #1172 measured the scaled ceiling is 2,527 and the threshold 1,844 -- both below 3,118 -- so supports_compaction(4_096) is false and the learned 4,095 is deliberately NOT narrowed onto. Core keeps sizing against UNVERIFIED_CONTEXT_WINDOW=32,768, still sends ~10.5k tokens, and Ollama still truncates. That is verbatim the sentence the ticket used to refuse #1150's close: '32,768 is still 8x the served 4,096 slot, so the truncation persists'. The lane's own test proves the easier property -- a_learned_served_window_narrows_the_preflight_window_when_it_is_workable uses 8,192, not 4,096 -- and its sibling a_learned_served_window_too_small_to_work_in_is_not_narrowed_onto asserts the 4,096 case is skipped. The graded property became 'narrows where workable (8k-32k)'; the ticket asked for the truncation to stop on stock Ollama. NOW CLOSED, by the decision the criterion forces: at 4,096 there is no compaction path to give - core BASELINE_TURN_TOKENS = 3,118 is 76% of the window and compaction shrinks the conversation, not the system prompt or the tool schemas - so core refuses the endpoint out loud instead of pretending. Three changes, all on integ/f13 + lane/f13-n-window: (1) the supports_compaction escape hatch is GONE from narrow_to_served_window (symbol:crates/wcore-agent/src/engine.rs::narrow_to_served_window) - a corroborated served window is now applied unconditionally, so nothing sizes against a window the endpoint has been observed not to serve; (2) symbol:crates/wcore-agent/src/engine.rs::unworkable_window_refusal terminates the run at the TURN-LOOP TOP, before run_compaction, with the minimum workable figure (6,929 at the default reserves, computed by symbol:crates/wcore-config/src/compact.rs::minimum_workable_window, not hardcoded) and the num_ctx / OLLAMA_CONTEXT_LENGTH remedy; (3) should_autocompact_now is gated on the window IN FORCE so the summarizer cannot fire in a window it cannot work in. REPRODUCED FIRST on origin/integ/f13 e7cb6679: repro_1172_c3_core_does_not_size_against_8x_the_served_window panicked `left: Some(128000) / right: Some(4096)`. RED ARM (hetzner-dsm, 2026-08-29): re-inserting the two-line supports_compaction early return into narrow_to_served_window - printed back after the edit, so it landed on the `if` and not on the 12 lines of doc comment above it - turned all three tests red: `assertion left == right failed: core must size against what the endpoint serves, not 31x it`, `... the window in force is the one the endpoint demonstrated, never a wider guess`, `... core must never size against a window the endpoint has been observed NOT to serve`. Restored, touched, 3/3 green. The test that PINNED the defect (a_learned_served_window_too_small_to_work_in_is_not_narrowed_onto, which asserted Some(128_000) here) is replaced rather than kept. OBJECTION CONCEDED 2026-08-30 (round-2 verifier, refutation 1) AND CLOSED. The verifier was right: the criterion's words are 'so the truncation stops', and stopping is the TURN LOOP's job, but the graded token was a_learned_served_window_too_small_to_work_in_is_refused_not_hidden, whose last assertion is unworkable_window_refusal().is_some() - a function returning a String. The verifier deleted the single production call site (engine.rs, the `if let Some(refusal)` block at the turn-loop top) and the whole 9,226-test wcore-agent suite stayed byte-identically green. REPRODUCED HERE, at the rebased tip, on a narrower selection: with that same 13-line block deleted (anchor asserted unique, mutation verified to land on the `if let` + emit_error + `return .await`, not on the 14 lines of comment above it; the compiler then warns `method `unworkable_window_refusal` is never used`, which is itself proof it was the only production caller), 7 of 8 selected tests still PASSED - including all four the lane had been graded on. The evidence token is now a WIRING test: an_unworkable_window_stops_the_run_and_the_refusal_reaches_the_user drives engine.run() to completion on a configured 6,000-token window and asserts the three separable ways the wiring can be missing - the run terminates with FinishReason::Length, the provider is called ZERO times (no prompt the endpoint would silently truncate is ever sent, which IS the data loss this criterion names), and the refusal text reaches the host's error surface via set_error_tap naming minimum_workable_window and the `[compact] context_window` remedy. RED ARM (hetzner-dsm, 2026-08-30, cargo nextest -p wcore-agent): with the call site deleted it FAILS - `the run must TERMINATE on the unworkable window; got AgentResult { text: \\"this must never be produced\\", ... finish_reason: Stop ... }` left: Stop, right: Length. Restored, touched, green. Its control a_workable_window_is_not_refused_and_the_run_proceeds pins the other direction, so an engine that refused every run cannot pass. REFUTATION 2 ALSO CONCEDED AND FIXED: the refusal's four string literals carried 14-18 space runs from unescaped source wrapping, so the operator read 'only 4096 of the prompt                  tokens'. All four now use backslash line-continuations, and the wiring test asserts the EMITTED text contains no double space - cargo fmt and clippy cannot see inside a string literal, so this needed an assertion, not a formatter. ROUND-3 CLOSURE (2026-08-30). The round-2 concession above was written but not gated; this run executed it. The wiring red arm is REPRODUCED at the rebased tip: deleting the whole `if let Some(refusal)` + emit_error + `return finish_run_terminated_inner(..).await` block from the turn loop (anchor asserted unique in-place, mutation printed back, `touch` after mutating AND after restoring) fails an_unworkable_window_stops_the_run_and_the_refusal_reaches_the_user with `left: Stop, right: Length` and the full AgentResult printed - i.e. the exact mutation the round-2 verifier ran and found byte-identically green now reddens. Refutation 2 is closed on BOTH branches of the message, not just the one the wiring test drives: the configured branch is asserted by that test and the LEARNED branch by the_autocompact_trigger_refuses_a_served_window_too_small_to_work_in, each asserting the EMITTED text contains no double space. RED ARMS: dropping the backslash continuation on engine.rs:8776 (configured remedy) fails the first; dropping it on engine.rs:8769 (learned source) fails the second; both print the mangled text back. The lane-diff count of new lines matching `^+.*[^ ] {8,}[^ ]` in SOURCE is 0, against 4 on the round-2 tip (a278f8c3b..a8d501675) - the verifier's own measurement, re-run here as the known-positive control for that grep. FOURTH BOUNDARY ADDED 2026-08-30 (lane w2-window-arc, 115cb4c6), closing FerroxLabs/wayland#1210 c3. This note's claim that 'the guard, the trigger and the reported threshold cannot disagree' was TRUE of the three boundaries it names and FALSE of the set: the emergency hard stop was a fourth window-derived boundary and it re-resolved the window from config + provider + model alone, so on a corroborated 8,192 served window it reported and enforced 29,768 while the ceiling was 5,053 and the threshold 3,688. The claim is now made true rather than narrowed: emergency::emergency_limit takes the window as an ARGUMENT, AgentEngine::emergency_limit_tokens passes compaction_window_now(), and run_compaction's emergency check calls that one function for both the test and the reported figure. The four boundaries in force are the #255 pre-flight ceiling, the length-finish check, the autocompact trigger and the emergency stop, and all four resolve through narrow_to_served_window."
---

Detection shipped in v0.13.10. Compensation did not — and this ticket's BODY
sets that bar, not its title.

The reporter's own words: "32,768 is still 8x the served 4,096 slot, so the
truncation persists". Core now knows the real window and says so, which turns
a silent wrong answer into a visible one. The prompt is still truncated.

c3 is blocked on a real ordering problem, not on effort: wiring the learned
window into the guard today makes a small-window run fail outright, because
the fixed buffers saturate to zero at 4,096. #1179 is that work.

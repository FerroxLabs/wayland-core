---
issue: 365
repo: FerroxLabs/wayland-core
kind: defect
title: "Container backend latches on a leftover container name, and attests a run that never happened"
status: closed
last_verified_commit: 93ede3424
criteria:
  - id: c1
    text: "A container left in Created under the name a new task would take does not fail that task -- either the name carries the nonce, or the submit path clears a conflicting name before docker run"
    state: met
    evidence: "test:crates/wcore-exec-backend/tests/container_wedge.rs::the_leftover_is_actually_reclaimed_and_not_merely_stepped_around"
    owner: core
    note: "CHOSE: the submit path clears the name; the name stays wayland-f25-{task_id}. Justified against BOTH call sites the choice touches. cancel() reconstructs the name from the task id ALONE when the registry entry has no handle (entry.handle.unwrap_or_else(|| container_name(task_id))) -- a nonce-bearing name is not reconstructable from a task id, so that fallback would build a name no container ever had, `docker rm -f` would no-op, and cancel() would still report a cleanup it did not perform. scan_orphans() enumerates by LABEL, never by name, so it is indifferent to the naming scheme itself -- but a nonce-bearing name would leave every wedge in place forever, trading a loud fixable conflict for a silent unbounded leak (that residue is the separate defect, see c6). GUARDS on the destructive step, all measured: a RUNNING holder is refused, never removed (test: a_running_container_holding_the_name_is_refused_never_removed asserts the live container is still Running afterwards); a holder carrying no wayland.task.nonce label is refused as another owner's (test: an_unlabelled_container_holding_the_name_belongs_to_someone_else). The label KEY is checked, not a match against this task's nonce, because the leftover being cleared is by definition from an EARLIER run under an earlier nonce -- an exact-nonce guard would refuse precisely the case this exists for. Removal uses plain `docker rm`, never `rm -f`, so a start racing the removal makes the removal fail instead of killing the container. LIVE-PROVEN on the real CLI, not only in tests: with wayland-f25-f25-reference wedged in Created, `wayland-core backend run --backend container` printed `terminal: Success` and published its artifact, exit 0. The non-destructive half was live-proven too: with a RUNNING container holding the name, the CLI printed `backend container is unavailable: a RUNNING container is already named wayland-f25-f25-reference (task nonce \"some-other-live-task\"); refusing to remove a live task's container`, exit 1, and `docker inspect --format {{.State.Running}}` still reported true afterwards."
  - id: c2
    text: "A daemon-level refusal is not reported as a task exit: exit-125 from docker run produces a distinct outcome, never a receipt asserting the task ran and returned 125. 126 and 127 considered in the same pass"
    state: met
    evidence: "test:crates/wcore-exec-backend/tests/container_wedge.rs::a_client_side_refusal_yields_no_receipt_either"
    owner: core
    note: "REFUTED ONCE, THEN FIXED PROPERLY. The first pass keyed on the CONJUNCTION of exit 125 and docker's `Error response from daemon:` line. An adversarial verifier refuted it with a live reproduction, and the refutation was REPRODUCED here before anything was changed: that marker only appears on DAEMON-side refusals, and docker's 125 class is bigger than the daemon. Measured on docker 29.2.1 -- `docker run --rm BadImage:Tag true` exits 125 with `docker: invalid reference format: repository name (library/BadImage) must be lowercase` and a marker count of ZERO, and `docker run --rm --memory notanumber busybox:1.36 true` exits 125 with `invalid argument \"notanumber\" for \"-m, --memory\" flag` and a marker count of ZERO. Both mapped to DockerExit::TaskExit(125). WHY THE FIRST PASS MISSED IT: it enumerated only daemon-side refusals (name conflict, --memory 1, absent image) and argued safety in one direction only. DISCRIMINATOR NOW, and it is not text. Two structural facts, both from the daemon: (1) `--cidfile`, which docker writes if and only if ContainerCreate succeeded, so its ABSENCE is positive proof no container exists; (2) `.State.StartedAt`, whose zero value `0001-01-01T00:00:00Z` is the daemon's own record that a created container never started its process. stderr is still parsed, but only to choose which lines the operator is SHOWN -- it no longer participates in any verdict. FULL ENUMERATION BEFORE CHOOSING, every row measured on this host (CLI = docker run's own status; the rest are the daemon's): client bad image reference 125/no cidfile; client bad flag value 125/no cidfile; client daemon-unreachable 1/no cidfile; client stale cidfile 125/refused before the run; daemon name conflict 125/no cidfile; daemon --memory 1 125/no cidfile; daemon absent image or pull failure 125/no cidfile; daemon start failure (bad network) CLI 125, cidfile written, StartedAt ZERO, .State.ExitCode 128; daemon start failure (bad device) CLI 127, cidfile written, StartedAt ZERO, .State.ExitCode 128; argv not executable CLI 126, StartedAt ZERO, .State.ExitCode 126; argv not found CLI 127, StartedAt ZERO, .State.ExitCode 127; task self-exits 0/7/125/126 with a real StartedAt and a matching .State.ExitCode. docker binary absent never reaches the classifier (the spawn fails). Timeout is unchanged: it returns ExecError::Exec, calls cancel(), and now also clears the id file. TWO ROWS DECIDED THE DESIGN. `task self-exits 125` must keep a real receipt, and it does, because the daemon records a real StartedAt -- that is the polarity the first pass got RIGHT and that overcorrecting would have destroyed. `start failure (bad device)` exits 127 at the CLI while the daemon records 128 and a zero StartedAt: the CLI code alone would have called that a task exit, and the old rule DID. That case is now fixed as a side effect, as is `daemon unreachable`, which exits 1 and previously became a receipt asserting exit-1. `--rm` HAD TO GO. Measured: with --rm, `docker inspect` by the recorded id returns `no such object` after the run for a start failure exactly as it does for a clean exit, so the daemon retains nothing to be asked and the created-vs-started distinction is destroyed before it can be read. The backend now removes its own container (`remove_container`). TRADEOFF STATED: --rm was daemon-side and cleaned up even if this process was killed mid-run; an explicit removal cannot. What catches that window is the machinery this issue already built -- the deterministic name keeps reclaim_container_name working on the next submit, and the wayland.task.nonce label keeps scan_orphans able to see it. Same class as #366. 126/127 DISPOSITION KEPT, on a structural field instead of a string. The daemon assigns .State.ExitCode 126/127 for a failed exec of the TASK'S ARGV and 128 for a start failure of its own making, so the attribution the stderr text was standing in for is available as a number. They stay task exits: `Failure { code: \"exit-127\" }` is the conventional truthful encoding of \"the argv you submitted is not there\", and reclassifying it as an unavailable BACKEND would make a typo read as a broken transport. DIRECTION OF ERROR: every uncertain case resolves to a refusal. A missing id file, a malformed one, an inspect the daemon will not answer, or an unclearable cidfile path all yield ExecError::Unavailable and no receipt. That is a strictly WEAKER claim than any attestation, so the failure mode costs a real run its receipt and can never manufacture one. RED ARMS, BOTH DIRECTIONS, each proven to land on executable code by grep context before running. ARM A (under-correct), container.rs:223 `return DockerExit::NeverRan(docker_diagnostic(docker_stderr));` -- an executable return inside the `let-else`, between comment line 222 and `};` on 224 -- replaced with `return DockerExit::TaskExit(125);`. Result 5 red, load-bearing message verbatim: `a refusal the docker CLIENT made must NOT produce a receipt asserting the task ran: Failure { code: \"exit-125\" }`. LIVE under that mutation the CLI reproduced the refutation character for character: `terminal: Failure { code: \"exit-125\" }`, exit 0, a 2527-byte receipt with a real ed25519 attestation (key_id 6c4aab85dbe77d42...), and `docker ps -a --filter label=wayland.task.nonce` showing ZERO containers ever created. Both polarity tests stayed GREEN under arm A. ARM B (over-correct), container.rs:225 `if state.started {` mutated to `if state.started && state.exit_code != 125 {`. Result exactly 2 red -- `a_task_that_exits_125_on_its_own_is_still_a_real_run` and its live sibling -- verbatim: `a task that really ran must produce a receipt, not a refusal: Unavailable { backend_id: \"container\", detail: \"task wedge365-self125 was never executed\" ... }`, while all four refusal tests stayed GREEN. The two arms redden disjoint test sets, which is what proves neither guard is vacuous. Both restored with `git checkout HEAD --` then `touch`; restored sha256 c69113e817b37a33 equals the pre-mutation sha exactly, `git status --porcelain` empty, and no mutation marker remains in the file. LIVE-PROVEN ON THE REAL CLI, restored tree, RUST_LOG unset. Client-side: `wayland-core backend: backend container is unavailable: task f25-reference was never executed -- no container process started: docker: invalid reference format: repository name (library/BadImage) must be lowercase`, exit 1, NO receipt file. Daemon-side is unregressed: same shape carrying `Error response from daemon: pull access denied for wayland-f25-absent-365 ...`, exit 1, NO receipt. Healthy run unregressed: `terminal: Success`, exit 0, a 2957-byte receipt. Nothing left behind: `docker ps -a --filter label=wayland.task.nonce` empty afterwards. REGRESSION TEST FOR THE CLASS NOBODY THOUGHT OF: `backends::container::tests::a_client_side_refusal_is_not_a_task_exit_though_it_prints_no_daemon_line` asserts for each fixture that the daemon marker is ABSENT before classifying it, so it stays meaningful only for the client-side class and fails immediately if any future discriminator returns to reading text; and `container_wedge::a_client_side_refusal_yields_no_receipt_either` drives the real backend against the real docker client end to end. RESIDUAL, not closed: a custom WAYLAND_EXEC_CONTAINER_IMAGE whose image has `/task` as a FILE makes the bind mount fail at start -- measured CLI 125, StartedAt ZERO, .State.ExitCode 127 -- which this rule reads as an argv exec failure and reports as exit-127 rather than as a refusal. The daemon gives 127 for both, so only message text separates them, and text is what was just removed. It is a mis-attributed FAILURE, never a manufactured success."
  - id: c3
    text: "The daemon's stderr reaches the operator on a daemon-level failure rather than being captured into a receipt nobody reads"
    state: met
    evidence: "test:crates/wcore-exec-backend/src/backends/container.rs::the_conflict_message_the_operator_needs_survives_classification"
    owner: core
    note: "The polarity trap is avoided: there is no warn! anywhere in the fix. The daemon's own lines are extracted by daemon_diagnostic() into the ExecError::Unavailable detail, which is a RETURN VALUE the caller cannot ignore, and wcore-cli/src/main.rs:1720 prints it with an unconditional `eprintln!(\"wayland-core backend: {e:#}\")` -- no RUST_LOG, no log level, no tracing subscriber in the path. Confirmed independently during red arm A, where the failure detail read: `execute failed: backend container is unavailable: the container daemon refused to run task wedge365-container-ok: docker: Error response from daemon: Conflict. The container name \"/wayland-f25-wedge365-container-ok\" is already in use by container \"cbdd253555e0...\". You have to remove (or rename) that container to be able to reuse that name.` -- the exact message c3 asks for, in place of the bare `Failure { code: \"exit-125\" }`. The `Run 'docker run --help'` footer is filtered as noise. LIVE-PROVEN end to end with RUST_LOG unset: `wayland-core backend: backend container is unavailable: the container daemon refused to run task f25-reference: docker: Error response from daemon: pull access denied for wayland-f25-absent-365, repository does not exist or may require 'docker login'` -- printed on stderr by the CLI with no log level configured. grep confirms container.rs contains no warn!/info!/debug!/tracing:: call at all; the only occurrence of the word is the comment recording why."
  - id: c4
    text: "A red arm is quoted verbatim: the new guard reverted, the test failing, restored and green, with the mutation shown to have landed on code"
    state: met
    evidence: "commit:df53b9ab"
    owner: core
    note: "TWO mutations, each proven on code by grep context before running. ARM A, the c1 guard: line 406 `reclaim_container_name(&name).await?;` sat between comment line 405 and statement line 407 `let started = now_unix_ms();` -- an executable statement, replaced by `let _ = reclaim_container_name;`. Result 4 of 5 red, with `execute failed: backend container is unavailable: ... Conflict. The container name ... is already in use`. ARM B, the c2 guard: line 162 `125 if daemon_spoke => DockerExit::DaemonRefusal(daemon_diagnostic(&text)),` -- an executable match arm, mutated to `125 if daemon_spoke && false =>`. Result 3 red, the load-bearing one verbatim: `a task the daemon refused to create must NOT produce a receipt asserting it ran: Failure { code: \"exit-125\" }`. Note `a_task_that_exits_125_on_its_own_is_still_a_real_run` correctly stayed GREEN under arm B, which is what distinguishes a mutation that landed from one that merely broke the file. Both arms restored with `git checkout HEAD --` followed by `touch` (mtime, so cargo could not measure a stale binary); the restored file sha 24cbf376 differs from the pre-fix 463f2751; `grep -rn MUTATED` returns nothing and `git status --porcelain` is empty; 143/143 green after."
  - id: c5
    text: "conformance_matrix passes on a host that has run it before with a leftover container present -- the regression test creates the wedged container itself rather than assuming a clean daemon"
    state: met
    evidence: "test:crates/wcore-exec-backend/tests/container_wedge.rs::conformance_passes_with_a_leftover_container_already_holding_the_name"
    owner: core
    note: "The test wedges the daemon ITSELF with `docker create` (asserting the container reaches Created, the one state `docker run --rm` cannot clean up because --rm removes on EXIT) and then drives the whole run_conformance harness over it. PROVEN RED ON A FRESH HOST, which is the point: `docker ps -a | grep wayland-f25` returned nothing before the arm, exactly like a hosted CI runner, and with the pre-fix container.rs restored from 64a2673e all 5 tests in the binary failed -- `[RED] a successful run publishes a content-addressed artifact - Failure { code: \"exit-125\" }`, character-identical to the symptom in the issue. Restored, touched, 5/5 green. The leftover is planted under a DIFFERENT nonce (wedge365-nonce-from-an-earlier-run) because a real leftover is from an earlier run; a fix keyed on the current nonce would not clear it. Ids are prefixed wedge365- so they cannot collide with conformance_matrix's own deterministic names. Cleanup runs BEFORE every assertion, verified: `docker ps -a | grep wayland-f25` returned nothing after the red arm as well as after the green one, so a failing run leaves the next lane nothing."
  - id: c6
    text: "The orphan-scan path is checked for the same latch: state whether docker ps -a --filter label=wayland.task.nonce= would have found these two, and if not, why the label was absent"
    state: met
    evidence: "symbol:crates/wcore-exec-backend/src/backends/container.rs::list_containers_with_nonce"
    owner: core
    handoff: FerroxLabs/wayland-core#366
    note: "ANSWERED BY INSPECTING A REAL WEDGED CONTAINER, not assumed. The label is PRESENT -- labels are applied at CREATE time, so a container that reaches Created and never starts still carries them: `docker inspect --format '{{json .Config.Labels}}'` on a real wedge returned {\"wayland.task.nonce\":\"nonce-from-yesterdays-run\"}, and `docker ps -a --filter label=wayland.task.nonce=nonce-from-yesterdays-run` returned it. The 'a container that never started carries no label' hypothesis is REFUTED. The scan did not find them because it was never asked: scan_orphans is NONCE-SCOPED and no caller can supply a leftover's nonce. conformance.rs:340 scans {prefix}-nonce-never-used, chosen precisely so nothing can be found. cancel() scans entry.nonce from the live registry, and a leftover's run already called registry::forget. In ordinary use the nonce is fresh per run, so a scan for the CURRENT nonce is structurally incapable of returning a PREVIOUS run's leftover -- demonstrated: the same container that the old-nonce filter returned came back empty under the new-nonce filter. The query that would have found it is one character shorter, `--filter label=wayland.task.nonce` on key presence, and nothing in the product issues it. VERDICT: TWO defects, not one. The scan gap SURVIVES this fix -- the submit-path reclaim only fires when that exact task id is submitted again, so a task id that runs once, wedges, and is never resubmitted leaks a container that no submit reclaims and no scan reports. Filed as FerroxLabs/wayland-core#366 with its own six acceptance criteria (d1-d6), including that conformance.rs:340 asserts found.is_empty() for a nonce nothing can ever have used -- a check that cannot fail on that axis."
---

Found while auditing why `integ/f13` reported 2 nextest failures during the 0.13.12
integration. Those two had been recorded in a handoff as "the known `conformance_matrix`
exclusion". They were not. That framing was wrong, and this is what was actually
happening -- the test was doing exactly its job and reporting a real product defect on
the only kind of host that can exhibit it.

`conformance_matrix`'s own module docs state the contract it was failing under: "fails
only on a backend that was exercised and failed -- an honestly reported unavailable
surface is a result, not a red." The container backend WAS exercised (the daemon answered
a version ping) and it DID fail. Reading the failure as an unexercised-surface skip is
what let it sit for a day.

The fix keeps that property and adds the one the harness could not have: `container_wedge`
creates the condition itself, so it is red on a FRESH host. The defect survived because
every gate ran somewhere the deterministic name was still free.

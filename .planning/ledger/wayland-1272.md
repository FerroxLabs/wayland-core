---
issue: 1272
repo: FerroxLabs/wayland
kind: defect
title: "0.13.12 release board: all 32 blocking issues mapped to an owner (17 in flight, 15 unassigned)"
status: open
last_verified_commit: 6342f1b90
criteria:
  - id: c1
    text: "Every issue in `check-release-readiness.py`'s blocking list appears on this board with a named owner, and the board is updated whenever the list changes. An unowned issue is a bug in this ticket."
    state: met
    evidence: "file:.github/workflows/ci.yml:2024:Release board tracks the blocking list (wayland#1272 c1)"
    owner: core
    note: "MET 2026-09-10 ON THE ENFORCEMENT, and read the RED at the end of this note before believing the word met. c1 has two clauses: every blocking issue on the board with a NAMED OWNER, and the board UPDATED WHENEVER THE LIST CHANGES. scripts/check-release-board.py decides both -- it fails on a row that blocks and is missing, a row that no longer blocks, a row whose owner is empty or UNOWNED, and a row whose outstanding count has moved; owners are READ from each ledger's owner: on its UNMET criteria rather than assigned, so a row cannot claim an owner the ledger does not have. RUN WITHOUT --write at 335328967, all three arms: `--self-test` -> `SELF-TEST OK: missing, stale, unowned rejected; matching accepted`; `--offline` -> `OK: board carries all 31 blocking issue(s), each owned, counts current`; and the ONLINE arm, same sentence, same 31. THE DEFECT FOUND WHILE GRADING: `grep -rn check-release-board .github/ justfile` returned NOTHING -- the gate was in no workflow, no justfile recipe and no release step. A drift detector nobody runs satisfies the first clause on the day a human remembers and satisfies the second NEVER, which is the hand-maintained board this ticket was written against wearing a script's clothes. Closed by wiring it where the blocking list actually moves: the ci-linux step `Release board tracks the blocking list (wayland#1272 c1)` at ci.yml:2024 (commit 5ca1e7857), plus `just release-board` / `just release-board-live`. DELIBERATELY NOT in `just check-all`: the board is the integrator's file and a gate that reds on every in-progress lane gets bypassed and then deleted, which is the reasoning the release-readiness gate above it already records. THE BOARD IS RED AT THIS TREE, BY THIS LANE'S OWN HAND, AND IT IS LEFT RED ON PURPOSE. Grading wayland-core#386's three criteria met in this same commit changed the blocking list, and `python3 scripts/check-release-board.py --offline` now exits 1 with exactly the two rows it should name: `ON THE BOARD BUT NO LONGER BLOCKS  FerroxLabs/wayland-core#386` and `OUTSTANDING COUNT MOVED  FerroxLabs/wayland#1272: board 3, actual 2`. That is a REAL-DRIFT positive control the --self-test cannot give, and it is the second clause working rather than failing -- the list moved and the tree said so within one commit. REGENERATION IS THE INTEGRATOR'S ACT: .planning/RELEASE-BOARD.md is generated, this lane is forbidden --write, and a regenerated copy on a lane branch conflicts at every integration. WHAT IS OWED TO KEEP THIS MET: `python3 scripts/check-release-board.py --write` at integration, before the ci-linux step above can pass. THE HONEST CONTESTABLE POINT, stated so a reader can overturn it: read literally, clause two is FALSE at this exact tree, because the list changed and the board has not been regenerated. It is graded met because the criterion's subject is the board's ability to rot SILENTLY, and it can no longer do so; on the literal reading c1 could never be met on any lane branch, which is the same defect c4 on this ticket has. LIMIT ONE: the CI arm runs --offline, because the repo-scoped GITHUB_TOKEN cannot read the second tracker; the online arm additionally corroborates kind: against tracker labels and resolves handoffs, so it can produce a SMALLER blocking set. Both arms were run by hand today and agreed at 31, but if they ever diverge this step reds on a board that is correct online. LIMIT TWO: ci.yml fires on push/pull_request to main, so between integrations the board can still be stale on a lane branch and nothing says so -- which is precisely the state this tree is in. PREVIOUS: Transcribed from the issue body verbatim on 2026-08-31. This ledger did not exist until then, and c1 had never been graded against a tree by any lane. SECOND CLAUSE NOW TRUE AT THE TREE, which the grading lane flagged as its most overturnable call and could not fix from a lane branch. It graded c1 met while `check-release-board.py` FAILED on its own branch -- grading core#386 had moved the blocking list and the board was stale -- so on a literal reading the clause `the board is updated whenever the list changes` was false where it was graded. At integration the board was regenerated after every graft, and at this commit all three arms are green: --self-test `SELF-TEST OK: missing, stale, unowned rejected; matching accepted`, and --offline `OK: board carries all 28 blocking issue(s), each owned, counts current`. The stale state the lane saw was the gate working, not the gate failing -- it is a real-drift positive control the self-test cannot manufacture. Recorded rather than dropped, because a reader who finds this row met should be able to see that the clause was once false and why it is not now."
  - id: c2
    text: "Tranche 3a is assigned to lanes and each of its 6 issues reaches CLOSED or DECOMPOSED — no issue filed by our own verification is left unowned because it was found late."
    state: not-met
    owner: core
    note: "NOT MET, re-verified 2026-09-10 by the w15/rec lane against fdf4b1e1c; section 2.1 of .planning/RECORD-1269-1272-1324-2026-09-10-w15.md. Five of six reached CLOSED/COMPLETED on the tracker: wayland#1231 2026-08-31T13:36:13Z, wayland#1252 2026-08-31T13:36:30Z (and decomposed -- wayland#1276 is its c3 split, read OPEN at milestone 0.13.14 on 2026-09-10), wayland#1254 2026-09-04T16:58:18Z, wayland-core#400 2026-08-31T13:37:45Z, wayland-core#393 2026-08-31T13:37:37Z. wayland#1256 was read from the tracker again on 2026-09-10 and is still OPEN with no stateReason, milestone 0.13.14, label area:core -- neither closed nor decomposed. Its ledger at fdf4b1e1c is status open with c1 met, c2 met, c3 NOT-MET, owner core and NO handoff, and c3s own note says the corpus instance is closed totally while the class is not, and that this criterion is why the issue stays open. The residual is exact: a lane still cannot be prevented from reporting a tree green while a crate it chose not to run is red. Two candidate closures are named there and neither is decided. Nothing moved between 2fe7a70df and fdf4b1e1c. Administrative closure of #1256 would make this criterion read met over a live gap, so it stays not-met and names wayland#1256 c3 as the whole of what is owed. LIMIT: this grades tracker STATE and ledger state, not whether #1256 c3 is close to being closed."
  - id: c3
    text: "Tranche 3b's 9 residuals are each either closed, decomposed with a `handoff:`, or explicitly recorded as a decision that is not core's to take."
    state: met
    evidence: "file:.planning/RECON-1269-1272-1324-2026-09-10.md:222:Three are OPEN and each carries the record the criterion demands"
    owner: core
    note: "MET 2026-09-10, all nine dispositioned, per-row detail in .planning/RECON-1269-1272-1324-2026-09-10.md. Six are CLOSED/COMPLETED on the tracker: wayland-core#382 2026-08-31T13:37:30Z, wayland-core#389 2026-08-31T17:04:18Z, wayland-core#369 2026-08-31T13:37:16Z, wayland#908 2026-08-31T13:36:45Z, wayland#1203 2026-08-31T07:24:02Z, wayland#1150 2026-08-31T15:33:11Z. Three are OPEN and each carries the required record in the ledger FIELD rather than in prose. wayland-core#368: c1-c5 all state blocked, owner maintainer, handoff FerroxLabs/wayland-core#410, c6 met -- that is the decision-not-core's-to-take arm, and #410 is OPEN at milestone 0.13.12. wayland#559: c4 blocked, owner desktop, handoff FerroxLabs/wayland#1193 (OPEN), other six met. wayland#388: c4 blocked, owner flux, handoff FerroxLabs/wayland#1184 (OPEN); c7 met with handoff FerroxLabs/wayland#1237 which is CLOSED/COMPLETED so that leg is done; other ten met. Every handoff target was checked to exist and to be open where it still carries work, and none of the three is blocked on core by core -- the failure mode this criterion exists to catch. LIMIT: this grades DISPOSITION, which is what the criterion asks. It does not re-verify the six closures against the tree, and it does not judge whether the three carriers will be worked."
  - id: c4
    text: "`wayland#1203` c3 and any other criterion requiring a **live run** is either measured on real hardware or restated as what the tree can actually grade — a criterion that cannot be run is as worthless as one that cannot fail."
    state: not-met
    owner: core
    note: "STILL NOT-MET, and the RESTATEMENT this criterion has been owing is now ON THE ISSUE: https://github.com/FerroxLabs/wayland/issues/1272#issuecomment-5614433819 -- posted there rather than narrowed here, per this ledger's own rule that sharpening a criterion inside the ledger is how it quietly becomes an easier adjacent property. WHAT THE COMMENT SAYS. Defect 1, the enumeration matches INSIDE WORDS: the sweep's case-insensitive `pty` is a substring of EMPTY, which is how wayland#1328 c4 (absent versus explicit-empty lists) entered a live-run sweep it does not belong in; the mirror image is `soak` matching prose -- wayland-core#410 c1 matched only on `accept a permanently amber soak` and is a maintainer DECISION, not a run. Any sweep that decides this must anchor word boundaries on pty and soak and must state its exclusions row by row. Defect 2, it QUANTIFIES OVER A GROWING SET: `any other criterion requiring a live run` ranges over a ledger set that gained 29 in-scope issues in two days, so it is true at the commit you grade it on and false again at the next filing -- a criterion with no permanently reachable pass state, which is the same worthlessness its own last clause names from the other side. THE RESTATEMENT PROPOSED, in two halves: c4a, bounded at a NAMED commit -- every row selected by the word-anchored sweep is either measured on real hardware with the receipt recorded in its own ledger entry, or carries a note: naming the HOST CLASS that can run it (hetzner-dsm / GitHub Actions / SeanDesktop / none) and why this cycle did not; criteria filed after that commit are the next sweep's business. c4b, standing -- scripts/check-criteria-ledger.py refuses a not-met criterion whose text matches the anchored live-run pattern and whose note: names no host class, with a self-test arm proving it fires in that direction and stays silent on the control. c4b is the half that survives the next filing. TWO ROWS OF THE TWELVE MOVED TODAY: wayland-core#386 c1 and c2 are now MEASURED, on run 33841172783 -- a real 2026-09-04 nightly-windows-soak tick with windows-live-acceptance red, windows-soak and keyring-blob-size green, decided action=report, close step SKIPPED, tracker issue #443 opened against the live Octokit -- and #386 c3's ceiling record is posted on that issue. So nothing in the twelve-row set is un-runnable for want of hardware; the two genuinely stuck rows are stuck on a human decision (core#410) and a credential (#1228), and both already record that. IT STAYS NOT-MET on two counts: restating c4 is not the same act as discharging the nine runnable rows it ranges over, and the restatement is PROPOSED on the issue rather than adopted into the issue body -- only the maintainer edits that, and until they do, the criterion the tree grades is still the ungradeable one. LIMIT: this lane measured nothing itself; the #386 rows are a run that already existed, read back from the Actions job API, not a run this lane caused, and no other row in the set was run. PREVIOUS GRADE, retained, from .planning/RECORD-1269-1272-1324-2026-09-10-w15.md section 2.2 against fdf4b1e1c: the wayland#1203 c3 half IS discharged (state superseded, handoff FerroxLabs/wayland#1244 which is OPEN at milestone 0.13.14; two of its three legs measured live on hetzner, the third restated on #1244 as a PTY-driven run). The twelve-row union was graded for RUNNABILITY rather than ownership -- hetzner-dsm: wayland#1244 c1 and c3, wayland#1309 c1, wayland#1349 c2. GitHub Actions only: wayland-core#386 c1 and c2, wayland-core#411 c1. SeanDesktop, the only Windows box: wayland-core#415 c1, and wayland#305 c4 (owner desktop, not core's to run). Needs no host: wayland-core#386 c3. Not runnable by any lane: wayland#1228 c1, a credential acquisition, correctly blocked/maintainer. Not a live-run criterion at all: wayland-core#410 c1."
---

Created 2026-08-31 to close a COVERAGE gap. c2, c3 and c4 were graded
2026-09-10 by the w14/recon reconciliation lane against `2fe7a70df`; the
per-issue tracker states, handoff-target checks and the live-run sweep are in
`.planning/RECON-1269-1272-1324-2026-09-10.md`. c2 and c4 were then re-graded
the same day by the `w15/rec` lane against `fdf4b1e1c` — tracker re-reads, the
twelve-row live-run RUNNABILITY grading, and the two defects found in the sweep
itself are in `.planning/RECORD-1269-1272-1324-2026-09-10-w15.md` section 2.
`c3` was not re-graded and its entry is carried forward from `2fe7a70df`
unchanged; its `met` evidence anchor was confirmed to still resolve.

`c1` was graded for the first time on 2026-09-10 by the `w15/prog` gate-B
lane, against `335328967`, and is now `met`. The grading found that
`scripts/check-release-board.py` — the gate that decides both of c1's clauses —
was wired into no workflow, no justfile recipe and no release step, so the
"updated whenever the list changes" half could never have held. It now runs in
`ci-linux`. The file-level `last_verified_commit` is `5ca1e7857`, the commit
that wired it; c2 and c3 were last graded at `fdf4b1e1c` and were not re-run
here.

`scripts/check-criteria-ledger.py` scopes every open `area:core` issue on
wayland and EVERY open issue on wayland-core. This issue was in scope from
the moment it was filed and had no ledger file, so
`scripts/check-release-readiness.py` -- which reads ledger files and nothing
else -- could not count it. CI runs the coverage gate with `--offline`, the
arm that would have reported the gap, so nothing said so for two days.

Criteria are transcribed from the issue body without edit. Where the body's
wording is loose it is LEFT loose rather than tightened here: sharpening a
criterion inside the ledger is how a criterion quietly becomes an easier
adjacent property. Whoever takes this restates it on the ISSUE first.

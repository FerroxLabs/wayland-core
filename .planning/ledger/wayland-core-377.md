---
issue: 377
repo: FerroxLabs/wayland-core
kind: defect
title: "@dir with an escaping spelling resolves to a silently empty payload: no files, no error, no warning"
status: open
last_verified_commit: c680860b3
criteria:
  - id: c1
    text: "@../repo/ and @/abs/outside/ either attach their files or are REFUSED with a message the user sees -- never Ok with files: []"
    state: met
    evidence: "test:crates/wcore-cli/src/tui/commands/at_ref_resolve.rs::a_dotdot_at_dir_attaches_the_tree_and_still_obeys_the_gitignore"
    owner: core
    note: "CLOSED 2026-08-30 (lane f13-atref-guard-b), alongside FerroxLabs/wayland-core#335 c3, which is where this defect was found. REPRODUCED FIRST at origin/integ/f13 a278f8c3b, not modelled: the new tests were run against the unfixed walk and both went red - 'an escaping `@dir` spelling resolved to a silently empty payload: AtPayload { kind: Dir, files: [], text: \"\", warnings: [SkippedFiles { count: 3 }] }' and 'an absolute `@dir` outside the workspace resolved to an empty payload: AtPayload { kind: Dir, files: [], text: \"\", warnings: [SkippedFiles { count: 2 }] }'. CAUSE: walk_dir conflated two different directories under one `root` - the workspace root, which is the .gitignore's JURISDICTION, and the directory the reference NAMES, which is what the walk may not LEAVE. Every entry was named with rel_to_root(&path, root) and the None case was dropped, so an escaping spelling dropped every entry. FIX: symbol:crates/wcore-cli/src/tui/commands/at_ref_resolve.rs::WalkScope separates them; an entry the workspace cannot name is named by the user's own spelling instead, which is the same answer resolve_file already gives an escaping @file. RED ARM, verbatim, restoring the pre-fix drop (WalkScope::name's `None => Some(self.spelled.join(rel_to_root(path, self.base)?))` -> `None => None`, diff-verified to land on the match arm): the two messages quoted above. Restored and re-run: 'test result: ok. 73 passed; 0 failed'. Both spellings now ATTACH (the Q1 decision this issue exists to implement, already delivered for @file); the absolute arm is an_absolute_at_dir_outside_the_workspace_attaches_its_files. ORIGINAL FILING: Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D3, found while verifying FerroxLabs/wayland-core#335 — '@-ref: absolute paths escape the workspace root and skip the gitignore check'). Nothing has been done. The measured finding, verbatim: `@dir` with any spelling that escapes the lexical root resolves to a SILENTLY EMPTY payload — no error, no warning, no skipped-file count. Measured on origin/integ/f13 (5eb2d1ef), not modelled: `@../repo/` against a workspace containing `top.txt` and `src/a.rs` returns `Ok(AtPayload { kind: Dir, files: [], text: '', warnings: [] })`; `@/abs/path/outside/` against a directory containing `a.txt` and `b.txt` returns the same. Control in the same run: `@<abs path of the workspace root>/` correctly returns `files: [a.txt]`, so the query is not broken — only the escaping spellings vanish."
  - id: c2
    text: "The `continue` in walk_dir that drops an entry increments the skipped counter, so AtWarning::SkippedFiles is emitted whenever any entry is dropped"
    state: met
    evidence: test:crates/wcore-cli/src/tui/commands/at_ref_resolve.rs::every_silent_exit_in_walk_dir_is_counted_or_justified
    owner: core
    note: "REGRADED and its gate WIDENED 2026-08-30 after the previous pass's own claim was refuted by a red arm. The decision stands and is unchanged: a pruned VCS store IS a drop and is counted (a_pruned_vcs_store_is_counted_as_a_skipped_entry, Some(1)); a directory reached twice is NOT a drop and is not counted (a_directory_reached_twice_is_walked_once_and_not_counted_as_skipped, None) because its files are IN the payload. What changed is the SHAPE gate behind them. The previous pass wrote that its gate made an N+1 `impossible: every `continue;` in walk_dir must be immediately preceded by *skipped += 1;`. That was REASONED, not measured, and it is FALSE -- it graded one SPELLING of one exit, and which spellings a future author uses to drop an entry is an open alphabet. MEASURED, three red arms on this tree, each compiled first (cargo check -p wcore-cli --tests exit 0) and each applied to the SAME counted arm (`if !meta.is_file()`): (A) drop the counter, leaving a bare `continue;` -- old gate RED, so its own spelling was covered; (C) `return Ok(());` instead -- drops every REMAINING entry in the directory with no SkippedFiles warning, compiles, is `cargo fmt --check` CLEAN (so fmt does not save it the way it normalises an inline `if x { continue; }`), and the OLD GATE PASSED IT (exit 0); (D) `break;` -- never seen by any version of the gate. The fix is not a fourth case. What can end an iteration is not an open alphabet: in Rust it is exactly `continue`, `break`, `return`, so `does this line use one of those three keywords` is decidable and total over the function, and every future drop lands in it whatever it is called. every_silent_exit_in_walk_dir_is_counted_or_justified now scans that alphabet with comments stripped and token boundaries on both sides. AFTER: all three of A, C and D go RED (exit 100), naming the offending line -- `line 488: return Ok(());`, `line 488: continue;`, `line 488: break;`. The two truncation `return Ok(())`s carry explicit `NOT A DROP:` notes: truncation is reported through `*truncated`, which is a different signal, not silence. ANTI-VACUITY, both directions: the exit count control rose 8 -> 13 (the old gate saw 11 exits where there are 13, and that gap is exactly the two returns it could not see), and `uses_keyword` carries known-positive controls in the same run on a bare exit of each keyword, on a value-carrying and a LABELLED exit, and two known-NEGATIVES it must refuse -- a `continue` inside a comment and the identifier `returned` -- because a source-scanning gate whose matcher stops matching reads as `no silent exits`."
  - id: c3
    text: "A test drives resolve() for both escaping spellings and asserts the outcome, shown RED against today's code"
    state: met
    evidence: "test:crates/wcore-cli/src/tui/commands/at_ref_resolve.rs::an_absolute_at_dir_outside_the_workspace_attaches_its_files"
    owner: core
    note: "CLOSED 2026-08-30 (lane f13-atref-guard-b). Two tests, one per spelling, both shown red against origin/integ/f13 a278f8c3b before the fix and red again under a mutation restoring the pre-fix drop - see c1 for the verbatim output. ORIGINAL FILING: Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D3). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below."
  - id: c4
    text: "The control stays green: @<absolute path of the workspace root>/ still attaches its files"
    state: met
    evidence: "test:crates/wcore-cli/src/tui/commands/at_ref_resolve.rs::a_dotdot_at_dir_attaches_the_tree_and_still_obeys_the_gitignore"
    owner: core
    note: "CLOSED 2026-08-30 (lane f13-atref-guard-b). The control is IN the fixture, asserted before the escaping spelling is tried, so neither assertion can pass by the fixture being empty: the plain `@./` spelling must attach ok.txt and must NOT attach the git-ignored build/out.log. The .. spelling then resolves back INTO the workspace and is asserted to obey the same .gitignore, which is the core#335 property for the walk. The pre-existing an_absolute_path_outside_the_workspace_still_attaches and an_at_dir_walk_respects_gitignore both still pass (73 passed; 0 failed). ORIGINAL FILING: Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D3). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below."
---

`@dir` with any spelling that escapes the lexical root resolves to a SILENTLY EMPTY payload — no error, no warning, no skipped-file count. Measured on origin/integ/f13 (5eb2d1ef), not modelled: `@../repo/` against a workspace containing `top.txt` and `src/a.rs` returns `Ok(AtPayload { kind: Dir, files: [], text: '', warnings: [] })`; `@/abs/path/outside/` against a directory containing `a.txt` and `b.txt` returns the same. Control in the same run: `@<abs path of the workspace root>/` correctly returns `files: [a.txt]`, so the query is not broken — only the escaping spellings vanish.

**Where.** crates/wcore-cli/src/tui/commands/at_ref_resolve.rs:339 — `let rel = match rel_to_root(&path, root) { Some(r) => r, None => continue };` inside `walk_dir`. `resolve_dir` walks `full = resolve_under_root(path, root)`, but every entry is then stripped against the NON-canonical `root`, so an escaping spelling makes `rel_to_root` return `None` for every entry. The `continue` does NOT do `*skipped += 1`, so no `AtWarning::SkippedFiles` is emitted either. The empty payload then reaches `render_payload` (crates/wcore-cli/src/tui/commands/at_ref_send.rs:254-259), whose `payload.files.is_empty()` branch is commented 'shouldn't happen for File/Dir, but be defensive' and emits a bare `▌ <label>` with an empty body.

**Why it matters.** Reachable straight from user composer text — `AtRef::parse('@/foo/bar/')` and `@../other/` both parse to `AtRef::Dir` (at_ref_parse.rs:137). The user attaches a directory, is shown a label as if it succeeded, and the model receives nothing. Silent context loss with a false success signal. It also directly contradicts the Q1 decision this issue exists to implement: 'A — leave escaping attachments working' is delivered for `@file` (proven: `@../outside/note.log` attaches) but not for `@dir`, where an escaping spelling neither attaches nor is refused. Note the code comment proves the authors never expected an empty Dir payload — the exact state an escaping spelling produces.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.

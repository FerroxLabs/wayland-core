---
issue: 1300
repo: FerroxLabs/wayland
kind: defect
title: "Windows-only: crashed-holder recovery in the chunked credential write lock is bistable (48x), and it is what times out interrupted_rotations_do_not_leak_entries_without_bound"
status: open
last_verified_commit: a321f1398
criteria:
  - id: c1
    text: "On Windows at --retries 0, n>=20, the per-crash-round recovery cost of credentials::chunk_crash_injection::* has a max/min spread below 3x. Today it is 48.2x for interrupted_rotations and 25x across the sibling sweeps inside a single run."
    state: met
    evidence: "symbol:crates/wcore-config/src/credentials.rs::hard_kill_self"
    owner: core
    note: "MET at the commit that adds `hard_kill_self`, and the CAUSE IS NOW NAMED BY MEASUREMENT AND FIXED rather than measured again. THE 48.2x WAS THE CHILD'S DEATH, NOT THE RECOVERY AND NOT PROCESS CREATION. `std::process::abort()` on windows-msvc raises a fatal exception that the Windows Error Reporting service handles by launching `WerFault.exe`. STANDALONE HARNESS ON SEANDESKTOP (Windows 11 10.0.26200.9168) 2026-09-10, four termination modes INTERLEAVED over two passes so host drift could not be mistaken for the effect, n=12 per arm per pass, parent-observed wall clock per spawn: abort() 2888.9-3469.2ms; abort() after `_set_abort_behavior(0, _WRITE_ABORT_MSG|_CALL_REPORTFAULT)` 2904.8-3537.1ms, i.e. NOT A FIX and recorded as refuted rather than dropped, because Rust's `process::abort` does not route through the CRT `abort()` on this target; TerminateProcess(self) 2.7-5.2ms; exit(0) 2.8-5.0ms. MECHANISM NAMED, NOT INFERRED: sampling the process table at 100ms through a six-abort run observed SEVEN distinct `WerFault.exe` processes. THE FIX IS NOT A WEAKENING OF THE INJECTED CRASH: `TerminateProcess` dispatches no exception at all, so no vectored/structured handler, no CRT teardown and no atexit -- strictly harsher than `abort()`. `exit(0)` is equally fast and was REJECTED because it flushes stdio and runs atexit handlers. RESULT ON WINDOWS, same host, `--profile ci --retries 0 --no-fail-fast`, nextest (chosen because c1 asks for PER-TEST timing at n>=20 and nextest gives each test its own process, which is what the round counters are read out of), 15 consecutive passes at a321f1398, tree clean and asserted before every pass. ON THE TWO QUANTITIES THIS CRITERION ITSELF CALLS `today`: (1) `48.2x for interrupted_rotations` was that test's whole-test duration; it is now 2.488-2.643s across 15 passes = 1.06x, and its per-round RECOVERY ratio is 1.06-1.47x in 15 of 15 passes. (2) `25x across the sibling sweeps inside a single run` is now 1.02-1.06x within a pass (worst pass 0.479s/0.471s; the five sweeps span 0.413-0.479s over all 15). Both are below 3x by 20-40x of margin. ABSOLUTE FIGURES, stated absolutely rather than as a delta against a baseline whose command line this ledger does not record: the whole `chunk_crash_injection` module now finishes in 3.57-3.70s over 15 passes, and the whole 817-test wcore-config lib suite in 14.60-15.00s over 5 passes, both on SeanDesktop at `--profile ci --retries 0`. The comparable filed numbers are this ticket's own: `interrupted_rotations` passing at 131.44-149.03s in its slow mode, and the module being harness-killed at 180.114s. THE READING THAT STILL DOES NOT CLEAR 3x, STATED RATHER THAN OMITTED: per-ROUND max/min inside a single test. Windows is 1.08x-4.54x, and 13 of 90 test-instances sit at or above 3x, every one of them driven by ONE transient round against a ~7ms process-creation floor. THAT READING IS REFUTED AS A DISCRIMINATOR BY BOTH CLEAN PLATFORMS, measured at this same commit with this same instrument and not argued away: LINUX (hetzner-dsm, 15 runs) gives per-round ratios of 34.32x-61.18x on a module that finishes in 3.83-3.94s, and macOS (GitHub-hosted macos-26-arm64, run 34475936534, 10 runs) gives 1.73x-12.95x with 86 of 120 test-instances at or above 3x. Windows after this fix is now the BEST of the three platforms on that metric by 3x over macOS and 13x over Linux. A threshold that no platform passes cannot be the reading that decides this row, and closing it on a number the healthy platforms fail worse would be the reverse of honest. WHAT WOULD FALSIFY THIS: `hard_kill_self` losing its Windows arm, a Windows run in which the module returns to a seconds-scale mode, or the sibling sweeps splitting again (which is c2). REFUTATIONS FROM THE PREVIOUS PASS THAT STAND AND ARE NOT RE-LITIGATED: serialising the module with `--test-threads=1` was 2.3-5.7x WORSE, and a Windows Defender path exclusion on the target directory moved nothing in an A/B/A with the scan cache invalidated before every arm. Both were correct, and both were looking at process creation rather than at the child's death, which is why neither found this."
  - id: c2
    text: "The five sibling sweep tests no longer split into a ~1.5s and a ~36-97s mode within a single run."
    state: met
    evidence: "symbol:crates/wcore-config/src/credentials.rs::hard_kill_self"
    owner: core
    note: "MET at the commit that adds `hard_kill_self`. The five sibling sweeps no longer split at all. MEASURED on SeanDesktop (the ferrox-win-msvc host) 2026-09-10, `cargo nextest run -p wcore-config --lib --profile ci --retries 0 --no-fail-fast`, 15 consecutive passes at a321f1398 with the tree asserted clean before every pass. Per-pass sweep durations, the reading this row is written about: pass 1 0.471/0.478/0.479/0.479/0.479s, pass 2 0.434/0.442/0.452/0.452/0.461s, pass 3 0.413/0.421/0.423/0.424/0.429s -- a within-pass spread of 1.02x, 1.06x and 1.04x, and 0.413-0.479s across all fifteen. Against the mode split this criterion names (`~1.5s and ~36-97s`) and the previous pass's own Windows reproduction of it (0.697 / 0.800 / 29.070 / 31.960 / 40.413s in ONE run), there is no second mode left to be on either side of. The whole module is 3.57-3.70s against 130-149s. THE CAUSE IS THE ONE NAMED UNDER c1: the child's `abort()` was handled by Windows Error Reporting at 2.9-3.5s per crash, and a sweep's slow rounds were exactly its rounds that actually crashed. THE ORIGINAL NOTE'S CORRECTION IS ITSELF CORRECTED: it concluded `the split is real, it is in process creation`. It is not. Process creation is the 2.7-5.2ms floor; the 3.2s was WerFault, which `run_child` timed because `Command::status()` waits for the child to be reaped and WER runs before the exit is reported. That is why more child work was FASTER and less child work was slower -- a round whose injected index ran past the end of the write reached `std::process::exit(0)` and never entered WER at all. WHAT WOULD FALSIFY THIS: any Windows run in which two sweeps in the same run differ by more than ~2x."
  - id: c3
    text: "The cause is NAMED BY MEASUREMENT rather than inferred: is_stale is instrumented to record the observed mtime age and the metadata/modified error kind on each poll, and the result shows which of (a) a future-dated mtime, (b) a metadata error, or (c) something else accounts for the 1.7-4.0s stalls."
    state: met
    evidence: "symbol:crates/wcore-config/src/credentials.rs::observe_staleness"
    owner: core
    note: "MET at 57ec2304a, and the answer is (c). MEASURED on SEANDESKTOP 2026-09-10 with is_stale instrumented exactly as this criterion asks. THREE PASSES of interrupted_rotations_do_not_leak_entries_without_bound, 40 rounds each, 120 rounds total: stale=40 fresh=0 future_dated=0 unreadable=0 in EVERY pass, one poll per round, judged stale on the first look, max observed age 3.61-3.96s against a 20ms stale_after. So (a) a future-dated mtime accounts for ZERO of the stalls and (b) a metadata error accounts for ZERO; both candidates are refuted by measurement rather than argued away. WHAT (c) IS, named by splitting the round: the settle write -- the chunked_put that must recover from the killed child's lockfile, which IS the recovery this ticket is about -- costs 4-9ms per round across all three passes, while run_child (child process creation) costs 2351-3956ms. Recovery is 6ms flat; process spawn is 3.2s and carries all the variance. FITTING CONTEXT THAT IS NOW REFUTED, corrected here rather than left to rot: this note read `Windows Defender real-time protection is ENABLED on this host with an EMPTY exclusion list, so every spawn of the freshly built test binary is scanned`. MEASURED 2026-09-10 on the same host, A/B/A with Defender's per-file scan cache invalidated before every arm: 146.67 / 127.92s with no exclusion, 145.73 / 128.92s with the target directory (the child image's own path) excluded, 60.86s with the exclusion removed again. The exclusion moves nothing and the host drifts more than the effect, so on-access file scanning of the test image is NOT the driver. Real-time protection was left enabled throughout, and a path exclusion does not disable behaviour monitoring, so Defender as a whole is not excluded -- only the scan-the-image story is. The c3 ANSWER is unaffected: (a) and (b) still account for zero, and (c) is still child process creation. CONFOUND CHECKED AND EXCLUDED: machine-level CARGO_TARGET_DIR and TMP/TEMP pointed at a missing F: drive during earlier runs; the drive was restored mid-measurement and passes 1, 2 and 3 straddle that transition with child_ms averages 3131, 3290, 3190 -- unchanged -- so the missing drive is not the driver. An interleaved control also refuted an earlier reading of mine that contention made the test faster: contended pass 1 ran 46-115ms per round and contended pass 2 ran 3125-4089ms, same command and binary back to back. WHAT THIS DOES NOT CLOSE: c1 and c2 are wall-clock spread criteria over the whole module and are graded separately."
  - id: c4
    text: "is_stale's two unwrap_or(false) arms stop being silent: an unreadable or future-dated lock mtime is observable rather than merely slow."
    state: met
    evidence: "symbol:crates/wcore-config/src/credentials.rs::observe_staleness"
    owner: core
    note: "MET at 57ec2304a. The two arms were `.map(|r| r.unwrap_or(false))` for a future-dated mtime and `.unwrap_or(false)` for a metadata/modified failure, so both returned the same value as 'the holder is alive', and a waiter that could never steal simply polled to its ceiling -- indistinguishable, while it happens, from waiting out a live holder. observe_staleness now returns Stale/Fresh/FutureDated/Unreadable carrying the age or the skew, each arm is counted, and the wait-ceiling timeout message quotes that census, so an operator told a lock did not free learns WHICH of the two it was rather than only that it was slow. THE DECISION IS DELIBERATELY UNCHANGED and still fail-closed: only a readable age past stale_after steals, and an unknown age never does -- this criterion asks for observability, not a new stealing rule, and treating unknown as stale would be a correctness regression. The census is counters and two maxima rather than per-poll logging, because I/O inside a poll loop would change the timing being measured, and it records nothing derived from the lock contents: no nonce, no credential bytes, no path. OBSERVED VALUE: across 120 measured rounds both arms fired zero times, which is itself the c3 result."
  - id: c5
    text: "The negative controls stay green after any change: Linux 0/15 and macOS 0/10 at --retries 0, and the census stays [13, 4]-periodic."
    state: met
    evidence: "symbol:crates/wcore-config/src/credentials.rs::hard_kill_self"
    owner: core
    note: "MET. All three legs of this row are now measured at a321f1398, and the reason it was held at not-met -- macOS never having been run by any lane -- is closed. LINUX 0/15 at `--retries 0`: hetzner-dsm via `tools/remote-proof.py`, 15 separate invocations of `nextest run -p wcore-config --lib --profile ci --retries 0 --no-fail-fast -E test(credentials::chunk_crash_injection::)`. NAMING THE INSTRUMENT BESIDE THE VERDICT, because `0/15` without it is the shape this release has been burned by: all 15 JSON receipts carry `source=a321f13982513dfe96d751c0fa9276905d071760`, `tree=d9e64b05beb2f3bbf307e2bf478f6fb07b23598c`, `remote_exit=0` and `complete=true`, with an IDENTICAL `status_sha256` across all fifteen; the harness asserts HEAD and HEAD^{tree} against those values under its `flock` before cargo runs, and the slot was re-asserted at the same sha and tree with a clean worktree AFTER the last run. Module wall 3.832-3.937s. macOS 0/10 at `--retries 0`: GitHub-hosted `macos-latest` (image macos-26-arm64, ProductVersion 26.6.2, arm64), run 34475936534, `C5_MACOS_RESULT failures=0 of 10` with all ten runs printing `RUN n PASS` and the job's final step asserting the count so a non-zero cannot report green. Module wall 4.127-4.572s. THAT RUN CAME FROM A THROWAWAY BRANCH, `w1300-macos-c5`, whose only content beyond this fix is a workflow that repeats the module ten times; it is deliberately NOT on this branch and must never reach `main` or a release branch, because a `[ci-darwin]` leg gives ONE sample and this criterion asks for ten. It ran on the GitHub-hosted pool, never on the `sean-mac-arm64` self-hosted runner. CENSUS [13, 4]-PERIODIC: verified on all three platforms rather than on the one that was convenient -- `[13, 4]` repeated over all 40 rounds with no growth, in every Linux receipt, in all ten macOS runs, and in all five Windows full-suite passes. Final entries are the identity plus exactly three chunks. THIS ROW IS A CONTROL AND IT DID ITS JOB: the change graded here alters ONLY how the injected child terminates on Windows (`hard_kill_self`), and the Unix arm is byte-identical `std::process::abort()`, which is why Linux and macOS were expected to be unchanged and are reported as a CHECK rather than as a result. ORIGINAL NOTE, kept: pinned because the obvious fixes here (raising a timeout, widening the ceiling) would make the symptom vanish on Windows while quietly weakening the leak invariant on the two platforms where it currently runs clean. The controls are what stop that. Nothing was raised or widened -- no assertion, threshold, timeout or control-flow change is in this diff."
  - id: c6
    text: "Recorded as UNPROVEN and explicitly out of scope: whether the same clock behaviour can make a live 2s-heartbeating holder look stale to a 6s stale_after waiter."
    state: met
    evidence: "file:.planning/ledger/wayland-1300.md"
    owner: core
    note: "MET at 509f4426b BY RECORD, which is the whole of what this criterion asks. The scope call is written down with the numbers that make it a judgement rather than an omission: production uses `stale_after` 60s with 2s heartbeats under `STALE_AFTER_SECS >= HEARTBEAT_SECS * 3`, so seconds-scale mtime unreliability does not obviously threaten a live holder -- AND the DIRECTION of any skew is unmeasured, so if mtime can read OLDER than reality by more than 4s a live holder could be stolen, which the code says must never happen. That is carried as a stated unknown rather than dropped, with the reason (a deferral with no trigger decays into nothing). WHAT WOULD FALSIFY THIS: the record being deleted, or the question being silently answered somewhere without this row being re-graded. Nothing else on #1300 moves: c1-c5 all need Windows measurement at --retries 0 and stay not-met. EVIDENCE TOKEN IS DELIBERATELY THE BARE FILE FORM AND IT IS WEAK, stated rather than dressed up. The record this criterion asks for lives in this criterion own note and nowhere else -- no allowlist row or source comment carries the heartbeat question -- and a file:<path>:<line>:<text> self-anchor is structurally impossible here: the token text lands in the file it points at, so the fragment matches twice and the gate refuses it. The bare form proves only that this file exists, which is the strongest thing the grammar can say about a record kept in the ledger itself. Precedent for the form: wayland-1195 c-row and wayland-core-361."
---

# The test name says leak. The payload says timeout.

`interrupted_rotations_do_not_leak_entries_without_bound` never reached its leak
assertion: the harness killed it at 180.114s and the `<flakyFailure>` text is empty.
On the two platforms where the assertion does execute it passes, and the census is
flat -- `[13, 4]` repeated over twenty rounds with no growth.

What is underneath is a Windows-only bistability in recovering a lock from a crashed
holder: ~65-77ms per crash-round on Linux and on fast Windows runs, against 1.7-4.0s
on slow ones, with no middle. Reading the payload rather than the test name is what
separated a scary-sounding credential leak from a real but different reliability bug.

---
issue: 1285
repo: FerroxLabs/wayland
kind: defect
title: "Two more macOS-only retry flakes: harness_tui_flow resume_repaints and wcore-mcp f016_real_spawn"
status: open
last_verified_commit: 34ca36888
criteria:
  - id: c1
    text: "Both tests are either made deterministic under parallel nextest on macOS, or their allowlist entries are deleted at expiry because a normal-duration run stopped reproducing them."
    state: met
    evidence: "test:crates/wcore-mcp/src/transport/stdio.rs::eperm_over_a_group_that_has_not_yet_settled_is_not_a_containment_failure"
    owner: core
    note: "MET 2026-09-10 by the w15/mac2 lane, on a REAL macOS host for the first time in this cluster's life. Both tests are now measured at --retries 0 on native macOS under parallel nextest, one of them behind a causal repair with a red arm that reproduces the CI payload on demand. NEITHER ALLOWLIST ENTRY WAS DELETED and neither should be until a CI window has passed; the criterion's first branch is what is satisfied, not its second. THE INSTRUMENT, named beside every number below. GitHub-hosted `macos-latest`, macOS 26.6.2 arm64, 3 logical and 3 physical CPUs -- which is the same 3-4 vCPU oversubscribed shape gh#1289 established as the regime this whole cluster fails in. Work ran from THROWAWAY branches `measure/w15-mac2` (run 34478038368) and `measure/w15-mac2c`, scoped to `measure/**` so `ci.yml` (which lists `lane/**`, not `measure/**`) never fired and no Windows self-hosted leg was burned. Those branches must never reach the release tree and are not merged here. ===== f016_real_spawn: ROOT-CAUSED, MEASURED IN THE KERNEL, REPAIRED, RED-ARMED ===== THE MECHANISM IS NO LONGER A HYPOTHESIS. The previous grading said the XNU question 'was NOT asserted and must not be asserted without a macOS host'. It now has one. A C probe (spin the real `kill(-pgid, SIGKILL)` against a leader that exits immediately until it first FAILS, then take `KERN_PROC_PGRP` immediately) ran 400 iterations idle and 400 under a 2x-logical-CPU spinner load: IDLE 384/400 EPERM-with-a-live-census, 16/400 EPERM-with-a-clean-census, 0 ESRCH, 0 other, 0 spin-bailouts; LOADED 388/400 and 12/400 on the same zeros. Every single disagreeing member carried `p_stat = 2` (SRUN) with `p_flag = 0x00002004`, i.e. P_WEXIT (0x00002000) SET -- 384 of 384 idle and 388 of 388 loaded, zero counter-examples. So the member `killpg` declines to signal is already inside exit(2), which is exactly the state the census's only corpse predicate (`p_stat != SZOMB`) cannot see. TWO THINGS FOLLOW THAT NOBODY KNEW. The disagreement is not rare, it is the NORMAL outcome at 96 %; the flake was rare only because most callers of `close()` do not race it, and f016's fixture (`read line; printf ...`, a server that exits the instant it has answered) does. And the window is short: settle time mean 52 us / max 361 us idle, mean 71 us / max 8354 us under load. THE REPAIR, at crates/wcore-mcp/src/transport/stdio.rs. `classify_group_kill_failure`'s EPERM arm now delegates to `settle_group_census`, which RE-READS the census until it holds nothing that can execute or until GROUP_TEARDOWN_SETTLE_BUDGET (500 ms, 60x the 8.4 ms worst case measured above) expires. It does not weaken the guard: a member that is genuinely alive and unsignalable never reaches SZOMB and never leaves the group, so it is still reported -- later, not never. `Indeterminate` is deliberately NOT retried: group 0 and a kinfo_proc layout self-check failure are properties of the question, not states that settle. RED ARM, on a committed tree, and it is an exact one. `GROUP_TEARDOWN_SETTLE_BUDGET` was mutated to `from_millis(0)` -- with a zero budget the deadline has already passed when it is first read, so the very first `Live(n>0)` errors, which is bit-for-bit the pre-repair behaviour with everything else about the tree identical. THE MUTATION WAS PROVED TO LAND ON CODE before the build (exactly one anchored match, re-grepped after, printed as `347:const GROUP_TEARDOWN_SETTLE_BUDGET: Duration = Duration::from_millis(0);`) because a mutation that silently matches nothing, or matches a doc comment quoting the call, is a second green arm wearing a red label. RESULT: the FIRST invocation of `nextest run -p wcore-mcp --lib --profile ci --retries 0 --no-fail-fast` failed, `FAIL [0.046s] (123/155) wcore-mcp transport::stdio::tests::f016_real_spawn_uses_sanitized_launch_context`, payload `close environment fixture: Transport(\"failed to kill MCP process group: SIGKILL to MCP process group 7316 was refused with EPERM and the group still holds 1 LIVE member(s) after 0ms of settling; containment has failed, not completed\")` -- the CI payload verbatim apart from the settle clause the repair added. The new regression test failed 1/1 in the same arm. GREEN ARM: the same command, THIRTY invocations, 30/30 `155 tests run: 155 passed, 0 skipped` (4650 test executions), zero f016 failures and zero containment payloads across all thirty. POSITIVE CONTROL: f016 is present as an executed testcase in 30 of 30 logs, so those zeros are reports of health and not of absence. NON-VACUITY OF THE NEW TEST, printed rather than asserted: `unsettled-census window observed in 16/16 iterations` on this host. It is not asserted because a host fast enough to reap the leader before the first census would then be red for being fast, which is a gate with a wrong failure mode; it is printed so a green can be told from a vacuous one. ===== resume_repaints: THE macOS WINDOW THAT WAS OWED ===== The repair (separate project directory, both markers waited on together, an assertion that no 'while I was thinking' notice appears) landed on w15/mac and is unchanged here. What was owed was a CI-grade macOS window, because n=11 on one idle Linux host is not a CI rate. MEASURED: 20 invocations of `nextest run -p wcore-cli --test harness_tui_flow --profile ci --retries 0 --no-fail-fast` -- the WHOLE binary, so the parallel-nextest condition the criterion names is the real one -- 20/20 `36 tests run: 36 passed, 0 skipped`, 25.3 s to 28.6 s each. resume_repaints_prior_conversation_into_the_transcript present as an executed testcase in 20 of 20 and failing in 0. The assertion the repair added never fired: ZERO occurrences of 'while I was thinking' across all twenty logs. Free rider: tui_renders_the_chrome_and_every_tab_on_boot (gh#1288, the heaviest Linux member at 5.3 %) is also 0/20 present-and-passing on macOS here. THE LIMIT ON THAT WINDOW, stated because it is easy to over-read. Against this test's measured macOS incidence in census W (4 of 104, 3.8 %), zero in 20 has p ~ 0.46 under the null. It is CONSISTENT with the repair and is not, by itself, evidence for it. What carries the verdict is the cause having been identified and removed, plus the Linux red arm (1/1 with the cwd argument put back) and 11/11 green the previous lane recorded; the macOS arm is the confirmation those were owed on the platform the ticket was filed about, and it is now on the record. ===== ALLOWLIST ===== Lines 66 and 67 are RENEWED with their cause text replaced, not deleted. 66 loses both 'MEASURED FLAKY ON macOS ONLY' and the 'LINUX flake by a factor of four' framing as CAUSES -- the census incidences are kept as incidences -- and gains the edit-notice scroll. 67 loses 'Spawns a real process, so it is exposed to hosted-runner process-startup latency' and gains the close()/EPERM payload and the kernel measurement above. Line 69 (gh#1287) was DELETED, which its own body instructed and gh#1287 c1's grading confirmed was due; its text is preserved verbatim as a comment block in the same file, with the p ~ 0.38 limit on that repair's zero-recurrence window kept beside it. ===== A SECOND, UNFIXED COPY OF THE SAME DEFECT, found while grading this one and NOT in this criterion's scope ===== crates/wcore-eval-scenarios/src/process_tree.rs::kill() carries the IDENTICAL single-sample classifier -- `kill(-pgid, SIGKILL)` then one `process_group_census`, `Live(0)` tolerated and `Live(n)` reported as 'containment has failed, not completed' -- against the same Darwin behaviour, in its own words in its own comment block. The measurement above says that call disagrees with itself 96 % of the time on a macOS host, so this copy will raise the same false containment failure whenever it races a mid-exit member. It was NOT repaired here: it is a different crate with no dependency on wcore-mcp, the project's no-duplicate-code rule points at extracting the classifier down to wcore_types::process_liveness beside the census rather than patching a second copy, and no Linux build slot was available to this lane to test a workspace-wide extraction. Filed as a finding, not fixed. ===== DISCLOSURE ===== crates/wcore-mcp/src/transport/stdio.rs is a `SOURCE_INPUTS` file and this lane edited it. The Desktop contract corpus was NOT regenerated here -- `desktop_contract_corpus` and `quiescence_contract` will be red on this branch as a pure hash rebase, and the integrator regenerates once over the merged tree."
---

# Two macOS retry flakes

Ledgered for coverage. Repair or expiry is 0.13.13 work.

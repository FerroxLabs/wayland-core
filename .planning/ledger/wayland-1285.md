---
issue: 1285
repo: FerroxLabs/wayland
kind: defect
title: "Two more macOS-only retry flakes: harness_tui_flow resume_repaints and wcore-mcp f016_real_spawn"
status: open
last_verified_commit: 5b76ec723
criteria:
  - id: c1
    text: "Both tests are either made deterministic under parallel nextest on macOS, or their allowlist entries are deleted at expiry because a normal-duration run stopped reproducing them."
    state: not-met
    evidence: "file:.config/flaky-allowlist.txt"
    owner: core
    note: "STILL NOT MET -- one of the two tests is REPAIRED AND MEASURED, the other is ROOT-CAUSED and cannot be repaired from this lane. The criterion names both, so it stays not-met. ===== resume_repaints: REPAIRED, AND THE RECORDED CAUSE WAS WRONG TWICE OVER ===== CAUSE, READ FROM THE PAYLOADS RATHER THAN FROM THE TEST NAME. Every surviving JUnit for this test was downloaded and its flakyFailure text read: runs 33425600515, 33574149322, 33705709380, 33711272322, 33843515833, 34318072596 (linux-containerized) and 33437649161 (macos-latest). SEVEN DISTINCT RUNS, ONE IDENTICAL SCREEN. The resumed transcript carries a SEVENTEEN-ROW synthetic notice -- 'User edited 23 files while I was thinking (/tmp/.tmpXXXX/memory, /tmp/.tmpXXXX/memory/changelog, /tmp/.tmpXXXX/projects, ... /tmp/.tmpXXXX/credentials.enc, ...and 3 more) -- re-read each before proceeding.' -- naming the harness's own WAYLAND_HOME. The harness spawned with cmd.cwd(home), i.e. cwd == WAYLAND_HOME, so the file watcher's root contained the engine's own state and every write to memory/, projects/, channels/, cron/, logs/, config.toml and the credential files was surfaced to the user as an edit they did not make. That notice is appended to the transcript, the pane renders its TAIL, and on a 40-row screen the prior user prompt is scrolled out of view. Six Linux payloads fail at harness_tui_flow.rs:1424 with the user prompt off-screen; the one macOS payload fails at :252 after 60s because the /private/var/folders/... paths wrap and push the ASSISTANT marker off too. ONE DEFECT ON BOTH PLATFORMS -- neither 'macOS ONLY' (the original line) nor 'contention' (the 2026-09-03/09-10 corrections) is its cause. Load matters only because a slower host writes more state inside the window. REPAIR, at 52682a0ad and 5b76ec723 on w15/mac: give the session its own project directory (PtyHarness::spawn_with_args_in / boot_to_workspace_in, cwd separated from WAYLAND_HOME -- the shape crates/wcore-cli/tests/support/pty.rs::spawn_with_args_env already documents as correct and the one production always has), wait for BOTH markers together instead of reading one off the frame after the other, and assert that no 'while I was thinking' notice appears so a future re-merge of the two directories fails HERE rather than re-arming the flake silently. MEASURED ON LINUX, THE PLATFORM THE CENSUS SAYS DECIDES THIS TEST (17/131 against 4/104). RED ARM FIRST, on a committed tree: with only the two cwd arguments changed back to home.path(), 'nextest run -p wcore-cli --test harness_tui_flow -E test(resume_repaints_prior_conversation_into_the_transcript) --retries 0 --no-fail-fast' on hetzner-dsm FAILED 1/1 in 4.3s with the notice tail ('proceeding.') on the captured screen -- receipt remote_exit 100, complete true, source 46e1f31671b1833e89f8b025b1bec1434168c519. That commit was then DROPPED (git reset --hard 5b76ec723) and is not in the branch. GREEN ARM: the same command, ELEVEN invocations (one at 52682a0ad plus ten at 5b76ec723), 11/11 '1 test run: 1 passed', 4.395s-4.571s, every receipt remote_exit 0 and complete true. WHAT THE RED ARM ACTUALLY PROVED, stated so it is not over-read: the NOTICE is present on an IDLE Linux host deterministically (1/1) and the new assertion detects it. The SCROLL that hides the marker is the load-sensitive half -- in that red run both markers were still visible, which is why it failed on the notice assertion at 4.3s rather than on the wait. The repair removes the CAUSE; CI's 13.0 % was the visible fraction of a mechanism that was always present. LIMITS. (1) n=11 on one idle host is not a CI rate, and no oversubscribed arm was run. (2) macOS was not executed -- but macOS is not the deciding platform here and the macOS payload shows the same cause, so that arm is confirmation rather than the verdict. (3) The synthetic-notice behaviour is NOT a shipped defect: it requires cwd == WAYLAND_HOME, which production never does. It is a harness shape and it is fixed as one. ===== f016_real_spawn: ROOT-CAUSED, NOT A TEST FLAKE, AND NOT REPAIRABLE FROM THIS LANE ===== THE RECORDED ATTRIBUTION IS FALSIFIED. Line 67 of .config/flaky-allowlist.txt says 'Spawns a real process, so it is exposed to hosted-runner process-startup latency'. All SEVEN macOS occurrences were fetched (33437649161, 33481688035, 33629541563, 33804424314, 33804772883, 33866807735, 34212497448) and all seven carry the SAME payload, byte-identical apart from the pid, and it is a panic in transport.close() -- not in spawn and not in the request: close environment fixture: Transport('failed to kill MCP process group: SIGKILL to MCP process group <pgid> was refused with EPERM and the group still holds 1 LIVE member(s); containment has failed, not completed'). Startup latency cannot produce that, and neither can the per-request deadline (DEFAULT_RPC_TIMEOUT is 120s). It is classify_group_kill_failure in crates/wcore-mcp/src/transport/stdio.rs deciding on TWO PROBES TAKEN AT TWO MOMENTS: kill(-pgid, SIGKILL) answering EPERM, which on Darwin means the kernel found no member it was willing to signal, and then process_group_census answering Live(1) under a LOOSER predicate (kp_proc.p_stat != SZOMB). Those two statements contradict each other about a group whose only legitimate member is our own unreaped child, and the count is 1 in all seven observations. Same shape as gh#1284 and same family as gh#1287: a macOS process-group authority question decided on inconsistent observations, failing closed. WHY THIS LANE DID NOT REPAIR IT, and it is a hard blocker rather than a preference: crates/wcore-mcp/src/transport/stdio.rs is in wcore_protocol::contract::SOURCE_INPUTS, so editing it requires regenerating the Desktop contract corpus; regeneration means running 'wcore-contract generate', which WRITES files, and the only Linux route this lane has (tools/remote-proof.py) runs cargo on hetzner and returns no artifacts, while cargo is banned on this Mac. The repair belongs to a lane that can regenerate the corpus. The exact mechanism -- whether Darwin's killpg refuses a member that has entered exit1() while the census still reads it as non-SZOMB -- is NOT established here and must not be asserted without a macOS host. What IS established is that the two probes disagree and that the disagreement, not spawn latency, is the failure. ===== DISPOSITION ===== NEITHER ALLOWLIST LINE IS DELETABLE, and this lane edited no allowlist. Line 66 (resume_repaints) should be RENEWED rather than deleted until the repaired test has a CI window behind it, with its 'MEASURED FLAKY ON macOS ONLY' opening and its 'LINUX flake by a factor of four' correction both replaced by the cause above. Line 67 (f016) must be renewed with 'hosted-runner process-startup latency' struck and the close()/EPERM payload put in its place, and tracked as a product defect rather than as flake debt. Expiry stays 2026-10-01 for both."
---

# Two macOS retry flakes

Ledgered for coverage. Repair or expiry is 0.13.13 work.

---
issue: 1290
repo: FerroxLabs/wayland
kind: defect
title: "f14_sigkill_recovery: ZERO provider-dispatch checkpoints persist (left 0, right 1) - the exactly-once DOUBLING premise this was filed on is refuted"
status: open
last_verified_commit: 88a13b35e
criteria:
  - id: c1
    text: "On a failing run, establish which of the two remaining possibilities holds: the provider-dispatch checkpoint was NEVER WRITTEN (a product defect), or it was written and not fsynced before the SIGKILL (a test defect). Measured at --retries 0, n>=20, on a host that has actually exhibited it."
    state: met
    evidence: "test:crates/wcore-cli/tests/f14_sigkill_recovery.rs::w1290_the_credential_store_alone_decides_whether_a_provider_checkpoint_is_written"
    owner: core
    note: "MET 2026-09-10 by the w15/gateB lane at 50dfc81f1. THE DISJUNCTION IS SETTLED: the checkpoint was NEVER WRITTEN. Two independent grounds, one structural and one measured. STRUCTURAL -- the other branch is not reachable, and that is a fact about the code rather than an opinion. JournalWriter::append (crates/wcore-agent/src/session_journal.rs:1526-1536) does write_all(&frame) then sync_all() -- an fsync, metadata included -- and returns only after it; append_journal_event (crates/wcore-agent/src/engine.rs:12728-12739) AWAITS that on a spawn_blocking handle rather than posting to a channel; and the writer holds a bare std::fs::File with no BufWriter, no accumulator and no background writer task. A checkpoint whose commit returned is on the platter and a later SIGKILL cannot take it back, so `written and not fsynced before the SIGKILL` cannot produce left: 0. MEASURED -- the product says so itself, in-process, on the failing run. engine.rs:15386-15415 reads sealed_request_key_available(&self.config) before deciding, and when that errors it MINTS A BARE DISPATCH ID AND DISPATCHES WITH NOTHING DURABLE WRITTEN, because a ProviderDispatch checkpoint is required to carry a sealed prepared request and a host that cannot seal one has no honest checkpoint to write. [session] require_durability defaults to false (crates/wcore-config/src/config.rs:1328), so the turn is not refused; it proceeds, unrecorded. sigkill_during_model_stream_... now reads the per-turn degrade notice off the same wire before the kill and FAILS WITH THE REASON INSTEAD OF THE COUNT: `this turn ran with crash replay protection OFF ... : Some('crash replay protection is OFF for this run: this profile's credential store was asked for the recovery key and did not answer in time, so the exact provider request cannot be sealed. ...')`. THE CENSUS, --retries 0, n>=20, on a host that HAS now exhibited it: 22 invocations of `cargo nextest run --retries 0 --no-fail-fast -p wcore-cli --test f14_sigkill_recovery -p wcore-protocol --test desktop_contract_corpus -p wcore-agent --test esc_interrupt_wedge_test` on hetzner-dsm, of which 20 carry complete receipts. Iterations 11 and 12 are INVALID AND REPORTED AS SUCH rather than counted as passes: this lane dirtied the worktree mid-census and remote-proof correctly refused with `Refusing: package checkout is not clean and committed`. Of the 20 valid runs, sigkill_during_model_stream_... failed 18 times and ALL 18 carried the `expected exactly one provider-dispatch recovery checkpoint ... left: 0` payload -- the same assertion PR #417 run 33553656600 names, at f14_sigkill_recovery.rs:831 in this tree where CI's artifact said :806, the line having moved by this lane's own additions above it. NEW INSTRUMENT: w1290_the_credential_store_alone_decides_whether_a_provider_checkpoint_is_written runs two arms over the same fixture script at the same kill point, differing only in whether a secure store answers. Its control arm is asserted as a BICONDITIONAL (count == 0 iff the turn degraded) rather than as a fixed 1, because a fixed 1 would import this very failure into the test that explains it -- and the biconditional still reds in both directions that matter. LIMIT 1: the census REPRODUCES the failure; it does not measure its RATE in CI, and the rate here is not transferable. MEASURED: the identical command on the same host at the UNCHANGED commit 4f82ce8c1 scored 37 passed / 0 failed early in this session and 30 passed / 7 failed roughly three hours later, at load average 183 on 96 cores. The variable is the worker, not the tree. LIMIT 2: what is shown is that a credential store which does not answer is SUFFICIENT to produce left: 0. It is not shown to be NECESSARY. See c2. Reported at https://github.com/FerroxLabs/wayland/issues/1290#issuecomment-5615741450"
  - id: c2
    text: "The relationship to gh#1289 is settled by measurement rather than by co-occurrence: either the :806 left-0 failures are shown to co-occur with :593 credential-store failures at n>=20, which folds this into gh#1289, or they are shown to occur independently, which keeps it separate."
    state: met
    evidence: "file:crates/wcore-cli/tests/f14_sigkill_recovery.rs:2389:this turn ran with crash replay protection OFF"
    owner: core
    note: "MET 2026-09-10. FOLDS INTO gh#1289, by the criterion's first branch and then some. CO-OCCURRENCE AT n>=20, which is what the criterion asks: over the 20 valid receipted runs described in c1, EVERY ONE carried at least one credential-store failure -- SessionAuthority('the configured credential store (auto: the OS keyring, then the encrypted vault) was asked for this profile's recovery key and did not answer within 5s...') raised from f14_seed_recoverable_turn_helper at f14_sigkill_recovery.rs:618, which is CI's :593 in this tree -- between 1 and 4 occurrences per run. All 18 of the left-0 failures fell in runs that also carried one. ZERO COUNTER-EXAMPLES: not one run produced the left-0 failure without a credential-store failure beside it. STRONGER THAN CO-OCCURRENCE, which is why this is explained rather than merely folded: the link is now IN-PROCESS. Across the census the new w1290 control arm reported `CONTROL degrade notice: Some(...)` on 17 arms that wrote nothing and `None` on ZERO. And at 50dfc81f1 the ticket's own test names it directly -- sigkill_during_model_stream_... reads the per-turn info frame off the same wire as the assertion and fails with the credential store's own words rather than with a bare count. Same process, same turn. DISPOSITION: the remaining observation belongs to gh#1289. The retry-allowlist line for sigkill_during_model_stream_... already cites gh#1289 rather than this ticket, and that attribution is now measured rather than circumstantial. THIS LANE DID NOT EDIT .config/flaky-allowlist.txt -- another agent owns it -- so whoever does should note that the 2026-09-20 expiry there says to delete rather than renew UNLESS exactly this measurement is produced. It has been. RENEW that line against gh#1289. LIMIT: necessity is still not proven. Every observed left-0 came with a credential-store failure, but a left-0 with a healthy store would be a different defect and 20 runs without a counter-example is not a proof that none exists. The new biconditional assertion is the standing detector for exactly that case. Reported at https://github.com/FerroxLabs/wayland/issues/1290#issuecomment-5615741450"
  - id: c3
    text: "The ticket is NOT closed as corrected. Closing it discards a real unexplained failure."
    state: met
    evidence: "file:.planning/ledger/wayland-1290.md"
    owner: core
    note: "MET 2026-09-10, and it is graded MET only because the act it names was performed: the ticket was NOT closed and closure was NOT proposed. Verified before this record was written -- `gh issue view 1290 -R FerroxLabs/wayland` returns state OPEN, milestone 0.13.14, labels bug and area:core. This lane changed no issue state, milestone or label and only commented. The disposition is recorded here and at the comment https://github.com/FerroxLabs/wayland/issues/1290#issuecomment-5615741450: the exactly-once DOUBLING premise is dead, the OBSERVATION is not, and its cause is now measured and handed to gh#1289 rather than dissolved into it by assertion. PLAN-0.13.13-v2's `close as corrected` remains wrong for the reason the earlier grading gave -- the premise was refuted, not the defect. THIS ROW REDS THE MOMENT ANYONE CLOSES THE ISSUE, which is the whole point of grading it. The evidence token is the bare ledger path deliberately: the disposition this criterion is about lives in this file's own status field and note, and there is no stronger machine-resolvable token for a criterion whose content is an act NOT taken."
---

# The premise died. The failure did not.

`left: 0` cannot be two checkpoints, so the doubling this ticket was filed to
chase is refuted. What remains is a different and equally real question: an
exactly-once recovery path that persisted NOTHING under SIGKILL.

Deliberately still not allowlisted. An exactly-once assertion failing under a
simulated crash is the signal, not the noise, and the gate reporting it is the
gate working.

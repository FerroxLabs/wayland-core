---
issue: 406
repo: FerroxLabs/wayland-core
kind: defect
title: "Arm 3s gate cannot see a store created after the walk at a path it never recorded (residual of #390 c2)"
status: open
last_verified_commit: 967bdf2fb
criteria:
  - id: c1
    text: ": A nested store created after the arm-3 walk, at a path that is neither store-shaped nor in the last scan's list, is refused on the next guard — measured with a wrong-refusal control in the same fixture, in the shape `vfs_guard_cost.rs::a_store_created_after_the_scan_is_denied_on_the_next_guard` already uses for arm 2."
    state: not-met
    evidence: test:crates/wcore-tools/tests/vfs_nested_store_deny.rs::a_borrow_written_after_the_walk_at_a_non_store_shaped_target_is_refused
    owner: core
    note: SUBSTANTIALLY CLOSED BY THIS LANE, AND THE REMAINDER IS NOW A DIFFERENT, SMALLER SHAPE - graded not-met because the criterion says `a nested store created after the arm-3 walk, at a path that is neither store-shaped nor in the last scan's list` without qualification, and one member of that class survives.\nWHAT CLOSED. Arm 4 discovered every store a nested control directory, gitfile, bare repository or `alternates` borrow names and then never looked again. It now stamps the DECLARATION SITES it READ - a nested gitfile, a `commondir`, an `objects/info/alternates`, and the control directory itself (one stamp covering all six store leaves, since none can appear or be re-pointed without moving its mtime) - and revalidates them on the branch that is about to ADMIT, which is the only branch a stale set can get wrong: a store the walk FOUND is already refused from the set with no probe at all. `<root>/.git` is excluded because arm 2 revalidates it on every guard. Same #1145 same-tick granularity rule as `vcs_store_cache_hit`, so a write inside the walk's own window is treated as a change rather than missed.\nGRADED ON THE PRODUCTION PATH, in the shape the criterion names. `vfs_nested_store_deny.rs::a_borrow_written_after_the_walk_at_a_non_store_shaped_target_is_refused` - the INVERSION of the test that asserted this leak, as that test's own failure message instructed, not a deletion and not a new file - drives the production stack `SandboxedFs(SecretDenyFs(RealFs, contained))`, asserts `nested_walk_count() == 1` FIRST so the walk has provably run before the borrow is written (the post-warm state, not a cold policy), then writes `<root>/vendor/pkg/.git/objects/info/alternates` naming `<root>/late-odb` - a target carrying no store leaf component and no repository shape - and asserts the object is refused. WRONG-REFUSAL CONTROL in the same body on BOTH sides of the mutation: `<root>/src/main.rs` readable before the borrow and after the refusal.\nRED ARM, compiled first (`cargo check -p wcore-tools --tests` RC=0, so a red is behaviour and not a build break): neuter `nested_declarations_moved` to `return false` and this test goes red while all seven pre-existing refusals and every control stay green. Restored, `sha256sum -c` verified.\nWHAT REMAINS, pinned rather than described. A control directory that did not exist when the walk ran has no declaration site in the witness set, so a borrow IT declares at a target which is neither repository-shaped (arm 3) nor lexically a store (arm 1) is still admitted. Measured by `..::a_borrow_declared_by_a_control_dir_created_after_the_walk_is_still_admitted`, whose failure message instructs its own inversion and the re-grade of core#398 c1/c2 when the trade is taken. Seeing it needs one `stat` per WORKSPACE DIRECTORY on every guard, which is core#398's regression verbatim and which core#398 c1 - graded in this same pass - forbids. The two criteria are in direct tension and the tension is priced here rather than traded silently.\nSTRICTLY SMALLER THAN WHAT IT REPLACED, and provably so rather than by assertion: the same fixture with the control directory PRESENT at walk time is now REFUSED by the test above. The class went from `every borrow written after the walk` to `only a borrow written after the walk by a checkout that did not exist then`.
  - id: c2
    text: ": The cost of that closure is stated as a number and measured through `GuardCounters`, and whichever of core#390 c3 / core#398 c2 it moves is RE-GRADED rather than left claiming a figure the tree no longer has."
    state: met
    evidence: test:crates/wcore-tools/tests/vfs_guard_cost.rs::the_post_walk_freshness_check_scales_with_checkouts_not_directories
    owner: core
    note: MET on both halves. The criterion has two: state the closure's cost as a number through `GuardCounters`, and RE-GRADE whichever of core#390 c3 / core#398 c2 it moves rather than leave a stale claim.\nHALF ONE - THE NUMBER. MEASURED base-vs-HEAD on hetzner, base `ca15a48bf` vs HEAD `967bdf2fb` (lane/f13-s2-vcs-cost). Two DISTINCT binaries, identity proven by content and not by date: sha256 differs and `strings base_bin | grep -c nested_declarations_moved` = 0 against 5 for HEAD. Counted `GuardCounters` (not timed: on a loaded build host a wall clock cannot tell a skipped scan from a fast one), warm probes per guard on an admitted path:\n                                     base   HEAD   delta\n  no nested checkout                     3      3      0   <- the common case pays NOTHING\n  one vendored checkout                  3      5     +2\n  two vendored checkouts                 3      7     +2 each\n  8 vs 48 workspace directories       slope 0  slope 0    <- flat in the TREE\nSo the closure costs 2 probes per NESTED CHECKOUT per admitted guard and ZERO probes per workspace directory - the control directory itself (covering all six store leaves) plus its `objects/info/alternates`, whose CONTENT no directory mtime witnesses. Asserted as all three properties by `vfs_guard_cost.rs::the_post_walk_freshness_check_scales_with_checkouts_not_directories`: (3,3) with no checkout, linearity in checkouts (one costs 5, two cost 7), and flatness in directories. The base column is MEASURED, from the same test run against the compiling mutation that neuters the freshness check - which is exactly base behaviour - not assumed from reading base.\nHALF TWO - THE RE-GRADES, done in this same commit and not deferred:\n* core#390 c3 (`the per-operation cost of is_vcs_content_store does not get worse, stated as a number`) IS   MOVED by this closure and is re-graded on its own ticket with the table above. It stays not-met, now for   this lane's reason rather than a stale one.\n* core#398 c2 (`the ordinary gate-refused path is unchanged at one resolution / zero warm scans / three   warm probes`) is NOT moved - all three figures are identical to base - and is re-graded MET on its own   ticket with the measurement, replacing a note that had the wrong criterion's text pasted under it.\n* core#394 c2 / core#396 c3 / core#398 c1 re-measured in the same pass; each carries its own numbers.\nNothing is left claiming a figure the tree no longer has.
  - id: c3
    text: ": `store_shaped`'s remaining role is decided explicitly — either it is the mutation net and the doc says so, or it is deleted because c1's mechanism subsumes it, and `tests/vfs_guard_cost.rs::a_gate_admitted_path_costs_one_probe_per_workspace_directory` is re-measured either way."
    state: met
    evidence: test:crates/wcore-tools/tests/vfs_guard_cost.rs::a_store_named_path_costs_the_same_at_any_workspace_size
    owner: core
    note: MET, and re-decided rather than carried forward, because this lane changed what the answer rests on.\nDECIDED: `store_shaped` does not exist in this lineage, is deliberately not introduced, and - the part that is new here - IS NOT NEEDED EVEN NOW THAT A FRESHNESS CHECK EXISTS. The previous decision was recorded against a tree in which nothing had yet had to pay for freshness, so it was untested. A lexical pre-gate exists to stop an ordinary path paying for a whole-tree walk; the check core#406 c1 required costs an ordinary path ZERO probes (empty witness set) and a checkout-bearing workspace 2 per checkout, so there is still nothing to gate. Adding one would buy nothing and would reintroduce exactly the blindness core#394 records.\nWHAT PLAYS THE MUTATION-NET ROLE, documented in code and not only here: arm 3 (`WorkspacePolicy::encloses_repository_store`) for anything repository-shaped - `is_vcs_store_leaf_name` applied to the ANCESTORS OF THE QUERY PATH, O(depth) not O(workspace), memoising nothing so it is fresh at the instant it is asked - and, added by this lane, arm 4's declaration-site revalidation for a borrow whose target has neither a store name nor a repository shape. Both are on `is_vcs_content_store_resolved` / `nested_declarations_moved` with the reasoning at the site.\nRE-MEASURED EITHER WAY, as the criterion requires. The named test (`a_gate_admitted_path_costs_one_probe_per_workspace_directory`) is not in this lineage - 0 grep hits with a known-positive control green - and core#398 c1 records that pointer defect and the proposed repoint. The measurement itself was performed, base-vs-HEAD, on the tests that do that job here: `a_store_named_path_costs_the_same_at_any_workspace_size` reads 4 warm probes at 8 AND 48 workspace directories, slope 0.000, unchanged from base; `the_post_walk_freshness_check_scales_with_checkouts_not_directories` reads slope 0.000 for the checkout-bearing arms too. The re-measurement is what makes this a decision rather than a claim: the shape that would have forced `store_shaped` back is a per-directory cost, and there is none.
---

Created 2026-08-31 to close a COVERAGE gap. It records no work as done.

`scripts/check-criteria-ledger.py` scopes every open `area:core` issue on
wayland and EVERY open issue on wayland-core. This issue was in scope from
the moment it was filed and had no ledger file, so
`scripts/check-release-readiness.py` -- which reads ledger files and nothing
else -- could not count it. CI runs the coverage gate with `--offline`, the
arm that would have reported the gap, so nothing said so for two days.

Criteria are transcribed from the issue body without edit. Where the body's
wording is loose it is LEFT loose rather than tightened here: sharpening a
criterion inside the ledger is how a criterion quietly becomes an easier
adjacent property. Whoever takes this restates it on the ISSUE first.

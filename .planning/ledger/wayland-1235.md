---
issue: 1235
repo: FerroxLabs/wayland
kind: defect
title: "One mock turn with a 480 KB tool result costs 63.6 s of CPU; the spill/read-back test is killed by the default nextest budget 3/3"
status: closed
last_verified_commit: 93ede3424
criteria:
  - id: c1
    text: "The 63.6 s of USER CPU is ATTRIBUTED to a named function on the turn loop by measurement -- a profiler or bisecting instrumentation inside the turn path -- and not inferred from reading. 0.10 s system and 38 MB RSS already rule out disk and swap, so the remaining question is which computation, not whether."
    state: met
    evidence: "file:.config/nextest.toml:358:debug:    run 46.530s  of which PIIScrubber::scrub 46.446s (99.8%)"
    owner: core
    note: "MET BY MEASUREMENT 2026-08-31 on lane/f13-s2-turn-cost. THE NAMED FUNCTION IS wcore_safety::pii::decoded_contains_secret, reached from the turn loop through wcore_agent::output_redaction::redact_tool_output -> PIIScrubber::scrub. Bisecting instrumentation compiled into the turn path (commit f06890a24, reverted by 83258da7e to a byte-identical tree) timed seven candidate sites at once, so a near-zero reading is as informative as the hit. DEBUG -- the profile this issue measured and the one CI runs -- one turn with a 480,000-char tool result, host load 42.58: run 46.530s, of which PIIScrubber::scrub 46.446s (99.8%) and decoded_contains_secret 46.106s (99.1%) over 4 calls. RELEASE, same fixture, host load 43.08: run 3.018s, scrub 3.004s, decoded_contains_secret 2.981s. The function base64-decodes the ENTIRE candidate run under four alphabets and re-scans each decode with scrub_direct; the engine scrubs each tool result twice per turn, so the turn costs ~2 whole-payload passes. RULED OUT BY THE SAME RUN: estimate_tokens_from_messages 0.0000s, the #636 shed 0.0013s, scrub_direct 0.0015s, fixture construction and the read-back through the jail 0.001s each -- which is consistent with this issue's own 0.10s system time and 38 MB RSS. SCOPE STATED HONESTLY: the 63.6s in the body was measured on another tree; the graded test itself now costs 44.05 / 45.00 / 45.42s of USER CPU on this tree (three runs alone, /usr/bin/time -v, host load 41-50, RSS 39-41 MB), and the attribution above is of the same 480,000-char payload through the same harness with the shed lever exposed. hetzner has no perf, no gdb and no valgrind and linux-perf has no candidate for kernel 6.8.0-101-generic, so the profiler route was checked and is unavailable; the criterion's second arm, bisecting instrumentation inside the turn path, is what was used. Host: hetzner-dsm, 96 cores; host load is quoted with every figure because a loaded box has invalidated three measurements this cycle."
  - id: c2
    text: "Whether the cost is superlinear in payload size is SETTLED with measurements at three or more payload sizes, since the body records that the cost is not proportional to the payload. The exponent or the shape is stated as a number."
    state: met
    evidence: "file:.config/nextest.toml:370:release  exponent 0.986  R^2 0.9987   6.49 s per MB of tool output"
    owner: core
    note: "MET 2026-08-31, and it SETTLES THE QUESTION AGAINST THE BODY'S SUSPICION. Four payload sizes -- 60,000 / 120,000 / 240,000 / 480,000 chars -- in both profiles, min of two reps, the #636 shed forced OFF via SPILL_PROBE_CEILING so every size takes one identical code path, host load 52-79 recorded per reading. engine.run() seconds: release 0.402 / 0.754 / 1.493 / 3.127; debug 5.622 / 11.025 / 23.279 / 46.438. THE EXPONENT, stated as a number by least-squares fit on log(size) vs log(seconds): RELEASE 0.986 with R^2 0.9987; DEBUG 1.022 with R^2 0.9996. Per-byte slope 6.49 s/MB release and 97.18 s/MB debug, a 15.0x profile gap. So the cost is LINEAR, not superlinear, and the body's 'the cost is not proportional to the payload' is REFUTED rather than confirmed: that table's non-monotonicity (60,000 -> 56.15s but 120,000 -> 14.64s) is the 120,000 row FAILING, i.e. taking a different path with no spill at all, not a superlinear term. This matters for the fix direction -- there is no pathological size to avoid, the price is simply per byte. A 960,000-char arm was run and then DISCARDED rather than reported: MockTool::with_max_result_size(600_000) truncates it to an effective 600,036 bytes, so it is not a valid point on a payload sweep and including it would have flattened the fit. Host: hetzner-dsm, 96 cores; host load is quoted with every figure because a loaded box has invalidated three measurements this cycle."
  - id: c3
    text: "Either the test completes inside [profile.default]'s 30 s slow-timeout, or it carries an explicit per-test budget in .config/nextest.toml with the reason recorded beside it."
    state: met
    evidence: "file:.config/nextest.toml:400:test(=the_engine_spills_where_this_session_can_read_it_back) + test(=tc_2_6_context_overflow_sheds_tool_output_and_continues)"
    owner: core
    note: "MET 2026-08-31 by the criterion's SECOND branch, which is also the issue's own 'Asked for' item 4 -- an explicit per-test budget carrying the measured number -- and it is taken only now that items 1-3 are answered by c1 and c2. The test is NOT under 30s and cannot be made so without either changing the product cost or shrinking the fixture, and the fixture is load-bearing (this issue records that halving it makes the test FAIL outright) and is owned by wayland-core#378. MEASURED ON THIS TREE, debug, each test alone on hetzner-dsm at host load 41-54: the_engine_spills_where_this_session_can_read_it_back 44.08 / 45.04 / 45.47s, and tc_2_6_context_overflow_sheds_tool_output_and_continues 49.60 / 44.59s -- the second is the same class, recorded on wayland-core#395 as a FLAKY 2/2, and is covered by the same entry rather than left to be rediscovered. Both sit ~20% under the default 30s x terminate-after 2 = 60s hard kill, which is exactly why the kill is load-sensitive and why this issue saw it 3/3. THE PERIOD IS DELIBERATELY LEFT AT 30s and only terminate-after moves, 2 -> 4: the SLOW notice still prints on every run so the price stays visible in the log, and the hard kill becomes 120s, 2.4x the slowest measured run. The 59-line comment block above the override records where the time goes, the exponent, the per-MB slope and the host loads, so the next person measuring a slow wcore-agent test finds the answer at the first place they will look. PROVEN LIVE, not asserted: the filter selects exactly the two tests (cargo nextest list -E returns those two and nothing else), and with the block temporarily set to period 2s / terminate-after 1 the test was killed at exactly 2.005s -- so this block demonstrably governs these tests. Restored, tree clean, and the committed budget re-run green: both PASS at 44.487s and 43.924s with '2 slow' reported and no kill. Host: hetzner-dsm, 96 cores; host load is quoted with every figure because a loaded box has invalidated three measurements this cycle."
  - id: c4
    text: "If c1 finds a product cost on the path a real user hits, a regression guard fails when the per-byte term grows. If c1 finds a fixture artifact, that is recorded where the next person measuring a slow wcore-agent test will find it."
    state: met
    evidence: "file:crates/wcore-agent/tests/turn_cost_per_byte_guard.rs:209:passes < 2.5,"
    owner: core
    note: "MET 2026-08-31. c1 found A PRODUCT COST on a path a real user hits -- a tool returning a megabyte is ordinary (Read on a large file, a verbose Bash) and the term survives --release at 6.49 s/MB -- so this criterion takes its first branch. crates/wcore-agent/tests/turn_cost_per_byte_guard.rs drives the PRODUCTION AgentEngine::run() path at N and 2N bytes and asserts (t(2N) - t(N)) / one_scrub_pass(N), which is the number of whole-payload scrub passes a turn costs, stays in [1.5, 2.5]. It is 2.0 today. RED ARMS, each compiled clean first (cargo check RC=0, so the red is behaviour not a build break): adding ONE extra redact_tool_output call at the production site crates/wcore-agent/src/orchestration/mod.rs:2594 (2 passes -> 3) FAILS at median 2.81; removing the PII scrub from redact_tool_output FAILS at -0.06 on the lower bound and simultaneously reddens the companion control with 'the tool result reached history with a live GitHub PAT in it'. Both restored, files touched, git status --porcelain empty, green re-verified at median 2.02. The LOWER bound and the control exist because the obvious way to make this cost go away is to stop scrubbing tool output, which a naive speed guard would reward; the control also asserts ordinary tool output survives byte-intact, so scrubbing everything is not a pass either. Estimator validated under adversarial load rather than assumed: 9 runs at host load 34-45 gave 1.94-2.11 and 5 runs with 48 spinners on 96 cores (load 68-88) gave 1.97-2.03; the first estimator tried produced a 0.98 false red under that load and was replaced. Runtime 8.4-13.0s in debug -- inside the 30s slow-timeout, so the guard needs no override of its own. Host: hetzner-dsm, 96 cores; host load is quoted with every figure because a loaded box has invalidated three measurements this cycle."
  - id: c5
    text: "It is NOT closed by adding a line to .config/flaky-allowlist.txt. The issue is explicit that doing so would suppress the symptom of a product cost nobody has measured, and that refusal is the reason it is an issue at all."
    state: met
    evidence: "file:.config/nextest.toml:392:NOT an entry in .config/flaky-allowlist.txt, deliberately. #1235 is explicit"
    owner: core
    note: "MET 2026-08-31 by NOT DOING IT, and verified rather than asserted. No line naming the_engine_spills_where_this_session_can_read_it_back, tc_2_6_context_overflow_sheds_tool_output_and_continues or spill_readback was added to .config/flaky-allowlist.txt, and none was there before: grep for 'spill_readback|the_engine_spills' over that file returns nothing, with a POSITIVE CONTROL in the same call showing the file does hold 11 live non-comment entries (so the zero is absence, not a broken query). git diff --name-only ca15a48bf..HEAD does not list .config/flaky-allowlist.txt. The refusal is honoured in substance and not only in letter: the reason the issue gives for it -- that allowlisting would suppress the symptom of a product cost NOBODY HAD MEASURED -- no longer applies, because the cost is now measured (c1), its shape is fitted (c2) and it is guarded against growth (c4). What ships instead is a slow-timeout budget carrying that measurement, which keeps the SLOW notice printing on every run and therefore keeps the price visible, where an allowlist entry would have hidden a kill behind a retry. The distinction is recorded in the override's own comment so a later reader cannot mistake one for the other. Host: hetzner-dsm, 96 cores; host load is quoted with every figure because a loaded box has invalidated three measurements this cycle."
---

Created 2026-08-31. This issue was filed 2026-08-29/30 by this cycle's own
verification, was in scope for the release gate from that moment, and had no
ledger file -- so scripts/check-release-readiness.py, which reads ledger files
and nothing else, could not count it. CI runs the coverage arm with --offline,
which is the arm that would have said so.

Its body declared no acceptance criteria, so it could not have been closed as
filed either. The criteria above are AUTHORED from measurements the body
already records.

Overlaps wayland-core#395, which asks the same debug-vs-release question about
the same per-byte term and carries handoff: FerroxLabs/wayland-core#378. Whoever
takes either should take both, or state which one owns the measurement.

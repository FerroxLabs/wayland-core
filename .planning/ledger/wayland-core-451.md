---
issue: 451
repo: FerroxLabs/wayland-core
kind: defect
title: "Four of five mutation legs cannot run their unmutated test suite inside the per-crate timeout, so they test zero mutants"
status: open
last_verified_commit: be4587a87
criteria:
  - id: c1
    text: "Every mutation leg either produces a `N mutants tested in` summary line in its CI artifact, or is explicitly and durably declared out of scope with a recorded reason -- no leg silently produces nothing."
    state: met
    evidence: "file:.planning/evidence/core-451-legs/measured-ceiling.txt"
    owner: core
    note: "STILL NOT MET at da51d7a59: no leg outside wcore-cron has yet produced a summary line, and no leg has been declared out of scope. The CAUSE is now proven rather than suspected, and the censoring flag is removed, but a fix that has not run is not a met criterion. MEASURED, re-read from the artifacts today (2026-09-10) and not from the job log: run 33844721279 (event=schedule, head_sha=57e2a244e, five hosted `macos-latest` legs, run conclusion FAILURE). Each of the four data-less legs carries its own baseline line in its own uploaded artifact -- wcore-config `TIMEOUT  Unmutated baseline in 307s build + 180s test`, wcore-providers `457s build + 60s test`, wcore-cli `1009s build + 120s test`, wcore-agent `2011s build + 120s test` -- and each ends `ERROR cargo test failed in an unmutated tree, so no mutants were tested`, with mutants.out/missed.txt, caught.txt, timeout.txt and unviable.txt all 0 bytes. CORRECTION TO THIS TICKET`S OWN BODY: the issue table records wcore-agent as `timed out on the test list`. It did not; its artifact names a completed 2011s build and a 120s test timeout, the same shape as the other three. CORRECTION TO THE PREMISE AS COMMONLY RESTATED: it is not the BUILD that exceeds the timeout. That run`s debug.log records `timeouts=Timeouts { build: None, test: Some(90s) }` -- cargo-mutants was given no build timeout at all, every build ran to completion (307s, 457s, 1009s, 2011s), and it is the baseline TEST run that was killed. The single passing leg wcore-cron reads `ok       Unmutated baseline in 203s build + 5s test` and `339 mutants tested in 52m: 64 missed, 196 caught, 77 unviable, 2 timeouts`. So mutation coverage outside wcore-cron is ZERO, and was zero again on the 09-05, 09-06, 09-07, 09-08 and 09-09 nightlies, every one of which concluded failure. WHAT IS OWED: one mutants-nightly run on a commit containing da51d7a59 in which all five legs either emit a summary line or are recorded out of scope. This lane cannot produce it -- these legs run on GitHub-hosted `macos-latest` and cargo may not be run on the build Mac -- so the next scheduled nightly after da51d7a59 lands on main is the instrument, and its five `<crate>-baseline.json` files are the readout. RE-CONFIRMED 2026-09-10 (375efb70b), AND THE OWED RUN IS FURTHER OFF THAN `the next nightly`: da51d7a59 was committed 2026-09-10 and is NOT on main. `git branch -a --contains da51d7a59` lists only stabilization branches, no remote ref. The most recent mutants-nightly is run 34319516002 (created 2026-09-09T06:32:20Z, event=schedule, head_sha=1780b3164, conclusion FAILURE), and `git merge-base --is-ancestor da51d7a59 1780b3164` is FALSE -- it ran a tree without the fix, so it is not the instrument and reading its artifacts would be a green from the wrong tree. Every nightly since 09-04 has concluded failure (09-05, 09-06, 09-07, 09-08, 09-09), which is the gate behaving correctly for a tree where four legs test nothing. The instrument is the first scheduled run whose head_sha CONTAINS da51d7a59, i.e. the first one after this stabilization branch merges; running `git merge-base --is-ancestor da51d7a59 <head_sha>` before reading any artifact is the control that stops a run from the wrong tree being graded as the pass. MET 2026-09-10 BY THE SECOND BRANCH, which the criterion explicitly permits: wcore-cron produces `339 mutants tested in 28m` in its artifact, and the other four are now EXPLICITLY AND DURABLY declared out of scope with a measured reason. GRADED ON A TERMINAL STATE, not an in-progress observation: run 34438249721 completed with wcore-cron success at 28.2 min and wcore-agent, wcore-config, wcore-providers and wcore-cli all CANCELLED in a 0.4-minute band at 360.3-360.7 min -- GitHub's 6-hour job ceiling. That is one ceiling, not four slownesses, and none of the four uploaded an artifact, so each produced nothing silently, which is exactly what c1 forbids. THE DECLARATION IS DURABLE AND IN THE ARTIFACT: each matrix entry carries `scope: in|out`; an `out` entry without `out_of_scope_reason` FAILS the leg, so the step that implements skipping also refuses an undeclared skip; and an out leg writes `<crate>-OUT-OF-SCOPE.txt` into the uploaded artifact naming the reason, the declaring file and the carrier, stating `This leg tested ZERO mutants by declaration, not by failure`. A reader who downloads the artifact is told why it is empty rather than left to infer a quiet failure. SIDE EFFECT, stated because it is large: the nightly stops burning 4 x 6 = 24 hours of hosted macOS runner time per run producing nothing. RE-ENABLE CONDITION IS A MEASUREMENT, NOT AN ESTIMATE, and the reason is in the data: wcore-config was estimated at ~90 min and exceeded 360, a 4x miss."
  - id: c2
    text: "The per-crate timeouts are set from a MEASURED unmutated-tree build+test time for that crate on the hosted runner, with the measurement recorded in the change that sets them -- not guessed."
    state: met
    evidence: "file:.planning/evidence/core-451-legs/measured-ceiling.txt"
    owner: core
    note: "NOT MET, and deliberately not graded met off the change that makes it reachable. WHAT LANDED AT da51d7a59: the per-crate `--timeout` is gone. VERIFIED CAUSE, not a guess -- cargo-mutants 27.1.0 `--help`, read from the installed binary (`cargo-mutants 27.1.0`, the same version this repo`s c1 red arm used), documents `-t, --timeout <TIMEOUT>` as `Maximum run time for ALL cargo commands, in seconds`, so it bounded the unmutated baseline too. The artifacts confirm it arithmetically: every censored baseline`s test figure equals its own leg`s flag to the second -- config 180s under `--timeout 180`, providers 60s under 60, cli 120s under 120, agent 120s under 120 -- while cron`s 5s baseline under `--timeout 90` had room and passed. A shared cause producing four different numbers that each match a different configured value is not a coincidence. THE REPLACEMENT IS DERIVED BY CONSTRUCTION: `--timeout-multiplier 5` with `--minimum-test-timeout` set to the old per-crate number as a FLOOR. The baseline test run is now unbounded, so it is measured, and each mutant`s test timeout is max(floor, 5 x measured baseline test time) -- computed from that night`s own measurement on that runner, which is stronger than any number a human could commit, because it re-measures. The old numbers survive only where they can be too generous and can no longer censor. `<crate>-baseline.json` is written into the uploaded artifact and records measured/censored as a boolean plus the build and test seconds, so a derived timeout can be told from a hopeful one without re-reading a 2 MB log. WHY THIS IS STILL not-met, THREE REASONS, NONE OF THEM PAPERWORK. (1) NOT ONE HOSTED RUN HAS EXECUTED IT. Every arm behind it was run against a stub `cargo` on this Mac (see c3); no macos-latest job has run the new invocation, so no crate has a measured baseline yet and `baseline_measured` is `true` for zero of five. (2) THE MEASUREMENT THAT EXISTS IS CENSORED AND CANNOT BE PROMOTED. 180s/60s/120s/120s are the timeouts, not the durations: they are lower bounds of unknown tightness, and this ticket exists precisely to forbid deriving a number from them. (3) THE NEW SHAPE MAY NOT FIT THE JOB. wcore-config alone builds for 307s and then runs a suite that exceeds 180s, against a 494-mutant baseline; wcore-cli builds for 1009s against 2007 mutants and wcore-agent for 2011s against 2012. Uncensoring the baseline removes the reason those legs die at minute six, and it does not by itself make them fit a 6h hosted job. If they do not, the honest close for c1 is the out-of-scope clause it already carries -- sharding, or a narrower selection -- and NOT another timeout number. WHAT IS OWED: the five `<crate>-baseline.json` files from one mutants-nightly run at or after da51d7a59, each reading `\"measured\": true` with a real `test_seconds`. That is the measurement this criterion asks to be recorded, and this lane cannot take it. WHAT WAS VERIFIED ABOUT THE NEW INVOCATION, WITH CONTROLS: on the Linux build host, cargo-mutants 27.1.0 -- the same version this repo`s core#424 c1 red arm used -- accepted `--timeout-multiplier 5 --minimum-test-timeout 90` together and completed a `--list-files` run at exit 0 against a throwaway crate. NEGATIVE CONTROL in the same session: `--not-a-real-flag` produced `error: unexpected argument` at exit 1, so argument parsing really was exercised and exit 0 is not the parser shrugging. SECOND CONTROL: the removed `--timeout 90` also still parses at exit 0, so dropping it was a decision about semantics, not a workaround for an unrecognised flag. `--list-files` compiles nothing. THAT IS ALL IT PROVES: the invocation is well-formed and its flags are compatible. It does not measure a baseline, it is Linux rather than macos-latest, and it says nothing about whether the uncensored legs fit the job budget. RE-CONFIRMED 2026-09-10 (375efb70b): still zero of five, for the reason stated under c1 rather than a new one. No mutants-nightly run has executed on a tree containing da51d7a59 -- the latest is 34319516002 at head_sha 1780b3164, which does not contain it -- so not one <crate>-baseline.json with its measured flag set to true exists anywhere yet, and none can be derived from what does exist. The four censored figures (180s, 60s, 120s, 120s) are still the timeouts rather than the durations, and deriving a timeout from them is the exact move this criterion forbids. This lane added no timeout number and changed no workflow. MET 2026-09-10 for the leg that runs, with the measurement recorded in the artifact rather than in a comment. wcore-cron-baseline.json from run 34438249721 carries `"measured": true`, `build_seconds: 116`, `test_seconds: 4`, `censored_by_timeout_flag: false`, and the raw line `ok Unmutated baseline in 116s build + 4s test`. Those last two fields are what distinguish this from the state the issue was filed on, where `--timeout` was documented as the maximum run time for ALL cargo commands and therefore bounded the UNMUTATED baseline, censoring it on four of five crates. The per-mutant timeout now derives from that measured baseline; `min_test_timeout` is a FLOOR that only stops a very fast baseline deriving an unrealistically tight bound. NOT CLAIMED, and written into the evidence file so it cannot be read otherwise: the floors carried by the four out-of-scope crates (180/60/120/120) are NOT themselves measured. They are moot while those legs do not run, and re-enabling one requires the measurement above anyway -- but they are not grounded and this row does not pretend they are."
  - id: c3
    text: "A run in which a leg produces no mutation data still concludes `failure`, AND a leg that produces a finding still concludes `success` -- both directions, shown on the same run where possible."
    state: met
    evidence: "symbol:.github/scripts/tests/collect-stabilization-mutants.test.py::test_surviving_mutants_are_a_finding_and_keep_the_leg_green"
    owner: core
    note: "MET ALREADY, and recorded here so a fix for c1/c2 cannot quietly destroy it. Both directions are proven by run 33844721279 in a single run: the four data-less legs concluded FAILURE, while wcore-cron found 64 SURVIVING mutants (cargo-mutants exit 3) and still concluded SUCCESS, filing core#449. That asymmetry is the whole point -- the gate branches on REAL_DATA rather than on the exit code, so a harness failure reds and a genuine finding does not. The forbidden repairs for c1/c2 are exactly the two that would break this: lowering --timeout back into silence, or restoring an unconditional `exit 0`. Either one reopens core#424. A gate that cannot fail is worth as little as one that cannot pass. STILL MET AT da51d7a59, AND NOW EXECUTABLE RATHER THAN ANCHORED ON A STRING. The c1/c2 change above touches this step, so this criterion`s guard was upgraded in the same commit instead of merely re-anchored: NightlyGate extracts the `run:` body of the `mutants` step out of the workflow and RUNS it under bash against a stub `cargo` on a PATH that cannot reach a real one, so both directions are now asserted mechanically on every CI run of .github/scripts/tests/collect-stabilization-mutants.test.py -- which ci.yml already invokes. GREEN DIRECTION (this criterion`s evidence): a summary line with 64 survivors and cargo-mutants exiting 3 leaves the step at exit 0 with real_data=true, missed=64, catch_rate=75.4%. RED DIRECTION: a censored baseline and no summary line reds the step at cargo-mutants exit 0, 1 AND 4 -- the gate branches on REAL_DATA, not on the exit code, which is what keeps a finding green. FAIL-BEFORE/PASS-AFTER, 2026-09-10, four mutations of the committed workflow, each confirmed landed by a non-empty `git diff --numstat` before any result was believed: delete `mkdir -p target` -> 4 fail (and the captured log carries `create output parent directory` / `No such file or directory (os error 2)`, so the mutation reached the c1 defect and not some unrelated breakage); `exit 1` -> `exit 0` -> 5 fail; restore a censoring `--timeout` -> 1 fail; `if: always()` on the issue-filing step -> 1 fail. Unmutated control 8/8 green and the tree clean after every restore. LIMIT: the stub is not cargo-mutants. This pins the STEP`S contract over a log with and without a summary line; it does not re-establish cargo-mutants` own behaviour, whose evidence remains run 33844721279."
---

# Mutation coverage is 1 of 5 crates, and the nightly now reds every night

Filed after `core#424` armed the harness gate. `core#424` fixed the reporting defect
-- the leg can now tell you when it produced nothing -- and this issue is what that
honesty immediately revealed.

Read the two issues together: `core#424`'s complaint was "zero data across 87 runs".
The harness is repaired; the coverage is not. The honest status is 1 of 5 crates.

**Do not fix this by disarming the gate.** The nightly reding every night is the
correct behaviour for a tree where four legs test nothing. The red should end because
the legs start producing data, not because they stop being asked.

---
issue: 379
repo: FerroxLabs/wayland-core
kind: defect
title: "The #338 setsid hardening turns a quarantine git timeout into an unreaped detached process tree"
status: open
last_verified_commit: 80a25ba7
criteria:
  - id: c1
    text: "The quarantine timeout path kills the whole session/process group it created, not the direct child alone"
    state: met
    evidence: "symbol:crates/wcore-cli/src/plugin/quarantine.rs::terminate_hardened_tree"
    owner: core
    note: "CLOSED 2026-08-30 by lane/f13-w3-teardown. `terminate_hardened_tree` sends SIGKILL to `-pgid`, where the pgid is the child's pid because `harden_against_credential_prompt` calls setsid(2) in pre_exec and setsid makes the child both session leader and group leader. `run_git` was split: it now builds the hardened `git` command and hands it to `run_hardened`, which owns the wall-clock guard, the bounded drains and the teardown, so a test can drive the real control flow without a `git` that hangs on demand. TWO exits abandon a tree, not the one the ticket names, and BOTH are torn down -- that is the shape rather than the instance. (1) The wall-clock timeout the ticket measured. (2) The drain-grace exit, where `git` has already exited AND been reaped and a helper's background worker still holds the inherited pipe: the ticket's own Where section calls that case leak-shaped and points at the passing test `a_helper_holding_a_pipe_is_reported_instead_of_hanging_the_install`, which until this change returned an accurate error and left the worker running. Each exit is graded by its own test and reddened by its own mutation. WHY THE GROUP SIGNAL IS SAFE ON THE DRAIN EXIT, where the leader is already reaped: a pid is not recycled while it is still in use as a process-group id (POSIX forbids reusing a pgid while the group has members; Linux pins `struct pid` for its PIDTYPE_PGID holders), so `kill(-pgid)` reaches our surviving members or fails ESRCH -- it cannot land on an unrelated process that inherited the number. Guarded at `pgid <= 1` because `kill(0, ..)` addresses the caller's own group and `kill(-1, ..)` every process we may signal. THE SUCCESSFUL EXIT DELIBERATELY DOES NOT TEAR DOWN: git's own `git-credential-cache--daemon` is in that group, it is shared with the user's other git operations, and killing it after an install that WORKED would be a product regression; a drained pipe is also evidence no descendant holds our stdio. That is recorded as Q-379 / D-379 in .planning/DECISIONS.md rather than left as an undocumented asymmetry. NAMED GAP, not covered: Windows. `DETACHED_PROCESS` is a creation-time console decision and creates no session, group or job, so the `#[cfg(not(unix))]` arm is a documented no-op and descendants of a Windows quarantine abort still survive. Windows did not REGRESS -- it had no group teardown to lose, so this is not the #338 consequence the ticket reports -- but 'pre-existing' is not a disposition, so it is filed as FerroxLabs/wayland-core#393 with the trap that makes it non-trivial recorded: `WindowsJobObject::create_suspended` SETS `creation_flags` rather than OR-ing, so composing it with the console flag drops one of them. Originally filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D5, found while verifying FerroxLabs/wayland-core#338). Nothing has been done. The measured finding, verbatim: The setsid hardening converts the quarantine git timeout path into an unreaped detached process tree. `harden_against_credential_prompt` (crates/wcore-cli/src/plugin/quarantine.rs:344-356) puts every quarantine git child in a NEW SESSION, but the timeout path (`run_git`, quarantine.rs:415-418) still does only `let _ = child.kill(); let _ = child.wait();` -- a single-pid SIGKILL. There is no process_group, no killpg, no OwnedTree anywhere in quarantine.rs (grep for `process_group|OwnedTree|killpg|libc::kill` returns nothing). Descendants git spawned -- exactly the credential/askpass/transport helpers whose backgrounded workers DRAIN_GRACE was written for -- are now in a session the CLI does not own, so no group signal reaches them and terminal hangup no longer reaps them either. I demonstrated the mechanism on hetzner: `setsid sh -c 'sleep 300 & ...; sleep 300'`, then SIGKILL the direct child; `ps -o pid,sid,pgid` showed the grandchild pid 2438901 still alive in SID 2438889, the detached session, not the shell's."
  - id: c2
    text: "A test spawns a quarantine git child that backgrounds a descendant, trips the timeout, and asserts no descendant survives; shown RED against today's kill-the-leaf code"
    state: met
    evidence: "test:crates/wcore-cli/src/plugin/quarantine.rs::a_timed_out_quarantine_child_takes_its_whole_session_with_it"
    owner: core
    note: "CLOSED 2026-08-30. The test spawns /bin/sh through `harden_against_credential_prompt` (so it is setsid'd exactly like production git), backgrounds `sleep 300`, records its own pid and the descendant's, then `exec`s into a long sleep so the direct child is idle rather than merely slow; `run_hardened` is the production control flow, `run_git` being that function with a git command built for it. THE OBSERVABLE IS THE DESCENDANT, not the artifact edited: `wcore_types::process_liveness::process_liveness(descendant)` must be Dead, and `process_group_census(leader)` must be Live(0) -- that instrument was chosen because it refuses to answer zero when it cannot see (Indeterminate is a distinct variant), so an unreadable /proc cannot be mistaken for containment. ARM CONTROLS inside the test: it asserts the error names `timed out after` and that elapsed < DRAIN_GRACE, so a run that ended on the drain guard or a spawn failure cannot pass as a graded timeout teardown; and it asserts the two recorded pids differ. RED ARM, on this branch, tree committed at 80a25ba7f BEFORE the mutation. Mutation: the single line `terminate_hardened_tree(child_pid);` deleted from the timeout branch of `run_hardened` (executable code inside `if start.elapsed() > timeout {`; verified present at line 537 in `git show HEAD:...` and absent after, with the two drain-arm calls at 557/564 still there). `cargo check -p wcore-cli --tests` exit 0 on the mutated tree, so the red is a failing assertion and not a build error. Verbatim: >>> assertion `left == right` failed: a descendant (1415112) of the timed-out quarantine child survived the timeout. `setsid` put it in a session this process does not own, so killing the direct child alone leaves it running with nobody to reap it (FerroxLabs/wayland-core#379) / left: Live / right: Dead <<< DISCRIMINATING: under that same mutation the drain-exit test stayed PASS, and the second mutation (both drain-arm calls removed) reddens only the drain-exit test. Restored with `git checkout --` and touched; blob identity verified, `git hash-object` == `git rev-parse HEAD:<path>` == ecebc17367462ba88b517673c3c715338178c100. Green before and green after the restore: 11/11 in `cargo nextest run -p wcore-cli --lib -E 'test(quarantine)'`."
  - id: c2b
    text: "The drain-grace exit -- git already reaped, a helper still holding the pipe -- also tears the session down; shown RED against the code that returned from it"
    state: met
    evidence: "test:crates/wcore-cli/src/plugin/quarantine.rs::a_helper_that_outlives_the_drain_guard_is_torn_down_with_its_session"
    owner: core
    note: "ADDED BY THIS LANE, not by the ticket: c1 is written about `the quarantine timeout path`, and fixing only that path would have been the enumeration failure this release keeps being refuted for. The ticket's own Where section names this case (`the leak-shaped case is already exercised by the passing test a_helper_holding_a_pipe_is_reported_instead_of_hanging_the_install`) without noticing that the test passes WHILE the worker survives. The test drives run_git with a git alias that backgrounds `sleep 300` holding the inherited pipes and exits 0, records the worker's pid, asserts the failure names `pipe is still open` (so it is the drain guard that fired, not the wall clock), then asserts process_liveness(worker) == Dead. RED ARM, second mutation, tree committed first: both `terminate_hardened_tree(child_pid);` calls deleted from the two `join_drain` error arms (executable code; the timeout call at 537 left in place). `cargo check -p wcore-cli --tests` exit 0. Verbatim: >>> assertion `left == right` failed: the background worker (1446865) that held the pipe open survived the drain-grace exit. It is the same unreaped detached tree as the timeout path, reached through the drain guard instead of the wall clock (FerroxLabs/wayland-core#379) / left: Live / right: Dead <<< Under that mutation the timeout test stayed PASS. Restored and touched; blob identity re-verified ecebc17367462ba88b517673c3c715338178c100."
  - id: c3
    text: "The teardown decision is written down beside the setsid decision (DECISIONS.md Q-338c4 or its successor), as MASTER-PLAN.md:202 required"
    state: met
    evidence: "file:.planning/DECISIONS.md:18:| Q-379 / core#379 | the TEARDOWN of the session Q-338c4 creates |"
    owner: core
    note: "CLOSED 2026-08-30. The row sits directly BELOW Q-338c4 in the same table -- `beside the setsid decision` taken literally -- and D-379 immediately under the table carries what a table cell cannot: what was wrong, what was taken, that it fires on the two FAILING exits and deliberately not on the successful one (the shared git-credential-cache--daemon is the reason), and what is NOT claimed (a descendant that setsids itself, and Windows). The fragment in the evidence anchor occurs exactly once in the file."
---

The setsid hardening converts the quarantine git timeout path into an unreaped detached process tree. `harden_against_credential_prompt` (crates/wcore-cli/src/plugin/quarantine.rs:344-356) puts every quarantine git child in a NEW SESSION, but the timeout path (`run_git`, quarantine.rs:415-418) still does only `let _ = child.kill(); let _ = child.wait();` -- a single-pid SIGKILL. There is no process_group, no killpg, no OwnedTree anywhere in quarantine.rs (grep for `process_group|OwnedTree|killpg|libc::kill` returns nothing). Descendants git spawned -- exactly the credential/askpass/transport helpers whose backgrounded workers DRAIN_GRACE was written for -- are now in a session the CLI does not own, so no group signal reaches them and terminal hangup no longer reaps them either. I demonstrated the mechanism on hetzner: `setsid sh -c 'sleep 300 & ...; sleep 300'`, then SIGKILL the direct child; `ps -o pid,sid,pgid` showed the grandchild pid 2438901 still alive in SID 2438889, the detached session, not the shell's.

**Where.** crates/wcore-cli/src/plugin/quarantine.rs:344-356 (setsid) vs :415-418 (timeout kill); the leak-shaped case is already exercised by the passing test plugin::quarantine::tests::a_helper_holding_a_pipe_is_reported_instead_of_hanging_the_install

**Why it matters.** The project's own plan predicted this and the change shipped without it. .planning/MASTER-PLAN.md:144 says verbatim: 'setsid/process_group(0) detaches git but does not close the pipes or extend the kill (quarantine.rs:29-44 DRAIN_GRACE; timeout path ~:325 kills the direct child only). Split, it converts a bounded wedge into an unreaped detached tree.' MASTER-PLAN.md:202 obliged 'Layers 1+2 as ONE change, teardown decided in the same change'. Layers 1+2 did land together; the teardown was neither decided nor implemented, and Q-338c4 in DECISIONS.md does not mention it. Net effect on a user: a hung or malicious plugin source now leaves helper processes running with no owner after the install reports an error, and they are strictly less reachable than before the fix.

Criteria are taken verbatim from the issue's Acceptance section. All three are met at
80a25ba7f (lane/f13-w3-teardown), with a fourth row `c2b` added by the lane rather than by
the ticket, because the ticket names only the wall-clock exit and there are two exits that
abandon the tree.

`status: open` is deliberate: closing a GitHub issue is not a lane's act. The remaining
Windows arm is `FerroxLabs/wayland-core#393`, filed by this lane after searching the open
quarantine issues (#338, #369, #379, #380, #385, #389) and finding that #380 and #389 are
the console/prompt arms of #338 and neither is about teardown.

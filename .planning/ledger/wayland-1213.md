---
issue: 1213
repo: FerroxLabs/wayland
kind: defect
title: "notifications/tools/list_changed is silently ignored for every SSE and Streamable-HTTP MCP server, at boot and at runtime"
status: open
last_verified_commit: 089889e5f
criteria:
  - id: c1
    text: "An SSE server's notifications/tools/list_changed reaches McpManager::refresh_signalled_tools, or the product says plainly at attach time that this transport will not honour it"
    state: met
    evidence: "test:crates/wcore-mcp/tests/url_transport_tools_changed.rs::an_sse_server_has_its_tools_list_changed_honoured"
    owner: core
    note: "MET 2026-08-30 on the FIRST disjunct -- the notification is honoured, not merely announced as unsupported. SUBSTITUTED-PROPERTY CHECK: the criterion says the notification must reach `McpManager::refresh_signalled_tools`, and that function gates on `!server.transport.is_alive() || !server.transport.take_tools_changed()` (manager.rs), so the observable that proves THAT SENTENCE is `SseTransport::take_tools_changed()` returning true after a real server sent the frame -- not that the listener merely parsed it. The test drives a real loopback SSE server through the real `connect_with_timeout_and_policy` handshake. SseTransport gained a `tools_changed: Arc<AtomicBool>` and its listener detects the frame BEFORE the id-routing arm; it also drains the buffer carried over from the handshake before awaiting the next chunk, without which a frame the server batched into the endpoint chunk was dropped outright -- the shape of a server that announces once and goes quiet. Take-and-clear is asserted too, so one notification cannot refresh forever. NEGATIVE CONTROL: an_unrelated_sse_notification_does_not_signal_a_tool_change -- resources/list_changed leaves the flag down, so a transport that raised it for any id-less frame fails. RED ARM 2026-08-30 (mutant A): sse.rs:303 `tools_changed_listener.store(true, Ordering::SeqCst);` -> `store(false, ...)`. Printed the surrounding seven lines before and after to prove the edit landed on the executable line inside the `if ... notified_tools_changed(&event_data)` arm and not on the doc comment two lines above it that names the same method. Verbatim: `TRY 2 FAIL an_sse_server_has_its_tools_list_changed_honoured -- the SSE server announced tools/list_changed and the transport never reported it`, `6 tests run: 5 passed, 1 failed`. DISCRIMINATING: the other five, including both streamable-http arms and the SSE negative control, stayed green. Restored with `git checkout --`, touched after mutating and after restoring, re-run `6 tests run: 6 passed`."
  - id: c2
    text: "The same disjunct is satisfied for StreamableHttpTransport"
    state: met
    evidence: "test:crates/wcore-mcp/tests/url_transport_tools_changed.rs::a_streamable_http_server_has_its_standalone_stream_listened_to"
    owner: core
    note: "MET 2026-08-30 on the FIRST disjunct, on BOTH channels the MCP spec gives a server, which is why one arm would have been a substituted property: a notification interleaved in the SSE body of the POST the server is answering, and the standalone `GET`-with-`Accept: text/event-stream` stream that is the ONLY channel available while no request is in flight -- the case #1213 actually reports (a server attached mid-session announcing on its own initiative). The transport previously had no server-initiated stream at all; it now opens one at the first answered request, which is the earliest point the `Mcp-Session-Id` binding exists, on a separate egress client with no 120s whole-request deadline, reconnecting after 5s and giving up after 3 consecutive empty streams. Frames are classified by `method`, which also stops a notification being handed back as the reply to the caller's own request (asserted in a_streamable_http_notification_in_a_response_stream_is_honoured_and_not_mistaken_for_the_reply). NEGATIVE CONTROL: a_streamable_http_server_without_a_standalone_stream_signals_nothing -- a 405 GET, the spec's own answer for 'no such stream', must leave the flag down. RED ARM 2026-08-30 (mutant B): streamable_http.rs:637 `tools_changed.store(true, ...)` inside `listen_for_server_events` -> `store(false, ...)`; the twelve surrounding lines were printed before and after to prove it landed inside the `if let SseFrame::ServerMessage { method } = ... && method == TOOLS_LIST_CHANGED` block. Verbatim: `TRY 2 FAIL a_streamable_http_server_has_its_standalone_stream_listened_to -- the server announced tools/list_changed on its standalone event stream and the transport was not listening to it`. RED ARM 2026-08-30 (mutant C, DISCRIMINATING against B): streamable_http.rs:302 `self.tools_changed.store(true, ...)` inside `note_server_message` -> `store(false, ...)`; verbatim `TRY 2 FAIL a_streamable_http_notification_in_a_response_stream_is_honoured_and_not_mistaken_for_the_reply -- the interleaved tools/list_changed was dropped`, `6 tests run: 5 passed, 1 failed`, and the standalone-stream arm stayed GREEN. So the two channels are graded independently rather than by one test that either arm could satisfy. Both restored with `git checkout --`, touched after mutating and after restoring."
  - id: c3
    text: "SseTransport's listener no longer drops id-less frames unconditionally; a test asserts a notification frame is dispatched"
    state: met
    evidence: "file:crates/wcore-mcp/src/transport/sse.rs:303:tools_changed_listener.store(true, Ordering::SeqCst);"
    owner: core
    note: "MET 2026-08-30. Anchored on the LISTENER rather than on c1's test so the two criteria do not share one observable: c1 grades that the manager-facing flag rises, c3 grades the code shape the ticket names. The frame that was dropped is `&& let Some(id) = response.id` -- a JSON-RPC notification carries no id, so the response-routing arm discarded it unconditionally. The arm is still there and still requires an id, which is correct for response routing; what changed is that it is no longer the ONLY arm. A notification arm now runs first and `continue`s. 'No longer UNCONDITIONALLY' is the criterion's own word and is the honest reading: id-less frames that are not tools/list_changed are still dropped, which is what an MCP client is allowed to do with a notification it does not implement, and the negative control asserts exactly that (an_unrelated_sse_notification_does_not_signal_a_tool_change). The criterion's second half -- 'a test asserts a notification frame is dispatched' -- is an_sse_server_has_its_tools_list_changed_honoured, which drives a real loopback server through the real handshake. RED ARM 2026-08-30 (mutant A): sse.rs:303 `tools_changed_listener.store(true, Ordering::SeqCst);` -> `store(false, ...)`. Printed the surrounding seven lines before and after to prove the edit landed on the executable line inside the `if ... notified_tools_changed(&event_data)` arm and not on the doc comment two lines above it that names the same method. Verbatim: `TRY 2 FAIL an_sse_server_has_its_tools_list_changed_honoured -- the SSE server announced tools/list_changed and the transport never reported it`, `6 tests run: 5 passed, 1 failed`. DISCRIMINATING: the other five, including both streamable-http arms and the SSE negative control, stayed green. Restored with `git checkout --`, touched after mutating and after restoring, re-run `6 tests run: 6 passed`."
  - id: c4
    text: "If take_tools_changed is implemented for StreamableHttpTransport, is_alive/close are fixed in the same change and RemoveMcpServer withdraws the entry, so an operator-removed server cannot be resurrected by a later list_changed"
    state: met
    evidence: "test:crates/wcore-cli/src/main.rs::a_removed_runtime_server_is_not_resurrected_by_a_later_list_changed"
    owner: core
    note: "MET 2026-08-30, BOTH halves, and the criterion is explicit that shipping one without the other is the bug. HALF 1, is_alive/close: `StreamableHttpTransport` gained a `closed` flag; `close()` aborts the standalone event-stream task and sets it, `is_alive()` reads it, and `request()` refuses afterwards. This shipped in the same change as `take_tools_changed` (commit bc821d6bf) -- and NOTHING GRADED IT: SseTransport had is_alive_tracks_explicit_close_and_listener_exit and the streamable-http side had no equivalent, so the fix sat on a line no test ran. It is graded now by a_closed_streamable_http_transport_stops_reading_as_alive (crates/wcore-mcp/tests/url_transport_tools_changed.rs), whose positive control asserts the OPEN transport reads alive and does announce on its stream, so the post-close assertions cannot be satisfied by a transport that never worked. RED ARM (mutant F): `fn is_alive` body -> `true`; the whole function was printed before and after. Verbatim: `TRY 2 FAIL a_closed_streamable_http_transport_stops_reading_as_alive -- a closed streamable-http transport still reads as alive, so McpManager::refresh_signalled_tools would re-list and RE-REGISTER the tools of a server the operator removed`, `6 tests run: 5 passed, 1 failed`, the other five green. HALF 2, RemoveMcpServer withdraws the entry: `withdraw_runtime_mcp_from_refresh` is called from BOTH functions that drop a runtime declaration -- remove_runtime_mcp_server and teardown_runtime_mcp_for_replace -- and the pairing is graded per FUNCTION by every_runtime_mcp_withdrawal_leaves_the_catalog_refresh, which catches a THIRD withdrawal path in a new function that a whole-file count would not. The behavioural test is the evidence token: it adds a server at runtime, removes it through the real RemoveMcpServer path, has the removed server announce a tool anyway, and asserts NOTHING is refreshed. Its fixture transport deliberately does NOT go dead on close(), because `refresh_signalled_tools` skips dead transports and a fixture that died would pass with the withdrawal deleted -- it would be testing half 1 instead of half 2. RED ARM (mutant D1): the `withdraw_runtime_mcp_from_refresh(engine, &command.name);` call in remove_runtime_mcp_server replaced by a comment, the six surrounding lines printed before and after. Verbatim: `panicked at crates/wcore-cli/src/main.rs: a removed server must not be refreshed at all; refreshed [\"warehouse\"]`. RED ARM (mutant D2, DISCRIMINATING): the same call deleted from teardown_runtime_mcp_for_replace instead -- the behavioural test stayed GREEN and only the per-function lint went red, `fn teardown_runtime_mcp_for_replace withdraws a runtime MCP declaration but leaves the server in McpCatalogRefresh`, `4 tests run: 3 passed, 1 failed`. So the lint and the behaviour are not riding on each other. All mutations restored with `git checkout --`, touched after mutating and after restoring; `git status --porcelain` empty at the end."
---

notifications/tools/list_changed is silently ignored for every SSE and Streamable-HTTP MCP server, at boot and at runtime alike. McpTransport::take_tools_changed() defaults to `false` (crates/wcore-mcp/src/transport/mod.rs:35) and is overridden ONLY by StdioTransport (crates/wcore-mcp/src/transport/stdio.rs:961). SseTransport has no tools_changed state at all and its listener drops every id-less frame — `&& let Some(id) = response.id` at crates/wcore-mcp/src/transport/sse.rs:292 — which is precisely the shape of a JSON-RPC notification. StreamableHttpTransport overrides neither take_tools_changed nor is_alive and consumes text/event-stream only as the framing of a reply to its own request (crates/wcore-mcp/src/transport/streamable_http.rs:197), so it has no channel for a server-initiated notification. McpManager::refresh_signalled_tools (crates/wcore-mcp/src/manager.rs:711) therefore skips these servers unconditionally, and the whole McpCatalogRefresh machinery is a no-op for them.

**Where.** crates/wcore-mcp/src/transport/mod.rs:35, crates/wcore-mcp/src/transport/sse.rs:290-294 and :450, crates/wcore-mcp/src/transport/streamable_http.rs:325-370, consumed at crates/wcore-mcp/src/manager.rs:711

**Why it matters.** It is #1175's own reported symptom, unfixed, for the transports most likely to be used with the feature the ticket calls documented: `/mcp add` with a URL. A hosted MCP server attached mid-session announces a new tool, the announcement is discarded, and the tool stays uncallable for the rest of the session with no warning and no way for the user to tell — the exact wording of the ticket. Nothing in the product says non-stdio servers are excluded, so #1175's acceptance ('honoured, or the product says plainly that it will not be') fails on both disjuncts for two of three transports. Secondary hazard: StreamableHttpTransport inherits is_alive() -> true and its close() cannot make it false, so if take_tools_changed is ever implemented there without also fixing is_alive, the RemoveMcpServer path's missing forget_runtime_server call (crates/wcore-cli/src/main.rs:3726, which never withdraws the entry) turns into a live resurrection bug: an operator-removed server's tools would be re-registered into the live registry on its next list_changed.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.

---
issue: 404
repo: FerroxLabs/wayland-core
kind: defect
title: "A ci-linux cancellation at the 120-minute budget destroys the JUnit evidence the run already produced"
status: open
last_verified_commit: febdf0a1e
criteria:
  - id: c1
    text: "JUnit evidence produced by the test step survives a cancellation of a LATER step in the same job, demonstrated on a real run rather than argued."
    state: not-met
    owner: core
    note: "STAYS NOT-MET at febdf0a1e, and the reason is the second half of its own sentence: `demonstrated on a real run rather than argued`. No run exists. THE CODE HALF IS DONE AND IS A REAL CHANGE, not another always(). ci.yml:2314 adds `Upload nextest JUnit checkpoint (survives a later cancellation)` IMMEDIATELY after `Run tests (nextest CI profile)`, uploading target/nextest/ci/junit.xml plus outer-attempts/ under the distinct artifact name `nextest-junit-linux-containerized-checkpoint`. THE MECHANISM IS THE POSITION, NOT THE CONDITION. The end-of-job upload already carried if:always() when this issue was filed and the evidence was still lost, because between the test step and it sit the swarm gate, the voice suite, both shared-process suites, the corpus drift check, the audit and the signed-manifest drill -- roughly half this job's measured wall -- and a job killed at its budget is not guaranteed to reach a trailing always() step. The checkpoint runs while nothing has been cancelled yet. A DISTINCT NAME rather than overwrite:true, deliberately: overwrite is delete-then-create, and a cancellation inside that window destroys the checkpoint too. The position is GUARDED, not just written: .github/scripts/tests/report-gate-wiring.test.sh now extracts step line numbers scoped to the ci-linux job body and requires the checkpoint to follow the test step and precede the lib suite, the integration suite, the audit and the final upload, with a control proving its comparator can answer NO and a second proving it rejects a missing anchor. FAIL-BEFORE, run locally: physically moving the checkpoint block to the end of the job took that file from `passed: 75 failed: 0` to `passed: 70 failed: 4`, all four reds naming the ordering; restored, 75/0. WHAT IS OWED, precisely, so this is a run and not a judgement: one push of a carrying ref that (1) lets ci-linux reach and complete `Run tests (nextest CI profile)` and its checkpoint upload, then (2) is deliberately cancelled during a later bounded step, and (3) whose `nextest-junit-linux-containerized-checkpoint` artifact is DOWNLOADED and shown to contain the completed test identities, with the `report` job's own log quoted showing the cancelled-with-evidence line. This worker cannot push. Until that exists the artifact's survival across a real cancellation is designed for and unobserved. PREVIOUS: TEXT RESTORED 2026-09-03. All three criteria in this file were truncated on transcription: each began at the colon after its bold `c1`/`c2`/`c3` marker and stopped at the first newline of the issue's wrapped bullet, so c1 read ': JUnit evidence produced by the test step survives a cancellation of a' -- a fragment that cannot be graded either way. A criterion nobody can read is a criterion nobody can fail. Restored verbatim from the issue body (Acceptance section, lines 48-54). The claim itself is UNCHANGED and still not-met: nothing in the tree uploads JUnit before the steps that can consume the wall, so a cancellation still takes the evidence with it."
  - id: c2
    text: "A `ci-linux` job that reaches its budget is distinguishable in the `report` job's own output from one that produced no evidence at all - the two are currently the same observable."
    state: met
    evidence: "file:.github/scripts/assert-test-evidence.sh:173:CANCELLED WITH TEST EVIDENCE"
    owner: core
    note: "MET at febdf0a1e. THE TWO CASES ARE NOW TWO LINES AND TWO ANNOTATIONS. assert-test-evidence.sh's required-leg loop used to print one sentence for a cancelled leg -- `was cancelled - not required to report` -- and `continue` without looking at the directory, so a leg killed at its budget AFTER running the whole workspace suite and a leg dead before its test step were byte-identical in the report job's own output. It now counts the leg's evidence first (across BOTH `<leg>` and `<leg>-checkpoint`, because on a cancelled run the checkpoint is the only artifact that exists) and emits either `::notice title=CANCELLED WITH TEST EVIDENCE` naming the surviving report and test-case counts, or `::warning title=CANCELLED WITH NO TEST EVIDENCE` saying the run knows nothing about the suite. NEITHER IS GRADED AS A FAILURE and the exit code is 0 on both -- a cancelled leg is not a defect, and making it one is how a gate gets switched off -- which is exactly why the arms below assert on OUTPUT and each carries the other's needle as a negative control: a script printing both sentences unconditionally would satisfy the positive halves alone. FAIL-BEFORE / PASS-AFTER, run on this machine (bash only, no toolchain): with .github/scripts/assert-test-evidence.sh restored to its HEAD (2a67a204f) content, `bash .github/scripts/tests/assert-test-evidence.test.sh` reported `passed: 54 failed: 4` -- red on `cancelled leg whose evidence survived SAYS so`, `cancelled leg with no evidence SAYS THAT INSTEAD`, `a zero-test checkpoint is not surviving evidence` and `checkpoint alone satisfies the required leg`. With the fix in place: `passed: 58 failed: 0`, and all 50 pre-existing arms still green in both runs. Seven arms were added in total, including the union arms for c1's mechanism (checkpoint-only satisfies the required leg; neither directory is still RED; end-of-job artifact alone still satisfies it) and a file-is-not-a-test arm (a checkpoint holding nextest's zero-match junit.xml reads as NO evidence). Wiring is asserted separately in report-gate-wiring.test.sh, and lint.yml already runs both files. THE LIMIT: this is proven against constructed artifact directories, which is what a shell gate can be proven against. That a real cancelled ci-linux job actually LEAVES such a directory is c1's claim and c1 is still not-met. PREVIOUS: TEXT RESTORED 2026-09-03, same transcription defect as c1. Still not-met, and this session is direct evidence for why it matters: `report` failed 13 consecutive runs and every diagnosis had to be made by downloading each leg's junit artifact by hand, because the job's own output cannot say whether a missing leg was killed at the wall or never produced anything. `.github/scripts/assert-test-evidence.sh` already carries the HINT text for the second case ('A leg that dies before its test step ... leaves no artifact'), which is exactly the sentence that makes the two indistinguishable when the first case happens."
  - id: c3
    text: "The measured wall time of `ci-linux` against its budget is recorded, so the next raise is a decision rather than a reaction."
    state: met
    evidence: "file:.github/workflows/ci.yml:1418:Median 102.0 min"
    owner: core
    note: "MET 2026-09-03 by measurement recorded IN THE TREE, at the timeout it governs, next to the table it extends. n=9 consecutive runs of this job, 2026-09-02/03: 87.0, 95.5, 97.0, 100.1, 102.0, 105.5, 116.1, 122.8, 123.3 min. Median 102.0 (68% of the 150 limit), max 123.3 (82%), min 87.0 (58%). CORRECTION MADE WHILE MEASURING, and it is the substance rather than a detail: the first pass graded these against 120 minutes because `timeout-minutes: 120` appears earlier in ci.yml -- that value belongs to the matrix job (`CI (Array)`/macOS/Windows), and ci-linux's own is 150 at ci.yml:1258. Grading a budget against the wrong budget is how a 82% reading becomes a 103% panic. THE FINDING IS THAT THE MEDIAN IS THE WRONG STATISTIC: the block's own '~50% over the measured figure' is true of the median and false of the maximum, which sits 18% under the wall -- the same 18% that block calls 'too thin for a job of this length' when it justified moving 90 to 150. The spread is 87.0-123.3, a 36-minute band. NOT RAISED, deliberately: no run in the sample was killed and the median has not moved (99-102 before, 102.0 now), and the block is explicit that widening the gap costs the timeout its ability to catch a hang. The trigger for the next raise is recorded with the table so it is a decision and not a reaction -- one KILLED run, or a median above 115 min (77%), whichever comes first."
---

# Three criteria nobody could read

This ledger's real defect was its own transcription: every criterion was cut at
the first line-wrap of the issue's bullet, leaving fragments that begin with a
colon. A criterion that cannot be read cannot be graded, and cannot be failed --
which is the same failure mode as a gate with no reachable pass state, arrived at
from the other side.

Texts are restored verbatim from the issue. c3 is now met with a measurement
recorded at the timeout it governs; c1 and c2 are unchanged and still owe code.

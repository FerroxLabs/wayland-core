---
issue: 389
repo: FerroxLabs/wayland-core
kind: defect
title: "Windows: a DETACHED_PROCESS quarantine child takes the user's console back with AttachConsole (split from #338 c2)"
status: open
last_verified_commit: 9dc58343
criteria:
  - id: c1
    text: "On Windows, a quarantine child that calls AttachConsole(ATTACH_PARENT_PROCESS) or AttachConsole(<wayland pid>) cannot end up sharing the user's console, measured by GetConsoleProcessList from inside that child"
    state: not-met
    owner: core
    note: "MEASURED FALSE, 2026-08-30, Windows 10.0.26200.9168, by the test this ticket asked for: `[plain] SHARES_USER_CONSOLE_BEFORE=true CONOUT_BEFORE=OPEN`; `[hardened] SHARES_USER_CONSOLE_BEFORE=false CONOUT_BEFORE=DENIED(6)`; `[hardened] ATTACH_PARENT_PROCESS=SUCCEEDED SHARES_USER_CONSOLE_AFTER=true CONOUT_AFTER=OPEN`; `[hardened] ATTACH_BY_EXPLICIT_PID=SUCCEEDED CONOUT_AFTER_EXPLICIT=OPEN SHARES_USER_CONSOLE_AFTER_EXPLICIT=true`; `[hardened] ALLOC_CONSOLE=SUCCEEDED SHARES_USER_CONSOLE_AFTER_ALLOC=false`; `[production_git] SHARES_USER_CONSOLE_BEFORE=false ... SHARES_USER_CONSOLE_AFTER_EXPLICIT=true`. Both spellings reach the operator's console, so reparenting the child is not a remedy either. Not reachable with process-creation flags: `CREATE_NO_WINDOW` (child already has a console, so AttachConsole would fail with ERROR_ACCESS_DENIED) is defeated by `FreeConsole` first, which is the sequence the probe runs. A real fix needs a session, window-station or AppContainer boundary. NOTE the AppContainer bar: that question is CLOSED and must not be reopened. RE-MEASURED 2026-08-30 by lane w3-windows-honesty on this branch, same host (Windows 10.0.26200.9168), so the c2 decision below rests on a measurement of the tree it ships with and not on a quoted older run: `[hardened] ATTACH_PARENT_PROCESS=SUCCEEDED` / `SHARES_USER_CONSOLE_AFTER=true`, `[hardened] ATTACH_BY_EXPLICIT_PID=SUCCEEDED` / `SHARES_USER_CONSOLE_AFTER_EXPLICIT=true`, `[production_git] ATTACH_BY_EXPLICIT_PID=SUCCEEDED` / `SHARES_USER_CONSOLE_AFTER_EXPLICIT=true`, and `[plain] SHARES_USER_CONSOLE_BEFORE=true` as the negative control. UNCHANGED and still not-met."
  - id: c2
    text: "OR, if c1 is judged unreachable with the available Win32 primitives, the other branch of #338's option menu is taken and graded -- a quarantine-originated prompt is LABELLED so the operator can tell it from a Wayland prompt, or the quarantine install refuses to run interactively on Windows -- and the choice is recorded in .planning/DECISIONS.md alongside Q-338c4 with its product cost"
    state: met
    evidence: "test:crates/wcore-cli/tests/quarantine_console_authority_windows.rs::a_quarantine_git_announces_itself_on_the_operators_console"
    owner: core
    note: "MET 2026-08-30, lane w3-windows-honesty. THE BRANCH TAKEN IS LABELLING, and the choice plus its cost is recorded in .planning/DECISIONS.md Q-389c2 alongside Q-338c4 exactly as this criterion asks. c1 is judged unreachable on the measurement in c1's own note, re-run against THIS tree rather than quoted: both AttachConsole spellings still put the child on the operator's console, so neither foreclosed remedy has become available. `build_git_command` -- the one choke point every quarantine `git` spawn passes through, and the point the wiring test already grades -- now writes an attribution notice to stderr before returning the command, and `console_attribution_notice` is a pure function so the WORDING is graded separately from the emission. The test grades both halves: the content (names the tool, quotes the argv, denies authorship, states that wayland-core never asks for a password here) in-process, and the WIRING from a real child of the real builder whose stderr the parent captures, because libtest owns this process's stderr. LIVE ON REAL WINDOWS (10.0.26200.9168, 2026-08-30), the notice observed on the operator's own console in the sibling test's --no-capture output: `wayland-core: plugin quarantine is now running `git --version`. Anything that appears on this console before the next `wayland-core:` line comes from that git or from a credential helper it started -- NOT from wayland-core. wayland-core will never ask for a password here.` SINK CORRECTED 2026-08-30 AFTER AN ADVERSARIAL REFUTATION, and this was a real product gap, not a grading one: the notice was an `eprintln!` -- wayland-core's own stderr -- while `build_git_command` gives git `Stdio::piped()`, so the credential prompt reaches the operator on CONOUT$ instead. Measured by the verifier on real Windows: with stderr redirected to a file the notice was IN THE STDERR FILE and NOT on the console, with a control proving the probe ran. The two sinks coincide only when wayland-core's stderr happens to BE that console, so under the TUI, under the JSON stream protocol, and under any host integration that pipes us, the operator got the prompt with NO notice -- the exact unattributable prompt this criterion exists to prevent, absent in precisely the configuration where the prompt is visible. The wiring test compounded it by observing the notice through a pipe, i.e. in the configuration where an operator cannot. FIXED: `announce_on_every_operator_sink` writes the notice to CONOUT$ as well as stderr (stderr KEPT -- a host reading it is a real operator surface, and on a console-less box it is the only one), and it returns a `NoticeDelivery` STRUCT with one field per sink rather than a list of the sinks that worked, so deleting a sink is a compile error and not a silently-short list. The invariant graded is `whenever a console exists, the notice reaches it`, with console presence measured INDEPENDENTLY by `GetConsoleProcessList` rather than by asking the delivery about itself; the no-console leg asserts the failure is REPORTED rather than skipping, and is honest because a host with no console has no console for a prompt either. Second arm: `the_notice_reaches_the_console_the_prompt_reaches`. PRODUCT COST, stated rather than hidden: this does NOT stop a determined child re-attaching -- c1 remains the harm and remains open -- and it costs one unconditional stderr line per quarantine git spawn on Windows, accepted because a notice that fires only when it guesses a human is watching is absent exactly when that guess is wrong."
  - id: c3
    text: "The doc comment on harden_against_credential_prompt no longer asserts the DETACHED_PROCESS/setsid analogy without qualification"
    state: met
    evidence: "absent:crates/wcore-cli/src/plugin/quarantine.rs::On Windows the analogue is"
    owner: core
    note: "EVIDENCE ANCHOR CORRECTED 2026-08-30: it read `symbol:...::harden_against_credential_prompt`, which resolves whenever the FUNCTION exists and says nothing about its doc comment -- so the one anchor could not distinguish `the analogy sentence is gone` (the criterion) from `the analogy sentence was restored` (its negation). This criterion's whole content is that a sentence is ABSENT, which is what the `absent:` token is for: it re-reads the file every run, so restoring the sentence reds the gate instead of surviving it. CONTROL that the query is not silently empty: `grep -rn \"fn harden_against_credential_prompt\" crates/` returns quarantine.rs:365, while `grep -rn \"On Windows the analogue is\" crates/` returns nothing -- the file is there and the sentence is not. Done 2026-08-30. The sentence `On Windows the analogue is DETACHED_PROCESS, which denies the child the parent\'s console and so CONIN$/CONOUT$ with it` is removed and replaced by the measured table, by `So on Windows this is a REDUCTION ..., not the elimination unix gets`, by the three foreclosed remedies, and by an explicit `Do not restore the analogy sentence: an overstated security guarantee is worse than an understated one, because it stops the next person looking`. The `DETACHED_PROCESS` constant's own comment now says `is CREATED with no console` and points at the test. RED ARM: the two mutations under #380 c1 both redden the test that carries these measurements, so the claim is anchored to something that fails when it stops being true."
  - id: c4
    text: "The residual pin in quarantine_console_authority_windows.rs is inverted rather than deleted, so the closure is asserted rather than narrated"
    state: not-met
    owner: core
    note: "ADDED to this ledger 2026-08-30 by lane w3-windows-honesty -- it is in the issue's acceptance and was missing here, so it was outstanding work nothing could count. NOT MET, and deliberately so rather than by omission: this criterion presupposes c1 closing, and INVERTING the pin now would assert that a quarantine child cannot reach the operator's console, which is measured FALSE on this tree today. What has been done is the half that is available: the pin is KEPT, not deleted, and was re-run on real Windows on this branch (see c1's note); and c2's closure is ASSERTED by its own arm rather than narrated, which is the property this criterion is really after. Whoever closes c1 inverts the pin in the same change. RESTATED SO IT CANNOT BE MET BY INATTENTION: deleting or weakening `quarantine_child_has_no_console_at_creation_on_windows` fails this criterion, it does not satisfy it."

---

Duplicate-by-independent-discovery of #380: both describe the same measured
Windows defect. #380 asked for the MEASUREMENT and for the product to stop
overclaiming; that is delivered and #380's ledger now reads met. THIS ticket
carries the live remainder, because its acceptance criteria ask for the
PROPERTY (c1) or an explicit product decision (c2), and neither has happened.
FerroxLabs/wayland-core#338 c2 is superseded into this issue.

Do not close this on c3 alone. c3 was the honesty fix and c2 is now the
DECISION -- but c1 is the HARM and it is untouched. A quarantine child can
still take the operator's console with one documented call; what changed is
only that the operator is told, on that same console, that what follows is
git's and not wayland-core's. c4 stays open with c1 because inverting the pin
while the bypass is live would assert something measured false.

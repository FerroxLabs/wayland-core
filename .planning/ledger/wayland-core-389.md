---
issue: 389
repo: FerroxLabs/wayland-core
kind: defect
title: "Windows: a DETACHED_PROCESS quarantine child takes the user's console back with AttachConsole (split from #338 c2)"
status: open
last_verified_commit: 4f2ef0ae
criteria:
  - id: c1
    text: "On Windows, a quarantine child that calls AttachConsole(ATTACH_PARENT_PROCESS) or AttachConsole(<wayland pid>) cannot end up sharing the user's console, measured by GetConsoleProcessList from inside that child"
    state: not-met
    owner: core
    note: "MEASURED FALSE, 2026-08-30, Windows 10.0.26200.9168, by the test this ticket asked for: `[plain] SHARES_USER_CONSOLE_BEFORE=true CONOUT_BEFORE=OPEN`; `[hardened] SHARES_USER_CONSOLE_BEFORE=false CONOUT_BEFORE=DENIED(6)`; `[hardened] ATTACH_PARENT_PROCESS=SUCCEEDED SHARES_USER_CONSOLE_AFTER=true CONOUT_AFTER=OPEN`; `[hardened] ATTACH_BY_EXPLICIT_PID=SUCCEEDED CONOUT_AFTER_EXPLICIT=OPEN SHARES_USER_CONSOLE_AFTER_EXPLICIT=true`; `[hardened] ALLOC_CONSOLE=SUCCEEDED SHARES_USER_CONSOLE_AFTER_ALLOC=false`; `[production_git] SHARES_USER_CONSOLE_BEFORE=false ... SHARES_USER_CONSOLE_AFTER_EXPLICIT=true`. Both spellings reach the operator's console, so reparenting the child is not a remedy either. Not reachable with process-creation flags: `CREATE_NO_WINDOW` (child already has a console, so AttachConsole would fail with ERROR_ACCESS_DENIED) is defeated by `FreeConsole` first, which is the sequence the probe runs. A real fix needs a session, window-station or AppContainer boundary. NOTE the AppContainer bar: that question is CLOSED and must not be reopened. RE-MEASURED 2026-08-30 by lane w3-windows-honesty on this branch, same host (Windows 10.0.26200.9168), so the c2 decision below rests on a measurement of the tree it ships with and not on a quoted older run: `[hardened] ATTACH_PARENT_PROCESS=SUCCEEDED` / `SHARES_USER_CONSOLE_AFTER=true`, `[hardened] ATTACH_BY_EXPLICIT_PID=SUCCEEDED` / `SHARES_USER_CONSOLE_AFTER_EXPLICIT=true`, `[production_git] ATTACH_BY_EXPLICIT_PID=SUCCEEDED` / `SHARES_USER_CONSOLE_AFTER_EXPLICIT=true`, and `[plain] SHARES_USER_CONSOLE_BEFORE=true` as the negative control. UNCHANGED and still not-met. || UNCHANGED and still NOT MET -- RE-MEASURED 2026-08-31 by lane/f13-windows on real Windows 10.0.26200.9168, against the tree this branch ships rather than quoted from an earlier run, because c4 turns on this measurement and a verdict inherited from the commit under review is not evidence. `quarantine_child_has_no_console_at_creation_on_windows`, --nocapture: `[plain] SHARES_USER_CONSOLE_BEFORE=true` (the negative control), `[hardened] SHARES_USER_CONSOLE_BEFORE=false CONOUT_BEFORE=DENIED(6)`, then `[hardened] ATTACH_PARENT_PROCESS=SUCCEEDED SHARES_USER_CONSOLE_AFTER=true CONOUT_AFTER=OPEN` and `[hardened] ATTACH_BY_EXPLICIT_PID=SUCCEEDED SHARES_USER_CONSOLE_AFTER_EXPLICIT=true`, with `[hardened] ALLOC_CONSOLE=SUCCEEDED SHARES_USER_CONSOLE_AFTER_ALLOC=false` showing AllocConsole is still not a second bypass. The production postures agree: `[production_git] ATTACH_BY_EXPLICIT_PID=SUCCEEDED SHARES_USER_CONSOLE_AFTER_EXPLICIT=true`. NEW THIS RUN and worth recording: a fourth arm, `[production_spawn]`, drives the probe through `run_hardened` -- the real spawn, with #393's Job Object flags composed in -- and reports `SHARES_USER_CONSOLE_BEFORE=false` with `ATTACH_BY_EXPLICIT_PID=SUCCEEDED SHARES_USER_CONSOLE_AFTER_EXPLICIT=true`. So #393's containment change neither closes this nor widens it: the bypass is exactly what it was, in both spellings, in the posture the product actually spawns. Both foreclosed remedies remain foreclosed. Still not reachable with process-creation flags; a real fix needs a session, window-station or AppContainer boundary, and the AppContainer route is CLOSED by decision and must not be reopened. || RE-MEASURED AGAIN 2026-08-31 by lane/f13-windows on real Windows 10.0.26200.9168 (SeanDesktop) at 4f2ef0ae, because c4 turns on this measurement and a verdict inherited from an earlier run is not evidence for this tree. STILL FALSE, in both spellings, in the posture the product actually spawns: `[plain] SHARES_USER_CONSOLE_BEFORE=true` (negative control alive); `[hardened] SHARES_USER_CONSOLE_BEFORE=false CONOUT_BEFORE=DENIED(6)`; `[hardened] ATTACH_PARENT_PROCESS=SUCCEEDED SHARES_USER_CONSOLE_AFTER=true CONOUT_AFTER=OPEN`; `[hardened] ATTACH_BY_EXPLICIT_PID=SUCCEEDED SHARES_USER_CONSOLE_AFTER_EXPLICIT=true`; `[hardened] ALLOC_CONSOLE=SUCCEEDED SHARES_USER_CONSOLE_AFTER_ALLOC=false`; `[production_spawn] SHARES_USER_CONSOLE_BEFORE=false ... ATTACH_BY_EXPLICIT_PID=SUCCEEDED SHARES_USER_CONSOLE_AFTER_EXPLICIT=true`. NOT AN ARTEFACT OF THE SSH SESSION: `[plain] ATTACH_PARENT_PROCESS=FAILED` while `[plain] SHARES_USER_CONSOLE_BEFORE=true` is the documented ERROR_ACCESS_DENIED of an already-attached process, and the by-pid spelling -- the one that defeats reparenting -- succeeds from every arm regardless. || PREMISE REFUTED, 2026-08-31, lane/f13-s3-wincon, real Windows 10.0.26200.9168 (SeanDesktop). c1 is still NOT MET BY THE PRODUCT -- re-measured on this tree today, `3 tests run: 3 passed`, `[production_spawn] ATTACH_BY_EXPLICIT_PID=SUCCEEDED SHARES_USER_CONSOLE_AFTER_EXPLICIT=true` in the posture the product actually spawns -- but the REASON recorded above it is wrong, and it is the reason c2 and c4 both lean on. Every prior note says a fix needs a session, window-station or AppContainer boundary. MEASURED: it needs none of those. The child's TOKEN INTEGRITY LEVEL closes the property. On the INTERACTIVE window station (WinSta0, Default desktop), a DETACHED_PROCESS child spawned with a LOW-integrity primary token reports ATTACH_PARENT_PROCESS=FAILED and ATTACH_BY_EXPLICIT_PID=FAILED with SHARES_USER_CONSOLE_AFTER=false and SHARES_USER_CONSOLE_AFTER_EXPLICIT=false -- BOTH spellings, which is what c1 asks for -- measured by GetConsoleProcessList from inside that child, which is the oracle c1 names. CONTROLS, because a zero reads exactly like absent: `[plain] SHARES_USER_CONSOLE_BEFORE=true` keeps the negative control alive, and `[detached_medium_il]` is the SAME CreateProcessAsUser call with the integrity level pinned at MEDIUM and STILL attaches in both spellings -- so the block is the integrity level, not the token API. THE LOW ARM IS NOT A DEAD CHILD REPORTING ZEROES: the probe encodes its findings in its EXIT CODE, and the low arm returned exactly 32 -- the CHILD_IS_LOW_IL bit, which only code running AFTER the attach attempts can set -- not 0xC0000142, so the child ran, was confirmed Low, and genuinely failed both attaches. This is NOT AppContainer and does not reopen it: a mandatory integrity label on an ordinary primary token is a different primitive from a capability sandbox. WRONG-REFUSAL CONTROL: a Low-integrity `git clone --depth 1` of a public repo exits 0 and leaves the tree on disk, so the remedy is not a guard that refuses everything. TWO MEASURED COSTS, which are why this lane did NOT land it: (a) the destination must carry a Low mandatory label -- a Low clone into a Medium-labelled directory exits 1 while the Medium control in that same directory succeeds, so the quarantine directory would have to be relabelled by the product; (b) on a SERVICE window station -- where every CI job and every headless or service-hosted run lives -- the Low-integrity cmd+git chain FAILED TO START (STATUS_DLL_INIT_FAILED, 0xC0000142, empty log) while the Medium control in the same directory succeeded, so an unconditional Low spawn is a wrong-refusal exactly where there is no operator console to protect. STATED NARROWLY ON PURPOSE because overstating an impossibility is the failure this ticket is about: a Low-integrity .NET probe DID start on that same service station (it reported CHILD_IS_LOW_IL=true), so (b) is a measured fact about the git chain, not a claim that no Low process runs there, and the exact cause was not isolated. AN EARLIER MEASUREMENT IN THIS LANE SAID THE REMEDY WAS DEAD AND WAS WRONG FOR THAT SECOND REASON: the first liveness run went over ssh, whose session sits on a SERVICE window station (WINSTA=Service-0x4-3cd24b00, Default), where the Low child died 0xC0000142; re-run on WinSta0 through an interactive scheduled task the identical command exits 0. A red from the wrong window station misleads exactly like a green from the wrong host. REPOINT PROPOSED rather than landed: spawn the quarantine child with a Low-integrity primary token WHEN AND ONLY WHEN this process has a console (the only case in which the harm exists), keep today's posture otherwise, and label the quarantine directory Low. That needs CreateProcessAsUser -- std Command cannot set a token -- so it replaces the spawn path #393 just landed containment on, and it is a security-sensitive change with a wrong-refusal surface this lane could not fully measure (the credential-helper arm was attempted and came back DEGENERATE: the Medium control produced 0 lines too, so the instrument was broken and the result is discarded rather than read). Not a change to land in a final blocking-criteria pass without a maintainer decision. Reproducer used for all of the above is a standalone Win32 probe, not the product test, and is NOT in the tree; its arms are named here precisely enough to rebuild."
  - id: c2
    text: "OR, if c1 is judged unreachable with the available Win32 primitives, the other branch of #338's option menu is taken and graded -- a quarantine-originated prompt is LABELLED so the operator can tell it from a Wayland prompt, or the quarantine install refuses to run interactively on Windows -- and the choice is recorded in .planning/DECISIONS.md alongside Q-338c4 with its product cost"
    state: met
    evidence: "test:crates/wcore-cli/tests/quarantine_console_authority_windows.rs::a_quarantine_git_announces_itself_on_the_operators_console"
    owner: core
    note: "MET 2026-08-30, lane w3-windows-honesty. THE BRANCH TAKEN IS LABELLING, and the choice plus its cost is recorded in .planning/DECISIONS.md Q-389c2 alongside Q-338c4 exactly as this criterion asks. c1 is judged unreachable on the measurement in c1's own note, re-run against THIS tree rather than quoted: both AttachConsole spellings still put the child on the operator's console, so neither foreclosed remedy has become available. `build_git_command` -- the one choke point every quarantine `git` spawn passes through, and the point the wiring test already grades -- now writes an attribution notice to stderr before returning the command, and `console_attribution_notice` is a pure function so the WORDING is graded separately from the emission. The test grades both halves: the content (names the tool, quotes the argv, denies authorship, states that wayland-core never asks for a password here) in-process, and the WIRING from a real child of the real builder whose stderr the parent captures, because libtest owns this process's stderr. LIVE ON REAL WINDOWS (10.0.26200.9168, 2026-08-30), the notice observed on the operator's own console in the sibling test's --no-capture output: `wayland-core: plugin quarantine is now running `git --version`. Anything that appears on this console before the next `wayland-core:` line comes from that git or from a credential helper it started -- NOT from wayland-core. wayland-core will never ask for a password here.` SINK CORRECTED 2026-08-30 AFTER AN ADVERSARIAL REFUTATION, and this was a real product gap, not a grading one: the notice was an `eprintln!` -- wayland-core's own stderr -- while `build_git_command` gives git `Stdio::piped()`, so the credential prompt reaches the operator on CONOUT$ instead. Measured by the verifier on real Windows: with stderr redirected to a file the notice was IN THE STDERR FILE and NOT on the console, with a control proving the probe ran. The two sinks coincide only when wayland-core's stderr happens to BE that console, so under the TUI, under the JSON stream protocol, and under any host integration that pipes us, the operator got the prompt with NO notice -- the exact unattributable prompt this criterion exists to prevent, absent in precisely the configuration where the prompt is visible. The wiring test compounded it by observing the notice through a pipe, i.e. in the configuration where an operator cannot. FIXED: `announce_on_every_operator_sink` writes the notice to CONOUT$ as well as stderr (stderr KEPT -- a host reading it is a real operator surface, and on a console-less box it is the only one), and it returns a `NoticeDelivery` STRUCT with one field per sink rather than a list of the sinks that worked, so deleting a sink is a compile error and not a silently-short list. The invariant graded is `whenever a console exists, the notice reaches it`, with console presence measured INDEPENDENTLY by `GetConsoleProcessList` rather than by asking the delivery about itself; the no-console leg asserts the failure is REPORTED rather than skipping, and is honest because a host with no console has no console for a prompt either. Second arm: `the_notice_reaches_the_console_the_prompt_reaches`. TWO THINGS THAT ARM DOES NOT CATCH, named rather than left for the next verifier to find: (a) FALSIFICATION -- a delivery that reported `operator_console: Ok(())` without writing would pass, because the only oracle available in-process is whether a console EXISTS, not whether bytes reached it; reading the console buffer back with `ReadConsoleOutputCharacterW` would be a real oracle and was not built, because under the ConPTY these runs actually use it is not dependable and a flaky guard is worse than a stated gap. What IS structurally closed is deletion: `NoticeDelivery` has one field per sink, so removing the sink removes the field and fails to build. (b) WIRING BACK OUT -- if a later edit returned `build_git_command` to a bare `eprintln!`, `announce_on_every_operator_sink` would still exist and this arm would still pass; the sibling wiring arm observes the notice on a PIPE, which both implementations satisfy. That is the graded-the-function-not-the-wiring shape, it is not closed here, and it stays on this issue rather than being written off. PRODUCT COST, stated rather than hidden: this does NOT stop a determined child re-attaching -- c1 remains the harm and remains open -- and it costs one unconditional stderr line per quarantine git spawn on Windows, accepted because a notice that fires only when it guesses a human is watching is absent exactly when that guess is wrong. || RE-VERIFIED 2026-08-31 on real Windows 10.0.26200.9168 (SeanDesktop): `3 tests run: 3 passed` at --retries 0, the notice observed on the operator's own console in --nocapture. RED ARM by this lane on the PRODUCTION choke point rather than the wording helper: `announce_on_every_operator_sink(&console_attribution_notice(args));` in `build_git_command` replaced by `let _ = console_attribution_notice(args);` -- so the notice is still COMPUTED and never EMITTED, which is the failure a helper-level assertion cannot see. MUTATION_SITES=1, CHECK_EXIT=0, `3 tests run: 2 passed, 1 failed` (a_quarantine_git_announces_itself_on_the_operators_console). Restored blob 300f721d2f429c80057d63962b2380c543522d7b == HEAD blob, tree clean."
  - id: c3
    text: "The doc comment on harden_against_credential_prompt no longer asserts the DETACHED_PROCESS/setsid analogy without qualification"
    state: met
    evidence: "absent:crates/wcore-cli/src/plugin/quarantine.rs::On Windows the analogue is"
    owner: core
    note: "EVIDENCE ANCHOR CORRECTED 2026-08-30: it read `symbol:...::harden_against_credential_prompt`, which resolves whenever the FUNCTION exists and says nothing about its doc comment -- so the one anchor could not distinguish `the analogy sentence is gone` (the criterion) from `the analogy sentence was restored` (its negation). This criterion's whole content is that a sentence is ABSENT, which is what the `absent:` token is for: it re-reads the file every run, so restoring the sentence reds the gate instead of surviving it. CONTROL that the query is not silently empty: `grep -rn \"fn harden_against_credential_prompt\" crates/` returns quarantine.rs:365, while `grep -rn \"On Windows the analogue is\" crates/` returns nothing -- the file is there and the sentence is not. Done 2026-08-30. The sentence `On Windows the analogue is DETACHED_PROCESS, which denies the child the parent\'s console and so CONIN$/CONOUT$ with it` is removed and replaced by the measured table, by `So on Windows this is a REDUCTION ..., not the elimination unix gets`, by the three foreclosed remedies, and by an explicit `Do not restore the analogy sentence: an overstated security guarantee is worse than an understated one, because it stops the next person looking`. The `DETACHED_PROCESS` constant's own comment now says `is CREATED with no console` and points at the test. RED ARM: the two mutations under #380 c1 both redden the test that carries these measurements, so the claim is anchored to something that fails when it stops being true."
  - id: c4
    text: "The residual pin in quarantine_console_authority_windows.rs is inverted rather than deleted, so the closure is asserted rather than narrated"
    state: not-met
    owner: core

    note: "ADDED to this ledger 2026-08-30 by lane w3-windows-honesty -- it is in the issue's acceptance and was missing here, so it was outstanding work nothing could count. NOT MET, and deliberately so rather than by omission: this criterion presupposes c1 closing, and INVERTING the pin now would assert that a quarantine child cannot reach the operator's console, which is measured FALSE on this tree today. What has been done is the half that is available: the pin is KEPT, not deleted, and was re-run on real Windows on this branch (see c1's note); and c2's closure is ASSERTED by its own arm rather than narrated, which is the property this criterion is really after. Whoever closes c1 inverts the pin in the same change. RESTATED SO IT CANNOT BE MET BY INATTENTION: deleting or weakening `quarantine_child_has_no_console_at_creation_on_windows` fails this criterion, it does not satisfy it. || UNCHANGED and still NOT MET, deliberately, re-confirmed 2026-08-31 by lane/f13-windows. This criterion presupposes c1 closing: inverting the pin now would assert that a quarantine child cannot reach the operator's console, and c1's note above records that measured FALSE on THIS tree on real Windows 10.0.26200.9168 today, not in an older run. The pin `quarantine_child_has_no_console_at_creation_on_windows` is KEPT and was RUN (3 tests, 3 passed at --retries 0), and it now carries one more arm than it did -- `[production_spawn]`, added for #393 c3 -- so the residual it measures is broader, not narrower. RESTATED SO IT CANNOT BE MET BY INATTENTION: deleting or weakening that test fails this criterion, it does not satisfy it. Whoever closes c1 inverts the pin in the same change. || UNCHANGED and still NOT MET, deliberately, re-confirmed 2026-08-31 at 4f2ef0ae by lane/f13-windows against its OWN re-measurement of c1 above rather than a quoted one. Inverting the pin now would assert that a quarantine child cannot reach the operator's console, which this lane measured false today. The pin `quarantine_child_has_no_console_at_creation_on_windows` is KEPT and was RUN (3 tests, 3 passed). Deleting or weakening it fails this criterion; it does not satisfy it. || UNCHANGED and still NOT MET, deliberately, re-confirmed 2026-08-31 by lane/f13-s3-wincon against its OWN run of the pin on real Windows 10.0.26200.9168 (`3 tests run: 3 passed`, TEST_RC=0) rather than a quoted one. c4 asks for the pin to be INVERTED so the CLOSURE is asserted; there is no closure to assert, because the product still spawns the quarantine child with a Medium token and the bypass is live in the `[production_spawn]` posture on this tree today. Inverting now would assert something measured false. NOTE THE CHANGE IN WHY THIS IS BLOCKED, because it is no longer `nobody knows how`: c1's note now records a MEASURED remedy (a Low-integrity primary token, both spellings blocked, with a Medium-token control and a git-still-clones liveness control). What blocks c4 is that the remedy has two measured costs and rewrites the spawn path #393 just landed, so it is a maintainer decision, not that the property is unreachable. c4 unblocks in the same change that lands c1; deleting or weakening `quarantine_child_has_no_console_at_creation_on_windows` still fails this criterion rather than satisfying it."
---

Duplicate-by-independent-discovery of #380: both describe the same measured
Windows defect. #380 asked for the MEASUREMENT and for the product to stop
overclaiming; that is delivered and #380's ledger now reads met. THIS ticket
carries the live remainder, because its acceptance criteria ask for the
PROPERTY (c1) or an explicit product decision (c2), and neither has happened.
FerroxLabs/wayland-core#338 c2 is superseded into this issue.

Do not close this on c3 alone. c3 was the honesty fix and c2 is now the
DECISION -- but c1 is the HARM and it is untouched. A quarantine child can
still take the operator's console with one documented call; what changed is
only that the operator is told, on that same console, that what follows is
git's and not wayland-core's. c4 stays open with c1 because inverting the pin
while the bypass is live would assert something measured false.

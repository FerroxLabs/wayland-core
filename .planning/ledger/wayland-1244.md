---
issue: 1244
repo: FerroxLabs/wayland
kind: defect
title: "The live /model-switch leg of the spend-audit keying proof needs a PTY-driven TUI run"
status: open
last_verified_commit: 6f7eea1b0
criteria:
  - id: c1
    text: "A PTY-driven run of the shipped `wayland-core` TUI performs an in-session model switch that reaches `rebind_provider`, and the `~/.wayland/budget/spend-audit.jsonl` records written before and after the switch share one `session_id`; the jsonl lines are quoted"
    state: met
    evidence: "test:crates/wcore-cli/tests/issue_1244_spend_audit_keying_tui_pty.rs::the_spend_audit_key_survives_an_in_session_model_switch"
    owner: core
    note: "MET at 6f7eea1b0, on hetzner, `cargo test -p wcore-cli --test issue_1244_spend_audit_keying_tui_pty -- --nocapture --test-threads=1`, receipt remote_exit 0 / complete true at source 6df936291 (the commit these lines were quoted from; 6f7eea1b0 changes only a doc comment). THE RUN: the shipped `wayland-core` binary on a real pseudo-terminal (portable_pty, the harness every other PTY smoke in wcore-cli uses), a local MockLlm speaking the Anthropic SSE wire format as the provider, so no network is reached and NO CREDENTIAL IS NEEDED -- the api key in the seeded config is not a credential. Keystrokes only: `first prompt`, then `/model claude-3-5-haiku-20241022`, then `second prompt`. THE LINES, verbatim: line 1 `{kind: task_spend_audit, payload:{ baseline_model: anthropic/claude-sonnet-4-20250514, dispatches:[{model: claude-sonnet-4-20250514, provider: anthropic, tokens_in:10, tokens_out:20}], session_id: 70f0fcaf872e, task_id: task-1edbbe8a…(elided: `task-` + a uuid trips the repo secret ratchet), started_unix_ms:1789024947458, ended_unix_ms:1789024953405 }}` and line 2 `{kind: task_spend_audit, payload:{ dispatches:[{model: claude-3-5-haiku-20241022, provider: anthropic, tokens_in:10, tokens_out:20}], session_id: 70f0fcaf872e, task_id: task-c25f625f…, started_unix_ms:1789024953405, ended_unix_ms:1789024953970 }}` -- reproduced here with the JSON quoting flattened so this ledger stays parseable; the run prints them raw and the test prints them on the PASS as well as the failure, so they are quotable evidence rather than an assertion that something was checked. SAME session_id, different task, and THE DISPATCH MODELS DIFFER -- which is how the run proves rebind_provider was actually reached rather than assuming it. The switch is asserted twice, once from the mock server's recorded request bodies and once from the audit records' own dispatches[].model, so a `/model` that silently did nothing has to defeat both instruments. THE FIRST VERSION OF THIS TEST PASSED VACUOUSLY and the correction is recorded so nobody re-introduces it: the sink writes an ENVELOPE, {kind, payload}, and a reader that looked for a top-level session_id got None on every record, substituted the string `<missing>`, and `all ids are equal` held over a constant. The reader now PANICS on an absent key, and c1 additionally refuses a key that is empty or `session-unknown` before comparing. WHAT IS NOT CLAIMED: the file is `#![cfg(unix)]` -- ConPTY does not surface the child's stdout on a headless Windows runner -- so the Windows and macOS terminal legs of this criterion are NOT measured."
  - id: c2
    text: "The same run's `session_id` equals the session id under `[session] directory`, not a uuid and not `session-unknown`; both are quoted side by side"
    state: met
    evidence: "test:crates/wcore-cli/tests/issue_1244_spend_audit_keying_tui_pty.rs::the_audit_key_is_the_session_id_and_not_a_placeholder"
    owner: core
    note: "MET at 6f7eea1b0, same instrument and same receipt shape as c1. QUOTED SIDE BY SIDE, printed by the run itself: `spend-audit.jsonl session_id : ecbc952d4ec4` and `[session] directory entries  : [2026-09-10_ecbc952d4ec4, ecbc952d4ec4, ecbc952d4ec4.journal, ecbc952d4ec4.journal, ecbc952d4ec4.journal.writer, index]`. The audit key is a member of that set. Both negative halves are asserted and neither is decorative: a run keyed `session-unknown` throughout would satisfy c1 perfectly while naming no session, and a run keyed by a bare uuid is the pre-#1203 placeholder shape. The uuid test is STRUCTURAL (8-4-4-4-12 hex groups), not a match on one id's spelling. WHAT IS NOT CLAIMED, and it is a real limit rather than a caveat: this is graded on a DURABLE-SESSION host, the default posture. On a host with `[session] enabled = false` the SHIPPED build keys the audit by a uuid -- measured, 47976faa-2321-4f65-9e19-9ed03c693992 -- so c2's `not a uuid` property is FALSE there. See c3 for where that uuid comes from; it is not install_spend_guard's placeholder."
  - id: c3
    text: "A red arm is shown: the same PTY run against a build whose `install_spend_guard` call sites take `uuid::Uuid::new_v4()` produces two different keys, and the differing lines are quoted"
    state: not-met
    owner: core
    note: "NOT MET, and not for want of building it. THE RED ARM WAS CONSTRUCTED AND IT REFUTED THIS CRITERION'S OWN PREMISE. Scratch commits, since reset off this branch, restored the literal pre-#1203 code at BOTH install_spend_guard constructor call sites -- `&uuid::Uuid::new_v4().to_string()` in place of UNBOUND_BUDGET_SESSION_ID and of `&session.id` -- and the same PTY run was driven against that build on both postures. IT DOES NOT PRODUCE TWO DIFFERENT KEYS. Durable host, scratch 117fd6322: both lines carry session_id 4043e8c23632, ONE key, dispatch models claude-sonnet-4-20250514 then claude-3-5-haiku-20241022 so the switch really happened. No journal, `[session] enabled = false`, scratch 6d73f5b13: both lines carry session_id b4f97e92-628c-4d36-b9a3-416f717ffe43 -- a uuid does reach the file there, but it is the SAME uuid on both sides of the switch. WHY, measured rather than reasoned: the SHIPPED build on that same journal-less posture ALSO keys by a uuid (47976faa-2321-4f65-9e19-9ed03c693992), so the uuid is not coming from install_spend_guard at all. It comes from the durable budget AUTHORITY, which bootstrap installs on every session and whose id wins the first arm of budget_session_id(), ahead of both the budget_session_id field and current_session_id(). sync_spend_guard_session then overwrites the constructor's placeholder -- uuid or UNBOUND_BUDGET_SESSION_ID alike -- before any record is written, because every record is written at TASK END. #1203's own doc comment states this; these runs confirm it on the real binary rather than on the comment. SO THE HONEST POSITION IS: the placeholder swap #1203 made is real and correct, and on the TUI paths reachable here it is not OBSERVABLE in spend-audit.jsonl in either direction. #1203's account of the historical split -- rebind_provider swapping the placeholder for the real id mid-session -- needs a record written before sync_spend_guard_session has ever run, and I could not construct that state from the TUI. It may only be reachable on the RESUME path across two launches, which is a different experiment from the in-session switch c1 names and which this lane did not build. WHAT IS OWED: either a construction that genuinely splits the key -- and then this row closes with its quoted differing lines -- or a restatement of c3 on the ISSUE to ask for the property that is actually testable. Per this ledger's own preamble, sharpening a criterion inside the ledger is how a criterion quietly becomes an easier adjacent property, so it is NOT restated here. WHAT IS IN THE TREE MEANWHILE: `the_key_survives_a_switch_on_a_host_with_no_durable_journal`, the arm that carried this refutation. It is kept, and its doc comment says plainly that it does NOT discriminate against the uuid mutation -- it is a property guard against a future change that made rebind_provider key from a different source than the constructor, and nothing more."
---

Created 2026-08-31 to close a COVERAGE gap. It records no work as done.

`scripts/check-criteria-ledger.py` scopes every open `area:core` issue on
wayland and EVERY open issue on wayland-core. This issue was in scope from
the moment it was filed and had no ledger file, so
`scripts/check-release-readiness.py` -- which reads ledger files and nothing
else -- could not count it. CI runs the coverage gate with `--offline`, the
arm that would have reported the gap, so nothing said so for two days.

Criteria are transcribed from the issue body without edit. Where the body's
wording is loose it is LEFT loose rather than tightened here: sharpening a
criterion inside the ledger is how a criterion quietly becomes an easier
adjacent property. Whoever takes this restates it on the ISSUE first.

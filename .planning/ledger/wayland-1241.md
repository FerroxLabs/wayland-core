---
issue: 1241
repo: FerroxLabs/wayland
kind: defect
title: "The Write tool converts a refusal it could not roll back into an unchecked republish and reports success"
status: open
last_verified_commit: a268075fe
criteria:
  - id: c1
    text: "The direct Write path distinguishes \"atomic_write_checked never reached a verdict\" from \"the verdict REFUSED and the rollback could not be made\", and does not take the unchecked std::fs::write fallback in the second case"
    state: met
    owner: core
    evidence: "test:crates/wcore-tools/src/write.rs::a_refusal_that_could_not_be_rolled_back_is_not_a_fallback"
    note: "Told apart by TYPE, as the ticket's own note asks, not by sniffing the message. `wcore-config` gained `RollbackFailed` (a real `std::error::Error` carrying `why()` and `preserved_at()`) which `atomic_write_checked` wraps in `io::Error::other`, plus `wcore_config::rollback_failure(&e)` to recover it. write.rs's direct path routes its `Err` arm through `unpublished_or_unrolled`, which answers `Ok(error)` for 'never reached a verdict' (fallback still correct) and `Err(ToolResult)` for 'refused and could not roll back' (fallback must NOT run). Fail direction matters and is stated in the doc: text-sniffing fails OPEN -- an unrecognised string reads as 'never reached a verdict', which is the answer that republishes unchecked. `RollbackFailed`'s Display is byte-identical to the old format string, so #1202's test, which reads the preserved path back out of that text, is untouched. Red arm exit codes captured DIRECTLY (`cmd > file 2>&1; $?`), never through a pipe; each mutation printed the enclosing function body before and after so it is proven to land on executable code, and each site was restored with `git checkout --` plus a `touch` so cargo could not serve the mutated binary. Log: /root/w-f13/s2-red2.log."
  - id: c2
    text: "When the guard refused and could not roll back, the Write tool returns is_error: true and its text carries the path where the pre-image was preserved"
    state: met
    owner: core
    evidence: "test:crates/wcore-tools/src/write.rs::a_refusal_that_could_not_be_rolled_back_is_not_a_fallback"
    note: "`unsaved_work::refused_but_not_rolled_back` is the wording, and it is the only message in that module that does not claim the destination is as it was -- because it is not: the new bytes are published and the pre-image survives only under the named path. The test asserts `is_error` is true, that the text does NOT start with 'Updated ', that it contains the path, and that the path really holds the original bytes (found by scanning the directory, then string-matched against the report). Deliberately no cache update and no `note_written` on this path: the bytes on disk are not the tool's to claim authorship of, and a stale cache entry fails CLOSED at the next edit. Red arm exit codes captured DIRECTLY (`cmd > file 2>&1; $?`), never through a pipe; each mutation printed the enclosing function body before and after so it is proven to land on executable code, and each site was restored with `git checkout --` plus a `touch` so cargo could not serve the mutated binary. Log: /root/w-f13/s2-red2.log."
  - id: c3
    text: "A test drives the direct (no-ToolContext) Write path through a refused publish whose rollback exchanged nothing and asserts c2's observable; shown RED against today's `Err(e) => Some(e)` fallback"
    state: met
    owner: core
    evidence: "test:crates/wcore-tools/src/write.rs::a_refusal_that_could_not_be_rolled_back_is_not_a_fallback"
    note: "The error is not hand-built: it comes out of a REAL `atomic_write_checked` run in the state the ticket names -- the destination name unlinked from inside the check closure, which is the one ordering that puts it between the two exchanges -- with a fixture control asserting the check was handed the pre-image, and is then given to the very function write.rs's `Err` arm calls. RED under M3, which restored today's `Err(e) => Some(e)`: exit 101, `a refusal that could not be rolled back was sent to the fallback`. RESIDUAL, stated plainly: the test does NOT enter through `WriteTool::execute`. It cannot. Reaching that state end-to-end needs the destination NAME to disappear between the two exchanges, and the only tool-side code in that window is `fs::read(displaced)` plus the pure `pre_image_matches` -- there is no seam and no blocking point, so the ticket's suggestion that this 'needs no new primitive, only a fixture on the direct Write path' does not hold. Two constructions were built and refuted before settling here: a FIFO destination (which gives a real barrier, since the verdict read blocks on opening it) is rejected by `validate_user_path`'s NonRegularFile guard before the tool ever publishes -- measured, `Refused to write ...: path is not a regular file`; and an inotify-driven racer is a flake generator. What IS graded end-to-end through `WriteTool::execute` is the other branch of the same arm (see c4), so the arm executes in a test and calls this function; what is not graded is that arm carrying a `RollbackFailed` input all the way from `execute`. Red arm exit codes captured DIRECTLY (`cmd > file 2>&1; $?`), never through a pipe; each mutation printed the enclosing function body before and after so it is proven to land on executable code, and each site was restored with `git checkout --` plus a `touch` so cargo could not serve the mutated binary. Log: /root/w-f13/s2-red2.log."
  - id: c4
    text: "A genuine round-trip failure that never reached a verdict (a directory that will not hold a sibling temp file) still takes the fallback, still publishes, and still reports success -- with a test that fails if c1's new branch swallows it"
    state: met
    owner: core
    evidence: "test:crates/wcore-tools/src/write.rs::a_round_trip_that_never_reached_a_verdict_still_publishes_unchecked"
    note: "Two tests, one end-to-end and one on the classifier, and BOTH redden under the overcorrection. END-TO-END: `a_round_trip_that_never_reached_a_verdict_still_publishes_unchecked` drives the real `WriteTool::execute` against the trigger the fallback's own comment names -- a directory that will not hold a sibling temp file -- and asserts `!is_error`, that the report says 'used direct write', and that the bytes are on disk. The fixture is found by MEASUREMENT, not arithmetic: nest until the destination path is as long as the platform accepts, then take the first length where `NamedTempFile::new_in` fails and `fs::write` to a child succeeds, restated as an assertion at the moment of use. A permission-based fixture (mode 0555) was rejected: every run of this suite on the build host is root, where it is no obstacle, and the test would pass vacuously there. Unix-only, because the search is over PATH_MAX and Windows' equivalent is lifted by `long_path_safe_dest` on the very path under test -- named gap, not silence. CLASSIFIER: `a_round_trip_that_never_reached_a_verdict_is_handed_to_the_fallback` (plus `an_error_that_never_reached_a_verdict_is_not_a_rollback_failure` in wcore-config) runs everywhere, root included. RED under M4, which made `unpublished_or_unrolled` answer `Err` for every error: exit 101, `a genuine round-trip failure was swallowed as a refusal`, while M3's target stayed green. Red arm exit codes captured DIRECTLY (`cmd > file 2>&1; $?`), never through a pipe; each mutation printed the enclosing function body before and after so it is proven to land on executable code, and each site was restored with `git checkout --` plus a `touch` so cargo could not serve the mutated binary. Log: /root/w-f13/s2-red2.log."
---

`WriteTool`'s direct, no-`ToolContext` path (crates/wcore-tools/src/write.rs:452) treats every `Err` out of `atomic_write_checked` as "the tempfile round trip failed before any verdict" and falls into an unchecked `std::fs::write`, reporting success. Since wayland#1202 that error ALSO means "the guard ran, REFUSED, and the rollback exchanged nothing", in which case the error text is the only record of where the pre-image was preserved. The direct path discards it, so the tool reports success for a write its own guard refused and leaves the user's original under a `.tmpXXXXXX` sibling nothing will clean up.

Not new data loss -- in that state the new bytes are already published, so the fallback rewrites identical bytes. What is lost is the refusal signal and the pre-image's whereabouts.

Pre-existing in shape, widened in reach: before wayland#1202 the `keep_displaced` branch already returned `Err` when the restore exchange genuinely errored (EIO, EXDEV). wayland#1202 widens the trigger to the reachable `Swap::Vacant` / `Swap::Unsupported` case.

Distinct from wayland#1239, whose subject is a rollback that DID exchange and the concurrent save `discard_displaced` then unlinks. Criteria are taken verbatim from the issue's Acceptance section.

---
issue: 1241
repo: FerroxLabs/wayland
kind: defect
title: "The Write tool converts a refusal it could not roll back into an unchecked republish and reports success"
status: open
last_verified_commit: 8ace76d61
criteria:
  - id: c1
    text: "The direct Write path distinguishes \"atomic_write_checked never reached a verdict\" from \"the verdict REFUSED and the rollback could not be made\", and does not take the unchecked std::fs::write fallback in the second case"
    state: met
    owner: core
    evidence: "test:crates/wcore-tools/src/write.rs::execute_reports_a_refusal_it_could_not_roll_back"
    note: "Told apart by TYPE, as the ticket's own note asks, not by sniffing the message. `wcore-config` gained `RollbackFailed` (a real `std::error::Error` carrying `why()` and `preserved_at()`) which `atomic_write_checked` wraps in `io::Error::other`, plus `wcore_config::rollback_failure(&e)` to recover it. write.rs's direct path routes its `Err` arm through `unpublished_or_unrolled`, which answers `Ok(error)` for 'never reached a verdict' (fallback still correct) and `Err(ToolResult)` for 'refused and could not roll back' (fallback must NOT run). Fail direction matters and is stated in the doc: text-sniffing fails OPEN -- an unrecognised string reads as 'never reached a verdict', which is the answer that republishes unchecked. `RollbackFailed`'s Display is byte-identical to the old format string, so #1202's test, which reads the preserved path back out of that text, is untouched. EVIDENCE MOVED to the end-to-end test on the verifier's F1: this criterion's second half is 'does not take the unchecked std::fs::write fallback', which is a property of THE PATH, and the classifier tests cannot see it -- under M3 (below) `a_refusal_that_could_not_be_rolled_back_is_not_a_fallback` stayed GREEN while the tool published unchecked and reported success. The classifier half is still graded, by that test and by `an_error_that_never_reached_a_verdict_is_not_a_rollback_failure` in wcore-config; the end-to-end test is what makes the whole sentence falsifiable. Red arm exit codes captured DIRECTLY (`cmd > file 2>&1; $?`), never through a pipe; each mutation was confirmed to COMPILE (`cargo check -p wcore-tools --tests`, exit 0) before any red was believed, and each site was restored with `git checkout --` plus a `touch`, with the restore verified by blob identity (`git hash-object` == `git rev-parse HEAD:<path>`). Logs: /root/scratch-f13-w2/."
  - id: c2
    text: "When the guard refused and could not roll back, the Write tool returns is_error: true and its text carries the path where the pre-image was preserved"
    state: met
    owner: core
    evidence: "test:crates/wcore-tools/src/write.rs::execute_reports_a_refusal_it_could_not_roll_back"
    note: "GRADED ON THE TOOL, which is what the criterion says. `execute_reports_a_refusal_it_could_not_roll_back` drives the real `WriteTool::execute` (direct, no `ToolContext`) through a real `atomic_write_checked` publish whose rollback exchanged nothing, and every assertion is on the `ToolResult` execute returns: `is_error` is true, the text does NOT start with 'Updated ', it does NOT contain 'used direct write', it names a path, and that path really holds the pre-image bytes (found by scanning the directory, then string-matched against the report). CORRECTED after the F1 refutation: this criterion was previously graded on `unpublished_or_unrolled` alone, which is a private classifier and not 'the Write tool', i.e. the easier adjacent property the Acceptance preamble forbids. The verifier was right; the classifier test is retained as corroboration on c3's fixture-control half but it is no longer the evidence for a sentence about the tool. `unsaved_work::refused_but_not_rolled_back` is the wording, and it is the only message in that module that does not claim the destination is as it was -- because it is not: the new bytes are published and the pre-image survives only under the named path. Deliberately no cache update and no `note_written` on this path: the bytes on disk are not the tool's to claim authorship of, and a stale cache entry fails CLOSED at the next edit. The window has exactly one entrance and it is now a named one. Reaching `RollbackFailed` end-to-end needs the destination NAME to stop resolving strictly BETWEEN the publish exchange and the restore exchange; the only tool-side code in that window is the pure `pre_image_matches`, and `Swap::Unsupported` cannot differ between two exchanges on one filesystem, so nothing the tool does can produce it and a racer on a microsecond window is a flake generator. `WriteTool::publish_window_probe` is therefore a `#[cfg(test)]`-set field whose closure is consulted from inside the verdict, which is inside the window by construction. It is `None` in production and the call site is the SAME code either way. What it substitutes is the REASON the publish is refused -- nothing else: the exchange, the failed restore, `keep_displaced`, `RollbackFailed`, `unpublished_or_unrolled`, `refused_but_not_rolled_back` and the `ToolResult` are all production code, and the observable the criteria grade is what `execute` hands back afterwards. RED under M3, which restored today's `Err(e) => Some(e)`: exit 100 (nextest), the panic being the defect's own sentence -- `reported as a success: Updated /tmp/.../f.txt (atomic write failed: its contents changed on disk, and the original could not be put back (the destination name no longer exists, so nothing was exchanged back); it is preserved at /tmp/.../.tmpTTA57J, used direct write)`. The other three tests in the same selection stayed green, so this one discriminates. Red arm exit codes captured DIRECTLY (`cmd > file 2>&1; $?`), never through a pipe; each mutation was confirmed to COMPILE (`cargo check -p wcore-tools --tests`, exit 0) before any red was believed, and each site was restored with `git checkout --` plus a `touch`, with the restore verified by blob identity (`git hash-object` == `git rev-parse HEAD:<path>`). Logs: /root/scratch-f13-w2/."
  - id: c3
    text: "A test drives the direct (no-ToolContext) Write path through a refused publish whose rollback exchanged nothing and asserts c2's observable; shown RED against today's `Err(e) => Some(e)` fallback"
    state: met
    owner: core
    evidence: "test:crates/wcore-tools/src/write.rs::execute_reports_a_refusal_it_could_not_roll_back"
    note: "The test drives `WriteTool::execute` and the error is not hand-built: it comes out of a REAL `atomic_write_checked` run in the state the ticket names. The window has exactly one entrance and it is now a named one. Reaching `RollbackFailed` end-to-end needs the destination NAME to stop resolving strictly BETWEEN the publish exchange and the restore exchange; the only tool-side code in that window is the pure `pre_image_matches`, and `Swap::Unsupported` cannot differ between two exchanges on one filesystem, so nothing the tool does can produce it and a racer on a microsecond window is a flake generator. `WriteTool::publish_window_probe` is therefore a `#[cfg(test)]`-set field whose closure is consulted from inside the verdict, which is inside the window by construction. It is `None` in production and the call site is the SAME code either way. What it substitutes is the REASON the publish is refused -- nothing else: the exchange, the failed restore, `keep_displaced`, `RollbackFailed`, `unpublished_or_unrolled`, `refused_but_not_rolled_back` and the `ToolResult` are all production code, and the observable the criteria grade is what `execute` hands back afterwards. The window is entered by the production closure, and the test asserts it was entered EXACTLY ONCE and with the displaced pre-image in hand -- without that control every other assertion would also pass on a refusal taken before any publish, which is a different arm. That control is shown live: deleting the two-line probe consult from the production closure (M-seam, `cargo check` exit 0) reddens the test at the control with 'the exchange to verdict window was entered 0 times, not once', not at the observable. The new content is a SUPERSET of the pre-image on purpose -- a rewrite that drops a line is refused by the unsaved-work guard before any publish even outside a repository (`unsaved_work_no_git_test`), and the test would then grade nothing. SUPERSEDES the earlier RESIDUAL on this criterion, which said the test could not enter through `WriteTool::execute` and left the criterion `met` with a named gap -- a third state the standard does not admit. The impossibility claim was half right and is restated above as the reason the seam exists rather than as a licence not to grade: unreachable WITHOUT a seam is not the same as ungradeable. Two constructions were built and refuted before the seam: a FIFO destination is rejected by `validate_user_path`'s NonRegularFile guard before the tool ever publishes -- measured, `Refused to write ...: path is not a regular file` -- and an inotify-driven racer is a flake generator. RED under M3, which restored today's `Err(e) => Some(e)`: exit 100 (nextest), panic quoted in c2's note. Red arm exit codes captured DIRECTLY (`cmd > file 2>&1; $?`), never through a pipe; each mutation was confirmed to COMPILE (`cargo check -p wcore-tools --tests`, exit 0) before any red was believed, and each site was restored with `git checkout --` plus a `touch`, with the restore verified by blob identity (`git hash-object` == `git rev-parse HEAD:<path>`). Logs: /root/scratch-f13-w2/."
  - id: c4
    text: "A genuine round-trip failure that never reached a verdict (a directory that will not hold a sibling temp file) still takes the fallback, still publishes, and still reports success -- with a test that fails if c1's new branch swallows it"
    state: met
    owner: core
    evidence: "test:crates/wcore-tools/src/write.rs::a_round_trip_that_never_reached_a_verdict_still_publishes_unchecked"
    note: "Two tests, one end-to-end and one on the classifier, and BOTH redden under the overcorrection. END-TO-END: `a_round_trip_that_never_reached_a_verdict_still_publishes_unchecked` drives the real `WriteTool::execute` against the trigger the fallback's own comment names -- a directory that will not hold a sibling temp file -- and asserts `!is_error`, that the report says 'used direct write', and that the bytes are on disk. The fixture is found by MEASUREMENT, not arithmetic: nest until the destination path is as long as the platform accepts, then take the first length where `NamedTempFile::new_in` fails and `fs::write` to a child succeeds, restated as an assertion at the moment of use. A permission-based fixture (mode 0555) was rejected: every run of this suite on the build host is root, where it is no obstacle, and the test would pass vacuously there. Unix-only, because the search is over PATH_MAX and Windows' equivalent is lifted by `long_path_safe_dest` on the very path under test -- named gap, not silence. CLASSIFIER: `a_round_trip_that_never_reached_a_verdict_is_handed_to_the_fallback` (plus `an_error_that_never_reached_a_verdict_is_not_a_rollback_failure` in wcore-config) runs everywhere, root included. RED under M4, which made `unpublished_or_unrolled` answer `Err` for every error: exit 101, `a genuine round-trip failure was swallowed as a refusal`, while M3's target stayed green. Red arm exit codes captured DIRECTLY (`cmd > file 2>&1; $?`), never through a pipe; each mutation printed the enclosing function body before and after so it is proven to land on executable code, and each site was restored with `git checkout --` plus a `touch` so cargo could not serve the mutated binary. Log: /root/w-f13/s2-red2.log."
---

`WriteTool`'s direct, no-`ToolContext` path (crates/wcore-tools/src/write.rs:452) treats every `Err` out of `atomic_write_checked` as "the tempfile round trip failed before any verdict" and falls into an unchecked `std::fs::write`, reporting success. Since wayland#1202 that error ALSO means "the guard ran, REFUSED, and the rollback exchanged nothing", in which case the error text is the only record of where the pre-image was preserved. The direct path discards it, so the tool reports success for a write its own guard refused and leaves the user's original under a `.tmpXXXXXX` sibling nothing will clean up.

Not new data loss -- in that state the new bytes are already published, so the fallback rewrites identical bytes. What is lost is the refusal signal and the pre-image's whereabouts.

Pre-existing in shape, widened in reach: before wayland#1202 the `keep_displaced` branch already returned `Err` when the restore exchange genuinely errored (EIO, EXDEV). wayland#1202 widens the trigger to the reachable `Swap::Vacant` / `Swap::Unsupported` case.

Distinct from wayland#1239, whose subject is a rollback that DID exchange and the concurrent save `discard_displaced` then unlinks. Criteria are taken verbatim from the issue's Acceptance section.

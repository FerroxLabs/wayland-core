---
issue: 1287
repo: FerroxLabs/wayland
kind: defect
title: "macOS process-tree containment intermittently refuses to attach - root NOT in its own group at recheck (fails closed)"
status: open
last_verified_commit: 5b76ec723
criteria:
  - id: c1
    text: "process_tree.rs stops refusing containment when the root process is spawned with process_group(0) but is observed outside its own group at recheck, or the recheck is corrected to tolerate the macOS reassignment it is actually observing."
    state: not-met
    evidence: "file:crates/wcore-sandbox/src/backends/process_tree.rs"
    owner: core
    note: "STILL NOT MET, and it is now precisely one arm away. THE FIX IS IN THE TREE AND ON MAIN. Re-graded 2026-09-10 by the w15/mac lane against HEAD rather than against the earlier note: crates/wcore-sandbox/src/backends/process_tree.rs no longer derives root_ok from macos_process_group(root.pid).is_ok_and(..). It calls mac_root_authority(process_group, &root_state, probe), a five-armed value (LeadsItsGroup / Exited / LeftItsGroup / Replaced / Unreadable) that makes the DECISION and the DIAGNOSTIC one object, reads ESRCH from the group probe as Exited -- the zombie case Darwin answers ESRCH for, reached one window after MacIdentityRecheck::Corpse -- and reads every other errno as Unreadable, i.e. never as absence. leaves_the_group_ours() accepts LeadsItsGroup and Exited only, so a LIVE root that called setpgid/setsid during the attach window is still refused and the message now names both groups. That is the criterion's second branch, and it is the branch the root-cause analysis says is correct: the root was never observed outside its own group, the group was never READ. ON MAIN: the change landed as 5cd389f53 'fix(sandbox): read a zombie root's ESRCH as exited, not as a lost group' (2026-09-04), and `git merge-base --is-ancestor 5cd389f53 origin/main` succeeds. The first of the two conditions this row was holding for is discharged. EXERCISED ON NATIVE macOS, PARTIALLY. In CI run 34212497448 (CI (macos-latest), head 131678faec2aa2a28b15c6e33fc975eb4e017e85, which `gh api .../compare/5cd389f53...131678fa` reports as `ahead`, so the tree CONTAINS the fix), the uploaded macOS JUnit carries all SEVEN mac_root_authority_tests cases -- a_live_root_leading_its_own_group_is_accepted, a_root_that_exits_between_the_recheck_and_the_group_probe_keeps_containment, a_corpse_root_is_accepted_without_probing_its_group, a_live_root_that_left_its_group_is_still_refused_and_names_both_groups, a_replaced_root_is_refused, an_unreadable_group_probe_is_never_read_as_absence, an_unreadable_identity_carries_its_diagnosis_into_the_message -- each with no failure element, alongside the eleven live macos_tests process_tree cases. So the decision table runs green on the platform that decides it. WHY THAT IS NOT ENOUGH, and the earlier note said so first: those are table tests over an injected probe closure. They cannot schedule the race -- root alive at recheck(), corpse by the group probe -- and a test that cannot reach the arm that was wrong is not the arm's evidence. The LIVE observation is the swarm test the defect surfaced through, wcore-swarm::workspace_authority::independent_cli_processes_cannot_overbook_shared_capacity, whose panic payload named process_tree.rs:683. THE ONE ARM STILL OWED, stated so it can be run rather than admired: that swarm test's macOS occurrences must be dated against 5cd389f53. CENSUS W records it at 1 of 104 macos-latest executions in 2026-08-31..2026-09-10 with one attempt, and neither the census nor .config/flaky-allowlist.txt line 69 names the RUN, so it is not established here whether that single observation is before or after the fix. Downloading the macos-latest JUnit for every run in W and locating that occurrence answers it directly; a pre-fix date plus zero post-fix occurrences over the remaining macOS executions is what makes this row met, and a post-fix occurrence would falsify the repair. THIS LANE DID NOT RUN THAT ARM -- it needs the full 104-artifact sweep, not the seven runs fetched here. WHAT THIS LANE COULD VERIFY WITHOUT macOS: `WCORE_PROOF_SLOT=default python3 tools/remote-proof.py worktrees/w15-allow clippy -p wcore-sandbox --target aarch64-apple-darwin --all-targets -- -D warnings` at 5b76ec723 on hetzner-dsm, receipt remote_exit 0 and complete true. That compiles and lints the cfg(target_os = macos) code INCLUDING the test module, under CI's own -D warnings, from Linux. It proves the repair builds on the platform it is for; it cannot run it. NO CODE WAS CHANGED HERE. The repair was already correct; what was wrong was the grading, which had not been re-checked against HEAD. Line 69 of .config/flaky-allowlist.txt still says 'REMOVE when gh#1287 lands; do NOT renew' -- and gh#1287's code HAS landed on main. It must NOT be removed on that instruction alone until the dating arm above is run, because the line is what keeps a pre-existing fail-closed macOS reliability bug from holding the release, and nothing yet shows the last macOS occurrence postdates the fix."
---

# A product bug that was nearly filed as a flake

The symptom is a timeout; the cause is a production containment refusal. Fails closed.

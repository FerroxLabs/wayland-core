---
issue: 1287
repo: FerroxLabs/wayland
kind: defect
title: "macOS process-tree containment intermittently refuses to attach - root NOT in its own group at recheck (fails closed)"
status: open
last_verified_commit: 3c6d9a78c
criteria:
  - id: c1
    text: "process_tree.rs stops refusing containment when the root process is spawned with process_group(0) but is observed outside its own group at recheck, or the recheck is corrected to tolerate the macOS reassignment it is actually observing."
    state: met
    evidence: "test:crates/wcore-sandbox/src/backends/process_tree.rs::a_root_that_exits_between_the_recheck_and_the_group_probe_keeps_containment"
    owner: core
    note: "MET 2026-09-10 by the w15/mac lane, on the criterion's SECOND branch, with every limit stated below. Read the branch carefully before using this row: the FIRST branch ('stops refusing containment when the root ... is observed outside its own group at recheck') is deliberately NOT satisfied and must not be -- a LIVE root that has genuinely moved to another group is still refused (MacRootAuthority::LeftItsGroup), because tearing down a group the workload has left is the failure that refusal exists to prevent. The SECOND branch, 'the recheck is corrected to tolerate the macOS reassignment it is actually observing', is the correct one and is what landed, because the root cause is that NO reassignment was ever observed: the group was never read. THE CODE. crates/wcore-sandbox/src/backends/process_tree.rs no longer derives root_ok from `macos_process_group(root.pid).is_ok_and(..)`, which collapsed a FAILED probe into the same `false` as a differing group. It calls `mac_root_authority(process_group, &root_state, probe)`, a five-armed value -- LeadsItsGroup / Exited / LeftItsGroup(pid) / Replaced / Unreadable(String) -- that makes the DECISION and the DIAGNOSTIC one object, so the message can no longer disagree with the branch that produced it. ESRCH from the group probe reads as Exited (Darwin answers ESRCH for a zombie's getpgid; this is MacIdentityRecheck::Corpse reached one window later, and it is the exact race that produced 'same generation, but NOT in its own group' -- a claim about a group nothing had read). Every other errno reads as Unreadable and is refused: an unreadable probe is never read as absence. leaves_the_group_ours() accepts LeadsItsGroup and Exited only. ON MAIN. Landed as 5cd389f53, 'fix(sandbox): read a zombie root's ESRCH as exited, not as a lost group', 2026-09-04; `git merge-base --is-ancestor 5cd389f53 origin/main` succeeds. EXERCISED ON NATIVE macOS. CI run 34212497448 (CI (macos-latest), head 131678faec2aa2a28b15c6e33fc975eb4e017e85, `gh api .../compare/5cd389f53...131678fa` = `ahead`, so the tree CONTAINS the fix). Its uploaded macOS JUnit carries, each with no failure element: all SEVEN mac_root_authority_tests decision-table cases, and -- this is the part the earlier grading was right to hold out for -- ELEVEN LIVE macos_tests cases that drive the real attach path through the changed function on real processes, including attaching_to_an_already_exited_root_is_not_a_containment_failure, a_corpse_root_with_a_live_descendant_still_yields_containment, root_exit_after_sentinel_joins_still_yields_containment, identity_drift_never_signals_foreign_process and process_group_generation_drift_fails_closed. So the production decision runs, green, on the platform that decides it. NO RECURRENCE, AND THE ARITHMETIC THAT SAYS HOW MUCH THAT IS WORTH. The reported symptom is wcore-swarm::workspace_authority::independent_cli_processes_cannot_overbook_shared_capacity, whose panic payload named process_tree.rs:683. This lane swept ALL 201 ci.yml runs in CENSUS W (2026-08-31T04:13Z..2026-09-10T04:42Z), downloaded every unexpired nextest-junit-macos-latest artifact (104 found, which reproduces the census denominator exactly), and classified every one BY ANCESTRY rather than by date via `gh api .../compare/5cd389f53...<headSha>`. RESULT: 51 macOS executions on trees CONTAINING the fix, ZERO occurrences; 53 on trees without it (49 diverged, 4 behind), ONE occurrence -- run 33481688035, head a4d157bda65cf5e9915e97d2393f487ad8caaed5, which compares as `diverged` and therefore does NOT contain the fix. LIMIT, and it is a real one: at a pre-fix base rate of 1 in 53, the chance of seeing zero in 51 post-fix executions even with NOTHING fixed is about 0.38. The zero is CONSISTENT with the repair and is NOT evidence for it. It is recorded because a post-fix occurrence would have FALSIFIED the repair, and none exists. CROSS-COMPILE, from Linux: `WCORE_PROOF_SLOT=default python3 tools/remote-proof.py worktrees/w15-allow clippy -p wcore-sandbox --target aarch64-apple-darwin --all-targets -- -D warnings` at 5b76ec723 on hetzner-dsm, receipt remote_exit 0 and complete true -- the cfg(target_os = macos) code and its test module compile and lint under CI's own -D warnings. WHY THIS IS GRADED ON THE CODE. The defect is a race between two syscalls -- the root alive at recheck() and a corpse by the group probe -- and the file says in its own words that no test can schedule it; the live macos_tests all drive roots that are comfortably alive or comfortably dead. A criterion that could only be closed by observing a microsecond race, against a base rate of one observation, is a gate with no reachable pass state. The criterion as WRITTEN is a statement about process_tree.rs, and process_tree.rs now has the property. LIMITS RESTATED SO NOBODY OVER-READS THIS ROW: (1) the raced arm itself has never been observed executing on macOS, only its table twin; (2) the zero-recurrence window is not statistically decisive (p ~ 0.38 under the null); (3) nothing here shows the OTHER macOS process-group defect found in the same sweep is fixed -- see gh#1285's note on wcore-mcp f016_real_spawn, an EPERM/census disagreement in MCP stdio teardown with 7 of 7 identical payloads, which is a different site and is still open. ALLOWLIST. Line 69 of .config/flaky-allowlist.txt says 'REMOVE when gh#1287 lands; do NOT renew'. That instruction is now due and this lane did not act on it (the file is held by another worker). Deleting it is correct: the code has landed on origin/main, and the one macOS occurrence in the whole census is on a tree that predates it. NOTE FOR WHOEVER DELETES IT: the line's body is still worth preserving somewhere -- it is the record that a 15s swarm timeout was a PRODUCTION containment refusal and was one allowlist entry away from being filed as a sixth timing flake."
---

# A product bug that was nearly filed as a flake

The symptom is a timeout; the cause is a production containment refusal. Fails closed.

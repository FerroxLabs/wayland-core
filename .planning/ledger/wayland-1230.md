---
issue: 1230
repo: FerroxLabs/wayland
kind: defect
title: "A served context slot below core's own baseline turn still truncates: 2,457 of a 4,096 slot is tool schemas before the user types"
status: closed
last_verified_commit: 93ede3424
criteria:
  - id: c1
    text: "— The un-compactable floor of a turn (system prompt + tool schemas —"
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1230_context_floor_test.rs::the_floor_is_derived_from_a_real_assembled_request"
    owner: core
    note: "MET. The floor is now COMPUTED from the assembled request. `uncompactable_floor_tokens(system, tools)` (crates/wcore-agent/src/compact/estimate.rs) is `estimate_request_tokens` evaluated at an EMPTY message list, which is exactly the part no degradation rung can reach: rung 1 sheds tool results, rung 2 truncates or drops messages, and neither can touch the system prompt or the tools array. The production caller is the turn loop in engine.rs, which computes it from the same `system` and `tools` it is about to send. THE FIRST VERSION OF THIS WAS WRONG AND THE ERROR IS RECORDED HERE RATHER THAN QUIETLY FIXED: it derived the floor from `tools().to_tool_defs()`, the RAW registry - 49 schemas, 19,101 estimator tokens - while the loop actually sends 8 after plan-mode filtering, MCP curation, the provider tool cap and cold-tool deferral, which is 4,091. A guard built on the raw registry would have been wrong by 5x and confidently so. The assembly is therefore EXTRACTED as `AgentEngine::assemble_turn_prelude` and both the turn loop and the grader go through it, so a test cannot drift onto a different object than the product sends. LIVE FIGURE, quoted beside it as the criterion asks - hetzner-dsm 2026-08-31, a PRIVATE Ollama on port 21434 (deliberately not the ambient service on 11434) serving qwen3:8b at CONTEXT 4096, through a logging proxy: core assembled a 4,454-character system prompt and 8 tool schemas totalling 8,903 characters; it scored that floor at 4,091 tokens and the refusal it raised quoted that number; on the 16,384-slot arm, where nothing was truncated, the endpoint reported 3,193 real prompt tokens for the same assembled prompt. Estimator runs ~28% high, which is the conservative direction for a floor. The test asserts three separable properties, each a different way for a floor to be fake: non-zero on a real bootstrapped engine; invariant to messages (including the 26,054-character tool result #1230 measured being discarded); and a lower bound on the full request estimate."
  - id: c2
    text: "— `BASELINE_TURN_TOKENS = 3,118` (`compact.rs:126`) is a snapshot of a"
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1230_context_floor_test.rs::the_baseline_constant_still_describes_this_tree"
    owner: core
    note: "MET, and the criterion's own premise needed correcting first - stated here rather than worked around. `the_baseline_constant_still_describes_this_tree` reds when this tree's assembled floor drifts from BASELINE_TURN_TOKENS. THE UNIT ERROR: the constant is in REAL tokens (it was read off a `usage` block by #1172) and `uncompactable_floor_tokens` is in ESTIMATOR tokens. Compared raw, the first version of this guard reported a 48.7% drift and demanded the constant be raised to 4,636. MEASURED, that would have been actively harmful: `minimum_workable_window` would move from 6,929 to roughly 10,300, `supports_compaction(8_192)` would become false, and every 8,192-token endpoint would start refusing - contradicting the band #1179 measured as workable and breaking c5 of this very issue. The guard therefore converts through ESTIMATOR_INFLATION = 1.286, itself measured on the c4 run (4,105 estimated over 3,193 real). In the constant's own units the drift is 15.6% for the bootstrap fixture and 1.8% for the shipped CLI turn - i.e. the constant is NOT materially stale in real tokens, and the issue body's '2.8% low' was right. DRIFT_TOLERANCE = 0.25 is set from the MEASURED spread between two honest configurations of the same tree (14%), not chosen to make today pass. The guard additionally asserts the load-bearing consequence: the remedy figure the refusal names is DERIVED from the measured floor via `CompactConfig::minimum_window_for_input_floor` and actually holds it, and it is strictly larger than the snapshot-derived `minimum_workable_window` - which is the concrete gap this criterion is about. BASELINE_TURN_TOKENS is deliberately NOT changed: it is accurate in its own units, and the floor decision no longer depends on it."
  - id: c3
    text: "— At a served window below that floor core takes a **named** decision,"
    state: met
    evidence: "symbol:crates/wcore-agent/src/engine.rs::context_floor_refusal"
    owner: core
    note: "MET. The named decision is REFUSE THE TURN, naming both numbers, before dispatch - `AgentEngine::context_floor_refusal`, called from the turn loop immediately after the request is assembled and before any provider call. The other two options #1230 c3 offered were considered and one of them was REFUTED BY MEASUREMENT rather than by argument: negotiating the slot upward is not available on the wire core speaks. hetzner-dsm 2026-08-31, Ollama 0.30.7, a ~5,000-token prompt to /v1/chat/completions reported prompt_tokens=4095 with no num_ctx, with a top-level num_ctx, AND with an options.num_ctx - byte-identical in all three arms; the OpenAI-compatible surface does not carry the field, and the native /api/chat route that does is served by wayland-ollama, which has no tool support at all ('Ollama's /api/chat tools API is not implemented yet') and so cannot host the tool-using turn this issue is about. Shedding the tool schemas was rejected with its tradeoff stated in the code: it buys the room but turns the agent into a chatbot, so every tool-shaped task then fails with a fluent wrong answer instead of an honest stop - the same failure this issue exists to end, moved one layer up. HOW THE SLOT IS KNOWN BEFORE THE FIRST REQUEST: `probe_ollama_served_window` reads /api/ps, which reports the slot the RUNNING instance was loaded with (4096), not the window the model advertises via /api/show (40960 for qwen3:8b - measured, the two disagree by 10x). ServedWindowTracker's own docs record that probing was tried and backed out twice because 'probing the endpoint means deciding WHICH endpoints to probe' and loopback cannot separate a real server from a mock; that objection is about SNIFFING and is answered the way `probe_ollama_tool_support` already answers it - the gate is the OPERATOR-DECLARED `provider_type == 'ollama'`, read from configuration, never inferred from an address. Bounded to 2 attempts per session (a stock Ollama unloads an idle model and then reports nothing, so turn 1 against a cold server is legitimately unanswerable and turn 2 is not) and fail-open in every direction: a non-Ollama provider issues no request at all, and an unknown slot can never refuse a run. The learned tracker is untouched and remains the fallback for every endpoint that will not say."
  - id: c4
    text: "— LIVE PROOF, not a mock: a run against a real stock Ollama serving"
    state: met
    evidence: "test:crates/wcore-agent/src/engine.rs::a_slot_below_the_computed_floor_stops_the_run_before_any_request"
    owner: core
    note: "MET by a LIVE RUN, three arms, one binary. Binary asserted identical BY CONTENT across all three: sha256 5885bb876a9e7c5428c22068d26a1b8ff6db6a4f2ab57ddc8d6b652b655a2042. Endpoints were MINE, stood up for this proof on ports 21434 (stock, CONTEXT 4096) and 21435 (OLLAMA_CONTEXT_LENGTH=16384), never the ambient service on 11434; `ollama ps` was read before and after every arm and recorded in the log. RED ARM a0 - the defect, reproduced on this same binary with the endpoint NOT declared, so the probe is off and only the learned tracker is in play. Proxy log, 4 requests: turns 1-2 served in full (prompt_tokens 3196, 3243, under the slot); turn 3 sent ~10,281 estimated and the server processed 3,218; turn 4 sent ~10,530 and it processed 3,439. TWO prompts silently truncated - including the 23,292-byte file the user asked about - before the corroborated path could stop the run. That is exactly what #1230 says: nothing could act before the first truncated request. GREEN ARM a - same binary, same endpoint, same 4,096 slot, endpoint declared. The entire proxy log is ONE line: `1 GET /api/ps`. ZERO chat completions. `[turns: 0 | tokens: 0 in / 0 out]`, exit 4, and the error surface carried: 'Run stopped: this endpoint states it is serving `qwen3:8b` with a 4096-token context slot, and the floor of this turn is 4091 tokens - the system prompt plus 8 tool schemas, which no compaction rung can shrink. Inside a 4096-token slot that leaves an input ceiling of 2527 tokens, below the floor ... Raise the context length of the server to at least 6634 tokens'. So the run stopped with the c3 refusal before the first truncated request, and in fact before any request at all. Captures: hetzner-dsm /root/s2/cf-live/{a0,a,b}/proxylog/, instrument /root/s2/cf-scratch/cf-proxy.py, driver /root/s2/cf-scratch/live.sh. The in-tree wiring guard drives run() end to end and asserts the provider call counter is 0. RED ARM M1, hetzner-dsm: replacing the production call site `self.context_floor_refusal(uncompactable_floor, tools.len())` with a None-returning closure - git diff -U0 read back to prove the edit landed on CODE, not a comment - compiles (cargo check -p wcore-agent --tests RC=0, so the red is behaviour and not a build break) and turns this test RED on both tries while the wrong-refusal control beside it stays green. Restored, touched, re-run: 2 tests run: 2 passed."
  - id: c5
    text: "— NEGATIVE CONTROL: the same binary against a slot that *can* hold a turn"
    state: met
    evidence: "test:crates/wcore-agent/src/engine.rs::a_slot_that_can_hold_the_floor_still_completes_the_turn"
    owner: core
    note: "MET, live, same binary by content (sha256 5885bb87..., identical to the c4 arms - asserted, not assumed). Arm b: my own Ollama on port 21435 with OLLAMA_CONTEXT_LENGTH=16384, `ollama ps` confirming CONTEXT 16384, same task and same 23,292-byte file. EXIT=0 and the task COMPLETED CORRECTLY - 'The file `big.txt` contains **300 lines**', which is the true line count. Proxy log, every chat turn served in full and none truncated: prompt_tokens 3,193 / 10,561 / 10,631 against estimates of 3,619 / 10,214 / 10,310 - the reported counts EXCEED our estimate on the large turns, which is the signature of a fully-processed prompt rather than a discarded head. The cold-start path was exercised for free: this instance had no model loaded, so the first /api/ps returned nothing and core proceeded; the second probe, after turn 1 had loaded the model, returned 16,384, and the gate correctly did NOT refuse (ceiling ~10,110 against a 4,091 floor). That is the 2-attempt probe budget working live. The in-tree control `a_slot_that_can_hold_the_floor_still_completes_the_turn` is the same shape at 8,192 and asserts the provider WAS called and no error was emitted, so a gate that refused everything could not pass; `the_floor_gate_refuses_exactly_the_windows_that_cannot_hold_the_floor` sweeps 1,024..65,536 and asserts both arms are non-empty."
---

Created 2026-08-31 to close a COVERAGE gap, and GRADED the same day by the
`context-floor` lane of the 0.13.12 release gate. All five criteria are met;
c4 and c5 are live runs against private Ollama instances, not mocks.

`scripts/check-criteria-ledger.py` scopes every open `area:core` issue on
wayland and EVERY open issue on wayland-core. This issue was in scope from
the moment it was filed and had no ledger file, so
`scripts/check-release-readiness.py` -- which reads ledger files and nothing
else -- could not count it. CI runs the coverage gate with `--offline`, the
arm that would have reported the gap, so nothing said so for two days.

Criteria are transcribed from the issue body without edit. Where the body's
wording is loose it is LEFT loose rather than tightened here: sharpening a
criterion inside the ledger is how a criterion quietly becomes an easier
adjacent property. Whoever takes this restates it on the ISSUE first.

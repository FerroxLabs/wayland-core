---
issue: 350
repo: FerroxLabs/wayland-core
kind: defect
title: "[nightly-windows-soak] FAIL - 2026-08-28"
status: closed
last_verified_commit: 43848f75
criteria:
  - id: c1
    text: "The latency half of the streaming-timeout test refuses to grade when the derived timeout does not dominate the host's own measured timer allowance"
    state: met
    evidence: "symbol:crates/wcore-tools/src/bash/tests.rs::decisive_walk_floor"
    owner: core
    note: "Two changes, both present: timeout_ms is floored at a MEASURED timer allowance (tests.rs:2894-2900), and the latency half gates on floor <= decisive with decisive = (timeout+allowance)*2, the SAME expression the attribution half already used - so the factor was not widened. The allowance is bracketed before AND after the call and the larger is used."
  - id: c2
    text: "On a host that cannot establish the premise, the test skips loudly with the measured numbers instead of panicking"
    state: met
    evidence: "test:crates/wcore-tools/src/bash/tests.rs::the_streaming_bash_timeout_bounds_the_secret_deny_walk"
    owner: core
    note: "On the last attempt it prints SKIP (wayland-core#350) carrying timeout_ms, the measured walk floor, decisive and the measured allowance, then returns. No panic. Earlier attempts grow the tree and re-race."
  - id: c3
    text: "A positive control on a fine-tick host still turns red when the manifest build is moved back outside the timeout scope"
    state: met
    evidence: "symbol:crates/wcore-tools/src/bash/tests.rs::decisive_walk_floor"
    owner: core
    note: "POSITIVE CONTROL RUN on hetzner-dsm 2026-08-29, and the host is fine-tick BY THE TEST'S OWN MEASUREMENT inside the failing run: a 4.096452ms timer allowance, against the 15.809ms the source comment records for the self-hosted Windows runner. Unmutated baseline: `PASS [   5.204s]` with an EMPTY stderr, i.e. no `SKIP (wayland-core#350)` line -- it graded rather than skipping. Mutation, in crates/wcore-tools/src/bash.rs::execute_streaming_with_ctx only: the manifest build's deadline was replaced with now + 3600s and the real `now + timeout` deadline restarted AFTER the build, which is the pre-#1111 shape the criterion names. The diff was read back; both edits landed on `let deadline = ...` CODE statements. Red arm, verbatim: `thread 'bash::tests::the_streaming_bash_timeout_bounds_the_secret_deny_walk' (3598533) panicked at crates/wcore-tools/src/bash/tests.rs:2974:9: / a 3ms streaming timeout floors at 35.341322ms over 3 calls, at or above the 14.192904ms this host's deadline alone can cost (timeout + a measured 4.096452ms timer allowance, doubled), against a walk flooring at 32.031928ms -- the manifest build is outside the timeout scope`. CONTROL IN THE SAME INVOCATION: the buffered path was deliberately left unmutated and `PASS [   3.564s] (1/2) wcore-tools bash::tests::the_bash_timeout_bounds_the_secret_deny_walk` in the same run, so the red is attributable to the moved build and not to a broken instrument or a loaded box. `Summary [   3.602s] 2 tests run: 1 passed, 1 failed, 1810 skipped`. bash.rs was restored to a clean git diff afterwards."
  - id: c4
    text: "The retries=0 override on this test survives the fix"
    state: met
    evidence: "file:.config/nextest.toml:775:test(=bash::tests::the_streaming_bash_timeout_bounds_the_secret_deny_walk)"
    owner: core
    note: "RE-ANCHORED 2026-08-30 for wayland#1198: was nextest.toml:624, a line of the block's rationale comment; it now cites this test's own operand inside the `retries = 0` override, which is what `survives the fix` means here. the override naming this test by literal operand is correct and should be kept - the failure must not become retryable. A separate script fails if any of the three named tests stops being a literal operand of a retries=0 predicate. EXTENDED 2026-08-30 to the profile the SOAK runs, and proved non-vacuous. The ci-side pin was never in doubt; what was missing is that `[profile.default]` carries `retries = 1` (`.config/nextest.toml:70`) and PHASE G passes no `--profile`, so the data-loss binary was retryable in the soak. A mirrored `[[profile.default.overrides]]` `binary(=inv2_round5_adversarial_test)` / `retries = 0` now closes that. MEASURED on hetzner-dsm (Linux) with the SUBJECT and a CONTROL mutated in the same tree and graded in ONE invocation, so the two cannot differ by build or by run: a `panic!` was injected into a `#[tokio::test]` body in `inv2_round5_adversarial_test` (the binary the override names) and into one in `tool_context_test` (a sibling binary in the same crate with no retries override anywhere). Each injection site was asserted to be an executable `fn`-body opener and not a comment before it was written. Result, verbatim, from a single `cargo nextest run -p wcore-tools -E 'binary(=inv2_round5_adversarial_test) or binary(=tool_context_test)'`: SUBJECT `FAIL [   0.023s] ( 5/23) wcore-tools::inv2_round5_adversarial_test a_work_tree_git_calls_bare_is_not_reported_as_no_repository` with NO `TRY` line at all, and CONTROL `TRY 1 FAIL [   0.018s]` then `TRY 2 FAIL [   0.009s]` `wcore-tools::tool_context_test real_fs_round_trips_a_file`. So the ambient `retries = 1` is live in that same run - the absence of a retry on the subject is the override acting, not the detection being blind. RESTORE VERIFIED: both files `git checkout`-ed and `touch`-ed so cargo could not serve a mutated binary, `git diff --stat` empty, and the identical command then gives `Summary [   3.070s] 23 tests run: 23 passed, 0 skipped` with zero `TRY` lines. This arm was run on Linux deliberately and that is adequate: the override carries no `platform =` key and nextest's config resolution is not platform-conditional, so the arm grades the config, not the OS"
  - id: c5
    text: "The issue's own close condition is met: a green nightly-windows-soak run against this tree"
    state: superseded
    successor: FerroxLabs/wayland-core#368
    evidence: "test:crates/wcore-tools/src/bash/tests.rs::the_streaming_bash_timeout_bounds_the_secret_deny_walk"
    owner: core
    note: "SOAK RUN AGAINST THIS TREE 2026-08-29: https://github.com/FerroxLabs/wayland-core/actions/runs/33258858506 (workflow_dispatch on lane/f13-fin-windows-runs at bd184563). NOT GREEN - and the reason is the whole finding, so read the next two paragraphs before reading the conclusion. THIS ISSUE'S OWN DEFECT IS FIXED AND THE SOAK PROVES IT. #350 was filed because `the_streaming_bash_timeout_bounds_the_secret_deny_walk` failed on BOTH attempts of the 2026-08-28 nightly. In this run, on `windows-2025`, PHASE G reports it verbatim as `SLOW [> 30.000s]` then `PASS [  49.842s] (3060/4123) wcore-tools bash::tests::the_streaming_bash_timeout_bounds_the_secret_deny_walk`. It graded, on the coarse-tick host class the c2 skip arm exists for, and it passed. That is the close condition's substance. WHAT KEPT THE RUN RED IS TWO OTHER DEFECTS, BOTH NOW FILED, NEITHER THIS ONE. (1) The self-hosted msvc `Windows live-acceptance` job failed PHASE L on `concurrent_allow_and_deny_identities_do_not_interfere`, verbatim `thread 'concurrent_allow_and_deny_identities_do_not_interfere' (20672) panicked at crates\\wcore-sandbox\\tests\\live_fs_acl.rs:471:5: / ordinary allow identity must retain access`, twice, then `x PHASE L failed: live_fs_acl` - that is wayland-core#324, measured and root-caused this same day and carried to FerroxLabs/wayland-core#368. (2) PHASE G's single hard failure in 3060 tests, 3 of 3 tries, was `wcore-tools path_validation::tests::a_path_whose_metadata_fails_for_a_reason_other_than_absence_is_refused` - a #238 c6 test whose Unix ENOTDIR provocation maps to `NotFound` on Windows so it cannot establish its own premise, filed as FerroxLabs/wayland-core#374 and reproduced on the workstation at retries=0. `a_cancelled_streaming_bash_does_not_wait_for_the_secret_deny_walk` was FLAKY 2/2 (TRY 1 FAIL, TRY 2 PASS) and did not fail the phase. SO: this criterion is a whole-run predicate over defects it does not own, and it is DECOMPOSED onto the two that own them - #368 and #374. It becomes true when they do, with no further work on #350 itself. The MASTER-PLAN.md:373 residual is unchanged and still unclaimed: it asks the latency half be confirmed ABSENT from the Windows nextest list rather than self-skipping, and as implemented it self-skips - though note this run did not exercise that path, because the host graded rather than skipping. BLOCKER LIST HALVED 2026-08-29 on lane/f13-u-win-native: of the two defects that kept run 33258858506 red, #374 is now CLOSED - the PHASE G hard failure `path_validation::tests::a_path_whose_metadata_fails_for_a_reason_other_than_absence_is_refused`, the only hard failure in 3060 tests, now passes on Windows 11 build 26200 with its premise genuinely established. #368 is the SOLE remaining blocker on this criterion, and it now has a ledger entry with gradeable criteria where before it had none. SAID PLAINLY so it is not re-derived: no green soak has been obtained, and one cannot be until #368 lands, because the self-hosted msvc `Windows live-acceptance` job fails PHASE L on `concurrent_allow_and_deny_identities_do_not_interfere` on roughly one run in five. This criterion is a whole-run predicate over a defect it does not own and stays superseded onto #368 alone. A SOAK WAS ACTUALLY RUN AGAINST THIS TREE, because the criterion says obtain one: https://github.com/FerroxLabs/wayland-core/actions/runs/33266413002 (workflow_dispatch on lane/f13-u-win-native at b52fb934). WHAT IT SETTLES, in order. (1) #374 IS FIXED IN THE SOAK ITSELF, not merely in a unit run: PHASE G on windows-2025 reports `PASS [   0.041s] (3060/4126) wcore-tools path_validation::tests::a_path_whose_metadata_fails_for_a_reason_other_than_absence_is_refused`, at the exact index where the previous soak died. (2) PHASE L still fails on #368, verbatim `panicked at crates\\wcore-sandbox\\tests\\live_fs_acl.rs:471:5` / `ordinary allow identity must retain access`, then `x PHASE L failed: live_fs_acl`. THAT WAS NOT THE WHOLE STORY, AND THE REST IS THE REASON THIS NOTE IS LONG. PHASE G still failed, on THREE TIMEOUTS THAT HAD NEVER BEEN SEEN, in `wcore-tools::bash_unsaved_guard_bound_live`. They are not a regression from this branch and they are not new code: the soak's PHASE G ran WITHOUT `--no-fail-fast`, so nextest CANCELLED at the first failure and every test ordered after it was invisible. Measured: the previous soak stopped at `3060/4123`; with #374 alone fixed this one reached `3883/4126` and the three surfaced. Fixing one defect did not create three, it stopped hiding them. ROOT-CAUSED ON SEANDESKTOP (Windows 11 build 10.0.26200.9168), same tree, same binaries, the two profiles back to back, the ONLY variable being which profile nextest was given: default `Summary [  60.026s] 4 tests run: 1 passed, 3 timed out` with each killed at 60.024-60.025s; `--profile ci` `Summary [  52.485s] 4 tests run: 4 passed` with the three finishing at 52.482-52.484s. The work takes ~52s against a 60s budget - the instrument is inside its own noise, which is the same disease c1 and c2 of this very issue exist to cure. It matters because `just test-ci` and both ci.yml Windows legs pass `--profile ci` (90s x 2), so they pass, while the soak - the one job whose green IS this criterion - passes no profile and grades at 60s. BOTH ARE FIXED HERE, minimally and with the evidence recorded at each site: `scripts/wayland-e2e-windows-soak.ps1` PHASE G now passes `--no-fail-fast`, and `.config/nextest.toml` gains a `[[profile.default.overrides]]` for `binary(bash_unsaved_guard_bound_live)` at 60s x 3 = 180s, matching the ci profile's ceiling and following the `packaged_driver_gate` override immediately above it, which was added for the identical reason. Adopting `--profile ci` wholesale is still the wrong change, but THE REASON FIRST RECORDED HERE WAS FALSE AND IS WITHDRAWN. It said `--profile ci` was rejected because that profile carries retries, and a soak that retries is a soak that hides flakes. The soak ALREADY retries: `.config/nextest.toml:70` sets `retries = 1` under `[profile.default]`, which is the profile PHASE G runs, and this run's own PHASE G log shows it SPENDING that retry - `TRY 1 TMT [  60.010s]` then `TRY 2 TMT [  60.019s]` for each of the three timeouts. The delta `--profile ci` would buy is 1 -> 2, not 0 -> 2, so the argument as written condemned the status quo it was defending. The per-binary override is kept because it is the NARROWER change, not because the wider one adds retries. AND THE FALSE ARGUMENT WAS COVERING A REAL HOLE, which is closed here rather than merely noted. Every `retries = 0` anti-vacuity override in `.config/nextest.toml` - `pipeline_test`, the two deny-walk timing tests, `inv2_round5_adversarial_test`, and the three harness-ownership binaries - sits under `[[profile.ci.overrides]]`, and NOT ONE of them sits under `[[profile.default.overrides]]`. The soak runs the default profile at `retries = 1`. So the one job whose green IS this criterion could retry a DATA-LOSS assertion into a pass - precisely the disease the `inv2` ci override was written to cure, and it escaped through the profile the soak happens to use. `binary(=inv2_round5_adversarial_test) retries = 0` is therefore mirrored into `[[profile.default.overrides]]`. Scope is held deliberately to that one binary: it is the one whose every assertion is a data-loss assertion, and it is the arm that actually went red on Windows this cycle (core#387 c3, attributed to FerroxLabs/wayland-core#370). KNOWN RESIDUAL, stated rather than left to be found: the other four `retries = 0` overrides remain ci-only, so the soak can still retry those. GREEN ARM for the budget fix, same host, default profile, after the override: `Summary [  53.285s] 4 tests run: 4 passed, 0 skipped`. PLATFORM EVIDENCE FOR THE COMMIT THAT MADE THIS CHANGE, WHICH IT DID NOT HAVE. The commit carrying the override and the `--no-fail-fast` edit (5fd7be29e) went GREEN in CI run 33271752618 with BOTH Windows test legs SKIPPED - `CI (windows-latest, hosted)` and the matrixed `CI` leg both report `skipped`, because that commit message carried no `[ci-windows]`. A green whose Windows legs never ran says nothing about a Windows-only change, so both halves were re-measured directly. MEASURED ON SEANDESKTOP 2026-08-30 (Windows 11 build 10.0.26200.9168), tree D:\\u-win at d081df20, the binary alone, default profile, WITH the override: `Summary [  51.142s] 4 tests run: 4 passed, 0 skipped`, the three cases at 50.270 / 51.115 / 51.141s. AND THE RED ARM DID NOT GO RED, WHICH RETIRES PART OF THIS ENTRY'S OWN CLAIM RATHER THAN CONFIRMING IT. The override block was deleted from `.config/nextest.toml` - config only, no recompile, so the test binaries are byte-identical across the two arms - and the same command re-run: `Summary [  47.837s] 4 tests run: 4 passed (3 slow), 0 skipped`, the three at 47.356 / 47.793 / 47.837s against the default `30s x 2` = 60s kill. So on THIS host at THIS tree the three cases pass with or without the override, and the sentence written above - that the soak `could not reach a pass state` - is TOO STRONG as a general claim and is withdrawn. RESTORE VERIFIED: `git checkout -- .config/nextest.toml`, size back to 41120 bytes, `git status --porcelain` reports no tracked change. WHAT STILL LICENSES THE OVERRIDE, stated precisely so it is not re-inflated: the 60s kill was NOT reproduced on SeanDesktop today, but it WAS observed on the host the soak actually runs on. Soak run 33266413002 PHASE G on `windows-2025` reports `TRY 1 TMT [  60.010s]` then `TRY 2 TMT [  60.021s]` for all three cases. That is CI's host class, not this workstation's - SeanDesktop does the same work in 47.8-51.1s. The earlier SeanDesktop A/B recorded above (60.026s default vs 52.485s ci, at tree b52fb934) does NOT reproduce at d081df20 and must not be read as the root cause. The honest shape: the work costs roughly 48s to over 60s depending on host and tree against a 60s budget, the measurement has straddled the line in both directions, and the override buys headroom on the host that actually timed out. A budget inside its own noise is still the defect and the fix is still right; the REASON first recorded for it was overstated. AND `--no-fail-fast` IS NOW MEASURED DIRECTLY RATHER THAN INFERRED. The justification recorded above - the previous soak stopping at `3060/4123` and this one reaching `3883/4126` - is CONFOUNDED, because #374 was fixed between those two runs, so it cannot separate the flag from the fix. Replaced with a clean A/B on hetzner-dsm at this tree, one `panic!` injected into an executable `async fn` body in `tool_context_test` (asserted to be a fn-body opener, not a comment), the flag the ONLY variable and the binaries identical between arms: WITHOUT the flag `Summary [  35.767s] 1735/1814 tests run: 1734 passed (1 slow, 1 leaky), 1 failed, 4 skipped`; WITH it `Summary [  34.677s] 1814 tests run: 1813 passed (1 slow), 1 failed, 4 skipped`. So one failure cost 79 tests their execution, and the `1735/1814` numerator is nextest saying so out loud. That is the defect the flag closes, now shown without borrowing #374's fix for the evidence. RESTORE VERIFIED: `git checkout` plus `touch`, `git diff --stat` empty. A first attempt at this A/B over only 13 tests in 4 binaries showed NO difference (both arms 13/13) - with that few tests nothing is still queued when the first failure lands, so there is nothing to cancel. Recorded because it is the way this measurement fails silently and reads as \"the flag does nothing\". THE SOAK SCRIPT EDIT WAS CHECKED ON WINDOWS TOO, since it had no platform evidence either: at HEAD `scripts/wayland-e2e-windows-soak.ps1` parses with 0 errors through `[System.Management.Automation.Language.Parser]::ParseInput` on UTF-8-decoded text (negative control, a deliberately unclosed block: 1 error), and `--no-fail-fast` is present on the PHASE G invocation at line 528. Read instead with `ParseFile`, which decodes this BOM-less UTF-8 file as ANSI, it reports 15 errors - but the PRE-EDIT blob at b52fb934 reports the SAME 15 when read byte-identically, so that count is a decoder artefact of the check and not a regression from the edit. SO THE BLOCKER LIST IS NOW EXACTLY ONE: #368. Everything else this soak found is closed on this branch."
---

This is an auto-filed nightly soak failure. Its body says to grep the build,
nextest and mutants sections, which is boilerplate and is wrong here: the run
log shows a single test failing on both attempts,
the_streaming_bash_timeout_bounds_the_secret_deny_walk in wcore-tools.

It is not stale and it is not the AppContainer ACL race. The failing lines are
byte-identical at the shipped tree - the ship diff to that test file is 77 added
lines, all appended well below them - so this reproduces on v0.13.10.

The root cause is that the instrument is grading below its own noise floor. The
timeout under test is derived as one tenth of a measured walk, which came out at
6 to 7 milliseconds on a host whose timer tick the test itself measured at 15.8
milliseconds. The residue being asserted on is quantization, not product
latency. The other half of the same test already knows this and refuses to grade
when the premise fails; this half does not.

One arm of the fix is verifiable on hetzner, the positive control. The skip arm
needs a coarse-tick host. Criteria come from the cluster C verification note of
2026-08-29.

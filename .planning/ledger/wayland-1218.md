---
issue: 1218
repo: FerroxLabs/wayland
kind: defect
title: "The scaled output_reserve is decoupled from the max_tokens core actually sends, so the pre-flight ceiling admits a request that cannot fit"
status: open
last_verified_commit: 6e98ef975
criteria:
  - id: c1
    text: "size_output_cap and scaled_reserves agree: the max_tokens sent is never larger than the reserve the ceiling withheld"
    state: blocked
    evidence: "test:crates/wcore-agent/tests/issue_1218_output_ask_wiring_test.rs::the_ask_on_the_wire_fits_the_window_in_force"
    owner: maintainer
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D37, found while verifying FerroxLabs/wayland#1179). Nothing has been done. The measured finding, verbatim: Scaling `output_reserve` down decoupled it from the `max_tokens` core actually asks for, so the pre-flight ceiling now certifies inputs that cannot fit alongside the output request. The scaled reserve is exactly w/3 (20,000 x 0.55w/33,000), while `size_output_cap` sizes `max_tokens` from the CATALOGUED window (or `UNKNOWN_CAP` = 8,192 for an unlisted model) and never sees the compact window or #1172's learned served window. Concretely, on the endpoint #1172 measured (unknown model, openai-compat, which is NOT omit-safe so the field IS sent): learned/narrowed window 8,192 -> ceiling 5,053, reserve 2,730, but max_tokens sent = 8,192; total ask 13,245 on an 8,192 slot. Configured window 16,384 -> ceiling 10,104, reserve 5,461, max_tokens 8,192; total 18,296 on 16,384. Before #1179 this could not happen: the flat 20,000 reserve always exceeded any UNKNOWN_CAP or catalogued output ceiling. Now the reserve is under 8,192 for every window below 24,576 and under gpt-4o's 16,384 ceiling for every window below 49,152. GRADED 2026-08-30 by lane w2-window-arc at 115cb4c6. size_output_cap takes window_in_force and ends `sized.min(room(window))`, so the ask is bounded by the room the window in force actually leaves after the input the guard admitted. The clamp is IDENTITY wherever the window in force is the catalogued one (every registry model absent a #1172 narrowing), pinned by a_window_in_force_that_is_the_catalogued_one_changes_no_sizing -- the literal reading of this ticket's title, clamping the ask to the withheld reserve at every input, would cut a 200,000-window Claude turn from its real 64,000-token ceiling to the ~20,000-token compaction reserve. That choice is recorded as Q-1218 in .planning/DECISIONS.md. RED ARM (hetzner-dsm): replacing the final `match window_in_force` with a bare `sized` -- printed back after the edit, so it landed on the match expression and not on the 14 lines of comment above it -- reddens c2 and c3; restored, touched, green. SAID PLAINLY 2026-08-30, after this lane's verifier objected that the re-reading was recorded only in a decision file and a test doc comment: THIS CRITERION'S LITERAL WORDS ARE NOT MET, AND ARE DELIBERATELY NOT MET. At window 8,192 with an input estimate of 0 the guard leaves room = 8,192 - 0 - 512 = 7,680, the ask is 7,680, and the reserve withheld is 3,139 -- so the max_tokens sent IS larger than the reserve, at every input below the ceiling. What is met is the property the ticket's own arithmetic evaluates and that its title is about: `est + ask <= window` at every input the guard admits, and `ask <= withheld` at the worst-case input (est = ceiling), both asserted across 4,096..49,152 by the_output_ask_never_outgrows_the_reserve_the_ceiling_withheld. The literal reading was refused because clamping the ask to the withheld reserve at EVERY input would cut a 200,000-window Claude turn from 64,000 output tokens to about 20,000 -- a regression, not a fix. That is an engineering call on a criterion, not a criterion met as written, and it is a maintainer decision to accept or reject: see Q-1218 in .planning/DECISIONS.md. RE-GRADED 2026-08-31 by lane f13-ctx-1218 at 6e98ef975, from met to blocked (owner maintainer). Q-1218 is NOT reopened and no production code changed: the clamp to the room left in the window in force stands, and RED ARM 1 below shows it is load-bearing. What changed is the honesty of the grade -- the previous entry recorded state: met while its own note said THIS CRITERION'S LITERAL WORDS ARE NOT MET, AND ARE DELIBERATELY NOT MET, and a green that contradicts its own note is the kind that has had to be withdrawn. MEASURED at HEAD: at window 8,192 with an input estimate of 0 the ask is 8,192 - 0 - 512 = 7,680 while the reserve the ceiling withheld is 3,139, so the max_tokens sent IS larger than the reserve at every input below the ceiling, and that request still fits the window. The literal reading cannot be satisfied without a regression, and the regression is now MEASURED rather than argued. RED ARM 2 (hetzner-dsm /root/w-f13/ctx1218, baseline cargo check -p wcore-agent --tests RC=0 before the edit): the final clamp replaced by sized.min(room(window)).min(output_reserve + emergency_buffer) -- the literal reading -- printed back after the edit so it landed on the match expression and not on the comment above it; a_window_in_force_that_is_the_catalogued_one _changes_no_sizing then FAILS with left: 23000, right: 128000, i.e. claude-opus-4-7 cut from its real 128,000-token output ceiling to the 23,000-token compaction reserve on its own catalogued 200,000-token window, with no narrowing anywhere. Restored, touched, git status --porcelain empty, 32/32 green again. So: not met as written, its literal form is a wrong-refusal, and accepting the recorded deviation (Q-1218) is a maintainer call -- core cannot grade its own refusal green. What IS verified: est + ask <= window at every input the guard admits and ask <= withheld at the worst-case input across 4,096..49,152 (c2), the two measured cases (c3), and -- new here -- that the ask reaching the WIRE carries the window in force, graded through the real AgentEngine turn loop by the_ask_on_the_wire_fits_the_window_in_force. That last one was NOT graded before: every earlier test drives size_output_cap directly with an explicit Some(window), and RED ARM 3 (the production call site passing None, the pre-#1179 wiring) reddens ONLY the new test while all 114 other tests in the same run stay green."
  - id: c2
    text: "A test asserts reserve >= the max_tokens that will be sent, across the window range 4,096..49,152 for an unlisted model; shown RED against today's UNKNOWN_CAP = 8,192"
    state: met
    evidence: "test:crates/wcore-agent/src/engine.rs::the_output_ask_never_outgrows_the_reserve_the_ceiling_withheld"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D37). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below. GRADED 2026-08-30 by lane w2-window-arc at 115cb4c6. Sweeps 4,096..49,152 in steps of 64 for an UNLISTED model (openai-compat/qwen3:8b), asserting at each window that the ask is no larger than output_reserve + emergency_buffer -- the reserve the ceiling withheld -- at the input the guard actually admits, and that est + ask <= window at five inputs spanning 0..ceiling. It carries its own control: it asserts model_output_ceiling(provider, model).is_none() first, so if qwen3:8b is ever catalogued the test says the case has moved rather than passing on the wrong arm. RE-VERIFIED 2026-08-31 by lane f13-ctx-1218 at 6e98ef975 on hetzner-dsm. Baseline: cargo check -p wcore-agent --tests RC=0 -- reached only after repairing an OutputSink::emit_error signature the f13 merge left on the 3-parameter form in issue_1150_unknown_context_window_test.rs, so at the lane tip the whole wcore-agent test tree failed to compile and NO test on this branch, this one included, could have been run. RED ARM 1: the final match window_in_force replaced by let _ = window_in_force; sized, printed back after the edit so it landed on the match expression and not on the 14 lines of comment above it -- this test then fails at the FIRST window of its sweep: window 4096: the guard admits 2527 input tokens having withheld 1569 for the output, and the ask is 8192. 32 tests run, 29 passed, 3 failed (this one, the_measured_1218_overflows_no_longer_hold, the_output_ask_fits_inside_the_window_actually_in_force); the catalogued-identity control stayed GREEN under it, which is the shape a correct clamp must have. Restored, touched, git status --porcelain empty, 32/32 green."
  - id: c3
    text: "The measured cases here no longer hold: learned 8,192 -> ceiling 5,053 / reserve 2,730 / max_tokens 8,192, and configured 16,384 -> 10,104 / 5,461 / 8,192"
    state: met
    evidence: "test:crates/wcore-agent/src/engine.rs::the_measured_1218_overflows_no_longer_hold"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D37). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below. GRADED 2026-08-30 by lane w2-window-arc at 115cb4c6. The two measured cases, stated in the ticket's own figures: window 8,192 -> ceiling 5,053 / reserve 2,730, and window 16,384 -> ceiling 10,104 / reserve 5,461, both asserted as equalities so the arithmetic a re-grade reads is the arithmetic the ticket wrote. The ask is asserted != 8,192 (the measured value) and ceiling + ask <= window. RE-VERIFIED 2026-08-31 by lane f13-ctx-1218 at 6e98ef975 on hetzner-dsm, same RC=0 baseline and same RED ARM 1 as c2: this test goes red with the clamp removed and green with it restored, git status --porcelain empty after the restore. The PRODUCTION path is graded as of this commit too: the_ask_on_the_wire_fits_the_window_in_force drives the real AgentEngine turn loop (capturing provider, unlisted model, [compact] context_window = 8,192, user cap 64,000 so the cap is not what binds) and asserts the max_tokens on the request the engine hands the provider is below UNKNOWN_CAP = 8,192 and leaves the 512-token window buffer free, with a control that the turn reached the provider at all. RED ARM 3: passing None for window_in_force at the one production call site -- the pre-#1179 wiring, which is the state this ticket was filed against -- reddens ONLY that test out of 115 (this test and c2's sweep both stay green, which is the gap it closes)."
---

Scaling `output_reserve` down decoupled it from the `max_tokens` core actually asks for, so the pre-flight ceiling now certifies inputs that cannot fit alongside the output request. The scaled reserve is exactly w/3 (20,000 x 0.55w/33,000), while `size_output_cap` sizes `max_tokens` from the CATALOGUED window (or `UNKNOWN_CAP` = 8,192 for an unlisted model) and never sees the compact window or #1172's learned served window. Concretely, on the endpoint #1172 measured (unknown model, openai-compat, which is NOT omit-safe so the field IS sent): learned/narrowed window 8,192 -> ceiling 5,053, reserve 2,730, but max_tokens sent = 8,192; total ask 13,245 on an 8,192 slot. Configured window 16,384 -> ceiling 10,104, reserve 5,461, max_tokens 8,192; total 18,296 on 16,384. Before #1179 this could not happen: the flat 20,000 reserve always exceeded any UNKNOWN_CAP or catalogued output ceiling. Now the reserve is under 8,192 for every window below 24,576 and under gpt-4o's 16,384 ceiling for every window below 49,152.

**Where.** crates/wcore-agent/src/engine.rs:1148 `size_output_cap` (production call at crates/wcore-agent/src/engine.rs:13450) vs crates/wcore-config/src/compact.rs:541 `scaled_reserves` / :578 `input_ceiling_for_window`; the #255 guard at crates/wcore-agent/src/engine.rs:13237 compares only the input estimate

**Why it matters.** It undercuts #1179's own premise. The point of narrowing onto a learned window was to make a small served window safe, but the run then asks the endpoint for more output tokens than the whole window holds — on Ollama that generates until the context is exhausted, which re-creates the silent prompt truncation #1172 is about, this time with core reporting the request as inside its ceiling. Nothing asserts `scaled output_reserve >= the max_tokens that will actually be sent`.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.


## Re-verification, 2026-08-31 (lane f13-ctx-1218)

The premise this lane was handed -- that `size_output_cap` still cannot see the window in force --
is STALE: the clamp landed with the window-arc lane and is in the tree. What was actually left, and
what this lane did:

1. **The lane tip did not compile its tests.** `cargo check -p wcore-agent --tests` failed at
   `7cfae79f6` on an `OutputSink::emit_error` signature the f13 merge left on its 3-parameter form
   in `issue_1150_unknown_context_window_test.rs`. Every wcore-agent test on this branch, the three
   #1218 tests included, was unrunnable until that was repaired. Anti-vacuity requires an RC=0
   baseline before any mutation, so the repair is the first commit here.
2. **c2 and c3 hold, and their red arms are real** (RED ARM 1 above).
3. **c1 is not met as written, deliberately** -- re-graded `met` -> `blocked` (owner: maintainer).
   The engineering is done; the literal wording is a wrong-refusal worth 128,000 -> 23,000 output
   tokens on a healthy 200,000-token Claude window, measured (RED ARM 2). Accepting Q-1218's
   deviation is a maintainer call, not core's.
4. **The wiring was ungraded.** All three #1218 tests drive `size_output_cap` directly with an
   explicit `Some(window)`; none of them can see the one production call site. `issue_1218_output_
   ask_wiring_test.rs` closes that through the real turn loop, and RED ARM 3 shows it is the only
   test of 115 that notices the call site reverting to `None`.

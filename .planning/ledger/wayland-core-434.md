---
issue: 434
repo: FerroxLabs/wayland-core
kind: defect
title: "The #338 c2 Windows residual pin reports the escape CLOSED while the same report shows it open"
status: open
last_verified_commit: aed0d2ee0
criteria:
  - id: c1
    text: "The pin no longer depends on the PARENT owning the operator console: it is either re-anchored on the _EXPLICIT probe, which does not, or it asserts the precondition (CONSOLE_WINDOW_AT_CREATION != NONE) and skips honestly when it does not hold."
    state: met
    evidence: "file:crates/wcore-cli/tests/quarantine_console_authority_windows.rs:948:PARENT_DONATED_ARM=QUIET"
    owner: core
    note: "MET at 5e585f88a, and BOTH branches this criterion offers are taken rather than one. RE-ANCHORED: the three hard pins that hung on the parent-donated route (ATTACH_PARENT_PROCESS / SHARES_USER_CONSOLE_AFTER / CONOUT_AFTER, all reached through AttachConsole(ATTACH_PARENT_PROCESS)) are replaced by three on the explicit-pid route (ATTACH_BY_EXPLICIT_PID / SHARES_USER_CONSOLE_AFTER_EXPLICIT / CONOUT_AFTER_EXPLICIT), which names the driver to the kernel and does not ask the parent for anything. SKIPS HONESTLY: the parent-donated arm is kept as an observation and asserted ONLY where it is live -- when SHARES_USER_CONSOLE_AFTER reads anything but `true` the block prints PARENT_DONATED_ARM=QUIET, says in the same line that this is NOT evidence the residual closed, and moves on without a red. PROVEN BY A/B ON REAL WINDOWS, not by inspection, on SeanDesktop (the ferrox-win-msvc host) 2026-09-10 with the CI condition INJECTED -- `shares_console_with_driver()` forced to report false at the parent-donated print site, which reproduces run 33713740549's payload exactly (ATTACH_PARENT_PROCESS=SUCCEEDED, SHARES_USER_CONSOLE_AFTER=false, SHARES_USER_CONSOLE_AFTER_EXPLICIT=true). ARM A, the tree at 4f82ce8c1 with that injection: FAIL, exit 100, `assertion left == right failed ... the reattached child is no longer on the USER'S console. Re-grade c2 on Windows and replace this pin` with `left: Some(\"false\") right: Some(\"true\")` -- the CI failure, reproduced on demand. ARM B, the tree at 5e585f88a with the SAME injection: PASS, exit 0, printing PARENT_DONATED_ARM=QUIET. Mutation verified to have landed on CODE and the tree restored clean afterwards. UNINJECTED, n=10 at --retries 0 on the same host: 10 PASS / 0 FAIL, 1.73-1.76s, with PARENT_DONATED_ARM=LIVE and both sharing fields `true`. ALSO CLOSED HERE, because it is a second route to the same false reading: shares_console_with_driver() used a fixed 64-entry buffer, and GetConsoleProcessList writes NOTHING and returns the REQUIRED size when a console has more clients than the buffer holds -- so on a busy console the oracle answered `false` for a console the driver was on. It now asks for the count first and grows. LIMIT: SeanDesktop cannot reproduce the uninjected failure (interactive session), so the 10/10 green is a liveness control and NOT evidence of a rate; what is proven is that the failing CONDITION no longer produces a red or a misleading message."
  - id: c2
    text: "A failure of this pin can no longer be read as 'the residual closed' while another field in the same report shows the child reaching the operator console: the assertion message names BOTH fields."
    state: met
    evidence: "test:crates/wcore-cli/tests/quarantine_console_authority_windows.rs::a_quiet_parent_arm_does_not_read_as_the_residual_closing"
    owner: core
    note: "MET at 5e585f88a. Every residual message now renders through `residual_arms`, which prints BOTH routes with their values -- ATTACH_PARENT_PROCESS / SHARES_USER_CONSOLE_AFTER / CONOUT_AFTER for the parent-donated arm and ATTACH_BY_EXPLICIT_PID / SHARES_USER_CONSOLE_AFTER_EXPLICIT / CONOUT_AFTER_EXPLICIT for the explicit-pid arm -- followed by the rule that makes them readable together: the residual is CLOSED only if BOTH sharing fields are `false`, and either one `true` means the escape is still open and the pin must NOT be inverted. The old message is gone, and with it the instruction to `Re-grade c2 on Windows and replace this pin` standing alone. GRADED IN-TREE rather than by reading the source: the named test feeds the field values CI run 33713740549 reported to `residual_arms` and asserts the rendered message carries the parent-donated field AND its value, the explicit-pid field AND its value, the BOTH-must-be-false rule, and the refusal to invert. It ran on Windows in the n=10 sweep above (4 tests per run, 10/10). MEASURED, and this is the part inspection could not give: with the CI condition injected, the message the tree at 5e585f88a actually prints is `PARENT_DONATED_ARM=QUIET SHARES_USER_CONSOLE_AFTER=Some(\"false\") -- SKIPPED, and this is NOT evidence that the #338 c2 residual closed. ... SHARES_USER_CONSOLE_AFTER_EXPLICIT=Some(\"true\") ...`, against the tree at 4f82ce8c1 which printed the old single-field message under the identical payload. LIMIT: the fixture is a reconstruction of run 33713740549's FIELD VALUES in the order the probe prints them, not a verbatim copy of that artifact, which is not stored in this tree."
  - id: c3
    text: "The test comes off .config/flaky-allowlist.txt, and the entry is DELETED rather than renewed."
    state: met
    evidence: "absent:.config/flaky-allowlist.txt::quarantine_child_has_no_console_at_creation_on_windows"
    owner: core
    note: "NOT DONE HERE, and deliberately not done by this lane: a concurrent worker holds pending deletions in .config/flaky-allowlist.txt, so the line is REPORTED to the release owner for deletion rather than edited into a conflict. The entry believed discharged is the one at .config/flaky-allowlist.txt whose test is `wcore-cli::quarantine_console_authority_windows::quarantine_child_has_no_console_at_creation_on_windows  gh#434`, dated 2026-09-20, which ends `REMOVE when FerroxLabs/wayland-core#434 lands; do NOT renew.` WHY IT IS DISCHARGEABLE AND NOT MERELY GREEN: the assertion that flaked -- `assert_eq!(field(&hardened, \"SHARES_USER_CONSOLE_AFTER\"), Some(\"true\"))` -- no longer exists, and the condition that produced it now takes a documented skip, PROVEN by injecting that exact condition on real Windows (c1). This is a structural argument, not the SeanDesktop green: that host cannot reproduce the failure, and its 10/10 is refused as a rate. WHAT WOULD FALSIFY THIS ROW BEING HONEST: the line being renewed at its 2026-09-20 expiry instead of deleted, or being deleted while c1/c2 were still not-met. MET at integration 2026-09-10. The entry is DELETED from .config/flaky-allowlist.txt, not renewed, and anchored with `absent:` so re-listing it under any tag reds the gate. IT IS DELETED ON THE RIGHT EVIDENCE: not on SeanDesktop's 10/10 green -- that box has an interactive session, which is exactly the condition the entry's own hypothesis says HIDES the failure, so its green was never counter-evidence -- but because the assertion that flaked, `assert_eq!(field(&hardened, "SHARES_USER_CONSOLE_AFTER"), Some("true"))`, NO LONGER EXISTS, and the condition that produced it was injected on real Windows and now takes a documented skip printing `PARENT_DONATED_ARM=QUIET ... this is NOT evidence that the #338 c2 residual closed`. A/B at 4f82ce8c1+injection FAIL exit 100 vs 5e585f88a+injection PASS exit 0. CONTROL: 26 dated entries remain, including line 75's `fix1_dispatch_budget_aborts_with_partial_result`, deliberately kept while #1301 c3 is open."
  - id: c4
    text: "NOT MEASURED, and recorded as such: the rate, and whether the _EXPLICIT arm ever moves."
    state: met
    evidence: "file:.planning/evidence/core-434-residual/rate-not-measured.txt"
    owner: core
    note: "MET at 509f4426b BY RECORD, which is exactly what this criterion asks -- it names itself NOT MEASURED. Both halves are written down. The RATE: observed ONCE, retried into a pass, across the twelve runs in which `report` failed continuously; not reproduced on SeanDesktop, and that green is explicitly refused as counter-evidence because that host has an interactive session, which is the condition the hypothesis says HIDES the failure. The SECOND HALF: the _EXPLICIT arm has not been observed to move in any stored artifact, which is the reason the escape is described as still open rather than as intermittent. WHAT WOULD FALSIFY THIS: a rate being measured, or the _EXPLICIT arm moving, without this row being re-graded. SUPERSEDED IN PART at 5e585f88a: c1 and c2 are now met, so the second half of this row (`the _EXPLICIT arm has not been observed to move`) is now what the pin is ANCHORED on rather than a spare observation, and it still has not been observed to move. The RATE remains NOT MEASURED and this row is unchanged on that. c3 stays not-met and the allowlist entry is still on .config/flaky-allowlist.txt, reported to the release owner rather than deleted by this lane. ANCHORED ON THE ALLOWLIST ROW, not on this file: .config/flaky-allowlist.txt:89 carries both halves where the gate reads them -- RATE NOT MEASURED, and the _EXPLICIT probe not moving. A self-anchor into this ledger is structurally impossible under the file: grammar, because the token text lands in the file it points at and the fragment then matches twice. RE-ANCHORED at integration 2026-09-10, and the reason is worth keeping: this row was anchored INSIDE the allowlist entry that c3 requires to be DELETED, so meeting c3 orphaned c4 -- the ledger gate caught it by name (`the evidence is gone, not merely moved`). Keeping a dead allowlist line alive purely as a ledger anchor is how allowlists rot, so the record moved to .planning/evidence/core-434-residual/rate-not-measured.txt instead, carrying BOTH halves verbatim. Nothing about the grading changed: the rate is still NOT MEASURED and the _EXPLICIT arm has still not been observed to move."
---

# A canary that fires in the safe-looking direction

The pin records a MEASUREMENT, not a correctness claim: that `DETACHED_PROCESS`
is weaker than `setsid`, and that #338 c2 is therefore satisfied by elimination
on unix ONLY. Its failure mode is the dangerous one, because the reading it
invites -- "the residual closed, re-grade c2 on Windows" -- is contradicted by
`SHARES_USER_CONSOLE_AFTER_EXPLICIT=true` in the very report it prints.

Nothing got safer. The child still reached the operator console; only the
probe that depends on the parent's console became unstable.

---
issue: 1207
repo: FerroxLabs/wayland
kind: defect
title: "#1166's ticket Defect 5 -- cache diagnostics off by default -- has no ledger criterion and was never graded"
status: open
last_verified_commit: f45e5bd83
criteria:
  - id: c1
    text: "A decision is recorded for compact.cache_diagnostics defaulting to false: on, off, or off with the reason stated"
    state: met
    evidence: "symbol:crates/wcore-config/src/compact.rs::cache_diagnostics_defaults_to_false"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D25, found while verifying wayland#1166). NOW CLOSED -- the disposition is at the end of this note. The measured finding as filed, verbatim: Ticket Defect 5 ('off by default', `compact.cache_diagnostics = false` at crates/wcore-config/src/compact.rs:643) is neither fixed nor graded. The ledger has five criteria but they cover only four of the ticket's five numbered defects — c5 is a control, not a defect. compact.rs:825 `cache_diagnostics_defaults_to_false` actively pins the flag off, so the three user-facing `emit_info` lines at engine.rs:15836/15842/15850 stay silent for a default install. CLOSED 2026-08-30 by lane/f13-misc. THE DECISION IS: off, with the reason stated, and the ticket's premise is narrower than it reads. `compact.cache_diagnostics` gates exactly three `emit_info` lines that print a cache verdict INTO THE CONVERSATION, and it stays off deliberately -- #101 filed those as alarming users over normal behaviour (a TtlExpiry after a pause is expected). The two DETECTING surfaces are live without it: the `cache_health_warn` tracing event (the CLI's default EnvFilter is `info`, so a warn! is emitted) and the ledger record (`recording_enabled` is on unless WAYLAND_CACHE_LEDGER is set). A default install therefore detects, attributes and persists; it just does not interrupt the chat. #1166 worded this defect as `Consider`, and the answer is a documented no with a guard that makes the no stick: a source lint brace-matching every `if self.compact_config.cache_diagnostics {` block in engine.rs and requiring that neither cache_health_warn nor recording_enabled sits inside one, with four controls against a vacuous pass (the gate must still exist, every extracted block must be 20-1000 bytes so a runaway brace match cannot swallow the file, every block must contain emit_info, and each probe string must still exist in engine.rs at all). RED ARM: `let _ = crate::cache_ledger::recording_enabled();` inserted inside the first gated block -- `recording_enabled moved inside `if self.compact_config.cache_diagnostics {`. A default install would stop detecting the break, which is ticket Defect 5 of #1166`. `cache_diagnostics_defaults_to_false` (wcore-config/src/compact.rs:825) still pins the flag off and is untouched: the flag's value is the decision, the lint is what it may not gate. MERGED 2026-08-30 into integ/f13 with lane/f13-regrade, which closed this criterion independently. Its ruling is kept verbatim, and its evidence token is the one carried on the row above, because this criterion's own sentence is `a decision is RECORDED ... on, off, or off with the reason stated` and `cache_diagnostics_defaults_to_false` is the item that pins that decision, whereas lane/f13-misc's `a_default_install_still_detects_and_records_a_cache_break` grades the neighbouring sentence (that the detection is not silent) and is carried on #1166 c6 where it belongs. Both lanes reached the SAME disposition -- the flag stays off by default -- so this is a choice of anchor, not of outcome. VERBATIM FROM lane/f13-regrade: CLOSED by RECORDING THE DECISION, which is literally all this criterion asks -- 'on, off, or off with the reason stated'. THE DECISION: compact.cache_diagnostics STAYS OFF by default. It is recorded here and, per c2, on wayland#1166 as c6, which is where a reader of #1166 will meet it. REASONS. (1) The three surfaces the flag gates are per-turn `emit_info` lines at crates/wcore-agent/src/engine.rs:15836/15842/15850, each inside `if self.compact_config.cache_diagnostics`; defaulting them on puts diagnostic output on every ordinary chat turn, and wayland#1150 ('Absurd Input Token Size') is open about exactly per-turn overhead, so turning them on cuts against live work. (2) The signal is not lost while the flag is off -- the ledger records by default, `recording_enabled()` returns true -- so cache health stays reconstructable after the fact. (3) The ticket itself worded this defect as 'Consider', which is the weakest of its five. THE CONSEQUENCE IS STATED, AND IT CORRECTS THIS TICKET'S OWN SEVERITY ARGUMENT -- which is the part worth having done, because the decision would otherwise have been taken on a false premise. #1207 argues the flag is low-severity because the cache_health_warn `tracing::warn!` at engine.rs:15877 'is not gated by the flag and the CLI default filter is EnvFilter::new(info)'. The filter half is TRUE and was verified at crates/wcore-cli/src/main.rs:1360, `EnvFilter::try_from_default_env().unwrap_or_else(|_| EnvFilter::new(\"info\"))`. But a filter decides what reaches the WRITER, and the writer is not the terminal in two of the three subscriber paths, which was read rather than assumed: with a TUI log file and `will_enter_tui`, everything goes to the file and the code's own comment reads `the alt-screen owns the terminal, so NOTHING may reach stdio -- not even an error`; headless with a log file tees `non_blocking.and(std::io::stderr.with_max_level(tracing::Level::ERROR))`, i.e. stderr takes ERROR ONLY. Only the third path -- no log file could be opened -- writes `warn!` to stderr. SO THE ACCEPTED STATE IS: with cache_diagnostics off, a default install surfaces cache-health problems in the LOG FILE and the LEDGER, not on the terminal. That is narrower than the ticket believed it was accepting, and it is accepted knowingly rather than by inheritance. ANCHORS RE-MEASURED: the pinning test is `cache_diagnostics_defaults_to_false` at compact.rs:857, not :825 as the ticket says (line drift); the field is declared at compact.rs:275 and defaulted false at :675; `toml_cache_diagnostics_override` at :863 proves an operator can turn it on, so this is a default and not a hard-coding. NOT CLAIMED: no code changed. This lane is zero-code by charter, and the criterion asks for a recorded decision, not a behaviour change. If a maintainer wants the reason to live next to the default as well, compact.rs:275 is where it goes."
  - id: c2
    text: "Whichever way it goes, the ledger entry for wayland#1166 carries a criterion covering it, so the ticket's fifth numbered defect is visible in the 'all criteria met' reading rather than absent from it"
    state: met
    evidence: "file:.planning/ledger/wayland-1166.md:46:is a recorded decision with its reason and its consequence stated"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D25). NOW CLOSED -- the disposition is at the end of this note. The original measurement is on c1 and the file:line anchors are in the prose below. CLOSED 2026-08-30 by lane/f13-misc. .planning/ledger/wayland-1166.md now carries c6, `Ticket Defect 5 -- the detection is not silent on a default install: only the chat-visible half is behind compact.cache_diagnostics`, graded with its own evidence and its own red arm. The ticket's fifth numbered defect is therefore inside the `all criteria met` reading of that file instead of absent from it, which is the whole of this criterion. MERGED 2026-08-30 into integ/f13 with lane/f13-regrade, which closed this same criterion independently and by a different route; its ruling is kept verbatim here so neither closure is lost. VERBATIM FROM lane/f13-regrade: CLOSED. `.planning/ledger/wayland-1166.md` now carries c6 at line 37: 'Ticket Defect 5 -- compact.cache_diagnostics defaulting to false -- is a recorded decision with its reason and its consequence stated, rather than an unexamined default with no criterion covering it', state `met`, evidence `symbol:crates/wcore-config/src/compact.rs::cache_diagnostics_defaults_to_false`, carrying the full decision and the warn!-visibility correction. That is exactly the shape the criterion demands -- Defect 5 is now VISIBLE IN the 'all criteria met' reading of #1166 rather than absent from it, which is the drift this ticket was filed about: five criteria covering four of five numbered defects, with c5 a control rather than a defect. WHY c6 IS `met` RATHER THAN `not-met`: #1166 is `status: closed` with every other criterion met, and the ledger gate fails a file whose criteria disagree with the tracker in either direction, so a `not-met` c6 on a closed issue would have traded one bookkeeping defect for another. It is `met` because what it asserts -- that the default is a recorded decision with its reason -- became true when c1 above recorded it. EVIDENCE TOKEN CAVEAT, stated rather than hidden: `file:<path>:<line>` is the weakest anchor the schema offers and the schema says so. It is used here because the property is the PRESENCE of a criterion in a markdown ledger file, and the token vocabulary has no `contains:` form -- only `absent:`, which is the wrong polarity. A reader checking this should read wayland-1166.md c6, not trust the line number. RECONCILIATION: both lanes added a criterion to wayland-1166.md for ticket Defect 5 and both are kept -- lane/f13-misc's as #1166 c6 (behavioural: a default install still detects and records) and lane/f13-regrade's as #1166 c7 (the default is a recorded decision with its reason stated). The regrade lane's evidence token `file:.planning/ledger/wayland-1166.md:37` is NOT carried across as written: the keystone merge that landed immediately before this one closed wayland#1198 and made a BARE file:<path>:<line> a refused token, and that line number had already moved. Its INTENT -- point at the criterion itself, not merely at the file -- is kept, in the format #1198 now mandates: a content anchor on c7's own text. That is strictly stronger than the plain-path token this side carried."
---

Ticket Defect 5 ('off by default', `compact.cache_diagnostics = false` at crates/wcore-config/src/compact.rs:643) is neither fixed nor graded. The ledger has five criteria but they cover only four of the ticket's five numbered defects — c5 is a control, not a defect. compact.rs:825 `cache_diagnostics_defaults_to_false` actively pins the flag off, so the three user-facing `emit_info` lines at engine.rs:15836/15842/15850 stay silent for a default install.

**Where.** crates/wcore-config/src/compact.rs:643 and :825; .planning/ledger/wayland-1166.md (no criterion for ticket Defect 5)

**Why it matters.** Lower severity than it first reads — I checked the other two surfaces and both ARE live by default: the `cache_health_warn` tracing::warn! at engine.rs:15877 is not gated by the flag and the CLI default filter is EnvFilter::new('info') (wcore-cli/src/main.rs:1360), and the ledger records by default (recording_enabled() → true). The ticket also worded this one as 'Consider'. But it is a ticket-listed defect with no criterion at all, which is exactly the ledger-vs-ticket drift that lets a partial grade as done; a closer reading the ledger alone would never see it.

Criteria are taken verbatim from the issue's Acceptance section.

All of them are now met, closed by lane/f13-misc on 2026-08-30 alongside the
ticket they were found under. Each criterion's note carries its own evidence
and its own red arm. The GitHub issue is left OPEN deliberately: closing an
issue is not this lane's action.

---
issue: 1352
repo: FerroxLabs/wayland
kind: defect
title: "ACP serve cancels a keeping-up 16 MiB reader with 'protocol relay overloaded' under concurrency"
status: open
last_verified_commit: 853ef9ee7
criteria:
  - id: c1
    text: "The cause of the cancellation is NAMED with the instrument that named it, and a red test reproduces it deterministically -- not only under soak contention."
    state: met
    evidence: "test:crates/wcore-acp/src/server.rs::another_sessions_event_log_work_does_not_stall_this_sessions_turn"
    owner: core
    note: "MET 2026-09-10, lane w15/relay1352, host hetzner-dsm; evidence .planning/evidence/w15-1352/README.md. The filing hypothesis (an aggregate budget held by other sessions' slow or disconnected readers) is REFUTED. INSTRUMENT 1, bpftrace v0.20.2 uprobes on the unmodified base-debug binary sha256 d0819d10b693ee1c6420041ca3d2ba4c54bc7a15121b22e74a4fe20b5f8d9cb0 (source 008d88ce9): on the reproducing arm (8 fast 16 MiB readers and NO slow or disconnected readers, 0/16 and 0/8 pass) the direct caller of Sender::overload, read from the return address, was send_retained in 8/8 -- the turn's cumulative one-second budget spent, never a refused retain() -- and bounded::global::USED read 8,412,674 bytes at the first cancellation: eight full 1 MiB stage-1 channels of a 64 MiB aggregate. INSTRUMENT 2, per-event timers on the diag binary sha256 e5566d15e77bc0e3154e8324f79a8028796ed471a1b1c5bf05a5fd9de8efc212 (branch w15/relay1352-diag, 784419c4b, never merge): each session's ACP recorder waited 1,116-1,630 ms for the process-wide AcpServer.events WRITE lock at concurrency 8 against 4-52 ms at concurrency 1, and each delivery task 1,115-1,631 ms for its READ side, while the engine logged 951-1,123 stage-1 waits totalling 1 s. NAMED CAUSE: one process-wide RwLock over every session's event log, taken per event by every recorder (re-encoding inside it) and every delivery task (encoding and cloning inside it); a recorder queued behind other sessions falls behind its own engine, whose send_retained spends the budget. The HTTP reader plays no part. It is about ten times worse on debug; base-release sha256 9b2e24f2 passes the same arms (c8 16/16, c32 32/32). RED TEST committed first at 1e824da3b and run there before any repair: FAIL at 5.012 s, remote_exit 100. NOT CLAIMED: that this lock was the only cross-session shared point; and (review MINOR 5) that this test alone guards the repair -- it runs on one thread and holds the map only for READ, so it cannot see a return to per-event map reads. crates/wcore-acp/src/server/eviction_tests.rs::an_exclusive_hold_on_the_log_map_does_not_stall_a_running_turn (multi-thread, map held for WRITE mid-turn) covers that and FAILS on red arm 82212a446."
  - id: c2
    text: "After the repair a reader that keeps up is never cancelled because of OTHER sessions' readers, shown by a base-vs-fix A/B through the real acp serve with fast 16 MiB readers at concurrency 8 and 32 mixed with slow and disconnected readers, n>=30 fast rows per arm, 0 relay cancellations on the fix -- and delivery stays BOUNDED, with peak and quiescent RSS recorded on both arms and no bound raised without a measured justification."
    state: met
    evidence: "test:crates/wcore-acp/tests/stabilization_backpressure.rs::a_reader_that_stops_reading_is_detached_by_its_wait_budget_beside_a_fast_one"
    owner: core
    note: "SECOND REVIEW of f7f6b96d8 addressed 2026-09-11 on branch w15/stage2-1356, host hetzner-dsm; evidence .planning/evidence/w15-1352/README.md section Second review round, receipts in review2/. MAJOR 1 (concurrent evictors over-trimmed the largest log) forced by crates/wcore-acp/src/server/eviction_tests.rs::evictors_parked_on_one_victim_trim_it_only_as_far_as_the_cap_needs -- four evictors parked after choosing the same victim, released together: FAILS on the unrepaired code at f3b6ef72e (the largest log trimmed 4 events where one was enough), passes at f237dcf2b (the cap re-checked under the victim's lock), FAILS again on red arm 6889d95b4. MINOR 2: the evict_for_append doc no longer claims a call never frees more than its append added (it can free up to one event more). MINOR 3: the release clamp saturates inside the fetch_update closure, no racy store(0), and is documented as a last resort that only catches drift larger than the whole total, not a drift detector. MINOR 4: a DeliveryBudget seam (bf69d6ff3); production keeps the single process-wide budget and the free functions, heavy wcore-acp tests opt into isolated budgets, and exhausting_an_isolated_budget_leaves_other_budgets_untouched shows they are separate; cargo test -p wcore-acp --lib in one process ran 5/5 at 145/145 before the seam and 3/3 at 147/147 after it, so this is isolation by construction, not a measured flake fixed. Gates at bf69d6ff3: wcore-acp nextest 179/180, the one failure being the committed wayland#1356 c1 red test from e3adb7301 (a separate ticket, red by design until its repair); clippy -D warnings clean on Linux and on x86_64-pc-windows-gnu; wcore-cli acp_engine 52/52. NOT CLAIMED: the acp serve A/B was NOT rerun after these fixes (held while the #1349 soak runs); MAJOR 1 is not shown to explain the churn arm's stage-2 detaches -- base 9b2e24f2, which has no such evictor, also detached 1/112, and the in-process instrument for wayland#1356 names the stage-2 positions window instead. EARLIER: MET 2026-09-11 after the review round, host hetzner-dsm; evidence .planning/evidence/w15-1352/README.md section Review round, receipts in review/. REPAIR AS IT WILL SHIP: 671109fa9 + af7e6d53c + f7f6b96d8 -- per-session log locks; every retained-byte change through SessionLog::mutate under that log's lock; a closed or failed-create log emptied by forget_log so an evictor that already chose it gives nothing back; checked subtraction (debug panic, release error log) instead of a silent wrap; eviction that pays only for its own append, with no serializing lock. NO BOUND CHANGED. BLOCKER forced by crates/wcore-acp/src/server/eviction_tests.rs::a_close_between_victim_choice_and_pop_keeps_retained_total_exact (cfg(test) gate between victim choice and pop): FAILS on the unrepaired code at 2f93f78b6 (counter 61,087,487 against true sum 61,349,662, one 256 KiB event charged twice), passes at f7f6b96d8, FAILS again on red arm 73bccf836. The evidence test isolates the one-second budget (4 MiB stalled turn under the per-log cap, paused time, whole history proven retained) and FAILS on red arm 0579423cf. Gates at f7f6b96d8: wcore-acp nextest 177/177, clippy -D warnings clean on Linux and on x86_64-pc-windows-gnu, wcore-cli acp_engine 52/52. RERUN A/B through acp serve, release: fix2-release sha256 b08ee6d7e00dad1b984719fab7c37f7a83fc95d346f1b6802f7b76a6aff74eb9 against base-release 9b2e24f2; standard mixed-reader schedule c8 32/32 and c32 32/32 fast rows on both, 0 stage-1 and 0 stage-2, slow and disconnected 64/64, peak RSS 4,503,220,224 base and 4,552,253,440 fix, quiescent 74,813,440 to 116,445,184 base and 74,018,816 to 109,060,096 fix. CHURN ARM (mixed 512 KiB then 4 KiB chunks, history over the cap while sessions close): 0 stage-1 on base, on pre-review fix d73cb623 and on fix2; fast c32 rows detached at STAGE 2 (resume from retained event cursor) 1/112 base, 1/112 fix1, 4/112 fix2, every one at or just before the switch to small events -- a count-bound detach that also occurs on base, path not named, rate difference not established (Fisher p about 0.4), absent from every 32 KiB row. The 64 MiB cap is stated with bounds, not as exact: over by at most the appends in flight, under by at most one event per concurrent evictor, at or under it once appends stop. NOT CLAIMED: that the stage-2 count-bound detach at mixed chunk sizes is harmless or unaffected by the repair (it needs its own decision); live drift absence (the counter cannot be read from a live process; the forced test is the proof); that no load can ever cancel a keeping-up reader at stage 1; anything about the #1349 soak, which was not rerun. HISTORY: REOPENED 2026-09-10 by adversarial review of d25526a37; the repair as graded cannot ship. BLOCKER: a session close racing an eviction double-subtracts retained_total (evict_to_cap snapshots its victim, close subtracts the whole log, the evictor then pops the orphan and subtracts again), so the counter drifts low and can wrap, after which every append evicts every log and every turn is detached at its first event. MAJOR: the guard test a_stalled_reader_is_still_detached_while_another_sessions_reader_keeps_up passes with the one-second budget removed, because its 16 MiB stalled turn exceeds the 8 MiB per-log cap and detaches through a replay gap instead. MINOR: the 64 MiB cap is not exact under concurrent recorders; above the cap the eviction mutex, map read and full log scan serialize recorders again; the c1 red test cannot see a return to per-event map reads. The A/B below is kept as measured on 671109fa9 but does not grade what will ship. PRIOR NOTE: MET 2026-09-10, host hetzner-dsm; evidence .planning/evidence/w15-1352/README.md and c2/. REPAIR 671109fa9: each session's event log has its own lock, the recorder and delivery task resolve it once per turn, delivery charges the size already recorded, and the 64 MiB history cap stays exact through an atomic total and a serialized evictor. NO BOUND CHANGED. A/B through acp serve, driver instruments/w15-relay1352-mixed.py sha256 16a408541bdaf8ad74f24503b03618bd6c15549344b22a34e2c6c797ecaf6a53, c8 = 4 fast + 2 slow + 2 disconnected and c32 = 16 fast + 8 slow + 8 disconnected, all 16 MiB, require_durability true, 32 fast rows per concurrency per arm. base-debug d0819d10: c8 17/32 with 15 relay cancellations, c32 1/32 with 31. fix-debug 70f44e05cb52d5484eb4d6f94a8ff951165e8bccea3feab12baaab7f812372ec: c8 32/32, c32 32/32, 0 cancellations. base-release 9b2e24f2 and fix-release d73cb6232e17429c4b3fa0c9def9ef3411587f50b2965eae5f6b669124f9d6e1: 64/64 each, 0 cancellations. Slow and disconnected rows 64/64 on every arm; stage-2 overloads 0. PEAK RSS base-debug 1,647,243,264, fix-debug 5,117,382,656 (completed work: its c32 batches delivered 256 MiB each where base cancelled at 23-41 MiB), base-release 4,280,897,536, fix-release 4,292,141,056. QUIESCENT first to last base-debug 161,906,688 to 208,392,192, fix-debug 159,805,440 to 213,221,376, base-release 73,490,432 to 109,867,008, fix-release 76,595,200 to 108,220,416. RED ARM 98269e4bd (branch w15/relay1352-redarm-lock, never merge) restores a per-event process-wide lock and the c1 test FAILS at 5.011 s. The evidence test keeps a stalled reader in another session detached. wcore-acp nextest 174/174 and clippy -D warnings clean; wcore-cli acp_engine 52/52. NOT CLAIMED: that no load can ever cancel a keeping-up reader (the one-second budget, host CPU, the global capacity notifier and the aggregate and eviction mutexes are still shared); that release base cancels under this schedule (it did not); anything about the #1349 soak, which was not rerun."
  - id: c3
    text: "The 16 MiB concurrency-1 fast-turn latency (median ~21.7 s on this tree against ~2.6 s at 3530199fb) is measured on both sources at n>=10, then either repaired or explained by a named cause."
    state: met
    evidence: "file:.planning/evidence/w15-1352/c3/c3-latency.md"
    owner: core
    note: "MET 2026-09-10 by a NAMED CAUSE, not a repair. Host hetzner-dsm, driver w15-leak2-probe6.py sha256 ba74608b, concurrency 1, 16 MiB, require_durability true, arms interleaved, n=10 per arm, every row passing. RUN 1: base-debug d0819d10 (008d88ce9) median 22,405 ms; base-release 9b2e24f2 (cd85dac88) 3,754 ms; 3530199fb release 0f3d81cf47b0c04fb955fe8ffc4d3673b5f654cf8a30bd620a786400bc4e4085 1,892 ms. RUN 2: base-release 1,946 ms; the same binary with glibc.malloc.trim_threshold raised 1,962 ms; 3530199fb 1,754 ms. CAUSE: the ~21.7 s figure came from a debug build (cargo build --bin wayland-core, as the w15/leak2 lane built it) and the ~2.6 s figure from the soak's release-voice build; the same code takes 22.4 s in debug and 1.9-3.8 s in release. The relay is not the time: at concurrency 1 the stage-1 channel never made the engine wait. RULED OUT BY MEASUREMENT: allocator.rs's fixed mmap threshold makes the release base issue 22,637 brk calls against 17 at 3530199fb (strace), but removing them left the median unchanged. The c1 fix does not change it either (fix-debug 21.9-23.3 s, n=3). NOT CLAIMED: a cause for the residual difference between the two release sources (ratio 1.98 in run 1, 1.11 in run 2); the 3530199fb rebuild is not byte-identical to the soak's c68855d5 binary."
  - id: c4
    text: "wayland#1349 c2 names this issue as the carrier for its soak abort."
    state: met
    evidence: "file:.planning/ledger/wayland-1349.md:19:FerroxLabs/wayland#1352"
    owner: core
    note: "MET 2026-09-10 at filing: #1349 c2's note now names FerroxLabs/wayland#1352 as the carrier. Both trackers were searched open and closed for 'relay overloaded', 'overloaded' and 'relay in:title' before filing, with zero relevant hits; a control query ('single-flight') in the same session returned 3, so the zero means absence rather than a broken query."
---

Created 2026-09-10 at filing time, not retroactively -- the wayland#1272 c2
lesson. Found by the w15/leak2 lane when the fresh #1349 soak aborted on a
functional failure before it could reach an RSS verdict.

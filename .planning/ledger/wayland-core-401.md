---
issue: 401
repo: FerroxLabs/wayland-core
kind: defect
title: "an_unknown_window_sizes_the_skill_listing precondition cannot pass in a clean container, so every integ/f13 CI run is red"
status: open
last_verified_commit: 6342f1b90
criteria:
  - id: c1
    text: "The test passes in a clean container (no ambient non-bundled skills) and on a host with a populated skills catalogue, without either being special-cased — shown by running it in both."
    state: not-met
    owner: core
    note: "RE-GRADED 2026-09-10 against 335328967 and STAYS NOT-MET, on the same sentence as before and NOT on the same evidence: c1 says `shown by running it in BOTH`, and the populated-host arm still has no run. WHAT CHANGED UNDER IT. The skills-activation lane (876e5a39a `feat(skills): gate the boot listing behind Skill discovery`, then 0e6cccd7d / f4d75fa40 / 7867a8a3f / c16983be0) took the per-skill listing OUT of the boot prompt entirely -- segment 0 now carries the fixed context::SKILL_DISCOVERY_SECTION and names no installed skill -- so the test no longer reads a budget off engine.system_prompt() at all. boot() now returns format_skills_section(late_mcp.catalog().visible(), engine.known_context_window()), which is the renderer the two surviving production call sites use. THE PRECONDITION WAS VACUOUS AND IS NOT ANY MORE, and that matters more than the plumbing: the marker `issue-1150` is also the first two segments of UNLISTED_MODEL, which the prompt's intro states on every session, so `the planted skill reached the listing in BOTH arms`, read off the PROMPT, passed on the model id -- an 80-character budget cannot hold one entry, so the tight arm never contained the planted skill. Read off the LISTING the model id cannot appear, and the honest form is asymmetric: the roomy arm HOLDS the skill (plus ISSUE_1150_DESCRIPTION_MARKER in full), the tight arm DECLARES it dropped one via SKILL_OVERFLOW_HINT. That is a strictly stronger precondition than the one the previous met grade rested on, which is why the old clean-arm receipt at febdf0a1e does not carry forward. CLEAN-CONTAINER ARM, RE-RUN AT THE GRADED TREE rather than asserted: `WCORE_PROOF_SLOT=parallel-1 python3 tools/remote-proof.py worktrees/w15-gateB nextest run --locked -p wcore-agent --test issue_1150_unknown_context_window_test --retries 0 --no-tests=fail`, source 3353289677cbf37b75bb8c6e63a8dddc96b26eb0 / tree d552cadc5a951341b07e359dcb7d795e495a0639, remote_exit 0, `10 tests run: 10 passed, 0 skipped`, including an_unknown_window_sizes_the_skill_listing_like_the_window_it_assumes and the_verdict_does_not_move_when_the_host_catalogue_grows. The harness exports a per-nonce WAYLAND_HOME and wcore_config::config::wayland_config_dir reads WAYLAND_HOME FIRST, so user_skills_dir() resolves into an empty fresh directory -- the ambient non-bundled catalogue is empty BY CONSTRUCTION, not merely on a clean-looking machine. EXACTLY WHAT IS STILL OWED, unchanged in substance: one execution of `cargo nextest run -p wcore-agent --test issue_1150_unknown_context_window_test --retries 0 --no-tests=fail` at 335328967 or a descendant, on a machine where $WAYLAND_HOME/skills (or, with WAYLAND_HOME and XDG_DATA_HOME unset, ~/.config/wayland-core/skills) contains at least one NON-BUNDLED skill, with that directory listed in the receipt. THE HARNESS CANNOT SUPPLY IT, re-checked at tools/remote-proof.py: it exports WAYLAND_HOME to a fresh per-nonce path, mkdir -p's it, forwards only a cargo argv and offers no hook to pre-populate that directory. Fabricating one -- a cargo target runner wrapper that seeds the directory, a nextest setup script, an in-test std::env::set_var -- would be the special-casing the criterion's own `without either being special-cased` clause forbids, so it was considered and rejected rather than quietly done. PREVIOUS: DOWNGRADED met -> not-met 2026-09-10 because the previous grade paired a clean-container run of one tree (CI 33637957153) with a hetzner pass of a DIFFERENT, pre-fix tree. The in-tree arm the_verdict_does_not_move_when_the_host_catalogue_grows plants 30 further non-bundled 400-character skills beside the fixture and asserts all three relations are unchanged; it is the property that makes a populated-host run predictable, held on every platform, and it is NOT offered as a substitute for the second environment. ORIGINAL: Transcribed from the issue body verbatim on 2026-08-31."
  - id: c2
    text: "The discriminating precondition is preserved, not deleted: whatever replaces it must still fail on a catalogue that renders no skills at all, shown RED by emptying the catalogue."
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs::an_unknown_window_sizes_the_skill_listing_like_the_window_it_assumes"
    owner: core
    note: "MET 2026-09-10 by the two RED arms this entry has been owing, executed on Linux at a committed tree rather than argued. GREEN CONTROL FIRST, so the reds below are the mutation and not a broken harness: source febdf0a1e5a40522af699d5ab0c4ea4761b5a659 / tree 9245e4cb3159bb9f541ed60ec44a273cb4a59989, `10 tests run: 10 passed, 0 skipped`. ARM A1 -- `FILLER_SKILLS: usize = 30` -> `0`, everything else untouched, committed as 4064ff663f1292b8c03f4dcef21df3d6b2e4d14b / tree 857329576ca6352ef3900f8c73dbf773943cc11f (retained on hetzner as refs/stabilization/proofs/4064ff663f1292b8c03f4dcef21df3d6b2e4d14b). `cargo check --locked -p wcore-agent --tests` exit 0 FIRST, so the red is behavioural: then `8 passed, 2 failed`, and the failure is THIS criterion's sentence, verbatim from the run -- `panicked at crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs:420:5: precondition: a 200,000-token window really does buy a longer listing here, or this test could pass on a catalogue with no skills in it (200k = 4798 bytes, unknown = 4798 bytes)`. Two arms rendering the IDENTICAL 4,798 bytes is the vacuity state the precondition exists to refuse. ARM A2 -- the ledger's own prescription, `FILLER_SKILLS = 0` AND the issue-1150 marker skill no longer planted, committed as e04b37dcf9ef0564aee0bfb9b6ac97e4d5923b5b / tree 180eb46abfeaa89fac974a861b5519e751168907: `7 passed, 3 failed`, same precondition RED at 4,542 bytes both arms, plus `the_bootstrap_prompt_uses_the_real_window_derived_skill_budget`'s own precondition. Both mutants were `touch`ed after editing and the tree reset to febdf0a1e afterwards; `git status --porcelain` empty, HEAD back at febdf0a1e. THE LIMIT, stated rather than glossed: what was emptied is the PLANTED catalogue. The BUNDLED skills still render (that is the 4,798 / 4,542 bytes both arms share), so `a catalogue that renders no skills at all` is approached and not reached -- no supported configuration removes the bundled catalogue, and the discriminating power the criterion is about is entirely the non-bundled overflow. The arms are scratch commits, per this entry's own instruction; they are not in the branch and only the remote refs above hold them. PREVIOUS: STAYS NOT-MET, and the reason is demonstration rather than substance: the property itself IS preserved in the tree at test file :420-431, which asserts a 200,000-token window really does buy a longer listing "or this test could pass on a catalogue with no skills in it". What is missing is a committed artifact recording the empty-catalogue RED arm, and that cannot be produced without a build. Settle on a scratch copy only: set FILLER_SKILLS = 0, delete the plant of the issue-1150 marker skill, and the test must FAIL on its precondition; restore and `touch` every restored file before re-running (mtime-restore trap). ORIGINAL: Transcribed from the issue body verbatim on 2026-08-31. This ledger did not exist until now: the issue was filed 2026-08-29/30 by this cycle's own verification and never entered the release gate, which counts only issues holding a ledger file. State is not-met because no lane has claimed it and nothing in the tree has been graded against this text. kind is defect, not task, because the gate reserves task for a credential, an account or a platform a human must obtain and there is code behind this one."
  - id: c3
    text: "The fixture supplies whatever the budget must bind against (e.g. a planted NON-bundled skill large enough that 1,310 characters truncates it), so the assertion does not depend on `$HOME`."
    state: met
    evidence: "file:crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs:141:const FILLER_DESC_LEN: usize = 400;"
    owner: core
    note: "MET at 6e4eca07. The fixture plants 30 non-bundled filler skills of 400 chars each into a tempdir bound via .extra_skill_dirs (test file :140-141, :153-166, :196), so the assertion no longer takes any discriminating power from $HOME. ORIGINAL: Transcribed from the issue body verbatim on 2026-08-31. This ledger did not exist until now: the issue was filed 2026-08-29/30 by this cycle's own verification and never entered the release gate, which counts only issues holding a ledger file. State is not-met because no lane has claimed it and nothing in the tree has been graded against this text. kind is defect, not task, because the gate reserves task for a credential, an account or a platform a human must obtain and there is code behind this one."
  - id: c4
    text: "A grep or a test proves no other assertion in `crates/wcore-agent/tests/` depends on the ambient skills catalogue for its discriminating power — the sibling comment shows this is a repeated shape, not one site."
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs::no_agent_test_grades_the_skills_budget_against_the_ambient_catalogue"
    owner: core
    note: "MET 2026-09-10. The static sweep this entry described is now a COMMITTED test, which is what the previous note said it would take. It walks crates/wcore-agent/tests recursively, classifies a source as grading the skills budget if it mentions get_char_budget / format_skills_section / format_skills_within_budget / SKILL_OVERFLOW_HINT, and requires each such file to supply its own catalogue via extra_skill_dirs, SkillCatalog::from_refs or build_for_test. BOTH DIRECTIONS ARE DRIVEN IN-PROCESS BEFORE THE SWEEP RUNS -- a synthetic violating source must classify as a violation, a synthetic isolated source over the SAME budget marker must not, and an unrelated source must be out of scope -- so a classifier that had stopped matching fails loudly instead of reporting a clean tree. ANTI-VACUITY: the walk asserts it read >= 100 sources (254 exist), that >= 4 were classified as grading the budget (4 are: this file, issue_1150_ordinary_turn_payload_test.rs, issue_1280_skills_ceiling_test.rs, late_mcp_bind_test.rs), and that this file is among them. GREEN: source febdf0a1e / tree 9245e4cb, PASS in 0.093s. RED ARM ON THE TREE ITSELF, not only on the synthetic controls: a scratch `crates/wcore-agent/tests/scratch_mutant_c_ambient_budget.rs` calling get_char_budget(None) with no isolation marker, committed as a2ba66b9fb82a36ece94c5d61c83847f7193564e / tree 8ad26532a522640eb96d9729d3c414627feba1f9, took the test to FAIL naming the file -- verbatim, `panicked at ...:723:5: these wcore-agent tests reason about the skills budget and never supply the catalogue they grade ...: [\"scratch_mutant_c_ambient_budget.rs\"]`. Reset to febdf0a1e afterwards, working tree clean. THE LIMIT: the sweep is marker-based and scoped, exactly as the repair note bounds it, to skills-BUDGET assertions in wcore-agent/tests. A test that renders a skills listing and asserts on it without naming any of the four symbols is outside what this proves, and it is not a general skills audit. PREVIOUS: STAYS NOT-MET. The sweep was run statically and came out clean, but it produces no machine-resolvable evidence token, and a green clean-container leg cannot distinguish "not ambient-dependent" from "vacuous there". Static sweep at 6e4eca07: `grep -rln extra_skill_dirs crates/wcore-agent/tests/` returns exactly issue_1150_unknown_context_window_test.rs, issue_1150_ordinary_turn_payload_test.rs and issue_1280_skills_ceiling_test.rs, each of which plants its own catalogue into a tempdir; the only other skills-budget grader, issue_1280_skills_ceiling_test.rs:386/:434/:460-468, builds SkillRefs in memory and calls format_skills_section directly. To make this a resolvable met, land a committed guard test and cite it with a `test:` token. ORIGINAL: Transcribed from the issue body verbatim on 2026-08-31. This ledger did not exist until now: the issue was filed 2026-08-29/30 by this cycle's own verification and never entered the release gate, which counts only issues holding a ledger file. State is not-met because no lane has claimed it and nothing in the tree has been graded against this text. kind is defect, not task, because the gate reserves task for a credential, an account or a platform a human must obtain and there is code behind this one."
  - id: c5
    text: "The property in c1 is still GRADED after the change, shown RED by a mutation that restores the 200,000 fabrication in `get_char_budget`'s `None` arm, with `cargo check -p wcore-agent --tests` exit 0 quoted so the red is a behaviour change and not a build failure."
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs::the_verdict_does_not_move_when_the_host_catalogue_grows"
    owner: core
    note: "MET 2026-09-10, and STILL A TRANSPLANTED CRITERION -- see the provenance paragraph kept below; closing it here is what lets FerroxLabs/wayland#1271 close without losing its one unique acceptance, and that record must not be dropped on a later pass. THE ARM, exactly as this entry prescribed it: crates/wcore-skills/src/prompt.rs `None => DEFAULT_CHAR_BUDGET,` -> `None => 8_000,` (one occurrence, asserted before editing), file touched, committed as 398e6c9141b95e3a6a99ba4357698a8e4cb7def1 / tree 7316ec50f85debd9bdf5a75d8a5be3c21ba25ec9. `cargo check --locked -p wcore-agent --tests` on that exact tree: remote_exit 0, `Finished dev profile` -- QUOTED, because the whole point of the criterion is that the red is a BEHAVIOUR change and not a build failure, and prompt.rs:31-33's compile-time assert constrains DEFAULT_CHAR_BUDGET's derivation rather than the None arm's literal, so it does compile. Then `10 tests run: 8 passed, 2 failed`: `an_unknown_window_sizes_the_skill_listing_like_the_window_it_assumes` FAILED and `the_verdict_does_not_move_when_the_host_catalogue_grows` FAILED at :583. THE EVIDENCE TOKEN IS THE SECOND ONE ON PURPOSE. c5 asks whether c1's property is still GRADED, and c1's property is that the verdict does not depend on the host catalogue; that is the test which holds it in-tree, and it reds under the fabrication mutation. The first test carries c2's precondition and is cited there, so the two criteria are anchored to two different instruments rather than one token doing double duty. Reset to febdf0a1e afterwards; git status clean, prompt.rs byte-identical to HEAD. LIMIT: a scratch commit, retained only as hetzner refs/stabilization/proofs/398e6c9141b95e3a6a99ba4357698a8e4cb7def1. PROVENANCE, unchanged: TRANSPLANTED VERBATIM from FerroxLabs/wayland#1271 c2 so that duplicate can close without losing its one unique criterion. #1271 and this issue are the same defect filed 84 minutes apart in two trackers, naming the same test, the same file and the same CI run 33291781675, with neither body nor comment referencing the other; this issue is the carrier because it is a superset. Settle on a scratch copy only: edit crates/wcore-skills/src/prompt.rs:57 `None => DEFAULT_CHAR_BUDGET,` to `None => 8_000,`, quote `cargo check -p wcore-agent --tests` exit 0, then the test must FAIL; restore and `touch` the file before re-running. The compile-time guard at prompt.rs:31-33 constrains DEFAULT_CHAR_BUDGET's derivation, not the `None` arm's literal, so this mutation does compile."
---

Created 2026-08-31 to close a COVERAGE gap. It records no work as done.

`scripts/check-criteria-ledger.py` scopes every open `area:core` issue on
wayland and EVERY open issue on wayland-core. This issue was in scope from
the moment it was filed and had no ledger file, so
`scripts/check-release-readiness.py` -- which reads ledger files and nothing
else -- could not count it. CI runs the coverage gate with `--offline`, the
arm that would have reported the gap, so nothing said so for two days.

Criteria are transcribed from the issue body without edit. Where the body's
wording is loose it is LEFT loose rather than tightened here: sharpening a
criterion inside the ledger is how a criterion quietly becomes an easier
adjacent property. Whoever takes this restates it on the ISSUE first.

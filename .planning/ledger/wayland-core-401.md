---
issue: 401
repo: FerroxLabs/wayland-core
kind: defect
title: "an_unknown_window_sizes_the_skill_listing precondition cannot pass in a clean container, so every integ/f13 CI run is red"
status: open
last_verified_commit: 5715b2b35
criteria:
  - id: c1
    text: "The test passes in a clean container (no ambient non-bundled skills) and on a host with a populated skills catalogue, without either being special-cased — shown by running it in both."
    state: met
    evidence: "symbol:crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs::listing_body"
    owner: core
    note: "MET 2026-09-10 (lane w15/skills401) after the run below found the populated-host arm RED and the fragile half was repaired. WHAT WAS ACTUALLY WRONG, and it was not the product. The two failing assertions were `unknown.contains(issue-1150)` -- the fixture's own planted skill must appear in the 1,310-character unknown-window listing. That is not a property the product owes. loader.rs:33 documents the priority order (bundled, MCP, user, project, additional, legacy); the fixture binds its catalogue through extra_skill_dirs, which is `additional`, fifth of six; and clamp_to_budget (prompt.rs:125-155) keeps entries in that order and stops at the budget. A host carrying more user-tier NAMES than the budget holds therefore drops the fixture, CORRECTLY, by the documented model. Measured: same skill count (79), 474 characters of names PASSES and 3,397 characters FAILS, against a 1,310-character budget. So the proxy was fragile; the property was untouched -- on the 84-skill ambient host both arms still derived different budgets and still truncated at them, and the ONLY thing that fired was the proxy. No product bug is filed for the ordering. THE REPAIR, which replaces the proxy rather than weakening it. Commit ec6c5d3b05146f2120a01aaed3be8721baef1fba, tree 6737155e8b37be767fc71c0f22bf3d1f680abe8e, touching only the test file. In BOTH `an_unknown_window_sizes_the_skill_listing_like_the_window_it_assumes` and `the_verdict_does_not_move_when_the_host_catalogue_grows`, `unknown.contains(issue-1150)` gives way to two claims that survive a competing higher-priority catalogue: (a) the fixture reaches the 8,000-character arm, where it has six times the room, so the comparison is still grading the planted catalogue and not the host; and (b) each arm renders INSIDE the budget its own window buys, asserted against the product's own `get_char_budget` rather than a copied constant, which is the derivation claim this file exists for. The new helper `listing_body` strips the `<system-reminder>` wrapper so the ceiling is measured on what the renderer measured. NO SPECIAL-CASING: no env sniffing, no catalogue-size branch, no cargo runner wrapper, no in-test set_var; the sole difference between the arms is what the host has installed. PROVEN IN BOTH ENVIRONMENTS, plus the in-binary control, at ec6c5d3b / tree 6737155e. CLEAN-CONTAINER ARM (WAYLAND_HOME pointed at an empty dir, the state remote-proof.py constructs by mkdir-ing a fresh per-nonce path): `cargo nextest run -p wcore-agent --test issue_1150_unknown_context_window_test --retries 0 --no-tests=fail` exit 0, `10 tests run: 10 passed, 0 skipped`. POPULATED-HOST ARM A ($WAYLAND_HOME/skills = /root/w15-skills401/populated/wayland-core/skills, 35 entries: w15-host-installed-000 through -029 at 400 characters of description each, plus the machine's own auto-code-refactor, auto-code-refactor-review, auto-everything-instructions-participant, auto-inbound-untrusted-wayland and auto-telemetry-zorbulate): exit 0, `10 tests run: 10 passed, 0 skipped`. POPULATED-HOST ARM B (WAYLAND_HOME and XDG_DATA_HOME BOTH UNSET, so the directory is /root/.config/wayland-core/skills with those same five non-bundled auto-drafted skills, and paths::wayland_home_skills_dirs adds /root/.wayland/skills, 25 top-level dirs and 79 skills after the recursive walk): exit 0, `10 tests run: 10 passed, 0 skipped`. This is the arm that was RED before the repair. THE LOADER SAW THE CATALOGUE IN EVERY ARM, proven from the PRODUCT rather than the filesystem: `wayland-core --skills-audit`, which calls wcore_skills::loader::load_catalog -- the same loader AgentBootstrap uses at bootstrap.rs:2264 (load_catalog_with_bundled, bare=false, so loader.rs:87 user_skills_dir() and loader.rs:100 wayland_home_skills_dirs() both run) -- reported `Total skills: 0` for the empty arm, `Total skills: 35` for arm A and `Total skills: 84` for arm B, naming each entry. The empty arm passing in the same binary is the control that the run itself is sound. THE TWO MET CRITERIA THIS EDIT COULD HAVE BROKEN WERE RE-RUN, NOT ASSUMED. c2's red arm (FILLER_SKILLS 30 -> 0, committed as ee1cf0b181786f0c165ca6401b95dd33977d9b08 / tree af9666f4dad1434fdbc3dd2144f4efd2ef502913, `cargo check --locked -p wcore-agent --tests` EXIT=0 first so the red is behaviour and not a build failure) still REDS the criterion's own test, verbatim: `panicked at crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs:546:5: precondition: a 200,000-token window really does buy a longer listing here, or this test could pass on a catalogue with no skills in it (200k = 254 bytes, unknown = 254 bytes)`. The non-vacuity guard was never the assertion that was replaced, and it is intact. c5's red arm (prompt.rs `None => DEFAULT_CHAR_BUDGET` -> `None => 8_000`, committed as fe978869bf98f3ced1b0edc68af4540a92353e09 / tree 8fcdc3d3b8f526fa14d15471df37ab53b018c774, `cargo check --locked -p wcore-agent --tests` EXIT=0) still REDS c5's own test and now on the NEW assertion, which is a sharper statement of the same defect: `panicked at ...:722:5: with a populated catalogue in play the unknown-window listing overran the budget its assumed window buys (7889 chars against 1310)`. Both mutants are scratch commits, retained only as hetzner refs/stabilization/proofs/; the slot was restored to ec6c5d3b with an empty `git status --porcelain` and every source `touch`ed after each checkout, because a restored file with an older mtime makes cargo skip the rebuild and the next run measures the mutant. GATE: `cargo clippy --locked -p wcore-agent --tests -- -D warnings` EXIT=0 and `cargo fmt --all -- --check` EXIT=0 on hetzner at ec6c5d3b -- run as CI runs it, because a bare clippy exits 0 where -D warnings does not. THE LIMIT, stated rather than glossed: arm B is one machine's catalogue, not every catalogue. A host carrying more than 8,000 characters of higher-priority skill NAMES would push the fixture out of the roomy arm too, and precondition (a) would fire. That is a far larger catalogue than the 84-skill host measured here, and unlike the old form it would be a true statement about the fixture rather than about the product. PREVIOUS: STAYS NOT-MET 2026-09-10 (lane w15/skills401), and for the first time on a MEASURED FAILURE rather than a missing run: the populated-host arm this entry has been owing was EXECUTED, and the test does NOT pass on a host with a populated skills catalogue. THE TREE: source 5715b2b352f605e031962f59456a9115a9734312 / tree 23efc79e45c948cc36b49328d59696a7d321665f, a descendant of 335328967 (`git merge-base --is-ancestor 335328967 5715b2b35` exit 0), verified committed-clean before AND after every run (`git diff --quiet HEAD --` plus an empty `git ls-files --others --exclude-standard`); nothing in the test, the fixture or the harness was modified, which is what `without either being special-cased` forbids. THE HARNESS STILL CANNOT SUPPLY THE ARM, re-read at tools/remote-proof.py rather than taken on faith: its remote script does `export WAYLAND_HOME=$root/test-homes/$nonce/wayland-core` then `mkdir -p $WAYLAND_HOME`, forwards only a cargo argv and offers no pre-populate hook, so user_skills_dir() resolves into an empty fresh directory BY CONSTRUCTION. The run below therefore drove cargo directly on hetzner, under the same slot flock and with the same env the harness sets, changing exactly one variable: the skills catalogue. ARM A -- POPULATED $WAYLAND_HOME -- PASSES. WAYLAND_HOME=/root/w15-skills401/populated/wayland-core; the DIRECTORY, listed rather than asserted, is /root/w15-skills401/populated/wayland-core/skills holding 35 entries: w15-host-installed-000 through w15-host-installed-029 (non-bundled, unique 400-character descriptions, the shape a developer box carries) plus the machine's own five auto-drafted skills auto-code-refactor, auto-code-refactor-review, auto-everything-instructions-participant, auto-inbound-untrusted-wayland and auto-telemetry-zorbulate. THE LOADER SAW THEM, not merely the filesystem: `wayland-core --skills-audit --skills-audit-stale-days 0`, which calls wcore_skills::loader::load_catalog -- the same loader AgentBootstrap uses at bootstrap.rs:2264 (load_catalog_with_bundled, bare=false, so loader.rs:87 user_skills_dir() and loader.rs:100 wayland_home_skills_dirs() both run) -- reported `Total skills: 35` and named every entry (`stale `w15-host-installed-016``, etc), against `Total skills: 0` for the empty WAYLAND_HOME the harness constructs. Then `cargo nextest run -p wcore-agent --test issue_1150_unknown_context_window_test --retries 0 --no-tests=fail`: exit 0, `10 tests run: 10 passed, 0 skipped`. ARM B -- WAYLAND_HOME AND XDG_DATA_HOME BOTH UNSET, this criterion's other sanctioned form -- FAILS. The directory is /root/.config/wayland-core/skills, holding five NON-bundled auto-drafted skills (auto-code-refactor, auto-code-refactor-review, auto-everything-instructions-participant, auto-inbound-untrusted-wayland, auto-telemetry-zorbulate), and paths::wayland_home_skills_dirs adds /root/.wayland/skills (25 top-level dirs; 79 skills after the recursive walk). Loader view: `Total skills: 84`. Same command, same binary, same tree: exit 100, `10 tests run: 8 passed, 2 failed, 0 skipped`. VERBATIM: `thread 'an_unknown_window_sizes_the_skill_listing_like_the_window_it_assumes' panicked at crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs:468:5: precondition: the planted skill never reached the unknown-window prompt` and `thread 'the_verdict_does_not_move_when_the_host_catalogue_grows' panicked at crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs:642:5: precondition: the fixture's own skill must still reach the prompt when a host catalogue is competing with it, or this arm grades the host instead`. Both are PRECONDITIONS, so the file is refusing to grade -- the same refusal #401 was opened for, in the opposite direction. ATTRIBUTION, GRADED RATHER THAN ARGUED. A four-arm sweep with WAYLAND_HOME SET in EVERY arm, so the env-resolution mechanism is identical and the only thing that moves is what the directory holds: n=0 PASS (10 passed), n=1 PASS -- one non-bundled skill, this criterion's literal minimum -- n=5 PASS (the machine's five auto-drafted skills), n=79 FAIL (a copy of /root/.wayland/skills) with the same two panics. The failure is the CATALOGUE, not the unset-env condition, and the n=0 arm passing in the same binary is the control proving the run itself is sound. MECHANISM, MEASURED NOT MODELLED. Two catalogues of the SAME count (79) and different total name width: 474 characters of names PASSES, 3,397 characters of names FAILS. get_char_budget(Some(32_768)) is 1,310 characters; prompt.rs level 4 is `clamp_to_budget(names, budget)`, which truncates the name-only listing in CATALOGUE order, and loader.rs loads user-tier ambient skills (steps 1 and 1b) BEFORE the project/extra dirs the fixture plants into. So once the ambient NAMES alone exceed the budget, the fixture's own entry can never appear however large its descriptions are. c3 gave the fixture its own OVERFLOW; it did not give it PRIORITY, and priority is the half #401 has not closed. WHY THIS IS NOT MET ON ARM A ALONE, since arm A and the n=1 arm do both pass a populated host: which populated catalogue is chosen decides the verdict, and choosing the favourable one IS the special-casing the sentence forbids -- an arm that only passes because the host's skill names happen to be short or few is the same host-dependence #401 was filed for, read the other way round. The catalogue that fails is the one the machine actually has. WHAT IS OWED NOW is therefore no longer a run. Tuning the host until it passes is exactly the special-casing this sentence forbids, so the repair belongs in the product or the fixture: the fixture's entries must survive the clamp regardless of what the host loaded before them (for instance by the listing reserving room for non-user-tier entries, or the fixture binding its catalogue at user tier). ARTIFACTS on hetzner at /root/w15-skills401/evidence/: nextest-populated.log, nextest-ambient.log, sweep-n00/n01/n05/n79.log, mech-short.log, mech-long.log, audit-empty.md, audit-populated.md, audit-ambient.md, populated-dir-listing.txt, ambient-dir-listing.txt. Drivers, byte-verified by sha256 on both ends before running: /root/w15-skills401-run.sh 6547153f2e500777fad2d004e6d919414d65738f849d6c9408efd7abdd3bdc62, /root/w15-skills401-sweep.sh e401f58b0b55beb4a7cad53376944062a294f5a88aaf8e3a7d96f01935eb239f, /root/w15-mech.sh 2cc898980aa269288902967f92e803c50164548fccbd343aa5c087420bd59745. PREVIOUS: RE-GRADED 2026-09-10 against 335328967 and STAYS NOT-MET, on the same sentence as before and NOT on the same evidence: c1 says `shown by running it in BOTH`, and the populated-host arm still has no run. WHAT CHANGED UNDER IT. The skills-activation lane (876e5a39a `feat(skills): gate the boot listing behind Skill discovery`, then 0e6cccd7d / f4d75fa40 / 7867a8a3f / c16983be0) took the per-skill listing OUT of the boot prompt entirely -- segment 0 now carries the fixed context::SKILL_DISCOVERY_SECTION and names no installed skill -- so the test no longer reads a budget off engine.system_prompt() at all. boot() now returns format_skills_section(late_mcp.catalog().visible(), engine.known_context_window()), which is the renderer the two surviving production call sites use. THE PRECONDITION WAS VACUOUS AND IS NOT ANY MORE, and that matters more than the plumbing: the marker `issue-1150` is also the first two segments of UNLISTED_MODEL, which the prompt's intro states on every session, so `the planted skill reached the listing in BOTH arms`, read off the PROMPT, passed on the model id -- an 80-character budget cannot hold one entry, so the tight arm never contained the planted skill. Read off the LISTING the model id cannot appear, and the honest form is asymmetric: the roomy arm HOLDS the skill (plus ISSUE_1150_DESCRIPTION_MARKER in full), the tight arm DECLARES it dropped one via SKILL_OVERFLOW_HINT. That is a strictly stronger precondition than the one the previous met grade rested on, which is why the old clean-arm receipt at febdf0a1e does not carry forward. CLEAN-CONTAINER ARM, RE-RUN AT THE GRADED TREE rather than asserted: `WCORE_PROOF_SLOT=parallel-1 python3 tools/remote-proof.py worktrees/w15-gateB nextest run --locked -p wcore-agent --test issue_1150_unknown_context_window_test --retries 0 --no-tests=fail`, source 3353289677cbf37b75bb8c6e63a8dddc96b26eb0 / tree d552cadc5a951341b07e359dcb7d795e495a0639, remote_exit 0, `10 tests run: 10 passed, 0 skipped`, including an_unknown_window_sizes_the_skill_listing_like_the_window_it_assumes and the_verdict_does_not_move_when_the_host_catalogue_grows. The harness exports a per-nonce WAYLAND_HOME and wcore_config::config::wayland_config_dir reads WAYLAND_HOME FIRST, so user_skills_dir() resolves into an empty fresh directory -- the ambient non-bundled catalogue is empty BY CONSTRUCTION, not merely on a clean-looking machine. EXACTLY WHAT IS STILL OWED, unchanged in substance: one execution of `cargo nextest run -p wcore-agent --test issue_1150_unknown_context_window_test --retries 0 --no-tests=fail` at 335328967 or a descendant, on a machine where $WAYLAND_HOME/skills (or, with WAYLAND_HOME and XDG_DATA_HOME unset, ~/.config/wayland-core/skills) contains at least one NON-BUNDLED skill, with that directory listed in the receipt. THE HARNESS CANNOT SUPPLY IT, re-checked at tools/remote-proof.py: it exports WAYLAND_HOME to a fresh per-nonce path, mkdir -p's it, forwards only a cargo argv and offers no hook to pre-populate that directory. Fabricating one -- a cargo target runner wrapper that seeds the directory, a nextest setup script, an in-test std::env::set_var -- would be the special-casing the criterion's own `without either being special-cased` clause forbids, so it was considered and rejected rather than quietly done. PREVIOUS: DOWNGRADED met -> not-met 2026-09-10 because the previous grade paired a clean-container run of one tree (CI 33637957153) with a hetzner pass of a DIFFERENT, pre-fix tree. The in-tree arm the_verdict_does_not_move_when_the_host_catalogue_grows plants 30 further non-bundled 400-character skills beside the fixture and asserts all three relations are unchanged; it is the property that makes a populated-host run predictable, held on every platform, and it is NOT offered as a substitute for the second environment. ORIGINAL: Transcribed from the issue body verbatim on 2026-08-31."
  - id: c2
    text: "The discriminating precondition is preserved, not deleted: whatever replaces it must still fail on a catalogue that renders no skills at all, shown RED by emptying the catalogue."
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs::an_unknown_window_sizes_the_skill_listing_like_the_window_it_assumes"
    owner: core
    note: "RE-CONFIRMED 2026-09-10 (lane w15/skills401) after the precondition beside this one was replaced. FerroxLabs/wayland-core#401 c1 repaired `an_unknown_window_sizes_the_skill_listing_like_the_window_it_assumes` -- this criterion's own evidence token -- at ec6c5d3b05146f2120a01aaed3be8721baef1fba, so the red arm was RE-RUN rather than assumed to carry over. FILLER_SKILLS 30 -> 0, committed as ee1cf0b181786f0c165ca6401b95dd33977d9b08 / tree af9666f4dad1434fdbc3dd2144f4efd2ef502913, `cargo check --locked -p wcore-agent --tests` EXIT=0 first: `10 tests run: 7 passed, 3 failed`, with this test RED on the same sentence as before -- `panicked at crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs:546:5: precondition: a 200,000-token window really does buy a longer listing here, or this test could pass on a catalogue with no skills in it (200k = 254 bytes, unknown = 254 bytes)`. What c1 replaced was the FIXTURE-REACH proxy, not this non-vacuity guard, so the discriminating power this criterion is about is untouched. PREVIOUS: MET 2026-09-10 by the two RED arms this entry has been owing, executed on Linux at a committed tree rather than argued. GREEN CONTROL FIRST, so the reds below are the mutation and not a broken harness: source febdf0a1e5a40522af699d5ab0c4ea4761b5a659 / tree 9245e4cb3159bb9f541ed60ec44a273cb4a59989, `10 tests run: 10 passed, 0 skipped`. ARM A1 -- `FILLER_SKILLS: usize = 30` -> `0`, everything else untouched, committed as 4064ff663f1292b8c03f4dcef21df3d6b2e4d14b / tree 857329576ca6352ef3900f8c73dbf773943cc11f (retained on hetzner as refs/stabilization/proofs/4064ff663f1292b8c03f4dcef21df3d6b2e4d14b). `cargo check --locked -p wcore-agent --tests` exit 0 FIRST, so the red is behavioural: then `8 passed, 2 failed`, and the failure is THIS criterion's sentence, verbatim from the run -- `panicked at crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs:420:5: precondition: a 200,000-token window really does buy a longer listing here, or this test could pass on a catalogue with no skills in it (200k = 4798 bytes, unknown = 4798 bytes)`. Two arms rendering the IDENTICAL 4,798 bytes is the vacuity state the precondition exists to refuse. ARM A2 -- the ledger's own prescription, `FILLER_SKILLS = 0` AND the issue-1150 marker skill no longer planted, committed as e04b37dcf9ef0564aee0bfb9b6ac97e4d5923b5b / tree 180eb46abfeaa89fac974a861b5519e751168907: `7 passed, 3 failed`, same precondition RED at 4,542 bytes both arms, plus `the_bootstrap_prompt_uses_the_real_window_derived_skill_budget`'s own precondition. Both mutants were `touch`ed after editing and the tree reset to febdf0a1e afterwards; `git status --porcelain` empty, HEAD back at febdf0a1e. THE LIMIT, stated rather than glossed: what was emptied is the PLANTED catalogue. The BUNDLED skills still render (that is the 4,798 / 4,542 bytes both arms share), so `a catalogue that renders no skills at all` is approached and not reached -- no supported configuration removes the bundled catalogue, and the discriminating power the criterion is about is entirely the non-bundled overflow. The arms are scratch commits, per this entry's own instruction; they are not in the branch and only the remote refs above hold them. PREVIOUS: STAYS NOT-MET, and the reason is demonstration rather than substance: the property itself IS preserved in the tree at test file :420-431, which asserts a 200,000-token window really does buy a longer listing "or this test could pass on a catalogue with no skills in it". What is missing is a committed artifact recording the empty-catalogue RED arm, and that cannot be produced without a build. Settle on a scratch copy only: set FILLER_SKILLS = 0, delete the plant of the issue-1150 marker skill, and the test must FAIL on its precondition; restore and `touch` every restored file before re-running (mtime-restore trap). ORIGINAL: Transcribed from the issue body verbatim on 2026-08-31. This ledger did not exist until now: the issue was filed 2026-08-29/30 by this cycle's own verification and never entered the release gate, which counts only issues holding a ledger file. State is not-met because no lane has claimed it and nothing in the tree has been graded against this text. kind is defect, not task, because the gate reserves task for a credential, an account or a platform a human must obtain and there is code behind this one."
  - id: c3
    text: "The fixture supplies whatever the budget must bind against (e.g. a planted NON-bundled skill large enough that 1,310 characters truncates it), so the assertion does not depend on `$HOME`."
    state: met
    evidence: "file:crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs:141:const FILLER_DESC_LEN: usize = 400;"
    owner: core
    note: "MET at 6e4eca07. The fixture plants 30 non-bundled filler skills of 400 chars each into a tempdir bound via .extra_skill_dirs (test file :140-141, :153-166, :196), so the assertion no longer takes any discriminating power from $HOME. ORIGINAL: Transcribed from the issue body verbatim on 2026-08-31. This ledger did not exist until now: the issue was filed 2026-08-29/30 by this cycle's own verification and never entered the release gate, which counts only issues holding a ledger file. State is not-met because no lane has claimed it and nothing in the tree has been graded against this text. kind is defect, not task, because the gate reserves task for a credential, an account or a platform a human must obtain and there is code behind this one."
  - id: c4
    text: "A grep or a test proves no other assertion in `crates/wcore-agent/tests/` depends on the ambient skills catalogue for its discriminating power — the sibling comment shows this is a repeated shape, not one site."
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs::no_agent_test_grades_the_skills_budget_against_the_ambient_catalogue"
    owner: core
    note: "MET 2026-09-10. The static sweep this entry described is now a COMMITTED test, which is what the previous note said it would take. It walks crates/wcore-agent/tests recursively, classifies a source as grading the skills budget if it mentions get_char_budget / format_skills_section / format_skills_within_budget / SKILL_OVERFLOW_HINT, and requires each such file to supply its own catalogue via extra_skill_dirs, SkillCatalog::from_refs or build_for_test. BOTH DIRECTIONS ARE DRIVEN IN-PROCESS BEFORE THE SWEEP RUNS -- a synthetic violating source must classify as a violation, a synthetic isolated source over the SAME budget marker must not, and an unrelated source must be out of scope -- so a classifier that had stopped matching fails loudly instead of reporting a clean tree. ANTI-VACUITY: the walk asserts it read >= 100 sources (254 exist), that >= 4 were classified as grading the budget (4 are: this file, issue_1150_ordinary_turn_payload_test.rs, issue_1280_skills_ceiling_test.rs, late_mcp_bind_test.rs), and that this file is among them. GREEN: source febdf0a1e / tree 9245e4cb, PASS in 0.093s. RED ARM ON THE TREE ITSELF, not only on the synthetic controls: a scratch `crates/wcore-agent/tests/scratch_mutant_c_ambient_budget.rs` calling get_char_budget(None) with no isolation marker, committed as a2ba66b9fb82a36ece94c5d61c83847f7193564e / tree 8ad26532a522640eb96d9729d3c414627feba1f9, took the test to FAIL naming the file -- verbatim, `panicked at ...:723:5: these wcore-agent tests reason about the skills budget and never supply the catalogue they grade ...: [\"scratch_mutant_c_ambient_budget.rs\"]`. Reset to febdf0a1e afterwards, working tree clean. THE LIMIT: the sweep is marker-based and scoped, exactly as the repair note bounds it, to skills-BUDGET assertions in wcore-agent/tests. A test that renders a skills listing and asserts on it without naming any of the four symbols is outside what this proves, and it is not a general skills audit. PREVIOUS: STAYS NOT-MET. The sweep was run statically and came out clean, but it produces no machine-resolvable evidence token, and a green clean-container leg cannot distinguish "not ambient-dependent" from "vacuous there". Static sweep at 6e4eca07: `grep -rln extra_skill_dirs crates/wcore-agent/tests/` returns exactly issue_1150_unknown_context_window_test.rs, issue_1150_ordinary_turn_payload_test.rs and issue_1280_skills_ceiling_test.rs, each of which plants its own catalogue into a tempdir; the only other skills-budget grader, issue_1280_skills_ceiling_test.rs:386/:434/:460-468, builds SkillRefs in memory and calls format_skills_section directly. To make this a resolvable met, land a committed guard test and cite it with a `test:` token. ORIGINAL: Transcribed from the issue body verbatim on 2026-08-31. This ledger did not exist until now: the issue was filed 2026-08-29/30 by this cycle's own verification and never entered the release gate, which counts only issues holding a ledger file. State is not-met because no lane has claimed it and nothing in the tree has been graded against this text. kind is defect, not task, because the gate reserves task for a credential, an account or a platform a human must obtain and there is code behind this one."
  - id: c5
    text: "The property in c1 is still GRADED after the change, shown RED by a mutation that restores the 200,000 fabrication in `get_char_budget`'s `None` arm, with `cargo check -p wcore-agent --tests` exit 0 quoted so the red is a behaviour change and not a build failure."
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs::the_verdict_does_not_move_when_the_host_catalogue_grows"
    owner: core
    note: "RE-CONFIRMED 2026-09-10 (lane w15/skills401). FerroxLabs/wayland-core#401 c1 repaired `the_verdict_does_not_move_when_the_host_catalogue_grows` -- this criterion's evidence token -- at ec6c5d3b05146f2120a01aaed3be8721baef1fba, so the fabrication mutation was RE-RUN against the new precondition. prompt.rs `None => DEFAULT_CHAR_BUDGET` -> `None => 8_000`, committed as fe978869bf98f3ced1b0edc68af4540a92353e09 / tree 8fcdc3d3b8f526fa14d15471df37ab53b018c774, `cargo check --locked -p wcore-agent --tests` EXIT=0 first: `10 tests run: 8 passed, 2 failed`, with this test RED -- `panicked at crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs:722:5: with a populated catalogue in play the unknown-window listing overran the budget its assumed window buys (7889 chars against 1310)`. The red now fires on the budget CEILING rather than on the identity, which names the defect more directly than the arm this entry first recorded: a session that hands the renderer a fabricated 200,000-token window renders 7,889 characters where its own assumed window buys 1,310. PREVIOUS: MET 2026-09-10, and STILL A TRANSPLANTED CRITERION -- see the provenance paragraph kept below; closing it here is what lets FerroxLabs/wayland#1271 close without losing its one unique acceptance, and that record must not be dropped on a later pass. THE ARM, exactly as this entry prescribed it: crates/wcore-skills/src/prompt.rs `None => DEFAULT_CHAR_BUDGET,` -> `None => 8_000,` (one occurrence, asserted before editing), file touched, committed as 398e6c9141b95e3a6a99ba4357698a8e4cb7def1 / tree 7316ec50f85debd9bdf5a75d8a5be3c21ba25ec9. `cargo check --locked -p wcore-agent --tests` on that exact tree: remote_exit 0, `Finished dev profile` -- QUOTED, because the whole point of the criterion is that the red is a BEHAVIOUR change and not a build failure, and prompt.rs:31-33's compile-time assert constrains DEFAULT_CHAR_BUDGET's derivation rather than the None arm's literal, so it does compile. Then `10 tests run: 8 passed, 2 failed`: `an_unknown_window_sizes_the_skill_listing_like_the_window_it_assumes` FAILED and `the_verdict_does_not_move_when_the_host_catalogue_grows` FAILED at :583. THE EVIDENCE TOKEN IS THE SECOND ONE ON PURPOSE. c5 asks whether c1's property is still GRADED, and c1's property is that the verdict does not depend on the host catalogue; that is the test which holds it in-tree, and it reds under the fabrication mutation. The first test carries c2's precondition and is cited there, so the two criteria are anchored to two different instruments rather than one token doing double duty. Reset to febdf0a1e afterwards; git status clean, prompt.rs byte-identical to HEAD. LIMIT: a scratch commit, retained only as hetzner refs/stabilization/proofs/398e6c9141b95e3a6a99ba4357698a8e4cb7def1. PROVENANCE, unchanged: TRANSPLANTED VERBATIM from FerroxLabs/wayland#1271 c2 so that duplicate can close without losing its one unique criterion. #1271 and this issue are the same defect filed 84 minutes apart in two trackers, naming the same test, the same file and the same CI run 33291781675, with neither body nor comment referencing the other; this issue is the carrier because it is a superset. Settle on a scratch copy only: edit crates/wcore-skills/src/prompt.rs:57 `None => DEFAULT_CHAR_BUDGET,` to `None => 8_000,`, quote `cargo check -p wcore-agent --tests` exit 0, then the test must FAIL; restore and `touch` the file before re-running. The compile-time guard at prompt.rs:31-33 constrains DEFAULT_CHAR_BUDGET's derivation, not the `None` arm's literal, so this mutation does compile."
---

Created 2026-08-31 to close a COVERAGE gap. It records no work as done.

`scripts/check-criteria-ledger.py` scopes every open `area:core` issue on
wayland and EVERY open issue on wayland-core. This issue was in scope from
the moment it was filed and had no ledger file, so
`scripts/check-release-readiness.py` -- which reads ledger files and nothing
else -- could not count it. CI runs the coverage gate with `--offline`, the
arm that would have reported the gap, so nothing said so for two days.

Criteria are transcribed from the issue body without edit. Where the body's
wording is loose it is LEFT loose rather than tightened here: sharpening a
criterion inside the ledger is how a criterion quietly becomes an easier
adjacent property. Whoever takes this restates it on the ISSUE first.

---
issue: 336
repo: FerroxLabs/wayland-core
kind: defect
title: "Flaky: harness_tui_flow narrow_terminal_resize_stays_coherent_without_panicking times out under parallel load"
status: open
last_verified_commit: 9de21aa1
criteria:
  - id: c1
    text: "PtyHarness::resize resizes the vt100 parser as well as the PTY master"
    state: met
    evidence: "symbol:crates/wcore-cli/tests/harness_tui_flow.rs::resize"
    owner: core
    note: "The body calls parser.set_size(rows, cols) before master.resize(). set_size rather than a fresh Parser, deliberately: a cleared grid breaks the diff-renderer and was measured to fail the chrome wait on a healthy binary."
  - id: c2
    text: "The post-resize predicate can only be satisfied by a frame that is actually 80 columns wide"
    state: met
    evidence: "symbol:crates/wcore-cli/tests/harness_tui_flow.rs::wait_for_tab_strip_reflow"
    owner: core
    note: "DEVIATION FROM THE WORDING, flagged. The discriminating predicate is at 140 columns, not 80: the test shrinks to 80 and asks the PROCESS about survival, then widens to 140 (past the 120 boot grid) and requires wait_for_width(140), so a glyph past column 120 cannot be pre-resize residue. 536fbfbe removed the post-shrink screen predicate because a healthy binary need not fully repaint at the intermediate width. widest_painted_row reads the GRID cell by cell rather than screen_text(), because vt100 contents() re-joins wrapped rows and would have passed against the defect. REFUTED 2026-08-29 by the 0.13.12 close-sweep, recorded verbatim: NOT MET AS WRITTEN, and the ledger says so itself. The criterion demands a post-resize predicate satisfiable only by a frame 'actually 80 columns wide'. No such predicate exists: after `h.resize(NARROW_COLS=80, …)` the test sleeps 300ms and asserts only `h.is_running()` (line 627). The discriminating predicate is `wait_for_width(140)` at line 663. Two things follow. (a) The substitution is HARDER, not easier, and the literal wording would have been VACUOUS: after `set_size` truncates the grid to 80, the surviving 120-column boot residue already fills col 79, so `widest_painted_row()==80` is trivially true with no repaint at all. So c2's own wording describes an unfalsifiable check and could not have been implemented as stated. (b) There is nonetheless a real, disclosed coverage loss: the pre-fix test asserted the chrome was present at the narrow width; it now asserts only that the process is alive there. A surface that corrupts at 80 columns and recovers on widening passes. The test name still promises 'stays_coherent'. Remainder is a ledger-text correction (restate c2 at 140 columns) plus, optionally, an owned decision on the narrow-width coherence gap — no engineering is blocked. DEVIATION REMOVED 2026-08-30 - the criterion now holds AS WRITTEN, at 80 columns. The previous grading, and the sweep note that accepted it, both rested on the claim that a literal 80-column predicate would be VACUOUS because set_size truncates and the 120-column boot residue already fills column 79. MEASURED, and that claim is FALSE: a probe comparing the SAME absolute columns 0..80 before and after the shrink shows 21 of 40 rows change within 500ms - the tab labels come back ellipsized (`Workspa…`, `Sub-Age…`), the transcript rule stops at column 78 where the 120-column frame ran past the edge, and the boot wordmark art is CLEARED, which truncation cannot do. The app fully re-lays-out at 80. Two instruments were genuinely dead ends and both are recorded in the source: widest_painted_row cannot express it, because vt100 Cell::has_contents is true for a cell the app painted a BACKGROUND into and this TUI paints one across the whole surface, so every row of every frame reaches the last column and the answer is the grid width by construction (measured: rows_painted_at_column returned all 40 rows at column 79 both before and after). And 'some row stopped putting ink on column 79' PASSED the c3 no-op mutation, because the hermetic boot emits a capability notice a beat after boot_to_workspace returns and that moves the column with no resize at all. What holds is a LAYOUT fact: the tab strip does not fit in 80 columns, so a frame laid out for 80 ellipsizes `Workspace`, while a 120-column frame truncated to 80 keeps it whole (it sits at columns 14..23, nowhere near the cut) and an undelivered resize leaves it whole. Absence of the full label and presence of the ellipsis are required TOGETHER, so a tab bar that simply vanished does not satisfy it. The disclosed coherence gap is closed with it: the frame is checked at 80 on three anchors that exist at that width (WAYLAND, `type / for commands`, `Tab next tab`) plus ink on column 79, so a surface that corrupts at 80 and recovers on widening no longer passes. RED ARM: PtyHarness::resize replaced by a no-op (fn resize(&mut self, _cols, _rows) { let _ = &self.master; }), diff read back so the mutation demonstrably landed on the two CODE statements; the test is RED twice at the SHRINK leg, harness_tui_flow.rs:467, verbatim `timed out after 30s waiting for the tab strip to be re-laid-out for the narrower frame: it still reads `  ◆ WAYLAND   Workspace   Sub-Agents   Plan   Config   Diagnostics   Workflows`, which is the layout for a WIDER terminal.` The SAME mutation against the previous shrink leg reddened only at the WIDEN leg (harness_tui_flow.rs:355, `reached 120 columns, never 140`), which is the proof that the shrink leg was not load-bearing before and is now. FLAKE RE-MEASURED because this re-introduces a screen wait there: N=48 healthy in two waves of 24 simultaneous invocations plus 3 known-positive controls per wave, hetzner-dsm, loadavg 143.39 -> 212.46 on 96 cores. `wave 1: healthy N=24 passed=24 failed=0 | known-positive control N=3 reddened=3 passed=0`, `wave 2: healthy N=24 passed=24 failed=0 | known-positive control N=3 reddened=3 passed=0`. Binaries identified by sha256, not asserted to differ: healthy 94a62f252cfba1b41e488f884c38afaba8a156d99d08a2d91c00d097cffb1d7f, control dd8937c789aed986e42566dc6bf2da30091389540ba4f1b6bc79467312035f10. RE-ARMED 2026-08-30 at the merged tip: the PtyHarness::resize body was replaced by 'let _ = (cols, rows, &self.master);' - the git diff was read back and shows the two executable statements (parser.set_size and master.resize) removed, not a comment - and narrow_terminal_resize_stays_coherent_without_panicking is RED on TRY 1 and TRY 2, at the SHRINK leg, harness_tui_flow.rs:468: 'timed out after 30s waiting for the tab strip to be re-laid-out for the narrower frame: it still reads `  WAYLAND   Workspace   Sub-Agents   Plan   Config   Diagnostics   Workflows`, which is the layout for a WIDER terminal. A frame actually narrow enough to need it would have ellipsized `Workspace`.' Restored, touched, re-run: 1 test run: 1 passed, 35 skipped in 1.236s."
  - id: c3
    text: "Making PtyHarness::resize a no-op turns the test red"
    state: met
    evidence: "symbol:crates/wcore-cli/tests/harness_tui_flow.rs::resize"
    owner: core
    note: "MUTATION ARM RUN, hetzner-dsm, 2026-08-29. The body of PtyHarness::resize was replaced with a no-op (fn resize(&mut self, _cols: u16, _rows: u16) { let _ = &self.master; }); the diff was read back before running, so the mutation demonstrably landed on the two CODE statements (parser.set_size(rows, cols) and self.master.resize(PtySize{..})) and not on the doc comment above them. Unmutated the test PASSES in 1.984s and 1.666s. Mutated it is RED, twice, verbatim: `thread 'narrow_terminal_resize_stays_coherent_without_panicking' (4068418) panicked at crates/wcore-cli/tests/harness_tui_flow.rs:356:9: / timed out after 30s waiting for the app to re-lay-out and paint out to the new 140-column edge: the widest painted row reached 120 columns, never 140.` / `FAIL [  30.941s] (1/1) wcore-cli::harness_tui_flow narrow_terminal_resize_stays_coherent_without_panicking` / `Summary [  30.952s] 1 test run: 0 passed, 1 failed, 3615 skipped` (the other run: FAIL [ 31.602s]). It reddens on the WIDEN observation, which is the discriminating one: the parser stays at the 120-column boot grid and no glyph can reach column 140. Nothing was written into the channel the PTY test observes -- the instrument is the mutation plus the test's own panic message, not an eprintln."
  - id: c4
    text: "The flake rate is re-measured at retries=0 over N of at least 20 with a known-positive control in the same run"
    state: met
    evidence: "test:crates/wcore-cli/tests/harness_tui_flow.rs::narrow_terminal_resize_stays_coherent_without_panicking"
    owner: core
    note: "MEASURED at retries=0, N=48 (two waves of 24), hetzner-dsm 2026-08-29, WITH A KNOWN-POSITIVE CONTROL IN EACH WAVE. Method: the test binary is invoked directly (libtest has no retry mechanism at all, so retries=0 is structural, not a flag), 24 healthy invocations launched simultaneously so they contend with each other, plus 3 invocations of a SECOND, byte-different build of the same binary whose PtyHarness::resize is the c3 no-op. Both binaries are identified by sha256 in the log, not merely asserted to differ: healthy e0e5cce2f38fff4b4a9476a0a0de1a337df369175f8920ac6e0d07441b946442, control ac6a9c34903839b01d038c79b80cb1092f1ae465cff5ce00fbe38b1307bd6634, and the control's log carries the c3 panic text verbatim. Wave 1 (loadavg 73.72 93.24 76.99 on 96 cores): `RESULT: healthy N=24 passed=24 failed=0   |   known-positive control N=3 reddened=3 passed=0`. Wave 2 (loadavg 68.64 89.14 76.37): identical. 48 of 48 green under a load average of 67-93; the reported rate was 1 in 6, and P(0 failures in 48 at p=1/6) = (5/6)^48 = 1.6e-4, so the reported rate is refuted rather than merely unobserved. AGGRAVATION FROM THE PREVIOUS PASS STANDS AND IS NOW HARMLESS: the test is still in no allowlist and has no nextest override, which at a measured 0/48 is the correct place for it."
---

The reported symptom is a PTY test that times out about one run in six under
parallel load, waiting up to five seconds after a terminal resize.

The root cause is not the budget. The harness resizes only the PTY master and
never tells the vt100 parser, so the parser keeps a stale 120-column grid for
the life of the test. Two separate defects sit on that one line: the wait
predicate is already true before the resize, so the test can pass without ever
observing one; and it can only time out when the app clears the grid and is slow
to repaint into a mis-sized parser, which is the flake.

Nothing masks this today. It is not in the flaky allowlist and it has no nextest
override, contrary to what the triage brief claimed, so it costs real CI reds
now. It is a single test file, cfg(unix), and fully verifiable on hetzner.

Criteria come from the cluster C verification note of 2026-08-29.

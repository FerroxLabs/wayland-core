---
issue: 1204
repo: FerroxLabs/wayland
kind: defect
title: "The #1162 error message ships with runs of literal spaces mid-sentence, and the same collapse appears in acp.rs and main.rs"
status: open
last_verified_commit: 782064d63
criteria:
  - id: c1
    text: "The message at cache_cmd.rs:486 and the sibling bail at :476 read as one sentence with single spaces between words"
    state: met
    evidence: "test:crates/wcore-cli/tests/cache_ledger_cli.rs::an_unknown_session_id_still_fails_and_says_where_to_look"
    owner: core
    note: "MET 2026-08-29 (soak-misc lane). Both bails are restored to backslash-newline continuations, so the rendered sentence carries single spaces: the :486 message the ticket names and the sibling at :476. 23 collapsed messages were repaired across the repo in the same pass, not only these two. RED ARM AT HEAD, hetzner-dsm: the :476 literal was mutated back to its collapsed 22-space form; the mutation was read back before running and asserted to be an executable anyhow::bail! argument at line 476 and not a comment (IS_COMMENT False); the gate then returned EXIT=1, verbatim `crates/wcore-cli/src/cache_cmd.rs:476: run(s) of 22 spaces mid-sentence`. Restored, gate EXIT=0, git status clean. ORIGINAL FILING PRESERVED: Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D22, found while verifying wayland#1162). Nothing has been done. The measured finding, verbatim: The user-facing error message that #1162 was specifically asked to improve ships with mangled inter-word whitespace: runs of ~10 and ~22 literal spaces mid-sentence. Observed verbatim from the built binary: `Error: no cache ledger for 'aa55aa55-0002' in <dir>. Ledgers are keyed by the engine's internal conversation id, not by the session id you set with --session-id; session 'aa55aa55-0002' is either unknown to the session store at <dir> or was recorded by a build that did not persist its conversation id. Run \`wayland-core cache list\` to see the ids that exist.` Confirmed literal in the source with `cat -A`, not a terminal artifact — the string is a single-line literal whose `\`-newline continuations were collapsed while keeping the source indentation. Wider pattern, same shape, at crates/wcore-cli/src/acp.rs:266 and crates/wcore-cli/src/main.rs:8616 and :8624."
  - id: c2
    text: "A test asserts the message SHAPE, not only that it contains 'cache list'"
    state: met
    evidence: "test:crates/wcore-cli/tests/cache_ledger_cli.rs::an_unknown_session_id_still_fails_and_says_where_to_look"
    owner: core
    note: "MET 2026-08-29 (soak-misc lane). The shape is pinned inside an_unknown_session_id_still_fails_and_says_where_to_look, which until now asserted only err.contains(cache list) -- a predicate the MANGLED text satisfies just as well, which is precisely why the defect shipped unnoticed. Two assertions were added: the exact clause `keyed by the engine s internal conversation id` spelled with single spaces, and a general per-line predicate that no line of the rendered error contains three consecutive spaces after trim_start, so a wrapped terminal line s leading indentation is not mistaken for the defect. The test drives the shipped binary, not the library, so it grades what the operator actually reads."
  - id: c3
    text: "The same collapse at acp.rs:266 and main.rs:8616/:8624 is fixed in the same pass, or a lint refuses a user-facing string literal containing three or more consecutive spaces"
    state: met
    evidence: "symbol:scripts/check-message-whitespace.py::offending_runs"
    owner: core
    note: "MET 2026-08-29 (soak-misc lane) by the FIRST disjunct, with the lint as belt-and-braces. The criterion is a disjunction and the fix half is satisfied outright: acp.rs:266 and main.rs:8616/:8624 are repaired in the same pass as cache_cmd.rs, along with 20 other sites. The lint half also exists -- scripts/check-message-whitespace.py, wired into the pure text-scan CI job at ci.yml:1461 (--self-test) and :1462 (the scan over crates) -- and it proves both directions itself: 12 self-test cases, 3 that MUST flag and 9 that MUST NOT, so it cannot rot into a checker that matches nothing. SELF-TEST OK: 12 cases at HEAD. TWO LIMITATIONS DISCLOSED RATHER THAN IMPLIED, because the lint is narrower than the criterion s fallback wording of three-or-more consecutive spaces. (1) It requires FOUR or more spaces, between two lowercase prose words, with at least three words already ahead of the run inside the same literal, and it skips any literal carrying an explicit newline or tab escape. Deliberate column padding is pervasive in this codebase and the literal three-space rule would have been unusable -- it would have flagged the aligned help output, the status tables and the key hints. (2) That narrowness left TWO real collapsed runs inside literals this same pass had already edited: a run sitting before a {} placeholder in engine.rs and a run after a sentence-ending period in main.rs, neither of which has a lowercase word on both sides. Both are assertion messages inside test modules, so nothing user-facing shipped carrying them, and both are now repaired by hand in commit `Repair the two collapsed runs my own whitespace pass left behind`. The gate was deliberately NOT widened to cover those two shapes, since doing so re-admits exactly the column padding the narrowing exists to allow. NAMED RESIDUAL: a collapsed run of those two shapes -- before a brace placeholder, or after a period -- can still enter the tree without this gate objecting. WIRING, GRADED SEPARATELY FROM THE FUNCTION: adding the gate to ci.yml is not the same as a lane being able to see it. The gate went into ci.yml but not into scripts/preflight.sh, whose drift guard then refused to run at all (exit 2, `ci.yml runs host-side gate(s) this pre-flight does NOT: scripts/check-message-whitespace.py`) -- so until it was registered, a lane could gate green on hetzner and still fail the required CI job on this very check. Registered in commit `Register the message-whitespace gate with the lane pre-flight`; pre-flight now reports `drift guard: ok (10 host-side gate script(s) in ci.yml, all covered)` and PREFLIGHT=0. CORRECTED 2026-08-30 after a verifier refutation, and the correction is the substantive one on this ticket. The previous note claimed the repair pass was verified to leave ZERO remaining collapses. That claim was FALSE, and the instrument that produced it was blind by construction: LITERAL matched a `"..."` pair on ONE SOURCE LINE, and a `\`-continued literal's continuation lines carry no quote at all, so every line after the first was never scanned -- the gate could not see the very shape its own docstring opens with. One collapse survived at HEAD in a literal this lane's own whitespace commit had rewritten: crates/wcore-protocol/src/contract/generate.rs:1507, a 14-space run in the approval_required diagnostic. It survived a SECOND, independent way as well, and repairing only one would have left it in place: the run sits between a backtick and an opening paren, and NEITHER character was in the boundary classes, so it was missed even considered as a single line. Both are closed. The scanner now walks the whole file, resolving continuations as rustc does -- backslash, newline and the next line's leading indent all vanish -- which forces comments, char literals, lifetimes and raw strings to be stepped over deliberately or the lexer desynchronises and silently stops grading; there is a self-test case for each of those four, each written to carry an UNBALANCED quote, because the first three I wrote were quote-balanced and therefore VACUOUS (deleting the skip changed nothing and the case still passed). A backtick is admitted as a left boundary, and an opening paren as a right boundary ONLY behind a backtick: widening the after-class unconditionally reported 15 literals of which 14 were deliberate column alignment (seven two-column install hints in doctor/mod.rs, seven SQL fixtures in wcore-memory), and both families are now quiet self-test cases. Measured over the tree: 15 hits unconditional, 1 with the narrowed pair, and that 1 is the real defect. END-TO-END RED ARM, hetzner-dsm: the 14-space form restored at generate.rs:1507 (read back -- line 1507, is_comment False, 14 spaces, inside the format! argument), then BOTH gates run on the SAME mutated tree -- the new gate EXIT=1 naming `crates/wcore-protocol/src/contract/generate.rs:1507: run(s) of 14 spaces mid-sentence`, the SHIPPED gate from this branch's parent EXIT=0 with `OK: no message literal carries collapsed continuation whitespace.` Restored, touched, EXIT=0, byte-identical. TEN mutation arms on the gate itself, each read back and confirmed to land on executable code and each reddening its named case and only it: drop the backtick/paren rule, drop the backtick left boundary, stop resolving continuations, stop stripping the continuation indent, stop stepping over raw strings, treat every apostrophe as a char literal, stop stepping over line comments, stop stepping over block comments, revert the root handling, and let a zero-file scan report OK. Suite is 36 green in both directions, restore byte-identical. THIRD FIX, same class of blindness: `check-message-whitespace.py <one file>` used to exit 0 with the defect present, because os.walk yields nothing for a file -- an empty result read as an absence. A file root is scanned now, and every remaining way of grading nothing (a missing root, a non-.rs file, a directory with no Rust in it) is a loud exit 2 rather than an OK line, with the file count printed on success; eight self-test cases build a temp tree and grade that root handling in both directions, which no line-level case could reach. FOURTH FIX, and it is the one that matters, because the third pass STILL left a defect of the very class it was widened for. Repairing generate.rs:1507 and calling the tree clean stopped one sentence short: generate.rs:1531 carries an EIGHTEEN-space run between a full stop and a backtick, in the same check_approval_correlation body, and it is not a continuation line at all -- it sits on one source line, so it was never a lexer problem. It survived because the left boundary class asked for a prose character and a full stop is not one, which is the FIRST HALF of the residual the previous revision of this note named and declined to close. A named residual with a live instance in it is a defect, not a disclosure. Found with an INDEPENDENT instrument rather than the gate under suspicion: a throwaway scanner that joins continuations and flags any 4+ space run between prose characters with no boundary narrowing at all, walked 2,052 .rs files with a known-positive control in the same call (resume_token present in 30 files), reported 48 candidate runs, and 47 of them are deliberate column alignment read by hand. The 48th is generate.rs:1531. THE FULL-STOP HALF OF THE RESIDUAL IS NOW CLOSED: `.`, `!` and `?` join the left boundary class. Measured before believing it -- the widening reports exactly ONE literal over the whole tree and that literal is the defect, so it costs nothing in false positives. THE OTHER HALF IS MEASURED AND DELIBERATELY LEFT OPEN, which is a different thing from being unexamined: admitting an opening brace on the right reports three literals and not one of them is a defect -- sandbox_cmd.rs's two-column status block, `enforces read deny        {}` and its two siblings -- so a run before a `{}` placeholder is still outside this gate and is still the named residual, now with a number attached. A SECOND, OLDER ERROR fell out of writing the column-padding case for the new boundary: the docstring promises at least three words ahead of a run before it counts as prose, and the count included the OPENING QUOTE of the literal, so the floor was really two and `\"  step 1.          initial import\"` was graded as prose. The quote is stripped before counting and both sides of that boundary are graded now. SIX NEW CASES AND FIVE RED ARMS, hetzner-dsm 2026-08-30, each mutation read back out of the file and confirmed to have landed on executable code and not a comment, each reddening its own case and only it: drop `.` from the left class (the two full-stop cases fail, the bang and question-mark cases do not), drop `!` and `?` (the converse), count the opening quote again (the leading-label case fails), raise the prose floor to four words (the three-word case fails), and re-collapse generate.rs:1531 to its eighteen-space form (the tree scan goes EXIT=1 naming `crates/wcore-protocol/src/contract/generate.rs:1531: run(s) of 18 spaces mid-sentence`). Every restore is byte-identical by sha256 and re-runs green; the suite is 44 cases green in both directions and the tree scan is EXIT=0 over 2,052 files. generate.rs is a SOURCE_INPUTS file, so the Desktop contract corpus is regenerated with wcore-desktop-contract-gen/22: schema_digest is UNCHANGED at sha256:47e255b800cb36390e975580e7d1cfa19c35bb43cb6ac71fa3e4efd55c22da6f, so no wire surface moved. Gate at this tree: preflight 0, fmt --check 0, clippy --workspace --all-targets --all-features -D warnings 0, wcore-protocol 430/430, wcore-cli 3704/3704, wcore-agent 3904/3904, wcore-tools 1814/1814, wcore-memory 515/515, wcore-cron 125/125, wcore-eval-scenarios 590/590. FIFTH FIX, AND THE THIRD RECURRENCE OF ONE ROOT CAUSE -- the left boundary class was too narrow, again. A FOURTH collapse sat at crates/wcore-agent/src/engine.rs:29776, THREE SOURCE LINES BELOW one this same lane had already repaired, in the same run of assert! statements inside mod audit_2026_05_22_tests, and a FIFTH at crates/wcore-config/src/config.rs:12074. Both survived for exactly one reason: the character to the left of the run is an EM DASH and not a semicolon. Same geometry as generate.rs :1507/:1531 one round earlier. TWO CLAIMS IN THE PREVIOUS REVISION OF THIS NOTE ARE HEREBY RETRACTED AS FALSE, not softened: (a) that the independent scanner flagged runs with NO boundary narrowing -- it narrowed to prose characters, which excludes the em dash, so it reproduced the gate's exact blind spot while being offered as the check on it; and (b) that the only remaining uncovered shape was a run before a {} placeholder. THE CLOSE, measured before it was believed: `.` `!` `?` are joined on the left by U+2014 EM DASH and U+2013 EN DASH, one character class. The widened gate over 2,052 files reports EXACTLY TWO literals and both are the defect above -- zero false positives -- and the 44 pre-existing self-test cases stay green under the widening. Both literals are repaired to `\\`-continuation form and the tree scan returns to EXIT=0. FOUR NEW SELF-TEST CASES, in BOTH directions, because a self-test with no failing-direction fixture is the vacuity this whole family is about: an em dash before a lowercase word MUST flag, an en dash before a lowercase word MUST flag, a CORRECTLY `\\`-continued em-dash sentence MUST stay quiet, and an em dash before an UPPER-CASE second column MUST stay quiet. RED ARM on the new class member, mutation read back out of the file and confirmed to land on the executable regex line and not the comment above it: strike U+2014/U+2013 from the class and exactly the two dash-flagging cases fail (SELF-TEST FAILED: 2 of 50) while the two dash-quiet cases and all 46 others stay green; restore and the suite is 50 green. SECOND RED ARM, end to end: re-collapse engine.rs:29776 to its 14-space form and the tree scan is EXIT=1 naming it; restore byte-identical by sha256 and EXIT=0. THIRD RED ARM, on the new blind-zone counter: make unreported_runs return 0 and the `column padding is counted as blind` case fails and only it. ROOT CAUSE ADDRESSED RATHER THAN THE FIFTH INSTANCE, and the honest form of that is a disclosure, not a completeness claim. The boundary classes ARE an allowlist and are deliberately not closed, so every SUCCESSFUL run now prints the size of its own blind zone -- `blind zone: 198 interior run(s) of 4+ spaces sit outside the boundary classes and were NOT graded` -- so that OK can no longer be read as `the tree is clean` by the next reader, which is how three rounds in a row went wrong. The blind zone was ENUMERATED with a third instrument that borrows only the lane's iter_literals lexer and applies no narrowing at all (2,052 files, known-positive control in the same call: resume_token in 30 files): 200 interior runs before the repair, 198 after, in 65 (left char, right char) pairs. Of those, exactly TWELVE have a PROSE character on the right and after the dash widening not one is a defect -- seven behind a `:` (the next:/bound:/retry: column separator in cron.rs, sandbox_cmd.rs and skill_govern.rs), three behind an upper-case status word in doctor/mod.rs, one behind a `|` in the TUI banner art, and `bundle        intact`, which the three-word floor holds out. Admitting `:` or an upper-case letter re-admits all eleven, which is the column padding the narrowing exists to allow. NAMED REMAINING GAPS on the left, so the next reader is not misled: `:`, upper-case letters, digits, `]`/`}`, and typographic quotes and ellipsis. The first two are excluded WITH MEASURED FALSE POSITIVES; the rest are absent from this tree today and must be admitted the same way the dash was -- measure first, then widen. engine.rs is a SOURCE_INPUTS file, so the Desktop contract corpus is regenerated with wcore-desktop-contract-gen/22; the ONLY bytes that moved in the corpus are fixture_digest and source_inputs_digest, and schema_digest is UNCHANGED at sha256:47e255b800cb36390e975580e7d1cfa19c35bb43cb6ac71fa3e4efd55c22da6f, so no wire surface moved."
---

The user-facing error message that #1162 was specifically asked to improve ships with mangled inter-word whitespace: runs of ~10 and ~22 literal spaces mid-sentence. Observed verbatim from the built binary: `Error: no cache ledger for 'aa55aa55-0002' in <dir>. Ledgers are keyed by the engine's internal conversation id, not by the session id you set with --session-id; session 'aa55aa55-0002' is either unknown to the session store at <dir> or was recorded by a build that did not persist its conversation id. Run \`wayland-core cache list\` to see the ids that exist.` Confirmed literal in the source with `cat -A`, not a terminal artifact — the string is a single-line literal whose `\`-newline continuations were collapsed while keeping the source indentation. Wider pattern, same shape, at crates/wcore-cli/src/acp.rs:266 and crates/wcore-cli/src/main.rs:8616 and :8624.

**Where.** crates/wcore-cli/src/cache_cmd.rs:486 (and the sibling bail at :476, `...but its ledger could not be read: {e}`)

**Why it matters.** The ticket's own fix shape says 'at minimum make the error name the real key and point at `cache list`, so the message stops implying the run was never recorded.' This message is the deliverable, and it is the first thing a scripted/CI operator sees on the failure path. No test asserts on its shape — `an_unknown_session_id_still_fails_and_says_where_to_look` only checks `err.contains('cache list')`, which a mangled string satisfies. Cosmetic, not functional; it does not block closing #1162, but it should be filed rather than shipped silently, and the same collapse in acp.rs/main.rs suggests a tooling pass did this repo-wide.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.

---
issue: 1301
repo: FerroxLabs/wayland
kind: defect
title: "First Windows retry-flake cluster: a wall-clock ratio guard and a dispatch-budget test at 94% of its kill line (CI (Array))"
status: open
last_verified_commit: 5faceff7a
criteria:
  - id: c1
    text: "one_turn_costs_about_two_whole_payload_scrub_passes has its estimator changed to reduce variance WITHOUT moving either bound, and is re-measured at n>=40 under load with a stated failure rate. Widening the 1.5..2.5 window does not satisfy this."
    state: met
    evidence: "file:crates/wcore-agent/tests/turn_cost_per_byte_guard.rs:200:MEASURED 2026-09-10 (wayland#1301 c1) on SeanDesktop"
    owner: core
    note: "MET at 29cbff29. The estimator is now a median of SEVEN interleaved rounds (`const ROUNDS: usize = 7` at :218, `ratios[ratios.len() / 2]` at :232); both bounds are byte-identical at 1.5 and 2.5, and every raw ratio is still printed. RE-MEASURED on SeanDesktop -- the box that hosts the `CI (Array)` runner services -- 32 cores with 16 CPU spinners pinned against them, `--retries 0`, n=40 per arm, the old 3-round estimator at fdf4b1e1c against the new one at 785a22ea. STATED FAILURE RATE: 0/40 at BOTH arms. That population did NOT reproduce the 1/10 seen on CI (Array) or the 1/41 seen on Linux, so this run cannot and does not claim a rate improvement it never observed -- what it measures is DISTANCE to failure on the same box under the same load. Asserted median across the 40 runs: 1.690-2.020 sd 0.070 at 3 rounds against 1.820-2.130 sd 0.058 at 7, worst approach to a bound +0.190 against +0.320. Resampling every 3-subset of each 7-round run (which holds the load epoch fixed, where drawing rounds independently would not) a median of 3 came within +0.030 of a bound over 1400 subsets while the median of 7 of those same rounds stayed +0.320 away. The individual rounds are as noisy as ever: 34 of 280 fell outside 1.5..2.5 on their own, min 1.20 max 2.69, which is the point -- the estimator, not the product, was the flake. LIMIT: the box was otherwise idle, its three runner services were not live, and n=40 is far too small to bound a ~2 percent event. ORIGINAL FILING, kept: Windows 1/10 (run 33707672948, 'one turn now costs 3.06 whole-payload PIIScrubber passes per byte ... small=0.7434s large=1.4755s one_scrub=0.2744s'), and Linux 1/41 at --retries 0 under load 62-151, payload 'ratios=[1.46, 2.68, 4.61] median_passes=2.68' -- ONE invocation crossing BOTH bounds on a byte-identical tree. NOT A REGRESSION: over 41 Linux runs the asserted median-of-3 is min 1.81 / median 2.00 / max 2.68, exactly the 2.0 that wayland-core#395 measured. so it is not a platform artifact: Windows 1/10 (run 33707672948, 'one turn now costs 3.06 whole-payload PIIScrubber passes per byte ... small=0.7434s large=1.4755s one_scrub=0.2744s'), and Linux 1/41 at --retries 0 under load 62-151 with payload 'ratios=[1.46, 2.68, 4.61] median_passes=2.68' -- ONE invocation crossing BOTH bounds on a byte-identical tree. Over 120 rounds, 7 were >= 2.5 (max 4.61) and 3 were <= 1.5; only the median-of-3 hides them. NOT A REGRESSION: over 41 Linux runs the asserted median-of-3 is min 1.81 / median 2.00 / max 2.68, exactly the 2.0 that wayland-core#395 measured. The estimator is a DIFFERENCE of two wall-clock samples divided by a third, so noise is amplified and ~290ms of jitter in the large arm moves it a full pass."
  - id: c2
    text: "The justifying comment in .config/nextest.toml no longer claims CI's 180s budget covers fix1_dispatch_budget_aborts_with_partial_result; it states the real 240s inherited override and the measured 185-226s range."
    state: met
    evidence: "file:.config/nextest.toml:139:CORRECTED 2026-09-03 (gh#1301 c2)"
    owner: core
    note: "MET at 509f4426b. The justifying comment in .config/nextest.toml no longer certifies the 180s budget, and it states both figures this criterion names. Read in the tree at :139-149: it records that the block used to read `this test already PASSES in CI, whose 90s x 2 = 180s budget covers it` and that this was FALSE -- all ten measured Windows (`CI (Array)`) runs exceed 180s, at 185.46-226.28s, so the test has never once passed inside the budget the comment certified. It names the real inherited override (`nextest inherits THIS override into --profile ci, giving it 120s x 2 = 240s`, matching `slow-timeout = { period = 120s, terminate-after = 2 }` at :162) and the measured range and its drift (185-194s on 2026-09-02 against 216.9/221.0/226.3s on 2026-09-03, i.e. 90-94 percent of the kill line). Landed in 774c40f5a. c3 is NOT graded by this: the headroom is still 90-94 percent, not the 1.5x the criterion requires."
  - id: c3
    text: "fix1_dispatch_budget_aborts_with_partial_result has at least 1.5x headroom against its kill line on Windows, measured at n>=10. Today it sits at 90-94%."
    state: not-met
    evidence: ""
    owner: core
    note: "STILL NOT MET at the head of the w15/win-cred lane, and NOT closable from a lane. Nothing in this branch touches this test, its fixture or its cost -- the whole diff is `mod chunk_crash_injection` inside `crates/wcore-config/src/credentials.rs` plus three deleted `.config/flaky-allowlist.txt` rows -- so the 1.56x idle / 1.28x loaded / 1.06-1.11x real-CI ladder the previous pass measured stands exactly as written and is NOT re-derived here. THE ONE THING THAT DID CHANGE, AND IT IS ON THIS TEST'S OWN LEG, stated because it moves the CONDITION this criterion grades even though it does not meet it: the wayland#1300 fix removes Windows Error Reporting from every injected crash in `credentials::chunk_crash_injection`, and `CI (Array)` runs the whole workspace in one nextest pool, so `fix1_dispatch_budget_aborts_with_partial_result` now runs beside materially less concurrent work than it did when 216.9-226.3s was recorded. Measured on SeanDesktop at `--profile ci --retries 0`: that module is 3.57-3.70s over 15 passes and the whole 817-test wcore-config lib suite is 14.60-15.00s over 5, where the same module used to be harness-killed at 180.114s and its slowest passing member ran 131.44-149.03s. A REDUCTION IN A NEIGHBOUR'S LOAD IS NOT A MEASUREMENT OF THIS TEST, and it is recorded as context, not as headroom. WHY THIS STAYS NOT MET ON ITS MERITS, two independent reasons, either of which is sufficient. (1) The criterion asks for n>=10 UNDER REAL CI (Array) CONCURRENCY. There is no route to ten such legs from a lane: each one is a whole `ci.yml` run whose Linux leg alone is ~67 minutes, and the release has been explicitly burned before by lanes spending CI runs to rediscover one fact. (2) No fixture cost reduction exists that preserves the genuine crossing. THAT WAS RE-CHECKED AGAINST THE TREE RATHER THAN INHERITED: `MAX_TOTAL_DISPATCHES` is a plain `pub const usize = 1000` at `crates/wcore-agent/src/orchestration/workflow/limits.rs:34` with no injection seam, so a test-only budget would stop the test crossing the REAL constant and this criterion says `genuine`; and the budget's ARITHMETIC is already covered without dispatching anything by the unit tests at `limits.rs:183-196`, so the only thing this test uniquely buys is ~1000 real dispatches through the runner, which is exactly the cost. Fewer dispatches deletes the property. The largest remaining lever is build optimisation -- `.config/nextest.toml` records 47ms per dispatch in debug against 6.9ms in release -- and changing the test profile's opt-level is a workspace-wide change to every test's build and cache, not a lane-scoped fixture change, and would need its own measurement. STILL EXCLUDED, unchanged: raising `terminate-after` 2 -> 3 satisfies the arithmetic and is refused, because a criterion is not closable by moving its own kill line. Also refused, again: the idle-box 1.56x, which reads as a pass against the literal text and is the absence of the very contention the row exists to grade. A THIRD LANE HAS NOW DECLINED TO CLOSE THIS ROW ON THE ARM THAT WOULD HAVE ALLOWED IT. WHAT IT WOULD TAKE, concretely: ten `CI (Array)` legs at `--retries 0` with this test's in-test seconds harvested from each -- cheapest as a dedicated repeat-the-one-test workflow on a throwaway branch, the same shape as the wayland#1300 c5 macOS control, rather than ten full `ci.yml` runs -- OR a real per-dispatch cost reduction, which is the `pipeline-cpu-floor` LOW already filed against this same block. Neither is this lane's, and neither is reachable by restating the row."
  - id: c4
    text: "Both allowlist entries added for these two tests are DELETED, not renewed, when c1 and c3 land."
    state: not-met
    owner: core
    note: "STILL NOT MET, and HALF OF IT IS ALREADY DONE, which is worth recording precisely so the remaining half is not mistaken for the whole. VERIFIED IN THIS TREE with a known-positive control in the same call, because an empty grep result reads as `absent` whether or not the query worked: `one_turn_costs_about_two_whole_payload_scrub_passes` returns 0 occurrences in `.config/flaky-allowlist.txt` -- that row is ALREADY GONE, deleted by a concurrent worker, which is what the previous note anticipated when it said the edit was being reported rather than made -- while the control, `fix1_dispatch_budget_aborts_with_partial_result`, returns 1 and is therefore still present. So the query is working and the zero means what it says. The `fix1_dispatch_budget_aborts_with_partial_result` row STAYS, deliberately, because c3 is open: deleting it would assert that the test is stable at a headroom that has been measured at 1.06-1.11x under the condition that decides it, and it would re-red `report` at the first contended run. THIS LANE DELETED THREE OTHER ROWS IN THE SAME FILE IN THE SAME CHANGE (the two wayland#1303 `chunk_write_lock_verification` rows and the wayland#1300 `interrupted_rotations` row, whose own text said REMOVE when gh#1300 lands) and left this one alone, which is the distinction this criterion exists to enforce: a row comes out when the debt is discharged, never to make a gate green. Flip to met with `absent:.config/flaky-allowlist.txt::fix1_dispatch_budget_aborts_with_partial_result` once c3 genuinely lands -- note that anchor rather than the `one_turn` one named in the previous note, because `one_turn` is already absent and an `absent:` token on it would resolve today and prove nothing about the row that is actually still there."
---

# Windows had no flake-cluster ticket. These are the first two.

macOS has #1286 and Linux has #1288. This is the Windows equivalent, and the two
members are different diseases sharing one condition: a self-hosted 32-core box
running up to three runner services at once.

One is a wall-clock ratio guard whose estimator amplifies scheduler noise; the other
is a test that has quietly grown to 94% of its kill line while a comment in the repo
certified that a budget it has never passed inside covers it.

METHOD NOTE WORTH KEEPING. All three of that run's flakes were first attributed to
`linux-containerized`. Grading the raw `<flakyFailure>` entries per leg gave
Array 3, linux-containerized 1 (an already-allowlisted `f14_*`), macos-latest 0.
Grade every leg's artifact, not the one you expect to be guilty.

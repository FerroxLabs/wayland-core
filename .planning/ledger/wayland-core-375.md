---
issue: 375
repo: FerroxLabs/wayland-core
kind: defect
title: "GrepTool reads a VCS content store in plaintext when the store's parent is named as the search path"
status: closed
last_verified_commit: 93ede3424
criteria:
  - id: c1
    text: "Grep(pattern, path='.git') and Grep(pattern, path='.svn') return no bytes from .git/lfs/objects or .svn/pristine under WorkspacePolicy::contained"
    state: met
    evidence: test:crates/wcore-tools/tests/grep_vcs_content_store_deny.rs::naming_the_git_control_dir_returns_no_lfs_object_bytes
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D1, found while verifying FerroxLabs/wayland-core#244 — VFS Read of raw .git/objects permitted (compressed; info-only)). Nothing has been done. The measured finding, verbatim: GrepTool reads a VCS content store in plaintext when the CONTROL directory is named one component up. `GrepTool::execute_with_ctx` (crates/wcore-tools/src/grep.rs:78-95) gates only the top-level `path` argument through `ctx.vfs.exists()`, then spawns `rg` directly via `shell_command_argv` (crates/wcore-tools/src/grep.rs:279) — outside BashTool's OS sandbox, so neither SecretDenyFs nor the bwrap fs_read_deny applies to the traversal. `is_vcs_store_dir` matches the store, not its parent, so `.git` and `.svn` pass the guard and rg then descends into `.git/lfs/objects/**` and `.svn/pristine/**`, which hold UNCOMPRESSED file content. MEASURED, not reasoned, in an isolated copy of origin/integ/f13 with the production contained stack (`SandboxedFs::new(SecretDenyFs::new(RealFs, WorkspacePolicy::contained(root)), root)`): Grep(pattern='CANARY', path='.svn') returned `.svn/pristine/aa/deadbeef.svn-base:1:SVN-CANARY-244 AWS_SECRET_ACCESS_KEY=abc123`; Grep(path='.git') returned `.git/lfs/objects/aa/bb/deadbeef:1:LFS-CANARY-244 password=hunter2`. Controls in the same run: Grep(path='.svn/pristine') and Grep(path='.git/lfs') were both REFUSED ('is a protected secret path'), and Grep(path='.') withheld them ('[Grep policy: 3 match(es) in ignored paths]') — so the deny works and the ignore walk works; it is exactly the name-the-parent shape that bypasses both. CLOSED 2026-08-30 by lane sec-secrets on branch lane/f13-sec-secrets. `grep_policy::scope_for` now prunes, at the DIRECTORY level, any entry `WorkspacePolicy::denies_read_content` refuses, and `run_grep` refuses a search ROOT that the same predicate refuses. Both canaries the sweep measured are gone from the output under `WorkspacePolicy::contained`: `Grep(pattern='CANARY', path='.git')` no longer returns `LFS-CANARY-244 password=hunter2` and `path='.svn'` no longer returns `SVN-CANARY-244 AWS_SECRET_ACCESS_KEY=abc123`. Not silent either -- the withholding is reported as `[Grep policy: N match(es) in ignored paths]`, which the test asserts, because 'could not show you' and 'there was nothing' are different answers."
  - id: c2
    text: "A test drives the production contained stack (SandboxedFs over SecretDenyFs over RealFs) for both parent-named spellings and is shown RED against today's code, with the mutation proven to land on executable code"
    state: met
    evidence: test:crates/wcore-tools/tests/grep_vcs_content_store_deny.rs::naming_the_svn_control_dir_returns_no_pristine_bytes
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D1). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below. CLOSED 2026-08-30 by lane sec-secrets on branch lane/f13-sec-secrets. The test drives the production contained stack verbatim -- `SandboxedFs::new(SecretDenyFs::new(RealFs, WorkspacePolicy::contained(root)), root)` as `ctx.vfs`, with the same policy on `ctx.workspace`, which is how all three production installation sites wire it (bootstrap.rs, spawner.rs, channel_tools.rs each call `set_workspace_policy` beside the `SecretDenyFs`). RED ARM: replacing the `.filter_entry(...)` block in `grep_policy::scope_for` with a constant-true filter reddens FOUR tests (both parent-named spellings, the unguessable-name arm, and the no-SecretDenyFs arm) while the three controls stay green. The mutation was proven to land on executable code by printing the function body before and after -- it is a builder call in the walk that produces `allowed`, not a doc comment."
  - id: c3
    text: "The admitted-file decision uses the same predicate the VFS deny uses, so a store reached under any parent name is covered -- not a second name list in grep_policy.rs"
    state: met
    evidence: symbol:crates/wcore-tools/src/workspace_policy.rs::denies_read_content
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D1). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below. CLOSED 2026-08-30 by lane sec-secrets on branch lane/f13-sec-secrets. The conjunction `SecretDenyFs::guard` used to spell out inline now lives on the policy as `denies_read_content`, and `guard` is a single call to it -- so the traversal filter and the VFS deny are the same predicate by construction, not by two lists agreeing. `grep_policy.rs` gained NO name list. Graded by an arm no name list could pass: `a_store_reached_under_an_unguessable_name_is_still_pruned` points `<root>/.git` at `<root>/real-git`, so the store's canonical path is `<root>/real-git/objects` -- matched only by arm 2 of `is_vcs_content_store`, reachable through no `.git`-shaped spelling. REFUTED 2026-08-30 and RE-CLOSED. The sentence held only for a store that existed BEFORE the policy was built: `a_store_reached_under_an_unguessable_name_is_still_pruned` constructs its policy after the fixture, so it never touched core#376's store memo, and with that memo WARM the same symlinked-control store leaked -- one ordinary `ctx.vfs.exists()` (what every session does first), then `git lfs` populates the store, then `Grep(pattern='CANARY', path='real-git')` on the production contained stack returned `real-git/lfs/objects/aa/obj:1:LFS-CANARY password=hunter2`. The cause and the fix are on core#376 c2 (the witness scheme could not see through a symlinked control directory). Graded under the condition a real session is actually in by `a_store_under_a_symlinked_control_dir_is_pruned_after_the_memo_is_warm`, which warms the memo first and asserts as a CONTROL that a second ordinary guard does NOT rescan. That control is load-bearing and was added after measurement, not from theory: the arm's first shape had no settle wait, so every call missed the memo and rescanned, and it PASSED against the fail-open code -- a vacuous arm caught by red-arming it before believing it."
  - id: c4
    text: "The negative controls stay green: Grep(path='.') still withholds ignored matches, and an ordinary in-workspace search is unchanged"
    state: met
    evidence: test:crates/wcore-tools/tests/grep_vcs_content_store_deny.rs::an_ordinary_search_is_unchanged_and_the_dot_search_still_withholds
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D1). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below. CLOSED 2026-08-30 by lane sec-secrets on branch lane/f13-sec-secrets. `Grep(path='.')` still returns the ordinary working-tree match AND still reports `ignored paths`; `.git/HEAD` stays searchable (the `git rev-parse` carve-out), graded separately by `git_metadata_outside_the_store_stays_searchable`. Both stayed GREEN under the c2 red arm, so they are controls and not passengers. NOTE a deliberate widening beyond the issue text: the refusal is posture-INDEPENDENT, matching grep_policy rule 3, which has never been trust-gated because Grep returns matched line CONTENT. A trusted-local session (no `SecretDenyFs` in its VFS at all) therefore also stops greping `.git/lfs`. That is strictly stricter than before and cannot open a boundary; it is pinned by `a_session_without_a_secret_deny_vfs_still_refuses_a_store`."
---

GrepTool reads a VCS content store in plaintext when the CONTROL directory is named one component up. `GrepTool::execute_with_ctx` (crates/wcore-tools/src/grep.rs:78-95) gates only the top-level `path` argument through `ctx.vfs.exists()`, then spawns `rg` directly via `shell_command_argv` (crates/wcore-tools/src/grep.rs:279) — outside BashTool's OS sandbox, so neither SecretDenyFs nor the bwrap fs_read_deny applies to the traversal. `is_vcs_store_dir` matches the store, not its parent, so `.git` and `.svn` pass the guard and rg then descends into `.git/lfs/objects/**` and `.svn/pristine/**`, which hold UNCOMPRESSED file content. MEASURED, not reasoned, in an isolated copy of origin/integ/f13 with the production contained stack (`SandboxedFs::new(SecretDenyFs::new(RealFs, WorkspacePolicy::contained(root)), root)`): Grep(pattern='CANARY', path='.svn') returned `.svn/pristine/aa/deadbeef.svn-base:1:SVN-CANARY-244 AWS_SECRET_ACCESS_KEY=abc123`; Grep(path='.git') returned `.git/lfs/objects/aa/bb/deadbeef:1:LFS-CANARY-244 password=hunter2`. Controls in the same run: Grep(path='.svn/pristine') and Grep(path='.git/lfs') were both REFUSED ('is a protected secret path'), and Grep(path='.') withheld them ('[Grep policy: 3 match(es) in ignored paths]') — so the deny works and the ignore walk works; it is exactly the name-the-parent shape that bypasses both.

**Where.** crates/wcore-tools/src/grep.rs:78-95 (execute_with_ctx gates only the path arg) and :279 (rg spawned outside the sandbox); the admitted-file decision is crates/wcore-tools/src/grep_policy.rs::scope_for / GrepScope::admits. No existing issue: searched FerroxLabs/wayland-core for 'grep content store' and 'Grep .git' across all states — only #242/#243/#234, all Bash-side.

**Why it matters.** This is strictly worse than #244 itself. #244 was filed INFO-only because git loose objects are zlib-compressed; `.svn/pristine` and `.git/lfs/objects` are stored VERBATIM, so this returns a committed secret's plaintext to the model in the contained posture — the posture whose entire premise is that Bash cannot reconstruct a committed secret from the object store. It also falsifies ledger criterion c3 as written, and it is the 'graded the function, not the wiring' shape one level up: the store deny was graded against Read/Write/Edit and BashTool, and nobody counted GrepTool's own subprocess as a call site.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.

---
issue: 1269
repo: FerroxLabs/wayland
kind: defect
title: "Unmerged lane/f13-* branches held out of 0.13.12: four carry work integ lacks, two await their owning lane, five archived as redundant"
status: open
last_verified_commit: fdf4b1e1c
criteria:
  - id: c1
    text: "Each of the four 'carries work `integ` lacks' branches reaches a terminal state: merged after an adversarial pass, archived with the reason recorded, or filed as its own ticket. None is left as a bare branch."
    state: not-met
    owner: core
    note: "Re-graded 2026-09-10 against fdf4b1e1c by the w15/rec lane; full method in .planning/RECORD-1269-1272-1324-2026-09-10-w15.md section 1.1, building on .planning/RECON-1269-1272-1324-2026-09-10.md. The prior grading is CORRECTED on one point: lane/f13-n-dataloss has NO test residual. Its three named tests were absent BY NAME only; all seven of its tests are covered in the tree, four of them on byte-identical inputs. a_prose_mention_of_a_reasoning_tag_survives_in_history is wayland1221_c1_prose_that_mentions_a_reasoning_tag_survives_intact_in_stored_history (crates/wcore-agent/tests/reasoning_eos_flush_test.rs:130) on the identical string `Use the <thinking> tag to wrap reasoning. Then answer.`; an_answer_ending_in_an_ambiguous_prefix_keeps_every_character is a_trailing_angle_bracket_is_flushed_as_text and a_partial_tag_prefix_is_flushed_as_text (crates/wcore-types/tests/reasoning_filter_eos_flush.rs:24,32) on the identical strings `the answer is 5 <` and `result: <th`; the split-delta and closed-block control cases are at reasoning_eos_flush_test.rs:146 and :206. The two egress tests are covered by an_egress_ask_on_the_json_stream_path_reaches_the_host (crates/wcore-cli/tests/issue_1219_json_stream_egress_consent.rs:105) and a_blocking_doorbell_is_never_installed_over_a_sink_that_cannot_prompt (:207) plus the_install_guard_refuses_a_mute_sink_and_accepts_a_real_one (crates/wcore-agent/src/egress/bridge_doorbell.rs:325); the first is STRICTLY STRONGER and its own docstring says it was written to avoid bridge.pending_tokens(), the in-process shortcut the branch test uses and which the tree calls vacuous. ReasoningFilter::finish (crates/wcore-types/src/reasoning_filter.rs:231) has 17 call sites, 7 of them production, so it is wired not merely defined; the inline unclosed_tag_eats_to_end at :764 is not a contradiction because its run helper never calls finish. Filing the missing-test ticket was authorised for this lane and is deliberately NOT done: there are no missing tests, and the one it would name is the vacuous one. NOT MET on ONE act, which is a ref operation this lane may not perform: rename all FOUR heads on hetzner-dsm:/root/w-f13/integ citing that record -- lane/f13-n-ci-evidence ee1c95157afcf4698e6e8543a911c859d1dc3271, lane/f13-relgate-ci 000db814eea27c202b60b8f05afcc4b9d02ba05d, lane/f13-serde-float-roundtrip 715781e00dbc976aa39c25cd3206e90e39601242 and lane/f13-n-dataloss 4eed60a4b69a2fa3442f7f379da7c258ce85782f, each to archive/f13-*. All four resolve to commit objects in this worktree on 2026-09-10. n-dataloss joins the rename list by this grading; the prior grading held it back for a test port that is not needed. LIMIT: this grades COVERAGE by input and assertion identity, read from source. No test was run -- cargo is banned on this host and all three Linux slots were held -- so it does not prove the tree tests PASS, only that the branch adds no uncovered subject."
  - id: c2
    text: "`lane/f13-serde-float-roundtrip` specifically: the corpus question is answered with a measurement — regenerate and key-diff, confirming whether `schema_digest` moves — before it is merged or discarded."
    state: not-met
    owner: core
    note: "NOT MET 2026-09-10, re-graded against fdf4b1e1c; section 1.2 of .planning/RECORD-1269-1272-1324-2026-09-10-w15.md. Still no regenerate: cargo is banned on this host and all three Linux slots were held. But the key the criterion NAMES is now settled by construction, with in-tree evidence. schema_digest CANNOT MOVE: schemas_digest (crates/wcore-protocol/src/contract/generate.rs:1412-1417) hashes only artifacts under schema/, the three checked-in schema files contain ZERO non-integer numeric literals (measured), and the sibling wire_shape_digests doc at generate.rs:1422-1424 states the rule -- a schema branch records types, enums and nesting, never fixture values. source_inputs_digest CANNOT MOVE: the root Cargo.toml is not among the 43 source_inputs entries (measured, zero entries containing Cargo.toml). CORRECTION to the prior grading, which reached that same conclusion from a wrong premise: 25 of the 43 entries are OUTSIDE crates/wcore-protocol/src/, including wcore-agent/src/engine.rs, bootstrap.rs, wcore-cli/src/main.rs and wcore-tools/src/registry.rs. fixture_digest is NARROWED: fixtures_digest (generate.rs:1376-1408) hashes fixture bytes VERBATIM with exactly one exception, six negotiation fixtures re-parsed and re-emitted at generate.rs:1386-1401, and all six carry zero non-integer numeric literals (measured). The 24 float-carrying files are all outside that list so their bytes enter unparsed. SECOND CORRECTION: at this commit there are 200 fixture files carrying 7 distinct non-integer values (0.0, 0.0125, 0.875, 1.25, 2.5, 5.0, 87.5), max 3 significant digits -- not 156 files over 14 values, which was a 2fe7a70df measurement the corpus has since moved past. PREDICTION a regen would falsify if wrong: no key moves. Two of the three reasons this is not-met are now PERMANENT, not pending: no regenerate was run, and float_roundtrip is already shipped at Cargo.toml:236 via 93ede3424, so `before it is merged or discarded` cannot be satisfied by anybody. What remains achievable is a post-hoc confirming regen on a Linux host."
  - id: c3
    text: "`lane/f13-n-security` and `lane/f13-u-protocol` are re-checked against `wayland-core#356` / `#366` **after** their owning lanes land, and either merged for what those lanes missed or archived with that comparison recorded."
    state: not-met
    owner: core
    note: "Precondition SATISFIED and the comparison is DONE and recorded in .planning/RECON-1269-1272-1324-2026-09-10.md; the disposing act is not. Read 2026-09-10: wayland-core#356 CLOSED/COMPLETED 2026-08-31T13:37:05Z, wayland-core#366 CLOSED/COMPLETED 2026-08-31T13:37:13Z, and the ledgers for core#244, #314, #356 and #366 are all status closed with every criterion met at 93ede3424. The tree reimplements both subjects independently: for #366 the unscoped sweep is at contract.rs:340-361 with per-backend declarations and conformance.rs:380, and the tree's test file is container_orphan_scan.rs where the branch's is container_orphan_sweep.rs. For #356 both trees carry a grep_vcs_store_deny.rs but they are DIFFERENT files with different tests; the branch names five cases the tree does not (svn pristine, absolute control-dir spelling, symlink alias, vendored checkout, context-free entry point) and carries git_secret_content_test.rs and workspace_policy_resolver_class.rs which the tree lacks entirely. The tree guards GitTool through SecretDenyFs (git.rs:733-761) where the branch used withhold_secret_diff_sections / refuse_secret_path. Whether those are behaviourally equivalent for git diff and git blame pointed at a committed secret is NOT settled by reading, and the branch's own test file is the instrument -- which is a build. NOT MET: owed is that run on a Linux slot, then archive both branches citing the record, or treat a failure as a live wcore-tools security gap rather than a branch chore. Re-verified 2026-09-10 by the w15/rec lane against fdf4b1e1c (section 1.3 of .planning/RECORD-1269-1272-1324-2026-09-10-w15.md): wayland-core#356 and #366 both still CLOSED/COMPLETED at those timestamps, and the open question is unchanged. This lane could not run the instrument either -- cargo is banned on this host and all three Linux slots were held -- so nothing is added beyond the date and the confirmation that the residual is a build, not bookkeeping."
  - id: c4
    text: "The archived branches stay recoverable until 0.13.12 ships; deleting them is a separate, deliberate act."
    state: met
    evidence: "commit:01b2ac64c4c5d02110c3e3bc15d20f96135e1179"
    owner: core
    note: "MET 2026-09-10. All five archived heads resolve to commit objects with full trees in this worktree and match the SHAs the issue body records: 01b2ac64c4c5d02110c3e3bc15d20f96135e1179 atref-guard, 20f0a060deaea69a20b3a15c1d7489f830e0e1da n-atref, 6a9906cd4b16bea6da46d8aaeba6a9af860e9c21 n-cache, b1e925473377a2186ea4f1f0a74da32e9569853d n-mcp-prov, 760f4f9a20799187b102d6eb23eca3e6fe959149 n-win-escape. git ls-remote hz returns all five at those exact SHAs from ssh://hetzner-dsm/root/w-f13/integ on 2026-09-10. Tag v0.13.12 exists and the tree is at 0.13.14, so the criterion's window closed and was honoured; this lane changed no ref and deleted nothing. The evidence token is one archived head deliberately, because commit: re-resolves on every gate run and reds if the object is ever collected. LIMIT, measured not assumed: git ls-remote gh on 2026-09-10 returns NO archive/f13-* ref at all and does not return lane/f13-relgate-ci either, though it does return the other five lane branches. The only network-reachable copy of all five archived heads and of lane/f13-relgate-ci is one Hetzner working repository at /root/w-f13/integ; fetched clones' object stores are not a backup. Recoverable today, one rm -rf from not being. Pushing archive/f13-* and lane/f13-relgate-ci to FerroxLabs/wayland-core is the mirror-image deliberate act and is recommended, not done here. RE-VERIFIED 2026-09-10 by the w15/rec lane at fdf4b1e1c: all five heads still resolve to commit objects with full trees (676c55689, ae9033144, 92168a28e, 100a48d3b, 0f49ad61c respectively). This lane changed no ref and deleted nothing either. The remote-reachability limit above was NOT re-measured at this commit and is carried forward from 2fe7a70df."
---

Created 2026-08-31 to close a COVERAGE gap; re-graded 2026-09-10 by the w14/recon
reconciliation lane against `2fe7a70df`. The full archaeology -- fork points,
per-branch artifact tracing and the remote-reachability measurement -- is in
`.planning/RECON-1269-1272-1324-2026-09-10.md`.

c1, c2 and c3 were re-graded the same day by the `w15/rec` lane against
`fdf4b1e1c`, and c4's objects were re-resolved there. That lane's corrections
-- the `lane/f13-n-dataloss` test residual does not exist, `schema_digest`
cannot move, and `source_inputs` is not confined to `wcore-protocol/src` -- are
in `.planning/RECORD-1269-1272-1324-2026-09-10-w15.md` sections 0 and 1. No ref
was renamed by either lane; c1 names the four renames it is waiting on.

`scripts/check-criteria-ledger.py` scopes every open `area:core` issue on
wayland and EVERY open issue on wayland-core. This issue was in scope from
the moment it was filed and had no ledger file, so
`scripts/check-release-readiness.py` -- which reads ledger files and nothing
else -- could not count it. CI runs the coverage gate with `--offline`, the
arm that would have reported the gap, so nothing said so for two days.

Criteria are transcribed from the issue body without edit. Where the body's
wording is loose it is LEFT loose rather than tightened here: sharpening a
criterion inside the ledger is how a criterion quietly becomes an easier
adjacent property. Whoever takes this restates it on the ISSUE first.

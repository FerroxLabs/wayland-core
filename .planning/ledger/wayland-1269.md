---
issue: 1269
repo: FerroxLabs/wayland
kind: defect
title: "Unmerged lane/f13-* branches held out of 0.13.12: four carry work integ lacks, two await their owning lane, five archived as redundant"
status: open
last_verified_commit: 7cd078806
criteria:
  - id: c1
    text: "Each of the four 'carries work `integ` lacks' branches reaches a terminal state: merged after an adversarial pass, archived with the reason recorded, or filed as its own ticket. None is left as a bare branch."
    state: met
    evidence: "commit:ee1c95157"
    owner: core
    note: "Re-graded 2026-09-10 against fdf4b1e1c by the w15/rec lane; full method in .planning/RECORD-1269-1272-1324-2026-09-10-w15.md section 1.1, building on .planning/RECON-1269-1272-1324-2026-09-10.md. The prior grading is CORRECTED on one point: lane/f13-n-dataloss has NO test residual. Its three named tests were absent BY NAME only; all seven of its tests are covered in the tree, four of them on byte-identical inputs. a_prose_mention_of_a_reasoning_tag_survives_in_history is wayland1221_c1_prose_that_mentions_a_reasoning_tag_survives_intact_in_stored_history (crates/wcore-agent/tests/reasoning_eos_flush_test.rs:130) on the identical string `Use the <thinking> tag to wrap reasoning. Then answer.`; an_answer_ending_in_an_ambiguous_prefix_keeps_every_character is a_trailing_angle_bracket_is_flushed_as_text and a_partial_tag_prefix_is_flushed_as_text (crates/wcore-types/tests/reasoning_filter_eos_flush.rs:24,32) on the identical strings `the answer is 5 <` and `result: <th`; the split-delta and closed-block control cases are at reasoning_eos_flush_test.rs:146 and :206. The two egress tests are covered by an_egress_ask_on_the_json_stream_path_reaches_the_host (crates/wcore-cli/tests/issue_1219_json_stream_egress_consent.rs:105) and a_blocking_doorbell_is_never_installed_over_a_sink_that_cannot_prompt (:207) plus the_install_guard_refuses_a_mute_sink_and_accepts_a_real_one (crates/wcore-agent/src/egress/bridge_doorbell.rs:325); the first is STRICTLY STRONGER and its own docstring says it was written to avoid bridge.pending_tokens(), the in-process shortcut the branch test uses and which the tree calls vacuous. ReasoningFilter::finish (crates/wcore-types/src/reasoning_filter.rs:231) has 17 call sites, 7 of them production, so it is wired not merely defined; the inline unclosed_tag_eats_to_end at :764 is not a contradiction because its run helper never calls finish. Filing the missing-test ticket was authorised for this lane and is deliberately NOT done: there are no missing tests, and the one it would name is the vacuous one. NOT MET on ONE act, which is a ref operation this lane may not perform: rename all FOUR heads on hetzner-dsm:/root/w-f13/integ citing that record -- lane/f13-n-ci-evidence ee1c95157afcf4698e6e8543a911c859d1dc3271, lane/f13-relgate-ci 000db814eea27c202b60b8f05afcc4b9d02ba05d, lane/f13-serde-float-roundtrip 715781e00dbc976aa39c25cd3206e90e39601242 and lane/f13-n-dataloss 4eed60a4b69a2fa3442f7f379da7c258ce85782f, each to archive/f13-*. All four resolve to commit objects in this worktree on 2026-09-10. n-dataloss joins the rename list by this grading; the prior grading held it back for a test port that is not needed. LIMIT: this grades COVERAGE by input and assertion identity, read from source. No test was run -- cargo is banned on this host and all three Linux slots were held -- so it does not prove the tree tests PASS, only that the branch adds no uncovered subject. DONE 2026-09-10 at e16de82cd: all FOUR heads renamed on hetzner-dsm:/root/w-f13/integ, citing this record. lane/f13-n-ci-evidence -> archive/f13-n-ci-evidence, lane/f13-relgate-ci -> archive/f13-relgate-ci, lane/f13-serde-float-roundtrip -> archive/f13-serde-float-roundtrip, lane/f13-n-dataloss -> archive/f13-n-dataloss. Verified after the act by `git for-each-ref refs/heads/archive/f13-*`: nine archive heads, the four new ones at ee1c95157afcf4698e6e8543a911c859d1dc3271, 000db814eea27c202b60b8f05afcc4b9d02ba05d, 715781e00dbc976aa39c25cd3206e90e39601242 and 4eed60a4b69a2fa3442f7f379da7c258ce85782f -- the SHAs this note already recorded, unchanged by the rename -- and the four lane/ names now resolve to nothing. A rename moves no object, so c4's recoverability is untouched. THE COVERAGE LIMIT ABOVE STANDS: no test was run, so this grades that no branch is left bare, not that the tree passes."
  - id: c2
    text: "`lane/f13-serde-float-roundtrip` specifically: the corpus question is answered with a measurement — regenerate and key-diff, confirming whether `schema_digest` moves — before it is merged or discarded."
    state: met
    evidence: "file:crates/wcore-protocol/contracts/desktop/v1/manifest.json:1:8497e92e4ab2599201f95b2aa62c359ae2328429305e79a96761356483fc6e33"
    owner: core
    note: "MET 2026-09-10 by the w15/gateB lane. THE REGEN WAS RUN. Method, an A/B with the feature as the only manipulated variable, both arms built and executed on Linux with complete receipts: ARM ON = 4f82ce8c1, the tree as shipped (Cargo.toml:236 carries float_roundtrip), tree c89359ab, remote_exit 0. ARM OFF = the identical tree with float_roundtrip removed from that one line, committed at ce4af4cf7, tree 4e086b2a, remote_exit 0; serde_json v1.0.150 recompiled in that arm, so the two arms genuinely differ. Both ran `cargo run -p wcore-protocol --bin wcore-contract -- generate`. RESULT: git status --porcelain was EMPTY after BOTH generates -- each arm reproduced the checked-in corpus byte for byte. A 201-file sha256 census of the emitted corpus is IDENTICAL between the arms (b9387704b15076cfd12495897fded174b4f7a30818c4340f1c60b38b8a4e7fb2), and the two manifest.json files are byte-identical (5735719f68af980bb571aef35a5468e8dc69e5e01ca1c85a3283a0438df8753d). All 15 top-level manifest keys compare equal. THE KEY THE CRITERION NAMES, verified as an IDENTICAL SET before anything else was believed: schema_digest = sha256:8497e92e4ab2599201f95b2aa62c359ae2328429305e79a96761356483fc6e33 on both sides. fixture_digest (sha256:2525ef0a...) and source_inputs_digest (sha256:c930f2b2...) -- the two that MAY legitimately move -- did not move either. The prior grading's PREDICTION, `no key moves`, is CONFIRMED rather than restated. CONTROLS, because a null result needs them: all 201 files were re-emitted by the arm-OFF binary (every mtime falls inside the arm-OFF run window at 07:37:36Z), so this is not arm-ON's output left untouched; and the census diff was shown to report a difference when one exists. LIMIT: this measures the OUTCOME, not the mechanism. It shows that no corpus key moves with the feature off vs on at this commit; it does not independently prove why. The construction argument in the prior grading remains the explanation, now with a confirming measurement rather than instead of one. SECOND LIMIT, carried forward unchanged: `before it is merged or discarded` is unsatisfiable by anybody -- float_roundtrip shipped at 93ede3424 -- so this is the post-hoc confirming regen that grading said was what remained achievable, and nothing more. NOTE ON THE ANCHOR: fixture_digest and source_inputs_digest MOVED later on this same branch for an unrelated reason (the wayland#1116 engine.rs change at 0097f6a8b, regenerated at 7cd078806). The A/B above was taken at 4f82ce8c1 and ce4af4cf7, before that. schema_digest is unchanged by both, which is why it is the anchor. Reported on the issue at https://github.com/FerroxLabs/wayland/issues/1269#issuecomment-5615401478"
  - id: c3
    text: "`lane/f13-n-security` and `lane/f13-u-protocol` are re-checked against `wayland-core#356` / `#366` **after** their owning lanes land, and either merged for what those lanes missed or archived with that comparison recorded."
    state: met
    evidence: "test:crates/wcore-tools/tests/w1269c3_branch_security_instrument.rs::a_rename_into_a_secret_name_is_withheld_too"
    owner: core
    note: "MET 2026-09-10 by the w15/gateB lane. THE INSTRUMENTS WERE RUN, NOT READ, AND THERE IS NO LIVE SECURITY GAP. lane/f13-n-security: its own crates/wcore-tools/tests/git_secret_content_test.rs was ported into the tree at 0602d2da6 as crates/wcore-tools/tests/w1269c3_branch_security_instrument.rs with exactly two harness adaptations, both recorded in the ported file's header -- the WorkspacePolicy is attached to the ToolContext (this tree gates on ctx.workspace.secret_read_deny_required() at git.rs:755-761 and returns self.execute(input) UNFILTERED when workspace is None, where the branch gated inside the vfs, so running the file unadapted grades a context production never builds), and the repo root is canonicalized as the tree's own harness does. EVERY ASSERTION IS THE BRANCH'S. FIRST RUN, 0602d2da6, --retries 0 on Linux, remote_exit 100, complete true: 3 of the branch's 4 tests PASS against the tree, in the same invocation as all 5 of the tree's own git_content_store_deny tests (8 passed, 1 failed of 9). THE ONE FAILURE IS NOT A LEAK, payload verbatim: `it must be refused, not empty: diff --git a/.env b/.env / [Git] hunks withheld: .env is denied for content reads in this workspace posture / [Git] 1 file(s) hunks withheld (.env)`. The confidentiality assertion -- !out.content.contains(PROBE-GIT-9931) -- PASSED; what failed is the branch's NEXT assertion, out.is_error. The branch REFUSES a named-path diff; this tree WITHHOLDS the hunks and names the file with is_error false, a disposition its own git_content_store_deny.rs::a_contained_diff_withholds_a_denied_file_named_in_path pins deliberately. THE ANSWER TO THE OPEN QUESTION: the tree's SecretDenyFs/posture guard on GitTool and the branch's withhold_secret_diff_sections / refuse_secret_path ARE behaviourally equivalent for confidentiality on all four subjects the branch names -- diff at a named secret, whole-tree diff, blame, and a rename INTO a secret name -- and differ only in whether the refusal is total or per-hunk. The divergence is recorded in the file and the one test renamed to what it asserts at df2d8026e, where all 4 pass (remote_exit 0, complete true). WHAT THE TREE MISSED, NOW MERGED: a_rename_into_a_secret_name_is_withheld_too has no counterpart in git_content_store_deny.rs. The post-image b/ side is what decides there and nothing else in the tree grades it; that is why this criterion's evidence token names that test and not one of the three the tree already covered. lane/f13-u-protocol: its instrument is container_orphan_sweep.rs and the tree's counterpart is container_orphan_scan.rs. The TREE'S was run LIVE against real Docker 29.2.1 with busybox:1.36 on hetzner-dsm at df2d8026e -- 2 passed, 0 failed, --retries 0 -- so the tree's #366 reimplementation is proven live, not merely present. Its two tests cover the branch's three subjects by assertion: the unscoped scan finding a leftover from a nonce this process never used, with the SCOPED scan in the same pass as the discriminating control; an unlabelled container not being claimed; and a known nonce not being reported as a leftover. Porting the branch's near-duplicate would put two tests in a fight over deterministic container names for no added subject. DISPOSITION, DONE 2026-09-10: both heads renamed on hetzner-dsm:/root/w-f13/integ citing this record -- lane/f13-n-security 228c5c4d29db2391235df1c31e5d945810bb8474 -> archive/f13-n-security, and lane/f13-u-protocol 5a66afffed527936f12ebae1ddc4fd0ebb31bf5b -> archive/f13-u-protocol. Verified AFTER the act: both archive names resolve to those exact SHAs, git cat-file reports both objects still commit, neither lane/ name resolves any more, and refs/heads/archive/f13-* is now ELEVEN heads where c1's grading left nine. A rename moves no object, so c4's recoverability is untouched. RESIDUAL, named rather than buried: the branch's grep_vcs_store_deny.rs names five spellings the tree's same-named file does not (svn pristine, absolute control-dir spelling, symlink alias, vendored checkout, context-free entry point), and its workspace_policy_resolver_class.rs has no counterpart at all. Neither is what c3 asks about and neither was run here. If that coverage is wanted it is its own ticket, not a silent carry-forward on this one. LIMIT: the ported instrument is the BRANCH'S ASSERTIONS on THIS TREE'S wiring. It does not prove the branch's own implementation would pass here, and it grades neither of the branch's other two test files. Reported on the issue at https://github.com/FerroxLabs/wayland/issues/1269#issuecomment-5615401478"
  - id: c4
    text: "The archived branches stay recoverable until 0.13.12 ships; deleting them is a separate, deliberate act."
    state: met
    evidence: "commit:01b2ac64c4c5d02110c3e3bc15d20f96135e1179"
    owner: core
    note: "MET 2026-09-10. All five archived heads resolve to commit objects with full trees in this worktree and match the SHAs the issue body records: 01b2ac64c4c5d02110c3e3bc15d20f96135e1179 atref-guard, 20f0a060deaea69a20b3a15c1d7489f830e0e1da n-atref, 6a9906cd4b16bea6da46d8aaeba6a9af860e9c21 n-cache, b1e925473377a2186ea4f1f0a74da32e9569853d n-mcp-prov, 760f4f9a20799187b102d6eb23eca3e6fe959149 n-win-escape. git ls-remote hz returns all five at those exact SHAs from ssh://hetzner-dsm/root/w-f13/integ on 2026-09-10. Tag v0.13.12 exists and the tree is at 0.13.14, so the criterion's window closed and was honoured; this lane changed no ref and deleted nothing. The evidence token is one archived head deliberately, because commit: re-resolves on every gate run and reds if the object is ever collected. LIMIT, measured not assumed: git ls-remote gh on 2026-09-10 returns NO archive/f13-* ref at all and does not return lane/f13-relgate-ci either, though it does return the other five lane branches. The only network-reachable copy of all five archived heads and of lane/f13-relgate-ci is one Hetzner working repository at /root/w-f13/integ; fetched clones' object stores are not a backup. Recoverable today, one rm -rf from not being. Pushing archive/f13-* and lane/f13-relgate-ci to FerroxLabs/wayland-core is the mirror-image deliberate act and is recommended, not done here. RE-VERIFIED 2026-09-10 by the w15/rec lane at fdf4b1e1c: all five heads still resolve to commit objects with full trees (676c55689, ae9033144, 92168a28e, 100a48d3b, 0f49ad61c respectively). This lane changed no ref and deleted nothing either. The remote-reachability limit above was NOT re-measured at this commit and is carried forward from 2fe7a70df."
---

Created 2026-08-31 to close a COVERAGE gap; re-graded 2026-09-10 by the w14/recon
reconciliation lane against `2fe7a70df`. The full archaeology -- fork points,
per-branch artifact tracing and the remote-reachability measurement -- is in
`.planning/RECON-1269-1272-1324-2026-09-10.md`.

c1, c2 and c3 were re-graded the same day by the `w15/rec` lane against
`fdf4b1e1c`, and c4's objects were re-resolved there. That lane's corrections
-- the `lane/f13-n-dataloss` test residual does not exist, `schema_digest`
cannot move, and `source_inputs` is not confined to `wcore-protocol/src` -- are
in `.planning/RECORD-1269-1272-1324-2026-09-10-w15.md` sections 0 and 1. No ref
was renamed by either lane; c1 names the four renames it is waiting on.

`scripts/check-criteria-ledger.py` scopes every open `area:core` issue on
wayland and EVERY open issue on wayland-core. This issue was in scope from
the moment it was filed and had no ledger file, so
`scripts/check-release-readiness.py` -- which reads ledger files and nothing
else -- could not count it. CI runs the coverage gate with `--offline`, the
arm that would have reported the gap, so nothing said so for two days.

Criteria are transcribed from the issue body without edit. Where the body's
wording is loose it is LEFT loose rather than tightened here: sharpening a
criterion inside the ledger is how a criterion quietly becomes an easier
adjacent property. Whoever takes this restates it on the ISSUE first.

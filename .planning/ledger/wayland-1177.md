---
issue: 1177
repo: FerroxLabs/wayland
kind: defect
title: "An outer workflow retry overwrites junit.xml, erasing a genuine failure before any grader sees it"
status: open
last_verified_commit: 9de21aa1
criteria:
  - id: c1
    text: "A failure on attempt 1 followed by a pass on attempt 2 leaves evidence the required report check can read"
    state: not-met
    evidence: "file:.github/scripts/run-tests-with-attempt-evidence.sh:101:cp "$JUNIT_PATH" "$ATTEMPT_DIR/outer-attempt-${attempt}.xml""
    owner: core
    handoff: "FerroxLabs/wayland-core#404"
    note: "STILL not-met 2026-08-31. THE LIVE ARM FINALLY DECIDED, AND IT REFUTED THE FIX IT WAS BUILT TO PROVE -- THEN NAMED THE NEXT LAYER EXACTLY. Run 33320774111, branch lane/f13-w3-1177-verify @ 9320f7495 (lane HEAD 925ded18d + the probe), read off the jobs API rather than inferred. THE ANTECEDENT OF c1 HAPPENED FOR THE FIRST TIME: `CI (linux-containerized)` log carries `attempt 1 FAILED (exit 100); preserved its JUnit as outer-attempt-1.xml` at 18:31:28Z and `attempt 2 passed` at 18:51:04Z, `outer-retry attempt 2` appears, and the step `Run tests (nextest CI profile)` is SUCCESS with `Upload nextest JUnit report` SUCCESS. A failure on attempt 1 followed by a pass on attempt 2, with the evidence uploaded. THE INVERSION'S KEY PROPERTY IS CONFIRMED: `CI (${{ matrix.os }})` and `CI (windows-latest, hosted)` were both SKIPPED -- the exact condition under which the old enumerated `if:` excluded itself -- and the step `Assert test evidence exists` nevertheless RAN. Cause (b) also fired: `No dependency failed` FAILED on a red `ci-linux`, and `Run actions/checkout@v6` and `Download nextest JUnit artifacts` were both SKIPPED with it, while the two steps carrying status-check functions (`Annotate Windows coverage`, `always()`; the evidence gate, `!cancelled()`) both ran. That is the control and the refutation in one job. AND THE GATE STILL GRADED NOTHING: it exited 127 with `bash: .github/scripts/assert-test-evidence.sh: No such file or directory`, because the CHECKOUT that puts that file on disk had been skipped by the same mechanism the gate was just made immune to. A required check reporting `No such file` where it means `a dependency failed` is a red naming the wrong cause, and grade-retry-flakes.sh never read the outer-attempt-1.xml sitting in the artifact store. ADMITTING THE GATE IS NOT ENOUGH -- ITS PREREQUISITES MUST BE ADMITTED ON THE SAME TERMS, and that is now enforced structurally rather than for these three steps: in ANY job that itself runs unconditionally (`if: always()` / `!cancelled()` -- which is exactly what an aggregate report job is), every step up to and including its last gate must be admitted unconditionally too. gate-admission.py computes that from the parsed workflows, so it covers `report` and `E2E report` today and any aggregate job added later. Steps AFTER the last gate are deliberately unconstrained (`Publish test report` is guarded on hashFiles on purpose, and by then the gate has graded). ci.yml's checkout and artifact download and e2e.yml's checkout, download and scope steps now carry `!cancelled()`. RED ARMS for that rule, hetzner-dsm 2026-08-31, each YAML-parsed first and each restored: R8 drop the `if:` from ci.yml's report checkout -> report-gate-wiring 42/1; R9 drop it from e2e.yml's download -> 42/1; R10 invert the rule's own job filter inside the guard -> the anti-vacuity arm reds as well, so the rule is not scoped to nothing. WHY c1 IS STILL not-met: the criterion asks the required check to CONCLUDE FAILURE naming the erased test. On 33320774111 `report` did conclude failure, but for a dependency failure and a 127, not for the erased test -- and `ci-linux` itself was red on `Shared-process integration suite` (the pre-existing wayland#1199 / FerroxLabs/wayland-core#401 blocker), which is not this ticket's. Closing it needs the same probe re-pushed on THIS tree, where the gate's prerequisites survive, and the report job's annotation quoted. That is one push away and is this row's residual, not a grade. AND THE SECOND FIX FOR IT HAD ALREADY BEEN REFUTED AND REPLACED BEFORE THAT RUN WAS READ. WHAT WAS REFUTED, 2026-08-31, second adversarial re-verification: not the GRADE (c1 stays not-met, and the verifier upheld that) but the FIX. The guard added by 49b23e43c asserted that the evidence gate condition contained no `needs` reference and this note asserted three times that that made the class impossible -- `so the class cannot recur`, `it is the property that makes the defect impossible`, `it proves the condition cannot exclude itself`. ALL THREE WERE FALSE. `needs`-absence is NECESSARY, NEVER SUFFICIENT: it is still a NEGATIVE test over an OPEN alphabet of switchable conditions. The verifier produced two, both YAML-valid, both passing all 129 assertions on the branch, both restoring the exact inert-gate class. M1 `if: ${{ hashFiles(junit-reports/**/*.xml) != }}` -- the literal idiom already in use twelve lines below at the `Publish test report` step, so it is the form somebody copies, and it goes inert PRECISELY when there is no evidence, which is the case the step exists to catch. M2 `if: ${{ success() }}` -- inert once any earlier step in the job has failed, which is cause (b) recorded further down this very note. A third arm, deleting the `if:` line entirely, reddened at 29 passed 1 failed, so the guard was live and simply testing the wrong property. THE PREDICATE IS NOW POSITIVE AND CLOSED (commit f025dda2d): the condition must be EXACTLY one of {always(), !cancelled()}, which is decidable by string equality over a two-element set. Those two are the only GitHub expressions that BOTH survive an earlier failed step in the same job AND cannot be falsified by an upstream job result -- the property the note below derives from run 33312653107 and then did not assert. Every other condition, named or unnamed, present or invented later, is rejected without the guard having to know what it is. IT IS PROVEN IN BAND, because an allowlist read backwards accepts everything and reports a clean tree: a polarity arm feeds it M1, M2, the pre-fix e2e form, the retracted enumeration, `always() && needs.ci.result != skipped` and `failure()`, and requires every one to be REJECTED, plus an anti-vacuity arm requiring the two sanctioned forms to be ACCEPTED. RED ARMS, hetzner-dsm 2026-08-31, each YAML-parsed before its red was believed and each restored blob-identical to HEAD with touch afterwards: R1 (M1) -> report-gate-wiring 40/1 and outer-retry-evidence 31/1, both on the admission assertion; R2 (M2) -> the same two, the same way; R6, adding `success()` to the allowlist inside the guard itself -> the polarity arm reds, so the control is not decoration. THE RULE IS A SWEEP, NOT A STEP. `needs`-absence was also only ever asserted for ONE of the shared gate two call sites: the verifier found e2e.yml `Assert E2E evidence exists` still carrying `needs.e2e.result != cancelled` after ci.yml was fixed. Scripts under .github/scripts/ now declare `ADMISSION: unconditional` or `ADMISSION: caller-decides`; every script any workflow invokes must carry exactly one (9 of 11 are invoked, all 9 classified), and .github/scripts/tests/gate-admission.py finds the call sites by parsing all 18 workflow files and grades every caller of an unconditional gate. e2e.yml is inverted with ci.yml and its behaviour change is stated in the file. Red arms: R4, reverting e2e.yml to its old condition -> report-gate-wiring 40/1; R5, deleting the ADMISSION line from assert-test-evidence.sh -> the classification arm reds, so a gate cannot join the repository unclassified. TWO N+1s OF THE SAME SHAPE, TWELVE LINES ABOVE THE FIXED STEP, ARE CLOSED WITH IT. (i) `No dependency failed` hand-listed six job names beside a six-entry `needs:` with nothing keeping them in sync; the verifier deleted `check ci-linux` and all five suites stayed green, which would have let the required check pass silently over a failed ci-linux. It now runs .github/scripts/assert-no-dependency-failed.sh over `${{ toJSON(needs) }}`, so the set graded IS the set depended on by construction and there is no second list to drift; 17 self-tests in dependency-aggregate.test.sh, including fail-closed on an unrecognised conclusion string, on an empty needs object, on unparseable input and on absent input, with a red arm (R7: let the unknown branch pass -> 15/2). (ii) `report.needs` is now checked against the ci.yml job roster: every job must be aggregated or declared `not-aggregated` with a reason, and a declaration naming a job that no longer exists also reds. Red arm R3, dropping ci-linux from `needs:` -> report-gate-wiring 40/1. build-darwin-selfhosted is declared rather than aggregated -- it runs no tests, is gated to lane/** pushes, and aggregating it would red every lane push whenever the personal self-hosted Mac is offline -- carrier FerroxLabs/wayland-core#405. PRIOR NOTE PRESERVED. STILL not-met 2026-08-30, AND THE FIRST FIX FOR IT IS SUPERSEDED BY THIS LANE ITSELF. (1) THE ENUMERATION IS RETRACTED. fc22f891b closed the self-skip by adding `needs['ci-linux']` to the step's `if:`. That answers the run it was built from and nothing else: \"was one of these NAMED jobs admitted?\" ranges over an open set of job names, so a producing leg added later, or `ci-linux` itself cancelling, switches the gate off again with the evidence sitting in the artifact it just downloaded. 49b23e43c inverts the predicate instead: the condition names NO job (`if: ${{ !cancelled() }}`) and assert-test-evidence.sh decides from the artifact directory, which is the one question this step exists to answer and is decidable and total. THAT SENTENCE REPLACES A FALSE ONE, AND THE CORRECTION IS THE POINT OF THIS EDIT: the previous revision of this note claimed `UPSTREAM_RESULT is rendered from toJSON(needs), so every dependency reports and none is enumerated`. It is not and was not. A toJSON form was tried and REVERTED in commit 925ded18d, on this branch, and the note was not updated with the revert -- so on the row whose whole argument is that the enumeration is retracted, the graded artifact claimed a non-enumerating field that reads `UPSTREAM_RESULT: ci=${{ needs.ci.result }} ci-linux=${{ needs[ci-linux].result }}` at ci.yml. The field is still an enumeration, it is still a DIAGNOSTIC and not the admission gate, and it is now labelled as such in ci.yml rather than described as something it is not. The same ci.yml comment credited failing-set.test.sh with asserting the literal `run: bash .github/scripts/assert-test-evidence.sh`; that assertion is at report-gate-wiring.test.sh:54 and is not in failing-set.test.sh at all (grep, both files, in one call). Both statements are corrected at HEAD. The wiring arm is inverted with it -- it no longer asks whether the condition mentions ci-linux (which passes for any list of names) but whether it contains a `needs` reference AT ALL, with an anti-vacuity arm that reds if the condition cannot be located, because a grep for absence is satisfied for free by an empty extraction. Both arms proven by mutation on a tree that parsed as YAML first: putting a job name back -> FAIL `the evidence gate's condition names no upstream job`, rc=1; renaming the step -> FAIL `...can be located (anti-vacuity)`, rc=1; restored 30 passed 0 failed. (2) THE ENUMERATING FIX WAS MEASURED INSUFFICIENT ON THE VERY RUN BUILT TO PROVE IT, and this is evidence rather than argument. Run 33312653107 (branch lane/f13-w3-1177-verify @ a9ac3b72, carrying fc22f891b): `CI (${matrix.os})` skipped, `CI (windows-latest, hosted)` skipped -- the exact defect condition -- and the step `Assert test evidence exists` was SKIPPED AGAIN, for a cause the disjunction never touched. TWO independent causes, read off the job rather than inferred: (a) `ci-linux` was CANCELLED at its 120-minute budget, and BOTH arms of the disjunction exclude `cancelled`, so the whole condition is false; (b) the earlier step `No dependency failed (a required check must grade what it aggregates)` (ci.yml:2720, which carries no `if:` of its own) FAILED, and GitHub skips every later step whose `if:` is a plain expression once a step has failed -- only a status-check function (`always()`, `failure()`, `!cancelled()`) overrides that. The disjunction is a plain expression. `!cancelled()` is a status-check function, so the inverted condition runs in both (a) and (b) where the enumeration does not. `Annotate Windows coverage`, the one report step carrying `if: always()`, DID run on that job, which is the control for that reading. (3) THE LIVE ARM HAS NOT DECIDED, AND IT IS BLOCKED ON CI CAPACITY RATHER THAN ON THE FIX. c1 needs a real `report` job to CONCLUDE FAILURE naming an erased probe test. Two attempts, both lost: run 33309809060 was cancelled by a superseding push to the same branch; run 33312653107 lost `ci-linux` to the 120-minute timeout at step 28, BEFORE step 30 `Upload nextest JUnit report`, so the report job's `Download nextest JUnit artifacts` was skipped and there was no evidence to grade at all. That destruction-of-evidence-by-cancellation is filed as FerroxLabs/wayland-core#404 and is this row's handoff: while it holds, no criterion that must quote a live required check is closable. GRADING c1 MET ON THE AUTHORED FIX AND ITS WIRING TEST WOULD BE THE SUBSTITUTION THIS TICKET HAS ALREADY SHIPPED SIX TIMES -- \"the condition is right\" is strictly weaker than \"a failure retried into a green step reds the required check\", and the difference is the branch that has never executed end to end. PRIOR NOTE PRESERVED. THE DECIDING CONDITION IS NOW LOCATED AND PROVEN, 2026-08-30 (w3-test-infra lane), and the criterion is graded on the required check's CONCLUSION rather than on the wrapper this ticket edited. THE RETRY PATH FINALLY EXECUTED FOR REAL. Run 33303418632, job 99238946686, branch lane/f13-w3-test-infra-probe (origin/integ/f13 plus a probe test that reads the wrapper's own attempt counter and fails while it says 1, plus an #[ignore] on the pre-existing wayland#1199 red that would otherwise red the FINAL attempt). Log, verbatim: `attempt 1 FAILED (exit 100); preserved its JUnit as outer-attempt-1.xml` at 10:42:13Z after `Summary [1081.980s] 17460 tests run: 17459 passed, 1 failed`, then `attempt 2 passed` at 11:00:25Z after `Summary [1068.824s] 17460 tests run: 17460 passed`. `outer-retry attempt 2` appears once; `probe INERT` appears ZERO times, so the probe's own fail-open path did not fire and it really did grade the counter. The step `Run tests (nextest CI profile)` is GREEN. THAT IS THE ANTECEDENT OF c1, ON A REAL RUNNER, FOR THE FIRST TIME. AND THE REQUIRED CHECK STILL WENT GREEN, WHICH IS WHY c1 IS STILL not-met. The report job (99251025833) downloaded the evidence -- its own log reads `artifacts downloaded : junit.xml outer-attempts` and its summary table renders `junit-reports/outer-attempts/outer-attempt-1.xml  17459 OK  1 FAIL` -- and concluded SUCCESS in 14 seconds. CAUSE, read off the job's own step list rather than inferred: the step `Assert test evidence exists (a skipped test step is not a pass)` was SKIPPED. That step is the sole invoker of assert-test-evidence.sh, which is the sole invoker of grade-retry-flakes.sh, so the grader never ran and nothing read the file that had just been preserved. Its `if:` named ONLY `needs.ci.result`, and `ci` is the macOS matrix, which is opt-in behind a `[ci-darwin]` commit marker: on that run `CI (${matrix.os})` = skipped and `CI (windows-latest, hosted)` = skipped while `CI (linux-containerized)` = success. So the one gate that reads outer-retry evidence excluded itself on EVERY lane/** push that does not opt into the macOS matrix -- HOW OFTEN THAT HAPPENS IS NOT CHARACTERISED AND IS NOT CLAIMED: the matrix was SKIPPED on run 33303418632 and RUNNING on run 33309835036 minutes later, both plain lane/** pushes with no [ci-darwin] marker, so its admission is budget-dependent and any reading of this note as 'every lane push' is RETRACTED as unmeasured. The defect does not depend on the rate: a gate whose execution is decided by an unrelated job's admission is inert whenever that job is not admitted. This is the third layer of the same defect: #1169 closed the nextest layer, this ticket's wrapper closed the preservation layer, and the READING layer was inert. It is NOT wayland#1216: that ticket is about EXPECTED_MIN counting per-leg and about preserved attempts inflating the coverage count; this is the step not running at all. FIX AUTHORED AT HEAD (commit fc22f891b, ci.yml): the condition now also consults `needs['ci-linux']`, the job that actually runs the workspace suite, and UPSTREAM_RESULT reports both. A wiring arm in .github/scripts/tests/outer-retry-evidence.test.sh asserts the condition names ci-linux, and says in its own comment that it is a wiring check and not the demonstration. VERIFICATION IS IN FLIGHT AND THIS CRITERION MUST NOT BE MOVED TO `met` WITHOUT IT: run 33309809060 on branch lane/f13-w3-1177-verify (this lane's HEAD + the same probe + the same #1199 ignore) is the arm. It closes c1 only if its `report` job CONCLUDES FAILURE naming the erased probe test; if it concludes success the fix is wrong and the cause above is incomplete. A green report on that branch is a REFUTATION, not a pass. WHAT WAS SUBSTITUTED BEFORE AND IS NOT SUBSTITUTED NOW: the previous revision of this note recorded run 33264117264 -- `outer-retry attempt 1` present, `outer-retry attempt 2` absent, leg green -- which proves the mkdir blocker is gone and proves nothing about c1, because the retry never ran. The run above is the opposite shape: the retry ran, the evidence survived, and the check that was supposed to read it was switched off. PRIOR NOTE PRESERVED: RE-ANCHORED 2026-08-30 for wayland#1198: was :90, which the 2026-08-29 sweep recorded as the `cp` line and which is now an echo -- the file drifted 11 lines under the anchor. The preservation copy is at :101. Each outer attempt's junit.xml is copied to outer-attempt-<n>.xml; wired at ci.yml:1657 under nick-fields/retry max_attempts: 2, uploaded at ci.yml:2005-2007 and read by grade-retry-flakes.sh:168 inside the required report job. The outer retry was PRESERVED, not dropped: the script also removes the stale report before each attempt and writes final-status.txt so 'retried into green' is distinguishable from 'red on the final attempt'. REFUTED 2026-08-29 by the 0.13.12 close-sweep, recorded verbatim: Evidence resolves — `.github/scripts/run-tests-with-attempt-evidence.sh:90` is the `cp '$JUNIT_PATH' '$ATTEMPT_DIR/outer-attempt-${attempt}.xml'` line, wired at ci.yml:1676 under nick-fields/retry@v3 max_attempts:2, uploaded at ci.yml:2024-2026, read by grade-retry-flakes.sh:167-202 which runs inside the required `report` job via assert-test-evidence.sh:85-91. The logic is right on paper and I confirmed it end-to-end against fixtures. But it has NEVER once worked on a real runner, and the one time it executed it did not merely fail to preserve evidence — it destroyed the whole test leg. On the fix's own CI run (33227927478, job 99035159787, 2026-08-29T02:33:48Z, sha 2282de368) the log reads verbatim: `##[group]Attempt 1` `mkdir: cannot create directory ‘target/nextest’: Permission denied` `##[warning]Attempt 1 failed. Reason: Child_process exited with error code 2` `##[group]Attempt 2` `mkdir: cannot create directory ‘target/nextest’: Permission denied` `##[error]Final attempt failed. Child_process exited with error code 2` Cause: ci.yml:1156 documents that `docker run` here has NO `-u`, so the container runs as root while the bind-mounted workspace is uid 1001. `target/` is therefore created root-owned by the earlier `Pre-build`/`clippy` container steps, and the wrapper — which runs on the HOST as uid 1001 — dies at `mkdir -p '$ATTEMPT_DIR' || exit 2` (line 66) before it ever invokes nextest. So nextest never ran, no junit.xml was written, no attempt was preserved, and the required `report` check received zero evidence from this leg. Every subsequent run I checked (33255873933, 33256631848, 33257261615, 33257637102, 33258318108, 33258433476, 33258531983, 33258742623, 33258852685) has `Run tests (nextest CI profile)` = skipped because the job died earlier, so nothing has re-exercised it. No later commit repairs it: `git log 2282de36..HEAD -- ci.yml run-tests-with-attempt-evidence.sh` shows only unrelated changes, there is no `chown`/`sudo` of `target/` anywhere in ci.yml, and line 66 at HEAD (5eb2d1ef) is still the bare `mkdir -p`. The criterion as written — 'leaves evidence the required report check can read' — is false in the only environment that matters. UPDATE 2026-08-30: the mkdir BLOCKER is removed. e7144c30 reserves target/nextest/ci/outer-attempts as the runner user before any container step makes target/ root-owned, and on run 33264117264 the wrapper reached `outer-retry attempt 1` with `attempt dir : target/nextest/ci/outer-attempts` and no permission error; the leg then ran 17,341 tests, all passing. THAT IS NOT THIS CRITERION AND MUST NOT BE READ AS IT. That run contains ZERO occurrences of `outer-retry attempt 2` -- attempt 1 passed, so the retry path was never exercised and nothing was preserved or read. The criterion needs attempt 1 to FAIL and attempt 2 to PASS, with the required report job reading outer-attempt-1.xml. TO CLOSE IT: make one test fail deterministically on the first attempt only (an env-var-gated fixture the wrapper unsets between attempts), push, and quote the report job reading the preserved attempt. A green run where attempt 1 succeeded is evidence for the BLOCKER being gone and evidence for nothing else -- grading it as c1 is the substituted-property failure this cycle has now shipped six times. LIVE RUN 2026-08-30, AND IT IS THE FIRST TIME THE RETRY PATH HAS EVER EXECUTED -- BUT IT DOES NOT CLOSE THIS ROW. Run 33300234922 on lane/f13-w3-test-infra-probe @ 94aee056c, job 99226871126 (CI (linux-containerized)). A deliberate red arm was pushed for exactly this criterion: crates/wcore-types/tests/outer_retry_attempt_evidence_probe.rs reads the wrapper's own counter at target/nextest/ci/outer-attempts/.attempt and asserts attempt >= 2, so it fails on attempt 1 and passes from attempt 2 on. WHAT THE RUN PROVES, verbatim from the job log: `##[group]Attempt 1` / `-- outer-retry attempt 1 (wayland#1177) -----------------------` ... `attempt 1 FAILED (exit 100); preserved its JUnit as outer-attempt-1.xml` / `##[warning]Attempt 1 failed. Reason: Child_process exited with error code 100` / `##[group]Attempt 2` / `-- outer-retry attempt 2 (wayland#1177) -----------------------`. So: the mkdir blocker really is gone, the wrapper ran on a real runner, attempt 1 really failed, the preservation copy at :101 really executed, and BOTH `outer-retry attempt 1` and `outer-retry attempt 2` appear -- which no previous run on this ticket has ever achieved. Every earlier grading quoted a run containing attempt 1 and ZERO occurrences of attempt 2. THE PROBE BEHAVED EXACTLY AS DESIGNED, established by arithmetic rather than by absence-from-log: attempt 1 `Summary [1065.579s] 17461 tests run: 17459 passed (1 leaky), 2 failed` with `test outer_retry_evidence_probe_is_red_on_attempt_1_only ... FAILED` (three times -- the ci profile's retries = 2, and the counter does not move within one nextest process); attempt 2 `Summary [1030.923s] 17461 tests run: 17460 passed (1 leaky), 1 failed`. The delta between the two failure counts is exactly the probe. It is also NOT vacuous elsewhere: on CI (macos-latest), where the wrapper never runs and the counter file does not exist, the same test is collected and runs INERT -- `PASS [ 0.024s] (17262/17321) wcore-types::outer_retry_attempt_evidence_probe outer_retry_evidence_probe_is_red_on_attempt_1_only` -- which also disproves the vacuity risk that the test target might never be collected at all (wcore-types has no `autotests = false` and already carries four tests/ targets). WHY THIS IS STILL not-met, stated as the reason rather than worked around: the criterion is 'a failure on attempt 1 followed by a PASS ON ATTEMPT 2'. Attempt 2 did NOT pass. A SECOND, unrelated test failed on BOTH attempts -- `wcore-agent::issue_1150_unknown_context_window_test an_unknown_window_sizes_the_skill_listing_like_the_window_it_assumes`, TRY 1/2/3 FAIL on each -- so the step's conclusion is `failure`, the wrapper wrote final-status.txt = failure, and the grader's 'retried into a green run' branch (the ONE branch this criterion is about) was therefore never taken. The `report` job had not run at all when this was recorded, because it waits on the CI jobs and the run was still in progress. GRADING IT MET ON THIS RUN WOULD BE THE SEVENTH SUBSTITUTION ON THIS TICKET: 'the retry path executed and preserved an attempt' is a strictly weaker property than 'a failure retried into a GREEN step is visible to the required report check', and the difference is precisely the branch that has never been exercised. THE BLOCKER IS PRE-EXISTING AND ALREADY OWNED -- searched before concluding, both repos listed and grepped with a known-positive control in the same call: `an_unknown_window_sizes_the_skill_listing_like_the_window_it_assumes` fails on CI (linux-containerized), CI (macos-latest) AND CI (Array)/Windows at this base, and its carrier is FerroxLabs/wayland#1199, which names crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs explicitly in its Where section. It is NOT caused by the probe. TO CLOSE THIS ROW: re-push the same probe branch on a base where #1199's test is green (or with that one test filtered out of the leg), and quote the report job reading outer-attempt-1.xml against final-status.txt = success. The probe file is written to be deleted once that is quoted; it is on lane/f13-w3-test-infra-probe and is marked NOT FOR MERGE."
  - id: c2
    text: "A test demonstrates that visibility and fails against today's workflow"
    state: met
    evidence: "file:.github/scripts/tests/outer-retry-evidence.test.sh:436:report_gate "attempt-1 failure retried into a pass REDS the required report check""
    owner: core
    note: "MET 2026-08-30 (w3-test-infra lane), by removing the substitution the close-sweep refuted rather than by adding more cases beside it. PART E now RUNS the required check instead of grepping for its name. The sweep's refutation was precise: Parts A/B exercise the wrapper and the grader as separate programs in a tmp dir the test owns, and Part C was three `grep -qF` calls; a grep for the string `bash .github/scripts/run-tests-with-attempt-evidence.sh` is not a demonstration that the report check sees anything, and the proof was that all 19 cases were GREEN on a tree where the mechanism was 100% inoperative on the real runner. Part E builds the artifact tree the `report` job actually downloads -- attempt 2's clean junit.xml beside outer-attempts/outer-attempt-1.xml and final-status.txt=success -- and invokes `.github/scripts/assert-test-evidence.sh`, which IS the entry point the required check runs (it is the file that locates and calls grade-retry-flakes.sh, fail-closed on its absence). MEASURED on hetzner-dsm at HEAD: exit 1, and the emitted ::error carries both `wayland#1177` and the erased test's name `races_under_load`. TWO NEGATIVE CONTROLS in the same call, because a gate that reds everything proves nothing: the identical evidence set with NO preserved attempt exits 0, and a preserved attempt behind final-status=failure exits 0 (an ordinary red is not double-reported). Suite is 28 passed / 0 failed at HEAD, up from 19/0. THREE RED ARMS, hetzner-dsm 2026-08-30, each on a DIFFERENT deciding condition, each read back out of the file before and after, each `bash -n` exit 0 on the mutated file so no arm is a syntax error masquerading as a red, and each restored blob-identical (git hash-object == git rev-parse HEAD:path) with touch afterwards. M1, assert-test-evidence.sh:96 `bash "$GRADER" || RETRY_RC=$?` -> `RETRY_RC=0`: the required check stops invoking the grader and exactly the three Part E positive cases fail (want exit=1, got exit=0); both negative controls stay green. M2, grade-retry-flakes.sh:171 `if [ -f "$st" ] && grep -qx 'failure' "$st"; then continue; fi` -> `:`: the grader stops honouring final-status and only the two double-reporting cases fail (want exit=0, got exit=1). M3, grade-retry-flakes.sh:311, drop `|| [ "$OUTER_UNLISTED" -gt 0 ]` from the failing condition: 24 passed / 4 failed, the four being the outer-attempt cases in Parts A, B and E. Baseline returns to 28/0 after each restore. WHAT THIS STILL DOES NOT DO, stated rather than left to be found: it runs the report gate as a program over a fixture evidence tree, so it grades the composed script stack and not a live GitHub Actions replay. That residue is what c1 exists for, and c1 is graded on a real runner rather than here. PRIOR NOTE PRESERVED: RE-ANCHORED 2026-08-30 for wayland#1198: moved off the comment at :84 onto the line that builds the failed-attempt-1 fixture, which is the demonstration itself. Three parts: grade-retry-flakes.sh on fixture evidence, the writer against a stub that fails then passes, and a grep of the ci.yml wiring. Two negative controls (a clean suite stays green; an ordinary red is not re-reported) plus the agent-crash shape where no JUnit is written. It grades the scripts and the wiring, not a live CI replay. REFUTED 2026-08-29 by the 0.13.12 close-sweep, recorded verbatim: The test exists and is real work. `.github/scripts/tests/outer-retry-evidence.test.sh:84` resolves (a comment inside the primary defect case at :83-93). I ran it at HEAD on hetzner: 19 passed, 0 failed. It is wired into CI at `.github/workflows/lint.yml:129` (`Self-test the CI evidence gates`, default `bash -e -o pipefail`, so a failure reds the step). It genuinely fails against the pre-fix tree: I reconstructed 2282de36^ (974e2474) into a scratch dir and ran today's test against it — 8 passed, 11 failed, including `attempt-1 failure retried into a green step reds the report (want exit=1, got exit=0)`. It is not vacuous: five mutations, each verified to land on CODE and each reverted with `touch` afterward — delete the `cp` at wrapper:90 → 17/2; break the fail-closed on absent final-status at grader:171 → 18/1; delete `rm -f '$JUNIT_PATH'` at wrapper:82 → 18/1; drop `[ '$OUTER_UNLISTED' -gt 0 ] ||` from grader:311 → 16/3; strip the wrapper from ci.yml:1676 → 18/1. Baseline restored to 19/0 after each. Both negative controls pass in both arms. Why it still does not hold: the ticket asks for 'a test demonstrating that a failure on attempt 1 followed by a pass on attempt 2 is still visible to the required `report` check'. This test substitutes a weaker property — Part A/B run the scripts in a tmp dir the test itself owns, and Part C is two `grep -qF` calls against ci.yml plus one against assert-test-evidence.sh. A grep for the string `bash .github/scripts/run-tests-with-attempt-evidence.sh` is not a demonstration that the report check sees anything. The proof is that this suite is 19/19 GREEN on a tree where the mechanism is provably 100% inoperative on the real runner. The ledger note concedes it ('It grades the scripts and the wiring, not a live CI replay') — that concession is exactly the hole the break shipped through. This is the same substituted-property failure already recorded on wayland#559 c6 and wayland-core#358 c1."
  - id: c3
    text: "The #1169 retry-flake grader keeps working on the nextest layer it already covers"
    state: met
    evidence: "file:.github/scripts/grade-retry-flakes.sh:119:find "$EVIDENCE_DIR" -type f -name "*.xml" -print0"
    owner: core
    note: "The #1169 nextest-layer scan is intact at :119; the outer-attempt scan at :168 is an ADDITIONAL pass keyed on outer-attempt-*.xml plus final-status.txt, with an absent status graded fail-closed."
---

`.github/workflows/ci.yml` wraps the containerized Linux workspace test run in
`nick-fields/retry@v3` with `max_attempts: 2`. The second attempt overwrites
`target/nextest/ci/junit.xml`, so a test that genuinely failed on attempt 1 and
passed on attempt 2 leaves no structured trace at all. It is not even recorded
as a flake, because the flake record lived in the file that was just
overwritten.

This is the same defect as #1169 one layer up, and strictly worse. #1169's
grader runs inside the required `report` job and reads that XML — so it cannot
see this layer, because the outer retry destroys the evidence before the grader
runs. The fix for #1169 is real and incomplete.

Nothing has been done. The issue also records a practical obstacle for whoever
picks it up: editing `ci.yml` needs a token with `workflow` scope, which the
hetzner build box does not have. That is a routing detail, not a blocker on
another lane, so this stays core's and not-met rather than blocked.

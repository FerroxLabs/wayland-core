---
issue: 1203
repo: FerroxLabs/wayland
kind: defect
title: "The spend-audit trail is keyed by a throwaway UUID minted per engine construction, then swapped for the real session id on a /model rebind"
status: open
last_verified_commit: dba806bc4
criteria:
  - id: c1
    text: "install_spend_guard receives the authoritative budget_session_id() at all three call sites, not uuid::Uuid::new_v4() at two of them"
    state: met
    owner: core
    note: "MET, and the substitution is declared rather than hidden. The criterion says install_spend_guard must receive budget_session_id() at all three call sites. Taken as a literal instruction that is UNIMPLEMENTABLE at two of them: both constructors run before any self exists and before init_session binds the budget authority, so there is no authoritative id in scope to pass. What is delivered is the property the criterion is FOR, and it is strictly stronger: install_spend_guard now takes a SHARED wcore_budget::SessionIdentity handle (spend.rs, a Arc<Mutex<String>>), the guard reads it when a record is SEALED rather than when it is constructed, and AgentEngine::publish_spend_session (engine.rs:6229) is the single writer, called from emit_task_spend_audit, the operator escalation path and rebind_provider. So the key equals the run authoritative identity at every WRITE, which is what makes the trail groupable; passing a string at construction could only ever be right by luck. The two former uuid::Uuid::new_v4() arguments are gone: the fresh path seeds from the engine own conversation_id (engine.rs:5008) so an unsessioned run still keys per conversation rather than collapsing to a shared session-unknown placeholder, and the resume path seeds from session.id (:5294), which is exactly what bind_budget_authority will resolve. Third call site (rebind_provider, :7022) now carries the SAME handle forward instead of resolving a fresh string. Verified at HEAD, not inherited from the prior commit."
    evidence: "file:crates/wcore-agent/src/engine.rs:5294:let spend_session = wcore_budget::SessionIdentity::new(&session.id);"
  - id: c2
    text: "A test asserts the SpendAuditRecord.session_id written on a fresh construction, on a resume, and after rebind_provider are the SAME id"
    state: met
    owner: core
    note: "MET. The test drives ONE conversation through all three guard-installing paths against the real engine and the real JsonlSpendAuditSink under an isolated WAYLAND_HOME: a fresh construction, a resume_with_provider of the same session in a NEW engine (a second launch), then set_model plus a real rebind_provider, then a third task. It asserts the set of session_id values across every record on disk -- task records AND the operator escalation record -- is exactly one. A sibling test, an_unsessioned_run_keys_its_own_conversation_and_two_of_them_do_not_collide, is the negative half: it forbids the fix being read as always use the session id, which would merge every unsessioned conversation on the host into one bucket. RED-ARMED 2026-08-30 on hetzner with two DISCRIMINATING mutations, each compiled first (cargo check -p wcore-agent --tests exit 0) and each proven to reach the built binary (Compiling wcore-budget in the rebuild log). ARM A made SessionIdentity::set a compiling no-op: the shared-key test FAILED (one conversation must produce ONE spend-audit key) while the unsessioned test PASSED. ARM B made SessionIdentity::new ignore its seed and return a constant: the unsessioned test FAILED (not a shared placeholder: session-shared) while the shared-key test PASSED. Each test is reddened by a DIFFERENT mutation. Both restores verified by blob identity against git rev-parse HEAD:<path>, and the suite is green after."
    evidence: "test:crates/wcore-agent/tests/spend_governance_test.rs::one_conversations_spend_records_share_one_key_across_a_resume_and_a_rebind"
  - id: c3
    text: "A live run is quoted: one conversation's records in ~/.wayland/budget/spend-audit.jsonl share one key across a /model switch and across a --resume"
    state: superseded
    owner: core
    note: "DECOMPOSED into FerroxLabs/wayland#1244 (open, area:core), which carries the remainder with its own gradeable criteria. TWO of the three legs are closed LIVE on hetzner with the shipped debug binary against a loopback OpenAI-compatible stub under an isolated WAYLAND_HOME. Fresh launch with --session-id aa551203-0000-4000-8000-000000001203, then a second process with --resume of the same id; ~/.wayland/budget/spend-audit.jsonl then held 2 task_spend_audit records, DISTINCT_KEYS = 1, and the key is the operator own session id, not a uuid and not session-unknown. A/B AGAINST BASE, which is what makes this a measurement rather than a screenshot: the same five product files were reverted to 9e0d05b7e^ (the pre-fix tree, with &uuid::Uuid::new_v4().to_string() printed at engine.rs:4991 and :5265 to prove the arm landed on executable code), rebuilt, and the identical two runs produced 2 records under TWO DIFFERENT random keys, 346dfe04-... and 4a9e72d0-... . All five files restored and verified by blob identity. THE MISSING LEG is across a /model switch. It is not reachable headlessly: rebind_provider has exactly one production caller, the TUI engine bridge (engine_bridge.rs:2469 and :2499), and wcore_agent::slash registers no model handler -- verified with a known-positive control in the same query, where grepping the same files for the registered commands returns help / clear / exit / agent / style / memory / plugin / skill and grepping for model returns nothing. A PTY-driven TUI run was attempted and is recorded as a failure, not a pass: the TUI never rendered into the pty (1,289 bytes captured, all of it pre-TUI stderr) and both audit records came back with baseline_model openai/stub-model-v1, i.e. the rebind never fired. The engine-level rebind IS covered by c2 above. See #1244."
---

The spend-audit trail is keyed by a throwaway UUID that is minted fresh on every engine construction, never persisted, never restored on resume -- and then silently swapped for the REAL session id mid-session on a `/model` rebind. This is the exact defect class #1161 just closed for the cache ledger, still open one crate over in the spend ledger. `install_spend_guard` is called with `&uuid::Uuid::new_v4().to_string()` as its `session_id` at BOTH construction sites -- engine.rs:4758-4765 (`new_with_provider`) and engine.rs:5031-5038 (`resume_with_provider_parts`, three lines above the #1161 fix itself). `SpendGuard::new` (spend_guard.rs:137-157) hands that string straight into `SpendAuditor::new(...)` and `EscalationGate::new(session_id, ...)`, and it lands verbatim as `SpendAuditRecord.session_id` (wcore-budget/src/spend_audit.rs:70, populated at :310 and copied at :372), which the `JsonlSpendAuditSink` appends to `~/.wayland/budget/spend-audit.jsonl` (path built by `spend_audit_log_path`, engine.rs:4701-4705). The give-away that the random uuid is not intended is the third call site: `rebind_provider` (engine.rs:6706-6713) passes `&self.budget_session_id()` -- the authoritative id (engine.rs:5942-5950, authority id -> `budget_session_id` -> `current_session_id()`).

**Where.** crates/wcore-agent/src/engine.rs:4764 and :5037 (`install_spend_guard` session_id argument), against crates/wcore-agent/src/engine.rs:6712 which passes the real `budget_session_id()`; consumed at crates/wcore-agent/src/spend_guard.rs:143-153 and crates/wcore-budget/src/spend_audit.rs:302-310.

**Why it matters.** Three concrete consequences, all of them accounting the operator cannot repair after the fact: (1) the spend-audit records for one conversation cannot be grouped -- a fresh launch and each `--resume` write under different random keys, so a session's true authorized spend can never be totalled from the log; (2) within a SINGLE session, a `/model` switch moves subsequent records from the random uuid to the real session id, so one uninterrupted session appears in the log as two unrelated sessions; (3) `EscalationGate` is constructed with the same random id, so any per-session escalation reasoning is scoped to an identity that does not survive a resume. Scope of my claim, stated honestly: I verified this by reading every call site and the record-construction chain; I did not run a live session and inspect `spend-audit.jsonl`, so the wiring is proven and the on-disk symptom is inferred from it. Not caused by the #1161 fix -- it is symmetric on the fresh and resume paths and predates it -- but it is the same unfiled defect the #1161 investigation walked straight past.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.


## 2026-08-30 -- lane/f13-w3-cache-spend

c1 and c2 are closed by commit `9e0d05b7e`. c3 is NOT closed and is this lane's declared residual: the `/model` leg of the
live run needs a PTY-driven TUI session because `rebind_provider` has no headless entry point. The equivalent
wiring is covered by the c2 integration test against the real on-disk sink.

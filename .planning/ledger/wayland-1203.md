---
issue: 1203
repo: FerroxLabs/wayland
kind: defect
title: "The spend-audit trail is keyed by a throwaway UUID minted per engine construction, then swapped for the real session id on a /model rebind"
status: open
last_verified_commit: 7a7cf1f6
criteria:
  - id: c1
    text: "install_spend_guard receives the authoritative budget_session_id() at all three call sites, not uuid::Uuid::new_v4() at two of them"
    state: met
    evidence: "absent:crates/wcore-agent/src/engine.rs::&uuid::Uuid::new_v4().to_string(),"
    owner: core
    note: "Neither constructor mints an identity any more. `resume_with_provider_parts` passes &session.id, which is exactly what budget_session_id() resolves to post-construction on that path (no authority yet, no host-supplied id, so it falls through current_session_id()). `new_with_provider` passes UNBOUND_BUDGET_SESSION_ID -- the same `session-unknown` string budget_session_id() itself falls back to -- because a fresh engine genuinely has no session, no authority and no host id at that instant; the guard is then LATE-BOUND by sync_spend_guard_session at set_budget_session_id, install_budget_authority, inherit_budget_authority, the session-switch path, set_model (an escalation record is written there before any task record is) and immediately before every task record is emitted. rebind_provider already passed budget_session_id() and is unchanged. The evidence is an absence check with its own control: the token `&uuid::Uuid::new_v4().to_string(),` is now 0 in engine.rs while `uuid::Uuid::new_v4()` is still there 17 times, so the query is not silently failing. Shown RED through the gate itself: restoring `&uuid::Uuid::new_v4().to_string(),` at both constructors makes `python3 scripts/check-criteria-ledger.py --offline` exit 1 with `wayland-1203.md:9: c1 evidence does not resolve -- crates/wcore-agent/src/engine.rs still contains '&uuid::Uuid::new_v4().to_string(),'`."
  - id: c2
    text: "A test asserts the SpendAuditRecord.session_id written on a fresh construction, on a resume, and after rebind_provider are the SAME id"
    state: met
    evidence: "test:crates/wcore-agent/tests/spend_governance_test.rs::one_conversation_keys_every_spend_record_by_the_same_session_id"
    owner: core
    note: "Three arms in the ticket's own order against the real AgentEngine and the real JsonlSpendAuditSink: a fresh construction bound to a SessionManager-created session, a real rebind_provider on that same engine, then a real resume_with_provider of the same Session in a new engine. Asserts all three SpendAuditRecord.session_id values equal the session's own id, with an anti-vacuity assertion that the shared key is not the `session-unknown` placeholder. RED ARM, and an honest correction: restoring uuid::Uuid::new_v4() at the two constructors leaves this test GREEN. That mutation is not what governs this observable -- the late binding repairs the id before any record is written, which is the whole point of it. The load-bearing change is sync_spend_guard_session, and making its body a no-op reddens the test with `all three must be keyed by the conversation, not by three coin tosses / left: [\"session-unknown\", \"73e196158df4\", \"73e196158df4\"] / right: [\"73e196158df4\", \"73e196158df4\", \"73e196158df4\"]` -- the fresh-construction arm is the one that needs it, because rebind_provider and resume already carry the real id themselves."
  - id: c3
    text: "A live run is quoted: one conversation's records in ~/.wayland/budget/spend-audit.jsonl share one key across a /model switch and across a --resume"
    state: not-met
    owner: core
    note: "DECOMPOSED, not partial. Left `not-met` rather than `blocked` because the gate refuses a criterion blocked by its own lane, and the remainder is core work -- it is carried by the handoff below, not suppressed. Two of the three legs are closed LIVE on hetzner with the shipped debug binary against a loopback OpenAI-compatible fixture: `printf 'hello\\nsecond\\n' | wayland-core --no-tui --provider openai --model fixture-chat-v1 --base-url http://127.0.0.1:47891` then `printf 'third\\n' | wayland-core --no-tui --resume 20244249fc21 ...` wrote three task records to ~/.wayland/budget/spend-audit.jsonl, all keyed `20244249fc21` -- the session's own id -- across the fresh launch AND the --resume. The remaining leg, `across a /model switch`, is NOT reachable headlessly: the in-session switch that re-installs the guard is AgentEngine::rebind_provider, whose only production callers are the TUI engine bridge (crates/wcore-cli/src/tui/engine_bridge.rs:2469 and :2499), and the --no-tui REPL routes input through wcore_agent::slash, which registers no `model` handler. That leg needs a PTY-driven TUI run and is FerroxLabs/wayland#1244 with its own gradeable criteria. The path itself is covered against the real engine by c2's test; what #1244 adds is the same fact observed through the shipped binary's TUI. Searched before filing: `spend-audit`, `spend audit session id`, `1203`, and `rebind_provider OR \"model switch\"` over open FerroxLabs/wayland issues -- the only hits were #1203 itself and #174, the parent budget-guard feature, neither a gradeable carrier for this leg."
    handoff: FerroxLabs/wayland#1244
---

The spend-audit trail is keyed by a throwaway UUID that is minted fresh on every engine construction, never persisted, never restored on resume -- and then silently swapped for the REAL session id mid-session on a `/model` rebind. This is the exact defect class #1161 just closed for the cache ledger, still open one crate over in the spend ledger. `install_spend_guard` is called with `&uuid::Uuid::new_v4().to_string()` as its `session_id` at BOTH construction sites -- engine.rs:4758-4765 (`new_with_provider`) and engine.rs:5031-5038 (`resume_with_provider_parts`, three lines above the #1161 fix itself). `SpendGuard::new` (spend_guard.rs:137-157) hands that string straight into `SpendAuditor::new(...)` and `EscalationGate::new(session_id, ...)`, and it lands verbatim as `SpendAuditRecord.session_id` (wcore-budget/src/spend_audit.rs:70, populated at :310 and copied at :372), which the `JsonlSpendAuditSink` appends to `~/.wayland/budget/spend-audit.jsonl` (path built by `spend_audit_log_path`, engine.rs:4701-4705). The give-away that the random uuid is not intended is the third call site: `rebind_provider` (engine.rs:6706-6713) passes `&self.budget_session_id()` -- the authoritative id (engine.rs:5942-5950, authority id -> `budget_session_id` -> `current_session_id()`).

**Where.** crates/wcore-agent/src/engine.rs:4764 and :5037 (`install_spend_guard` session_id argument), against crates/wcore-agent/src/engine.rs:6712 which passes the real `budget_session_id()`; consumed at crates/wcore-agent/src/spend_guard.rs:143-153 and crates/wcore-budget/src/spend_audit.rs:302-310.

**Why it matters.** Three concrete consequences, all of them accounting the operator cannot repair after the fact: (1) the spend-audit records for one conversation cannot be grouped -- a fresh launch and each `--resume` write under different random keys, so a session's true authorized spend can never be totalled from the log; (2) within a SINGLE session, a `/model` switch moves subsequent records from the random uuid to the real session id, so one uninterrupted session appears in the log as two unrelated sessions; (3) `EscalationGate` is constructed with the same random id, so any per-session escalation reasoning is scoped to an identity that does not survive a resume. Scope of my claim, stated honestly: I verified this by reading every call site and the record-construction chain; I did not run a live session and inspect `spend-audit.jsonl`, so the wiring is proven and the on-disk symptom is inferred from it. Not caused by the #1161 fix -- it is symmetric on the fresh and resume paths and predates it -- but it is the same unfiled defect the #1161 investigation walked straight past.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.

---
issue: 1203
repo: FerroxLabs/wayland
kind: defect
title: "The spend-audit trail is keyed by a throwaway UUID minted per engine construction, then swapped for the real session id on a /model rebind"
status: open
last_verified_commit: 9e0d05b7e
criteria:
  - id: c1
    text: "install_spend_guard receives the authoritative budget_session_id() at all three call sites, not uuid::Uuid::new_v4() at two of them"
    state: met
    owner: core
    note: "COMPLETED 2026-08-30 on lane/f13-w3-cache-spend. STATED PLAINLY FIRST, because this criterion is not met by its literal words and pretending otherwise is the substitution this ledger exists to catch: `install_spend_guard` does NOT receive the string `budget_session_id()` at the two constructor sites, and it CANNOT. It is a free function called from `new_with_provider` and `resume_with_provider_parts` BEFORE the struct literal exists, so there is no `self` to ask; and the authoritative identity is not established until `init_session` binds the budget authority (engine.rs, `init_session` calls `bind_budget_authority`), which runs after the engine is built. Passing a literal `budget_session_id()` there would have yielded the `session-unknown` placeholder, which merges every unsessioned conversation on the host into ONE bucket -- a worse accounting failure than the random uuid this ticket is about. WHAT WAS BUILT INSTEAD, and why it is stronger rather than easier: the parameter is now a SHARED HANDLE (`wcore_budget::SessionIdentity`, an `Arc<Mutex<String>>`) read at RECORD-SEAL time instead of a string copied at construction time. All three call sites pass that handle -- `new_with_provider` seeds it from the engine's own conversation id, `resume_with_provider_parts` seeds it from `session.id` (exactly what `bind_budget_authority` will bind, so a resumed conversation appends to the key it already wrote under), and `rebind_provider` carries the EXISTING handle into the replacement guard after republishing onto it. `AgentEngine::publish_spend_session` is the single writer and is called at every point a durable record is sealed (`emit_task_spend_audit`) or an escalation is authorized. The property the criterion was reaching for -- every record of one conversation carries the run's authoritative identity -- therefore holds at ALL THREE sites, including the two where the literal wording is unreachable, which a `&str` argument could not have delivered. `uuid::Uuid::new_v4()` is gone from both `install_spend_guard` call sites; grep for `new_v4` in engine.rs now finds it only on the conversation-id and task-id mints (control: the same grep over spend_guard.rs still finds the per-task `task-{uuid}` mint, so the query is not silently empty)."
    evidence: "symbol:crates/wcore-budget/src/spend.rs::SessionIdentity"
  - id: c2
    text: "A test asserts the SpendAuditRecord.session_id written on a fresh construction, on a resume, and after rebind_provider are the SAME id"
    state: met
    owner: core
    note: "COMPLETED 2026-08-30. `one_conversations_spend_records_share_one_key_across_a_resume_and_a_rebind` drives ONE conversation through all three install paths -- a fresh `new_with_provider`, a `resume_with_provider` of the same session id in a NEW engine (a second launch), and an in-session `set_model` + `rebind_provider` -- and asserts the set of `payload.session_id` values read back off the real `JsonlSpendAuditSink` on disk is exactly one element. It covers the escalation record as well as the task records (the `>= 4` line assertion), because the escalation gate was constructed with the same throwaway id. NEGATIVE CONTROL in the same file: `an_unsessioned_run_keys_its_own_conversation_and_two_of_them_do_not_collide` proves the fix did not collapse to 'always use the session id' -- with durable sessions off there is no authoritative id, and two such runs must produce TWO keys, not one shared placeholder. Without that control the first test would also pass if every record were keyed by a constant."
    evidence: "test:crates/wcore-agent/tests/spend_governance_test.rs::one_conversations_spend_records_share_one_key_across_a_resume_and_a_rebind"
  - id: c3
    text: "A live run is quoted: one conversation's records in ~/.wayland/budget/spend-audit.jsonl share one key across a /model switch and across a --resume"
    state: not-met
    owner: core
    note: "NOT MET, and not attempted to a standard worth quoting. What this criterion asks for is a live run of the shipped binary whose `~/.wayland/budget/spend-audit.jsonl` is quoted across BOTH a `--resume` and a `/model` switch. The resume leg is drivable headlessly. The `/model` leg is NOT: `rebind_provider` is reachable from exactly two call sites outside the engine, both in `crates/wcore-cli/src/tui/engine_bridge.rs` (:2469 and :2499), i.e. only through the interactive TUI -- there is no JSON-stream command and no CLI flag that reaches it (control: the same grep finds the engine's own definition and the test call sites, so it is not an empty query). Quoting only the resume half would be a partial dressed as evidence. The c2 test exercises the identical three paths against the real on-disk `JsonlSpendAuditSink`, so the wiring is proven; what remains unproven is the shipped binary end to end through a PTY. Carried as this lane's residual -- see the handoff note below."
---

The spend-audit trail is keyed by a throwaway UUID that is minted fresh on every engine construction, never persisted, never restored on resume -- and then silently swapped for the REAL session id mid-session on a `/model` rebind. This is the exact defect class #1161 just closed for the cache ledger, still open one crate over in the spend ledger. `install_spend_guard` is called with `&uuid::Uuid::new_v4().to_string()` as its `session_id` at BOTH construction sites -- engine.rs:4758-4765 (`new_with_provider`) and engine.rs:5031-5038 (`resume_with_provider_parts`, three lines above the #1161 fix itself). `SpendGuard::new` (spend_guard.rs:137-157) hands that string straight into `SpendAuditor::new(...)` and `EscalationGate::new(session_id, ...)`, and it lands verbatim as `SpendAuditRecord.session_id` (wcore-budget/src/spend_audit.rs:70, populated at :310 and copied at :372), which the `JsonlSpendAuditSink` appends to `~/.wayland/budget/spend-audit.jsonl` (path built by `spend_audit_log_path`, engine.rs:4701-4705). The give-away that the random uuid is not intended is the third call site: `rebind_provider` (engine.rs:6706-6713) passes `&self.budget_session_id()` -- the authoritative id (engine.rs:5942-5950, authority id -> `budget_session_id` -> `current_session_id()`).

**Where.** crates/wcore-agent/src/engine.rs:4764 and :5037 (`install_spend_guard` session_id argument), against crates/wcore-agent/src/engine.rs:6712 which passes the real `budget_session_id()`; consumed at crates/wcore-agent/src/spend_guard.rs:143-153 and crates/wcore-budget/src/spend_audit.rs:302-310.

**Why it matters.** Three concrete consequences, all of them accounting the operator cannot repair after the fact: (1) the spend-audit records for one conversation cannot be grouped -- a fresh launch and each `--resume` write under different random keys, so a session's true authorized spend can never be totalled from the log; (2) within a SINGLE session, a `/model` switch moves subsequent records from the random uuid to the real session id, so one uninterrupted session appears in the log as two unrelated sessions; (3) `EscalationGate` is constructed with the same random id, so any per-session escalation reasoning is scoped to an identity that does not survive a resume. Scope of my claim, stated honestly: I verified this by reading every call site and the record-construction chain; I did not run a live session and inspect `spend-audit.jsonl`, so the wiring is proven and the on-disk symptom is inferred from it. Not caused by the #1161 fix -- it is symmetric on the fresh and resume paths and predates it -- but it is the same unfiled defect the #1161 investigation walked straight past.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.


## 2026-08-30 -- lane/f13-w3-cache-spend

c1 and c2 are closed by commit `9e0d05b7e`. c3 is NOT closed and is this lane's declared residual: the `/model` leg of the
live run needs a PTY-driven TUI session because `rebind_provider` has no headless entry point. The equivalent
wiring is covered by the c2 integration test against the real on-disk sink.

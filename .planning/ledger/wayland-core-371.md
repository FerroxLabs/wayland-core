---
issue: 371
repo: FerroxLabs/wayland-core
kind: defect
title: "The UNIX half of the harness-ownership guard leaks the tree: harness_owns_spawned_trees fails 3/3 on integ/f13 and reddens every lane gate"
status: open
last_verified_commit: 1798076f
criteria:
  - id: c1
    text: "The Linux descendant snapshot (or the fixture's ordering, whichever the measurement indicts) is fixed, with which one established BEFORE either is changed"
    state: met
    evidence: "test:crates/wcore-cli/tests/harness_owns_spawned_trees.rs::dropping_the_guard_kills_a_detached_grandchild_and_reaps_the_direct_child"
    owner: core
    note: "CLOSED. MEASURED ON LINUX at integ/f13 1798076fe (hetzner-dsm), which is the measurement this criterion asks for and which the seeding pass never took. `cargo test -p wcore-cli --test harness_owns_spawned_trees` -> `test result: ok. 24 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`, exit 0, with the named test reported `dropping_the_guard_kills_a_detached_grandchild_and_reaps_the_direct_child ... ok`. Because the ticket's own claim is `fails 3 of 3`, the counter-measurement was taken at the same n: three consecutive runs of that test alone, exit 0 each time, `1 passed; 0 failed` each time. WHICH OF THE TWO WAS INDICTED, established before anything was changed and without changing anything: NEITHER. The Linux descendant snapshot and the fixture ordering are both correct at this tree. The 3/3 failure was caused by a third thing the ticket did not consider -- the never-merge red-arm instrument of wayland-core#367, ten lines added to `crates/wcore-cli/tests/support/owned_tree.rs` by 8d6add71 which returned from `OwnedTree::snapshot` before recording any descendant. #371 and #367 c1 are the SAME DEFECT observed from two ends: #367 c1 names the cause (the instrument) and is already `met`; #371 names the symptom (this test failing 3/3). CROSS-LINKED, NOT CLOSED HERE -- closing an issue is the maintainer's action. `OwnedTree::snapshot` at owned_tree.rs:345-354 now contains the real walk, `for kid in descendants(pid)`, with no `black_box` and no early return. RED ARM, 2026-08-30, tree committed at 1798076fe before the mutation and `git status --porcelain` empty: owned_tree.rs:349 changed from `for kid in descendants(pid) {` to `for kid in Vec::<u32>::new() {` and the file touched. That line is executable code inside `pub fn snapshot`, printed before and after, not a comment or a doc line. The test went to exit 101 with the ticket's OWN failure text, verbatim -- `thread 'dropping_the_guard_kills_a_detached_grandchild_and_reaps_the_direct_child' panicked at crates/wcore-cli/tests/harness_owns_spawned_trees.rs:121:5: the grandchild 376313 outlived the guard - killing the direct child does not reach a backgrounded descendant, which is exactly the surviving process TREE the ticket reported (FerroxLabs/wayland#1156)` -- so the descendant snapshot is demonstrably the mechanism that decides this test, and the green above is caused by it working rather than by the test measuring nothing. Restored with `git checkout --`, touched (an older mtime makes cargo skip the rebuild and silently re-measure the mutant), re-run green at exit 0."
  - id: c2
    text: "The Windows twin's anti-vacuity shape is kept: the grandchild is asserted to be inside what the guard owns BEFORE anything is killed, so the test cannot pass by measuring nothing"
    state: met
    evidence: "test:crates/wcore-cli/tests/harness_owns_spawned_trees_windows.rs::dropping_the_guard_kills_a_detached_grandchild_on_windows"
    owner: core
    note: "CLOSED, and this is a SOURCE-SHAPE criterion -- it says the shape is KEPT, which is decidable by reading the file, and it must be, because the file is `#![cfg(windows)]` and no Linux run can execute it. The shape is present at harness_owns_spawned_trees_windows.rs:66-80, quoted: `// Not vacuous. Both processes are alive, and the grandchild really is / // INSIDE the guard's job - asked of the kernel, before anything is killed.` followed by `let job = guard.job().expect(...); assert!(job.contains(grandchild).expect(...), ...)`. That assertion runs BEFORE the drop that kills anything, and its own failure message states the anti-vacuity purpose in as many words: `the grandchild {grandchild} is not in the guard's job, so this test would prove nothing about the guard - the fixture created it outside the job (FerroxLabs/wayland-core#358)`. The file's header table records the same intent per assertion: `the job CONTAINS the grandchild | nothing - this is the anti-vacuity check, and it fails on a fixture whose grandchild escaped rather than on the guard`. NOT CLAIMED: this lane did not execute the Windows test. It is graded on hardware under wayland-core#358, and SeanDesktop is the only Windows box; a Linux lane asserting a Windows runtime result would be a green from the wrong host. What is claimed is exactly what the criterion says -- the shape is kept -- and that is read directly from the file at 1798076fe."
---

Criteria are the ticket's own acceptance wording, transcribed so the release gate can count this work. Nothing has been done by the bookkeeping pass that created this file, and nothing here has been graded against the tree.

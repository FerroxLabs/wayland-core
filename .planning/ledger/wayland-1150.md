---
issue: 1150
repo: FerroxLabs/wayland
kind: defect
title: "[Bug]: Absurd Input Token Size"
status: open
last_verified_commit: dba806bc4
criteria:
  - id: c1
    text: "An unlisted model no longer gets a fabricated 200,000-token window; it sizes from the bottom of the range"
    state: met
    evidence: "symbol:crates/wcore-config/src/limits.rs::model_output_ceiling"
    owner: core
    note: "COMPLETED 2026-08-29 (sweep finding D16): the fabricated 200,000 survived in ONE consumer c1 doc comment names as a caller that `must not act on a guess` - the skills prompt budget. wcore_skills::prompt::get_char_budget(None) returned DEFAULT_CHAR_BUDGET = 8,000, source comment `Fallback: 1% of 200k x 4`, for exactly the unlisted-model-no-config case c1 is about, while every other boundary in the same session sized against UNVERIFIED_CONTEXT_WINDOW = 32,768. Measured: 1,310 chars intended, 8,000 granted, 6.1x, about 2,000 tokens of a 32,768-token window. The existing guard could not see it - the_bootstrap_prompt_uses_the_real_window_derived_skill_budget boots BOTH arms with an explicit window (Some(1_000_000) / Some(2_000)), so the None path was untested. FIXED: DEFAULT_CHAR_BUDGET is now DERIVED from UNVERIFIED_CONTEXT_WINDOW with a const assert linking them, so the two cannot drift apart again (file:crates/wcore-skills/src/prompt.rs:24). New tests: test:crates/wcore-skills/src/prompt.rs::the_unknown_window_budget_is_the_unverified_window_not_the_old_200k and the end-to-end test:crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs::an_unknown_window_sizes_the_skill_listing_like_the_window_it_assumes, which boots the None arm through the real AgentBootstrap and asserts identity against a Some(32_768) boot, with a Some(200_000) boot as the positive control. RED ARM (hetzner-dsm, 2026-08-29): restoring `= 8_000` gives `left: 8000 / right: 1310` on the unit arm and `left: 12613 / right: 6704` bytes of system prompt on the end-to-end arm. Restored, touched, green."
  - id: c2
    text: "Not every tool and MCP server is sent on every prompt"
    state: met
    evidence: "symbol:crates/wcore-tools/src/registry.rs::admit_hydrated_tools"
    owner: core
    note: "satisfied by MCP curation and tool deferral that shipped BEFORE this release, not by anything in it. Credit where due, but do not credit 0.13.10 for it"
  - id: c3
    text: "Large fetched content is truncated or summarised before it enters the context"
    state: met
    evidence: "test:crates/wcore-tools/src/web_fetch.rs::a_large_fetched_page_does_not_enter_the_context_whole"
    owner: core
    note: "WEB_FETCH_MAX_TEXT_CHARS = 20,000 caps text on a char boundary, flips truncated and adds truncation_notice; max_result_size was raised above the tool's own worst case so orchestration::truncate_result cannot mangle the JSON envelope. Negative control a_page_under_the_cap_is_untouched_and_not_marked_truncated is present."
  - id: c4
    text: "Accumulated prior tool RESULTS are not re-sent whole on every turn, and prompt/KV cache is reused where possible"
    state: met
    evidence: "test:crates/wcore-agent/src/compact/micro.rs::accumulated_tool_results_are_bounded_across_a_session"
    owner: core
    note: "Both halves. RESULTS: `symbol:crates/wcore-agent/src/compact/micro.rs::bound_accumulated_tool_results` is a ceiling on the SUM, wired into `run_compaction` step 0b. The gap it closes is precise — per-result truncation caps ONE result at ingestion (`Tool::max_result_size()`, 50,000 chars) and `microcompact` only clears old ones once real pressure reaches a fraction of the autocompact threshold, so between them N results at the cap ride at full size and are re-sent whole every turn. So this pass is ungated and applies to every tool, not just `compactable_tools`: a ceiling a tool can opt out of is not a ceiling. Guarantee: carried bytes never exceed `total_budget_bytes` plus the `keep_recent` newest results, both constants — the evidence test measures 20 AND 100 tool calls against the SAME ceiling, which is the claim (it stops growing with the session), and asserts a 20x shrink at 100. Monotone and epoch-quantized like the tool-call-args pass, so a bounded message never changes bytes again: `the_ceiling_is_byte_stable_on_a_second_pass`. Controls: `a_session_under_the_budget_is_untouched`, `the_ceiling_can_be_switched_off`. RED ARM: an early `return none` in the pass fails the evidence test on `the ceiling must have bitten`. CACHE REUSE: the second half is #559 c3/c5/c6, all three closed on the same branch — caching enabled on Bedrock and Vertex where it was off, per-turn transients kept out of every cache write point, and sub-call prefix stability measured on real dispatched requests. RE-VERIFIED 2026-08-29: the red arm was re-applied (an unconditional `return none;` immediately after the enabled checks in bound_accumulated_tool_results) and re-run - accumulated_tool_results_are_bounded_across_a_session panics at micro.rs:2076 with `the ceiling must have bitten`. Restored, touched, and re-run green: all 6 ceiling tests pass, including the two controls and the epoch/byte-stability pair. REFUTED 2026-08-29 by the 0.13.12 close-sweep, recorded verbatim: FIRST HALF HOLDS. `bound_accumulated_tool_results` (micro.rs:647) is wired ungated at run_compaction step 0b (engine.rs:18098) and compact_now (engine.rs:6791); run_compaction itself runs at the top of every turn iteration (engine.rs:12842, comment 'Run multi-level compaction before each API call'). RAN: 6 ceiling tests green — accumulated_tool_results_are_bounded_across_a_session, the_newest_results_are_never_dropped_by_the_ceiling, a_session_under_the_budget_is_untouched, the_ceiling_can_be_switched_off, the_ceiling_is_byte_stable_on_a_second_pass, the_ceiling_advances_in_epoch_sized_batches, plus the_ceiling_applies_to_tools_outside_the_compactable_list. The ledger's red-arm claim is credible: micro.rs:2074 is the `assert!(result.cleared_count > 0, 'the ceiling must have bitten')` the note quotes (the note says :2076, off by two lines — cosmetic drift, message matches). SECOND HALF DOES NOT HOLD as an independently verified claim. The criterion text is 'and prompt/KV cache is reused where possible'. The stated evidence (a micro.rs tool-result test) says nothing about caching; the ledger delegates it entirely to wayland#559 c3/c5/c6. Three problems with that delegation: (1) #559 is still OPEN in the same ledger directory and its own written close condition — c4, 'ONE real 26-turn Desktop team run showing non-zero cache_read' — is recorded `not-met` / needs-platform-run, so #1150 c4 is leaning on a ticket that has not itself been proven end to end; (2) #559 c6 is closed by the lane's own words as 'a cache-boundary fact, not a positional one' against a criterion whose text is 'no longer land at messages[1] on turn 1' — an admitted substitution, and the exact pattern this sweep was told to catch; (3) most materially for THIS reporter, #559 c6's own recorded residual states that 'on implicit-cache providers (OpenAI-shaped, incl. FluxRouter) there is no write point to move'. #1150's reporter is on LM Studio over an OpenAI-compatible endpoint — precisely the provider shape where the delivered cache work does the least, and no measurement exists on any OpenAI-shaped endpoint. I confirmed both #559 evidence symbols resolve (config.rs:1085 prompt_caching_on_by_default, config.rs:12068 every_breakpoint_provider_defaults_prompt_caching_on; prompt_cache_prefix_test.rs:219 and :280), so the delegated work is real — it is the CLOSURE that is borrowed from an open ticket, not the code. RE-GRADED 2026-08-30: the RESULTS half stands as written and its red arm was re-confirmed by the sweep. The CACHE half is no longer graded here - it was borrowed from an open ticket and is now c6, with its own in-tree evidence on the reporter's provider shape. RE-ARMED 2026-08-30 at the merged tip: an unconditional 'return none;' inserted as the first statement after the enabled checks in bound_accumulated_tool_results (diff read back: two added CODE lines at micro.rs:660, the return plus the #[allow(unreachable_code)] the compiler then requires) reddens the evidence test and two siblings - 59 tests run: 56 passed, 3 failed: accumulated_tool_results_are_bounded_across_a_session, the_ceiling_advances_in_epoch_sized_batches and the_ceiling_applies_to_tools_outside_the_compactable_list, the last with 'an unlisted tool must not be able to escape the ceiling' at micro.rs:2208. Restored, touched, re-run: 59 tests run: 59 passed. OPEN REMAINDER, recorded 2026-08-29 (sweep finding D17) and NOT claimed by this criterion: the ceiling is window-INDEPENDENT. total_budget_bytes = 120,000 and keep_recent = 4 are absolute (compact.rs:733-741), and the four protected newest results are exempt however large, each up to Tool::max_result_size() = 50,000 chars - a worst-case carried payload of 120,000 + 4 x 50,000 = 320,000 bytes, about 80,000 tokens, roughly 2.4x the entire 32,768-token window the same release assumes for an unlisted model. The guarantee this criterion's text and evidence actually deliver is `carried bytes stop growing with the session`; the guarantee a 32k user needs is `carried bytes fit the window`, and it is neither claimed nor delivered. Deriving total_budget_bytes from known_context_window closes only the budget term - the protected-tail term still dominates at a small window - so this was left OPEN rather than half-closed and graded met. It needs a decision about the protected tail (shrinking it trades against the model's live working set and the prefix-cache discipline the pass is built on), which is why it is not folded into this criterion."
  - id: c5
    text: "Tool schemas AND SKILLS are injected only when relevant or explicitly activated, rather than on every ordinary chat turn"
    state: not-met
    owner: core
    note: "ADDED 2026-08-29 by the 0.13.12 close-sweep. This half of the issue had NO criterion, so it was invisible in the 'all criteria met' reading. Recorded verbatim: TICKET-VS-LEDGER DRIFT. The issue's Expected Behavior says, verbatim: 'Tool schemas and skills should be injected only when relevant or explicitly activated, rather than on every ordinary chat turn.' The reporter's payload inventory also names 'active skills' as its own line item alongside the 29 MCP tool definitions. No ledger criterion covers skills. c2 was narrowed to 'Not every tool and MCP server is sent on every prompt' — the tool half only. In the tree, skills are still listed unconditionally on every turn: context.rs:192 `format_skills_section` emits a </system-reminder> listing for every visible skill, and wcore-skills/src/prompt.rs:61 `format_skills_within_budget` only DEGRADES the listing (full -> truncated descriptions -> names-only) against a char budget. There is no relevance gate and no explicit-activation gate anywhere on that path. That half of the ticket's ask is neither delivered nor graded, so it is invisible in the 'all criteria met' reading. It is not a false claim by the lane — it is an unrepresented ask, which is why it only surfaces when you read the ticket instead of the ledger. MEASURED 2026-08-30 by lane w3-cache-spend, and the headline finding is about the INSTRUMENT, not the feature. c5 remains not-met -- no relevance or activation gate was built. What was tested is the check this work was to be validated against, every_dispatch_extends_the_previous_dispatchs_byte_prefix (issue_1150_implicit_prefix_cache_test.rs:301), on the expectation that mutating the skills listing per turn would report fifteen prefix breaks. IT REPORTS NONE, and twice: ARM 1 replaced the cached skills section in build_system_prompt with an unconditional per-call listing carrying a turn counter -- test PASSED; ARM 2 additionally disabled the SystemPromptCache.joined memo, so the churn could not be swallowed by the fast path -- test PASSED again. Both compiled (cargo check -p wcore-agent --tests exit 0). THE POSITIVE CONTROL IS WHAT MAKES THIS CONCLUSIVE: ARM 3 appended a per-dispatch nonce to LlmRequest.system itself (engine.rs:13534) and the test FAILED instantly -- dispatch 1 changed the system prompt -- so the oracle CAN see segment-0 churn and the two greens mean BLIND, not stable. The cause is positive, not inferred: the fixture openai_shaped_config sets system_prompt: Some(You are a test assistant.) directly on Config (:119), and the engine takes config.system_prompt verbatim, so build_system_prompt -- the ONLY place the skills listing is assembled, via cache.sections skills at context.rs:398 calling format_skills_section at :192 -- is never called on that test path at all. Segment 0 there is a fixed literal. CONSEQUENCE FOR WHOEVER BUILDS c5: that test is a valid oracle for the tool-schema and message segments and is worthless for the skills half of this criterion; building against it yields a false green. Note also that it aborts at the segment-0 equality assertion (:325) before it ever computes the breaks vector, so the predicted fifteen-break output cannot be produced by it in any case. The design constraint recorded on this ticket still stands and was re-verified against source: the skills listing lives in the system prompt, the same test requires segment 0 be byte-identical across all sixteen dispatches, and the positional escape of moving skills into the message stream is foreclosed by #559 c6 recorded refusal that trusted product context must not follow attacker-reachable text on a channel session -- so append-only, epoch-quantized activation remains the shape that satisfies both. All three arms restored and verified by blob identity; the file suite is green."
  - id: c6
    text: "prompt/KV cache is reused where possible"
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1150_implicit_prefix_cache_test.rs::every_dispatch_extends_the_previous_dispatchs_byte_prefix"
    owner: core
    note: "SPLIT OUT OF c4 on 2026-08-30 as c6 (c5 is the sweep-added skills criterion), because c4 is two claims and one evidence token cannot anchor both - the ledger's own rule that a criterion needing two pieces of evidence is two criteria. c4's evidence covers the tool-RESULTS half; this is the CACHE half, and it was previously closed by delegation to wayland#559 c3/c5/c6. That delegation had three problems, all confirmed at e7144c30a: #559 is still open and its own c4 is not-met; #559 c6 closed an admitted substitution; and #559 c6's recorded residual says 'on implicit-cache providers there is no write point to move' - which is #1150's reporter's exact shape (LM Studio over an OpenAI-compatible endpoint), where no measurement existed at all. VERIFIED at HEAD: wcore-observability::cache::mark_cache_boundaries returns before writing any breakpoint when compat.cache_message_breakpoints() is false, so #559's instrument reads ZERO on this route - an_openai_shaped_route_places_no_cache_write_point asserts that as the precondition. Where there is no write point, reuse is a property of the BYTES, so the claim is measured on the real LlmRequest the engine hands the provider: across a 4-turn, 16-dispatch session every dispatch repeats the previous dispatch's messages verbatim and appends (#559's own file stops at the sub-calls inside ONE turn; the reported session was 26). a_bounded_tool_result_is_rewritten_once_and_then_frozen measures the interaction with c4's first half over 18 dispatches - a tool result goes verbatim->stub ONCE and is then frozen, and the boundary is epoch-quantized. RED ARMS, all on executable code in compact/micro.rs, diff read back before each run, file touched after mutate and after restore: (A) `if total <= tr.total_budget_bytes` and `if running <= tr.total_budget_bytes` -> `if false` reddens every_dispatch_extends_the_previous_dispatchs_byte_prefix with 'dispatch 6 diverges from dispatch 5 at message 2 of 11'; (B) dropping the `!*stubbed` monotone filter reddens a_bounded_tool_result_is_rewritten_once_and_then_frozen with 'message 2 changed bytes 3 times across the session'; (C) `let epoch = tr.epoch_results.max(1)` -> `let epoch = 1` reddens the quantization bound with '8 of 17 dispatch pairs'. Arm C is recorded because the FIRST cut of that bound survived it - a gate that could not fail - and was re-pinned from the measurement (quantized 5, unquantized 8). All three green after restore. RE-ARMED INDEPENDENTLY 2026-08-30 at the merged tip, every arm on executable code in compact/micro.rs with the diff read back and the file touched after mutate and after restore. (D) the arm that pins THIS criterion's own evidence token: 'if total <= tr.total_budget_bytes' -> 'if false' at micro.rs:673 together with 'if running <= tr.total_budget_bytes' -> 'if false' at :692, so the ceiling engages on every pass - every_dispatch_extends_the_previous_dispatchs_byte_prefix goes RED with 'assertion left == right failed: dispatch 6 diverges from dispatch 5 at message 2 of 11: everything from there on re-bills at full price on an implicit-cache endpoint, every turn, for the rest of the session; left: 2, right: 11'. (C) 'let epoch = tr.epoch_results.max(1)' -> 'let epoch = 1' at :706 reddens the quantization bound with 'the ceiling broke the prefix on 8 of 17 dispatch pairs; the epoch-quantized boundary measures 5 and an unquantized one measures 8 ... [7, 8, 10, 11, 13, 14, 16, 17]' (test file :342). (B) dropping the monotone filter, '.filter(|(_, _, _, stubbed)| !*stubbed)' -> a filter that admits everything, at :681, reddens the frozen bound with 'message 2 changed bytes 3 times across the session ... [0, 0, 3, 0, 3, 0, 0, 0, 3, ...]' (:319). The c4 arm also reddens this file, on its own stated precondition: 'the tool-result ceiling never rewrote a single message, so this test measures nothing'. Restored after each: 3 tests run: 3 passed, 0 skipped. REFUTED 2026-08-30 by the partials verifier and CONCEDED IN FULL: the evidence token was blind to the part of the prompt the ticket reports. shared_prefix compared request.messages ONLY - a grep for `.system`, `.tools` and `.role` in the test file returned zero, with 13 `.messages` hits in the same call as the known-positive control. On an OpenAI-shaped body the system prompt and the tool schemas sit AHEAD of the entire conversation, so a per-dispatch nonce in the system prompt is a total loss of implicit reuse at token 0, on every single request, and this token passed anyway. REPRODUCED INDEPENDENTLY at fbad9212e with the verifier's own mutation - `system,` -> `system: format!("{system} verifier-cache-buster-{}", ... as_nanos())` at engine.rs:13020, a struct-field initializer inside `let mut request = LlmRequest {`, executable code and not a comment, diff read back, file touched after mutating and after restoring - and every_dispatch_extends_the_previous_dispatchs_byte_prefix PASSED. FIXED at f96d1933e: the comparison is now over the prefix segments the endpoint actually keys on - segment 0 the system prompt, segment 1 the tool schemas (name, description, input_schema, deferred), then one segment per message carrying role AND content. UN-BLINDING IT FOUND A REAL BREAK the old instrument could not see, and the claim is now stated as a measured exception SET rather than as an absolute, because the absolute form is false: admit_hydrated_tools (wcore-tools/src/registry.rs:572) un-defers a tool on FIRST USE and moves it to the tail of tools[], and openai.rs:622 truncates a deferred tool's description on the wire, so both halves change bytes ahead of the whole conversation. MEASURED over a 4-turn, 16-dispatch session on the real engine loop, off the real LlmRequest: exactly ONE break, at dispatch 1, at segment 1. The SHAPE of that bound is what keeps this criterion met - it is one break per distinct tool FIRST USED, never one per turn, and #1150's reported failure is the per-turn shape, which would show here as fifteen entries rather than one. A second tool is registered and never called so per-tool hydration is PINNED rather than assumed: `idle` is still a deferred stub on the last dispatch of the session. The epoch-quantization bound in the sibling test is split by CAUSE - the ceiling rewrites MESSAGES, so it counts divergences at segment 2 and later, and everything ahead of the conversation is separately asserted to be that one hydration and nothing else, so the split cannot quietly absorb a new churn. RED ARMS at f96d1933e, each diff read back, each on executable code, file touched after mutating and after restoring: (E) the verifier's system buster at engine.rs:13020 now fails the evidence token at issue_1150_implicit_prefix_cache_test.rs:325 with 'dispatch 1 changed the system prompt, so every dispatch from the first one on re-bills its entire context at full price'; (F) an order-ONLY tool churn at engine.rs:13022 (t.reverse() on alternating dispatches - no description and no schema change, so ONLY segment 1 moves) fails it at :373 naming 'the TOOL SCHEMAS (nothing after the system prompt is reused)' on 7 to 9 of the session's dispatch pairs; (C, re-run under the new filter) `let epoch = tr.epoch_results.max(1)` -> `let epoch = 1` at micro.rs:706 still fails the quantization bound with 'the ceiling broke the prefix on 8 of 17 dispatch pairs' against the quantized 5, so splitting the bound by cause did not make it unfalsifiable. Restored after each; 15 tests run: 15 passed, 0 skipped. RESIDUAL, stated rather than left to be discovered: with K deferred tools first-used in a session the prefix breaks K times, not once, and each break re-bills the whole conversation on an implicit-cache endpoint. It is bounded by the TOOL count and never by the turn count - which is the property #1150 is about, and why this stays met - but it is not zero, and #1150's own reporter inventory names 29 MCP tool definitions. CARRIER, searched before recording rather than filed again: `gh issue list -R FerroxLabs/wayland --search 'hydration prompt cache'` and `--search 'admit_hydrated_tools'` both return wayland#1209 (OPEN, bug/area:core) 'With builtin_tools.defer_cold.catalog = false, ToolSearch hydration still re-sorts tools[] mid-array: #1171 is fully live on that path', whose own measurement is the same registry.rs:572 tail-move with 'first differing wire index: Some(1)' - the same index this fixture measures, on the stub path this fixture is on (no ToolSearch tool, so the catalog fold never applies). No new issue filed. The description-truncation half is inherent to deferral rather than a defect; the re-sort half is #1209's."
---

Partially fixed in v0.13.10.

The compaction half landed: a model that is not in the limits table no longer
gets handed a fabricated 200,000-token window, which was silently
under-compacting and producing the input sizes this reporter saw.

c2 is recorded as met-but-not-by-this-release deliberately. Grading it as an
outstanding ask would be wrong; grading it as something 0.13.10 delivered
would be a false claim. Both are avoidable by saying which release did it.

c3's remainder — a quarter-megabyte web fetch entering the context untouched
— is closed, and c4 closes the shape behind it: not one oversized result, but
N capped ones summing without limit. The distinction matters because the two
fixes live in different places. c3 is a per-tool cap at ingestion; c4 is a
ceiling over history that runs before every dispatch, because no per-result
cap can bound a sum.

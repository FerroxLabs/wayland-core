---
issue: 1215
repo: FerroxLabs/wayland
kind: defect
title: "The #1177 evidence wrapper cannot create target/nextest and kills the containerized Linux test leg on both attempts"
status: open
last_verified_commit: 1798076f
criteria:
  - id: c1
    text: "run-tests-with-attempt-evidence.sh creates its attempt directory successfully on the hosted runner in the presence of a root-owned target/ left by the container steps"
    state: met
    evidence: "file:.github/workflows/ci.yml:1768:mkdir -p target/nextest/ci/outer-attempts"
    owner: core
    note: "CLOSED. Re-graded 2026-08-30 against CI run 33264117264, job `CI (linux-containerized)` = 99130983871, head 8b6920fa793fa4073fad693dacbe6fc91e5b6f54 on integ/f13. THE FIX IS A NEW WORKFLOW STEP, NOT A WRAPPER CHANGE: ci.yml:1743-1744, `Reserve the outer-retry evidence tree for the runner user` / `run: mkdir -p target/nextest/ci/outer-attempts`, executes on the hosted runner as uid 1001 BEFORE any container step can create target/ as root. THE PRECONDITION THE CRITERION NAMES DID HOLD IN THAT RUN -- this is the half that could have been substituted away, so it was checked rather than assumed. The job's own environment, quoted verbatim from its log, is `DOCKER_RUN: docker run --rm --init --network=host -e WAYLAND_BUILD_SOURCE_SHA=8b6920fa... -v /home/runner/work/wayland-core/wayland-core:/work -w /work` -- no `-u`, exactly as the ci.yml comment block intends -- and steps 15 through 19 (Pre-build tool_token_bench, Pre-build wcore-cli release binary, Check formatting, Clippy, Clippy live-docker) all ran through it BEFORE the wrapper step, so target/ was root-owned at the moment the wrapper ran. THE OBSERVABLE THAT PROVES THE SENTENCE: at 17:23:06.364Z the wrapper printed `-- outer-retry attempt 1 (wayland#1177)`, `junit path  : target/nextest/ci/junit.xml`, `attempt dir : target/nextest/ci/outer-attempts`. Those are wrapper:88-90, which sit AFTER the `mkdir -p` guard at wrapper:72 whose failure branch exits 2 at wrapper:75; reaching them is possible only if the mkdir returned 0. Contrast the run this ticket was filed on, 33227927478, where the step died with `mkdir: cannot create directory 'target/nextest': Permission denied`. THE TICKET'S LINE NUMBERS ARE STALE: the wrapper was rewritten and the mkdir it calls :66 is now :72. RED ARM, 2026-08-30, tree committed at 1798076fe before the mutation: ci.yml:1743-1744 deleted and the file touched, taking `.github/scripts/tests/outer-retry-evidence.test.sh` from `23 passed, 0 failed` exit 0 to `22 passed, 1 failed` exit 1, with exactly one arm red -- verbatim, `FAIL the evidence tree is reserved before any root-owned target/ exists (missing: mkdir -p target/nextest/ci/outer-attempts)`. Restored with `git checkout --` and touched; 23/23 and exit 0 again, `git status --porcelain` empty. The mutation landed on two executable workflow lines, a step `name:` and its `run:`, printed before and after. RE-ANCHORED 2026-08-30 on merge into integ/f13 for FerroxLabs/wayland#1198, which made a bare file:<path>:<line> a REFUSED token. Same line, now carrying the `mkdir -p target/nextest/ci/outer-attempts` the criterion is entirely about -- the attempt directory whose creation under a root-owned target/ is the property. The fragment occurs exactly once in ci.yml. No re-grade."
  - id: c2
    text: "A real CI run is cited BY URL in which 'Run tests (nextest CI profile)' executes and uploads junit.xml"
    state: met
    evidence: "file:.github/workflows/ci.yml:2192:name: nextest-junit-linux-containerized"
    owner: core
    note: "CLOSED. THE RUN, BY URL, as the criterion demands: https://github.com/FerroxLabs/wayland-core/actions/runs/33264117264 -- job `CI (linux-containerized)` (databaseId 99130983871), conclusion success, created 2026-08-29T16:52:03Z, head 8b6920fa793fa4073fad693dacbe6fc91e5b6f54. `Run tests (nextest CI profile)` is step 20 and its conclusion is `success`: it EXECUTED. That is the whole of this ticket -- in run 33227927478 every step after `Run tests` was `skipped`, and here steps 20 through 31 all report success. `Upload nextest JUnit report` is step 29, conclusion `success`; ci.yml:2164 is that step and its artifact name is `nextest-junit-linux-containerized`. THE ARTIFACT WAS DOWNLOADED AND OPENED RATHER THAN INFERRED FROM A GREEN TICK: 409,444 bytes as stored, containing `junit.xml` of 3,154,088 bytes whose root element reads `<testsuites name=\"nextest-run\" tests=\"17341\" skipped=\"0\" failures=\"0\" errors=\"0\" uuid=\"ad47bb91-706c-4bde-a3cd-5a9929772462\" timestamp=\"2026-08-29T17:34:03.825+00:00\" time=\"1060.410\">`. 17,341 tests is the full workspace suite that this leg exists to run and that the reported defect prevented from running at all. RE-ANCHORED 2026-08-30 on merge into integ/f13 for FerroxLabs/wayland#1198, which made a bare file:<path>:<line> a REFUSED token. Moved 2164 -> 2168, four lines inside the same step, because the step's own `- name: Upload nextest JUnit report` at :2164 occurs THREE times in ci.yml (:792, :1141, :2164) and a fragment that matches more than once pins nothing. The artifact name at :2168, `nextest-junit-linux-containerized`, is unique and additionally identifies the job the run cited in this note is from. No re-grade."
  - id: c3
    text: "In that same run the rm -f at wrapper:82 and the cp at wrapper:90 succeed -- an attempt file is preserved and named"
    state: superseded
    owner: core
    note: "SUPERSEDED, handed to FerroxLabs/wayland#1177, which is OPEN and whose own c1 -- 'A failure on attempt 1 followed by a pass on attempt 2 leaves evidence the required report check can read' -- is `not-met` and IS precisely this remainder. SAYING PLAINLY WHAT IS NOT MET, because a reader seeing c1 and c2 close should not infer this one did: in run 33264117264 attempt 1 PASSED, so the `cp` never executed and NO attempt file was preserved or named. That is measured, not inferred -- the uploaded artifact's `outer-attempts/` directory contains exactly one file, `final-status.txt`, 8 bytes, `success\\n` under `od -c`, and no `outer-attempt-*.xml`. The preserve-and-name path cannot be exercised on demand: it requires a genuine test failure on attempt 1 followed by a passing retry, which is not manufacturable and which no lane can schedule. WHY SUPERSEDED RATHER THAN LEFT not-met. (a) The criterion cannot be evaluated AS WRITTEN any more: it names `the rm -f at wrapper:82 and the cp at wrapper:90`, and after the wrapper was rewritten :82 is `attempt=$(tr -dc '0-9' <$COUNTER ...)` and :90 is `echo attempt dir : ...`; the statements it means are now :93 and :101. (b) The mechanism it describes IS graded, by `.github/scripts/tests/outer-retry-evidence.test.sh` -- `the failed attempt's JUnit survives the next attempt`, `a passing attempt is not preserved as a failure`, `an attempt that wrote no JUnit preserves nothing` -- which runs on every PR from lint.yml:130 and which was run at this tree on 2026-08-30: 23 passed, 0 failed, exit 0. (c) The PERMISSION property this ticket is actually about IS observed in that same run: `outer-attempts/final-status.txt` was written by the wrapper at wrapper:110 as the runner uid, into the attempt directory underneath a root-owned target/, which is the same permission class as the `cp` at wrapper:101 and the `rm -f` at wrapper:93. What genuinely remains is the live observation of a preserved attempt file, and #1177 c1 owns it."
---

The #1177 evidence wrapper aborts with `mkdir: cannot create directory 'target/nextest': Permission denied` (exit 2) on both retry attempts, so the containerized Linux nextest suite never runs at all. The wrapper executes on the GitHub-hosted runner as uid 1001, but ci.yml's `docker run` intentionally passes no `-u` (see the comment block at ci.yml:1133-1156), so `target/` is created root-owned by the earlier `Pre-build tool_token_bench` / `Pre-build wcore-cli release binary` / clippy container steps. `mkdir -p '$ATTEMPT_DIR' || exit 2` at line 66 then cannot create `target/nextest`. The same wall applies to `rm -f '$JUNIT_PATH'` (line 82) and the `cp` (line 90), which need the runner to own `target/nextest/ci`.

**Where.** .github/scripts/run-tests-with-attempt-evidence.sh:66 (also :82, :90), invoked from .github/workflows/ci.yml:1676. Reproduced in CI run 33227927478, job 99035159787 (FerroxLabs/wayland-core), 2026-08-29T02:33:48Z, at commit 2282de368. Still unrepaired at integ/f13 HEAD 5eb2d1ef.

**Why it matters.** This is the leg that runs the full ~12,775-test workspace suite, the swarm delegated-dispatch certification, the contract-corpus drift check, the release binary smoke, the F01 packaged driver gate and cargo audit — every step after `Run tests` was marked `skipped` in that run. A fix filed to stop CI evidence being erased currently prevents the evidence from existing. It is masked right now only because nine consecutive runs died at an earlier gate and skipped the test step; the first run that gets past those gates will red at mkdir on every attempt.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.

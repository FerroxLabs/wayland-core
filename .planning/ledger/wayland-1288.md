---
issue: 1288
repo: FerroxLabs/wayland
kind: defect
title: "Three Linux retry-flakes surfaced in the run that overran its 120-min timeout; rate unmeasured, entries expire 2026-09-20"
status: open
last_verified_commit: 93ede3424
criteria:
  - id: c1
    text: "Each of the three entries is either deleted at expiry because a normal-duration ci-linux run does not reproduce it, or carries a measured rate from a run that did not overrun its timeout."
    state: not-met
    evidence: "file:.config/flaky-allowlist.txt"
    owner: core
    note: "STILL NOT MET, and the expected disposition is now INVERTED. The three entries each say `if a normal-duration ci-linux run does not reproduce it, DELETE this line rather than renew it`. That condition is FALSIFIED by measurement: normal-duration ci-linux runs DO reproduce all three. CI-HISTORY CENSUS, 2026-09-10, and it REFUTES the deletion branch of this criterion for all six lines this lane was asked to grade. Method: every `ci.yml` run `gh run list` returns at limit 200 (2026-08-31T04:13 .. 2026-09-09T12:32); for each, every unexpired `nextest-junit-<leg>` artifact downloaded and every `<testcase>` carrying `<flakyFailure>`/`<flakyError>` counted. 126 of the 200 runs had JUnit artifacts: macos-latest 103, linux-containerized 112, Array 110, windows-latest-hosted 8. This is a per-RUN flake incidence at the CI profile's `retries = 2`, NOT a per-execution failure rate at `--retries 0` -- a run counts once if the test failed at least one attempt and then passed. It cannot substitute for the `--retries 0` population job any of these tickets asks for, and is not offered as one. Against a STRICT denominator -- ci-linux concluded `success` AND ran under 118 min, n=57, durations 83.8/102.4/107.3 min min/median/max -- `tui_renders_the_chrome_and_every_tab_on_boot` is 3/57 (runs 33843483817 at 94.0 min, 33930737901 at 101.1 min, 34173361738 at 83.8 min) and the two f14_sigkill_recovery members are 0/57. Against an INCLUSIVE normal-duration denominator -- any conclusion, under 118 min, n=96 -- all three reproduce: `sigkill_during_tool_execution_requires_reconciliation_without_reexecution` 1/96 (run 33474156159, ci-linux 88.8 min, leg cancelled -- cancellation is the workflow cancelling in-flight jobs, not a load signal), `stop_during_active_host_continue_preserves_unknown_provider_authority` 1/96 (run 33584616526, ci-linux 102.9 min, leg concluded failure -- squarely inside the 99-102 min norm the entries themselves cite), `tui_renders_the_chrome_and_every_tab_on_boot` 4/96. The remaining observations are in overrun-class runs (33618437825 at 122.5 min, 33733145558 at 120.9 min, 33752019921 at 121.8 min, 34108695034 at 122.0 min). NOTE the 120-min overrun is NOT rare, which the entries assume it is: 16 of 112 ci-linux legs in this window ran >=118 min. THE RECOMMENDATION IS THEREFORE: DO NOT DELETE ANY OF THE THREE LINES AT THE 2026-09-20 EXPIRY. The strict figure is the only reading under which two of them could be deleted, and it excludes exactly the two runs that reproduce them; picking it would be choosing the denominator that gives the convenient answer. WHAT IS OWED to move this to met: the second branch -- each entry rewritten to carry the measured incidence above instead of `RATE NOT MEASURED`. That is an edit to `.config/flaky-allowlist.txt`, which this lane was explicitly forbidden to make because two other workers hold pending deletions in that file, so the criterion stays not-met until the owner of that file applies it. A `--retries 0` Linux characterisation at n>=20 per test would upgrade the incidence to a real per-execution rate; the census above cannot, because CI runs at `retries = 2`."
---

# Three Linux flakes seen once, under a degraded run

Ledgered for coverage; the honest disposition is deletion at expiry unless reproduced.

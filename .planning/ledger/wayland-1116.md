---
issue: 1116
repo: FerroxLabs/wayland
kind: defect
title: "Desktop: no abandon verb on the host wire leaves a crash-interrupted session wedged in-app"
status: open
last_verified_commit: 88a13b35e
criteria:
  - id: c1
    text: "A crash-interrupted session can be ended through the host wire and Desktop recovery surface while preserving unknown external-effect outcomes."
    state: met
    evidence: "test:crates/wcore-cli/tests/f14_sigkill_recovery.rs::wayland1116_host_wire_abandon_ends_a_crash_interrupted_turn_and_keeps_the_unknown"
    owner: core
    note: "MET 2026-09-10 by the w15/gateB lane at 0097f6a8b, measured on Linux at --retries 0 with complete receipts. THE TITLE'S DIAGNOSIS IS DEAD, THE SYMPTOM WAS REAL. ResumeTurnAction::Abandon is at crates/wcore-protocol/src/commands.rs:697-704, the published corpus carries `abandon` in host-command.schema.json, and manifest.json declares turn_abandon_v1 available -- so `no abandon verb on the host wire` has not been the diagnosis for some time. What wedged the session was different: abandon_interrupted_turn reached terminalize_interrupted_turn_for_cancellation with the interrupted turn's effects still Unknown or Running, and that routine refuses. MEASURED, as the deliberate RED ARM of the new test at 85f2a4171 (receipt remote_exit 100, complete true), against the real packaged binary over --json-stream after a real SIGKILL: `resume_turn refused: Session persistence authority unavailable: interrupted turn cannot be cancelled until started external outcomes are reconciled`, followed by session_recovery_unavailable reason unknown_critical_state. ONE WORD, TWO IMPLEMENTATIONS: the TUI and --resume go through settle_interrupted_turn_for_resume, which admits the unobserved effects first, and only the surface the Desktop host can reach was broken. The fix runs those same four admission steps in the same order before terminalizing. THE HALF THAT IS EASY TO FAKE IS ASSERTED ON THE DURABLE RECORD, NOT THE WIRE: the dispatched attempt's receipt must be outcome.status failed carrying, in words, `may have served it in full, in part, or not at all`, plus a response_digest -- an abandon that reported it not_started or succeeded, or dropped the receipt, would end the session just as tidily and fails this test. The wedge itself is asserted before the verb is sent (lifecycle suspended, reconcile_reason provider_outcome_unknown), so an abandon against an already-clean session could not pass. Green at 0097f6a8b (remote_exit 0, complete true) and green again inside a contended 64-test run at 7cd078806. LIMIT 1, deliberate: an unknown CHILD or DELIVERY is still not admitted by any path, so this verb still refuses for those. Releasing a possibly-booked external effect as settled is how one interruption becomes two charges. So `can be ended` is proven for the provider/tool wedge and NOT for a child/delivery wedge. LIMIT 2: NO ELECTRON CODE WAS EXECUTED. `Desktop recovery surface` is graded here as the recovery projection the host reads off the wire -- session_recovery_snapshot lifecycle and pending_turn, and turn_recovery_lifecycle -- which the test drives on both sides of the verb. If the phrase is meant to include the Electron surface itself, that half is c2's and is not gradeable in this repo. LIMIT 3: engine.rs is SOURCE_INPUTS entry 27, so this moved source_inputs_digest c930f2b2 -> 73bfa23f and fixture_digest 2525ef0a -> 3ed68b57; the corpus is regenerated at 7cd078806 and schema_digest is UNCHANGED at 8497e92e, so no wire shape moved. NAMED SO NOBODY RE-DERIVES IT: two in-tree comments still claim the wire rejects abandon with `unknown variant abandon` (engine.rs ~10412, esc_interrupt_wedge_test.rs ~531). Both were already wrong before this change and are left untouched."
  - id: c2
    text: "The Desktop consumer handles the host-stop recovery behavior and verifies the resulting recovery control."
    state: not-met
    owner: core
    note: "NOT MET, AND NOT GRADEABLE IN THIS REPO -- established 2026-09-10 by search, not assumed. There is no Electron consumer in wayland-core: no root package.json, no app/, and no generated TypeScript beyond scripts/engine-rebrand.ts and a repomap test fixture. What IS here is producer-side only. crates/wcore-protocol/src/contract/mod.rs describes contracts/desktop/ as the PRODUCER contract corpus, and check_contract regenerates the artifacts in memory and rejects byte drift -- a byte-identity check on Core's own emitted wire shape, which executes no host code and asserts no host behaviour. crates/wcore-protocol/tests/host_decoder_contract.rs says so in its own header: the production host at app/src/process/agent/wcore/index.ts is the actual consumer, and `This file is NOT a substitute for verifying the production host's conformance.` .planning/evidence/uat-desktop-contract/ is transcript evidence from a Desktop checkout that is not present here, not a runnable gate. PROPOSED RESTATEMENT, posted on the ISSUE at https://github.com/FerroxLabs/wayland/issues/1116#issuecomment-5615409515 and deliberately NOT written into this criterion, because sharpening a criterion inside the ledger is how it quietly becomes an easier adjacent property: `Desktop's wcore host decoder (app/src/process/agent/wcore/index.ts) sends resume_turn action=abandon from the recovery surface for a lifecycle=suspended session, renders the resulting turn_recovery_lifecycle=cancelled, and surfaces the unknown provider outcome rather than reporting the effect as not-having-happened -- verified against a Core build carrying wayland-core 0097f6a8b.` Whoever takes it restates it on the issue first. Closing c2 on any core-side proxy would grade the wire shape, which already passes, in place of the consumer, which is the subject."
---

Release metadata reconciliation only. Sources: https://github.com/FerroxLabs/wayland/issues/1116 and its 2026-09-08 coordination comments. Both requirements remain unmet. The live milestone is 0.13.14, verified before this record was written; no milestone, issue state, runtime behavior or release threshold is changed here.

Tagged source: 28634521b854d58672819843865f6ab7dd16d72c. The release metadata revision does not alter that tag, its generated Desktop contract, or its qualified binaries.

Provenance: the existing publisher metadata entry is retained with both criteria
not met. Its source tag `28634521b854d58672819843865f6ab7dd16d72c` and
squash integration `1780b3164c6973279fd49d4dda17e8605f51d047` share tree
`90e028ae6de0537c60c1d86450ceb3083ae4610b`; this pointer reconciliation adds
no runtime proof and does not waive the recorded limitation.

---
issue: 1202
repo: FerroxLabs/wayland
kind: defect
title: "atomic_write_checked treats a restore that exchanged nothing as a successful rollback, then deletes the user's only copy"
status: open
last_verified_commit: a268075fe
criteria:
  - id: c1
    text: "restore() distinguishes an actual exchange from Swap::Vacant / Swap::Unsupported, and a non-exchange is a restore FAILURE"
    state: met
    owner: core
    evidence: "absent:crates/wcore-config/src/atomic_io.rs::publish_displacing(displaced, dest).map(|_| ())"
    note: "Closed on lane/f13-dur-atomic at a268075fe. The linux/macos `restore` now matches the Swap: `Displaced(_) => Ok(())`, `Vacant => Err(NotFound, \"the destination name no longer exists, so nothing was exchanged back\")`, `Unsupported => Err(other, \"the exchange primitive that published is no longer available\")`. The evidence token is an ABSENCE because the defect WAS an expression: `.map(|_| ())` discarding the discriminant. It names the full `publish_displacing(displaced, dest).map(|_| ())` and not the bare fragment on purpose -- the bare `.map(|_| ())` still appears at atomic_io.rs:710 inside the new doc comment that quotes the defect, so a token matching that would be satisfied by prose forever. Verified with a control: `grep -cF` of the full expression is 0, of the replacement `publish_displacing(displaced, dest)?` is 1. Only `Vacant` is drivable from a live test (a filesystem that served RENAME_EXCHANGE one syscall ago still serves it); `Unsupported` leaves through the same match and is verified by reading it -- see the residual on c3."
  - id: c2
    text: "On a restore failure the displaced pre-image is preserved by the existing keep_displaced path, and the caller does NOT report the destination as unchanged"
    state: met
    owner: core
    evidence: "test:crates/wcore-config/src/atomic_io.rs::a_restore_that_exchanged_nothing_is_a_failure_not_a_rollback"
    note: "Both clauses. PRESERVATION: the test scans the directory for the original bytes and asserts exactly one survivor, then asserts the error's `preserved at <path>` names that same file and that it reads back byte-identical. Isolated from the other clause by mutation M2 -- `keep_displaced`'s `return Ok(tmp.keep()?)` replaced by `let path = tmp.to_path_buf(); drop(tmp); return Ok(path);`, which leaves the failure detection intact and drops the file: the test went RED at atomic_io.rs:763 with `the directory holds []` while the other nine atomic_io tests stayed green. NOT UNCHANGED: a restore failure now returns `Err(io)`, never `Ok(Err(why))`, and the test asserts that shape. Wiring checked rather than assumed -- all six `changed_under_write` call sites enumerated (unsaved_work.rs:1518 decl; edit.rs:321,336,538; write.rs:227,249,458); the three reachable from `atomic_write_checked` are guarded by `Ok(Err(why))` or by `FileMutationOutcome::Conflict`, which on the real-FS backend is constructed from an `atomic_write_checked` result at exactly one site, vfs.rs:570, and only from `Ok(Err(_))` -- the other Conflict sites are pre-flight classification refusals taken before the call (vfs.rs:529,539), the InMemoryFs backend (1506,1514,1517) or the containment wrapper (2001), none of which an io error can reach. An `Err(io)` instead leaves through the `??` at vfs.rs:561 as `VfsError::Io`, and it cannot be misrouted into the racy re-read fallback that ALSO renders `changed_under_write`: `vfs::is_compare_exchange_unsupported` is true only for `ErrorKind::Unsupported`, and the wrapper this path returns is `std::io::Error::other` (kind Other). It lands instead on the catch-all arms edit.rs:353 (VFS path) and edit.rs:544 (direct path), both `Failed to write file: {e}`, which carry the preserved-at path to the user. write.rs's direct path at 462 does NOT -- see the residual filed as FerroxLabs/wayland#1241."
  - id: c3
    text: "A test drives a refused publish where the destination name disappears between the two exchanges and asserts the original bytes survive; shown RED against today's `.map(|_| ())`"
    state: met
    owner: core
    evidence: "test:crates/wcore-config/src/atomic_io.rs::a_restore_that_exchanged_nothing_is_a_failure_not_a_rollback"
    note: "The window is DRIVEN, not sampled: the check closure calls `std::fs::remove_file(&p)` and only then returns Err, which is the one ordering that puts the unlink between the publish exchange and the rollback exchange. RED arm is the criterion's own wording -- mutation M1 restored today's `publish_displacing(displaced, dest).map(|_| ())` verbatim; the mutation landed on the executable body of `fn restore` (atomic_io.rs:199, printed before and after, not on the doc comment above it), the file was touched after mutating and after `git checkout --`, and the test failed at atomic_io.rs:751: `the original bytes did not survive a rollback that exchanged nothing; the directory holds []` -- an empty directory, i.e. the exact double loss the ticket describes, the destination gone and the pre-image unlinked. The other nine atomic_io tests stayed GREEN under M1, so this test does not ride on their coverage and they did not cover this. The test carries its own fixture control (the check must have been handed the pre-image, else the publish never happened and nothing below measures a rollback). Windows is split off in the test and documented: `restore` there is a replacing rename that SUCCEEDS against an absent destination, so the pre-image comes back at the destination, the refusal is honest, and the outcome is the ordinary `Ok(Err(why))`."
  - id: c4
    text: "The existing happy-path, successful-rollback, absent-destination and mode/long-path tests stay green"
    state: met
    owner: core
    evidence: "test:crates/wcore-config/src/atomic_io.rs::a_refused_publish_puts_the_destination_back"
    note: "All ten atomic_io tests green at a268075fe, inside `cargo nextest run -p wcore-config -p wcore-tools`. By name: the_check_is_handed_the_bytes_the_publish_displaced (happy path), a_refused_publish_puts_the_destination_back (successful rollback, incl. its inode-identity and no-strays assertions), an_absent_destination_is_reported_as_no_pre_image, atomic_write_preserves_the_destination_files_mode, atomic_write_still_creates_new_files_private, atomic_write_survives_a_path_past_windows_max_path, plus the three plain atomic_write cases. Green is not proof a test is live, so the named one was shown non-vacuous: mutation M3 replaced `drop(tmp)` with `let _ = tmp.keep();` in the executable body of `fn discard_displaced` (atomic_io.rs:225, body printed before and after) and it went RED with `left behind [\".tmpK8mbVE\"]` while the new #1202 test and the other eight stayed green -- a mutation that discriminates exactly this test."
---

`restore()` treats a non-exchange as a successful rollback, silently converting a refusal into published data loss. In `atomic_write_checked`, when the verdict rejects the pre-image the code calls `restore(&displaced, dest)`; on Linux/macOS that is `publish_displacing(displaced, dest).map(|_| ())`. The `.map(|_| ())` discards the `Swap` discriminant, so `Swap::Vacant` (ENOENT) and `Swap::Unsupported` (EINVAL/ENOSYS/EOPNOTSUPP) both return `Ok(())` having exchanged NOTHING. Control then falls to `discard_displaced(tmp, &displaced)`, which unlinks the pre-image — the only surviving copy of the user's bytes — and returns `Ok(Err(why))`, whose documented contract is 'the destination is exactly as it was'. The caller (edit.rs:536, write.rs:454) renders `changed_under_write(...)` and tells the user the write was refused, while the destination in fact holds the new content and the displaced original has been deleted. That is silent data loss plus a false refusal, inside the guard whose whole purpose is to prevent it — and it is fail-open in exactly the way this module argues against elsewhere (vfs.rs:492: 'Degrading on ANY observation failure would be fail-open on a data-loss guard').

**Where.** crates/wcore-config/src/atomic_io.rs:114 (call) and :188-191 (`#[cfg(any(target_os = 'linux', target_os = 'macos'))] fn restore` = `publish_displacing(displaced, dest).map(|_| ())`)

**Why it matters.** The trigger is narrow — the first exchange already proved the filesystem supports the primitive, so it needs the destination name to disappear between the two calls (an external `rm` + rewrite, a `git checkout`, an editor that unlinks before writing) — but that is precisely the non-cooperating-concurrent-writer scenario #1155 exists to survive, and the failure is the worst possible pairing: the bytes are gone AND the user is told nothing was changed. There is no test for it: the tests module in atomic_io.rs covers the happy path (`the_check_is_handed_the_bytes_the_publish_displaced`), the successful rollback (`a_refused_publish_puts_the_destination_back`, which also asserts inode identity), the absent destination, and the mode/long-path cases — nothing exercises a restore that fails to exchange. Fix is one line: match the `Swap` and treat `Vacant`/`Unsupported` as a restore failure so the existing `keep_displaced` preservation path runs.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.

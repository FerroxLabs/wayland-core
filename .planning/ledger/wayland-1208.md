---
issue: 1208
repo: FerroxLabs/wayland
kind: defect
title: "The Current date line is frozen for the life of a session while the same prompt forbids the model correcting it"
status: open
last_verified_commit: e4cf8c10
criteria:
  - id: c1
    text: "A session that crosses midnight either reports the real date or stops telling the model the baked date is authoritative"
    state: met
    evidence: "test:crates/wcore-agent/tests/date_rollover_wire_test.rs::a_turn_after_the_day_rolled_over_declares_todays_date_on_the_wire"
    owner: core
    note: "CLOSED 2026-08-31 (lane f13-misc). The date is re-rendered when the DAY changes and only then. `context::refresh_current_date` rewrites a `Current date: YYYY-MM-DD` declaration naming another day; `AgentEngine::refresh_current_date_line` applies it once per turn from `run_inner`, which every turn entry funnels through, and keeps `rebind_system_prefix` in step so a `/model` switch cannot re-prepend yesterday. Graded on the WIRE, not on the helper: the engine starts holding a prompt baked on 2020-01-01 - the state an overnight session is in - a real turn runs against a wiremock Anthropic endpoint, and the captured request body's system field is read. RED ARM, compiled first (`cargo check -p wcore-agent --tests` exit 0): replacing `self.refresh_current_date_line();` with `let _ = Self::refresh_current_date_line;` in `run_inner` turns BOTH c1 and c2 red - 'engine-turn: the turn still declares "Current date: 2020-01-01" as the authoritative today' - while the c3 builder tests stay green, so each criterion is reddened by a different mutation. Restored, `git diff --quiet` clean. engine.rs is in SOURCE_INPUTS, so the Desktop contract corpus was regenerated and key-diffed: schema_digest UNCHANGED at sha256:e31365bb55fe78c2afbf957329d20f4f085a2fbc3dbb1fcb0bf9f7ac6c38b07b, only fixture_digest and source_inputs_digest moved. ORIGINAL FILING: Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D26, found while verifying wayland#1168). Nothing has been done. The measured finding, verbatim: The `Current date:` line is now frozen for the entire lifetime of a session and never refreshes, while the same system prompt instructs the model to treat it as authoritative and forbids correcting it: 'use the current date given above as the authoritative \'today\'. Do NOT substitute a different month or year'. `bootstrap.rs:2349` builds the prompt once into a plain `system_prompt: String` (engine.rs:3919); nothing re-renders it on a day rollover — the only rebind path is the explicit `set_system_prompt` (engine.rs:6755), which re-prepends the same baked prefix. The previous per-turn tail injection re-rendered the date every turn, so this is a behavioural regression introduced by the #1168 fix, not a pre-existing gap."
  - id: c2
    text: "The channel-gateway engine pool is covered: a long-lived per-channel engine does not answer date-bound questions with the day the gateway started"
    state: met
    evidence: "test:crates/wcore-agent/tests/date_rollover_wire_test.rs::the_pooled_channel_engine_does_not_answer_with_the_gateways_start_day"
    owner: core
    note: "CLOSED 2026-08-31 (lane f13-misc). Graded through the REAL `ChannelTurnDispatcher` - its own pool, its own `remote_channel_config`, its own `engine.run` - over two dispatches on ONE session key, so the second turn is served by the engine the first one built. The pooling is asserted rather than assumed: the second request body must carry turn 1's exchange in `messages`, or the test would be grading two fresh engines and could never see the defect. The gateway's start day is played by a prompt declaring 2020-01-01, and BOTH captured bodies must declare today. Same red arm as c1, and its failure output shows the pooled engine putting 'Current date: 2020-01-01' on the wire alongside the intro's real date. The engine pool's own unbounded-lifetime TODO (channel_dispatch.rs TODO(phase) 1) is NOT closed by this and was never this criterion: what is closed is that the pooled engine no longer answers with a day other than today. ORIGINAL FILING: Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D26)."
  - id: c3
    text: "A test drives a day rollover against the prompt builder and asserts the outcome; current_date_present_and_stable_in_cached_system_prefix is restated to whatever the decision is rather than pinning the defect"
    state: met
    evidence: "test:crates/wcore-agent/src/context.rs::a_day_rollover_re_renders_the_date_in_the_cached_prefix"
    owner: core
    note: "CLOSED 2026-08-31 (lane f13-misc). `a_day_rollover_re_renders_the_date_in_the_cached_prefix` drives `build_system_prompt` - the entry point bootstrap.rs calls - against a cache put into exactly the state a session started yesterday holds: an `intro` section whose date line names another day and an `intro_day` recording it. The rebuild must carry today and must not carry the aged day. RED ARM, compiled first (exit 0): deleting `cache.refresh_for_day(&today);` from `build_system_prompt` turns this test red and leaves `current_date_present_and_stable_in_cached_system_prefix` green, which is what makes it the load-bearing assertion. RESTATED, second half of the criterion: that test's 'a session crossing midnight cannot shift its own prefix mid-flight' asserted the defect. Its name is kept (this criterion names it), its comment now says stable WITHIN the day, and its equality is restated as 'byte-identical across builds within one day'. `the_same_day_never_invalidates_the_intro_section` pins the other side so the daily invalidation cannot creep into a per-turn one. ORIGINAL FILING: Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D26)."
---

The `Current date:` line is now frozen for the entire lifetime of a session and never refreshes, while the same system prompt instructs the model to treat it as authoritative and forbids correcting it: 'use the current date given above as the authoritative \'today\'. Do NOT substitute a different month or year'. `bootstrap.rs:2349` builds the prompt once into a plain `system_prompt: String` (engine.rs:3919); nothing re-renders it on a day rollover — the only rebind path is the explicit `set_system_prompt` (engine.rs:6755), which re-prepends the same baked prefix. The previous per-turn tail injection re-rendered the date every turn, so this is a behavioural regression introduced by the #1168 fix, not a pre-existing gap.

**Where.** crates/wcore-agent/src/context.rs:277-296 (frozen intro section) + crates/wcore-agent/src/bootstrap.rs:2349 (built once) + crates/wcore-agent/src/channel_dispatch.rs:89 (per-session engine pool) and :30 ('TODO(phase): (1) the engine pool is unbounded — add LRU / idle eviction')

**Why it matters.** The source comment at context.rs:272-275 waves this off as 'A long session crossing midnight therefore sees a stale date; a new session mints a new prefix' — which assumes short sessions. The channel gateway breaks that assumption by design: channel_dispatch.rs holds 'one AgentEngine per channel session' in an `Arc<Mutex<HashMap<String, Arc<Mutex<AgentEngine>>>>>` with no eviction (the TODO is unimplemented), so a Slack/Discord/Telegram bot running for a week answers every date-bound question in that conversation with the day the gateway first saw it, and is told in the same breath not to substitute a different month or year. Nothing tests day-rollover behaviour (`current_date_present_and_stable_in_cached_system_prefix` asserts the OPPOSITE — that the prefix must not change), and a search of both trackers ('stale date', 'Current date', 'midnight', 'date drift') turns up no issue for it. Cheap fix if wanted: re-render the intro when `today_string()` differs from the baked value, accepting one prefix invalidation per day per long session — the same daily cost #1168 already priced in and accepted.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.

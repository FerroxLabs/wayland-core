---
issue: 378
repo: FerroxLabs/wayland-core
kind: defect
title: "spill_readback_engine_wiring is the slowest test in wcore-agent and was killed at 60.010s on an otherwise-completing run"
status: closed
last_verified_commit: 93ede3424
criteria:
  - id: c1
    text: "The runtime is ATTRIBUTED by measurement: whether the >30s is product latency on the spill/read-back path or the fixture's own cost, established rather than inferred"
    state: met
    evidence: "test:crates/wcore-agent/tests/spill_timing_probe.rs::timing_probe"
    owner: core
    note: "ATTRIBUTED BY MEASUREMENT, and the attribution is a CONTROL rather than a reading of the code. hetzner-dsm, this test binary alone, phases timed separately by the instrument `spill_timing_probe::timing_probe` (`#[ignore]`d, so it costs the suite nothing; run with --run-ignored all). PHASE LINES VERBATIM: `PHASE bytes=240000 ceiling=60000 shed=true read=240000 setup_s=0.001 run_s=24.586 readback_s=0.000 total_s=24.587` and `PHASE bytes=480000 ceiling=60000 shed=true read=480000 setup_s=0.001 run_s=48.763 readback_s=0.001 total_s=48.765`. THE FIXTURE IS NOT THE COST: constructing the payload is 0.001-0.002 s and reading the spilled file back through the session's own jail is 0.000-0.001 s -- together under 0.005% of the total. Effectively all of it is inside `engine.run()`, and it is ~linear in the payload (2.00x the bytes cost 1.98x the seconds). THE SPILL PATH IS NOT THE COST EITHER, which is the half that could not be reached before: the instrument gained `SPILL_PROBE_CEILING` so the SAME payload can be run with the shed switched OFF and every other byte of the fixture identical. With the ceiling raised so the shed never fires, 480,000 chars still cost 48.029 s (`shed=false`). SHED-ON vs SHED-OFF ARE NOT DISTINGUISHABLE, measured INTERLEAVED in one session because the first, unpaired pair said otherwise and was wrong: shed-on 52.779 s then a >60 s kill; shed-off a >60 s kill then 48.029 s. A single-sample reading of that pair would have concluded 'the shed is slower', and the interleaved repeat is what refuted it -- that claim is RETRACTED here rather than softened. So the answer to c1 as written is: neither the fixture's own cost NOR the spill/read-back path. The cost tracks the PAYLOAD through the engine turn and is present in full with the spill mechanism entirely disabled. SHED-OFF PAYLOAD SCALING, same session: 60,000 -> 7.845 s; 120,000 -> 11.310 s; 240,000 -> 30.717 s; 480,000 -> 48.029 s (host load moved 40 -> 124 across this series, so read it as monotonic-and-roughly-linear, not as clean coefficients; the cleanest pair is the load-~40 shed-on pair above, which is 2.00x for 2.00x). CAVEAT STATED RATHER THAN BURIED: hetzner-dsm is a shared 96-core box carrying many lanes; every number here carries its measured load, and the arms that matter were interleaved for exactly that reason. REMAINDER, NOT CLOSED HERE: the ~linear per-byte cost of carrying a large tool result through a turn is a product finding this criterion SURFACES but no criterion on this ticket repairs, and naming the function inside `engine.run()` needs a profiler (hetzner has neither perf nor gdb). Filed as wayland-core#395 rather than left in this note."
  - id: c2
    text: "Either the test completes inside the default profile's 30s slow-timeout, or it carries an explicit per-test budget with the reason recorded in .config/nextest.toml"
    state: met
    evidence: "test:crates/wcore-agent/tests/spill_readback_engine_wiring.rs::the_engine_spills_where_this_session_can_read_it_back"
    owner: core
    note: "MET BY THE FIRST DISJUNCT -- the test completes inside the default profile's 30 s slow-timeout, so no per-test budget and no `.config/nextest.toml` override is added. THE FIX IS THE FIXTURE, and it follows from c1: since the cost tracks the payload and not the spill, the payload is the only lever that moves the runtime without changing what is graded -- and the payload was never load-bearing. What the test grades is that the shed FIRES and that this session's own jail can read the spilled file back; neither depends on half a megabyte. THE OVER-CEILING RATIO IS PRESERVED EXACTLY, so this is a re-sizing and not a weakening: the old fixture was 480,000 chars (120,000 tok) against a 60,000 - 10,000 - 10,000 = 40,000 tok effective ceiling, i.e. 3x over; the new one is 60,000 chars (15,000 tok) against 25,000 - 10,000 - 10,000 = 5,000 tok, also 3x over. MEASURED AFTER, hetzner-dsm, binary alone, `--retries 0`, default profile: 10.421 / 9.807 / 9.323 s at host load 206 / 220 / 219. The OLD fixture took 48.500 / 48.557 / 52.743 s at host load ~40 and was killed at TIMEOUT [60.010s] in the filing. So the new figure is inside the 30 s budget at FIVE TIMES the load under which the old one took 48 s; margin stated rather than claimed as immunity: 2.9x at the highest load observed (10.4 s against 30 s), where the old one had 0.6x. It has not been measured above load 220. RED ARM, and it is the one this change actually needs -- the risk of shrinking a fixture is that it stops exercising the thing it grades and starts passing VACUOUSLY. Mutation: `let huge = 'x'.repeat(60_000)` -> `20_000`, i.e. below the shed threshold. Read back out of the file before running and confirmed to land on EXECUTABLE code -- line 113, and the is-comment check returned 0. `cargo check -p wcore-agent --tests` rc=0 BEFORE believing the red, because a mutation that does not compile proves nothing. MUTATED ARM VERBATIM: rc=100, `thread 'the_engine_spills_where_this_session_can_read_it_back' (3877304) panicked at crates/wcore-agent/tests/spill_readback_engine_wiring.rs:44:5: / no spilled tool result in the persisted history — the shed never ran`, `FAIL [ 2.118s]`. So a fixture that stops crossing the ceiling reds LOUDLY instead of passing empty. RESTORED with `git checkout --` + `touch`, verified by BLOB IDENTITY (`git hash-object` == `git rev-parse HEAD:<path>` == ce232d34272f5c55c080ebb4c761dd06a35d4889), and re-green afterwards at PASS [5.671s]. BOUNDARY MEASURED, not guessed: at 40,000 chars / ceiling 24,000 the shed does NOT fire (`shed=false`), so 60,000 sits above the trigger rather than on it. THE SECOND, DISCRIMINATING RED ARM IS NOW RUN, AND IT IS THE ONE THAT DECIDES WHETHER THE SHRINK GUTTED THE TEST. It was deferred once, honestly, because a co-tenant soak was mid-flight in this worktree at streak 9 of 10 and a lib mutation would have reddened its final run; it has since been run twice, independently, on trees with no soak in flight. (a) BY THE VERIFIER, 2026-08-30, in its own worktree at commit 0a432c538: `CHECK_RC=0`, then `RED_RC=100` with `FAIL [5.574s] wcore-agent::spill_readback_engine_wiring the_engine_spills_where_this_session_can_read_it_back` and `panicked at spill_readback_engine_wiring.rs:130:9: the engine told the model to read /tmp/wayland-results/big.txt and this session's own file tools refused: path \"/tmp/wayland-results/big.txt\" is outside sandbox root \"/tmp/.tmpopstk0\"`. (b) REPRODUCED HERE at HEAD 2a4b43af1, hetzner-dsm, host load average 30.85 read immediately before the arm. Mutation applied by asserting the exact source line first (`let storage = self.tools.spill_storage();` at engine.rs:13712, is-comment check 0) and replacing only the receiver with `wcore_tools::tool_result_storage::StorageDir::os_default()`. `cargo check -p wcore-agent --tests` rc=0 BEFORE believing the red -- a mutation that does not compile proves nothing. MUTATED ARM VERBATIM: `RED_RC=100`, `FAIL [ 6.344s] (1/1) wcore-agent::spill_readback_engine_wiring the_engine_spills_where_this_session_can_read_it_back`, `panicked at crates/wcore-agent/tests/spill_readback_engine_wiring.rs:130:9: the engine told the model to read /tmp/wayland-results/big.txt and this session's own file tools refused: path \"/tmp/wayland-results/big.txt\" is outside sandbox root \"/tmp/.tmpyhYFtO\"`. RESTORED and verified by blob identity three ways in the same call -- `git hash-object` == `git rev-parse HEAD:crates/wcore-agent/src/engine.rs` == `git rev-parse origin/integ/f13:crates/wcore-agent/src/engine.rs` == be8c706e968401ce4e48b0870b63cca3ddaa393c, working tree clean -- then re-green at `GREEN_RC=0`, `PASS [5.591s]`. WHAT THAT SETTLES: the 8x fixture shrink did NOT buy c2 by gutting the discrimination. The regression the test's own docstring names -- 'swapping it back to StorageDir::os_default() keeps every existing test green' -- still reds this test at 60,000 chars, and reds it in 6.3 s rather than 48 s. The vacuity arm above proves a fixture below the ceiling reds loudly; this arm proves a correct fixture still catches the product regression. Both directions are now covered. THIRD INDEPENDENT CONFIRMATION, 2026-08-31, second adversarial re-verification, in the verifier own worktree /root/w-f13/v2b-w3-test-infra at 925ded18d: the same mutation (`self.tools.spill_storage()` -> `wcore_tools::tool_result_storage::StorageDir::os_default()`, asserted to land on code and not a comment), `cargo check -p wcore-agent --tests` CHECK_RC=0 before the red was believed, then RED_RC=100 with `FAIL [5.534s] the_engine_spills_where_this_session_can_read_it_back` and `panicked at spill_readback_engine_wiring.rs:130:9: ... path /tmp/wayland-results/big.txt is outside sandbox root /tmp/.tmpGn73er`, at host load average 27.44; restored be8c706e9... == origin/lane == origin/integ/f13, then GREEN_RC=0 PASS [5.505s]. Three runs, three worktrees, two graders, same verdict: the 8x fixture shrink did not gut the discrimination. This row is folded in as standing evidence and is not re-derived again."
  - id: c3
    text: "If it stays load-sensitive it is listed in .config/flaky-allowlist.txt with an expiry and a measured rate, so an unlisted flake cannot redden the required report check with no diagnostic attached"
    state: met
    evidence: "file:.config/flaky-allowlist.txt:1:Retry allowlist"
    owner: core
    note: "MET BECAUSE ITS ANTECEDENT IS NO LONGER TRUE, and that is stated as the reason rather than dressed up as a clearance. c3 reads 'IF it stays load-sensitive it is listed in .config/flaky-allowlist.txt with an expiry and a measured rate'. After c2 the test runs 10.421 / 9.807 / 9.323 s against a 30 s default budget at host load 206-220, and 5.671 / 5.871 s at load ~90 -- it no longer crosses, or approaches, a kill budget, so there is no flake to list and adding a line would be exactly the 'quiet a race you have not investigated' entry the allowlist header forbids. NOTHING WAS ADDED TO .config/flaky-allowlist.txt AND NOTHING TO .config/nextest.toml. The file still contains no `spill` entry, which is also what it contained before this lane -- the difference is that the reason is now measured instead of absent. SCOPE NOTE, carried from the ticket's own comments and NOT closed by this row: two later comments on #378 report the SAME class in `wcore-cli::deterministic_openai_loop::packaged_f04_run_is_repeatable_and_content_addressed` (58.6-87.2 s against a default 30 s x 2 = 60 s kill, a 1.9% margin, while `[[profile.ci.overrides]]` already pins it at 120 s x 4). c1-c3 are scoped to `spill_readback_engine_wiring` BY NAME, so that test is untouched here and its default-profile margin is still 1.9%. It is not covered by this ticket's criteria and is not claimed as covered."
---

`wcore-agent::spill_readback_engine_wiring::the_engine_spills_where_this_session_can_read_it_back` is the slowest test in the crate and is not in the flaky allowlist. It reported SLOW [>30.000s] in all three of my full-suite runs and in one of them was killed: `TIMEOUT [ 60.010s] (3894/3895)`. That run finished `3894 passed, 1 timed out`. The other two runs it completed. Caveat stated up front so this is not oversold: I ran the `default` nextest profile (slow-timeout 30s, terminate-after 2 -> kill at 60s, matching the observed 60.010s). `[profile.ci]` gives 90s x 2 = 180s, so CI would probably not kill it -- but it is the one test in this crate whose runtime is load-sensitive enough to cross a kill budget at all, it is unlisted, and with grade-retry-flakes.sh gating `report` an unlisted flake fails the run.

**Where.** crates/wcore-agent/tests/spill_readback_engine_wiring.rs::the_engine_spills_where_this_session_can_read_it_back ; budgets at .config/nextest.toml:72 (default) and :278 (ci)

**Why it matters.** Either the spill/read-back path really can take >60s under contention, which is a product latency finding on a path users hit, or the test's fixture is doing something disproportionate. Nobody has graded which. It surfaced only because I ran the suite at --retries 0; under CI's retries=2 it would come back as FLAKY-then-pass and, being unlisted, would redden the required `report` context with no diagnostic attached -- the exact failure shape #337 itself was filed about.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.

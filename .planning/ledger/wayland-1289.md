---
issue: 1289
repo: FerroxLabs/wayland
kind: defect
title: "macOS keyring does not answer within the 5s credential-store timeout; f14_sigkill_recovery hard-fails 3/3"
status: open
last_verified_commit: 6e4eca07
criteria:
  - id: c1
    text: "The 5s credential-store wait is either shown to be adequate under parallel nextest on macOS, or the timeout is raised, retried, or the affected tests are serialised so a contended keychain does not hard-fail them."
    state: not-met
    evidence: "file:crates/wcore-cli/tests/f14_sigkill_recovery.rs"
    owner: core
    note: "MEASURED AND ROOT-CAUSED 2026-09-03, and the ISSUE'S OWN HYPOTHESIS IS REFUTED. It said 'nextest runs at num-cpus parallelism and macOS SERIALISES keychain access, so concurrent tests may queue past 5s'. On Linux that is false: neither rung of the store is slow. The keyring rung fails in 0-4ms (no bus, or a locked `login` collection) and the fallback vault is local CPU -- Argon2id measured at 142ms single-threaded and 1244ms worst case at 192-way. Nothing approaches 5s. KEY_STORE_ACQUIRE_BUDGET (recovery_confidential.rs:223) is a WALL-CLOCK deadline on a thread doing that work, so exceeding it means the thread was not SCHEDULED. INTERLEAVED A/B, same tree, same box, same detector, only --test-threads differing: 96 (the configured num-cpus) 0/20 runs and 0 occurrences; 192 (2x oversubscription) 19/20 runs and 79 occurrences. Load sampled per run: 67-106 vs 105-151. So it does NOT reproduce on Linux at the parallelism CI uses -- 80 runs at nominal -j were clean, 17,812/17,812 -- and the title's 'reproduces on BOTH macOS and Linux' holds only under deliberate oversubscription. THAT IS ALSO WHY macOS FAILS: a 3-4 vCPU hosted runner, where each of these tests spawns several full engine processes (a seeder child plus a CoreProcess), is ALREADY oversubscribed at nominal num-cpus -- the same regime as the 192-thread arm. CORRECTION TO THE ISSUE'S POPULATION COUNT: the macOS failure hit 100% of its exposed population, not 3 of 4. The fourth caller of seed_recoverable_profile is cfg(target_os = \"linux\") (f14_sigkill_recovery.rs:1882) and does not compile on macOS, so three tests reach the seeder there and all three failed -- universal to that path, not selective. FIX APPLIED HERE: threads-required = 4 on binary(f14_sigkill_recovery) in .config/nextest.toml, NOT a test-group. The repo's own swarm block records why, from its own measurement: a group with max-threads bounds concurrency WITHIN the group, and the pressure comes from the other ~17,800 tests in the run. NOT VERIFIED ON macOS, which is the platform that decides -- Linux cannot exhibit it at nominal parallelism, so macOS CI is the only arm, and c1 stays not-met until a macOS run exercises it. The MESSAGE the timeout raises is a separate product defect and is filed as FerroxLabs/wayland#1302: the same payload was produced 104 times against a healthy store, and it tells the operator to repair a keyring that is not broken. ORIGINAL: Filed 2026-09-01. NOT FIXED IN 0.13.12 and NOT CAUSED BY IT. Milestoned 0.13.13. In run 33499226403 three tests failed ALL THREE attempts -- deterministic within that run, so no allowlist entry could or should have rescued them -- with SessionAuthority('the configured credential store did not answer within 5s') raised at f14_sigkill_recovery.rs:593 and surfaced at :641 as a non-zero seeder exit. That string appears ZERO times across all five previously stored macos-latest artifacts, and no credential, session or keyring code is in the 0.13.12 diff. DID NOT REPRODUCE on the next run (17,672/17,672 passed, 0 retries), and that clean run was 37% faster (1,332s vs 2,098s), which is consistent with the contention hypothesis -- nextest runs at num-cpus and macOS serialises keychain access -- but one datapoint does not measure a rate, so this stays open rather than being closed as transient."
---

# A 5s ceiling on the OS keyring

Also a real user-facing failure mode on a locked login keychain, not only a CI artifact.

---
issue: 1289
repo: FerroxLabs/wayland
kind: defect
title: "macOS keyring does not answer within the 5s credential-store timeout; f14_sigkill_recovery hard-fails 3/3"
status: open
last_verified_commit: 5b76ec723
criteria:
  - id: c1
    text: "The 5s credential-store wait is either shown to be adequate under parallel nextest on macOS, or the timeout is raised, retried, or the affected tests are serialised so a contended keychain does not hard-fail them."
    state: not-met
    evidence: "file:crates/wcore-cli/tests/f14_sigkill_recovery.rs"
    owner: core
    note: "STILL NOT MET, and the reason has CHANGED: the applied remedy has never been exercised by any measurement anyone has taken, including the one this ticket's own lane and gh#1290's lane ran. THE REMEDY IS SCOPED TO A PROFILE NOBODY MEASURES WITH. `threads-required = 4` sits under `[[profile.ci.overrides]]` at .config/nextest.toml:702-704 and `filter = 'binary(f14_sigkill_recovery)'` occurs EXACTLY ONCE in that file (verified: one match, line 703). nextest applies a profile's overrides only when that profile is selected; `profile.ci` inherits from `profile.default`, not the reverse. CI runs `--profile ci` (.github/workflows/ci.yml:709,713) so CI has it. tools/remote-proof.py does NOT pass `--profile`, and every receipt it prints says `with nextest profile: default`. THEREFORE: gh#1290's n=20 hetzner census, which is the largest body of evidence anyone has about this failure, ran WITHOUT the remedy in effect, and so did every other Linux run cited on this ticket. The remedy is unmeasured on Linux as well as on macOS. That is not an argument against it; it is a statement that nothing yet distinguishes it from a no-op. WHAT THIS LANE MEASURED, 2026-09-10, and it is a NEGATIVE result reported as one. Five invocations of `WCORE_PROOF_SLOT=default python3 tools/remote-proof.py worktrees/w15-allow nextest run -p wcore-cli --test f14_sigkill_recovery --retries 0 --no-fail-fast --test-threads 192` at 508bd6c4d on hetzner-dsm (96 cores), i.e. the 2x-oversubscription regime this ticket established as the macOS-equivalent one. RESULT: 5/5 `39 tests run: 39 passed, 1 skipped`, 11.07-11.56s each, remote_exit 0 and complete true on all five, and ZERO occurrences of `did not answer` across all five outputs. Box load at the start of each run, sampled from /proc/loadavg: 38.5, 36.9, 37.8, 41.2, 41.0. WHAT THAT DOES AND DOES NOT SHOW. It shows the 5s budget is ADEQUATE on Linux at 2x thread oversubscription when the box is at load ~40 on 96 cores -- 195 test executions, no timeout. It does NOT show the budget is adequate, because the regime that breaks it is not thread count: gh#1290 recorded 18 of 20 runs failing at load 183, and this ticket's own earlier A/B got 19/20 at 192 threads on a box that was busy at the time. The variable is the BOX, not the flag, which is the same conclusion gh#1290 c1 reached ('the variable is the worker, not the tree'). CONSEQUENCE FOR THE A/B THIS LANE WANTED TO RUN: with arm A (no remedy) clean 5/5, an arm B (`--profile ci`, remedy active) cannot distinguish anything. Manufacturing load ~180 on a box two other lanes are building on was not done deliberately -- it would have produced the measurement by degrading their runs. THE MESSAGE HALF IS DONE AND IS NOT THIS CRITERION. wayland#1302's `KeyStoreReach` is in the tree (crates/wcore-agent/src/recovery_confidential.rs): the loader closure sets `asked` as it ENTERS the store call, so a wait that expires with `NeverAsked` is reported as 'this host did not run the load in time, so the store was never asked, reported nothing, and nothing here shows it to be at fault' rather than as 'unlock or repair the OS keyring'. That fixes a false accusation. It does not make the wait adequate, which is what c1 asks. THE REPAIR THIS LANE WOULD MAKE, and why it did not. The nextest.toml block already names the right trigger: 'delete it when KEY_STORE_ACQUIRE_BUDGET stops being a wall-clock deadline on a schedulable thread'. `KeyStoreReach::NeverAsked` is precisely the observation that the deadline expired without the store being asked anything -- so the budget measured OUR OWN scheduling, not the store's silence, and spending it as if it had is the defect. The repair is to grant one bounded extension when the wait expires with `NeverAsked` (the load has not reached the store, so nothing about the store has been learned) while leaving `Asked` at 5s, which is the case the budget was designed for. NOT IMPLEMENTED HERE, deliberately: it changes the user-visible dead-air budget on the pre-provider path of every journaled turn, and the failing regime cannot be summoned on this box at this load, so it could only have been shipped with a green arm and no red one -- a fix verified on a host that cannot exhibit the failure, which is the exact class this cluster keeps producing. WHAT IS OWED. (1) The macOS arm: this platform decides it and cannot be run from this lane. (2) An A/B of the remedy in the load regime that actually exhibits the failure (load >= ~150 on 96 cores, or a 3-4 vCPU host), `--profile ci` against `--profile default`, since the remedy has never been in effect in any measurement. (3) If (2) shows the remedy does not move it, the `NeverAsked` extension above, red-armed in that same regime. c1 stays not-met on all three. PRESERVED FROM THE 2026-09-03 GRADING, which is unchanged and still correct: the issue's own hypothesis is refuted (neither store rung is slow -- keyring fails in 0-4ms, Argon2id 142ms to 1244ms at 192-way, nothing approaches 5s), the budget is a wall-clock deadline on a thread doing that work so exceeding it means the thread was not SCHEDULED, macOS fails because a 3-4 vCPU hosted runner is already oversubscribed at nominal num-cpus, and the macOS failure hit 100% of its exposed population (three callers reach the seeder there; the fourth is cfg(target_os = linux)) rather than 3 of 4."
---

# A 5s ceiling on the OS keyring

Also a real user-facing failure mode on a locked login keychain, not only a CI artifact.

---
issue: 395
repo: FerroxLabs/wayland-core
kind: defect
title: "engine.run() cost is ~linear in tool-result size (~100 s/MB in the test profile), and it is not the spill path"
status: open
last_verified_commit: 4a738f2e
criteria:
  - id: c1
    text: "The debug-vs-release question is SETTLED by measurement: the same probe is run under `--release` at 240,000 and 480,000 chars, and the per-byte term is either reproduced (a product finding) or shown to collapse (a test-profile artifact). Whichever it is, the numbers and the host load are recorded."
    state: met
    evidence: "file:.config/nextest.toml:339:THE SHAPE, four payload sizes 60,000 -> 480,000 chars, shed off so every"
    owner: core
    note: "MET BY MEASUREMENT 2026-08-31 on lane/f13-s2-turn-cost. The release arm was RUN, at the two sizes this criterion names and three more. The per-byte term is REPRODUCED under --release, so this is a product finding and NOT a test-profile artifact. Debug and release arms interleaved on the same host minutes apart, shed forced OFF (SPILL_PROBE_CEILING=100000000) so both sizes take one identical path, engine.run() only (setup and read-back timed separately at 0.001s each): 240,000 chars -> debug 23.362s / release 1.630s at host load 42.54; 480,000 chars -> debug 46.530s / release 3.018s at host load 42.58-43.08. Release is 14.3x-15.4x cheaper and still LINEAR: 2.00x the bytes costs 1.85x the seconds in release and 1.99x in debug. Full four-size sweep, min of two reps, host load 52-79 recorded per reading: release 0.402 / 0.754 / 1.493 / 3.127s and debug 5.622 / 11.025 / 23.279 / 46.438s at 60k / 120k / 240k / 480k. Least-squares log-log fit: release exponent 0.986 (R^2 0.9987), 6.49 s per MB of tool output; debug exponent 1.022 (R^2 0.9996), 97.18 s per MB. The numbers, the fit and the host loads are recorded in product-adjacent code -- .config/nextest.toml around the override at line 372 and the module doc of crates/wcore-agent/tests/turn_cost_per_byte_guard.rs -- not only here. A 960,000-char arm was taken and then DISCARDED rather than reported: MockTool::with_max_result_size(600_000) truncates it, so its effective payload is 600,036 bytes and it is not a valid point on a payload sweep. Instrument: crates/wcore-agent/tests/spill_timing_probe.rs, carried into this lane at 033252c9e. Host: hetzner-dsm, 96 cores; host load is quoted with every figure because a loaded box has invalidated three measurements this cycle. || SECOND INSTRUMENT 2026-08-31 (lane/f13-s2-turn-cost-audit @ ca15a48bf + lane artifact): CONFIRMED by an INDEPENDENT instrument that does not use the lane's probe, the engine, or the lane's arithmetic. crates/wcore-safety/tests/zz_audit_scrub_shape.rs times PIIScrubber::scrub directly over 60k/120k/240k/480k chars. RELEASE, host load 77.5: 0.1955 / 0.4125 / 0.7585 / 1.4730 s, exponent 0.9619, R^2 0.9984, 3.069 s/MB for ONE pass. DEBUG, host load 58.4: 2.7525 / 5.4441 / 10.8792 / 21.6927 s, exponent 0.9934, R^2 1.0000, 45.193 s/MB for one pass. At two passes per turn that predicts 2.95 s release and 43.4 s debug for a 480,000-char turn, against the lane's measured engine.run() of 3.018 s and 46.530 s -- agreement to 2% and 7% from a completely separate measurement path. THE PER-BYTE TERM DOES NOT COLLAPSE UNDER RELEASE. c1's answer stands."
  - id: c2
    text: "If the per-byte term survives release, the function carrying it is NAMED by measurement — a profiler, or bisecting instrumentation inside `run_turn` — and not inferred. Two inferences are already recorded as unpromising above; naming a third by reading is not a close."
    state: met
    evidence: "file:.config/nextest.toml:328:of which decoded_contains_secret 2.981s (98.8%)"
    owner: core
    note: "MET BY MEASUREMENT 2026-08-31, not by reading. The term survives release (c1), so this criterion is live. Named with bisecting instrumentation compiled INTO the turn path -- hetzner has no perf, no gdb and no valgrind, and linux-perf has no candidate for kernel 6.8.0-101-generic, so the profiler route was checked and is unavailable. The instrumentation is commit f06890a24, reverted by 83258da7e whose tree is byte-identical to 033252c9e (git diff 033252c9e HEAD reported 0 lines), so it is reproducible and it is not in the shipped tree. It timed seven candidate sites at once and reported an accounting, so a near-zero reading is as informative as the hit. Verbatim at 480,000 chars, shed off: RELEASE run 3.018s, redact_tool_output 3.004s over 2 calls, PIIScrubber::scrub 3.004s, decoded_contains_secret 2.981s over 4 calls -- 98.8% of the whole turn. DEBUG run 46.530s, PIIScrubber::scrub 46.446s (99.8%), decoded_contains_secret 46.106s (99.1%). THE FUNCTION IS wcore_safety::pii::decoded_contains_secret, reached through wcore_agent::output_redaction::redact_tool_output -> PIIScrubber::scrub. It base64-decodes the ENTIRE candidate run under four alphabets (STANDARD, STANDARD_NO_PAD, URL_SAFE, URL_SAFE_NO_PAD, all four evaluated eagerly) and re-scans every decode with scrub_direct; scrub reaches it twice (the base64_candidates loop 1.506s and the wrapped_base64 loop 1.487s in release), and the engine scrubs each tool result twice per turn, so one turn costs ~2 whole-payload passes. RULED OUT BY THE SAME RUN, so no fourth inference is needed: estimate_tokens_from_messages 0.0000s over 6-14 calls (the reading already recorded as unpromising on the issue, now measured), the #636 shed 0.0013s, scrub_direct 0.0015s, redact_active_tokens 0.0003s, split_secret_windows 0.0074s, fixture construction 0.001s, read-back through the jail 0.001s. Host load 42-59 across these readings. Host: hetzner-dsm, 96 cores; host load is quoted with every figure because a loaded box has invalidated three measurements this cycle. || SECOND INSTRUMENT 2026-08-31: CONFIRMED. The named carrier was verified without the lane's instrumentation commit: timing wcore_safety::PIIScrubber::scrub alone (crates/wcore-safety/src/pii.rs, decoded_contains_secret at line 163) accounts for 43.4 s of the 44.63 s of USER CPU I measured for the whole graded test (/usr/bin/time -v, host load 43.4, 0.01 s system, 38.7 MB RSS) -- so the attribution is closed arithmetically by an instrument the lane did not use. decoded_contains_secret was also read and matches the description: a 4-element array of base64 alphabet decodes, built eagerly before .into_iter(), each decode re-scanned with scrub_direct. Anchor note: this criterion's evidence points at a COMMENT in .config/nextest.toml. A stronger anchor exists and was available -- crates/wcore-safety/src/pii.rs:163 is the product code that carries the cost."
  - id: c3
    text: "A regression guard exists for whichever answer c1 gives: if it is a product cost, a test that fails when the per-byte term grows; if it is an artifact, the finding is recorded where the next person measuring a slow wcore-agent test will find it, so this is not re-derived a third time."
    state: not-met
    evidence: "symbol:crates/wcore-agent/tests/turn_cost_per_byte_guard.rs::one_turn_costs_about_two_whole_payload_scrub_passes"
    owner: core
    note: "MET 2026-08-31. c1 returned A PRODUCT COST, so this criterion takes its first branch: a test that fails when the per-byte term grows. crates/wcore-agent/tests/turn_cost_per_byte_guard.rs drives the PRODUCTION path -- a real AgentEngine::run() over a MockLlmProvider turn carrying a tool result, exactly the harness the graded spill test uses -- at N and 2N bytes, and divides (t(2N) - t(N)) by the cost of ONE PIIScrubber::scrub pass over N bytes measured in the same process. N and the fixed per-turn cost both cancel, leaving the number of whole-payload passes one turn costs: 2.0 today. IT IS A RATIO ON PURPOSE. An absolute wall-clock bound cannot work here -- the same term is 15x larger in debug than release and this box is shared -- so a constant would either flake or catch nothing. RED ARMS, both compiled clean first (cargo check RC=0, so the red is behaviour and not a build break). (1) ONE extra redact_tool_output call added at the production site crates/wcore-agent/src/orchestration/mod.rs:2594, i.e. 2 passes -> 3: FAIL, median 2.81 against the < 2.5 bound. (2) The PII scrub removed from redact_tool_output: FAIL at -0.06 against the > 1.5 bound, AND the companion control test failed with 'the tool result reached history with a live GitHub PAT in it'. Both arms restored, files touched so cargo could not serve a stale binary, git status --porcelain empty, and the green arm re-run: PASS at median 2.02. WRONG-REFUSAL CONTROL in the same file: the_turn_still_redacts_a_secret_and_leaves_ordinary_output_alone asserts a real ghp_ token IS redacted out of the tool result AND that ordinary `ls -l` output survives byte-intact, so neither deleting the scrub nor scrubbing everything can satisfy this guard. STABILITY MEASURED, not assumed: 9 runs at host load 34-45 gave 1.94-2.11, and 5 runs with the box deliberately oversubscribed (48 spinners on 96 cores, load 68-88) gave 1.97-2.03 -- the first estimator tried (per-sample min) gave 0.98 under that load, a false red, which is why the shipped one takes a per-round ratio and the median of three rounds. Runtime 8.4-13.0s in debug, inside the 30s slow-timeout with no override. Host: hetzner-dsm, 96 cores; host load is quoted with every figure because a loaded box has invalidated three measurements this cycle. || DOWNGRADED met -> not-met BY THE SECOND INSTRUMENT 2026-08-31. The guard EXISTS, drives the production AgentEngine::run() path, is collected by nextest (not #[ignore]d), carries a wrong-refusal control, and I reproduced its green at median 2.01/2.01/2.02 -- none of that is in dispute. What it does NOT do is the thing this criterion's sentence asks: FAIL WHEN THE PER-BYTE TERM GROWS. RED ARM THE LANE DID NOT RUN: crates/wcore-safety/src/pii.rs decoded_contains_secret mutated from 4 base64 alphabets to 8 (the same four repeated), i.e. the per-byte cost of the named carrier DOUBLED while the number of scrub passes per turn stayed at 2. cargo check -p wcore-agent --tests RC=0 first, file touched. THE MUTATION LANDED, proven by the guard's own printout: min_small 0.8060 -> 1.5751 s, min_large 1.5572 -> 3.1256 s, min_scrub 0.3719 -> 0.7541 s, i.e. every absolute timing almost exactly doubled and the turn's per-byte term went from ~97 to ~194 s/MB in debug. THE GUARD PASSED, median_passes = 2.04. It is structurally blind to this: the assertion is (t(2N)-t(N)) / one_scrub_pass(N), and one_scrub_pass is measured with the SAME mutated scrubber, so the growth cancels in numerator and denominator. The 'large < 2.8 * small' shape assert is a ratio too and is equally blind. The lane's two red arms both changed the NUMBER of whole-payload passes (adding a redact_tool_output call; deleting the scrub), which is the one axis the ratio can see. Restored, touched, green re-verified at 2.01/2.02/2.01. This matters because the most likely real regression on this exact cost is a change INSIDE the carrier -- the function is literally an array of alphabets -- and a 2x regression also stays under the new 120 s nextest budget (44 s -> ~88 s), so neither artefact in this lane would report it. WHAT WOULD MEET THE SENTENCE: keep the ratio assertion (it catches added passes) and add a second assertion bounding the scrub unit cost per byte against a recorded baseline normalised by an in-process CPU reference, so a slower carrier reds even though the pass count is unchanged."
---

Created 2026-08-31 to close a COVERAGE gap. It records no work as done.

`scripts/check-criteria-ledger.py` scopes every open `area:core` issue on
wayland and EVERY open issue on wayland-core. This issue was in scope from
the moment it was filed and had no ledger file, so
`scripts/check-release-readiness.py` -- which reads ledger files and nothing
else -- could not count it. CI runs the coverage gate with `--offline`, the
arm that would have reported the gap, so nothing said so for two days.

Criteria are transcribed from the issue body without edit. Where the body's
wording is loose it is LEFT loose rather than tightened here: sharpening a
criterion inside the ledger is how a criterion quietly becomes an easier
adjacent property. Whoever takes this restates it on the ISSUE first.

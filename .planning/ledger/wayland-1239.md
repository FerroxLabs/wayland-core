---
issue: 1239
repo: FerroxLabs/wayland
kind: defect
title: "atomic_write_checked destroys a concurrent save that lands inside the exchange-to-verdict window, and the refusal is byte-identical to one that lost nothing"
status: open
last_verified_commit: 8ace76d61
criteria:
  - id: c1
    text: "A save that lands on the displaced inode inside the exchange-to-verdict window is not silently destroyed on the refusal path: it is preserved on disk, or the caller is told, or the write is not refused over it"
    state: met
    owner: core
    evidence: "test:crates/wcore-config/src/atomic_io.rs::a_save_made_inside_the_verdict_window_survives_the_rollback"
    note: "Closed on lane/f13-w2-dataloss. The restore is an exchange, so it is also an OBSERVATION: `fn restore` now returns the name under which whatever `dest` held AT THE INSTANT OF THE RESTORE now lives (`Option<PathBuf>`; `None` only where the platform overwrote rather than exchanged). `atomic_write_checked` compares that file against the bytes it published: equal means it is the copy this module put there a moment ago and it is discarded as before; UNEQUAL means somebody saved onto the published inode inside the exchange->verdict window, and it is preserved via the existing `keep_displaced` path and named in `Refusal::intercepted_save`. Both disjuncts of the criterion are therefore satisfied -- preserved on disk AND the caller told. Detection is atomic, not a re-read: the capture is handed back by the same syscall that put the original back, so there is no second observation to be stale, which is the same argument the publish rests on. Failure direction is fail-SAFE: `holds_exactly` answers false for an unreadable file, so a file this module cannot prove is its own is KEPT. Red arm exit codes captured DIRECTLY (`cmd > file 2>&1; $?`), never through a pipe; each mutation printed the enclosing function body before and after so it is proven to land on executable code, and each site was restored with `git checkout --` plus a `touch` so cargo could not serve the mutated binary. Log: /root/w-f13/s2-red2.log."
  - id: c2
    text: "The caller can distinguish the ARM_A outcome from the ARM_B outcome - a refusal that destroyed someone's save is not byte-identical to a refusal that destroyed nothing"
    state: met
    owner: core
    evidence: "test:crates/wcore-tools/src/unsaved_work/tests.rs::a_refusal_that_displaced_a_save_does_not_read_like_one_that_did_not"
    handoff: "FerroxLabs/wayland#1248"
    note: "Two levels, because the criterion says CALLER and the release theme says what the user is TOLD. TYPED: `atomic_write_checked`'s refusal payload changed from a bare `String` to `wcore_config::Refusal`, which carries `why()` and `intercepted_save()`; the atomic_io triple asserts `ref_a != ref_b`. SURFACED: `unsaved_work::refusal_message` is the single selector, called from both tool sites (edit.rs and write.rs -- grep-verified at exactly two call sites plus the declaration), and the named test drives REAL `atomic_write_checked` runs for ARM_A and ARM_B, then asserts the two rendered strings differ, that the lossy one names the preserved path, that it does NOT contain 'Nothing was changed.', and that the control still does. The red arm is the criterion restated: M2 collapsed the selector to `changed_under_write(...)` and the test failed with the two strings printed byte-identical -- the defect's own sentence. Red arm exit codes captured DIRECTLY (`cmd > file 2>&1; $?`), never through a pipe; each mutation printed the enclosing function body before and after so it is proven to land on executable code, and each site was restored with `git checkout --` plus a `touch` so cargo could not serve the mutated binary. Log: /root/w-f13/s2-red2.log. DECOMPOSED REMAINDER, handed off as FerroxLabs/wayland#1248: the criterion's subject is the CALLER of atomic_write_checked and both callers can now distinguish the two refusals, but only the two DIRECT tool paths render the distinction. The VFS path (SandboxedFs::compare_exchange_file) matches the publish result with `Err(_)` and maps the whole `Refusal` onto `FileMutationOutcome::Conflict { current }`, so write.rs:219 and edit.rs:318 reconstruct a `why` from the re-observed state and render the wording that ends 'Nothing was changed.' -- with the displaced save preserved on disk under a name nothing reports. Not data loss and not a counterexample to c1, whose disjunction is satisfied by the preservation on both paths; what is missing on the VFS path is the notice. Plumbing it needs a field on `FileMutationOutcome::Conflict`, which is also constructed by three paths where no publish ran at all (the pre-flight classification arms, the InMemoryFs backend, the containment wrapper), so it is its own change with its own anti-overcorrection criterion rather than a line here. Searched before filing -- compare_exchange_file, FileMutationOutcome, 'vfs Conflict changed_under_write', 1239, atomic_write_checked, write_through_vfs, 'preserved at' -- which returned only wayland#1155, #1202, #1239 and #1241, none of which covers it (#1241 says in as many words that its scope is 'only the direct Tool::execute path'). CORRECTION, on the verifier's F2 -- the completeness argument this note was summarised with was FALSE AS WRITTEN, and in a lane whose whole method is enumerate-and-count that matters even where the verdict survives. What is true and re-measured at HEAD: `refusal_message` has exactly two production call sites (edit.rs:541, write.rs:502) plus its declaration, and that part stands. `changed_under_write` has FOUR production call sites, not three -- edit.rs:321, edit.rs:336, write.rs:227, write.rs:249 -- plus the declaration and the one call inside `refusal_message` itself. And it is NOT true that all of the others take a `&str` from paths that never see a `Refusal`: two of the four, edit.rs:321 and write.rs:227, are the `Ok(FileMutationOutcome::Conflict { .. })` arms, and vfs.rs:551 reaches them from the same `atomic_write_checked`, with vfs.rs:566 throwing the `Refusal` away as `Err(_) => Ok(FileMutationOutcome::Conflict { .. })`. So on the VFS path a refusal whose retraction displaced somebody's save renders 'Nothing was changed.' verbatim over a `.tmpXXXXXX` file nothing names -- and that orphan is NEW, created by this lane's own preservation fix, where the same bytes used to be deleted. The other two sites (edit.rs:336, write.rs:249) are the `is_compare_exchange_unsupported` re-read arms, which genuinely do build a `&str` with no publish and no `Refusal`. COVERED, checked rather than assumed: FerroxLabs/wayland#1248 -- the decomposed remainder filed by this lane, OPEN -- names `crates/wcore-tools/src/write.rs:219` and `crates/wcore-tools/src/edit.rs:318` as the two renderers in its Where section, quotes the `Err(_)` arm verbatim, and grades exactly this in c2 ('a refusal that displaced a save does not end \"Nothing was changed.\" and does name where those bytes are') with c3 requiring a test that asserts the SURFACED tool text and c4 protecting the `Conflict` sites where no publish ran. Nothing needed to be added to #1248 and nothing new was filed. The corrected count does not change this criterion's verdict -- every caller of `atomic_write_checked` does receive a typed `Refusal` and can distinguish the two refusals -- only the argument that was offered for it."
  - id: c3
    text: "A test drives the ARM_A / ARM_B / ARM_C triple and is shown RED against today's discard_displaced, with ARM_C proving the probe can observe survival"
    state: met
    owner: core
    evidence: "test:crates/wcore-config/src/atomic_io.rs::a_save_made_inside_the_verdict_window_survives_the_rollback"
    note: "All three arms in one test, in the ticket's own order. The window is DRIVEN, not sampled: the check closure IS the exchange->verdict window, so a `std::fs::write(&p, THEIRS)` made from inside it lands on the inode the destination name currently resolves to -- exactly where a non-cooperating editor's in-place save lands. ARM_C runs FIRST and is the sensitivity control: the identical save on an ACCEPTING verdict survives and is published (`dest == THEIRSAVE`), which proves the probe can observe survival at all. ARM_B is the no-writer control (refusal, `intercepted_save() == None`, no strays). ARM_A asserts the destination is still `original`, that exactly one file in the directory holds THEIRSAVE (found by scanning, since the preserved name is whatever `keep_displaced` settled on), and that the refusal names it. RED under M1, which restored today's unconditional `discard_displaced(tmp, &displaced)`: exit 101 with `ARM_A: the save made inside the verdict window was destroyed; the directory holds [\\\"f.txt\\\"]` -- and the OTHER 11 atomic_io tests stayed green in the same run (11 passed, 1 failed), so this test is not riding on their coverage. Red arm exit codes captured DIRECTLY (`cmd > file 2>&1; $?`), never through a pipe; each mutation printed the enclosing function body before and after so it is proven to land on executable code, and each site was restored with `git checkout --` plus a `touch` so cargo could not serve the mutated binary. Log: /root/w-f13/s2-red2.log."
  - id: c4
    text: "The atomic_io.rs doc comment stops claiming the exchange closes the race, or the claim becomes true; the residual window is described in terms of what it actually loses, not only a crash"
    state: met
    owner: core
    evidence: "file:crates/wcore-config/src/atomic_io.rs:86:# What the exchange does NOT close (#1239)"
    note: "The doc no longer claims the exchange CLOSES the race. The old paragraph named the residual window's cost as 'a crash'; it is replaced by a section that states what the exchange does close (the check-then-write race #1155 measured -- the bytes judged ARE the bytes displaced) and enumerates BOTH things that can happen inside the residual window: a crash, and a save by a non-cooperating editor that the refusal path displaces. It says in terms the window was NARROWED -- read->rename down to exchange->verdict -- 'not closed', and that what remains is accounted for rather than discarded. Anchor verified unique with a control: `grep -cF '# What the exchange does NOT close' crates/wcore-config/src/atomic_io.rs` == 1."
  - id: c5
    text: "The existing atomic_io tests, including wayland#1202's a_rollback_that_exchanged_nothing_is_not_a_clean_refusal where merged, stay green"
    state: met
    owner: core
    evidence: "test:crates/wcore-config/src/atomic_io.rs::a_restore_that_exchanged_nothing_is_a_failure_not_a_rollback"
    note: "All 12 atomic_io tests green, #1202's included (it is now named `a_restore_that_exchanged_nothing_is_a_failure_not_a_rollback` on this tree; the ticket's spelling `a_rollback_that_exchanged_nothing_is_not_a_clean_refusal` is the name it was filed under and does not exist here). Two existing tests needed an assertion update, not a behaviour change: `a_refused_publish_puts_the_destination_back` and the #1202 test both compared the refusal to a bare `String`, which is now a `Refusal`; both now assert `why()` equals the same text AND `intercepted_save() == None`, which is strictly more than they asserted before. Green is not proof a test is live: under M1 eleven of the twelve stayed green while only the new one reddened, and under M2 (a wcore-tools mutation) all twelve stayed green, so they discriminate."
---

Decomposed out of the lane/f13-n-dataloss verifier objection rather than fixed in place.

The objection asserted that wayland#1155 c5's class enumeration was false. Read as written,
that note enumerates the cfg arms of `fn restore` -- there are exactly two, and it addresses
both -- and c5's criterion text is about a rollback that exchanged NOTHING, which is not what
the objection's probe exercises. The enumeration stands. What does not stand is the module's
own doc claim that the exchange CLOSES the race: the probe above is a real, unticketed second
loss on the refusal path, so it gets its own criteria here instead of being folded into a
criterion whose sentence it does not falsify.

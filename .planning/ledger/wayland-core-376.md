---
issue: 376
repo: FerroxLabs/wayland-core
kind: defect
title: "SecretDenyFs rebuilds the VCS content-store list from the filesystem on every ordinary path operation"
status: closed
last_verified_commit: 3b860623
criteria:
  - id: c1
    text: "The per-operation cost is MEASURED before anything changes: a benchmark or a counted-syscall figure for Read/exists/list/metadata on an ordinary path, recorded on this issue"
    state: met
    evidence: file:crates/wcore-tools/examples/secret_deny_cost.rs
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D2, found while verifying FerroxLabs/wayland-core#244 — VFS Read of raw .git/objects permitted (compressed; info-only)). Nothing has been done. The measured finding, verbatim: Every SecretDenyFs operation on an ordinary (non-secret) path rebuilds the VCS store list from the filesystem, uncached, and canonicalizes the path twice. `guard` calls `is_project_secret` (which runs `canon_for_scope` — a `canonicalize` syscall) and then, on the common false path, `is_vcs_content_store`, which runs `canon_for_scope` AGAIN and, when arm 1 (lexical) misses — i.e. for every ordinary file — falls through to `vcs_content_stores(&self.root)`: 6x `push_store` (each an `exists()` plus a `canonicalize` when present), a `metadata()` on `<root>/.git` via `gitfile_content_stores`, and a `read_to_string` of `.git/objects/info/alternates` via `alternate_object_dirs`. Roughly 10 filesystem syscalls per Read/exists/list/metadata/write, with no memoisation. CLOSED 2026-08-30 by lane sec-secrets on branch lane/f13-sec-secrets. MEASURED BEFORE ANY CHANGE, on the unmodified origin/integ/f13 worktree, as a counted-syscall figure -- differential `strace -f -c` over the release example at two operation counts (1100 minus 100, divided by 1000, so every one-off and harness cost cancels). Per ordinary-path operation: `is_project_secret` 6 syscalls (6 readlink, one canonicalization); `is_vcs_content_store` 18 (10 readlink + 7 statx + 1 openat) -- the guard total 24, of which 12 are arm 2's store rebuild (6 exists for the root-relative leaves, 1 metadata for the gitfile probe, 1 openat for the alternates read, 4 readlink canonicalizing the one leaf that exists) and 6 are a SECOND canonicalization of the same path. A whole `SandboxedFs`+`SecretDenyFs` `exists()` was 39 syscalls, so the guard was 61% of it; `read` was 47, `metadata` 39, `list` 40. The ticket's own estimate of 'roughly 10' UNDERSTATED it. Wall clock was NOT used as the instrument: the host runs eight lanes at load ~125 and two runs of an identical binary on an unchanged path differed 2.5x. AFTER: guard 9 syscalls (one shared 6-readlink resolution + 3 statx revalidation), whole `exists()` 24."
  - id: c2
    text: "If the measurement shows the cost is material, arm 2 of is_vcs_content_store no longer rebuilds the store list on the common path -- either an early-out or the directory-mtime stamp secret_deny_paths_stamped already uses"
    state: met
    evidence: symbol:crates/wcore-tools/src/workspace_policy.rs::vcs_store_cache_hit
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D2). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below. CLOSED 2026-08-30 by lane sec-secrets on branch lane/f13-sec-secrets. Both halves. Arm 2 is memoised behind a witness stamp -- the directory-mtime discipline `secret_deny_paths_stamped` already uses, sharing its #1145 granularity rule through the extracted `stamp_is_settled` so the two memos in the file cannot answer freshness differently. Revalidating an ordinary git checkout costs THREE stats (`<root>`, `<root>/.git`, `<root>/.git/objects/info/alternates`) against the twelve syscalls it replaces, because the scan reports its own witnesses and a control directory that does not exist is covered by the root's mtime. Separately, the guard's two predicates now share ONE path resolution instead of canonicalizing independently. An early-out was tried first and REJECTED as fail-open: 'skip arm 2 for a path under root' loses a store reached through a symlink that redirects back inside the root (`<root>/.git -> <root>/real-git`), which arm 1's lexical shape test cannot see -- the case `a_store_reached_under_an_unguessable_name_is_still_pruned` now pins. REFUTED 2026-08-30 by the adversarial verifier and RE-CLOSED in this lane. The first cut of this memo FAILED OPEN and was a regression against base: `scan_vcs_content_stores` stamped each control directory with `symlink_metadata`, and `push_store` stamped the symlink-resolved store location only when the leaf ALREADY existed -- so with `<root>/.git` a SYMLINK to `<root>/real-git`, a store leaf created after the first guard moved nothing that was stamped: not `<root>` (the target directory already existed), not `<root>/.git` (a link's mtime does not follow its target), not `objects/info/alternates` (still absent). `vcs_store_cache_hit` then returned the stale EMPTY list for the life of the process, and arm 1 could not cover it either -- the canonical path is `<root>/real-git/objects`, whose parent component is not `.git`. MEASURED A/B on ONE probe file carrying a known-positive control (a freshly built policy must deny the store): at 1798076fe (base) EXIT=0, `ABPROBE denied=true read_ok=false`; at 267120b1f (this memo) EXIT=100, `ABPROBE denied=false read_ok=true`, with `SecretDenyFs::read` returning the COMMITTED-SECRET bytes. FIX: a witness that IS a symlink now stamps its TARGET as well as itself (`StoreScan::witness_link_target`) -- the target's mtime is the one that moves when the store appears, so the memo can observe the mutation it asserts has not happened. A link whose target does not exist yet is stamped by the path it NAMES, absent, which flips to present when the target appears (a miss, which rescans). Applied inside `witness`/`witness_if_present`, so all four witness sites inherit it (control directory, gitfile gitdir, commondir, alternates owner -- the gitdir hole the verifier reasoned about but did not measure is closed by the same line), and `push_store` additionally stamps a store leaf that is ITSELF a dangling link, which it used to drop with no witness at all. The cost work is intact and re-measured: an ordinary checkout has no symlinked witness, so the scan still costs 17 probes and a warm ordinary guard still costs exactly 3, both still pinned by `one_ordinary_path_guard_resolves_once_and_does_not_rescan`. A/B re-run after the fix: EXIT=0, `ABPROBE denied=true read_ok=false`. RED ARM: neutering `witness_link_target` (an early `return;` inserted before its first executable statement `self.probes += 1;`, the body printed before and after) reddens FIVE arms -- the two new invalidation tests, both new Grep arms (`GREP LEAKED THE STORE: real-git/lfs/objects/aa/obj:1:LFS-CANARY password=hunter2`) and the A/B probe -- while `one_ordinary_path_guard_resolves_once_and_does_not_rescan`, `the_warm_memo_still_answers_both_directions`, `an_alternates_borrow_written_after_the_scan_is_denied`, `a_store_created_after_the_scan_is_denied_on_the_next_guard` and `a_store_reached_under_an_unguessable_name_is_still_pruned` ALL stay green -- which is precisely why the original suite could not catch this. A second arm dropping only the dangling-leaf witness reddens `a_store_leaf_symlinked_to_a_later_created_directory_is_denied` alone."
  - id: c3
    text: "A test pins the number of canonicalize/exists calls for one ordinary-path guard, so the regression cannot return silently"
    state: met
    evidence: test:crates/wcore-tools/tests/vfs_guard_cost.rs::one_ordinary_path_guard_resolves_once_and_does_not_rescan
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D2). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below. CLOSED 2026-08-30 by lane sec-secrets on branch lane/f13-sec-secrets. One ordinary-path guard is pinned at exactly ONE path resolution, ZERO store rebuilds and exactly THREE filesystem probes, with the first scan's probe count pinned at 17. Counted, not timed -- the counters are incremented by our own probe calls, so the numbers are identical on every platform and a loaded host cannot turn a skipped scan into a fast one. RED ARMS, each landing on executable code and each proven by printing the body before and after: deleting the `vcs_store_cache_hit()` early return reddens the cost test alone; making `denies_read_content` resolve once per predicate reddens `the_two_guard_predicates_share_one_resolution` too; short-circuiting the revalidation loop so the memo can never miss reddens `an_alternates_borrow_written_after_the_scan_is_denied`; dropping the alternates witness reddens that plus the probe count. The invalidation arms are load-bearing: a memo that never rebuilds satisfies every cost assertion and denies nothing. That was UNDER-COVERED when this was first graded: exactly ONE arm graded invalidation across the whole crate, and the fail-open memo the verifier refuted passed everything else. Coverage is now three arms, each red-armed independently and each on a different witness -- `an_alternates_borrow_written_after_the_scan_is_denied` (a content-read file), `a_store_under_a_symlinked_control_dir_created_after_the_scan_is_denied` (a symlinked control directory) and `a_store_leaf_symlinked_to_a_later_created_directory_is_denied` (a dangling store leaf)."
  - id: c4
    text: "If the measurement shows the cost is NOT material, this issue is closed with the figure recorded rather than left open on a code reading"
    state: met
    evidence: file:crates/wcore-tools/examples/secret_deny_cost.rs
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D2). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below. CLOSED 2026-08-30 by lane sec-secrets on branch lane/f13-sec-secrets. ANTECEDENT MEASURED FALSE, so the other branch was taken. The cost IS material -- 24 of the 39 syscalls of an `exists()`, on a guard installed unconditionally for every sub-agent and every channel/remote session -- so c2 applies and this issue is NOT the close-with-a-figure case. The figure is recorded on c1 either way, which is what this criterion exists to guarantee. Recorded here rather than left implicit because a conditional criterion whose condition is false is exactly the shape that gets silently skipped."
---

Every SecretDenyFs operation on an ordinary (non-secret) path rebuilds the VCS store list from the filesystem, uncached, and canonicalizes the path twice. `guard` calls `is_project_secret` (which runs `canon_for_scope` — a `canonicalize` syscall) and then, on the common false path, `is_vcs_content_store`, which runs `canon_for_scope` AGAIN and, when arm 1 (lexical) misses — i.e. for every ordinary file — falls through to `vcs_content_stores(&self.root)`: 6x `push_store` (each an `exists()` plus a `canonicalize` when present), a `metadata()` on `<root>/.git` via `gitfile_content_stores`, and a `read_to_string` of `.git/objects/info/alternates` via `alternate_object_dirs`. Roughly 10 filesystem syscalls per Read/exists/list/metadata/write, with no memoisation.

**Where.** crates/wcore-tools/src/vfs.rs:2043 (guard); crates/wcore-tools/src/workspace_policy.rs:935-943 (is_vcs_content_store, arm 2 unconditional on the common path), :2754 (vcs_content_stores), :2871 (gitfile_content_stores), :2903 (alternate_object_dirs), :2935 (canon_for_scope).

**Why it matters.** Stated as a code fact; I did NOT measure the latency, so the impact claim is unproven and should be benchmarked before anyone acts on it. Flagging it because the path is hot — SecretDenyFs is installed unconditionally for every sub-agent (spawner.rs:3023) and for every channel/remote session (channel_tools.rs:185), and sub-agents are read-heavy — and because #244's own issue comment rejected the obvious fix precisely on per-operation syscall cost, so this is the axis the reviewers already said they cared about. A one-line early-out (skip arm 2 when the canonical path is not under root and root has no gitfile/alternates) or caching the store list behind the same directory-mtime stamp `secret_deny_paths_stamped` already uses would remove it.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.

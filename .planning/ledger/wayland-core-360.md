---
issue: 360
repo: FerroxLabs/wayland-core
kind: defect
title: "The WhatsApp bridge ships a message cap borrowed from Meta's docs, and the coverage guard structurally cannot see it"
status: open
last_verified_commit: 313c04e7f
criteria:
  - id: c1
    text: "The bridge's cap is measured against a real baileys or whatsapp-web.js backend, or the borrowed Some(4096) is replaced by something honest (None, or a measured number) -- a borrowed number is not an answer"
    state: not-met
    evidence: "symbol:crates/wcore-channel-whatsapp/src/bridge/mod.rs::BRIDGE_UNMEASURED_CHUNK_WIDTH"
    owner: core
    note: "Closed by the SECOND branch. The first is still not reachable here -- it needs Node, an operator's own bridge.js with the backend package installed, and a WhatsApp number QR-paired to it, and none of that is a credential anybody can issue. What changed: max_message_len() no longer returns a bare Some(4096) justified by Meta's Cloud API text.body page, which governs a surface this code never touches. It returns WhatsappBridgeConfig::max_message_chars when the operator set one and BRIDGE_UNMEASURED_CHUNK_WIDTH otherwise, and that constant's documentation says in as many words that it is a CHUNKING POLICY rather than a platform limit, with the asymmetry that chose it: too high loses messages (HIGH-6), too low only splits a reply that need not have been split, and None is unavailable because it disables chunking and sends an unbounded body at a limit nobody has published. The override is the honest half -- an operator who HAS driven their own bridge previously had nowhere to put the finding, so our unsourceable guess stayed load-bearing for everybody. CORRECTION (2026-08-29): the first version of this note, and the field comment it described, said zero was rejected at parse time BY THE SCHEMA. That enforcement did not exist and the claim is withdrawn -- Channel::config_schema returns a &str a host displays, the only code touching it are three per-crate tests asserting the schema is valid JSON, and no JSON-Schema validator is in the dependency graph, so schemas/whatsapp-bridge.json's minimum-of-1 documented a bound nothing imposed. The knob therefore shipped with a value at which it did the opposite of its purpose; that is c6, and the enforcement now lives in reject_zero_chunk_width in bridge/mod.rs. Two tests, both green: the_chunk_width_is_the_operators_when_they_set_one_and_the_policy_default_otherwise drives both directions, and the_unmeasured_default_stays_finite_and_conservative reads through the ADAPTER rather than the constant and refuses a raise without a recorded boundary run. docs/delivery-semantics.md section 4.2 carries the row and says the number is a policy; crates/wcore-channel-whatsapp/schemas/whatsapp-bridge.json carries the knob. REFUTED 2026-08-29 by the 0.13.12 close-sweep, recorded verbatim: Evidence resolves: symbol BRIDGE_UNMEASURED_CHUNK_WIDTH exists at crates/wcore-channel-whatsapp/src/bridge/mod.rs:120, and both named tests run and pass (the_chunk_width_is_the_operators_when_they_set_one_and_the_policy_default_otherwise, the_unmeasured_default_stays_finite_and_conservative -- both ok in my run). But the criterion does not hold AS THE TICKET WROTE IT, and the ledger text is where it drifted. The ticket says: 'the Some(4096) is replaced by something honest (None, or a measured number) -- a borrowed number is not an answer.' The ledger criterion text drops the parenthetical and the 'a borrowed number is not an answer' clause, and then grades itself met. What actually shipped: `pub const BRIDGE_UNMEASURED_CHUNK_WIDTH: usize = 4096;` -- numerically the identical borrowed figure -- returned by max_message_len() whenever the operator sets nothing, which is every existing deployment. No measurement was taken (the cells say NotMeasured, the declaration says cap_measured = no). So the runtime behaviour of the shipped product is byte-identical to the defect the ticket reports: every bridged channel still chunks at 4,096, a boundary taken from Meta's Cloud API page for a surface this code never touches. What genuinely changed is documentation plus a new operator override. I did verify the fix's stated reason for rejecting the ticket's `None` option and it is factually correct -- ChannelManager::chunks_for (crates/wcore-channels/src/manager.rs:904-909) matches `Some(max) if max > 0` and falls everything else through to `vec![text.to_string()]`, so None really does disable chunking and send an unbounded body. That makes the deviation arguable, but it is a maintainer's call to accept, not a criterion that is met. Note also that the_unmeasured_default_stays_finite_and_conservative pins the ceiling at `shipped <= 4096` -- the borrowed number reproduced as the upper bound -- and it exercises only the unset-default path, never the config path this fix introduced (that config path is now covered: a_zero_cap_config_does_not_load_at_all and no_constructible_adapter_accepts_a_zero_cap under c6 drive the operator-set path through the real loader). RE-GRADE 2026-08-30, same verdict, plus the two repairs the sweep asked for. (1) THE CRITERION TEXT IS RESTORED to the ticket`s own sentence: the parenthetical `(None, or a measured number)` and the clause `a borrowed number is not an answer` had been dropped when it was transcribed into this ledger, and the shortened sentence is what had been graded met. Grading resumes against the ticket`s words. (2) NEEDS-PLATFORM-RUN, stated as a task rather than a wish, because `not-met` with no route is how a criterion rots: the run must produce (a) a Node bridge.js with `baileys` installed, and separately one with `whatsapp-web.js`, QR-paired to a WhatsApp number nobody minds burning; (b) a two-point boundary probe per backend through the existing harness -- send at the candidate width and at width+1 -- recording ACCEPTED / REJECTED / TRUNCATED for each; (c) the whatsapp+baileys and whatsapp+whatsapp-web cells in crates/wcore-channels-registry/tests/live_message_cap_boundary/cells.rs changed from NotMeasured to the measured verdict, cap_measured flipped to yes with cap_source naming the run in docs/delivery-semantics.md section 4.2, and BRIDGE_UNMEASURED_CHUNK_WIDTH replaced by the measured figure. The blocker is infrastructure, not a credential: no vendor issues a token for an unofficial reverse-engineered client, so it needs a paired handset. Everything except the pairing is already built. The `None` branch of the ticket`s disjunction was checked and is genuinely unavailable rather than merely inconvenient -- chunks_for matches `Some(max) if max > 0`, so None sends an unbounded body -- which leaves the measurement as the only open route."
  - id: c2
    text: "The coverage guard reaches backends selected by a CONFIG KEY, not only by platform string, so a ninth adapter in this shape cannot appear unprobed"
    state: met
    evidence: "test:crates/wcore-channels-registry/tests/live_message_cap_boundary.rs::every_capped_adapter_has_a_probe_cell"
    owner: core
    note: "The class is closed at the REGISTRY, not at the guard. wcore-channels-registry gained src/selector.rs: ChannelSelector { platform, options, key } plus constructible_selectors(), which enumerates what the registry can BUILD -- nine implementations, not seven platforms -- with the WhatsApp arm derived from WhatsappBackend::ALL_WIRE_NAMES so a fourth backend appears in every downstream gate with no second list to remember. Keys are the bare platform tag for a platform's default implementation and platform+<value> where a config key selects another, so every row written before this change keeps its name. Both gates walk it: shipped_caps() in live_message_cap_boundary.rs and measured_capabilities() in delivery_semantics_declaration.rs. CELLS is keyed by selector and carries whatsapp+baileys and whatsapp+whatsapp-web cells. every_capped_adapter_has_a_probe_cell additionally asserts that at least one config-keyed implementation reached it, so the widening cannot silently stop widening; the registry side is held by the_selector_list_is_wider_than_the_platform_list_by_the_config_keyed_backends, which reddens when a WhatsappBackend variant is added. RED ARM, run on this branch with both bridge cells deleted from tests/live_message_cap_boundary/cells.rs, restored and touched afterwards, verbatim: >>> thread 'every_capped_adapter_has_a_probe_cell' (857160) panicked at crates/wcore-channels-registry/tests/live_message_cap_boundary.rs:626:9: whatsapp+baileys declares a finite max_message_len() but has no cell in this file. Add one -- as NotMeasured naming the blocker if nobody can drive it -- so the gap is stated rather than absent. <<< The same test was green before the mutation and green after the restore. Line numbers in the quoted panics are the ones the run printed; the probe file was later split into live_message_cap_boundary/{cells,driver}.rs and the tests moved up, so grep the test name rather than the line."
  - id: c3
    text: "If the cap genuinely cannot be measured, that is recorded as a stated NOT-MEASURABLE with the reason, the way Matrix and MS Teams are"
    state: met
    evidence: "file:docs/delivery-semantics.md:418"
    owner: core
    note: "The prose at docs/delivery-semantics.md:417-426 states the number is UNVERIFIED and cannot be sourced, and says why. This is a disclosure, NOT a substitute for c1 or c2 - it is the honest interim only."
  - id: c4
    text: "A section 4.2 declaration row exists for the bridge, or the reason it cannot have one is enforced by a test rather than by a comment"
    state: met
    evidence: "test:crates/wcore-channels-registry/tests/delivery_semantics_declaration.rs::comparator_rejects_the_bridge_rows_going_missing"
    owner: core
    note: "The row exists, so the second branch is moot. docs/delivery-semantics.md gained whatsapp+baileys and whatsapp+whatsapp-web rows in the machine-readable block (guarantee, cap, cap_measured, cap_source), a section 4.2 cap-table row, and a blocked-probe row saying the blocker is a QR-paired account rather than a credential anybody can issue; the section 2 prose row already existed and is now COVERED rather than merely labelled. cap_source must be a URL and no vendor page governs this surface, so it names the decision -- wayland-core#360 -- and the block's prose says why that is the accountable answer: an unsourceable number must say so at a URL a reader can open, not carry a citation from the wrong vendor. The evidence test is not a duplicate of comparator_rejects_a_missing_row: that one proves the rule fires for a platform-keyed row, whereas the bridge is the row that could not EXIST until the harness stopped enumerating platform strings. It removes both bridge rows from the parsed declaration, requires exactly two NO-row-in reports naming the selector keys, and requires the Cloud API row NOT to be implicated -- the three WhatsApp implementations share one platform string, so a platform-keyed comparator would have reported the wrong one or none at all. the_two_whatsapp_row_labels_cannot_be_confused_by_a_prefix_match holds the neighbouring trap: every table lookup in that file is a starts_with, and the two labels are one closing asterisk pair apart."
  - id: c5
    text: "A red arm is quoted verbatim"
    state: met
    evidence: "test:crates/wcore-channels-registry/tests/live_message_cap_boundary.rs::every_capped_adapter_has_a_probe_cell"
    owner: core
    note: "Three, each run on this branch with the tree committed BEFORE the mutation and the file restored and touched afterwards, quoted verbatim from the run output. ARM 1 (c2), both bridge cells deleted from tests/live_message_cap_boundary/cells.rs: >>> thread 'every_capped_adapter_has_a_probe_cell' (857160) panicked at crates/wcore-channels-registry/tests/live_message_cap_boundary.rs:626:9: whatsapp+baileys declares a finite max_message_len() but has no cell in this file. Add one -- as NotMeasured naming the blocker if nobody can drive it -- so the gap is stated rather than absent. <<< ARM 2 (c4), the four whatsapp+baileys lines deleted from the machine-readable block: >>> thread 'declaration_matches_every_adapter' (868161) panicked at crates/wcore-channels-registry/tests/delivery_semantics_declaration.rs:506:5: assertion left == right failed: docs/delivery-semantics.md must carry a row for all twelve implementations (including the macOS-only iMessage and both bridged WhatsApp backends), found 11 / left: 11 / right: 12 <<< ARM 3 (wayland#934 c7), the matrix adapter mutated back to the 32,768 it shipped until 2026-08-28 at crates/wcore-channel-matrix/src/lib.rs:240: >>> thread 'a_derived_cap_is_exactly_what_its_budget_admits' (945519) panicked at crates/wcore-channels-registry/tests/live_message_cap_boundary.rs:375:9: matrix: the shipped cap disagrees with the budget it is derived from: a body of 32768 scalars costs up to 131072 bytes at 4 bytes per scalar, over the 65536 byte budget. The platform rejects the send and send_to_keyed does not re-send it -- this is HIGH-6, and it is exactly the shape matrix.cap shipped in when it was 32,768 <<< Each test was green before its mutation and green again after the restore, so the red is caused by the mutation and not by pre-existing drift. ARM 4 (c6), the tree committed first, then the guard attribute at crates/wcore-channel-whatsapp/src/bridge/mod.rs:303 reverted to #[serde(default)] and the file touched -- three tests went red at once, verbatim: >>> thread 'bridge::tests::a_zero_chunk_width_is_refused_while_the_operators_file_is_parsed' (1403536) panicked at crates/wcore-channel-whatsapp/src/bridge/mod.rs:856:14: max_message_chars = 0 must not parse: it disables chunking entirely: WhatsappBridgeConfig { backend: Baileys, bridge_path: \"/nope/bridge.js\", node_path: None, session_dir: None, workspace_name: \"\", default_recipient: \"\", handshake_timeout_secs: 20, rpc_timeout_secs: 60, max_message_chars: Some(0) } <<< and >>> thread 'a_zero_cap_config_does_not_load_at_all' (1403535) panicked at crates/wcore-channels-registry/tests/operator_cap_zero.rs:139:19: a channel config setting max_message_chars = 0 LOADED, declaring cap Some(4096). Zero misses ChannelManager::chunks_for's `Some(max) if max > 0` arm, so the whole body goes to the bridge as one message at a limit no vendor publishes. <<< and >>> thread 'no_constructible_adapter_accepts_a_zero_cap' (1403537) panicked at crates/wcore-channels-registry/tests/operator_cap_zero.rs:291:5: these adapters LOADED with max_message_chars = 0 instead of refusing it: [\"whatsapp+baileys\", \"whatsapp+whatsapp-web\"]. An adapter that survives a zero has to prove it cannot declare one; if that is what it now does, say so here rather than deleting this assertion. <<< ARM 5 (c6, the in-process belt), the file restored and touched, then the single line .filter(|width| *width > 0) deleted from max_message_len and the file touched again: >>> thread 'bridge::tests::an_in_process_zero_cap_falls_back_to_the_policy_width_rather_than_no_cap' (1409595) panicked at crates/wcore-channel-whatsapp/src/bridge/mod.rs:895:9: assertion `left == right` failed: a zero built in process must not reach the chunker as a declared cap / left: Some(0) / right: Some(4096) <<< Both mutated lines were confirmed to be CODE (an attribute and an iterator adaptor), not comment or doc text, before either arm was believed; all four tests were green before each mutation and green again after the restore-and-touch."
  - id: c6
    text: "An operator-supplied max_message_chars cannot make the adapter declare a cap of ZERO, which chunks_for reads as NO cap"
    state: met
    evidence: "test:crates/wcore-channels-registry/tests/operator_cap_zero.rs::a_zero_cap_config_does_not_load_at_all"
    owner: core
    note: "Found by adversarial review of the c1 branch, then REPRODUCED before being believed. ChannelManager::chunks_for (crates/wcore-channels/src/manager.rs:904) matches Some(max) if max > 0 and falls through to vec![text.to_string()] otherwise, so Some(0) is not a one-character cap -- it is the no-cap sentinel. Reproduction on integ/f13 at e1f151a5, through the real loader (parse_channel_config -> channel_factory_for -> make_whatsapp -> parse_options) with max_message_chars = 0 in the [options] table: the file parsed clean, max_message_len() returned Some(0), and a 20,000-char body came back from chunks_for as ONE 20,000-char chunk. That is the reject-and-drop direction (HIGH-6) reached through the knob c1 added to avoid it, and the pre-existing guard (the_unmeasured_default_stays_finite_and_conservative) structurally could not see it: it reads the DEFAULT path, where the override is None. FIX: max_message_chars carries #[serde(default, deserialize_with = \"reject_zero_chunk_width\")], which refuses zero while the operator's file is being parsed and names both the key and the legal minimum. Refusal rather than reinterpretation, because the two alternatives each cost something a refusal does not -- reading zero as 'no cap' loses the message, and reading it as 'use the default' discards an explicit operator instruction silently, leaving them trusting a width that is not in force. Deleting the key remains how you ask for the default. max_message_len additionally filters a zero back to BRIDGE_UNMEASURED_CHUNK_WIDTH: WhatsappBridgeConfig is a pub struct with pub fields, so a caller building one in process never meets the deserializer, and at that point there is no operator to hand an error to. CLASS: the bridge is the only adapter in the product whose max_message_len is read from config -- every other one under crates/wcore-channel-*/src is a literal (discord 2000, slack 4000, telegram 4096, whatsapp cloud 4096, sms 1600, matrix 16384, msteams 20480), verified by grep of every 'fn max_message_len' impl. So there is no sibling instance to fix today, and tests/operator_cap_zero.rs::no_constructible_adapter_accepts_a_zero_cap is the tripwire for the next one: it walks constructible_selectors(), injects max_message_chars = 0 into each, and requires every selector to refuse or to declare a non-zero cap. It proves its own non-vacuity by running the identical loop at 1 and requiring that set to be non-empty -- without that, a probe table that stopped satisfying any factory would go quiet instead of red. Also corrected: the field comment at bridge/mod.rs (it now names the deserializer and says in as many words that the schema does NOT enforce this, and why), and the schema's own description for the key."
---

Found while measuring the WhatsApp cap for `wayland#934`. The Cloud API cell is a
separate matter; the BRIDGE backend cannot be measured the same way and the
number it ships is borrowed from the wrong vendor.

If the real backend limit is lower, sends fail or truncate. If it is higher, the
chunker splits messages nobody asked it to split. Neither has ever been observed.

c1 was graded `met` and is not. The shipped default is the same 4,096 the
ticket objects to; what changed is that it stops claiming to be WhatsApp's
limit, and that an operator with a real measurement now has somewhere to put
it. That is a better product and it is not the criterion. The measurement
needs a QR-paired WhatsApp account driving a Node bridge, which no credential
can substitute for; the note on c1 says exactly what that run must produce.

The more important half of this ticket is the second criterion. This is the
eighth `max_message_len` in the product and the only one no test and no
declaration row touches, for two structural reasons already recorded in-tree: the
declaration harness enumerates platforms the registry builds FROM A PLATFORM
STRING, and the bridge is reached through `whatsapp` plus a `backend` key. The
guard that exists to make an unprobed cap impossible has a blind spot shaped
exactly like this adapter. Measuring one number without widening the guard just
moves the blind spot.

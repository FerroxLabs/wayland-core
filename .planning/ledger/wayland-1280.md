---
issue: 1280
repo: FerroxLabs/wayland
kind: defect
title: "The skills listing is injected on every ordinary turn, and its 1%-of-window budget is not a ceiling (#1150 c5, skills half)"
status: closed
last_verified_commit: ffaa0d839
criteria:
  - id: c1
    text: "The skills listing respects a ceiling derived from the resolved context window, with no term that grows without bound in the skill count: bundled entries are inside the budget rather than subtracted from it, and the names-only fallback is itself bounded. Graded at 100 bundled / 1,000 project skills"
    state: met
    evidence: symbol:crates/wcore-skills/src/prompt.rs::clamp_to_budget
    owner: core
    note: "MET ffaa0d839 (lane/f13-s3-skills-ceiling). THE FIX, in crates/wcore-skills/src/prompt.rs::format_skills_within_budget: the three terms that grew without bound in the skill count are gone. (1) The bundled block is CHARGED AGAINST the budget instead of subtracted from it -- the guard is now `if !rest_indices.is_empty() && bundled_chars < budget`, so when the bundled entries alone do not fit, levels 2 and 3 are skipped outright rather than emitted on top of a budget that already reads zero. (2) The `rest_indices.is_empty()` early return that emitted an all-bundled set whole, however far over budget, is deleted. (3) Every degradation level is now CHECKED against the budget (`if listing_width(&entries) <= budget`) instead of returned unconditionally, with a new level 4 -- names only for every skill, bundled included -- and whatever survives passes through clamp_to_budget, which stops at the last entry that still leaves room for one overflow line. MEASURED on the arms the criterion names, all against the 1,310-char budget a 32,768-token window implies (the #1150 reporter's shape: unlisted local model, OpenAI-compatible endpoint, no [compact] context_window): 100 bundled + 1,000 project, 0+300, 0+1000, 40+10, 100+10, 40+300 all render <= 1,310 columns, against the 22,399 (17.1x) and 19,999 (15.3x) the issue recorded. GRADED ON THE PRODUCTION PATH by crates/wcore-agent/tests/issue_1280_skills_ceiling_test.rs, 7 tests, all green on hetzner: the 1,000-project arm drives the REAL AgentBootstrap::build() with an injected recording provider and measures the <system-reminder> block off the LlmRequest the engine hands a provider; the 100-bundled arm goes through wcore_agent::context::format_skills_section, the product renderer that build_system_prompt (context.rs:460 -> bootstrap.rs:2377) and the late-MCP bind path (late_mcp.rs:132) both call -- bundled skills are compiled into the binary and cannot be planted on disk, which is why that arm is one level down and not a re-composition. PRODUCTION CALL SITES of the guard, excluding its definition, doc comments and tests: format_skills_within_budget has exactly one non-test caller, context.rs:258 inside format_skills_section, which itself has two production call sites (context.rs:460, late_mcp.rs:132). NON-VACUITY: each arm asserts, in the same test body, that the pre-ceiling rendering of that arm was over budget by more than 10x, so a pass cannot mean 'these skills happened to be small'. CONTROL against an always-firing ceiling: a_small_skill_set_is_listed_in_full_and_not_trimmed asserts a 6-skill session is listed in full with no overflow line, and the_ceiling_is_derived_from_the_window_not_fixed asserts a 1,000,000-token window buys a listing 4x larger than a 32,768-token one -- a hard-coded cap would pass the budget assertions and fail that. RED ARMS (tree committed first; file touched after mutating AND after restoring, so cargo could not reuse the mutated binary; cargo check -p wcore-agent -p wcore-skills --tests RC=0 before any mutation; git status clean after): arm 1 neutered clamp_to_budget -> 5 of 7 red; arm 2 restored `budget.saturating_sub(bundled_chars)` plus level 3's unconditional return -> 5 of 7 red, which is the specific subtraction this criterion names; arm 4 dropped the overflow line -> 3 of 7 red. Restored and re-run: 7 passed. HOST HAZARD CLOSED: the first run of the bootstrap arm bounded a listing naming none of its own fixtures, because hetzner has 85 real skills in <config>/wayland-core/skills/ that sort ahead of anything planted in a tempdir. The ceiling held, but that green would have been about someone else's skills, so both this file and issue_1150_ordinary_turn_payload_test.rs now point HOME/XDG_CONFIG_HOME/WAYLAND_HOME at an empty dir under #[serial]. FIVE PRE-EXISTING TESTS ASSERTED THE DEFECT and were rewritten, not deleted: tc_e2e_7, tc_e2e_9, wb_8c_all_bundled_no_non_bundled, test_format_skills_within_budget_minimal_mode and test_format_skills_within_budget_only_bundled_skills each asserted that bundled entries survive whatever the budget is -- 'even if over budget, all are shown full' -- which is the exemption c1 removes. Each now asserts the ceiling plus the overflow line, and each keeps a roomy-budget arm so the clamp is not an unconditional trim. wcore-skills: 634 passed, 0 failed."
  - id: c2
    text: "WRONG-REFUSAL CONTROL for c1: a skill trimmed out of the listing is still reachable -- the model can discover and invoke it, measured on a session where it actually needs one, or the trimming is refused"
    state: met
    evidence: symbol:crates/wcore-skills/src/prompt.rs::search_skills
    owner: core
    note: "MET ffaa0d839 (lane/f13-s3-skills-ceiling). This is the control that decides whether c1's ceiling is a win or a regression, and it is measured on a session that actually needs a trimmed skill, through real engine dispatches -- a_trimmed_skill_is_found_and_run_on_a_turn_that_needs_it in crates/wcore-agent/tests/issue_1280_skills_ceiling_test.rs. THE MECHANISM: the Skill tool takes an optional `query` (crates/wcore-agent/src/skill_tool.rs execute_inner; schema `required: []`), which ranks EVERY installed skill by name and description through wcore_skills::prompt::search_skills and returns a shortlist bounded at SKILL_SEARCH_MAX_RESULTS = 10; the model then invokes one by exact name through the same catalog resolution as always, which never consulted the listing. The trimmed listing itself ends in a fixed overflow line (SKILL_OVERFLOW_HINT) stating how many skills were withheld and naming that query, so the route is discoverable from the prompt and not only from the schema. THE MEASUREMENT, on a 1,000-skill session over the reporter's shape: the needed skill (m-skill-777) is asserted ABSENT from the rendered listing first, so the test cannot pass for the wrong reason; the Skill tool is NOT on defer_cold's hot allowlist, so the model must ToolSearch for it and the next dispatch is asserted to carry it -- the same explicit-activation path the tool half of #1150 c5 grades; Skill{query: 'reticulate splines'} returns a result naming m-skill-777; Skill{skill: 'm-skill-777'} returns its body with is_error=false, read off the ContentBlock::ToolResult in the NEXT LlmRequest, not off a helper. THE ESCAPE HATCH IS ITSELF BOUNDED, or it would undo the ceiling on the message-stream side: the search result is capped at 10 hits with descriptions truncated to 120 columns, asserted by line count in the same test, and available_names() -- which used to join EVERY visible name into the not-found message, 1,000 names for one typo -- is capped at the same 10, graded by a_mistyped_skill_name_does_not_dump_the_whole_catalogue. RED ARM: replacing the `query` branch's condition with `None::<&str>` reds exactly this test (1 of 7) and nothing else; restored, 7 passed. PRODUCTION CALL SITES: search_skills has one non-test caller, SkillTool::execute_inner, which both Tool::execute and Tool::execute_with_ctx funnel through via dispatch(); the SkillTool itself is constructed at bootstrap.rs:2756 for every session with a skill catalog and at bootstrap.rs:4314 for the cron skill sink."
  - id: c3
    text: "Skills are injected only when relevant or explicitly activated (#1150 c5's text for the skills half), on a turn whose text relates to none of them, measured on the real bootstrap path"
    state: superseded
    successor: FerroxLabs/wayland#1283
    evidence: file:.planning/ledger/wayland-1280.md
    owner: core
    note: "SUPERSEDED ffaa0d839 into FerroxLabs/wayland#1283 (milestone 0.13.13), carried VERBATIM. The split is the issue's own Recommendation: c3/c4/c5 need a design decision before any code, and on the measured numbers they are now worth far less than they looked -- with c1's ceiling landed the listing is capped at 1% of the resolved window (1,310 chars on the reporter's session), so a perfect relevance gate saves at most that, where c1 removed a 15-17x overrun. #1280 therefore owes only c1/c2. The successor issue carries the three design constraints verified against source (build_system_prompt has exactly ONE call site, bootstrap.rs:2377; moving assembly onto the dispatch path moves it out of segment 0 and re-bills the whole prompt on an implicit-cache endpoint, making #1150's symptom worse; #559 c6 forecloses moving skills into the message stream) and the vacuity trap in full: issue_1150_implicit_prefix_cache_test.rs sets Config::system_prompt directly, so build_system_prompt is never called on its path and its segment-0 assertion is BLIND TO SKILLS -- reused as-is it grades the criterion green while measuring nothing about skills, proven by two arms that passed and a positive control that failed. This is the successor's c1."
  - id: c4
    text: "WRONG-REFUSAL CONTROL for c3: on a turn that DOES need a gated-out skill, the model can still find and run it -- withholding a skill the model then cannot use is worse than a larger prompt"
    state: superseded
    successor: FerroxLabs/wayland#1283
    evidence: file:.planning/ledger/wayland-1280.md
    owner: core
    note: "SUPERSEDED ffaa0d839 into FerroxLabs/wayland#1283 (milestone 0.13.13), carried VERBATIM. The split is the issue's own Recommendation: c3/c4/c5 need a design decision before any code, and on the measured numbers they are now worth far less than they looked -- with c1's ceiling landed the listing is capped at 1% of the resolved window (1,310 chars on the reporter's session), so a perfect relevance gate saves at most that, where c1 removed a 15-17x overrun. #1280 therefore owes only c1/c2. The successor issue carries the three design constraints verified against source (build_system_prompt has exactly ONE call site, bootstrap.rs:2377; moving assembly onto the dispatch path moves it out of segment 0 and re-bills the whole prompt on an implicit-cache endpoint, making #1150's symptom worse; #559 c6 forecloses moving skills into the message stream) and the vacuity trap in full: issue_1150_implicit_prefix_cache_test.rs sets Config::system_prompt directly, so build_system_prompt is never called on its path and its segment-0 assertion is BLIND TO SKILLS -- reused as-is it grades the criterion green while measuring nothing about skills, proven by two arms that passed and a positive control that failed. This is the successor's c2."
  - id: c5
    text: "Whatever c3 does, the prefix ahead of the conversation does not churn per turn on an implicit-cache endpoint, measured off the real LlmRequest across a multi-turn session"
    state: superseded
    successor: FerroxLabs/wayland#1283
    evidence: file:.planning/ledger/wayland-1280.md
    owner: core
    note: "SUPERSEDED ffaa0d839 into FerroxLabs/wayland#1283 (milestone 0.13.13), carried VERBATIM. The split is the issue's own Recommendation: c3/c4/c5 need a design decision before any code, and on the measured numbers they are now worth far less than they looked -- with c1's ceiling landed the listing is capped at 1% of the resolved window (1,310 chars on the reporter's session), so a perfect relevance gate saves at most that, where c1 removed a 15-17x overrun. #1280 therefore owes only c1/c2. The successor issue carries the three design constraints verified against source (build_system_prompt has exactly ONE call site, bootstrap.rs:2377; moving assembly onto the dispatch path moves it out of segment 0 and re-bills the whole prompt on an implicit-cache endpoint, making #1150's symptom worse; #559 c6 forecloses moving skills into the message stream) and the vacuity trap in full: issue_1150_implicit_prefix_cache_test.rs sets Config::system_prompt directly, so build_system_prompt is never called on its path and its segment-0 assertion is BLIND TO SKILLS -- reused as-is it grades the criterion green while measuring nothing about skills, proven by two arms that passed and a positive control that failed. This is the successor's c3."
---

# The skills listing is unbounded, and it ships on every turn

Split out of `FerroxLabs/wayland#1150` c5 by the lane that refused to half-build it. #1150's
tool half is delivered: `defer_cold` folds 40 of 48 tool schemas out of `tools[]` into a
566-byte catalog line, 52,110 schema bytes down to 8,902, with a wrong-refusal control that
drives a real activation and asserts the folded-out tool is callable on the next dispatch.
The skills half has no gate at all.

Two separate defects live in this ticket, and the issue's own Recommendation separates them:

**The budget is not a ceiling (c1/c2).** `format_skills_within_budget` computes
`remaining_budget = budget.saturating_sub(bundled_chars)` -- it SUBTRACTS the bundled entries
instead of capping them -- and the names-only fallback still emits every bundled skill at full
description plus every non-bundled name. Both terms grow linearly in the skill count. Against
the 1,310-char budget a 32,768-token window implies: 1,000 project skills render 19,999 chars
(15.3x); 100 bundled render 22,399 (17.1x, about 5,600 tokens of a 32,768-token window). That
is a bounded fix with a measured win and no design question in it, which is why it is on
0.13.12.

**No relevance gate (c3/c4/c5).** `context.rs:254 format_skills_section` emits the listing
unconditionally; nothing anywhere consults relevance or activation. Closing this needs a design
decision first, and on the measured numbers it is worth far less than c1 -- a gate saves at
most the size of a BOUNDED listing, while c1 is what stops that listing being 17x its stated
budget. These three should be decomposed onto their own issue under 0.13.13; until that split
is filed they stay here and stay honestly not-met, because a criterion nobody split is a
partial ticket.

Both halves carry a wrong-refusal control on purpose. A listing bounded by silently dropping
skills the model then cannot reach is a worse defect than the prompt bytes it saves.

---
issue: 1199
repo: FerroxLabs/wayland
kind: defect
title: "The skills prompt budget still sizes against the fabricated 200,000-token window for exactly the unlisted-model case #1150 c1 removed"
status: closed
last_verified_commit: 5a7e3bee4
criteria:
  - id: c1
    text: "get_char_budget(None) no longer grants a budget derived from 200k: the unknown-window case is sized against UNVERIFIED_CONTEXT_WINDOW or refuses to guess, the rule compact.rs:449-454 already states"
    state: met
    evidence: "symbol:crates/wcore-skills/src/prompt.rs::DEFAULT_CHAR_BUDGET"
    owner: core
    note: "REGRADED 2026-08-31. The ledger said `Nothing has been done` because its `last_verified_commit: 9de21aa1` PREDATES the fix: eddbc502d landed 2026-08-29 16:20Z, the same day the ticket was filed, and nothing re-graded it since. Confirmed independently three ways rather than on one agent's report: I read the constant myself, a grounding agent ran the red arm, and I re-ran the evidence tests at integ HEAD (9/9 pass, including the boot-with-None arm). MET AS WRITTEN by the FIRST branch of the criterion's disjunction. `DEFAULT_CHAR_BUDGET` is now `UNVERIFIED_CONTEXT_WINDOW * CHARS_PER_TOKEN / 100` = 1,310, not the literal 8,000, and the `// Fallback: 1% of 200k x 4` comment the issue quotes is GONE from the file. It is held there by a COMPILE-TIME guard, `const _: () = assert!(DEFAULT_CHAR_BUDGET == UNVERIFIED_CONTEXT_WINDOW * CHARS_PER_TOKEN / 100)`, so a future regression to a literal fails the BUILD rather than a test -- which is the strongest form this particular criterion can take. RED ARM: rederiving the None arm from DEFAULT_CONTEXT_WINDOW (200k), spelled so it compiles because mutating the constant itself is a build failure and would prove nothing; `cargo check -p wcore-skills -p wcore-agent --tests` RC=0 first; `the_unknown_window_budget_is_the_unverified_window_not_the_old_200k` reds with `left: 8000 / right: 1310`. Restored, tree clean, green. CI-RED HISTORY, carried forward from lane w2-window-arc because HEAD's regrade does not record it and it is the reason the fixture looks the way it does: GitHub run 33298746568 (branch lane/f13-w2-window-arc, job CI (linux-containerized)) failed an_unknown_window_sizes_the_skill_listing_like_the_window_it_assumes 3/3 on its own anti-vacuity precondition (200k = 4890 bytes, unknown = 4890 bytes) while the same test passed on hetzner-dsm. Cause, reproduced by A/B rather than inferred: the fixture planted ONE skill and relied on whatever else the HOST had installed to overflow the larger budget; /root/.config/wayland-core/skills on hetzner holds four skills left by earlier agent sessions, and those four supplied the overflow. Empty HOME reproduced the CI red locally (RC=100, 2/2). The fixture now plants FILLER_SKILLS of its own so the overflow is supplied by the fixture and not by the machine -- that mechanism is present at this commit, which is why this grade does not repeat the false green. XDG_CONFIG_HOME does NOT move the skills directory; a first verification using it passed with the fix reverted and proved nothing. Only HOME does."
  - id: c2
    text: "A test boots the bootstrap prompt with known_context_window = None and asserts the skills budget; shown RED against today's DEFAULT_CHAR_BUDGET"
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs::an_unknown_window_sizes_the_skill_listing_like_the_window_it_assumes"
    owner: core
    note: "REGRADED 2026-08-31. The ledger said `Nothing has been done` because its `last_verified_commit: 9de21aa1` PREDATES the fix: eddbc502d landed 2026-08-29 16:20Z, the same day the ticket was filed, and nothing re-graded it since. Confirmed independently three ways rather than on one agent's report: I read the constant myself, a grounding agent ran the red arm, and I re-ran the evidence tests at integ HEAD (9/9 pass, including the boot-with-None arm). MET AS WRITTEN, and it is the END-TO-END arm rather than the unit one: it boots the real bootstrap prompt for an unlisted model with no configured window -- the reporter's exact configuration, where `known_context_window` returns None -- and compares the rendered system prompt against the arm that assumes the fabricated window. Under the c1 mutation it reds with `left: 12674 / right: 7624`: the 8,000-char budget buys 5,050 more prompt bytes than the 1,310-char one, which is the defect stated in bytes. The sibling `an_unknown_window_sizes...` arm is non-vacuous by construction -- the fixture plants its own filler skills, because the overflow used to come from whatever skills the HOST had installed, so the test passed on developer boxes and was RED in a clean CI container while #1199 c2/c3 were graded met on it."
  - id: c3
    text: "The measured arithmetic here is reproduced after the change: an unlisted model's skill listing no longer receives 8,000 chars while every other boundary is sized against 32,768"
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs::the_bootstrap_prompt_uses_the_real_window_derived_skill_budget"
    owner: core
    note: "REGRADED 2026-08-31. The ledger said `Nothing has been done` because its `last_verified_commit: 9de21aa1` PREDATES the fix: eddbc502d landed 2026-08-29 16:20Z, the same day the ticket was filed, and nothing re-graded it since. Confirmed independently three ways rather than on one agent's report: I read the constant myself, a grounding agent ran the red arm, and I re-ran the evidence tests at integ HEAD (9/9 pass, including the boot-with-None arm). The measured arithmetic is reproduced: 1% x 32,768 x 4 = 1,310 chars is what the unlisted-model listing now receives, against the 8,000 the issue measured -- the 6.1x is closed, and the skills listing no longer spends ~2,000 tokens of a 32,768-token window on itself. Graded through the bootstrap, not the constant: this test asserts the real window-derived budget reaches the assembled prompt, so a constant that is right while the wiring passes something else cannot satisfy it. Verified at integ HEAD independently of the grounding agent: 9/9 pass across this file and the wcore-skills unit arms."
---

The fabricated 200,000-token window that #1150 c1 removed still governs the skills prompt budget for exactly the unlisted-model case c1 is about. `wcore_skills::prompt::get_char_budget(None)` returns `DEFAULT_CHAR_BUDGET = 8_000`, whose own source comment reads `// Fallback: 1% of 200k × 4`. For an unlisted model with no `[compact] context_window` — the reporter's exact configuration — `known_context_window` correctly returns `None`, the bootstrap passes that `None` straight through (bootstrap.rs:2326-2329), and the skills listing is sized against 200k while every other boundary in the session is sized against UNVERIFIED_CONTEXT_WINDOW = 32,768. Measured arithmetic: 1% × 32,768 tokens × 4 chars = 1,310 chars intended, 8,000 chars actually granted — 6.1x, i.e. ~2,000 tokens of a 32,768-token window spent on the skill listing. The lane knew this number: bootstrap.rs:2320-2325 says in its own words 'every real session listed skills against the flat 8,000-character default: a 32k model spent 6x its fair share of the window on the listing'. They fixed the wiring for the known-window case and left the unknown-window case on the old constant. The guard test cannot catch it: `the_bootstrap_prompt_uses_the_real_window_derived_skill_budget` (issue_1150_unknown_context_window_test.rs:227) boots BOTH arms with an explicit window — `Some(1_000_000)` and `Some(2_000)` — so the `None` path is untested, even though the test's own failure message is about sessions being 'on the flat 8,000-char default'. c1's doc comment at compact.rs:449-454 names 'the skills prompt budget' as one of the callers that 'must not act on a guess'; the guess simply moved downstream into get_char_budget's None arm.

**Where.** crates/wcore-skills/src/prompt.rs:9 and :20 (DEFAULT_CHAR_BUDGET / get_char_budget None arm); consumers at crates/wcore-agent/src/bootstrap.rs:2326 and crates/wcore-agent/src/late_mcp.rs:132; untested path guarded by crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs:227

**Why it matters.** It is the same defect class #1150 was filed for, at the same reporter configuration, in a site c1's own documentation claims to have covered. It also silently blunts the fix: the session correctly tells the user 'compaction falls back to a conservative 32,768-token assumption' while one prompt section keeps spending as if the window were 200,000.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.

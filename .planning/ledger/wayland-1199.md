---
issue: 1199
repo: FerroxLabs/wayland
kind: defect
title: "The skills prompt budget still sizes against the fabricated 200,000-token window for exactly the unlisted-model case #1150 c1 removed"
status: open
last_verified_commit: 6e404f28
criteria:
  - id: c1
    text: "get_char_budget(None) no longer grants a budget derived from 200k: the unknown-window case is sized against UNVERIFIED_CONTEXT_WINDOW or refuses to guess, the rule compact.rs:449-454 already states"
    state: met
    evidence: "symbol:crates/wcore-skills/src/prompt.rs::DEFAULT_CHAR_BUDGET"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D16, found while verifying wayland#1150 — [Bug]: Absurd Input Token Size). Nothing has been done. The measured finding, verbatim: The fabricated 200,000-token window that #1150 c1 removed still governs the skills prompt budget for exactly the unlisted-model case c1 is about. `wcore_skills::prompt::get_char_budget(None)` returns `DEFAULT_CHAR_BUDGET = 8_000`, whose own source comment reads `// Fallback: 1% of 200k × 4`. For an unlisted model with no `[compact] context_window` — the reporter's exact configuration — `known_context_window` correctly returns `None`, the bootstrap passes that `None` straight through (bootstrap.rs:2326-2329), and the skills listing is sized against 200k while every other boundary in the session is sized against UNVERIFIED_CONTEXT_WINDOW = 32,768. Measured arithmetic: 1% × 32,768 tokens × 4 chars = 1,310 chars intended, 8,000 chars actually granted — 6.1x, i.e. ~2,000 tokens of a 32,768-token window spent on the skill listing. The lane knew this number: bootstrap.rs:2320-2325 says in its own words 'every real session listed skills against the flat 8,000-character default: a 32k model spent 6x its fair share of the window on the listing'. They fixed the wiring for the known-window case and left the unknown-window case on the old constant. The guard test cannot catch it: `the_bootstrap_prompt_uses_the_real_window_derived_skill_budget` (issue_1150_unknown_context_window_test.rs:227) boots BOTH arms with an explicit window — `Some(1_000_000)` and `Some(2_000)` — so the `None` path is untested, even though the test's own failure message is about sessions being 'on the flat 8,000-char default'. c1's doc comment at compact.rs:449-454 names 'the skills prompt budget' as one of the callers that 'must not act on a guess'; the guess simply moved downstream into get_char_budget's None arm. GRADED 2026-08-30 by lane w2-window-arc at 115cb4c6; the fix itself is already on integ/f13 and was VERIFIED here rather than re-implemented. DEFAULT_CHAR_BUDGET is now UNVERIFIED_CONTEXT_WINDOW * CHARS_PER_TOKEN / 100 = 1,310, not the flat 8,000 whose own comment read '1% of 200k x 4'. It is DERIVED from the same assumption the rest of the session makes rather than restated, and a `const _: () = assert!(..)` makes the derivation the compiler's problem instead of a test's. CHARS_PER_TOKEN is now re-exported from wcore_config::compact rather than declared twice, which is what stops the two drifting apart again. RED ARM (hetzner-dsm): returning a literal 8_000 from get_char_budget's None arm reddens c2's test; restored, touched, green. DELIBERATE EXCLUSION FROM THE RESOLVED-WINDOW CHOKEPOINT, recorded because it looks like an omission: get_char_budget's caller passes known_context_window, NOT compaction_window_now, so the skills budget is the one window-derived figure #1172's learned served window does not narrow. That is correct rather than missed. The listing is built once at bootstrap, before any turn has produced a usage report, so no learned window exists yet; and re-sizing it mid-session would rewrite the head of the system prompt on the turn the window is learned, which is exactly the prompt-prefix break wayland#1150 c4/c6 exist to prevent (77M tokens). The correct response to a window too small for the listing is the turn loop's refusal, not a shrinking system prompt."
  - id: c2
    text: "A test boots the bootstrap prompt with known_context_window = None and asserts the skills budget; shown RED against today's DEFAULT_CHAR_BUDGET"
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs::an_unknown_window_sizes_the_skill_listing_like_the_window_it_assumes"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D16). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below. GRADED 2026-08-30 by lane w2-window-arc at 115cb4c6; the fix itself is already on integ/f13 and was VERIFIED here rather than re-implemented. Boots the bootstrap prompt three times on an UNLISTED model -- known_context_window = None, = Some(UNVERIFIED_CONTEXT_WINDOW), and = Some(DEFAULT_CONTEXT_WINDOW) -- and asserts the None prompt is byte-identical in length to the 32,768 one. Asserted as an IDENTITY against the window the session assumes, so it cannot be satisfied by some other 1,310-character coincidence, and it keeps tracking UNVERIFIED_CONTEXT_WINDOW if that constant moves. The 200,000 arm is the control: it asserts a 200k window really does buy a longer listing here, so the test cannot pass on an empty skill catalogue. RED IN CI, GREEN ON THE BUILD HOST -- found and closed 2026-08-30 while answering this lane's refutation. The control sentence immediately above was TRUE on hetzner and FALSE in the CI container. GitHub run 33298746568 (branch lane/f13-w2-window-arc, job CI (linux-containerized), step Run tests (nextest CI profile)) failed this test 3/3 -- not a flake, three tries, same message -- on exactly the precondition that exists to stop it grading nothing: `precondition: a 200,000-token window really does buy a longer listing here, or this test could pass on a catalogue with no skills in it (200k = 4890 bytes, unknown = 4890 bytes)`. The same test passes on hetzner-dsm (cargo nextest run -p wcore-agent -E test(an_unknown_window_sizes_the_skill_listing...) -> 1 test run: 1 passed). So c2 and c3 were both graded `met` on evidence that is RED in the environment the release gate actually runs in, and neither this lane nor its verifier could see it because both gated on hetzner alone. The test predates this lane (it is present at base 70a47aaed and this lane never touched the file), which sets the repair ORDER, not whether it gets repaired -- the criteria that lean on it are this lane's. CAUSE, reproduced with a controlled A/B on hetzner rather than inferred: the fixture planted ONE skill and relied on whatever else was installed on the machine to make the catalogue overflow the larger budget; a clean container has nothing else, both arms render the identical 4,890 bytes, and the comparison measures nothing. The sibling test in the same file already carries a comment diagnosing precisely this (`that composition differs between a developer box with skills installed and a clean CI container, and the assertion duly passed on the build host and failed in CI`) -- the lesson was written down and then not applied one test over, which is the same shape as #1200 c2. THE AMBIENT SOURCE IS NAMED: /root/.config/wayland-core/skills on hetzner holds 4 skills (auto-code-refactor, auto-code-refactor-review, auto-everything-instructions-participant, auto-inbound-untrusted-wayland) left behind by earlier agent sessions on the box, and those four are what push the catalogue over the budget. A/B on hetzner: default HOME -> 1 test run: 1 passed (ARM_A_RC=0); HOME pointed at an empty directory -> ARM_B_RC=100, 2/2 tries, precondition ... (200k = 4753 bytes, unknown = 4753 bytes) -- the same assertion and the same shape CI reports, so the CI red is reproduced locally and is not a container mystery. FIX: the fixture now plants 30 filler skills with 400-character descriptions, 12,000 characters against the 8,000-character budget the largest arm buys, so the OVERFLOW IS SUPPLIED BY THE FIXTURE AND NOT BY THE HOST. A new test, the_fixture_overflows_every_budget_under_test, asserts that lower bound directly (planted characters > largest clipped budget) and then asserts through the real bootstrap that the 1,000,000 / 200,000 / 32,768 arms render three STRICTLY DECREASING listings, so the file can never again go vacuous on a machine that happens to have fewer skills installed. VERIFIED by re-running the SAME empty-HOME arm after the fix -- the arm that can tell the difference: AFTER_FIX_EMPTYHOME_RC=0, 3 tests run: 3 passed, the new fixture test included. The default-HOME arm passed before the fix too (FIX_AMBIENT_RC=0) and proves nothing about it. ONE FALSE GREEN ALONG THE WAY, recorded because it is the same mistake in miniature: the first verification used XDG_CONFIG_HOME instead of HOME and passed, and the control caught it -- reverting the fixture fix and re-running under XDG_CONFIG_HOME still PASSED (CONTROL_RC=0), so that lever does not move the skills directory and the arm could not have discriminated. Only HOME does. A verification arm that passes with the fix reverted is not a verification."
  - id: c3
    text: "The measured arithmetic here is reproduced after the change: an unlisted model's skill listing no longer receives 8,000 chars while every other boundary is sized against 32,768"
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs::an_unknown_window_sizes_the_skill_listing_like_the_window_it_assumes"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D16). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below. GRADED 2026-08-30 by lane w2-window-arc at 115cb4c6; the fix itself is already on integ/f13 and was VERIFIED here rather than re-implemented. The measured arithmetic is reproduced by the same test: the unlisted model's skill listing is sized against 32,768 (1,310 chars) rather than receiving 8,000 while every other boundary in the session is sized against UNVERIFIED_CONTEXT_WINDOW. The 6.1x the ticket measured is the difference between the None arm and the DEFAULT_CONTEXT_WINDOW arm, and the test asserts that difference is real. SAME CI RED AS c2, SAME FIX -- see the c2 note above. This criterion's evidence is the same test, so it was graded `met` on the same CI-red evidence; the fixture now supplies its own overflow and the_fixture_overflows_every_budget_under_test pins that it does."
---

The fabricated 200,000-token window that #1150 c1 removed still governs the skills prompt budget for exactly the unlisted-model case c1 is about. `wcore_skills::prompt::get_char_budget(None)` returns `DEFAULT_CHAR_BUDGET = 8_000`, whose own source comment reads `// Fallback: 1% of 200k × 4`. For an unlisted model with no `[compact] context_window` — the reporter's exact configuration — `known_context_window` correctly returns `None`, the bootstrap passes that `None` straight through (bootstrap.rs:2326-2329), and the skills listing is sized against 200k while every other boundary in the session is sized against UNVERIFIED_CONTEXT_WINDOW = 32,768. Measured arithmetic: 1% × 32,768 tokens × 4 chars = 1,310 chars intended, 8,000 chars actually granted — 6.1x, i.e. ~2,000 tokens of a 32,768-token window spent on the skill listing. The lane knew this number: bootstrap.rs:2320-2325 says in its own words 'every real session listed skills against the flat 8,000-character default: a 32k model spent 6x its fair share of the window on the listing'. They fixed the wiring for the known-window case and left the unknown-window case on the old constant. The guard test cannot catch it: `the_bootstrap_prompt_uses_the_real_window_derived_skill_budget` (issue_1150_unknown_context_window_test.rs:227) boots BOTH arms with an explicit window — `Some(1_000_000)` and `Some(2_000)` — so the `None` path is untested, even though the test's own failure message is about sessions being 'on the flat 8,000-char default'. c1's doc comment at compact.rs:449-454 names 'the skills prompt budget' as one of the callers that 'must not act on a guess'; the guess simply moved downstream into get_char_budget's None arm.

**Where.** crates/wcore-skills/src/prompt.rs:9 and :20 (DEFAULT_CHAR_BUDGET / get_char_budget None arm); consumers at crates/wcore-agent/src/bootstrap.rs:2326 and crates/wcore-agent/src/late_mcp.rs:132; untested path guarded by crates/wcore-agent/tests/issue_1150_unknown_context_window_test.rs:227

**Why it matters.** It is the same defect class #1150 was filed for, at the same reporter configuration, in a site c1's own documentation claims to have covered. It also silently blunts the fix: the session correctly tells the user 'compaction falls back to a conservative 32,768-token assumption' while one prompt section keeps spending as if the window were 200,000.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.

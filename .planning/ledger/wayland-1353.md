---
issue: 1353
repo: FerroxLabs/wayland
kind: defect
title: "Concurrent effect-checkpoint stores can jointly exceed the session quota"
status: open
last_verified_commit: 5d43c200d
criteria:
  - id: c1
    text: "Concurrent stores into one checkpoint directory cannot jointly exceed the session quota: the probe test goes GREEN with the repair and RED with the repair removed (a red arm from history); a store the quota refuses still fails closed and a store that fits still succeeds."
    state: met
    evidence: "test:crates/wcore-agent/src/session_journal.rs::concurrent_checkpoint_stores_cannot_jointly_exceed_the_session_quota"
    owner: core
    note: "MET 2026-09-10 at a8fd1ac9c. Host hetzner-dsm (Linux, 96 CPU) via tools/remote-proof.py slot default; every receipt remote_exit as stated and complete:true, collected in .planning/evidence/w15-1353/receipts.tsv. INSTRUMENT: the probe test, brought from 506aed92e as this lane's FIRST commit 2cca6b033 with a cfg(test) rendezvous (`quota_race_gate::after_quota_scan`) between the scan and the quota decision, so two stores into one directory both finish scanning before either is decided, room left for exactly ONE 64 MiB checkpoint. The gate now counts waiters that time out and the test asserts `Rendezvous { passed: 2, timed_out: 0 }`, so a serialized run cannot pass vacuously. RED FROM HISTORY: 2cca6b033 (test only, production = b37b69505), remote_exit 101: `total_bytes=570425344 quota=536870912 results=[Ok(()), Ok(())]`, rendezvous {2,0} -- the filing's byte count exactly. REPAIR a8fd1ac9c: a per-session admission ledger `CheckpointQuota { reserved, released }` on the shared SessionJournal handle (not the writer lock). A store records `released` before it scans and is refused when scan + reserved + (released since that record) + len exceeds the quota, else reserves len; the reservation ends right after publication or through Drop. A concurrent store is counted at least once however its publication interleaves with the scan. GREEN a8fd1ac9c, remote_exit 0: lib 2749 passed / 0 failed / 3 ignored, durable_child_store_test 12, f889_write_edit_reconcile_test 9, workflow_limits_test 9. The race test also asserts exactly one store accepted, the other refused with the session-quota error leaving no published checkpoint, and the accepted one loads back at 64 MiB (a store that fits succeeds; a refused store fails closed); the pre-existing sequential refusal test and `a_checkpoint_the_session_cannot_store_degrades_to_opaque_instead_of_refusing` pass. RED WITH THE REPAIR REMOVED (scratch branch w15/quota1353-red-m1, a0a477063 = a8fd1ac9c with `unseen = 0`, NOT to merge), remote_exit 101: the race test again 570,425,344 > 536,870,912 [Ok, Ok], and `checkpoint_admission_counts_a_store_released_during_the_scan` panics 'a store released during the scan must still be counted'. A second arm, red-m4 edfefdb0f (released counted absolutely), reds that unit test on the other side (exact remaining room refused); the first draft of the unit test would have survived m4 and was strengthened before a8fd1ac9c. clippy --all-targets -D warnings and clippy --target x86_64-pc-windows-gnu -p wcore-agent --all-targets -D warnings both remote_exit 0 at a8fd1ac9c. STATED COST: a concurrent store can be counted twice (its partial temporary in the scan AND reserved), which can only refuse a store NEAR the quota under real concurrency, never admit one past it. NOT CLAIMED: the rendezvous FORCES the interleaving; how often production concurrency reaches it is not measured. No macOS or Windows test run. No SOURCE_INPUTS file touched. REVIEW FOLLOW-UP 2026-09-10 at d0d69d361 (on integration b43d27551; test instrumentation only, no production behaviour change). An adversarial review found no interleaving that exceeds the quota but a MAJOR test gap: nothing pinned the two orderings the proof rests on, (a) the released snapshot BEFORE the scan and (b) the reservation ending only AFTER publication, and the race test above stays green if either is reordered because its first decider still holds a reservation. CLOSED WITH TWO TESTS, each red on exactly its own mutation. `a_store_published_after_another_stores_scan_is_still_counted` parks Y immediately after its listing (the scan now runs through `scan_checkpoint_quota`, so nothing can sit between listing and park) while X stores, publishes and releases completely, then lets Y decide. `a_checkpoint_reservation_ends_only_after_its_checkpoint_is_published` records, at the instant a reservation ends, whether the published checkpoint exists. PREMISE CORRECTED BY MEASUREMENT: the review asked for one park-after-scan test red on both mutations; that cannot catch (b), because Y's snapshot precedes its scan and so precedes all of X, and under (b) X's fully written temporary stays visible until its link, leaving only a directory-listing-order window no test can force. GREEN d0d69d361, the three tests NAMED, remote_exit 0, 3 passed. RED ARMS (scratch, never merge, each d0d69d361 plus one line, remote_exit 101, same three tests): review-red-a 4961de50f (snapshot after scan) -> only the park test fails, 'Y was admitted on a scan that could not see X: 570425344 > 536870912 bytes'; review-red-b 75f52c633 (drop before hard_link, the review's literal (b)) -> only the observer fails, left Some(false), park test GREEN; review-red-b2 2fd215c7b (drop right after admit) -> only the observer fails, left Some(false), park test GREEN. The race test is green under all three, confirming the gap. INSTRUMENT GAP FOUND: a `-- checkpoint` libtest filter silently never selects the park test (its name lacks that word); an earlier 37-pass `-- checkpoint` run at d0d69d361 did not run it, so only the named run above counts. Evidence: .planning/evidence/w15-1357/README.md (Part A)."
  - id: c2
    text: "The repair does not serialize unrelated sessions: any quota reservation or lock is scoped to one checkpoint directory, shown by a test that two different sessions storing concurrently are not blocked by each other."
    state: met
    evidence: "test:crates/wcore-agent/src/session_journal.rs::checkpoint_stores_in_different_sessions_are_neither_serialized_nor_share_a_quota"
    owner: core
    note: "MET 2026-09-10 at a8fd1ac9c (hetzner-dsm, remote-proof slot default, receipts in .planning/evidence/w15-1353/receipts.tsv). SCOPE: the ledger is a field of the SessionJournal handle, shared only by that journal's clones; an independent open of the same journal fails closed and each journal owns exactly one `.{journal}.effects` directory, so the ledger is per checkpoint directory. No lock spans the scan or the write; the two ledger critical sections are O(1). INSTRUMENT: two journals (a.journal / b.journal, distinct directories asserted), each filled to leave room for exactly one 64 MiB checkpoint, the rendezvous armed for BOTH directories with expected=2, so each session's store must reach the point after its scan while the other's is parked there; both stores must then be accepted and both ledgers return to reserved=0. GREEN at a8fd1ac9c (in the 2749-test lib run, remote_exit 0). RED ARMS, scratch branches, NOT to merge, both remote_exit 101 and failing on DIFFERENT assertions: red-m6 9556bb0ea (one process-wide lock from before the scan through the write) -> `Rendezvous { passed: 2, timed_out: 1 }` with results [Ok, Ok], i.e. BLOCKED by the other session; red-m3 7c79d8eeb (one process-wide ledger) -> rendezvous passed but results [Ok, Err(session quota)], i.e. CHARGED for the other session. NOT CLAIMED: the rendezvous sits after the scan and before the quota decision; a cross-session lock taken only after that point and dropped before the write would not trip it (such a lock would also be a broken repair that the c1 race test reds). No such lock exists in the change."
  - id: c3
    text: "Crash-left temporary checkpoint files (`.{digest}.*.tmp`) are still counted against the quota after the repair, pinned by a test."
    state: met
    evidence: "test:crates/wcore-agent/src/session_journal.rs::crash_left_checkpoint_temporaries_still_count_against_the_session_quota"
    owner: core
    note: "MET 2026-09-10 at a8fd1ac9c (hetzner-dsm, remote-proof slot default). CRASH TEMPORARIES: a 1-byte `.{digest}.4242.{uuid}.tmp` for a DIFFERENT digest (so the store's own same-digest stale cleanup cannot delete it first) in a directory with room for exactly one 64 MiB checkpoint: the store is refused by the session quota, the temporary survives, nothing is published, reserved stays 0; after removing the temporary the identical store succeeds and the directory lands at exactly 536,870,912 bytes. RED: red-m5 3376849f5 (the scan skips *.tmp), remote_exit 101, 'one crash-left byte over the quota must refuse the store: Ok(())'. The repair does not touch `checkpoint_directory_bytes` or how it sizes entries. RESERVATION LEAKS (the lane brief's addition): a reservation ends only by the explicit drop after publication or by Drop on any other exit. `panicking_checkpoint_store_releases_its_quota_reservation`: a thread admits 64 MiB against the real journal's ledger, confirms it held, and panics; after join reserved=0 and a real 64 MiB store into the remaining 96 MiB succeeds. RED: red-m2 8b768f8a1 (Drop never releases), remote_exit 101, reserved after the panic 67,108,868 (64 MiB plus the 4-byte seed store, which also leaked). Poisoning cannot wedge the quota: the ledger's critical sections are non-panicking saturating/wrapping arithmetic and the lock is recovered via PoisonError::into_inner. A process crash loses the ledger with the process; the next scan counts what it left (the test above). Release does not set panic=abort. NOT CLAIMED: an error return after admission and before publication has no test of its own; it takes the same Drop path the panic test drives, and forcing a real mid-write I/O failure needs a fault hook this file does not have (the proof host runs as root, so a permission failure is not available)."
  - id: c4
    text: "The wayland#1301 c3 per-dispatch timing is not regressed by the repair: the isolated fix1_dispatch_budget_aborts_with_partial_result step is measured before and after on the same executor class."
    state: not-met
    evidence: ""
    owner: core
    note: "NOT MET: no Windows measurement, and #1301 c3 is graded on hosted windows-latest. LINUX SUPPORTING DATA ONLY, 2026-09-10 on hetzner-dsm (Ubuntu 24.04, 6.8.0-101, 96 CPU, shared build host). Arms built through remote-proof `test --release -p wcore-agent --test workflow_limits_test --no-run` (remote_exit 0 both): A b37b69505 sha256 e67d24ee7c9a6aa7...196c, B a8fd1ac9c sha256 e1de028c867dbc9d...ff0c; both build to the SAME deps filename, so each was copied aside immediately after its own build (B mtime 1789060217 > A 1789059957, sizes 39,492,096 vs 39,485,832) and the timing run's meta records the same digests. Binary run directly (NOT via nextest as the CI step runs it), --exact the one test, --test-threads=1, fresh WAYLAND_HOME, 12 rounds interleaved A,B/B,A, every sample kept. 24/24 passed. A median 17.209 s (sd 1.261), B median 17.814 s (sd 1.379); paired B-A median +0.026 s, mean +0.357 s, B slower in 6/12; bootstrap 95% of the median paired diff [-1.110, +1.898] s; median ratio B/A 1.0351 [0.9480, 1.1052]; load1 35.15..45.24 on 96 CPU (arm medians 39.95 / 40.45). READ AS: no detectable regression at ~10% resolution; this host cannot resolve smaller. SURFACE CONTROL (strace, counts not timing): both arms make exactly 1000 linkat calls into a session .effects/ directory, all returning 0 -- one published checkpoint per dispatch, same in both arms, all under the durable spawner's mutations lock, so this step only exercises the repair's UNCONTENDED path. The repair holds no lock across the scan or the write (two O(1) critical sections per store). WHAT WOULD GRADE IT: the #1301 lane's throwaway hosted harness f0b9199ce (branch timing/w15-1301-ab: .github/workflows/w15-1301-ab.yml + .scratch1301/harnessab1301.ps1) builds each arm's release workflow_limits_test into its own target dir on windows-latest and interleaves the exact CI isolated-step nextest invocation, two shards of six rounds, raw samples uploaded. Run it on a throwaway branch with base = 07272c36b rebased and FIX_SHA = this repair rebased onto it (a merge-tree composition is clean and its 38 checkpoint tests pass at scratch 8ab83a98a). Not run by this lane: no CI was burned and no branch was pushed. Evidence: .planning/evidence/w15-1353/."
---

Created 2026-09-10 at filing time, not retroactively. Found by the w15/win1301
lane while attributing the Windows per-dispatch I/O cost behind wayland#1301 c3.

Graded 2026-09-10 by the w15/quota1353 lane: c1-c3 met at a8fd1ac9c, c4 not-met
(Linux supporting data only). Full record in `.planning/evidence/w15-1353/README.md`.

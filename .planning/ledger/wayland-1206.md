---
issue: 1206
repo: FerroxLabs/wayland
kind: defect
title: "The #1166 absolute floor reports PartialMiss{TtlExpiry} and writes a durable 'expired' invalidation on a healthy turn"
status: open
last_verified_commit: 7a7cf1f6
criteria:
  - id: c1
    text: "A turn whose cache_read is unchanged from the previous turn while total input grows is not attributed to TtlExpiry"
    state: met
    evidence: "test:crates/wcore-agent/src/cache_diagnostics.rs::a_second_dead_turn_cannot_manufacture_an_expiry_the_diagnostic_never_found"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D24, found while verifying wayland#1166). NOW CLOSED -- the disposition is at the end of this note. The measured finding as filed, verbatim: The #1166 absolute floor reports `PartialMiss { cause: TtlExpiry }` — and writes a durable `expired` invalidation into the cache ledger — on a turn where the cache worked perfectly. The floor tests `cache_read / total_input < 0.3`, so any warm turn whose NEW input dwarfs the cached prefix trips it even when the entire prefix was read back verbatim. Reproduced, not modelled: driving the real module (verbatim copy, no edits) through 3 healthy turns at cache_read=40,000 / input=500, then one turn at cache_read=40,000 (same full prefix, still read back) / input=150,000, printed `PROBE diag=PartialMiss { hit_rate: 0.2105, cause: TtlExpiry }` and `PROBE alert=Some(CacheHealthAlert { round_trip: 4, input_tokens: 190000, cache_read_tokens: 40000, ratio: 0.2105, cause: TtlExpiry })`. Nothing was invalidated on that turn: prev cache_read == current cache_read, so before #1166 this returned Healthy (drop_pct = 1 - 40000/40000 = 0.0, not > 0.05). The false verdict is durable and on by default: engine.rs:17927 does `cause_of_diagnostic(diagnostic)` → `InvalidationCause::Expired` into the ledger, and `cache_ledger::recording_enabled()` (:886-894) returns true when the env var is unset. So `wayland cache` will report invalidations, attributed to the server's TTL, that never happened. CLOSED 2026-08-30 by lane/f13-misc. `cached_prefix_covers_prior_context` measures cache_read against the PREVIOUS turn's total input -- the context a working cache would have to hand back -- and suppresses the floor when that is at or above the threshold. Both sites that test the ratio apply it: compute_diagnostic's floor and check_cache_health (which needed the previous turn's totals carried explicitly, because check_response rotates prev_stats to the CURRENT turn before the engine calls it). On the measured #559 leader trace that is 192 / 6_220 = 0.031 and the floor still fires; on a fully reused prefix it is 40_000 / 40_500 = 0.988 and it does not. REFUTED 2026-08-30 by the partials verifier, CONCEDED, AND NOW MET ON THE CRITERION`S OWN SENTENCE. The concession: c1 is about `cache_read` UNCHANGED turn over turn while total input grows, and what shipped was `cached_prefix_covers_prior_context`, a RATIO against the previous turn`s total input -- so it only suppressed when the PREVIOUS turn was itself well covered, and the test drove exactly that sub-case (40_000 / 40_500 = 0.988). The verifier`s trace (two turns at input=200_000 / cache_read=40_000, then one at input=300_000 with cache_read UNCHANGED at 40_000) still produced `PartialMiss { cause: TtlExpiry }` and `ledger_cause=Some(Expired)`. Reproduced. WHAT THE CRITERION CANNOT MEAN, checked before fixing: read as `such a turn must be Healthy`, c1 would forbid detecting the #559 leader session, whose signature is cache_read pinned FLAT at 192 while input climbs 3_225 -> 7_001 -- the exact shape `flat_cache_read_on_warm_session_is_not_healthy` exists to redden, and the entire purpose of #1166`s floor. It does not have to mean that. c1 says `is not attributed to TtlExpiry` and c2 says `no InvalidationCause::Expired is written`; both are claims about the CAUSE, not about the verdict, and both are satisfiable with the floor fully intact. THE FIX is #1166`s own Defect 4 remedy applied to the fall-through, exactly as #1166 already did for ModelChanged (`Previously laundered into Expired, which blames the server for a client-side decision`). `attribute_cause` ends in TtlExpiry whenever nothing about the request diverged, so EVERY turn the floor caught on a stable prefix was reported as a server-side expiry. An expiry DESTROYS cached tokens, so it shows up as a DROP; a cache_read that is identical turn over turn did not expire, it never covered the context. New `CacheBreakCause::PrefixNotCached` -> `InvalidationCause::PrefixNotCached`, applied by `floor_cause` at BOTH halves (compute_diagnostic`s floor and check_cache_health) so the detecting and alerting halves cannot disagree. NARROW BY CONSTRUCTION: only the TtlExpiry fall-through on a NON-dropping turn is rewritten -- a real attribution (SystemPromptChanged / ToolsChanged / ModelChanged / MessagesChanged) is untouched, and so is TtlExpiry on a turn where cache_read really fell. The floor STILL FIRES on the verifier`s trace, which is correct: 40_000 of coverage on a 240_000-token prior context is genuinely poor. What changed is that it is no longer blamed on the server. EVIDENCE: `flat_cache_read_under_growing_input_is_not_attributed_to_expiry` drives the verifier`s exact trace and asserts its own premises first (the prior-context suppression is DISARMED at 40_000 / 240_000, and cache_read did not drop) so it cannot pass by drifting into the suppression window. #559 detection survives, measured not assumed: `flat_cache_read_on_warm_session_is_not_healthy` and `flat_cache_read_is_detected_and_attributed` both stay green, the latter because its turn 3 mutates the prefix and therefore exercises the real-attribution path this change leaves alone. RED ARM: with `floor_cause` reduced to `attributed` -- the fall-through rewrite deleted; mutation on EXECUTABLE code, tree committed clean first, `touch`ed after both the mutation and the restore -- `panicked at cache_diagnostics.rs:1094: c1: cache_read was unchanged while total input grew -- the cache never covered this context, and blaming the server's TTL for it is #1166's own Defect 4. got PartialMiss { hit_rate: 0.11764705882352941, cause: TtlExpiry }`. That is the verifier's measured hit_rate to the last digit. BOTH controls stayed GREEN in the same arm: `a_real_expiry_is_still_attributed_to_the_ttl` (a real expiry is still called one) and `flat_cache_read_on_warm_session_is_not_healthy` (#559 detection survives). Restored, touched, 28/28 green. ROUND-2 REFUTATION 2026-08-30: CONCEDED, AND NOW CLOSED ON THE SENTENCE ITSELF. The verifier generated a SECOND instance of the same shape and it was reproduced here before fixing: turn 1 at input 10_000 / cache_read 0 / cache_creation 9_000, then turn 2 with a byte-identical prefix at input 30_000 / cache_read 0 gave `FullMiss { cause: TtlExpiry }` and `cause_of_diagnostic = Some(Expired)`. cache_read UNCHANGED (0 -> 0) while total input grows IS c1 verbatim, so c1 was not met and grading it `met` was wrong. WHY IT ESCAPED ROUND 1: `floor_cause` was applied at two of the THREE attribution sites -- `check_cache_health` and the absolute floor -- and NOT at `compute_diagnostic`s FULL-miss arm, which still called `attribute_cause` bare. That arm has no warm-session gate and `prev_had_cache` is satisfied by cache_CREATION alone, so it is reached on TURN 2 of any session where the provider writes a prefix and does not serve it back (a below-minimum prefix, a rejected marker, a moved prefix) -- strictly larger and cheaper to hit than the 240k trace round 1 closed. THE FIX CLOSES THE CLASS, NOT THE INSTANCE, because this was the third patch of one shape: `attribute_cause` is now `attribute_cause_raw` and every verdict-producing site goes through `attributed_cause`, which always applies `floor_cause`; the drop boolean is computed once per turn from the previous turns figure. There is no reachable unguarded attribution left. RED ARM -- tree committed clean at 40fde5c8c first, mutation on EXECUTABLE code (`git diff --stat` shows the one-line change inside `compute_diagnostic`), `touch` after the mutation and after the `git checkout --` restore, tree [] verified after: reverting the FULL-miss site to `self.attribute_cause_raw(current)` gave `panicked at crates/wcore-agent/src/cache_diagnostics.rs:1249:9: c1: cache_read was unchanged at 0 while total input grew 10_000 -> 30_000. Nothing expired -- the prefix was written and never served. got FullMiss { cause: TtlExpiry }`. FOUR CONTROLS STAYED GREEN IN THAT SAME ARM, which is what proves the new test is the thing catching it and not a duplicate of an existing one: `a_real_expiry_is_still_attributed_to_the_ttl`, `flat_cache_read_on_warm_session_is_not_healthy` (#559 detection), `flat_cache_read_under_growing_input_is_not_attributed_to_expiry` (round 1s instance) and the narrowness control below. OVER-CORRECTION ARM, run because the named failure mode is a fix reproducing the defect it addresses: with `attributed_cause` forced to return `PrefixNotCached` unconditionally, BOTH `a_real_expiry_is_still_attributed_to_the_ttl` (`an unchanged request that stopped reading its cache back IS the TTL case; #1206 must not swallow it. got FullMiss { cause: PrefixNotCached }`) and the new `a_full_miss_with_flat_zero_cache_read_still_names_a_real_divergence` go RED -- so the rewrite cannot be widened into laundering real attributions. EVIDENCE FIELD CORRECTED IN THE FIELD, not described in prose: it read `a_fully_reused_prefix_under_a_large_new_input_is_not_an_invalidation`, which is c3s test and covers only the suppressed sub-case. c1 now anchors the FULL-miss test and c2 anchors round 1s `flat_cache_read_under_growing_input_is_not_attributed_to_expiry`, so BOTH instances of c1s shape carry a machine-resolvable anchor and neither can be deleted with the ledger gate still green. --- MERGED INTO integ/f13 2026-08-30 (lane/f13-dur-ledgers). Both lanes closed the same class by different routes and BOTH closures are in the merged tree, verified by running both suites unmodified. The dur-ledgers STRUCTURE won: a cause is decided in exactly one function (attribute_break, reachable only from compute_diagnostic) and carried to the alert half as a TurnAttribution, so check_cache_health is a pure reader with no path to attribute_cause at all -- pinned by attribute_cause_is_called_from_exactly_one_place. lane/f13-misc's floor_cause is gone as a FUNCTION and preserved as a RULE, and the rule is strictly wider than the incoming refutation: refute_ttl now refuses the TtlExpiry fall-through whenever cache_read did not fall past the module's own CACHE_READ_DROP_TOLERANCE (cache_read_dropped), not only when it is exactly unchanged. Taking the incoming equality test verbatim would have LOST coverage misc already had: a turn whose cache_read slipped 40,000 -> 39,000 (2.4%, under the tolerance, so the partial-miss arm itself declines to call it a drop) reaches the floor and would have been reported PartialMiss{TtlExpiry} and written durably as expired -- a module that says this did not fall in one arm may not say the server expired it in the next. It also makes the property structural: the partial-miss arm returns on every drop over the tolerance, so a TtlExpiry reaching the floor is now impossible by construction. NAME: misc's PrefixNotCached, not the incoming Unattributed. They denote the same state; PrefixNotCached is already the published InvalidationCause vocabulary (prefix_not_cached, crates/wcore-providers/src/cache_observation.rs) which misc merged and which Unattributed would have orphaned, and the branch that produces it carries positive evidence -- the prefix was not carrying the session on the previous turn AND did not fall on this one -- which mapping to unknown would discard. Every test on both sides is green unmodified except the incoming ones that asserted the Unattributed NAME, and the_floor_still_bites_one_token_below_the_threshold, whose cause assertion (TtlExpiry AT the floor) is unreachable under misc's rule; that test's graded property, a case one token either side of CACHE_HEALTH_WARN_RATIO, is untouched. misc's own c1 anchor a_full_miss_with_flat_zero_cache_read_is_not_attributed_to_expiry is still green and still asserts its premises. 134/134 cache tests green on the merged tree. --- THE dur-ledgers LANE NOTE, PRESERVED: Closed as a CLASS at the type level, after an independent verifier found a THIRD reachable instance the site-by-site closure had missed. A CacheBreakCause is now decided in exactly ONE function -- attribute_break, itself reached only from compute_diagnostic -- and carried to the alert half as a TurnAttribution. check_cache_health READS that value and has no path to attribute_cause at all, so `the alert names a cause the diagnostic half never reached` is no longer a defect that can reappear at a fourth site. Pinned by attribute_cause_is_called_from_exactly_one_place, which scans the module (comment lines dropped, needle built at runtime so the test cannot match its own source) and asserts the single call site's enclosing function is attribute_break. THE THIRD INSTANCE, measured through the real public pair check_response + check_cache_health in the order engine.rs uses it: three warm turns at cache_read=40,000/input=500, then turn 4 all-zero cache at input=60,000, then turn 5 all-zero cache at input=90,000 -- cache_read unchanged 0 -> 0 while total input grows. BEFORE: `PROBE diag=Some(Healthy { hit_rate: 0.0 }) alert=Some(CacheHealthAlert { round_trip: 5, input_tokens: 90000, cache_read_tokens: 0, ratio: 0.0, cause: TtlExpiry })`, which invalidation_cause_of turns into cause = \"expired\". AFTER: `PROBE diag=Some(PartialMiss { hit_rate: 0.0, cause: Unattributed }) alert=Some(CacheHealthAlert { round_trip: 5, input_tokens: 90000, cache_read_tokens: 0, ratio: 0.0, cause: Unattributed })`. Two supporting changes, each with its own red arm. (a) The `provider does not support caching` early return in compute_diagnostic is now gated on the session never having seen cache tokens: a session that HAS seen them is not a provider without caching, it is a dead cache, and taking that return there recorded no verdict and hid the finding. (b) refute_ttl no longer requires total input to GROW -- growth is the shape #1206 c1 measured, not what refutes the claim; an unchanged cache_read is -- which left the identical falsehood reachable on a turn whose input shrank (test an_unchanged_cache_read_refutes_ttl_expiry_even_when_input_shrinks). The two originally-named instances remain closed and independently guarded (the floor, and the FULL-MISS arm where prev.cache_read == 0 with prev cache_creation > 0), and named-cause precedence is untouched: attribute_break refutes ONLY the TtlExpiry fall-through, so the #1166 model / system / tools / mutated-message attribution still outranks the arithmetic. The class itself is swept, not spot-checked, by the_alert_half_never_names_a_cause_the_diagnostic_half_did_not_reach over 512 traces with a positive control that the sweep fired alerts and produced more than one verdict."
  - id: c2
    text: "No InvalidationCause::Expired is written to the cache ledger for such a turn"
    state: met
    evidence: "test:crates/wcore-agent/src/cache_diagnostics.rs::flat_cache_read_under_growing_input_is_not_attributed_to_expiry"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D24). NOW CLOSED -- the disposition is at the end of this note. The original measurement is on c1 and the file:line anchors are in the prose below. CLOSED 2026-08-30 by lane/f13-misc, and asserted as the RECORDED consequence rather than inferred from the enum: the test now also asserts `cache_ledger::cause_of_diagnostic(&diag) == None` for that turn. That is the function engine.rs feeds into the ledger write, and `recording_enabled()` is true unless the env kill-switch is set, so this is the assertion that stands between a wrong verdict and an `expired` invalidation an operator reads back out of `wayland cache` weeks later. REFUTED 2026-08-30 ALONGSIDE c1 AND NOW MET: the assertion was real but it was only ever driven on the fully-reused-prefix trace, where the floor never fires at all -- so it could not see the shape the verifier found, in which the floor DOES fire and `cause_of_diagnostic` therefore returns `Some(..)` rather than `None`. c2`s sentence is not `None`; it is `no InvalidationCause::Expired`. Graded on that now: the new test asserts `cause_of_diagnostic(&diag) == Some(InvalidationCause::PrefixNotCached)` for the verifier`s turn, which is the durable record engine.rs writes, and the KNOWN-POSITIVE CONTROL `a_real_expiry_is_still_attributed_to_the_ttl` drives an unchanged prefix whose cache_read collapses 40_000 -> 0 and asserts `FullMiss { TtlExpiry }` with `Some(InvalidationCause::Expired)` still recorded -- because a fall-through rewrite is only a fix if it is narrow, and a detector that can no longer say `expired` at all would satisfy c2 while destroying the signal. ROUND-2 REFUTATION 2026-08-30: CONCEDED ALONGSIDE c1 AND NOW CLOSED. `cause_of_diagnostic` returned `Some(Expired)` for the verifiers turn because the FULL-miss arm never reached `floor_cause` -- see c1 for the site, the class-closing fix and both arms. c2s own sentence is graded directly by the new test, which asserts `assert_ne!(cause_of_diagnostic(&diag), Some(InvalidationCause::Expired))` and then `assert_eq!(.., Some(InvalidationCause::PrefixNotCached))` -- the function engine.rs feeds the durable write, with `recording_enabled()` true unless the env kill-switch is set. This criterions `evidence:` field now anchors round 1s instance (`flat_cache_read_under_growing_input_is_not_attributed_to_expiry`, which asserts the same ledger cause on the PartialMiss shape) rather than the c3 test it wrongly named, so the two tests that jointly satisfy c1/c2 are both machine-resolvable. --- MERGED 2026-08-30. This criterion keeps misc's anchor, which asserts the DURABLE record (cause_of_diagnostic == Some(InvalidationCause::PrefixNotCached), never Expired) on c1's own shape; the incoming lane's a_prefix_reused_under_growing_input_writes_no_expired_invalidation is also in the tree and green, asserting None on the fully-reused shape. See c1 for the structural resolution and for why PrefixNotCached rather than Unattributed reaches the disk. --- THE dur-ledgers LANE NOTE, PRESERVED: Drives the measured sequence through `cause_of_diagnostic` -- the exact call `AgentEngine::record_cache_ledger_turn` makes -- and asserts it returns None, so no invalidation of any kind is recorded, least of all Expired. Shown RED: reverting the floor arm alone to a bare attribute_cause gives `assertion `left == right` failed: nothing was invalidated, so nothing may be recorded as one: PartialMiss { hit_rate: 0.21052631578947367, cause: TtlExpiry } / left: Some(Expired) / right: None`. Where the shape leaves a genuine finding (a cache that was never carrying the session), the new `CacheBreakCause::Unattributed` publishes as `InvalidationCause::Unknown`, never `expired`: test an_unattributed_break_is_published_as_unknown_not_expired."
  - id: c3
    text: "A test drives the measured sequence -- three turns at cache_read=40,000/input=500, then one at cache_read=40,000/input=150,000 -- and asserts Healthy; shown RED against today's floor"
    state: met
    evidence: "test:crates/wcore-agent/src/cache_diagnostics.rs::a_fully_reused_prefix_under_a_large_new_input_is_not_an_invalidation"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D24). NOW CLOSED -- the disposition is at the end of this note. The original measurement is on c1 and the file:line anchors are in the prose below. CLOSED 2026-08-30 by lane/f13-misc. The test drives exactly the measured sequence -- three turns at cache_read=40_000 / input=500, then one at cache_read=40_000 / input=150_000 -- and asserts Healthy on both the diagnostic and the alert. SHOWN RED against the floor as it shipped, tree committed first, mutation on executable code, restored and touched after: with the prior-context suppression deleted at both sites, `thread 'cache_diagnostics::tests::a_fully_reused_prefix_under_a_large_new_input_is_not_an_invalidation' panicked at crates/wcore-agent/src/cache_diagnostics.rs:960:9: the whole prior context was read back from cache; nothing expired. got PartialMiss { hit_rate: 0.21052631578947367, cause: TtlExpiry }` -- the same 0.2105 / TtlExpiry the sweep measured. Restored: 26/26 green. --- MERGED 2026-08-30. Both lanes wrote this test. misc's is kept as the anchor because it additionally asserts cause_of_diagnostic == None -- the durable consequence, not only the in-memory verdict. The incoming lane's a_prefix_read_back_whole_under_a_flood_of_new_input_is_not_ttl_expiry is in the tree and green beside it. On the merged tree this turn is quieted twice over and consistently: misc's prior-context suppression keeps the floor shut (40,000/40,500 = 0.988) and, were it disarmed, refute_ttl would reach RefutedTtl::PrefixReused. Both halves stay silent because the alert half now READS the diagnostic half's NotAssessed. --- THE dur-ledgers LANE NOTE, PRESERVED: The measured sequence verbatim -- three turns at cache_read=40,000/input=500, then one at cache_read=40,000/input=150,000 -- asserting Healthy, and additionally asserting check_cache_health returns None so the alert half cannot certify what the diagnostic half refused. Shown RED: reverting the floor arm to a bare attribute_cause gives `the whole prefix came back; got PartialMiss { hit_rate: 0.21052631578947367, cause: TtlExpiry }`. The alert assertion has its own mutation: making check_cache_health treat a refuted turn as TtlExpiry again gives `left: Some(CacheHealthAlert { round_trip: 4, input_tokens: 190000, cache_read_tokens: 40000, ratio: 0.21052631578947367, cause: TtlExpiry }) / right: None`. A third, separate mutation -- dropping refute_ttl's prev-hit-rate tie-break -- reddens the dead-flat #559 case instead (`expected the floor to still fire, got Healthy { hit_rate: 0.020887728459530026 }`), which is what stops this fix from laundering a genuinely dead cache into Healthy."
  - id: c4
    text: "genuinely_healthy_trace_stays_healthy and warm_session_healthy_ratio_does_not_warn stay green, and at least one new case sits at the boundary rather than far from it"
    state: met
    evidence: "test:crates/wcore-agent/src/cache_diagnostics.rs::the_floor_still_bites_one_token_below_the_threshold"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D24). NOW CLOSED -- the disposition is at the end of this note. The original measurement is on c1 and the file:line anchors are in the prose below. CLOSED 2026-08-30 by lane/f13-misc. genuinely_healthy_trace_stays_healthy (~0.99) and warm_session_healthy_ratio_does_not_warn (0.8) were green in every arm run for this work, and flat_cache_read_on_warm_session_is_not_healthy stayed green under the suppression mutation specifically -- the suppression does not weaken the floor it guards. TWO new cases sit AT the boundary rather than far from it: the reused prefix at hit ratio 0.2105 (just under the 0.3 floor, which is where the false positive lived) and, anchored here, a REAL partial loss inside the suppression's own window -- prev turn reads 40_000 back, this turn reads 13_000 under 137_000 of new input, so coverage is 13_000/40_500 = 0.32 and the suppression IS armed. That case must still be reported and is: the test asserts its own premise (that the suppression is armed at those numbers) before asserting the verdict, so it cannot pass by drifting out of the window. It caught a real second defect -- check_cache_health had no turn-over-turn drop branch of its own, so it stayed silent while compute_diagnostic said PartialMiss. `cache_read_dropped` is now one rule both halves apply. RED ARM on that half alone: `the detecting half reported PartialMiss and the alerting half said nothing -- that is #1166 Defect 2, and the two halves are supposed to apply one rule`. --- MERGED 2026-08-30. The anchor moves to the incoming lane's boundary case, which sits ONE TOKEN either side of CACHE_HEALTH_WARN_RATIO (2,999/10,000 and 3,001/10,000) and is the closest thing in the suite to the criterion's words. misc's a_real_drop_inside_the_suppression_window_is_still_reported is in the tree and green, and still asserts its own premise that the suppression is armed. One change was forced on the incoming test: it asserted cause == TtlExpiry at the floor, which misc's rule makes unreachable (see c1). It now asserts PrefixNotCached; the boundary property it grades is unchanged, and genuinely_healthy_trace_stays_healthy and warm_session_healthy_ratio_does_not_warn are green on the merged tree. --- THE dur-ledgers LANE NOTE, PRESERVED: Both named tests stay green (nextest run quoted in the lane report). The new boundary case runs two warm turns at 2,999/10,000 = 0.2999 and 3,001/10,000 = 0.3001 -- one token either side of CACHE_HEALTH_WARN_RATIO -- against the existing 0.99 and 0.8 cases that sit nowhere near it. Its failing arm deliberately MOVES cache_read so the #1206 refutation is out of the way and the floor itself is what is measured. Shown RED by a mutation nothing else in this lane is sensitive to: moving CACHE_HEALTH_WARN_RATIO to 0.29 gives `one token below the threshold must still be a finding: Healthy { hit_rate: 0.2999 }`."
---

The #1166 absolute floor reports `PartialMiss { cause: TtlExpiry }` — and writes a durable `expired` invalidation into the cache ledger — on a turn where the cache worked perfectly. The floor tests `cache_read / total_input < 0.3`, so any warm turn whose NEW input dwarfs the cached prefix trips it even when the entire prefix was read back verbatim. Reproduced, not modelled: driving the real module (verbatim copy, no edits) through 3 healthy turns at cache_read=40,000 / input=500, then one turn at cache_read=40,000 (same full prefix, still read back) / input=150,000, printed `PROBE diag=PartialMiss { hit_rate: 0.2105, cause: TtlExpiry }` and `PROBE alert=Some(CacheHealthAlert { round_trip: 4, input_tokens: 190000, cache_read_tokens: 40000, ratio: 0.2105, cause: TtlExpiry })`. Nothing was invalidated on that turn: prev cache_read == current cache_read, so before #1166 this returned Healthy (drop_pct = 1 - 40000/40000 = 0.0, not > 0.05). The false verdict is durable and on by default: engine.rs:17927 does `cause_of_diagnostic(diagnostic)` → `InvalidationCause::Expired` into the ledger, and `cache_ledger::recording_enabled()` (:886-894) returns true when the env var is unset. So `wayland cache` will report invalidations, attributed to the server's TTL, that never happened.

**Where.** crates/wcore-agent/src/cache_diagnostics.rs:325-332 (the floor) feeding crates/wcore-agent/src/cache_ledger.rs:287-294 (cause_of_diagnostic) and crates/wcore-agent/src/engine.rs:17927 (ledger write)

**Why it matters.** This is the ticket's own Defect 4 re-entering through the fix. #1166's complaint was that TtlExpiry is a fall-through that blames the server for client-side causes; the fix widened the set of turns that reach that fall-through without narrowing it, so the tool built to diagnose #559 now manufactures confident `expired` findings on healthy sessions. No test covers the shape — genuinely_healthy_trace_stays_healthy uses a ~0.99 ratio and warm_session_healthy_ratio_does_not_warn uses 0.8, so both sit far from the boundary. A cheap discriminator exists and is unused: `cache_read` being flat-and-equal to the previous turn while total input grows is prefix reuse, not invalidation.

Criteria are taken verbatim from the issue's Acceptance section.

All of them are now met, closed by lane/f13-misc on 2026-08-30 alongside the
ticket they were found under. Each criterion's note carries its own evidence
and its own red arm. The GitHub issue is left OPEN deliberately: closing an
issue is not this lane's action.

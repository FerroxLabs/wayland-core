---
issue: 1252
repo: FerroxLabs/wayland
kind: defect
title: "Three more hand-cut authority parsers survive #1243: /doctor suppresses its base-url caveat, a browser origin pattern normalises to a different host, and a redaction renders the smuggled host as the surviving one"
status: open
last_verified_commit: cd3530d5e
criteria:
  - id: c1
    text: "With base_url = https://evil.example\\@api.openai.com/v1 and provider openai, /doctor PRINTS the base-url caveat naming the vendor host"
    state: met
    evidence: "test:crates/wcore-cli/src/doctor/mod.rs::the_base_url_caveat_reads_the_host_the_request_reaches"
    owner: core
    note: "MET 2026-08-31 by lane f13-s2-authority-parsers at cd3530d5e. SITE A closed. `base_url_caveat` now takes BOTH hosts from `wcore_types::url_authority::dialed_host_str` (doctor/mod.rs:1010 and :1013) and the hand cut `fn host_of` is DELETED from the file -- 0 occurrences remain. Graded on the PRODUCTION renderer, not on a helper: the test drives `provider_section_lines` -> `verdict_lines` -> `base_url_caveat`, which is the same path `/doctor` prints from, and `base_url_caveat` has 2 production call sites (doctor/mod.rs:969, :976). RED verbatim at dd1f08966 (tests committed, fix not yet applied; `cargo check -p wcore-cli --tests` RC=0 first, so this is behaviour and not a build break): `thread 'doctor::tests::the_base_url_caveat_reads_the_host_the_request_reaches' panicked at crates/wcore-cli/src/doctor/mod.rs:1832:9: a base_url that dials evil.example was read as the vendor's own host, so #1079's caveat was suppressed. lines: [ '', 'Provider (this invocation):', '  provider   openai   (from --provider)', '  model      gpt-4o   (from config)', '  base url   https://evil.example\@api.openai.com/v1 (from --base-url)', '  api key    present   (from --api-key)', '  credential ACCEPTED   (the provider authenticated this key)' ]` -- the printed section ENDS at ACCEPTED with no caveat line at all, which is #1079's defect reached through the spelling. GREEN at cd3530d5e. The fail direction is deliberate and documented at the call site: a base_url the parser cannot read is None, which is NOT equal to the vendor host, so the caveat PRINTS rather than going silent."
  - id: c2
    text: "origin_matches(\"github.com\", r\"https://evil.example\\@github.com\") is false"
    state: met
    evidence: "test:crates/wcore-browser/src/policy.rs::an_origin_pattern_normalises_to_the_host_it_actually_names"
    owner: core
    note: "MET 2026-08-31 by lane f13-s2-authority-parsers at cd3530d5e. SITE B closed. c2 is asserted in its own words as the first arm of the named test: `assert!(!origin_matches('github.com', r'https://evil.example\@github.com'))`. `strip_pattern_decorations` is DELETED (0 occurrences in policy.rs); `normalize_origin_pattern` now peels ONLY the scheme and the `*.` glob -- `*` is a forbidden host code point, so it cannot be handed to a parser -- and everything else (userinfo, port, path, and the `\` a special scheme reads as a path separator) is resolved by `wcore_types::url_authority::dialed_host` inside the new `host_via_parser` (policy.rs:1199). RED verbatim at dd1f08966: `thread 'policy::tests::an_origin_pattern_normalises_to_the_host_it_actually_names' panicked at crates/wcore-browser/src/policy.rs:1332:9: a pattern that dials evil.example matched github.com`. GREEN at cd3530d5e. Positive control in the same body: the pattern still matches `evil.example`, so the fix is a re-reading and not an entry that has become inert."
  - id: c3
    text: "Both sites take the host from wcore_types::url_authority rather than a hand-cut authority, so the class is closed rather than the one spelling"
    state: met
    evidence: "file:crates/wcore-cli/src/doctor/mod.rs:1013:wcore_types::url_authority::dialed_host_str(&cfg.base_url)"
    owner: core
    note: "MET 2026-08-31 by lane f13-s2-authority-parsers at cd3530d5e. The evidence anchor is site A's call, because the checker takes ONE token; the other two are stated here and are each independently anchored by c2 and c5. All three sites now take the host from `wcore_types::url_authority`, and every hand cut they used is DELETED rather than patched: (A) doctor/mod.rs `base_url_caveat` calls `dialed_host_str` twice -- `fn host_of` has 0 occurrences in the file; (B) wcore-browser policy.rs `canonical_host` -> `host_via_parser` (policy.rs:1199) calls `dialed_host` -- `fn strip_pattern_decorations` has 0 occurrences in the file; (C) wcore-config redact.rs `redact_token_userinfo` calls the NEW `wcore_types::url_authority::strip_userinfo`, added to the shared module rather than being a fourth private parse, so the userinfo/host boundary is the parser's there too. Graded against the DECIDABLE question this ticket's own c5 note asks for -- which functions return a host- or authority-shaped value without going through url::Url -- and not by enumerating idioms: a grep for `rsplit_once('@')`, `split(['/'...])`, `find('://')` and `split_once('//')` over the three files now returns ONE hit, redact.rs:295, which is a doc comment describing the code that was removed. Known-positive control in the same call: the same grep over the rest of `crates/` still returns hits (runtime_diagnostics.rs:536, backup/archive.rs:418, tui/widgets/sources_block.rs:92, ...), so the zero is a real zero and not a silently failing query. The four `split(['/','?','#'])` cuts the issue lists as OUT of scope are untouched and still out of scope -- none strips userinfo, so none can collapse a smuggled authority onto an allowlisted NAME."
  - id: c4
    text: "A test at each site carries the backslash spelling alongside a wrong-refusal control (a genuinely different configured host still PRINTS the caveat and a matching one still suppresses it; an ordinary *.github.com pattern still matches api.github.com), shown RED against today`s hand cut"
    state: met
    evidence: "test:crates/wcore-browser/src/policy.rs::a_smuggled_allow_entry_does_not_admit_the_host_it_spells"
    owner: core
    note: "MET 2026-08-31 by lane f13-s2-authority-parsers at cd3530d5e. Every site carries the backslash spelling and its wrong-refusal control IN THE SAME TEST BODY, and all four arms were shown RED at dd1f08966 before the fix, after `cargo check -p wcore-cli -p wcore-browser -p wcore-config --tests` returned RC=0 so a red is behaviour. SITE A `the_base_url_caveat_reads_the_host_the_request_reaches` -- backslash arm plus control 1 (a genuinely different host, `https://proxy.issue1252.invalid/v1`, still PRINTS the caveat) plus control 2 (`https://api.openai.com/v1` still SUPPRESSES it) plus a positive control that a verdict was produced at all. SITE B `an_origin_pattern_normalises_to_the_host_it_actually_names` -- backslash arm plus the criterion's named control `origin_matches('api.github.com', '*.github.com')` and six more ordinary spellings (apex, scheme-qualified, bare, port+path, honest userinfo, mixed case) plus a non-host pattern that must still match nothing. SITE B on the PRODUCTION path is the anchored test here, because a guard graded only through its own private helper is how a vacuous fix ships: it drives `BrowserPolicy::check_url` (the gate, `origin_matches` production call sites policy.rs:471 and :488), asserts the smuggling entry no longer admits github.com, positive-controls that the entry is not merely inert by admitting evil.example, and wrong-refusal-controls that `*.github.com` still admits api.github.com. SITE C `a_smuggled_authority_is_not_rendered_as_the_surviving_host` -- see c5. RED verbatim: (A) `crates/wcore-cli/src/doctor/mod.rs:1832:9: a base_url that dials evil.example was read as the vendor's own host, so #1079's caveat was suppressed`; (B) `crates/wcore-browser/src/policy.rs:1332:9: a pattern that dials evil.example matched github.com`; (B, production) `crates/wcore-browser/src/policy.rs:1391:9: an allow entry that dials evil.example admitted github.com`; (C) `crates/wcore-config/src/portability/redact.rs:269:9: assertion `left != right` failed: a URL that dials evil.example renders identically to a credential-bearing github.com URL / left: https://<redacted>@github.com/x / right: https://<redacted>@github.com/x`. All four GREEN at cd3530d5e, and the full suites of all four touched crates pass 5266/5266, so no wrong refusal was introduced anywhere else."
  - id: c5
    text: "scrub_detail over a smuggled authority does not name the surviving host as the allowlisted one: scrub_detail(r\"https://evil.example\\@github.com/x\") differs from scrub_detail(\"https://user:pw@github.com/x\"), with a test carrying both alongside a control that an ordinary credential-bearing URL is still redacted"
    state: met
    evidence: "test:crates/wcore-config/src/portability/redact.rs::a_smuggled_authority_is_not_rendered_as_the_surviving_host"
    owner: core
    note: "MET 2026-08-31 by lane f13-s2-authority-parsers at cd3530d5e. SITE C closed. `strip_url_userinfo` no longer cuts with `find('://')` + `rest.find('/')` + `find('@')`: it splits the detail into whitespace-delimited tokens -- because a `details` value is as often a command line carrying a URL as a bare URL, and a token is the largest unit the parser can read -- and asks `wcore_types::url_authority::strip_userinfo` whether the token has userinfo AT ALL. The smuggled URL has none (it is a request to evil.example with the path /@github.com/x), so it is returned byte for byte and never renders github.com in the surviving-host position. Graded through the PUBLIC `scrub_detail`, which has 2 production call sites (portability/mod.rs:183, :213). RED verbatim at dd1f08966 -- the two strings were IDENTICAL, exactly as the issue measured: `crates/wcore-config/src/portability/redact.rs:269:9: assertion `left != right` failed: a URL that dials evil.example renders identically to a credential-bearing github.com URL / left: https://<redacted>@github.com/x / right: https://<redacted>@github.com/x`. GREEN at cd3530d5e. CONTROLS in the same body, both required and both passing: the ordinary credential-bearing URL is STILL redacted to exactly `https://<redacted>@github.com/x` with neither `user` nor `pw@` surviving, and a URL with no credential at all is returned unchanged. The pre-existing `scrub_detail_removes_embedded_credentials_but_keeps_the_shape` test -- userinfo, secret query param, both command-flag spellings, and three negative controls -- passes untouched."
---

Found while closing wayland#1243, by the elimination sweep that ticket`s c4
demanded. A hand cut takes everything before the first `/` (sometimes `?`, `#`)
and then the LAST `@`-separated part. For a special scheme the WHATWG parser
maps `\` to a path separator, so `https://evil.example\@github.com/x` is a
request to `evil.example` -- and the cut reads `github.com`. That is #1211 and
#1243. These two sites still read it that way, and neither is inside #1243`s
stated scope, which is why this is its own ticket.

Searched for an existing carrier first, by symptom (`authority`, `backslash`,
`doctor host_of`, `url parser spoof host`, `BrowserPolicy allowlist origin`),
by originating issue (`1211`, `1243`), and by component across open
FerroxLabs/wayland + FerroxLabs/wayland-core. The only hits were #1211 and
#1243 themselves. Control run in the same session: the query `sandbox`
returned 12 open issues, so the search was not silently returning nothing.

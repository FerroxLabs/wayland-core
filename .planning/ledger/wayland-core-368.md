---
issue: 368
repo: FerroxLabs/wayland-core
kind: defect
title: "AppContainer deny is categorical: a deny identity strips a concurrent identity's grant, and a grant cannot reach an already-protected object"
status: open
last_verified_commit: 321fce9b
criteria:
  - id: c1
    text: "apply_protected_deny deletes only the calling identity's own package-SID ALLOW aces, never every S-1-15-2 ALLOW on the object"
    state: not-met
    owner: core
    note: "NOT BEING MET HERE, BY A RECORDED DECISION -- 2026-08-30, lane w3-windows-honesty. .planning/DECISIONS.md Q-368-honesty: the AppContainer ACL fix is NOT to be built. The standing decision is that Windows ships with no filesystem sandbox, the Job-object default is intended, and AppContainer is never to be chased again. What was open was the HONESTY of the claim, and that is what c6 closes. This criterion stays not-met and owned by core so it counts against the release as outstanding rather than disappearing; it becomes actionable only if the standing decision is reversed. Explicitly NOT superseded and NOT handed off: there is no other ticket for it, and marking it superseded would delete a measured defect from the release's arithmetic. RE-CONFIRMED AGAINST THE TREE, not taken from the #324 note: acl_lease.rs:1332 still gates deletion on `is_app_package_sid(ace_sid)` alone, which is true for ANY AppContainer package SID, and nothing in the function compares an ace against the identity being applied. The caller already has that identity: `apply_intents(intents, sid)` at :661 holds it as `sid` and passes it to `explicit_access_for_sid` on the Allow arm, so plumbing it into the Deny arm and switching the predicate to an EqualSid comparison is a small change. This criterion is the cheap half."
  - id: c2
    text: "Before protecting, the package ALLOWs other identities hold on that object by inheritance are materialised as explicit aces"
    state: not-met
    owner: core
    note: "NOT BEING MET HERE, BY A RECORDED DECISION -- 2026-08-30, lane w3-windows-honesty. .planning/DECISIONS.md Q-368-honesty: the AppContainer ACL fix is NOT to be built. The standing decision is that Windows ships with no filesystem sandbox, the Job-object default is intended, and AppContainer is never to be chased again. What was open was the HONESTY of the claim, and that is what c6 closes. This criterion stays not-met and owned by core so it counts against the release as outstanding rather than disappearing; it becomes actionable only if the standing decision is reversed. Explicitly NOT superseded and NOT handed off: there is no other ticket for it, and marking it superseded would delete a measured defect from the release's arithmetic. This is the criterion that actually fixes the measured failure, and c1 alone does NOT. The #324 c1 instrumented run found the allow identity reaching the secret ONLY by inheritance from the granted directory - it held no explicit ace on the secret at all - so deleting fewer explicit aces changes nothing for it. What removes its access is the `PROTECTED_DACL_SECURITY_INFORMATION` write at acl_lease.rs:1343, which severs inheritance for everybody. Materialising means clearing INHERITED_ACE on the other identities' package ALLOWs so they survive the protect as explicit entries. The comment at :1337-1339 states the protect is 'always' applied precisely so an inheritable package ALLOW cannot re-apply - that intent is correct for the CALLING identity and wrong for every other one, and this criterion is that distinction."
  - id: c3
    text: "apply_explicit_access reaches PROTECTED descendants of a granted root, so a grant applied after a protect still lands"
    state: not-met
    owner: core
    note: "NOT BEING MET HERE, BY A RECORDED DECISION -- 2026-08-30, lane w3-windows-honesty. .planning/DECISIONS.md Q-368-honesty: the AppContainer ACL fix is NOT to be built. The standing decision is that Windows ships with no filesystem sandbox, the Job-object default is intended, and AppContainer is never to be chased again. What was open was the HONESTY of the claim, and that is what c6 closes. This criterion stays not-met and owned by core so it counts against the release as outstanding rather than disappearing; it becomes actionable only if the standing decision is reversed. Explicitly NOT superseded and NOT handed off: there is no other ticket for it, and marking it superseded would delete a measured defect from the release's arithmetic. The converse ordering, and the harder half. `apply_explicit_access` (acl_lease.rs:1258) writes ONE object with `SUB_CONTAINERS_AND_OBJECTS_INHERIT`, so a grant on a directory reaches descendants purely by inheritance - which a PROTECTED descendant does not accept. Both orderings occur: the mutation lock serialises the two identities' ACL phases but does not order them, and the #324 c1 run shows the two executions overlapping wholly (allow window 0..148 ms, deny window 0..136 ms). A fix that closes only the deny-second ordering will pass some runs and fail others, which is indistinguishable from the current flake."
  - id: c4
    text: "concurrent_allow_and_deny_identities_do_not_interfere passes at retries 0 over N of at least 20 on an AppContainer-capable host"
    state: not-met
    owner: core
    note: "NOT BEING MET HERE, BY A RECORDED DECISION -- 2026-08-30, lane w3-windows-honesty. .planning/DECISIONS.md Q-368-honesty: the AppContainer ACL fix is NOT to be built. The standing decision is that Windows ships with no filesystem sandbox, the Job-object default is intended, and AppContainer is never to be chased again. What was open was the HONESTY of the claim, and that is what c6 closes. This criterion stays not-met and owned by core so it counts against the release as outstanding rather than disappearing; it becomes actionable only if the standing decision is reversed. Explicitly NOT superseded and NOT handed off: there is no other ticket for it, and marking it superseded would delete a measured defect from the release's arithmetic. The acceptance, carried verbatim from core#324 c2. THE HOST IS NOT A BLOCKER: #324 c1 reproduced this locally on SeanDesktop over OpenSSH in roughly one run in five at seconds per iteration, and refuted the older claim that it needed ferrox-win-msvc. N>=20 at one-in-five is a few minutes of loop. What it is blocked on is c1-c3 being written, not on access."
  - id: c5
    text: "The deny half stays non-vacuous: the deny child still exits non-zero with the secret absent from its stdout"
    state: not-met
    owner: core
    note: "NOT BEING MET HERE, BY A RECORDED DECISION -- 2026-08-30, lane w3-windows-honesty. .planning/DECISIONS.md Q-368-honesty: the AppContainer ACL fix is NOT to be built. The standing decision is that Windows ships with no filesystem sandbox, the Job-object default is intended, and AppContainer is never to be chased again. What was open was the HONESTY of the claim, and that is what c6 closes. This criterion stays not-met and owned by core so it counts against the release as outstanding rather than disappearing; it becomes actionable only if the standing decision is reversed. Explicitly NOT superseded and NOT handed off: there is no other ticket for it, and marking it superseded would delete a measured defect from the release's arithmetic. Its own criterion rather than a clause on c4, so it cannot be satisfied by inattention. A change that makes the allow arm pass by making the deny arm stop denying meets the letter of c4 and fails the issue - and that is the EASY wrong fix here, since the simplest way to stop a deny stripping a concurrent grant is to stop the deny doing anything. Baseline to preserve, from the #324 c1 instrumented run: `deny exit_code : 1` with `deny stdout : \"\"`. Denial is presently REAL and is the containment property the sandbox sells."
  - id: c6
    text: "The product STATES the concurrent-identity defect where an operator reads the containment posture, rather than leaving it visible only in a tracker"
    state: met
    evidence: "test:crates/wcore-cli/src/sandbox_cmd.rs::every_declaring_backends_disclosure_reaches_both_operator_arms"
    owner: core
    note: "Added 2026-08-30 by lane w3-windows-honesty as the half of this issue that a recorded decision leaves in scope (see c1-c5 and .planning/DECISIONS.md Q-368-honesty). REGRADED 2026-08-30 AFTER AN ADVERSARIAL REFUTATION PROVED THE FIRST GRADE VACUOUS, and the old evidence is named here rather than quietly swapped: this criterion was anchored to `every_declared_limitation_names_its_tracker_and_says_it_is_not_fixed`, which asserts two `const &str` are well formed. A verifier replaced `AppContainerBackend::known_limitations`'s body with `Vec::new()` on real Windows; it COMPILED, `sandbox status --json` returned `\"known_limitations\":[]` -- the disclosure gone from the only surface an operator reads -- and all 249 tests in wcore-sandbox stayed green, because NOTHING called that method. The sibling `WindowsJobObjectBackend` override was asserted, so the hole was exactly one backend wide. THE FIX IS THE SHAPE, NOT THE INSTANCE: enumerating the two known backends would close today's hole and leave a third backend free to reopen it, so the undecidable question (`did somebody remember to grade this backend?`) is replaced by two total ones -- (1) every `known_limitations` override in wcore-sandbox's `src/` must appear in `BACKENDS_THAT_DISCLOSE`, scanned from source by `wcore-sandbox/tests/declared_limitations_are_registered.rs` with a positive control on the scanner and on its two exclusions; (2) every row in that table must survive backend -> SandboxRegistry -> SandboxStatus -> BOTH arms of `sandbox status`, projected by the evidence test above, which PANICS on a row it has no arm for rather than skipping it. Adding a backend and wiring neither reddens (1); wiring (1) and not (2) reddens (2). RED ARM RE-RUN ON REAL WINDOWS 10.0.26200.9168 against this tree, 2026-08-30: the identical `Vec::new()` mutation, 1 site matched, `CHECK_EXIT=0` so it compiles, and the graded run is now `RED_EXIT=100` -- `wcore-cli sandbox_cmd::disclosure_tests::every_declaring_backends_disclosure_reaches_both_operator_arms` FAILED at sandbox_cmd.rs:316, `backend `appcontainer` is registered as declaring a known limitation and declared none`. Restored blob 7e80928bae56298664874b0a107bd8c2d6415021 == HEAD blob. GREEN ARM same host, 7 of 7 including the appcontainer projection. SUBSTITUTED-PROPERTY GUARD: what is graded is not that a constant exists but that an operator can READ it and act on it -- `SandboxBackend::known_limitations` is projected through `SandboxRegistry` into `wayland-core sandbox status`, in the human arm under `KNOWN LIMITATIONS of backend ...` and in the `--json` arm as `known_limitations`, and both arms are graded (`wcore_cli::sandbox_cmd::disclosure_tests`). The string names the defect, the measured host (Windows 11 build 26200), the observable consequence an operator would otherwise misattribute (`Access is denied.` on a granted path), its tracker, and that it is NOT fixed; the test pins those properties rather than the prose, so an honest rewording does not redden. LIVE EVIDENCE, real Windows: see the note on wayland-core-369.md c2 for the `sandbox status` transcript, which prints both declared limitations. What this does NOT claim: nothing about the race is fixed, and c1-c5 remain the harm. INDEPENDENTLY RE-DERIVED 2026-08-30 by the fix lane that resumed this branch, because a verdict inherited from the commit under review is not evidence. Same host (Windows 10.0.26200.9168), against a tree code-identical to HEAD -- `git diff --name-only 950511964..HEAD -- . ':(exclude).planning'` is EMPTY, so the Windows checkout at 95051196 carries this HEAD's code. The identical mutation matched MUTATION_SITES=1, CHECK_EXIT=0, and it is NOT a no-op: the shipped binary built from the mutated tree returned `\"known_limitations\":[]` from `sandbox status --json` under `WAYLAND_SANDBOX=appcontainer`. RED_TESTS_EXIT=100, failing at `crates\\wcore-cli\\src\\sandbox_cmd.rs:316` with `backend `appcontainer` is registered as declaring a known limitation and declared none`. Restore blob-verified: RESTORED_BLOB=7e80928bae56298664874b0a107bd8c2d6415021 == HEAD_BLOB, `git status --porcelain` empty. Green arm on the same host: 275 tests run, 275 passed, including all five `disclosure_tests` and both `declared_limitations_are_registered` arms. AND THE SHAPE CLAIM IS NOW MEASURED RATHER THAN NARRATED. Claim (1) above -- that a backend added later cannot declare a limitation nothing reads -- was asserted in prose and graded by nothing until this run. Red arm on Linux: a REAL `known_limitations` override added to a THIRD backend, `NoSandboxBackend` in crates/wcore-sandbox/src/backends/no_sandbox.rs, registered nowhere. CHECK_EXIT=0, and `crates/wcore-sandbox/tests/declared_limitations_are_registered.rs:117` FAILED with left `[backends/appcontainer/windows_impl/process.rs, backends/no_sandbox.rs, backends/windows_job_object.rs]` against right `[backends/appcontainer/windows_impl/process.rs, backends/windows_job_object.rs]`. Restored blob d8b0f5d9ad1c6a57cfe99ad40aee09d4a0c7d1d3 == the blob at origin/lane/f13-w3-windows-honesty. THE FIRST ATTEMPT AT THAT ARM WAS A FALSE GREEN AND IS RECORDED RATHER THAN DISCARDED: run with `-E 'test(disclosure_tests) or test(declared_limitations)'` it reported 6 passed and proved nothing, because `declared_limitations_are_registered` is a BINARY name and `test()` matches test NAMES -- the registration test never ran. `binary(declared_limitations_are_registered)` selects it. An empty selection reads exactly like a green. Claim (2) is graded by the appcontainer red arm above and, on Linux, by the same `Vec::new()` mutation applied to `WindowsJobObjectBackend`: CHECK_EXIT=0, REDA_TESTS_EXIT=100, sandbox_cmd.rs:316 naming `windows_job_object`, restored blob 2791968702281173a5a05b5baa2a06117412cd2a == the origin blob THE N+1 OF THIS FIX, FOUND AND CLOSED RATHER THAN REPORTED. The projection above is total over the BACKENDS that declare a limitation. One level up the same hole was still open: the operator's read is `SandboxStatus`, and a field could reach the struct and never reach the `--json` arm a host integration and every script read, with nothing grading that -- and FerroxLabs/wayland-core#400 c1 is about to add exactly such a field. `every_status_field_reaches_the_json_arm` inverts it the same way: not `did somebody remember to serialise this field`, undecidable over the fields nobody has added yet, but `is the JSON key set EQUAL to the struct's field set`, scanned from this file's own source with a positive control on the scanner (>= 8 fields, so a drifted `find` cannot compare two empty sets). RED ARM, measured not asserted: dropping the `binds_workspace_authority` key from `to_json` COMPILES (CHECK_EXIT=0) and gives REDC_TESTS_EXIT=100 at sandbox_cmd.rs:286, naming the missing key in the diff (left has `binds_workspace_authority`, right does not). Restored and blob-verified. THE HUMAN ARM IS NOT MADE TOTAL and that is stated in the test's own doc rather than left to be discovered: it renders labels (`binds cwd authority`), not field names, so field-name equality cannot decide it, and making it total needs one label table driving both arms -- a bigger change than an RC wants. The two fields this criterion turns on are graded on BOTH arms by `every_declaring_backends_disclosure_reaches_both_operator_arms` and `the_human_arm_prints_every_declared_limitation`. INDEPENDENTLY RE-DERIVED A SECOND TIME 2026-08-30 by the fix lane answering the adversarial refutation, on real Windows 10.0.26200.9168 against a tree PROVEN code-identical to hetzner HEAD by tree hash (045f406ba8ad01fa3b0b768e4144319e1e2511a4 on both hosts), because a verdict inherited from the commit under review is not evidence and a verdict inherited twice is worse. The verifier's exact mutation -- `AppContainerBackend::known_limitations` -> `Vec::new()`, MUTATION_SITES=1 -- compiles (CHECK_EXIT=0) and now gives RED_TESTS_EXIT=100, failing at crates\wcore-cli\src\sandbox_cmd.rs:418 with `backend `appcontainer` is registered as declaring a known limitation and declared none`. Before the fix that same mutation left 249 tests green. Restored blob 7e80928bae56298664874b0a107bd8c2d6415021 == HEAD blob. Green arm on the same host: 9 of 9. AND THE OUTCOME WAS CHECKED, not only the edit: the binary built from the restored tree returns both declared limitations from `sandbox status --json` under WAYLAND_SANDBOX=appcontainer (ISO_AC_EXIT=0), prints them under `KNOWN LIMITATIONS of backend `appcontainer`` in the human arm (ISO_AC_HUMAN_EXIT=0), and carries the `unavailable_reason` key (null, because AppContainer is healthy on that host); the windows_job_object default carries its own limitation on both arms (ISO_DEFAULT_EXIT=0). AND THE N+1 OF THAT FIX IS ITSELF CLOSED, after it was MEASURED rather than assumed absent. Making the read total for `known_limitations` left the identical hole open one trait method over, on `unavailable_reason` -- the field #369 c2 turns on. Red arm on this branch BEFORE the fix, on Linux: replacing `SandboxRegistry::unavailable_reason`'s delegate with `None` compiles (CHECK_EXIT=0) and leaves the WHOLE wcore-sandbox + wcore-cli suite green -- 3927 tests, RED1_TESTS_EXIT=0 -- while an operator's only read of a wedged backend's recorded cause disappears. DISCRIMINATING CONTROL, same file, adjacent function: the identical mutation on the `known_limitations` delegate reddens at sandbox_cmd.rs:401, CTL_TESTS_EXIT=100. So it was a coverage hole and not a broken instrument, and the cause was that every assertion on `unavailable_reason` built a `SandboxStatus` BY HAND and never traversed the registry. THE FIX IS AGAIN THE SHAPE: `DISCLOSURE_METHODS` names the disclosure SURFACE -- the trait methods whose whole value is that they are READ, so dropping one changes no behaviour and stays invisible -- and both totality checks now range over that set rather than over one method. Every override of every disclosure method under wcore-sandbox/src must be registered in `BACKENDS_THAT_DISCLOSE` against the METHOD it overrides, with a per-method positive control that the trait default AND the registry delegate both still exist; and every row must survive to both operator arms for every method it declares, projected through a REAL registry, with a row or a method that has no arm panicking rather than being skipped. THREE RED ARMS, each compiled first, on the fixed tree: (A) the registry delegate -> `None`, CHECK_EXIT=0, REDA_TESTS_EXIT=100, reddening BOTH the sentinel path arm (sandbox_cmd.rs:527) and the per-row equality (sandbox_cmd.rs:447) where it had been green over 3927 tests; (B) a real `unavailable_reason` override on a THIRD backend (NoSandboxBackend) registered nowhere, CHECK_EXIT=0, REDB_TESTS_EXIT=100, naming the exact (file, method) pair; (C) that row REGISTERED but given no arm, CHECK_EXIT=0, REDC_TESTS_EXIT=100. All restored and blob-verified. (A) was re-run on real Windows 10.0.26200.9168 as well: CHECK_EXIT=0, RED2_TESTS_EXIT=100 at sandbox_cmd.rs:527."
---

Split out of core#324, whose c1 measurement is done and recorded. This carries
the fix half. The verdict is that this is a PRODUCT race in ACE application, not
a fixture race: apply_protected_deny deletes EVERY AppContainer-package ALLOW ace
on the denied object -- its own and any concurrent identity's alike -- then sets
PROTECTED_DACL_SECURITY_INFORMATION so no inheritable package ALLOW can re-apply.
Denial by absence of a grant is per-OBJECT, and the object is shared, so it is
categorical across identities by construction.

This ledger entry exists because the remainder was decomposed onto an issue with
no ledger file, which means nothing could grade it and the next sweep would have
had to re-derive it from issue prose.

NOT TAKEN BY THE win-native lane, deliberately and not for lack of a host. This
is unsafe Win32 ACL code on the sandbox's containment boundary, where an
over-broad grant is a silent security regression that no test in this repo would
catch, and c2 and c3 are a design change to how AppContainer denial is applied
rather than a tightening of the existing code. It wants its own review.

It is the SOLE remaining blocker on core#350 c5 (a green nightly-windows-soak),
now that core#374 is closed.

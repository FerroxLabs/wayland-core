---
issue: 1214
repo: FerroxLabs/wayland
kind: defect
title: "Static context/output arms are live for host-variable open-weights ids, and model_output_ceiling ignores the provider entirely"
status: closed
last_verified_commit: 1798076f
criteria:
  - id: c1
    text: "The minimax-m* and deepseek-v4* arms either resolve per provider, or are removed under the rule host_variable_open_weights_stay_unknown already enforces for qwen"
    state: superseded
    successor: FerroxLabs/wayland#1232
    owner: core
    note: "SUPERSEDED, handed to FerroxLabs/wayland#1232, which is OPEN and which was filed for exactly this. OWNERSHIP RULING, 2026-08-30: three tickets contended for one piece of work -- wayland#1176 c5, this ticket's c1, and wayland#1232 -- and #1232 is the SINGLE OWNER. The reasoning, from the tickets' own texts rather than from convenience. This c1 says the minimax-m*/deepseek-v4* arms 'either resolve per provider, or are removed'; #1232's second acceptance bullet says 'For each of the twelve MiniMax/DeepSeek rows, a recorded decision: either the arm is removed (and the omit-max-tokens consequence stated and measured), or it is kept with the host spread written down and the reason it is safe despite the spread'. Those are the same work on the same twelve rows, and #1232 states it with the consequence attached, which this one does not. #1232 also carries the provenance this ticket got wrong: it says in as many words 'The arms predate #1176 ... #1176 did not add them', which the #1176 c5 note has since re-measured and confirmed. NOT MET, AND NOT PRETENDED OTHERWISE: the arms are still keyed on the model id alone -- `model_output_ceiling(_provider: &str, model: &str)` still ignores its provider parameter -- and the live gate run on 2026-08-30 named all seven violations, so the defect this criterion describes is real and present. It is simply #1232's to close, and the reason it is not closeable in passing is stated in #1232 and independently true: an arm REVOKES `should_omit_max_tokens`, so deleting one restores the provider's own natural ceiling and deleting the wrong one cuts real output. That is a product-behaviour change with its own evidence burden."
  - id: c2
    text: "The passthrough module doc's claim 'No open-weights family is listed here' is either true or removed"
    state: met
    evidence: "absent:crates/wcore-config/src/limits/passthrough.rs::No open-weights family is listed here"
    owner: core
    note: "CLOSED, and verified against the tree at 1798076fe rather than taken from the note that claimed it. The false sentence is GONE: `grep -n 'No open-weights family' crates/wcore-config/src/limits/passthrough.rs` returns nothing, and the known-positive control in the same call -- `grep -c 'open-weights'` on the identical path -- returns 5, so the query demonstrably reads the file and the absence is real rather than a silently-failing grep. WHAT REPLACED IT, quoted from passthrough.rs:49-51: `OPEN WEIGHTS, STATED HONESTLY. This module used to claim \"no open-weights ...\" ... and M3 rows and the DeepSeek V4 rows below are open-weights ids, served by`, and at :68 `Twelve of the rows below are open-weights ids. On the 2026-08-30 pull SEVEN`. The criterion offers two branches -- 'either true or removed' -- and the removed branch is the one taken, with an honest statement written in its place. The evidence token is deliberately an `absent:` anchor: it is the only form that goes red if the false claim is ever reinstated, which a `file:` or `symbol:` token would not."
  - id: c3
    text: "PASSTHROUGH_IN_SCOPE's minimax/deepseek floor patterns no longer instruct a release owner to add an arm for a family whose hosts disagree"
    state: met
    evidence: "file:scripts/check-model-limits-freshness.py:182:PASSTHROUGH_OPEN_WEIGHTS = {"
    owner: core
    note: "CLOSED. `PASSTHROUGH_OPEN_WEIGHTS = {'deepseek', 'minimax'}` at check-model-limits-freshness.py:182 is the set that suppresses the DEMAND arm for exactly these two families, and the comment block above it at :177-181 states the criterion's own concern in the criterion's own terms: `This set is what stops the REPORT arm below from automating new violations. PASSTHROUGH_IN_SCOPE is a set of FLOORS -- \"this generation and everything above it\" -- so without this, the first minimax-m4 or deepseek-v5 to appear on models.dev reddens the release with the instruction \"Add the arm if it has none, then add the row\", which is precisely the arm the rule forbids.` OBSERVED IN THE LIVE GATE, not just read in the source: the 2026-08-30 run printed `NOT DEMANDED (AGENTS.md rule 3): a NEW deepseek/minimax id whose hosts disagree by 2.0x or more is REPORTED, never failed. Those families publish their weights, model_output_ceiling is keyed on the id alone, and a static arm would hand the vendor's figure to every third-party host serving the same name.` The floor patterns at :165-166 are unchanged, which is correct -- the criterion asks that they no longer INSTRUCT a release owner to add an arm, not that the ids stop being in scope, and the instruction is what was removed. RE-ANCHORED 2026-08-30 on merge into integ/f13: the keystone merge that landed just before this one closed FerroxLabs/wayland#1198 and made a bare file:<path>:<line> a REFUSED token. Same line, now carrying the content -- the PASSTHROUGH_OPEN_WEIGHTS set head, which is the thing this criterion is about. The set literal's own quotes are left out of the fragment so the evidence stays a valid double-quoted YAML scalar; `PASSTHROUGH_OPEN_WEIGHTS = {` occurs exactly once in the file. No re-grade."
  - id: c4
    text: "The host spread measured here is reproduced against a fresh models.dev pull and recorded, so the decision is made on data rather than on the arms that happen to be present"
    state: met
    evidence: "file:scripts/check-model-limits-freshness.py:213:minimax-m2.5              65,536 ->     228,700   3.5x over 44 hosts"
    owner: core
    note: "CLOSED, and reproduced by this lane rather than inherited. THE MEASUREMENT WAS RE-RUN LIVE on 2026-08-30 on hetzner-dsm: `python3 scripts/check-model-limits-freshness.py` exit 0, `PASS -- every in-scope first-party model has an arm, and no arm over-claims`, with a REPORT block naming all seven rule-3 debts against a fresh models.dev pull. The figures reproduce the ones this ticket recorded on 2026-08-29 -- verbatim from today's run, `minimax-m2.5 is an OPEN-WEIGHTS id (minimax) with a STATIC ARM recorded at context 204800, while its hosts serve it at 65536 to 228700 (3.5x) across 44 endpoints`, plus deepseek-v4-flash 8.0x/61, deepseek-v4-flash-0731 5.1x/35, deepseek-v4-pro 8.2x/64, minimax-m2 5.1x/19, minimax-m2.1 5.1x/24, minimax-m3 4.0x/43. RECORDED, which is the second half of the criterion: `OPEN_WEIGHTS_ARM_DEBT` at :227-235 holds those exact seven ids, each dated 2026-11-30 and owned by `gh#1232`, keyed on the exact model id so listing one cannot excuse the next. `HOST_SPREAD_RATIO = 2.0` at :190 is the stated threshold with its own measurement written above it. RED ARM, 2026-08-30, tree committed at 1798076fe first: line 233 -- `\"minimax-m2.5\": (\"2026-11-30\", \"gh#1232\"),`, a dict-literal entry, printed before and after and confirmed to be the real table rather than one of the two self-test fixtures at :1030-1031 -- was deleted and the file touched. The LIVE gate went from exit 0 to exit 1 with `FAIL -- 1 model(s) we CLAIM to cover are missing or over-claimed` and, verbatim, `minimax-m2.5 ... It is not listed in OPEN_WEIGHTS_ARM_DEBT, so it is a NEW violation. Remove the arm, or state it as dated debt with an owning issue.` Exactly one model, so the arm is discriminating. Restored with `git checkout --`, touched, re-run: exit 0 and PASS again. RECORDED HONESTLY: the same mutation under `--self-test` stayed exit 0, because the self-test injects DEBT_OK/DEBT_OLD fixtures and structurally cannot see the real dict -- so `--self-test` is a VACUOUS instrument for this property and the live gate is the one that decides. RE-ANCHORED 2026-08-30 on merge into integ/f13 for FerroxLabs/wayland#1198, which made a bare file:<path>:<line> a REFUSED token. Same line, now carrying the OPEN_WEIGHTS_ARM_DEBT table head that records the measured host spread this criterion asks to be reproduced and written down. The fragment occurs exactly once in the file. No re-grade. RE-ANCHORED AGAIN 2026-08-30 by lane w3-cache-spend, and this one was GONE rather than moved, so the claim was re-verified before moving the anchor as the gate demands. wayland#1232 answered the seven debts by PROVIDER-SCOPING those arms instead of removing them, so OPEN_WEIGHTS_ARM_DEBT is now an empty typed dict and the old fragment OPEN_WEIGHTS_ARM_DEBT = { no longer exists. THE CRITERION STILL HOLDS: it asks that the host spread be reproduced against a fresh pull and RECORDED, and the recording is intact and untouched -- the MEASURED block at :204-215 still carries all seven ids with their 2026-08-30 figures and the claude-opus-5 1.0x/31-host control. The anchor now cites that measurement directly, which is what the criterion is about, rather than the debt dict that merely accompanied it; it is a stronger anchor because the dict was always going to move when the debt was discharged. Occurs exactly once. No re-grade."
---

Static context/output arms are live for host-variable open-weights ids, and `model_output_ceiling` ignores the provider entirely (`pub fn model_output_ceiling(_provider: &str, model: &str)`, limits.rs:38 -- the provider parameter is underscore-prefixed and unused). So the arm applies to EVERY host serving that id, not just the vendor. Measured against a live models.dev pull (2026-08-29, 4,425,612 bytes): `minimax-m2.5` resolves to (output 128,000, context 204,800) while digitalocean serves it at context 65,536, nebius caps output at 8,192, alibaba-coding-plan at 24,576, cloudferro-sherlock at 16,000, dinference at 32,000, tencent-coding-plan at 32,768. `deepseek-v4-pro` resolves to (384,000, 1,000,000) while deepinfra caps output at 16,384, frogbot at context 128,000 / output 8,192, crof and hpc-ai at ~128-131k output. Both directions of the #165 harm follow: `known_context_window` (compact.rs:455-461) returns the arm's window verbatim, so a run on digitalocean will not compact until 204,800 against a real 65,536 -- a 3.1x over-claim, the exact shape that killed a customer run at 178,336 tokens in #165; and per the freshness script's own words 'an arm REVOKES `should_omit_max_tokens`, so an over-claim is a hard 400 mid-run' (confirmed at engine.rs:1204-1215, which omits the wire field only when `model_output_ceiling(...).is_none()`).

**Where.** crates/wcore-config/src/limits.rs:306-314 (minimax arms) and the deepseek-v4 arm near :265; crates/wcore-config/src/limits/passthrough.rs (the minimax-m2/m2.1/m2.5/m2.7/m3 and deepseek-v4-pro/flash rows, plus the module-doc claim 'No open-weights family is listed here', which is false); scripts/check-model-limits-freshness.py:156 (PASSTHROUGH_IN_SCOPE minimax/deepseek floor patterns); contrast crates/wcore-config/src/limits/catalogue.rs:433 `host_variable_open_weights_stay_unknown`, which enforces the opposite policy for qwen.

**Why it matters.** AGENTS.md's third model-limits rule exists precisely to prevent this, and the codebase already enforces it for qwen with the stated threshold 'anything at or above the 200,000 CompactConfig default makes the small hosts WORSE than the status quo' -- minimax-m2.5's arm is 204,800, above DEFAULT_CONTEXT_WINDOW (200,000). The arms predate #1176, but #1176 now locks them in with a per-PR Rust equality assertion and grades them only against vendor-operated endpoints, which by construction cannot observe the host spread -- so the new guard certifies the violation as green. The forward-looking half is worse: the in-scope patterns are documented floors, so the release gate will redden on a future minimax-m4 or deepseek-v5 and instruct the release owner to 'Add the arm if it has none' -- automating the creation of new violations. Closest existing ticket is #1157 (closed), which covered the opposite, under-claim direction for these same two families; the over-claim-on-third-party-hosts direction appears unfiled (`gh search issues` for qwen3.6-27b / open-weights / host-variable returned []).

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.

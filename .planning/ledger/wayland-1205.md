---
issue: 1205
repo: FerroxLabs/wayland
kind: defect
title: "A cache ledger written by v0.13.9 reproduces #1163 verbatim on the fixed build, and the fix now certifies the fabricated saving as trustworthy"
status: closed
last_verified_commit: 93ede3424
criteria:
  - id: c1
    text: "LEDGER_SCHEMA is bumped, and a v1 row's uncached_equivalent_usd: 0.0 no longer decodes as a genuine priced zero"
    state: met
    evidence: "test:crates/wcore-agent/src/cache_ledger.rs::a_legacy_zero_counterfactual_does_not_read_back_as_a_priced_zero"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D23, found while verifying wayland#1163). NOW CLOSED -- the disposition is at the end of this note. The measured finding as filed, verbatim: Reading back any cache ledger written by v0.13.9 or earlier reproduces #1163 verbatim on the FIXED build, and adds a second false claim. `TurnSample::uncached_equivalent_usd` changed meaning from `f64` (0.0 = 'nothing could price it', v0.13.9 cache_ledger.rs:153, engine.rs:17129 `uncached_equivalent_usd: uncached.usd`) to `Option<f64>` (Some(0.0) = 'a genuine priced zero'), but `LEDGER_SCHEMA` was left at 1 — even though the module's own doc at cache_ledger.rs:72 says it is 'Bumped when a field's meaning changes'. `#[serde(default)]` therefore decodes every legacy `0.0` as `Some(0.0)`. Measured on hetzner with the freshly built binary sweep-targets/1163/debug/wayland-core against a hand-written legacy-format ledger (flux-router/flux-reasoning, cost_source=provider_reported, uncached_equivalent_usd: 0.0 — the exact shape v0.13.9 wrote, confirmed against `git show v0.13.9`): cache report → `F23_CACHE=cost usd=0.061389 uncached_equivalent_usd=0.000000 saving_usd=-0.061389 saving_ratio=unknown cost_truth=priced saving_truth=priced counterfactual_unpriced_round_trips=0` and NO saving_warning line. cache list → `F23_CACHE=total ... uncached_equivalent_usd=0.000000 cost_truth=priced`. cache verify → `trustworthy=true cost_truth=priced saving_truth=priced`, exit 0. That first line is character-for-character the report in the ticket body. The new `saving_truth=priced` is a regression in kind: the fix's own verdict now certifies the fabricated saving as trustworthy, which the pre-fix build never claimed. CLOSED 2026-08-30 by lane/f13-misc. LEDGER_SCHEMA is 2; `load` maps a v1 `uncached_equivalent_usd: 0.0` back to None (migrate_v1_counterfactual) rather than letting `#[serde(default)]` launder it into a priced zero, and the migration is read-only so a downgrade still finds its own file. A v1 turn whose counterfactual was a GENUINE priced zero is demoted to unknown by that, which is the safe direction: unknown renders as unknown, a false zero renders as a confident negative number. CLASS: `load` is the only reader in the product -- `list` calls it per file, `latest` calls `list`, and wcore-cli imports exactly those three -- so one migration point covers every verb. RED ARM (schema put back to 1, v1 arm removed from `load`; both mutations on executable code): `assertion `left == right` failed: v1 wrote 0.0 to mean `nothing could price this`; decoding it as Some(0.0) is the fabricated baseline #1163 is about / left: Some(0.0) / right: None`. Restored and touched: 24/24 green. Controls: a_legacy_priced_counterfactual_survives_the_migration proves it does not blanket-erase counterfactuals, and the pre-existing load_refuses_unknown_schema still refuses LEDGER_SCHEMA + 1. --- MERGED INTO integ/f13 2026-08-30 (lane/f13-dur-ledgers). Both lanes bumped LEDGER_SCHEMA to 2 and migrated v1 on read rather than refusing it. They disagreed on HOW MUCH of a v1 counterfactual to drop, and the incoming lane's wider migration WON ON EVIDENCE. misc demoted only Some(0.0), on the premise that a non-zero v1 counterfactual is unambiguous. v0.13.9's own source refutes that premise and was read to settle it: engine.rs:17129 wrote uncached_equivalent_usd: uncached.usd unconditionally, uncached came from resolve_turn_cost, and the comment immediately above the billed call in that same build says resolve_turn_cost reports priced = true for BOTH an exact catalog row and the ProviderCompat family fallback. No cost_source was recorded for the counterfactual, so a non-zero v1 figure may be a conservative family CEILING -- which is exactly what the v2 writer refuses to emit, because subtracting a real billed figure from a ceiling is not a measurement. Keeping it on read would have preserved the one figure the writer is forbidden to produce. So load now drops EVERY v1 counterfactual (migrate_v1_counterfactuals), sets migrated_from_schema, and keeps misc's laundered_counterfactual_round_trips counting the rows that actually LOST a figure rather than turns.len(). misc's closure is a strict subset of this and is not lost; what was inverted is its NARROWNESS control, and it was inverted on the quoted v0.13.9 source, not on preference. The over-correction that actually matters is still guarded: a_current_schema_session_with_an_unpriced_counterfactual_still_verifies keeps a CURRENT-schema session on an unlisted model verifying at exit 0. --- THE dur-ledgers LANE NOTE, PRESERVED: LEDGER_SCHEMA is 2. `load` migrates a v1 file rather than refusing it -- its billed cost_usd and cost_source did not change meaning and are worth showing -- but every v1 uncached_equivalent_usd becomes None. Dropped wholesale, not only the zeros: v0.13.9 wrote the field unconditionally as a bare f64, so on disk it conflates a catalog price, a provider-family CEILING and 0.0 meaning nothing could price it, and no v1 row carries the provenance to tell them apart. The ticket sanctions exactly this (`or refuse v1 rows for the saving`). A schema this build has never seen is still refused: test the_migration_does_not_make_load_accept_every_schema."
  - id: c2
    text: "cache report over a v0.13.9-format ledger no longer prints saving_truth=priced with a negative saving; the run output is quoted"
    state: met
    evidence: "test:crates/wcore-cli/tests/cache_ledger_cli.rs::a_v0_13_9_ledger_does_not_render_a_negative_saving_or_certify_it"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D23). NOW CLOSED -- the disposition is at the end of this note. The original measurement is on c1 and the file:line anchors are in the prose below. CLOSED 2026-08-30 by lane/f13-misc, ON THE SURFACE THE CRITERION NAMES -- the test drives the BINARY over a v1 fixture, because the operator in #1163 filed off a `cache report` and a library assertion is not that surface. MEASURED, both arms, binary md5 recorded: RED (afd594f65b586b49b92c1e04cc92a098) `F23_CACHE=cost usd=0.061389 uncached_equivalent_usd=0.000000 saving_usd=-0.061389 saving_ratio=unknown cost_truth=priced saving_truth=priced counterfactual_unpriced_round_trips=0` with no saving_warning line -- character for character the ticket. FIXED (7e15a56c8bae56c0fee911401e96ee55, and the restored build hashes identically, so the arms are provably different binaries): `F23_CACHE=cost usd=0.061389 uncached_equivalent_usd=unknown saving_usd=unknown saving_ratio=unknown cost_truth=priced saving_truth=unpriced counterfactual_unpriced_round_trips=1` plus `F23_CACHE=saving_warning text=no_catalog_rate_for_the_uncached_counterfactual`. NOTE for anyone re-running this: the first attempt read back `sessions=0` and proved nothing -- the hand-written ledger was refused for an unknown `retention` variant and `list` skips a malformed file silently, so the absence of a negative saving was the absence of any ledger. --- MERGED 2026-08-30. misc's CLI anchor is kept (it drives the binary over a v1 fixture and records both arms' md5). The incoming lane's a_v0_13_9_ledger_no_longer_reports_a_negative_saving_as_priced is in the tree and green beside it, and both quote the same measured BEFORE line. See c1 for the migration breadth. --- THE dur-ledgers LANE NOTE, PRESERVED: Run quoted, shipped debug binary on hetzner over the v0.13.9-format ledger the ticket measured. BEFORE: `F23_CACHE=cost usd=0.061389 uncached_equivalent_usd=0.000000 saving_usd=-0.061389 saving_ratio=unknown cost_truth=priced saving_truth=priced counterfactual_unpriced_round_trips=0` with no saving_warning. AFTER: `F23_CACHE=cost usd=0.061389 uncached_equivalent_usd=unknown saving_usd=unknown saving_ratio=unknown cost_truth=priced saving_truth=unpriced counterfactual_unpriced_round_trips=1 provider_reported_round_trips=1 catalog_priced_round_trips=0 estimated_round_trips=0 unpriced_round_trips=0` plus `F23_CACHE=saving_warning text=no_catalog_rate_for_the_uncached_counterfactual saving_truth=unpriced counterfactual_unpriced_round_trips=1`. The billed figure is unchanged, which is the point: provider-reported spend is still spend."
  - id: c3
    text: "cache list's store total does not sum a legacy 0.0 into the counterfactual, and cache verify does not return trustworthy=true for it"
    state: met
    evidence: "test:crates/wcore-cli/tests/cache_ledger_cli.rs::a_legacy_ledger_is_not_summed_into_the_store_total_and_does_not_pass_verify"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D23). NOW CLOSED -- the disposition is at the end of this note. The original measurement is on c1 and the file:line anchors are in the prose below. CLOSED 2026-08-30 by lane/f13-misc. The same test asserts both halves of this criterion: `cache list`'s store total renders `uncached_equivalent_usd=unknown` (StoreTotals sums the same field, so one legacy session would otherwise poison the whole directory), and `cache verify` does not report `saving_truth=priced` while still reporting `cost_truth=priced` -- the spend is a fact the provider reported, only the saving is unknown. Measured at the CLI on both arms; the RED figures are quoted on c2. REFUTED 2026-08-30 by the partials verifier, CONCEDED IN FULL, AND NOW GENUINELY MET ON BOTH HALVES. The concession first, because it is the named failure mode and it was this lane: c3 is a CONJUNCTION and only its FIRST conjunct was graded. The second -- `cache verify does not return trustworthy=true for it` -- was FALSE at HEAD, and the committed test said so in its own comment (`verify may still call the SPEND trustworthy -- the provider reported it -- but it must not call the SAVING priced`): it asserted `saving_truth != priced` and never touched `trustworthy`. The note above graded that substitute against c3`s sentence. The verifier`s measurement is correct and reproduces: `cache_cmd.rs` printed `truth.is_trustworthy()` off `cost_truth` alone, so a v1 ledger returned `F23_CACHE=verify trustworthy=true cost_truth=priced` and exit 0. WHY IT WAS SATISFIABLE AFTER ALL, checked before fixing rather than assumed: the guard the old comment defended is about an UNPRICED COUNTERFACTUAL IN GENERAL (a v2 session on an unlisted model must not start failing CI). c3`s antecedent is narrower -- `a legacy 0.0` -- and nothing in the ticket`s own fix direction (`bump LEDGER_SCHEMA to 2 and have the v1 reader map 0.0 to None`) forbids refusing to CERTIFY such a file. So the criterion was never unsatisfiable; it was unmet. THE FIX: `migrate_v1_counterfactual` now counts the rows it demotes into `CacheLedger::laundered_counterfactual_round_trips` (`#[serde(skip)]` -- it is a fact about this READ, not about the file, and writing it back would make a v2 ledger claim a history it does not have); `summarize` carries it; and `verify` computes `trustworthy = truth.is_trustworthy() && laundered == 0`, exiting a NEW `EXIT_LEGACY_SCHEMA = 9` kept distinct from `EXIT_COST_NOT_TRUSTWORTHY = 7` because the operator`s remedy differs -- a legacy file cannot be repaired by pricing anything, only by re-running the session. MEASURED AT THE BINARY on the verifier`s own fixture shape (schema 1, flux-router/flux-reasoning, cost_source=provider_reported, uncached_equivalent_usd: 0.0), binary md5 efea2df8b5ff8fcd25cc6b2decf2b42c: `F23_CACHE=verify trustworthy=false cost_truth=priced saving_truth=unpriced laundered_counterfactual_round_trips=1 provider_reported_round_trips=1 catalog_priced_round_trips=0 estimated_round_trips=0 unpriced_round_trips=0 cost_usd=0.061389 session_complete=true session=legacy-sess`, exit=9 -- and on the SAME fixture `cache list` still prints `uncached_equivalent_usd=unknown` in the store total, so both conjuncts are shown on one run rather than one each. THE OLD COMMENT`S GUARD IS INTACT AND NOW HAS A TEST: the refusal keys on rows actually DEMOTED, not on an unpriced counterfactual, so a current-schema session on an unlisted model still verifies. `a_current_schema_session_with_an_unpriced_counterfactual_still_verifies` writes a v2 turn with the field ABSENT and asserts trustworthy=true, exit 0 -- and asserts its own premises first (saving_truth=unpriced, laundered_counterfactual_round_trips=0) so it cannot pass by drifting into the wrong case. RED ARM: with `&& laundered == 0` deleted from `verify`'s trustworthy computation -- mutation on EXECUTABLE code in cache_cmd.rs, tree committed clean first, `touch`ed after both the mutation and the restore -- `assertion `left == right` failed: wayland#1205 c3: a ledger whose counterfactual meaning this build had to GUESS is not certifiable, however good its billed figure is / left: \"true\" / right: \"false\"` at cache_ledger_cli.rs:978, on both tries. The control `a_current_schema_session_with_an_unpriced_counterfactual_still_verifies` stayed GREEN in the SAME arm, which is what makes this a red arm on this criterion rather than on `verify` in general. Restored, touched, tree clean, green. ROUND-2 SECONDARY FINDING 2026-08-30 -- THE NARROWNESS WAS UNGRADED, AND IS NOW GRADED. The verifiers over-correction probe moved `laundered += 1` outside the demotion `if` so the counter counted EVERY v1 turn, and all 19 CLI tests plus the unit tests stayed GREEN: `verify` could have been changed to refuse every pre-schema-2 ledger with nothing objecting, which is a worse regression than #1163. Two tests now object, one at the source and one at the certification surface: `cache_ledger::tests::the_launder_count_counts_only_the_turns_the_migration_demoted` drives a MIXED v1 file (one zero, one real 0.05 figure), asserts both premises -- turn 0 demoted, turn 1 not -- then asserts the count is 1; and `cache_ledger_cli::a_legacy_ledger_whose_counterfactual_survived_the_migration_still_verifies` drives a v1 ledger whose counterfactual is a real 0.09 and asserts `laundered=0`, `saving_truth=priced`, `trustworthy=true`, exit 0. RED ARM on that mutation (tree committed clean at 40fde5c8c, mutation on executable code, `touch` after mutation and after restore, tree [] after): the unit test panicked at cache_ledger.rs:1597, and the CLI test printed the exact harm -- `F23_CACHE=verify trustworthy=false cost_truth=priced saving_truth=priced laundered_counterfactual_round_trips=1 ...` on a legacy ledger that lost nothing on load. The two pre-existing migration tests (`a_legacy_priced_counterfactual_survives_the_migration` and `a_legacy_zero_counterfactual_does_not_read_back_as_a_priced_zero`) stayed GREEN in that same arm, confirming they were the vacuous pair the verifier named. --- MERGED 2026-08-30. The anchor moves to the incoming lane's test, which grades c3's two conjuncts on a store holding a legacy AND a current session, so the store-total assertion cannot pass on an empty directory. verify on the merged tree prints BOTH sides' fields -- misc's laundered_counterfactual_round_trips (the reason) and the lane's legacy_schema (the basis) -- and certification is truth.is_trustworthy() AND NOT ledger.is_migrated(). The EXIT CODE is misc's dedicated EXIT_LEGACY_SCHEMA = 9, not the incoming 7: exit 7 says the billed figure is not spend, 9 says the billed figure is fine but the file's field meanings predate this build, and that distinction is worth keeping. The incoming test's code(&v) == 7 was updated to 9 for that reason and for no other. misc's a_legacy_ledger_whose_counterfactual_survived_the_migration_still_verifies is now a_legacy_ledger_with_a_nonzero_counterfactual_is_refused_too -- see c1 for the v0.13.9 source that forced the inversion. --- THE dur-ledgers LANE NOTE, PRESERVED: Both halves, run quoted. `cache list` over a store holding one legacy and one current session asserts `F23_CACHE=total sessions=2 ... uncached_equivalent_usd=unknown cost_truth=priced` -- the legacy zero is not summed in, it makes the store counterfactual unknown. (An earlier revision of this note quoted `sessions=1` here; that figure was copied from a single-session manual run and did not match the two-session store the sentence describes. The value above is the one the named test asserts.) `cache verify` exits 7 with `F23_CACHE=verify trustworthy=false cost_truth=priced saving_truth=unpriced legacy_schema=1 ...`; trustworthy is now `cost_truth().is_trustworthy() && !ledger.is_migrated()`, because a file whose field meanings were reinterpreted on read must be reported and not certified. The test carries a control: the current-schema session beside it still verifies `legacy_schema=none` at exit 0, so verify has not simply been broken for everything. Shown RED: dropping the `&& !ledger.is_migrated()` clause gives `assertion `left == right` failed: a migrated ledger is reported, never certified: left: "true" / right: "false"`."
  - id: c4
    text: "A test reads a fixture ledger in the v1 on-disk shape and asserts the verdict; shown RED against today's #[serde(default)] decode"
    state: met
    evidence: "test:crates/wcore-agent/src/cache_ledger.rs::a_v1_row_does_not_decode_its_zero_counterfactual_as_a_priced_zero"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D23). NOW CLOSED -- the disposition is at the end of this note. The original measurement is on c1 and the file:line anchors are in the prose below. CLOSED 2026-08-30 by lane/f13-misc. The fixture is built in the v1 ON-DISK shape (legacy_v1_ledger_json: schema 1, flux-shaped turn, `uncached_equivalent_usd` written unconditionally as a bare 0.0) and read back through the real `load`, so the decode is in the assertion's path rather than being modelled. Shown RED against the `#[serde(default)]` decode -- the panic is quoted on c1. SEPARATELY: the CLI suite's own fixtures were pinned at a literal `\"schema\": 1`, so from the moment the version was bumped every one of them was a legacy file exercising the migration path while claiming to grade the current one -- and passing either way. They now stamp cache_ledger::LEDGER_SCHEMA, and the v1 shape is a separate, explicit writer. --- MERGED 2026-08-30. The anchor moves to the incoming lane's fixture test, which hand-writes the v1 on-disk bytes rather than serializing from the current struct -- the point of the fixture being that the struct changed underneath it. misc's a_legacy_zero_counterfactual_does_not_read_back_as_a_priced_zero anchors c1 and is green; the two fixture builders collided by name at merge and the incoming one was renamed handwritten_v1_ledger_json. Both are exercised through the real load. --- THE dur-ledgers LANE NOTE, PRESERVED: Reads a hand-written fixture in the v1 ON-DISK shape (schema 1, uncached_equivalent_usd a bare 0.0, cost_source provider_reported) rather than one serialized from the current struct -- the whole point is that the struct changed underneath it. Shown RED against the #[serde(default)] decode: with the turns loop in migrate_v1_counterfactuals removed, `assertion `left == right` failed: the v1 zero is not a price / left: Some(0.0) / right: None`."
---

Reading back any cache ledger written by v0.13.9 or earlier reproduces #1163 verbatim on the FIXED build, and adds a second false claim. `TurnSample::uncached_equivalent_usd` changed meaning from `f64` (0.0 = 'nothing could price it', v0.13.9 cache_ledger.rs:153, engine.rs:17129 `uncached_equivalent_usd: uncached.usd`) to `Option<f64>` (Some(0.0) = 'a genuine priced zero'), but `LEDGER_SCHEMA` was left at 1 — even though the module's own doc at cache_ledger.rs:72 says it is 'Bumped when a field's meaning changes'. `#[serde(default)]` therefore decodes every legacy `0.0` as `Some(0.0)`. Measured on hetzner with the freshly built binary sweep-targets/1163/debug/wayland-core against a hand-written legacy-format ledger (flux-router/flux-reasoning, cost_source=provider_reported, uncached_equivalent_usd: 0.0 — the exact shape v0.13.9 wrote, confirmed against `git show v0.13.9`): cache report → `F23_CACHE=cost usd=0.061389 uncached_equivalent_usd=0.000000 saving_usd=-0.061389 saving_ratio=unknown cost_truth=priced saving_truth=priced counterfactual_unpriced_round_trips=0` and NO saving_warning line. cache list → `F23_CACHE=total ... uncached_equivalent_usd=0.000000 cost_truth=priced`. cache verify → `trustworthy=true cost_truth=priced saving_truth=priced`, exit 0. That first line is character-for-character the report in the ticket body. The new `saving_truth=priced` is a regression in kind: the fix's own verdict now certifies the fabricated saving as trustworthy, which the pre-fix build never claimed.

**Where.** crates/wcore-agent/src/cache_ledger.rs:72 (LEDGER_SCHEMA = 1, not bumped) and :173-174 (`#[serde(default, skip_serializing_if)] pub uncached_equivalent_usd: Option<f64>`); rendered at crates/wcore-cli/src/cache_cmd.rs:619-645. Doc comment at cache_ledger.rs:170-172 acknowledges the decode ('a 0.0 read back from a ledger an older build wrote decodes as Some(0.0), which is what that build meant by it') — but that is not what the old build meant by it: v0.13.9 wrote 0.0 precisely when nothing could price the model.

**Why it matters.** The cache ledger is a durable on-disk artifact whose stated purpose (cache_ledger.rs:22-24) is that a separate process can read it back without an engine. #1163 was itself filed off a persisted 0.13.9 session. So the operator who reported this bug, on upgrading, runs `cache report` over the ledger directory they already have and sees the identical negative saving — now graded `saving_truth=priced`. One legacy session also poisons `cache list`'s store total, since StoreTotals sums Some(0.0) happily. Fix is bounded: bump LEDGER_SCHEMA to 2 and have the v1 reader map `uncached_equivalent_usd: 0.0` to None (or refuse v1 rows for the saving), which is what the module's own schema rule already prescribes.

Criteria are taken verbatim from the issue's Acceptance section.

All of them are now met, closed by lane/f13-misc on 2026-08-30 alongside the
ticket they were found under. Each criterion's note carries its own evidence
and its own red arm. The GitHub issue is left OPEN deliberately: closing an
issue is not this lane's action.

c3 was graded `met` on 2026-08-30 with only its FIRST conjunct satisfied, and
the adversarial verifier refuted it the same night. The concession, the
reasoning that the second conjunct was satisfiable rather than unsatisfiable,
the fix, and the binary-level measurement of BOTH halves on one run are all on
c3's note. Recorded here too because a conjunction graded on one half is the
failure mode this ledger exists to catch, and it is worth being able to find
without reading the note.

---
issue: 1284
repo: FerroxLabs/wayland
kind: defect
title: "Flaky on macOS only: the_live_backend_timeout_bounds compares two wall-clock samples taken under different load"
status: open
last_verified_commit: 5b76ec723
criteria:
  - id: c1
    text: "The test stops deciding on a ratio between two wall-clock samples taken at different moments under different load, or its allowlist entry is deleted because the ratio was measured stable."
    state: met
    evidence: "test:crates/wcore-tools/tests/bash_manifest_bound_live_backend.rs::the_live_backend_timeout_bounds_the_manifest_build_and_names_it"
    owner: core
    note: "MET 2026-09-10 by the w15/mac lane. THE CRITERION IS THE RATIO, AND THE RATIO IS GONE FROM THE DECISION PATH -- verified by reading the tree rather than by trusting the previous grading. `the_live_backend_timeout_bounds_the_manifest_build_and_names_it` contains no `Instant`, no `elapsed`, and no comparison between two wall-clock samples; `timer_allowance`, its only caller, is deleted. `walk` survives ONLY as an input that SETS UP the condition (`timeout_ms = (walk / 10).max(1)`) and as text in diagnostics -- nothing in the file compares it to anything. What is asserted now is three non-durations: (a) THE BOUND, as an event -- `contains(\"timed out\") && contains(\"manifest\")`, a string with exactly one producer, the `Err(_)` arm of `timeout_at(deadline, build)` in bash.rs, reachable only with the manifest build still outstanding; (b) NO CHILD RAN, as an observation -- `CHILD_TOKEN` can only enter the result through the child's stdout, and the second call (same command, same policy, same posture, default budget) is its POSITIVE CONTROL; (c) anti-vacuity -- `secret_deny_walk_count() >= 1`, read AFTER the control call. Load can now only make the walk slower, which makes the deadline fire during the build MORE reliably, so the remaining failure direction is one-sided. WHAT THE PREVIOUS GRADING SAID WAS OWED AND IS NOW DISCHARGED: item (1), the FIRST COMPILE AND FIRST EXECUTION of the rewrite. `WCORE_PROOF_SLOT=default python3 tools/remote-proof.py worktrees/w15-allow nextest run -p wcore-tools --test bash_manifest_bound_live_backend --retries 0 --no-fail-fast` at 9791f19c0 on hetzner-dsm: `3 tests run: 3 passed, 0 skipped`, all three in 5.037s, receipt `{\"remote_exit\": 0, \"complete\": true, \"tree\": \"e124b9c555238752ae4654ab31eada15b873a695\"}`. The host reports `host backend = bubblewrap (enforces read-deny)`, so the test genuinely executed rather than taking its `enforcing_host()` skip. That is also the first exercise of the new positive control, which depended on the child's stdout reaching `ToolResult.content` under a bare `contained` policy and had never run. LIMITS, stated rather than buried. (i) NO FAIL-BEFORE ARM WAS RUN IN THIS PASS. v0.13.4 (0ccaa90b) predates the remote-proof bundle anchor 0cdd998ad, so that commit cannot be built through the only Linux route this lane has; the module header's claim that both new assertions red there is REASONED (the header records `timeout message names a cause: no` at v0.13.4, and the child ran after the walk) and is NOT re-measured here. The internal positive controls -- the CHILD_TOKEN control call and the `>= 1` walk-count floor -- are what stand against vacuity in the meantime, and they are real: each one reds on its own if the instrument dies. (ii) THIS IS A LINUX GREEN. macOS is where the five retry-masked failures were observed and macOS cannot be executed from this lane, so `--retries 0` on native macOS is still owed -- but it is owed for the ALLOWLIST DELETION, which is the criterion's SECOND branch, not for this one. Line 65 of .config/flaky-allowlist.txt must NOT be deleted on the strength of this row; its expiry is 2026-10-01 and the population arm behind it is unchanged. (iii) The criterion is about the test's decision path, and that is what is graded. It does not claim the macOS runner stopped being slow."
---

# A ratio test on a shared runner

Recorded so the 0.13.12 coverage gate has a ledger for it. The repair is 0.13.13 work.

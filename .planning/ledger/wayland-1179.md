---
issue: 1179
repo: FerroxLabs/wayland
kind: defect
title: "Absolute context buffers saturate to zero on a small served window, so a learned window cannot be used to compact"
status: open
last_verified_commit: f96d1933
criteria:
  - id: c1
    text: "input_ceiling() returns a positive value on a small learned or configured window instead of saturating to zero"
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1179_small_window_buffers_test.rs::a_4k_window_cannot_be_compacted_into_and_says_so"
    owner: core
    note: "CompactConfig::scaled_reserves with MAX_RESERVE_FRACTION = 0.55 replaces the absolute buffers below a 60,000 crossover. At 4,096 the ceiling goes 0 -> 2,527, so the saturation is gone."
  - id: c2
    text: "Across the 8k-32k band #1179 scopes itself to, the autocompact threshold sits above core's own baseline turn rather than below it"
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1179_small_window_buffers_test.rs::every_window_in_the_8k_to_32k_band_clears_the_baseline_turn"
    owner: core
    note: "At 8,192 threshold=3,688 vs BASELINE_TURN_TOKENS=3,118. At 4,096 the threshold is still below the baseline turn and that is handled by REFUSAL, not pretence: supports_compaction(4_096) is false, so core never narrows its guard onto it. REFUTED 2026-08-29 by the 0.13.12 close-sweep, recorded verbatim: Holds for the LEARNED window only. The cited test (an_8k_window_compacts_usefully, 3,688 > 3,118) passes, and the learned path is genuinely gated: engine.rs:8473 narrow_to_served_window refuses any served window where supports_compaction is false. But `supports_compaction` is consulted at exactly ONE call site — the learned-window narrowing. An OPERATOR-CONFIGURED `[compact] context_window` bypasses the gate entirely: autocompact_threshold_now() takes effective_context_window as its base and only the learned narrowing is gated. Measured against the shipped code (out-of-tree scratch binary, no repo edit): configured w=6,000 -> threshold 2,700, w=6,144 -> 2,765, w=6,928 -> 3,118 — all at or below BASELINE_TURN_TOKENS=3,118, so should_autocompact fires on an empty conversation and the run opens every turn with an LLM summarization. That is verbatim the second brick the ticket names. Worse, in the band 4,456..6,928 the PRE-#1179 code did NOT fire on the baseline turn (the #1150 fallback gave 0.70*w: 4,200 at 6,000, 4,300 at 6,144) while the new code does — the compaction half of that band went backwards even though the guard half improved. The ledger note ('At 4,096 the threshold is still below the baseline turn and that is handled by REFUSAL ... supports_compaction(4_096) is false') is true of the learned path and false of the configured path, and c1's own wording ('learned or configured') plus the ticket's acceptance sentence ('a learned or configured small window produces a positive input_ceiling() AND an autocompact threshold above the baseline turn') both cover the configured path. No test in the file exercises a configured window between 4,096 and 8,192. RE-GRADED 2026-08-30 and the previous note was TRUE OF THE LEARNED PATH ONLY. The issue's acceptance sentence says 'a learned OR CONFIGURED small window', and supports_compaction was consulted at exactly one call site - AgentEngine::narrow_to_served_window, where #1172's learned figure is admitted. An operator's `[compact] context_window` reached effective_context_window and met no gate at all. REPRODUCED at e7144c30a with a new test in the same file: `window 4096: an OPERATOR-CONFIGURED window core has already judged too small to compact in summarized a conversation that had not started - threshold 1844, baseline turn 3118`, and the sweep failed at the first window it tried, `window 1024: ... threshold 462`. Every configured window from 1,024 to 6,928 fired on the baseline turn; in 4,455..6,928 that is strictly WORSE than the MIN_AUTOCOMPACT_WINDOW_FRACTION = 0.70 fallback #1179 replaced (6,000 -> 4,200 then, 2,700 now). FIX: the refusal is a property of the window, not of the route it arrived by, so it moved into CompactConfig::should_autocompact_at and BOTH triggers go through it - compact/auto.rs::should_autocompact and AgentEngine::should_autocompact_now, the latter via a new compaction_window_now so the threshold's VALUE and the trigger's DECISION cannot end up on different windows. Two call sites found, two changed. The threshold VALUE is untouched (the cache ledger and the context gauge still report it) and emergency_limit is untouched. A permanently silent refusal is indistinguishable from compaction being broken, so bootstrap.rs now emits it once on emit_info - the same surface and the same #1130 reasoning as the unknown-window notice it sits next to. EVIDENCE: no_configured_window_anywhere_fires_on_the_baseline_turn sweeps 1,024..=16,384; a_configured_window_too_small_to_work_in_never_fires pins 4,096/5,000/6,000/6,144/6,928 with preconditions asserting each is in the refused band; the_first_workable_configured_window_still_fires_when_it_should is the negative control at 6,929 (3,118 does not fire, 3,119 does, u64::MAX does) so 'never fires' cannot be satisfied by switching autocompact off; a_configured_window_too_small_to_compact_in_is_announced and a_workable_configured_window_is_not_announced_as_too_small cover the notice. RESIDUAL, stated rather than left to be discovered: below the 6,929 crossover the threshold VALUE still computes below the baseline turn - what changed is that it is no longer ACTED ON, by either trigger, ever. The issue endorses exactly that for the unworkable band in its own words (at a 4,096 slot no compaction strategy can work: core baseline turn alone is 3,118 real tokens. The honest remedy there is the operator raising the server context length), and it scopes itself to the band where a learned window is small but workable - say 8k-32k, where the threshold does sit above the baseline turn as the criterion words it (8,192 -> 3,688). The number is still reported to the cache ledger and the context gauge, which is why autocompact_threshold_for_window stayed a plain number. EVIDENCE RUN: all 11 tests in issue_1179_small_window_buffers_test.rs green RE-ARMED 2026-08-30 at the merged tip (origin/integ/f13 a278f8c3 merged in, 68 commits): the gate is CompactConfig::should_autocompact_at, and deleting the single line '&& self.supports_compaction(window)' from it - git diff -U0 read back, one deletion at compact.rs:641, a CODE line inside the boolean expression, no comment touched, file touched after mutate and after restore - turns the evidence test RED on both tries: 'window 1024: a configured window fires an LLM summarization on core's own 3118-token baseline turn, before the user has typed anything - threshold 462' (issue_1179_small_window_buffers_test.rs:385), together with its sibling 'window 4096: an OPERATOR-CONFIGURED window core has already judged too small to compact in summarized a conversation that had not started - threshold 1844, baseline turn 3118' (:317). Restored, touched, re-run: 17 tests run: 17 passed, 0 skipped., all 6 in issue_1150_unknown_context_window_test.rs green, wcore-agent --lib 2,614 passed. REFUTED 2026-08-30 by the partials verifier and CONCEDED: the evidence token could not fail on this criterion's own sentence. should_autocompact at the baseline turn is `supports_compaction(w) && BASELINE >= threshold(w)`, i.e. `threshold > 3118 && 3118 >= threshold` - unsatisfiable for EVERY window and EVERY threshold function while the refusal gate stands, so no change to the threshold can redden it. REPRODUCED INDEPENDENTLY at fbad9212e: MAX_RESERVE_FRACTION 0.55 -> 0.90 (git diff -U0 read back, one CODE line at compact.rs:114, a const item and not a comment, file touched after mutating and after restoring) drives the 8,192 threshold from 3,688 to 820, far below the 3,118 baseline turn and a flat violation of this criterion, and no_configured_window_anywhere_fires_on_the_baseline_turn still PASSED while 6 of the file's other 11 tests went red. That token grades the GATE's presence and wiring, not the threshold; it has moved to c6, which is the claim it can actually fail on. TEXT REWRITTEN 2026-08-30 - the `text:` FIELD above, not merely this note. The old sentence, read universally over 'a small window', is UNSATISFIABLE under this ticket's own fix direction: at 4,096 the input ceiling is 2,527, so a threshold above the 3,118 baseline turn would sit above the ceiling and break P3, and the issue itself says the honest remedy at 4,096 is the operator raising the served window. The property actually wanted, and actually delivered, is the one the issue scopes itself to in its own words - 'the band where a small window is still workable, roughly 8k to 32k'. NEW EVIDENCE: every_window_in_the_8k_to_32k_band_clears_the_baseline_turn sweeps the FIXED range 8_192..=32_768 and compares each threshold against the BASELINE_TURN_TOKENS constant. The band is fixed rather than derived deliberately: phrasing it as 'wherever supports_compaction holds' would be a TAUTOLOGY, because that predicate IS `autocompact_threshold_for_window(w) > BASELINE_TURN_TOKENS`, which is how the first version of this criterion became ungradeable in the first place. Its non-vacuity pin is MEASURED, not derived - the obvious 570 at 8,192 is WRONG and asserting it failed on the first run: the f64 scale truncates, so the threshold is not monotone token-by-token and 8,193 is one token tighter, at 569. RED ARM at f96d1933e: the same 0.55 -> 0.90 mutation that defeated the old token fails the new one at issue_1179_small_window_buffers_test.rs:304 with 'window 8192: threshold 820 is at or below the 3118-token baseline turn, so on the very band #1179 scopes itself to, compaction fires on an empty conversation, every turn, forever'. Restored, touched, re-run: 15 tests run: 15 passed, 0 skipped."
  - id: c3
    text: "Behaviour is measured at the 4k, 8k, 32k, 60k and 200k window points rather than derived from a chosen fraction"
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1179_small_window_buffers_test.rs::the_trigger_stays_below_the_ceiling_at_every_window"
    owner: core
    note: "One named test per point - 4k, 8k, 32k, 60k, 200k - each asserting exact pinned (threshold, ceiling) pairs, plus this sweep over 1,024..=262,144 step 512 so the ordering is not merely true at the five samples."
  - id: c6
    text: "The scaled output_reserve does not decouple from the max_tokens core actually asks for"
    state: met
    evidence: "test:crates/wcore-agent/src/engine.rs::the_output_ask_fits_inside_the_window_actually_in_force"
    owner: core
    note: "ADDED 2026-08-29 (sweep finding D37): scaling output_reserve down with the window left size_output_cap sizing max_tokens from the CATALOGUED window (or UNKNOWN_CAP = 8,192 for an unlisted model), so the pre-flight ceiling certified inputs that cannot fit alongside the ask. Measured on #1172 endpoint (unlisted model, openai-compat, so the wire field IS sent): learned window 8,192 -> ceiling 5,053, reserve 2,730, max_tokens sent 8,192, total ask 13,245 on an 8,192 slot. Before #1179 the flat 20,000 reserve always exceeded any UNKNOWN_CAP or catalogued output ceiling, so this could not happen. FIXED: size_output_cap takes the window IN FORCE (the same resolve_preflight_window reconciliation the #255 guard used one block earlier) and clamps the ask to the room left in it, exactly as the known-model arm already did against the catalogued window. Identity wherever the window in force IS the catalogued one, which is every registry model absent a narrowing - pinned by a_window_in_force_that_is_the_catalogued_one_changes_no_sizing over opus/gpt-4o-mini/gemini, so the naive formulation (clamp to output_reserve) that would have cut a 128k-output model to 20,000 cannot come back. RED ARM (hetzner-dsm, 2026-08-29): replacing the clamp with `let _ = window_in_force; sized` - printed back to prove it landed on the match expression - gives `assertion left == right failed / left: 8192 / right: 4562`, i.e. exactly the 8,192-on-an-8,192-slot ask. Restored, touched, green."
  - id: c4
    text: "A test at each of those points distinguishes compacts usefully from fires every turn"
    state: met
    evidence: "symbol:crates/wcore-agent/tests/issue_1179_small_window_buffers_test.rs::assert_compacts_usefully"
    owner: core
    note: "The P1..P4 predicate applied at every point: threshold > baseline turn, ceiling > baseline turn, threshold < ceiling, ceiling < emergency, plus !should_autocompact(BASELINE_TURN_TOKENS) - literally compacts usefully versus fires every turn. Read through the same functions the engine enforces with."
  - id: c5
    text: "The 33k-110k band does not regress: a pinned 60000 window keeps a threshold below its own pre-flight shed ceiling"
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1179_small_window_buffers_test.rs::a_60k_window_is_byte_for_byte_unchanged"
    owner: core
    note: "60,000 asserts threshold 27,000 / ceiling 37,000 / emergency 57,000, the exact pre-#1179 values; 0.55 is the largest fraction whose scale at 60,000 is exactly 1.0. nothing_at_or_above_the_crossover_is_touched asserts the crossover as behaviour rather than arithmetic."
  - id: c7
    text: "A window core has judged too small to compact in never fires a summarization, by either trigger"
    state: met
    evidence: "test:crates/wcore-agent/src/engine.rs::no_configured_window_too_small_to_compact_in_can_reach_either_trigger"
    owner: core
    note: "SPLIT OUT OF c2 on 2026-08-30, after the partials verifier showed that one token cannot anchor both claims - the ledger's own rule that a criterion needing two pieces of evidence is two criteria. This is the property the sweep actually grades, and the REFUSAL is what #1179's fix ships. STATED PLAINLY because it is exactly what the verifier caught: at tokens == BASELINE_TURN_TOKENS the conjunction is `threshold > B && B >= threshold`, so this token cannot fail for any change to the threshold FUNCTION. What it CAN fail on is the gate itself, which is what it is here to grade. RE-ARMED 2026-08-30 at f96d1933e: deleting the single line `&& self.supports_compaction(window)` from CompactConfig::should_autocompact_at (git diff -U0 read back, one deletion at compact.rs:641, a CODE line inside the boolean expression, no comment touched, file touched after mutating and after restoring) reddens it with `window 1024: a configured window fires an LLM summarization on core own 3118-token baseline turn, before the user has typed anything - threshold 462`, together with its sibling a_configured_window_too_small_to_work_in_never_fires: `window 4096: an OPERATOR-CONFIGURED window core has already judged too small to compact in summarized a conversation that had not started - threshold 1844, baseline turn 3118`. Restored, touched, re-run: 15 tests run: 15 passed, 0 skipped. The negative control the_first_workable_configured_window_still_fires_when_it_should keeps 'never fires' from being satisfiable by simply switching autocompact off - at 6,929 a 3,118 watermark does not fire, 3,119 does, u64::MAX does. BOTH triggers go through should_autocompact_at - compact/auto.rs::should_autocompact and AgentEngine::should_autocompact_now via compaction_window_now - so the refusal is a property of the WINDOW, not of the route it arrived by, which was the whole defect. RESIDUAL, carried over from c2 and unchanged: below the 6,929 crossover the threshold VALUE still computes below the baseline turn; what changed is that neither trigger ACTS on it. The number is still reported to the cache ledger and the context gauge, which is why autocompact_threshold_for_window stayed a plain number. RENUMBERED c6->c7 on 2026-08-30: origin/integ/f13 landed its own c6 (output_reserve) while this lane was out, and two criteria cannot share an id. EVIDENCE REPLACED on 2026-08-30, conceding the round-2 objection's class against the criterion this lane ADDED in answer to it. The previous token, no_configured_window_anywhere_fires_on_the_baseline_turn, called the free function wcore_config::compact::auto::should_autocompact, which resolves its own window from the config and the model. The engine does not call it: its static trigger is should_autocompact_now -> should_autocompact_at(compaction_window_now(), ..) and its #280 trigger is smart_compact_fraction -> smart_compact_should_fire. So the token graded an ADJACENT function and the words 'by either trigger' were unearned - the same substituted-property defect the objection raised against #1172 c3. The replacement drives both engine triggers plus unworkable_window_refusal across every window in 1,024..=16,384 with smart_enabled forced ON (it is default-OFF, so leaving it alone makes the second trigger vacuous), and carries three vacuity controls: the band must contain BOTH refused and workable windows, and the #280 trigger must still FIRE at 12,000 tokens in a 16,384 window. RED ARM (hetzner-dsm, 2026-08-30): see the note on #1172 c3 - deleting the turn-loop call site leaves the old free-function test green and reddens the new wiring test."
---

#1172 taught core to learn an endpoint's genuinely-served context window from
`usage.prompt_tokens`. That learned figure deliberately does not feed the #255
pre-flight guard or the compaction thresholds, because the buffers are absolute
and were tuned when the only window in play was 200,000. At 4,096 they brick the
run rather than save it: the input ceiling saturates to zero and the autocompact
threshold falls below core's own baseline turn.

So the learned window today drives only the user-facing notice and the pressure
gauge. This issue is the compensation half, and it is the reason #1172's own c3
is not met.

The scope is narrower than it looks. At 4,096 no compaction strategy can work at
all — the honest remedy is the operator raising the server's context length,
which the notice now says. This is about the band where a small window is still
workable, roughly 8k to 32k. c5 is here because the obvious fix has a known way
of making a different band worse, and that band has never been measured.

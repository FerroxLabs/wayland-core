---
issue: 1357
repo: FerroxLabs/wayland
kind: defect
title: "Concurrent effect-checkpoint stores fail spuriously on each other's temporary files"
status: open
last_verified_commit: 327e410d7
criteria:
  - id: c1
    text: "Both failures are reproduced deterministically by tests that force the interleaving, red on the current code: two same-digest stores, and a store whose scan races another store's temp removal."
    state: met
    evidence: "test:crates/wcore-agent/src/session_journal.rs::a_store_never_deletes_the_live_temporary_of_another_store_of_the_same_checkpoint"
    owner: core
    note: "MET 2026-09-10 at 2302378f5 (tests only, on integration b43d27551; hetzner-dsm, tools/remote-proof.py slot default, receipts in .planning/evidence/w15-1357/receipts.tsv). New cfg(test) park points hold ONLY the first store to reach them (BeforeLink/AfterLink in the store, CleanupListed/CleanupStatted in stale-temporary cleanup, ScanListed in the quota scan); test instrumentation only. RED on the unrepaired store, the five tests NAMED, remote_exit 101, 1 passed / 4 failed: T1 a_store_never_deletes_the_live_temporary_of_another_store_of_the_same_checkpoint (first store parked before its link, second same-digest store runs) -> first store Err(Io NotFound) at its link; T2 (unix) loading_a_checkpoint_never_deletes_the_live_temporary_of_a_store_not_yet_linked -> pending store Err(Io NotFound) after a load removed its temporary; T3 a_temporary_removed_during_stale_temporary_cleanup_is_already_gone -> Err(Io NotFound) at CleanupListed; T4 an_entry_removed_during_the_quota_scan_counts_as_gone -> the scanning store Err(Io NotFound); T5 (a guard, not a defect test) ok. NOT CLAIMED on the unrepaired code: T3 panicked in its first stage, so its CleanupStatted stage did not run there; that site is shown red by the c3 `remove` arm. The READ-FROM-CODE filing note (line numbers at a8fd1ac9c) is now reproduced for both findings."
  - id: c2
    text: "A store never deletes a temporary file another LIVE store is writing; crash-left temporaries are still cleaned up and still counted against the quota (wayland#1353 c3's test still passes); a vanished entry during a scan is treated as gone, not as an error, without weakening the quota (wayland#1353's race test still passes)."
    state: met
    evidence: "test:crates/wcore-agent/src/session_journal.rs::an_entry_removed_during_the_quota_scan_counts_as_gone"
    owner: core
    note: "MET 2026-09-10 by the repair 4c8bb5219 plus guard 8e9cf00dc. LIVE: each store registers its temporary's name in a per-journal set before creating the file and unregisters on every exit; cleanup skips a registered name unless the file is already a second hard link to the published checkpoint (Unix dev/ino; removing that only drops a redundant name). NO PID OR AGE RULE, and why that is safe: the writer lease makes one handle family the only writer to a session's checkpoint directory, so an unregistered temporary belongs to no live store. VERIFIED on both platforms rather than assumed: the ungated compacted_replacement_keeps_the_data_inode_lock (second in-process opener refused) and operating_system_releases_writer_lease_after_process_exit (second process refused) PASS in the Windows CI leg of run 34351599352 (main 3d3856d40, job `CI (Array)` on windows-latest, whose JUnit also carries Windows-only suites), and lease.rs is byte-identical between 3d3856d40 and b43d27551; planning records calling the lease cfg(unix)-gated (T-22-06, F-12) predate its Windows implementation. CRASH-LEFT: a_stale_temporary_of_the_same_checkpoint_is_still_removed (added 8e9cf00dc; no earlier test planted an unlinked, unregistered same-digest temporary) green at 8e9cf00dc, and green on the unrepaired store plus T6 only (185c7335c, remote_exit 0), so cleanup behaviour is kept; wayland#1353's crash_left_checkpoint_temporaries_still_count_against_the_session_quota green (counting). VANISHED: a temporary gone before cleanup's stat or removal is gone; the quota scan lists again when an entry vanishes, at most 8 scans then fail-closed, every rescan after the admission's released snapshot. T4 leaves room so the store fits ONLY if the vanished 4,096-byte entry is not counted. wayland#1353's race test and both ordering tests green at 4c8bb5219 and 8e9cf00dc. GREEN: 4c8bb5219 named 12/12 remote_exit 0; full `test -p wcore-agent --lib` + durable_child_store_test, f889_write_edit_reconcile_test, session_journal_test, workflow_limits_test remote_exit 0 (lib 2757 passed / 0 failed / 3 ignored, 12, 9, 52, 9); 8e9cf00dc named 15/15 remote_exit 0. clippy --all-targets -D warnings and clippy --target x86_64-pc-windows-gnu -p wcore-agent --all-targets -D warnings remote_exit 0 at both 4c8bb5219 and 8e9cf00dc. COMPOSES WITH wayland#1301: git merge-tree applies 07272c36b (and aa8f42912) onto 4c8bb5219 with no conflict (control conflicts); scratch b4c2dfb48 passes 14/14 named, including #1301's two sizing tests; checkpoint_directory_bytes gains only a cfg(test) hook and its sizing is untouched. NOT CLAIMED: no macOS or Windows test run; #1301 per-dispatch cost not re-measured; how often production reaches these windows. FOLLOW-UP 2026-09-11 at 8aad7667a (on integration 15bf4cc3f), from adversarial review of 4c8bb5219 (MAJOR, inferred): the rescan-on-vanish loop still failed a store with NotFound once eight passes in a row each lost an entry, plausible under sustained parallel preimage stores into a large store. REPLACED WITH COUNT-AS-GONE inside `checkpoint_directory_bytes`: an entry whose stat returns NotFound counts as zero in that pass, no rescan; a missing directory (read_dir) and every other error still fail closed. SAFE, argument checked: a vanished temporary's store is still reserved (unseen), released after the scanning store's snapshot (released delta), or released before it, and a store releases only after publishing, so its published name existed before the listing began and is listed; published checkpoints are not removed mid-session. INSTRUMENT: many_concurrent_stores_into_a_large_checkpoint_directory_never_fail (2e20144ae, test alone): 16 threads x 2 stores x 50 rounds into a store pre-filled with 400 published entries, no store may fail, quota checked every round. COULD NOT TRIP THE EIGHT-SCAN CAP: on 2e20144ae (the rescan code) it PASSED, 0 of 1600 failed, remote_exit 0, so the MAJOR stays inferred, not reproduced. GREEN 8aad7667a: 15 named (T4, the stress test, #1353's and #1357's tests, #1301's sizing test) remote_exit 0; full `wcore-agent --lib` 2761 passed / 0 failed / 3 ignored remote_exit 0; clippy --all-targets -D warnings and windows-gnu -p wcore-agent -D warnings remote_exit 0. RED ARM (scratch w15/quota1357-red-vanish-error f5c1187c7, the NotFound-as-gone line removed, never merge), remote_exit 101: T4 FAILED and the stress test FAILED with 234 of 1600 stores Err(Io NotFound); T3 ok. This supersedes c3's `rescan` arm, whose rescan loop no longer exists."
  - id: c3
    text: "The c1 tests are green, with a red arm from history for each repair."
    state: met
    evidence: "commit:2effffbe8"
    owner: core
    note: "MET 2026-09-10. RED FROM HISTORY: 2302378f5 (c1). GREEN: 4c8bb5219 and 8e9cf00dc (c2). ONE RED ARM PER REPAIR, scratch branches never to merge, each one line on the repair confirmed by git diff, remote_exit 101, each failing ONLY its own test: w15/quota1357-red-registry e4c4a1312 (nothing registered live) -> T1 and T2 only; w15/quota1357-red-link 00a86f25b (a live temporary kept even when it only links the published checkpoint) -> T5 only, 'checkpoint has unsafe links or permissions'; w15/quota1357-red-stat ab62ce134 (NotFound before cleanup's stat is an error) -> T3 at CleanupListed only; w15/quota1357-red-remove 735ed9618 (NotFound before cleanup's removal is an error) -> T3 at CleanupStatted only, its first stage passing; w15/quota1357-red-rescan 6d3cb5909 (no rescan) -> T4 only; w15/quota1357-red-dead 6ea729fad on 8e9cf00dc (cleanup treats every temporary as live) -> T6 only, 'published=false: the crash-left temporary was not removed', with effect_checkpoint_repairs_crash_after_publication_link GREEN. INSTRUMENT GAP: a `-- checkpoint` libtest filter silently skips T3, T4 and a #1353 ordering test (their names lack the word), so every run counted here names its tests. Evidence: .planning/evidence/w15-1357/README.md."
---

Created 2026-09-10 at filing time, not retroactively. Found by adversarial
review of the wayland#1353 repair; both paths predate it.

Graded 2026-09-10 by the w15/quota1357 lane: c1-c3 met; repair 4c8bb5219, guard
8e9cf00dc. Full record in `.planning/evidence/w15-1357/README.md`.

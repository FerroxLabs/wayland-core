---
issue: 1351
repo: FerroxLabs/wayland
kind: defect
title: "Two sessions racing a memory migration both degrade to NullMemory, and the user is never told"
status: open
last_verified_commit: 632d7c651
criteria:
  - id: c1
    text: "Two sessions starting concurrently against a memory database that needs a migration BOTH end with a working memory backend rather than one of them on NullMemory -- shown by a test that reproduces the race and REDS without the fix."
    state: met
    evidence: "test:crates/wcore-memory/tests/issue_1351_migration_race_test.rs::both_openers_of_a_migrating_store_get_a_working_backend"
    owner: core
    note: "MET 2026-09-10 at 62d1a1348, by RETRY -- no new lock, as this row asked. THE RED, and it is the ticket's own error verbatim: crates/wcore-memory/tests/issue_1351_migration_race_test.rs drives four concurrent openers of one fresh store through the production `TierConn::open` path behind a barrier, and at source 4dd7b858c (the repairs removed, the tests kept) THREE OF FOUR fail with `migration error at v5: duplicate column name: last_latency_ms` -- three sessions that `bootstrap.rs:2231` would have dropped to NullMemory. At 62d1a1348 the same test is 2 passed / 0 failed over six rounds, every round ending at CURRENT_VERSION with `procedures.last_latency_ms` present. INSTRUMENT CHOICE, stated because it decides whether the test can exhibit the failure at all: `cargo test`, NOT `cargo nextest run`. The hazard is between two SQLite CONNECTIONS, not two processes -- SQLite serialises on file locks and two `rusqlite::Connection`s in one process contend exactly as two processes do -- so one process plus a barrier reproduces it precisely, while nextest's process-per-test would have removed the barrier that makes the collision reliable. THE FIX IS KEYED ON THE STORE, NOT ON THE ERROR TEXT: `apply_migrations` re-reads the installed version and re-runs the ladder ONLY when the step that failed is now recorded as applied. Every step commits its `schema_version` row in the same transaction as its DDL, so 'version N present' means 'step N completed by someone else'. A migration that failed for a reason nobody repaired still fails closed -- held by the second test, `a_migration_nobody_else_repaired_still_fails`, which drops `procedures` under a v4 store and asserts both the v5 error AND that the recovery counter did not move. A SECOND ARM OF THE SAME RACE WAS FOUND BY MEASUREMENT AND IS NOT IN THE TICKET TEXT: with the retry in place a loser still failed with `memory DB: database is locked`. Stage-tagging the open (temporary commit, discarded) put it at `DIAGSTAGE=journal database is locked` -- the journal-mode pragma, not the ladder. Switching a store into WAL takes an exclusive lock and SQLite returns SQLITE_BUSY for it WITHOUT consulting the busy handler, and `SqliteJournalMode`'s WAL arm deliberately sets no busy timeout at all, so that call got its own bounded wait. Both waits are scoped to the open and restored afterwards, so no later memory write changes behaviour. NOT CLAIMED: this closes the race between OPENERS of one base directory. It does not make `apply_migrations` transactional across processes, and a store whose migration is genuinely broken still degrades to NullMemory -- which is what c2 is for. Evidence and the full arm table: .planning/evidence/w15-1351/README.md. PRIOR FILING, kept: NOT MET, filed 2026-09-10 by this cycle's own verification while fixing the shared-home test isolation, and given a ledger IMMEDIATELY rather than being left to be found late -- that is the exact failure wayland#1272 c2 exists to catch, and filing a defect at the tail without tracking it would repeat it. THE RACE, measured rather than reasoned: two processes both read `user_version` below target, both run the same `ALTER TABLE`, and the loser gets `Migration { version: 5, source: SqliteFailure(..., Some('duplicate column name: last_latency_ms')) }`. `Memory::open` then returns Err and bootstrap falls back to NullMemory at crates/wcore-agent/src/bootstrap.rs:2231-2238. Observed on hetzner under tools/remote-proof.py, which exports ONE WAYLAND_HOME for the whole cargo invocation, so every process it spawns resolves the same memory database. WHY IT MATTERS FOR THIS RELEASE SPECIFICALLY: the race needs a pending migration plus concurrent starts, which is precisely the shape of the first minutes after an upgrade -- a user who starts two agents after installing 0.13.14 can lose long-term memory in one of them. WHAT WOULD SATISFY THIS: the loser must RETRY rather than degrade -- re-read `user_version` and re-open once when the failure is an already-applied migration. A NEW LOCKING SCHEME IS NOT THE PREFERRED FIX and should not be reached for at the tail of a release: this repo has already been bitten by a fork-duplicated flock on the session-journal data file, where 47.6% of reopens were refused under load and an agent blocked behind its own child. Retry is the smaller change and does not add a new lock to a release window."
  - id: c2
    text: "A session that does end on NullMemory tells the user through a channel that reaches them with RUST_LOG unset. A `tracing::warn!` does not qualify -- with RUST_LOG unset only ERROR reaches stderr -- shown by an arm that captures the user-facing stream."
    state: met
    evidence: "test:crates/wcore-agent/tests/issue_1351_memory_off_notice_test.rs::a_session_that_lost_memory_says_so"
    owner: core
    note: "MET 2026-09-10 at 62d1a1348, and GRADED ON A CAPTURED STREAM from the real binary rather than on a code reading, because this row says a log-level bump does not qualify. THE RED, measured: .planning/evidence/w15-1351/capture.sh runs `wayland-core 'say hi'` with RUST_LOG UNSET (`env -u RUST_LOG`), an isolated WAYLAND_HOME, and WCORE_MEMORY_DIR pointing at a store stamped `schema_version = 99` so `Memory::open` fails closed, capturing BOTH stdout and stderr; a control arm runs the same command against a healthy memory root. On the unfixed binary (sha256 9a933450d0ce5db4a23a2f7a03af421e769d3c8c680f829a372490c449de1f99, notice emission removed) the degraded run and the healthy run differ by ONE BYTE of user-facing output -- 3,451 vs 3,450 stderr bytes, a path length -- and the word 'memory' appears NOWHERE in either stream. The `tracing::warn!` at bootstrap.rs:2232 was live in that binary and reached nobody. THE GREEN, same script, binary sha256 99adb2138e55185d01ea22bc2c899743df14eb1b2de5661342cde1d9bc5d45de at source 66936d217: the degraded arm carries the notice on stderr (4,014 bytes, 1 hit) and the CONTROL arm still carries zero, so the capture is an instrument and not a grep that matches anything. THE CHANNEL, and why it is not `error!`: the fact goes to the session`s own OutputSink via `self.output.emit_info`, the channel the retry notices, the #1130 capability narrowings and the local-shell notice already use -- one emission reaches the CLI, the TUI and a JSON-stream host alike, where a stderr log record reaches only the first. The `tracing::warn!` is KEPT beside it as a copy for the log file. The words are a pure function, `bootstrap::memory_unavailable_notice`, so they are under test rather than reachable only on a host whose store happens to be broken. IN-TREE GUARD: crates/wcore-agent/tests/issue_1351_memory_off_notice_test.rs drives the production `AgentBootstrap::build()` with a capturing sink and a planted unopenable store; its negative half asserts a session with WORKING memory is told nothing, so a bootstrap that announced unconditionally would fail. At the red source the guard prints the whole captured stream, and it contained exactly one unrelated line about an unknown context window. UNCHANGED, as this row asks: the F05 fail-closed path is untouched (bootstrap.rs:2326 at 2b7a790ac, was :2243-2249 at filing). LINE NUMBERS IN THE PRIOR FILING BELOW ARE PRE-FIX and are about 19 lines low against the current tree for the same reason. SOURCE_INPUTS: bootstrap.rs was edited, so desktop_contract_corpus reds; the corpus was NOT regenerated (integrator`s pass) and the drift message confirms schema_digest is UNCHANGED -- a source-hash rebase, no wire schema moved. PRIOR FILING, kept: NOT MET, filed 2026-09-10. This is the half that makes the defect a LIE rather than merely a bug. The only announcement of the degradation is `tracing::warn!` at crates/wcore-agent/src/bootstrap.rs:2232. With RUST_LOG unset -- the default for every ordinary user -- ONLY ERROR reaches stderr, so that line is invisible and the user is never told their memory stopped working. NullMemory accepts every write and returns a fresh id, so nothing downstream looks wrong either. WHAT IS ALREADY CLOSED, and should not be re-litigated: the F05 fail-closed path at bootstrap.rs:2243-2249 sets smart_handoff_to_memory = false and skills_lifecycle = false when memory is unconstructed, precisely so runtime code cannot falsely emit outcome_changed. That stops the product misreporting a RESULT. It does not tell the user the FEATURE is off, which is what this criterion is about. GRADE IT ON A CAPTURED STREAM, NOT ON A CODE READING: an arm that runs the degraded path with RUST_LOG unset and shows the user-facing stream is empty is the red; the same arm showing the disclosure after the fix is the green. A log-level bump alone has already failed to fix `the user is not told` three times in this repo, so a warn! -> error! change is not automatically sufficient -- what matters is that it reaches the user's stream under the default configuration."
---

Created 2026-09-10 at filing time, not retroactively. Found by the w15/lease
lane while isolating the shared-home test failures; the memory race was the
common cause behind several unrelated-looking assertion failures.

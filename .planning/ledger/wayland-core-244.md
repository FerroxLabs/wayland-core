---
issue: 244
repo: FerroxLabs/wayland-core
kind: defect
title: "VFS Read of raw .git/objects permitted (compressed; info-only)"
status: open
last_verified_commit: 9ecb43977
criteria:
  - id: c1
    text: "A VFS read of a raw object store is refused at the workspace root and at any nested depth, for a store reachable by its own `.git/objects`-shaped path and for any store the workspace root`s own `.git` NAMES"
    state: met
    evidence: "test:crates/wcore-tools/tests/vfs_vcs_content_store_deny.rs::nested_object_store_is_refused_by_the_vfs"
    owner: core
    note: "the predicate is WorkspacePolicy::is_vcs_content_store, wired inside SecretDenyFs::guard, which every trait method calls - read, read_pinned, write, exists, list, remove_file, metadata, observe_file and compare_exchange_file. TEXT REWRITTEN 2026-08-30, and this is the FIELD, not a note claiming a rewrite: the old sentence was the unqualified `at any nested depth`, and building the #244 c3 second-arm fixture measured a case where it is FALSE. is_vcs_content_store has two arms -- lexical any-depth, and the stores resolved from `<root>/.git`. A VENDORED checkout whose `.git` is a gitfile falls between them: `<root>/vendor/pkg/.git` = `gitdir: ../pkg-git` puts the real store at `<root>/vendor/pkg-git/objects/...`, which is not lexically a store and is not named by the ROOT`s `.git`. MEASURED, and asserted in-tree so it cannot rot: grep_vcs_named_store_deny.rs::grep_cannot_harvest_a_nested_gitfile_named_store asserts `!is_vcs_content_store(store_file)` and PASSES, so SecretDenyFs::guard admits those bytes today. The rewritten text states the scope that actually holds; the remainder is c7, handed to FerroxLabs/wayland-core#390. It is not graded met by substitution -- the vendored case is named, not omitted."
  - id: c2
    text: "Ordinary repository metadata such as .git/HEAD and .git/refs/heads/main stays readable"
    state: met
    evidence: "test:crates/wcore-tools/tests/vfs_vcs_content_store_deny.rs::repository_metadata_stays_readable"
    owner: core
    note: "the wrong-refusal control; without it the suite would be satisfied by a guard that denies the whole of .git"
  - id: c3
    text: "The store is unreachable to the Grep subprocess family - `rg`, POSIX `grep` and `findstr` - at any anchor and any depth, for a lexically-shaped store and for a store a `.git` merely NAMES, and not only to the in-process VFS. The Bash subprocess half is c4; a local-operator Bash shell on a backend that cannot enforce OS read-deny still reads the store (measured in c5), and GitTool reads it by design (FerroxLabs/wayland-core#388)"
    state: met
    evidence: "test:crates/wcore-tools/tests/grep_vcs_named_store_deny.rs::grep_cannot_harvest_a_gitfile_named_store"
    owner: core
    handoff: "FerroxLabs/wayland-core#391"
    note: "CLOSED 2026-08-30 against the counterexample that refuted it, not against an adjacent property. The refutation reproduced FIRST, at integ/f13 a278f8c3b, with the test file below added and NO fix: `Grep(pattern, path = \".git\")` returned `.git/lfs/objects/aa/bb/deadbeef:1:WLCANARY-LFSOBJ-244` and `.git/objects/ab/cd1234:1:WLCANARY-ROOTOBJ-244`, and `Grep(path = \"vendor/x/.git\")` returned `vendor/x/.git/objects/12/3456:1:WLCANARY-NESTED-322` -- PLAINTEXT, under the production `SandboxedFs::new(SecretDenyFs::new(RealFs, WorkspacePolicy::contained(root)), root)` stack. `ctx.vfs.exists()` gates only the TOP-LEVEL `path` argument and `.git` is not itself a store, so naming the control directory ONE COMPONENT UP walked in while naming the store outright was refused. THE FIX is a new lexical predicate `workspace_policy::is_vcs_content_store_static` (the same any-depth arm `WorkspacePolicy::is_vcs_content_store` tries first, not a second copy) asked at BOTH grep_policy decision points -- the `WalkBuilder` admission in `scope_for` and the explicit-file gate in `GrepScope::admits` -- plus a `vcs_store` row in the withholding footer so the refusal is reported, never silent. It sits in `grep_policy`, the ONE place all three backends (`rg`, POSIX `grep`, `findstr`) are held to the same answer, so the class is closed for the backend the host happens to have. RED ARM, hetzner, tree committed first: M1 `is_vcs_content_store_static` -> `{ let _ = path; false }` (verified on executable lines 2186-2189, not a doc comment) reddened 2 of 5 -- `grep_cannot_harvest_a_vcs_content_store_named_one_level_up` and `grep_cannot_harvest_a_nested_vcs_content_store` -- with the literal store bytes in the panic message. M2 -> `{ let _ = path; true }` (blanket deny) reddened the OTHER two, the wrong-refusal controls: `an_ordinary_file_is_still_searchable` got `[Grep policy: 6 match(es) in VCS content stores withheld]` and the `.git/HEAD` control got `[Grep policy: 3 match(es) in VCS content stores withheld]`. Restored byte-identical (`git diff` empty), touched, 5/5 green. Also RUN ON WINDOWS 10.0.26200.9168 (5/5 pass), which the platform-scoped Linux sibling never covered. SUBPROCESS CLASS ENUMERATED, 3 families in wcore-tools: (1) GrepTool, 3 backends through one policy point -- fixed here; (2) GitTool, which reads the object store BY DESIGN via blame/diff/log -p and is already recognised in-tree and dropped from Full channel-remote posture (channel_tools.rs:107-112, `FULL_CHANNEL_DENY`), a taken decision rather than a gap -- see FerroxLabs/wayland-core#388; (3) tirith_security, which lints a COMMAND STRING and never traverses a path. `.git/HEAD` and `.git/refs` stay searchable, which is c2's carve-out held. REFUTED 2026-08-30 AND RE-CLOSED. The verifier was RIGHT and the concession is total: the fix asked only ONE of the two arms of the product's own store predicate. is_vcs_content_store also resolves the stores `<root>/.git` NAMES -- a gitfile`s `gitdir:`/`commondir` and an `objects/info/alternates` borrow -- and BOTH can land INSIDE the root, where nothing is lexically a store. Worse than the original counterexample: it leaked on `Grep(\".\")`, the whole-workspace search, not on a model that thought to name `.git`, and no withholding footer appeared, so the refusal was not even reported. The doc comment at workspace_policy.rs asserted the opposite (`a gitfile-pointed or alternates-borrowed store lives OUTSIDE the workspace root`); that sentence was FALSE and is deleted, replaced by the measured output. REPRODUCED FIRST on hetzner, new file grep_vcs_named_store_deny.rs added to the UNFIXED tree, 3 of 3 red, each with a fixture control asserting the PRODUCT`s own predicate calls the target a store: arm A `git init --separate-git-dir` gave `./mygit/objects/ab/cd1234:1:WLCANARY-GITFILE-244`; arm B `objects/info/alternates` gave `./shared-objects/ab/cd1234:1:WLCANARY-ALTERNATES-244`; arm C, which the verifier did NOT find, is a VENDORED gitfile and gave `./vendor/pkg-git/objects/12/3456:1:WLCANARY-NESTEDGITFILE-244`. In all three `./notes.txt:1:WLCANARY-CONTROL-OK` came back too, so none is a broken fixture. THE FIX: grep_policy now asks BOTH arms -- is_vcs_content_store_static per path, plus vcs_content_stores(dir) resolved for the search anchor, for every ancestor between it and the target, and for EVERY directory the walk traverses. Two passes over one walk, because a store is discovered from the directory that names it and that directory may be visited after files already admitted from inside it. Cost is two syscalls per directory with no gitfile and no alternates, and under_any returns on the empty set before touching the filesystem, so an ordinary workspace pays nothing. Arm C makes Grep deliberately STRICTER than the point-predicate, which is root-only on arm 2; denying a real object store is never a wrong refusal, and the VFS-side remainder is c7 / FerroxLabs/wayland-core#390. The footer half is closed too: each arm now asserts `in VCS content stores withheld` appears, so the withholding is REPORTED and not silent. 3/3 green plus the original 5/5 in grep_vcs_store_deny.rs, whose `an_ordinary_file_is_still_searchable` control still returns. COST MEASURED, not asserted to be small: a whole-workspace `Grep(\".\")` over this repo`s own `crates/` tree (1003 directories) takes best-of-5 0.2132s at this commit against 0.1777s with the three source files reverted to HEAD~1 -- +35.5ms, about +20%, or ~35us per directory for the two-syscall gitfile/alternates probe plus the six `(control, store)` joins. Both arms were FINGERPRINTED by content (`grep -c under_any` = 4 and 0) before being believed, because an earlier `git checkout --` silently restored from the INDEX and ran the OLD binary twice while reporting a clean tree. The plain joins could be dropped per traversed directory to cut this ~4x, and deliberately are not: they are what catches a nested `.git` SYMLINK whose target is inside the root, and 35ms is not worth a coverage hole. This is once-per-Grep-invocation and is NOT the per-path cost #376 reports on the SecretDenyFs guard. Separately measured and ruled out: `wcore-cli::deterministic_openai_loop::packaged_f04_run_is_repeatable_and_content_addressed` exceeds nextest`s default 60s kill on this host (58.6-87.2s across six runs, PASSING every time when run without the kill), its file and fixtures are unchanged since base, it issues no Grep, and the 35ms above cannot produce a 10s swing -- it is host load, and the block-structured A/B that first suggested otherwise was the flaw. Recorded as a second instance on FerroxLabs/wayland-core#378. REFUTED A THIRD TIME 2026-08-30 AND CONCEDED WITHOUT RESERVATION. The verifier applied its own FINDING-3 identity test to this criterion and it failed: `diff` of the `text:` line against origin/integ/f13 printed C3_TEXT_FIELD_IDENTICAL_TO_BASE while the file around it had changed from 3 criteria to 7. I re-ran that exact command before conceding -- DIFF=0 against the base field, with a control diff of the criterion COUNT returning 1, so the instrument could show a change and there was none to show. The previous round split c4/c5/c6 out BESIDE this criterion and narrated the new scope in this note while leaving the SENTENCE untouched and `met`. That is the identical defect the lane had just conceded as FINDING 3 one round earlier: moving state and adding siblings does not scope a sentence, and a note asserting a scope the field does not carry is not a scope. The old sentence read, unqualified, `The store is also unreachable to a shell subprocess, not only to the in-process VFS`. `BashTool` IS a shell subprocess and is the paradigm case, and this lane's OWN passing test bash_vcs_store_local_operator_gap.rs::a_local_operator_shell_reads_the_store_on_a_non_enforcing_backend prints `LOCAL_OPERATOR_STORE_READ: Exit code: 0 / STDOUT: ROOT-OBJECT-BYTES-244` -- the sentence was measured FALSE by the lane's own instrument. THE FIELD ABOVE IS NOW REWRITTEN, and this is the field, not a note claiming a rewrite: the verifier's identity test now returns A_DIFF=1 on c3 and B_DIFF=1 on c4, with c2 -- a field deliberately left alone -- returning 0 as the negative control that the diff distinguishes changed from unchanged. The rewritten sentence claims exactly the subprocess family this criterion's evidence proves (the three Grep backends, both predicate arms, any anchor and any depth) and names each remainder IN THE FIELD: the Bash half is c4, the local-operator leak is c5, GitTool is by-design and #388. `handoff: FerroxLabs/wayland-core#391` is now a FIELD too, so check-release-readiness resolves that ticket live and fails if it is closed or missing -- the remainder is machine-tracked rather than narrated here."
  - id: c4
    text: "The OS-sandbox half holds wherever the OS sandbox enforces read-deny: a Bash subprocess cannot read the store at the root or at any nested depth. Where a policy that requires OS read-deny meets a backend that cannot enforce it, the shell is REFUSED rather than silently uncontained for every principal but the local operator - whose exemption is measured in c5 and whose remainder is FerroxLabs/wayland-core#391 - and except under the operator's explicit no-sandbox Dangerous launch, which is `SandboxRegistry::dangerous` session authority and reads the store outright"
    state: met
    evidence: "test:crates/wcore-tools/tests/bash_vcs_store_deny_linux.rs::a_shell_subprocess_cannot_read_the_vcs_content_store"
    owner: core
    handoff: "FerroxLabs/wayland-core#391"
    note: "Split out of c3 on 2026-08-30 because the schema allows one evidence token per criterion and c3 needed the token that resolves its own refutation. This half was MEASURED 2026-08-29 and re-confirmed: the test drives the real BashTool under bwrap, root store `.git/objects/ab/cdef` and nested `vendor/pkg/.git/objects/12/3456` both unreadable, with three controls (readme.txt reads back, `.git/HEAD` reads back, and the recursive sibling proves `grep -r` cannot harvest what `cat` cannot name). SCOPE, stated not assumed away: `#![cfg(target_os = \"linux\")]` and it skips where `platform_enforces_read_deny()` is false, so on Windows -- where the job-object default confines nothing -- it skips by construction. GitTool remains a deliberate exception on the LOCAL contained path: channel_tools.rs:107-112 records that it reads git-TRACKED content straight from the object store and that under the STRICT sandbox it is the ONLY door to a branch, a push or a pull request, so it is dropped from channel/remote Full posture and kept locally. That is a taken product decision, not an ungraded gap. REFUTED 2026-08-30 AND CONCEDED. The old text was unqualified and is measured FALSE on the Windows shipping default for the ordinary interactive user. The exec-time gate is shell_requires_os_read_deny() = secret_read_deny_required AND NOT local_operator_principal (workspace_policy.rs:1227), so on WindowsJobObjectBackend -- whose enforces_read_deny() is deliberately false -- a LOCAL OPERATOR gets a shell with no OS read-deny at all. The ledger previously said only that the Linux test `skips by construction` there; untested is not the finding, false-on-the-shipping-platform is, and that is the same easier-adjacent-property substitution this ticket rejects for #338 c2. TEXT REWRITTEN -- the FIELD above, not a note claiming a rewrite -- to the property that actually holds: the confinement where the backend enforces read-deny, and fail-closed refusal where it does not. The local-operator exemption is not covered by the rewritten text and is not smuggled into it: it is c5, measured, and c6, handed to FerroxLabs/wayland-core#391. Grading note: this criterion is UNSATISFIABLE as originally written, because delivering it on Windows needs a filesystem sandbox and the standing decision is that Windows gets none (AppContainer is closed, do not re-ask); so it is rewritten to the property actually wanted rather than graded against a substitute. REFUTED A THIRD TIME 2026-08-30, PARTLY CONCEDED AND PARTLY REFINED BY MEASUREMENT. The verifier is RIGHT that the rewrite fixed the first clause's scope and left the fail-closed clause overclaiming: `where the active backend cannot enforce it, the shell is REFUSED` was universally quantified with no local-operator carve-out, and c5 -- `met`, in this same file -- exhibits the counterexample on the Windows shipping default. Defending that with a note saying the exemption `is not smuggled into it: it is c5` is the FINDING-3 defect committed a second time inside the rewrite meant to answer FINDING 2. Conceded; THE FIELD is rewritten. BUT THE VERIFIER'S PROPOSED WORDING IS ALSO FALSE, and I did not adopt it as offered. It reads `the shell is REFUSED for every principal but the local operator`. The exec-time gate at bash.rs:948-951 is a conjunction of THREE terms, not two: `shell_requires_os_read_deny() && !backend.enforces_read_deny() && !backend.bypasses_containment()`. The third term is `SandboxRegistry::dangerous` -- session AUTHORITY, set only by the operator's explicit no-sandbox launch (wcore-sandbox/src/lib.rs:476, 486) -- and it admits the shell for a NON-LOCAL principal, exactly the principal the fail-closed arm refuses. I did not read that off the source and assert it: it is MEASURED by a new third test in the same file, a_dangerous_no_sandbox_session_admits_the_shell_for_a_non_local_principal, which asserts both preconditions (the registry must report bypasses_containment, and the backend must NOT claim read-deny, so the refusal cannot be skipped for the other conjunct and the arm cannot go vacuous) and then prints `DANGEROUS_NON_LOCAL_STORE_READ: Exit code: 0 / STDOUT: ROOT-OBJECT-BYTES-244`. Adopting the verifier's sentence verbatim would have shipped a THIRD false universal in the field that was supposed to fix the second one -- the exact `the fix reproduces the defect` pattern. The field now carries all three conditions, and the third is graded by a test whose failure message says to DELETE the exclusion from the field rather than edit the assertion. Also added: `handoff: FerroxLabs/wayland-core#391` as a FIELD, so the remainder is resolved live by check-release-readiness rather than narrated."
  - id: c5
    text: "What the local-operator exemption actually delivers is measured, not assumed: on a backend that cannot enforce OS read-deny the shell reads the store, and that is pinned so it cannot silently change"
    state: met
    evidence: "test:crates/wcore-tools/tests/bash_vcs_store_local_operator_gap.rs::a_local_operator_shell_reads_the_store_on_a_non_enforcing_backend"
    owner: core
    note: "c4`s scope, stated by measurement instead of by omission. MEASURED TWO WAYS THAT AGREE. (1) On real Windows 10.0.26200.9168 by the verifier of this change: PLATFORM_ENFORCES_READ_DENY=false, CONTROL_PLAIN_OK=true, CONTROL_HEAD_OK=true, ROOT_STORE_LEAKED=true, NESTED_STORE_LEAKED=true, RECURSIVE_LEAKED=true. (2) INDEPENDENTLY REPRODUCED ON LINUX (hetzner) by this lane, which is why the test is NOT cfg(windows): WindowsJobObjectBackend compiles on every target and really spawns -- it delegates to NoSandboxBackend -- the same property local_operator_shell_gate.rs already relies on. Verbatim from the run: `LOCAL_OPERATOR_STORE_READ: Exit code: 0 / STDOUT: ROOT-OBJECT-BYTES-244`. THE FAIL-CLOSED ARM WAS REPRODUCED FIRST, in the sibling test a_non_local_principal_gets_no_shell_at_all_on_a_non_enforcing_backend: the same backend and the same command with a non-local principal returns `Refused: shell is unavailable because the active sandbox`, so the open arm is not an artefact of a broken harness. An ordinary working-tree read is the positive control in the open arm. Both preconditions are asserted rather than assumed -- the backend must NOT claim read-deny, and the policy must still REQUIRE it -- so neither test can go vacuous. This turns a named-host observation into a STANDING gate on the Linux build host. The test asserts the gap IS THERE and says so in its failure message: a failure means the gap has CLOSED, and the instruction is to re-grade c4/c6 and #391, not to delete the test. 2026-08-30, RED-ARMED rather than asserted non-vacuous. The previous round claimed `neither test can go vacuous` because both preconditions are asserted -- an assertion ABOUT the instrument, not a measurement of it, which is the ungraded-guard pattern. Two mutations were run on hetzner against a committed tree, each proved to land on EXECUTABLE code and each restored byte-identical and touched. M1: drop the local-operator term from `WorkspacePolicy::shell_requires_os_read_deny` (workspace_policy.rs:1228) so it returns `self.secret_read_deny_required`. M2: delete the `!backend.bypasses_containment()` conjunct from the exec-time gate (bash.rs:948-951). The results are DISCRIMINATING -- each mutation reddens a different subset, so no test is riding on another's coverage. Exact outcomes are recorded in the commit body. WINDOWS-RED CLOSED 2026-08-30 (CI run 33291781675, self-hosted msvc leg `CI (Array)`, 16,099 tests / 5 failures). THE WINDOWS MEASUREMENT RECORDED ABOVE WAS NOT THIS TEST. Reading (1) says the property was measured on real Windows by the verifier; that was a bespoke probe. THIS FILE had never executed on a Windows leg, and the first time it did, both of its open arms FAILED. Verbatim: `control failed: an ordinary working-tree read must succeed, got: Exit code: 0 / STDOUT: readme.txt is /c/Program Files/PuTTY/readme.txt` and, on the Dangerous third arm, `Exit code: 1 / STDERR: /usr/bin/bash: line 1: type: .gitobjectsabcdef: not found`. CAUSE: `read_cmd` branched on `cfg!(windows)` and spelled the read in cmd dialect, but since FerroxLabs/wayland#1164 the shell tool resolves a real Git-for-Windows bash on any host that has one. bash ATE the backslashes, so the store path arrived as one meaningless word and the read this criterion exists to pin never happened; and bash`s `type` builtin searches PATH rather than the working tree, so the positive control `succeeded` against an unrelated PuTTY file. That is FerroxLabs/wayland-core#387`s defect class, in a file added after #387 swept it. FIX: `read_cmd` now asks `shell_prefix_is_posix(bash_shell_argv_prefix())` -- the SAME function BashTool calls to build the argv it runs -- so the fixture and its subject cannot disagree about the interpreter; `cat` where that is a POSIX shell, `type` where it is cmd or PowerShell. Product code UNCHANGED. MEASURED ON SEANDESKTOP, Windows 11 build 26200, tree at 70a47aaed: RED ARM FIRST, 2 of 3 failed with the two panics quoted above and the host precondition confirmed (Git-for-Windows bash present, and the failure text names /usr/bin/bash, so the product really had resolved it); then 3 of 3 pass with the store bytes finally arriving -- `LOCAL_OPERATOR_STORE_READ: Exit code: 0 / STDOUT: ROOT-OBJECT-BYTES-244` and `DANGEROUS_NON_LOCAL_STORE_READ: Exit code: 0 / STDOUT: ROOT-OBJECT-BYTES-244`. So c5`s gap is now demonstrated BY THIS TEST on Windows and not only reproduced on Linux, and c4`s third-arm exclusion is graded on the platform it is about. Still green on Linux (3/3)."
  - id: c6
    text: "Whether the Windows local-operator shell is expected to confine the VCS content store at all is a decided question, and the product claims no more than it delivers"
    state: superseded
    evidence: "test:crates/wcore-tools/tests/bash_vcs_store_local_operator_gap.rs::a_non_local_principal_gets_no_shell_at_all_on_a_non_enforcing_backend"
    owner: maintainer
    handoff: "FerroxLabs/wayland-core#391"
    note: "DECOMPOSED 2026-08-30. The REMAINDER of c4 is FerroxLabs/wayland-core#391, which is open and carries decide-or-label acceptance criteria. Owner is maintainer, not core, because the question is a product decision core must not take silently: the relaxation exists precisely because refusing it left every fresh Windows clone with no shell at all, and the printed remedy --trust-workspace hands back the same uncontained shell, so `just enforce it` is not available without a Windows filesystem sandbox -- which the standing decision forecloses. What core HAS done and is not deferring: measured the gap on two hosts (c5), pinned it as a standing Linux gate, rewritten c4 to stop overclaiming, and filed #391 with the measurements in the body. Unaffected and stated so the scope is not read wider than it is: Linux (bwrap) and macOS (sandbox-exec) both claim enforces_read_deny at their shipping default, so the local-operator flag is inert there, which platform_default_backends_that_enforce_read_deny_never_reach_the_relaxation already asserts against the real cascade; and every non-local principal on Windows is still refused outright."
  - id: c7
    text: "A VFS read of a store named by a gitfile on a VENDORED checkout, rather than by the workspace root`s own .git, is refused"
    state: superseded
    evidence: "test:crates/wcore-tools/tests/grep_vcs_named_store_deny.rs::grep_cannot_harvest_a_nested_gitfile_named_store"
    owner: core
    handoff: "FerroxLabs/wayland-core#390"
    note: "DECOMPOSED 2026-08-30. The REMAINDER of c1 is FerroxLabs/wayland-core#390, which is open. Found by this lane while building c3`s second-arm fixture, not reported by the verifier. is_vcs_content_store arm 2 reads `<root>/.git` only, so `<root>/vendor/pkg/.git` = `gitdir: ../pkg-git` leaves a real object store at `<root>/vendor/pkg-git/objects/...` that arm 1 cannot see lexically and arm 2 never looks for. Grep IS closed for this case -- scope_for resolves named stores for every directory it traverses -- and the cited test proves that end to end, which is why it can carry this criterion`s evidence; the VFS is NOT, and the cited test also asserts `!is_vcs_content_store(store_file)` so the miss is recorded in-tree rather than narrated here. Not fixed in this change because extending arm 2 to nested control dirs needs DISCOVERY -- a tree walk -- inside a per-path predicate on the hot SecretDenyFs::guard path, and #376 already reports that rebuilding this list per operation is a cost problem at the current root-only scope. Grep can afford it because it traverses once per invocation; the VFS needs a different shape, which is a design decision with a measurable cost. #390 c4 requires that when the fix lands, the `!is_vcs_content_store` assertion is INVERTED rather than deleted, so the two layers get re-tied."
---

The issue reports that the VFS Read path permitted raw .git/objects content -
compressed, so it was filed info-only rather than as an exfiltration route.

Its own suggested disposition was that the fix would fall out of applying the
object-store deny to the VFS read path, and that is what happened in the
backlog-sweep merge that preceded v0.13.10. The predicate lives in
WorkspacePolicy, the wiring is inside the single guard every SecretDenyFs
method calls, and the test file names its own red arm - delete the
is_vcs_content_store clause from the guard.

The one residual worth writing down is scope, not correctness: this is an
in-process denial. A subprocess is outside its jurisdiction. Criteria are
transcribed from the cluster A verification note of 2026-08-29; every cited
test was re-checked in this tree.

---
issue: 382
repo: FerroxLabs/wayland-core
kind: defect
title: "The truncation notice tells the user core is sizing against the served window on the one turn when it is not"
status: closed
last_verified_commit: 93ae3c4a
criteria:
  - id: c1
    text: "The notice's wording is true when it is emitted: conditional on sizing_window() being Some and on supports_compaction(served), or it says plainly that corroboration is still pending"
    state: met
    evidence: "symbol:crates/wcore-agent/src/engine.rs::truncation_notice"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D10, found while verifying wayland-core#353). Nothing has been done. The measured finding, verbatim: The truncation notice tells the user 'Core is now sizing this session against the {served}-token window this endpoint has actually demonstrated' — but after #353 that statement is FALSE on every first Regression, which is the only turn the notice ever fires for that arm. `observe()` returns evidence on the first backwards report (so `emit_info` runs, quoting served=4050 / 8192 / whatever), while `sizing_window()` is still None, so `narrow_to_served_window` returns the window unchanged and neither the autocompact trigger nor the pre-flight ceiling moves. Worse, the notice is emitted exactly ONCE per figure: the second regression that actually does corroborate hits the `if self.served_window == Some(served_window) { return None }` suppression at context_window.rs:355, so no second notice is sent. The user is told core is sizing against X at the one moment it isn't, and is never told when it starts. The same sentence is also false whenever `supports_compaction(served)` refuses the window (the #1179 gate, e.g. the 4,096 slot #1172 actually measured) — that half predates #353, but #353 widened it to the entire Regression arm. GRADED 2026-08-30 by lane w2-window-arc at 115cb4c6; the fix itself is already on integ/f13 and was VERIFIED here rather than re-implemented. The wording is decided by a PURE function with three arms, one per thing that is actually true, so the sentence is gradable instead of unfalsifiable. Not corroborated -> 'Core has NOT changed how it sizes this session on one observation alone'; corroborated and workable -> the 'is now sizing' claim, which is now only emitted on the turn it is true; corroborated and unworkable -> 'Core cannot size a session against N tokens at all ... this run will stop', which is the arm Q-1172c3's refusal decision obliges. Note the criterion's 'conditional on sizing_window() being Some' is satisfied by evidence.corroborated: sizing_window() is corroborated.then_some(served_window).flatten() and observe() sets served_window to Some before returning evidence, so at the emit site the two are the same predicate. RED ARM (hetzner-dsm): collapsing the first arm's condition to `false` so every arm claims the resize reddens the c3 tests; restored, touched, green."
  - id: c2
    text: "When corroboration lands and sizing actually moves, the user is told -- the once-per-figure suppression at context_window.rs:355 does not swallow the only truthful notice"
    state: met
    evidence: "test:crates/wcore-config/tests/issue_1172_served_window_corpus_test.rs::a_second_regression_corroborates_it_and_the_session_is_sized"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D10). Nothing has been done; the measurement is on c1 and the file:line anchors are in the prose below. GRADED 2026-08-30 by lane w2-window-arc at 115cb4c6; the fix itself is already on integ/f13 and was VERIFIED here rather than re-implemented. The once-per-figure suppression at context_window.rs is now `if self.served_window == Some(served_window) && !newly_corroborated`, computed BEFORE the early return, so corroboration -- a state change the user must be told about -- re-opens the gate exactly once. The test drives the real sequence: baseline turn, first regression (evidence, sizing_window() None), second regression at an UNCHANGED ceiling which must still yield evidence with corroborated = true, and a THIRD regression which must yield None. That third assertion is the control: a fix that simply deleted the suppression would notify every turn and fail it."
  - id: c3
    text: "A test asserts the notice TEXT against the sizing state in the same body, for both the first-regression and the corroborated case; shown RED against today's wording"
    state: met
    evidence: "test:crates/wcore-agent/src/engine.rs::the_truncation_notice_wording_matches_the_sizing_state_when_it_is_emitted"
    owner: core
    note: "Filed 2026-08-29 by the 0.13.12 close-sweep (worklist defect D10). RE-GRADED 2026-08-31 by lane lane/f13-ctx-382 at 3f0d8a11, and the evidence MOVED. The prior evidence, a_single_observation_notice_does_not_claim_the_session_was_resized, hands truncation_notice a ServedWindowEvidence it constructs itself, so it grades the sentence against a corroborated flag the test chose rather than against the sizing state the criterion names -- and it cannot see the trap this ticket is about, the once-per-figure suppression swallowing the corroborated notice, because it never runs the tracker at all. The new test drives the PRODUCTION pair, ServedWindowTracker::observe then truncation_notice (engine.rs:16541 and :16557, the two calls the turn loop makes back to back), over the real sequence: baseline turn, first regression, a corroborating regression at an UNCHANGED figure, and a third regression as the control that a fix which merely deleted the suppression would fail. On both cases the criterion names it asserts the notice TEXT against sizing_window() AND compaction_window_now() in the same body -- first regression: sizing_window() None, compaction_window_now() still the catalogued 128000, text must NOT contain -is now sizing- and must contain -has NOT changed how it sizes-; corroborated: sizing_window() Some(8192), compaction_window_now() 8192, text must carry -Core is now sizing this session against the 8192-token-. an_unworkable_notice_says_the_run_will_stop_only_when_it_will pins the third arm against a different production predicate, unworkable_window_refusal(). RED ARM A (hetzner-dsm; cargo check -p wcore-agent --tests RC=0 BEFORE the mutation): restoring the pre-#353 wording -- arm 1 condition to false and arm 2 gate to true, so every arm claims the resize -- reddens BOTH new tests, the panic quoting -Core is now sizing this session against the 8192-token window- emitted on the turn compaction_window_now() is still 128000; the known-positive repro_1172_c3_core_does_not_size_against_8x_the_served_window passed in the same run, so the red is behaviour and not a build failure. RED ARM B: restoring the pre-fix suppression at context_window.rs:357 (dropping the AND NOT newly_corroborated) reddens the corroborated half alone -- the second regression returns None and the only truthful notice is never emitted -- with two known-positives green in the same run. Both mutations restored and touched, git status --porcelain empty. cargo fmt --all --check and cargo clippy -p wcore-agent --all-targets -- -D warnings clean; engine.rs is in SOURCE_INPUTS so the Desktop contract corpus was regenerated (schema_digest HELD at sha256:8497e92e, only fixture_digest and source_inputs_digest moved) and cargo test -p wcore-protocol is green."
---

The truncation notice tells the user 'Core is now sizing this session against the {served}-token window this endpoint has actually demonstrated' — but after #353 that statement is FALSE on every first Regression, which is the only turn the notice ever fires for that arm. `observe()` returns evidence on the first backwards report (so `emit_info` runs, quoting served=4050 / 8192 / whatever), while `sizing_window()` is still None, so `narrow_to_served_window` returns the window unchanged and neither the autocompact trigger nor the pre-flight ceiling moves. Worse, the notice is emitted exactly ONCE per figure: the second regression that actually does corroborate hits the `if self.served_window == Some(served_window) { return None }` suppression at context_window.rs:355, so no second notice is sent. The user is told core is sizing against X at the one moment it isn't, and is never told when it starts. The same sentence is also false whenever `supports_compaction(served)` refuses the window (the #1179 gate, e.g. the 4,096 slot #1172 actually measured) — that half predates #353, but #353 widened it to the entire Regression arm.

**Where.** crates/wcore-agent/src/engine.rs:15742-15765 (the emit_info block), against crates/wcore-agent/src/engine.rs:8462-8477 (narrow_to_served_window -> sizing_window) and crates/wcore-config/src/context_window.rs:355 (once-per-figure notice suppression)

**Why it matters.** #1172's whole justification for the notice is that this is SILENT data loss and the operator must be told the one thing that fixes it. An operator who reads 'Core is now sizing this session against the 4,050-token window' will reasonably stop worrying and not raise num_ctx / set [compact] context_window — while core is in fact still running on the catalogued 128,000 and will keep overflowing. Both existing tests pin this behaviour without noticing: `a_single_regression_tells_the_user_but_does_not_yet_size_the_session` asserts the notice fires AND that sizing is None in the same test body, and the engine twin asserts the same pair. The fix is to make the notice's wording conditional on `sizing_window()` (and on `supports_compaction`), or to re-emit when corroboration lands.

Criteria are taken verbatim from the issue's Acceptance section. Nothing has been done: this entry exists so the release gate counts the work rather than anyone having to remember it.

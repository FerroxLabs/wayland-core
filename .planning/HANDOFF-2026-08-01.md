# HANDOFF — wayland-core, 2026-08-01

Supersedes `HANDOFF-2026-07-31.md`. One current handoff; do not add a second.

---

## 0. THE ONE FACT THAT EXPLAINS EVERYTHING

**For this project's entire history, no CI run had ever executed tests on all
three platforms. On 2026-08-01, run 30680721949 finally did.** Numbers in §7.

Three *separate* mechanisms had been stopping it, and each reported something
other than "nothing ran":

| platform | what actually happened | fixed in |
|---|---|---|
| Windows | `just test-ci` shells to `scripts/fd-budget.sh`; `set windows-shell := pwsh` cannot execute a `.sh`. Died in 0.4 s. **8 consecutive runs x 0 tests.** | `7e31e538` |
| macOS | clippy failed at step 11, so step 12 `Run tests` was **skipped**. Same shape that hid Windows. | `bb2e3d0e` |
| Linux (F01) | the only leg that ran these 4 tests at all; nextest fail-fast then hid a 3rd failure behind "1/4 tests were not run" | `41e0026d` |

The deeper fact, which outlived all three: **this project's gates have repeatedly
been unable to reach one of their two states**, and a gate that cannot fail and a
gate that cannot pass are indistinguishable from outside. Seven instances are
catalogued in §8. §10 is the mechanism that stops the eighth.

## 1. RULES (violating either cost real time this session)

**NEVER run `cargo` on Sean's Mac.** Not build/test/clippy/check/nextest, not
"just one crate". `cargo fmt` only. The previous handoff said the Mac "does
compile Rust" — it is *capable*, it is not *allowed*. Acting on that drove the
machine to load average 46 and he interrupted twice.

**"Pre-existing" is not a disposition.** Sean built all of this; there is no
upstream to blame. Establishing that a defect predates your change proves you
did not regress it and sets repair ORDER — then you still fix it. He has
corrected this twice; it is also a Project Learning in AGENTS.md.

**Decide, don't park.** Only main-merge, PRs, tags, releases, closing issues and
credentials are Sean's. Everything else — including "is this policy?" — you
decide, record the evidence, and proceed.

| need | where |
|---|---|
| Linux gate | `ssh hetzner-dsm`, 96 cores, `/root/.cargo/bin/cargo` (not on non-interactive PATH) |
| Windows | `ssh SeanD@seandesktop`, `D:` only, **NEVER** `C:\actions-runner-*`; sshd kills the tree on disconnect → `Invoke-CimMethod Win32_Process Create` + poll a log |
| macOS **check/clippy** | hetzner cross-compiles Darwin now — see §5 |
| macOS **tests** | CI only |

---

## 2. STATE

**Integration `plan/f20-unified-audit-repair` = `4c19e58e`** (plus local docs
commit `201da671`). Gated on hetzner: `SHA_OK`, `FMT_OK`, `LOCKED_OK`, clippy
clean on Linux AND `aarch64-apple-darwin`, **13,740 / 13,740 passed, 0 observed
failures, baseline exactly matched**.

Landed 2026-08-01 after the measurement: gate ordering, the live-docker lint gap
plus the defect it found, the junit.path fix, and the `lane/w9-authority` merge.

## 3. PATH TO RC

Superseded by §11 — read that, not this. In one line: **a release cannot be cut
at all right now** (§9, R1), and the evidence chain must be fixed before any
green can be trusted as evidence.

## 4. WHAT LANDED, AND THE FOUR TIMES I WAS WRONG

Closed this session: macOS root causes **B** (Darwin EPERM + its paired
unfalsifiable containment proof), **D** (`ps` field list two-way for a
three-platform matrix), **F** (Gatekeeper 26.4 s vs a 5 s probe budget), **G**
(attach could not establish containment for any fast-exiting root), the Windows
lint blocker, W17, W3, the nightly soak's green-on-zero-tests, and two F01
failures.

**Corrections I owe, all mine:**

1. "Windows CI ran its tests for the first time." **False.** The step failed
   having run ZERO tests. I read "step 12 = failure" as "tests failed".
2. "The Windows skip is a rustc crash." **False.** Zero matches for that crash
   in 25 runs. It was clippy, in a file the ci-clippy lane never touched.
3. "W14 needs a runner credential fix from Sean." **Refuted.** Already closed by
   `c73ac417` on 07-30; the measurement that filed it predates the fix.
   **Nothing is owed by Sean.**
4. "The F01 failure is containerization." **False.** Reproduces byte-for-byte on
   plain hetzner, no Docker. F01 is simply the only place those tests ever run.

Also: the 4-way cross-audit found **two HIGH defects in my own work** — the
`/proc` census reading EACCES as "exited" (unanimous across all four legs), and
a dropped root-membership conjunct. Both fixed in `90861a56`.

---

## 5. macOS WITHOUT THE MAC (new capability, built this session)

`hetzner-dsm` now cross-compiles Darwin:

```sh
export SDKROOT=/opt/macsdk/MacOSX15.4.sdk
export CARGO_TARGET_AARCH64_APPLE_DARWIN_LINKER=zcc-macos
export CC_aarch64_apple_darwin=zcc-macos
export CXX_aarch64_apple_darwin=zcxx-macos
export AR_aarch64_apple_darwin=zar-macos
cargo clippy -p <crate> --all-targets --target aarch64-apple-darwin -- -D warnings
```

**Works:** `check` and `clippy`. This is not a nicety — it caught a `-D warnings`
dead-code break inside `#[cfg(target_os = "macos")]` code that was failing CI's
macOS leg while the separate `Lint` workflow passed green.

**Does not work:** `nextest archive` — zig's linker SIGSEGVs on the larger test
binaries. Next thing to try is `cargo-zigbuild`.

Three gotchas already solved: zig target query is `aarch64-macos-none`; the
wrappers MUST strip incoming `--target=`/`-target`/`-arch` (the `cc` crate emits
`--target=arm64-apple-macosx`, rustc emits `-arch arm64`); and do NOT pass
`--sysroot` alongside absolute `-L`/`-I`/`-F` or they get re-rooted.

---

## 6. MEASURED PLATFORM FACTS — do not re-derive

One `cc`-compiled C probe, seconds to run, no cargo:

```text
getpgid(unreaped zombie)                 -> -1 ESRCH
proc_pidinfo(PROC_PIDTBSDINFO, zombie)   -> -1 ESRCH
kill(-pgid, 0)   corpse-only group       -> -1 EPERM   <- NOT discriminating
kill(-pgid, SIGKILL) corpse-only group   -> -1 EPERM
setpgid(0, <reaped, empty group>)        -> -1 EPERM   <- the safety anchor
```

Linux answers all of these for a corpse. **Darwin answers none.** That single
difference is the root of B, of G, and of the docker-gate lane's blocked
dispatch.

```text
sizeof(kinfo_proc), KERN_PROC_PID DATA query =  648   <- the real stride
the same query's null-buffer SIZE query      = 3888   <- 6x over-report
first exec of a freshly copied 241 MB binary = 26.37 s <- Gatekeeper
second exec                                  =  0.04 s
```

---

## 7. THE FIRST THREE-PLATFORM MEASUREMENT — 2026-08-01, run 30680721949

| platform | run | passed | failed | skipped |
|---|---|---|---|---|
| Linux | 13,740 | 13,740 | **0** | 74 |
| macOS | 13,680 | 13,654 | **26** | 105 |
| Windows | 13,352 | 13,276 | **76** | 130 |

Worst platform is 99.43% green. This is a TAIL, not a broken product. 102 failures
collapse to roughly a dozen causes. Seven tests fail on BOTH non-Linux platforms
while Linux passes — that set was the highest-signal cut and is fully analysed.

**Retracted:** snapshot-substitution containment and credential plaintext fallback
were BOTH escalated as possible security defects and BOTH refuted. Enforcement is
live on macOS and Windows; only the reported path STRING differs. Proven four ways,
decisively by reproducing all four failures on Linux with the same binary just by
pointing TMPDIR at a symlink. Do not re-raise without new evidence.

## 8. SEVEN PERMANENTLY-RED GATES, ONE CLASS

Every one is a gate whose two states were never both observed. Absence of signal
wearing the costume of success.

1. anti-vacuity gate behind `if: runner.os == 'Linux'` in a matrix with no Linux entry
2. same gate, then placed BEFORE `actions/checkout` — ENOENT, could never pass
3. `.config/nextest.toml` junit.path doubled — killed THREE downstream gates
4. `packaged_driver_gate` killed at the 60s slow-timeout under the profile CI uses
5. the milestone clause rejecting a local receipt — unreachable, because our own
   signer stamped `Observed` over the honest `Unavailable`
6. seven `wcore-swarm` tests asserting a backend macOS deliberately refuses
7. `release-please.yml` `release` job — `on:` is `workflow_dispatch` only but the
   condition reads `github.event.head_commit.message`. **NEVER EXECUTABLE.**

1-5 are FIXED and merged. 6-7 are in flight.

## 9. THE DEFECT INVENTORY (from the 9-agent census + 3 analyses)

### Release-blocking
- **R1** `release-please.yml:36,:62` — the release job cannot run under its only
  trigger. **A release cannot be cut at all.** Everything else is downstream. S.
- **R2** `release.yml:317-333,551,568` — a missing `WAYLAND_RELEASE_ACCEPTANCE_SEED`
  skips seven integrity steps (manifest, SBOM, signing, trust-root, asset-set) but
  `Upload release assets` is UNCONDITIONAL. Publishes unsigned, reports green. S.
- **P1+P2** `credentials.rs:348-353` + `:602-611`,`:492-511` — keyring availability
  decided by a READ-only probe; a read-ok/write-fail host passes startup then kills
  every turn. The marker then PINS the unwritable backend permanently and
  re-validates with the same bad probe. **P2 must ship with P1 or P1 fixes nobody
  already affected.** Clears 13 of the 76 Windows failures. S/M.
- **P5** `grep.rs:254-263,:179-184,:283` — **this is #661, the data-loss defect**,
  still open on Windows because its regression test is `#[cfg(unix)]`. Grep reports
  "No matches found" for a target it could not OPEN, so the model concludes a symbol
  is undefined and deletes code. Confidently wrong instead of erroring. S.
- **R1-macOS** `sandbox_exec.rs:95-157` — seatbelt denies any path spelled through a
  symlinked ancestor. `scratch_dir()` is NOT canonicalized while `root` is, so the
  agent's own scratch is unmatched. Every Bash command touching `/tmp`, `$TMPDIR` or
  scratch fails, and `filter_macos_sandbox_noise` (`bash.rs:212-230`) STRIPS the
  diagnostic. VERIFIED BY DIRECT sandbox-exec PROBE, not inference.
- **E2/E3** `ci.yml:1301-1302,:1319-1361` — `report` cannot distinguish three-legs-
  green from one-leg-green-two-dark (every leg uploads a file named `junit.xml` and
  the download uses `merge-multiple: true`), and it `needs: ci` only while globbing
  an artifact `ci-linux` also uploads. S.

### Real, ship as known issue
- **P3** per-profile recovery key written to the OS credential store and NEVER
  deleted. Unbounded leak, one credential per profile. **This is what FILLED the
  Windows store and produced `Windows error code 8`** — i.e. it is the CAUSE behind
  P1's symptom. Do not let it hide behind a green probe test. M.
- **P4** `vfs.rs:406-418` — `observe_file` unimplemented off Unix. We ship an
  exactly-once filesystem guarantee that cannot execute on Windows; it fails at first
  crash recovery instead of at negotiation. Interim: refuse the capability up front.
- **P6** journal path divergence, **P7** card head-truncation (Linux passes ONLY
  because `/tmp` paths are short — any real project path breaks it), **R3** npm no-op.

### Not defects
7 `wcore-swarm` + 3 voice + several stale expectations — product correct, gates
ungated or environment-dependent.

## 10. THE MECHANISM THAT STOPS THE RECURRENCE

`.github/gate-ledger.yml` — one entry per gate across 218 steps / 30 jobs / 14
workflows. Three kinds: `gate` (needs a RED proof and a GREEN proof carrying a
positive count), `instrument` (cannot fail; MUST name a backing gate), `setup`
(verb deny-list so it cannot become the hiding place). Enforced by
`scripts/check-gate-ledger.py` in `lint.yml`.

The two rules that make it real:
- **UNDECLARED**: a workflow step with no ledger entry fails the build — that is how
  a NEW dead gate is caught on the PR that adds it.
- **body_digest drift**: change a gate and its proof is void; re-cite a run.

Plus a static trigger/condition coherence table (~40 lines) that catches the
`release-please.yml` class mechanically.

## 11. RC POSITION

**Not yet, and the reason is now precise rather than vague.** A release cannot be
cut at all (R1). Fix the evidence chain FIRST — it is all S-sized — because until
`report` can tell three-green from one-green-two-dark, you cannot tell whether the
next fix worked.

Honest prior: this was the FIRST Windows measurement and it found 27 real product
defects. More remain. An RC claiming Windows support on one run claims more than
the evidence carries.

**Audit gap, deliberately recorded:** the gate census was TRUNCATED partway through
`release.yml`, reaching roughly a third of its 45 named steps. R2 and R3 both came
out of the third that WAS audited, so the base rate there is high. The remainder is
assigned to `lane/release-evidence`.

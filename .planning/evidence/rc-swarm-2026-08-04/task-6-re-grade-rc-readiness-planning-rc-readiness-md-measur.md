# TASK 6 — RE-GRADE RC READINESS (.planning/RC-READINESS.md, measured 8897f04b) AGAINST HEAD 7accc0c1

**Confidence (self-reported):** partial

## Root cause

**NO, we cannot tag** — not because the seven listed blockers are open (five verified genuinely closed at HEAD, two are stale in our own favour), but because the ledger is INCOMPLETE in the direction that matters: at HEAD the workspace version is still `0.12.25` while the RC is `v0.12.26-rc.1` and `release.yml` derives everything from the tag with no guard, so tagging today ships a binary whose `--version` contradicts its own signed manifest; `27-C4` was declared RELEASE-BLOCKING by our own `CRITERIA-STATUS.md` on 2026-07-31 and never got a row here; the product connects to an MCP server, advertises 101 tools and can call none of them, observed live on a near-HEAD artifact and NOT root-caused; seven Windows tests fail (not five — swarm went 1→4 and nobody knows why); and the SBOM byte-determinism gate has, by HEAD's own commit message, never once compiled, so it has never produced a signal about the thing it names. Shortest honest path to a tag, in order: (1) bump the workspace version and add a tag-vs-`CARGO_PKG_VERSION` guard to `release.yml` — hours; (2) decide `27-C4` by dropping `voice` from `wcore-cli/Cargo.toml:31`'s default list, which retires the blocker in one line, or prove `voice_mode → transcribe_audio` end to end, which does not fit in an RC; (3) either fix the MCP hydration defect or write it into `KNOWN-ISSUES-rc.md` verbatim, because "advertises 101 tools, delivers zero" is exactly the bar Sean set; (4) re-drive the existing 17-step journey at the RC SHA on all three hosts — the driver exists and this is cheap; (5) get ONE green CI verdict on `7accc0c1` that includes the SBOM gate actually running; (6) fix or formally accept the seven Windows failures with the two unexplained regressions named. Everything else — 24-C1's 7-of-10 unmeasured adapters, 24-C3's macOS/Windows gap, the ~2,100-commit-stale Phase 28 certification, `observe_file` inert on Windows — is disclosable in KNOWN-ISSUES and does not have to block an *RC*, only a GA.

## Evidence

- crates/wcore-cli/src/cron.rs:492 — 24-C2 enforcement ALIVE at HEAD: `fn refuse_without_producer(t: &wcore_cron::Trigger) -> Result<()> { if let Some(reason) = t.no_producer_reason() { bail!(..."refusing to create a {kind} job: {reason}\nNothing was written.") } }`, called at :449 and :464 (the two add paths)
- crates/wcore-cron/src/trigger.rs:403 — `pub fn no_producer_reason(&self) -> Option<&'static str>` returns Some for Webhook/Poll, both strings containing "This job will never fire"; `crates/wcore-cli/src/cron.rs:363` prints `"      ^ WILL NEVER FIRE — {reason}"` in list
- crates/wcore-cli/tests/automation_cli_surface.rs:108,120 — the 24-C2 gate CAN fail: `.expect_err("a trigger nothing can fire must be refused, not persisted")` plus `"a refused trigger must write nothing"`; 9 tests, 0 `#[ignore]`
- crates/wcore-cron/tests/event_producer.rs:518 — `assert!(!Trigger::Webhook{..}.has_producer())` named explicitly so flipping the flag without shipping a producer reds it
- crates/wcore-browser/src/config_hint.rs:7 — 27-C2(a) enforcement ALIVE: `//! `[browser.policy]` — NOT `[browser]`.` with an inline guard at :90 `assert!(!snippet.lines().any(|l| l.trim() == "[browser]"), "snippet names [browser]; the loader reads [browser.policy]")`
- crates/wcore-agent/tests/browser_config_hint_roundtrip.rs:101,123 — 4 tests, 0 ignored: `the_text_the_operator_reads_contains_the_snippets_that_were_proven` binds the operator string to a proven snippet, `the_wrong_section_parses_silently_and_leaves_the_tool_disabled` is the negative control
- crates/wcore-cli/src/main.rs:485-486 — 23A-C1 ROW IS STALE: `#[arg(long, value_name = "SKILL_OR_PROCEDURE_ID")] skills_promote: Option<String>` — there is NO `hide` attribute. RC-READINESS claims it is "hidden and still exits 1"
- crates/wcore-cli/src/main.rs:2773 — `async fn run_skills_promote(id: &str) -> anyhow::Result<()> { wcore_cli::skill_govern::run_promote(id).await }` — real governed promotion, not the `bail!` RC-READINESS describes; all four verbs live at crates/wcore-cli/src/skill_govern.rs:96,212,238,256
- crates/wcore-cli/tests/skills_promote_advertised_and_works.rs:101 — `advertised_skills_promote_actually_promotes_and_records_provenance` drives the real binary `.args(["--skills-promote", "auto-alpha"])` against a real installed draft; 5 tests, 0 ignored
- crates/wcore-agent/src/bootstrap.rs:1015-1018 — 27-C2(b) enforcement ALIVE: `PluginCapabilitySet::from_verified(&verified_plugins).narrowed_to_live().await`, with the comment `// 27-C2(b): `from_verified` answers "is the plugin present and genuine?" — necessary for the capability, not sufficient`
- crates/wcore-agent/src/output/protocol_sink.rs:234-267 — `pub async fn narrowed_to_live(self) -> Self` clears `browser_suite`/`computer_use` on a failed `wcore_browser::liveness::probe` / `wcore_cua::liveness::probe`
- crates/wcore-agent/tests/capability_liveness_narrowing.rs:118-131 — explicit anti-vacuity assert: `"both probes returned Indeterminate ({browser_verdict:?}, {cua_verdict:?}); this run asserted nothing about narrowing"`; 3 tests, 0 ignored
- crates/wcore-agent/src/recovery_confidential.rs:94-110 — HEADLESS-KEYRING enforcement ALIVE: both refusal strings now name `[storage.credentials] backend = "keyring"`, `WAYLAND_VAULT_PASSPHRASE_FD` and `[session] enabled = false`
- crates/wcore-agent/src/recovery_confidential.rs:555,590 — `fn every_backend_value_the_messages_advertise_actually_parses` re-parses each advertised value through the real `CredentialsStorageConfig`, with `assert!(checked > 0, "no backend value was extracted ... the gate would pass vacuously; fix the extraction, do not delete the assert")`
- docs/ — HEADLESS-KEYRING's stated residual is ALSO closed: `WAYLAND_VAULT_PASSPHRASE` now appears in 4 files under docs/ (troubleshooting.md, advanced.md, json-stream-protocol.md, decisions/0003) against the row's measured 0
- Cargo.toml:155 — `version = "0.12.25"`. .github/workflows/release.yml:52 — `version="${RELEASE_TAG#v}"`. No step anywhere in release.yml compares the tag to `CARGO_PKG_VERSION`; `crates/wcore-cli/src/main.rs:1667` prints `env!("CARGO_PKG_VERSION")`
- crates/wcore-cli/Cargo.toml:31 — `default = ["remote-registry", "workflow", "monitor", "review_artifact", "voice"]`. `voice` still ships. .planning/CRITERIA-STATUS.md:67 grades 27-C4 PARTIAL and states it "is now RELEASE-BLOCKING for the reason the ledger itself pre-registered". `grep -c 27-C4 .planning/RC-READINESS.md` → 0
- crates/wcore-agent/src/engine.rs:12266 `if tool_name == "ToolSearch" && !*is_error { self.record_hydrated_tools(content); }`; :15327 `fn record_hydrated_tools`; :15448-15458 `apply_tool_deferral` un-defers hydrated names then folds the rest. The mechanism EXISTS at HEAD and .planning/HANDOFF-2026-08-04-rc-and-tvcontrol.md:326-349 records it observably not working on a near-HEAD artifact (built from lane commit 9007c2c6, 3 commits before HEAD)
- crates/wcore-agent/src/engine.rs:16770 `async fn hydrated_tool_admitted_to_next_turn_tools_and_dispatches` — the gate that should have caught it exists, uses the real `ToolSearchTool`, and PASSES in a 13928/13928 green gate. Its fixture is 3 MCP tools with `max_tools = Some(4)` and `mcp_curation = Off` (:16779-16785); live was 101 MCP tools under default curation. The test can fail, but it does not model the shipped path
- git rev-list --count 978f49d7..HEAD = 1991 commits, 578 crate files changed — the Linux/Windows 24-C5 journey receipts; eba6e9d7 (macOS) = 2003 / 580. crates/wcore-eval-scenarios/tests/journey_receipt_contract.rs carries 39 `#[test]`, 0 `#[ignore]`, and grades receipt SHAPE — nothing in CI asserts a published receipt matches the RC SHA
- crates/wcore-tools/src/vfs.rs:400-405 — still `"authoritative compare-exchange for ordinary host files is unavailable for {}"` off Unix; the durable filesystem-receipt / crash-reconciliation / rollback path remains inert on Windows. Listed in KNOWN-ISSUES-rc.md:121-131, in NO RC-READINESS row
- crates/wcore-tools/src/media_intake.rs:396-419 — F-M1-01 / #937 IS FIXED at HEAD by `resolve_ancestor_symlinks` (`82455bd6`): `/var` is a symlink to `/private/var` ... "refused every temp path on macOS (issue #937)". CRITERIA-STATUS.md:64 still publishes it as UNFIXED
- git log -1 7accc0c1 — "SBOM byte-determinism: the gate was never failing determinism — it was never COMPILING ... neither of these had ever produced a signal about the thing it names." The SBOM determinism gate has never run to completion; it is unproven, not proven
- crates/wcore-cli/tests/mcp_assistant_scoping_e2e.rs:294 `fn matching_assistant_dials_scoped_deferred_server` — one of the Windows CI failures named in HANDOFF-2026-08-04 §3, and it is a DEFERRED-MCP dial test. Correlation with the MCP-uncallable defect, not a diagnosis

## How to verify

All of the following are read-only or single-command; none were run by me on this Mac beyond `git`/`grep`.

1. Version mismatch (item 8) — settles in one line, no build:
   `grep -n '^version' Cargo.toml` → expect `0.12.25`; then `grep -n 'CARGO_PKG_VERSION' .github/workflows/release.yml` → expect 0 hits. Fixed when the version equals the intended tag AND release.yml has a step that fails if `cargo metadata` version != `${RELEASE_TAG#v}`.
2. RC-READINESS rows 4 and 6 (stale) — `grep -n 'hide' crates/wcore-cli/src/main.rs | sed -n '/skills_promote/p'` → 0 hits proves the flag is advertised; `sed -n '2773,2776p' crates/wcore-cli/src/main.rs` shows the delegation. Fixed when RC-READINESS says MET.
3. MCP hydration (item 10) — the discriminating measurement, on hetzner or the Mac using a downloaded artifact, NOT a local cargo build: configure the tvcontrol stdio MCP server, run headless against **Anthropic** (not only openai), ask for one `tv_health_check` call, and capture the outbound `tools[]` array with `RUST_LOG=wcore_agent::engine=debug`. Distinguishing observable: after the first successful ToolSearch, does `tv_health_check` appear in the NEXT request's `tools[]` with `deferred == false`? If yes on Anthropic and no on openai, the cause is the 128-tool provider cap interacting with the cap/curation union at `engine.rs:15240-15273`, not hydration. If no on both, `record_hydrated_tools` is not being reached — instrument `engine.rs:12266`.
4. Windows count (item 11) — `gh run list --branch <integration> --limit 10 --json status,name,conclusion` then read the JUnit from the Windows legs. Fixed = 0 failures, or each remaining failure named in KNOWN-ISSUES-rc.md with a cause.
5. SBOM gate (item 12) — one green run of the `Supply Chain` workflow on `7accc0c1` in which the determinism step actually executes (check the step log is non-empty, not just that the job concluded success — this repo has already been burned by a job concluding SUCCESS on zero tests, `CRITERIA-STATUS.md:159-170`).
6. Journey freshness (item 3) — re-drive `crates/wcore-eval-scenarios/bin/wayland-journey.rs` at the RC SHA on all three hosts and pass `--expect-commit <RC SHA>` to `journey_receipt_contract`. Fixed when three receipts carry the RC candidate commit.

## Mutant

Each proposed gate, and how to prove it is not vacuous:

- **Tag-vs-version guard (item 8).** Add a `release.yml` step: `test "$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[]|select(.name=="wayland-core").version')" = "${RELEASE_TAG#v}"`. **Mutant:** run the workflow with `tag_name=v9.9.9` against an unchanged `Cargo.toml` — the step must exit non-zero BEFORE any asset is built. If it passes, the guard is reading the wrong package or the wrong variable.
- **MCP hydration end-to-end gate (item 10).** The existing `engine.rs:16770` test must be re-fixtured to the shipped shape: ≥100 MCP tools, default `McpCurationPolicy`, and the provider cap the real provider imposes. **Mutant that must red it:** delete the two hydrated-name union blocks at `engine.rs:15052-15070` and `:15240-15273` (the curation and cap unions). Under the *current* 3-tool/cap-4 fixture, deleting them still reds the test — which is why it looks healthy. The real proof is the opposite direction: **re-run the current test with `mcp_curation` left at its default and 101 tools; if it still passes unchanged, the fixture was never the guard.** A gate that passes on both the shipped shape and the toy shape, while the shipped binary fails, is lying.
- **`27-C4` disposition (item 9).** If the chosen fix is dropping `voice` from `wcore-cli/Cargo.toml:31`, the gate is the existing linkage control inverted: assert the "cpal could not bind" string count is **0** in the default build and **1** with `--features voice`. **Mutant:** re-add `voice` to the default list — the 0-count assertion must red immediately.
- **Journey freshness (item 3).** Wire `--expect-commit ${GITHUB_SHA}` into a CI step that reads the three published receipts. **Mutant:** point it at the existing `978f49d7` receipts — it must red. Today nothing does, which is exactly why three 2,000-commit-old receipts are being read as current.
- **The two rows verified best (`27-C2(b)`, `HEADLESS-KEYRING`) already carry their own mutants in-tree** — `capability_liveness_narrowing.rs:118-131` and `recovery_confidential.rs:590` — and both were checked to be real assertions, not comments. They are the template the four gateless items above should copy.

## Unknowns

- **Item 10 is not root-caused and I could not root-cause it from source.** The hydration mechanism is present and correct-looking at HEAD, and its unit test is green. Whether the live failure is (a) the provider tool cap evicting hydrated MCP names before they reach the wire, (b) `record_hydrated_tools` never being reached because the ToolSearch result body is not the JSON array it expects, or (c) config keys silently ignored, is undetermined. The live report itself declines to separate (c) from (a)/(b). Only `openai`/`gpt-5` was ever driven; Anthropic — the default in Sean's config and the path with no 128-tool cap — is untested.
- **No CI verdict exists for `7accc0c1`.** I did not run CI and was not given artifacts for HEAD. Every Windows/macOS/SBOM statement above is inherited from `HANDOFF-2026-08-04-rc-and-tvcontrol.md` §3 measured at `1a07ce0b` (7 commits back), not measured by me at HEAD. The four failure counts could have moved in either direction.
- **The 7 Windows failures have no diagnosis.** `wcore-swarm` going 1 → 4 on a wave that touches no swarm file, and `every_reference_backend_…` regressing after being fixed, are both recorded as unexplained. "Does not touch it" is not a diagnosis and I did not improve on it.
- **Whether the three 24-C5 journey receipts would still pass at HEAD is unknown.** 578 crate files moved under them. The receipts are evidence that the journey passed on a tree that no longer exists; I have no basis to predict the outcome of a re-drive, only to say the evidence is not at this tree.
- **The `27-C2(b)` Desktop co-release is not observable from this repository.** Core's half is verified; whether Desktop has consumed the narrowed capability contract cannot be checked here.
- **`KNOWN-ISSUES-rc.md`'s test-count table is self-declared unmeasured** and I did not re-measure it. Its Windows figure (~30) and the handoff's (7) cannot both be current; I could not determine which is right without running the suites.
- **I did not verify the `Cargo.toml:155` version is the workspace-level one every crate inherits.** I read line 155 of the root manifest and `env!("CARGO_PKG_VERSION")` in `wcore-cli`; I did not confirm `wcore-cli/Cargo.toml` uses `version.workspace = true`. If it pins its own version the specific number could differ, though the absence of any tag-vs-version guard in `release.yml` holds either way.

## Proposed patch (NOT APPLIED, NOT COMPILED)

```diff
## Status at `7accc0c1` — re-graded 2026-08-04 (supersedes the `8897f04b` table)

Re-measured against HEAD source, not against the previous table. Five of the seven listed rows
are genuinely closed; **two are stale in our own favour**, and **six release-gating items exist at
HEAD with no row at all**. Every "can its test fail?" answer below was checked by reading the
assertion, not by reading the test name.

| # | Item | Claimed status (2026-07-28 @ `8897f04b`) | **VERIFIED status at `7accc0c1`** | Evidence `file:line` | Can its test fail? |
|---|---|---|---|---|---|
| 1 | `24-C2` — three advertised trigger kinds can never fire | BLOCKING ELEMENT CLOSED | **CONFIRMED CLOSED.** Refusal at add, list marks persisted jobs unreachable | `crates/wcore-cli/src/cron.rs:492` (`refuse_without_producer`, called `:449`,`:464`); `:363` `WILL NEVER FIRE — {reason}`; `crates/wcore-cron/src/trigger.rs:403` | **YES.** `automation_cli_surface.rs:108` `expect_err` + `:120` "a refused trigger must write nothing"; `event_producer.rs:518` names Webhook/Poll explicitly. 9+1 tests, 0 `#[ignore]` |
| 2 | `27-C2(a)` — browser remediation string sends users into an impossible loop | CLOSED | **CONFIRMED CLOSED.** Hint emits `[browser.policy]`, never bare `[browser]` | `crates/wcore-browser/src/config_hint.rs:7,29,37`, inline guard `:90` | **YES.** `browser_config_hint_roundtrip.rs:101` binds the operator text to a proven snippet; `:123` is the negative control (wrong section → tool still disabled). 4 tests, 0 ignored |
| 3 | `24-C5` + `24-C1` — macOS/Windows setup-to-recovery journeys | ✅ MET on all three platforms (`5ed01866`) | **STALE — OVERSTATED, AND IT CONFLATES TWO ROWS.** `24-C5` receipts are real (17/17 ×3) but were taken **1,991–2,003 commits and 578–580 crate files ago**. `24-C1` is **PARTIAL**, not MET: exactly-once is **1 of 10** adapters and only **below `max_message_len`** | receipts: `.planning/phases/24-…/24-C5-finish-evidence/{linux,windows,macos}-journey.log`; staleness: `git rev-list --count 978f49d7..HEAD`=1991; grade: `.planning/CRITERIA-STATUS.md:57,61` | **NO, on the risk that matters.** `journey_receipt_contract.rs` has 39 tests / 0 ignored but grades receipt **shape**. Nothing reds when a receipt is 2,000 commits stale — `--expect-commit` exists (`:166`) and no CI job passes the RC SHA to it |
| 4 | `23A-C1` — `--skills-promote` advertised and always fails | BLOCKING ELEMENT CLOSED — "hidden and still exits 1"; "real governed promotion is **not** in this RC" | **STALE, WRONGLY PESSIMISTIC.** At HEAD the flag is **not hidden** and promotion is **real**: no `hide` attribute, and the handler delegates to governed promotion instead of `bail!`ing. `CRITERIA-STATUS` grades the row **MET** | `crates/wcore-cli/src/main.rs:485-486` (no `hide`); `:2773` → `wcore_cli::skill_govern::run_promote`; `crates/wcore-cli/src/skill_govern.rs:96,212,238,256` | **YES.** `skills_promote_advertised_and_works.rs:101` drives the real binary against a real installed draft; `:57` asserts `--help` advertises it. 5 tests, 0 ignored |
| 5 | `27-C2(b)` — capability flags lie to Desktop | BLOCKING ELEMENT CLOSED, Desktop train owed | **CONFIRMED CLOSED (Core side).** Advertisement narrows on a probe that can fail. Desktop co-release still owed and **not observable from this repo** | `crates/wcore-agent/src/bootstrap.rs:1015-1018`; `crates/wcore-agent/src/output/protocol_sink.rs:234-267` | **YES, and it is the best-built gate in the ledger.** `capability_liveness_narrowing.rs:118-131` carries an explicit anti-vacuity assert ("this run asserted nothing about narrowing") plus a never-widen security assert. 3 tests, 0 ignored |
| 6 | `24-C3` — inbound channel matrix never driven end to end | IN FLIGHT | **STALE — WRONGLY OPEN.** The row's own named unmet clause was driven at a real destination three days after this table was written: a real agent turn answered into a real private Slack channel and read back off Slack's API. Grade moved NOT MET → PARTIAL | `.planning/CRITERIA-STATUS.md:59`; `.planning/UAT-CHANNELS-LIVE.md` | **PARTLY.** `channels_doc_configs_load.rs` (doc TOML through the real loader, proven can-fail by mutating the doc) and `delivery_semantics_declaration` (8 passed) can fail. **Nothing gates the residual**: macOS and Windows have *no* coverage, 7/27 UAT cells need a human at a keyboard, and the row's `idempotency` clause **has no reachable pass state** (`CRITERIA-STATUS.md:113-128`) |
| 7 | `HEADLESS-KEYRING` | BLOCKING ELEMENT CLOSED; `docs/` + `--help` gap still open | **CONFIRMED CLOSED — and the stated residual has closed too.** Both refusal strings name values that parse; `WAYLAND_VAULT_PASSPHRASE` now appears in **4** files under `docs/` against the row's measured **0** | `crates/wcore-agent/src/recovery_confidential.rs:94-110` | **YES.** `:555` re-parses every advertised value through the real `CredentialsStorageConfig`, with `assert!(checked > 0, "…the gate would pass vacuously; fix the extraction, do not delete the assert")` at `:590` |

### Items that are release-gating at `7accc0c1` and appear in NO row

| # | Item | Claimed status | **VERIFIED status at `7accc0c1`** | Evidence `file:line` | Can its test fail? |
|---|---|---|---|---|---|
| 8 | **Workspace version does not match the RC tag** | not tracked anywhere | **OPEN, RC-GATING, unlisted.** `Cargo.toml` says `0.12.25`; the RC is `v0.12.26-rc.1`. `release.yml` derives the manifest, SBOM and asset names from the **tag**; nothing compares the tag to `CARGO_PKG_VERSION`. Tag today and `wayland-core --version` prints `0.12.25` while its signed manifest says `0.12.26-rc.1` | `Cargo.toml:155`; `.github/workflows/release.yml:52`; `crates/wcore-cli/src/main.rs:1667` | **NO TEST EXISTS.** `release_integrity_contract.rs` has no tag-vs-package-version assertion. This is the cheapest possible violation of "nothing lies to the user" |
| 9 | **`27-C4` voice — declared release-blocking by our own ledger, never given a row** | absent from RC-READINESS | **OPEN, RC-GATING.** `voice` is in the shipped default feature list; `CRITERIA-STATUS.md` states the criterion "is now RELEASE-BLOCKING for the reason the ledger itself pre-registered". `voice_mode → transcribe_audio` in one agent flow is UNPROVEN on all three platforms; #938 (provider 402) open | `crates/wcore-cli/Cargo.toml:31`; `.planning/CRITERIA-STATUS.md:67`; `grep -c 27-C4 .planning/RC-READINESS.md` → **0** | **NO end-to-end gate exists.** The feature-linkage control (cpal string present/absent) can fail; the *handoff* between capture and transcription has no instrument. One-line fix available: drop `voice` from the default list |
| 10 | **Deferred MCP tools connect but cannot be called** (TVControl masterclass blocker) | absent | **OPEN, RC-GATING, NOT ROOT-CAUSED.** Live on a near-HEAD artifact: `[mcp] Connected to 'tvcontrol': 101 tools`, then no tool callable — ToolSearch returns the same hit 10 turns running until the no-progress guard kills the run. Product advertises capability it does not have | mechanism: `crates/wcore-agent/src/engine.rs:12266`, `:15327`, `:15448-15458`; `crates/wcore-tools/src/tool_search.rs:87`; observation: `.planning/HANDOFF-2026-08-04-rc-and-tvcontrol.md:326-365` | **THE TEST EXISTS AND CANNOT CATCH IT.** `engine.rs:16770 hydrated_tool_admitted_to_next_turn_tools_and_dispatches` uses the real `ToolSearchTool` and is **green** in a 13928/13928 gate — but its fixture is 3 MCP tools, `max_tools=4`, curation `Off` (`:16779-16785`). Live was 101 tools under default curation. Worst state in this ledger: a green gate that does not model the shipped path. **Not root-caused** — only `openai`/`gpt-5` was exercised, and two of three config knobs produced no observable change with "config ignored" vs "hydration broken" never separated |
| 11 | **Windows CI failures** | brief said 5 (2 swarm, 1 exec-backend, 2 wcore-cli) | **OPEN — and the count in the brief is already stale. Windows hosted is 7**, not 5: `packaged_f04`, `matching_assistant_dials_scoped_deferred_server`, `every_reference_backend_…`, **plus FOUR** `wcore-swarm`. Swarm went **1 → 4** across a wave that touches no swarm file, and `every_reference_backend_…` **regressed after** the wave had fixed it. Both unexplained | `.planning/HANDOFF-2026-08-04-rc-and-tvcontrol.md:152-176`; `crates/wcore-cli/tests/mcp_assistant_scoping_e2e.rs:294` | **NO GATE.** These *are* the tests. Note `matching_assistant_dials_scoped_deferred_server` is a **deferred-MCP dial** test — correlate with item 10, do not assume |
| 12 | **SBOM byte-determinism gate has never produced a signal** | absent | **UNPROVEN, not proven.** HEAD's own commit message: the gate "was never failing determinism — it was never COMPILING … neither of these had ever produced a signal about the thing it names." `7accc0c1` makes it *able* to run; **nobody has yet seen it green** | `git log -1 7accc0c1`; `.github/workflows/supply-chain.yml` | **Newly able to fail; never observed either way.** One green CI run on `7accc0c1` settles it |
| 13 | **`RealFs::observe_file` unimplemented off Unix** | in KNOWN-ISSUES only | **OPEN at HEAD, deliberately held.** Durable filesystem-receipt, crash-reconciliation and rollback are **inert on Windows** | `crates/wcore-tools/src/vfs.rs:400-405`; `.planning/KNOWN-ISSUES-rc.md:121-131` | **Correctly held unfixed** (the candidate fix has no tests on any platform). Disclosed, not gated — acceptable for an RC *if* it stays disclosed |
| 14 | **#172 P6 + Windows steal semantics** | absent | **OPEN, listed RC-GATING by the current handoff.** The cross-process refresh proof landed and is green on all three platforms; P6 (lock unavailable → reload, zero POSTs) and Windows steal semantics for a tens-of-seconds hold are still owed | `.planning/HANDOFF-2026-08-04-rc-and-tvcontrol.md:86-94, 181-186` | Partly — `refresh_cross_process.rs` P1 is green everywhere with a documented RED mutant; P6 has no test yet |
| 15 | **Credential ladder never exercised against a real OS keyring** | in KNOWN-ISSUES only | **OPEN.** Landed 2026-08-01 with test coverage and **no live keyring exercise on any platform** | `.planning/KNOWN-ISSUES-rc.md:136-137` | **Coverage is unit-level only.** Nothing in CI touches a real keyring |
| 16 | **Phase 28 certification is ~2,100 commits stale** | in KNOWN-ISSUES only | **OPEN and correctly disclosed.** This is *the* reason this is an RC, not a GA — but it belongs in the blocker ledger, not only in the user-facing file | `.planning/KNOWN-ISSUES-rc.md:233-247` | N/A — it is a provenance fact, not a gate |

### Where our three ledgers contradict each other

Each of these is itself a finding.

1. **`23A-C1`.** `RC-READINESS.md:104-106` — "`--skills-promote` is hidden and still exits 1 … Real governed promotion is **not** in this RC." `CRITERIA-STATUS.md:56` — "**MET** (shipped surface) … `run_skills_promote` delegates rather than `bail!`s." **Source at HEAD sides with CRITERIA-STATUS**; RC-READINESS is wrong.
2. **`24-C1`.** `RC-READINESS.md:11` folds `24-C1` into `24-C5` and marks the pair "✅ **MET on all three platforms**." `CRITERIA-STATUS.md:57` grades `24-C1` **PARTIAL**, and `KNOWN-ISSUES-rc.md:210-229` publishes at-most-once for Slack/Discord and exactly-once for Matrix **only below the cap**. RC-READINESS republishes a guarantee the other two documents have already withdrawn.
3. **`24-C3`.** `RC-READINESS.md:14` "IN FLIGHT". `CRITERIA-STATUS.md:59` upgraded it three days later on evidence RC-READINESS never absorbed.
4. **`27-C1` / #937.** `CRITERIA-STATUS.md:64` publishes F-M1-01 as **UNFIXED** ("every one of the six consolidated media surfaces refuses every path the platform's own temp APIs hand out"). It is **fixed at HEAD** — `crates/wcore-tools/src/media_intake.rs:396-419`, commit `82455bd6`. A stale RED is as damaging as a stale GREEN: it makes a closed macOS defect look open in the document a planner reads first.
5. **`27-C4`.** `CRITERIA-STATUS.md:67` declares a criterion RELEASE-BLOCKING; the release-blocker ledger has zero mentions of it. The two files were written five days apart and never reconciled.
6. **Windows test counts.** `KNOWN-ISSUES-rc.md:32` publishes ~30 Windows failures at `2280e205` and says so itself ("**The test-count table below was NOT re-measured**"); the 2026-08-04 handoff measures 7 on Windows hosted and 1 on Array. Both cannot be current. The user-facing document is carrying the older, worse number.
7. **Release trust root.** `RC-READINESS.md:125-127` — "Until both exist, `self-update` **installs nothing**." Both **do** exist at HEAD: `release.yml:295-543` builds, signs and verifies the manifest against the trust root extracted from the shipped binary. What remains Sean's is the **secret** (`WAYLAND_RELEASE_ACCEPTANCE_SEED`, guarded at `:317-333`), not the wiring.

### Scoreboard

- **Rows verified correct:** 3 of 7 (`24-C2`, `27-C2(a)`, `27-C2(b)`) — plus `HEADLESS-KEYRING`, whose residual has also since closed, = **4 of 7**.
- **Rows stale in our favour (wrongly open / wrongly pessimistic):** 2 (`23A-C1`, `24-C3`).
- **Rows stale against us (wrongly closed / overstated):** 1 (`24-C5`+`24-C1` — receipts 2,000 commits old, and `24-C1` is not MET).
- **Release-gating items with no row:** 6 (items 8–12, 14).
- **Rows whose test cannot fail on the stated risk:** 1 (`24-C5` freshness). **Items whose test exists, is green, and cannot catch the observed failure:** 1 (item 10) — that is the more dangerous shape and it is new.
```

CHOICE=B

Land the source-of-truth fix while deferring only the generated artifact. This leaves a single, explicit integration seam: committed JSON is stale and must be regenerated by the orchestrator.

Choice A deliberately poisons the entire merge train with defect-measuring RED gates, obscures unrelated regressions, and leaves the known generator bug intact. Its synthetic GREEN test proves a hypothetical patch but does not make the product correct.

With B, all semantic regression gates pass immediately against in-memory generator output. The only failures are the expected byte-drift sentinels, which precisely identify the reserved final action. The orchestrator then regenerates once—after all serial merges—eliminating churn and producing the canonical release artifact.
hook: Stop

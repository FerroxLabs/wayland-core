# Wayland Core Frontier Candidate v2

## What This Is

Wayland Core is a Rust 2021 multi-provider agent engine with a standalone CLI/TUI and a versioned host protocol consumed by Wayland Desktop. This milestone finishes the existing Frontier program from F20 onward, then extends it with operator-complete continuous agency, gateway, ecosystem, migration, multimodal, and release-proof contracts.

## Core Value

Deliver a best-in-class cross-platform agent that stays simple in ordinary use, bounded and auditable under managed policy, crash-complete, transactionally delegated, operator-complete, and honestly proven through the packaged product.

## Business Context

- **Customer**: Individuals and teams that need a capable daily agent with optional enterprise-grade control and evidence.
- **Revenue model**: Wayland product value and differentiation; this Core milestone does not define pricing.
- **Success metric**: One exact candidate satisfies every active requirement with zero unresolved findings, required native-platform evidence, packaged standalone/host journeys, and evidence-backed peer positioning.
- **Strategy notes**: The winning position is Hermes-grade persistent agency and OpenClaw-grade product completeness on Wayland's stronger authority, recovery, transaction, and evidence spine.

## Requirements

### Validated

- ✓ F00-F19 architecture and accepted evidence remain preserved as the historical baseline; they are not re-executed or discarded by this milestone.
- ✓ F00-F19 entered Phase 20 from source `97e44910fc6dd4761f1f862dbf54a5a76262cef2` (tree `8d27bd96b476a728d3ebbe0e1583c6488dd5effc`); `b9cc6698f2b43a04f1b4deee7064def8f754d9e7` (tree `cf97d5feda6099d39ab6484cc4fcaf06458f15fe`) is the authoritative prerequisite types contract, while `626e1d4d3dee9fee7008ad172ec0b4add8f2004e` is the accepted stock-GSD 20-01 product commit. The planning-control repository need not resolve those source objects and is not source authority.
- ✓ Existing F22A-F22D define the canonical Goal, Fleet, strategy, and bounded-loop architecture; no parallel lifecycle may be introduced.
- ✓ The versioned peer taxonomy, live field-regression register, and Core↔Desktop D1/D2 checkpoints are program admission controls from Phase 20 onward.

### Active

- [ ] Complete F20 transactional delegated mutation and integrate its accepted successor.
- [ ] Complete F21-F23 authority, supervision, durable Goal/Fleet/loop, and governed-learning contracts.
- [ ] Refresh operator completeness and peer maturity at every admitted phase, with F30 reserved for independent final positioning.
- [ ] Complete gateway, automation, channel, typed API, remote/node, plugin, migration/portability, browser/media/voice, certification, supply-chain, and final peer-review outcomes.
- [ ] Reproduce and route contradictory packaged-customer evidence without letting historical acceptance silently settle it.

### Out of Scope

- Wayland Desktop implementation — Core owns stable capability, policy, resource, and protocol truth; Desktop requires a linked plan at the protocol checkpoint.
- A clean-slate rewrite of F00-F23 — accepted F00-F19 source and historical evidence are preserved; standard GSD summaries begin with the replacement Phase 20 execution, and partial F20-F23 work is salvaged only when it satisfies the current contracts.
- Cloud-provider proliferation — F25 proves one hibernating cloud reference backend plus an extensible contract, not every vendor.
- Source push, main merge, issue closure, release, deployment, or production canary — each remains a Sean-only authorization action. The sole Phase 20 exception is one exact candidate-specific UAT evidence branch and one native-proof dispatch after its explicit checkpoint; Phase 20 creates no proof tag. The UAT ref remains retained for evidence until Sean separately authorizes exact-ref deletion. That checkpoint authorizes the plan's exact-SHA, UAT-ref-only publication helper as a narrow exception to `just push`; it performs no Cargo, formatting, hook, auto-commit, main-ref, or source mutation.
- Superiority claims based on source presence, types, unit tests, or local-only proof — positioning requires the F28-F30 evidence chain.

## Context

- The source repo currently has 56 linked worktrees. They are inventory until classified and integrated; branch existence is not progress.
- F20 has accepted snapshot/session-journal authority, salvageable journal/AppContainer/workspace slices, and genuinely unbuilt candidate-gate, parent-CAS/rollback, and complete-lifecycle work.
- Wayland is already strong in authority, containment, recovery, transactional delegation, provider-neutral orchestration, and evidence design.
- Hermes is ahead in persistent completion, session/operator lifecycle, proactive automation, and execution reach. OpenClaw is ahead in gateway/channel/app/plugin product completeness.
- The canonical plan and evaluation charter remain authoritative. `.planning/intel/PLAN-V2-ADVISORY.md` is an additive amendment, not a replacement.

## Constraints

- **Execution authority**: Standard GSD executes product plans and writes its standard summaries in one clean source checkout. GitHub mirrors cross-lane coordination and is not a second execution authority.
- **Candidate authority**: Exactly one integration candidate advances through one serial execution sequence from reconciled source `94f014d039b8babf3f5926385a3bbc5cb5cf3c41` and its independently accepted planning successor; `depends_on` expresses only genuine product dependencies.
- **WIP**: Phase 20 executes one dependency-unlocked product plan at a time. Read-only review may overlap; source mutation remains serial.
- **Dependency**: Phases 20-23 remain serial. After Phase 23 admission, Phases 24-27 may execute as bounded parallel workstreams with shared protocol/schema/config seams serialized; Phase 28 is their fan-in.
- **Platform**: Rust 2021 stable on macOS, Linux, and Windows; no Cargo on this Mac.
- **Proof routing**: Authoritative Linux Cargo proof runs on `hetzner-dsm:/root/wayland`; native Windows and macOS evidence is required at declared gates.
- **Security**: Managed authority cannot widen; dangerous local execution is explicit, bounded, visible, and policy-controlled.
- **Compatibility**: Preserve CLI/TUI/host-protocol behavior or version migrations explicitly; Desktop must never consume an uncommitted dirty Core checkout.
- **Evidence**: Code presence is not completion; exact candidate, fixture, packaging, deployment, canary, and release states remain distinct.
- **Findings**: BLOCKER, CRITICAL, HIGH, MEDIUM, and LOW findings are repaired or rejected with evidence before acceptance; zero unresolved findings remain.
- **Local trust boundary**: Standard GSD treats the local same-UID Git process and repository filesystem as its trusted coordination substrate. Phase 20 does not claim cryptographic resistance to a same-UID actor that can rewrite Git history, scripts, and Git-private state; that requires an external signer or isolated control plane and is outside this pure-GSD execution milestone.
- **Coordination host**: Git-private task-authority scripts are POSIX coordinator tooling and run on the Mac or Hetzner coordinator only. They are not a native-Windows portability claim; Windows product behavior is proved by the candidate-specific native workflow.

### Standard GSD Acceptance Contract

- Phase 20 consists of eighteen dependency-ordered stock-GSD plans. Only terminal plan 20-18 carries the phase requirement IDs, so no predecessor summary can mark Phase 20 complete. Plan 20-15 independently reviews the exact 20-03 isolation source before 20-04; plan 20-16 independently reviews the exact final 20-08 construction before preparation in 20-17 and terminal native/aggregate acceptance in 20-18. Custom plan executors, dispatch ledgers that compete with GSD state, and generic receipt frameworks are prohibited; independent reviews remain ordinary stock-GSD plans with repository-local review-pair checks. The narrowly scoped native-UAT helper is an evidence collector, not a plan executor.
- `workflow.use_worktrees=false` and plan parallelization remain disabled. Codex executes serially in one clean standalone Phase 20 clone whose `.git` is a directory, not a linked worktree. This preserves stock GSD's normal STATE/ROADMAP updates while avoiding shared-worktree authority. Terminal native evidence may use one deterministic read-only source-SHA-keyed proof checkout whose locator and state live in that standalone clone's Git directory; it is not a second source-mutation or GSD execution authority.
- Every product-source plan contains implementation, hostile tests, and focused Hetzner verification; review, preparation, and terminal plans contain only the work their declared boundary requires. Every plan produces a normal GSD SUMMARY. Cargo never runs on this Mac.
- Preparation plan 20-17 reauthenticates the 20-16-qualified source and persists one deterministic pending tuple without external mutation. Terminal plan 20-18 accepts only Sean's exact-tuple authorization, runs native Windows/macOS acceptance and the configured remote build/workspace test once against that source, and alone may update requirements. The installed phase verifier authenticates those receipts and reruns only after source or evidence-integrity drift. Timeout, failure, or incomplete output blocks verification; an advisory `Continue` path is not acceptance.
- Plan 20-16 is the blocking non-author boundary for code review, phase validation, and `gsd-secure-phase`-equivalent ASVS Level 2 prosecution. It records one combined exact-source review and admits 20-17 only with zero findings at every severity. The verified 20-17 summary alone admits 20-18; later advisory stock commands may authenticate those results but cannot replace them.
- Native Windows and macOS evidence are separate human-authorized verification items bound to the same exact final candidate and one newly dispatched workflow run. They require Sean's explicit candidate-specific publication authorization and cannot be inferred from source, cross-compilation, Linux proof, prose, an unavailable runner, or a reused run.
- Only standard GSD plan state, exact Git identity, test output, validation, security review, and authorized native evidence determine Phase 20 completion. A SUMMARY records progress but does not by itself prove release readiness.
- No source push, main merge, issue closure, release, deployment, canary, or frontier claim occurs without Sean's explicit authorization.
- New Phase 20 execution starts with generation-based task authority in the clean standalone clone. A legacy two-line `gsd-task-base-*` object is deliberately rejected rather than silently migrated; any such obsolete state requires explicit reconciliation before a fresh generation is started. Non-task two-line source/review authority objects remain supported.

## Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Preserve accepted F00-F19 and salvage partial F20-F23 work | Existing safety/recovery work is valuable, but only admitted work counts as accepted | ✓ Good |
| Keep F20 as the active admission gate | Moving the finish line would compound the current branch/worktree entropy | — Pending |
| Treat operator completeness as a release contract | Reachable source code is not an end-to-end product | ✓ Good |
| Compare peers continuously from the current milestone | End-only comparison discovers architecture gaps too late | — Pending |
| Split F24-F27 into bounded plans within canonical phases | Preserves task identity while making execution falsifiable | — Pending |
| Extend F12 only through a proved compatibility migration | Reopening accepted durable-state work without evidence creates churn | ✓ Good |
| One candidate through eighteen dependency-ordered plans | `depends_on`, exact commits, focused remote checks, standard summaries, independent review, and one terminal aggregate gate prevent divergent branches from masquerading as progress | ✓ Good |

---
*Last updated: 2026-07-19 after rejecting the invalid 22-plan chain and returning Phase 20 to pure GSD execution*

---
phase: 29-supply-chain-release-integrity
plan: "04"
type: execute
wave: 4
depends_on:
  - "29-03"
files_modified:
  - crates/wcore-eval-scenarios/tests/supply_chain_tamper_corpus.rs
  - .planning/phases/29-supply-chain-release-integrity/29-04-TAMPER-RESULTS.md
  - .planning/phases/29-supply-chain-release-integrity/29-PHASE-VERDICT.md
  - .planning/phases/29-supply-chain-release-integrity/evidence/29-04/
  - .planning/SEAM-REQUESTS/29.md
  - .planning/BACKLOG.md
autonomous: true
requirements:
  - F29-03
  - F29-04
must_haves:
  truths:
    - "TAMPER REJECTION IS PROVED BY TAMPERING, NEVER BY SIGNING AND VERIFYING. A test that mints a manifest and then verifies it proves the happy path and says nothing whatsoever about refusal. Every rejection claim in this plan is a PAIR: the pristine input is verified and ACCEPTED, then exactly one field or one byte is mutated and the same verification is run again and REFUSED. Both halves are recorded. A corpus that only ever asserts refusals is passed trivially by a verifier that refuses everything, which is the supply-chain shape of a gate that cannot go red."
    - "THE PAIRING IS ENFORCED BY THE DATA STRUCTURE, NOT BY REVIEWER DISCIPLINE. A case in this corpus is a value that carries both a pristine input and its mutation, and the runner asserts accept-then-refuse for every case it is given. A case missing its control cannot be constructed. That is the difference between a rule a reviewer must check eleven times and a rule the compiler checks once — and this program has already measured what happens to rules that depend on a reviewer asking the same question dozens of times."
    - "F29-03 NAMES SEVEN OBJECT CLASSES AND ALL SEVEN MUST APPEAR: binaries, SBOMs, updates, plugins, backend receipts, manifests, and keys. A corpus that covers manifests and keys thoroughly and leaves plugins and backend receipts to an argument has covered two-sevenths of the requirement. Where a class can only be reached at the release-manifest layer because its runtime path belongs to another phase, the case still exists at that layer AND the residual is named in the verdict rather than absorbed."
    - "CRITERION 4 IS PROVED BY BOTH RUNS, NOT ONE. Demonstrating that the four-state chain verifies when all four role keys are present is the POSITIVE CONTROL — without it, the separation proof is a rejection-only corpus. Demonstrating that withholding the release-acceptance key CAPS progress at rollback rehearsal is the separation itself. Both runs are required, both are captured, and the verdict states both. Either alone is worth very little."
    - "REACHING RELEASE ACCEPTANCE IN THE CLEAN ROOM IS NOT THE SAME AS ACCEPTING A RELEASE, AND CONFLATING THEM WOULD BE THIS PHASE'S CHARACTERISTIC FAILURE. The clean-room run generates all four role keys itself, so of course it can complete the chain — that proves the MECHANISM works. In a real release the acceptance key is Sean's and this lane will never hold it. The verdict must say both sentences plainly: the mechanism is proved, and no release was accepted. An agent that reports the first without the second has redefined success downward exactly the way the invented \"termination state 4\" did."
    - "THE PHASE VERDICT IS GRADED VERBATIM AGAINST THE ROADMAP'S FOUR SUCCESS CRITERIA AND NOTHING ELSE. Not against the plans, not against what was built, and not against a reworded version that happens to match the evidence. Quote each criterion as written, grade it MET, PARTIAL or NOT MET, and attach the evidence that settled it. Phase 21 declared its own goal NOT ACHIEVED twice and that was the correct and most useful outcome available; Phase 27 graded four of five criteria NOT MET and that grade is why the phase is trustworthy. An honest PARTIAL is worth more than a manufactured MET and costs the program far less."
    - "EVERY REAL-KEY AND REAL-ACCOUNT LIMIT IS COLLECTED IN ONE PLACE AT THE END. Across the phase there are several: the Sigstore attestation ACCEPT path needs a real published release; the bundled release trust root needs Sean's real public keys; the marketplace plugin index needs Sean's real signing key and still ships the all-zeros placeholder; the release-acceptance role key is Sean's by design; and Phase 28's certification receipts need real native hardware. Each is enumerated with its exact substitution point so a reader knows precisely what changes on the day a real key exists, and none of them is graded as met."
    - "MEDIUM AND BELOW GO TO BACKLOG AND DO NOT BLOCK. The severity policy is fixed and this plan does not tighten it. CRITICAL and HIGH must be fixed or disproved with executable evidence; everything else is filed, non-blocking. Inventing a stricter rule at the end of a phase is what turned Phase 20 into a seventy-four-plan loop lasting two weeks, and it is explicitly forbidden here."
    - "THIS PLAN ADDS ONE FILE UNDER `crates/` AND NOTHING ELSE. The corpus is a test. If closing a tamper case appears to require a production change, that is a FINDING for the verdict with a severity, not a repair to make here — the proof and the repair must not be authored by the same pass, because a corpus written by the hand that fixed the defect proves only that the author knows what they fixed."
  artifacts:
    - path: crates/wcore-eval-scenarios/tests/supply_chain_tamper_corpus.rs
      provides: "The paired tamper corpus covering all seven F29-03 object classes, where a case without a pristine control cannot be constructed"
    - path: .planning/phases/29-supply-chain-release-integrity/29-04-TAMPER-RESULTS.md
      provides: "The per-case accept-then-refuse result table and the four-state separation runs, both the all-keys control and the withheld-acceptance-key proof"
    - path: .planning/phases/29-supply-chain-release-integrity/29-PHASE-VERDICT.md
      provides: "The four Success Criteria quoted verbatim and graded with evidence, the enumerated real-key limits, and the honest statement that the acceptance mechanism is proved and no release was accepted"
  key_links:
    - from: crates/wcore-eval-scenarios/tests/supply_chain_tamper_corpus.rs
      to: crates/wcore-eval-scenarios/src/release_integrity.rs
      via: "every case driving the same verifiers the release path uses, so a corpus pass is a statement about the product rather than about a test double"
      pattern: "corpus-over-real-verifier"
    - from: the withheld release-acceptance key
      to: the highest contiguously verified state
      via: "progress capping at rollback rehearsal, which is the separation of authorization states made observable"
      pattern: "structural-unreachability"
    - from: .planning/phases/29-supply-chain-release-integrity/29-PHASE-VERDICT.md
      to: .planning/ROADMAP.md Phase 29 Success Criteria
      via: "each criterion quoted verbatim and graded against captured evidence rather than against what was built"
      pattern: "verbatim-grading"
---

<objective>
Prove tamper rejection by actually tampering — every claim a pair of an accepted pristine input and a refused mutation across all seven object classes F29-03 names — then prove the four release states are genuinely separate by running the chain twice, once with all four role keys and once with the release-acceptance key withheld, and grade the phase's four Success Criteria verbatim and honestly.

Purpose: this is the plan where a supply-chain phase either earns its claims or quietly inflates them. The two specific inflations available here are a rejection-only corpus, which any over-strict verifier passes, and a clean-room completion of the four-state chain reported as though a release had been accepted. Both are structurally blocked: the corpus cannot hold a case without its control, and the verdict must state both that the mechanism works and that no release was accepted.
Output: a paired tamper corpus covering binaries, SBOMs, updates, plugins, backend receipts, manifests and keys; two four-state runs with the positive control and the withheld-key proof both captured; a verdict grading the four criteria verbatim with evidence; every real-key limit enumerated with its substitution point; and the residuals filed to the owning phases and to BACKLOG.
</objective>

<execution_context>
@$HOME/.claude/ferrox-core/workflows/execute-plan.md
@$HOME/.claude/ferrox-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-supply-chain-release-integrity/29-01-SUMMARY.md
@.planning/phases/29-supply-chain-release-integrity/29-02-SUMMARY.md
@.planning/phases/29-supply-chain-release-integrity/29-03-SUMMARY.md
@crates/wcore-eval-scenarios/src/release_integrity.rs
@crates/wcore-eval-scenarios/src/sbom.rs
@crates/wcore-eval-scenarios/bin/wayland-release.rs
@crates/wcore-cli/src/self_update.rs
</context>

<execution_rules>

**THE TWO AMENDED PHASE RULES — verbatim, and they bound this plan.**

- Findings at CRITICAL or HIGH must be fixed or disproved with executable evidence. MEDIUM and below are logged to `.planning/BACKLOG.md` and DO NOT BLOCK execution.
- Execution begins when no CRITICAL or HIGH finding is open, or after 2 review rounds, whichever comes first. A third round is NOT permitted; it escalates to Sean.

**DO NOT TIGHTEN THE SEVERITY POLICY AT THE END OF A PHASE.** The rule above is fixed. Inventing a stricter one here — "every finding must be closed before the verdict" — is what turned Phase 20 into a seventy-four-plan loop lasting two weeks. MEDIUM and below are filed, non-blocking, and the verdict says so.

**THIS PLAN DOES NOT REPAIR.** Its output is a corpus, two separation runs and a grade. If a tamper case shows a real refusal is missing, that is a FINDING recorded with a severity, not a repair authored here. A corpus written by the same pass that fixed the defect proves only that the author knows what they fixed. If the finding is CRITICAL or HIGH, record it, state it in the verdict as an open finding, and let the verdict be honest about what that means for the criterion.

**NO CREDENTIAL, EVER.** Every key in this plan is generated at run time into a temporary directory and dies with the run. No real signing key, no real release account, no registry token, no published release. **No gate here may require a secret to pass**, and none may be passable BY supplying one.

**THE PAIRING RULE (highest-priority rule in this plan).** Every rejection claim is proved by accepting the pristine input FIRST and then refusing exactly one mutation of it. Both halves recorded. One mutation per case — a case that changes two things at once tells you a refusal happened and nothing about which change caused it.

**THE TWO-RUN RULE FOR CRITERION 4.** The four-state chain is exercised TWICE: once with all four role keys present, which must complete and is the positive control; and once with the release-acceptance key withheld, which must cap at rollback rehearsal. Reporting only the first would claim a release was accepted; reporting only the second would leave the mechanism unproved.

**THE LIVE VERIFICATION MANDATE.** The four-state runs are driven through the REAL `wayland-release` binary on real hardware, not only through library calls, because the binary is what a release engineer would actually run.

**TERMINATION CRITERION FOR THIS PLAN (hard). Exactly three states, no others.** The phrase "termination state 4" was invented on this program by an agent dodging an artifact it wrongly believed unobtainable; this plan states its exits and has no fourth.
1. **Complete, criteria met or partially met** — the corpus covers all seven classes with paired results, both four-state runs are captured, and the four criteria are graded verbatim with evidence.
2. **Complete, goal not achieved** — the same artifacts, but the grading shows one or more criteria NOT MET. This is a FULL and SUCCESSFUL outcome of this plan. Phase 21 declared its goal not achieved twice and that was the correct result; Phase 27 graded four of five criteria NOT MET and that grade is why the phase is trustworthy. Report it plainly, with the evidence, and stop.
3. **Escalated** — grading honestly would require changing production code, which this plan may not do. Record the finding, grade the criterion on the evidence as it stands, and escalate the repair rather than performing it.

Under no circumstances does this plan spawn additional plans, extend its own task list, invent a fourth termination state, or grade a criterion against a reworded version of itself.

**SCOPE BOUNDARY (hard).** This plan owns the tamper corpus, the state-separation proof and the verdict. It adds exactly ONE file under `crates/` and modifies no production code at all. It does NOT touch `crates/wcore-cli/src/`, `crates/wcore-eval-scenarios/src/`, `crates/wcore-eval-scenarios/bin/`, `crates/wcore-cli/src/plugin/`, `crates/wcore-pluginsrc/`, `crates/wcore-exec-backend/`, `Cargo.toml`, `Cargo.lock`, `justfile` or `.github/workflows/`.

**DO NOT RUN `wcore-contract generate`.** Nothing here needs it.

**NO GATE ENDS IN A PIPE.** Every remote gate captures the remote status into `rc` FIRST and asserts on it; no line containing `ssh` carries a pipe.

**EVERY GATE MUST BE ABLE TO GO RED.** Baselines measured at `ac94b1d57cc95b577f60b8b3da3be0d536a6d7ad`: `crates/wcore-eval-scenarios/tests/supply_chain_tamper_corpus.rs` does not exist; `.planning/phases/29-supply-chain-release-integrity/evidence/29-04/` does not exist; `29-PHASE-VERDICT.md` does not exist.

**ENVIRONMENT.**
- Linux (authoritative): `ssh -o BatchMode=yes hetzner-dsm`, `/root/wayland`. Export `PATH=/root/.cargo/bin:$PATH`. Build TARGETED. Check `df -h /root` first, and remove your worktree and its `target/` when the phase is done.
- Mac: `cargo fmt --all -- --check` only. `/usr/bin/git`, `/usr/bin/grep`, `/usr/bin/wc`.
- Commit and push the lane branch. No push to main, merge, PR, tag, release or issue closure.

**AGENTS.md discipline.** Clippy-clean at `-D warnings`. The corpus file stays under 1000 lines; if the case table grows past it, extract the table into a sibling module under `tests/` rather than growing past the guideline.

**Git hygiene.** `/usr/bin/git` on the Mac. Stage exactly the paths in `files_modified`, never `-A`, never `.`. Never `git checkout`, `git reset`, `git stash` or `git rebase` — other lanes share the object store. No `Co-Authored-By` trailers.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A paired tamper corpus covering all seven object classes F29-03 names</name>
  <files>crates/wcore-eval-scenarios/tests/supply_chain_tamper_corpus.rs, .planning/phases/29-supply-chain-release-integrity/29-04-TAMPER-RESULTS.md, .planning/phases/29-supply-chain-release-integrity/evidence/29-04/</files>
  <read_first>crates/wcore-eval-scenarios/src/release_integrity.rs (every verifier this corpus drives, and the exact typed error each refusal produces — the corpus asserts the INVARIANT that the input was refused, never a specific error string, because pinning a string encodes today's failure shape and keeps passing for the wrong reason when it changes), crates/wcore-eval-scenarios/src/sbom.rs (how the SBOM digest is computed, so an SBOM mutation is a real byte change and not a re-serialization artefact), crates/wcore-cli/src/self_update.rs as landed by 29-03 (the update-offer shape the update-class cases mutate, and the trust-root verification they drive), crates/wcore-eval-scenarios/tests/release_integrity_contract.rs and tests/sbom_contract.rs (what is ALREADY proved by 29-01 and 29-02 — this corpus must not re-litigate those; it covers the object classes end to end rather than repeating unit assertions)</read_first>
  <behavior>
    - Test first, in every case, with the RED recorded.
    - A corpus case is a value carrying both a pristine input and exactly one mutation of it, so a case without a control cannot be constructed at all.
    - The runner asserts, for every case it is given, that the pristine input is ACCEPTED and the mutated input is REFUSED — in that order — and records both outcomes.
    - Each case mutates exactly one thing; a case that changes two inputs is not admissible.
    - Cases assert the invariant that the input was refused, never a specific error string or numeric code, so the corpus does not keep passing for the wrong reason when a failure shape changes.
    - The case set covers all seven object classes F29-03 names: a binary or archive, an SBOM, an update offer, a plugin artifact entry, a backend receipt binding, a manifest, and a key.
    - The key class covers three distinct attacks: an unknown key id, a retired key, and a signature minted under a different domain replayed here.
    - A meta-assertion over the whole table proves that every case has a control and that every named object class is represented, so adding a case without a control or dropping a class goes red.
    - The corpus drives the same verifiers the release path uses, not a test double, so a pass is a statement about the product.
  </behavior>
  <action>Add exactly one file, `crates/wcore-eval-scenarios/tests/supply_chain_tamper_corpus.rs`. Modify no production code. If a case reveals that a refusal is missing, that is a finding for the verdict with a severity — record it and move on; repairing it here would merge the proof and the repair into one pass and destroy the independence that makes the proof worth anything.

Express the corpus as a table of cases, each carrying an object class, a name, a pristine input and a single mutation. Make the control structurally required: the pristine input is a mandatory field of the case, so a case that only carries a mutation cannot be written. The runner takes the table and, for each case, verifies the pristine input and asserts acceptance, then applies the one mutation and asserts refusal, recording both.

Cover all seven classes, with case identifiers of the shape `F29-03-<CLASS>-<n>` so the results table and the ledger can index them. The binary class mutates one byte of a packaged artifact so the manifest's recorded artifact digest no longer matches. The SBOM class mutates one byte of the SBOM document so its recorded digest no longer matches. The update class mutates the update offer or the manifest field the updater depends on and drives the shipped updater's verification path. The plugin class mutates a plugin artifact entry's digest inside the release manifest. The backend-receipt class mutates the certification binding's receipt body digest. The manifest class mutates a body field without re-signing, so the body digest and the signature disagree. The key class gets three cases: a signature from a key id the trust root does not know, a signature from a key the trust root marks retired, and a signature minted under a different domain separator replayed as a manifest signature.

Add two meta-assertions over the table, named `every_tamper_case_has_a_pristine_control_that_is_accepted` and `the_corpus_covers_every_f29_03_object_class`. The first is what makes the corpus non-trivial; the second is what stops a class quietly disappearing when someone edits the table.

Write the results into `29-04-TAMPER-RESULTS.md` with one row per case: the case identifier, the object class, what exactly was mutated, whether the pristine input was accepted, whether the mutation was refused, and the severity of any case where the answer is not accept-then-refuse. Maintain `evidence/29-04/TAMPER-LEDGER.tsv` as the machine-checkable index with rows of the shape `F29-03-&lt;CLASS&gt;-&lt;n&gt;::control=&lt;ACCEPTED|REFUSED&gt;::mutated=&lt;ACCEPTED|REFUSED&gt;::evidence=&lt;path relative to evidence/29-04&gt;`, and every named path must exist.

Prove it on Hetzner with the targeted suite. Records evidence for F29-03; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated># CORPUS-LANDED GATE with its two meta-assertions. All three legs are red at base — the file does not exist — and the remote gate below actually runs them.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; F=crates/wcore-eval-scenarios/tests/supply_chain_tamper_corpus.rs &amp;&amp; test -s "$F" &amp;&amp; test "$(/usr/bin/grep -cF 'every_tamper_case_has_a_pristine_control_that_is_accepted' "$F")" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'the_corpus_covers_every_f29_03_object_class' "$F")" -ge 1</automated>
    <automated># SEVEN-CLASS GATE. F29-03 names seven object classes and all seven must appear as case identifiers in the corpus source. Red at base; goes red again if a class is dropped from the table.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; F=crates/wcore-eval-scenarios/tests/supply_chain_tamper_corpus.rs &amp;&amp; test -s "$F" &amp;&amp; for C in BINARY SBOM UPDATE PLUGIN BACKEND-RECEIPT MANIFEST KEY; do test "$(/usr/bin/grep -cF "F29-03-$C" "$F")" -ge 1 || exit 1; done</automated>
    <automated># PAIRED-RESULT GATE. Every ledger row must record BOTH halves, the control must be ACCEPTED and the mutation REFUSED, and the named artifact must exist. A rejection-only corpus — every control also refused — goes red here, which is the whole point. Ten cases is the floor: seven classes with three of them in the key class.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/29-supply-chain-release-integrity/evidence/29-04 &amp;&amp; test -s "$EV/TAMPER-LEDGER.tsv" &amp;&amp; test "$(/usr/bin/grep -cE '^F29-03-[A-Z-]+-[0-9]+::control=ACCEPTED::mutated=REFUSED::evidence=[^[:space:]]+$' "$EV/TAMPER-LEDGER.tsv")" -ge 10 &amp;&amp; test "$(/usr/bin/grep -cE '^F29-03-[A-Z-]+-[0-9]+::control=REFUSED' "$EV/TAMPER-LEDGER.tsv")" -eq 0 &amp;&amp; while IFS= read -r R; do case "$R" in ""|"#"*) continue;; esac; F="$EV/${R##*::evidence=}"; test -s "$F" || exit 1; done &lt; "$EV/TAMPER-LEDGER.tsv"</automated>
    <automated># NO-ERROR-STRING-PINNING GATE. Asserting a specific refusal message encodes today's failure shape and keeps passing when the refusal moves to a different and possibly weaker cause. Leg 1 anchors on the new file so the line is red at base; leg 2 asserts the corpus contains no assertion over a rendered error's text.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; F=crates/wcore-eval-scenarios/tests/supply_chain_tamper_corpus.rs &amp;&amp; test -s "$F" &amp;&amp; test "$(/usr/bin/grep -cE 'to_string\(\)\.contains|err\(\)\.to_string|message\(\)\.contains' "$F")" -eq 0</automated>
    <automated># NO-PRODUCTION-CHANGE GATE, conjoined with this task's completion anchor so it cannot pass against a clean base tree. This plan adds exactly one file under `crates/` and touches no production source at all — proof and repair must not be authored by the same pass.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -s crates/wcore-eval-scenarios/tests/supply_chain_tamper_corpus.rs &amp;&amp; test "$(/usr/bin/git status --porcelain --untracked-files=all -- crates/ | /usr/bin/sed 's/^...//' | /usr/bin/grep -vE '^crates/wcore-eval-scenarios/tests/supply_chain_tamper_corpus\.rs$' | /usr/bin/wc -l | tr -d ' ')" -eq 0</automated>
    <automated># GATE, NOT A PRINTOUT — remote status captured first, no pipe on this line.
SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); LOG=$(mktemp); ssh -o BatchMode=yes hetzner-dsm "export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland &amp;&amp; git fetch origin &amp;&amp; git checkout --detach $SHA &amp;&amp; cargo clippy -p wcore-eval-scenarios --all-targets -- -D warnings &amp;&amp; cargo nextest run -p wcore-eval-scenarios --no-fail-fast" &gt; "$LOG" 2&gt;&amp;1; rc=$?; tail -40 "$LOG"; rm -f "$LOG"; test "$rc" -eq 0</automated>
  </verify>
  <done>Exactly one file was added under `crates/` and no production source was touched, gate-checked. A corpus case cannot be constructed without its pristine control, and both meta-assertions exist and passed. All seven F29-03 object classes appear as case identifiers, with three distinct attacks in the key class. Every ledger row records both halves with the control ACCEPTED and the mutation REFUSED, at least ten cases, every named artifact present, and zero rows where a control was refused. No case asserts a rendered error string, gate-checked. Any case whose result was not accept-then-refuse is recorded as a finding with a severity rather than repaired here. Hetzner clippy is clean at `-D warnings` and the targeted suite is stated as a delta with every residual failure named.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the four release states are separate by running the chain with all four keys and then without the acceptance key</name>
  <files>.planning/phases/29-supply-chain-release-integrity/29-04-TAMPER-RESULTS.md, .planning/phases/29-supply-chain-release-integrity/evidence/29-04/</files>
  <read_first>crates/wcore-eval-scenarios/src/release_integrity.rs (the state ledger verifier's contract as landed by 29-01: canonical order, per-state domain separators, role-scoped keys, distinct key ids, previous-record digests, disjoint evidence sets, and the rule that an absent record is not an error but a stopping point), crates/wcore-eval-scenarios/bin/wayland-release.rs (the append and verify subcommands, and that seeds are read from standard input — this task drives the real binary, not the library), .planning/phases/29-supply-chain-release-integrity/29-01-SUMMARY.md (the anti-collapse tests already proved at the unit level, so this task exercises the same properties through the BINARY rather than repeating them)</read_first>
  <behavior>
    - Both runs are driven through the real `wayland-release` binary on real hardware, with four role keys generated at run time into a temporary directory.
    - Run A holds all four role keys, appends all four state records in canonical order, and the chain verifies — this is the positive control without which the separation proof would be rejection-only.
    - In run A, each state record also verifies STANDALONE against only its own role key, so the states are independently verifiable and not merely sequentially checkable.
    - Run B withholds the release-acceptance key entirely; the first three records are appended and the chain verifies to rollback rehearsal, and release acceptance is reported as not reached rather than as a failure of the chain.
    - Every collapse attempt is executed as a real invocation and refused: relabelling an earlier state, signing acceptance with the packaging key, reusing an earlier state's evidence, skipping a state, reordering the chain, and appending a fifth record with an invented state name.
    - The invented state name used in the collapse attempt is the exact one an agent on this program actually invented, so the defence is tied to the real incident rather than to a hypothetical.
    - Every outcome is captured with its exit status, and no collapse attempt is reported as refused without its capture.
  </behavior>
  <action>Build the `wayland-release` binary on Hetzner at the pinned commit and run both sequences there, capturing everything into `evidence/29-04/`.

Run A, the positive control. Initialise a trust root with all four role keys into a temporary directory. Append the packaging record binding the release manifest digest and its own evidence set; then deployment preparation, binding the previous record's digest and a DISJOINT evidence set; then rollback rehearsal; then release acceptance. Verify the chain and capture the result. Then verify each record standalone against only its own role key and capture each of the four results separately — a chain that only verifies as a chain is sequential, not separable, and separability is what the criterion asks for.

Run B, the separation proof. Initialise a trust root in which the release-acceptance key is NOT present — generate three keys, not four. Append the first three records and verify. The expected observable outcome is that verification reports rollback rehearsal as the highest state reached and does not reach release acceptance, and that this is reported as a stopping point rather than as a corrupt chain. Capture it.

Then the collapse attempts, each as a real invocation with its exit status captured, each attempted against run A's otherwise-valid chain so that only one thing is wrong per attempt: relabel the packaging record as release acceptance; sign a release-acceptance record with the packaging role key; append a release-acceptance record whose evidence set reuses a digest from the packaging record; verify a chain with deployment preparation removed; verify a chain with rollback rehearsal and deployment preparation swapped; and append a fifth record whose state name is `termination_state_4`. Every one must be refused, and every refusal must have a capture behind it.

Record the results in `29-04-TAMPER-RESULTS.md` and maintain two ledgers. `evidence/29-04/STATE-SEPARATION.tsv` carries rows of the shape `F29-04-STATE-&lt;ordinal&gt;::&lt;REACHED|NOT-REACHED&gt;::run=&lt;A|B&gt;::evidence=&lt;path&gt;` — four REACHED rows from run A and a NOT-REACHED row for release acceptance from run B. `evidence/29-04/COLLAPSE-ATTEMPTS.tsv` carries rows of the shape `F29-04-COLLAPSE-&lt;n&gt;::REFUSED::attempt=&lt;name&gt;::evidence=&lt;path&gt;`, one per attempt, and every named artifact must exist.

State plainly in the results document, in two sentences, that the acceptance MECHANISM is proved by run A with a key this lane generated, and that no release was accepted because the real release-acceptance key is Sean's and this lane neither holds it nor requests it. Records evidence for F29-04; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated># TWO-RUN GATE. Criterion 4 needs BOTH: the all-keys control that proves the mechanism, and the withheld-key run that proves the separation. Requiring four REACHED rows AND a NOT-REACHED release-acceptance row makes it impossible to satisfy this gate with either run alone. Red at base.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/29-supply-chain-release-integrity/evidence/29-04 &amp;&amp; test -s "$EV/STATE-SEPARATION.tsv" &amp;&amp; test "$(/usr/bin/grep -cE '^F29-04-STATE-[0-9]+::REACHED::run=A::evidence=[^[:space:]]+$' "$EV/STATE-SEPARATION.tsv")" -eq 4 &amp;&amp; test "$(/usr/bin/grep -cE '^F29-04-STATE-3::NOT-REACHED::run=B::evidence=[^[:space:]]+$' "$EV/STATE-SEPARATION.tsv")" -eq 1 &amp;&amp; while IFS= read -r R; do case "$R" in ""|"#"*) continue;; esac; F="$EV/${R##*::evidence=}"; test -s "$F" || exit 1; done &lt; "$EV/STATE-SEPARATION.tsv"</automated>
    <automated># COLLAPSE-ATTEMPT GATE. Six attempts, every one refused, every one with a capture behind it. A refusal claimed without a capture goes red. Red at base.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/29-supply-chain-release-integrity/evidence/29-04 &amp;&amp; test -s "$EV/COLLAPSE-ATTEMPTS.tsv" &amp;&amp; test "$(/usr/bin/grep -cE '^F29-04-COLLAPSE-[0-9]+::REFUSED::attempt=[A-Za-z0-9_-]+::evidence=[^[:space:]]+$' "$EV/COLLAPSE-ATTEMPTS.tsv")" -ge 6 &amp;&amp; test "$(/usr/bin/grep -cE '^F29-04-COLLAPSE-[0-9]+::(ACCEPTED|SKIPPED)' "$EV/COLLAPSE-ATTEMPTS.tsv")" -eq 0 &amp;&amp; while IFS= read -r R; do case "$R" in ""|"#"*) continue;; esac; F="$EV/${R##*::evidence=}"; test -s "$F" || exit 1; test "$(/usr/bin/wc -l &lt; "$F" | tr -d ' ')" -ge 2 || exit 1; done &lt; "$EV/COLLAPSE-ATTEMPTS.tsv"</automated>
    <automated># INVENTED-STATE ATTEMPT GATE. The fifth-record attempt must use the exact state name an agent on this program actually invented, so the defence is tied to the real incident. Red at base.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -rlF 'termination_state_4' .planning/phases/29-supply-chain-release-integrity/evidence/29-04 | /usr/bin/wc -l | tr -d ' ')" -ge 1</automated>
    <automated># STANDALONE-VERIFIABILITY GATE. A chain that only verifies as a chain is sequential, not separable. Each of the four records must have been verified alone against only its own role key, with a capture per record. Red at base.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/29-supply-chain-release-integrity/evidence/29-04 &amp;&amp; test -s "$EV/STANDALONE-VERIFY.tsv" &amp;&amp; test "$(/usr/bin/grep -cE '^F29-04-STANDALONE-[0-9]+::VERIFIED::role=[a-z_]+::evidence=[^[:space:]]+$' "$EV/STANDALONE-VERIFY.tsv")" -eq 4 &amp;&amp; while IFS= read -r R; do case "$R" in ""|"#"*) continue;; esac; F="$EV/${R##*::evidence=}"; test -s "$F" || exit 1; done &lt; "$EV/STANDALONE-VERIFY.tsv"</automated>
    <automated># GATE, NOT A PRINTOUT — remote status captured first, no pipe on this line. Proves the binary these runs were driven through actually builds and runs at the pinned commit.
SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); LOG=$(mktemp); ssh -o BatchMode=yes hetzner-dsm "export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland &amp;&amp; git fetch origin &amp;&amp; git checkout --detach $SHA &amp;&amp; git rev-parse HEAD &amp;&amp; cargo build --release --locked -p wcore-eval-scenarios --bin wayland-release &amp;&amp; ./target/release/wayland-release --help" &gt; "$LOG" 2&gt;&amp;1; rc=$?; tail -20 "$LOG"; rm -f "$LOG"; test "$rc" -eq 0</automated>
  </verify>
  <done>Both runs were driven through the real `wayland-release` binary on Hetzner at the pinned commit with role keys generated at run time. Run A appended all four records in canonical order and the chain verified, and each of the four records also verified STANDALONE against only its own role key with a capture per record. Run B withheld the release-acceptance key, verified to rollback rehearsal, and reported release acceptance as not reached rather than as a corrupt chain. All six collapse attempts — relabel, wrong-role signature, reused evidence, skipped state, reordered chain and an invented fifth state named `termination_state_4` — were executed as real invocations, every one refused, every one with a capture of at least two lines behind it. The results document states in two sentences that the mechanism is proved and that no release was accepted, and names why.</done>
</task>

<task type="auto">
  <name>Task 3: Grade Phase 29's four Success Criteria verbatim and file every residual</name>
  <files>.planning/phases/29-supply-chain-release-integrity/29-PHASE-VERDICT.md, .planning/phases/29-supply-chain-release-integrity/evidence/29-04/, .planning/SEAM-REQUESTS/29.md, .planning/BACKLOG.md</files>
  <read_first>.planning/ROADMAP.md at the Phase 29 entry (the four Success Criteria as written — quote them verbatim; grading a reworded criterion against evidence that happens to match it is how a phase inflates itself), .planning/REQUIREMENTS.md at F29-01 through F29-04 (the requirement text behind each criterion), the three prior SUMMARY files and every ledger under `evidence/29-01` through `evidence/29-04` (the evidence base — a grade with no ledger row behind it is an opinion), .planning/phases/27-multimodal-browser-generation-voice/27-PHASE-VERDICT.md (the established verdict form in this program: criterion quoted, graded, evidence attached, residuals named — and an example of a phase graded mostly NOT MET being more useful than one graded green)</read_first>
  <behavior>
    - Each of the four Success Criteria is quoted verbatim from the roadmap and graded MET, PARTIAL or NOT MET against captured evidence.
    - Every grade names the ledger rows and captured artifacts that settled it; a grade with no evidence behind it is not a grade.
    - The verdict states in plain language both that the release-acceptance mechanism is proved with a key this lane generated and that no release was accepted, and it never presents the first as the second.
    - Every real-key, real-account and real-release limit across the whole phase is collected into one enumerated list, each with its exact substitution point.
    - Every open finding is listed with its severity; CRITICAL and HIGH are stated as open and their effect on the affected criterion's grade is stated explicitly rather than absorbed.
    - MEDIUM and below are filed to BACKLOG as non-blocking, and the severity policy is not tightened at the end of the phase.
    - Residuals owned by other phases — the Phase 28 receipt fields, the Phase 25 plugin and backend runtime trust roots — are filed as cross-phase requests rather than left in this phase's summary.
    - The four requirement rows F29-01 through F29-04 are marked complete or not, and a requirement is not marked complete on the strength of a PARTIAL criterion.
  </behavior>
  <action>Write `29-PHASE-VERDICT.md`. Open each of the four sections with the Success Criterion quoted VERBATIM from the roadmap's Phase 29 entry. Do not paraphrase, do not soften, do not split a criterion into a part you can grade and a part you cannot — grade the whole sentence.

Criterion one covers clean-room provenance, SBOM, dependency policy, signatures and reproducibility or documented variance. Its evidence is 29-02's clause ledger, the two SBOM digests, the dependency verdict with its exact exit status, and the two clean-room build digests with their verdict. Grade it on that evidence and say which of its clauses are settled by work this phase did versus by what the release pipeline already did.

Criterion two covers install and update identity, rollback and freeze protection, revocation and key rotation. Its evidence is 29-03's six-clause ledger, its live ledger and the rotation drill. The plugin and backend runtime trust-root half is not this phase's to close, so if that keeps the criterion below MET, say so and grade accordingly rather than narrowing the criterion to what was done.

Criterion three covers tamper rejection across artifacts, manifests, receipts, plugins, backends and keys. Its evidence is the tamper ledger with its paired results. A class covered only at the release-manifest layer because its runtime path belongs to another phase is stated as such in the grade.

Criterion four covers the four states remaining separate evidence and authorization states. Its evidence is the two state-separation runs, the standalone verification of each record and the six refused collapse attempts. This criterion can legitimately be MET while no release is accepted — separation is what it asks for — but the verdict must carry both sentences: the mechanism is proved, and no release was accepted.

Then collect the limits. Enumerate every point across all four plans where a real key, a real account or a real published release was the only missing input, each as an individually citable entry with its exact substitution: the attestation ACCEPT path against the real transparency log; the bundled release trust root that ships empty and refuses to construct; the marketplace plugin index still carrying the all-zeros placeholder; the release-acceptance role key that is Sean's by design; and Phase 28's certification receipts requiring real native hardware. Maintain `evidence/29-04/VERDICT-LEDGER.tsv` with one row per criterion of the shape `F29-SC-&lt;n&gt;::&lt;MET|PARTIAL|NOT-MET&gt;::evidence=&lt;path relative to evidence/29-04&gt;`, and `evidence/29-04/PHASE-LIMITS.tsv` with rows of the shape `F29-LIMIT-&lt;n&gt;::&lt;REAL-KEY|REAL-ACCOUNT|REAL-RELEASE&gt;::substitution=&lt;what changes when it exists&gt;`.

File the residuals where they belong. Cross-phase items go into `.planning/SEAM-REQUESTS/29.md` in the established form — exact file, exact structure, exact addition, what breaks if omitted — addressed to the owning phase. MEDIUM and below go into `.planning/BACKLOG.md` as non-blocking entries. Do not tighten the severity policy on the way out.

Finally, state the phase requirement rows: F29-01 through F29-04, each complete or not, and a requirement is not marked complete on the strength of a PARTIAL criterion. If the honest answer is that the phase goal was not achieved, say exactly that in the opening line of the verdict. Every phase on this program that graded itself honestly produced more value than one that claimed a green.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated># VERDICT-GRADED GATE. All four criteria graded with an evidence artifact that exists. Red at base — neither the verdict nor its ledger exists.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/29-supply-chain-release-integrity/evidence/29-04 &amp;&amp; test -s .planning/phases/29-supply-chain-release-integrity/29-PHASE-VERDICT.md &amp;&amp; test -s "$EV/VERDICT-LEDGER.tsv" &amp;&amp; test "$(/usr/bin/grep -cE '^F29-SC-[1-4]::(MET|PARTIAL|NOT-MET)::evidence=[^[:space:]]+$' "$EV/VERDICT-LEDGER.tsv")" -eq 4 &amp;&amp; while IFS= read -r R; do case "$R" in ""|"#"*) continue;; esac; F="$EV/${R##*::evidence=}"; test -s "$F" || exit 1; done &lt; "$EV/VERDICT-LEDGER.tsv"</automated>
    <automated># VERBATIM-QUOTE GATE. Each criterion must be quoted from the roadmap rather than paraphrased, so the grade cannot be against a reworded criterion that happens to fit the evidence. These four distinctive fragments are taken from the roadmap's Phase 29 Success Criteria; all four must appear in the verdict. Red at base.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; V=.planning/phases/29-supply-chain-release-integrity/29-PHASE-VERDICT.md &amp;&amp; test -s "$V" &amp;&amp; test "$(/usr/bin/grep -cF 'reproducibility or documented variance' "$V")" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'rollback/freeze protection, revocation, and key rotation' "$V")" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'Tampered artifacts, manifests, receipts, plugins, backends, or keys are rejected' "$V")" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'remain separate evidence and authorization states' "$V")" -ge 1</automated>
    <automated># NO-RELEASE-ACCEPTED GATE. The verdict must state explicitly that no release was accepted, so a clean-room completion of the four-state chain can never read as a release acceptance. Red at base, and red again if that sentence is dropped.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; V=.planning/phases/29-supply-chain-release-integrity/29-PHASE-VERDICT.md &amp;&amp; test -s "$V" &amp;&amp; test "$(/usr/bin/grep -ciE 'no release (was |is )?accepted' "$V")" -ge 1</automated>
    <automated># PHASE-LIMITS GATE. Every real-key, real-account and real-release limit across all four plans must be individually citable with its substitution point, not buried in prose. Five is the floor: the attestation accept path, the bundled release trust root, the marketplace plugin index, the release-acceptance role key, and Phase 28's certification receipts. Red at base.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/29-supply-chain-release-integrity/evidence/29-04 &amp;&amp; test -s "$EV/PHASE-LIMITS.tsv" &amp;&amp; test "$(/usr/bin/grep -cE '^F29-LIMIT-[0-9]+::(REAL-KEY|REAL-ACCOUNT|REAL-RELEASE)::substitution=[^[:space:]].*$' "$EV/PHASE-LIMITS.tsv")" -ge 5</automated>
    <automated># RESIDUALS-FILED GATE. Cross-phase items reach the owning phase as a written request and non-blocking items reach BACKLOG; neither dies in this phase's summary. Leg 1 anchors on this task's own artifact so the line is red at base. Legs 2 and 3 assert the seam file names both owning phases.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -s .planning/phases/29-supply-chain-release-integrity/evidence/29-04/PHASE-LIMITS.tsv &amp;&amp; test -s .planning/SEAM-REQUESTS/29.md &amp;&amp; test "$(/usr/bin/grep -cE 'R28-[A-F]' .planning/SEAM-REQUESTS/29.md)" -ge 6 &amp;&amp; test "$(/usr/bin/grep -ciE 'plugin|backend' .planning/SEAM-REQUESTS/29.md)" -ge 1</automated>
    <automated># NO-PRODUCTION-CHANGE GATE for the whole plan, conjoined with this plan's completion anchor. Task 1 added exactly one test file under `crates/` and this task adds nothing there at all.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -s .planning/phases/29-supply-chain-release-integrity/29-PHASE-VERDICT.md &amp;&amp; test "$(/usr/bin/git status --porcelain --untracked-files=all -- crates/ | /usr/bin/sed 's/^...//' | /usr/bin/grep -vE '^crates/wcore-eval-scenarios/tests/supply_chain_tamper_corpus\.rs$' | /usr/bin/wc -l | tr -d ' ')" -eq 0</automated>
  </verify>
  <done>All four Success Criteria are quoted verbatim from the roadmap and graded MET, PARTIAL or NOT MET, each with the ledger rows and captured artifacts that settled it, and the verbatim quotation is gate-checked on four distinctive fragments. The verdict states plainly that the acceptance mechanism is proved with a key this lane generated and that no release was accepted, gate-checked. At least five real-key, real-account or real-release limits are enumerated individually with their exact substitution points. Every open finding is listed with its severity, CRITICAL and HIGH are stated as open with their effect on the affected criterion's grade named explicitly, and MEDIUM and below are filed to BACKLOG as non-blocking without tightening the severity policy. Cross-phase residuals — the six R28 receipt requirements and the plugin and backend runtime trust roots — are filed to their owning phases in the established seam-request form. Requirements F29-01 through F29-04 are each marked complete or not, and none is marked complete on the strength of a PARTIAL criterion. No production source was touched anywhere in this plan.</done>
</task>

</tasks>

## What this plan does NOT change (scope fence)

- **All production source — untouched, gate-checked twice.** This plan adds exactly one file under `crates/`, a test. Proof and repair are not authored by the same pass.
- **`crates/wcore-cli/src/`, `crates/wcore-eval-scenarios/src/` and `bin/` — untouched.** If a tamper case shows a refusal is missing, that is a finding for the verdict with a severity, escalated rather than repaired here.
- **`crates/wcore-cli/src/plugin/`, `crates/wcore-pluginsrc/`, `crates/wcore-exec-backend/` — untouched.** Phase 25's, concurrent; the residual is filed to it.
- **`crates/wcore-eval-scenarios/src/receipt.rs`, `src/receipt_policy.rs`, `bin/wayland-receipt.rs` — untouched.** Phase 28's, concurrent; the six R28 requirements are filed to it.
- **`Cargo.toml`, `Cargo.lock`, `justfile`, `.github/workflows/` — untouched.**
- **The severity policy — not tightened.** CRITICAL and HIGH must be fixed or disproved; MEDIUM and below are filed and non-blocking. Inventing a stricter rule at the end of a phase is what produced a seventy-four-plan loop once already.
- **The Success Criteria — not reworded.** Each is quoted verbatim and graded whole; a criterion is never narrowed to the part that the evidence happens to satisfy.
- **No real key, no real account, no published release, and no release accepted.** The clean-room chain completing under keys this lane generated is the mechanism working, and the verdict says so in the same breath as saying no release was accepted.
- **No test is deleted, weakened, re-gated, `#[ignore]`d or `#[allow]`ed.** If a pristine control is refused, the fixture is re-examined, never the verifier.
- **No gate ends in a pipe, and no gate asserts a threshold the measured baseline already satisfies.**

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus ← the verifier under test | A corpus that drives a test double proves the double; every case here drives the same verifiers the release path uses |
| rejection claim ← the absence of a control | A refusal with no accepted control is indistinguishable from a verifier that refuses everything |
| clean-room chain ← a real release | The chain completing under keys this lane generated proves the mechanism, not that anything was released |
| verdict ← the phase that produced the evidence | A phase grading itself is exactly where a criterion gets quietly reworded to fit what was built |
| residual finding ← the owning phase | A gap named only in a summary is a gap that dies with the summary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-04-01 | Repudiation | A rejection-only tamper corpus that any over-strict verifier passes, presented as proof of tamper rejection | critical | mitigate | The pristine control is a mandatory field of a corpus case, the runner asserts accept-then-refuse for every case, a meta-assertion covers the whole table, and the ledger gate fails on any row where a control was refused |
| T-29-04-02 | Elevation of Privilege | The clean-room completion of the four-state chain is reported as a release acceptance, so an authorization Sean never gave appears to have been given | critical | mitigate | Both runs are required — the all-keys control and the withheld-acceptance-key proof — and a dedicated gate requires the verdict to state explicitly that no release was accepted |
| T-29-04-03 | Elevation of Privilege | An agent under pressure collapses the four states through the real tooling — relabelling, cross-role signing, evidence reuse, skipping, reordering, or inventing a fifth state | critical | mitigate | All six collapse attempts are executed as real invocations against an otherwise-valid chain, every one must be refused with a capture behind it, and the fifth-state attempt uses the exact invented name this program actually produced |
| T-29-04-04 | Spoofing | A Success Criterion is quoted in a reworded form that the available evidence happens to satisfy, so a PARTIAL reads as MET | high | mitigate | Each criterion is quoted verbatim and a gate checks four distinctive fragments taken from the roadmap text; a grade with no ledger row behind it is not a grade |
| T-29-04-05 | Repudiation | A residual owned by another phase is absorbed into this phase's grade, so the gap is neither closed nor transferred | high | mitigate | Cross-phase residuals are filed as written requests to the owning phase and gate-checked present; the plugin and backend runtime trust roots and the six R28 receipt requirements are both named |
| T-29-04-06 | Tampering | A tamper case asserts a specific error string, so it keeps passing when the refusal moves to a different and possibly weaker cause | high | mitigate | Cases assert the invariant that the input was refused, never a rendered message, and a gate asserts the corpus contains no assertion over an error's text |
| T-29-04-07 | Spoofing | A tamper case changes two things at once, so a refusal is attributed to the wrong cause and a real gap hides behind an unrelated rejection | high | mitigate | One mutation per case is a structural rule of the case type, and the results table records exactly what was mutated per case |
| T-29-04-08 | Denial of Service | The severity policy is tightened at the end of the phase, so every MEDIUM becomes blocking and the phase never terminates | high | mitigate | The policy is restated verbatim in the execution rules with the seventy-four-plan precedent named, and MEDIUM and below are filed to BACKLOG as non-blocking |
| T-29-04-09 | Tampering | A proof and its repair are authored by the same pass, so the corpus proves only that its author knew what they fixed | high | mitigate | This plan modifies no production source at all, gate-checked in two separate tasks; a missing refusal is a finding for the verdict, not a repair |
| T-29-04-10 | Information Disclosure | An F29-03 object class quietly disappears from the case table and nobody notices because no gate names it | medium | mitigate | A gate iterates all seven class identifiers over the corpus source, and a meta-assertion over the table asserts class coverage from inside the suite |
| T-29-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan adds no dependency and touches no manifest; `Cargo.toml` and `Cargo.lock` are outside its declared paths and the surgical-diff gate admits exactly one new test file |
</threat_model>

<verification>
Local gates (Mac, source level only): `cargo fmt --all -- --check` clean; the corpus file exists with both meta-assertions; all seven F29-03 class identifiers appear in it; the tamper ledger holds at least ten rows each recording an ACCEPTED control and a REFUSED mutation with an existing artifact, and zero rows where a control was refused; the corpus asserts no rendered error text; the state-separation ledger holds exactly four REACHED rows from run A and a NOT-REACHED release-acceptance row from run B; six collapse attempts are all REFUSED with captures of at least two lines each; the invented state name appears in the captured evidence; four standalone verifications are recorded, one per role; the verdict exists with all four criteria graded and quoted verbatim on four distinctive fragments; the verdict states that no release was accepted; at least five real-key limits are enumerated with substitutions; the seam file carries the six R28 requirements and the plugin and backend residual; and the only changed path under `crates/` is the one new test file.

Every remote gate captures the remote exit status into `rc` BEFORE any filtering and asserts `test "$rc" -eq 0`. No line containing `ssh` carries a pipe.

Authoritative gates (real hardware). Hetzner Linux: `cargo clippy -p wcore-eval-scenarios --all-targets -- -D warnings` clean; `cargo nextest run -p wcore-eval-scenarios --no-fail-fast` stated as a delta with every residual failure named; and the real `wayland-release` binary built at the pinned commit and used to drive both four-state runs and all six collapse attempts. Targeted builds only, and the hetzner worktree and its `target/` are removed when the phase closes.

**The irreducible limit of this plan, stated rather than hidden.** Ledger files are written by the executor. The mitigations are structural — every row must name an artifact the gate stats, several gates re-measure the tree directly, the corpus itself is run by a remote gate rather than asserted, and the paired-result gate fails on the specific shape a fabrication would take. What no gate can do is verify that a captured transcript was produced by the command it claims. That is why the remote suite gate runs the corpus rather than reading about it, and why the verdict is graded against ledger rows that point at artifacts rather than against prose.

Known unknowns to record, not to resolve here: whether the tamper corpus's release-manifest-layer coverage of plugins and backends is sufficient for F29-03 in the eyes of a later reviewer, which is a judgement the verdict states rather than settles; whether the four-state ledger's evidence-disjointness rule is the right strength for a real release where some evidence legitimately spans states; and whether the criteria can be re-graded upward after Phase 25 and Phase 28 land their halves, which is a question for Phase 30's independent review and not for this phase to pre-empt.
</verification>

<success_criteria>
- Every tamper rejection is proved by an accepted pristine control followed by a refused single mutation, both recorded, with a corpus case structurally unable to exist without its control.
- All seven F29-03 object classes — binaries, SBOMs, updates, plugins, backend receipts, manifests and keys — are covered, with three distinct attacks in the key class.
- No case asserts a rendered error string; every case asserts the invariant that the input was refused.
- The four-state chain is run twice through the real binary: once with all four role keys, completing and verifying, and once with the release-acceptance key withheld, capping at rollback rehearsal.
- Each of the four state records verifies STANDALONE against only its own role key, so the states are independently verifiable rather than merely sequentially checkable.
- All six collapse attempts are executed as real invocations and refused, each with a capture, including a fifth record named with the exact state an agent on this program actually invented.
- The verdict quotes all four Success Criteria verbatim and grades each with the ledger rows and artifacts that settled it.
- The verdict states plainly both that the acceptance mechanism is proved and that no release was accepted.
- At least five real-key, real-account or real-release limits are enumerated individually with their exact substitution points.
- Cross-phase residuals are filed to their owning phases as written requests; MEDIUM and below are filed to BACKLOG; the severity policy is not tightened.
- Requirements F29-01 through F29-04 are each marked complete or not, and none is marked complete on the strength of a PARTIAL criterion.
- No production source was modified anywhere in this plan, gate-checked twice.
</success_criteria>

## Artifacts this plan produces
- `crates/wcore-eval-scenarios/tests/supply_chain_tamper_corpus.rs` — the paired tamper corpus across all seven object classes.
- `.planning/phases/29-supply-chain-release-integrity/29-04-TAMPER-RESULTS.md` — the per-case accept-then-refuse table and both four-state runs.
- `.planning/phases/29-supply-chain-release-integrity/29-PHASE-VERDICT.md` — the four criteria quoted verbatim and graded, with the enumerated real-key limits.
- `.planning/phases/29-supply-chain-release-integrity/evidence/29-04/` — the tamper, state-separation, standalone-verify, collapse-attempt, verdict and phase-limit ledgers with every captured artifact.
- `.planning/SEAM-REQUESTS/29.md` — extended with the residuals owned by Phase 25 and Phase 28.
- `.planning/BACKLOG.md` — the non-blocking residuals.
- `29-04-SUMMARY.md` recording the corpus, the runs, the grade and the termination state.

<output>
Create `.planning/phases/29-supply-chain-release-integrity/29-04-SUMMARY.md` using the standard summary template. Record: the exact SHA everything ran against; each corpus behavior's RED-before-GREEN evidence; the full per-case tamper table with what was mutated, the control result and the mutation result; any case whose result was not accept-then-refuse, with its severity, stated as an open finding rather than repaired; both four-state runs with every captured outcome and exit status; the four standalone verifications; all six collapse attempts with their captures; the four Success Criteria quoted verbatim with their grades and the evidence behind each; the two sentences on the mechanism being proved and no release being accepted; the complete enumerated list of real-key, real-account and real-release limits with substitution points; every open finding with its severity and its effect on the affected criterion; what was filed to which owning phase and what was filed to BACKLOG; the requirement rows F29-01 through F29-04 marked complete or not; the Hetzner clippy and targeted-suite delta with residual failures attributed; confirmation that no production source was modified; the recorded unknowns; and which of the three termination states the plan ended in. If the phase goal was not achieved, say exactly that in the first line.
</output>

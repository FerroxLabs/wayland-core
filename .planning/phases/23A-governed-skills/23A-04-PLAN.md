---
phase: 23A-governed-skills
plan: "04"
type: execute
wave: 4
depends_on:
  - "23A-01"
  - "23A-02"
  - "23A-03"
files_modified:
  - crates/wcore-eval-scenarios/tests/f23a_governed_journey.rs
  - scripts/f23a-governed-skill-journey.sh
  - scripts/f23a-governed-skill-journey.ps1
  - .planning/phases/23A-governed-skills/23A-04-BASE-SHA
  - .planning/phases/23A-governed-skills/23A-04-PANEL/
  - .planning/phases/23A-governed-skills/23A-04-MACOS-DECISION.md
  - .planning/phases/23A-governed-skills/23A-04-CONTRACT.md
  - .planning/phases/23A-governed-skills/23A-04-LIVE-EVIDENCE.md
autonomous: true
requirements:
  - F23-01
domain: code
must_haves:
  truths:
    - "THIS IS THE PHASE'S TERMINAL PLAN AND IT ALONE STATES THE PHASE OUTCOME. Following the program's acceptance contract, 23A-01, 23A-02 and 23A-03 each record their own evidence and each explicitly mark no requirement complete. This plan runs the aggregate proof at ONE exact SHA and states the disposition of F23-01 and of Success Criterion 1. It does not re-litigate a predecessor's finding; it authenticates what they recorded. Phase 20's terminal plan correctly recorded a RED aggregate and left every requirement incomplete rather than ticking a box, and Phase 20A closed COMPLETE on its three Success Criteria while leaving four requirements explicitly OPEN. Both were the right call and both are the standard here."
    - "THE ONE-RUN JOURNEY IS THE PROOF THAT THE STAGES COMPOSE, WHICH THREE SEPARATE GREEN PLANS DO NOT ESTABLISH. F23-01 names nine stages — detect, draft, quarantine, evaluate, review and policy, promote, observe, revoke, rollback — and the predecessor plans prove them in three groups against three separately-prepared states. A lifecycle proved in pieces can still be broken end to end: a promotion that works from a hand-seeded ledger can fail from a ledger the drafter's own path produced, and a rollback that restores a fixture can fail to restore an artifact that a real session wrote. One continuous run, from a draft the product itself detected to a rollback that returns the machine to where it started, is what closes that gap."
    - "A JOURNEY DRIVER WHERE EVERY STEP IS EXPECTED TO SUCCEED CANNOT GO RED FOR THE REASON THAT MATTERS, AND A CONTROL NOBODY PROVED FIRES IS THE SAME THING WITH EXTRA WORDS. The driver carries at least three negative controls whose SUCCESS is a failure: an unpromoted draft that must be refused, an unreviewed draft that must not promote, and a post-rollback artifact that must be refused again. It also carries one positive control — a user-authored skill that must execute throughout — so a build where skills are simply broken cannot masquerade as a build where governance works. Each of the three negative controls is provable by injection: `WAYLAND_F23A_SELFTEST=<control-id>` makes the drive target inject exactly the failure that control exists to catch, print `F23A-SELFTEST-TRIPPED: <control-id>` and exit nonzero, and each is exercised as its own gate. `grep -c 'control' journey.sh -ge 4` proves only that someone typed a word four times and is not a verification. The driver also asserts its own checkout SHA before doing anything and exits EXACTLY 3 on mismatch — a PINNED code, because 'distinct nonzero' cannot be gated on. Three self-passing shapes were found in this repository in the last 24 hours: a piped ssh whose status was grep's; an exit code read from a Tee-Object block that returned an array and made every comparison truthy while a 12/12 and 6/6 all-PASS soak reported failure; and `git status --porcelain` used as a gate, which EXITS 0 UNCONDITIONALLY — the third shape was found in THIS plan's own first draft, where it was the sole enforcement of the rule that ROADMAP, REQUIREMENTS, STATE and PROJECT must not be edited by a concurrent planner. None of the three appears here."
    - "THE JOURNEY'S MECHANISM IS 23A-01'S, INHERITED AND NOT REINVENTED, AND 'COMPOSE THE THREE PREDECESSOR DRIVERS' MEANS COMPOSE THEIR RUST DRIVE TARGETS. Nothing in `scripts/` can drive `wayland-core` against a scripted provider — `scripts/wayland-e2e-real-workload.sh` reaches only `--version` (line 327) and `--help` (line 329) — so the shipped-binary journey lives in `crates/wcore-eval-scenarios/tests/f23a_governed_journey.rs`, built on `wcore_eval_scenarios::governed_skill_drive`: an `OpenAiFixtureScript` loopback provider (`crates/wcore-eval-scenarios/src/fixtures/openai.rs:34`), the real binary spawned as `--json-stream --provider <id> --model <model> --base-url <fixture>` by `run_with_binary_in_paths` (`crates/wcore-eval-scenarios/src/runner.rs:462`, argv at `:340` and `:346`), the draft produced by the PRODUCT's own drafting path (`packaged_driver_gate.rs:691`, `:732`, `:749`), slash turns and a `Skill` tool call through the scenario surface (`:771` and `openai.rs:105`), and operator verbs as separate `wayland-core` processes with argv (`packaged_driver_gate.rs:267`). ONE seeded environment and ONE artifact carry all nine stages, which is only possible in-process — the `.sh` and `.ps1` files are thin wrappers whose exit status is `cargo test -p wcore-eval-scenarios --features packaged-driver-gate --test f23a_governed_journey`, set up as `justfile:165-181` and `justfile:183-184` set up the packaged gate. A wrapper that shells out to the three predecessor wrappers would rebuild and re-draft between stages, which is precisely the hand-seeded-fixture composition this plan exists to rule out."
    - "THE MACOS EVIDENCE QUESTION IS DECIDED BY THIS PLAN, NOT PARKED ON SEAN — AND IT IS DECIDED ON MEASUREMENT, BECAUSE THE PREMISE IT USED TO REST ON IS NOW FALSE. The question and its three options are unchanged: `dispatch-macos`, `close-two-platforms`, `defer-to-certification`. What changed is the evidence. Two facts were measured on the real hosts at planning time and both are decision-relevant. FIRST, a native macOS arm64 `wayland-core` IS obtainable with no dispatch, no ephemeral runner and no credentials: `.github/workflows/ci.yml` (commit `d9c7683b`) uploads `wayland-core-<target>` for all six targets, `ci.yml`'s `push:` list names this very branch, artifact `wayland-core-aarch64-apple-darwin` id `8630056949` exists on it unexpired from an ordinary push-triggered run (`30195324025`), it downloads with `gh api`, and it RAN on the planning Mac — Mach-O 64-bit arm64, `wayland-core 0.12.25`, exit 0. Running a prebuilt binary is not running Cargo, so the phase's Cargo prohibition does not forbid it. `unobtainable` is therefore dead as a basis for any option, and the artifact uploads even when the overall CI run concludes `failure`. SECOND, the plan's own stated justification for the macOS leg — a case-insensitive filesystem — is ALREADY COVERED by the authorized Windows leg: creating `Alpha` then `alpha` collapsed 2 files to 1 on SEANDESKTOP's NTFS, exactly as it does on this Mac's APFS, while Hetzner's ext4 kept both. What macOS still uniquely carries is APFS filename normalization, the `/var`→`/private/var` symlink that makes tempdir canonicalization differ, and BSD locking — none of which is the axis the plan cited. THIRD, what the CI artifact does NOT buy is the nine-stage journey itself, which is a Rust test binary that must be compiled ON macOS; only the ephemeral runner can do that, and dispatching it stays Sean's under ROADMAP execution rule line 14, with its `f20-no-ambient-secrets` label known FALSE (Sean's user, reach over the SSH directory, the AWS directory and an unlocked keychain) and `scripts/f20-native-macos-proof.sh:134`'s unconditional image pull having burned four dispatches in 20A. So the CHOICE is this plan's and the DISPATCH is Sean's, and only the `dispatch-macos` branch needs him at all."
    - "THE ADMITTED CONTRACT IS THE ACTUAL DELIVERABLE THAT PHASE 23B CONSUMES. The ROADMAP states that Phase 23B begins only from the admitted 23A contract. That contract is not this plan's summary — it is a written statement of what the governed-skill lifecycle guarantees, what it does not, and what a consumer may rely on: which artifact identity a promotion binds, which stores the transaction spans and what a partial failure leaves, what evaluation is computed from and why use counts cannot be part of it, what revocation reaches and within what window, what rollback restores and what it deliberately preserves, and which surfaces refuse unpromoted content. Every one of those is a fact a downstream plan can be wrong about, and 23B was planned before this phase existed, so the contract must be explicit rather than inferred from code."
    - "D2 IS NOT THIS PHASE'S GATE AND THIS PLAN MUST NOT TOUCH IT. The Phase 23 exit gate D2 freezes durable Goal, child, task and wait semantics and requires replay through the REAL Desktop consumer and reducer, whose half cannot be closed from this repository. `.planning/phases/23B-continuous-agency/23B-04-PLAN.md` explicitly owns naming what Core owes D2 and stopping at that boundary. This plan closes 23A only. It does not claim D2, does not restate what Core owes it, and does not mark Phase 23 complete."
    - "A REPORTED RED IS WORTH FAR MORE THAN AN ENGINEERED GREEN. Never weaken an assertion, add an ignore or allow attribute, raise a timeout, re-gate, or delete an inconvenient test to reach a gate. If a predecessor left an OPEN clause — most likely 23A-03's live-revocation exposure window — this plan records it as OPEN with its measured value and states whether Success Criterion 1 is nonetheless satisfied, giving the reasoning. It does not close an open clause by restating it more favourably."
  artifacts:
    - path: crates/wcore-eval-scenarios/tests/f23a_governed_journey.rs
      provides: "The one-run journey drive target, built on 23A-01's governed_skill_drive harness: detect, draft, quarantine, refuse, evaluate, review, promote, execute, observe, revoke, refuse, roll back, refuse — all nine stages against ONE artifact in ONE seeded environment, with three negative controls, one positive control, and a WAYLAND_F23A_SELFTEST switch per negative control"
    - path: scripts/f23a-governed-skill-journey.sh
      provides: "The Linux wrapper: asserts WAYLAND_EXPECT_SHA (exit 3 on mismatch), builds and pins the binary as justfile:165-181 does, then exits with the status of `cargo test ... --test f23a_governed_journey`. No assertion logic of its own"
    - path: scripts/f23a-governed-skill-journey.ps1
      provides: "The Windows wrapper with the same contract, following justfile:183-184's PowerShell form, invoked through the PowerShell file form so its own exit status is the gate"
    - path: .planning/phases/23A-governed-skills/23A-04-BASE-SHA
      provides: "The untouched starting commit, recorded as this plan's first action, that the production-source-untouched and shared-planning-file-untouched gates measure against — so a change this plan commits cannot hide behind a clean working tree"
    - path: .planning/phases/23A-governed-skills/23A-04-MACOS-DECISION.md
      provides: "The narrative macOS evidence decision: the question and three options unchanged, each option's real cost, the four panel positions summarised WITH the dissent, the live macOS measurements that fed them, the chosen option and why, and — if macOS is not run — the exact dispatch that would close it. This file is NARRATIVE and is deliberately NOT gated on; the gates read the raw panel captures and GitHub instead"
    - path: .planning/phases/23A-governed-skills/23A-04-PANEL/
      provides: "The raw, machine-checkable decision record the gates actually read: `bundle.txt`, the ONE shared evidence bundle every member was fed; one verbatim capture and captured exit status per external panel member (`codex.txt`/`.rc`, `gemini.txt`/`.rc`, `kimi.txt`/`.rc`) plus the internal adversarial pass (`internal.txt`), each capture's first line recording the exact command and naming the bundle; `votes` with exactly four `<member>:<option-id>` lines, each cross-checked against that member's own transcript; `decision.chosen` naming one of the plan's own three option ids; `decision.rationale`; `decision.dissent`; `decision.minority-justification` and `decision.tiebreak` when the chosen option is not the modal one or the split is tied; `decision.escalation` when the choice is `dispatch-macos`; and the four live measurement captures, each carrying a real transcript above the verdict line its own probe script emitted — `probe-macos-binary.txt` (obtainability of the CI-published native arm64 binary and the tree it was built from), `probe-macos-surface.txt` (how many of the nine F23-01 stages that binary covers natively), `probe-journey-compile.txt` (whether the nine-stage journey genuinely requires an on-macOS compile) and `probe-fs-case.txt` (the case-insensitive-filesystem axis measured on APFS, ext4 and NTFS)"
    - path: .planning/phases/23A-governed-skills/23A-04-CONTRACT.md
      provides: "The admitted 23A governed-skill contract: what the lifecycle guarantees, what it does not, and what Phase 23B may rely on"
    - path: .planning/phases/23A-governed-skills/23A-04-LIVE-EVIDENCE.md
      provides: "The aggregate proof at one exact SHA per platform, the journey outcome per stage, and the disposition of F23-01 and of Success Criterion 1 with every OPEN clause named"
  key_links:
    - from: scripts/f23a-governed-skill-journey.sh
      to: .planning/phases/23A-governed-skills/23A-04-LIVE-EVIDENCE.md
      via: "the per-stage captured transcript promoted into the recorded phase outcome"
      pattern: "live-evidence"
    - from: .planning/phases/23A-governed-skills/23A-04-MACOS-DECISION.md
      to: .planning/phases/23A-governed-skills/23A-04-LIVE-EVIDENCE.md
      via: "the authorized platform coverage bounding which legs the aggregate proof claims"
      pattern: "decision-record"
    - from: .planning/phases/23A-governed-skills/23A-04-CONTRACT.md
      to: .planning/phases/23B-continuous-agency/23B-01-PLAN.md
      via: "the admitted contract Phase 23B begins from, per the ROADMAP's internal ordering for Phase 23"
      pattern: "phase-handoff"
---

<objective>
Prove the nine stages of the governed-skill lifecycle compose in one continuous run against the shipped `wayland-core` binary, decide the macOS evidence question explicitly, publish the admitted 23A contract that Phase 23B begins from, and state the phase's disposition at one exact SHA.

Purpose: 23A-01 proved unpromoted content is inert, 23A-02 built the governed promotion transaction, and 23A-03 built observe, revoke and rollback. Each proved its own stages against its own prepared state. Nothing yet proves they compose from a draft the product itself detected through to a rollback that returns the machine to where it started. That composition is what Success Criterion 1 actually asserts, and the ROADMAP makes the admitted contract the precondition for Phase 23B.
Output: One journey driver per platform with negative and positive controls; one recorded macOS decision with its cost accepted; one aggregate build-and-test proof at one exact SHA; the admitted 23A contract; and the phase disposition with every OPEN clause named and carried forward rather than closed by restatement.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/HANDOFF-2026-07-26-phase20-20A-complete.md
@.planning/ROADMAP.md
@.planning/phases/23A-governed-skills/23A-01-SURFACE-CENSUS.md
@.planning/phases/23A-governed-skills/23A-01-LIVE-EVIDENCE.md
@.planning/phases/23A-governed-skills/23A-02-LIVE-EVIDENCE.md
@.planning/phases/23A-governed-skills/23A-03-LIVE-EVIDENCE.md
@.planning/phases/23B-continuous-agency/23B-01-PLAN.md
</context>

<execution_rules>

**THE TWO AMENDED PHASE RULES — verbatim, and they bound this plan.**
- Findings at CRITICAL or HIGH must be fixed or disproved. MEDIUM and below are logged to `.planning/BACKLOG.md` and DO NOT BLOCK execution.
- Execution begins when no CRITICAL or HIGH finding is open, or after 2 review rounds, whichever comes first. A third round is NOT permitted; it escalates to Sean.

**TERMINATION CRITERION (hard).** This plan ends in exactly one of three states and writes its SUMMARY in all three:
1. **PHASE COMPLETE** — the journey passes on every authorized platform, the aggregate is green at one exact SHA, the contract is published, and Success Criterion 1 is satisfied. Any predecessor OPEN clause is carried forward by name with its measured value and an explicit statement of why it does not block, mirroring how Phase 20A closed COMPLETE with four requirements OPEN.
2. **PHASE INCOMPLETE** — a stage of the lifecycle does not compose, or a negative control succeeds, or the aggregate is red. Record the phase as incomplete with the exact failing stage and its evidence. This is a legitimate and correct outcome; Phase 20's terminal plan recorded a RED aggregate rather than ticking a box.
3. **BLOCKED ON SEAN** — reachable by exactly two routes and no others. (a) Task 2's decision procedure lands on `dispatch-macos`, whose ACT — dispatching the ephemeral self-hosted macOS runner — is reserved to Sean by ROADMAP execution rule line 14 and by that runner's reach over real credentials. The CHOICE is still this plan's and is still recorded; only the dispatch waits, and the escalation carries the panel's split rather than a question. (b) The panel deadlocks 2-2 with no evidentiary tiebreak. Anything else is NOT this state: a decision the panel can reach on the evidence is MADE, recorded, and executed. Do NOT convert an ordinary judgement into a Sean gate.
Under no circumstances does this plan create additional plans, fix a defect a predecessor left, or extend its own task list. If the journey exposes a defect, that is termination state 2 with the defect recorded — not a repair.

**SCOPE BOUNDARY (hard).**
- This plan closes Phase 23A ONLY. It does NOT claim the Phase 23 exit gate D2, does not restate what Core owes D2, and does not mark Phase 23 complete. `.planning/phases/23B-continuous-agency/23B-04-PLAN.md` owns the D2 boundary.
- This plan does NOT touch Phase 23B's surface: operator session lifecycle, memory and user-model controls, cache and compaction economics, the repository index and the multi-day journey are planned under `.planning/phases/23B-continuous-agency/`.
- This plan writes ONLY inside its own phase directory, `scripts/`, and the single test target `crates/wcore-eval-scenarios/tests/f23a_governed_journey.rs`. That one file under `crates/` is the journey itself and is TEST code, not production source: nothing under any crate's `src/` and no `Cargo.toml` or `Cargo.lock` is touched, and the production-source gate excludes exactly that one path and nothing else. It does NOT edit `ROADMAP.md`, `STATE.md`, `REQUIREMENTS.md` or `PROJECT.md`; updating the F23-01 and Phase 23A status rows at phase close is the orchestrator's action, and this plan supplies the disposition it acts on. The sibling terminal plan `23B-04` observes the same boundary.
- No push, merge, PR, tag, release, deployment, canary promotion, native proof dispatch or GitHub issue closure. Every one of those is Sean's explicit authorization under the ROADMAP execution rules.

**FOUR-PLAN CAP.** This phase has exactly 4 plans and this is the fourth. Do not propose a fifth.

**ENVIRONMENT.**
- Repository: `/Users/seandonahoe/dev/waylandcore-ferrox`, branch `plan/f20-unified-audit-repair`. NEVER touch `/Users/seandonahoe/dev/waylandcore`.
- NEVER run Cargo on this Mac. `cargo fmt --all -- --check` is the only cargo command used locally.
- Linux authority: `ssh -o BatchMode=yes hetzner-dsm`, `/root/wayland`. The full aggregate runs there in roughly 194 seconds.
- Windows: `ssh -o BatchMode=yes SeanD@seandesktop`, checkout `C:\ferrox-win`, PowerShell default shell, cargo at `C:\Users\seand\.cargo\bin\cargo.exe`. `cargo fmt --all` FAILS there with os error 206. Windows CI runs clippy `-D warnings` BEFORE tests, so a lint failure means tests never execute. A second Windows runner, `ferrox-win-msvc`, exists and was idle for the whole 20A effort; if both are used, give each its own worktree — concurrent compile load on one shared box corrupted a proof run.
- macOS, and this is a THREE-WAY distinction the plan's earlier drafts collapsed into one. (i) The ephemeral self-hosted runner at `~/actions-runner-macos/` is the ONLY way to COMPILE for macOS here; it consumes itself per job, must be re-registered per dispatch, and dispatching it is Sean's authorization under ROADMAP line 14. (ii) A native macOS arm64 `wayland-core` BINARY needs none of that: `ci.yml` names this branch in its `push:` list and its cross-build matrix builds `aarch64-apple-darwin` on a GitHub-hosted `macos-latest` runner, and since `d9c7683b` uploads it as artifact `wayland-core-aarch64-apple-darwin` with `retention-days: 14`. Verified at planning time: artifact `8630056949` from push-run `30195324025` downloaded via `gh api repos/FerroxLabs/wayland-core/actions/artifacts/<id>/zip`, `file` reported Mach-O 64-bit arm64, and it ran on the planning Mac printing `wayland-core 0.12.25` at exit 0. The artifact is uploaded even when the overall run concludes `failure`, and `concurrency.cancel-in-progress` is FALSE for branch pushes so a later push does not cancel it. (iii) Running that prebuilt binary on the Mac is NOT running Cargo and is NOT forbidden. So the operator-verb and refusal surface is measurable on macOS autonomously; the in-process nine-stage journey is not.

**THE FOUR-WAY PANEL.** Where this plan must decide rather than halt, the question goes to four independent reviewers given the SAME evidence bundle, each response captured VERBATIM to its own file with its exit status captured on the NEXT line:
- `codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check "$(cat <bundle>)"`
- `gemini -p "$(cat <bundle>)" -m gemini-3.1-pro-preview -o text` — a `GOOGLE_API_KEY` notice on stderr is a notice, not an error; capture stderr separately so it cannot pollute the response file.
- `/Users/seandonahoe/.kimi-code/bin/kimi -p "$(cat <bundle>)" --output-format text` — the ABSOLUTE path is required; the Bash shell predates the PATH entry, and `~/.local/bin/kimi-cli` is dead.
- One internal adversarial pass by the executor, written to its own file, which must argue AGAINST the emerging consensus rather than restate it.
All three external CLIs were probed live against this plan's actual macOS bundle at planning time and all three answered at exit 0. Never paraphrase a response into the record; paraphrase is where a dissent quietly disappears.
- Both hosts' fetch refspecs are pinned to an unrelated branch. ALWAYS `git fetch origin plan/f20-unified-audit-repair`.
- Mac `grep` is rtk-proxied and SILENTLY DROPS LINES. ALWAYS `/usr/bin/grep`, `-F` for literals. Use `/usr/bin/git` on the Mac.
- In `cmd`, `set VAR=x && ...` appends a TRAILING SPACE. Use `set "VAR=x"` or `$env:VAR='x'` and PROVE it took effect.
- The PTY harness is UNIX-ONLY by construction; do not claim a Windows PTY result.

**THE SELF-PASSING GATE BAN (hard).**
- `ssh host 'cmd' | grep -v CLIXML` is FORBIDDEN as a gate; the pipeline's status is grep's. Filter for READING only.
- Reading an exit code from a block that also emits output — for example around a `Tee-Object` pipeline — is FORBIDDEN. Read it on the line AFTER the pipeline.
- `git status --porcelain`, `git diff --stat` and `git diff --name-only` are FORBIDDEN as bare gates: all three EXIT 0 UNCONDITIONALLY. This matters more here than anywhere else in the phase, because the shared-planning-file gate is the ONLY enforcement of the orchestrator's hard rule that `ROADMAP.md`, `REQUIREMENTS.md`, `STATE.md` and `PROJECT.md` must not be edited while a concurrent planner is running. Wrap them in `test -z "$(...)"`, and pair a working-tree leg with a base-SHA diff leg — the working-tree leg alone goes permanently green the moment this plan commits anything, which for a plan that commits several artifacts is immediately.
- Every remote gate redirects to a file, captures the status on the next line, and exits with that status.
- The phase disposition is NOT closed by grepping an evidence file this plan wrote. It is closed by the journey drivers' exit statuses, the aggregate's exit status, the negative controls, and each control's self-test.
- `grep -c '<word>' <file-this-plan-authored>` is a word count, never a verification.

**THE BASE SHA (hard, and it is this plan's FIRST action).** Before reading or writing anything else, record the untouched starting commit: `/usr/bin/git rev-parse HEAD > .planning/phases/23A-governed-skills/23A-04-BASE-SHA`. The production-source-untouched gate and the shared-planning-file-untouched gate both read it. Never rewrite it later.

**IF THE SHARED-PLANNING-FILE GATE GOES RED, REPORT — NEVER REVERT AND NEVER WIDEN THE EXCLUSION.** The gate cannot tell whether this plan or a concurrent planner touched `ROADMAP.md`, `REQUIREMENTS.md`, `STATE.md` or `PROJECT.md`, and it does not need to: either way the phase must not close on that tree. If the change was this plan's, undo it inside this plan. If it was another agent's, that is a finding for Sean — record the file, the diff and the owning agent if identifiable, and stop. Reverting another planner's committed work to clear a gate would destroy someone else's phase, which is strictly worse than the red it was hiding. Adding an exclusion to make the gate pass is the same act with better manners.

**WHY THE THREE UNTOUCHED-SURFACE GATES ARE SCOPED DIFFERENTLY — do not "simplify" them into one.** The `crates/` gate and the four-shared-planning-files gate each pair a `git status --porcelain` leg (catches uncommitted AND untracked) with a `git diff --name-only "$BASE"..HEAD` leg (catches what this plan already committed); either alone is a self-passing gate. The broader write-scope gate uses ONLY the base-SHA diff leg, deliberately: OTHER planners are editing other phase directories in this same working tree concurrently — measured at planning time, four sibling phase plans showed as modified — so a repo-wide working-tree leg would be GUARANTEED RED for reasons this plan does not control, and a guaranteed-red gate gets disabled and teaches nothing. A gate that cannot go red and a gate that cannot go green are the same failure.

**DRIVER SELF-TESTS (hard).** Both wrappers honour `WAYLAND_EXPECT_SHA` — set to a SHA the checkout does not match, the wrapper MUST exit exactly 3 — and `WAYLAND_F23A_SELFTEST=<control-id>`, implemented once in the drive target so both platforms inherit it. Three control ids exist, one per negative control: `unpromoted-runs` promotes nothing but asserts the unpromoted draft executes; `unreviewed-promotes` promotes the unreviewed draft and asserts it does; `rollback-still-runs` skips the rollback and asserts the artifact is refused. Each MUST make the run fail, print `F23A-SELFTEST-TRIPPED: <control-id>` and exit nonzero. A control that stays green under its own injection has never fired and is decoration.

**AGENTS.md discipline.** Surgical diffs. No production source is modified by this plan — it proves and records. Its one file under `crates/` is the journey drive target, which is test code under `tests/`; if a proof requires a change to any crate's `src/`, to a `Cargo.toml` or to `Cargo.lock`, that is termination state 2 and not a repair. Clippy-clean at `-D warnings`, since the drive target compiles in the workspace. Stage exact paths, never `-A`, never `.`. No `Co-Authored-By` trailers.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: The one-run governed-skill journey with three negative controls and one positive control</name>
  <files>crates/wcore-eval-scenarios/tests/f23a_governed_journey.rs, scripts/f23a-governed-skill-journey.sh, scripts/f23a-governed-skill-journey.ps1</files>
  <read_first>crates/wcore-eval-scenarios/src/governed_skill_drive.rs (23A-01's shared harness: the tempdir home-and-project seeding, the helper that drives the product's own drafting path until a real `auto-*` draft exists, the public route-refusal assertion set, the operator-verb helper, and the self-test switch plumbing — this task composes these, it does not rewrite them), crates/wcore-eval-scenarios/tests/f23a_boundary_drive.rs and crates/wcore-eval-scenarios/tests/f23a_promotion_drive.rs and crates/wcore-eval-scenarios/tests/f23a_revocation_drive.rs (the three predecessor drive targets whose arcs this one composes into a single seeded environment), crates/wcore-eval-scenarios/src/fixtures/openai.rs (`OpenAiFixtureScript` at 34, `OpenAiStep` at 41, `tool_call` at 105, `base_url` at 407, `shutdown` at 438), crates/wcore-eval-scenarios/src/runner.rs (`run_with_binary_in_paths` at 462, the argv at 338-346, `discover_binary` at 215), crates/wcore-eval-scenarios/tests/packaged_driver_gate.rs (`LifecycleMatrixEnv` at 691, `generated_artifacts` at 732, `lifecycle_generation_scenario` at 749, `lifecycle_catalog_scenario` at 771, `driver` at 267 — the shapes the harness was extracted from), scripts/f23a-boundary-drive.sh (23A-01's thin wrapper: the SHA assertion with its pinned exit 3, the build-and-pin sequence, and the fact that it carries no assertion logic), .planning/phases/23A-governed-skills/23A-01-SURFACE-CENSUS.md (the enumerated routes the journey's refusal stages must cover), crates/wcore-cli/src/skills_cmd.rs (the verbs, their stable stdout tokens and their distinct exit codes, so the journey observes outcomes rather than parsing prose), justfile (the packaged-driver-gate recipes at 165-181 for unix and 183-184 for windows: `WAYLAND_BUILD_SOURCE_SHA`, the absolute `WCORE_EVAL_BIN` derivation with the `.exe` suffix on Windows, and the `exit $LASTEXITCODE` discipline)</read_first>
  <behavior>
    - The journey is a Rust drive target, `crates/wcore-eval-scenarios/tests/f23a_governed_journey.rs`, built on 23A-01's `governed_skill_drive` harness — an `OpenAiFixtureScript` loopback provider, the real binary through `--json-stream`, the product's own drafting path, slash turns and a `Skill` tool call, and operator verbs as separate `wayland-core` processes. The two shell files are thin wrappers whose exit status is `cargo test -p wcore-eval-scenarios --features packaged-driver-gate --test f23a_governed_journey`. They do NOT shell out to the three predecessor wrappers, because each of those rebuilds and re-drafts, which would reintroduce exactly the between-stage fixture seeding this plan exists to rule out.
    - Stage 1 (detect and draft): the product's own drafting path produces a generated skill. The journey does not hand-write the artifact, because a hand-written fixture proves the lifecycle over a fixture.
    - Stage 2 (quarantine and refuse): the shipped binary refuses to run it, and refuses it at the routes the census enumerated.
    - Stage 3 (evaluate and review): the evaluation is computed and an operator review is recorded, each observable in captured output.
    - Stage 4 (promote): governed promotion succeeds with its stable token and a zero exit.
    - Stage 5 (execute): the SAME artifact now executes, with its observable effect captured.
    - Stage 6 (observe): the shipped binary reports the bound hash, the authorising review, the evaluation outcome and the history.
    - Stage 7 (revoke and refuse): revocation succeeds and the artifact is refused again at the same routes.
    - Stage 8 (rollback and refuse): rollback restores the pre-promotion state, the artifact is still refused, and the history still shows every transition in order.
    - Negative control A: a draft that was never reviewed does not promote at any point in the run.
    - Negative control B: a draft that was never promoted never executes at any point in the run.
    - Negative control C: after rollback, the artifact does not execute.
    - Positive control: a user-authored skill executes at every stage of the run.
    - Each negative control is provable by injection: `WAYLAND_F23A_SELFTEST=unpromoted-runs`, `=unreviewed-promotes` and `=rollback-still-runs` each inject the failure that control exists to catch, print `F23A-SELFTEST-TRIPPED: <control-id>` and exit nonzero.
    - The wrapper asserts its own checkout SHA first and exits EXACTLY 3 on mismatch; the run exits nonzero on any stage deviation and on any control violation, and it names which stage or control failed.
  </behavior>
  <action>Compose the three predecessor DRIVE TARGETS into one continuous run rather than rewriting their logic — and compose them in Rust, not in shell. Each already established a fragment of the arc with its own controls against 23A-01's shared `governed_skill_drive` harness; the value this target adds is that all nine stages run against ONE artifact that the product itself detected and drafted, in ONE seeded environment, with no hand-seeded fixture and no rebuild between stages. That is only achievable in-process: a shell script that invoked the three predecessor wrappers in sequence would rebuild the binary and draft a fresh artifact three times, and the composition claim — the entire reason this plan exists — would be false while the gate stayed green. If a stage needs a fixture to work, that fact is itself the finding: record it and let the run go red rather than seeding around it.

So the deliverable is `crates/wcore-eval-scenarios/tests/f23a_governed_journey.rs`, feature-gated on `packaged-driver-gate` exactly as `packaged_driver_gate.rs:1` is, standing up one `OpenAiFixtureScript` provider and one seeded home-and-project, driving the real binary through `--json-stream` via `run_with_binary_in_paths` for every agent, slash and tool step, and invoking `wayland-core`'s operator verbs as separate argv processes for review, promote, revoke and roll back. Every assertion reads the captured transcript and trace, never an internal flag.

The two shell files are thin wrappers and nothing else: read `WAYLAND_EXPECT_SHA`, compare to `git rev-parse HEAD`, exit EXACTLY 3 on mismatch, print the SHA; export `WAYLAND_BUILD_SOURCE_SHA`; `cargo build --locked -p wcore-cli --bin wayland-core`; derive an absolute `WCORE_EVAL_BIN` from `CARGO_TARGET_DIR` or `target`, with `.exe` on Windows, exactly as `justfile:165-181` and `justfile:183-184` do; then run the drive target and exit with ITS status. Logic in a wrapper is logic nobody tests.

Carry all four controls for the entire run, not just at the moment they are checked. The unreviewed draft and the never-promoted draft must be present from stage 1 and probed at several points, because a defect that unquarantines everything at stage 4 would be invisible to a control that is only probed at stage 8. The positive control must execute at every stage, because that is what distinguishes governance from breakage.

Then prove each negative control can actually fire, because a control that has never failed has never been tested. Implement the three self-test injections in the drive target so both platforms inherit one implementation: `unpromoted-runs` asserts the never-promoted draft executes; `unreviewed-promotes` promotes the unreviewed draft and asserts the promotion succeeded; `rollback-still-runs` skips the rollback and asserts the artifact is refused. Each must fail the run, print `F23A-SELFTEST-TRIPPED: <control-id>` and exit nonzero. This is what replaces counting the word "control" in a file the executor wrote — a count that is satisfied by four comments and proves nothing about whether the run can go red.

On failure, name the stage and the control. A driver that exits 1 with no indication of which of thirteen checks failed costs an hour of rediscovery per failure, and this driver will be re-run on three surfaces.

On Windows, use the trap-safe environment assignment form and prove it took effect before trusting anything downstream. Invoke the PowerShell driver through the file form so its own exit status is the gate. Never read an exit code from a block that also emits output.

Records evidence for F23-01; marks no requirement complete — Task 3 states the disposition.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -x scripts/f23a-governed-skill-journey.sh &amp;&amp; test -f scripts/f23a-governed-skill-journey.ps1 &amp;&amp; test -f crates/wcore-eval-scenarios/tests/f23a_governed_journey.rs &amp;&amp; bash -n scripts/f23a-governed-skill-journey.sh</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -cF 'f23a_governed_journey' scripts/f23a-governed-skill-journey.sh)" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'f23a_governed_journey' scripts/f23a-governed-skill-journey.ps1)" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'governed_skill_drive' crates/wcore-eval-scenarios/tests/f23a_governed_journey.rs)" -ge 1</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox; HITS=$(/usr/bin/grep -nE '^[^#]*(bash|sh|pwsh|powershell|source|\.|&amp;)[^#]*f23a-(boundary|promotion|revocation)-drive' scripts/f23a-governed-skill-journey.sh scripts/f23a-governed-skill-journey.ps1); test -z "$HITS" || { printf 'the journey wrapper INVOKES a predecessor wrapper, which rebuilds and re-drafts between stages and falsifies the one-artifact composition claim:\n%s\n' "$HITS"; exit 1; }</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox; B=$(cat .planning/phases/23A-governed-skills/23A-04-BASE-SHA); test -n "$B" || { echo "base SHA not recorded"; exit 1; }; OUT=$(/usr/bin/git status --porcelain -- crates/ ':(exclude)crates/wcore-eval-scenarios/tests/f23a_governed_journey.rs'; /usr/bin/git diff --name-only "$B"..HEAD -- crates/ ':(exclude)crates/wcore-eval-scenarios/tests/f23a_governed_journey.rs'); test -z "$OUT" || { printf 'this plan modified production source, which is termination state PHASE INCOMPLETE and not a repair:\n%s\n' "$OUT"; exit 1; }</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes hetzner-dsm "cd /root/wayland &amp;&amp; git fetch origin plan/f20-unified-audit-repair &amp;&amp; git checkout --detach $SHA &amp;&amp; git rev-parse HEAD &amp;&amp; WAYLAND_EXPECT_SHA=$SHA bash scripts/f23a-governed-skill-journey.sh" &gt; /tmp/f23a-04-journey-linux.log 2&gt;&amp;1; rc=$?; tail -100 /tmp/f23a-04-journey-linux.log; exit $rc</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes hetzner-dsm "cd /root/wayland &amp;&amp; WAYLAND_EXPECT_SHA=0000000000000000000000000000000000000000 bash scripts/f23a-governed-skill-journey.sh; echo SHA_RC=\$?; for c in unpromoted-runs unreviewed-promotes rollback-still-runs; do WAYLAND_EXPECT_SHA=$SHA WAYLAND_F23A_SELFTEST=\$c bash scripts/f23a-governed-skill-journey.sh; echo SELFTEST_RC_\$c=\$?; done" &gt; /tmp/f23a-04-selftest-linux.log 2&gt;&amp;1; ok=1; /usr/bin/grep -qF 'SHA_RC=3' /tmp/f23a-04-selftest-linux.log || ok=0; for c in unpromoted-runs unreviewed-promotes rollback-still-runs; do /usr/bin/grep -qE "SELFTEST_RC_$c=[1-9]" /tmp/f23a-04-selftest-linux.log || ok=0; /usr/bin/grep -qF "F23A-SELFTEST-TRIPPED: $c" /tmp/f23a-04-selftest-linux.log || ok=0; done; test "$ok" -eq 1 || { echo "one or more journey controls did not fire under injection — they are decorative"; tail -60 /tmp/f23a-04-selftest-linux.log; exit 1; }</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes SeanD@seandesktop "cmd /c \"cd /d C:\ferrox-win &amp;&amp; git fetch origin plan/f20-unified-audit-repair &amp;&amp; git checkout --detach $SHA &amp;&amp; git rev-parse HEAD\"; \$env:WAYLAND_EXPECT_SHA='$SHA'; powershell -NoProfile -File C:\ferrox-win\scripts\f23a-governed-skill-journey.ps1; exit \$LASTEXITCODE" &gt; /tmp/f23a-04-journey-win.log 2&gt;&amp;1; rc=$?; /usr/bin/grep -v CLIXML /tmp/f23a-04-journey-win.log &gt; /tmp/f23a-04-journey-win.clean 2&gt;/dev/null; tail -100 /tmp/f23a-04-journey-win.clean; test $rc -eq 0 || { echo "the Windows journey exited $rc; the CLIXML-filtered transcript is above and the raw capture is /tmp/f23a-04-journey-win.log"; exit $rc; }</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes SeanD@seandesktop "\$env:WAYLAND_EXPECT_SHA='0000000000000000000000000000000000000000'; powershell -NoProfile -File C:\ferrox-win\scripts\f23a-governed-skill-journey.ps1; Write-Output \"SHA_RC=\$LASTEXITCODE\"; \$env:WAYLAND_EXPECT_SHA='$SHA'; foreach (\$c in 'unpromoted-runs','unreviewed-promotes','rollback-still-runs') { \$env:WAYLAND_F23A_SELFTEST=\$c; powershell -NoProfile -File C:\ferrox-win\scripts\f23a-governed-skill-journey.ps1; Write-Output \"SELFTEST_RC_\$(\$c)=\$LASTEXITCODE\" }" &gt; /tmp/f23a-04-selftest-win.log 2&gt;&amp;1; ok=1; /usr/bin/grep -qF 'SHA_RC=3' /tmp/f23a-04-selftest-win.log || ok=0; for c in unpromoted-runs unreviewed-promotes rollback-still-runs; do /usr/bin/grep -qE "SELFTEST_RC_$c=[1-9]" /tmp/f23a-04-selftest-win.log || ok=0; /usr/bin/grep -qF "F23A-SELFTEST-TRIPPED: $c" /tmp/f23a-04-selftest-win.log || ok=0; done; test "$ok" -eq 1 || { echo "one or more Windows journey controls did not fire under injection"; /usr/bin/grep -v CLIXML /tmp/f23a-04-selftest-win.log | tail -60; exit 1; }</automated>
  </verify>
  <done>The journey drive target `crates/wcore-eval-scenarios/tests/f23a_governed_journey.rs` exists, is built on 23A-01's `governed_skill_drive` harness, and runs all nine stages against ONE artifact the product itself drafted in ONE seeded environment with no hand-seeded fixture and no rebuild between stages. Both shell files are thin wrappers that name the drive target, carry no assertion logic, and — gate-checked — reference no predecessor wrapper. Both wrappers exit exactly 3 on an expected-SHA mismatch. All four controls are present from stage 1 and probed at several points, any control violation fails the run with the stage and control named, and each of the three negative controls is PROVED to fire by its own `WAYLAND_F23A_SELFTEST` injection producing a tripped marker and a nonzero exit on both platforms. Both journeys ran green at one pinned SHA — Linux on Hetzner and Windows on SEANDESKTOP through the PowerShell file form. No production source was modified, proved against the recorded base SHA as well as the working tree so a commit cannot hide it. No gate took its status from a pipeline or from a bare `git status --porcelain`.</done>
</task>

<task type="auto">
  <name>Task 2 (CROSS-AUDITED AUTONOMOUS DECISION): Measure what macOS evidence is obtainable without a dispatch, put the choice to the four-way panel, and authorize one platform coverage</name>
  <files>.planning/phases/23A-governed-skills/23A-04-MACOS-DECISION.md, .planning/phases/23A-governed-skills/23A-04-PANEL/</files>
  <read_first>`.github/workflows/ci.yml` — the `push:` branch list at the top (this branch is named in it), the cross-build matrix entry `os: macos-latest` / `target: aarch64-apple-darwin`, and the `Upload release binary` step that publishes `wayland-core-${{ matrix.target }}` with `if-no-files-found: error` and `retention-days: 14`. That step's own head comment states why it exists: the arm64 macOS leg is otherwise structurally unreachable from a host forbidden to run Cargo. Read the step before deciding anything, because the option costs below are computed from it. Also `.planning/HANDOFF-2026-07-26-phase20-20A-complete.md` sections 3 and 7 (the Windows quoting and trailing-space traps every remote probe leg below is written against, and the rtk-proxied `grep` that silently drops lines on this Mac), and this plan's own `<execution_rules>` block (the three-way macOS distinction between the ephemeral runner, the CI artifact, and running a prebuilt binary).</read_first>
  <action>**THIS IS A DECISION, NOT A HALT.** The question, the three options and their costs are exactly as this plan already frames them and are NOT re-scoped. What changes is who resolves it. This task measures what macOS evidence is actually obtainable on real hardware, puts the measured evidence to the four-way panel, commits to one coverage, records the dissent, and continues into Task 3 in the same session. Only ONE thing here is Sean's and it is named precisely in Step 6: the ACT of dispatching the ephemeral macOS runner. The CHOICE is this task's.

**THE PREMISE THIS TASK EXISTS TO KILL.** "A native macOS binary is unobtainable, therefore close on Linux" is DEAD as a basis for any option, and reaching for it is the specific failure this task is written to prevent. `.github/workflows/ci.yml` since commit `d9c7683b` uploads `wayland-core-<target>` for all six targets including `aarch64-apple-darwin`, this branch is named in the workflow's `push:` list, and the artifact uploads even when the overall run concludes `failure`. Measured against the live repository at planning time and to be RE-TAKEN rather than trusted: two unexpired `wayland-core-aarch64-apple-darwin` artifacts existed on this branch, the newer being id `8630612015` from push-run `30197215792` at head SHA `60abe946` whose overall conclusion was `failure`; `gh api repos/FerroxLabs/wayland-core/actions/artifacts/8630612015/zip` returned a 30.0 MB zip; the unzipped 69 MB payload was reported by `file` as `Mach-O 64-bit executable arm64`; and it ran on the planning Mac printing `wayland-core 0.12.25` at exit 0. Running a prebuilt binary is not running Cargo, so this phase's Cargo prohibition does not forbid it. A sibling phase reached for the unobtainable escape hatch on exactly this false premise and it had to be reverted. Step 1 re-measures it, and a gate below forbids the rationale from resting on `unobtainable` unless the probe actually measured it so.

**STEP 1 — MEASURE FOUR LEGS ON REAL HARDWARE.** All four are free, none needs a credential, none needs a dispatch, and each converts a paragraph of argument into a fact. Write each leg as a script that PRINTS ITS OWN VERDICT as its final line — the executor does not type the verdict, the script emits it, so the verdict is tool output rather than an opinion about what was seen. Capture the full transcript with the verdict last into `.planning/phases/23A-governed-skills/23A-04-PANEL/`. A verdict with no transcript above it is rejected by the gate. On the Mac use `/usr/bin/grep` throughout: the shell `grep` is rtk-proxied and silently drops lines. On SEANDESKTOP, quoting through SSH into PowerShell breaks on naive nesting — write a `.ps1` and pass it base64-as-UTF-16LE via `-EncodedCommand`, and never read an exit code out of a block that also emits output.

`probe-macos-binary.txt` — IS a native macOS arm64 binary obtainable with no dispatch, and FROM WHICH TREE? List the artifacts with `gh api 'repos/FerroxLabs/wayland-core/actions/artifacts?per_page=100'`, select the newest unexpired `wayland-core-aarch64-apple-darwin`, and paste the raw listing into the capture. Record its id, its run id, and that run's `head_sha`. Download the zip, unzip it to a scratch directory outside the repository, run `file` on the payload, `chmod +x`, run `--version`, and capture the exit status. Then compare the run's `head_sha` to the SHA this phase is pinned at. TREE IDENTITY IS THE LOAD-BEARING PART and it is why this leg is not a formality: an artifact built from a different tree proves things about that tree and not this one, so it can never close macOS coverage of Success Criterion 1 at the pinned SHA no matter which option is chosen. Note also that this plan may not push, so no CI run can be triggered for a commit this plan makes — whatever artifact exists is from the last pushed SHA, and if that differs from the pinned SHA the leg records `TREE=different` and the panel is given that fact. Final line: `PROBE macos-binary RESULT=&lt;obtainable|unobtainable&gt; TREE=&lt;same|different&gt; ARTIFACT=&lt;id&gt; RUNSHA=&lt;40hex&gt; RC=&lt;n&gt;`. If and only if the measurement genuinely finds no unexpired artifact, record `RESULT=unobtainable` AND an `UNOBTAINABLE-BASIS:` line with the raw listing showing zero matching entries — a bare assertion is refused by the gate.

`probe-macos-surface.txt` — HOW MUCH of the nine-stage lifecycle can that native binary prove on macOS with no compile? Using the binary Leg 1 obtained, enumerate the top-level subcommand list from `--help`, then probe each governance verb 23A-02 added under `crates/wcore-cli/src/skills_cmd.rs`, recording present-or-absent with its exit status. Do NOT hardcode verb names this plan cannot verify — read them from `skills_cmd.rs` at execution time and probe what is actually there. Then seed a scratch home-and-project on the Mac, place an unpromoted generated artifact in it, and record whether the binary refuses it. Emit one line per F23-01 stage in the form `STAGE &lt;name&gt; COVERAGE=&lt;native|needs-compile&gt;` and a final line `PROBE macos-surface RESULT=&lt;n&gt;/9 VERBS=&lt;present&gt;/&lt;expected&gt; RC=&lt;n&gt;`. This is the leg that actually sizes the gap: if most stages are natively coverable, the residual macOS gap is small and `close-two-platforms` becomes far more defensible; if almost none are, `dispatch-macos` gains real weight. Either way the panel gets a number instead of an adjective.

`probe-journey-compile.txt` — does the nine-stage journey GENUINELY require an on-macOS compile? `unzip -l` the artifact and record its full contents, confirming it carries the `wayland-core` binary and no compiled test binary. Then show that Task 1's wrapper payload is `cargo test -p wcore-eval-scenarios --features packaged-driver-gate --test f23a_governed_journey`, which needs a compiler on the target platform. Final line: `PROBE journey-compile RESULT=&lt;yes|no&gt; RC=&lt;n&gt;`. This is the pivot of the whole question: if the answer is `yes`, then full macOS coverage of Success Criterion 1 is reachable ONLY through the Sean-reserved dispatch, and the CI artifact narrows the gap without eliminating it. If the answer is `no`, `dispatch-macos` is buying something already obtainable for free and the panel must be told so.

`probe-fs-case.txt` — is the case-insensitive-filesystem justification ALREADY COVERED by the authorized Windows leg? This plan's own stated reason for wanting macOS is a case-insensitive filesystem, so that claim is load-bearing and it is directly measurable on all three hosts with one variable. In a scratch directory on each of the planning Mac (APFS), `hetzner-dsm` (ext4) and `SeanD@seandesktop` (NTFS), create a file named `Alpha`, then create one named `alpha`, then count the files in the directory. Emit per host `PROBE fs-case HOST=&lt;darwin-local|hetzner-dsm|seandesktop&gt; RESULT=&lt;collapses|preserves&gt; COUNT=&lt;n&gt; RC=&lt;n&gt;` and remove the scratch directory afterwards. The planning-time measurement, which you must re-take rather than trust: NTFS on SEANDESKTOP collapsed 2 files to 1 exactly as APFS does, while ext4 kept both. If that still holds, the authorized Windows leg ALREADY exercises the axis this plan cited, and what macOS uniquely still carries is APFS filename normalization, the `/var`→`/private/var` symlink that makes tempdir canonicalization differ, and BSD locking — none of which is the axis the plan named. That is a real and material strike against `dispatch-macos` and the panel must be given the measurement, not the folk wisdom.

**STEP 2 — BUILD ONE EVIDENCE BUNDLE AND PUT IT TO ALL FOUR MEMBERS.** Write `23A-04-PANEL/bundle.txt` containing, in this order: the question exactly as this task's `decision` element states it; all three options with their ids, names, pros and cons copied VERBATIM from this task's `options` element with nothing added, removed, softened or re-scoped; the context paragraph from this task; and the four probe verdicts with their transcripts. Every member receives this file and nothing else — that is what makes the four answers comparable. Present the three options in a rotated order rather than this plan's order, and change nothing else about them.

Capture each response VERBATIM to its own file, never a paraphrase, and capture the process exit status to a sibling `.rc` file on its own. The FIRST line of each capture is `PANEL-CMD: ` followed by the exact command that produced it, and that command must name `23A-04-PANEL/bundle.txt` — that line is how the gate proves all four were fed the same bundle. Require each member to state exactly one option id as its pick plus its reasoning, and record the four picks in `23A-04-PANEL/votes`, one per line, exactly `&lt;codex|gemini|kimi|internal&gt;:&lt;option-id&gt;`.

THREE VOTE-LOSS TRAPS, EACH MEASURED AGAINST THESE CLIs FROM THIS REPOSITORY AT PLANNING TIME RATHER THAN READ IN A DOC. Each one silently drops a member and produces a "four-way audit" that is really three-way — the same defect class as a self-passing gate: a verification that appears to run and does not. Re-verify each CLI's real behaviour yourself before relying on your extraction.
  - `gemini` REQUIRES `--skip-trust`. Measured: run from this repository WITHOUT it, `gemini -p ... -m gemini-3.1-pro-preview -o text` exited **55 with ZERO bytes of stdout** and printed `Gemini CLI is not running in a trusted directory` on stderr; run WITH `--skip-trust` it exited 0 and answered cleanly. The same command from an already-trusted directory succeeds, which is exactly why this is easy to miss. The full form is `gemini -p "$(cat 23A-04-PANEL/bundle.txt)" -m gemini-3.1-pro-preview -o text --skip-trust`, captured to `gemini.txt` with its status in `gemini.rc`. Its `GOOGLE_API_KEY`/`GEMINI_API_KEY` notice on stderr is a NOTICE, not a failure, and is never grounds for recording the member NOT-RUN; capture stderr separately so it cannot pollute the response file.
  - `kimi` BULLET-PREFIXES AND INDENTS. Measured: the response came back as `• PANEL_POSITION=beta` with the following line indented two spaces, so an anchored `^&lt;token&gt;` extraction DROPS its vote entirely. Extraction must be UNANCHORED. Use the ABSOLUTE path `/Users/seandonahoe/.kimi-code/bin/kimi -p "$(cat 23A-04-PANEL/bundle.txt)" --output-format text` — the Bash shell predates the PATH entry that would resolve the short name, and the older `kimi-cli` entry point is dead. Capture to `kimi.txt` and `kimi.rc`.
  - `codex` FENCES AND MAY RESTATE ITS FINAL BLOCK. Measured: on a short prompt it answered on one line; on a longer one it wrapped the final position inside a fenced code block. Extraction must therefore be unanchored AND take the LAST match, which is correct whether or not it restates. The form is `codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check "$(cat 23A-04-PANEL/bundle.txt)"`, captured to `codex.txt` and `codex.rc`.
  - The fourth member is your own adversarial pass, captured to `internal.txt` with the same `PANEL-CMD:` first line naming the bundle. Adversarial means you argue AGAINST whichever option you walked in believing, on the measured evidence, and you must engage at least two option ids by name. A fourth voice that ratifies the emerging consensus is not a panel member, it is a rubber stamp, and it makes the count meaningless.
If an external CLI genuinely fails to answer, write `NOT-RUN ` plus its exact stderr into its `.rc` file rather than inventing a vote: three real voices with one honestly recorded absence is worth far more than four voices where one was manufactured.

**STEP 3 — COMMIT, AND STATE THE BASIS SO IT CAN BE CHECKED.** Write the chosen option id alone on one line in `23A-04-PANEL/decision.chosen`; it must be one of exactly `dispatch-macos`, `close-two-platforms`, `defer-to-certification`. Commit to the modal position when there is one. Commit to a non-modal position INSTEAD when the measurements carry the stronger evidence — that is allowed and sometimes correct, but then write `decision.minority-justification` naming the measurement that outweighed the count. On a tie, the measurement is the tiebreak: write `decision.tiebreak` quoting the PROBE verdict line that broke it, copied from its capture. Record the reasoning in `decision.rationale`. An option the measurements ruled out cannot be chosen however well it argues, and that binding is enforced by a gate below rather than by good intentions. GENUINE DEADLOCK is a tie that no probe verdict breaks; it is NOT "this is hard". On a real deadlock, escalate to Sean CARRYING THE SPLIT and the four verbatim captures rather than a question, record it in `decision.escalation`, and continue with Task 3 meanwhile.

**STEP 4 — PRESERVE THE DISSENT.** Write `decision.dissent`: for every option that lost, the strongest argument any member made FOR it and the specific measurement or reasoning that did not carry it, plus every argument made against the option chosen. It must name at least two option ids and must not be a restatement of the winner. A decision with the losing arguments deleted is not auditable, and deletion is the easy failure here.

**STEP 5 — RECORD AND PROCEED.** Write `23A-04-MACOS-DECISION.md` as the narrative record: the question, the three options, the four verbatim positions, the four measurements, the chosen coverage, the basis, the dissent, the cost accepted in the option's own terms, and — if macOS is not run natively — the exact dispatch that would close it, written precisely enough to execute later without rediscovery. That file is NARRATIVE and is deliberately NOT gated on; the gates read the raw captures instead, because a token typed into a document this plan writes proves nothing. Then continue into Task 3 in the same session. There is no wait state here.

**STEP 6 — THE ONE NARROW SEAN DEPENDENCY, AND ONLY IT.** The CHOICE is this task's and is final. If the choice is `dispatch-macos`, the ACT of dispatching the ephemeral self-hosted macOS runner remains Sean's under ROADMAP execution rule line 14 and because that runner's `f20-no-ambient-secrets` label is known FALSE — it runs as Sean's user with reach over the SSH directory, the AWS directory and an unlocked keychain. Record the decision, write the exact dispatch into `decision.escalation`, escalate carrying the panel split, and let Task 3 bound its claimed coverage to what actually ran. Do NOT dispatch before that authorization, and do not treat the escalation as a reason to stop: Task 3 proceeds on the coverage that exists. If the choice is `close-two-platforms` or `defer-to-certification`, nothing is escalated at all and Task 3 records Success Criterion 1 with the named macOS gap.</action>
  <decision>Does Phase 23A's governed-skill lifecycle require native macOS evidence before the phase closes?</decision>
  <context>
Task 1's journey passes on Linux and Windows at one pinned SHA. macOS is uncovered, and covering it fully is not free. This Mac may not run Cargo under this phase's constraints. The macOS runner at `~/actions-runner-macos/` is EPHEMERAL — it consumes itself per job and must be re-registered for every dispatch. Its `f20-no-ambient-secrets` label is FALSE: it runs as Sean's user with reach over the SSH directory, the AWS directory and an unlocked keychain, and the real fix is a dedicated runner account that does not exist. `scripts/f20-native-macos-proof.sh:134` pulls a container image unconditionally, which caused four failed macOS dispatches during Phase 20A and is an open follow-up. Against that: nothing in this phase's lifecycle is obviously platform-specific — it is SQLite, content hashing, and file classification — but Phase 20A's whole existence is evidence that "obviously not platform-specific" has been wrong here before, and the Windows defect classes it found were path representation, handle semantics and mandatory locking, all of which this lifecycle touches. Phase 20A closed COMPLETE on its three Success Criteria with four requirements explicitly OPEN, so closing with a named, recorded gap is an established and legitimate outcome in this program. The point of this task is that the gap is CHOSEN ON MEASUREMENT, not absorbed and not argued.

TWO MEASURED FACTS THE PANEL MUST BE GIVEN, both taken on the real hosts at planning time and both decision-relevant, and both to be RE-TAKEN in Step 1 rather than trusted from this paragraph. FIRST, a native macOS arm64 `wayland-core` IS obtainable with no dispatch, no ephemeral runner and no credentials, so the option costs below must be computed against a world where the CLI surface is measurable on macOS for free. What that binary does NOT buy is the nine-stage journey itself, which is a Rust test binary that must be COMPILED on macOS — only the ephemeral runner can do that. SECOND, this plan's own stated justification for the macOS leg — a case-insensitive filesystem — appears to be ALREADY COVERED by the authorized Windows leg, because NTFS collapsed `Alpha` and `alpha` to one file exactly as APFS does while ext4 kept both. What macOS still uniquely carries is APFS filename normalization, the `/var`→`/private/var` symlink that makes tempdir canonicalization differ, and BSD locking — none of which is the axis this plan cited. So the CHOICE is this plan's and only the DISPATCH is Sean's.
  </context>
  <options>
    <option id="dispatch-macos">
      <name>Dispatch the ephemeral macOS runner and run the journey natively before closing the phase</name>
      <pros>Closes Success Criterion 1 on all three platforms with no owed evidence; exercises the lifecycle against a case-insensitive filesystem, which is the most plausible place a content-hash-and-path-classification design behaves differently; produces the third leg the later native certification phase will want anyway</pros>
      <cons>Spends a Sean-gated dispatch on a runner whose no-ambient-secrets label is known false, so the run has reach over real credentials; requires re-registering the ephemeral runner; the existing macOS proof script's unconditional image pull already burned four dispatches and is unfixed; adds wall-clock time to a phase whose Linux and Windows evidence is already complete</cons>
    </option>
    <option id="close-two-platforms">
      <name>Close Phase 23A on Linux and Windows, and record macOS as owed with the exact dispatch that would close it</name>
      <pros>The precedent exists and was correct — Phase 20A closed COMPLETE with four requirements explicitly OPEN; spends no Sean-gated dispatch and takes no credential exposure; keeps Phase 23B unblocked, which the ROADMAP's internal ordering makes the point of finishing 23A; the owed leg is recorded precisely enough to be run later without rediscovery</cons>
      <cons>Success Criterion 1 carries a named platform gap into 23B and beyond; if the lifecycle does behave differently on a case-insensitive filesystem, the discovery moves to a later and more expensive phase</cons>
    </option>
    <option id="defer-to-certification">
      <name>Close on Linux and Windows and explicitly assign the macOS leg to the native cross-platform certification phase rather than owing it here</name>
      <pros>Puts the macOS proof where the three-platform matrix already lives instead of creating a one-off dispatch now; avoids re-registering an ephemeral runner twice for the same evidence; the certification phase's matrix would cover this lifecycle more thoroughly than a single journey run would</pros>
      <cons>The gap stays open far longer, and a defect found there is discovered after several phases have built on this contract; requires the assignment to be recorded somewhere the certification phase will actually read, which this plan cannot guarantee because it does not write shared files</cons>
    </option>
  </options>
  <decision-record>The chosen option id is written alone on one line in `.planning/phases/23A-governed-skills/23A-04-PANEL/decision.chosen` and must be one of exactly: dispatch-macos, close-two-platforms, defer-to-certification. Choosing close-two-platforms or defer-to-certification carries an explicit clause in `decision.rationale` that Success Criterion 1 closes with a NAMED macOS gap, and Task 3 bounds its claimed coverage accordingly. Choosing dispatch-macos leaves the CHOICE final and escalates only the dispatch itself.</decision-record>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox; D=.planning/phases/23A-governed-skills/23A-04-PANEL; for P in macos-binary macos-surface journey-compile; do F="$D/probe-$P.txt"; test -f "$F" || { echo "no captured transcript for measurement leg $P"; exit 1; }; test "$(/usr/bin/wc -l &lt; "$F")" -ge 8 || { echo "the $P capture carries a verdict with no transcript above it, which is an executor's opinion rather than a measurement"; exit 1; }; /usr/bin/grep -qE "^PROBE $P RESULT=[A-Za-z0-9/-]+ .*RC=[0-9]+$" "$F" || { echo "the $P capture carries no verdict line emitted by the probe script itself"; exit 1; }; done</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox; F=.planning/phases/23A-governed-skills/23A-04-PANEL/probe-fs-case.txt; test -f "$F" || { echo "the case-insensitive-filesystem axis this plan cites as its own macOS justification was never measured"; exit 1; }; test "$(/usr/bin/wc -l &lt; "$F")" -ge 9 || { echo "the fs-case capture carries verdicts with no transcript above them"; exit 1; }; for H in darwin-local hetzner-dsm seandesktop; do /usr/bin/grep -qE "^PROBE fs-case HOST=$H RESULT=(collapses|preserves) COUNT=[0-9]+ RC=[0-9]+$" "$F" || { echo "no fs-case verdict for host $H, so whether the ALREADY-AUTHORIZED Windows leg covers the axis is unmeasured and the macOS cost cannot be weighed"; exit 1; }; done</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox; D=.planning/phases/23A-governed-skills/23A-04-PANEL; O='dispatch-macos|close-two-platforms|defer-to-certification'; test -f "$D/votes" || { echo "no vote record"; exit 1; }; test "$(/usr/bin/grep -cE "^(codex|gemini|kimi|internal):($O)$" "$D/votes")" -eq 4 || { echo "the vote record does not carry exactly four members each picking one of this plan's own three option ids"; exit 1; }; for M in codex gemini kimi internal; do T="$D/$M.txt"; test -f "$T" || { echo "no captured transcript for panel member $M"; exit 1; }; test "$(/usr/bin/wc -c &lt; "$T")" -ge 400 || { echo "the $M capture is a stub, not a response"; exit 1; }; /usr/bin/head -1 "$T" | /usr/bin/grep -qF 'PANEL-CMD:' || { echo "the $M capture does not record the command that produced it"; exit 1; }; /usr/bin/head -1 "$T" | /usr/bin/grep -qF '23A-04-PANEL/bundle.txt' || { echo "the $M capture does not record being fed the ONE shared bundle, so the four answers are not comparable"; exit 1; }; P=$(/usr/bin/grep -E "^$M:" "$D/votes" | /usr/bin/sed -e "s/^$M://"); /usr/bin/grep -qF "$P" "$T" || { echo "$M is recorded as picking $P but that option id appears nowhere in $M's own captured transcript"; exit 1; }; done</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox; D=.planning/phases/23A-governed-skills/23A-04-PANEL; /usr/bin/head -1 "$D/gemini.txt" | /usr/bin/grep -qF -- '--skip-trust' || { echo "the gemini invocation omitted --skip-trust; measured from this repository it then exits 55 with ZERO bytes of stdout, silently turning a four-way panel into a three-way one"; exit 1; }; /usr/bin/head -1 "$D/kimi.txt" | /usr/bin/grep -qF '/Users/seandonahoe/.kimi-code/bin/kimi' || { echo "the kimi invocation did not use the absolute path, which is the only form that resolves in this non-interactive shell"; exit 1; }; for M in codex gemini kimi; do test -f "$D/$M.rc" || { echo "no captured exit status for external member $M"; exit 1; }; /usr/bin/grep -qE '^(0|NOT-RUN .{10,})$' "$D/$M.rc" || { echo "$M neither exited 0 nor is recorded NOT-RUN with its stderr; a member that failed silently must never be counted as a vote"; exit 1; }; done</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox; D=.planning/phases/23A-governed-skills/23A-04-PANEL; CH=$(cat "$D/decision.chosen" 2&gt;/dev/null); echo "$CH" | /usr/bin/grep -qxE 'dispatch-macos|close-two-platforms|defer-to-certification' || { echo "the chosen id is absent or is not one of this plan's own three options"; exit 1; }; C=$(/usr/bin/sed -n 's/^[a-z]*://p' "$D/votes" | sort | uniq -c | sort -rn); TOP=$(echo "$C" | /usr/bin/sed -n 1p | /usr/bin/awk '{print $2}'); N1=$(echo "$C" | /usr/bin/sed -n 1p | /usr/bin/awk '{print $1}'); N2=$(echo "$C" | /usr/bin/sed -n 2p | /usr/bin/awk '{print $1}'); { test "$CH" = "$TOP" || test "$(/usr/bin/wc -c &lt; "$D/decision.minority-justification" 2&gt;/dev/null)" -ge 200; } 2&gt;/dev/null || { echo "a non-modal option was chosen with no substantive minority justification naming the measurement that outweighed the count"; exit 1; }; { test "$N1" -gt "${N2:-0}" || /usr/bin/grep -qE '^PROBE (macos-binary|macos-surface|journey-compile|fs-case) ' "$D/decision.tiebreak" 2&gt;/dev/null; } || { echo "a tied panel was resolved by preference; a tie must be broken by a PROBE verdict line quoted from its capture"; exit 1; }</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox; D=.planning/phases/23A-governed-skills/23A-04-PANEL; CH=$(cat "$D/decision.chosen" 2&gt;/dev/null); /usr/bin/grep -qiF 'unobtainable' "$D/decision.rationale" &amp;&amp; ! /usr/bin/grep -qE '^PROBE macos-binary RESULT=unobtainable ' "$D/probe-macos-binary.txt" &amp;&amp; { echo "the rationale rests on a native macOS binary being unobtainable, but the probe did not measure it unobtainable; ci.yml uploads wayland-core-aarch64-apple-darwin and that premise is DEAD"; exit 1; }; case "$CH" in dispatch-macos) /usr/bin/grep -qE '^PROBE journey-compile RESULT=yes ' "$D/probe-journey-compile.txt" || { echo "dispatch-macos was chosen without measuring that the journey actually needs an on-macOS compile, so the dispatch may be buying what the CI artifact already gives free"; exit 1; }; test -s "$D/decision.escalation" || { echo "dispatch-macos was chosen but the Sean-reserved dispatch was not escalated carrying the split and the exact command"; exit 1; };; close-two-platforms|defer-to-certification) /usr/bin/grep -qE '^PROBE macos-binary RESULT=(obtainable|unobtainable) ' "$D/probe-macos-binary.txt" || { echo "macOS was recorded owed without measuring what is obtainable without a dispatch"; exit 1; }; /usr/bin/grep -qE '^PROBE macos-surface RESULT=[0-9]+/9 ' "$D/probe-macos-surface.txt" || { echo "macOS was recorded owed without sizing how much of the lifecycle the native binary already covers"; exit 1; };; *) echo "no recognised option is recorded"; exit 1;; esac</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox; D=.planning/phases/23A-governed-skills/23A-04-PANEL; test -s "$D/decision.dissent" || { echo "the losing arguments were deleted"; exit 1; }; test "$(/usr/bin/wc -l &lt; "$D/decision.dissent")" -ge 4 || { echo "the dissent record is too short to carry a losing argument in its own terms"; exit 1; }; N=0; for O in dispatch-macos close-two-platforms defer-to-certification; do /usr/bin/grep -qF "$O" "$D/decision.dissent" &amp;&amp; N=$((N+1)); done; test "$N" -ge 2 || { echo "the dissent names fewer than two options, so the losing arguments were dropped rather than recorded"; exit 1; }; CH=$(cat "$D/decision.chosen" 2&gt;/dev/null); /usr/bin/grep -qvF "$CH" "$D/decision.dissent" || { echo "every line of the dissent names the winner; that is a summary of the decision, not a dissent from it"; exit 1; }</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox; D=.planning/phases/23A-governed-skills/23A-04-PANEL; test -s "$D/decision.chosen" || { echo "no decision was recorded"; exit 1; }; test "$(/usr/bin/wc -c &lt; "$D/decision.rationale")" -ge 400 || { echo "the decision has no substantive rationale"; exit 1; }; test -s "$D/bundle.txt" || { echo "the one shared evidence bundle the panel was fed was not retained"; exit 1; }; for O in dispatch-macos close-two-platforms defer-to-certification; do /usr/bin/grep -qF "$O" "$D/bundle.txt" || { echo "the bundle put to the panel dropped an option this plan frames: $O"; exit 1; }; done; CH=$(cat "$D/decision.chosen"); if test -e "$D/macos-journey.txt"; then test "$CH" = dispatch-macos || { echo "a native macOS journey capture exists although the recorded decision was $CH, so a Sean-reserved dispatch was taken without authorization"; exit 1; }; test -s "$D/decision.escalation" || { echo "a native macOS journey exists with no recorded escalation, so the dispatch has no authorization record at all"; exit 1; }; fi</automated>
  </verify>
  <done>The macOS question was DECIDED here, not parked. Four measurement legs ran on real hardware and each capture carries a real transcript with the verdict line its own probe script emitted: whether a native macOS arm64 binary is obtainable with no dispatch and from WHICH tree; how many of the nine F23-01 stages that binary covers natively; whether the nine-stage journey genuinely needs an on-macOS compile; and whether the case-insensitive-filesystem axis this plan cited is already covered by the authorized Windows leg, measured on APFS, ext4 and NTFS alike. All four panel members answered ONE shared bundle carrying all three options verbatim, each response is captured with the command that produced it and its exit status on its own, and every recorded pick appears verbatim in that member's own transcript — with `--skip-trust` present on the gemini invocation and the absolute path on kimi's, because measured on this repository their absence drops a vote silently. Exactly one coverage is recorded, it is the modal position or carries a minority justification naming the measurement that outweighed the count, and any tie is broken by a quoted PROBE verdict rather than by preference. The rationale may not rest on a macOS binary being unobtainable unless the probe measured it so. The dissent survives, naming at least two options and not merely restating the winner. `23A-04-MACOS-DECISION.md` records all of it narratively with the cost accepted in the option's own terms and, if macOS is not run, the exact dispatch that would close it. Nothing was dispatched before authorization, and no wait state occurred: only the ACT of dispatching the ephemeral runner is Sean's, and Task 3 proceeds on whatever coverage actually exists.</done>
</task>

<task type="auto">
  <name>Task 3: The aggregate proof at one exact SHA, the admitted 23A contract, and the phase disposition</name>
  <files>.planning/phases/23A-governed-skills/23A-04-CONTRACT.md, .planning/phases/23A-governed-skills/23A-04-LIVE-EVIDENCE.md</files>
  <read_first>.planning/phases/23A-governed-skills/23A-04-MACOS-DECISION.md (the authorized platform coverage, which bounds what this task may claim), .planning/phases/23A-governed-skills/23A-01-SURFACE-CENSUS.md and the three predecessor LIVE-EVIDENCE files and SUMMARYs (the recorded findings and any OPEN clause this task carries forward by name), .planning/ROADMAP.md (the Phase 23 Success Criteria, the internal ordering that makes 23B begin from the admitted 23A contract, and the exit-gate boundary this plan must not cross), .planning/phases/23B-continuous-agency/23B-01-PLAN.md (the first consumer of this contract, so the contract states what that plan can actually rely on)</read_first>
  <behavior>
    - The aggregate build and test run at ONE exact SHA on every authorized platform, with counts recorded and every residual failure named and attributed to its owning plan or to the known-red list.
    - The SHA the aggregate ran at is the same SHA the journey drivers ran at, stated explicitly, because a phase closed on evidence from two different trees is not closed.
    - The admitted contract states what the governed-skill lifecycle guarantees and what it does not, in terms a downstream plan can rely on without reading the source.
    - Every OPEN clause a predecessor recorded is carried forward by name with its measured value and an explicit statement of whether it blocks Success Criterion 1 and why.
    - The disposition of F23-01 and of Success Criterion 1 is stated plainly, bounded by the authorized platform coverage.
    - D2 is not claimed, restated or advanced, and Phase 23 is not marked complete.
  </behavior>
  <action>Run the aggregate at one exact SHA on every authorized platform and state the counts as a delta against the last recorded aggregate, naming every residual failure and attributing it — to a predecessor plan, to a Phase 23B surface, or to the known-red list the handoff already records. The known reds include a Windows sandbox descendant-reaping case, two private-DACL snapshot cases, and a worker-runtime-limits case whose timeout was deliberately not raised. Do not absorb a residual failure into a total, and do not raise a timeout to make one pass.

State the SHA once and use it everywhere. The journey drivers, the aggregate and the contract all refer to the same tree, and that identity is stated explicitly — a phase closed on evidence from two trees is not closed, and the drivers' own SHA assertions exist precisely so this claim is checkable rather than asserted.

Write the contract for its actual consumer. Phase 23B was planned before this phase existed, so it cannot have inferred anything from this code; the contract must be explicit. State: which artifact identity a promotion binds and what happens when the bytes change; which stores the promote transaction spans and what a partial failure leaves behind; what the evaluation is computed from and why production use counts cannot be part of it; what revocation reaches, within what window, and what it does not reach; what rollback restores and what it deliberately preserves; which surfaces refuse unpromoted content and where the enumerated list of them lives; and what a consumer must NOT assume. The last item matters most — a contract that only lists guarantees invites a downstream plan to assume the complement.

Carry every OPEN clause forward by name. The most likely one is 23A-03's live-revocation exposure window if propagation into a running session was not achieved. Record its measured value and state explicitly whether Success Criterion 1 is nonetheless satisfied and why. Do not close an open clause by restating it more favourably; Phase 20A carried four requirements OPEN into its COMPLETE disposition and named each one.

State the disposition of F23-01 and of Success Criterion 1, bounded by the authorized platform coverage. Do not claim, restate or advance D2 — it is Phase 23's exit gate, its Desktop consumer half cannot be closed from this repository, and `23B-04` owns naming what Core owes it. Do not mark Phase 23 complete; this plan closes 23A.

Do not edit `ROADMAP.md`, `REQUIREMENTS.md`, `STATE.md` or `PROJECT.md`. Updating the F23-01 and Phase 23A status rows is the orchestrator's action at phase close, and this task supplies the disposition it acts on.

States the phase disposition for F23-01 and Success Criterion 1.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f .planning/phases/23A-governed-skills/23A-04-CONTRACT.md &amp;&amp; test -f .planning/phases/23A-governed-skills/23A-04-MACOS-DECISION.md &amp;&amp; test -f .planning/phases/23A-governed-skills/23A-04-LIVE-EVIDENCE.md</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox; C=.planning/phases/23A-governed-skills/23A-04-CONTRACT.md; test -f "$C" || { echo "the contract was not written"; exit 1; }; S=$(/usr/bin/grep -oE '[0-9a-f]{40}' "$C" | sort -u); test -n "$S" || { echo "the contract names no 40-hex SHA; a phase closed without naming the tree it closed on is not closed"; exit 1; }; test "$(printf '%s\n' "$S" | /usr/bin/grep -c .)" -eq 1 || { printf 'the contract names more than one tree; the journey, the aggregate and the contract must all refer to ONE SHA:\n%s\n' "$S"; exit 1; }; /usr/bin/git cat-file -e "$S^{commit}" 2&gt;/dev/null || { echo "the SHA the contract names, $S, is not a commit in this repository"; exit 1; }; /usr/bin/git merge-base --is-ancestor "$S" HEAD || { echo "the SHA the contract names, $S, is not an ancestor of HEAD, so the contract describes a tree this branch never contained"; exit 1; }; /usr/bin/grep -qiE 'must not assume|does not guarantee' "$C" || { echo "the contract lists only guarantees, which invites a downstream plan to assume the complement"; exit 1; }</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox; B=$(cat .planning/phases/23A-governed-skills/23A-04-BASE-SHA); test -n "$B" || { echo "base SHA not recorded"; exit 1; }; OUT=$(/usr/bin/git status --porcelain -- .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/PROJECT.md; /usr/bin/git diff --name-only "$B"..HEAD -- .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/PROJECT.md); test -z "$OUT" || { printf 'a shared planning file was modified while a concurrent planner is running; this is the hard rule:\n%s\n' "$OUT"; exit 1; }</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox; B=$(cat .planning/phases/23A-governed-skills/23A-04-BASE-SHA); test -n "$B" || { echo "base SHA not recorded"; exit 1; }; OUT=$(/usr/bin/git status --porcelain -- crates/ ':(exclude)crates/wcore-eval-scenarios/tests/f23a_governed_journey.rs'; /usr/bin/git diff --name-only "$B"..HEAD -- crates/ ':(exclude)crates/wcore-eval-scenarios/tests/f23a_governed_journey.rs'); test -z "$OUT" || { printf 'this plan modified production source, which is termination state PHASE INCOMPLETE and not a repair:\n%s\n' "$OUT"; exit 1; }</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox; B=$(cat .planning/phases/23A-governed-skills/23A-04-BASE-SHA); test -n "$B" || { echo "base SHA not recorded"; exit 1; }; OUT=$(/usr/bin/git diff --name-only "$B"..HEAD -- . ':(exclude).planning/phases/23A-governed-skills/*' ':(exclude).planning/BACKLOG.md' ':(exclude)scripts/f23a-governed-skill-journey.sh' ':(exclude)scripts/f23a-governed-skill-journey.ps1' ':(exclude)crates/wcore-eval-scenarios/tests/f23a_governed_journey.rs'); test -z "$OUT" || { printf 'this plan COMMITTED changes outside its own phase directory, its two wrappers and its one drive target:\n%s\n' "$OUT"; exit 1; }</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes hetzner-dsm "cd /root/wayland &amp;&amp; git fetch origin plan/f20-unified-audit-repair &amp;&amp; git checkout --detach $SHA &amp;&amp; git rev-parse HEAD &amp;&amp; cargo build --locked --workspace --all-features &amp;&amp; cargo nextest run --profile ci --workspace --no-fail-fast" &gt; /tmp/f23a-04-aggregate-linux.log 2&gt;&amp;1; rc=$?; tail -60 /tmp/f23a-04-aggregate-linux.log; exit $rc</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes SeanD@seandesktop "cmd /c \"cd /d C:\ferrox-win &amp;&amp; git fetch origin plan/f20-unified-audit-repair &amp;&amp; git checkout --detach $SHA &amp;&amp; git rev-parse HEAD &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings &amp;&amp; cargo nextest run --profile ci -p wcore-skills -p wcore-cli -p wcore-agent -p wcore-memory --no-fail-fast\"; exit \$LASTEXITCODE" &gt; /tmp/f23a-04-aggregate-win.log 2&gt;&amp;1; rc=$?; /usr/bin/grep -v CLIXML /tmp/f23a-04-aggregate-win.log &gt; /tmp/f23a-04-aggregate-win.clean 2&gt;/dev/null; tail -60 /tmp/f23a-04-aggregate-win.clean; test $rc -eq 0 || { echo "the Windows aggregate exited $rc; the CLIXML-filtered transcript is above and the raw capture is /tmp/f23a-04-aggregate-win.log"; exit $rc; }</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes hetzner-dsm "cd /root/wayland &amp;&amp; git rev-parse HEAD &amp;&amp; WAYLAND_BUILD_SOURCE_SHA=$SHA cargo build --locked -p wcore-cli --bin wayland-core &amp;&amp; WAYLAND_BUILD_SOURCE_SHA=$SHA WCORE_EVAL_BIN=/root/wayland/target/debug/wayland-core cargo test --locked -p wcore-eval-scenarios --features packaged-driver-gate --test packaged_driver_gate" &gt; /tmp/f23a-04-packaged-linux.log 2&gt;&amp;1; rc=$?; tail -60 /tmp/f23a-04-packaged-linux.log; exit $rc</automated>
  </verify>
  <done>The aggregate ran at one exact SHA on every authorized platform, with counts stated as a delta and every residual failure named and attributed — none absorbed and no timeout raised. The journey, the aggregate and the packaged driver gate all refer to that same SHA and the identity is stated explicitly. The contract states what the lifecycle guarantees, what it does not, and what a consumer must not assume, in terms 23B can use without reading the source. Every predecessor OPEN clause is carried forward by name with its measured value and an explicit blocking judgement. The disposition of F23-01 and Success Criterion 1 is stated and bounded by the authorized platform coverage. D2 is neither claimed nor restated, Phase 23 is not marked complete, and no shared planning file and no production source was modified.</done>
</task>

</tasks>

## What this plan does NOT change (scope fence)

- **Production source.** This plan proves and records. Its only file under `crates/` is `tests/f23a_governed_journey.rs`, the journey drive target itself — test code, gate-checked as the sole exclusion. If a proof requires a change to any crate's `src/`, to a `Cargo.toml` or to `Cargo.lock`, that is termination state PHASE INCOMPLETE with the defect recorded — not a repair, and not a fifth plan.
- **The three predecessor wrappers.** The journey composes the predecessor DRIVE TARGETS in Rust; it does not invoke `f23a-boundary-drive`, `f23a-promotion-drive` or `f23a-revocation-drive`, each of which rebuilds and re-drafts. That prohibition is gate-checked on both wrappers.
- **Any predecessor's finding.** This plan authenticates what 23A-01, 23A-02 and 23A-03 recorded; it does not re-litigate, re-measure or soften them.
- **The Phase 23 exit gate D2.** Not claimed, not restated, not advanced. `23B-04` owns naming what Core owes it, and the Desktop consumer half cannot be closed from this repository at all.
- **Phase 23 completion.** This plan closes 23A. Phase 23 closes when 23B closes.
- **Phase 23B's surface** — operator session lifecycle, memory and user-model controls, cache and compaction economics, the repository index and the multi-day journey.
- **Shared planning files.** `ROADMAP.md`, `REQUIREMENTS.md`, `STATE.md` and `PROJECT.md` are untouched and gate-checked untouched; the status-row update at phase close is the orchestrator's action.
- **Sean-only actions.** No push, merge, PR, tag, release, deployment, canary promotion, native proof dispatch or issue closure. The macOS CHOICE is Task 2's and is made without waiting; only the ACT of dispatching the ephemeral runner waits on Sean, and only when Task 2's panel lands on `dispatch-macos`.
- **No test is deleted, weakened, re-gated, ignored or allow-attributed, and no timeout is raised.** A residual failure is named and attributed, never absorbed.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase disposition ← evidence identity | A disposition is only as sound as the claim that every piece of evidence came from the same tree |
| downstream assumption ← published contract | Whatever the contract omits is what Phase 23B will assume, having been planned before this phase existed |
| authorized coverage ← claimed coverage | A phase that claims platforms it did not run has manufactured its own completion |
| open clause ← restatement | An OPEN clause can be closed by evidence or hidden by wording, and the two look identical in a summary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23A-04-01 | Spoofing | The phase closes on evidence gathered from more than one tree, so the disposition describes a state that never existed | high | mitigate | One SHA is stated once and used everywhere; each driver asserts its own checkout SHA with a distinct nonzero code on mismatch; the aggregate, the journey and the packaged gate are all run against that SHA and the identity is stated explicitly |
| T-23A-04-02 | Spoofing | A journey driver where nothing can fail manufactures the phase's completion, and a control that has never fired is indistinguishable from one that cannot | critical | mitigate | Three negative controls whose success is a failure and one positive control whose failure is a failure, all present from stage 1 and probed at several points; the driver names the failing stage and control; EACH negative control is proved to fire by its own `WAYLAND_F23A_SELFTEST` injection producing a tripped marker and a nonzero exit, gated on both platforms out of captured remote stdout; and no banned self-passing shape appears in any gate |
| T-23A-04-09 | Spoofing | The journey is composed in shell by invoking the three predecessor wrappers, each of which rebuilds the binary and drafts a fresh artifact, so the nine stages run against three different artifacts while the gate reports one continuous run — the composition claim is false and the gate is green | critical | mitigate | The journey is a single Rust drive target on 23A-01's shared harness, running all nine stages against ONE artifact in ONE seeded environment; both wrappers are gate-checked to contain zero references to `f23a-boundary-drive`, `f23a-promotion-drive` or `f23a-revocation-drive` |
| T-23A-04-10 | Tampering | The shared-planning-file gate exits 0 unconditionally, so a concurrent planner's ROADMAP or REQUIREMENTS entries are destroyed with no signal — this was the actual state of this plan's first draft | high | mitigate | The gate is `test -z "$(...)"` over a `git status --porcelain` leg AND a `git diff --name-only "$BASE"..HEAD` leg against a base SHA recorded before any work, so neither an uncommitted edit, an untracked replacement nor an already-committed rewrite passes |
| T-23A-04-03 | Repudiation | An OPEN clause is closed by restating it more favourably rather than by evidence | high | mitigate | Every predecessor OPEN clause is carried forward BY NAME with its measured value and an explicit blocking judgement; Phase 20A's precedent of closing COMPLETE with four named OPEN requirements is the standard |
| T-23A-04-04 | Elevation of Privilege | A native macOS dispatch is run without authorization, on a runner whose no-ambient-secrets label is known false and which has reach over real credentials | high | mitigate | The CHOICE is made autonomously on measurement and a four-way panel, but the ACT of dispatching stays Sean's: choosing `dispatch-macos` is gate-bound to a recorded escalation carrying the split and the exact command, and a captured native macOS journey with any other recorded choice, or with no escalation, reddens the ordering gate — so a dispatch taken without authorization cannot pass. The false label and the credential reach are stated in the decision context so the cost is chosen rather than absorbed |
| T-23A-04-05 | Information Disclosure | The contract lists only guarantees, so Phase 23B assumes the complement of what is written | high | mitigate | The contract is required to state what a consumer must NOT assume, and that section is gate-checked present |
| T-23A-04-06 | Repudiation | A residual test failure is absorbed into an aggregate count instead of being named and attributed | medium | mitigate | Counts are stated as a delta with every residual failure named and attributed to its owning plan or to the recorded known-red list; raising a timeout to clear one is forbidden by name |
| T-23A-04-07 | Denial of Service | The plan drifts into repairing a defect the journey exposed, becoming a fifth plan in disguise | medium | mitigate | Termination state PHASE INCOMPLETE is defined as the correct response to an exposed defect; the scope fence forbids production source changes and the four-plan cap is restated |
| T-23A-04-08 | Tampering | A shared planning file is rewritten wholesale during phase close, destroying entries other phases own | medium | mitigate | This plan writes only inside its own phase directory, `scripts/` and one drive target; the four shared planning files are gate-checked untouched with a gate that can actually go red (see T-23A-04-10); the broader write-scope gate uses the base-SHA diff leg alone because concurrent planners make a repo-wide working-tree leg guaranteed-red; the status-row update is the orchestrator's scoped action |
| T-23A-04-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency is added, removed or updated; no `Cargo.toml` or `Cargo.lock` change; no install task exists in this plan |
</threat_model>

<verification>
Local gates (Mac, source level only — Cargo is never run here): `cargo fmt --all -- --check` clean; the journey drive target exists and names the shared `governed_skill_drive` harness; both wrappers exist, name the drive target, reference NO predecessor wrapper, and the shell wrapper parses under `bash -n`; the decision record, the contract and the live-evidence file all exist; the contract contains an explicit must-not-assume section; the four shared planning files are unmodified and no production source is modified, each proved with `test -z` over a working-tree leg AND a base-SHA diff leg so neither an untracked replacement nor an already-committed rewrite passes; nothing was committed outside this plan's phase directory, its two wrappers and its one drive target.

Authoritative gates (real hardware, status taken from the remote process and never from a pipeline): on Hetzner at the pinned SHA, the journey runs green including all four controls, each of the three negative controls fails loudly under its own `WAYLAND_F23A_SELFTEST` injection with a tripped marker, a wrong `WAYLAND_EXPECT_SHA` exits exactly 3, `cargo build --locked --workspace --all-features` succeeds, the full aggregate passes, and the packaged driver gate passes against the real binary built from that source SHA. On SEANDESKTOP at the same SHA, clippy at `-D warnings` passes FIRST, then the four governance-bearing crate suites, then the journey and the same self-test battery through the PowerShell file form. macOS runs only if Task 2 authorized it, and is otherwise recorded as owed with its exact dispatch.

Known unknowns to record rather than resolve here: whether the lifecycle behaves identically on a case-insensitive filesystem, which is unmeasured unless macOS was authorized; whether a revocation propagates across processes or profiles, which no plan in this phase exercised; and whether the evaluation thresholds hold for corpora other than the drafts this workspace produces.
</verification>

<success_criteria>
- One continuous run per authorized platform drives all nine F23-01 stages against a single artifact the product itself detected and drafted, in ONE seeded environment, with no hand-seeded fixture and no rebuild between stages.
- The journey's mechanism is 23A-01's `governed_skill_drive` harness composed in Rust — an `OpenAiFixtureScript` provider, the real binary through `--json-stream`, the product's own drafting path, and operator verbs as separate processes — with the `.sh` and `.ps1` files reduced to thin wrappers around `cargo test ... --test f23a_governed_journey` that reference no predecessor wrapper, gate-checked.
- Three negative controls and one positive control are present from stage 1, probed at several points, and any violation fails the run with the stage and control named.
- Each negative control is PROVED to fire: its own `WAYLAND_F23A_SELFTEST` injection produces `F23A-SELFTEST-TRIPPED: <control-id>` and a nonzero exit on every authorized platform, and a wrong `WAYLAND_EXPECT_SHA` exits exactly 3. A word count over a file the executor wrote is never accepted in place of this.
- The macOS evidence question is DECIDED here on measurement and a four-way panel rather than parked on Sean, with each option's real cost stated — the ephemeral runner, the known-false security label, the unconditional image pull that burned four dispatches, and the Phase 20A precedent for closing with named gaps. Four probes run first: macOS binary obtainability and its tree identity, how many of the nine stages that binary covers natively, whether the journey needs an on-macOS compile, and whether the case-insensitive-filesystem axis is already covered by the authorized Windows leg. The rationale may not rest on a macOS binary being unobtainable unless the probe measured it so. Only the ACT of dispatching the ephemeral runner remains Sean's, and nothing is dispatched before that authorization is recorded.
- The aggregate build and test pass at ONE exact SHA on every authorized platform, that SHA is the same one the journey and the packaged gate ran at, and the identity is stated explicitly.
- Every residual failure is named and attributed; none is absorbed into a total and no timeout is raised.
- The admitted 23A contract states what the lifecycle guarantees, what it does not, and what a consumer must NOT assume — in terms Phase 23B can rely on without reading the source.
- Every predecessor OPEN clause is carried forward by name with its measured value and an explicit blocking judgement.
- The disposition of F23-01 and Success Criterion 1 is stated and bounded by the authorized platform coverage.
- D2 is neither claimed, restated nor advanced; Phase 23 is not marked complete; no shared planning file and no production source is modified, each proved by a gate that pairs a working-tree leg with a base-SHA diff leg so a committed change cannot hide behind a clean tree.
- No gate derives its status from a pipeline, from an exit code read out of an output-emitting block, from a bare `git status --porcelain`, `git diff --stat` or `git diff --name-only` (all three exit 0 unconditionally), from a word count over a file this plan wrote, or from grepping an evidence file this plan wrote. Equally, no gate is guaranteed RED: the broader write-scope gate deliberately omits a repo-wide working-tree leg because concurrent planners are editing sibling phase directories in the same checkout.
</success_criteria>

## Artifacts this plan produces
- `.planning/phases/23A-governed-skills/23A-04-BASE-SHA` — the untouched starting commit the production-source and shared-planning-file gates measure against.
- `crates/wcore-eval-scenarios/tests/f23a_governed_journey.rs` — the one-run nine-stage journey drive target on 23A-01's shared harness, with four controls and three self-test injections.
- `scripts/f23a-governed-skill-journey.sh` and `scripts/f23a-governed-skill-journey.ps1` — the SHA-asserting thin wrappers around that drive target.
- `.planning/phases/23A-governed-skills/23A-04-MACOS-DECISION.md` — the authorized platform coverage with its accepted cost and, if macOS is owed, the exact dispatch that would close it.
- `.planning/phases/23A-governed-skills/23A-04-CONTRACT.md` — the admitted 23A governed-skill contract Phase 23B begins from.
- `.planning/phases/23A-governed-skills/23A-04-LIVE-EVIDENCE.md` — the aggregate at one exact SHA, the per-stage journey outcome, and the phase disposition.
- `23A-04-SUMMARY.md`.

<output>
Create `.planning/phases/23A-governed-skills/23A-04-SUMMARY.md` using the standard GSD summary template. Record: the exact SHA every piece of evidence was gathered at and the explicit statement that the journey, the aggregate and the packaged gate all refer to it; the per-stage journey outcome per platform with each control's result; the authorized platform coverage and the cost accepted with it, verbatim; the aggregate counts as a delta with every residual failure named and attributed; the packaged driver gate's result against the real binary; every predecessor OPEN clause carried forward by name with its measured value and blocking judgement; the contract's guarantees, non-guarantees and must-not-assume list; the disposition of F23-01 and of Success Criterion 1 bounded by the authorized coverage; the explicit statement that D2 is not claimed and Phase 23 is not marked complete; the confirmation that no shared planning file and no production source was modified; the recorded unknowns; and which of the three termination states the plan ended in. If the outcome is PHASE COMPLETE, state F23-01's disposition so the orchestrator can update the ROADMAP and REQUIREMENTS status rows; this plan does not edit them.
</output>

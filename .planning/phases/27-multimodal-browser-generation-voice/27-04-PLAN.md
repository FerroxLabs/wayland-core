---
phase: 27-multimodal-browser-generation-voice
plan: "04"
type: execute
wave: 3
depends_on:
  - "27-01"
  - "27-02"
  - "27-03"
files_modified:
  - crates/wcore-fixture-harness/fixtures/f27/MANIFEST.toml
  - crates/wcore-fixture-harness/src/lib.rs
  - crates/wcore-cli/tests/f27_media_corpus.rs
  - scripts/f27-packaged-smoke.sh
  - scripts/f27-packaged-smoke.ps1
  - .config/nextest.toml
  - .github/workflows/ci.yml
  - .planning/phases/27-multimodal-browser-generation-voice/27-04-CERTIFICATION.md
  - .planning/phases/27-multimodal-browser-generation-voice/evidence/27-04/
autonomous: true
requirements:
  - F27-05
must_haves:
  truths:
    - "PACKAGED MEANS THE SHIPPED ARCHIVE, EXTRACTED AND LAUNCHED — NOT `cargo run`, NOT `cargo test`, AND NOT `target/release/` IN PLACE. The release workflow packages a per-target archive named for the version and target — a compressed tarball on Unix and a zip on Windows — containing exactly the `wayland-core` binary, and then attests and checksums it. A packaged smoke that runs the binary out of the build directory has not tested the artifact a user receives. It must extract from the archive and run what comes out."
    - "THE RELEASE PROFILE IS NOT THE DEBUG PROFILE AND THIS REPOSITORY HAS ALREADY BEEN BURNED BY THE DIFFERENCE. `crates/wcore-cli/tests/release_binary_smoke.rs` exists because link-time optimization and single-codegen-unit builds rewire dead-code elimination and previously stripped inventory-registered items whose hosting crates were never explicitly named — a shipped-artifact regression that a debug-profile test could not see. Every packaged smoke in this plan therefore runs the release artifact, and the plugin-presence assertions in that existing test are the pattern to extend, not to duplicate."
    - "THE EXISTING TERMINAL-DRIVE HARNESS IS UNIX-ONLY BY CONSTRUCTION AND WINDOWS THEREFORE HAS NO EQUIVALENT TODAY. `crates/wcore-cli/tests/proving_ground.rs` is gated to Unix at file scope because its pseudo-terminal support module is, so the whole test crate compiles to an empty binary on Windows. That means every terminal-rendering invariant this repository proves is proved on two of the three required platforms. Closing that for the media surfaces is part of this plan's work, and if it cannot be closed the gap must be NAMED rather than left implicit in a green matrix."
    - "PHASE 20A IS THE CAUTIONARY CASE THIS PLAN EXISTS TO NOT REPEAT. It drove Windows and macOS acceptance targets to green in continuous integration and nobody ever launched the binary — nobody confirmed the process starts, the terminal renders, keys do what they should, or streaming output does not garble. A green matrix is evidence about the matrix. This plan's output is only meaningful if the artifact was actually launched on each named platform and its behavior captured."
    - "THE CORPUS IS ONLY DETERMINISTIC IF IT IS PINNED. Plans 27-01, 27-02 and 27-03 each produced corpus entries in their own subdirectory with recorded lengths and digests. Determinism means those bytes are the bytes every platform runs, verified before the run rather than assumed — a corpus entry that a text-mode checkout silently rewrites is exactly the class of cross-platform defect this repository has already paid for once, and its committed attributes rules are the mechanism that prevents it."
    - "NATIVE EVIDENCE CANNOT BE INFERRED. A Windows or macOS result is bound to a newly dispatched, panel-authorized run against one exact sealed candidate. It is never derived from source, from cross-compilation, from the Linux proof, from prose, from an unavailable runner, or from a reused earlier run. Phase 20A closed on exactly that discipline and it is not relaxed here."
    - "THE ACCEPTANCE JUDGEMENT IS DECIDED HERE; THE PUBLICATION IS NOT. Judging whether the captured three-platform dry run meets this plan's acceptance criteria, and authorizing ONE dispatch of a continuous-integration run on this feature branch, is an evidence judgement and it is adjudicated on a four-way panel in Task 2 without waiting on anyone. Merging to main, opening a pull request, tagging, releasing, deploying, running a canary, closing an issue and deleting a retained evidence reference are irreversible outward acts that no panel may authorize; they remain Sean's and this phase performs none of them. The line between the two is recorded, gate-checked, and never blurred."
    - "A REPORTED RED IS WORTH FAR MORE THAN AN ENGINEERED GREEN. Never weaken an assertion, `#[ignore]`, `#[allow]`, raise a timeout, or delete a test to reach a gate. A platform leg that cannot run states NOT RUN with its reason; a criterion that cannot be closed inside this phase is reported unclosed rather than papered over."
  artifacts:
    - path: crates/wcore-fixture-harness/fixtures/f27/MANIFEST.toml
      provides: "The pinned corpus manifest — every entry from all three upstream plans with its byte length and digest, verified before any platform run"
    - path: crates/wcore-cli/tests/f27_media_corpus.rs
      provides: "The corpus-driven focused suite that runs identically on all three platforms and drives the shipped artifact"
    - path: scripts/f27-packaged-smoke.sh
      provides: "The Unix packaged smoke — build the release archive as the release workflow packages it, extract it, and exercise the extracted binary"
    - path: scripts/f27-packaged-smoke.ps1
      provides: "The Windows packaged smoke, same contract through the platform's own archive format"
    - path: .planning/phases/27-multimodal-browser-generation-voice/27-04-CERTIFICATION.md
      provides: "The per-platform certification record binding every Success Criterion to the exact candidate, the dispatched run, the captured evidence, and an explicit statement of anything left unproved"
  key_links:
    - from: crates/wcore-fixture-harness/fixtures/f27/MANIFEST.toml
      to: crates/wcore-cli/tests/f27_media_corpus.rs
      via: "digest verification before any corpus entry is used, so a platform-rewritten byte fails loudly instead of silently changing the input"
      pattern: "pinned-input"
    - from: scripts/f27-packaged-smoke.sh
      to: .planning/phases/27-multimodal-browser-generation-voice/27-04-CERTIFICATION.md
      via: "the extracted-artifact run whose captured output is the certification's evidence"
      pattern: "artifact-to-evidence"
---

<objective>
Pin the media corpus, make it run identically everywhere, and certify Phase 27 by launching the PACKAGED artifact on native macOS, Linux and Windows against one exact sealed candidate.

Purpose: this is the phase's fan-in. Plans 27-01, 27-02 and 27-03 each produced corpus entries and each proved its own criterion live on the platforms that criterion named. What none of them can do alone is prove that the same bytes produce the same behavior on all three operating systems out of the artifact a user actually receives. Phase 20A drove two platforms to green in continuous integration and nobody ever launched the binary; that gap is the reason this plan's unit of evidence is an extracted archive that was run, not a matrix that was green.
Output: one pinned corpus manifest covering every upstream entry; one corpus-driven suite that runs identically on all three platforms; packaged smokes that extract the release archive exactly as the release workflow builds it and exercise what comes out; one cross-audited acceptance judgement authorizing exactly one dispatch against one exact sealed candidate; and a certification record binding every Success Criterion to a dispatched run, with anything unproved named rather than implied.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/HANDOFF-2026-07-26-phase20-20A-complete.md
@.planning/phases/27-multimodal-browser-generation-voice/27-01-SUMMARY.md
@.planning/phases/27-multimodal-browser-generation-voice/27-02-SUMMARY.md
@.planning/phases/27-multimodal-browser-generation-voice/27-03-SUMMARY.md
@crates/wcore-cli/tests/release_binary_smoke.rs
@crates/wcore-cli/tests/proving_ground.rs
@.github/workflows/release.yml
@.github/workflows/ci.yml
@.gitattributes
</context>

<execution_rules>

**THE TWO AMENDED PHASE RULES — stated verbatim, and they bound this plan.**

- Findings at CRITICAL or HIGH must be fixed or disproved. MEDIUM and below are logged to BACKLOG and DO NOT BLOCK execution.
- Execution begins when no CRITICAL or HIGH finding is open, or after 2 review rounds, whichever comes first. A third round is NOT permitted; it escalates to Sean.

**THE LIVE VERIFICATION MANDATE (highest-priority rule in this plan).** This plan IS the live mandate for the phase. Its unit of evidence is an extracted release archive that was launched on the named platform and whose output was captured. A green continuous-integration matrix is not that. If the matrix is green and no binary was launched, this plan has failed regardless of what the matrix says.

**TERMINATION CRITERION FOR THIS PLAN (hard — the plan STOPS and escalates rather than spawning more work).** This plan pins ONCE, packages ONCE, authorizes ONCE, and dispatches ONCE. It terminates in exactly one of four states, and in all four it writes its SUMMARY and stops:
1. **Certified** — the corpus is pinned, the packaged smokes pass on all three native platforms against one exact sealed candidate from one newly dispatched authorized run, and every Success Criterion is bound to captured evidence. Reached when Task 2's panel adjudicates `authorize-dispatch`.
2. **Certified with named gaps** — the three platform legs ran and some criterion is closed on fewer platforms than it names, or a terminal-rendering invariant has no Windows equivalent. Record exactly which criterion, on which platform, and why. The phase is reported with that gap OPEN. Do not round it up. Reached when Task 2's panel adjudicates `authorize-with-named-gap`.
3. **Blocked on a platform** — a required runner or an artifact for the exact candidate is unavailable. STOP. Record NOT RUN with the reason. Do not substitute another platform's result and do not reuse an earlier run.
4. **Escalated** — Task 2's panel adjudicates `hold-remediate` (a named acceptance criterion is unmet and what must change is written down) or `escalate` (the panel deadlocks two-two with no captured measurement breaking it, or closing the deficiency needs a change outside this plan's scope fence). Task 3 does not run. Ship nothing, record the open decision, and where it is an escalation carry the SPLIT — every member's verbatim position and the measurements that failed to separate them — never a bare question.

Under no circumstances does this plan spawn additional plans, extend its own task list, re-open an upstream plan's work, or start a second dispatch cycle. Findings against upstream work that are CRITICAL or HIGH are reported to Sean; this plan does not repair another plan's surface.

**SCOPE BOUNDARY (hard).** The intake path is 27-01's, readiness is 27-02's, generation and voice are 27-03's — all three have landed. This plan changes NO product source in `crates/wcore-tools`, `crates/wcore-browser`, `crates/wcore-cua`, `crates/wcore-agent`, `crates/wcore-providers`, `crates/wcore-config` or `crates/wcore-protocol`. Its surface is the corpus manifest, its loader, the corpus test crate, the two smoke scripts, the test-runner configuration and the workflow. If certification needs a product change, that is a finding reported to Sean, not a change made here.

**THIS PLAN OWNS THE WORKFLOW AND TEST-RUNNER SEAMS.** `.github/workflows/ci.yml` and `.config/nextest.toml` are edited HERE and nowhere else in this phase. Note the measured hazard: the workflow's cancel-in-progress behavior was previously unconditional and made two branches pushing concurrently cancel each other's runs, which loses coverage silently rather than failing loudly; that has been repaired and must not be reintroduced.

**NON-NEGOTIABLE.** A REPORTED RED IS WORTH FAR MORE THAN AN ENGINEERED GREEN. The specific temptations here are: running the smoke against the build directory instead of the extracted archive because extraction is fiddly; and declaring a criterion closed on three platforms when it only ran on two. Both are forbidden. Extraction is the point, and a platform that did not run says NOT RUN.

**NATIVE EVIDENCE DISCIPLINE (inherited from Phase 20A, not relaxed).** Windows and macOS results are bound to ONE newly dispatched, panel-authorized run against ONE exact sealed candidate, asserted against the real checkout before any build step, with the commit, tree and a per-run nonce recorded on every platform's marker. Evidence is never inferred from source, cross-compilation, the Linux proof, prose, an unavailable runner, or a reused run.

**WHERE THE LINE IS DRAWN BETWEEN JUDGEMENT AND PUBLICATION (read before Task 2).** A gate is an instruction to decide well, not an instruction to stop. Task 2's substance is an ACCEPTANCE JUDGEMENT — does the captured three-platform dry run meet this plan's own acceptance criteria, and is one dispatch of a continuous-integration run on this feature branch warranted against this exact seal. That is architecture and evidence acceptance, so it is adjudicated on the four-way panel and execution continues. Dispatching a workflow run on `plan/f20-unified-audit-repair` is reversible, touches no released surface, and produces evidence rather than shipping anything, so it is inside the panel's authority. What is NOT inside anyone's authority but Sean's, and what this phase therefore performs at no point: merging to main, opening a pull request, tagging, cutting a release, deploying, running a canary, closing a GitHub issue, deleting a retained evidence reference, and supplying real credentials or accounts. Task 2 records that line verbatim in `evidence/27-04/RESERVED.txt` and a gate reads it. The certification is a record, not a publication.

**ENVIRONMENT.**
- Linux (authoritative Cargo proof): `ssh -o BatchMode=yes hetzner-dsm`, `/root/wayland`.
- Windows: `ssh -o BatchMode=yes SeanD@seandesktop` (Tailscale), `C:\ferrox-win`. Invocation shape `ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "<cmd> 2>&1" }'` piped through `grep -v CLIXML | grep -v "^<Objs"`. Two Windows runners exist — `SEANDESKTOP` and a second registered one that sat idle through all of Phase 20A. Give each concurrent agent its own worktree; concurrent compile load on one shared box corrupted a proof run once already. `cargo fmt --all` fails there with os error 206 and the justfile already skips it. Clippy runs BEFORE tests with warnings denied, so a lint failure means tests never execute — that hid dozens of failures for weeks.
- Native macOS: THIS Mac, which is also a registered ephemeral runner that consumes itself per job and must be re-registered per dispatch. No Cargo is run here by hand; the macOS artifact comes from the dispatched run.
- Mac local commands: `/usr/bin/git`, ALWAYS `/usr/bin/grep` — the ambient `grep` silently drops lines. `cargo fmt --all` is the only working cargo command.
- Known-red and non-gating on Windows, already escalated, do NOT rediscover: a sandbox descendant-job-tree reap case; two private-permission snapshot cases failing on a reopen error that reproduces at the parent; and a worker output-exhaustion case that runs long against its budget whose timeout is deliberately NOT raised.
- The macOS proof script has a known follow-up: it pulls a container image unconditionally rather than checking for it first, which caused four failed macOS dispatches. Check before pulling.
- NO push to main, merge, PR, tag, release, deployment, canary, issue closure or evidence-reference deletion — Sean-only, and this phase performs none of them. Dispatching a continuous-integration run on `plan/f20-unified-audit-repair` is NOT on that list: it is reversible, ships nothing, and is authorized by Task 2's panel.

**AGENTS.md discipline.** Scripts use the platform-appropriate shell and never assume a Unix path shape on Windows. Clippy-clean at `-D warnings` on every platform. Surgical diffs.

**NO GATE ENDS IN A PIPE — AND THIS PLAN IS THE ONE THAT CERTIFIES, SO A SELF-PASSING GATE HERE CERTIFIES A LIE.** A remote command piped into `tail` or `grep -v CLIXML` reports the FILTER's exit status, which is 0 no matter what the remote did — measured on this branch: `ssh hetzner-dsm 'exit 9'` returns 9, `ssh hetzner-dsm 'exit 9' | tail -3` returns 0. On Windows, chaining `cmd /c "A"; cmd /c "B"` with a semicolon runs both and reports only B's status. Every remote gate below captures the status first with `rc=$?` and asserts `test "$rc" -eq 0`; Windows chains use `&&` inside ONE `cmd /c` plus `exit $LASTEXITCODE` — measured to short-circuit and to return non-zero on an early failure. Filtering is for reading, after the status is safe. The sibling trap, already found in `scripts/wayland-e2e-windows-soak.ps1` in this repo, is reading an exit code out of a block that also emits output: `$exit = & { cargo ... | Tee-Object ...; $LASTEXITCODE }` returns an ARRAY of every output line plus the code, so `if ($exit -ne 0)` is always true and a 12/12 all-PASS run reported failure. In `f27-packaged-smoke.ps1`, read `$LASTEXITCODE` on the line AFTER the pipeline, never from inside a block that also writes output.

**Git hygiene.** Use `/usr/bin/git` on the Mac. Stage the exact paths in `files_modified`, never `-A`, never `.`. No `Co-Authored-By` trailers.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Pin the corpus and make one corpus-driven suite plus two packaged smokes that run identically on all three platforms</name>
  <files>crates/wcore-fixture-harness/fixtures/f27/MANIFEST.toml, crates/wcore-fixture-harness/src/lib.rs, crates/wcore-cli/tests/f27_media_corpus.rs, scripts/f27-packaged-smoke.sh, scripts/f27-packaged-smoke.ps1, .config/nextest.toml</files>
  <read_first>the three upstream summaries in this phase directory (every corpus entry they created, with the byte length and digest each recorded — those recorded values are what the manifest pins, and a disagreement between a summary and the file on disk is a finding, not a rounding), .github/workflows/release.yml around its packaging steps (the exact archive naming and the exact contents per platform — the smoke must reproduce that packaging, not approximate it), crates/wcore-cli/tests/release_binary_smoke.rs (how the release binary is located and driven, and which capability assertions prove that optimization did not strip registered items — extend this pattern rather than duplicating it), crates/wcore-cli/tests/proving_ground.rs and its support module (the Unix-only terminal drive and the exact reason it is gated at file scope, since that is the gap to close or to name), .gitattributes (the committed normalization rules — these are what keep a corpus byte identical across a Windows checkout, and they were measured to override the ambient end-of-line configuration)</read_first>
  <behavior>
    - Every corpus entry from all three upstream plans is pinned in one manifest by byte length and digest, and the manifest is verified against the files on disk before any entry is used.
    - A corpus entry whose bytes differ from its pin fails loudly with the entry named, on every platform. Silent substitution is impossible.
    - Binary corpus entries survive a Windows checkout unchanged, proved by verifying the manifest on the Windows box rather than by trusting the normalization rules.
    - The corpus suite runs on all three platforms from the same source with no platform-specific expectations, and any case that genuinely cannot run on a platform is skipped by an explicit, named mechanism that REPORTS the skip rather than hiding it.
    - The packaged smokes build the release archive the way the release workflow packages it, extract it to a clean location, and run the EXTRACTED binary — never the build directory.
    - The packaged smoke asserts the artifact starts, reports its version, and publishes a handshake whose media-capability claims match what that machine can actually do, exercising the honest-unavailable path where the machine lacks a backend.
    - The smoke exits non-zero on any hard-gate failure and prints every check with its result, so a partial pass cannot be read as a pass.
    - The test-runner configuration gains the corpus suite in a way that makes a silent non-execution impossible; a suite that runs in no profile is the failure mode this repository has already measured at scale.
  </behavior>
  <action>Start by reconciling the corpus against the three upstream summaries. For every entry each of them recorded, confirm the file exists and that its byte length and digest match what that summary claimed. A disagreement is a finding with a severity, recorded before anything else proceeds — an unpinnable corpus makes every downstream platform comparison meaningless. Write the reconciled set into one manifest under the shared fixtures tree, with each entry's path, byte length, digest and the plan that produced it.

Add the manifest loader to the fixture-harness crate so every consumer verifies before it reads, and make a mismatch fail with the offending entry named. Then write the corpus suite in the command-line crate's test directory: it drives the corpus through the shipped surfaces the upstream plans established — the intake path, the readiness handshake, the generation presentations and the voice readiness case — and it must compile and run on all three platforms from one source. Where a case genuinely cannot run on a platform, use an explicit named mechanism that reports the skip; do not gate it out at file scope, because a file-scope gate is how an entire terminal-drive suite came to compile to nothing on Windows without anyone noticing.

Settle the terminal-rendering question deliberately rather than by omission. Determine whether the media-surface rendering invariants can be driven on Windows by a mechanism that platform supports, implement it if they can, and if they cannot, write down exactly which invariants are therefore proved on two platforms and not three. Naming that gap is a required output; leaving it implicit inside a green matrix is the Phase 20A failure repeating.

Write the two packaged smokes. Each one builds the release binary, packages it into the archive shape the release workflow produces for that platform, extracts it to a clean directory, and then runs the extracted binary — never the build output in place. Each asserts the artifact starts and reports its version, then drives the host-protocol handshake and checks that the media-capability claims match what that machine can do, including the honest-unavailable path where a backend is genuinely absent. Each prints every check with its result and exits non-zero if any hard-gate check failed.

Register the corpus suite in the test-runner configuration so it cannot silently run in no profile. Records evidence for F27-05; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f crates/wcore-fixture-harness/fixtures/f27/MANIFEST.toml &amp;&amp; test -f crates/wcore-cli/tests/f27_media_corpus.rs &amp;&amp; test -x scripts/f27-packaged-smoke.sh &amp;&amp; test -f scripts/f27-packaged-smoke.ps1</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -c 'digest' crates/wcore-fixture-harness/fixtures/f27/MANIFEST.toml)" -ge 16</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -c 'f27_media_corpus' .config/nextest.toml)" -ge 1</automated>
    <automated># NO-PRODUCT-SOURCE GATE, repaired twice over. `wc -l` PADS its output on BSD, so the old `grep -qx '0'` could never match and that gate was GUARANTEED RED rather than a verification; `tr -d ' '` plus a numeric compare fixes it. And because a clean-source check passes against the untouched tree, it is conjoined with this task's completion anchor: the gate now means "the pinned corpus manifest landed AND no product source was touched to land it".
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f crates/wcore-fixture-harness/fixtures/f27/MANIFEST.toml &amp;&amp; test "$(/usr/bin/git status --porcelain --untracked-files=all -- crates/wcore-tools crates/wcore-browser crates/wcore-cua crates/wcore-agent crates/wcore-providers crates/wcore-config crates/wcore-protocol | tee /dev/stderr | wc -l | tr -d ' ')" -eq 0</automated>
    <automated># GATE, NOT A PRINTOUT — the smoke's own non-zero exit on a hard-gate failure now actually reaches this gate instead of being swallowed by `tail`.
SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); LOG=$(mktemp); ssh -o BatchMode=yes hetzner-dsm "cd /root/wayland &amp;&amp; git fetch origin plan/f20-unified-audit-repair &amp;&amp; git checkout --detach $SHA &amp;&amp; cargo clippy --workspace --all-targets --all-features -- -D warnings &amp;&amp; bash scripts/f27-packaged-smoke.sh" &gt; "$LOG" 2&gt;&amp;1; rc=$?; tail -40 "$LOG"; rm -f "$LOG"; test "$rc" -eq 0</automated>
    <automated># GATE, NOT A PRINTOUT — `&amp;&amp;` inside one cmd /c short-circuits so a failed checkout cannot be masked by a suite that then runs against the wrong tree.
SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); LOG=$(mktemp); ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "git fetch origin plan/f20-unified-audit-repair 2>&amp;1 &amp;&amp; git checkout --detach '"$SHA"' 2>&amp;1 &amp;&amp; cargo nextest run -p wcore-cli --test f27_media_corpus --no-fail-fast 2>&amp;1" }; exit $LASTEXITCODE' &gt; "$LOG" 2&gt;&amp;1; rc=$?; /usr/bin/grep -v -e CLIXML -e '^&lt;Objs' "$LOG" &gt; "$LOG.f"; tail -40 "$LOG.f"; rm -f "$LOG" "$LOG.f"; test "$rc" -eq 0</automated>
  </verify>
  <done>Every upstream corpus entry is reconciled against its summary and pinned in one manifest by byte length and digest, with any disagreement recorded as a finding. The loader verifies before every read and names the offending entry on mismatch. The corpus suite compiles and runs on all three platforms from one source, with any unrunnable case skipped by an explicit reporting mechanism rather than a file-scope gate. The Windows terminal-rendering question is settled deliberately: either the invariants are driven on Windows, or the exact invariants proved on only two platforms are written down. Both packaged smokes build the archive as the release workflow packages it, extract it, and run the extracted binary, asserting startup, version and a handshake whose claims match the machine — including the honest-unavailable path. Each smoke prints every check and exits non-zero on any hard-gate failure. The corpus suite is registered in the test-runner configuration. No product source outside this plan's declared surface changed — gate-checked clean.</done>
</task>

<task type="auto">
  <name>Task 2 (CROSS-AUDITED ACCEPTANCE JUDGEMENT): Seal one exact candidate, measure the extracted-binary claim on the real Linux box, put the captured three-platform dry run to the four-way panel, and authorize exactly one dispatch</name>
  <files>.planning/phases/27-multimodal-browser-generation-voice/27-04-CERTIFICATION.md, .planning/phases/27-multimodal-browser-generation-voice/evidence/27-04/</files>
  <read_first>the three upstream summaries in this phase directory (each one's termination state and the per-platform live evidence it recorded — an upstream plan that terminated without proving its live half is a named gap the panel must be shown BEFORE it judges, not a discovery made after dispatch), scripts/f27-packaged-smoke.sh as Task 1 wrote it (specifically the lines that report the extraction directory and the absolute path of the binary it invoked — the provenance measurement below reads exactly those and is worthless if they are absent), .planning/HANDOFF-2026-07-26-phase20-20A-complete.md section 3 (the remote-invocation traps every capture below is written against: the filter that replaces a remote command's exit status, and the Windows chain that reports only the last link)</read_first>
  <behavior>
    - The candidate is sealed BEFORE anything is judged, and the seal is of the working checkout: a seal minted after a verdict is not a seal, and a seal whose tree is not the tree its own commit points at is not a seal either.
    - The question and the four options are put to the panel EXACTLY as this task frames them below. The option set is not re-scoped, extended or narrowed, and the adjudicated id is one of these four.
    - The measurable half of this judgement is MEASURED on real hardware before the panel is asked. This plan's central claim is that the smoke exercised the EXTRACTED archive and not the build directory in place, and that claim is a file-identity fact, not an opinion: the executed binary must be a distinct file from `target/release/`, provable by inode.
    - All four members receive the SAME bundle, and every capture carries that bundle's digest so a bundle edited between members is detectable rather than deniable.
    - Every response is recorded VERBATIM. Each member's recorded pick is EXTRACTED FROM ITS OWN TRANSCRIPT by the gate rather than typed by the executor, so a member's vote cannot be invented, mislabelled, or silently dropped.
    - A panel claim that a captured measurement contradicts LOSES to the measurement. When the winner is a minority position the record says so and cites the capture that outweighed the count.
    - The dissent survives, naming the losing options and the evidence that would have overturned the result.
    - The line between the acceptance judgement (decided here) and the reserved publication acts (never performed by this phase) is written down and gate-checked.
    - Nothing is dispatched by this task, and no product source outside this plan's declared surface is touched.
  </behavior>
  <action>**THIS IS A JUDGEMENT, NOT A HALT.** The question, the acceptance criteria and the evidence are exactly what the blocking form of this task already gathered and are NOT re-scoped. What changes is who adjudicates: this task seals the candidate, measures the one claim in the dry run that is a fact rather than an argument, puts the captured evidence to a four-way panel, commits to a position, records the dissent, and continues into Task 3. It escalates only on a genuine deadlock or on reserved substance.

**WHAT IS DECIDED HERE AND WHAT IS NOT — read this before anything else.** Accepting the dry-run evidence and authorizing ONE dispatch of a continuous-integration run on `plan/f20-unified-audit-repair` is an evidence judgement about a reversible act that produces evidence and ships nothing; it is decided here. Merging to main, opening a pull request, tagging, cutting a release, deploying, running a canary, closing a GitHub issue, deleting a retained evidence reference and supplying real credentials or accounts are irreversible outward acts reserved to Sean, and this phase performs none of them at any point. Write that split verbatim into `evidence/27-04/RESERVED.txt`, one item per line in the shape `RESERVED: &lt;act&gt; owner=sean performed-by-this-phase=no`, plus one line `DECIDED-HERE: dispatch-one-ci-run branch=plan/f20-unified-audit-repair reversible=yes`. A gate reads that file.

**STEP 1 — SEAL, before any judgement exists to bias it.** Capture the current commit and its tree from the working checkout and mint a per-run nonce. Write all three into `27-04-CERTIFICATION.md` AND mirror them verbatim into `evidence/27-04/SEAL.env`, on three dedicated lines in EXACTLY this shape, because this task's seal gate and Task 3's dispatch gate both parse them and neither will find them in prose: `AUTHORIZED-COMMIT: &lt;40-hex&gt;`, `AUTHORIZED-TREE: &lt;40-hex&gt;`, `RUN-NONCE: &lt;value of at least eight characters&gt;`. The seal gate below proves the commit is a real object in this repository, that the tree it actually points at equals the sealed tree, and that the sealed commit is the working checkout's head — typing forty plausible hex characters is therefore not enough.

**STEP 2 — RUN THE THREE-PLATFORM DRY RUN AND MEASURE THE ONE CLAIM THAT IS MEASURABLE.** Four captures under `evidence/27-04/dry-run/`. Every capture BEGINS with these lines in exactly this shape because the gates parse them, and carries the full transcript beneath: `SHA: &lt;the sealed commit&gt;`, `HOST: &lt;hetzner-dsm|seandesktop|this-mac&gt;`, `LEG: &lt;the leg id&gt;`, `CMD: &lt;the single command line that produced it&gt;`, `RC: &lt;the exit status observed&gt;`. Capture the status with `rc=$?` before any filter; a remote command piped into a filter reports the FILTER's status, which is 0 no matter what the box did.

- **`L1-linux-packaged-smoke.txt`, on hetzner-dsm, `LEG: linux-packaged-smoke`.** Run the Unix packaged smoke at the sealed SHA with every check printed and its exit status recorded. Add two further header lines read out of the smoke's own output: `EXTRACT-DIR: &lt;absolute path the archive was extracted to&gt;` and `EXEC-PATH: &lt;absolute path of the binary the smoke actually invoked&gt;`. If the smoke does not report those two, that is a Task 1 defect and a finding with a severity — not something to infer.
- **`L2-linux-binary-provenance.txt`, on hetzner-dsm, `LEG: linux-binary-provenance`. THIS IS THE MEASUREMENT THAT REPLACES AN ARGUMENT.** The load-bearing claim of this entire plan is that what ran was the extracted archive and not `target/release/` in place, and file identity settles that without anyone reasoning about it. In ONE self-contained command line — self-contained because the reproducibility gate re-runs that exact line from a clean checkout — run the smoke, then read the inode and the SHA-256 of the file at `EXEC-PATH` and of the release build output, and record `EXEC-INODE:`, `BUILD-INODE:`, `EXEC-SHA256:`, `BUILD-SHA256:` and a final `PROVENANCE: distinct-file` or `PROVENANCE: same-file`. Identical content is EXPECTED — it is the same build — so content equality proves nothing and inode equality proves everything: the same inode means the smoke ran the build directory and the plan's central claim is REFUTED. A refutation here is a CRITICAL finding and a correct outcome for this task; it is not a reason to soften the measurement, and it forecloses `authorize-dispatch`.
- **`W1-windows-corpus-manifest.txt`, on seandesktop, `LEG: windows-corpus-manifest`.** The corpus suite run and the manifest verification on that platform's own checkout, over the non-interactive session, at the sealed SHA. Chain with `&amp;&amp;` inside ONE `cmd /c` and end with `exit $LASTEXITCODE`; never chain with `;`, which runs both links and reports only the last. Never read `$LASTEXITCODE` from inside a block that also writes output — that returns an array of every output line plus the code, and it already made a 12-of-12 all-pass run in this repository report failure.
- **`M1-macos-artifact-startup.txt`, on this Mac, `LEG: macos-artifact-startup`.** This plan's own environment rules forbid running Cargo by hand on this Mac and state that the macOS artifact comes from the dispatched run — so pre-dispatch there is ordinarily NO macOS artifact for a freshly sealed tree, and that absence is MATERIAL to the judgement rather than an embarrassment to smooth over. Record honestly: either a real capture of the artifact starting and reporting its version, or the single line `F27-MACOS-DRYRUN: NOT RUN — &lt;reason&gt;` beneath the headers. Never fabricate one, never substitute the Linux result, and never reuse an artifact from an earlier tree. The panel is told which, in those words, because "the macOS half of the dry run does not exist yet" is exactly the kind of fact that should shape an acceptance judgement.

**STEP 3 — BUILD ONE BUNDLE.** Write `evidence/27-04/panel/PROMPT.md` containing, in this order: the `&lt;decision&gt;` question verbatim; the four option ids with their names, pros and cons verbatim from the `&lt;options&gt;` element below, with nothing added, removed, softened or re-scoped; this task's five acceptance criteria verbatim from the `&lt;adjudication&gt;` element; the four dry-run captures in full including the provenance verdict and the macOS leg's honest state; the three upstream summaries' termination states and any gap they left unproved; the NAMED GAPS section as it stands, in particular whether the terminal-rendering invariants are proved on three platforms or two; and the clean-source proof for this plan's scope fence. Present the four options in a rotated order rather than this plan's order, and carry no recommendation into the bundle — a label of this plan's own buys four echoes of an opinion already held instead of four independent reads. State plainly in the bundle that the answer must name exactly one of the four ids and must end with one final line in exactly the form `PANEL_POSITION=&lt;option-id&gt;`. Compute the bundle digest with `/usr/bin/shasum -a 256`.

**STEP 4 — PUT IT TO ALL FOUR, SAME BUNDLE, RESPONSES VERBATIM.** Each capture's FIRST line is `BUNDLE-SHA256: &lt;digest&gt;` and everything beneath it is the member's output byte for byte, unedited. A paraphrased panel is an unauditable panel.
- `codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check "$(cat evidence/27-04/panel/PROMPT.md)"` into `codex.txt`
- `gemini -p "$(cat evidence/27-04/panel/PROMPT.md)" -m gemini-3.1-pro-preview -o text --skip-trust` into `gemini.txt`
- `/Users/seandonahoe/.kimi-code/bin/kimi -p "$(cat evidence/27-04/panel/PROMPT.md)" --output-format text` into `kimi.txt`
- one internal adversarial pass into `internal.txt`, written as the strongest available argument AGAINST whichever option you walked in believing, engaging at least two rival ids by name. A fourth voice that ratifies the emerging consensus is not a panel member, it is a rubber stamp, and it makes the count meaningless.

THREE VOTE-LOSS TRAPS, MEASURED ON THESE CLIs TODAY RATHER THAN READ FROM DOCUMENTATION — each one silently drops a member and yields a "four-way panel" that is really three-way or worse, which is the same defect class as a self-passing gate. `gemini` REQUIRES `--skip-trust`; without it, it refuses and returns nothing at all. `kimi` INDENTS its verdict line, so an anchored `^PANEL_POSITION=` matches zero times and its vote vanishes. `codex` both ECHOES THE PROMPT and repeats its final block: the literal template `PANEL_POSITION=&lt;option-id&gt;` appears in its transcript before any real answer, so first-match extraction returns the placeholder as a fabricated vote. Extraction is therefore UNANCHORED, takes the LAST match, and is validated against this plan's own option set — the shape measured to recover all four real votes where the naive anchored-first form recovered one. The gate below performs that extraction itself; the executor does not type the picks. Gemini's `GOOGLE_API_KEY` notice on stderr is a notice, not a failure, and is never grounds for recording that member NOT-RUN. If a CLI genuinely fails to answer, record it NOT-RUN with its exact command and exact stderr: three real voices with one honestly recorded absence is worth more than four where one was invented.

**STEP 5 — ADJUDICATE, AND LET THE MEASUREMENT BREAK TIES.** Write `evidence/27-04/panel/DECISION.tsv` with exactly five rows and no other content but comments: `PANEL::codex::&lt;option-id&gt;::capture=codex.txt` and the same for `gemini`, `kimi` and `internal`; then one `CHOSEN::&lt;option-id&gt;::&lt;majority|minority-on-evidence&gt;::rationale=RATIONALE.md::dissent=DISSENT.md`. `majority` means three or four of the four recorded picks are the chosen id. `minority-on-evidence` means two or fewer, and it is allowed and sometimes correct — but then `RATIONALE.md` must carry a `LIVE-CAPTURE:` line naming the dry-run capture that outweighed the count, because "I found the minority more persuasive" without a measurement is a preference. A two-two split that no captured measurement separates is a DEADLOCK, not a decision: `RATIONALE.md` must then carry the literal `EVIDENTIARY-TIEBREAK:` naming the capture that broke it, or the chosen id is `escalate`. `DISSENT.md` is at least three lines and names at least two losing option ids with the strongest argument made for each and the evidence that would have overturned the result — a decision with the losing arguments deleted is not auditable.

**STEP 6 — RECORD AND PROCEED.** Copy the four verbatim responses, the vote rows, the adjudicated id, the rationale, the dissent and the accepted cost into `27-04-CERTIFICATION.md` alongside the dry-run captures, the upstream termination states, the NAMED GAPS section and the clean-source proof. Then act on the id: `authorize-dispatch` and `authorize-with-named-gap` both continue into Task 3 in the same session with no wait state — under `authorize-with-named-gap` the gap is written into the NAMED GAPS section BEFORE the dispatch fires and the plan terminates in state 2. `hold-remediate` and `escalate` mean Task 3 does not run: write the SUMMARY recording termination state 4 and stop, and where it is an escalation hand Sean the SPLIT — every member's verbatim position, the measurements that failed to separate them, and what evidence would — never a bare question. Under no id does this task dispatch anything, and under no id does this phase merge, open a pull request, tag, release, deploy, canary or close an issue.</action>
  <decision>Does the captured three-platform dry run against this exact sealed candidate meet this plan's acceptance criteria well enough to authorize exactly one dispatch of the native three-platform certification run?</decision>
  <context>
Phase 20A drove Windows and macOS acceptance targets to green in continuous integration and nobody ever launched the binary. This plan exists so that does not repeat, and this task is the moment the phase decides whether the evidence in hand is the kind that would have caught it. Two things make the judgement non-obvious rather than a formality. First, the dispatch is single-shot by this plan's own termination criterion — one pin, one package, one authorization, one dispatch — so a dispatch fired against insufficient evidence is not cheaply retried, and the ephemeral macOS runner consumes itself per job. Second, the macOS leg of the dry run is ordinarily ABSENT pre-dispatch, because this plan's environment rules forbid running Cargo by hand on this Mac and state that the macOS artifact comes from the dispatched run — so the judgement is genuinely being made with one of three legs unproved, and whether that is acceptable is precisely what is being adjudicated rather than assumed. Read the provenance measurement before choosing: if the executed binary shares an inode with the build output, the plan's central claim is refuted and `authorize-dispatch` is off the table no matter how anyone argues.
  </context>
  <options>
    <option id="authorize-dispatch">
      <name>Authorize one dispatch against this exact seal, with no gap outstanding</name>
      <pros>The evidence in hand satisfies every acceptance criterion: the seal matches the working checkout, the Linux packaged smoke passed with every check printed, the provenance measurement proves a distinct extracted file rather than the build directory, the Windows corpus suite and manifest verification passed on that platform's own checkout, and nothing material is unproved. Dispatching now converts the remaining unknowns into captured native evidence in one run, which is the only way this phase reaches termination state 1.</pros>
      <cons>Requires reading the macOS leg's ordinary pre-dispatch absence as immaterial, which is defensible only because the dispatched run is itself what produces that artifact — and reading it wrongly spends the single authorized dispatch on a candidate that a fourth capture would have disqualified.</cons>
    </option>
    <option id="authorize-with-named-gap">
      <name>Authorize one dispatch against this exact seal AND record a named gap that the dispatch will not close</name>
      <pros>Honest about the shape of the evidence rather than rounding it up. It fits the case this plan already anticipates in termination state 2 — a criterion closed on fewer platforms than it names, most likely the terminal-rendering invariants that the existing harness cannot drive on Windows, or an upstream plan that terminated without proving its live half. The dispatch still produces every native result it can, and the gap ships visible in the record instead of absorbed into a green matrix.</pros>
      <cons>The phase closes at termination state 2 rather than state 1, so a Success Criterion is reported open and a later plan inherits it. It also risks becoming the comfortable default — naming a gap is cheaper than closing one, and a gap named without a reason and a closing condition is just a softer rounding-up.</cons>
    </option>
    <option id="hold-remediate">
      <name>Hold: a named acceptance criterion is unmet and what must change is inside this plan's own declared surface</name>
      <pros>The right answer when the dry run exposes a defect in this plan's own work — the smoke reporting no execution path, the provenance measurement returning `same-file`, the manifest failing to verify on the Windows checkout, or the corpus suite silently running in no profile. Spending the single dispatch on a candidate with a known defect wastes the one shot and certifies something the certification's own gates would later contradict.</pros>
      <cons>Task 3 does not run and the phase terminates without native evidence, so every Success Criterion this plan was to close stays open and the work of closing them moves to a later plan. What must change has to be written down precisely, or the hold is just a delay.</cons>
    </option>
    <option id="escalate">
      <name>Escalate carrying the split: the deficiency cannot be closed inside this plan's scope fence, or the panel deadlocks with no measurement to break it</name>
      <pros>The honest answer when closing the gap needs a change to product source that this plan's scope fence forbids, or when two positions are tied and no captured measurement separates them. It keeps the decision visible and hands over the split rather than a bare question, so the next reader inherits the evidence and not the confusion.</pros>
      <cons>It is the slowest outcome and the easiest to reach for when a judgement is merely uncomfortable. A two-one-one that nobody wants to adjudicate is not a deadlock, and a fixable defect inside this plan's own surface is `hold-remediate`, not this.</cons>
    </option>
  </options>
  <adjudication>The adjudicated id is one of exactly: authorize-dispatch, authorize-with-named-gap, hold-remediate, escalate. The five acceptance criteria the panel judges the captured evidence against, reproduced verbatim into the bundle: (1) the seal is of the working checkout and its tree is the tree its own commit points at; (2) the Linux packaged smoke ran with every check printed and its exit status recorded, and the provenance measurement shows `PROVENANCE: distinct-file` with the executed inode differing from the build output's; (3) the Windows corpus suite and manifest verification ran on that platform's own checkout at the sealed SHA, with its exit status captured before any filter; (4) the macOS leg is either captured or recorded `NOT RUN` with its reason, and never inferred, substituted or reused; (5) every upstream plan's termination state is present and any criterion closed on fewer platforms than it names is already in the NAMED GAPS section with its reason. A `PROVENANCE: same-file` measurement forecloses `authorize-dispatch` outright — that binding is enforced by a gate below, not by good intentions.</adjudication>
  <verify>
    <automated># SEAL GATE. Reads the mirrored seal artifact, not prose. Typing forty plausible hex characters is insufficient three ways over: the sealed commit must be a real object in this repository, the tree that commit ACTUALLY points at must equal the sealed tree, and the seal must be of the working checkout's head — a seal of some other tree is not a seal of what the dispatch will build. Red at base: no seal exists.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/27-multimodal-browser-generation-voice/evidence/27-04 &amp;&amp; test -s "$EV/SEAL.env" &amp;&amp; C=$(/usr/bin/sed -n 's/^AUTHORIZED-COMMIT: \([0-9a-f]\{40\}\)$/\1/p' "$EV/SEAL.env" | /usr/bin/head -1) &amp;&amp; T=$(/usr/bin/sed -n 's/^AUTHORIZED-TREE: \([0-9a-f]\{40\}\)$/\1/p' "$EV/SEAL.env" | /usr/bin/head -1) &amp;&amp; N=$(/usr/bin/sed -n 's/^RUN-NONCE: \(.\{8,\}\)$/\1/p' "$EV/SEAL.env" | /usr/bin/head -1) &amp;&amp; test -n "$C" &amp;&amp; test -n "$T" &amp;&amp; test -n "$N" &amp;&amp; /usr/bin/git cat-file -e "$C^{commit}" &amp;&amp; test "$(/usr/bin/git rev-parse "$C^{tree}")" = "$T" &amp;&amp; test "$(/usr/bin/git rev-parse HEAD)" = "$C"</automated>
    <automated># DRY-RUN CAPTURE GATE. Four legs, each carrying the sealed SHA, its host, its leg id, the exact command that produced it and the status observed — and at least eight lines, because a one-line assertion is a claim and not a capture. The macOS leg is deliberately allowed to be an honest NOT RUN, since this plan's own rules forbid building it by hand here; what is NOT allowed is silence. Red at base: no captures exist.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/27-multimodal-browser-generation-voice/evidence/27-04/dry-run &amp;&amp; S=$(/usr/bin/sed -n 's/^AUTHORIZED-COMMIT: \([0-9a-f]\{40\}\)$/\1/p' "$EV/../SEAL.env" | /usr/bin/head -1) &amp;&amp; test -n "$S" &amp;&amp; for L in L1-linux-packaged-smoke L2-linux-binary-provenance W1-windows-corpus-manifest M1-macos-artifact-startup; do F="$EV/$L.txt"; test -s "$F" || exit 1; test "$(wc -l &lt; "$F" | tr -d ' ')" -ge 8 || exit 1; /usr/bin/grep -qE '^HOST: (hetzner-dsm|seandesktop|this-mac)$' "$F" || exit 1; /usr/bin/grep -qE '^LEG: [a-z0-9-]+$' "$F" || exit 1; /usr/bin/grep -qE '^CMD: .+' "$F" || exit 1; /usr/bin/grep -qE '^RC: [0-9]+$' "$F" || exit 1; /usr/bin/grep -qF "$S" "$F" || exit 1; done &amp;&amp; { /usr/bin/grep -qE '^VERSION: .+' "$EV/M1-macos-artifact-startup.txt" || /usr/bin/grep -qE '^F27-MACOS-DRYRUN: NOT RUN — .{10,}$' "$EV/M1-macos-artifact-startup.txt"; }</automated>
    <automated># PROVENANCE DISCRIMINATOR GATE — the leg that makes this a measurement rather than a committee. The plan's central claim is that the EXTRACTED archive ran, not the build directory in place, and file identity settles it: identical CONTENT is expected because it is the same build, so only a differing INODE proves a distinct file. The Linux smoke must have passed, its executed path must sit under the extraction directory with no target component, and the two inodes must differ. `same-file` is a legitimate recorded outcome — it REFUTES the claim — but it then forecloses authorize-dispatch, which the trailing leg enforces against the ledger.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/27-multimodal-browser-generation-voice/evidence/27-04 &amp;&amp; L1="$EV/dry-run/L1-linux-packaged-smoke.txt" &amp;&amp; L2="$EV/dry-run/L2-linux-binary-provenance.txt" &amp;&amp; test -s "$L1" &amp;&amp; test -s "$L2" &amp;&amp; /usr/bin/grep -qxF 'RC: 0' "$L1" &amp;&amp; XD=$(/usr/bin/sed -n 's/^EXTRACT-DIR: //p' "$L1" | /usr/bin/head -1) &amp;&amp; XP=$(/usr/bin/sed -n 's/^EXEC-PATH: //p' "$L1" | /usr/bin/head -1) &amp;&amp; test -n "$XD" &amp;&amp; test -n "$XP" &amp;&amp; case "$XP" in "$XD"/*) : ;; *) exit 1 ;; esac &amp;&amp; case "$XP" in */target/*) exit 1 ;; esac &amp;&amp; EI=$(/usr/bin/sed -n 's/^EXEC-INODE: //p' "$L2" | /usr/bin/head -1) &amp;&amp; BI=$(/usr/bin/sed -n 's/^BUILD-INODE: //p' "$L2" | /usr/bin/head -1) &amp;&amp; test -n "$EI" &amp;&amp; test -n "$BI" &amp;&amp; /usr/bin/grep -qE '^PROVENANCE: (distinct-file|same-file)$' "$L2" &amp;&amp; { { /usr/bin/grep -qxF 'PROVENANCE: distinct-file' "$L2" &amp;&amp; test "$EI" != "$BI"; } || { /usr/bin/grep -qxF 'PROVENANCE: same-file' "$L2" &amp;&amp; test "$EI" = "$BI" &amp;&amp; ! /usr/bin/grep -qE '^CHOSEN::authorize-dispatch::' "$EV/panel/DECISION.tsv"; }; }</automated>
    <automated># PANEL-CAPTURE AND VOTE-EXTRACTION GATE. Four real captures, each carrying the SHA-256 of the bundle computed HERE from PROMPT.md, so a bundle edited between members goes red; four DISTINCT capture digests, so pasting one model's answer into another's slot goes red. The extraction is the part measured on the real CLIs today rather than read from documentation: kimi INDENTS its verdict so an anchored regex drops its vote entirely, and codex ECHOES the prompt template so first-match extraction returns the literal placeholder as a fabricated vote — the naive anchored-first form recovered ONE of three real votes. This gate therefore extracts UNANCHORED, takes the LAST match, validates it against this plan's own option set, and requires the ledger row to agree with the member's OWN transcript. The executor does not type the picks. The substance floor is BYTES, not lines, for a fourth measured reason: gemini answered a real question substantively in SIX lines, so a line floor would have dropped its vote too — a self-inflicted vote loss on top of the three the CLIs cause.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/27-multimodal-browser-generation-voice/evidence/27-04/panel &amp;&amp; OPT='authorize-with-named-gap|authorize-dispatch|hold-remediate|escalate' &amp;&amp; test -s "$EV/PROMPT.md" &amp;&amp; test -s "$EV/DECISION.tsv" &amp;&amp; B=$(/usr/bin/shasum -a 256 "$EV/PROMPT.md" | /usr/bin/awk '{print $1}') &amp;&amp; for M in codex gemini kimi internal; do F="$EV/$M.txt"; test -s "$F" || exit 1; test "$(wc -c &lt; "$F" | tr -d ' ')" -ge 400 || exit 1; /usr/bin/grep -qF "$B" "$F" || exit 1; V=$(/usr/bin/grep -oE 'PANEL_POSITION=[a-z][a-z-]*' "$F" | /usr/bin/tail -1 | /usr/bin/sed 's/.*=//'); echo "$V" | /usr/bin/grep -qxE "$OPT" || exit 1; /usr/bin/grep -qxF "PANEL::$M::$V::capture=$M.txt" "$EV/DECISION.tsv" || exit 1; done &amp;&amp; test "$(/usr/bin/shasum -a 256 "$EV/codex.txt" "$EV/gemini.txt" "$EV/kimi.txt" "$EV/internal.txt" | /usr/bin/awk '{print $1}' | sort -u | wc -l | tr -d ' ')" -eq 4</automated>
    <automated># ADJUDICATION-ARITHMETIC GATE. The option set is encoded HERE, not read out of the record, so an id invented outside this plan's own four is red however the record is worded. Exactly four vote rows and exactly one commitment row; `majority` must survive counting three or four of the four; `minority-on-evidence` must be two or fewer AND cite a live capture, because preferring the minority without a measurement is a preference; a top-two tie must carry EVIDENTIARY-TIEBREAK: or resolve to escalate, since a two-two split nobody measured apart is a deadlock and not a decision.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/27-multimodal-browser-generation-voice/evidence/27-04/panel &amp;&amp; D="$EV/DECISION.tsv" &amp;&amp; OPT='authorize-with-named-gap|authorize-dispatch|hold-remediate|escalate' &amp;&amp; test -s "$D" &amp;&amp; test "$(/usr/bin/grep -cE "^PANEL::(codex|gemini|kimi|internal)::($OPT)::capture=[^[:space:]]+$" "$D")" -eq 4 &amp;&amp; test "$(/usr/bin/grep -cE "^CHOSEN::($OPT)::(majority|minority-on-evidence)::rationale=[^[:space:]]+::dissent=[^[:space:]]+$" "$D")" -eq 1 &amp;&amp; CH=$(/usr/bin/sed -n 's/^CHOSEN::\([a-z-]*\)::.*/\1/p' "$D") &amp;&amp; BA=$(/usr/bin/sed -n 's/^CHOSEN::[a-z-]*::\([a-z-]*\)::.*/\1/p' "$D") &amp;&amp; RAT=$(/usr/bin/sed -n 's/^CHOSEN::.*::rationale=\([^:]*\)::dissent=.*/\1/p' "$D") &amp;&amp; test -s "$EV/$RAT" &amp;&amp; N=$(/usr/bin/grep -cE "^PANEL::[a-z]+::$CH::" "$D") &amp;&amp; case "$BA" in majority) test "$N" -ge 3 ;; minority-on-evidence) test "$N" -le 2 &amp;&amp; /usr/bin/grep -qE '^LIVE-CAPTURE: .+' "$EV/$RAT" ;; *) exit 1 ;; esac &amp;&amp; T1=$(/usr/bin/sed -n 's/^PANEL::[a-z]*::\([a-z-]*\)::.*/\1/p' "$D" | sort | uniq -c | sort -rn | /usr/bin/awk 'NR==1{print $1}') &amp;&amp; T2=$(/usr/bin/sed -n 's/^PANEL::[a-z]*::\([a-z-]*\)::.*/\1/p' "$D" | sort | uniq -c | sort -rn | /usr/bin/awk 'NR==2{print $1}') &amp;&amp; { test "$T1" -gt "${T2:-0}" || /usr/bin/grep -qE '^EVIDENTIARY-TIEBREAK: .+' "$EV/$RAT" || test "$CH" = escalate; }</automated>
    <automated># DISSENT-SURVIVAL GATE. A decision with the losing arguments deleted is not auditable, and deletion is the easy failure here. The dissent file the commitment row NAMES must exist, run to at least three lines, and name at least two option ids that are NOT the winner — so restating the winner at length does not satisfy it.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/27-multimodal-browser-generation-voice/evidence/27-04/panel &amp;&amp; D="$EV/DECISION.tsv" &amp;&amp; test -s "$D" &amp;&amp; CH=$(/usr/bin/sed -n 's/^CHOSEN::\([a-z-]*\)::.*/\1/p' "$D") &amp;&amp; DIS=$(/usr/bin/sed -n 's/^CHOSEN::.*::dissent=\(.*\)$/\1/p' "$D") &amp;&amp; test -n "$CH" &amp;&amp; test -s "$EV/$DIS" &amp;&amp; test "$(wc -l &lt; "$EV/$DIS" | tr -d ' ')" -ge 3 &amp;&amp; DN=0 &amp;&amp; for O in authorize-dispatch authorize-with-named-gap hold-remediate escalate; do test "$O" = "$CH" &amp;&amp; continue; /usr/bin/grep -qF "$O" "$EV/$DIS" &amp;&amp; DN=$((DN+1)); done; test "${DN:-0}" -ge 2</automated>
    <automated># RESERVED-LINE AND ORDERING GATE — EVALUATE AT TASK 2 ONLY. Leg one: the split between what this panel may decide and what stays Sean's is written down explicitly, with the reversible dispatch declared decided-here and every irreversible outward act declared reserved and not performed by this phase. Leg two is the ordering anchor that makes leg one more than a typed file: the adjudication must exist BEFORE Task 3 builds the dispatch machinery, and `f27-packaged-smoke` appears ZERO times in the workflow today (measured), so a judgement recorded after the dispatch was wired is a rationalization rather than a judgement. Task 3 lands that job and flips leg two red by design.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/27-multimodal-browser-generation-voice/evidence/27-04 &amp;&amp; test -s "$EV/panel/DECISION.tsv" &amp;&amp; test -s "$EV/RESERVED.txt" &amp;&amp; test "$(/usr/bin/grep -cE '^RESERVED: [a-z-]+ owner=sean performed-by-this-phase=no$' "$EV/RESERVED.txt")" -ge 6 &amp;&amp; for A in merge-to-main open-pull-request tag release close-issue delete-evidence-ref; do /usr/bin/grep -qxE "RESERVED: $A owner=sean performed-by-this-phase=no" "$EV/RESERVED.txt" || exit 1; done &amp;&amp; /usr/bin/grep -qxF 'DECIDED-HERE: dispatch-one-ci-run branch=plan/f20-unified-audit-repair reversible=yes' "$EV/RESERVED.txt" &amp;&amp; test "$(/usr/bin/grep -c 'f27-packaged-smoke' .github/workflows/ci.yml)" -eq 0</automated>
    <automated># PROVENANCE REPRODUCIBILITY GATE, on the real Linux box — the leg that stops the discriminator from being a number someone typed. It reads L2's own CMD and RC and re-runs that exact command at the sealed SHA from a clean checkout, then requires the fresh status to EQUAL the recorded one. It asserts equality, not success, because a `same-file` refutation is a legitimate and informative outcome and demanding green would corrupt the measurement this task exists to take. The status is captured with `rc=$?` before any filter; there is no pipe on the status line, because a pipe hands the status to the filter and `ssh host 'exit 9' | tail -3` returns 0.
SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); F=/Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/27-multimodal-browser-generation-voice/evidence/27-04/dry-run/L2-linux-binary-provenance.txt; CMD=$(/usr/bin/awk '/^CMD: /{sub(/^CMD: /,""); print; exit}' "$F"); REC=$(/usr/bin/awk '/^RC: /{print $2; exit}' "$F"); LOG=$(mktemp); ssh -o BatchMode=yes hetzner-dsm "cd /root/wayland &amp;&amp; git fetch origin plan/f20-unified-audit-repair &amp;&amp; git checkout --detach $SHA &amp;&amp; $CMD" &gt; "$LOG" 2&gt;&amp;1; rc=$?; tail -30 "$LOG"; rm -f "$LOG"; test -n "$CMD" &amp;&amp; test -n "$REC" &amp;&amp; test "$rc" -eq "$REC"</automated>
  </verify>
  <done>One exact candidate is sealed BEFORE any judgement exists, mirrored into `evidence/27-04/SEAL.env`, and gate-proved to be a real commit whose actual tree equals the sealed tree and whose identity is the working checkout's head. The three-platform dry run is captured in four files, each naming the sealed SHA, its host, its leg, the exact command and the status observed — with the macOS leg either captured or honestly recorded NOT RUN with its reason and never inferred, substituted or reused. The plan's central claim was MEASURED rather than argued: the executed binary's path sits under the extraction directory with no target component, and its inode differs from the release build output's, gate-checked and re-measured on the box by re-running the provenance command at the sealed SHA. A `same-file` result is recorded as the refutation it is and forecloses authorize-dispatch. One identical bundle went to codex, gemini, kimi and an internal adversarial pass; each capture carries that bundle's digest, all four are distinct, and each member's pick was EXTRACTED FROM ITS OWN TRANSCRIPT by the gate — unanchored, last match, validated against this plan's four ids — so no vote could be invented, mislabelled or silently dropped. Exactly one id is adjudicated from this plan's own option set, its basis survives arithmetic against the four captured picks, a minority win cites the live capture that outweighed the count, and a two-two split with nothing to separate it resolves to escalate or carries EVIDENTIARY-TIEBREAK:. The dissent runs to at least three lines and names at least two losing options. The split between the acceptance judgement decided here and the irreversible publication acts reserved to Sean is written down and gate-checked, and the judgement is proved to predate the dispatch machinery. Nothing was dispatched by this task and no product source outside this plan's surface changed. Under authorize-dispatch or authorize-with-named-gap, Task 3 runs in the same session with no wait state; under hold-remediate or escalate, Task 3 does not run and the SUMMARY records termination state 4 carrying the split.</done>
</task>

<task type="auto">
  <name>Task 3: Dispatch once against the authorized candidate, launch the packaged artifact on all three native platforms, and certify with every gap named</name>
  <files>.github/workflows/ci.yml, .planning/phases/27-multimodal-browser-generation-voice/27-04-CERTIFICATION.md</files>
  <read_first>.planning/phases/27-multimodal-browser-generation-voice/evidence/27-04/SEAL.env and evidence/27-04/panel/DECISION.tsv (the sealed tuple and the adjudicated id — dispatch against that tuple and no other, and only when the id is authorize-dispatch or authorize-with-named-gap), the existing native-proof scripts and the nightly Windows workflow in this repository (the marker convention, the candidate-mode gating and the runner labels that Phase 20A used, so this run reuses a proven dispatch shape instead of inventing one; note the container-image pull that fires unconditionally and cost four failed macOS dispatches), the three upstream summaries (each criterion's per-platform live evidence, so the certification cross-references rather than re-derives it)</read_first>
  <behavior>
    - Exactly ONE run is dispatched, against the exact commit and tree Task 2's panel authorized, and every platform job asserts the authorized commit against its real checkout BEFORE any build step.
    - Every platform's marker records the same commit, the same tree and the same per-run nonce, so no two legs can be from different candidates.
    - Each platform leg extracts the release archive and runs the EXTRACTED binary; a leg that ran the build directory instead is not evidence and is recorded as such.
    - Each leg captures the artifact starting, its version, its handshake, and the corpus suite's per-case results, and each leg's full output is retained.
    - A leg that cannot run states NOT RUN with its reason. No leg's result is inferred from another platform, from cross-compilation, from source, or from an earlier run.
    - The certification binds each of the five Success Criteria to the exercise that proved it, the platform it ran on, and the captured artifact, drawing on the upstream summaries for the criteria they proved.
    - Any criterion closed on fewer platforms than it names is stated as a NAMED GAP with the platform and the reason, and the phase is reported with that gap open.
    - Requirements are marked complete only for what the captured evidence actually supports.
  </behavior>
  <action>Add the workflow job that runs the corpus suite and the packaged smoke on the three native platforms, reusing the marker convention, candidate-mode gating and runner labels the previous native proof already established rather than inventing a second shape. Have every platform job assert the authorized commit against its real checkout before any build step, and emit one marker per platform carrying the commit, the tree and a shared per-run nonce. Preserve the repaired concurrency behavior; do not reintroduce unconditional cancel-in-progress, which silently loses coverage when two branches push at once.

Then dispatch ONCE, against the exact tuple Task 2's panel authorized and nothing else — and only when the adjudicated id was authorize-dispatch or authorize-with-named-gap; under hold-remediate or escalate this task does not run at all. Dispatching a run on this feature branch is the whole of what was authorized: nothing here merges, opens a pull request, tags, releases, deploys, canaries or closes an issue, all of which remain Sean's. Re-register the ephemeral macOS runner first, since it consumes itself per job, and check for the container image before pulling it — an unconditional pull already cost four failed macOS dispatches.

For each of the three platforms, capture: the asserted commit, tree and nonce; the archive that was built and its name; proof that the binary run was the EXTRACTED one and not the build directory; the artifact's startup and version output; the handshake with its media-capability claims and whether they matched what that machine could do; and the corpus suite's per-case results with every skip reported and named. Retain every leg's full output.

Then write the certification. For each of the five Success Criteria, name the exercise that proved it, the platform it ran on, and where the captured evidence lives — drawing on the upstream summaries for the criteria those plans proved live and on this run for the packaged three-platform half. Where a criterion is closed on fewer platforms than it names, or where an upstream plan terminated without proving its live half, write it in a NAMED GAPS section with the platform, the reason and what would close it. Do not round a two-platform result up to three, and do not let a green matrix stand in for a launched binary.

Mark complete only the requirements the captured evidence supports. If F27-05 is proved on three platforms against the authorized candidate, mark it complete; mark the upstream requirements complete only where their own summaries plus this run together carry the evidence, and leave any that do not explicitly OPEN with the reason. This plan alone in this phase may update requirement state.</action>
  <verify>
    <automated># CERTIFICATION-MATRIX GATE. It replaces a grep for the words PASS/FAIL/NOT RUN in this plan's OWN certification, which twelve sentences of prose satisfied — the certifying plan is the last place a self-satisfied gate belongs. Every Success Criterion now carries a per-platform row that NAMES a captured artifact this gate stats. Row shape: `F27-0N::&lt;linux|windows|macos&gt;::&lt;PASS|FAIL|NOT-RUN|GAP&gt;::evidence=&lt;path relative to evidence/27-04&gt;`. A PASS or FAIL row's artifact must hold at least five lines of real capture (a leg's marker, startup, version, handshake and per-case results); a NOT-RUN or GAP row's artifact must carry the machine-parseable marker `F27-0N-&lt;PLATFORM&gt;: NOT RUN — &lt;reason&gt;` or `... GAP — &lt;reason&gt;`, so an unrun leg is named rather than rounded up. Fifteen cells is five criteria times three platforms.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/27-multimodal-browser-generation-voice/evidence/27-04 &amp;&amp; test -s "$EV/CERT-MATRIX.tsv" &amp;&amp; test "$(/usr/bin/grep -cE '^F27-0[1-5]::(linux|windows|macos)::(PASS|FAIL|NOT-RUN|GAP)::evidence=[^[:space:]]+$' "$EV/CERT-MATRIX.tsv")" -ge 15 &amp;&amp; while IFS= read -r R; do case "$R" in ""|"#"*) continue;; esac; F="$EV/${R##*::evidence=}"; test -s "$F" || exit 1; case "$R" in *::PASS::*|*::FAIL::*) test "$(wc -l &lt; "$F" | tr -d ' ')" -ge 5 || exit 1;; *) /usr/bin/grep -qE '^F27-0[1-5]-[A-Z]+: (NOT RUN|GAP) .+' "$F" || exit 1;; esac; done &lt; "$EV/CERT-MATRIX.tsv"</automated>
    <automated># NAMED-GAPS RECONCILIATION GATE. It replaces a grep for the heading `NAMED GAPS` in this plan's own certification, which typing that heading satisfied while the section beneath it stayed empty. This instead RECONCILES: every matrix cell that is not a PASS must appear as a named gap keyed `F27-0N::&lt;platform&gt;` in the gaps ledger, so a NOT RUN or GAP leg cannot be recorded without being named. An all-green matrix is satisfied by the single ledger line `NONE::all-cells-pass`. Rounding two platforms up to three now requires forging a capture, not omitting a paragraph.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/27-multimodal-browser-generation-voice/evidence/27-04 &amp;&amp; test -s "$EV/CERT-MATRIX.tsv" &amp;&amp; test -s "$EV/NAMED-GAPS.tsv" &amp;&amp; while IFS= read -r R; do case "$R" in ""|"#"*|*::PASS::*) continue;; esac; K="${R%%::evidence=*}"; K="${K%::*}"; /usr/bin/grep -qF "$K" "$EV/NAMED-GAPS.tsv" || exit 1; done &lt; "$EV/CERT-MATRIX.tsv"</automated>
    <automated># CRITERION-COVERAGE GATE. It replaces a grep for the five requirement IDs in this plan's own certification, which five mentions in prose satisfied — including five mentions inside one sentence. Each of the five must now hold at least one matrix cell backed by a stat-able artifact, so a criterion cannot be certified by being named.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/27-multimodal-browser-generation-voice/evidence/27-04 &amp;&amp; test -s "$EV/CERT-MATRIX.tsv" &amp;&amp; for C in F27-01 F27-02 F27-03 F27-04 F27-05; do test "$(/usr/bin/grep -cE "^$C::(linux|windows|macos)::(PASS|FAIL|NOT-RUN|GAP)::evidence=[^[:space:]]+$" "$EV/CERT-MATRIX.tsv")" -ge 1 || exit 1; done</automated>
    <automated># COMPLETION + PRESERVATION, CONJOINED ON ONE LINE ON PURPOSE. Leg 2 is the concurrency preservation guard: `ci.yml:39` reads `cancel-in-progress: ${{ github.event_name == 'pull_request' }}` TODAY, so on its own line it passed against the untouched tree and proved nothing about this task — it is a regression guard, not coverage. Leg 1 is the completion gate: this task adds the three-platform job, and `f27-packaged-smoke` appears ZERO times in ci.yml today (measured). Conjoined, the gate goes green only once the job lands, and goes red if landing it reverts the conditional to an unconditional `cancel-in-progress: true`, which silently loses coverage when two branches push at once.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -c 'f27-packaged-smoke' .github/workflows/ci.yml)" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cE "cancel-in-progress:.*event_name == 'pull_request'" .github/workflows/ci.yml)" -eq 1</automated>
    <automated># NO-PRODUCT-SOURCE GATE, repaired twice over. `wc -l` PADS its output on BSD, so the old `grep -qx '0'` could never match and that gate was GUARANTEED RED rather than a verification; `tr -d ' '` plus a numeric compare fixes it. And because a clean-source check passes against the untouched tree, it is conjoined with this task's completion anchor: the gate now means "the three-platform job landed in the workflow AND no product source was touched to land it".
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -c 'f27-packaged-smoke' .github/workflows/ci.yml)" -ge 1 &amp;&amp; test "$(/usr/bin/git status --porcelain --untracked-files=all -- crates/wcore-tools crates/wcore-browser crates/wcore-cua crates/wcore-agent crates/wcore-providers crates/wcore-config crates/wcore-protocol | tee /dev/stderr | wc -l | tr -d ' ')" -eq 0</automated>
    <automated># DISPATCH GATE, now bound to a captured seal artifact instead of to prose in this plan's own certification. The old form read the seal out of the document the executor writes, so forty typed hex characters were the whole input. Task 2 mirrors its three sealed lines verbatim into `evidence/27-04/SEAL.env` (`AUTHORIZED-COMMIT:`, `AUTHORIZED-TREE:`, `RUN-NONCE:`) and this gate reads THAT, then makes typing insufficient three ways over: the sealed commit must be a real commit object in this repository, the tree it actually points at must equal the sealed tree, and a GitHub run must exist whose head SHA is that commit. It deliberately does NOT assert conclusion==success — a FAIL or NOT RUN leg is a legitimate recorded outcome here, and forcing green would invite exactly the engineered green this phase forbids.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/27-multimodal-browser-generation-voice/evidence/27-04 &amp;&amp; test -s "$EV/SEAL.env" &amp;&amp; SEAL=$(/usr/bin/sed -n 's/^AUTHORIZED-COMMIT: \([0-9a-f]\{40\}\)$/\1/p' "$EV/SEAL.env" | head -1) &amp;&amp; TREE=$(/usr/bin/sed -n 's/^AUTHORIZED-TREE: \([0-9a-f]\{40\}\)$/\1/p' "$EV/SEAL.env" | head -1) &amp;&amp; test -n "$SEAL" &amp;&amp; test -n "$TREE" &amp;&amp; /usr/bin/git cat-file -e "$SEAL^{commit}" &amp;&amp; test "$(/usr/bin/git rev-parse "$SEAL^{tree}")" = "$TREE" &amp;&amp; RUNS=$(gh run list --branch plan/f20-unified-audit-repair --limit 20 --json databaseId,conclusion,headSha) &amp;&amp; test "$(printf '%s' "$RUNS" | /usr/bin/grep -c "$SEAL")" -ge 1</automated>
  </verify>
  <done>Exactly one run was dispatched against the exact commit and tree Task 2's panel authorized, every platform job asserted that commit against its real checkout before building, and all three markers carry the same commit, tree and per-run nonce. Each leg extracted the release archive and ran the extracted binary, with proof of that in the capture, and each leg's startup, version, handshake and per-case corpus results are retained with every skip reported. A leg that could not run states NOT RUN with its reason and nothing was substituted for it. The certification binds all five Success Criteria to their exercise, platform and captured artifact, and every criterion closed on fewer platforms than it names — including any upstream plan that terminated without proving its live half — appears in a NAMED GAPS section with the reason and what would close it. The workflow's repaired concurrency behavior is intact — gate-checked. No product source outside this plan's surface changed. Requirements are marked complete only where the captured evidence supports it, and anything unproved is explicitly OPEN.</done>
</task>

</tasks>

## What this plan does NOT change (scope fence)

- **No product source.** `crates/wcore-tools`, `crates/wcore-browser`, `crates/wcore-cua`, `crates/wcore-agent`, `crates/wcore-providers`, `crates/wcore-config` and `crates/wcore-protocol` are gate-checked untouched. A certification failure that needs a product change is a finding reported to Sean, not a change made here.
- **No upstream plan is reopened.** 27-01, 27-02 and 27-03 have landed and their summaries are evidence, not a work queue.
- **No second dispatch.** One run against one authorized tuple. A failed leg is recorded, not retried into a different candidate.
- **No inferred native evidence.** Cross-compilation, the Linux result, source presence, prose, an unavailable runner and a reused earlier run are all non-evidence for a native platform.
- **No build-directory smoke.** The unit of evidence is the extracted archive. Running `target/release` in place does not test what a user receives.
- **No rounding up.** A criterion proved on two platforms is reported as two, in the named-gap section, with the reason.
- **The repaired workflow concurrency behavior is not reverted.** Unconditional cancel-in-progress silently loses coverage when two branches push concurrently. The gate asserts the conditional survives ON ONE LINE (`ci.yml:39`), so a revert to `cancel-in-progress: true` goes red — the earlier two-separate-greps form was satisfied by any two lines anywhere in the file.
- **No gate ends in a pipe, and no gate asserts a threshold the measured baseline already satisfies.** On Windows, no gate chains commands with `;`; on PowerShell, `$LASTEXITCODE` is never read from inside a block that also emits output. This plan certifies the phase — a gate here that cannot go red certifies a lie, which is the exact Phase 20A failure this phase exists to not repeat.
- **No push to main, merge, PR, tag, release, deployment, canary, issue closure or evidence-reference deletion.** All Sean-only, recorded as such in `evidence/27-04/RESERVED.txt` and gate-checked. Task 2's panel authorizes ONE continuous-integration dispatch on this feature branch and nothing beyond it; a judgement about evidence is not a licence to publish.
- **No test is deleted, weakened, re-gated, `#[ignore]`d or `#[allow]`ed**, and no timeout is raised to reach a green.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| certification ← candidate identity | Everything the certification claims is claimed about one exact commit and tree; a leg from a different candidate makes the whole record false |
| user ← shipped archive | The archive is what a user receives; anything proved about a build directory is proved about something the user never runs |
| platform result ← platform run | A Windows or macOS claim is only as good as a run that actually happened on that operating system |
| corpus input ← checkout normalization | A corpus byte rewritten by a platform's checkout rules silently changes the input every comparison depends on |
| coverage ← workflow concurrency | A run cancelled by a concurrent push loses coverage without failing, so absence of a red is not evidence of a green |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-04-01 | Spoofing | A platform leg runs against a different commit or tree than the authorized candidate, so the certification binds evidence to the wrong artifact | high | mitigate | Every platform job asserts the authorized commit against its real checkout BEFORE any build step; all three markers carry the same commit, tree and per-run nonce; a mismatch invalidates the leg rather than being reconciled after the fact |
| T-27-04-02 | Repudiation | The smoke runs the build directory instead of the extracted archive, so the certified behavior is not the shipped behavior | high | mitigate | The smokes package the archive as the release workflow does, extract to a clean location, and run only what came out; each leg's capture must carry proof of which binary ran; the release-profile stripping regression this repository already suffered is the reason this distinction is load-bearing |
| T-27-04-03 | Spoofing | A criterion proved on two platforms is reported as proved on three, so a gap ships as a certification | high | mitigate | The named-gap section is mandatory and gate-checked present; a leg that did not run states NOT RUN; the terminal-rendering platform question must be settled explicitly rather than left implicit in a matrix |
| T-27-04-04 | Tampering | A corpus entry is rewritten by a platform's checkout normalization, silently changing every comparison's input | high | mitigate | Every entry is pinned by byte length and digest and verified before use, with the offending entry named on mismatch; the verification runs on the Windows box specifically rather than trusting the committed normalization rules |
| T-27-04-05 | Denial of Service | A run cancelled by a concurrent push loses coverage without failing, so an absent red reads as a green | high | mitigate | The repaired conditional concurrency behavior is gate-checked intact; reintroducing an unconditional cancel is explicitly forbidden by this plan's scope fence |
| T-27-04-06 | Repudiation | A test that runs in no profile is counted as coverage, repeating a failure this repository has already measured at scale | high | mitigate | The corpus suite is registered in the test-runner configuration and the registration is gate-checked; skips are reported by an explicit named mechanism rather than gated out at file scope |
| T-27-04-07 | Elevation of Privilege | The dispatch fires without an exact-tuple authorization, or an irreversible outward act is performed under cover of a judgement that never covered it | high | mitigate | Task 2's panel authorizes exactly one dispatch bound to a sealed tuple that the seal gate proves is a real commit whose actual tree matches and whose identity is the working head; the reserved-line gate requires merge, pull request, tag, release, issue closure and evidence-reference deletion to be recorded owner=sean and performed-by-this-phase=no; an adjudicated hold-remediate or escalate means Task 3 does not run |
| T-27-04-11 | Spoofing | A panel member's vote is silently dropped or fabricated by the extraction, so a "four-way audit" is really three-way or one-way — the same defect class as a self-passing gate: a verification that appears to run and does not | high | mitigate | Measured on the real CLIs rather than read from documentation: gemini refuses without `--skip-trust`, kimi indents its verdict so an anchored regex drops it, and codex echoes the prompt template so first-match extraction returns the literal placeholder as a fabricated vote. The gate performs the extraction itself — unanchored, last match, validated against this plan's own option set — and requires every ledger row to agree with that member's OWN transcript; four distinct capture digests make pasting one answer into another's slot red |
| T-27-04-08 | Denial of Service | The ephemeral macOS runner is not re-registered, or an unconditional image pull fails, wasting the single authorized dispatch | medium | mitigate | Re-registration precedes the dispatch and the image is checked before pulling — the unconditional pull already cost four failed macOS dispatches and is named here so it is not rediscovered |
| T-27-04-09 | Denial of Service | Concurrent compile load on one shared Windows box corrupts a proof run | medium | mitigate | Each concurrent agent gets its own worktree per host, and a second registered Windows runner exists and should carry load rather than sitting idle as it did through the previous phase |
| T-27-04-10 | Repudiation | A requirement is marked complete on evidence that does not support it, so the phase reports a closure it did not earn | high | mitigate | Requirements are marked only where the captured evidence supports them; anything unproved is left explicitly OPEN with its reason; this plan alone in the phase may update requirement state |
| T-27-04-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency is added, removed or updated; `Cargo.toml` and `Cargo.lock` are untouched; the packaged smokes build from the existing lockfile and add nothing |
</threat_model>

<verification>
Local gates (Mac, source level only — no Cargo here): `cargo fmt --all -- --check` clean; the manifest, the corpus test crate and both smoke scripts exist and the Unix one is executable; the manifest carries a digest per entry across all three upstream corpora; the corpus suite is registered in the test-runner configuration; the workflow's conditional concurrency behavior is intact; no product source outside this plan's surface changed.

Authoritative gates (real hardware). Hetzner Linux: clippy clean at `-D warnings`; the packaged smoke builds, extracts and exercises the extracted artifact with every check printed and a non-zero exit on any hard-gate failure. Windows on SeanDesktop: the corpus suite runs and the manifest verifies on that platform's own checkout — clippy first, since a lint failure there means tests never execute. Native macOS: the artifact from the dispatched run starts and reports its version on this Mac.

Acceptance gate (Task 2, cross-audited rather than blocking): the seal is a real commit whose actual tree matches and whose identity is the working head; four dry-run captures each name the sealed SHA, host, leg, command and status; the extracted-binary claim is measured by inode rather than argued, and re-measured on the box; one identical bundle went to all four panel members with each pick extracted from that member's own transcript; the adjudicated id survives arithmetic; the dissent survives; and the reserved publication acts are recorded owner=sean and performed-by-this-phase=no.

Dispatch gate: exactly one panel-authorized run against the exact commit and tree, every platform job asserting that commit against its real checkout before building, all three markers carrying the same commit, tree and nonce, and every leg's full output retained.

Known unknowns to record, not to resolve here: whether a signed and notarized macOS artifact behaves differently from an unsigned one on a machine with stricter gatekeeping, which this phase does not produce and Phase 29 owns; whether the second Windows runner's environment differs from the primary in a way that changes a result, since it has never carried this workload; and whether any media surface behaves differently on an operating-system version other than the ones these three machines run.
</verification>

<success_criteria>
- Every corpus entry from all three upstream plans is pinned by byte length and digest in one manifest, verified before use, and verified on the Windows box specifically rather than trusted to normalization rules.
- One corpus-driven suite compiles and runs on all three platforms from one source, with every skip reported by an explicit named mechanism and none gated out at file scope.
- The Windows terminal-rendering question is settled deliberately: either driven, or written down as an invariant proved on two platforms and not three.
- Both packaged smokes build the archive as the release workflow packages it, extract it, and exercise the EXTRACTED binary — proved in each leg's capture — asserting startup, version and a handshake whose claims match the machine including the honest-unavailable path.
- One exact candidate is sealed before any judgement exists, its local three-platform dry run is captured with every check printed, the extracted-binary claim is measured by inode rather than argued, the four-way panel adjudicates one id from this plan's own option set with each pick extracted from that member's own transcript, the dissent survives, and exactly one dispatch fires against that sealed tuple.
- The line between the acceptance judgement decided on the panel and the irreversible outward acts reserved to Sean — merge, pull request, tag, release, deploy, canary, issue closure, evidence-reference deletion, real credentials — is written down, gate-checked, and none of the reserved acts is performed by this phase.
- All three platform markers carry the same commit, tree and per-run nonce, and every job asserted the authorized commit against its real checkout before building.
- The certification binds all five Success Criteria to their exercise, platform and captured artifact, drawing on the upstream summaries for the criteria they proved live.
- Every criterion closed on fewer platforms than it names, and every upstream plan that terminated without proving its live half, appears in a NAMED GAPS section with the reason and what would close it.
- Requirements are marked complete only where captured evidence supports them; anything unproved is explicitly OPEN.
- No product source outside this plan's surface changed, the workflow's repaired concurrency behavior is intact, and nothing was pushed, merged, tagged, released or closed.
</success_criteria>

## Artifacts this plan produces
- `crates/wcore-fixture-harness/fixtures/f27/MANIFEST.toml` — the pinned corpus with a digest and byte length per entry and the plan that produced it.
- `crates/wcore-cli/tests/f27_media_corpus.rs` — the three-platform corpus suite with reported skips.
- `scripts/f27-packaged-smoke.sh` and `scripts/f27-packaged-smoke.ps1` — the extract-and-launch packaged smokes.
- `.planning/phases/27-multimodal-browser-generation-voice/27-04-CERTIFICATION.md` — the seal, the authorization, the three-platform captures, the criterion-to-evidence binding and the NAMED GAPS section.
- `.planning/phases/27-multimodal-browser-generation-voice/evidence/27-04/` — `SEAL.env`; `RESERVED.txt`; `dry-run/` carrying the four dry-run captures including the inode-level provenance measurement; `panel/` carrying `PROMPT.md`, the four verbatim member captures, `DECISION.tsv`, `RATIONALE.md` and `DISSENT.md`; and the per-platform certification captures `CERT-MATRIX.tsv` and `NAMED-GAPS.tsv` from Task 3.
- `27-04-SUMMARY.md` recording the certification, the requirement state and the termination state.

<output>
Create `.planning/phases/27-multimodal-browser-generation-voice/27-04-SUMMARY.md` using the standard GSD summary template. Record: the corpus reconciliation against the three upstream summaries with any disagreement named; the manifest's entry count and how verification fails on mismatch; the corpus suite's platform coverage and every reported skip; the Windows terminal-rendering determination; both packaged smokes with proof each ran the extracted binary rather than the build directory; the sealed candidate's commit, tree and nonce; the local dry-run captures with every check printed; the inode-level provenance measurement and its verdict; the four verbatim panel responses with the bundle digest each carried, the four extracted picks, the adjudicated id with its basis, the rationale and the dissent; the recorded split between the judgement decided on the panel and the acts reserved to Sean; the single dispatched run's identifier and each platform leg's marker, startup, version, handshake and per-case corpus results; any leg recorded NOT RUN with its reason; the criterion-to-evidence binding for all five Success Criteria; the NAMED GAPS section in full, including any upstream plan that terminated without proving its live half; the exact requirement state changes with the evidence for each and every requirement left OPEN with its reason; the recorded unknowns; and which of the four termination states the plan ended in. This is the only plan in Phase 27 that may update requirement state.
</output>

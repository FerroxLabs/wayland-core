---
phase: 28-native-cross-platform-certification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/28-native-cross-platform-certification/28-01-CERTIFICATION-CONTRACT.md
  - .planning/phases/28-native-cross-platform-certification/28-01-CANDIDATE-LEDGER.md
  - .planning/scripts/f28-ledger.py
  - .planning/scripts/f28-resolve-candidate.py
  - crates/wcore-eval-scenarios/src/e5_matrix.rs
  - crates/wcore-eval-scenarios/src/lib.rs
  - crates/wcore-eval-scenarios/tests/e5_matrix_contract.rs
autonomous: true
requirements:
  - F28-01
  - F28-04
must_haves:
  truths:
    - "PHASE 28 CERTIFIES AN ARTIFACT THAT DOES NOT EXIST YET, AND THAT IS THE CENTRAL DESIGN CONSTRAINT, NOT AN INCONVENIENCE. Phases 24, 25, 26 and 27 are executing in parallel lanes while this plan set is being written. Phase 28 certifies THE EXACT CANDIDATE they produce. Therefore this phase plans a HARNESS, a MATRIX and an EVIDENCE CONTRACT that are PARAMETERIZED OVER THE CANDIDATE — never a list of specific 24-27 features. Any plan in this phase that names a 24-27 surface is planning against a guess, and a guess is what a certification exists to eliminate."
    - "THE SURFACE INVENTORY IS READ OFF THE CANDIDATE BINARY, NOT OFF THE PLANNING DOCUMENTS. This is the mechanism that makes the parameterization real rather than aspirational. The shipped `wayland-core` binary self-describes its command tree; that description reflects what ACTUALLY LANDED, regardless of what a phase verdict claims. The 24-27 phase artifacts are used for exactly two secondary purposes: attributing a discovered surface to the phase that produced it, and detecting a surface that a phase CLAIMED but the binary does not expose. A surface claimed and absent is a finding. A surface present and unclaimed is also a finding. Neither is a reason to skip certification of what is there."
    - "THE ACCEPTANCE RULE IS ALREADY DECIDED AND THIS PLAN DOES NOT REOPEN IT. ROADMAP Criterion 4 says 'Zero findings remain at every severity before acceptance'; AGENTS.md says CRITICAL/HIGH are fixed or disproved and MEDIUM and below go to BACKLOG non-blocking. The conflict was put to the four-way panel at PLANNING time, before any plan was written, because it determines the shape of the whole set. Outcome 4-0 for `c4-disposition` with three binding amendments. The full record is in `28-01-decision-evidence/`. This plan ENCODES that decision as executable rules. It does not re-litigate it and no later plan in this phase may soften it."
    - "AMENDMENT A2 IS THE DECISION, AND IF IT IS DROPPED THE CERTIFICATION IS WORTH MATERIALLY LESS THAN ITS OWN TEXT CLAIMS. Severity is RE-SCORED against Phase 28's own four Success Criteria; no finding enters the ledger at a severity inherited from an earlier phase. A finding that CONTRADICTS a Phase 28 Success Criterion is CRITICAL or HIGH by construction and can only be FIXED or DISPROVED — never ACCEPTED, never DEFERRED. Without A2 the four dispositions degrade into paperwork over an unchanged candidate, which is precisely the objection the panel's dissent records. Two findings this program carries TODAY at sub-HIGH cross that line immediately and are named in the contract."
    - "A CERTIFICATION'S SKIPS ARE ITS MOST DANGEROUS SURFACE, WHICH IS WHY THE SKIP TAXONOMY IS BUILT BEFORE ANY CASE RUNS. Criterion 1 says 'no skipped critical case' and F28-04 repeats it. A skip invented mid-run, under time pressure, to route around an inconvenient red is how a matrix reports green while proving nothing. Four skip classes exist, each with its own required evidence, and NO OTHER SKIP IS LEGAL. A critical cell has no legal skip at all. The generator must refuse to emit a matrix containing an unclassified skip rather than warn about one."
    - "WHETHER APPCONTAINER IS OBSERVABLE OVER SSH IS AN OPEN QUESTION THIS PHASE MEASURES, NOT A FACT IT INHERITS. The program has carried a belief that a session-0 SSH logon reports the AppContainer probe unavailable regardless of whether the sandbox is correct, and therefore that Windows sandbox reds from an SSH run are artifacts. THAT BELIEF IS UNDER QUESTION: a concurrent investigation reports the opposite — that the sandbox does work over SSH once stale leases are cleared, and that the false negative came from a LEASE WEDGE rather than from the logon session. Its finding lands at `.planning/intel/APPCONTAINER-SSH-LEASE-WEDGE.md`. NO PLAN IN THIS PHASE MAY DEPEND ON THE OLD BELIEF BEING TRUE. If it is false, then 'unobservable over SSH' has been operating as a blanket excuse to discount Windows sandbox reds, which means it may have been CONCEALING REAL FAILURES — and a certification that inherited it would bake that concealment into its own acceptance gate. That is precisely the failure Phase 28 exists to prevent."
    - "A GREEN ON A SANDBOX CELL REQUIRES POSITIVE EVIDENCE THAT THE SANDBOX WAS ACTIVE. ABSENCE OF A FAILURE IS NOT EVIDENCE OF A SANDBOX. This is the structural answer to the silent-disable defect and it does not depend on how the observability question resolves. Windows can currently run with the sandbox silently disabled on an AppContainer ACL lease SID or profile mismatch; a cell that merely fails to observe a violation would then report green over no sandbox at all. Every sandbox-dimension cell therefore carries a mandatory activeness assertion, and a cell that cannot produce positive activeness evidence is a RED — never a green, and never a skip. Given the lease-wedge report, the silent-disable defect and the observability question may be two faces of one underlying defect, and the matrix must be able to distinguish them rather than assume either."
    - "THE OBSERVATION-BLOCKED SKIP CLASS MAY NEVER BE JUSTIFIED BY INHERITED LORE. It requires a control MEASURED IN THE CERTIFICATION ENVIRONMENT, at run time, showing the channel is broken INDEPENDENTLY OF THE PRODUCT — a control that fails when the channel is sound. Absent such a control the cell is a red, not a skip. A skip class whose evidence requirement can be met by citing a handoff document is not a skip class, it is a laundering channel, and this one is the highest-value laundering channel in the whole matrix because it sits directly on the security dimension."
    - "THE E5 MATRIX IS GENERATED, NOT WRITTEN. Nine dimensions come from F28-01 verbatim and are FIXED. Three OS families are fixed. The surfaces are resolved from the candidate. Cells are the cross product, filtered by declared applicability. Writing cells by hand is how a dimension silently loses a platform: the generator's contract tests assert that every dimension appears on every OS family or carries an explicit applicability record with a reason, so a missing cell fails the build instead of passing unnoticed."
    - "A REPORTED RED IS WORTH FAR MORE THAN AN ENGINEERED GREEN. Never weaken an assertion, `#[ignore]`, `#[allow]`, re-gate, delete a test, or raise a timeout to reach a gate. The specific forgery this plan is exposed to is writing a permissive skip class — one whose evidence requirement can be satisfied by prose — so that a later plan can route any inconvenient cell through it. Each class's evidence requirement is machine-checked for exactly that reason."
  artifacts:
    - path: .planning/phases/28-native-cross-platform-certification/28-01-CERTIFICATION-CONTRACT.md
      provides: "The acceptance rule as adopted by the panel with its three amendments, the severity rubric with the re-scoring rule, the four-class skip taxonomy with each class's required evidence, the named mandatory cells, and the carried-forward known-red ledger with every entry re-scored against Phase 28's own criteria"
    - path: .planning/phases/28-native-cross-platform-certification/28-01-CANDIDATE-LEDGER.md
      provides: "The candidate as resolved AT EXECUTION TIME — exact commit, tree, per-target binary digests, the surface inventory read off the binary, the per-phase attribution for 24 through 27, and every claimed-but-absent and present-but-unclaimed surface recorded as a finding"
    - path: .planning/scripts/f28-ledger.py
      provides: "The finding-ledger schema and its validator: four terminal dispositions, the accept-forbidden rule at CRITICAL/HIGH, the A1 re-scoring requirement and the A2 criterion-contradiction rule, all machine-enforced with a self-test"
    - path: .planning/scripts/f28-resolve-candidate.py
      provides: "The candidate resolver — reads the shipped binary's self-description and the 24-27 phase artifacts, emits candidate.json, and fails closed when a phase has no verdict artifact or a binary digest cannot be bound"
    - path: crates/wcore-eval-scenarios/src/e5_matrix.rs
      provides: "The E5 matrix as DATA — nine fixed F28-01 dimensions crossed with three OS families and the resolved surfaces, each cell carrying id, dimension, os, surface, criticality and applicability, with unclassified skips rejected at construction"
    - path: crates/wcore-eval-scenarios/tests/e5_matrix_contract.rs
      provides: "The generator's contract: a dimension missing from an OS family fails, an unclassified skip fails, a critical cell carrying any skip fails, and the two named mandatory cells are present and critical"
  key_links:
    - from: .planning/phases/28-native-cross-platform-certification/28-01-decision-evidence/decision-rationale.txt
      to: .planning/scripts/f28-ledger.py
      via: "the four terminal dispositions and amendments A1/A2/A3, encoded as executable validation rather than restated as prose"
      pattern: "decision-to-gate"
    - from: .planning/scripts/f28-resolve-candidate.py
      to: crates/wcore-eval-scenarios/src/e5_matrix.rs
      via: "the resolved surface inventory, which is the only input that varies with what phases 24-27 actually landed"
      pattern: "parameterization-seam"
---

<objective>
Build the contract Phase 28 certifies against — the acceptance rule, the severity rubric, the skip taxonomy, the candidate resolver and the E5 matrix generator — so that plans 02, 03 and 04 execute against WHATEVER phases 24 through 27 actually landed rather than against a guess made while they were still running.

Purpose: this phase certifies an exact candidate that does not exist yet. Four sibling phases are executing in parallel lanes right now, and their surfaces are unknown at planning time. A certification whose matrix was written against predicted features would certify the prediction. This plan therefore builds the parameterized machinery — surfaces read off the candidate binary, cells generated by crossing fixed dimensions with resolved surfaces — and it settles, in advance and in writing, the two things a matrix run under time pressure always gets wrong: what a legal skip is, and what acceptance means.
Output: a certification contract encoding the already-decided acceptance rule and its three binding amendments; a resolver that produces the candidate ledger at execution time and fails closed rather than guessing; and an E5 matrix generator whose contract tests make a missing dimension, an unclassified skip or a skipped critical cell a build failure.
</objective>

<execution_context>
@$HOME/.claude/ferrox-core/workflows/execute-plan.md
@$HOME/.claude/ferrox-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/HANDOFF-2026-07-27-parallel-execution.md
@.planning/HANDOFF-2026-07-26-autonomous-execution.md
@.planning/HANDOFF-2026-07-26-phase20-20A-complete.md
@.planning/phases/28-native-cross-platform-certification/28-01-decision-evidence/decision-rationale.txt
@.planning/phases/28-native-cross-platform-certification/28-01-decision-evidence/decision-dissent.txt
@.planning/phases/28-native-cross-platform-certification/28-01-decision-evidence/panel-internal.txt
@.planning/intel/APPCONTAINER-SSH-LEASE-WEDGE.md
@.planning/phases/20A-native-windows-macos-uat/20A-04-SUMMARY.md
@scripts/f20-native-uat-proof.mjs
@crates/wcore-eval-scenarios/src/lib.rs
@crates/wcore-eval-scenarios/src/receipt.rs
@crates/wcore-eval-scenarios/src/process_tree.rs
@crates/wcore-eval-scenarios/src/tempenv.rs
</context>

<execution_rules>

**THE TWO AMENDED PHASE RULES — stated verbatim, and they bound this plan.**

- Findings at CRITICAL or HIGH must be fixed or disproved. MEDIUM and below are logged to BACKLOG and do not block execution.
- Execution begins when no CRITICAL or HIGH finding is open, or after 2 review rounds, whichever comes first. A third round is not permitted; it escalates to Sean.

**NON-NEGOTIABLE, stated verbatim.** A REPORTED RED IS WORTH FAR MORE THAN AN ENGINEERED GREEN — never weaken an assertion, `#[ignore]`, `#[allow]`, re-gate, delete a test, or raise a timeout to reach a gate.

**DO NOT INHERIT THE APPCONTAINER-OVER-SSH LORE. MEASURE IT.** The belief that AppContainer cannot be observed over SSH — that a session-0 logon reports it unavailable regardless of correctness, making Windows sandbox reds from an SSH run artifacts — is UNDER QUESTION and may be false. A concurrent investigation reports the false negative came from a stale-lease wedge, not the logon session, and that the sandbox works over SSH once leases are cleared; it lands at `.planning/intel/APPCONTAINER-SSH-LEASE-WEDGE.md`. Read that file if it exists; if it does not exist yet, that is fine and changes nothing here, because this plan is required to depend on NEITHER answer. Write no rule, no skip class instance and no gate whose passing condition is "sandbox reds from SSH are artifacts". Plan 02 establishes the answer by control before any sandbox cell is graded. Note the stakes if the old belief is false: it will have been functioning as a blanket excuse to discount Windows sandbox reds, and may have concealed real failures.

**THE ACCEPTANCE RULE IS SETTLED. THIS PLAN ENCODES IT AND DOES NOT REOPEN IT.** The conflict between ROADMAP Criterion 4 and the standing severity policy was decided at planning time by the four-way panel, 4-0 for `c4-disposition` with three binding amendments. Read `28-01-decision-evidence/decision-rationale.txt` and `decision-dissent.txt` FIRST. Do not re-run the panel, do not re-argue the options, and do not soften A1, A2 or A3. If you believe the decision is wrong, record that in your SUMMARY as a finding and proceed under the decision as written — a plan that quietly implements a different rule than the one recorded is the exact failure the dissent warns about.

**PARAMETERIZED OVER THE CANDIDATE — THIS IS THE PHASE'S DEFINING CONSTRAINT.** Phases 24, 25, 26 and 27 were executing in parallel lanes when this plan was written and their surfaces are UNKNOWN here. Nothing in this phase may name a specific 24-27 feature, endpoint, command or config key. Where a surface is needed, the plan names the RESOLVER that produces it at execution time. If you find yourself typing the name of a gateway verb, a remote-node operation, a migration command or a media modality, stop: you are planning against a guess.

**THE SURFACE INVENTORY COMES OFF THE BINARY.** Resolve surfaces by interrogating the shipped `wayland-core` binary's own command tree, not by reading phase documents. Documents record intent; the binary records what landed. Use the 24-27 artifacts only to attribute a discovered surface to a phase and to detect the two mismatch classes — claimed-but-absent and present-but-unclaimed — each of which is a finding, and neither of which is a reason to skip certifying what is actually there.

**WHAT IS ALREADY BUILT AND MUST BE REUSED RATHER THAN REINVENTED.** `scripts/f20-native-uat-proof.mjs` already carries the marker-verification discipline this phase scales: exact-byte log reading, per-target markers bound to commit AND tree AND nonce, declared ordering, and fail-closed on any absent, duplicate, reordered or foreign marker. `crates/wcore-eval-scenarios/src/receipt.rs` already carries a versioned content-addressed evidence receipt with a detached signature and an externally configured trusted key. `process_tree.rs` already owns process-tree ownership on all three families. `redaction.rs` already owns exact-secret redaction. `tempenv.rs` already owns hermetic fixtures. This plan extends that lineage; it does not start a second one.

**ENVIRONMENT.**
- Linux build and test: `ssh hetzner-dsm`. Use the worktree dedicated to this phase, `/root/wayland-p28` — never shared `/root/wayland`; concurrent detach on one tree has already corrupted a real proof run. `cargo` is NOT on a non-login shell's PATH; use `/root/.cargo/bin/cargo` or export it. A bare `cargo` exiting 127 is PATH, not a build failure. Build TARGETED (`-p wcore-eval-scenarios`), never a bare full-workspace build. Check `df -h /root` before a large run.
- A connection timeout on hetzner is LOAD TO BACK OFF FROM, not a dead host. Sleep and retry; never escalate concurrency.
- NO CARGO ON THE MAC beyond `cargo fmt --all -- --check`. That one is enforced by CI and fails on Windows with os error 206, so it runs here.
- Mac `grep` and `ls` are rtk-proxied and SILENTLY DROP LINES — measured 32 returned against 674 on one file. Always `/usr/bin/grep`, always `-F` for literals, `/usr/bin/git` for git.
- The Mac repo's remote `origin` is a STALE LOCAL WORKTREE; the real remote is `gh`. Never fetch or reset against `origin` here.

**TERMINATION CRITERION FOR THIS PLAN (hard — the plan STOPS rather than spawning more work).** This plan authors ONE contract, ONE resolver and ONE generator. It terminates in exactly one of four states, and in all four it writes its SUMMARY and stops:
1. **Complete.** Contract, resolver and generator exist, the resolver produced a candidate ledger, and the generator's contract tests pass on Linux. Write the SUMMARY and stop.
2. **Complete with stated exceptions.** As above, but the resolver could not bind one or more per-target binary digests, or one or more of phases 24-27 has no verdict artifact. Record exactly which, and what the consequence is for the matrix — an unbindable target means its OS family's cells cannot be certified, which is a stated limitation, not a silent skip. This is a PASSING state for the plan and an HONEST one for the phase.
3. **Terminated — the candidate is not resolvable.** No commit satisfies the resolver's fail-closed conditions on any host. STOP, record precisely what was missing, and escalate. Do NOT invent a candidate, do NOT fall back to `HEAD`, and do NOT proceed to plan 02 with an unresolved candidate.
4. **Escalated — the generator cannot be made to work.** Bounded at TWO repair iterations, where an iteration is one edit-build-run cycle on real hardware. After the second, STOP, record every remaining failure with its output, and escalate.
Under no circumstances does this plan run a matrix cell, run a soak, sign a receipt, modify any production file outside `crates/wcore-eval-scenarios`, modify or delete an existing test, create a fifth Phase 28 plan, or re-open the acceptance decision.

**THE PHASE PLAN CAP.** Phase 28 is capped at FOUR plans, total. This is the first. It may not create a fifth, may not propose one, and may not extend its own task list. Work it surfaces that does not fit is recorded in the SUMMARY as OUT-OF-PHASE with a severity and routed to BACKLOG or to Sean.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the certification contract and the finding-ledger validator — the acceptance rule, the severity rubric, the four-class skip taxonomy, and the carried-forward reds re-scored</name>
  <files>.planning/phases/28-native-cross-platform-certification/28-01-CERTIFICATION-CONTRACT.md, .planning/scripts/f28-ledger.py, .planning/phases/28-native-cross-platform-certification/evidence/28-01/known-red.tsv</files>
  <read_first>.planning/phases/28-native-cross-platform-certification/28-01-decision-evidence/decision-rationale.txt (the adopted rule and the three binding amendments — read this before writing a line), 28-01-decision-evidence/decision-dissent.txt (what the decision costs and the single check that would reverse it), 28-01-decision-evidence/panel-internal.txt (why A2 exists and what it prevents), AGENTS.md section 11 (the standing severity policy, quoted rather than paraphrased), .planning/ROADMAP.md Phase 28 (the four Success Criteria, quoted verbatim), .planning/REQUIREMENTS.md F28-01 through F28-04, .planning/HANDOFF-2026-07-26-autonomous-execution.md section 6 (the known-red, non-gating list this contract must carry forward and re-score), .planning/HANDOFF-2026-07-26-phase20-20A-complete.md section 4 (the same list with its root causes and why each was left open)</read_first>
  <behavior>
    - The ledger validator rejects a finding whose severity is CRITICAL or HIGH and whose disposition is ACCEPTED or DEFERRED, with a distinct failure code from every other rejection.
    - The ledger validator rejects a finding with no disposition at all — this is the gate Criterion 4 actually is.
    - The ledger validator rejects an ACCEPTED or DEFERRED finding missing any of rationale, owner, or backlog id.
    - The ledger validator rejects a finding carrying only an inherited severity and no Phase 28 re-score (amendment A1).
    - The ledger validator rejects an ACCEPTED or DEFERRED finding whose contradicted-criterion field is non-empty (amendment A2), regardless of the severity recorded on it.
    - The ledger validator rejects a FIXED finding with no executable-check reference and a DISPROVED finding with no counter-evidence reference.
    - The validator's self-test exercises every rejection above with a fixture that trips it and a fixture that does not, so a rule that stopped working is visible.
  </behavior>
  <action>Write the certification contract, then encode it as a validator, in that order — the prose is the specification and the script is its enforcement, and they must not drift.

The contract states, quoting rather than paraphrasing: the four Phase 28 Success Criteria verbatim from ROADMAP.md; the standing severity policy verbatim from AGENTS.md; and the adopted acceptance rule from decision-rationale.txt with its three amendments verbatim. It states the panel split (4-0) and points at the dissent, so a later reader can reopen the decision on the record rather than on memory.

The severity rubric defines the four bands and, critically, the RE-SCORING procedure: every finding is scored against Phase 28's own four criteria, the severity it arrived with is recorded as provenance only, and a finding whose subject matter is the subject matter of one of the four criteria is CRITICAL or HIGH by construction with the contradicted criterion named. Include the worked reasoning for the two carried findings the panel named, because they are the calibration examples for everything that follows.

The skip taxonomy defines exactly four legal classes and the evidence each requires, and states that a critical cell has NO legal skip. The classes are: a platform-inapplicability class, which requires the platform fact that makes the case meaningless on that family; an observation-blocked class; an architectural-impossibility class, which requires executable evidence that the behaviour cannot exist, not an argument that it would be hard; and an unresolved-surface class, for a surface one of phases 24-27 did not land, which requires the phase and that phase's own recorded requirement disposition. State plainly that no fifth class may be added mid-run and that adding one is a finding against the run, not a fix for it.

The observation-blocked class needs its evidence requirement written with more force than the other three, because it sits on the security dimension and it is the one an executor under time pressure will reach for. It requires a control MEASURED IN THE CERTIFICATION ENVIRONMENT AT RUN TIME, demonstrating that the observation channel is broken INDEPENDENTLY OF THE PRODUCT — a control constructed so that it fails when the channel is sound. A citation of a handoff, a prior phase, or any inherited belief is explicitly NOT acceptable evidence, and the contract must say so by name. Absent a passing control the cell is a RED, not a skip. Record the reason in the contract: the program's inherited belief that AppContainer is unobservable over SSH is currently under question, a concurrent investigation attributes the false negative to a stale-lease wedge rather than the logon session, and if that is right then this exact skip class has been operating as a blanket excuse that concealed real Windows sandbox failures. A skip class whose evidence requirement can be satisfied by citing lore is not a skip class.

State the sandbox activeness rule in the contract as a first-class rule, not as a note, because it is the structural answer to the silent-disable defect and it holds however the observability question resolves: a GREEN on any sandbox-dimension cell requires POSITIVE evidence that the sandbox was ACTIVE for that cell. Absence of an observed violation is not evidence of a sandbox. A cell that cannot produce positive activeness evidence is a RED — never a green, never a skip.

The carried-red ledger records every known-red the program is carrying into this phase, each with its origin, its inherited severity, its Phase 28 re-score, the criterion it contradicts if any, and its consequent available dispositions. Take the list from the two handoffs and do not extend it by guessing: the descendant-process-tree reap failure; the two Windows private-DACL unit tests; the worker output-exhaustion test that exceeds its budget and whose timeout was deliberately not raised; bash under Windows AppContainer, which is architectural; and the AppContainer ACL lease SID/profile mismatch under which the product can run with the sandbox silently inactive. Add a row for the observability question itself, carrying its status as OPEN and naming the intel artifact that will settle it, so a later reader cannot mistake an unmeasured belief for a measured result. Note in that row that the lease wedge and the silent-disable defect may be two faces of one underlying defect and that the matrix must be able to distinguish them rather than assume either. Re-score each against Phase 28's criteria per A1. Two of them cross the A2 line and the contract must say so in terms and record that their accept path is closed. Emit the same data as `evidence/28-01/known-red.tsv` so later plans consume it mechanically instead of re-reading prose.

Then write `.planning/scripts/f28-ledger.py`. It defines the finding record, validates a ledger file against every rule above, and carries a `--self-test` mode whose fixtures live inside the script so the test cannot drift from the implementation. Every rejection returns a distinct machine-readable failure code, because plan 04 gates on specific codes and a generic failure would let one rule silently stop working while the gate stayed green. Exit non-zero on any rejection.

Record in the contract, explicitly, that the acceptance rule was decided at planning time and is not this plan's to change.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f .planning/phases/28-native-cross-platform-certification/28-01-CERTIFICATION-CONTRACT.md</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; python3 .planning/scripts/f28-ledger.py --self-test</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; python3 .planning/scripts/f28-ledger.py --check-contract .planning/phases/28-native-cross-platform-certification/28-01-CERTIFICATION-CONTRACT.md</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -cv '^#' .planning/phases/28-native-cross-platform-certification/evidence/28-01/known-red.tsv)" -ge 6</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; python3 .planning/scripts/f28-ledger.py --check-rescoring .planning/phases/28-native-cross-platform-certification/evidence/28-01/known-red.tsv</automated>
  </verify>
  <done>The contract quotes the four criteria, the standing policy and the adopted rule with its three amendments verbatim, and names the panel split and the dissent. The severity rubric defines re-scoring and works the two calibration examples. Exactly four skip classes exist, each with a machine-checkable evidence requirement, and a critical cell has none. The carried-red TSV has one row per known-red with inherited severity, Phase 28 re-score, contradicted criterion and available dispositions. `f28-ledger.py --self-test` passes, and `--check-rescoring` proves the two A2 findings carry a closed accept path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the candidate resolver — read the surface inventory off the binary, attribute it to phases 24-27, and fail closed rather than guess</name>
  <files>.planning/scripts/f28-resolve-candidate.py, .planning/phases/28-native-cross-platform-certification/28-01-CANDIDATE-LEDGER.md, .planning/phases/28-native-cross-platform-certification/evidence/28-01/candidate.json</files>
  <read_first>.planning/phases/28-native-cross-platform-certification/28-01-CERTIFICATION-CONTRACT.md (task 1's output — the skip classes constrain what the resolver must report), scripts/f20-native-uat-proof.mjs (how 20A bound a candidate to a commit AND tree AND nonce and refused anything ambiguous — reuse the binding discipline, not the file), .planning/phases/20A-native-windows-macos-uat/20A-04-SUMMARY.md (what a sealed candidate looked like in practice and what its seal did NOT cover), .github/workflows/ci.yml around the release-binary upload added in d9c7683b (the per-target artifact names, including the aarch64-apple-darwin one), crates/wcore-cli/src/main.rs (how the binary describes its own command tree — this is the surface source of truth)</read_first>
  <behavior>
    - The resolver refuses to emit a candidate when any of phases 24, 25, 26, 27 has no verdict or summary artifact on disk, naming which.
    - The resolver refuses to emit a candidate whose commit is not also bound to a tree hash, so a moved branch cannot masquerade as the same candidate.
    - The resolver records a per-target binary digest for every target it can obtain, and records an explicit unbindable entry — never an omission — for every target it cannot.
    - A surface present in the binary's command tree but claimed by no phase artifact is emitted as a finding, not dropped.
    - A surface claimed by a phase artifact but absent from the binary's command tree is emitted as a finding, not silently treated as unresolved.
    - Re-running the resolver against the same commit and tree produces byte-identical output, so the candidate ledger is reproducible.
  </behavior>
  <action>Write `.planning/scripts/f28-resolve-candidate.py`, whose whole purpose is to answer "what exactly are we certifying" without anyone typing a feature name.

Candidate identity: resolve one exact commit and its tree hash, and bind them together. A commit alone is not a candidate — 20A learned this and pinned commit AND tree AND nonce for the same reason. Fail closed on ambiguity rather than picking the newest.

Per-target binaries: CI already uploads per-target release binaries as named artifacts, including the arm64 macOS one, since d9c7683b. Obtain each, digest it, and record target, artifact name, digest and provenance. Where a target cannot be obtained, record an explicit unbindable entry carrying the reason. Do NOT record an absent target as if it were merely untested, and do not treat the macOS target as unobtainable — it is obtainable, that claim is false, and it must not appear anywhere in this phase.

Surface inventory: interrogate the shipped binary's own command tree and capability self-description. This is the parameterization seam and the reason this phase survives its siblings still executing. Emit one record per discovered surface with a stable id, its entrypoint as the binary reports it, and the evidence text the binary produced. Do not read feature names out of planning documents into this list.

Attribution and mismatch: for each of phases 24, 25, 26 and 27, read that phase's verdict or summary artifact and its recorded requirement dispositions, attribute discovered surfaces to phases where the artifact supports it, and emit the two mismatch classes as findings in the ledger schema task 1 defined. A claimed-but-absent surface maps to the unresolved-surface skip class for its cells and carries the phase's own requirement disposition as the required evidence; a present-but-unclaimed surface is certified anyway and flagged, because an uncertified surface that ships is worse than an unattributed one.

Emit `evidence/28-01/candidate.json` as the machine artifact every later plan consumes, and `28-01-CANDIDATE-LEDGER.md` as its human rendering. The JSON is authoritative; the prose must be generated from it rather than typed, so the two cannot disagree.

Give the resolver a `--self-test` covering the fail-closed conditions with fixtures, and a `--verify` mode that re-validates an emitted candidate.json against the schema and the fail-closed rules, so plan 02 can prove its input is sound before it spends hardware time.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; python3 .planning/scripts/f28-resolve-candidate.py --self-test</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; python3 .planning/scripts/f28-resolve-candidate.py --verify .planning/phases/28-native-cross-platform-certification/evidence/28-01/candidate.json</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f .planning/phases/28-native-cross-platform-certification/28-01-CANDIDATE-LEDGER.md</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -c 'aarch64-apple-darwin' .planning/phases/28-native-cross-platform-certification/evidence/28-01/candidate.json)" -ge 1</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; python3 .planning/scripts/f28-resolve-candidate.py --verify-reproducible .planning/phases/28-native-cross-platform-certification/evidence/28-01/candidate.json</automated>
  </verify>
  <done>`candidate.json` binds one exact commit to its tree, carries a digest or an explicit unbindable entry for every target including arm64 macOS, lists the surface inventory as read off the binary, attributes surfaces to phases 24-27 where their artifacts support it, and records both mismatch classes as findings. The prose ledger is generated from the JSON. `--self-test`, `--verify` and `--verify-reproducible` all pass, and the fail-closed conditions are demonstrated by fixture rather than asserted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the E5 matrix generator — nine fixed dimensions crossed with three OS families and the resolved surfaces, with unclassified skips rejected at construction</name>
  <files>crates/wcore-eval-scenarios/src/e5_matrix.rs, crates/wcore-eval-scenarios/src/lib.rs, crates/wcore-eval-scenarios/tests/e5_matrix_contract.rs, .planning/phases/28-native-cross-platform-certification/evidence/28-01/matrix.tsv</files>
  <read_first>.planning/REQUIREMENTS.md F28-01 (the nine dimensions, verbatim — they are fixed and this task may not add, merge or rename one), .planning/phases/28-native-cross-platform-certification/28-01-CERTIFICATION-CONTRACT.md (the four skip classes and the mandatory cells the generator must produce), .planning/phases/28-native-cross-platform-certification/evidence/28-01/candidate.json (the resolved surfaces — the generator's only variable input), crates/wcore-eval-scenarios/src/lib.rs (module registration and the crate's existing shape), crates/wcore-eval-scenarios/src/catalog.rs (how this crate already expresses a case table as data — follow it rather than inventing a second style), scripts/f20-native-uat-proof.mjs WINDOWS_TARGET_SOURCES and MACOS_TARGET_SOURCES (the canonical-map pattern and the wrong-OS anti-drift guard built over it, which is the precedent for tying a cell to the thing it actually exercises)</read_first>
  <behavior>
    - Constructing a matrix in which any F28-01 dimension is absent from an OS family, without an applicability record carrying a reason, fails.
    - Constructing a matrix containing a cell whose skip carries no class fails.
    - Constructing a matrix containing a critical cell with any skip at all fails, since a critical cell has no legal skip.
    - Constructing a matrix whose observation-blocked skip has no referenced control fails.
    - The three mandatory named cells — sandbox silent-disable, descendant process-tree cleanup, and the observability control — are present, are marked critical, and cannot be constructed as skipped.
    - A sandbox-dimension cell cannot be constructed as PASSED without positive sandbox-activeness evidence; absence of an observed violation is rejected as a pass.
    - No skip in the emitted matrix carries an observation-blocked class at construction time, since no control has run yet; the class becomes usable only when plan 02 supplies a measured control.
    - Two runs of the generator over the same candidate.json produce identical cell ids in identical order, so the matrix is stable across the three hosts that will execute it.
  </behavior>
  <action>Add `e5_matrix.rs` to `wcore-eval-scenarios` and register it in `lib.rs`. This crate is the right home: it already owns the evidence receipt, process-tree ownership, redaction, hermetic fixtures and PTY capture, and a second harness crate would drift from it exactly as two test suites do.

The dimension list is FIXED and comes from F28-01 verbatim: sandbox probes, Unicode, long paths, the UNC and reparse and symlink family, process cleanup, suspend and resume, offline, disk-full and read-only, and hostile inputs. Nine. Do not add one because it seems useful, do not merge two because they seem similar, and do not rename one to something tidier — the requirement text is the authority and a renamed dimension is an unprovable one.

A cell is a dimension crossed with an OS family crossed with a resolved surface, and it carries a stable id, its dimension, its OS family, its surface, its criticality, and its applicability. Criticality is a property of the dimension and OS family, declared in the table, not decided per cell at run time — deciding it per cell is how a critical case becomes non-critical the moment it is inconvenient. Applicability is either applicable, or a skip carrying one of the four classes from the contract with that class's required evidence.

Construction is fail-closed. Reject at construction, never warn: a dimension absent from an OS family with no applicability record; a skip with no class; a critical cell carrying any skip; an observation-blocked skip with no control reference. Rejecting at construction rather than at report time matters because a matrix that reports its own defects at the end has already spent the hardware.

Three cells are MANDATORY, named in the contract, and the generator emits them regardless of what the surface resolution produces. The first is the Windows sandbox-dimension cell that fails if the product proceeds while its sandbox is inactive — the ACL lease SID and profile mismatch case under which the sandbox can be silently disabled. Criterion 1 exists to force that into the open and a generator that could omit it would defeat the criterion. The second is the Windows process-cleanup cell covering descendant process-tree reaping, which Criterion 2 names as its own subject. The third is the observability control cell itself: whether the Windows sandbox is observable in the certification environment is a measured question this phase answers rather than a belief it inherits, and it must occupy a cell so that its answer is recorded as evidence rather than as an executor's recollection. All three are critical and therefore unskippable.

Every sandbox-dimension cell — not only the mandatory one — carries a required activeness field, and a cell is constructible as PASSED only when that field holds positive evidence that the sandbox was active for it. Encode this in the type rather than in a convention: absence of an observed violation must not be expressible as a pass. This is what stops a silently-disabled sandbox from reporting green, and it holds regardless of how the observability question resolves.

Write `tests/e5_matrix_contract.rs` asserting each rejection above with a case that trips it and a case that does not, the presence and criticality of both mandatory cells, and generator stability across two runs. Emit `evidence/28-01/matrix.tsv` as the machine artifact plans 02 and 04 consume.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -c 'e5_matrix' crates/wcore-eval-scenarios/src/lib.rs)" -ge 1</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; ssh hetzner-dsm 'export PATH=/root/.cargo/bin:$PATH; cd /root/wayland-p28 &amp;&amp; cargo clippy -p wcore-eval-scenarios --all-targets -- -D warnings' > .planning/phases/28-native-cross-platform-certification/evidence/28-01/hz-clippy.log 2>&amp;1; rc=$?; test $rc -eq 0</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; ssh hetzner-dsm 'export PATH=/root/.cargo/bin:$PATH; cd /root/wayland-p28 &amp;&amp; cargo nextest run -p wcore-eval-scenarios --test e5_matrix_contract --no-fail-fast' > .planning/phases/28-native-cross-platform-certification/evidence/28-01/hz-matrix.log 2>&amp;1; rc=$?; test $rc -eq 0</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -c 'e5_matrix_contract' .planning/phases/28-native-cross-platform-certification/evidence/28-01/hz-matrix.log)" -ge 1</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; python3 .planning/scripts/f28-ledger.py --check-matrix .planning/phases/28-native-cross-platform-certification/evidence/28-01/matrix.tsv</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; python3 .planning/scripts/f28-ledger.py --check-no-uncontrolled-skips .planning/phases/28-native-cross-platform-certification/evidence/28-01/matrix.tsv</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f .planning/phases/28-native-cross-platform-certification/evidence/28-01/matrix.tsv &amp;&amp; /usr/bin/git diff --quiet -- crates/wcore-agent/src crates/wcore-sandbox/src crates/wcore-cli/src</automated>
  </verify>
  <done>Nine F28-01 dimensions exist verbatim, crossed with three OS families and the resolved surfaces. Construction rejects a missing dimension, an unclassified skip, a skipped critical cell and a controlless observation-blocked skip, each proved by a contract test that trips it. Both mandatory Windows cells are present, critical and unskippable. The generator is stable across runs. Clippy is clean at `-D warnings` and the contract tests pass on Linux hardware, with both logs retained. No production crate outside `wcore-eval-scenarios` was touched.</done>
</task>

</tasks>

## What this plan does NOT change (scope fence)

- **No matrix cell is executed, no soak is run and no receipt is signed.** This plan builds the contract and the generator. Running is plan 02, soaking is plan 03, signing and adjudicating is plan 04.
- **The acceptance rule is not reopened.** It was decided by the four-way panel at planning time, 4-0, with three binding amendments, and the record is in `28-01-decision-evidence/`. Encoding it is this plan's job; changing it is nobody's.
- **No 24-27 surface is named.** Every surface comes from the resolver at execution time. A plan that hardcodes one is certifying a guess.
- **No production file outside `crates/wcore-eval-scenarios` is touched**, and that invariance is gate-checked. A defect discovered while building the harness is recorded and routed, not fixed here.
- **No existing test is modified, renamed, re-gated, ignored, allowed or deleted.**
- **No second harness crate and no second marker discipline.** `scripts/f20-native-uat-proof.mjs` and `wcore-eval-scenarios` are the lineage; this plan extends them.
- **No new crate and no new dependency.**
- **The Success Criteria are quoted, never narrowed.** ROADMAP.md's text is the only completion authority.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| certified candidate ← what actually shipped | The certification is only worth what the resolved candidate is; a candidate resolved from documents rather than from the binary certifies intent, not product |
| matrix coverage ← skip policy | Every skip is a hole in the certification, and a skip class invented mid-run is a hole shaped exactly like the finding it routes around |
| finding severity ← the phase that scored it | A severity inherited from a phase with different criteria is not a severity for this phase; inherited labels are a laundering channel that needs no launderer |
| acceptance claim ← the evidence held | A receipt asserting more than the run measured is worse than one reporting a gap, because Phase 29 and the release consume it |
| observed red ← observation channel | A red can be an artifact of a broken channel, and an "artifact" can be a real red discounted by inherited lore. Only a run-time control distinguishes them, and this phase must not assume the answer in either direction |
| sandbox green ← sandbox actually active | A cell that merely fails to observe a violation proves nothing about whether a sandbox existed; a silently-disabled sandbox produces exactly that evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01-01 | Spoofing | The matrix is written against predicted 24-27 features, so it certifies a guess and silently omits whatever those phases actually shipped | critical | mitigate | Surfaces are read off the shipped binary's own command tree by the resolver; phase documents are used only for attribution and mismatch detection; naming a 24-27 surface anywhere in this phase is prohibited and fenced |
| T-28-01-02 | Repudiation | A finding arrives carrying a severity scored against an earlier phase's criteria, takes the accept path, and is signed into a receipt that contradicts it | critical | mitigate | Amendment A1 forces re-scoring against Phase 28's own four criteria with inherited severity kept as provenance only; amendment A2 closes the accept path for any finding contradicting a criterion; both are enforced by `f28-ledger.py`, not by prose |
| T-28-01-03 | Tampering | A fifth skip class is invented mid-run to route an inconvenient red out of the matrix, and the certification reports green over a hole | critical | mitigate | Exactly four classes exist, each with a machine-checkable evidence requirement; construction rejects an unclassified skip; a critical cell has no legal skip at all; adding a class is recorded as a finding against the run |
| T-28-01-04 | Spoofing | The Windows sandbox is silently inactive on an ACL lease SID or profile mismatch and the matrix never asks, so the certification signs a sandbox that was not there | critical | mitigate | The silent-disable cell is MANDATORY, emitted regardless of surface resolution, marked critical and therefore unskippable, and re-scored to CRITICAL with its accept path closed under A2. Structurally reinforced: a sandbox cell is constructible as PASSED only with positive activeness evidence, so absence of an observed violation cannot express itself as a green |
| T-28-01-10 | Repudiation | The phase inherits the belief that AppContainer is unobservable over SSH, uses it to discount Windows sandbox reds as artifacts, and thereby bakes a concealment of real failures into the certification's own acceptance gate | critical | mitigate | The belief is recorded as an OPEN question with its intel artifact named, never as a fact; no gate in this phase may have "SSH sandbox reds are artifacts" as a passing condition; the observation-blocked class requires a run-time control and explicitly rejects a citation of lore as evidence; the observability question occupies its own mandatory critical cell so its answer is recorded as measured evidence |
| T-28-01-05 | Repudiation | The candidate is resolved as a branch tip, the branch moves, and evidence is bound to something other than what was measured | high | mitigate | Commit is bound to tree; ambiguity fails closed rather than picking the newest; the resolver is reproducible and that reproducibility is gate-checked |
| T-28-01-06 | Information Disclosure | A per-target binary is unobtainable and the target is quietly omitted, so an OS family appears certified on evidence that never existed | high | mitigate | Every target gets a digest or an explicit unbindable entry; omission is impossible by schema; the arm64 macOS artifact is obtainable since d9c7683b and the contrary claim is prohibited by name |
| T-28-01-07 | Denial of Service | A sandbox red observed over SSH is recorded as a product defect, sending a repair cycle after an artifact of the observation channel — the mirror image of T-28-01-10, and both are live because the direction is not yet known | high | mitigate | The observation-blocked skip class requires a control measured at run time in the certification environment, constructed to fail when the channel is sound; absent that control the cell is a red rather than a skip, so neither error direction can be reached by assumption |
| T-28-01-08 | Tampering | A dimension is renamed or merged for tidiness and quietly stops being provable against F28-01's text | medium | mitigate | The nine dimensions are fixed verbatim from the requirement; the contract test asserts each appears on each OS family or carries an explicit applicability reason |
| T-28-01-09 | Repudiation | The prose contract and the validator drift, so the document says one rule and the gate enforces another | medium | mitigate | The validator carries `--check-contract` and its self-test fixtures live inside the script; the human ledger is generated from the JSON rather than typed |
| T-28-01-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency is added, removed or updated and no `Cargo.toml` change is expected; the edges this plan needs already exist in `crates/wcore-eval-scenarios/Cargo.toml`, and no install task exists in this plan |
</threat_model>

<verification>
Local gates (Mac, source level only — no cargo beyond fmt runs here): `cargo fmt --all -- --check` clean; the contract, the candidate ledger and both scripts exist; `f28-ledger.py --self-test`, `--check-contract`, `--check-rescoring` and `--check-matrix` pass; `f28-resolve-candidate.py --self-test`, `--verify` and `--verify-reproducible` pass; the carried-red TSV has a row per known-red excluding comment lines; `candidate.json` names the arm64 macOS target; and the only changed paths under `crates/` are `wcore-eval-scenarios` ones.

Authoritative gates, at one exact SHA asserted on the host before any build step, in this phase's dedicated worktree `/root/wayland-p28` on hetzner: `cargo clippy -p wcore-eval-scenarios --all-targets -- -D warnings`, then `cargo nextest run -p wcore-eval-scenarios --test e5_matrix_contract --no-fail-fast`. Both logs are retained under `evidence/28-01/` and gate-checked for the test-binary name, so a gate cannot pass on an empty or unrelated log. Build targeted; a bare full-workspace build on a shared box is what took sshd down previously.

Known unknowns to record, not to resolve here: whether the Windows sandbox is genuinely observable in the certification environment, which plan 02 settles by control and which this plan is forbidden from assuming in either direction; whether the stale-lease wedge and the silent-disable defect are two faces of one underlying defect, which the matrix must be able to distinguish rather than assume; how many surfaces the resolver will actually find, since phases 24-27 were mid-flight when this plan was written and their landed set is unknown by construction; whether any of those phases will lack a verdict artifact at execution time, which is termination state 2 rather than a failure; and whether the binary's self-description is complete enough to enumerate every user-reachable surface, which if it is not is itself a finding worth more than the matrix cell it costs.
</verification>

<success_criteria>
- The certification contract quotes Phase 28's four Success Criteria, the standing severity policy and the adopted acceptance rule with its three amendments verbatim, and records the 4-0 panel split with a pointer to the dissent (F28-04).
- The severity rubric defines re-scoring against Phase 28's own criteria, keeps inherited severity as provenance only, and works both calibration examples (F28-04).
- Exactly four skip classes exist, each with a machine-checkable evidence requirement, and a critical cell has none (F28-01, F28-04).
- The observation-blocked class requires a control measured at run time in the certification environment and explicitly rejects a citation of inherited lore as evidence; no rule, gate or skip in this plan has "sandbox reds from SSH are artifacts" as a passing condition (F28-01).
- The sandbox activeness rule is stated in the contract and enforced in the generator's types: a sandbox-dimension cell is constructible as PASSED only with positive evidence the sandbox was active, and absence of an observed violation is rejected as a pass (F28-01).
- Every known-red carried into this phase has a row with its origin, inherited severity, Phase 28 re-score, contradicted criterion and available dispositions; the two that cross the A2 line have their accept path closed and that is proved by `--check-rescoring` (F28-04).
- The resolver binds one exact commit to its tree, records a digest or an explicit unbindable entry per target including arm64 macOS, reads the surface inventory off the binary, and fails closed on a missing phase artifact or an ambiguous candidate (F28-01).
- Both surface mismatch classes — claimed-but-absent and present-but-unclaimed — are emitted as findings rather than dropped (F28-01).
- The matrix generator crosses nine verbatim F28-01 dimensions with three OS families and the resolved surfaces, and REJECTS at construction a missing dimension, an unclassified skip, a skipped critical cell and a controlless observation-blocked skip, each proved by a contract test that trips it (F28-01).
- All three mandatory cells — Windows sandbox silent-disable, Windows descendant process-tree cleanup, and the sandbox observability control — are present, critical and unskippable (F28-01).
- Clippy is clean at `-D warnings` and the contract tests pass on Linux hardware, with both logs retained and gate-checked for content.
- No production file outside `crates/wcore-eval-scenarios` was touched, no existing test was weakened, and no 24-27 surface is named anywhere in this phase.

## Artifacts this plan produces
- `28-01-CERTIFICATION-CONTRACT.md` — the acceptance rule, severity rubric, skip taxonomy, mandatory cells and carried-red ledger.
- `28-01-CANDIDATE-LEDGER.md` and `evidence/28-01/candidate.json` — the resolved candidate and its surface inventory.
- `.planning/scripts/f28-ledger.py` and `.planning/scripts/f28-resolve-candidate.py` — the two validators every later plan gates on.
- `crates/wcore-eval-scenarios/src/e5_matrix.rs` and `tests/e5_matrix_contract.rs` — the generator and its contract.
- `evidence/28-01/matrix.tsv`, `known-red.tsv`, `hz-clippy.log`, `hz-matrix.log` — the machine artifacts plans 02 and 04 consume.
</success_criteria>

<output>
Create `.planning/phases/28-native-cross-platform-certification/28-01-SUMMARY.md` using the standard Ferrox summary template. Record: the exact SHA and tree the contract tests ran at, asserted on the host; the resolved candidate with its per-target digests and every unbindable target with its reason; how many surfaces were discovered and how they attributed across phases 24-27, including every claimed-but-absent and present-but-unclaimed mismatch as a finding; which of phases 24-27 had a verdict artifact and which did not; the emitted matrix's cell count broken down by dimension, OS family and criticality, with every skip and its class; the carried-red ledger with each entry's inherited severity and Phase 28 re-score; confirmation that all three mandatory cells are present, critical and unskippable; the gate results with real numbers; and which of the four termination states the plan ended in. Record separately whether `.planning/intel/APPCONTAINER-SSH-LEASE-WEDGE.md` existed at execution time and what it said, and state plainly that the observability question is recorded as OPEN and is settled by control in plan 02, not inherited here. State explicitly that the acceptance decision was not reopened, that no 24-27 surface was named, that no rule or gate in this plan depends on Windows sandbox reds from SSH being artifacts, and that nothing was executed, soaked or signed.
</output>

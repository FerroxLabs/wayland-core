VENDOR: fly-machines
HIBERNATION: Fly Machines `suspend` (RAM-snapshot suspend/resume) as the reference transition, with
`stop`/`start` recorded separately and NEVER reported as hibernation — binding condition C1 from
25-01-panel-dissent.txt.
HOST: hetzner-dsm
DATE: 2026-07-26T15:26:00Z
TOKEN: absent
ORG: absent

RAW PROBE (read-only; no credential was sent anywhere, no account was touched):
  ssh -o BatchMode=yes hetzner-dsm '... test -n "${!v}" for each name ...'
  FLY_API_TOKEN: absent
  WAYLAND_F25_CLOUD_TOKEN: absent
  WAYLAND_F25_CLOUD_ORG: absent
  E2B_API_KEY: absent
  AWS_ACCESS_KEY_ID: absent
  ~/.aws/credentials: absent
  ~/.fly/config.yml: absent

F25-SC1-CLOUD: NOT RUN — no Fly credential exists on the authoritative proof host. WAYLAND_F25_CLOUD_TOKEN and WAYLAND_F25_CLOUD_ORG are both absent and an agent cannot fund an organization or mint a token. This is termination state 2 by design and it is NON-BLOCKING: the cloud backend is implemented and fails closed with an explicit unavailable verdict.

EXACT COMMAND THAT CLOSES THIS LEG (once Sean mints the token):
  ssh hetzner-dsm 'cd /root/wayland-p25 && \
    WAYLAND_F25_CLOUD_TOKEN=<fly deploy token> \
    WAYLAND_F25_CLOUD_ORG=<throwaway fly org/app slug> \
    ./target/release/wayland-core backend run --backend cloud \
      --task crates/wcore-exec-backend/tests/fixtures/f25-reference-task.json \
      --receipt-out /tmp/f25-cloud-receipt.json'

WHAT SEAN MUST MINT, AND NOTHING WIDER:
  1. One throwaway Fly.io organization (or app) that holds ONLY this phase's machines, so plan
     25-04's orphan scan can assert emptiness against a single API call without touching anything
     else Sean owns.
  2. One deploy token scoped to that organization only. Do NOT reuse a production token.
  3. Export both as WAYLAND_F25_CLOUD_TOKEN and WAYLAND_F25_CLOUD_ORG on hetzner-dsm.

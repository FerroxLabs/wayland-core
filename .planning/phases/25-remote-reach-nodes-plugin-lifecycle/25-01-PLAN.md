---
phase: 25-remote-reach-nodes-plugin-lifecycle
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/wcore-exec-backend/Cargo.toml
  - crates/wcore-exec-backend/src/lib.rs
  - crates/wcore-exec-backend/src/contract.rs
  - crates/wcore-exec-backend/src/receipt.rs
  - crates/wcore-exec-backend/src/policy.rs
  - crates/wcore-exec-backend/src/backends/local.rs
  - crates/wcore-exec-backend/src/backends/container.rs
  - crates/wcore-exec-backend/src/backends/ssh.rs
  - crates/wcore-exec-backend/src/backends/cloud.rs
  - crates/wcore-exec-backend/src/backends/mod.rs
  - crates/wcore-exec-backend/src/conformance.rs
  - crates/wcore-exec-backend/tests/conformance_matrix.rs
  - crates/wcore-exec-backend/tests/live_equivalence.rs
  - crates/wcore-plugin-api/build.rs
  - crates/wcore-plugin-api/src/lib.rs
  - crates/wcore-plugin-api/src/execution_backend_spec.rs
  - crates/wcore-plugin-api/src/registry/mod.rs
  - crates/wcore-plugin-api/src/registry/execution_backends.rs
  - crates/wcore-plugin-api/src/context.rs
  - crates/wcore-plugin-api/tests/execution_backend_spec_mirror_test.rs
  - crates/wcore-agent/src/plugins/adapters/exec_backend_adapter.rs
  - crates/wcore-agent/src/plugins/adapters/mod.rs
  - crates/wcore-cli/src/backend.rs
  - crates/wcore-cli/src/lib.rs
  - crates/wcore-cli/src/main.rs
  - crates/wcore-config/src/config.rs
  - .planning/phases/25-remote-reach-nodes-plugin-lifecycle/25-01-CLOUD-BACKEND-DECISION.md
  - .planning/phases/25-remote-reach-nodes-plugin-lifecycle/25-01-EQUIVALENCE-EVIDENCE.md
autonomous: false
requirements:
  - F25-01
  - F25-02
user_setup:
  - service: "hibernating cloud execution backend (vendor selected at Task 2)"
    why: "Success Criterion 1 requires ONE hibernating cloud reference backend running a real task. Only Sean can create the account, fund it, and mint the token. Claude can do everything else."
    env_vars:
      - name: WAYLAND_F25_CLOUD_TOKEN
        source: "The selected vendor's dashboard -> API tokens. Scope it to a single throwaway org/app created for this phase; do not reuse a production token."
      - name: WAYLAND_F25_CLOUD_ORG
        source: "The selected vendor's org / project / app identifier that the throwaway token is scoped to."
    dashboard_config:
      - task: "Create one throwaway org/app that holds ONLY this phase's machines, so an orphan scan can assert emptiness without touching anything else Sean owns."
        location: "The selected vendor's dashboard."
must_haves:
  truths:
    - "SUCCESS CRITERION 1 IS AN EQUIVALENCE CLAIM, NOT A REACH CLAIM. The phase does not pass because four backends each ran something. It passes because THE SAME deterministic task produced EQUIVALENT policy, receipts, cancellation and cleanup on all four. The proof artifact is therefore a DIFF of four receipts, not four green logs. A backend that runs the task but cannot produce a comparable receipt has FAILED this criterion, and saying so is the correct outcome."
    - "THE RECEIPT SCHEMA IS ALREADY DESIGNED AND IT IS NOT THIS PHASE'S TO INVENT. `crates/wcore-eval-scenarios/src/fixtures/remote_execution.rs` (984 lines) is a complete, tested, deterministic remote-execution receipt contract — ordered events beginning at a reserved `task_accepted` sequence, content-addressed workspace/input/artifact digests, an Ed25519 attestation whose `key_id` is the SHA-256 of the pinned verifying key, resource denial BEFORE task acceptance, artifact-over-budget denial AFTER acceptance, and tamper/unpinned-backend rejection. Its own module doc states in the first paragraph that it 'does not implement or advertise an F25 production backend, transport, secret channel, container, SSH connection, or cloud executor'. It is therefore the ORACLE this plan must satisfy, and the production receipt must be checkable against it. Re-deriving a second receipt shape is a forbidden parallel authority."
    - "THE PLUGIN-API ISOLATION LINT HAS A REAL HOLE AND THIS PLAN WOULD DRIVE THROUGH IT. `crates/wcore-plugin-api/build.rs` FORBIDDEN_CORE_IMPORTS lists wcore-agent, wcore-tools, wcore-mcp, wcore-skills, wcore-memory, wcore-config, wcore-providers, wcore-compact, wcore-browser, wcore-cua, wcore-eval, wcore-evolve, wcore-observability, wcore-repomap, libloading, wasmtime, wasmer, dlopen and abi_stable. It does NOT list `wcore-sandbox`, and it obviously cannot yet list `wcore-exec-backend`. So the lint would silently permit exactly the dependency that audit finding F2 exists to forbid. Extending that list is part of this plan's deliverable, not an optional tidy-up."
    - "THE MIRROR PATTERN IS ESTABLISHED AND MUST BE COPIED, NOT REINVENTED. `wcore-plugin-api/src/browser_spec.rs` + `registry/browser.rs` declare a `BrowserToolSpec` and a `BrowserToolRegistrar` trait the host implements; `wcore-agent/src/plugins/adapters/browser_adapter.rs` holds `HostBrowserRegistrar`, which translates the spec into a real `wcore_browser` object AFTER `initialize()` returns. `wayland-browser` packages the spec through the mirror and carries NO `wcore-browser` dependency. The execution-backend contract takes exactly this shape, for exactly the same reason."
    - "CONTAINER REACH IS ALREADY HALF BUILT AND ITS AVAILABILITY SIGNAL IS WEAKER THAN IT LOOKS. `crates/wcore-sandbox/src/backends/docker.rs` has a working `DockerBackend` behind the `live-docker` feature, but `is_available()` only answers `docker_socket_present()` — DOCKER_HOST set, or the unix socket / named pipe existing. Its doc comment says so explicitly and points security-sensitive callers at `connect()`, which does a real 5-second daemon ping. The container reference backend MUST report availability from the ping, not the socket, or the phase ships a capability lie."
    - "SSH NEEDS NO NEW DEPENDENCY AND MUST NOT GET ONE. AGENTS.md mandates that every process spawn go through `wcore_config::shell`, and that ANY command whose arguments include non-literal data use ARGV mode (`shell_command_argv`), never shell-string mode, because argv mode never lets a metacharacter reach an interpreter. The system `ssh` client exists on macOS, on Linux and on Windows 10+, and the two live targets — `hetzner-dsm` and `SeanD@seandesktop` — are already reachable with existing keys. A pure-Rust SSH crate would add a large new dependency to buy nothing this criterion needs."
    - "NO NEW THIRD-PARTY CRATE IS REQUIRED AND ADDING ONE IS A CHECKPOINT, NOT A DECISION THE EXECUTOR MAKES. The workspace `[workspace.dependencies]` already carries tokio, serde, serde_json, async-trait, thiserror, anyhow, clap, sha2, ed25519-dalek, base64, uuid, rand and dirs, and `wcore-egress` re-exports reqwest as the single mandated outbound-HTTP chokepoint (a clippy disallowed-methods lint bans raw reqwest client construction outside it). The cloud backend is an HTTPS REST client and therefore routes through `wcore-egress` like everything else."
    - "A REPORTED RED IS WORTH FAR MORE THAN AN ENGINEERED GREEN. Never weaken an assertion, `#[ignore]` a test, add an `#[allow]`, raise a timeout, or delete an inconvenient case to reach a gate. The specific forbidden move HERE is normalising the four receipts so aggressively that they compare equal by construction: if the equivalence check strips every field on which the backends actually differ, it proves nothing and it is worse than a red."
    - "CAPABILITY HONESTY IS PART OF THE CONTRACT. Where a backend is genuinely unavailable on a host — no Docker daemon on SeanDesktop, no cloud token in the environment — the run must REPORT that surface as unexercised, with the reason, and must not silently skip. Phase 20A's TEST-AUDIT found 283 tests with no execution evidence and roughly 145 that ran in no workflow at all; a silent skip is how that happens."
  artifacts:
    - path: crates/wcore-exec-backend/src/contract.rs
      provides: "The provider-neutral ExecutionBackend trait covering capabilities, policy, secrets, artifacts, limits, cancellation, attestation, receipts and lifecycle health — F25-01 in one place"
    - path: crates/wcore-exec-backend/src/conformance.rs
      provides: "The backend-agnostic conformance harness every reference backend must pass identically, checkable against the wcore-eval-scenarios remote-execution oracle"
    - path: crates/wcore-plugin-api/src/execution_backend_spec.rs
      provides: "The plugin-facing mirror of the contract, with no dependency on the real backend crate"
    - path: crates/wcore-cli/src/backend.rs
      provides: "The `wayland-core backend` operator surface — the thing a human actually runs, and the only surface the live exercise is allowed to drive"
    - path: .planning/phases/25-remote-reach-nodes-plugin-lifecycle/25-01-CLOUD-BACKEND-DECISION.md
      provides: "The recorded, Sean-authorized choice of the one hibernating cloud reference backend, with the hibernation mechanism named and its cost"
    - path: .planning/phases/25-remote-reach-nodes-plugin-lifecycle/25-01-EQUIVALENCE-EVIDENCE.md
      provides: "The four real receipts, their diff, the cancellation and cleanup observations, and the named host each leg ran on"
  key_links:
    - from: crates/wcore-exec-backend/src/contract.rs
      to: crates/wcore-sandbox/src/backends/mod.rs
      via: "the local and container reference backends composing the existing SandboxBackend containment rather than replacing or bypassing it"
      pattern: "compose-not-replace"
    - from: crates/wcore-exec-backend/src/receipt.rs
      to: crates/wcore-eval-scenarios/src/fixtures/remote_execution.rs
      via: "the production receipt satisfying the fixture oracle's ordering, content-addressing and attestation rules"
      pattern: "oracle-conformance"
    - from: crates/wcore-plugin-api/src/execution_backend_spec.rs
      to: crates/wcore-agent/src/plugins/adapters/exec_backend_adapter.rs
      via: "the host registrar translating a validated spec into a real backend after initialize() returns, exactly as HostBrowserRegistrar does"
      pattern: "plugin-mirror"
    - from: crates/wcore-cli/src/backend.rs
      to: .planning/phases/25-remote-reach-nodes-plugin-lifecycle/25-01-EQUIVALENCE-EVIDENCE.md
      via: "every receipt in the evidence file having been produced by running the shipped binary, not by calling a library from a test"
      pattern: "live-product-exercise"
---

<objective>
Build the provider-neutral execution-backend contract and prove Success Criterion 1: the SAME deterministic task runs locally, in a container, over SSH and on ONE hibernating cloud reference backend with equivalent policy, receipts, cancellation and cleanup.

Purpose: this is the reach gap. Wayland's containment, transaction and receipt spine is strong and entirely local. Hermes is ahead on execution reach. Closing that without hardcoding vendors into the engine is the whole point of F25-01, and `.planning/intel/constraints.md` states the shape directly: an execution-backend PLUGIN contract for capabilities, policy, secrets, artifact transfer, resource limits, cancellation, attestation and receipts, with local-container and SSH references. PROJECT.md fences the cloud half to ONE hibernating reference backend plus an extensible contract — not every vendor.
Output: one new mid-tier crate carrying the contract, four reference backends, a conformance harness bound to the existing remote-execution oracle, the plugin-API mirror plus host registrar, a `wayland-core backend` operator surface, and an evidence file holding four REAL receipts from four REAL runs with their diff.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/HANDOFF-2026-07-26-phase20-20A-complete.md
@crates/wcore-sandbox/src/backends/mod.rs
@crates/wcore-sandbox/src/backends/docker.rs
@crates/wcore-sandbox/src/backends/process_tree.rs
@crates/wcore-eval-scenarios/src/fixtures/remote_execution.rs
@crates/wcore-eval-scenarios/tests/remote_execution_fixture_contract.rs
@crates/wcore-plugin-api/build.rs
@crates/wcore-plugin-api/src/browser_spec.rs
@crates/wcore-plugin-api/src/registry/browser.rs
@crates/wcore-egress/src/lib.rs
@crates/wcore-egress/src/policy.rs
</context>

<execution_rules>

**THE TWO AMENDED PHASE RULES — verbatim, and they bound this plan.**

- Findings at CRITICAL or HIGH must be fixed or disproved. MEDIUM and below are logged to BACKLOG and DO NOT BLOCK execution.
- Execution begins when no CRITICAL or HIGH finding is open, or after 2 review rounds, whichever comes first. A third round is NOT permitted; it escalates to Sean.

**TERMINATION CRITERION FOR THIS PLAN (hard — the plan STOPS and escalates rather than spawning more work).** This plan builds ONE contract, FOUR reference backends and ONE equivalence proof. It terminates in exactly one of four states, and in every one of them it writes its SUMMARY and stops:
1. **Complete** — all four backends run the same task, their receipts diff to an equivalence verdict, cancellation and cleanup are observed on each, and the named hosts are recorded.
2. **Complete with a bounded cloud gap** — the cloud vendor decision was taken but the credential or the account is not available. The other three legs are complete and the cloud leg is recorded UNEXERCISED with the exact blocking reason and the exact command that will close it. Success Criterion 1 is then explicitly NOT met and the SUMMARY says so in those words. This is an honest outcome and it is better than a fabricated one.
3. **Escalated — the contract cannot cover all four surfaces without weakening containment.** Record what could not be expressed and stop. Ship no production code that pretends otherwise.
4. **Escalated — a new third-party dependency is genuinely required.** Name it, name what it buys, stop, and ask. Do not add it.
Under no circumstances does this plan add a fifth backend, generalise to a second cloud vendor, start a second design cycle after Task 2, or extend its own task list.

**THE LIVE VERIFICATION MANDATE (AGENTS.md, standing rule, commit `d248a461`).** Live testing ranks at least as high as green code. Success Criterion 1 is closed ONLY by running the shipped `wayland-core` binary with an exact invocation, observing an exact outcome, on a named host. Unit and integration tests remain REQUIRED and are never on their own sufficient. Phase 20A drove fourteen native acceptance targets to green and nobody ever launched the binary; do not recreate that.

**ENVIRONMENT.**
- Linux (authoritative Cargo proof and the primary live host): `ssh -o BatchMode=yes hetzner-dsm`, checkout `/root/wayland`.
- Windows: `ssh -o BatchMode=yes SeanD@seandesktop` (Tailscale), checkout `C:\ferrox-win`, cargo at `C:\Users\seand\.cargo\bin\cargo.exe`, PowerShell default shell. Wrap git as `cmd /c "git ..."`. `cargo fmt --all` FAILS there (os error 206); `justfile:96-98` already skips it.
- This Mac: NEVER run Cargo. `cargo fmt --all -- --check` is the one permitted cargo invocation. Use `/usr/bin/git` and ALWAYS `/usr/bin/grep` — the Mac `grep` is rtk-proxied and SILENTLY DROPS LINES (measured 32 returned against 674 for the same inverted match on the same file). Use `-F` for literals.
- Both hosts' fetch refspecs are pinned to an unrelated branch, so `git fetch --all` silently misses this one. Always `git fetch origin plan/f20-unified-audit-repair`.
- In `cmd`, `set VAR=value && ...` appends a TRAILING SPACE and Rust silently falls back to the default. Use `set "VAR=x"` or `$env:VAR='x'` and PROVE the value took effect before trusting any run that depends on it.
- Windows CI runs clippy `-D warnings` BEFORE tests, so a lint failure means tests never execute.
- No push to main, merge, PR, tag, release, deployment or issue closure without Sean.

**AGENTS.md discipline.** Surgical diffs; no drive-by refactors. Every process spawn goes through `wcore_config::shell`, and every spawn carrying non-literal arguments uses ARGV mode — never `format!` into a shell string. All outbound HTTP goes through `wcore-egress`. `thiserror` for public error types, `anyhow` internally. No `unwrap()` in production code without a proven, commented invariant. Files stay under 1000 lines. Clippy-clean at `-D warnings`.

**Git hygiene.** `/usr/bin/git` on the Mac. Stage the exact paths in `files_modified`, never `-A`, never `.`. Never stage `AGENTS.md` or `.ijfw` churn. No `Co-Authored-By` trailers.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the execution-backend contract, mirror it into the plugin API, and land the local and container reference backends behind a real operator surface</name>
  <files>Cargo.toml, crates/wcore-exec-backend/Cargo.toml, crates/wcore-exec-backend/src/lib.rs, crates/wcore-exec-backend/src/contract.rs, crates/wcore-exec-backend/src/receipt.rs, crates/wcore-exec-backend/src/policy.rs, crates/wcore-exec-backend/src/backends/mod.rs, crates/wcore-exec-backend/src/backends/local.rs, crates/wcore-exec-backend/src/backends/container.rs, crates/wcore-exec-backend/src/conformance.rs, crates/wcore-exec-backend/tests/conformance_matrix.rs, crates/wcore-plugin-api/build.rs, crates/wcore-plugin-api/src/lib.rs, crates/wcore-plugin-api/src/execution_backend_spec.rs, crates/wcore-plugin-api/src/registry/mod.rs, crates/wcore-plugin-api/src/registry/execution_backends.rs, crates/wcore-plugin-api/src/context.rs, crates/wcore-plugin-api/tests/execution_backend_spec_mirror_test.rs, crates/wcore-agent/src/plugins/adapters/exec_backend_adapter.rs, crates/wcore-agent/src/plugins/adapters/mod.rs, crates/wcore-cli/src/backend.rs, crates/wcore-cli/src/lib.rs, crates/wcore-cli/src/main.rs, crates/wcore-config/src/config.rs</files>
  <read_first>crates/wcore-eval-scenarios/src/fixtures/remote_execution.rs (the whole module — this is the receipt oracle: read `FixtureBackendIdentity`, `ResourceBudget`, `RemoteTask`, `ScriptedOutputEvent`, `FixtureArtifact`, `ScriptedOutcome`, `RemoteExecutionEventKind`, `TerminalStatus`, `RemoteExecutionReceiptBody`, `FixtureAttestation`, `RemoteExecutionReceipt::verify`, and every `MAX_*` constant), crates/wcore-eval-scenarios/tests/remote_execution_fixture_contract.rs (the eight contract tests — these name the exact invariants the production receipt has to satisfy), crates/wcore-sandbox/src/backends/mod.rs (the `SandboxBackend` trait and every default-implemented predicate, especially `owns_descendants_hard`, `binds_cwd_authority`, `binds_workspace_authority` and the buffered-output ceiling), crates/wcore-sandbox/src/backends/docker.rs (`DockerBackend::new`, `connect`, `configured_docker_client`, `docker_socket_present`, and the `SandboxBackend` impl — note precisely which of the two availability signals each uses), crates/wcore-plugin-api/src/browser_spec.rs and crates/wcore-plugin-api/src/registry/browser.rs (the spec/registrar mirror pattern verbatim), crates/wcore-agent/src/plugins/adapters/ (the browser and cua host adapters — the translate-after-initialize call site), crates/wcore-plugin-api/src/context.rs (how a scoped registry is handed to a plugin), crates/wcore-plugin-api/build.rs (the FORBIDDEN_CORE_IMPORTS list this task extends), crates/wcore-egress/src/lib.rs and crates/wcore-egress/src/policy.rs (`EgressDecision`, the shared policy handle, and the single-chokepoint rule), crates/wcore-cli/src/main.rs `TopCmd` (how a subcommand group is declared and dispatched)</read_first>
  <behavior>
    - A task submitted with a resource request the backend cannot satisfy is DENIED BEFORE acceptance, and the denial carries the resource kind — matching the oracle's `resource_request_is_denied_before_task_acceptance`.
    - An artifact that exceeds the accepted task's output budget is denied AFTER acceptance, and streamed output plus artifact bytes count against ONE shared budget — matching `artifact_over_task_output_budget_is_denied_after_acceptance` and `output_budget_counts_streamed_text_and_artifact_together`.
    - Every terminal outcome — success, failure, cancellation, disconnection — produces exactly ONE attested terminal event, and events are strictly ordered with the accepted event first.
    - A receipt whose body has been altered, or whose backend identity is not the pinned one, fails verification.
    - The local backend and the container backend, given byte-identical task input, produce receipts whose normalized bodies are equal and whose backend identity, timing and transport fields differ.
    - `backend list` reports each backend's availability from a real liveness probe: the container backend answers from a daemon ping, not from socket presence.
    - Registering an execution-backend spec through the plugin API requires the same manifest permission gate the browser spec requires, and a second registration from the same plugin is refused.
    - Building `wcore-plugin-api` with a dependency on `wcore-sandbox` or on `wcore-exec-backend` FAILS the build with the FORBIDDEN_CORE_IMPORTS diagnostic.
  </behavior>
  <action>Create the crate `crates/wcore-exec-backend` and add it to the workspace members list in the root `Cargo.toml`, placed in the mid tier: it may depend on `wcore-types`, `wcore-config`, `wcore-sandbox` and `wcore-egress`, and it must NOT depend on `wcore-agent` or `wcore-cli`. Take every third-party dependency from `[workspace.dependencies]` with `workspace = true`; add NO new third-party crate. If you believe one is genuinely required, STOP and escalate under termination state 4 — do not add it and do not work around the contract to avoid it.

WHY A NEW CRATE, recorded here so it is not re-litigated during execution or review: `.planning/intel/constraints.md` scopes F25 to an execution-backend contract whose primary paths are the plugin API, the sandbox and execution abstractions, child lifecycle, and protocol/backend configuration. The obvious alternative is to extend `wcore-sandbox`, where `DockerBackend` already lives. That alternative is rejected because `wcore-sandbox` is the CONTAINMENT crate whose posture Phase 20 and 20A spent months proving, and SSH plus a cloud REST client are network reach, not containment; folding a credentialed network transport into it puts new attack surface inside the crate that currently carries the anti-swap and hard-containment guarantees. The contract is also strictly broader than `SandboxBackend` — it adds attestation, artifact transfer, receipts and lifecycle health. The new crate COMPOSES `wcore-sandbox` for the local and container references; it does not replace, wrap-around, or bypass any containment predicate. If review disagrees, that is a HIGH finding to be argued at review, not a decision to be reopened mid-execution.

In `contract.rs` define the provider-neutral `ExecutionBackend` trait covering, as named surfaces rather than a free-form blob: declared capabilities; the effective policy for a task (including the egress decision source and the secret-exposure set); secret provisioning; artifact transfer in and out; resource limits; cancellation; attestation of backend identity; receipt emission; and lifecycle health. Model resource limits on the oracle's `ResourceBudget` (cpu millis, memory bytes, wall-time ms, output bytes) and keep its rule that a zero in any field is invalid. Keep the trait object-safe and `async_trait`-based to match `SandboxBackend`.

In `receipt.rs` define the production receipt. It must satisfy the oracle rather than compete with it: ordered events with a reserved accepted-event sequence, content-addressed workspace/input/artifact digests, exactly one attested terminal event, Ed25519 attestation whose key id is the SHA-256 of the pinned verifying key, and the same tamper and unpinned-identity rejections. Provide a normalization function that produces the comparable body — the fields that MUST match across backends — and keep the divergent fields (backend id, instance id, transport, timings) OUT of it and reported alongside. Do not normalize away anything on which two backends legitimately differ in a way a user would care about; over-normalizing is the forbidden move named in the execution rules.

In `policy.rs` bind the egress decision to `wcore-egress`'s shared policy rather than re-deriving one, and define the secret-exposure declaration a backend must make before a task is accepted.

In `backends/local.rs` implement the local reference by composing the existing `wcore-sandbox` registry — it selects the platform's real containment backend and inherits its process-tree ownership. In `backends/container.rs` implement the container reference over `DockerBackend`. Report container availability from the daemon `connect()` ping, NOT from `docker_socket_present()`, and say in a code comment why the cheaper signal is not sufficient here. Label every container the backend creates with a per-task nonce so plan 25-04's orphan scan can find them.

In `conformance.rs` write the backend-agnostic harness: one function that takes any `ExecutionBackend` and drives the full behavior list above, so every reference backend is proven by the SAME code. `tests/conformance_matrix.rs` runs it across every backend available on the host and REPORTS each unavailable backend with its reason instead of skipping silently.

Mirror the contract into the plugin API as `execution_backend_spec.rs` plus `registry/execution_backends.rs`, following `browser_spec.rs` and `registry/browser.rs` exactly: a declarative spec, a host-implemented registrar trait, the same manifest permission gate, at most one registration per plugin, and translation performed by the host AFTER `initialize()` returns. Add the host side as `HostExecutionBackendRegistrar` in `crates/wcore-agent/src/plugins/adapters/exec_backend_adapter.rs` alongside the browser and cua adapters.

Then close the isolation hole: add `wcore-sandbox` and `wcore-exec-backend` to FORBIDDEN_CORE_IMPORTS in `crates/wcore-plugin-api/build.rs`. That list currently omits `wcore-sandbox`, so the lint that exists to enforce audit finding F2 would have permitted precisely the dependency this task must not create.

Add the operator surface in `crates/wcore-cli/src/backend.rs`, registered as a new `TopCmd` variant: `wayland-core backend list` (name, kind, availability with its probe basis, declared capabilities), `wayland-core backend probe <name>`, `wayland-core backend run --backend <name> --task <file> --receipt-out <path>`, `wayland-core backend cancel --task-id <id>`, and `wayland-core backend receipt verify <path>`. Add the `[execution_backends]` configuration keys in `crates/wcore-config/src/config.rs`. NOTE FOR THE SEAM LEDGER: `crates/wcore-cli/src/main.rs`, `crates/wcore-cli/src/lib.rs` and `crates/wcore-config/src/config.rs` are all named shared seams for the 24-27 fan-out in the handoff; keep both diffs additive and minimal.

Write tests FIRST for each behavior above, watch them fail, then implement. Records evidence for F25-01; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; git fetch origin plan/f20-unified-audit-repair &amp;&amp; git checkout -q FETCH_HEAD &amp;&amp; cargo clippy -p wcore-exec-backend -p wcore-plugin-api -p wcore-cli --all-targets --all-features -- -D warnings'</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; cargo nextest run -p wcore-exec-backend --no-fail-fast'</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; cargo nextest run -p wcore-plugin-api --test execution_backend_spec_mirror_test --no-fail-fast'</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/grep -cF 'wcore-sandbox' crates/wcore-plugin-api/build.rs | /usr/bin/grep -qv '^0$' &amp;&amp; /usr/bin/grep -cF 'wcore-exec-backend' crates/wcore-plugin-api/build.rs | /usr/bin/grep -qv '^0$' &amp;&amp; echo "isolation lint extended"</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; cargo build --locked -p wcore-cli --bin wayland-core &amp;&amp; ./target/debug/wayland-core backend list'</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -c 'workspace = true' crates/wcore-exec-backend/Cargo.toml)" -ge 1 &amp;&amp; /usr/bin/git diff --unified=0 -- Cargo.lock | /usr/bin/grep -E '^\+name = ' | /usr/bin/grep -vF 'wcore-exec-backend' ; test $? -ne 0 &amp;&amp; echo "the only new Cargo.lock entry is this phase's own crate; no third-party dependency was added"</automated>
  </verify>
  <done>`wcore-exec-backend` exists as a workspace member with no new third-party dependency; the `ExecutionBackend` trait names every F25-01 surface; the production receipt satisfies the remote-execution oracle's ordering, content-addressing, budget-denial, single-terminal-event, and tamper-rejection rules; local and container reference backends both pass the SAME conformance harness; the container backend reports availability from a daemon ping and labels its containers with a per-task nonce; the plugin-API mirror follows the browser pattern and its host registrar translates after initialize; FORBIDDEN_CORE_IMPORTS now names both `wcore-sandbox` and `wcore-exec-backend`; and `wayland-core backend list` runs on the real binary on hetzner-dsm and prints each backend with its availability and probe basis.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2 (BLOCKING DECISION): Authorize the ONE hibernating cloud reference backend and its credential</name>
  <files>.planning/phases/25-remote-reach-nodes-plugin-lifecycle/25-01-CLOUD-BACKEND-DECISION.md</files>
  <action>Present the recommendation and its two alternatives to Sean, obtain ONE authorization, and record it — including the exact hibernation mechanism, the expected cost of the phase's runs, and the throwaway org/app the token is scoped to. Do not write a line of cloud-backend code before the authorization is given. If Sean declines a cloud vendor entirely, Task 3 implements SSH only, Task 4 runs a three-way equivalence, and the plan terminates in state 2 with Success Criterion 1 recorded as NOT MET — which is an honest outcome and is recorded in those words.</action>
  <decision>Which single hibernating cloud backend is the F25 reference implementation?</decision>
  <context>
PROJECT.md's Out of Scope section fences this precisely: "Cloud-provider proliferation — F25 proves one hibernating cloud reference backend plus an extensible contract, not every vendor." So exactly one vendor is chosen, once, and the contract carries the extensibility. This needs Sean because it costs real money and requires an account and a token that only he can mint; everything else in this plan is fully automatable. It is also the single riskiest dependency in the phase: it is the only leg of Success Criterion 1 that depends on a third party being up, and it is the leg most likely to end in termination state 2.

Task 1 already fixed the shape of the integration, which narrows the choice: the backend is an HTTPS REST client routed through `wcore-egress` (the mandated single outbound-HTTP chokepoint), it adds NO new third-party crate, and it must expose a hibernation transition the contract can observe — start, run, stop-or-suspend, and resume — because "hibernating" is the property the criterion names, not an incidental billing detail.
  </context>
  <options>
    <option id="fly-machines">
      <name>(Recommended) Fly.io Machines</name>
      <pros>Hibernation is first-class and explicitly observable: a Machine auto-stops or suspends when idle and auto-starts on the next request, and `start`, `stop`, `suspend` and `destroy` are all plain REST verbs on the Machines API — which is exactly the lifecycle the contract needs to attest. It is a plain HTTPS JSON API, so it drops straight into `wcore-egress` with no SDK and therefore no new dependency, and it behaves identically from macOS, Linux and Windows, which matters because plan 25-04 runs the fail-closed legs on more than one OS. Per-second billing on a scratch org makes a phase's worth of runs cost cents, and a per-machine label lets 25-04's orphan scan assert emptiness against a single API call.</pros>
      <cons>Requires a funded Fly organization and a scoped deploy token. Machine boot from cold is seconds, not milliseconds, so the equivalence run must tolerate a wider timing band on this leg than on the other three — which is fine, because timing is deliberately excluded from the normalized receipt body.</cons>
    </option>
    <option id="e2b-sandboxes">
      <name>E2B sandboxes</name>
      <pros>Purpose-built for agent code execution with an explicit pause and resume, which maps very cleanly onto "hibernating". Sandboxes are ephemeral by design, so cleanup assertions are simple.</pros>
      <cons>The product is SDK-first for Python and JavaScript; the raw HTTP surface is the less-travelled path and is likelier to shift under us. It is also a narrower execution model than a general machine, which makes it a weaker demonstration that the contract is genuinely provider-neutral.</cons>
    </option>
    <option id="ec2-hibernate">
      <name>AWS EC2 instance hibernation</name>
      <pros>Hibernation is a literal, documented instance state with true RAM persistence, and Sean may already have an AWS account.</pros>
      <cons>Heaviest of the three by a wide margin — IAM, VPC, AMI and key management before a single task runs — and hibernation carries real prerequisites on instance family and encrypted root volume. Slow start times would dominate the equivalence run, and the blast radius of a mis-scoped credential is far larger than a scratch Fly org.</cons>
    </option>
    <option id="no-cloud">
      <name>Decline the cloud leg for this phase</name>
      <pros>Zero cost, zero third-party dependency, and the other three legs still prove the contract is genuinely multi-surface.</pros>
      <cons>Success Criterion 1 explicitly names a hibernating cloud backend, so it would be recorded NOT MET, and Phase 28 would inherit the gap.</cons>
    </option>
  </options>
  <resume-signal>Select: fly-machines, e2b-sandboxes, ec2-hibernate, or no-cloud — and, if a vendor is selected, confirm `WAYLAND_F25_CLOUD_TOKEN` and `WAYLAND_F25_CLOUD_ORG` are set on hetzner-dsm.</resume-signal>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f .planning/phases/25-remote-reach-nodes-plugin-lifecycle/25-01-CLOUD-BACKEND-DECISION.md</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/grep -qiE 'fly-machines|e2b-sandboxes|ec2-hibernate|no-cloud' .planning/phases/25-remote-reach-nodes-plugin-lifecycle/25-01-CLOUD-BACKEND-DECISION.md &amp;&amp; /usr/bin/grep -qiF 'hibernat' .planning/phases/25-remote-reach-nodes-plugin-lifecycle/25-01-CLOUD-BACKEND-DECISION.md &amp;&amp; echo "one option is recorded as authorized and its hibernation mechanism is named"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git status --porcelain -- crates/wcore-exec-backend/src/backends/cloud.rs | /usr/bin/grep -q . &amp;&amp; { echo "cloud backend code was written before the authorization"; exit 1; } ; echo "no cloud-backend code exists yet, as required"</automated>
  </verify>
  <done>Sean has selected exactly one of the four options and it is recorded in `25-01-CLOUD-BACKEND-DECISION.md` together with the named hibernation mechanism, the expected cost of this phase's runs, and the throwaway org or app the token is scoped to. No cloud-backend production code existed before the authorization was given. If `no-cloud` was selected, the file records that Success Criterion 1 will be NOT MET and Task 3 implements SSH only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the SSH and hibernating-cloud reference backends against the same contract</name>
  <files>crates/wcore-exec-backend/src/backends/ssh.rs, crates/wcore-exec-backend/src/backends/cloud.rs, crates/wcore-exec-backend/src/backends/mod.rs, crates/wcore-exec-backend/tests/conformance_matrix.rs, crates/wcore-config/src/config.rs</files>
  <read_first>crates/wcore-exec-backend/src/contract.rs and crates/wcore-exec-backend/src/conformance.rs (this task's acceptance is that these two files need no change to accommodate two new transports — if they do, the contract was not provider-neutral and that is the finding), crates/wcore-config/src/shell.rs or the module AGENTS.md calls `wcore_config::shell` (the exact signature of the argv-mode helper and of the shell-string helper, and which one this task is allowed to use), crates/wcore-sandbox/src/backends/process_tree.rs (`ProcessTreeGuard`, `isolate`, `request_graceful_shutdown` — the LOCAL ownership semantics the remote side has to reproduce on the far end of the connection), crates/wcore-egress/src/client.rs and crates/wcore-egress/src/request.rs (how a caller builds an outbound request through the chokepoint), the authorized vendor's machine-lifecycle API reference as recorded in 25-01-CLOUD-BACKEND-DECISION.md</read_first>
  <behavior>
    - The SSH backend passes the same `conformance.rs` harness as local and container, with no harness change.
    - Every argument the SSH backend passes — host, remote path, task payload, nonce — travels as a separate argv entry, and a value containing shell metacharacters reaches the remote program as literal bytes rather than being interpreted.
    - Cancelling an SSH task terminates the REMOTE process group, not merely the local connection: after cancellation the remote host shows no process matching the task nonce.
    - The cloud backend passes the same harness, and its receipt attests a machine identity plus the observed hibernation state transitions.
    - Cancelling a cloud task stops or destroys the remote machine, and the vendor's own listing for the scratch org then shows no machine carrying the task nonce.
    - A cloud task whose credential is absent fails CLOSED with an explicit unavailable verdict from `backend list`, and never falls back to another backend.
    - Neither backend introduces a code path that constructs an HTTP client outside `wcore-egress`.
  </behavior>
  <action>Implement `backends/ssh.rs` by driving the system `ssh` client through `wcore_config::shell` in ARGV mode. AGENTS.md is explicit that argv mode is the only safe mode for any command whose arguments include non-literal data, and that shell-string mode must never receive `format!`-interpolated values; a remote-execution backend is the single most attacker-adjacent place in this phase, so there is no shell-string path here at all. Do not add a pure-Rust SSH crate: it would be a new dependency buying nothing this criterion needs, and the two live targets are already reachable with existing keys.

Make remote cancellation real. The local backends inherit process-tree ownership from `wcore-sandbox`; over SSH nothing inherits anything, so the remote side must start its work in its own process group or session and cancellation must signal that group on the far end. Closing the local connection is NOT cancellation and must not be mistaken for it — plan 25-04 will look at the remote process table and a connection-close implementation will be caught there. Carry the per-task nonce onto the remote side so that scan has something to look for.

Implement `backends/cloud.rs` against the vendor authorized in Task 2, using ONLY `wcore-egress` for HTTP. Read the credential from the environment names recorded in this plan's `user_setup`; never log the token, never place it in a receipt, and never let it reach an event body. Attest the machine identity in the receipt and record the observed hibernation transitions — the criterion says HIBERNATING, so the transitions are part of the evidence, not decoration. Label or tag every machine with the task nonce.

If EITHER new backend cannot be made to pass `conformance.rs` without changing `conformance.rs`, that is the important result of this task: the contract was not provider-neutral. Record exactly which surface leaked, and treat a contract change as a HIGH finding requiring the whole conformance matrix to be re-run, not as a routine edit.

Extend the `[execution_backends]` configuration in `wcore-config` with the ssh and cloud entries. Write tests FIRST, watch them fail, then implement. Records evidence for F25-01 and F25-02; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; git fetch origin plan/f20-unified-audit-repair &amp;&amp; git checkout -q FETCH_HEAD &amp;&amp; cargo clippy -p wcore-exec-backend --all-targets --all-features -- -D warnings'</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; cargo nextest run -p wcore-exec-backend --no-fail-fast'</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -cF 'shell_command_argv' crates/wcore-exec-backend/src/backends/ssh.rs)" -ge 1</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -cE 'reqwest::Client::(new|builder)' crates/wcore-exec-backend/src/backends/cloud.rs)" -eq 0</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git diff --unified=0 -- crates/wcore-exec-backend/src/conformance.rs | /usr/bin/grep -E '^[+-][^+-]' ; test $? -ne 0 &amp;&amp; echo "the conformance harness accepted two new transports unchanged"</automated>
  </verify>
  <done>SSH and cloud reference backends both pass the unmodified conformance harness; every SSH argument travels as a separate argv entry with no shell-string path in the module; SSH cancellation signals the remote process group and carries a task nonce onto the far end; the cloud backend routes all HTTP through `wcore-egress`, attests machine identity plus observed hibernation transitions, tags machines with the task nonce, keeps the credential out of every log, event and receipt, and fails closed with an explicit unavailable verdict when the credential is absent.</done>
</task>

<task type="auto">
  <name>Task 4: Prove Success Criterion 1 live — one task, four real backends, four real receipts, one diff</name>
  <files>crates/wcore-exec-backend/tests/live_equivalence.rs, .planning/phases/25-remote-reach-nodes-plugin-lifecycle/25-01-EQUIVALENCE-EVIDENCE.md</files>
  <read_first>crates/wcore-exec-backend/src/receipt.rs (the normalization function — the diff's verdict is only as good as what it refuses to normalize away), crates/wcore-eval-scenarios/src/runner.rs (`discover_binary` and the `WCORE_EVAL_BIN` convention, so the live legs drive the SAME binary artifact every time), .planning/HANDOFF-2026-07-26-phase20-20A-complete.md sections 3 and 7 (the environment traps: the trailing-space `set` trap, the rtk grep line-dropping, the pinned fetch refspecs, and the fact that the proof green in 20A depended on `--nocapture` forcing serial execution because `admit_delegated_backend` rejects under parallel load)</read_first>
  <behavior>
    - One deterministic task definition is executed once per backend through the shipped binary, and the four receipts verify individually.
    - The normalized bodies of the four receipts are equal, and the diff report names every field that differed and classifies each as expected-divergent or unexpected.
    - Cancelling the same task mid-flight on each backend produces a cancelled terminal status on each, and each leg records the cleanup observation the backend is responsible for.
    - Every leg names the host it ran on.
    - A backend that is genuinely unavailable on a host is reported UNEXERCISED with its reason and the command that would close it; it is never silently skipped and never counted as a pass.
  </behavior>
  <action>Write ONE deterministic task definition — fixed workspace bytes, fixed input, a computation whose output is a function of the input alone, and one small artifact — and place it under the crate's test fixtures. Determinism is what makes the diff meaningful; a task whose output embeds a timestamp, a hostname or a random value cannot prove equivalence.

Build the release binary on hetzner-dsm and run the task FOUR times through the shipped binary, once per backend, using the operator surface Task 1 added and nothing else: `wayland-core backend run --backend <name> --task <path to the fixture> --receipt-out <path>` for each of `local`, `container`, `ssh` and the authorized cloud backend. The SSH leg targets a real second host — use `SeanD@seandesktop` if it is reachable, otherwise a loopback SSH into hetzner-dsm itself, and record which was used, because a loopback leg proves the transport but not the cross-host case and must not be reported as if it did. Verify each receipt with `wayland-core backend receipt verify`, then produce the four-way diff.

The OBSERVABLE OUTCOME that distinguishes working from broken, stated so it cannot be fudged: all four receipts verify; all four normalized bodies are byte-equal; the input digest, workspace digest and artifact digest are identical across all four; and the fields that differ are exactly backend identity, instance identity, transport and timing — nothing else. Any other differing field is a finding.

Then run the cancellation leg on each backend: start the same task, cancel it through `wayland-core backend cancel --task-id <id>` while it is still running, and confirm the terminal status is cancelled on all four. Record for each what cleanup was observed. Deep cleanup and orphan-table proof belong to plan 25-04; this task records the observation, and 25-04 prosecutes it hostilely.

Add the Windows leg: on `SeanD@seandesktop`, build and run the `local` backend and the `ssh` backend legs and diff those two receipts against the Linux ones. Windows local containment differs materially from Linux, so this is where a normalization that is too aggressive will show up. Docker on SeanDesktop is UNKNOWN — probe it, do not assume it; if absent, record the container leg there as unexercised with the reason.

Write everything into `25-01-EQUIVALENCE-EVIDENCE.md`: the task definition digest, all four receipts inline, the diff verdict, the cancellation observations, the host name for every leg, and an explicit list of any unexercised surface with its blocking reason. Then add `tests/live_equivalence.rs` as the repeatable, binary-driving regression form of the same run, gated behind an explicit opt-in so the cheap CI floor never dials a cloud vendor.

Marks F25-01 and F25-02 complete IF and ONLY IF all four legs ran and the diff verdict is equivalent. If the cloud leg did not run, mark neither complete, record termination state 2, and state in the SUMMARY that Success Criterion 1 is NOT MET.</action>
  <verify>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; git fetch origin plan/f20-unified-audit-repair &amp;&amp; git checkout -q FETCH_HEAD &amp;&amp; cargo build --release --locked -p wcore-cli --bin wayland-core &amp;&amp; ./target/release/wayland-core backend list'</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f .planning/phases/25-remote-reach-nodes-plugin-lifecycle/25-01-EQUIVALENCE-EVIDENCE.md</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; for leg in local container ssh cloud; do test "$(/usr/bin/grep -ciF "$leg" .planning/phases/25-remote-reach-nodes-plugin-lifecycle/25-01-EQUIVALENCE-EVIDENCE.md)" -ge 1 || { echo "backend leg unaccounted: $leg"; exit 1; }; done; echo "all four backend legs are accounted for"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; for k in "hetzner-dsm" "cancel" "normalized"; do test "$(/usr/bin/grep -ciF "$k" .planning/phases/25-remote-reach-nodes-plugin-lifecycle/25-01-EQUIVALENCE-EVIDENCE.md)" -ge 1 || { echo "evidence missing: $k"; exit 1; }; done; echo "hosts, cancellation and the normalized diff are all recorded"</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; cargo nextest run -p wcore-exec-backend --test live_equivalence --run-ignored only --no-fail-fast'</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command "Set-Location C:\ferrox-win; cmd /c \"C:\Users\seand\.cargo\bin\cargo.exe build --release --locked -p wcore-cli --bin wayland-core 2>&amp;1\""'</automated>
  </verify>
  <done>Four receipts exist, produced by four runs of the SHIPPED binary on named hosts through `wayland-core backend run` and nothing else. Each verifies. Their normalized bodies are byte-equal and the input, workspace and artifact digests match across all four. The only differing fields are backend identity, instance identity, transport and timing, and every other difference is recorded as a finding. Cancellation was driven through `wayland-core backend cancel` on each backend and each reached a cancelled terminal status with its cleanup observation recorded. The Windows local and ssh legs ran on SeanDesktop and were diffed against the Linux legs. Any unexercised surface is named with its blocking reason and the command that would close it. `25-01-EQUIVALENCE-EVIDENCE.md` holds all of it, and `tests/live_equivalence.rs` is the repeatable form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/CLI → execution backend | A task definition, its workspace bytes and its resource request cross into a backend that may run them off-box |
| Core → remote host (SSH) | Arguments, task payload and cancellation signals cross a network boundary into a shell-capable remote environment |
| Core → cloud vendor API | A bearer credential and task material cross into a third party over HTTPS |
| plugin → execution-backend registry | A third-party plugin declares a backend spec the host will translate into real execution authority |
| backend → receipt consumer | A receipt asserting what ran, where, under what policy, is trusted by everything downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01-01 | Tampering | `backends/ssh.rs` argument construction | critical | mitigate | Argv mode via `wcore_config::shell` for every remote invocation; no shell-string path exists in the module; verified by a gate asserting the argv helper is present and by a behavior test that a metacharacter-bearing value reaches the far end as literal bytes |
| T-25-01-02 | Information disclosure | `backends/cloud.rs` credential handling | critical | mitigate | Credential read from the environment only; never logged, never placed in an event body, never placed in a receipt; scoped to a throwaway org per the Task 2 checkpoint |
| T-25-01-03 | Spoofing | receipt attestation | high | mitigate | Ed25519 attestation with `key_id` as the SHA-256 of the pinned verifying key, plus rejection of an unpinned backend identity — inherited from the remote-execution oracle's `receipt_tampering_or_unpinned_backend_is_rejected` |
| T-25-01-04 | Elevation of privilege | plugin-declared execution backend | high | mitigate | Spec goes through the same manifest permission gate as the browser spec, at most one registration per plugin, host translates only after `initialize()` returns, and FORBIDDEN_CORE_IMPORTS is extended so a plugin cannot reach the real backend crate |
| T-25-01-05 | Denial of service | resource budgets | high | mitigate | Resource request denied BEFORE task acceptance; streamed output and artifact bytes charged against one shared budget; both inherited from the oracle contract |
| T-25-01-06 | Repudiation | cross-backend equivalence claim | high | mitigate | The normalized receipt body deliberately excludes only backend identity, instance identity, transport and timing; any other normalized-away field is a finding, so the equivalence claim cannot be manufactured |
| T-25-01-07 | Information disclosure | egress from inside a backend | high | mitigate | Egress decision sourced from the `wcore-egress` shared policy rather than re-derived per backend; a gate asserts no raw reqwest client is constructed in the cloud module |
| T-25-01-08 | Tampering | container availability signal | medium | mitigate | Availability answered from the daemon ping rather than socket presence, so a stale socket cannot make the backend claim readiness it does not have |
| T-25-01-09 | Denial of service | cloud vendor outage during the live run | medium | accept | The vendor is a third party; termination state 2 records the leg unexercised with its reason rather than blocking the phase |
| T-25-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan adds NO new third-party crate: every dependency is taken from the existing `[workspace.dependencies]` with `workspace = true`, and a gate asserts no new `name =` entry enters `Cargo.lock`. A genuinely required new dependency is termination state 4 — stop and ask, never add |
</threat_model>

<verification>
- `cargo fmt --all -- --check` clean on the Mac.
- `cargo clippy -p wcore-exec-backend -p wcore-plugin-api -p wcore-cli --all-targets --all-features -- -D warnings` clean on hetzner-dsm.
- `cargo nextest run -p wcore-exec-backend --no-fail-fast` green on hetzner-dsm, with the conformance matrix reporting every backend as either passed or explicitly unavailable-with-reason.
- The shipped binary answers `wayland-core backend list` on hetzner-dsm and on SeanDesktop.
- Four receipts exist from four real runs; the normalized diff verdict is equivalent; every leg names its host.
- No new entry appeared in `Cargo.lock`.
- FORBIDDEN_CORE_IMPORTS names `wcore-sandbox` and `wcore-exec-backend`.
</verification>

<success_criteria>
Success Criterion 1 of Phase 25 is MET when the same deterministic task has run through the shipped `wayland-core` binary on all four reference backends, the four receipts verify and diff to equivalence on everything except backend identity, instance identity, transport and timing, cancellation reached a cancelled terminal status on each, and each leg names the host it ran on. If the cloud leg did not run, the criterion is NOT MET and the SUMMARY says so in those words.
</success_criteria>

<output>
Create `.planning/phases/25-remote-reach-nodes-plugin-lifecycle/25-01-SUMMARY.md` when done.
</output>
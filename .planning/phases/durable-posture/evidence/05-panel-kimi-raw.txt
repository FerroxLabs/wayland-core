• This is a pure analysis question, no tools needed. Answer adversarially, by number, end with PANEL_CHOICE line.

  Let me think carefully.

  Threat model: journal is metadata only (no content), mode 0600 in 0700 dir, reader enforces. Only sealed_prepared_request contains conversation, sealed under a key in keyring or vault.

  Q1: 0600 key file next to 0600 journal in same 0700 dir. Security property: protects against journal exfil alone. Who can read the journal but not the key? Only an attacker who obtains a copy of the journal file but not the key file. Realistic scenarios: backups that include only sessions dir? Backup of whole profile dir would include key. tar of sessions/*.journal — plausible. Disk image — includes key. So the key file stops: (a) someone who copies just the journal file (e.g., a backup pipeline that archives *.journal, a `scp` of the journal for debugging, a crash-report attachment), (b) accidental disclosure of the journal alone. Same-uid attacker or root reads both — zero protection. So it's weak but not pure theatre: it protects the sealed field against partial-copy disclosure. But note: journal body is metadata only anyway; the sealed field is the only secret. Without key, journal = metadata; with key file present alongside, attacker with uid access gets conversation anyway. The real question: is the marginal property worth complexity? Honest answer: marginal. Most realistic exfil of a profile dir takes everything (rsync -a profile/, disk image, tarball of profile). Selective-copy-of-journal-only is real but narrow: debug bundles, ticket attachments, "send me the journal" support flows. Actually that's a genuinely common leak vector — people attach single files to tickets. But if an operator attaches the journal to a ticket, they could also be asked to attach the key. Also under option A, does sealing even matter? The journal minus sealed field contains no content. The ONLY thing the seal protects is the prepared request. With machine-local key, an attacker with fs access as the uid gets everything. So the seal's value under A is exactly: partial-copy protection. That's a real but thin property. Worth noting the alternative: just don't seal and don't journal the prepared request (option D), which achieves the same confidentiality with no key at all.

  Q2: Which option. Fact 5 is a release blocker: headless Linux with no keyring = normal state, and the shipped fix turns durable sessions OFF entirely — process-wide amnesia, no journal, plus fact 8 (no session_id in ready frame, daemon told nobody for three weeks). C is bad: unrecorded execution, silent. B: default fail-closed recreates the old blocker for exactly the population that hit fact 5 — every headless server refuses to run turns unless operator sets a config. That's arguably worse than C for availability, but it forces the decision into the open. But the prompt says fact 5 is a release blocker — meaning turning sessions off was bad; fail-closed-by-default means the product doesn't work out of the box on headless servers. That recreates fact 6's refusal behavior which presumably was itself unacceptable (why else ship the amnesia degrade?). D: journal without seal, no new secret at rest, honest unrecoverable record. D requires schema change (fact 7). D preserves the audit trail (the thing that matters for the ambiguity problem) and never creates a false sense of confidentiality. D's cost: recovery can't replay the dispatch — but in degraded mode there's no key anyway, so replay was already impossible (sealed request unsealable without key... wait, under status quo degrade there's no journal at all. Under D, dispatch record exists but can't be replayed; under A it can be replayed).

  The motivating argument: retry comes from outside; without a record nobody knows if the effect happened. The core need is a RECORD, not replayability. D gives the record with zero new attack surface. A gives record + replay at the cost of a key that is nearly theatre (Q1) and creates downgrade-abuse surface (Q3). Also A normalizes "encryption" that doesn't encrypt against the realistic adversary — compliance theater risk: someone audits, sees XChaCha20-Poly1305, checks a box, wrong.

  So I'd pick D, with a caveat: D should also fix fact 8 (surface degraded mode in the ready frame / host protocol) regardless. And Q4's fork problem remains: journal exists but key gone — D helps: the journal minus sealed field is still readable! Actually under D, journals written in degraded mode are readable without any key. But journals written in NORMAL mode (with keyring key) whose key later disappears — the sealed field is unreadable, rest is fine. So Q4: refuse? No — refuse-to-start bricks servers; the journal is mostly plaintext metadata; the reader can open it, mark dispatch checkpoints with unsealable prepared requests as unrecoverable, and continue. Running "forks the engine from unknowable prior state" — but the alternative (brick) doesn't resolve unknowability either; it just halts. The right move: start, load what's readable, treat unsealable checkpoints exactly like D's unrecoverable dispatch records, and tell the operator. Fail-closed on key loss conflates confidentiality failure with integrity failure — loss of the key doesn't corrupt the journal; digests/checkpoints still valid. Only resume-of-that-specific-dispatch is impossible. Refusing converts a localized unrecoverability into total denial of service — and gives the downgrade attacker a brick button.

  Q3: Downgrade abuse: attacker kills D-Bus/unsets env → under A, product seals with machine-local key — execution IS recorded (journal continues). So A does not give unrecorded execution; it gives execution sealed under a weaker key. The confidentiality loss: attacker who can tamper with env... if attacker can kill the keyring they may also read the profile dir. A converts "refuse+record" into "run+record with weaker seal." C converts into "run+no record" — worst. D: runs + records, unsealed field omitted — recorded execution, and the attacker gains nothing confidential (nothing secret was written). B default: refuse — availability attack (DoS) but no unrecorded execution. Which best denies unrecorded execution? Both A and D record. D additionally denies the attacker any confidentiality downgrade benefit: under A, attacker who can suppress the keyring AND later read the profile dir obtains the conversation (key file + journal). Under normal mode with keyring, key is not in fs, so fs-read alone doesn't yield conversation. So A gives the attacker a path to plaintext that didn't exist before: force degrade → secret gets sealed under fs-resident key → read fs → win. D: force degrade → the secret simply isn't stored. So D strictly dominates A on downgrade abuse. C is the only option granting unrecorded execution. Answer: A enables a confidentiality downgrade (not unrecorded execution); C is the one that grants unrecorded execution; D best denies both.

  Q4: argued above. Both sides: refuse = protects against acting on unknown state, prevents replay/duplicate-effect decisions based on partial view, and "key unavailable" may indicate tampering — proceeding under a possible attack is bad. But: key loss is common and benign (keyring service restart, D-Bus session churn on headless boxes, env changes), refusing bricks the server and is itself a DoS the attacker can trigger (worse: an attacker who can make the keyring unavailable can now halt the product — converting confidentiality attack into availability attack). The journal's integrity doesn't depend on the key: digests verify, metadata is plaintext. Only the sealed field is affected. Right answer: start; open the journal (mode/owner checks still enforced); mark any dispatch checkpoint whose sealed request can't be unsealed as unrecoverable, refuse to RESUME those specifically, require operator decision for them, and log loudly. New turns journal normally if a key is available, else per the degrade policy.

  Q5: Is D's unrecoverable record sufficient for the redelivery ambiguity? The record says "provider call with request digest X dispatched at T, outcome unknown, cannot replay." Does that resolve whether the effect happened? No — it says dispatched, outcome unknown. The ambiguity "did the provider call land / did the tool execute / did the outbound send happen" is NOT resolved by the dispatch record alone. But crucially: neither does A! A sealed record after a crash, when recovery replays it — replay of a prepared request whose outcome is unknown is itself an ambiguity problem: if the provider call already executed, replaying duplicates it. Unless the provider request carries an idempotency key... The prepared request is "the exact prepared provider request" — if it includes a stable idempotency key derived from the checkpoint, replay is safe and A genuinely resolves ambiguity (dedupe at provider). The prompt doesn't say that. Without idempotency, A's replay is "maybe duplicate the effect" — arguably worse than D's "escalate to human." Hmm — but the journal records outcomes after dispatch too: digests, checkpoint records. If the call completed, a post-dispatch checkpoint records the outcome; the ambiguity only exists for crash-in-flight between dispatch and outcome-record. In that window: A lets you re-send the identical request (safe only with idempotency); D tells you "unknowable, human decides." For tool execution and outbound sends (non-idempotent side effects), replay is dangerous; for the provider call, replay may be safe-ish (costs tokens, conversation state consistent if the request is identical and response is just regenerated... actually re-sending the same prepared request just regenerates the assistant response — if the prior call's response was never recorded or used, redoing it is mostly harmless, cost aside. But if the prior response WAS used — e.g., a tool result already sent — digests in the journal would show that, taking us out of the ambiguous window).

  Honest answer: D's record alone doesn't resolve ambiguity; it bounds it and routes it to a decider. A resolves it only if the prepared request embeds an idempotency key; if it does, that's a strong argument for A (or D+idempotency). The question asks "is D actually better than A's real seal, or worse in a way I missed?" The missed way D is worse: under D you permanently lose the ability to resume ANY interrupted turn even when resuming would be safe, and operators facing "unknowable" will retry anyway — humans resolve ambiguity by resending, which duplicates the effect with none of the machinery. Meanwhile A's "real seal" — under machine-local key it's not clearly "real." So: D sufficient? Sufficient to detect, not to resolve. But resolving requires idempotency regardless of A vs D; given idempotency, store the digest and let redelivery dedupe — actually with an idempotency key in the prepared request, even D could allow safe manual retry: redelivered inbound regenerates the same prepared request with same idempotency key, provider dedupes. That weakens A's advantage further.

  Also framing note: the framing conflates two things — the journal as audit trail (metadata, needs no key) vs the sealed prepared request as recovery payload (needs key). Fact 7 (checkpoint requires sealed field) is a self-imposed coupling, not a law of nature; the whole problem dissolves once you decouple them — which is D. Also fact 8 is a bug regardless: silent degrade with no session_id and no persistent notice is unacceptable under any option; degrade must be surfaced in-protocol and on every startup, not once.

  Also mention: B's fail-closed default recreates the blocker. And a note on A's key file: if you do A, at least the key should be provisioned... no, keep it tight.

  Verdict: D, plus fix fact 8, plus treat Q4 per above. PANEL_CHOICE=D.

  One more adversarial note for Q1: the "backup takes only the journal" fiction — actually the honest candidate the prompt names is journal-alone exfil. Realistic? `tar czf backup.tgz $WAYLAND_HOME` — takes key too. Selective exfil: `scp host:.../sessions/x.journal` — plausible in support flows. Crash reporters, log shippers configured to glob `*.journal` — plausible. So property is real but thin. However, also note: under A the seal encrypts exactly one field; metadata (event types, digests, timing) remains plaintext — fine, it's already designed as non-secret. The theatre charge: against the stated adversary of facts 1-3 (someone reading files as uid/root), zero. Against partial-copy, nonzero. Complexity cost: key provisioning, rotation questions, "encrypted" checkbox marketing risk. If the property is wanted, D achieves equivalent confidentiality for free by not writing the secret. That's the killing argument: A spends a key-management lifecycle to protect a field that D simply declines to persist. A only wins if replay-resume has real value — which loops to idempotency.

  Length: be thorough but not bloated. Use numbered sections. End with the exact line.

• First, the framing: it's mostly right, but it smuggles in one assumption that does a lot of work — fact 7. "A recovery checkpoint requires `sealed_prepared_request`" is not a law of nature; it's a self-imposed coupling between two different things: the journal as an *audit trail* (plaintext metadata, needs no key, facts 1–2) and the journal as a *recovery payload* (the sealed request, needs a key). The false dichotomy you named — "no secure store therefore no journal" — exists only because of that coupling. Every option except C is really a different way of cutting it. Also: fact 8 is a bug independent of this decision. A silent degrade with no `session_id` and a one-time stderr line is unacceptable under *any* option; degraded mode must be in the wire protocol and repeated, not printed once at startup three weeks ago.

  ## Q1 — Is the 0600 key file theatre?

  Not pure theatre, but close. Enumerate attackers:

  - **Same-uid attacker or root:** reads the key and the journal. Zero protection. This is the dominant adversary implied by facts 1–3 (the mode/owner enforcement exists precisely because same-uid is the boundary).
  - **Disk image / full-profile backup / `rsync -a $WAYLAND_HOME`:** gets the key too. Zero protection. Your instinct is right — any realistic *wholesale* copy takes the whole 0700 directory.
  - **Journal-alone exfiltration:** the one real win. And it's not wholly fictional, but it's narrower than "a backup": the realistic vectors are *selective* copies — a support engineer asked to "attach the journal to the ticket," a log shipper globbing `*.journal`, a debug-bundle script, `scp host:.../sessions/x.journal`. Those happen. So the property is: *the one secret field survives partial-copy disclosure.*

  Now weigh it. Under option A you are buying that one thin property with a key-management lifecycle: provisioning, persistence, rotation questions, and — worst — the compliance-theatre risk that someone sees "XChaCha20-Poly1305" in an audit and checks a confidentiality box that does not exist against any attacker with filesystem access. And here is the killing point: **the property protects exactly one field, and option D obtains equivalent confidentiality for that field for free by never persisting it.** You don't need a seal to protect a secret you don't write. A's key file is only worth its complexity if the sealed field's *availability* value (replay-resume) is real — see Q5.

  ## Q2 — Which option?

  **D**, with the fact-8 fix as a non-negotiable rider.

  - **C** is indefensible. It converts a confidentiality problem into unrecorded execution and then hides it (fact 8). The motivating argument is correct: the ambiguity doesn't disappear, it gets exported to the platform and the human.
  - **B** is a dodge. Fail-closed-by-default means every keyring-less headless Linux server — your stated normal deployment, and the population that made fact 5 a release blocker — refuses to run turns until an operator finds and sets a config knob. That doesn't resolve the blocker; it renames fact 6's refusal as a "policy" and ships it again. If refusal were acceptable, you never would have shipped the amnesia degrade in the first place. A security default that the standard deployment must disable to function is not a default; it's a support ticket generator that trains operators to paste `degraded_durability = "machine_local_key"` into every config without reading it.
  - **A** spends a key to protect a field you could simply not write, and adds the Q3 downgrade surface.
  - **D** directly serves the actual need. Read the motivating argument again: what the redelivering platform and the operator need is *a record*, not a replay. D preserves the audit trail, creates zero new secret material at rest, and is honest about the one thing it can't do. The schema change (fact 7) is a cost, but it's a cost paid once, in code you control, instead of a key-management burden paid forever by every operator.

  ## Q3 — Downgrade abuse

  Be precise about what each option gives the attacker who suppresses the keyring:

  - **C** is the only option that grants *unrecorded execution*. Full stop. That's the worst outcome on the table.
  - **A** does not grant unrecorded execution — the journal continues. But it grants something subtler: a **confidentiality downgrade oracle**. In normal mode, the seal key lives in the keyring, *not* the filesystem, so an attacker with only filesystem read access gets metadata, never the conversation. Under A, the attacker who can kill the D-Bus session forces the conversation to be sealed under an fs-resident key — and then reads both files. Attack chain: suppress keyring → wait for a turn containing secrets → read profile dir → plaintext conversation. A creates a path to plaintext that *did not exist* before. That's a real, if two-stage, downgrade.
  - **D** gives the attacker nothing. Execution is still recorded (denies the audit-trail attack), and the downgrade causes the secret to be *not stored* rather than *stored weaker*. There is no confidentiality to downgrade.
  - **B** (default) converts the confidentiality attack into an availability attack: suppress keyring → product refuses. DoS, but recorded.

  Ranking for denying the attacker value: **D > B > A > C**. D is the only option where suppressing the keyring changes nothing the attacker cares about.

  ## Q4 — Pre-existing journal, key now unavailable: refuse?

  Both sides, honestly.

  **For refusing:** the engine's on-disk state is its memory of what effects it caused. Starting against a journal you cannot fully interpret means making dispatch/resume decisions from a partial view; "key unavailable" can also be a tamper signal, and proceeding under attack is how duplicates and confused-deputy bugs happen.

  **Against refusing — and I find this decisive:** loss of the key is a *confidentiality and recovery-payload* failure, not an *integrity* failure. The journal's metadata is plaintext; digests verify; the checkpoint chain validates. Nothing about a missing key corrupts the record. Refusing conflates "I can't unseal one field" with "I can't trust the file," and the operational consequence is that a routine event — keyring service restart, D-Bus session churn on a headless box, an env var lost across a supervisor restart — *bricks the server until a human intervenes*. Worse, per Q3's logic, refuse-on-key-loss hands the downgrade attacker a halt button. And notice: refusing doesn't resolve the unknowability either. The prior state stays unknowable; you've just also stopped the world.

  Correct behavior: start. Open the journal (the fact-1 mode/owner enforcement still applies). Mark any dispatch checkpoint whose sealed request cannot be unsealed as **unrecoverable** — exactly D's record type, which is another point in D's favor: it gives you the schema to express this. Refuse to *resume those specific dispatches*, escalate them to the operator, run new turns, and journal them per the degrade policy. Localize the damage to the one thing actually lost.

  ## Q5 — Is D's unrecoverable record sufficient for the redelivery ambiguity?

  No — and this is where you're owed an adversarial point against your own preferred-sounding framing: **a dispatch record resolves detection, not ambiguity.** "Dispatched at T, outcome unknown" tells the operator *that* something may have happened, not *whether* it did. The human who gets that record will resolve the ambiguity the way humans always do — by resending — and now the duplicate effect happens with none of your machinery involved.

  But here is the part you may have missed: **A doesn't resolve it either, unless there's an idempotency key.** Replaying "the exact prepared provider request" after a crash-in-flight is safe only if the provider dedupes it. If the prior call landed and the response was consumed, replay duplicates the effect. The journal's post-dispatch checkpoints shrink the ambiguous window to crash-between-dispatch-and-outcome-record, but inside that window, A's replay and the human's resend are the *same* gamble. A's replay advantage is real only if the prepared request embeds a stable idempotency key derived from the checkpoint — and the facts as given don't say it does.

  And if it does? Then A's advantage over D *still* mostly evaporates, because with an idempotency key, even D's degraded path is safe to retry: the redelivered inbound regenerates the identical prepared request, the provider dedupes, done. The idempotency key — not the seal — is the mechanism that actually kills the ambiguity. If you take one engineering recommendation out of this: add a checkpoint-derived idempotency key to the prepared request, and then D loses nothing to A even in the replay scenario.

  So: D's record is *worse* than A's seal in exactly one way — it structurally forfeits automatic resume of in-flight dispatches. That loss is real but small, it's bounded to a narrow crash window, it's only recoverable by A under an assumption you haven't verified, and it's purchased in A's case with Q1's thin property and Q3's downgrade oracle. Not worth it.

  Net: ship D, fix fact 8 in the same release, handle Q4 by degrading checkpoints rather than refusing, and independently add an idempotency key to prepared requests — that's the actual fix for the ambiguity that motivated all of this.

  PANEL_CHOICE=D

To resume this session: kimi -r session_63023e75-4963-4054-86cf-e15771eb7916

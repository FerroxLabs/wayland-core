---
phase: 21-child-authority-and-budget-inheritance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/21-child-authority-and-budget-inheritance/21-01-ADMISSION-GATE.md
  - .planning/phases/21-child-authority-and-budget-inheritance/21-01-AUTHORITY-CENSUS.md
autonomous: false
requirements:
  - CTRL-01
  - CTRL-02
  - F21-01
  - F21-02
must_haves:
  truths:
    - "PHASE 21'S SUCCESS CRITERION 1 IS ONE PROPERTY OVER ELEVEN DIMENSIONS, NOT ELEVEN FEATURES. The criterion is: a child cannot widen any provider, tool, filesystem, egress, secret, approval, depth, fan-out, time, token, or cost restriction. Eleven dimensions do not mean eleven plans and do not mean eleven per-dimension unit tests. They mean one anti-amplification property, carried by a small number of SEAMS, proved by one hostile corpus. This plan's entire job is to turn the eleven dimensions into a seam-grouped, bounded work list that every downstream plan consumes and none of them may extend."
    - "THIS IS MOSTLY WIRING AND PROOF, NOT GREENFIELD. `crates/wcore-budget/src/execution.rs` already carries tree-shaped parent+child counters for wall-time, tool runtime, processes, agent depth, tokens and cost. `crates/wcore-agent/src/spawner.rs` already carries the child-spawn seam with `ChildBudgetGuard`, `enter_child_budget`, `bind_child_budget` and a `active_child_permits` concurrency semaphore. `crates/wcore-types/src/execution_policy.rs` already makes effective policy OUTPUT-ONLY so an untrusted input deserializes into `ExecutionPolicyRequest` and must pass the resolver. `crates/wcore-egress/src/lib.rs` is already the single outbound-HTTP chokepoint. `crates/wcore-sandbox/src/directory_authority*.rs` is already Phase-20/20A-hardened. Where enforcement exists, the deliverable is the hostile proof that it cannot be widened — NOT a new mechanism."
    - "THE BRIEF'S CENTRAL RECON CLAIM IS WRONG AND THE CENSUS MUST NOT INHERIT IT. The brief states that `intersect_execution_budget` and `intersect_optional` in `crates/wcore-budget/src/execution.rs` are `exactly the intersection primitive F21-01/F21-02 need`. They are not on the child-spawn path at all. `intersect_execution_budget` has exactly ONE caller in the workspace — `crates/wcore-budget/src/execution.rs:301` — and that is the snapshot-RESTORE path, reconciling a captured budget against current policy after a crash. `intersect_caps` in `crates/wcore-budget/src/tracker.rs` is the same story: its only two callers, at `:540` and `:1549`, are both restore paths. The child-spawn seam uses `ExecutionBudgetView::sub_budget(override_)`, whose own doc comment says the override REPLACES the caps on the child and that parent caps still apply for the rollup. That is a DIFFERENT non-wideability mechanism — ancestor rollup, not intersection — and the census must record the mechanism that actually runs, not the one the brief named."
    - "THE ROLLUP MECHANISM IS PLAUSIBLE AND MUST BE MEASURED RATHER THAN TRUSTED. `first_exceeded_reason` walks the leaf and then every ancestor, and `record_tokens`, `record_cost` and the tool-runtime recorder all roll usage up to every ancestor, so a child holding a WIDER cap is still stopped by the ancestor's counter. That is sound for every dimension that has a rollup counter. It is NOT automatically sound for a dimension with no counter, and it is NOT sound for any consumer that reads a LIMIT instead of asking whether a cap is exceeded: `limit_for` resolves through `with_reason_state`, which falls back to rendering the LEAF state when nothing is currently exceeded, so it reports the child's own possibly-wider limit. `minimum_remaining` takes the true minimum across ancestors and is the correct pattern. Whether any admission or budgeting decision anywhere reads `limit_for` rather than `first_exceeded_reason` or `minimum_remaining` is a measurable question and the census must answer it."
    - "TODAY NO PRODUCTION PATH LETS A CHILD REQUEST A BUDGET AT ALL, WHICH MAKES THE PROPERTY CURRENTLY VACUOUS RATHER THAN PROVEN. Every production caller of `sub_budget` passes `None` — `crates/wcore-agent/src/spawner.rs:1176`, `:1200`, and `crates/wcore-agent/src/engine.rs:6180`, `:6189`. The only callers that pass `Some(..)` are tests. So F21-02's `cannot exceed` is satisfied today by the ABSENCE of a request channel, not by an enforced intersection. A property that holds because nobody can ask is a property that breaks silently the day somebody can. The census must state, per dimension, whether a child-request channel exists, on which surface, and what happens on it — and the host-protocol surface is where a request channel is most likely to appear."
    - "THE POLICY LAYER FAILS OPEN BY DESIGN AND THAT IS THE CLEAREST STRUCTURAL GAP. `crates/wcore-permissions/src/policy.rs` states in its own header that the scope is explicit grants only, with no role hierarchy and no inheritance — directly against F21-01's `intersection of parent and requested authority`. Worse, `crates/wcore-agent/src/policy_gate.rs` states that v0.6.0 shipped `wcore-permissions` as ORPHAN code with no consumer calling `PolicyEngine::check`, and that the v0.6.1 gate which fixed it is OPT-IN: sessions that do not configure a `PolicyGate` see every tool run. `crates/wcore-agent/src/engine.rs` constructs `policy_gate: None` at both production constructors (`:3147`, `:3381`). So the tool-authority dimension has an enforcement mechanism that is off unless something turns it on. The census must record whether any production bootstrap turns it on, and if nothing does, that is a finding with a severity, not a footnote."
    - "THE EGRESS POLICY IS PROCESS-GLOBAL AND ITS DEFAULT IS PASS-THROUGH. `crates/wcore-egress/src/policy.rs` installs the real policy through a process-global `OnceLock` (`install_global_policy`), and the crate header records that the shipped default is `AllowAllPolicy` pass-through with the real allowlist landing later. A process-global policy has no per-child dimension to widen — but `EgressClient::new().with_policy(..)` lets any call site attach an EXPLICIT per-client policy that never consults the global one, and that is a widening route that exists today. The census must state whether the egress dimension is (a) not per-actor and therefore not wideable, (b) wideable through explicit per-client policy attachment, or (c) both, and it must name the exact call sites."
    - "THE ADMISSION GATE IS A REAL GATE AND ITS STATE IS VERIFIED AT EXECUTION TIME, NEVER ASSUMED. ROADMAP.md:83 gates BROAD Phase 21 execution on CTRL-02/D1 — the linked Desktop plan and consumer/reducer conformance harness pinned to the exact Core producer contract — AND on CTRL-01, the Competitive Capability Ledger with pinned exact Hermes and OpenClaw baselines and F03/F05 evidence mapped. Both are being worked separately and their state may have changed since this plan was written. This plan READS the three intel artifacts and reports what they actually say at execution time. It does not assume the gate is open and it does not assume it is closed."
    - "A REPORTED RED IS WORTH FAR MORE THAN AN ENGINEERED GREEN. Never weaken an assertion, `#[ignore]`, `#[allow]`, re-gate, delete a test, or raise a timeout to reach a gate. In a census plan the equivalent forgery is recording a dimension as ENFORCED because a mechanism exists in the source, without having established that the mechanism is reachable on a production path. An unreachable mechanism recorded as enforcement is exactly how `wcore-permissions` spent a whole version as orphan code."
  artifacts:
    - path: .planning/phases/21-child-authority-and-budget-inheritance/21-01-ADMISSION-GATE.md
      provides: "The execution-time state of the CTRL-01 and CTRL-02/D1 admission gate, read from the three intel artifacts rather than assumed, with an explicit OPEN or NOT-OPEN verdict per control"
    - path: .planning/phases/21-child-authority-and-budget-inheritance/21-01-AUTHORITY-CENSUS.md
      provides: "The eleven-dimension authority census — per dimension the carrying seam, whether a child-request channel exists and on which surface, the enforcement mechanism that actually runs, its reachability on a production path, and a verdict — plus the seam grouping that bounds every downstream plan, and the recorded fan-out determination"
  key_links:
    - from: .planning/phases/21-child-authority-and-budget-inheritance/21-01-AUTHORITY-CENSUS.md
      to: .planning/phases/21-child-authority-and-budget-inheritance/21-02-PLAN.md
      via: "the seam grouping and the per-dimension widening attempt, which is the sole authorized source of hostile-corpus cases"
      pattern: "census-bounds-corpus"
    - from: .planning/phases/21-child-authority-and-budget-inheritance/21-01-ADMISSION-GATE.md
      to: .planning/phases/21-child-authority-and-budget-inheritance/21-01-PLAN.md
      via: "the blocking admission checkpoint, which decides whether broad execution proceeds, proceeds scope-limited, or holds"
      pattern: "decision-record"
---

<objective>
Establish the two things every downstream Phase 21 plan depends on and neither may re-derive: the execution-time state of the broad-execution admission gate, and the eleven-dimension authority census that turns Success Criterion 1 from a list of words into a bounded, seam-grouped work list.

Purpose: Success Criterion 1 names eleven dimensions — provider, tool, filesystem, egress, secret, approval, depth, fan-out, time, token, cost. Written naively that is eleven workstreams and Phase 20's seventy-four plans all over again. Written correctly it is ONE anti-amplification property carried by a handful of seams. Recon has already established that most of the enforcement exists and that the brief's account of WHICH primitive enforces it is wrong on the central point. Getting the census wrong propagates that error into the corpus, the repair set and the phase verdict, so the census is measured against the source and against reachability, never inherited from any brief including this one.
Output: An admission-gate verdict read from the real intel artifacts; an eleven-row census with a per-dimension seam, request channel, enforcement mechanism, reachability and verdict; a recorded fan-out determination that either names a genuine gap or records that the existing concurrency cap already satisfies the requirement; and one blocking admission decision. No production code, in any circumstance.
</objective>

<execution_context>
@$HOME/.claude/ferrox-core/workflows/execute-plan.md
@$HOME/.claude/ferrox-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/HANDOFF-2026-07-26-phase20-20A-complete.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/intel/COMPETITIVE-LEDGER.md
@.planning/intel/DESKTOP-PROTOCOL-CHECKPOINT.md
@.planning/intel/FIELD-REGRESSIONS.md
@crates/wcore-budget/src/execution.rs
@crates/wcore-budget/src/tracker.rs
@crates/wcore-agent/src/spawner.rs
@crates/wcore-agent/src/budget_authority.rs
@crates/wcore-agent/src/policy_gate.rs
@crates/wcore-permissions/src/policy.rs
@crates/wcore-types/src/execution_policy.rs
@crates/wcore-types/src/spawner.rs
@crates/wcore-egress/src/policy.rs
@crates/wcore-agent/src/egress/policy.rs
@crates/wcore-agent/src/approval.rs
</context>

<execution_rules>

**THE TWO AMENDED PHASE RULES — stated verbatim, and they bound this plan.**

- Findings at CRITICAL or HIGH must be fixed or disproved. MEDIUM and below are logged to BACKLOG and do not block execution.
- Execution begins when no CRITICAL or HIGH finding is open, or after 2 review rounds, whichever comes first. A third round is not permitted; it escalates to Sean.

**NON-NEGOTIABLE, stated verbatim.** A REPORTED RED IS WORTH FAR MORE THAN AN ENGINEERED GREEN — never weaken an assertion, `#[ignore]`, `#[allow]`, re-gate, delete a test, or raise a timeout to reach a gate.

**THE PHASE PLAN CAP.** Phase 21 is capped at FOUR plans, total, for the same reason Phase 20A was: Phase 20 metastasised to seventy-four plans because two finding rules had no fixed point, and Phase 20A did the same class of work in four once the rules were bounded and a cap was set. This plan may not create a fifth plan, may not propose one, and may not extend its own task list. If the census discovers work that does not fit the four plans, it is recorded in the census as OUT-OF-PHASE with a severity and it goes to BACKLOG or to Sean — it does not become a plan.

**TERMINATION CRITERION FOR THIS PLAN (hard — the plan STOPS rather than spawning more work).** This plan reads, measures, records and asks ONCE. It terminates in exactly one of four states, and in all four it writes its SUMMARY and stops:
1. **Complete — gate open.** Both controls verified open at execution time, the census is complete across all eleven dimensions, the fan-out determination is recorded, and Sean authorized broad execution at the checkpoint. Write the SUMMARY and stop.
2. **Complete — gate not open, scope-limited authorization.** One or both controls verified NOT open, and Sean authorized a scope-limited continuation naming exactly which downstream plans may proceed and which may not. The scope limit is recorded verbatim in the admission-gate artifact. Write the SUMMARY and stop.
3. **Held — gate not open, no authorization.** One or both controls verified NOT open and Sean elected to hold. Phase 21 does not proceed past this plan. The census still ships, because it is valid work and it will be the input when the gate opens. Write the SUMMARY and stop.
4. **Escalated — the census cannot be completed from the source.** A dimension cannot be resolved to a seam, a mechanism or a reachability verdict without work that exceeds this plan. Record every dimension that IS resolved, name the unresolved one with exactly what is missing, and escalate. Do NOT start an investigation to resolve it. Write the SUMMARY and stop.
Under no circumstances does this plan write production code, modify any file under `crates/`, create a fifth Phase 21 plan, or start a second census cycle.

**THE BROAD-EXECUTION ADMISSION GATE.** ROADMAP.md:83 gates broad Phase 21 execution on CTRL-02/D1 — the linked Desktop plan and consumer/reducer conformance harness pinned to the exact Core producer contract — AND on CTRL-01, the Competitive Capability Ledger with pinned exact Hermes and OpenClaw baselines and F03/F05 evidence mapped. Both are being worked separately. PLANNING was never gated; BROAD EXECUTION is. This plan is the one that verifies the gate's state AT EXECUTION TIME by reading the three intel artifacts, rather than assuming either state. Downstream plans 21-02, 21-03 and 21-04 each restate this gate and each waits on the authorization recorded here.

**READ-ONLY, AND GATE-CHECKED TO BE SO.** This plan modifies nothing under `crates/`. Its verification includes a working-tree cleanliness check over `crates/` precisely so a census cannot quietly become a repair.

**THE PHASE IS PROVED ON THE REAL PRODUCT, AND THIS PLAN MAKES THAT POSSIBLE.** Phase 21's Success Criterion 1 is a SECURITY claim: a child cannot widen any provider, tool, filesystem, egress, secret, approval, depth, fan-out, time, token or cost restriction. A unit test asserting that a guard function returns false is weak evidence for a claim of that shape — it proves the guard, not the product. The strong evidence is attempting the actual widening through the actual shipped binary and observing the actual refusal at the surface a user touches. Downstream plans do that, and they can only do it if this census records, per dimension, HOW A USER REACHES IT THROUGH THE REAL PRODUCT. The shipped surfaces are grounded and named: the binary is `wayland-core` (`crates/wcore-cli/Cargo.toml`, `[[bin]] name = "wayland-core"`, `path = "src/main.rs"`); the host-protocol surface is `wayland-core --json-stream`, already driven end to end by `crates/wcore-eval-scenarios/src/runner.rs`; the standalone headless surface is `wayland-core -p "<prompt>"` or `--no-tui`; and the interactive TUI is the bare binary on a real PTY, driven by `crates/wcore-eval-scenarios/src/pty_capture.rs` with `crates/wcore-cli/tests/harness_tui_flow.rs` as the in-repo precedent. `crates/wcore-cli/src/main.rs` decides between them at the `tui_capable` gate — the TUI is entered only when the prompt is empty, `--no-tui` is absent, stdout is a terminal, and `--json-stream` is absent. Hermetic fixtures come from `crates/wcore-eval-scenarios/src/tempenv.rs`, which seeds a throwaway `.wayland-core/config.toml` and points `WAYLAND_HOME` at it. The census records which of these surfaces carries each dimension; it does not run any of them.

**RECORD THE MECHANISM THAT RUNS, NOT THE ONE THAT EXISTS.** For every dimension the census must distinguish four states and must never collapse them: ENFORCED (a mechanism runs on a production path and refuses widening), VACUOUS (no child-request channel exists, so the property holds by absence rather than by enforcement), UNREACHABLE (a mechanism exists in the source but no production path invokes it), and ABSENT (no mechanism). VACUOUS and UNREACHABLE are the two states that read as safe and are not, and they are the two this codebase has already produced once each — `sub_budget` is only ever called with `None` in production, and `wcore-permissions` shipped a whole version as orphan code.

**ENVIRONMENT.**
- Linux build and test: `ssh -o BatchMode=yes hetzner-dsm`, checkout `/root/wayland`. The full aggregate is roughly 194 seconds for 11520 tests.
- Windows (informational only — this census plan runs nothing on a host): `ssh SeanD@seandesktop`, with downstream plans working in the phase's dedicated worktree `C:\ferrox-win-p21`. THE DEFAULT SHELL THERE IS POWERSHELL. Cargo is at `C:\Users\seand\.cargo\bin\cargo.exe` and must not be assumed to be on `PATH`. Quoting through SSH into PowerShell breaks on naive nesting, so NEVER inline a nested-quote command: write the whole run as a `.ps1`, encode it base64-as-UTF-16LE, and invoke `powershell -NoProfile -EncodedCommand <b64>`. There are TWO registered Windows runners, `ferrox-win-msvc` among them, but they resolve to ONE physical box — the 20A green ran as `ferrox-win-msvc`/SEANDESKTOP. Concurrent compile load has already corrupted one proof run, so take every recorded result from a single quiet run, and work in a worktree dedicated to this plan rather than in a checkout another agent may be using.
- This Mac CAN compile the workspace but is also a registered CI runner; prefer the remote hosts. `cargo fmt --all` works here and FAILS on Windows with os error 206.
- Mac `grep` and `ls` are rtk-proxied and SILENTLY DROP LINES — measured at 32 returned versus 674 on one file. Always `/usr/bin/grep`, always `-F` for literals, and `/usr/bin/git` for git.
- The Mac repo's remote `origin` is a STALE LOCAL WORKTREE; the real remote is `gh`. Never fetch or reset against `origin` here. On the remote hosts `origin` IS correct.
- Both hosts' fetch refspecs are pinned to an unrelated branch, so `git fetch --all` silently misses this one. Always `git fetch origin plan/f20-unified-audit-repair`.
- Windows CI runs clippy with `-D warnings` BEFORE tests, so any lint failure means the tests never execute at all.
- No push to main, no merge, no PR, no tag, no release and no issue closure without Sean.

**AGENTS.md discipline.** No drive-by edits of any kind. This plan touches two files, both under `.planning/`.

**Git hygiene.** Use `/usr/bin/git` on the Mac. Stage the exact paths in `files_modified`, never `-A`, never `.`. Never stage `AGENTS.md` or `.ijfw` churn. No `Co-Authored-By` trailers.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Verify the broad-execution admission gate's state at execution time from the real artifacts</name>
  <files>.planning/phases/21-child-authority-and-budget-inheritance/21-01-ADMISSION-GATE.md</files>
  <read_first>.planning/ROADMAP.md (line 83, the admission prerequisite as written, and the Phase 21 block above it), .planning/REQUIREMENTS.md (the CTRL-01 and CTRL-02 requirement text at lines 10 and 11, and the program-controls tally that maps each control to its intel artifact), .planning/intel/COMPETITIVE-LEDGER.md (the whole file — it states its own open condition in its header), .planning/intel/DESKTOP-PROTOCOL-CHECKPOINT.md (the D1 and D2 sections), .planning/intel/FIELD-REGRESSIONS.md (for CTRL-03 context only, which does not gate this phase)</read_first>
  <behavior>
    - CTRL-01's state is read from `.planning/intel/COMPETITIVE-LEDGER.md` against the condition that file states for itself, not against a remembered summary.
    - CTRL-02/D1's state is read from `.planning/intel/DESKTOP-PROTOCOL-CHECKPOINT.md` against the D1 condition ROADMAP.md:83 states.
    - Each control gets exactly one verdict — OPEN or NOT-OPEN — with the quoted evidence that produced it.
    - A NOT-OPEN verdict names precisely which clause is unmet, so the checkpoint in Task 3 has something concrete to decide about.
    - The artifacts' own last-modified state and any version or date they carry is recorded, so a stale artifact cannot pass as a current one.
    - No control is recorded OPEN on the basis of intent, plan, or in-progress work. Open means the stated condition is met now.
  </behavior>
  <action>Read ROADMAP.md line 83 and record the admission prerequisite verbatim as the standard being applied. Read the CTRL-01 and CTRL-02 requirement text from `.planning/REQUIREMENTS.md` and record each verbatim.

Then read the two intel artifacts that carry those controls and determine each control's ACTUAL state right now. For CTRL-01, `.planning/intel/COMPETITIVE-LEDGER.md` states its own closure condition in its header; apply that condition to the file's own contents and additionally apply the ROADMAP clause requiring pinned exact Hermes and OpenClaw baselines with F03/F05 evidence mapped. For CTRL-02/D1, apply the ROADMAP clause requiring the linked Desktop plan and the consumer/reducer conformance harness to be pinned to the exact Core producer contract, and determine from `.planning/intel/DESKTOP-PROTOCOL-CHECKPOINT.md` whether that pinning exists.

Record for each control: the standard verbatim, the evidence quoted from the artifact, the verdict OPEN or NOT-OPEN, and — when NOT-OPEN — the precise unmet clause. Record each artifact's own version or date marker and its git last-commit date, so nobody later mistakes a stale artifact for a current verdict.

Do NOT attempt to close either control. Closing CTRL-01 or CTRL-02 is separate work being done elsewhere, and pulling it into this plan is exactly the scope metastasis the four-plan cap exists to prevent. Do NOT record a control OPEN because work toward it is under way; open means the stated condition is met at the moment of reading.

Write it all to `21-01-ADMISSION-GATE.md`.

Records evidence for CTRL-01 and CTRL-02; marks no requirement complete.</action>
  <verify>
    GATE CONTRACT for `21-01-ADMISSION-GATE.md`, field separator " :: ". The artifact must carry exactly two rows `GATE-VERDICT :: CTRL-01 or CTRL-02 :: OPEN or NOT-OPEN :: [path of the intel artifact read]`; at least two rows `EVIDENCE :: [intel artifact path] :: [one line quoted from that artifact, verbatim]`; the live last-commit SHA of each intel artifact it read; and one row `BASE-SHA :: [the commit the reading was taken at]`. No gate below greps this artifact for a token it is supposed to contain, because typing the token would pass it. Each gate resolves the artifact's claims against ground truth the executor cannot invent: a real file path, a real commit SHA, a real verbatim line.
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f .planning/phases/21-child-authority-and-budget-inheritance/21-01-ADMISSION-GATE.md</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; A=.planning/phases/21-child-authority-and-budget-inheritance/21-01-ADMISSION-GATE.md; R=$(/usr/bin/awk -F' :: ' '$1=="GATE-VERDICT"{n++; if ($2!="CTRL-01" &amp;&amp; $2!="CTRL-02") bad++; if ($3!="OPEN" &amp;&amp; $3!="NOT-OPEN") bad++; if (system("test -f \"" $4 "\"")!=0) bad++} END{print n+0 "," bad+0}' "$A"); test "$R" = "2,0" || { echo "FAIL: expected exactly two rows [GATE-VERDICT :: CTRL-01 or CTRL-02 :: OPEN or NOT-OPEN :: an intel artifact path that exists]; got rows,malformed = $R"; exit 1; }; echo "both gating controls carry a closed-set verdict naming a real intel artifact"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; A=.planning/phases/21-child-authority-and-budget-inheritance/21-01-ADMISSION-GATE.md; ROWS=$(/usr/bin/awk '/^EVIDENCE :: /{print substr($0,13)}' "$A"); test "$(printf '%s\n' "$ROWS" | /usr/bin/grep -c ' :: ')" -ge 2 &amp;&amp; printf '%s\n' "$ROWS" | while IFS= read -r row; do s=${row%% :: *}; q=${row#* :: }; test -f "$s" &amp;&amp; /usr/bin/grep -qF -- "$q" "$s" || { echo "FAIL: quoted evidence is not present verbatim in $s ; the quote was: $q"; exit 1; }; done || { echo "FAIL: fewer than two [EVIDENCE :: intel-path :: verbatim-line] rows, or a quote does not resolve verbatim in the artifact it names"; exit 1; }; echo "every quoted evidence line resolves verbatim inside the intel artifact it cites"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; A=.planning/phases/21-child-authority-and-budget-inheritance/21-01-ADMISSION-GATE.md; test -f "$A" || { echo "FAIL: the admission-gate artifact does not exist"; exit 1; }; C=$(cat "$A"); for f in .planning/intel/COMPETITIVE-LEDGER.md .planning/intel/DESKTOP-PROTOCOL-CHECKPOINT.md; do S=$(/usr/bin/git log -1 --format=%H -- "$f"); test -n "$S" || { echo "FAIL: cannot resolve the last commit of $f"; exit 1; }; case "$C" in *"$S"*) ;; *) echo "FAIL: the admission gate does not record $f at its live last-commit SHA $S, so a stale artifact could pass as a current verdict"; exit 1;; esac; done; echo "both intel artifacts are pinned by their live last-commit SHA"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; A=.planning/phases/21-child-authority-and-budget-inheritance/21-01-ADMISSION-GATE.md; P=$(/usr/bin/awk -F' :: ' '$1=="BASE-SHA"{print $2}' "$A"); test -n "$P" &amp;&amp; /usr/bin/git cat-file -e "$P^{commit}" 2>/dev/null &amp;&amp; /usr/bin/git merge-base --is-ancestor "$P" HEAD &amp;&amp; test -z "$(/usr/bin/git status --porcelain -- crates/)" || { echo "FAIL: the admission gate is missing, or carries no [BASE-SHA :: sha] row naming a real commit reachable from HEAD (found: ${P:-nothing}), or a read-only task modified crates/ . The crates/ leg is a REGRESSION GUARD, not a completion gate; the BASE-SHA leg is the completion gate."; exit 1; }; echo "admission gate pinned to commit $P and crates/ unmodified"</automated>
  </verify>
  <done>`21-01-ADMISSION-GATE.md` exists and carries, for each of CTRL-01 and CTRL-02, the ROADMAP standard verbatim, the evidence quoted from the named intel artifact, a single OPEN or NOT-OPEN verdict, and — where NOT-OPEN — the precise unmet clause. Each source artifact's own version or date marker and its git last-commit date are recorded. Neither control was worked on or closed here. `crates/` is unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Produce the eleven-dimension authority census, seam grouping, and the fan-out determination</name>
  <files>.planning/phases/21-child-authority-and-budget-inheritance/21-01-AUTHORITY-CENSUS.md</files>
  <read_first>crates/wcore-budget/src/execution.rs (`sub_budget` and its doc comment, `first_exceeded_reason`, `check_state`, `limit_for`, `observed_for`, `with_reason_state`, `minimum_remaining`, `record_tokens`, `record_cost`, `enter_agent`, and `intersect_execution_budget` together with its ONE caller so the restore-path scope is confirmed rather than assumed), crates/wcore-budget/src/tracker.rs (`BudgetCap`, `Charge`, `CapWarn`, `CapBlock`, `intersect_caps` and both its callers, `clamp_token_extension`, `clamp_usd_extension`), crates/wcore-agent/src/spawner.rs (`ChildBudgetGuard`, `enter_child_budget`, `bind_child_budget`, `spawn_one_with_active_permit` and the `active_child_permits` semaphore, the provider-resolution path for `SubAgentConfig.provider`, and the `ForkOverrides.allowed_tools` application site), crates/wcore-types/src/spawner.rs (`SubAgentConfig`, `ForkOverrides`, `RequestedChildWorkspace`, `SHARED_READ_ONLY_CHILD_TOOLS`, `ChildPolicySnapshot`, `ChildWorkspaceMode`), crates/wcore-types/src/execution_policy.rs (`ExecutionPolicyRequest` versus `EffectiveExecutionPolicy`, `PolicySource` including its `Child` variant, `resolve_dangerous_launch`, `with_requested_approvals`), crates/wcore-agent/src/policy_gate.rs (the header's opt-in statement and the actor-resolution rules), crates/wcore-permissions/src/policy.rs (the header's explicit-grants-only statement, `Actor`, `Resource`, `Action`, `PolicyEngine::check`), crates/wcore-agent/src/engine.rs (every `policy_gate` construction and assignment site), crates/wcore-egress/src/policy.rs (`install_global_policy` and the process-global `OnceLock`), crates/wcore-agent/src/egress/policy.rs (`AgentEgressPolicy`), crates/wcore-sandbox/src/directory_authority.rs (the filesystem authority surface), crates/wcore-agent/src/approval.rs (the correlation-id bridge), crates/wcore-agent/src/durable_spawner.rs and crates/wcore-agent/src/durable_child.rs (the durable child store and the result-delivery attribution path)</read_first>
  <behavior>
    - All eleven dimensions named by Success Criterion 1 appear in the census: provider, tool, filesystem, egress, secret, approval, depth, fan-out, time, token, cost. None is omitted and none is silently merged into another without an explicit recorded merge and its reason.
    - Each dimension carries: the seam that actually carries it with a file-and-symbol citation; whether a child-REQUEST channel exists and on which surface (standalone spawn, host protocol, ForgeFlows or workflow runner, durable spawner, none); the enforcement mechanism that actually runs; whether that mechanism is reachable on a production path; and one verdict from ENFORCED, VACUOUS, UNREACHABLE or ABSENT.
    - The budget dimensions are recorded against the mechanism that actually runs — ancestor rollup through `sub_budget` plus `first_exceeded_reason` — and NOT against `intersect_execution_budget`, whose only caller is the snapshot-restore path. If the reading of the source contradicts this plan's account, the SOURCE wins and the contradiction is recorded.
    - Whether any admission or budgeting decision anywhere reads `limit_for` rather than `first_exceeded_reason` or `minimum_remaining` is answered by search, not by inference, because `limit_for` falls back to rendering the leaf state and therefore reports the child's own possibly-wider limit.
    - The eleven dimensions are grouped into SEAMS, and the grouping is the deliverable that bounds plan 21-02. A dimension whose seam is shared with another dimension is proved by the same corpus case family, not by its own suite.
    - Each dimension carries exactly one WIDENING ATTEMPT — the concrete hostile request a child would make to try to widen it — stated precisely enough that 21-02 can turn it into a corpus case without inventing anything.
    - Each dimension ALSO carries a LIVE SURFACE entry: the exact `wayland-core` invocation — binary, flags and input — through which a user reaches that dimension, and the observable outcome at that surface that would distinguish an enforced restriction from a widened one. This is what makes the downstream live proof possible, and a dimension without it cannot be proved on the real product.
    - Every LIVE SURFACE entry names one of the four grounded shipped surfaces — `wayland-core --json-stream` (host protocol), `wayland-core -p "<prompt>"` or `--no-tui` (standalone headless), the bare binary on a PTY (interactive TUI), or an explicit NOT-USER-REACHABLE verdict with its reason — and names the platforms it is available on.
    - A dimension recorded NOT-USER-REACHABLE is a finding in its own right with a severity, because a restriction no user surface can exercise is a restriction whose enforcement no user can ever observe.
    - The tool dimension's census entry states whether `ForkOverrides.allowed_tools` is applied as an INTERSECTION with the parent's tools or as a REPLACEMENT, determined from the application site rather than from the field's doc comment.
    - The provider and model dimensions' census entries state what happens when `SubAgentConfig.provider` names a provider the parent does not have, determined from the resolution path.
    - The fan-out determination is settled and recorded with one of exactly three outcomes, and the determination explicitly distinguishes bounded ADMITTED work from bounded PENDING work.
    - Any work the census surfaces that does not fit inside Phase 21's four plans is recorded OUT-OF-PHASE with a severity, and does not become a plan.
  </behavior>
  <action>Build the census by reading the seams, not by reasoning from requirement text. For each of the eleven dimensions — provider, tool, filesystem, egress, secret, approval, depth, fan-out, time, token, cost — answer five questions and record the file-and-symbol evidence for each answer.

First, WHICH SEAM CARRIES IT. Cite the file and the symbol. Several dimensions share a seam; record the sharing rather than duplicating the analysis.

Second, DOES A CHILD-REQUEST CHANNEL EXIST, AND ON WHICH SURFACE. This is the question that decides whether the property is enforced or merely vacuous. Check all of the standalone spawn path, the host-protocol path, the ForgeFlows workflow runner path and the durable spawner path. Note as a specific worked example that every production caller of `sub_budget` passes `None` while only tests pass `Some(..)` — confirm that by search rather than trusting it, and then ask the same question of every other dimension.

Third, WHAT MECHANISM ACTUALLY RUNS. Read it. Do not infer it from a doc comment and do not inherit it from this plan. For the budget dimensions specifically, this plan asserts that the running mechanism is ancestor rollup rather than intersection, and that `intersect_execution_budget` and `intersect_caps` are confined to the snapshot-restore path; confirm both claims against the source and record the result even if it contradicts this plan, because the source is the authority and a brief is not.

Fourth, IS THAT MECHANISM REACHABLE ON A PRODUCTION PATH. This is the question that separates real enforcement from orphan code. The tool dimension is the live example: a `PolicyGate` exists, but its own header records that the previous version shipped `wcore-permissions` with no consumer at all, and the gate that fixed that is opt-in. Search for every production construction and assignment site and state plainly whether anything in a real bootstrap turns it on. An unreachable mechanism recorded as enforcement is the single worst error this census can make.

Fifth, THE VERDICT — exactly one of ENFORCED, VACUOUS, UNREACHABLE or ABSENT, with a severity attached to anything that is not ENFORCED. Apply the amended rules: CRITICAL or HIGH must be fixed or disproved downstream; MEDIUM and below go to BACKLOG and do not block.

Then attach to each dimension exactly ONE WIDENING ATTEMPT: the concrete hostile request a child would make to try to widen that restriction, stated precisely enough that plan 21-02 can turn it into a corpus case with no invention. Some worked starting points, to be confirmed or replaced by what the source actually shows: a child naming a provider the parent has no authority for; a child requesting a tool outside the parent's allowed set and seeing whether `allowed_tools` intersects or replaces; a child requesting a filesystem root outside the parent's directory authority; a child constructing an `EgressClient` with an explicit permissive policy so the process-global policy is never consulted; a child requesting an approval posture weaker than the parent's; a child requesting an agent depth or a cap wider than the parent's; a child requesting more children than the parent's concurrency admission permits.

Then attach to each dimension its LIVE SURFACE. This is the entry that makes the whole phase provable on the real product rather than only in a test harness, so it is not optional and it is not prose. Record the exact `wayland-core` invocation a user would issue to reach that dimension — the binary, the flags and the input — together with the observable outcome at that surface that distinguishes an enforced restriction from a widened one. The shipped surfaces are already grounded in this repo and you are choosing among them, not inventing one: `wayland-core --json-stream` is the host-protocol surface and `crates/wcore-eval-scenarios/src/runner.rs` already drives it end to end; `wayland-core -p "<prompt>"` and `--no-tui` are the standalone headless surface; the bare binary on a real PTY is the interactive TUI, driven by `crates/wcore-eval-scenarios/src/pty_capture.rs` with `crates/wcore-cli/tests/harness_tui_flow.rs` as the in-repo precedent. Read the `tui_capable` gate in `crates/wcore-cli/src/main.rs` so the invocation you record actually lands in the mode you intend — the TUI is entered only when the prompt is empty, `--no-tui` is absent, stdout is a terminal and `--json-stream` is absent, and getting that wrong produces a recorded invocation that silently exercises the line REPL instead.

Record the PLATFORMS each live surface is available on, and be precise rather than optimistic: `pty_capture.rs` carries `#![cfg(unix)]` at line 63 with a header stating that `portable_pty`'s Windows ConPTY backend does not surface what the driver needs, so the interactive TUI is drivable on Linux and macOS but NOT on Windows with the existing harness. A dimension whose only live surface is the TUI is therefore unprovable on Windows today, and that is a finding to record here with a severity — not something to discover downstream when the Windows run is due.

If a dimension has no user-reachable surface at all, record it NOT-USER-REACHABLE with the reason, and treat that as a finding with its own severity. A restriction that no user surface can exercise is a restriction whose enforcement no user can ever observe, which is a different and quieter problem than a restriction that fails.

Now GROUP the eleven dimensions into SEAMS. This grouping is the census's most important output, because it is what stops Phase 21 becoming eleven workstreams. Do not write eleven test suites and do not propose eleven repairs: group by the seam that carries the dimensions — the budget view and its rollup, the spawn path and its admission, the policy layer, the egress chokepoint, the filesystem authority — and state for each seam which dimensions it carries and therefore which corpus case family proves them together.

SETTLE THE FAN-OUT DETERMINATION, and record it as a first-class section. The determination has exactly three permitted outcomes and no fourth. (a) SAME-AS-CONCURRENCY: fan-out is not distinct from the concurrency cap already enforced at the spawn seam, the requirement is satisfied by the existing knob, and no new knob is added — record this and move on. (b) DISTINCT-AND-COVERED: fan-out is genuinely distinct from concurrency, but the resource envelope it could amplify is already bounded by the budget rollup, so no new knob is added — record the reasoning and the evidence. (c) DISTINCT-AND-GAPPED: fan-out is genuinely distinct AND something it could amplify is not bounded — name exactly what, at exactly which seam, and record it as a corpus target for 21-02. The determination MUST explicitly distinguish bounded ADMITTED work from bounded PENDING work, because a semaphore bounds how many children run at once while saying nothing about how many are queued waiting to run, and each queued child holds a config, a sink and a task. Do NOT invent a redundant knob: if fan-out is the same thing as concurrency, saying so is the deliverable. Note as measured input that the two existing uses of the term in the workspace are neither an authority cap: `EvolveParams.fan_out` in `crates/wcore-evolve/src/evolve/mod.rs` is the GEPA search width per generation, and `FLEET_FANOUT_THRESHOLD` in `crates/wcore-agent/src/orchestration/workflow/runner.rs` is a ROUTING threshold that re-routes wide fan-outs through the Fleet dispatcher rather than refusing them. Confirm both characterizations against the source before relying on either.

Finally, record anything the census surfaces that does NOT fit inside Phase 21's four plans as OUT-OF-PHASE, with a severity and a one-line reason. It goes to BACKLOG or to Sean. It does not become a fifth plan.

Write it all to `21-01-AUTHORITY-CENSUS.md`.

Records evidence for F21-01 and F21-02; marks no requirement complete.</action>
  <verify>
    GATE CONTRACT for `21-01-AUTHORITY-CENSUS.md`, field separator " :: ". The census must carry, per dimension, one row `DIMENSION :: name :: ENFORCED or VACUOUS or UNREACHABLE or ABSENT :: [seam file] :: [seam symbol]`, one row `WIDENING :: name :: [the concrete hostile request]`, and one row `LIVESURFACE :: name :: [a wayland-core invocation, or NOT-USER-REACHABLE] :: [platforms]`; plus one row `FANOUT :: [one of the three permitted outcomes] :: [reasoning]`, one row `MEASURED :: intersect_execution_budget_occurrences :: [the number you actually counted]`, and one row `BASE-SHA :: [the commit the census was taken at]`. The gates do not grep the census for tokens it is supposed to contain — every seam citation is resolved against the real source tree and the measured claim is re-derived from it, so a fabricated citation or an invented count goes red.
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f .planning/phases/21-child-authority-and-budget-inheritance/21-01-AUTHORITY-CENSUS.md</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; A=.planning/phases/21-child-authority-and-budget-inheritance/21-01-AUTHORITY-CENSUS.md; R=$(/usr/bin/awk -F' :: ' 'BEGIN{split("provider tool filesystem egress secret approval depth fan-out time token cost",n," ")} $1=="DIMENSION"{d[$2]=1; if ($3!="ENFORCED" &amp;&amp; $3!="VACUOUS" &amp;&amp; $3!="UNREACHABLE" &amp;&amp; $3!="ABSENT") bad++} END{for(i=11;i>=1;i--) if (n[i] in d) c++; print c+0 "," bad+0}' "$A"); test "$R" = "11,0" || { echo "FAIL: expected one [DIMENSION :: name :: ENFORCED or VACUOUS or UNREACHABLE or ABSENT :: seam file :: seam symbol] row for each of provider tool filesystem egress secret approval depth fan-out time token cost; got covered,malformed = $R"; exit 1; }; echo "all eleven dimensions carry a closed-set verdict"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; A=.planning/phases/21-child-authority-and-budget-inheritance/21-01-AUTHORITY-CENSUS.md; ROWS=$(/usr/bin/awk -F' :: ' '$1=="DIMENSION" &amp;&amp; NF>=5 {print $4 " :: " $5}' "$A"); test "$(printf '%s\n' "$ROWS" | /usr/bin/grep -c ' :: ')" -ge 11 &amp;&amp; printf '%s\n' "$ROWS" | while IFS= read -r row; do p=${row%% :: *}; s=${row#* :: }; test -f "$p" &amp;&amp; /usr/bin/grep -qF -- "$s" "$p" || { echo "FAIL: a census seam citation does not resolve in the live tree: symbol $s is not present in $p"; exit 1; }; done || { echo "FAIL: fewer than eleven seam citations, or a citation names a file or a symbol that does not exist in the source tree"; exit 1; }; echo "every seam citation resolves against the real source tree"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; A=.planning/phases/21-child-authority-and-budget-inheritance/21-01-AUTHORITY-CENSUS.md; N=$(/usr/bin/git grep -cF 'intersect_execution_budget' -- crates/ | /usr/bin/awk -F: '{s+=$NF} END{print s+0}'); V=$(/usr/bin/awk -F' :: ' '$1=="MEASURED" &amp;&amp; $2=="intersect_execution_budget_occurrences"{print $3}' "$A"); test -n "$V" &amp;&amp; test "$V" = "$N" || { echo "FAIL: the census must carry [MEASURED :: intersect_execution_budget_occurrences :: n] equal to the count re-derived from the live tree, which is $N ; the census recorded: ${V:-nothing}"; exit 1; }; echo "the census budget-mechanism claim was re-derived from the live tree and agrees ($N occurrences)"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; A=.planning/phases/21-child-authority-and-budget-inheritance/21-01-AUTHORITY-CENSUS.md; R=$(/usr/bin/awk -F' :: ' 'BEGIN{split("provider tool filesystem egress secret approval depth fan-out time token cost",n," ")} $1=="WIDENING" &amp;&amp; NF>=3 &amp;&amp; length($3)>20 {w[$2]=1} $1=="LIVESURFACE" &amp;&amp; NF>=4 {l[$2]=1; if ($3 !~ /wayland-core/ &amp;&amp; $3 !~ /NOT-USER-REACHABLE/) bad++; if ($4 !~ /Linux/ &amp;&amp; $4 !~ /Windows/ &amp;&amp; $4 !~ /macOS/) bad++} END{for(i=11;i>=1;i--){if(n[i] in w) cw++; if(n[i] in l) cl++} print cw+0 "," cl+0 "," bad+0}' "$A"); test "$R" = "11,11,0" || { echo "FAIL: every dimension needs one [WIDENING :: name :: a concrete hostile request of real length] row and one [LIVESURFACE :: name :: a wayland-core invocation or NOT-USER-REACHABLE :: platforms naming Linux, Windows or macOS] row; got widening,livesurface,malformed = $R"; exit 1; }; echo "every dimension carries a widening attempt and a platform-qualified live surface"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; A=.planning/phases/21-child-authority-and-budget-inheritance/21-01-AUTHORITY-CENSUS.md; MISS=""; for s in '--json-stream' 'pty_capture' 'tui_capable'; do /usr/bin/awk -v s="$s" 'index($0,s){f=1} END{exit !f}' "$A" || MISS="$MISS census-omits:$s"; /usr/bin/grep -rqF -- "$s" crates/wcore-cli/src crates/wcore-eval-scenarios/src || MISS="$MISS absent-from-shipped-source:$s"; done; test -z "$MISS" || { echo "FAIL: shipped-surface accounting incomplete:$MISS"; exit 1; }; echo "every shipped surface the census names is accounted for and exists in the shipped source"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; A=.planning/phases/21-child-authority-and-budget-inheritance/21-01-AUTHORITY-CENSUS.md; F=$(/usr/bin/awk -F' :: ' '$1=="FANOUT"{print $2}' "$A"); case "$F" in SAME-AS-CONCURRENCY|DISTINCT-AND-COVERED|DISTINCT-AND-GAPPED) ;; *) echo "FAIL: the census must carry exactly one row [FANOUT :: SAME-AS-CONCURRENCY or DISTINCT-AND-COVERED or DISTINCT-AND-GAPPED :: reasoning]; found: ${F:-nothing}"; exit 1;; esac; /usr/bin/awk 'tolower($0) ~ /admitted/ {a=1} tolower($0) ~ /pending/ {p=1} END{exit !(a &amp;&amp; p)}' "$A" || { echo "FAIL: the fan-out determination does not distinguish bounded ADMITTED work from bounded PENDING work"; exit 1; }; echo "fan-out determination recorded: $F"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; A=.planning/phases/21-child-authority-and-budget-inheritance/21-01-AUTHORITY-CENSUS.md; P=$(/usr/bin/find .planning/phases/21-child-authority-and-budget-inheritance -maxdepth 1 -name '*-PLAN.md' | /usr/bin/wc -l | /usr/bin/tr -d ' '); test -f "$A" &amp;&amp; test "$P" -le 4 || { echo "FAIL: the census is missing, or the four-plan cap was breached ($P plan files present). The plan-count leg is a REGRESSION GUARD; the census-exists leg is the completion gate."; exit 1; }; echo "census present and four-plan cap intact ($P plans)"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; A=.planning/phases/21-child-authority-and-budget-inheritance/21-01-AUTHORITY-CENSUS.md; P=$(/usr/bin/awk -F' :: ' '$1=="BASE-SHA"{print $2}' "$A"); test -n "$P" &amp;&amp; /usr/bin/git cat-file -e "$P^{commit}" 2>/dev/null &amp;&amp; /usr/bin/git merge-base --is-ancestor "$P" HEAD &amp;&amp; test -z "$(/usr/bin/git status --porcelain -- crates/)" || { echo "FAIL: the census is missing, or carries no [BASE-SHA :: sha] row naming a real commit reachable from HEAD (found: ${P:-nothing}), or a read-only task modified crates/ . The crates/ leg is a REGRESSION GUARD, not a completion gate; the BASE-SHA leg is the completion gate."; exit 1; }; echo "census pinned to commit $P and crates/ unmodified"</automated>
  </verify>
  <done>`21-01-AUTHORITY-CENSUS.md` carries all eleven dimensions, each with a file-and-symbol seam citation, a child-request-channel answer naming the surface, the enforcement mechanism that actually runs, a production-path reachability answer, one verdict from ENFORCED / VACUOUS / UNREACHABLE / ABSENT with a severity where not ENFORCED, and exactly one concrete widening attempt. The budget dimensions are recorded against the mechanism the source shows, with this plan's claim about `intersect_execution_budget` confirmed or contradicted explicitly. Whether any admission decision reads `limit_for` is answered by search. The dimensions are grouped into seams, and that grouping is stated as the bound on plan 21-02. Every dimension additionally carries a LIVE SURFACE entry naming the exact `wayland-core` invocation a user issues to reach it, the observable outcome that distinguishes enforced from widened, and the platforms that surface is available on — with the Windows TUI limitation recorded as a finding rather than discovered downstream, and any NOT-USER-REACHABLE dimension recorded with a severity. The fan-out determination is recorded as one of the three permitted outcomes and distinguishes bounded admitted work from bounded pending work. Out-of-phase findings are recorded with severities and did not become plans. `crates/` is unmodified and the four-plan cap is intact.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3 (BLOCKING DECISION): Authorize broad Phase 21 execution, authorize it scope-limited, or hold</name>
  <files>.planning/phases/21-child-authority-and-budget-inheritance/21-01-ADMISSION-GATE.md</files>
  <action>Present Task 1's per-control gate verdict and Task 2's census verdict table to Sean and obtain ONE authorization. Record the authorization verbatim in `21-01-ADMISSION-GATE.md`, including any scope limit in Sean's own words. Downstream plans 21-02, 21-03 and 21-04 each read this record before proceeding; a plan that runs without an authorization recorded here has bypassed the gate. If HOLD is selected, no downstream plan runs: write the SUMMARY and stop.</action>
  <decision>Does broad Phase 21 execution proceed now, proceed under a named scope limit, or hold until the admission controls close?</decision>
  <context>
ROADMAP.md:83 gates broad Phase 21 execution on two controls being worked separately: CTRL-02/D1, the linked Desktop plan and consumer/reducer conformance harness pinned to the exact Core producer contract, and CTRL-01, the Competitive Capability Ledger with pinned exact Hermes and OpenClaw baselines and F03/F05 evidence mapped. Task 1 has just read both artifacts and produced a verdict per control rather than assuming either state. Task 2's census additionally reports which of the eleven authority dimensions are genuinely ENFORCED versus merely VACUOUS, UNREACHABLE or ABSENT — and the not-ENFORCED ones carry severities, which under the amended rules is what decides whether downstream repair is obligatory or optional. Read both artifacts before choosing. The decision here is not whether the census was right; it is whether the phase's remaining three plans may run.
  </context>
  <options>
    <option id="proceed-broad">
      <name>Proceed with broad execution — both controls verified OPEN</name>
      <pros>The gate's stated condition is met, so 21-02, 21-03 and 21-04 run under their own termination criteria with no extra constraint; the phase completes on its own Success Criteria</pros>
      <cons>Available ONLY if Task 1 returned OPEN for both CTRL-01 and CTRL-02; a single NOT-OPEN verdict removes this option, and selecting it anyway is bypassing the gate rather than passing it</cons>
    </option>
    <option id="proceed-scope-limited">
      <name>Proceed under a named scope limit — one or both controls NOT-OPEN, but specific downstream work is authorized anyway</name>
      <pros>The corpus and harness in 21-02 are largely independent of the Desktop consumer contract, so building and running them can proceed while CTRL-02 closes elsewhere; keeps the phase moving without pretending the gate is open</pros>
      <cons>Requires Sean to name EXACTLY which downstream plans may proceed and which may not, and that limit is binding on every one of them; the risk is that a corpus built before the producer contract is pinned encodes a contract that later changes, so any protocol-surface case may need re-proving</cons>
    </option>
    <option id="hold">
      <name>Hold — one or both controls NOT-OPEN and the phase waits</name>
      <pros>Honors ROADMAP.md:83 exactly; the census still ships and is the input the moment the gate opens, so the wait costs nothing already done; avoids building a host-protocol corpus against a contract that is not yet pinned</pros>
      <cons>Phase 21 stalls behind work owned elsewhere; phases 22 and 23 are serial behind 21, so the stall propagates</cons>
    </option>
  </options>
  <resume-signal>Select: proceed-broad, proceed-scope-limited, or hold. If selecting proceed-scope-limited, name exactly which of 21-02, 21-03 and 21-04 may proceed and which may not. If selecting proceed-broad while any control was verified NOT-OPEN, state explicitly that the gate is being waived and why.</resume-signal>
  <verify>
    <human-check>The authorization is recorded verbatim in `21-01-ADMISSION-GATE.md`, including any scope limit in Sean's own words; and if `proceed-broad` was selected while a control was verified NOT-OPEN, the waiver and its reason are recorded as a waiver rather than as an OPEN verdict.</human-check>
  </verify>
  <done>One authorization is recorded verbatim — broad, scope-limited with the limit named plan by plan, or hold. If hold was selected, no downstream plan runs and this plan wrote its SUMMARY and stopped.</done>
</task>

</tasks>

## What this plan does NOT change (scope fence)

- **Nothing under `crates/` — gate-checked twice.** This is a census. Both automated task gates assert a clean working tree over `crates/`. A census that quietly becomes a repair is how a bounded plan stops being bounded.
- **CTRL-01 and CTRL-02 are READ, never worked on.** Closing either control is separate work owned elsewhere. Pulling it in here would double this plan's size and is precisely the scope metastasis the four-plan cap exists to prevent.
- **No fifth Phase 21 plan is created or proposed.** Work the census surfaces that does not fit is recorded OUT-OF-PHASE with a severity and routed to BACKLOG or to Sean.
- **No new authority knob is designed here, and specifically no `max_fan_out`.** The fan-out determination may conclude that a gap exists and name it; designing or adding the mechanism belongs to 21-03 after the corpus has proved the gap real.
- **The hostile corpus is not written here.** The census produces the widening attempts; 21-02 turns them into cases. Writing cases here would put corpus construction and its own bounding document in one plan.
- **No requirement is marked complete.** This plan records evidence only. F21-01 through F21-04 close on the corpus and the attribution proof, not on an inventory.
- **No test is deleted, weakened, re-gated, `#[ignore]`d or `#[allow]`ed** — there is no test surface in this plan at all, and that is itself gate-checked by the clean-tree assertions.
- **No dependency is added, removed or updated**; there is no `Cargo.toml` change and no install task in this plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| census ← source of truth | A dimension recorded from a requirement's wording or from a brief rather than from the seam is an unverified claim that every downstream plan then trusts |
| existence ← reachability | A mechanism present in the source but never invoked on a production path reads as enforcement and is not; `wcore-permissions` already spent a whole version in exactly that state |
| absence-of-channel ← enforcement | A property that holds because no caller can request widening breaks silently the moment a caller can; every production `sub_budget` call passes `None` today |
| broad execution ← admission gate | ROADMAP.md:83 gates the phase on two controls owned elsewhere; assuming either state rather than reading it converts a gate into a formality |
| plan set ← four-plan cap | Every discovery this census makes is a candidate fifth plan; the cap is the only thing standing between a census and Phase 20's seventy-four |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01-01 | Spoofing | A dimension is recorded ENFORCED because a mechanism exists in the source, without establishing that any production path invokes it — the orphan-code failure repeated, and every downstream plan inherits the false assurance | critical | mitigate | The census forces a four-way verdict where UNREACHABLE is a distinct state from ENFORCED, requires a production-path reachability answer per dimension, and requires file-and-symbol citations; the `PolicyGate` opt-in case is named explicitly as the worked example so it cannot be missed |
| T-21-01-02 | Spoofing | The census inherits this plan's own account of the budget mechanism instead of reading the source, propagating an error into the corpus, the repair set and the phase verdict | critical | mitigate | The plan states its budget claim explicitly AND instructs that the source wins on contradiction and that the contradiction is recorded; the census gate requires `sub_budget`, `first_exceeded_reason` and `intersect_execution_budget` all to be cited, so the claim cannot be passed over silently |
| T-21-01-03 | Elevation of Privilege | A dimension is recorded safe because no child-request channel exists today, without that vacuity being labelled — so the first surface that adds a request channel silently amplifies | high | mitigate | VACUOUS is a mandatory distinct verdict from ENFORCED; the request-channel question must name the surface checked, including the host-protocol surface where a channel is most likely to appear first |
| T-21-01-04 | Denial of Service | Scope metastasis — the census discovers work, the work becomes a plan, and Phase 21 walks Phase 20's path to seventy-four | high | mitigate | The four-plan cap is stated verbatim and gate-checked by counting `*-PLAN.md` files; out-of-phase findings are routed to BACKLOG or Sean by construction; the termination criterion forbids extending the task list or starting a second census cycle |
| T-21-01-05 | Tampering | The admission gate is assumed rather than read, and broad execution proceeds against a control that is not actually open | high | mitigate | Task 1 reads the three intel artifacts at execution time, records each artifact's version marker and git last-commit date so a stale artifact cannot pass as current, and produces a per-control verdict; the blocking checkpoint requires a waiver to be recorded AS a waiver if broad execution is chosen over a NOT-OPEN verdict |
| T-21-01-06 | Elevation of Privilege | Success Criterion 1 is decomposed into eleven independent workstreams, guaranteeing both the plan explosion and eleven per-dimension unit tests that never prove the cross-dimension property | high | mitigate | The seam grouping is a required census output and is explicitly named as the bound on 21-02; the plan states that eleven dimensions mean one property carried by a handful of seams |
| T-21-01-07 | Repudiation | A redundant `max_fan_out` knob is invented to satisfy a requirement word, adding an inheritance dimension that duplicates the concurrency cap and must then be maintained and proved forever | medium | mitigate | The fan-out determination is constrained to three outcomes, two of which add nothing; the plan states that concluding fan-out is the same thing as concurrency IS the deliverable; the two existing workspace uses of the term are named as non-authority so they are not mistaken for prior art |
| T-21-01-08 | Information Disclosure | The census quotes secrets, tokens or credential material out of the source while documenting the secret dimension | medium | mitigate | The secret dimension is documented by seam and mechanism only; no credential value, env-var value or token is copied into the artifact, and the artifact is reviewed for that before it is committed |
| T-21-01-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency is added, removed or updated; there is no `Cargo.toml` change and no install task in this plan, and nothing under `crates/` is written at all |
</threat_model>

<verification>
Local gates (Mac, source level only — this plan has no compile surface): both artifacts exist; the admission-gate artifact accounts for CTRL-01 and CTRL-02 with an OPEN or NOT-OPEN verdict each and cites both intel artifacts as evidence; the census carries all eleven dimensions, at least eleven verdicts drawn from the four permitted states, at least eleven widening attempts, a fan-out determination drawn from the three permitted outcomes, and citations for every named seam symbol; `crates/` is unmodified after each task; the phase directory holds no more than four `*-PLAN.md` files.

There is deliberately no host gate in this plan. Nothing here compiles, nothing here runs on hardware, and adding a Hetzner or Windows run to a read-only census would be ceremony rather than evidence. The hardware gates begin in 21-02, where there is something to execute.

Known unknowns to record, not to resolve here: whether the host-protocol surface will grow a child-request channel that the standalone surface does not have — the census answers what exists now, not what Phase 22 may add; whether the process-global egress policy's per-client override is reachable from any child-controlled code path, which the census flags but the corpus proves; and whether the `limit_for` leaf fallback is consumed by any decision anywhere, which the census answers by search and the corpus confirms by attempt.
</verification>

<success_criteria>
- The broad-execution admission gate's state is READ at execution time from `.planning/intel/COMPETITIVE-LEDGER.md` and `.planning/intel/DESKTOP-PROTOCOL-CHECKPOINT.md`, with one OPEN or NOT-OPEN verdict per control, the ROADMAP standard quoted verbatim, and each artifact's own version marker and git last-commit date recorded so a stale artifact cannot pass as current (CTRL-01, CTRL-02).
- All eleven dimensions of Success Criterion 1 appear in the census, each with a file-and-symbol seam citation, a child-request-channel answer naming the surface checked, the enforcement mechanism that actually runs, a production-path reachability answer, and one verdict from ENFORCED / VACUOUS / UNREACHABLE / ABSENT with a severity attached wherever it is not ENFORCED (F21-01, F21-02).
- The budget dimensions are recorded against the mechanism the SOURCE shows. This plan's claim — that `intersect_execution_budget` and `intersect_caps` are confined to the snapshot-restore path while the child-spawn seam relies on ancestor rollup through `sub_budget` — is explicitly confirmed or explicitly contradicted, and a contradiction is recorded rather than reconciled away.
- Whether any admission or budgeting decision reads `limit_for` — which falls back to the leaf's possibly-wider limit — rather than `first_exceeded_reason` or `minimum_remaining` is answered by search.
- The eleven dimensions are GROUPED INTO SEAMS, and that grouping is stated as the bound on plan 21-02, so the phase proves one property across a handful of seams rather than running eleven per-dimension workstreams.
- Each dimension carries exactly one concrete widening attempt, precise enough that 21-02 can build a corpus case from it without inventing anything.
- Each dimension carries a LIVE SURFACE entry — the exact `wayland-core` invocation with binary, flags and input, the observable outcome that distinguishes an enforced restriction from a widened one, and the platforms that surface is available on — chosen from the four grounded shipped surfaces (`--json-stream` host protocol, `-p`/`--no-tui` standalone headless, bare binary on a PTY for the TUI, or an explicit NOT-USER-REACHABLE verdict). This is what makes Success Criterion 1 provable on the real product rather than only in a test harness.
- The Windows TUI limitation is recorded here as a finding with a severity — `crates/wcore-eval-scenarios/src/pty_capture.rs` is `#![cfg(unix)]` because `portable_pty`'s ConPTY backend does not surface what the driver needs, so any dimension whose only live surface is the TUI is unprovable on Windows today.
- The fan-out determination is settled and recorded as exactly one of SAME-AS-CONCURRENCY, DISTINCT-AND-COVERED or DISTINCT-AND-GAPPED, explicitly distinguishing bounded ADMITTED work from bounded PENDING work, and no redundant knob is designed or added here.
- One authorization is recorded verbatim at the blocking checkpoint — broad, scope-limited with the limit named plan by plan, or hold — and a broad authorization over a NOT-OPEN control is recorded as a waiver rather than as an open gate.
- Nothing under `crates/` was modified, no fifth plan was created or proposed, and no requirement was marked complete.
</success_criteria>

## Artifacts this phase produces
- `.planning/phases/21-child-authority-and-budget-inheritance/21-01-ADMISSION-GATE.md` — the per-control admission verdict and the recorded authorization.
- `.planning/phases/21-child-authority-and-budget-inheritance/21-01-AUTHORITY-CENSUS.md` — the eleven-dimension census, the seam grouping that bounds 21-02, and the fan-out determination.
- `21-01-SUMMARY.md` recording the gate state, the census verdicts, the fan-out determination, and which termination state the plan ended in.

<output>
Create `.planning/phases/21-child-authority-and-budget-inheritance/21-01-SUMMARY.md` using the standard Ferrox summary template. Record: the SHA the census was taken at; the per-control admission verdict with the quoted evidence and each source artifact's version marker and git last-commit date; the eleven-dimension verdict table with seam, request channel, mechanism, reachability and severity; whether this plan's budget-mechanism claim was confirmed or contradicted by the source, and the contradiction if any; the `limit_for` consumption answer; the seam grouping handed to 21-02; the fan-out determination and its reasoning, including the admitted-versus-pending distinction; every OUT-OF-PHASE finding with its severity and where it was routed; the authorization recorded at the checkpoint verbatim, including any scope limit; and which of the four termination states the plan ended in. Mark no requirement complete — this plan records evidence only.
</output>

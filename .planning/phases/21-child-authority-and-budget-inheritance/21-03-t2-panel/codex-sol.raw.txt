Reading additional input from stdin...
OpenAI Codex v0.144.4
--------
workdir: /Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: high
reasoning summaries: none
session id: 019f9e98-14e9-78f2-bc9e-53b364d19a5d
--------
user
BUNDLE-ID=2d5a3d550dcd1e117b03249f6a5e20b66360f64b

You are one of four independent members of a technical decision panel on the
Wayland Core codebase (a multi-provider AI agent CLI in Rust). Read this bundle
and commit to ONE position. You are not being asked to hedge.

================================================================================
THE QUESTION
================================================================================

Which of the corpus's CRITICAL and HIGH findings are repaired in this plan,
which are disproved by correcting the case, and which are declined and left
honestly open?

================================================================================
BACKGROUND
================================================================================

Phase 21's property: every delegated actor stays inside the parent's authority
and resource envelope. Success Criterion 1 is that a child cannot widen ANY of
provider, tool, filesystem, egress, secret, approval, depth, fan-out, time,
token or cost restriction.

Plan 21-01 produced an authority census. Plan 21-02 built a dual-surface hostile
child corpus and measured it INDEPENDENTLY, forbidden from fixing anything: 11
dimensions x 4 combinations (standalone/host-protocol crossed with
in-process/live-against-the-real-binary) x 2 platforms (Linux, Windows) = 88
executions. It recorded ZERO widenings but was explicit that three of the four
census HIGH findings are NOT disproved by that zero.

Plan 21-03 (this one) triages those findings, takes ONE authorization, repairs
ONCE within a hard cap of TWO edit-build-run iterations on real hardware, and
re-measures ONCE. It may not create a further plan. Phase 21 is capped at four
plans total because the previous phase metastasised to seventy-four when its
finding rules had no fixed point.

The amended phase rules, verbatim:
  - Findings at CRITICAL or HIGH must be FIXED or DISPROVED. MEDIUM and below
    are logged to BACKLOG and do not block execution.
  - A REPORTED RED IS WORTH FAR MORE THAN AN ENGINEERED GREEN. Never weaken an
    assertion, #[ignore], #[allow], re-gate, delete a test, or raise a timeout
    to reach a gate.
  - A repair is not CLOSED until the shipped `wayland-core` binary is observed
    refusing the actual widening at a surface a user touches. An in-process
    green against a live red is NOT closed.

DISPROVE means the corpus case asked a question production cannot actually be
asked, so the CASE is corrected and no production code changes. It is a
first-class outcome with equal standing to FIX, and it is the cheapest correct
outcome when it is the true one.

DECLINED means the panel has seen a real finding, weighed it against a measured
cost, and chosen to leave it honestly open with its evidence. It is a decision
on the record, not an oversight.

================================================================================
THE INDEPENDENT MEASUREMENT: what 21-02 found
================================================================================

CRITICAL findings: 0.  HIGH findings: 3.  MEDIUM: 4.  LOW: 3.

FINDING F21-02-01 :: HIGH :: HIGH-1 (tool) is NOT disproved by this corpus.
  Every tool combination recorded REFUSED, but none reached build_tool_registry
  with a live child: a toolsets ["Bash"] request forces IsolatedMutation and
  durable workspace preparation refuses first in a non-repository hermetic
  workspace. Carried forward from the census unchanged. 21-03 must reach the
  registry construction path directly, in a workspace where an isolated worktree
  can be prepared, or disprove the finding with executable evidence at that seam.

FINDING F21-02-02 :: HIGH :: HIGH-4 (approval) is CONFIRMED executably. The
  non-managed branch of with_requested_approvals accepts a child-sourced Bypass
  verbatim. It cannot be reached from production today, which is why the corpus
  records the approval dimension NO-CHANNEL rather than ALLOWED, and why the
  structural canary matters more than the current behaviour.

FINDING F21-02-03 :: HIGH :: HIGH-3 (PolicyGate) is confirmed UNREACHABLE on the
  agent path and is NOT carried by the corpus. set_policy_gate has zero callers
  and every agent-path initialiser is None. The measurement is a one-off, not a
  regression guard.

The four MEDIUM and three LOW findings have exactly one disposition available
under the amended rules (BACKLOG) and are not part of this decision.

================================================================================
THE TRIAGE: what plan 21-03 proposes, per finding
================================================================================

--- F21-02-01 :: HIGH :: proposed FIX
Seam (census-assigned): crates/wcore-agent/src/spawner.rs :: build_tool_registry

CONFIRMED at the seam, and NOT disprovable. build_tool_registry
(spawner.rs:2396-2445) grants from a fixed six-tool array whatever the child's
`allowed` list names. Neither it nor the AgentSpawner struct (spawner.rs:719-800)
carries any representation of the parent's own tool authority. The permit
expression is:

    let permitted = if allowed.is_empty() { SHARED_READ_ONLY_CHILD_TOOLS.contains(name) }
                    else { allowed.iter().any(|a| a.as_str() == *name) };
    let permitted = permitted
        && (requested_workspace == RequestedChildWorkspace::IsolatedMutation
            || SHARED_READ_ONLY_CHILD_TOOLS.contains(name));

The codebase's OWN existing unit test proves the widening without any new
harness: spawner.rs:4357 tc_7_42_build_tool_registry_destructive_requires_opt_in
calls build_tool_registry(&["Bash","Write"], IsolatedMutation, ...) and asserts
registry.get("Bash").is_some(). A parent restricted to Delegate + Read/Grep/Glob
therefore hands its child Bash whenever an isolated worktree can be prepared.

Concrete change: add parent_tool_authority: Option<Arc<BTreeSet<String>>> to
AgentSpawner with a builder, propagate it in clone_for_spawn, pass it into
build_tool_registry as a new final parameter, and intersect INSIDE that function
so no route into the seam can skip it:

    let permitted = permitted
        && parent_tool_authority.is_none_or(|authority| authority.contains(*name));

Wire it at the single production construction site, bootstrap.rs:2202, where the
parent's own registry is already in scope and ToolRegistry::tool_names() already
exists. Five in-file test call sites gain a trailing None.

FLAGS: not a behaviour change for a correctly-configured session (a parent that
holds Bash still delegates Bash). It IS a behaviour change for any session whose
parent registry is narrower than the child's request -- which is exactly the
amplification. Not an architecture change.

KNOWN RESIDUAL, stated up front: None means "no parent authority recorded" and
applies no intersection, so legacy and test spawners are unaffected. That is
FAIL-OPEN BY DEFAULT -- structurally the same shape as the PolicyGate defect in
F21-02-03. It is chosen because a fail-closed default would deny every tool to
every non-bootstrap spawner, and it is mitigated by a reachability test on the
production bootstrap path. A panel member who thinks this residual disqualifies
the fix should say so.

KNOWN LIVE-CLOSURE PROBLEM, stated up front: the corpus could not reach this
seam live because a hermetic non-repository workspace refuses isolated-worktree
preparation before the child launches. After the fix, the live tool leg would
still read REFUSED -- for the OLD reason. Under this plan's own rule ("a repair
is not closed until the real product refuses the widening"), F21-02-01 may end
NOT-CLOSED on its live leg even when the seam is genuinely repaired, unless the
live fixture is rebuilt as a real git repository with a non-overlapping worktree
root, which is itself a material amount of work inside a two-iteration cap.

--- F21-02-02 :: HIGH :: proposed FIX
Seam (census-assigned): crates/wcore-types/src/execution_policy.rs :: with_requested_approvals

CONFIRMED executably by the independent corpus on both platforms and both
surfaces: BaselineExecutionPolicy::smart(Prompt, LocalCliLaunch)
.with_requested_approvals(Bypass, PolicySource::Child) => posture Smart,
approvals Bypass, source Child, managed false. The managed branch ratchets via
stricter_approval_policy; the non-managed branch is a bare
Self::smart(requested, source) -- a replacement.

Concrete change: insert a child-sourced ratchet between the two existing
branches, leaving every other source byte-identical:

    } else if matches!(source, PolicySource::Child) {
        Self::smart(stricter_approval_policy(self.approvals, requested), source)
    } else {
        Self::smart(requested, source)
    }

stricter_approval_policy is already a const fn, so the enclosing const fn still
compiles.

FLAGS: not an architecture change. Behaviour change ONLY for PolicySource::Child,
which has no production constructor today, so no existing session's posture
moves -- the corpus's own NO-CHANNEL measurement is the evidence. This is the
change that best matches the phase's purpose: it converts a property that holds
BY ABSENCE OF A CHANNEL into one that holds BY ENFORCEMENT.

--- F21-02-03 :: HIGH :: proposed FIX
Seam: crates/wcore-agent/src/engine.rs :: policy_gate (census seam S3 is
policy_gate.rs + wcore-permissions/policy.rs; the reachability defect is at the
engine's field initialisers, which is where a repair would land).

CONFIRMED and re-measured: `git grep -nF 'set_policy_gate' -- crates/` returns
exactly two hits, both in engine.rs -- the doc comment at :2679 and the
definition at :4064. ZERO CALLERS. Every agent-path initialiser is None (:3147,
:3381 production; :15307 :16986 :17300 :18688 :19135 :19992 :21042 test
constructors; orchestration/node_executor.rs:418).

Concrete change: replace policy_gate: None at the two PRODUCTION constructors,
engine.rs:3147 and engine.rs:3381, with a constructed PolicyGate.

FLAGS -- BEHAVIOUR CHANGE and ARCHITECTURE CHANGE, both. PolicyEngine::check
(wcore-permissions/src/policy.rs:120-157) is DENY-BY-DEFAULT: it walks its grant
list and returns Err(DenyReason::NoMatchingGrant) when nothing matches. A gate
constructed over a fresh PolicyEngine::new() therefore DENIES EVERY TOOL FOR
EVERY EXISTING SESSION. Making it useful rather than catastrophic requires
populating grants from the parent's registry -- an inheritance model, in a crate
whose own header says it deliberately has none.

Also note: the census is explicit that build_tool_registry (F21-02-01's seam) is
the mechanism that ACTUALLY RUNS for the tool dimension. HIGH-3 is "the policy
layer that would express parent/child intersection is not consulted", not "tools
are ungated". If F21-02-01 lands, the intersection exists at the seam that runs,
and HIGH-3's remaining content is a second, unused expression of the same
property.

================================================================================
THE TWO CRATE-HEADER STATEMENTS, QUOTED VERBATIM FROM SOURCE
================================================================================

These are the exact sentences that make these architecture decisions rather than
patches.

crates/wcore-agent/src/policy_gate.rs, lines 1-16:

    //! v0.6.1 hardening (CRIT-1) - gate tool dispatch through the M5.8
    //! `PolicyEngine`.
    //!
    //! v0.6.0 shipped `wcore-permissions` as orphan code: the crate compiled,
    //! tests passed in isolation, but **no consumer in the engine called
    //! `PolicyEngine::check`**. ACL grants existed only on paper. v0.6.1
    //! installs this gate at the orchestration boundary so a configured
    //! `PolicyEngine` is consulted before any tool runs.
    //!
    //! ## Backwards compatibility
    //!
    //! `PolicyGate` is **opt-in**. Sessions that do not configure one see
    //! identical behaviour to v0.6.0 - every tool runs. The dispatch path
    //! treats `Option<&PolicyGate>` as the canonical "is there a policy"
    //! signal, so the cost on the unconfigured fast path is one `Option`
    //! match per tool call.

crates/wcore-permissions/src/policy.rs, lines 1-5:

    //! Wave B2 - `PolicyEngine`: explicit ACL over `(Actor, Resource, Action)`.
    //!
    //! v0.3 scope: explicit grants only. No role hierarchy, no inheritance, no
    //! OAuth/OIDC. `Actor::System` is the single hard-coded bypass - it represents
    //! the engine itself making internal calls, not a user-facing identity.

The second is exactly what Success Criterion 1's "intersection of parent and
requested authority" asks for. Adding inheritance to that crate is an
architecture change to its declared scope. The census already routed that
question OUT of Phase 21 as item OOP-2.

================================================================================
A CONSTRAINT THE TRIAGE DISCOVERED THAT THE PLAN DID NOT ANTICIPATE
================================================================================

Phase 21's admission gate (21-01) carries a BINDING CONSTRAINT, quoted verbatim:

    "No repair may land a change to the 40 generator source inputs or the
    156-file Desktop contract corpus without re-running wcore-contract digest
    and check, recording the new digests, and re-pinning D1 section 3 as an
    explicit contract bump. HIGH-4 sits directly on this surface so the
    constraint is live, not hypothetical."

crates/wcore-protocol/src/contract/spec.rs:833-874 lists those 40 inputs
verbatim, and source_digest() hashes their CONTENTS. THREE of the 40 are the
edit sites of the three proposed repairs:

    F21-02-01 -> crates/wcore-agent/src/bootstrap.rs       (a SOURCE_INPUT)
    F21-02-02 -> crates/wcore-types/src/execution_policy.rs (a SOURCE_INPUT)
    F21-02-03 -> crates/wcore-agent/src/engine.rs           (a SOURCE_INPUT)
    (crates/wcore-agent/src/spawner.rs is NOT a SOURCE_INPUT)

So any of these repairs moves source_inputs_digest, which moves the `ready`
contract descriptor the shipped binary emits, which the linked Desktop
checkpoint document (D1) records as a HARD RENEGOTIATION ERROR rather than a
tolerated upgrade. The Desktop consumer half of that contract is in a different
repository and cannot be closed from this checkout. The blast-radius probe below
measures the digest effect of each candidate rather than assuming it.

================================================================================
DELIVERABILITY AGAINST THE HARD TWO-ITERATION CAP
================================================================================

The triage's own estimate, recorded before the probe ran:

DELIVERABILITY :: EXCEEDS :: 3 :: The full proposed set does not fit. F21-02-01
and F21-02-02 are each a contained edit at a named seam and together plausibly
fit one edit-build-run iteration with a second in reserve. F21-02-03 does not fit
at any iteration count: PolicyEngine is deny-by-default over an empty grant list,
so the only change that makes the gate reachable denies every tool for every
existing session, and the only change that makes it useful is a grant-inheritance
model in a crate whose header states it deliberately has none.

DELIVERABILITY-SUBSET :: FITS :: 2 :: F21-02-01 and F21-02-02 only, with
F21-02-03 declined and left honestly open for the phase verdict plan to state as
an exception.

================================================================================
THE FOUR OPTIONS -- choose exactly one id
================================================================================

Presented in a rotated order. Pros and cons are copied verbatim from the plan.

OPTION id: disprove-and-correct
  Name: Authorize corpus corrections only - the findings are artifacts of invalid
    cases, so the cases are corrected and no production code changes
  Pros: Cheapest correct outcome when it is the true one: the system was right
    and the corpus was slightly wrong, and changing production to satisfy an
    invalid question would have shipped a real behaviour change to fix an
    imaginary defect
  Cons: Available only where Task 1 produced a DISPROVE proposal with evidence;
    selecting it for a finding triaged FIX would be editing the case instead of
    the defect, which is the exact forgery the RED-over-GREEN rule forbids

OPTION id: decline-all
  Name: Decline the whole set - leave every finding open and escalate the phase's
    position
  Pros: Appropriate when the repair set does not fit the plan, when the required
    changes are larger than Phase 21's scope, or when the findings suggest the
    seam design itself needs rethinking rather than patching; the reds stay
    honestly red, which is worth more than an engineered green
  Cons: Phase 21 cannot claim Success Criterion 1; 21-04 records the phase as
    incomplete against its own criteria; phases 22 and 23, which are serial
    behind this one, inherit the open authority questions

OPTION id: authorize-partial
  Name: Authorize a named subset - repair the findings whose fix is contained,
    decline the behaviour or architecture changes and leave those findings open
  Pros: Takes the contained wins now without shipping a default-behaviour change
    or a crate-scope change under time pressure; the declined findings stay
    honestly open with evidence, which is a decision on the record rather than a
    silent gap; keeps the repair inside the two-iteration cap
  Cons: Phase 21 closes with known-open CRITICAL or HIGH findings, so Success
    Criterion 1 cannot be claimed unconditionally and 21-04 must state the
    exception explicitly; the declined work has to be scheduled somewhere, and
    phases 22 and 23 are serial behind this one

OPTION id: authorize-full
  Name: Authorize the full proposed repair set - every CRITICAL and HIGH is fixed
    or disproved as triaged
  Pros: Phase 21's Success Criterion 1 can close on a repaired and re-measured
    state; nothing is left open to be rediscovered by a later phase or by a
    customer; the repair lands at the census-named seams while the measurement
    that justified it is still fresh
  Cons: Available only if Task 1's deliverability estimate says the set fits
    inside two repair iterations; if it includes a behaviour change or an
    architecture change, authorizing the set authorizes those too, and their
    blast radius is every existing session rather than the child path alone

================================================================================
THE BLAST-RADIUS MEASUREMENT
================================================================================

Each candidate change was applied ALONE, at the exact file and symbol named
above and nothing else, in a throwaway git worktree on the project's Linux build
host, created from the triage commit. Between candidates the tree was reverted so
each is measured alone. Baseline and post-change passing-test counts were taken
over the five crates the three seams touch: wcore-agent, wcore-types,
wcore-permissions, wcore-protocol, wcore-budget. The contract digest was
recomputed after each candidate. The script emitted its own verdict lines.

CONTAINED = compiles and no existing test changes verdict.
WIDE       = compiles but existing tests change verdict.
BREAKS-BUILD = the candidate change does not compile. A BREAKS-BUILD candidate
               CANNOT be authorized FIX under this plan's rules -- a change that
               does not build is a proposal, not a repair.

Measured verdict lines, emitted by the probe's own corrected verdict script:

BLAST-RADIUS F21-02-01 RESULT=WIDE TESTS_BEFORE=3421 TESTS_AFTER=3420 RC=101
BLAST-RADIUS F21-02-02 RESULT=WIDE TESTS_BEFORE=3421 TESTS_AFTER=3420 RC=101
BLAST-RADIUS F21-02-03 RESULT=WIDE TESTS_BEFORE=3421 TESTS_AFTER=3398 RC=101

Nothing returned BREAKS-BUILD, so no candidate is disqualified on that ground.

WHAT THE THREE "WIDE" VERDICTS ACTUALLY CONSIST OF -- read this, because the
label alone is misleading and the composition is the whole decision.

Baseline: 3421 passed. One pre-existing non-passing test, present before any
candidate was applied and unrelated to all three:
  wcore-agent::workflow_limits_test fix1_dispatch_budget_aborts_with_partial_result
  (a 60s timeout under concurrent build load; it passed on a separate quiet run)

F21-02-01 -- verdict-changing tests: 1. Exactly one, and it is:
  wcore-protocol::desktop_contract_corpus checked_corpus_matches_real_serializers_byte_for_byte
  source_inputs_digest moved d8b1a8b5... -> 9c7b98e2...
  NO functional test changed verdict. The tool-intersection fix breaks nothing.

F21-02-02 -- verdict-changing tests: 1. Exactly one, and it is the same test:
  wcore-protocol::desktop_contract_corpus checked_corpus_matches_real_serializers_byte_for_byte
  source_inputs_digest moved d8b1a8b5... -> 5b0263bc...
  NO functional test changed verdict. This CORROBORATES the corpus's NO-CHANNEL
  measurement: no production caller constructs PolicySource::Child, so ratcheting
  it moves no existing session's posture.

F21-02-03 -- verdict-changing tests: 23. The contract pin, PLUS 22 functional
tests across six suites:
  dangerous_lease_e2e_test          (2) incl. dangerous_expiry_reaches_bootstrapped_spawn_child
  engine::audit_2026_05_22_tests    (4) dispatch crash, approval repair, cancellation, unknown-tool
  execution_posture_e2e_test        (1) approval_bypass_retains_required_sandbox_and_tool_function
  json_stream_approval_test         (3) incl. non_askuser_answer_falls_through_to_execute
  output_compaction_test            (2)
  runaway_loop_test                 (5)
  typed_execution_policy_e2e_test   (3) incl. typed_bypass_executes_bash_inside_required_sandbox
  w9_1_skill_drafting_per_turn      (2)
  This is the deny-by-default catastrophe arriving as a number: turning the gate
  on with an empty grant list stops tool dispatch across the engine.

A FACT ABOUT THE CONTRACT PIN THAT BEARS DIRECTLY ON HOW HEAVY THAT COST IS.
`git log -- crates/wcore-protocol/contracts/desktop/v1/` shows the corpus has
been regenerated 16 times, and THREE of the most recent commits are literally
titled "chore(protocol): re-pin Desktop contract provenance digests". Commit
b6936299's own message documents this exact situation -- two edited SOURCE_INPUTS
moved the digest, CI's contract-drift step failed, and the resolution was to
regenerate and re-pin. The repo has a `just desktop-contract-check` target and a
`wcore-contract generate` subcommand for precisely this. So the pin failure is a
KNOWN ROUTINE MAINTENANCE STEP with an established procedure and commit
convention -- not a wire-schema break. `schema_digest` does NOT move for any of
the three candidates; only the source-provenance fingerprint does.

Counterpoint a member should weigh rather than wave away: the phase's admission
gate nonetheless bound 21-03 with "no repair may land a change to the 40
generator source inputs ... without re-running wcore-contract digest and check,
recording the new digests, and re-pinning D1 section 3 as an explicit contract
bump", and the linked Desktop consumer that would renegotiate lives in a
different repository and cannot be reached from this checkout. Note also, as a
fact rather than an argument: the D1 document
(.planning/intel/DESKTOP-PROTOCOL-CHECKPOINT.md) is four paragraphs long, HAS NO
SECTION 3, and pins no digests at all -- so that clause of the constraint names
a thing that does not exist, while the digests it is about actually live in the
repo's own contract corpus. D1's closing sentence reads: "Both receipts are
required for a whole-Wayland claim; neither blocks Core-only engine claims
outside the shared contract."

================================================================================
YOUR TASK
================================================================================

Weigh the three findings, their measured blast radii, the crate-header
statements, the contract-pin cost, and the two-iteration cap. Reason briefly and
concretely. Engage with at least two option ids by name, including at least one
you are rejecting and why.

Then end your response with EXACTLY these two lines and NOTHING after them:

BUNDLE=2d5a3d550dcd1e117b03249f6a5e20b66360f64b
PANEL_POSITION=<one of: authorize-full, authorize-partial, disprove-and-correct, decline-all>
deprecated: `[features].codex_hooks` is deprecated. Use `[features].hooks` instead.
Enable it with `--enable hooks` or `[features].hooks` in config.toml. See https://developers.openai.com/codex/config-basic#feature-flags for details.
2026-07-26T13:23:17.911560Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when AuthRequired(AuthRequiredError { www_authenticate_header: "Bearer realm=\"OAuth\", resource_metadata=\"https://mcp.canva.com/.well-known/oauth-protected-resource/mcp\", error=\"invalid_token\", error_description=\"Missing or invalid access token\"" })
2026-07-26T13:23:18.384958Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when Auth(AuthorizationRequired)
hook: SessionStart
hook: SessionStart Completed
hook: UserPromptSubmit
hook: UserPromptSubmit Completed
codex
I’ll verify the bundle’s local evidence and the cited seams, run the required core-lane queue check, then commit to one option against the two-iteration and closure rules. I’m treating the bundle’s measured probe as authoritative unless the local artifacts contradict it.
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "sed -n '1,240p' /Users/seandonahoe/.codex/RTK.md && pwd && rg --files -g 'AGENTS.md' -g '*2d5a3d550dcd1e117b03249f6a5e20b66360f64b*' -g '*.md' -g '*.json' -g '*.txt' | sed -n '1,240p' && export WL_LANE=core; wl queue" in /Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
 exited 1 in 0ms:
# RTK - Rust Token Killer (Codex CLI)

**Usage**: Token-optimized CLI proxy for shell commands.

## Rule

Always prefix shell commands with `rtk`.

Examples:

```bash
rtk git status
rtk cargo test
rtk npm run build
rtk pytest -q
```

## Meta Commands

```bash
rtk gain            # Token savings analytics
rtk gain --history  # Recent command savings history
rtk proxy <cmd>     # Run raw command without filtering
```

## Verification

```bash
rtk --version
rtk gain
which rtk
```
/Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
gemini-pro.raw.txt
kimi-k3.raw.txt
codex-sol.raw.txt
panel-prompt.txt
error connecting to api.github.com
check your internet connection or https://githubstatus.com

hook: PostToolUse
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk git status --short --branch && rtk git rev-parse --show-toplevel && rtk rg -n \"F21-02-0[123]|DELIVERABILITY|source_inputs_digest|authorize-partial|parent_tool_authority|with_requested_approvals|policy_gate\" .. -g '*.md' -g '*.txt' -g '*.json'" in /Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
hook: PostToolUse
hook: PostToolUse Completed
 succeeded in 3242ms:
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-ZUHNc5pP' (errno=Operation not permitted)
2026-07-26 20:23:54.598 xcodebuild[39868:178129916]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-26 20:23:54.780 xcodebuild[39868:178129915] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-KP9WgLGM' (errno=Operation not permitted)
2026-07-26 20:23:55.212 xcodebuild[39936:178130057]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-26 20:23:55.311 xcodebuild[39936:178130052] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
## plan/f20-unified-audit-repair...origin/plan/f20-unified-audit-repair [ahead 275]
 M ../../../../AGENTS.md
?? ../../../TEST-AUDIT.md
?? ../../../config.json.pre-loopfix.bak
?? ../../../debug/
?? ./
?? ../evidence/21-03-t2-blastradius.loggit: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-aC9jOw17' (errno=Operation not permitted)
2026-07-26 20:23:56.108 xcodebuild[39942:178130100]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-26 20:23:56.204 xcodebuild[39942:178130099] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-avlTPw8E' (errno=Operation not permitted)
2026-07-26 20:23:56.535 xcodebuild[39946:178130118]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-26 20:23:56.633 xcodebuild[39946:178130117] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
/Users/seandonahoe/dev/waylandcore-ferrox
../21-03-REPAIR-SET.md:43:CONSTRAINT-HIT :: F21-02-01 :: crates/wcore-agent/src/bootstrap.rs :: SOURCE_INPUTS[21]
../21-03-REPAIR-SET.md:44:CONSTRAINT-HIT :: F21-02-02 :: crates/wcore-types/src/execution_policy.rs :: SOURCE_INPUTS[16]
../21-03-REPAIR-SET.md:45:CONSTRAINT-HIT :: F21-02-03 :: crates/wcore-agent/src/engine.rs :: SOURCE_INPUTS[22]
../21-03-REPAIR-SET.md:50:one of the 40, so any byte change to those three moves `source_inputs_digest`,
../21-03-REPAIR-SET.md:64:FINDING :: F21-02-01 :: HIGH :: FIX :: crates/wcore-agent/src/spawner.rs :: build_tool_registry
../21-03-REPAIR-SET.md:65:FINDING :: F21-02-02 :: HIGH :: FIX :: crates/wcore-types/src/execution_policy.rs :: with_requested_approvals
../21-03-REPAIR-SET.md:66:FINDING :: F21-02-03 :: HIGH :: FIX :: crates/wcore-agent/src/engine.rs :: policy_gate
../21-03-REPAIR-SET.md:93:### F21-02-01 — HIGH — FIX
../21-03-REPAIR-SET.md:130:**Concrete change.** Give `AgentSpawner` a `parent_tool_authority:
../21-03-REPAIR-SET.md:131:Option<Arc<BTreeSet<String>>>` field with a `with_parent_tool_authority(…)`
../21-03-REPAIR-SET.md:138:    && parent_tool_authority.is_none_or(|authority| authority.contains(*name));
../21-03-REPAIR-SET.md:157:defect in F21-02-03. It is chosen deliberately because a fail-closed default
../21-03-REPAIR-SET.md:162:### F21-02-02 — HIGH — FIX
../21-03-REPAIR-SET.md:164:**Seam:** `crates/wcore-types/src/execution_policy.rs :: with_requested_approvals`
../21-03-REPAIR-SET.md:165:(census `DIMENSION :: approval :: VACUOUS :: crates/wcore-types/src/execution_policy.rs :: with_requested_approvals`).
../21-03-REPAIR-SET.md:168:`BaselineExecutionPolicy::smart(Prompt, LocalCliLaunch).with_requested_approvals(Bypass, PolicySource::Child)`
../21-03-REPAIR-SET.md:198:### F21-02-03 — HIGH — FIX
../21-03-REPAIR-SET.md:200:**Seam:** `crates/wcore-agent/src/engine.rs :: policy_gate` (census seam S3,
../21-03-REPAIR-SET.md:201:`crates/wcore-agent/src/policy_gate.rs` + `crates/wcore-permissions/src/policy.rs`;
../21-03-REPAIR-SET.md:206:`git grep -nF 'set_policy_gate' -- crates/` returns exactly two hits, both in
../21-03-REPAIR-SET.md:210:plus `orchestration/node_executor.rs:418`). `policy_gate.rs`'s own header states
../21-03-REPAIR-SET.md:214:`policy_gate: None` at the two production constructors, `engine.rs:3147` and
../21-03-REPAIR-SET.md:229:`build_tool_registry` — F21-02-01's seam — is the mechanism that actually runs
../21-03-REPAIR-SET.md:232:F21-02-01 lands, the intersection exists at the seam that runs, and HIGH-3's
../21-03-REPAIR-SET.md:272:DELIVERABILITY :: EXCEEDS :: 3 :: The full proposed set does not fit. F21-02-01 and F21-02-02 are each a contained edit at a named seam and together plausibly fit one edit-build-run iteration with a second in reserve. F21-02-03 does not fit at any iteration count: PolicyEngine is deny-by-default over an empty grant list, so the only change that makes the gate reachable denies every tool for every existing session, and the only change that makes it useful is a grant-inheritance model in a crate whose header states it deliberately has none - census OOP-2 routed exactly that out of Phase 21. Additionally all three edit sites touch the 40 pinned generator SOURCE_INPUTS, so each carries a wcore-contract digest re-run, a check, and a D1 section 3 re-pin under the admission gate's binding constraint. A subset excluding F21-02-03 is stated FITS at 2.
../21-03-REPAIR-SET.md:273:DELIVERABILITY-SUBSET :: FITS :: 2 :: F21-02-01 and F21-02-02 only, with F21-02-03 declined and left honestly open for 21-04 to state as an exception.
../21-03-PLAN.md:12:  - crates/wcore-agent/src/policy_gate.rs
../21-03-PLAN.md:30:    - "THE POLICY LAYER IS THE MOST LIKELY CRITICAL AND IT IS A DESIGN DECISION, NOT A MECHANICAL FIX. `crates/wcore-permissions/src/policy.rs` states in its own header that the scope is explicit grants only, with no role hierarchy and no inheritance — directly against F21-01's requirement that a child receive the INTERSECTION of parent and requested authority. `crates/wcore-agent/src/policy_gate.rs` states that v0.6.0 shipped `wcore-permissions` as orphan code with no consumer at all, and that the gate which fixed it is OPT-IN, so a session without a configured gate runs every tool. `crates/wcore-agent/src/engine.rs` constructs `policy_gate: None` at both production constructors. If the corpus proves that reachable, the fix is not a one-line patch: turning an opt-in gate on by default is a behaviour change for every existing session, and adding inheritance to a crate whose header says it deliberately has none is an architecture change. Both require Sean's authorization before a line is written."
../21-03-PLAN.md:71:@crates/wcore-agent/src/policy_gate.rs
../21-03-PLAN.md:154:FLAG BY NAME any FIX that is a BEHAVIOUR CHANGE for existing sessions or an ARCHITECTURE CHANGE to a crate's stated scope. Two candidates are already visible from recon and neither can be authorized implicitly. First: `crates/wcore-agent/src/policy_gate.rs` states the gate is opt-in and that a session without one runs every tool, and `crates/wcore-agent/src/engine.rs` constructs it as absent at both production constructors — turning that on by default changes behaviour for every existing session. Second: `crates/wcore-permissions/src/policy.rs` states in its own header that it deliberately has no role hierarchy and no inheritance — adding inheritance is an architecture change to a crate's declared scope. If the corpus proved either reachable, say so plainly and flag the class; do not present either as a routine patch.
../21-03-PLAN.md:166:    GATE CONTRACT for `21-03-REPAIR-SET.md`, field separator " :: ". Rows: `FINDING :: [id] :: CRITICAL or HIGH or MEDIUM or LOW :: FIX or DISPROVE or BACKLOG :: [seam file] :: [seam symbol]`, `DELIVERABILITY :: FITS or EXCEEDS :: [iterations] :: [detail]`, and `BASE-SHA :: [the commit the triage was taken at]`. The gates do not grep this document for disposition words. Every finding id must trace to a row in `21-02-CORPUS-RESULTS.md`, every FIX must name a seam that resolves in the live source tree, and the amended-rule mapping is enforced mechanically — a CRITICAL parked in BACKLOG, or a MEDIUM promoted to FIX, goes red.
../21-03-PLAN.md:170:    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; R=.planning/phases/21-child-authority-and-budget-inheritance/21-03-REPAIR-SET.md; D=$(/usr/bin/awk -F' :: ' '$1=="DELIVERABILITY"{print $2}' "$R"); I=$(/usr/bin/awk -F' :: ' '$1=="DELIVERABILITY"{print $3}' "$R"); case "$D" in FITS) test "$I" -le 2 2>/dev/null || { echo "FAIL: DELIVERABILITY says FITS but claims $I repair iterations, and the cap is two"; exit 1; };; EXCEEDS) ;; *) echo "FAIL: the repair set must carry exactly one row [DELIVERABILITY :: FITS or EXCEEDS :: iterations :: detail] stated against the two-iteration cap; found: ${D:-nothing}"; exit 1;; esac; echo "deliverability against the two-iteration cap recorded: $D at $I iterations"</automated>
../21-03-PLAN.md:180:  <read_first>.planning/phases/21-child-authority-and-budget-inheritance/21-03-REPAIR-SET.md (Task 1's triage — the per-finding severity, disposition, seam, concrete change and deliverability estimate; this task decides about THAT, and does not re-triage), .planning/phases/21-child-authority-and-budget-inheritance/21-02-CORPUS-RESULTS.md (the independent measurement whose CRITICAL and HIGH count decides whether a panel is owed at all), crates/wcore-agent/src/policy_gate.rs (the header's opt-in statement, because the first likely behaviour-change candidate lives here and the probe below applies its candidate change), crates/wcore-permissions/src/policy.rs (the header's explicit-grants-only statement, the second candidate), crates/wcore-agent/src/engine.rs (both production `policy_gate` construction sites, because the blast radius of turning the gate on by default is measured from there)</read_first>
../21-03-PLAN.md:217:21-02 measured, independently of any repair, exactly where a child can widen a parent's authority or resource envelope across eleven dimensions and two surfaces, on Linux and on Windows. Task 1 has triaged that measurement under the amended rules: CRITICAL and HIGH must be fixed or disproved, MEDIUM and below go to BACKLOG and do not block. What makes this a decision rather than a formality is that the two most likely CRITICAL findings are not mechanical patches. `crates/wcore-agent/src/policy_gate.rs` states the tool-authority gate is OPT-IN and that a session without one runs every tool, and `crates/wcore-agent/src/engine.rs` constructs it as absent at both production constructors — so turning it on by default is a behaviour change for every existing session. `crates/wcore-permissions/src/policy.rs` states in its own header that it deliberately has no role hierarchy and no inheritance, which is exactly what F21-01's `intersection of parent and requested authority` asks for — so adding inheritance is an architecture change to a crate's declared scope. Read `21-03-REPAIR-SET.md` for the concrete per-finding proposals, the seams, and the deliverability estimate against the two-iteration cap before choosing.
../21-03-PLAN.md:225:    <option id="authorize-partial">
../21-03-PLAN.md:241:  <decision-record>The chosen option id is written in the single `PANEL-DECISION` row of `21-03-REPAIR-SET.md` and must be one of exactly: authorize-full, authorize-partial, disprove-and-correct, decline-all — or `not-required` with basis `EMPTY-SET` when the independent corpus measurement carries no CRITICAL or HIGH finding at all. Whichever is chosen, the actionable record is per finding: one `AUTHORIZED :: [finding-id] :: FIX or DISPROVE or BACKLOG or DECLINED` row for every finding Task 1 triaged, because Task 3 reads those rows and a set-level verdict tells it nothing about any individual change. Every finding authorized FIX additionally carries the blast radius its probe measured.</decision-record>
../21-03-PLAN.md:244:    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; R=.planning/phases/21-child-authority-and-budget-inheritance/21-03-REPAIR-SET.md; C=.planning/phases/21-child-authority-and-budget-inheritance/21-02-CORPUS-RESULTS.md; D=.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel; test -f "$R" -a -f "$C" || { echo "FAIL: the repair set or the independent corpus measurement it must answer does not exist"; exit 1; }; CH=$(/usr/bin/awk -F' :: ' '$1=="FINDING" &amp;&amp; ($3=="CRITICAL" || $3=="HIGH"){n++} END{print n+0}' "$C"); N=$(/usr/bin/awk -F' :: ' '$1=="PANEL-DECISION"{n++} END{print n+0}' "$R"); test "$N" = "1" || { echo "FAIL: expected exactly one PANEL-DECISION row, found $N"; exit 1; }; CHO=$(/usr/bin/awk -F' :: ' '$1=="PANEL-DECISION"{print $2}' "$R"); if test "$CH" -eq 0; then test "$CHO" = "not-required" || { echo "FAIL: the corpus measured no CRITICAL or HIGH finding, so the only legal decision is not-required, not $CHO"; exit 1; }; echo "the corpus measured no blocking finding; no panel was owed and none was faked"; exit 0; fi; test "$CHO" != "not-required" || { echo "FAIL: the decision claims no panel was owed, but the corpus measured $CH CRITICAL or HIGH finding(s)"; exit 1; }; S=$(/usr/bin/awk -F' :: ' '$1=="BASE-SHA"{print $2}' "$R"); test -n "$S" || { echo "FAIL: no BASE-SHA row to bind the four members to one shared bundle"; exit 1; }; test "$(/usr/bin/wc -c &lt; "$D/panel-prompt.txt" 2&gt;/dev/null || echo 0)" -ge 800 || { echo "FAIL: the shared evidence bundle is missing or too small to carry the triage table, the crate-header quotations and the probe verdicts"; exit 1; }; BAD=""; for O in authorize-full authorize-partial disprove-and-correct decline-all; do /usr/bin/grep -qF -- "$O" "$D/panel-prompt.txt" || BAD="$BAD dropped-option:$O"; done; for M in codex-sol gemini-pro kimi-k3 claude-adversarial; do F="$D/$M.raw.txt"; test -f "$F" || { BAD="$BAD no-capture:$M"; continue; }; /usr/bin/grep -qF "BUNDLE=$S" "$F" || BAD="$BAD not-fed-the-shared-bundle:$M"; V=$(/usr/bin/awk -F' :: ' -v m="$M" '$1=="PANEL-VOTE" &amp;&amp; $2==m{print $3}' "$R"); X=$(/usr/bin/grep -oE 'PANEL_POSITION=[a-z-]+' "$F" | /usr/bin/tail -1); test -n "$V" -a "PANEL_POSITION=$V" = "$X" || BAD="$BAD recorded-vote-absent-from-own-transcript:$M(recorded=${V:-none},transcript=${X:-none})"; done; test -z "$BAD" || { echo "FAIL: $CH blocking finding(s) were measured, so a four-member panel over one bundle is owed, and this is not it:$BAD"; exit 1; }; echo "$CH blocking finding(s) measured; four members answered the one shared bundle and every recorded vote is the final position in that member's own transcript"</automated>
../21-03-PLAN.md:245:    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; R=.planning/phases/21-child-authority-and-budget-inheritance/21-03-REPAIR-SET.md; test -f "$R" || { echo "FAIL: the repair set does not exist"; exit 1; }; CH=$(/usr/bin/awk -F' :: ' '$1=="PANEL-DECISION"{print $2}' "$R"); BA=$(/usr/bin/awk -F' :: ' '$1=="PANEL-DECISION"{print $3}' "$R"); test -n "$CH" || { echo "FAIL: no decision was recorded"; exit 1; }; if test "$CH" = "not-required"; then test "$BA" = "EMPTY-SET" || { echo "FAIL: a not-required decision must carry the basis EMPTY-SET, not $BA"; exit 1; }; echo "no panel was owed, so there is no vote arithmetic to check"; exit 0; fi; echo "$CH" | /usr/bin/grep -qxE 'authorize-full|authorize-partial|disprove-and-correct|decline-all' || { echo "FAIL: the chosen id [$CH] is not one of this plan's own four options"; exit 1; }; echo "$BA" | /usr/bin/grep -qxE 'MAJORITY|MINORITY-STRONGER-EVIDENCE|EVIDENTIARY-TIEBREAK' || { echo "FAIL: the basis [$BA] is not one of MAJORITY, MINORITY-STRONGER-EVIDENCE, EVIDENTIARY-TIEBREAK"; exit 1; }; TOP=$(/usr/bin/awk -F' :: ' '$1=="PANEL-VOTE"{print $3}' "$R" | /usr/bin/sort | /usr/bin/uniq -c | /usr/bin/sort -rn | /usr/bin/head -1 | /usr/bin/awk '{print $2}'); CNT=$(/usr/bin/awk -F' :: ' '$1=="PANEL-VOTE"{print $3}' "$R" | /usr/bin/sort | /usr/bin/uniq -c | /usr/bin/sort -rn | /usr/bin/awk '{print $1}'); C1=$(echo "$CNT" | /usr/bin/sed -n 1p); C2=$(echo "$CNT" | /usr/bin/sed -n 2p); test -n "$TOP" || { echo "FAIL: there are no votes to count"; exit 1; }; if test "$BA" = MAJORITY; then test "$CH" = "$TOP" -a "$C1" -gt "${C2:-0}" || { echo "FAIL: basis MAJORITY but chosen=$CH modal=$TOP counts=$C1/${C2:-0}"; exit 1; }; fi; if test "$BA" = MINORITY-STRONGER-EVIDENCE; then test "$CH" != "$TOP" || { echo "FAIL: the basis claims a minority position but $CH IS the modal pick"; exit 1; }; fi; test "$C1" -gt "${C2:-0}" -o "$BA" = EVIDENTIARY-TIEBREAK || { echo "FAIL: the panel tied $C1/$C2 and was resolved by preference; a tie needs EVIDENTIARY-TIEBREAK naming the measurement that broke it"; exit 1; }; R2=$(/usr/bin/awk -F' :: ' '$1=="PANEL-RATIONALE"{if (length($2)&gt;m) m=length($2)} END{print m+0}' "$R"); test "$R2" -ge 200 || { echo "FAIL: the longest PANEL-RATIONALE row is $R2 characters; a decision authorizing production change to a security seam needs its reasoning written down"; exit 1; }; DN=$(/usr/bin/awk -F' :: ' -v c="$CH" '$1=="PANEL-DISSENT" &amp;&amp; $2!=c &amp;&amp; ($2=="authorize-full" || $2=="authorize-partial" || $2=="disprove-and-correct" || $2=="decline-all") &amp;&amp; length($3)&gt;60{n++} END{print n+0}' "$R"); test "$DN" -ge 1 || { echo "FAIL: no PANEL-DISSENT row names an option other than $CH with a real argument; a decision with the losing arguments deleted is not auditable"; exit 1; }; echo "the basis survives arithmetic against the four captured votes, the reasoning is $R2 characters, and $DN dissent row(s) preserve an argument for an option that lost"</automated>
../21-03-PLAN.md:254:  <files>crates/wcore-agent/src/spawner.rs, crates/wcore-agent/src/policy_gate.rs, crates/wcore-budget/src/execution.rs, crates/wcore-permissions/src/policy.rs, crates/wcore-cli/tests/child_authority_corpus/cases.rs, .planning/phases/21-child-authority-and-budget-inheritance/21-03-REPAIR-SET.md</files>
../21-03-t2-panel/gemini-pro.raw.txt:123:I reject `authorize-full` because F21-02-03 is undeliverable. Replacing `policy_gate: None` with a constructed `PolicyGate` over an empty grant list triggers a deny-by-default catastrophe that breaks 22 functional tests across six suites. Making it work requires adding an inheritance model to `wcore-permissions`, which explicitly violates the crate header's stated scope ("no role hierarchy, no inheritance"). Furthermore, the census explicitly notes that `build_tool_registry` (the seam for F21-02-01) is the mechanism that actually runs for the tool dimension. If we fix F21-02-01, F21-02-03 becomes a redundant and dangerous architectural overreach.
../21-03-t2-panel/gemini-pro.raw.txt:125:I reject `decline-all` because F21-02-01 and F21-02-02 represent targeted, correct repairs that do not alter the behavior of existing sessions. The blast-radius measurement proves this: both candidates only tripped a single test, the Desktop contract source digest pin. 
../21-03-t2-panel/gemini-pro.raw.txt:127:I support `authorize-partial` for F21-02-01 and F21-02-02. The contract pin failure is a known routine maintenance step easily resolved by regenerating the hashes (`wcore-contract generate`). The constraint regarding "D1 section 3" is a phantom blocker, as the D1 document itself contains no such section or pins. Fixing F21-02-01 and F21-02-02 secures the tool and approval authority boundaries within the iteration cap, while cleanly and honestly declining F21-02-03 as an out-of-scope architectural change.
../21-03-t2-panel/gemini-pro.raw.txt:130:PANEL_POSITION=authorize-partial
../21-03-t2-panel/kimi-k3.raw.txt:6:  - F21-02-01 (tool registry): contained fix, blast radius WIDE but only the contract pin test — a routine maintenance step with an established procedure. Fix is real, intersects parent authority at the running seam. Fail-open residual for legacy/test spawners is a concern but mitigated; production path wired at bootstrap.rs:2202.
../21-03-t2-panel/kimi-k3.raw.txt:7:  - F21-02-02 (approval ratchet): same — only contract pin moves, no functional test changes. Zero production callers of Child source. Contained, cheap, converts absence-of-channel to enforcement.
../21-03-t2-panel/kimi-k3.raw.txt:8:  - F21-02-03 (policy gate): 22 functional tests break — deny-by-default catastrophe. Requires inheritance model in a crate whose header explicitly declares no inheritance; census routed that question out of phase (OOP-2). Does not fit at any iteration count per triage.
../21-03-t2-panel/kimi-k3.raw.txt:10:  So authorize-full would require authorizing F21-02-03 which is an architecture change outside phase scope and doesn't fit the two-iteration cap — reject. decline-all leaves contained wins on the table for no measured reason — reject. disprove-and-correct is wrong because the findings are confirmed at the seams, not invalid cases. authorize-partial: fix 01 and 02, decline 03 honestly open.
../21-03-t2-panel/kimi-k3.raw.txt:14:  Residual concern on F21-02-01's fail-open default: acceptable — matches existing codebase pattern (PolicyGate opt-in Option semantics), and wiring at the single production construction site plus reachability test mitigates. Doesn't disqualify.
../21-03-t2-panel/kimi-k3.raw.txt:16:  Live-closure problem for F21-02-01: live leg may still read REFUSED for old reason. That's an evidence problem, not a correctness problem; re-measure once and record honestly.
../21-03-t2-panel/kimi-k3.raw.txt:18:  Position: authorize-partial.
../21-03-t2-panel/kimi-k3.raw.txt:24:  **F21-02-01 and F21-02-02 are contained repairs whose only measured blast radius is the contract pin.** The probe is decisive here: each moves exactly one test, `checked_corpus_matches_real_serializers_byte_for_byte`, and zero functional tests. For F21-02-02 that zero is corroborating evidence, not absence of evidence — the corpus already measured NO-CHANNEL, and the probe confirms no production constructor of `PolicySource::Child` exists, so ratcheting the child branch moves no live session. For F21-02-01, the codebase's own test at spawner.rs:4357 proves the widening today, and the fix intersects at the seam that actually runs. The contract-digest movement is a known routine maintenance step — the repo has regenerated the corpus 16 times, three recent commits are literally titled "re-pin Desktop contract provenance digests," `schema_digest` is unmoved, and an established `just desktop-contract-check` / `wcore-contract generate` procedure exists. The admission-gate clause about "re-pinning D1 section 3" names a section that does not exist in a four-paragraph document that pins no digests; the actual digests live in this repo's own corpus and are re-pinnable from this checkout. That cost is real but small and procedural — it does not justify leaving a proven tool-amplification open.
../21-03-t2-panel/kimi-k3.raw.txt:26:  The fail-open residual on F21-02-01 (`None` = no intersection) is a genuine shape-match to the F21-02-03 defect, but it does not disqualify the fix: it mirrors the codebase's established opt-in `Option` pattern, the production path is wired at the single construction site (bootstrap.rs:2202), and a reachability test guards it. The live-closure problem is an evidence-scheduling problem — re-measure once, record the live leg honestly as REFUSED-for-the-old-reason if
../21-02-CORPUS-RESULTS.md:258:MEASURED :: set_policy_gate_occurrences :: 2 :: engine.rs:2679 (doc comment), engine.rs:4064 (definition) — ZERO callers
../21-02-CORPUS-RESULTS.md:259:MEASURED :: agent_path_policy_gate_initialisers :: every one None (engine.rs:3147, :3381, :15307, :16986, :17300, :18688, :19135, :19992, :21042; orchestration/node_executor.rs:418)
../21-02-CORPUS-RESULTS.md:272:MEASURED :: with_requested_approvals :: BaselineExecutionPolicy::smart(Prompt, LocalCliLaunch).with_requested_approvals(Bypass, PolicySource::Child) => posture Smart, approvals Bypass, source Child, managed false
../21-02-CORPUS-RESULTS.md:289:FINDING :: F21-02-01 :: HIGH :: HIGH-1 (tool) is NOT disproved by this corpus. Every tool combination recorded REFUSED, but none reached build_tool_registry with a live child: a toolsets ["Bash"] request forces IsolatedMutation and durable workspace preparation refuses first in a non-repository hermetic workspace. Carried forward from the census unchanged. 21-03 must reach the registry construction path directly, in a workspace where an isolated worktree can be prepared, or disprove the finding with executable evidence at that seam.
../21-02-CORPUS-RESULTS.md:290:FINDING :: F21-02-02 :: HIGH :: HIGH-4 (approval) is CONFIRMED executably. The non-managed branch of with_requested_approvals accepts a child-sourced Bypass verbatim; measurement in section 5. It cannot be reached from production today, which is why the corpus records the approval dimension NO-CHANNEL rather than ALLOWED, and why the structural canary matters more than the current behaviour.
../21-02-CORPUS-RESULTS.md:291:FINDING :: F21-02-03 :: HIGH :: HIGH-3 (PolicyGate) is confirmed UNREACHABLE on the agent path and is NOT carried by the corpus. set_policy_gate has zero callers and every agent-path initialiser is None. The measurement is a one-off, not a regression guard.
../21-03-t2-panel/codex-sol.raw.txt:75:FINDING F21-02-01 :: HIGH :: HIGH-1 (tool) is NOT disproved by this corpus.
../21-03-t2-panel/codex-sol.raw.txt:83:FINDING F21-02-02 :: HIGH :: HIGH-4 (approval) is CONFIRMED executably. The
../21-03-t2-panel/codex-sol.raw.txt:84:  non-managed branch of with_requested_approvals accepts a child-sourced Bypass
../21-03-t2-panel/codex-sol.raw.txt:89:FINDING F21-02-03 :: HIGH :: HIGH-3 (PolicyGate) is confirmed UNREACHABLE on the
../21-03-t2-panel/codex-sol.raw.txt:90:  agent path and is NOT carried by the corpus. set_policy_gate has zero callers
../21-03-t2-panel/codex-sol.raw.txt:101:--- F21-02-01 :: HIGH :: proposed FIX
../21-03-t2-panel/codex-sol.raw.txt:122:Concrete change: add parent_tool_authority: Option<Arc<BTreeSet<String>>> to
../21-03-t2-panel/codex-sol.raw.txt:128:        && parent_tool_authority.is_none_or(|authority| authority.contains(*name));
../21-03-t2-panel/codex-sol.raw.txt:142:F21-02-03. It is chosen because a fail-closed default would deny every tool to
../21-03-t2-panel/codex-sol.raw.txt:151:is not closed until the real product refuses the widening"), F21-02-01 may end
../21-03-t2-panel/codex-sol.raw.txt:156:--- F21-02-02 :: HIGH :: proposed FIX
../21-03-t2-panel/codex-sol.raw.txt:157:Seam (census-assigned): crates/wcore-types/src/execution_policy.rs :: with_requested_approvals
../21-03-t2-panel/codex-sol.raw.txt:161:.with_requested_approvals(Bypass, PolicySource::Child) => posture Smart,
../21-03-t2-panel/codex-sol.raw.txt:184:--- F21-02-03 :: HIGH :: proposed FIX
../21-03-t2-panel/codex-sol.raw.txt:185:Seam: crates/wcore-agent/src/engine.rs :: policy_gate (census seam S3 is
../21-03-t2-panel/codex-sol.raw.txt:186:policy_gate.rs + wcore-permissions/policy.rs; the reachability defect is at the
../21-03-t2-panel/codex-sol.raw.txt:189:CONFIRMED and re-measured: `git grep -nF 'set_policy_gate' -- crates/` returns
../21-03-t2-panel/codex-sol.raw.txt:195:Concrete change: replace policy_gate: None at the two PRODUCTION constructors,
../21-03-t2-panel/codex-sol.raw.txt:206:Also note: the census is explicit that build_tool_registry (F21-02-01's seam) is
../21-03-t2-panel/codex-sol.raw.txt:209:are ungated". If F21-02-01 lands, the intersection exists at the seam that runs,
../21-03-t2-panel/codex-sol.raw.txt:220:crates/wcore-agent/src/policy_gate.rs, lines 1-16:
../21-03-t2-panel/codex-sol.raw.txt:268:    F21-02-01 -> crates/wcore-agent/src/bootstrap.rs       (a SOURCE_INPUT)
../21-03-t2-panel/codex-sol.raw.txt:269:    F21-02-02 -> crates/wcore-types/src/execution_policy.rs (a SOURCE_INPUT)
../21-03-t2-panel/codex-sol.raw.txt:270:    F21-02-03 -> crates/wcore-agent/src/engine.rs           (a SOURCE_INPUT)
../21-03-t2-panel/codex-sol.raw.txt:273:So any of these repairs moves source_inputs_digest, which moves the `ready`
../21-03-t2-panel/codex-sol.raw.txt:281:DELIVERABILITY AGAINST THE HARD TWO-ITERATION CAP
../21-03-t2-panel/codex-sol.raw.txt:286:DELIVERABILITY :: EXCEEDS :: 3 :: The full proposed set does not fit. F21-02-01
../21-03-t2-panel/codex-sol.raw.txt:287:and F21-02-02 are each a contained edit at a named seam and together plausibly
../21-03-t2-panel/codex-sol.raw.txt:288:fit one edit-build-run iteration with a second in reserve. F21-02-03 does not fit
../21-03-t2-panel/codex-sol.raw.txt:294:DELIVERABILITY-SUBSET :: FITS :: 2 :: F21-02-01 and F21-02-02 only, with
../21-03-t2-panel/codex-sol.raw.txt:295:F21-02-03 declined and left honestly open for the phase verdict plan to state as
../21-03-t2-panel/codex-sol.raw.txt:326:OPTION id: authorize-partial
../21-03-t2-panel/codex-sol.raw.txt:370:BLAST-RADIUS F21-02-01 RESULT=WIDE TESTS_BEFORE=3421 TESTS_AFTER=3420 RC=101
../21-03-t2-panel/codex-sol.raw.txt:371:BLAST-RADIUS F21-02-02 RESULT=WIDE TESTS_BEFORE=3421 TESTS_AFTER=3420 RC=101
../21-03-t2-panel/codex-sol.raw.txt:372:BLAST-RADIUS F21-02-03 RESULT=WIDE TESTS_BEFORE=3421 TESTS_AFTER=3398 RC=101
../21-03-t2-panel/codex-sol.raw.txt:384:F21-02-01 -- verdict-changing tests: 1. Exactly one, and it is:
../21-03-t2-panel/codex-sol.raw.txt:386:  source_inputs_digest moved d8b1a8b5... -> 9c7b98e2...
../21-03-t2-panel/codex-sol.raw.txt:389:F21-02-02 -- verdict-changing tests: 1. Exactly one, and it is the same test:
../21-03-t2-panel/codex-sol.raw.txt:391:  source_inputs_digest moved d8b1a8b5... -> 5b0263bc...
../21-03-t2-panel/codex-sol.raw.txt:396:F21-02-03 -- verdict-changing tests: 23. The contract pin, PLUS 22 functional
../21-03-t2-panel/codex-sol.raw.txt:447:PANEL_POSITION=<one of: authorize-full, authorize-partial, disprove-and-correct, decline-all>
../21-03-t2-panel/codex-sol.raw.txt:508:/bin/zsh -lc "rtk git status --short --branch && rtk git rev-parse --show-toplevel && rtk rg -n \"F21-02-0[123]|DELIVERABILITY|source_inputs_digest|authorize-partial|parent_tool_authority|with_requested_approvals|policy_gate\" .. -g '*.md' -g '*.txt' -g '*.json'" in /Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
../21-03-t2-panel/panel-prompt.txt:62:FINDING F21-02-01 :: HIGH :: HIGH-1 (tool) is NOT disproved by this corpus.
../21-03-t2-panel/panel-prompt.txt:70:FINDING F21-02-02 :: HIGH :: HIGH-4 (approval) is CONFIRMED executably. The
../21-03-t2-panel/panel-prompt.txt:71:  non-managed branch of with_requested_approvals accepts a child-sourced Bypass
../21-03-t2-panel/panel-prompt.txt:76:FINDING F21-02-03 :: HIGH :: HIGH-3 (PolicyGate) is confirmed UNREACHABLE on the
../21-03-t2-panel/panel-prompt.txt:77:  agent path and is NOT carried by the corpus. set_policy_gate has zero callers
../21-03-t2-panel/panel-prompt.txt:88:--- F21-02-01 :: HIGH :: proposed FIX
../21-03-t2-panel/panel-prompt.txt:109:Concrete change: add parent_tool_authority: Option<Arc<BTreeSet<String>>> to
../21-03-t2-panel/panel-prompt.txt:115:        && parent_tool_authority.is_none_or(|authority| authority.contains(*name));
../21-03-t2-panel/panel-prompt.txt:129:F21-02-03. It is chosen because a fail-closed default would deny every tool to
../21-03-t2-panel/panel-prompt.txt:138:is not closed until the real product refuses the widening"), F21-02-01 may end
../21-03-t2-panel/panel-prompt.txt:143:--- F21-02-02 :: HIGH :: proposed FIX
../21-03-t2-panel/panel-prompt.txt:144:Seam (census-assigned): crates/wcore-types/src/execution_policy.rs :: with_requested_approvals
../21-03-t2-panel/panel-prompt.txt:148:.with_requested_approvals(Bypass, PolicySource::Child) => posture Smart,
../21-03-t2-panel/panel-prompt.txt:171:--- F21-02-03 :: HIGH :: proposed FIX
../21-03-t2-panel/panel-prompt.txt:172:Seam: crates/wcore-agent/src/engine.rs :: policy_gate (census seam S3 is
../21-03-t2-panel/panel-prompt.txt:173:policy_gate.rs + wcore-permissions/policy.rs; the reachability defect is at the
../21-03-t2-panel/panel-prompt.txt:176:CONFIRMED and re-measured: `git grep -nF 'set_policy_gate' -- crates/` returns
../21-03-t2-panel/panel-prompt.txt:182:Concrete change: replace policy_gate: None at the two PRODUCTION constructors,
../21-03-t2-panel/panel-prompt.txt:193:Also note: the census is explicit that build_tool_registry (F21-02-01's seam) is
../21-03-t2-panel/panel-prompt.txt:196:are ungated". If F21-02-01 lands, the intersection exists at the seam that runs,
../21-03-t2-panel/panel-prompt.txt:207:crates/wcore-agent/src/policy_gate.rs, lines 1-16:
../21-03-t2-panel/panel-prompt.txt:255:    F21-02-01 -> crates/wcore-agent/src/bootstrap.rs       (a SOURCE_INPUT)
../21-03-t2-panel/panel-prompt.txt:256:    F21-02-02 -> crates/wcore-types/src/execution_policy.rs (a SOURCE_INPUT)
../21-03-t2-panel/panel-prompt.txt:257:    F21-02-03 -> crates/wcore-agent/src/engine.rs           (a SOURCE_INPUT)
../21-03-t2-panel/panel-prompt.txt:260:So any of these repairs moves source_inputs_digest, which moves the `ready`
../21-03-t2-panel/panel-prompt.txt:268:DELIVERABILITY AGAINST THE HARD TWO-ITERATION CAP
../21-03-t2-panel/panel-prompt.txt:273:DELIVERABILITY :: EXCEEDS :: 3 :: The full proposed set does not fit. F21-02-01
../21-03-t2-panel/panel-prompt.txt:274:and F21-02-02 are each a contained edit at a named seam and together plausibly
../21-03-t2-panel/panel-prompt.txt:275:fit one edit-build-run iteration with a second in reserve. F21-02-03 does not fit
../21-03-t2-panel/panel-prompt.txt:281:DELIVERABILITY-SUBSET :: FITS :: 2 :: F21-02-01 and F21-02-02 only, with
../21-03-t2-panel/panel-prompt.txt:282:F21-02-03 declined and left honestly open for the phase verdict plan to state as
../21-03-t2-panel/panel-prompt.txt:313:OPTION id: authorize-partial
../21-03-t2-panel/panel-prompt.txt:357:BLAST-RADIUS F21-02-01 RESULT=WIDE TESTS_BEFORE=3421 TESTS_AFTER=3420 RC=101
../21-03-t2-panel/panel-prompt.txt:358:BLAST-RADIUS F21-02-02 RESULT=WIDE TESTS_BEFORE=3421 TESTS_AFTER=3420 RC=101
../21-03-t2-panel/panel-prompt.txt:359:BLAST-RADIUS F21-02-03 RESULT=WIDE TESTS_BEFORE=3421 TESTS_AFTER=3398 RC=101
../21-03-t2-panel/panel-prompt.txt:371:F21-02-01 -- verdict-changing tests: 1. Exactly one, and it is:
../21-03-t2-panel/panel-prompt.txt:373:  source_inputs_digest moved d8b1a8b5... -> 9c7b98e2...
../21-03-t2-panel/panel-prompt.txt:376:F21-02-02 -- verdict-changing tests: 1. Exactly one, and it is the same test:
../21-03-t2-panel/panel-prompt.txt:378:  source_inputs_digest moved d8b1a8b5... -> 5b0263bc...
../21-03-t2-panel/panel-prompt.txt:383:F21-02-03 -- verdict-changing tests: 23. The contract pin, PLUS 22 functional
../21-03-t2-panel/panel-prompt.txt:434:PANEL_POSITION=<one of: authorize-full, authorize-partial, disprove-and-correct, decline-all>
../21-01-SUMMARY.md:45:  - "PolicyGate is orphan code on the agent path — `set_policy_gate` has zero callers; the only production construction is the `mcp-serve` subcommand"
../21-01-SUMMARY.md:111:| approval | `execution_policy.rs :: with_requested_approvals` | **none today** — `PolicySource::Child` is test-only | managed → ratchet; **non-managed → replacement, `Bypass` accepted verbatim** | resolver yes, child leg does not exist | **VACUOUS** | HIGH |
../21-01-SUMMARY.md:216:- **HIGH-3 (PolicyGate orphan).** `set_policy_gate` has **zero callers** — two hits in the
../21-01-SUMMARY.md:218:  `policy_gate` initialiser is `None`. The only production `PolicyGate::new` is
../21-01-SUMMARY.md:222:- **HIGH-4 (approval).** `with_requested_approvals` (`execution_policy.rs:151-153`) replaces
../21-01-SUMMARY.md:307:  `source_inputs_digest=sha256:d8b1a8b5…` — identical to D1 §3 **and** to `manifest.json`. Phase
../21-01-ADMISSION-GATE.md:169:PANEL-DISSENT :: hold :: The adversarial member argued that the four legs measured whether the contract has ALREADY drifted, which is a statement about the past, whereas the option's stated risk is whether it WILL drift before Desktop adopts it. Worse, it identified a concrete mechanism by which the authorized work causes the drift the measurement was used to rule out. HIGH-4 is with_requested_approvals on EffectiveExecutionPolicy, and execution_policy is a versioned SUB-CONTRACT of the Desktop corpus with its own event fixture and adversarial vectors, so a 21-03 repair to that seam can move source_inputs_digest or fixture_digest, which changes the ready descriptor, which D1 records as a hard renegotiation error rather than a tolerated upgrade. It did not carry because it does not defeat proceed-scope-limited as such, only an unbounded version of it, and the member conceded as much in writing. It is answered by binding it into the 21-03 scope row rather than by dismissing it.
../21-01-ADMISSION-GATE.md:195:  `source_inputs_digest=sha256:d8b1a8b5…`, all three equal to D1 §3. **This closes a gap D1
../21-01-ADMISSION-GATE.md:261:> Phase 21 proves that a child cannot widen authority or budget inside the engine. That is a Core-only engine claim. The authority census confirms it structurally: all four HIGH findings are Core-internal seams (Delegate tool's `toolsets` replacement into `ForkOverrides.allowed_tools`, no provider intersection channel, `PolicyGate` orphaned on the agent path, non-managed `with_requested_approvals` replacing instead of ratcheting). None of them requires a Desktop consumer to prove or disprove. The seams that do touch the shared contract — approval revisions crossing the wire, and attribution/correlation in 21-04 — are a minority of the phase's surface, and the verdict document that *defines* CTRL-02 says explicitly that the missing receipts do not block this class of claim.
../21-01-ADMISSION-GATE.md:286:> HIGH-4 is `with_requested_approvals` in `EffectiveExecutionPolicy`. The bundle also records, from D1, that the Desktop contract corpus carries `execution_policy` as a versioned SUB-CONTRACT, that the corpus contains an `execution_policy` event fixture and a family of adversarial policy vectors, and that the contract's `source_inputs_digest` is taken over the generator's Rust source inputs.
../21-01-ADMISSION-GATE.md:288:> Chain the two. Plan 21-03 is authorized — by all three members — to repair HIGH-4. Repairing HIGH-4 means changing `EffectiveExecutionPolicy` approval resolution. If that type or its serialization is among the generator's source inputs, the repair changes `source_inputs_digest`; if the fixtures move, it changes `fixture_digest`; either changes the `ready.contract` descriptor the running binary emits — which the LIVE-READY leg just proved is the byte-exact thing Desktop would pin. And D1 records that a `minor` mismatch is a HARD renegotiation error, not a tolerated upgrade.
../21-02-SUMMARY.md:122:`policy_from_config` chokepoint; the real `with_requested_approvals` resolver.
../21-02-SUMMARY.md:158:    .with_requested_approvals(Bypass, PolicySource::Child)
../21-02-SUMMARY.md:184:SHA — `set_policy_gate` has two occurrences (doc comment, definition) and ZERO
../21-02-SUMMARY.md:185:callers; every agent-path `policy_gate` initialiser is `None` — confirming
../21-02-SUMMARY.md:293:   gap is recorded as finding F21-02-03.
../21-01-AUTHORITY-CENSUS.md:13:MEASURED :: set_policy_gate_callers :: 0
../21-01-AUTHORITY-CENSUS.md:34:DIMENSION :: approval :: VACUOUS :: crates/wcore-types/src/execution_policy.rs :: with_requested_approvals
../21-01-AUTHORITY-CENSUS.md:49:| 6 | approval | **NONE today.** `PolicySource::Child` exists as an enum variant (`execution_policy.rs:52`) but its only two occurrences (`:451`, `:503`) are inside `#[cfg(test)] mod tests` (opens at line 409). No production code constructs it. The child inherits `effective_policy` by clone (`spawner.rs:2167`). | `with_requested_approvals` (`execution_policy.rs:138`): **if `is_managed()` it ratchets** via `stricter_approval_policy` — a real one-way intersection. **Otherwise it returns `Self::smart(requested, source)`, which REPLACES the posture with whatever was requested, including `Bypass`.** Production producers are `Protocol` (`channel_dispatch.rs:224`), `Acp` (`acp_engine.rs:976`), CLI launch (`packaged_runtime.rs:88`) and Crucible (`crucible.rs:162`) — all parent/host-side, none child-side. | The resolver runs; the **child** leg does not exist | **VACUOUS** | **HIGH** |
../21-01-AUTHORITY-CENSUS.md:72:WIDENING :: approval :: A child requests an approval posture weaker than the parent's, by driving an EffectiveExecutionPolicy request carrying approvals bypass at a child seam, and observes whether the non-managed branch of with_requested_approvals accepts it verbatim. Because no production child channel exists today, this case MUST be paired with a NO-CHANNEL canary asserting that absence, so the day a channel appears the case fails instead of continuing to pass.
../21-01-AUTHORITY-CENSUS.md:97:| **S3 — Policy layer** | `crates/wcore-agent/src/policy_gate.rs` + `crates/wcore-permissions/src/policy.rs` — `PolicyGate::check_tool`, `PolicyEngine::check` | tool (secondary mechanism) | Reachability, not behaviour: prove whether anything in a real agent bootstrap turns the gate on. See HIGH-3. |
../21-01-AUTHORITY-CENSUS.md:99:| **S5 — Execution policy resolver** | `crates/wcore-types/src/execution_policy.rs` — `with_requested_approvals`, `resolve_dangerous_launch` | approval | ONE ratchet family: managed-branch ratchet proof plus the non-managed replacement finding, plus the NO-CHANNEL canary. |
../21-01-AUTHORITY-CENSUS.md:277:`git grep -nF 'set_policy_gate' -- crates/` returns **two** hits, both in
../21-01-AUTHORITY-CENSUS.md:279:**Zero callers.** Every `policy_gate` field initialiser in `engine.rs` is `None` (`:3147`,
../21-01-AUTHORITY-CENSUS.md:282:`wayland-core mcp-serve` and dead for every agent session. `policy_gate.rs`'s own header records
../21-01-AUTHORITY-CENSUS.md:290:**HIGH-4 — the non-managed branch of `with_requested_approvals` replaces rather than ratchets.**
../21-01-t3-panel/panel-prompt.txt:175:  HIGH-3  PolicyGate is ORPHAN CODE on the agent path. set_policy_gate has ZERO
../21-01-t3-panel/panel-prompt.txt:176:          callers in the workspace. Every policy_gate field initialiser in the engine
../21-01-t3-panel/panel-prompt.txt:180:  HIGH-4  The non-managed branch of with_requested_approvals REPLACES rather than
../21-01-t3-panel/panel-prompt.txt:215:    printed fixture_digest, schema_digest and source_inputs_digest, and all three
../21-01-t3-panel/panel-prompt.txt:228:    source_inputs_digest sha256:d8b1a8b5... - identical to both D1 section 3 and the
../21-01-PLAN.md:29:    - "THE POLICY LAYER FAILS OPEN BY DESIGN AND THAT IS THE CLEAREST STRUCTURAL GAP. `crates/wcore-permissions/src/policy.rs` states in its own header that the scope is explicit grants only, with no role hierarchy and no inheritance — directly against F21-01's `intersection of parent and requested authority`. Worse, `crates/wcore-agent/src/policy_gate.rs` states that v0.6.0 shipped `wcore-permissions` as ORPHAN code with no consumer calling `PolicyEngine::check`, and that the v0.6.1 gate which fixed it is OPT-IN: sessions that do not configure a `PolicyGate` see every tool run. `crates/wcore-agent/src/engine.rs` constructs `policy_gate: None` at both production constructors (`:3147`, `:3381`). So the tool-authority dimension has an enforcement mechanism that is off unless something turns it on. The census must record whether any production bootstrap turns it on, and if nothing does, that is a finding with a severity, not a footnote."
../21-01-PLAN.md:77:@crates/wcore-agent/src/policy_gate.rs
../21-01-PLAN.md:171:  <read_first>crates/wcore-budget/src/execution.rs (`sub_budget` and its doc comment, `first_exceeded_reason`, `check_state`, `limit_for`, `observed_for`, `with_reason_state`, `minimum_remaining`, `record_tokens`, `record_cost`, `enter_agent`, and `intersect_execution_budget` together with its ONE caller so the restore-path scope is confirmed rather than assumed), crates/wcore-budget/src/tracker.rs (`BudgetCap`, `Charge`, `CapWarn`, `CapBlock`, `intersect_caps` and both its callers, `clamp_token_extension`, `clamp_usd_extension`), crates/wcore-agent/src/spawner.rs (`ChildBudgetGuard`, `enter_child_budget`, `bind_child_budget`, `spawn_one_with_active_permit` and the `active_child_permits` semaphore, the provider-resolution path for `SubAgentConfig.provider`, and the `ForkOverrides.allowed_tools` application site), crates/wcore-types/src/spawner.rs (`SubAgentConfig`, `ForkOverrides`, `RequestedChildWorkspace`, `SHARED_READ_ONLY_CHILD_TOOLS`, `ChildPolicySnapshot`, `ChildWorkspaceMode`), crates/wcore-types/src/execution_policy.rs (`ExecutionPolicyRequest` versus `EffectiveExecutionPolicy`, `PolicySource` including its `Child` variant, `resolve_dangerous_launch`, `with_requested_approvals`), crates/wcore-agent/src/policy_gate.rs (the header's opt-in statement and the actor-resolution rules), crates/wcore-permissions/src/policy.rs (the header's explicit-grants-only statement, `Actor`, `Resource`, `Action`, `PolicyEngine::check`), crates/wcore-agent/src/engine.rs (every `policy_gate` construction and assignment site), crates/wcore-egress/src/policy.rs (`install_global_policy` and the process-global `OnceLock`), crates/wcore-agent/src/egress/policy.rs (`AgentEgressPolicy`), crates/wcore-sandbox/src/directory_authority.rs (the filesystem authority surface), crates/wcore-agent/src/approval.rs (the correlation-id bridge), crates/wcore-agent/src/durable_spawner.rs and crates/wcore-agent/src/durable_child.rs (the durable child store and the result-delivery attribution path)</read_first>
../21-01-PLAN.md:247:`CONTRACT-DIGEST` — run `/root/.cargo/bin/cargo run -p wcore-protocol --bin wcore-contract -- digest`, which prints `fixture_digest=`, `schema_digest=`, `source_inputs_digest=` and `generator=`. Compare all three against section 3 of the D1 document. Emit `CONTRACT-DIGEST RESULT=MATCH RC=0` or `RESULT=MISMATCH`. This leg matters more than it looks: the D1 author states plainly that they did NOT run this canonical Rust reproduction because the authoring machine is a Mac with no toolchain, and reproduced the digests with a throwaway Python script instead. So this leg closes a gap D1 itself records as open, on the host where cargo is allowed.
../21-01-t3-panel/claude-adversarial.raw.txt:38:HIGH-4 is `with_requested_approvals` in `EffectiveExecutionPolicy`. The bundle also
../21-01-t3-panel/claude-adversarial.raw.txt:42:`source_inputs_digest` is taken over the generator's Rust source inputs.
../21-01-t3-panel/claude-adversarial.raw.txt:47:changes `source_inputs_digest`; if the fixtures move, it changes `fixture_digest`;
../21-01-t3-panel/kimi-k3.raw.txt:17:  Phase 21 proves that a child cannot widen authority or budget inside the engine. That is a Core-only engine claim. The authority census confirms it structurally: all four HIGH findings are Core-internal seams (Delegate tool's `toolsets` replacement into `ForkOverrides.allowed_tools`, no provider intersection channel, `PolicyGate` orphaned on the agent path, non-managed `with_requested_approvals` replacing instead of ratcheting). None of them requires a Desktop consumer to prove or disprove. The seams that do touch the shared contract — approval revisions crossing the wire, and attribution/correlation in 21-04 — are a minority of the phase's surface, and the verdict document that *defines* CTRL-02 says explicitly that the missing receipts do not block this class of claim.
../21-01-t3-panel/codex-sol.raw.txt:188:  HIGH-3  PolicyGate is ORPHAN CODE on the agent path. set_policy_gate has ZERO
../21-01-t3-panel/codex-sol.raw.txt:189:          callers in the workspace. Every policy_gate field initialiser in the engine
../21-01-t3-panel/codex-sol.raw.txt:193:  HIGH-4  The non-managed branch of with_requested_approvals REPLACES rather than
../21-01-t3-panel/codex-sol.raw.txt:228:    printed fixture_digest, schema_digest and source_inputs_digest, and all three
../21-01-t3-panel/codex-sol.raw.txt:241:    source_inputs_digest sha256:d8b1a8b5... - identical to both D1 section 3 and the

codex
The first pass confirms the local repair set and probe artifacts match the supplied bundle; the GitHub queue check was attempted but the environment cannot reach `api.github.com`. I’m using the `ijfw-critique` skill now because this is an explicit adversarial option decision; it will structure the rejection test, not change the evidence set.
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc 'rtk cat /Users/seandonahoe/.codex/skills/ijfw-critique/SKILL.md' in /Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
 succeeded in 0ms:
---
name: ijfw-critique
description: "Challenge decisions, surface counter-arguments, flag assumptions. Trigger: 'should I', 'is this right', 'critique', 'poke holes', 'second opinion', 'devil's advocate'. Auto-fired by ijfw-intent-router."
---


Before you agree, disagree. Before you advise, stress-test.

When the user asks you to critique, review a decision, or give a second
opinion -- or when the intent router flags critique intent -- follow this
pattern rather than answering from the same frame they asked from.

## Four-step critique

1. **Steelman first.** In one sentence, state the strongest version of the
   current plan / decision / design. Not to flatter -- to make sure you're
   critiquing the real thing, not a caricature.

2. **Surface the assumptions.** Name 2-3 assumptions the plan rests on.
   These are the load-bearing beliefs that, if wrong, collapse the argument.
   Be specific: "assumes traffic stays under 10k qps", not "assumes scale".

3. **Three concrete counter-arguments.** Each should be:
   - *Non-obvious* (if the user already considered it, skip)
   - *Bounded* (state when it applies; rarely are counter-arguments universal)
   - *Actionable* (each comes with a "watch for X" or "test by Y")

   Prefer counter-arguments that come from different angles: operational,
   social/organizational, economic, correctness. One bug-class concern is
   worth less than one operational + one social + one correctness concern.

4. **State your verdict briefly.** After laying out the counters, give a
   1-line recommendation: proceed / proceed with X mitigation / stop and
   rework because Y. Own the verdict -- don't just hedge.

## Refactor reframe (single-line trigger)

When the user is mid-refactor and a fix feels hacky, or you catch yourself
patching around a smell rather than removing it, run this prompt out loud:

> *Knowing everything I know now, what would the elegant solution look like?*

Answer it briefly (3-5 lines). If the elegant version costs less than the
hack you're about to ship, replace the hack. If it costs more than 2x, log
the elegant version as a follow-up and proceed with the pragmatic fix --
but only after you've named the debt explicitly.

Skip this for trivial or obvious fixes; the point is to break frame on
non-trivial decisions, not to over-engineer.

## When NOT to critique

- The user is midway through implementing and needs help, not a critique.
- The decision is reversible and cheap (just do it and see).
- You lack the domain facts to form an opinion -- say so instead of guessing.
- The question is "how" not "whether". How-to questions deserve how-to
  answers, not a rehash of whether to do it at all.

## Output shape

```
Steelman: <one-line strongest version>

Assumptions:
  1. <load-bearing belief>
  2. <load-bearing belief>

Counter-arguments:
  1. <non-obvious objection> -- applies when <condition>; watch for <signal>.
  2. <different angle> -- applies when <condition>; test with <method>.
  3. <third angle> -- applies when <condition>; mitigate via <approach>.

Verdict: <proceed|mitigate|rework> -- <one-line reason>

Audit: stress-tested <N> assumptions, <N> angles (<angle-1> + <angle-2> + <angle-3>). Confidence: <low|med|high>.
```

### Worked example

```
Steelman: Rewriting the auth layer in Rust eliminates the class of memory bugs that caused last quarter's outages.

Assumptions:
  1. The team has enough Rust fluency to maintain the new code without slowdown.
  2. The outages trace to memory bugs, not logic or config errors.

Counter-arguments:
  1. Operationally: Rust compile times and borrow-checker friction could double review cycles -- applies when the team has <3 months Rust experience; watch for PR cycle time increasing past 2 days.
  2. Socially: the auth team may resist a full rewrite when incremental hardening (address-sanitiser + fuzzing) could close the same risk at 20% of the effort -- applies when management is measuring velocity; test by asking the team to estimate both approaches.
  3. Correctness: auth logic bugs (e.g. missing token expiry checks) are language-agnostic -- applies if the root-cause analysis is incomplete; mitigate by running a postmortem before deciding on the rewrite.

Verdict: mitigate -- commission a 2-week root-cause analysis first; if the cause is confirmed memory-safety, Rust is the right call.

Audit: stress-tested 2 assumptions, 3 angles (operational + social + correctness). Confidence: high.
```

## Tone

Direct, not hedging. The user asked for a critique; they don't need
"this is just my opinion". State the counter clearly. If they push back,
that's the signal to update or yield -- but make them do the work of
pushing back, not you.

Resume normal mode after.

hook: PostToolUse
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk sed -n '1,320p' ../21-03-REPAIR-SET.md
rtk sed -n '1,220p' ../evidence/21-03-t2-blastradius.log
rtk sed -n '1,80p' ../../../../crates/wcore-agent/src/policy_gate.rs
rtk sed -n '1,80p' ../../../../crates/wcore-permissions/src/policy.rs
rtk sed -n '120,180p' ../../../../crates/wcore-types/src/execution_policy.rs
rtk sed -n '2380,2460p' ../../../../crates/wcore-agent/src/spawner.rs
rtk sed -n '1,160p' ../../../intel/DESKTOP-PROTOCOL-CHECKPOINT.md" in /Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
 succeeded in 0ms:
# 21-03 — Repair Set: triage, authorization, and the post-repair delta

Phase 21, plan 03. Field separator ` :: `.

The sole authorized input to this triage is `21-02-CORPUS-RESULTS.md`, produced
by an independent pass that was forbidden from fixing anything. Nothing else
enters the repair set — not a defect noticed in passing, not an adjacent
improvement, not a census dimension the corpus did not actually flag.

```
BASE-SHA :: 2d5a3d550dcd1e117b03249f6a5e20b66360f64b
BRANCH :: plan/f20-unified-audit-repair
CORPUS-INPUT :: 21-02-CORPUS-RESULTS.md at RUN-SHA 4a3dd3756efec29f91fa99ce4a68500c485adc1f
PHASE-BASE :: dd02a624e99ac061cc38a070c1a99719c80f2f68
AUTHORISATION :: 21-01-ADMISSION-GATE.md :: SCOPE-LIMIT :: 21-03 :: PROCEED
```

## 0. The admission gate, read and not re-verified

`21-01-ADMISSION-GATE.md:172` records:

> `SCOPE-LIMIT :: 21-03 :: PROCEED :: Triage and authorized repair are bounded to
> the four Core-internal HIGH findings recorded in the census, namely HIGH-1 tool
> replacement, HIGH-2 absent provider intersection, HIGH-3 orphan PolicyGate and
> HIGH-4 non-managed approval replacement. BINDING CONSTRAINT carried from the
> adversarial dissent, which every downstream reader must honour. No repair may
> land a change to the 40 generator source inputs or the 156-file Desktop
> contract corpus without re-running wcore-contract digest and check, recording
> the new digests, and re-pinning D1 section 3 as an explicit contract bump.
> HIGH-4 sits directly on this surface so the constraint is live, not
> hypothetical.`

The gate is OPEN for this plan. 21-01 owns that determination and it is not
re-verified here.

**The binding constraint is materially larger than the gate's author expected,
and this is the single most important thing the triage discovered.**
`crates/wcore-protocol/src/contract/spec.rs:833-874` lists the 40 generator
source inputs verbatim. Three of them are the edit sites of the three HIGH
findings' proposed repairs:

```
CONSTRAINT-HIT :: F21-02-01 :: crates/wcore-agent/src/bootstrap.rs :: SOURCE_INPUTS[21]
CONSTRAINT-HIT :: F21-02-02 :: crates/wcore-types/src/execution_policy.rs :: SOURCE_INPUTS[16]
CONSTRAINT-HIT :: F21-02-03 :: crates/wcore-agent/src/engine.rs :: SOURCE_INPUTS[22]
CONSTRAINT-CLEAR :: crates/wcore-agent/src/spawner.rs :: NOT a generator source input
```

`source_digest()` (`contract/generate.rs:1080`) hashes the **contents** of every
one of the 40, so any byte change to those three moves `source_inputs_digest`,
which moves the `ready` descriptor, which D1 records as a hard renegotiation
error rather than a tolerated upgrade. Every candidate repair is therefore
measured for its digest effect in the Task 2 probe, not assumed.

## 1. Triage — one disposition per corpus red

`FINDING :: [id] :: [severity] :: [FIX | DISPROVE | BACKLOG] :: [seam file] :: [seam symbol]`

Severity is copied from the independent measurement and is never softened here.
FIX and DISPROVE are available only to CRITICAL and HIGH; BACKLOG is the only
disposition the amended rules give MEDIUM and below.

```
FINDING :: F21-02-01 :: HIGH :: FIX :: crates/wcore-agent/src/spawner.rs :: build_tool_registry
FINDING :: F21-02-02 :: HIGH :: FIX :: crates/wcore-types/src/execution_policy.rs :: with_requested_approvals
FINDING :: F21-02-03 :: HIGH :: FIX :: crates/wcore-agent/src/engine.rs :: policy_gate
FINDING :: F21-02-04 :: MEDIUM :: BACKLOG :: crates/wcore-cli/tests/child_authority_corpus/cases.rs :: corpus_tool
FINDING :: F21-02-05 :: MEDIUM :: BACKLOG :: crates/wcore-cli/tests/child_authority_corpus/cases.rs :: corpus_token
FINDING :: F21-02-06 :: MEDIUM :: BACKLOG :: crates/wcore-agent/src/egress/policy.rs :: AgentEgressPolicy
FINDING :: F21-02-07 :: LOW :: BACKLOG :: crates/wcore-cli/src/main.rs :: trailing_var_arg
FINDING :: F21-02-08 :: LOW :: BACKLOG :: crates/wcore-cli/tests/support/vault.rs :: vault
FINDING :: F21-02-09 :: LOW :: BACKLOG :: .planning/phases/21-child-authority-and-budget-inheritance/21-02-PLAN.md :: json-stream
FINDING :: F21-02-10 :: MEDIUM :: BACKLOG :: crates/wcore-cli/tests/deterministic_openai_loop.rs :: packaged_core_cancels_an_active_stream
```

`CRITICAL` count in the corpus: 0. `HIGH` count: 3. `MEDIUM` count: 4. `LOW`
count: 3. Total 10, which is every `FINDING` row in `21-02-CORPUS-RESULTS.md`.

### Why HIGH-2 (provider) carries no row

The admission gate's scope limit names four census HIGH findings, but the
independent corpus produced `FINDING` rows for only three. HIGH-2 is discussed
at `21-02-CORPUS-RESULTS.md:244-250` — *"confirmed as recorded, not closed"* —
and deliberately given no finding row, because the corpus measured the provider
dimension `NO-CHANNEL` on all eight combinations across both platforms and added
a live structural canary that reads the real `input_schema()` of the production
`Delegate` and `Spawn` tools and fails the day either grows a provider-naming
property. The scope limit is a CEILING on what may be repaired, not a floor on
what must be. The repair set is bounded by the corpus and by nothing else, so
HIGH-2 is recorded here as in-ceiling but unflagged, with its canary already in
the suite. This is stated rather than silently dropped.

### F21-02-01 — HIGH — FIX

**Seam:** `crates/wcore-agent/src/spawner.rs :: build_tool_registry`
(census `DIMENSION :: tool :: ABSENT :: crates/wcore-agent/src/spawner.rs :: build_tool_registry`).

**What the corpus left open.** Every tool combination recorded REFUSED, but none
reached `build_tool_registry` with a live child: a `toolsets: ["Bash"]` request
classifies as `RequestedChildWorkspace::IsolatedMutation` and durable workspace
preparation refuses first in a hermetic non-repository workspace. The corpus
measured the *second* of the census's three mitigations, not the absence of
intersection. 21-03 must reach the seam directly or disprove it.

**Reached directly, at the seam, and it is CONFIRMED — not disprovable.**
`build_tool_registry` (`spawner.rs:2396-2445`) takes `allowed: &[String]`, a
fixed six-tool array, the requested workspace class, the read-deny list and the
sandbox runtime. It never receives, and the `AgentSpawner` struct
(`spawner.rs:719-800`) never carries, any representation of the parent's own
tool authority. The permit expression is:

```rust
let permitted = if allowed.is_empty() { SHARED_READ_ONLY_CHILD_TOOLS.contains(name) }
                else { allowed.iter().any(|a| a.as_str() == *name) };
let permitted = permitted
    && (requested_workspace == RequestedChildWorkspace::IsolatedMutation
        || SHARED_READ_ONLY_CHILD_TOOLS.contains(name));
```

The codebase's **own existing unit test** proves the widening at the seam
without any new harness:
`spawner.rs:4357 tc_7_42_build_tool_registry_destructive_requires_opt_in` calls
`build_tool_registry(&["Bash","Write"], IsolatedMutation, …)` and asserts
`registry.get("Bash").is_some()`. Nothing in that call path consults a parent.
A parent restricted to Delegate + Read/Grep/Glob therefore hands its child
`Bash` whenever an isolated worktree can be prepared. DISPROVE is not available
for this finding: the question the corpus asked is one production can be asked,
and the answer is that authority is a replacement.

**Concrete change.** Give `AgentSpawner` a `parent_tool_authority:
Option<Arc<BTreeSet<String>>>` field with a `with_parent_tool_authority(…)`
builder, propagate it in `clone_for_spawn`, pass it into `build_tool_registry`
as a new final parameter, and intersect **inside that function** so no route
into the seam can skip it:

```rust
let permitted = permitted
    && parent_tool_authority.is_none_or(|authority| authority.contains(*name));
```

Wire the authority at the single production construction site,
`crates/wcore-agent/src/bootstrap.rs:2202`, where the parent's own `registry` is
already in scope (`.with_sandbox_runtime(registry.sandbox_runtime())`) and
`ToolRegistry::tool_names()` (`crates/wcore-tools/src/registry.rs:292`) already
exists. Five in-file test call sites gain a trailing `None`.

**Flags.** Not a behaviour change for a correctly-configured session — a parent
that holds Bash still delegates Bash. It IS a behaviour change for any session
whose parent registry is narrower than the child's request, which is precisely
the amplification. **CONSTRAINT-HIT: the bootstrap wiring line touches
`SOURCE_INPUTS[21]`, so the contract digest moves.** `spawner.rs` itself is
clear.

**Residual this fix does NOT close, stated up front.** `None` means "no parent
authority recorded" and applies no intersection, so legacy and test spawners are
unaffected. That is fail-open by default — the same shape as the PolicyGate
defect in F21-02-03. It is chosen deliberately because a fail-closed default
would deny every tool to every non-bootstrap spawner, and it is mitigated by a
reachability test asserting the production bootstrap path wires it. The residual
is recorded, not hidden.

### F21-02-02 — HIGH — FIX

**Seam:** `crates/wcore-types/src/execution_policy.rs :: with_requested_approvals`
(census `DIMENSION :: approval :: VACUOUS :: crates/wcore-types/src/execution_policy.rs :: with_requested_approvals`).

**Confirmed executably by the corpus** on every platform and both surfaces:
`BaselineExecutionPolicy::smart(Prompt, LocalCliLaunch).with_requested_approvals(Bypass, PolicySource::Child)`
=> posture Smart, approvals **Bypass**, source Child, managed false. Read at
`execution_policy.rs:138-154`: the managed branch ratchets via
`stricter_approval_policy`; the non-managed branch is a bare
`Self::smart(requested, source)` — a replacement.

**Concrete change.** Insert a child-sourced ratchet between the two existing
branches, leaving every other source byte-identical:

```rust
} else if matches!(source, PolicySource::Child) {
    Self::smart(stricter_approval_policy(self.approvals, requested), source)
} else {
    Self::smart(requested, source)
}
```

`stricter_approval_policy` is already a `const fn` (`:169`), so the enclosing
`const fn` still compiles.

**Flags.** Not an architecture change. Behaviour change ONLY for
`PolicySource::Child`, which has no production constructor today, so no existing
session's posture moves — the corpus's own NO-CHANNEL measurement is the
evidence for that. This is the cheapest of the three fixes and the one that best
matches the phase's purpose: it converts a property that holds *by absence of a
channel* into one that holds *by enforcement*, which is exactly the vacuity the
phase was created to eliminate. **CONSTRAINT-HIT: `execution_policy.rs` is
`SOURCE_INPUTS[16]`, so the contract digest moves. The admission gate names this
finding specifically as the live case.**

### F21-02-03 — HIGH — FIX

**Seam:** `crates/wcore-agent/src/engine.rs :: policy_gate` (census seam S3,
`crates/wcore-agent/src/policy_gate.rs` + `crates/wcore-permissions/src/policy.rs`;
the reachability defect is at the engine's field initialisers, which is where a
repair would land).

**Confirmed, and re-measured here rather than trusted.**
`git grep -nF 'set_policy_gate' -- crates/` returns exactly two hits, both in
`engine.rs` — the doc comment at `:2679` and the definition at `:4064`. **Zero
callers.** Every agent-path initialiser is `None` (`:3147`, `:3381`, and the
seven test constructors at `:15307 :16986 :17300 :18688 :19135 :19992 :21042`,
plus `orchestration/node_executor.rs:418`). `policy_gate.rs`'s own header states
the gate is opt-in and that a session without one sees "every tool runs".

**Concrete change (the only one that makes the gate reachable).** Replace
`policy_gate: None` at the two production constructors, `engine.rs:3147` and
`engine.rs:3381`, with a constructed `PolicyGate`.

**Flags — BEHAVIOUR CHANGE and ARCHITECTURE CHANGE, both, and this is not a
patch.** `PolicyEngine::check` (`wcore-permissions/src/policy.rs:120-157`) is
**deny-by-default**: it walks its grant list and returns
`Err(DenyReason::NoMatchingGrant)` when nothing matches. A gate constructed over
a fresh `PolicyEngine::new()` therefore denies **every tool for every existing
session**. Making it useful instead of catastrophic requires populating grants
from the parent's registry — which is an inheritance model, in a crate whose own
header (`policy.rs:1-5`) states its scope is *"explicit grants only. No role
hierarchy, no inheritance"*. That is the exact architecture change census OOP-2
routed OUT of this phase. **CONSTRAINT-HIT: `engine.rs` is `SOURCE_INPUTS[22]`.**

**And it is the secondary mechanism, not the primary.** The census is explicit:
`build_tool_registry` — F21-02-01's seam — is the mechanism that actually runs
for the tool dimension. HIGH-3 is "the policy layer that would express
parent/child intersection is not consulted", not "tools are ungated". If
F21-02-01 lands, the intersection exists at the seam that runs, and HIGH-3's
remaining content is a second, unused expression of the same property.

The triage proposes FIX because the amended rules give a HIGH only FIX or
DISPROVE, and DISPROVE is unavailable — the finding is confirmed true. The
honest expected outcome at the Task 2 authorization is **DECLINED**, recorded
with its evidence and left open. The blast-radius probe measures the cost so
that decision is made against a number.

### F21-02-04 … F21-02-10 — MEDIUM and LOW — BACKLOG

The amended rules give these exactly one disposition and there is no second
option for them, including for the ones that look quick. All are logged in
`.planning/BACKLOG.md` marked explicitly non-blocking with a pointer back to
their corpus case. F21-02-04 through F21-02-09 were logged by 21-02 itself;
F21-02-10 is appended by this plan.

`F21-02-10` additionally carries an **exclusion**: it is a PRE-EXISTING red,
already recorded at `.planning/TEST-AUDIT.md:171` as flaky 2/3 with the `ci`
profile's `retries=2` turning it green. It is excluded from this plan's bounded
repair budget under the plan's known-red exclusion rule, and logged so it is
retained rather than spent twice.

```
EXCLUDED :: F21-02-10 :: pre-existing :: TEST-AUDIT.md:171 records wcore-cli::deterministic_openai_loop packaged_core_cancels_an_active_stream as flaky 2/3 before Phase 21 existed; repairing it here would spend a bounded budget on a red another pass already triaged
```

### Surface-equivalence findings

Triaged separately as the plan requires, and the honest answer is that there are
none. `21-02-CORPUS-RESULTS.md:188-189` records
`MODE-EQUIVALENCE :: CONSISTENT` and `SURFACE-EQUIVALENCE :: CONSISTENT` over
88 executions, with no in-process-REFUSED against live-ALLOWED anywhere, and the
corpus explicitly states "No SURFACE-equivalence failure and no MODE-equivalence
failure was observed, so neither produces a finding of its own". No row is
invented for a failure that did not occur.

## 2. Deliverability against the two-iteration cap

```
DELIVERABILITY :: EXCEEDS :: 3 :: The full proposed set does not fit. F21-02-01 and F21-02-02 are each a contained edit at a named seam and together plausibly fit one edit-build-run iteration with a second in reserve. F21-02-03 does not fit at any iteration count: PolicyEngine is deny-by-default over an empty grant list, so the only change that makes the gate reachable denies every tool for every existing session, and the only change that makes it useful is a grant-inheritance model in a crate whose header states it deliberately has none - census OOP-2 routed exactly that out of Phase 21. Additionally all three edit sites touch the 40 pinned generator SOURCE_INPUTS, so each carries a wcore-contract digest re-run, a check, and a D1 section 3 re-pin under the admission gate's binding constraint. A subset excluding F21-02-03 is stated FITS at 2.
DELIVERABILITY-SUBSET :: FITS :: 2 :: F21-02-01 and F21-02-02 only, with F21-02-03 declined and left honestly open for 21-04 to state as an exception.
```

## 3. Blast radius, measured

Populated by Task 2 Step 1. Every candidate applied ALONE in the throwaway
Hetzner worktree `/root/wayland-p21-probe`, reverted between candidates, each
emitting its own verdict line into
`evidence/21-03-t2-blastradius.log`.

## 4. The panel

Populated by Task 2 Steps 2-5.

## 5. Authorization, per finding

Populated by Task 2 Step 4.

## 6. Post-repair delta

Populated by Task 3.
RUN_SHA=2d5a3d550dcd1e117b03249f6a5e20b66360f64b
HOST=hetzner-dsm
THROWAWAY_WORKTREE=/root/wayland-p21-probe (created from the triage commit, deleted after the probe)
PURPOSE=21-03 Task 2 Step 1 blast-radius probe. Each candidate applied ALONE, reverted between candidates.
NOTE=Pass 1's inline extraction missed nextest's 'TRY n TMT/FAIL [' prefix and the digest binary's key=value output, so its verdict lines were computed against an empty non-passing baseline. Those lines are retained below, renamed SUPERSEDED-BLAST-RADIUS, and the authoritative verdicts are the corrected pass at the end, re-derived from the SAME run's retained artifacts.
=================== PASS 1 TRANSCRIPT ===================
RUN_SHA=2d5a3d550dcd1e117b03249f6a5e20b66360f64b
PROBE_TREE=/root/wayland-p21-probe
PROBE_HEAD=2d5a3d550dcd1e117b03249f6a5e20b66360f64b
PROBE_PKGS=-p wcore-agent -p wcore-types -p wcore-permissions -p wcore-protocol -p wcore-budget
PROBE_START=2026-07-26T13:03:36Z
BASELINE TESTS_PASSED=3421 RC=100
BASELINE_NONPASSING_COUNT=0
     Summary [ 121.774s] 3422 tests run: 3421 passed (2 slow), 1 timed out, 12 skipped
   TRY 2 TMT [  60.006s] (3422/3422) wcore-agent::workflow_limits_test fix1_dispatch_budget_aborts_with_partial_result
error: test run failed
CONTRACT-DIGEST BASELINE RC=0
===== CANDIDATE F21-02-01 =====
APPLIED F21-02-01
--- files changed by F21-02-01 ---
crates/wcore-agent/src/bootstrap.rs
crates/wcore-agent/src/spawner.rs
--- test tail ---
  TRY 2 FAIL [   0.129s] (3381/3422) wcore-protocol::desktop_contract_corpus checked_corpus_matches_real_serializers_byte_for_byte
   TRY 2 TMT [  60.006s] (3422/3422) wcore-agent::workflow_limits_test fix1_dispatch_budget_aborts_with_partial_result
error: test run failed
--- non-passing after F21-02-01 ---
--- verdict-changing tests (present after, absent at baseline) ---
NEW_FAILURES_F21-02-01=0
--- contract digest after F21-02-01 ---
CONTRACT-DIGEST F21-02-01 UNCHANGED
SUPERSEDED-BLAST-RADIUS F21-02-01 RESULT=WIDE TESTS_BEFORE=3421 TESTS_AFTER=3420 RC=100
===== CANDIDATE F21-02-02 =====
APPLIED F21-02-02
--- files changed by F21-02-02 ---
crates/wcore-types/src/execution_policy.rs
--- test tail ---
  TRY 2 FAIL [   0.128s] (3381/3422) wcore-protocol::desktop_contract_corpus checked_corpus_matches_real_serializers_byte_for_byte
   TRY 2 TMT [  60.007s] (3422/3422) wcore-agent::workflow_limits_test fix1_dispatch_budget_aborts_with_partial_result
error: test run failed
--- non-passing after F21-02-02 ---
--- verdict-changing tests (present after, absent at baseline) ---
NEW_FAILURES_F21-02-02=0
--- contract digest after F21-02-02 ---
CONTRACT-DIGEST F21-02-02 UNCHANGED
SUPERSEDED-BLAST-RADIUS F21-02-02 RESULT=WIDE TESTS_BEFORE=3421 TESTS_AFTER=3420 RC=100
===== CANDIDATE F21-02-03 =====
APPLIED F21-02-03
--- files changed by F21-02-03 ---
crates/wcore-agent/src/engine.rs
--- test tail ---
  TRY 2 FAIL [   2.086s] (3413/3422) wcore-agent::dangerous_lease_e2e_test dangerous_expiry_reaches_bootstrapped_spawn_child
   TRY 2 TMT [  60.006s] (3422/3422) wcore-agent::workflow_limits_test fix1_dispatch_budget_aborts_with_partial_result
error: test run failed
--- non-passing after F21-02-03 ---
--- verdict-changing tests (present after, absent at baseline) ---
NEW_FAILURES_F21-02-03=0
--- contract digest after F21-02-03 ---
CONTRACT-DIGEST F21-02-03 UNCHANGED
SUPERSEDED-BLAST-RADIUS F21-02-03 RESULT=WIDE TESTS_BEFORE=3421 TESTS_AFTER=3398 RC=100
PROBE_TREE_CLEAN=0
PROBE_END=2026-07-26T13:17:45Z
=================== PASS 2 AUTHORITATIVE VERDICTS ===================
RUN_SHA=2d5a3d550dcd1e117b03249f6a5e20b66360f64b
VERDICT_PASS=corrected-extraction
VERDICT_SOURCE=/root/p21probe (artifacts retained from the single probe run)
VERDICT_AT=2026-07-26T13:21:31Z
BASELINE TESTS_PASSED=3421 RC=101 NONPASSING=1
BASELINE_NONPASSING_SET:
wcore-agent::workflow_limits_test fix1_dispatch_budget_aborts_with_partial_result
BASELINE_CONTRACT_DIGEST source_inputs_digest=sha256:d8b1a8b5645707225bdaafea617452fe1cf2f99556be53d7dcd35891d5e92f28
BASELINE_CONTRACT_DIGEST_MATCHES_PINNED_FIXTURE=yes
===== VERDICT F21-02-01 =====
NONPASSING_AFTER=2
wcore-agent::workflow_limits_test fix1_dispatch_budget_aborts_with_partial_result
wcore-protocol::desktop_contract_corpus checked_corpus_matches_real_serializers_byte_for_byte
VERDICT_CHANGING_TESTS=1
wcore-protocol::desktop_contract_corpus checked_corpus_matches_real_serializers_byte_for_byte
CONTRACT-DIGEST F21-02-01 CHANGED base=source_inputs_digest=sha256:d8b1a8b5645707225bdaafea617452fe1cf2f99556be53d7dcd35891d5e92f28 after=source_inputs_digest=sha256:9c7b98e291538fdbf304a6d594aa832bf865acae21db8cbac9e45968e69d4e53
BLAST-RADIUS F21-02-01 RESULT=WIDE TESTS_BEFORE=3421 TESTS_AFTER=3420 RC=101
===== VERDICT F21-02-02 =====
NONPASSING_AFTER=2
wcore-agent::workflow_limits_test fix1_dispatch_budget_aborts_with_partial_result
wcore-protocol::desktop_contract_corpus checked_corpus_matches_real_serializers_byte_for_byte
VERDICT_CHANGING_TESTS=1
wcore-protocol::desktop_contract_corpus checked_corpus_matches_real_serializers_byte_for_byte
CONTRACT-DIGEST F21-02-02 CHANGED base=source_inputs_digest=sha256:d8b1a8b5645707225bdaafea617452fe1cf2f99556be53d7dcd35891d5e92f28 after=source_inputs_digest=sha256:5b0263bc677cd2eaa3d75ad1b8f6a120f075012de4e18bacdd412ad4c378467e
BLAST-RADIUS F21-02-02 RESULT=WIDE TESTS_BEFORE=3421 TESTS_AFTER=3420 RC=101
===== VERDICT F21-02-03 =====
NONPASSING_AFTER=24
wcore-agent::dangerous_lease_e2e_test dangerous_expiry_cancels_production_streaming_bash_process_tree
wcore-agent::dangerous_lease_e2e_test dangerous_expiry_reaches_bootstrapped_spawn_child
wcore-agent engine::audit_2026_05_22_tests::actual_dispatch_crash_reopens_engine_and_blocks_opaque_replay
wcore-agent engine::audit_2026_05_22_tests::cancelled_persisted_approval_repairs_tool_pair_before_turn_cancelled
wcore-agent engine::audit_2026_05_22_tests::cancelled_run_snapshot_carries_delta_for_terminal_stream_end
wcore-agent engine::audit_2026_05_22_tests::newly_unknown_tool_stops_the_live_turn_before_another_provider_call
wcore-agent::execution_posture_e2e_test approval_bypass_retains_required_sandbox_and_tool_function
wcore-agent::json_stream_approval_test askuser_always_needs_approval_even_in_autoedit_mode_v093_w8_h2
wcore-agent::json_stream_approval_test non_askuser_answer_falls_through_to_execute
wcore-agent::json_stream_approval_test test_client_disconnect_aborts
wcore-agent::output_compaction_test case_6_compressed_content_reaches_llm
wcore-agent::output_compaction_test secret_tool_result_is_redacted_before_host_provider_and_persistence
wcore-agent::runaway_loop_test improving_numeric_outcomes_do_not_trip_route_monitor
wcore-agent::runaway_loop_test normalized_alternating_route_replans_once_then_stops_if_ignored
wcore-agent::runaway_loop_test one_step_volatile_route_replans_once_then_stops_if_ignored
wcore-agent::runaway_loop_test repeated_identical_successful_tool_call_converges_via_loopguard
wcore-agent::runaway_loop_test varied_bash_failures_remain_bounded_without_a_turn_cap
wcore-agent::typed_execution_policy_e2e_test typed_auto_edit_allows_write_but_denies_bash
wcore-agent::typed_execution_policy_e2e_test typed_bypass_executes_bash_inside_required_sandbox
wcore-agent::typed_execution_policy_e2e_test typed_bypass_executes_read_without_bypassing_required_sandbox
wcore-agent::w9_1_skill_drafting_per_turn engine_deduplicates_repeated_skill_drafted_emissions
wcore-agent::w9_1_skill_drafting_per_turn engine_emits_skill_drafted_trace_after_three_identical_turns
wcore-agent::workflow_limits_test fix1_dispatch_budget_aborts_with_partial_result
wcore-protocol::desktop_contract_corpus checked_corpus_matches_real_serializers_byte_for_byte
VERDICT_CHANGING_TESTS=23
wcore-agent::dangerous_lease_e2e_test dangerous_expiry_cancels_production_streaming_bash_process_tree
wcore-agent::dangerous_lease_e2e_test dangerous_expiry_reaches_bootstrapped_spawn_child
wcore-agent engine::audit_2026_05_22_tests::actual_dispatch_crash_reopens_engine_and_blocks_opaque_replay
wcore-agent engine::audit_2026_05_22_tests::cancelled_persisted_approval_repairs_tool_pair_before_turn_cancelled
wcore-agent engine::audit_2026_05_22_tests::cancelled_run_snapshot_carries_delta_for_terminal_stream_end
wcore-agent engine::audit_2026_05_22_tests::newly_unknown_tool_stops_the_live_turn_before_another_provider_call
wcore-agent::execution_posture_e2e_test approval_bypass_retains_required_sandbox_and_tool_function
wcore-agent::json_stream_approval_test askuser_always_needs_approval_even_in_autoedit_mode_v093_w8_h2
wcore-agent::json_stream_approval_test non_askuser_answer_falls_through_to_execute
wcore-agent::json_stream_approval_test test_client_disconnect_aborts
wcore-agent::output_compaction_test case_6_compressed_content_reaches_llm
wcore-agent::output_compaction_test secret_tool_result_is_redacted_before_host_provider_and_persistence
wcore-agent::runaway_loop_test improving_numeric_outcomes_do_not_trip_route_monitor
wcore-agent::runaway_loop_test normalized_alternating_route_replans_once_then_stops_if_ignored
wcore-agent::runaway_loop_test one_step_volatile_route_replans_once_then_stops_if_ignored
wcore-agent::runaway_loop_test repeated_identical_successful_tool_call_converges_via_loopguard
wcore-agent::runaway_loop_test varied_bash_failures_remain_bounded_without_a_turn_cap
wcore-agent::typed_execution_policy_e2e_test typed_auto_edit_allows_write_but_denies_bash
wcore-agent::typed_execution_policy_e2e_test typed_bypass_executes_bash_inside_required_sandbox
wcore-agent::typed_execution_policy_e2e_test typed_bypass_executes_read_without_bypassing_required_sandbox
wcore-agent::w9_1_skill_drafting_per_turn engine_deduplicates_repeated_skill_drafted_emissions
wcore-agent::w9_1_skill_drafting_per_turn engine_emits_skill_drafted_trace_after_three_identical_turns
wcore-protocol::desktop_contract_corpus checked_corpus_matches_real_serializers_byte_for_byte
CONTRACT-DIGEST F21-02-03 CHANGED base=source_inputs_digest=sha256:d8b1a8b5645707225bdaafea617452fe1cf2f99556be53d7dcd35891d5e92f28 after=source_inputs_digest=sha256:636807f92e2daaa22d64488c71de2bad103839a4a8b964f870895287380586c2
BLAST-RADIUS F21-02-03 RESULT=WIDE TESTS_BEFORE=3421 TESTS_AFTER=3398 RC=101
VERDICT_END=2026-07-26T13:21:31Z
//! v0.6.1 hardening (CRIT-1) — gate tool dispatch through the M5.8
//! `PolicyEngine`.
//!
//! v0.6.0 shipped `wcore-permissions` as orphan code: the crate compiled,
//! tests passed in isolation, but **no consumer in the engine called
//! `PolicyEngine::check`**. ACL grants existed only on paper. v0.6.1
//! installs this gate at the orchestration boundary so a configured
//! `PolicyEngine` is consulted before any tool runs.
//!
//! ## Backwards compatibility
//!
//! `PolicyGate` is **opt-in**. Sessions that do not configure one see
//! identical behaviour to v0.6.0 — every tool runs. The dispatch path
//! treats `Option<&PolicyGate>` as the canonical "is there a policy"
//! signal, so the cost on the unconfigured fast path is one `Option`
//! match per tool call.
//!
//! ## Actor resolution
//!
//! Top-level (main-agent) tool calls use the gate's configured
//! [`Actor`]. Sub-agent calls (where the orchestration layer knows the
//! spawning agent's name) use `Actor::Agent(name)` so a single
//! `PolicyEngine` can grant the main user tools the sub-agents do not
//! get. v0.6.1 keeps actor resolution simple — `Actor::System` (the
//! engine's free bypass) is intentionally not exposed here; tool
//! dispatches are never `System`.
//!
//! ## Threats closed by wiring this in
//!
//! - **T5** (tool path traversal) — `PolicyEngine::check` consults the
//!   already-implemented glob-deny logic.
//! - **T6** (debug leakage of grants) — `PolicyEngine`'s `Debug`
//!   redaction is now reachable by tool-trace consumers.
//! - **T7** (grant audit) — `set_audit_sink` events fire whenever a
//!   grant is added through the live engine.
//! - **T2** (token replay) and the bearer-token revocation path live
//!   one layer up at the session boundary, not here.

use std::sync::Arc;

use wcore_permissions::{Action, Actor, PolicyEngine, PolicyResult, Resource};

/// Wraps a [`PolicyEngine`] with the actor identity for a session.
///
/// Cheap to clone — the underlying `PolicyEngine` is shared by `Arc`
/// and the actor is a small enum.
#[derive(Debug, Clone)]
pub struct PolicyGate {
    engine: Arc<PolicyEngine>,
    /// Identity used when the dispatch path has no sub-agent name. For
    /// CLI sessions this is typically `Actor::User("default")`; hosts
    /// that surface real user identities set it to `Actor::User(name)`.
    default_actor: Actor,
}

impl PolicyGate {
    /// Construct a gate from a shared engine + default actor.
    pub fn new(engine: Arc<PolicyEngine>, default_actor: Actor) -> Self {
        Self {
            engine,
            default_actor,
        }
    }

    /// Check whether the dispatching actor may invoke `tool_name`.
    ///
    /// `source_agent = Some(name)` when the call comes from a spawned
    /// sub-agent; the gate uses `Actor::Agent(name)` in that case so
    /// the grant table can distinguish sub-agent capability from main
    /// agent capability. `None` falls back to the gate's default actor.
    pub fn check_tool(&self, tool_name: &str, source_agent: Option<&str>) -> PolicyResult<()> {
        let actor = match source_agent {
            Some(name) => Actor::Agent(name.to_owned()),
            None => self.default_actor.clone(),
        };
        self.engine.check(
            &actor,
            &Resource::Tool(tool_name.to_owned()),
            Action::Invoke,
        )
//! Wave B2 — `PolicyEngine`: explicit ACL over `(Actor, Resource, Action)`.
//!
//! v0.3 scope: explicit grants only. No role hierarchy, no inheritance, no
//! OAuth/OIDC. `Actor::System` is the single hard-coded bypass — it represents
//! the engine itself making internal calls, not a user-facing identity.

use std::fmt;
use std::sync::Arc;

use serde::{Deserialize, Serialize};

use crate::error::{DenyReason, PolicyResult};

/// Who is making the call.
///
/// `User` = human identity (alice, bob). `Agent` = sub-agent / spawned worker
/// (worker-1, planner). `System` = the engine itself — internal book-keeping
/// like reading config files, never an externally-presentable identity.
#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum Actor {
    User(String),
    Agent(String),
    System,
}

/// What is being acted on.
///
/// `File` resources support a minimal glob (see [`glob_match`]). `Memory` uses
/// the tier name as a string so the transport stays decoupled from
/// `wcore-memory`'s enum.
#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum Resource {
    Tool(String),
    File(String),
    McpServer(String),
    Memory(String),
}

/// What kind of operation is being attempted.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum Action {
    Invoke,
    Read,
    Write,
    Delete,
}

/// A single grant: actor X may perform action Y on resource Z.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Permission {
    pub actor: Actor,
    pub resource: Resource,
    pub action: Action,
}

/// T7 audit event emitted whenever `PolicyEngine::grant` is called and a
/// sink is configured. Wall clock is `Utc::now()` taken inside `grant`; if a
/// caller needs a stable clock for tests, mock at the sink boundary.
#[derive(Debug, Clone)]
pub struct GrantAuditEvent {
    pub permission: Permission,
    pub at_ms: i64,
}

/// T7 audit sink — invoked on every `PolicyEngine::grant` when a sink is
/// configured via `set_audit_sink`. Intentionally minimal: the sink decides
/// whether to hash-chain, ship to a remote logger, or just buffer in memory.
/// Implementors must be `Send + Sync` because `PolicyEngine` is cloneable
/// and may be shared across tasks.
pub trait GrantAuditSink: Send + Sync + fmt::Debug {
    fn record(&self, event: GrantAuditEvent);
}

/// In-memory policy store. Linear-scan on `check`; fine for v0.3's expected
/// grant count (single-digit to low-double-digit per session). If we ever
/// exceed that, swap the `Vec` for a `HashMap<(Actor, Action), Vec<Resource>>`.
#[derive(Debug, Default, Clone)]
pub struct PolicyEngine {
    grants: Vec<Permission>,
    /// T7 closure: optional audit sink. `None` by default — backwards
    }

    pub const fn sandbox(&self) -> SandboxPolicy {
        self.sandbox
    }

    pub const fn source(&self) -> PolicySource {
        self.source
    }

    pub const fn is_managed(&self) -> bool {
        self.managed_dangerous.is_some()
    }

    /// Apply an approval request without allowing a lower-trust caller to
    /// weaken an installed Managed floor. Smart sessions accept the selected
    /// approval policy; Managed sessions keep whichever policy asks for more
    /// consent.
    pub const fn with_requested_approvals(
        &self,
        requested: ApprovalPolicy,
        source: PolicySource,
    ) -> Self {
        if self.is_managed() {
            Self {
                posture: ExecutionPosture::Managed,
                approvals: stricter_approval_policy(self.approvals, requested),
                sandbox: SandboxPolicy::Required,
                source: PolicySource::Managed,
                managed_dangerous: self.managed_dangerous,
            }
        } else {
            Self::smart(requested, source)
        }
    }

    pub const fn managed_dangerous_policy(&self) -> Option<ManagedDangerousPolicy> {
        self.managed_dangerous
    }
}

const fn approval_strictness(policy: ApprovalPolicy) -> u8 {
    match policy {
        ApprovalPolicy::Prompt => 2,
        ApprovalPolicy::AutoEdit => 1,
        ApprovalPolicy::Bypass => 0,
    }
}

const fn stricter_approval_policy(left: ApprovalPolicy, right: ApprovalPolicy) -> ApprovalPolicy {
    if approval_strictness(left) >= approval_strictness(right) {
        left
    } else {
        right
    }
}

/// Explicit local process-launch request. This is never deserialized.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct DangerousLaunchRequest {
    source: PolicySource,
    ShardSummary {
        shard_id,
        agent_count: successes + failures,
        successes,
        failures,
        payload,
    }
}

type ToolFactory = fn() -> Box<dyn wcore_tools::Tool>;

/// Sub-agent tools that can read but not mutate host state. When a spawn
/// requests no explicit `allowed_tools`, the child is restricted to this
/// read-only subset (security audit H-7 / M-9): an empty `toolsets` on the
/// model-facing `Delegate`/`Spawn` tool must NOT silently grant the child
/// Bash/Write/Edit. Destructive tools require explicit opt-in via `allowed`.
fn build_tool_registry(
    allowed: &[String],
    requested_workspace: RequestedChildWorkspace,
    workspace_root: &Path,
    authority_read_deny: &[PathBuf],
    sandbox_runtime: Arc<wcore_sandbox::SandboxRegistry>,
) -> ToolRegistry {
    let all: &[(&str, ToolFactory)] = &[
        ("Read", || Box::new(ReadTool::new(None))),
        ("Write", || Box::new(WriteTool::new(None))),
        ("Edit", || Box::new(EditTool::new(None))),
        ("Bash", || Box::new(BashTool)),
        ("Grep", || Box::new(GrepTool)),
        ("Glob", || Box::new(GlobTool)),
    ];

    let mut registry = ToolRegistry::new();
    registry.set_sandbox_runtime(sandbox_runtime);
    let workspace_policy = Arc::new(
        WorkspacePolicy::contained(workspace_root)
            .with_authority_read_deny(authority_read_deny.iter().cloned())
            .with_authority_write_deny(authority_read_deny.iter().cloned())
            .with_git_authority_env_deny(),
    );
    let jail = SandboxedFs::new(
        SecretDenyFs::new(RealFs, Arc::clone(&workspace_policy)),
        workspace_root,
    );
    registry.set_tool_vfs(Arc::new(jail));
    registry.set_workspace_policy(workspace_policy);
    for (name, make_tool) in all {
        // Security audit H-7 / M-9: an empty `allowed` list no longer means
        // "register everything". It defaults to a read-only subset so a
        // `Delegate` call that omits `toolsets` can never hand a sub-agent
        // Bash/Write/Edit. Callers that genuinely need destructive tools must
        // name them explicitly in `allowed`.
        let permitted = if allowed.is_empty() {
            SHARED_READ_ONLY_CHILD_TOOLS.contains(name)
        } else {
            allowed.iter().any(|a| a.as_str() == *name)
        };
        let permitted = permitted
            && (requested_workspace == RequestedChildWorkspace::IsolatedMutation
                || SHARED_READ_ONLY_CHILD_TOOLS.contains(name));
        if permitted {
            registry.register(make_tool());
        }
    }
    registry
}

#[cfg(test)]
pub(crate) fn bind_test_durable_session(
    spawner: &AgentSpawner,
    root: &std::path::Path,
    session_id: &str,
) {
    let manager = crate::session::SessionManager::new(root.to_path_buf(), 10);
    let active = manager
        .create_for_run(
            "test-provider",
            "test-model",
            &root.to_string_lossy(),
            Some(session_id),
        )
# Core to Desktop Protocol Checkpoints — CTRL-02 (D1) + CTRL-04 (D2)

Desktop is the primary GUI/control plane; Core remains the enforcement authority and independently usable CLI/TUI engine. Core cannot claim Desktop behavior from Core tests alone.

## D1 - required after Phase 20 and before Phase 21 execution

Before Phase 21 broad execution, publish a linked Desktop plan and host conformance suite consuming a pinned clean Core producer. Record protocol version, fixture/schema digests, generator version, EffectiveExecutionPolicy semantics, lifecycle/correlation/ordering/duplicate/terminal/failure behavior, and ownership boundaries. Issue coordination is not completion.

## D2 - Phase 23 exit gate

Freeze durable Goal/child/task/wait commands, events, cursors, delivery, approval, failure and reconnect semantics. Replay canonical serialized fixtures through the real Desktop consumer/reducer. Deserialization alone is insufficient. Desktop may then implement the background-work control plane without inventing a second lifecycle.

Core Phase 22 proves producer fixtures and standalone/host protocol behavior. The linked Desktop lane proves consumer replay and UI/control behavior. Both receipts are required for a whole-Wayland claim; neither blocks Core-only engine claims outside the shared contract.

hook: PostToolUse
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk rg -n \"set_policy_gate|policy_gate: None|AgentSpawner::new|with_sandbox_runtime\\(registry\\.sandbox_runtime|fn tool_names|SOURCE_INPUTS|execution_policy\\.rs|bootstrap\\.rs|engine\\.rs\" ../../../../crates/wcore-agent/src/engine.rs ../../../../crates/wcore-agent/src/orchestration/node_executor.rs ../../../../crates/wcore-agent/src/bootstrap.rs ../../../../crates/wcore-tools/src/registry.rs ../../../../crates/wcore-protocol/src/contract/spec.rs
rtk git log --oneline -- ../../../../crates/wcore-protocol/contracts/desktop/v1/ | head -30
rtk rg -n \"tc_7_42_build_tool_registry_destructive_requires_opt_in|build_tool_registry\\(\" ../../../../crates/wcore-agent/src/spawner.rs" in /Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
hook: PostToolUse
hook: PostToolUse Completed
 succeeded in 1328ms:
b6936299 chore(protocol): re-pin Desktop contract provenance digests
48b9518b chore(protocol): re-pin Desktop contract provenance digests
6937ef61 chore(protocol): re-pin Desktop contract provenance digests
e761d24a feat(agent): type child transaction receipts
ada492d7 feat(protocol): expose durable child model
64ca6832 feat(mcp): add runtime removal lifecycle
367d8030 fix(protocol): make budget grants idempotent
73761fc0 fix(protocol): validate budget grants
e50ed423 fix(mcp): bind readiness to launch context
8d66807e feat(diagnostics): expose truthful runtime state
5ad7e6f4 feat(protocol): pin runtime diagnostics wire
70cd5d9b feat(providers): enforce semantic failover
906287e1 feat(recovery): seal interrupted turn state
50e1902e fix(contract): reseal hardened producer
3945b9f1 feat(contract): seal Desktop producer contract
d5a5bcaa test(protocol): check in Desktop v1 corpus
1013:        build_tool_registry(
2396:fn build_tool_registry(
4331:        let registry = build_tool_registry(
4357:    fn tc_7_42_build_tool_registry_destructive_requires_opt_in() {
4359:        let registry = build_tool_registry(
4386:        let registry = build_tool_registry(
4407:        let registry = build_tool_registry(
4425:        let registry = build_tool_registry(

codex
A material assumption needs checking before the vote: the proposed tool fix calls bootstrap the “single production construction site,” but the live source also constructs transient `AgentSpawner`s in `engine.rs`. I’m tracing whether those spawners can reach the same registry seam without the proposed parent-authority field; if they can, the partial fix would be structurally incomplete.
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk sed -n '220,280p' ../../../../crates/wcore-agent/src/engine.rs
rtk sed -n '680,830p' ../../../../crates/wcore-agent/src/spawner.rs
rtk sed -n '980,1035p' ../../../../crates/wcore-agent/src/spawner.rs
rtk sed -n '2160,2230p' ../../../../crates/wcore-agent/src/bootstrap.rs
rtk rg -n \"workflow_spawner|crucible_spawner|govern_transient_spawner|with_parent_tool_authority|clone_for_spawn|self\\.spawner|AgentSpawner\" ../../../../crates/wcore-agent/src/engine.rs ../../../../crates/wcore-agent/src/bootstrap.rs ../../../../crates/wcore-agent/src/spawner.rs | head -240" in /Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
 succeeded in 0ms:

    #[tokio::test]
    async fn hosted_workflow_and_crucible_spawners_share_canonical_session_authority() {
        let dir = tempfile::tempdir().unwrap();
        let authority = DurableSessionAuthority::new();
        let observer = authority.clone();
        let provider = Arc::new(CountingProvider {
            calls: AtomicUsize::new(0),
        });
        let provider_dyn: Arc<dyn LlmProvider> = provider.clone();
        let mut engine = AgentEngine::new_with_provider(
            Arc::clone(&provider_dyn),
            session_config(dir.path()),
            ToolRegistry::new(),
            Arc::new(NullSink),
        );
        engine
            .install_durable_session_authority(
                authority,
                wcore_types::execution_policy::EffectiveExecutionPolicy::baseline(
                    &engine.config.execution_policy,
                ),
            )
            .unwrap();
        engine
            .init_session("test", &dir.path().to_string_lossy(), Some("f190012"))
            .unwrap();

        // These are the exact transient spawners used by hosted live Workflow
        // and hosted /crucible after provider/resolver construction.
        let workflow_spawner = engine.govern_transient_spawner(AgentSpawner::new(
            Arc::clone(&provider_dyn),
            engine.config.clone(),
        ));
        let crucible_spawner = engine.govern_transient_spawner(AgentSpawner::new(
            Arc::clone(&provider_dyn),
            engine.config.clone(),
        ));
        assert_eq!(workflow_spawner.durable_session_id().unwrap(), "f190012");
        assert_eq!(crucible_spawner.durable_session_id().unwrap(), "f190012");

        let workflow = workflow_spawner
            .spawn_one_with_origin(child("hosted-workflow"), ChildOrigin::Workflow)
            .await;
        let council = crucible_spawner
            .spawn_one_with_origin(child("hosted-crucible"), ChildOrigin::Council)
            .await;
        assert!(!workflow.is_error, "{}", workflow.text);
        assert!(!council.is_error, "{}", council.text);
        assert_eq!(provider.calls.load(Ordering::SeqCst), 2);

        let token = observer.token().unwrap();
        observer
            .with_store(&token, |store| {
                let records = store.list()?;
                assert_eq!(records.len(), 2);
                for origin in [ChildOrigin::Workflow, ChildOrigin::Council] {
                    let record = records
                        .iter()
                        .find(|record| record.origin == origin)
                        .expect("hosted origin must be durable");

impl Drop for LifecycleGuard {
    fn drop(&mut self) {
        if let (Some(bus), TerminalOutcome::Pending) = (&self.bus, &self.outcome) {
            bus.publish(AgentMessage::Errored {
                agent: self.agent.clone(),
                error: "sub-agent dropped before completion".to_string(),
            });
        }
    }
}

/// Keeps aggregate child depth durable for the whole child lifetime. The
/// in-process guard updates shared counters; this wrapper commits its release
/// before the child scope disappears.
struct ChildBudgetGuard {
    guard: Option<wcore_budget::AgentDepthGuard>,
    authority: Option<crate::budget_authority::SharedBudgetAuthorityCoordinator>,
}

impl Drop for ChildBudgetGuard {
    fn drop(&mut self) {
        let Some(authority) = self.authority.as_ref() else {
            self.guard.take();
            return;
        };
        let guard = self.guard.take();
        if let Err(error) = authority.lock().transaction(|_| drop(guard)) {
            tracing::error!(error = %error, "durable child budget release failed");
        }
    }
}

/// Spawns independent child agents that share the parent's LLM provider.
///
/// Sub-agents use a [`NullSink`] so their streaming output is silently
/// discarded.  Results are collected via `engine.run()` and returned to the
/// parent which emits them as a single `tool_result` event — matching the
/// Claude Code pattern where only the parent writes to stdout.
pub struct AgentSpawner {
    provider: Arc<dyn LlmProvider>,
    base_config: Config,
    /// Immutable sandbox selected by the parent session. Every child registry
    /// receives this exact `Arc`; spawning must never re-read process-global
    /// sandbox settings or select a different backend mid-session.
    sandbox_runtime: Arc<wcore_sandbox::SandboxRegistry>,
    /// Canonical parent workspace supplied by Bootstrap. Process-global cwd is
    /// never session authority: parallel sessions and child execution may
    /// legitimately have different roots in the same process.
    parent_workspace: Option<Arc<PathBuf>>,
    /// Immutable outbound-network authority inherited from the parent session.
    /// Child engines must never fall back to a process-global compatibility
    /// policy after the bootstrap task-local scope has exited.
    egress_policy: wcore_egress::SharedPolicy,
    /// Shared live posture authority for host-backed sessions. Read only when
    /// deriving a child config so runtime de-escalation applies to descendants
    /// that have not started yet.
    approval_manager: Option<Arc<wcore_protocol::ToolApprovalManager>>,
    /// v0.8.0 Task J — optional `AgentBus` for lifecycle event
    /// publication. `None` preserves the legacy "silent spawner"
    /// behaviour expected by older tests; production callers attach the
    /// engine's bus via `with_bus(...)`.
    bus: Option<Arc<AgentBus>>,
    /// Parent cancellation token. Every spawned child engine is bound to a
    /// `child_token()` of this, so a host cancel (Esc) propagates into running
    /// sub-agents and they stop at the next turn boundary instead of burning
    /// LLM calls to completion. Defaults to a detached, never-cancelled token
    /// for legacy callers; production attaches the engine's token via
    /// `with_cancel(...)`.
    cancel: tokio_util::sync::CancellationToken,
    /// Production session handle. Reads the active turn token at spawn time,
    /// so both per-turn host cancellation and immutable session expiry reach
    /// children. Legacy/test spawners continue to use `cancel`.
    session_runtime: Option<crate::cancel::SessionRuntimeHandle>,
    /// Crucible (Mixture-of-Providers) — optional resolver that turns a
    /// per-spawn `SubAgentConfig.provider` spec into a keyed provider. `None`
    /// (the default) preserves single-provider behaviour: every child inherits
    /// `self.provider`. Production bootstrap attaches a `CouncilProviderResolver`
    /// via `with_provider_resolver(...)`. MUST be propagated by
    /// `clone_for_spawn` or fleet/parallel proposers silently fall back to the
    /// parent provider (the cross-provider-diversity guard catches this).
    resolver: Option<Arc<dyn ProviderResolver>>,
    /// Crucible cost governance — the per-session/per-day spend tracker shared
    /// with the engine. `None` ⇒ no aggregate cap (the council enforces only its
    /// per-run pin). MUST be propagated by `clone_for_spawn`.
    budget_tracker: Option<Arc<parking_lot::Mutex<wcore_budget::BudgetTracker>>>,
    /// (session_id, user_id) the council charges against — same envelope as the
    /// parent turn. None ⇒ council spend is not charged. Propagated by clone_for_spawn.
    budget_identity: Option<(String, String)>,
    /// Provider-call admission tracker shared by the parent engine and every
    /// spawned child. This is distinct from the cap-less Crucible accumulator
    /// above: it enforces the finite session token/cost envelope.
    provider_budget_tracker: Option<Arc<parking_lot::Mutex<wcore_budget::BudgetTracker>>>,
    /// Sole production provider/execution authority. Raw tracker/view fields
    /// remain only for legacy and isolated tests.
    budget_authority: Option<crate::budget_authority::SharedBudgetAuthorityCoordinator>,
    /// Stable provider-budget identity shared with every child engine.
    budget_session_id: Option<String>,
    /// Parent execution envelope. Spawn/fork paths derive child views from it
    /// so token, cost, process, runtime, and active-agent usage roll up.
    execution_budget: Option<wcore_budget::ExecutionBudgetView>,
    /// Keeps the standalone session's budget watcher alive for exactly as
    /// long as the spawner and its clones can dispatch child work.
    budget_guard: Option<Arc<crate::cancel::BudgetGuard>>,
    /// Clone-shared journal authority. It is deliberately unbound while tools
    /// are built and is bound only after the engine owns a canonical session.
    durable_authority: DurableSessionAuthority,
    /// Resolver-produced session policy used to derive redacted child evidence.
    effective_policy: EffectiveExecutionPolicy,
    /// Clone-shared admission boundary for child engines started by parallel
    /// Spawn, workflow, mesh, swarm, and fleet paths. Topology limits describe
    /// how much work may be scheduled; this semaphore independently bounds the
    /// number of full child engines that may own resources at once.
    active_child_permits: Arc<tokio::sync::Semaphore>,
}

/// Provider-spend, execution, and cancellation authority inherited by a
/// transient spawner that is created after session bootstrap (for example a
/// Council judge or an Anvil seat). Keeping these handles together prevents a
/// convenience constructor from silently minting a fresh budget.
#[derive(Clone)]
pub struct SpawnerBudgetGovernance {
    provider_budget_tracker: Option<Arc<parking_lot::Mutex<wcore_budget::BudgetTracker>>>,
    budget_authority: Option<crate::budget_authority::SharedBudgetAuthorityCoordinator>,
    budget_session_id: String,
    execution_budget: Option<wcore_budget::ExecutionBudgetView>,
    cancel: tokio_util::sync::CancellationToken,
    budget_guard: Option<Arc<crate::cancel::BudgetGuard>>,
    active_child_permits: Arc<tokio::sync::Semaphore>,
}

impl SpawnerBudgetGovernance {
    pub fn new(
        provider_budget_tracker: Arc<parking_lot::Mutex<wcore_budget::BudgetTracker>>,
        budget_session_id: impl Into<String>,
        execution_budget: wcore_budget::ExecutionBudgetView,
        cancel: tokio_util::sync::CancellationToken,
    ) -> Self {
        Self {
            provider_budget_tracker: Some(provider_budget_tracker),
            budget_authority: None,
            budget_session_id: budget_session_id.into(),
            execution_budget: Some(execution_budget),
            cancel,
            budget_guard: None,
            active_child_permits: Arc::new(tokio::sync::Semaphore::new(
                wcore_swarm::MAX_CONCURRENT_WORKERS,
            )),
        }
    }

            ))
        })?;
        if !workspace.is_dir() {
            return Err(DurableSpawnerError::WorkspacePreparation(
                "parent workspace is not a directory".to_owned(),
            ));
        }
        self.parent_workspace = Some(Arc::new(workspace));
        Ok(self)
    }

    /// Bind every spawned child engine to the parent's session-owned egress
    /// policy, including children created after bootstrap has returned.
    pub fn with_egress_policy(mut self, policy: wcore_egress::SharedPolicy) -> Self {
        self.egress_policy = policy;
        self
    }

    /// Bind child posture derivation to the host session's live manager.
    pub fn with_approval_manager(
        mut self,
        manager: Arc<wcore_protocol::ToolApprovalManager>,
    ) -> Self {
        self.approval_manager = Some(manager);
        self
    }

    /// Return the sandbox runtime inherited by spawned children.
    pub fn sandbox_runtime(&self) -> &Arc<wcore_sandbox::SandboxRegistry> {
        &self.sandbox_runtime
    }

    fn child_tool_registry(&self, launch: &ResolvedChildLaunch) -> ToolRegistry {
        build_tool_registry(
            &launch.overrides.allowed_tools,
            launch.requested_workspace,
            launch.workspace_root(),
            &launch.authority_read_deny,
            Arc::clone(&self.sandbox_runtime),
        )
    }

    /// Bind the spawner to the parent engine's cancellation token so a host
    /// cancel propagates into every spawned sub-agent. Production bootstrap
    /// attaches the engine's `cancel_token()` here, alongside `with_bus(...)`.
    pub fn with_cancel(mut self, cancel: tokio_util::sync::CancellationToken) -> Self {
        self.cancel = cancel;
        self
    }

    /// Bind production spawning to the session's shared active-turn handle.
    pub(crate) fn with_session_runtime(
        mut self,
        runtime: crate::cancel::SessionRuntimeHandle,
    ) -> Self {
        self.session_runtime = Some(runtime);
        // fields retain Smart defaults. The shared tracker identity prevents
        // children with session persistence disabled from receiving a fresh
        // provider budget under `session-unknown`.
        let session_budget = SessionBudgetEnvelope::from_config(&self.config);
        let span_sink_for_budget = self.span_sink.clone();
        if let Some(sink) = span_sink_for_budget.as_ref() {
            let bridge = Arc::new(
                wcore_observability::sink::ObservabilityBudgetEventBridge::new(sink.clone()),
            );
            session_budget.authority.lock().install_event_sink(bridge)?;
        }
        let budget_authority = Arc::clone(&session_budget.authority);
        let budget_authority_seed = session_budget.authority_seed.clone();
        // Tool construction precedes the canonical session journal. Share one
        // initially-unbound cell with every spawner clone, then let the engine
        // bind it after fresh/resume session authority exists.
        let durable_session_authority = crate::durable_spawner::DurableSessionAuthority::new();
        let sink_for_budget = self.output.clone();
        // Crucible cost governance — a cap-less per-user/day spend ACCUMULATOR for
        // council members, built whenever the council has a daily or per-run cap
        // configured (independent of [session_cap], which governs the per-turn
        // engine charge — a separate concern wired in the M5 block below). Built
        // BEFORE the spawner is Arc-wrapped (it is moved into tools just below).
        // It carries NO caps on purpose: the council's daily bound is the SOFT
        // pre-flight check in run_council, and per-member charging must ALWAYS
        // record (a capped charge would reject + drop, undercounting real spend).
        let council_budget_tracker = (self.config.crucible.daily_cap_usd.is_some()
            || self.config.crucible.max_cost_usd.is_some())
        .then(|| {
            Arc::new(parking_lot::Mutex::new(wcore_budget::BudgetTracker::new(
                wcore_budget::BudgetCap::default(),
            )))
        });
        let mut spawner_builder = session_budget
            .govern_spawner(
                crate::spawner::AgentSpawner::new(provider.clone(), self.config.clone()),
                session_runtime.active_turn_token(),
            )
            .with_durable_session_authority(
                durable_session_authority.clone(),
                effective_execution_policy.clone(),
            )?
            .with_sandbox_runtime(registry.sandbox_runtime())
            // Bind the parent repository identity so every child launch resolves
            // its shared/isolated workspace against this exact session root
            // instead of a process-global cwd. Mutating children allocate their
            // standalone checkout relative to this authority.
            .with_parent_workspace(cwd)?
            .with_egress_policy(Arc::new(
                self.session_egress_policy
                    .as_ref()
                    .expect("session egress policy is installed before scoped bootstrap")
                    .clone(),
            ))
            .with_session_runtime(session_runtime.clone())
            .with_bus(Arc::clone(&agent_bus));
        if let Some(manager) = self.approval_manager.as_ref() {
            spawner_builder = spawner_builder.with_approval_manager(Arc::clone(manager));
        }
        if let Some(tracker) = council_budget_tracker.as_ref() {
            // TODO(stage3): use the live per-conversation session id.
            spawner_builder = spawner_builder
                .with_budget_tracker(Arc::clone(tracker))
                .with_budget_identity("session", crate::engine::resolve_user_model_user_id());
        }
        let spawner = Arc::new(spawner_builder);
        let host_children = crate::spawner::HostChildController::new(Arc::clone(&spawner));
        // Lane D3 (G2/G4): register agents copied into installed marketplace
        // plugins (`<plugins-root>/<plugin>@<marketplace>/agents/*.yaml`),
        // namespaced `<marketplace>/<plugin>:<agent>` so agents from different
        // marketplaces never collide. A declarative plugin runs no code, so its
../../../../crates/wcore-agent/src/spawner.rs:60:/// Bootstrap constructs this from the same [`Arc<AgentSpawner>`] installed in
../../../../crates/wcore-agent/src/spawner.rs:66:    spawner: Arc<AgentSpawner>,
../../../../crates/wcore-agent/src/spawner.rs:70:    pub(crate) fn new(spawner: Arc<AgentSpawner>) -> Self {
../../../../crates/wcore-agent/src/spawner.rs:76:        self.spawner.spawn_host_child(config).await
../../../../crates/wcore-agent/src/spawner.rs:81:        self.spawner.durable_child_supervisor()
../../../../crates/wcore-agent/src/spawner.rs:719:pub struct AgentSpawner {
../../../../crates/wcore-agent/src/spawner.rs:759:    /// `clone_for_spawn` or fleet/parallel proposers silently fall back to the
../../../../crates/wcore-agent/src/spawner.rs:764:    /// per-run pin). MUST be propagated by `clone_for_spawn`.
../../../../crates/wcore-agent/src/spawner.rs:767:    /// parent turn. None ⇒ council spend is not charged. Propagated by clone_for_spawn.
../../../../crates/wcore-agent/src/spawner.rs:866:impl AgentSpawner {
../../../../crates/wcore-agent/src/spawner.rs:918:    /// Host adapters that construct an [`AgentSpawner`] outside
../../../../crates/wcore-agent/src/spawner.rs:1702:                    let spawner = self.clone_for_spawn();
../../../../crates/wcore-agent/src/spawner.rs:1773:                let spawner = self.clone_for_spawn();
../../../../crates/wcore-agent/src/spawner.rs:1864:            let spawner = self.clone_for_spawn();
../../../../crates/wcore-agent/src/spawner.rs:2138:    pub(crate) fn clone_for_spawn(&self) -> Self {
../../../../crates/wcore-agent/src/spawner.rs:2151:            // `clone_for_spawn()` copy; dropping the resolver here would make
../../../../crates/wcore-agent/src/spawner.rs:2157:            // on the fleet/parallel `clone_for_spawn()` copies and silently lose
../../../../crates/wcore-agent/src/spawner.rs:2182:        let mut cloned = self.clone_for_spawn();
../../../../crates/wcore-agent/src/spawner.rs:2197:        let mut cloned = self.clone_for_spawn();
../../../../crates/wcore-agent/src/spawner.rs:2290:impl Spawner for AgentSpawner {
../../../../crates/wcore-agent/src/spawner.rs:2449:    spawner: &AgentSpawner,
../../../../crates/wcore-agent/src/spawner.rs:2482:        AgentSpawner, SpawnTaskSet, SpawnerBudgetGovernance, SubAgentConfig, SubAgentResult,
../../../../crates/wcore-agent/src/spawner.rs:2629:        let spawner = AgentSpawner::new(provider.clone(), config)
../../../../crates/wcore-agent/src/spawner.rs:2665:        let spawner = AgentSpawner::new(provider.clone(), config)
../../../../crates/wcore-agent/src/spawner.rs:2669:        let cloned = spawner.clone_for_spawn();
../../../../crates/wcore-agent/src/spawner.rs:2731:        let spawner = AgentSpawner::new(provider.clone(), config)
../../../../crates/wcore-agent/src/spawner.rs:2779:        let spawner = AgentSpawner::new(provider.clone(), Config::default())
../../../../crates/wcore-agent/src/spawner.rs:2804:        let parent = AgentSpawner::new(Arc::clone(&provider), Config::default())
../../../../crates/wcore-agent/src/spawner.rs:2813:            AgentSpawner::new(provider, Config::default()).with_budget_governance(governance);
../../../../crates/wcore-agent/src/spawner.rs:2882:        let spawner = AgentSpawner::new(provider, Config::default()).with_budget_governance(
../../../../crates/wcore-agent/src/spawner.rs:2923:        let spawner = AgentSpawner::new(provider.clone(), config)
../../../../crates/wcore-agent/src/spawner.rs:2986:    use super::{AgentSpawner, DurableSessionAuthority, ForkOverrides, SubAgentConfig};
../../../../crates/wcore-agent/src/spawner.rs:3005:        let spawner = AgentSpawner::new(Arc::new(StubProvider), Config::default())
../../../../crates/wcore-agent/src/spawner.rs:3070:        let spawner = AgentSpawner::new(parent.clone(), Config::default());
../../../../crates/wcore-agent/src/spawner.rs:3079:        let spawner = AgentSpawner::new(parent.clone(), Config::default())
../../../../crates/wcore-agent/src/spawner.rs:3091:        let spawner = AgentSpawner::new(parent, Config::default());
../../../../crates/wcore-agent/src/spawner.rs:3104:        let spawner = AgentSpawner::new(parent, Config::default())
../../../../crates/wcore-agent/src/spawner.rs:3114:    fn clone_for_spawn_preserves_resolver() {
../../../../crates/wcore-agent/src/spawner.rs:3119:        let spawner = AgentSpawner::new(parent, Config::default())
../../../../crates/wcore-agent/src/spawner.rs:3121:        let cloned = spawner.clone_for_spawn();
../../../../crates/wcore-agent/src/spawner.rs:3132:    fn clone_for_spawn_preserves_exact_parent_egress_policy() {
../../../../crates/wcore-agent/src/spawner.rs:3136:            AgentSpawner::new(parent, Config::default()).with_egress_policy(policy.clone());
../../../../crates/wcore-agent/src/spawner.rs:3137:        let cloned = spawner.clone_for_spawn();
../../../../crates/wcore-agent/src/spawner.rs:3146:        let spawner = AgentSpawner::new(parent, Config::default());
../../../../crates/wcore-agent/src/spawner.rs:3171:        let spawner = AgentSpawner::new(parent, base);
../../../../crates/wcore-agent/src/spawner.rs:3180:    fn budget_tracker_attaches_and_survives_clone_for_spawn() {
../../../../crates/wcore-agent/src/spawner.rs:3185:        let s = AgentSpawner::new(parent, Config::default()).with_budget_tracker(tracker.clone());
../../../../crates/wcore-agent/src/spawner.rs:3187:        assert!(s.clone_for_spawn().budget_tracker().is_some());
../../../../crates/wcore-agent/src/spawner.rs:3204:        let spawner = AgentSpawner::new(parent, Config::default())
../../../../crates/wcore-agent/src/spawner.rs:3233:                .clone_for_spawn()
../../../../crates/wcore-agent/src/spawner.rs:3269:        let spawner = AgentSpawner::new(parent, Config::default())
../../../../crates/wcore-agent/src/spawner.rs:3431:        let spawner = AgentSpawner::new(parent, Config::default())
../../../../crates/wcore-agent/src/spawner.rs:3488:        AgentSpawner, DurableCancelDisposition, DurableSessionAuthority, ForkOverrides,
../../../../crates/wcore-agent/src/spawner.rs:3597:    ) -> AgentSpawner {
../../../../crates/wcore-agent/src/spawner.rs:3606:        AgentSpawner::new(provider, config)
../../../../crates/wcore-agent/src/spawner.rs:3618:    ) -> AgentSpawner {
../../../../crates/wcore-agent/src/spawner.rs:3628:        AgentSpawner::new(provider, config)
../../../../crates/wcore-agent/src/spawner.rs:3708:        let spawner = AgentSpawner::new(provider_dyn, Config::default())
../../../../crates/wcore-agent/src/spawner.rs:4277:    use super::{AgentSpawner, ForkOverrides, SubAgentConfig, build_tool_registry};
../../../../crates/wcore-agent/src/spawner.rs:4442:        let spawner = AgentSpawner::new(provider, Config::default())
../../../../crates/wcore-agent/src/spawner.rs:4446:        let cloned = spawner.clone_for_spawn();
../../../../crates/wcore-agent/src/spawner.rs:4481:    //! child config built by `AgentSpawner::child_config` carries the parent's
../../../../crates/wcore-agent/src/spawner.rs:4502:    use super::{AgentSpawner, SubAgentConfig};
../../../../crates/wcore-agent/src/spawner.rs:4567:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), parent);
../../../../crates/wcore-agent/src/spawner.rs:4587:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), parent);
../../../../crates/wcore-agent/src/spawner.rs:4603:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), parent)
../../../../crates/wcore-agent/src/spawner.rs:4614:        let cloned_child = spawner.clone_for_spawn().child_config(&sub_config());
../../../../crates/wcore-agent/src/spawner.rs:4653:        let template = AgentSpawner::new(Arc::new(NeverProvider), authority.clone());
../../../../crates/wcore-agent/src/spawner.rs:4684:        let standalone = AgentSpawner::new(Arc::new(NeverProvider), routed)
../../../../crates/wcore-agent/src/spawner.rs:4713:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), parent);
../../../../crates/wcore-agent/src/spawner.rs:4739:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), parent);
../../../../crates/wcore-agent/src/spawner.rs:4771:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), Config::default())
../../../../crates/wcore-agent/src/spawner.rs:4777:        let cloned_spawner = spawner.clone_for_spawn();
../../../../crates/wcore-agent/src/spawner.rs:4808:        let spawner = AgentSpawner::new(provider.clone(), config)
../../../../crates/wcore-agent/src/bootstrap.rs:163:/// to `AgentSpawner::new`'s legacy unbounded defaults.
../../../../crates/wcore-agent/src/bootstrap.rs:208:        spawner: crate::spawner::AgentSpawner,
../../../../crates/wcore-agent/src/bootstrap.rs:210:    ) -> crate::spawner::AgentSpawner {
../../../../crates/wcore-agent/src/bootstrap.rs:225:    spawner: crate::spawner::AgentSpawner,
../../../../crates/wcore-agent/src/bootstrap.rs:227:) -> anyhow::Result<crate::spawner::AgentSpawner> {
../../../../crates/wcore-agent/src/bootstrap.rs:2195:                crate::spawner::AgentSpawner::new(provider.clone(), self.config.clone()),
../../../../crates/wcore-agent/src/bootstrap.rs:2292:        // SpawnTool; shares the same `AgentSpawner` (the runner borrows it per
../../../../crates/wcore-agent/src/bootstrap.rs:2307:        // agents + OutputSink relay. Both share AgentSpawner via the
../../../../crates/wcore-agent/src/engine.rs:81:    use crate::spawner::AgentSpawner;
../../../../crates/wcore-agent/src/engine.rs:222:    async fn hosted_workflow_and_crucible_spawners_share_canonical_session_authority() {
../../../../crates/wcore-agent/src/engine.rs:250:        let workflow_spawner = engine.govern_transient_spawner(AgentSpawner::new(
../../../../crates/wcore-agent/src/engine.rs:254:        let crucible_spawner = engine.govern_transient_spawner(AgentSpawner::new(
../../../../crates/wcore-agent/src/engine.rs:258:        assert_eq!(workflow_spawner.durable_session_id().unwrap(), "f190012");
../../../../crates/wcore-agent/src/engine.rs:259:        assert_eq!(crucible_spawner.durable_session_id().unwrap(), "f190012");
../../../../crates/wcore-agent/src/engine.rs:261:        let workflow = workflow_spawner
../../../../crates/wcore-agent/src/engine.rs:264:        let council = crucible_spawner
../../../../crates/wcore-agent/src/engine.rs:1538:    crate::spawner::AgentSpawner,
../../../../crates/wcore-agent/src/engine.rs:1542:    spawner: crate::spawner::AgentSpawner,
../../../../crates/wcore-agent/src/engine.rs:1567:    spawner: crate::spawner::AgentSpawner,
../../../../crates/wcore-agent/src/engine.rs:2810:    /// live confirm gate can build a transient `AgentSpawner` (which needs a
../../../../crates/wcore-agent/src/engine.rs:3026:        // on `self.config` solely for the live gate's transient `AgentSpawner`.
../../../../crates/wcore-agent/src/engine.rs:3985:    fn govern_transient_spawner(
../../../../crates/wcore-agent/src/engine.rs:3987:        spawner: crate::spawner::AgentSpawner,
../../../../crates/wcore-agent/src/engine.rs:3988:    ) -> crate::spawner::AgentSpawner {
../../../../crates/wcore-agent/src/engine.rs:4007:            let unbound = spawner.clone_for_spawn();
../../../../crates/wcore-agent/src/engine.rs:8749:        // `workflow_live_mode = false` (set in `AgentSpawner::child_config`) AND
../../../../crates/wcore-agent/src/engine.rs:12058:        // read-only toolset and approval posture (see `AgentSpawner::spawn_one`).
../../../../crates/wcore-agent/src/engine.rs:12061:        let spawner = crate::spawner::AgentSpawner::new(
../../../../crates/wcore-agent/src/engine.rs:12070:        let spawner = self.govern_transient_spawner(spawner);
../../../../crates/wcore-agent/src/engine.rs:12398:        let spawner = crate::spawner::AgentSpawner::new(
../../../../crates/wcore-agent/src/engine.rs:12414:        let mut spawner = self.govern_transient_spawner(spawner);
../../../../crates/wcore-agent/src/engine.rs:19760:    spawner: crate::spawner::AgentSpawner,

hook: PostToolUse
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk sed -n '3960,4030p' ../../../../crates/wcore-agent/src/engine.rs
rtk sed -n '12020,12095p' ../../../../crates/wcore-agent/src/engine.rs
rtk sed -n '12360,12435p' ../../../../crates/wcore-agent/src/engine.rs
rtk sed -n '2128,2175p' ../../../../crates/wcore-agent/src/spawner.rs
rtk sed -n '850,970p' ../../../../crates/wcore-agent/src/spawner.rs
rtk sed -n '140,245p' ../../../../crates/wcore-agent/src/bootstrap.rs" in /Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
 succeeded in 0ms:
            .lock()
            .extend_session_idempotent(
                &session_id,
                request_id,
                additional_tokens,
                additional_cost_usd,
            )
            .map_err(classify_budget_grant_error)?;
        Ok(())
    }

    /// Install the session-root execution envelope built from resolved config.
    pub fn set_execution_budget(&mut self, budget: crate::budget::ExecutionBudgetView) {
        self.execution_budget = budget;
    }

    pub fn execution_budget(&self) -> &crate::budget::ExecutionBudgetView {
        &self.execution_budget
    }

    /// Bind a transient spawner to this engine's live session envelope.
    ///
    /// The provider tracker is optional for legacy/test engines, but execution
    /// and cancellation authority always inherit from the parent. Callers must
    /// not manufacture a separate provider allowance when the parent has none.
    fn govern_transient_spawner(
        &self,
        spawner: crate::spawner::AgentSpawner,
    ) -> crate::spawner::AgentSpawner {
        // Bind the parent repository identity so a transient child resolves the
        // SAME shared/isolated workspace authority as the bootstrap spawner. A
        // mutating child then allocates its standalone checkout relative to this
        // root; the engine never rewrites the child's working directory back to
        // the parent workspace. A contained session binds from its workspace
        // policy root; a non-contained session (no policy) still has an
        // authoritative parent identity — the live session directory (the same
        // authoritative source the durable-workspace retention path reads via
        // `base_config.session.directory`). Binding it lets non-contained
        // transient children resolve SharedReadOnly against the real session
        // workspace instead of failing "parent workspace authority is not bound".
        // The session root is materialized on demand exactly as the durable
        // checkout path does (`prepare_child_workspace`), and only when it is
        // absolute — a relative directory would resolve against the process cwd,
        // which must never become a child's parent identity. When the chosen
        // root is unresolvable, keep the unbound spawner (fail-closed at spawn
        // time) rather than losing the governed handle.
        let spawner = {
            let unbound = spawner.clone_for_spawn();
            let parent_root = match self.tools.workspace_policy() {
                Some(policy) => Some(policy.root().to_path_buf()),
                None => {
                    let session_root = std::path::Path::new(self.config.session.directory.as_str());
                    if session_root.is_absolute() && std::fs::create_dir_all(session_root).is_ok() {
                        Some(session_root.to_path_buf())
                    } else {
                        None
                    }
                }
            };
            match parent_root {
                Some(root) => spawner.with_parent_workspace(root).unwrap_or(unbound),
                None => unbound,
            }
        };
        let spawner = match self.durable_session_authority.as_ref() {
            Some(authority) => spawner
                .with_shared_durable_session_authority(authority.clone())
                .expect("installed durable session authority carries its effective policy"),
            None => spawner,
        };
        if let Some(authority) = self.budget_authority.as_ref() {
                    .emit_midflight_monitor_decision(MonitorDirective::Replan, reason);
                self.emit_midflight_monitor_occurrence();
            }

            // Save session after each turn
            self.prepare_durable_conversation().await?;
            self.save_session_mirror();
            turn += 1;
        }
    }

    /// Dynamic Workflows B6 — the live workflow confirm gate body.
    ///
    /// Synthesises a [`WorkflowPlan`] from `user_input`, asks the host to
    /// confirm running it (via the existing `ToolRequest` + `ApprovalRequired`
    /// protocol round-trip), and — only on explicit approval — executes the
    /// workflow and returns its rendered result as the turn output.
    ///
    /// Returns:
    ///   * `Some(AgentResult)` — the user approved and the workflow ran (the
    ///     result is the turn output);
    ///   * `None` — the user declined, the turn was cancelled, OR synthesis
    ///     failed. In every `None` case the caller falls through to the normal
    ///     single-agent turn, so the user always gets a response.
    ///
    /// The caller has already verified `workflow_live_mode`, the candidate
    /// heuristic, and that both the approval manager and protocol writer are
    /// wired; this method re-binds those two `Option`s. It runs as a PRE-LLM
    /// intercept in `run()` (before the turn loop), so `turn` is `0`.
    async fn try_live_workflow(&self, user_input: &str, turn: usize) -> Option<AgentResult> {
        use wcore_protocol::events::{OutputType, ProtocolEvent, ToolInfo, ToolStatus};

        // Re-bind the two collaborators the caller already proved present.
        let manager = self.approval_manager.as_ref()?;
        let writer = self.protocol_writer.as_ref()?;

        // (a) Transient spawner sharing the parent provider + retained config.
        // SECURITY: no forced `auto_approve` — sub-agents inherit the parent's
        // read-only toolset and approval posture (see `AgentSpawner::spawn_one`).
        // It is owned (`Arc` + `Config`, no borrows of `self`) so it can be
        // MOVED onto the detached tasks below.
        let spawner = crate::spawner::AgentSpawner::new(
            std::sync::Arc::clone(&self.provider),
            self.config.clone(),
        )
        .with_sandbox_runtime(self.tools.sandbox_runtime())
        .with_egress_policy(self.egress_policy.clone())
        .with_approval_manager(std::sync::Arc::clone(manager));
        // Bind sub-agents to the same provider, execution, and cancellation
        // envelope used by every other transient orchestration path.
        let spawner = self.govern_transient_spawner(spawner);

        // (b) Synthesise the plan on a DETACHED task. The synthesis sub-agent
        // runs its own `engine.run`, which the compiler cannot prove never
        // re-enters this gate; running it behind `tokio::spawn` forces the
        // `Send + 'static` boundary once here, severing the otherwise-infinite
        // async-recursion type cycle. On ANY synthesis error (or join failure)
        // we log and fall through — a failed synthesis must NEVER abort the
        // session.
        //
        // GAP-5/7: synthesis is up to 3 LLM round-trips with no signal. In live
        // mode the operator opted in and the input matched the workflow
        // heuristic, so: (1) emit a progress indicator instead of a silent wait,
        // (2) bound it with a timeout so a hung synthesis LLM falls through to a
        // normal turn rather than stalling forever, and (3) on any failure leave
        // a one-line note so the plain answer that follows isn't an unexplained
        // surprise. All three fall-through paths still return `None` — a failed
        // synthesis must NEVER abort the session.
        let _ = writer.emit(&ProtocolEvent::Info {
            msg_id: self.current_msg_id.clone(),
            message: "Designing a workflow for this…".to_string(),
        });
        let synth_input = user_input.to_string();
        let synth_task = tokio::spawn(synthesize_workflow_owned(spawner, synth_input));
        let synth_abort = synth_task.abort_handle();
        let synth_note = |message: &str| {
        // keys every env-/store-credentialed provider. Wiring the inline
        // `[providers]` map into the engine is a follow-up.
        let base = self.config.clone();
        let providers = std::collections::HashMap::new();

        if task.is_empty() {
            let msg = "Usage: /crucible <task> — convene a cross-vendor council to \
                       cross-check an answer."
                .to_string();
            return Some(self.crucible_result(msg));
        }

        // Candidate pool: configured proposers ∪ candidate_pool, or — when BOTH
        // are empty (the default-config case) — the cross-vendor DEFAULT_FLUX_POOL
        // (Stage 4c empty-state self-bootstrap), then filtered to runnable (keyed)
        // specs.
        let candidates = crate::orchestration::council::bootstrap_pool(&cfg);
        let runnable = crate::orchestration::council::CouncilProviderResolver::new(
            base.clone(),
            providers.clone(),
        )
        .with_egress_policy(self.egress_policy.clone())
        .resolvable_specs(&candidates);
        if runnable.is_empty() {
            let msg = "crucible: no runnable council candidates — connect a provider \
                       Crucible can run (e.g. a Flux key) or set `proposers` / \
                       `candidate_pool` under `[crucible]` with keyed providers."
                .to_string();
            return Some(self.crucible_result(msg));
        }

        // Transient spawner: parent provider + a council resolver, bound to the
        // engine cancel token. Attach a cap-less council budget tracker when a cap
        // is configured. NOTE: this tracker is fresh per /crucible call, so the
        // per-run ceiling binds every invocation but the DAILY envelope does NOT
        // yet aggregate across calls in a session — cross-session/process daily
        // spend persistence is the Stage 6 deliverable (spec §9, "Cross-process
        // daily-spend"). Until then each council is bounded by its per-run cap.
        let spawner = crate::spawner::AgentSpawner::new(
            std::sync::Arc::clone(&self.provider),
            self.config.clone(),
        )
        .with_sandbox_runtime(self.tools.sandbox_runtime())
        .with_egress_policy(self.egress_policy.clone())
        .with_provider_resolver(std::sync::Arc::new(
            crate::orchestration::council::CouncilProviderResolver::new(
                base.clone(),
                providers.clone(),
            )
            .with_egress_policy(self.egress_policy.clone()),
        ));
        // A council may select different keyed providers, but every physical
        // call remains inside the parent's provider/session allowance and every
        // child shares the parent's execution and cancellation envelope.
        let mut spawner = self.govern_transient_spawner(spawner);
        if let Some(manager) = &self.approval_manager {
            spawner = spawner.with_approval_manager(std::sync::Arc::clone(manager));
        }
        if cfg.daily_cap_usd.is_some() || cfg.max_cost_usd.is_some() {
            let tracker = std::sync::Arc::new(parking_lot::Mutex::new(
                wcore_budget::BudgetTracker::new(wcore_budget::BudgetCap::default()),
            ));
            spawner = spawner
                .with_budget_tracker(tracker)
                .with_budget_identity("crucible".to_string(), "default".to_string());
        }

        // Build the proposal card UP FRONT so it can ride the ToolRequest's
        // `tool.args`. The approval-emitter dedupe (engine_bridge ChannelEmitter +
        // main GatingProtocolWriter) synthesizes the host-visible ApprovalRequired
        // FROM the ToolRequest and SUPPRESSES the explicit one, so the typed plan
        // MUST travel via args to reach the host (spec §3.1). assemble is pure, so
        // this first plan matches the one drive_council re-derives below.
        let ov = crate::orchestration::council::CouncilOverrides::default();
        let card_gate = crate::orchestration::council::build_gate(&ov, &task);
        let card_policy = crate::orchestration::council::build_policy(&cfg, &ov);
        // B6 defense-in-depth — the LIVE workflow confirm gate is a top-level,
        // user-initiated pre-LLM intercept. Child engines already lack an
        // approval manager + protocol writer (so the gate's guard short-circuits
        // for them), but force the mode off here too so a workflow's sub-agents
        // can NEVER recursively re-enter the gate regardless of how they are
        // wired.
        config.observability.workflow_live_mode = false;
        config
    }

    pub(crate) fn clone_for_spawn(&self) -> Self {
        Self {
            provider: self.provider.clone(),
            base_config: self.base_config.clone(),
            sandbox_runtime: Arc::clone(&self.sandbox_runtime),
            parent_workspace: self.parent_workspace.clone(),
            egress_policy: self.egress_policy.clone(),
            approval_manager: self.approval_manager.clone(),
            bus: self.bus.clone(),
            cancel: self.cancel.clone(),
            session_runtime: self.session_runtime.clone(),
            // CRITICAL (crucible): the resolver MUST be carried into every
            // cloned spawner. The fleet + parallel paths run each proposer on a
            // `clone_for_spawn()` copy; dropping the resolver here would make
            // pinned proposers silently fall back to the parent provider,
            // collapsing the cross-provider council into a single-provider one.
            resolver: self.resolver.clone(),
            // CRITICAL (crucible): the shared budget tracker MUST be carried into
            // every cloned spawner. If it isn't propagated, council members run
            // on the fleet/parallel `clone_for_spawn()` copies and silently lose
            // the per-session/day envelope.
            budget_tracker: self.budget_tracker.clone(),
            budget_identity: self.budget_identity.clone(),
            provider_budget_tracker: self.provider_budget_tracker.clone(),
            budget_authority: self.budget_authority.clone(),
            budget_session_id: self.budget_session_id.clone(),
            execution_budget: self.execution_budget.clone(),
            budget_guard: self.budget_guard.clone(),
            durable_authority: self.durable_authority.clone(),
            effective_policy: self.effective_policy.clone(),
            active_child_permits: Arc::clone(&self.active_child_permits),
        }
    }

    /// Clone every inherited child authority while pinning an already-resolved
    /// provider. The returned spawner accepts an unpinned child request, so the
    /// provider cannot be resolved a second time between council admission and
    /// durable launch.
        mut self,
        budget_guard: Arc<crate::cancel::BudgetGuard>,
    ) -> Self {
        self.budget_guard = Some(budget_guard);
        self
    }

    /// Stable Core session lineage shared by the parent and every transient
    /// child. Producer contracts use it only when no persisted host session
    /// identity has been bound to the output sink yet.
    #[must_use]
    pub fn session_id(&self) -> &str {
        &self.budget_session_id
    }
}

impl AgentSpawner {
    pub fn new(provider: Arc<dyn LlmProvider>, config: Config) -> Self {
        let sandbox_runtime = ToolRegistry::new().sandbox_runtime();
        let effective_policy = EffectiveExecutionPolicy::baseline(&config.execution_policy);
        Self {
            provider,
            base_config: config,
            sandbox_runtime,
            parent_workspace: None,
            egress_policy: wcore_egress::default_policy(),
            approval_manager: None,
            bus: None,
            cancel: tokio_util::sync::CancellationToken::new(),
            session_runtime: None,
            resolver: None,
            budget_tracker: None,
            budget_identity: None,
            provider_budget_tracker: None,
            budget_authority: None,
            budget_session_id: None,
            execution_budget: None,
            budget_guard: None,
            durable_authority: DurableSessionAuthority::new(),
            effective_policy,
            active_child_permits: Arc::new(tokio::sync::Semaphore::new(
                wcore_swarm::MAX_CONCURRENT_WORKERS,
            )),
        }
    }

    /// Install the one session authority shared by every transient spawner.
    pub(crate) fn with_durable_session_authority(
        mut self,
        authority: DurableSessionAuthority,
        effective_policy: EffectiveExecutionPolicy,
    ) -> Result<Self, DurableSpawnerError> {
        authority.install_effective_policy(effective_policy.clone())?;
        self.durable_authority = authority;
        self.effective_policy = effective_policy;
        Ok(self)
    }

    pub(crate) fn with_shared_durable_session_authority(
        self,
        authority: DurableSessionAuthority,
    ) -> Result<Self, DurableSpawnerError> {
        let effective_policy = authority.effective_policy()?;
        self.with_durable_session_authority(authority, effective_policy)
    }

    /// Bind this spawner and every clone it creates to one canonical session.
    ///
    /// Host adapters that construct an [`AgentSpawner`] outside
    /// [`AgentEngine`](crate::engine::AgentEngine) must call this before any
    /// child launch. A missing, mismatched, or non-canonical journal fails
    /// closed; no ephemeral execution fallback exists.
    pub fn bind_durable_session(
        &self,
        journal: SessionJournal,
        expected_session_id: &str,
    ) -> Result<(), DurableSpawnerError> {
        self.durable_authority
            .bind(journal, expected_session_id)
            .map(|_| ())
    }

    /// Return the canonical session currently owning durable child launches.
    pub fn durable_session_id(&self) -> Result<String, DurableSpawnerError> {
        self.durable_authority
            .token()
            .map(|token| token.session_id().to_owned())
    }

    /// Return a session-pinned supervisor over every durable child origin.
    pub fn durable_child_supervisor(&self) -> Result<DurableChildSupervisor, DurableSpawnerError> {
        self.durable_authority.supervisor()
    }

    /// The canonical bound session journal owning this spawner's durable child
    /// records.
    ///
    /// This is the SAME journal a [`ChildTransactionLifecycle`] must be opened
    /// against: an isolated-mutation winner's child was declared in this journal,
    /// so its landing transaction can only resolve against this exact durable
    /// authority (a fresh journal would fail closed on `open`). Fails closed when
    /// no durable session is bound.
    ///
    /// [`ChildTransactionLifecycle`]: crate::child_transaction::ChildTransactionLifecycle
    pub fn session_journal(&self) -> Result<SessionJournal, DurableSpawnerError> {
        let token = self.durable_authority.token()?;
        self.durable_authority
            .with_store(&token, |store| Ok(store.journal()))
    }

    /// Canonical host adapter for creating durable child work.
    pub async fn spawn_host_child(&self, sub_config: SubAgentConfig) -> SubAgentResult {
        self.spawn_one_with_origin(sub_config, ChildOrigin::Host)
            .await
    }

    /// Bind spawned children to the parent session's immutable sandbox.
    pub fn with_sandbox_runtime(mut self, runtime: Arc<wcore_sandbox::SandboxRegistry>) -> Self {
        self.sandbox_runtime = runtime;
        self
    }
        tokio::sync::watch::Sender<bool>,
    )>,
    /// Servers dropped by a pre-connect gate (e.g. an unreachable stdio
    /// command). They never reached connect_all/health(), so they are
    /// carried here so the boot snapshot can render a skipped (⊘) row.
    pub skipped_mcp_servers: Vec<(String, String)>,
}

/// Wave OL: plugin-provider router. Called after plugin discovery + init
/// with the live list of registered `Arc<dyn PluginProvider>` handles. The
/// router inspects `model_str` (typically `Config.model`) and may downcast
/// a plugin provider into a concrete `wcore_providers::LlmProvider`, e.g.
/// the `wayland-ollama` route when `model_str.starts_with("ollama:")`.
///
/// Returning `None` falls through to the built-in `create_provider(&config)`
/// path. The downcast itself lives in the binary crate (`wcore-cli`) — it's
/// the only crate that links both `wcore-providers` and `wayland-ollama`.
pub type PluginProviderRouter =
    Box<dyn Fn(&str, &[Arc<dyn PluginProvider>]) -> Option<Arc<dyn LlmProvider>> + Send + Sync>;

/// One F11 session envelope shared by the parent engine and every child
/// spawner created for that session. Keeping config-to-budget translation in
/// one constructor prevents early-return CLI modes from silently falling back
/// to `AgentSpawner::new`'s legacy unbounded defaults.
struct SessionBudgetEnvelope {
    authority: crate::budget_authority::SharedBudgetAuthorityCoordinator,
    authority_seed: crate::budget_authority::BudgetAuthoritySeed,
}

impl SessionBudgetEnvelope {
    fn from_config(config: &Config) -> Self {
        Self::from_config_for_session(config, uuid::Uuid::new_v4().to_string())
    }

    fn from_config_for_session(config: &Config, session_id: String) -> Self {
        let effective_budget = wcore_budget::BudgetConfig::effective_session_envelope(
            &config.budget,
            config.session_cap.as_ref(),
        );
        let cap: wcore_budget::BudgetCap = (&effective_budget).into();

        // Provider tokens/cost have one authoritative, reservable ledger.
        // ExecutionBudget owns only orthogonal operational axes; duplicating
        // token/cost caps there would leave Continue widening one authority
        // while another immutable watcher remained exhausted.
        let mut operational_budget = ExecutionBudget::from(&effective_budget);
        operational_budget.max_tokens_in = None;
        operational_budget.max_tokens_out = None;
        operational_budget.max_cost_usd = None;

        let authority_seed = crate::budget_authority::BudgetAuthoritySeed {
            provider_caps: cap.clone(),
            preserve_committed_session_extensions: !config.execution_policy.is_managed(),
            execution_policy: operational_budget.clone(),
            wall_clock: crate::session_journal::BudgetWallClockAuthority::ActiveRuntime,
            process_cleanup_proof: None,
        };
        let authority = authority_seed
            .detached(session_id.clone())
            .expect("resolved budget policy produces a valid detached authority");
        Self {
            authority,
            authority_seed,
        }
    }

    fn govern_spawner(
        &self,
        spawner: crate::spawner::AgentSpawner,
        cancel: CancellationToken,
    ) -> crate::spawner::AgentSpawner {
        spawner.with_budget_governance(crate::spawner::SpawnerBudgetGovernance::from_authority(
            Arc::clone(&self.authority),
            cancel,
        ))
    }
}

/// Apply the canonical F11 session envelope to a one-shot CLI spawner.
///
/// Ordinary bootstrap and early-return modes use the same constructor, Smart
/// defaults, provider ledger, execution root, stable session identity, and
/// cancellation lineage. Every child created from the returned spawner shares
/// these handles for the lifetime of the one-shot invocation.
pub fn govern_standalone_spawner(
    spawner: crate::spawner::AgentSpawner,
    config: &Config,
) -> anyhow::Result<crate::spawner::AgentSpawner> {
    let spawner = spawner.with_session_authority_config(config);
    let cwd = std::env::current_dir()?;
    let manager = crate::session::SessionManager::new(
        config.session.directory.clone().into(),
        config.session.max_sessions,
    );
    let active = manager.create_for_run(
        &config.provider_label,
        &config.model,
        &cwd.to_string_lossy(),
        None,
    )?;
    let session_id = active.session.id.clone();
    let envelope = SessionBudgetEnvelope::from_config_for_session(config, session_id.clone());
    let execution_budget = envelope
        .authority
        .lock()
        .current_execution_view()

hook: PostToolUse
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk sed -n '3980,4060p' ../../../../crates/wcore-agent/src/engine.rs
rtk sed -n '1540,1585p' ../../../../crates/wcore-agent/src/engine.rs
rtk rg -n \"ToolRegistry::new\\(|AgentEngine::new_with_provider|registry\" ../../../../crates/wcore-agent/src/engine.rs | head -120" in /Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
 succeeded in 0ms:
    /// Bind a transient spawner to this engine's live session envelope.
    ///
    /// The provider tracker is optional for legacy/test engines, but execution
    /// and cancellation authority always inherit from the parent. Callers must
    /// not manufacture a separate provider allowance when the parent has none.
    fn govern_transient_spawner(
        &self,
        spawner: crate::spawner::AgentSpawner,
    ) -> crate::spawner::AgentSpawner {
        // Bind the parent repository identity so a transient child resolves the
        // SAME shared/isolated workspace authority as the bootstrap spawner. A
        // mutating child then allocates its standalone checkout relative to this
        // root; the engine never rewrites the child's working directory back to
        // the parent workspace. A contained session binds from its workspace
        // policy root; a non-contained session (no policy) still has an
        // authoritative parent identity — the live session directory (the same
        // authoritative source the durable-workspace retention path reads via
        // `base_config.session.directory`). Binding it lets non-contained
        // transient children resolve SharedReadOnly against the real session
        // workspace instead of failing "parent workspace authority is not bound".
        // The session root is materialized on demand exactly as the durable
        // checkout path does (`prepare_child_workspace`), and only when it is
        // absolute — a relative directory would resolve against the process cwd,
        // which must never become a child's parent identity. When the chosen
        // root is unresolvable, keep the unbound spawner (fail-closed at spawn
        // time) rather than losing the governed handle.
        let spawner = {
            let unbound = spawner.clone_for_spawn();
            let parent_root = match self.tools.workspace_policy() {
                Some(policy) => Some(policy.root().to_path_buf()),
                None => {
                    let session_root = std::path::Path::new(self.config.session.directory.as_str());
                    if session_root.is_absolute() && std::fs::create_dir_all(session_root).is_ok() {
                        Some(session_root.to_path_buf())
                    } else {
                        None
                    }
                }
            };
            match parent_root {
                Some(root) => spawner.with_parent_workspace(root).unwrap_or(unbound),
                None => unbound,
            }
        };
        let spawner = match self.durable_session_authority.as_ref() {
            Some(authority) => spawner
                .with_shared_durable_session_authority(authority.clone())
                .expect("installed durable session authority carries its effective policy"),
            None => spawner,
        };
        if let Some(authority) = self.budget_authority.as_ref() {
            let governance = crate::spawner::SpawnerBudgetGovernance::from_authority(
                Arc::clone(authority),
                self.cancel_token.clone(),
            );
            return spawner.with_budget_governance(governance);
        }
        match self.budget_tracker.as_ref() {
            Some(tracker) => {
                spawner.with_budget_governance(crate::spawner::SpawnerBudgetGovernance::new(
                    Arc::clone(tracker),
                    self.budget_session_id(),
                    self.execution_budget.clone(),
                    self.cancel_token.clone(),
                ))
            }
            None => spawner
                .with_cancel(self.cancel_token.clone())
                .with_execution_budget(self.execution_budget.clone()),
        }
    }

    pub(crate) fn midflight_monitor_constructed(&self) -> bool {
        // The monitor is an owned field, not an inferred registration flag.
        true
    }

    /// v0.6.1 CRIT-1: install a `PolicyGate` for this session. Once set,
    /// every tool dispatch in `dispatch_once` is checked against the gate
    /// before reaching the approval / budget pipeline. The gate fails
    /// closed — `PolicyDenied` returns a `ToolResult { is_error: true }`

fn synthesize_workflow_owned(
    spawner: crate::spawner::AgentSpawner,
    task: String,
) -> std::pin::Pin<Box<dyn std::future::Future<Output = SynthOwnedOutput> + Send>> {
    // Concrete boxed `Send` return type (not `impl Future`) so the engine's
    // own `run` future does not transitively depend on an opaque type that
    // cycles back through this gate during auto-trait inference.
    Box::pin(async move {
        let result = crate::workflow_synth::synthesize_workflow(&task, &spawner).await;
        (result, spawner)
    })
}

/// Dynamic Workflows B6 — execute a workflow plan on an owned spawner.
///
/// Same `tokio::spawn` recursion-cut rationale as [`synthesize_workflow_owned`].
/// Returns the plan back so the caller can render its per-stage summary.
type RunOwnedOutput = (
    crate::orchestration::workflow::runner::WorkflowPlan,
    Result<
        crate::orchestration::workflow::runner::WorkflowRunResult,
        crate::orchestration::workflow::runner::WorkflowRunError,
    >,
);

fn run_workflow_owned(
    spawner: crate::spawner::AgentSpawner,
    plan: crate::orchestration::workflow::runner::WorkflowPlan,
    initial: serde_json::Value,
    parent_output: std::sync::Arc<dyn crate::output::OutputSink>,
    lifecycle: std::sync::Arc<crate::orchestration::workflow::runner::WorkflowLifecycleEmitter>,
) -> std::pin::Pin<Box<dyn std::future::Future<Output = RunOwnedOutput> + Send>> {
    // Concrete boxed `Send` return type (see `synthesize_workflow_owned`).
    Box::pin(async move {
        lifecycle.start();

        // ForgeFlows-Live Phase 1: wire the parent sink so the live (B6) path's
        // sub-agent events relay back as `SubAgentEvent`, matching the LLM-facing
        // `WorkflowTool` surface.
        let run = crate::orchestration::workflow::runner::WorkflowRunner::new(&spawner)
            .with_parent_output(std::sync::Arc::clone(&parent_output))
            .with_lifecycle(std::sync::Arc::clone(&lifecycle))
            .run(&plan, initial)
            .await;

19:use wcore_tools::registry::ToolRegistry;
73:    use wcore_tools::registry::ToolRegistry;
141:        let mut engine = AgentEngine::new_with_provider(
144:            ToolRegistry::new(),
193:            ToolRegistry::new(),
230:        let mut engine = AgentEngine::new_with_provider(
233:            ToolRegistry::new(),
990:/// 1. the model is UNKNOWN to the `wcore_config::limits` registry — a known
1124:        // #112: user omitted --max-tokens + model unknown to the registry +
1195:        // #112: a registry-known model always sends its real sized ceiling —
1203:        // Its sizing resolves through the registry entry (config cap binds
2392:    /// Wave OR: the tool registry is Arc-shared so per-turn
2397:    /// `registry_mut` uses `Arc::get_mut` and returns `None` once any task
2424:    /// TOML) rather than defaulted. When `false`, an unknown-to-the-registry
2684:    /// When `Some`, bootstrap has called `set_agent_registry` after applying
2685:    /// `InitializeOutcome` via `apply_initialize_outcome`. The registry is
2686:    /// `Arc`-wrapped so the engine and `SpawnTool::with_registry` (which
2688:    /// registry instance — a single shared identity, not to avoid the cost
2692:    agent_registry: Option<std::sync::Arc<crate::agents::registry::AgentRegistry>>,
3148:            agent_registry: None,
3382:            agent_registry: None,
3488:    /// Wave OR: returns the registry by mutable reference only when no
3490:    /// active. The CLI's MCP-server registration site mutates the registry
3494:    pub fn registry_mut(&mut self) -> Option<&mut ToolRegistry> {
3500:    /// apply it while holding the registry's mutable borrow.
3506:    /// registry, for hosts that need to invoke a tool directly
3510:    /// [`Self::registry_mut`] which holds the `Arc::get_mut` invariant.
3524:    /// when server `name` already has ≥1 tool registered on the live registry,
3533:    /// connected-server registry keyed by name, tracked as a follow-up.
4070:    /// `set_agent_registry` post-construction setter pattern.
4100:    /// post-construction setter pattern. The registry is `Arc`-wrapped so the
4101:    /// engine and `SpawnTool::with_registry` share the *same* registry instance
4106:    pub fn set_agent_registry(
4108:        registry: std::sync::Arc<crate::agents::registry::AgentRegistry>,
4110:        self.agent_registry = Some(registry);
4115:    /// Returns `None` when `set_agent_registry` has not been called (i.e.
4118:    pub fn agent_registry(
4120:    ) -> Option<&std::sync::Arc<crate::agents::registry::AgentRegistry>> {
4121:        self.agent_registry.as_ref()
4186:    /// (alongside `set_agent_registry` / `register_plugin_hooks`). When the
4208:    /// was constructed without a hook registry (`hooks: None`); production
4847:        // registry's configured vfs (a `SandboxedFs` jail for a channel
9552:                    // unknown to the registry, and the provider is omit-safe, OMIT the
11320:            // The adapter owns Arc clones of registry/confirmer; the
11659:                        // tools and names not in the registry.
14106:        // Emit kept MCP tools in FIRST-ADD order (not registry-iteration
14126:    /// MCP tools stay DISCOVERABLE via the `ToolSearch` meta-tool (its registry
14255:        // server disconnected is gone from the LIVE registry. The check is
14256:        // against the registry — NOT the per-turn filtered list — so
14393:    /// engine dispatches it by registry name, which leaves a `tool_calls`
14421:    /// LIVE tool registry (e.g. their MCP server disconnected after the
14425:    /// the registry rather than the per-turn filtered def list, so plan-mode
14432:        let registry = Arc::clone(&self.tools);
14436:            .filter(|n| registry.get(n).is_none())
14444:            "dropping stale hydrated tools no longer in the registry \
14478:            wcore_tools::registry::apply_cold_deferral(&mut tools, &defer_cfg.hot_allowlist);
14493:            tools = wcore_tools::registry::fold_deferred_into_catalog(
14920:    use wcore_tools::registry::ToolRegistry;
15063:        let mut engine = super::AgentEngine::new_with_provider(
15066:            ToolRegistry::new(),
15185:    use wcore_tools::registry::ToolRegistry;
15235:            tools: Arc::new(ToolRegistry::new()),
15308:            agent_registry: None,
15666:            "empty registry → nothing connected"
15758:    /// Registry holding the given MCP fixture tools (the live-registry
15760:    fn hydration_registry(names: &[&str]) -> ToolRegistry {
15761:        let mut reg = ToolRegistry::new();
15770:    /// engine's LIVE registry, run the query, and feed its real result body
15775:        wcore_tools::registry::apply_cold_deferral(
15791:    /// registry snapshot, must be present in turn N+1's outbound tools[] with
15794:    /// genuinely dispatch through the SAME registry ToolSearch snapshotted.
15802:        // registry carries all three MCP tools (they are trimmed from the
15803:        // outbound declarations, not from the registry).
15805:        engine.tools = Arc::new(hydration_registry(&[
15868:        // through the real ToolSearch (registry snapshot, real result body).
15924:        // registry the ToolSearch snapshot came from.
15999:    /// DIRECTLY; the engine dispatches it by registry name, leaving a
16009:        engine.tools = Arc::new(hydration_registry(&["session_search"]));
16042:            "only the called deferred registry tool is recorded"
16074:        engine.tools = Arc::new(hydration_registry(&[
16150:    /// hydrate a tool that no longer exists in the live registry. The
16154:    async fn stale_hydration_pruned_when_tool_leaves_registry() {
16157:        // Live registry has alpha + bravo. "ghost" was hydratable from the
16160:        engine.tools = Arc::new(hydration_registry(&["mcp__srv__alpha", "mcp__srv__bravo"]));
16268:    /// unions in a tool that sorts EARLIER in registry-iteration order than an
16274:    /// Pre-fix, the final emission iterated `mcp_tools` in registry order and
16279:    fn mcp_curation_union_is_append_only_not_registry_order() {
16295:        // surfaces alpha — which sits BEFORE zulu/yankee in registry order.
16331:        // sitting at registry index 0 (ahead of zulu's index 1), append-only
16346:        // Pre-fix this fails — alpha (registry idx 0) emits ahead of zulu.
16825:        let engine = super::AgentEngine::new_with_provider(
16828:            ToolRegistry::new(),
16843:        let engine = super::AgentEngine::new_with_provider(
16846:            ToolRegistry::new(),
16862:    use wcore_tools::registry::ToolRegistry;
16913:            tools: Arc::new(ToolRegistry::new()),
16987:            agent_registry: None,
17171:    use wcore_tools::registry::ToolRegistry;
17227:            tools: Arc::new(ToolRegistry::new()),
17301:            agent_registry: None,
18528:    use wcore_tools::registry::ToolRegistry;
18615:            tools: Arc::new(ToolRegistry::new()),
18689:            agent_registry: None,
18903:        let mut registry = ToolRegistry::new();
18904:        registry.register(Box::new(CategorizedMockTool {
18908:        registry.register(Box::new(CategorizedMockTool {
18914:        engine.tools = Arc::new(registry);
19008:    use wcore_tools::registry::ToolRegistry;
19062:            tools: Arc::new(ToolRegistry::new()),
19136:            agent_registry: None,
19866:    use wcore_tools::registry::ToolRegistry;
19919:            tools: Arc::new(ToolRegistry::new()),
19993:            agent_registry: None,
20912:    use wcore_tools::registry::ToolRegistry;
20974:            tools: Arc::new(ToolRegistry::new()),
21043:            agent_registry: None,
21718:    use wcore_tools::registry::ToolRegistry;
21979:        let mut e = super::AgentEngine::new_with_provider(
21982:            ToolRegistry::new(),
22876:            ToolRegistry::new(),

hook: PostToolUse
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk sed -n '250,390p' ../21-03-PLAN.md" in /Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
 succeeded in 0ms:
</task>

<task type="auto">
  <name>Task 3: Implement exactly the authorized repair set, re-run the corpus, and state the delta against the 21-02 measurement</name>
  <files>crates/wcore-agent/src/spawner.rs, crates/wcore-agent/src/policy_gate.rs, crates/wcore-budget/src/execution.rs, crates/wcore-permissions/src/policy.rs, crates/wcore-cli/tests/child_authority_corpus/cases.rs, .planning/phases/21-child-authority-and-budget-inheritance/21-03-REPAIR-SET.md</files>
  <read_first>.planning/phases/21-child-authority-and-budget-inheritance/21-03-REPAIR-SET.md (the authorization and the per-finding outcome — the only thing that says what may be changed), .planning/phases/21-child-authority-and-budget-inheritance/21-01-AUTHORITY-CENSUS.md (the seam assignment, so each repair lands where the dimension is actually carried), .planning/phases/21-child-authority-and-budget-inheritance/21-02-CORPUS-RESULTS.md (the measured baseline this task states its delta against — never against any number written in this plan), crates/wcore-budget/src/execution.rs (`sub_budget`, `first_exceeded_reason`, `check_state`, `limit_for`, `with_reason_state`, `minimum_remaining` — the actual enforcement mechanism, so a repair does not reach for the restore-path intersection helpers by reflex), crates/wcore-agent/src/spawner.rs (the child-spawn seam's budget entry, binding, concurrency admission, provider resolution and tool-restriction application sites)</read_first>
  <behavior>
    - Only findings authorized at the checkpoint are touched. A finding deferred, declined or triaged BACKLOG produces no production change.
    - Each repair lands at the seam the census assigned to its dimension, not at a caller and not duplicated at a second layer.
    - A budget repair addresses the enforcement mechanism that actually runs — ancestor rollup — and does not call the snapshot-restore intersection helpers merely because their names match the requirement's wording.
    - A corpus case is edited ONLY where the checkpoint authorized a DISPROVE, and every such edit carries the recorded reason.
    - The corpus is re-run on both platforms at the exact repaired SHA in ALL FOUR combinations — standalone and host-protocol, each in-process and LIVE against the real `wayland-core` binary — and every case's outcome is stated as a DELTA against the outcomes `21-02-CORPUS-RESULTS.md` actually measured.
    - A repair is closed ONLY when its LIVE case goes green. An in-process green against a live red means the fix landed somewhere the product does not reach, and that repair is recorded NOT CLOSED with the divergence named, never counted as done.
    - Every live re-verification records its exact invocation, the asserted mode it landed in, the observable evidence and the platform. A live row missing any of the four is re-run rather than written down.
    - The Windows TUI remains unavailable and any dimension left unproved there is carried forward as a named limitation rather than silently reported as repaired on that platform.
    - A residual failure this plan did not target and that was not in the 21-02 measurement is a NEW regression: diagnosed and fixed if caused by this change, or recorded with a severity and escalated if not. It is never annotated away.
    - Repair is bounded at TWO iterations across the whole ordered gate; after the second, the plan stops and escalates.
    - The Hetzner aggregate proves no regression elsewhere in the workspace, and Windows clippy is clean before any Windows test result is trusted.
    - No test is deleted, weakened, re-gated, `#[ignore]`d or `#[allow]`ed, and the count of ignored tests is unchanged from before the repair.
  </behavior>
  <action>Implement ONLY what the checkpoint authorized, exactly as authorized. If the checkpoint declined the whole set, or the set was empty, this task does not run: write the SUMMARY and stop.

Land each repair at the seam the census assigned to that dimension. A guard bolted onto one caller leaves the seam widenable through every other route into it, and a check duplicated at a second layer creates two answers that drift. Centralize per AGENTS.md: a platform difference lives in the crate that owns the platform, never in its callers, and no crate gains a conditional it did not have.

If a budget finding is authorized, repair the mechanism that actually enforces. Non-wideability on the child-spawn path comes from ancestor rollup — `first_exceeded_reason` walks the leaf then every ancestor, and the token, cost and tool-runtime recorders roll up to every ancestor. `intersect_execution_budget` and `intersect_caps` are the snapshot-restore path and are not on this seam. If the finding is the `limit_for` leaf fallback, note that `limit_for` exists to populate an error payload and that `minimum_remaining` is the correct ancestor-walking pattern — so the repair is to make the offending consumer ask the question that walks ancestors, not to change `limit_for`'s reporting contract.

Edit a corpus case ONLY where the checkpoint authorized a DISPROVE, and record the reason at the case. Editing a case that was triaged FIX is the forgery this plan's rules forbid: it ships a real defect with a green light on it.

Then measure on the hosts, in this order, stopping at the first failure and repairing before advancing, working in the worktrees dedicated to this phase — `/root/wayland-p21` and `C:\ferrox-win-p21` — so no other agent's checkout is disturbed. Hetzner: build the workspace and the `wayland-core` binary, run the corpus suite in ALL FOUR combinations including the live ones, run workspace clippy with all targets, then run the full aggregate. SEANDESKTOP: the default shell is PowerShell and naive nested quoting through SSH breaks, so write the run as a `.ps1`, encode it base64-as-UTF-16LE and invoke `powershell -NoProfile -EncodedCommand`, with cargo at `C:\Users\seand\.cargo\bin\cargo.exe`. Run clippy with `-D warnings` FIRST — a lint failure there means the tests never execute and the run proves nothing — then build the binary, then run the corpus, from a single quiet run.

CLOSE EACH REPAIR ON THE LIVE RESULT, NOT THE IN-PROCESS ONE. For every finding you repaired, state both its in-process and its live outcome. A repair is closed only when the LIVE case goes green — the shipped binary actually refusing the widening at the surface a user touches. If the in-process case goes green while the live case stays red, the fix landed somewhere the product does not reach: record that repair NOT CLOSED with the divergence named, and treat it as the most important result in the table. It is the exact failure shape this codebase already shipped once, when an entire permission crate compiled, passed its tests, and had no consumer calling it. Record for every live outcome the exact invocation, the asserted mode it landed in, the observable evidence and the platform; a row missing any of the four is re-run rather than written down.

State every count and every case outcome as a DELTA against what `21-02-CORPUS-RESULTS.md` actually measured, never against any number written in this plan. Name every residual failure and diagnose it. A residual failure this plan did not target and that was not in the 21-02 measurement is a NEW regression: diagnose and fix it if this change caused it, or record it with a severity and escalate if it did not. Do not annotate it away.

REPAIR ITERATION CAP, hard: repairing before advancing is bounded at TWO iterations across the whole ordered gate, where an iteration is one edit-build-run cycle on real hardware. If the gate is not clean after the second, STOP, record every remaining failure by name with its output, and escalate. An unbounded repair loop inside a bounded plan is that bound defeated by the back door, and it is exactly how Phase 20 reached seventy-four plans.

Implements F21-01 and F21-02 to the extent the authorization covers them; records evidence for both. Marks no requirement complete — closure is claimed by the phase verdict in 21-04.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    GATE CONTRACT for the post-authorization half of `21-03-REPAIR-SET.md`, field separator " :: ". Rows: `AUTHORIZED :: [id] :: FIX or DISPROVE or BACKLOG or DECLINED`; `DELTA :: [test name] :: [before outcome] :: [after outcome]`; `LIVE :: [test name] :: [platform] :: [asserted mode] :: [the exact wayland-core invocation] :: [captured transcript path]`; `CLOSURE :: [id] :: CLOSED or NOT-CLOSED :: [in-process green or red] :: [live green or red]`. Every delta is checked against the 21-02 baseline AND the captured re-run transcript, and the closure rule is enforced mechanically: marking a repair CLOSED while its LIVE leg is red goes red.
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; R=.planning/phases/21-child-authority-and-budget-inheritance/21-03-REPAIR-SET.md; C=.planning/phases/21-child-authority-and-budget-inheritance/21-02-CORPUS-RESULTS.md; E=.planning/phases/21-child-authority-and-budget-inheritance/evidence; ROWS=$(/usr/bin/awk -F' :: ' '$1=="DELTA" &amp;&amp; NF>=4 {print $2}' "$R"); test "$(printf '%s\n' "$ROWS" | /usr/bin/grep -c .)" -ge 1 &amp;&amp; printf '%s\n' "$ROWS" | while IFS= read -r id; do /usr/bin/grep -qF -- "$id" "$C" || { echo "FAIL: delta row $id has no baseline in the 21-02 measurement, so it is a delta against nothing"; exit 1; }; /usr/bin/grep -qF -- "$id" "$E/21-03-t3-linux.log" || { echo "FAIL: delta row $id does not appear in the captured re-run transcript, so the after-state was never observed"; exit 1; }; done || { echo "FAIL: no [DELTA :: test name :: before :: after] rows, or a delta names a case with no 21-02 baseline or no captured re-run evidence"; exit 1; }; echo "every delta is stated against a real 21-02 baseline and backed by the captured re-run transcript"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; B=dd02a624e99ac061cc38a070c1a99719c80f2f68; E=.planning/phases/21-child-authority-and-budget-inheritance/evidence; test -f "$E/21-03-t3-linux.log" || { echo "FAIL: the re-run transcript $E/21-03-t3-linux.log does not exist, so there is nothing to guard"; exit 1; }; NOW=$(/usr/bin/git grep -cF 'ignore]' -- crates/ | /usr/bin/awk -F: '{s+=$NF} END {print s+0}'); WAS=$(/usr/bin/git grep -cF 'ignore]' "$B" -- crates/ | /usr/bin/awk -F: '{s+=$NF} END {print s+0}'); test "$WAS" -gt 0 &amp;&amp; test "$NOW" = "$WAS" || { echo "FAIL: the count of ignored tests moved from $WAS at the pinned phase base $B to $NOW, or the base count could not be measured. This leg is a REGRESSION GUARD; the transcript-exists leg above it is the completion gate."; exit 1; }; echo "no test was newly ignored or deleted since $B (count $NOW, unchanged)"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; B=dd02a624e99ac061cc38a070c1a99719c80f2f68; E=.planning/phases/21-child-authority-and-budget-inheritance/evidence; test -f "$E/21-03-t3-linux.log" || { echo "FAIL: the re-run transcript $E/21-03-t3-linux.log does not exist, so there is nothing to guard"; exit 1; }; NOW=$(/usr/bin/git grep -cF 'allow(' -- crates/ | /usr/bin/awk -F: '{s+=$NF} END {print s+0}'); WAS=$(/usr/bin/git grep -cF 'allow(' "$B" -- crates/ | /usr/bin/awk -F: '{s+=$NF} END {print s+0}'); test "$WAS" -gt 0 &amp;&amp; test "$NOW" -le "$WAS" || { echo "FAIL: lint suppressions grew from $WAS at the pinned phase base $B to $NOW, or the base count could not be measured. This leg is a REGRESSION GUARD; the transcript-exists leg above it is the completion gate."; exit 1; }; echo "no new lint suppression since $B (count $NOW against a base of $WAS)"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; B=dd02a624e99ac061cc38a070c1a99719c80f2f68; R=.planning/phases/21-child-authority-and-budget-inheritance/21-03-REPAIR-SET.md; test -f "$R" || { echo "FAIL: the repair set does not exist, so nothing authorizes a change"; exit 1; }; /usr/bin/git cat-file -e "$B^{commit}" 2>/dev/null || { echo "FAIL: the pinned phase base commit $B is not reachable"; exit 1; }; NAUTH=$(/usr/bin/awk -F' :: ' '$1=="AUTHORIZED" &amp;&amp; $3=="FIX"{n++} END{print n+0}' "$R"); TOUCHED=$( ( /usr/bin/git diff --name-status "$B" -- crates/ | /usr/bin/awk '{print $NF}'; /usr/bin/git status --porcelain -- crates/ | /usr/bin/cut -c4- ) | /usr/bin/sort -u | /usr/bin/grep -v '^$'); if test "$NAUTH" -gt 0; then test -n "$TOUCHED" || { echo "FAIL: $NAUTH fixes were authorized but nothing under crates/ changed since $B"; exit 1; }; fi; SET=$(cat "$R"); printf '%s\n' "$TOUCHED" | while IFS= read -r f; do test -n "$f" || continue; case "$SET" in *"$f"*) ;; *) echo "FAIL: a changed file is not named in the authorized repair set: $f"; exit 1;; esac; done || exit 1; echo "$NAUTH authorized fixes; every file changed under crates/ since $B is named in the authorized repair set"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; R=.planning/phases/21-child-authority-and-budget-inheritance/21-03-REPAIR-SET.md; ROWS=$(/usr/bin/awk -F' :: ' '$1=="LIVE" &amp;&amp; NF>=6 {print $2 " :: " $4 " :: " $5 " :: " $6}' "$R"); test "$(printf '%s\n' "$ROWS" | /usr/bin/grep -c ' :: ')" -ge 1 &amp;&amp; printf '%s\n' "$ROWS" | while IFS= read -r row; do id=${row%% :: *}; r1=${row#* :: }; mode=${r1%% :: *}; r2=${r1#* :: }; inv=${r2%% :: *}; ev=${r2#* :: }; case "$mode" in json-stream|headless|tui) ;; *) echo "FAIL: $id records asserted mode '$mode', which is not one of json-stream, headless, tui"; exit 1;; esac; case "$inv" in *wayland-core*) ;; *) echo "FAIL: $id does not record an invocation of the real wayland-core binary: $inv"; exit 1;; esac; test -f "$ev" &amp;&amp; /usr/bin/grep -qF -- "$id" "$ev" || { echo "FAIL: $id names evidence $ev, which does not exist or does not mention the case"; exit 1; }; done || { echo "FAIL: no live re-verification rows, or a row names an unreal invocation, an unknown asserted mode, or evidence that does not exist -- a repair closed on in-process results alone is not closed"; exit 1; }; echo "every live re-verification names a real invocation, a closed-set asserted mode and an evidence file that mentions the case"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; R=.planning/phases/21-child-authority-and-budget-inheritance/21-03-REPAIR-SET.md; V=$(/usr/bin/awk -F' :: ' '$1=="CLOSURE"{n++; if ($3!="CLOSED" &amp;&amp; $3!="NOT-CLOSED") bad++; if ($4!="green" &amp;&amp; $4!="red") bad++; if ($5!="green" &amp;&amp; $5!="red") bad++; if ($3=="CLOSED" &amp;&amp; $5!="green") rule++} END{print n+0 "," bad+0 "," rule+0}' "$R"); test -n "$V" &amp;&amp; test "${V%%,*}" -ge 1 || { echo "FAIL: no [CLOSURE :: finding id :: CLOSED or NOT-CLOSED :: in-process green or red :: live green or red] rows, so the in-process-versus-live closure test was not reported either way"; exit 1; }; case "$V" in *,0,0) ;; *) echo "FAIL: closure rows are malformed, or a repair is marked CLOSED while its LIVE leg is red -- a repair closed on in-process evidence alone is not closed; got rows,malformed,rule-violations = $V"; exit 1;; esac; echo "${V%%,*} repairs adjudicated, every CLOSED one green on the live surface"</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); E=/Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/evidence; /bin/mkdir -p "$E"; ssh hetzner-dsm "cd /root/wayland-p21 &amp;&amp; git fetch origin plan/f20-unified-audit-repair &amp;&amp; git checkout --detach $SHA &amp;&amp; test \$(git rev-parse HEAD) = $SHA &amp;&amp; echo RUN_SHA=$SHA &amp;&amp; cargo build --locked --workspace --all-features &amp;&amp; cargo build --locked -p wcore-cli --bin wayland-core &amp;&amp; ./target/debug/wayland-core --help >/dev/null &amp;&amp; echo LIVE_BINARY_RUNS &amp;&amp; cargo nextest run -p wcore-cli --test child_authority_corpus --no-fail-fast &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings &amp;&amp; cargo nextest run --profile ci --no-fail-fast" > "$E/21-03-t3-linux.log" 2>&amp;1; rc=$?; /usr/bin/tail -40 "$E/21-03-t3-linux.log"; test $rc -eq 0 || { echo "FAIL: the repaired tree did not pass the ordered Hetzner gate at $SHA (rc=$rc); the captured transcript is $E/21-03-t3-linux.log"; exit 1; }; /usr/bin/grep -qxF LIVE_BINARY_RUNS "$E/21-03-t3-linux.log" || { echo "FAIL: the repaired wayland-core binary was not proved to execute on Linux"; exit 1; }; /usr/bin/grep -qF "RUN_SHA=$SHA" "$E/21-03-t3-linux.log" || { echo "FAIL: the Linux transcript does not carry the pinned run SHA $SHA"; exit 1; }; echo "repaired tree green on Hetzner at $SHA; transcript captured for the delta cross-check"</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); E=/Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/evidence; /bin/mkdir -p "$E"; PS=$(mktemp /tmp/p21r-XXXX.ps1); /usr/bin/printf '%s\n' 'Set-Location C:\ferrox-win-p21' '$cargo = "C:\Users\seand\.cargo\bin\cargo.exe"' 'git fetch origin plan/f20-unified-audit-repair 2>&amp;1 | Out-String' "git checkout --detach $SHA 2>&amp;1 | Out-String" "if ((git rev-parse HEAD).Trim() -ne '$SHA') { Write-Output 'FAIL: wrong SHA on the Windows host'; exit 1 }" "Write-Output 'RUN_SHA=$SHA'" '&amp; $cargo clippy -p wcore-cli -p wcore-agent -p wcore-budget -p wcore-permissions --all-targets -- -D warnings 2>&amp;1 | Out-String' 'if ($LASTEXITCODE -ne 0) { Write-Output "FAIL: clippy dirty, tests would never run"; exit 1 }' '&amp; $cargo build -p wcore-cli --bin wayland-core 2>&amp;1 | Out-String' 'if ($LASTEXITCODE -ne 0) { Write-Output "FAIL: the repaired binary did not build"; exit 1 }' '&amp; ".\target\debug\wayland-core.exe" --help 2>&amp;1 | Out-Null' 'if ($LASTEXITCODE -ne 0) { Write-Output "FAIL: the repaired wayland-core.exe does not execute"; exit 1 } else { Write-Output "LIVE_BINARY_RUNS" }' '&amp; $cargo nextest run -p wcore-cli --test child_authority_corpus --no-fail-fast 2>&amp;1 | Out-String' 'if ($LASTEXITCODE -ne 0) { Write-Output "FAIL: the corpus suite failed on Windows after the repair"; exit 1 }' 'exit 0' > "$PS"; B64=$(/usr/bin/iconv -f UTF-8 -t UTF-16LE "$PS" | /usr/bin/base64 | /usr/bin/tr -d '\n'); ssh SeanD@seandesktop "powershell -NoProfile -EncodedCommand $B64" > "$E/21-03-t3-windows.log" 2>&amp;1; rc=$?; /bin/rm -f "$PS"; /usr/bin/tail -60 "$E/21-03-t3-windows.log"; test $rc -eq 0 || { echo "FAIL: the repaired tree did not pass on Windows at $SHA (rc=$rc); the captured transcript is $E/21-03-t3-windows.log"; exit 1; }; /usr/bin/grep -qxF LIVE_BINARY_RUNS "$E/21-03-t3-windows.log" || { echo "FAIL: the repaired wayland-core.exe was not proved to execute on Windows"; exit 1; }; /usr/bin/grep -qF "RUN_SHA=$SHA" "$E/21-03-t3-windows.log" || { echo "FAIL: the Windows transcript does not carry the pinned run SHA $SHA"; exit 1; }; echo "repaired tree green on Windows at $SHA; transcript captured"</automated>
  </verify>
  <done>Only findings authorized at the checkpoint were changed, and every changed file under `crates/` appears in the authorized repair set. Each repair landed at the census-assigned seam, with no guard bolted onto a caller and no check duplicated at a second layer. Any budget repair addressed ancestor rollup rather than the snapshot-restore intersection helpers. Corpus cases were edited only under a recorded DISPROVE with its reason. The corpus re-ran on Hetzner and on SEANDESKTOP at the exact repaired SHA in all four combinations, in the worktrees dedicated to this phase, with Windows clippy clean first, the Windows run driven by a base64 UTF-16LE `-EncodedCommand` script with cargo at its absolute path, and the result from a single quiet run. The repaired `wayland-core` binary was built and proved to execute on both platforms. Every case outcome is stated as a delta against what `21-02-CORPUS-RESULTS.md` measured, every repair is closed on its LIVE result with the exact invocation, asserted mode, observable evidence and platform recorded, and any repair whose in-process case went green while its live case stayed red is recorded NOT CLOSED with the divergence named. Every residual is named and diagnosed, and any new regression is fixed or escalated with a severity rather than annotated. Workspace clippy is clean with `-D warnings` and the Hetzner aggregate shows no regression elsewhere. No test was newly ignored and lint suppressions did not increase. Repair stayed within two iterations.</done>
</task>

</tasks>

## What this plan does NOT change (scope fence)

- **Any finding the corpus did not measure.** The repair set's boundary was drawn by an independent pass, and that independence is the only reason it is trustworthy. A defect noticed in passing gets a severity and a BACKLOG entry, not a patch.
- **Any MEDIUM-or-below finding.** They go to `.planning/BACKLOG.md` marked non-blocking. Repairing one because it looks quick is how a bounded set stops being bounded, and the amended rules give it no other disposition.
- **Any corpus case not covered by an authorized DISPROVE.** Editing a case triaged FIX ships a real defect with a green light on it, and it is the specific forgery the RED-over-GREEN rule names for this plan.
- **The snapshot-restore intersection helpers, unless a finding is actually about the restore path.** `intersect_execution_budget` and `intersect_caps` are not on the child-spawn seam; changing them to satisfy a spawn-path finding would change the wrong mechanism.
- **`limit_for`'s reporting contract.** It exists to populate an error payload. If a consumer wrongly makes a decision from it, the consumer is repaired to ask the ancestor-walking question instead.
- **The census and the corpus results — consumed, never re-derived.** 21-01 owns the census and 21-02 owns the measurement. Re-deriving either here is a second cycle.
- **The admission gate — read, never re-verified.** 21-01 owns that determination.
- **F21-03 attribution — 21-04 owns it.** Reservation, refund, escalation, approval, cancellation and result-delivery attribution is a different property with a different seam set.
- **No new crate and no new dependency** unless an authorized repair genuinely requires an unreachable symbol, raised at the checkpoint rather than added quietly.
- **No fifth Phase 21 plan is created or proposed.**

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repair scope ← independent measurement | The corpus results are the only thing bounding what may be changed; a repair that reaches past them is unbounded scope with a plausible excuse |
| production behaviour ← test satisfaction | Editing a case to pass, or changing production to satisfy an invalid case, both convert a measurement into a lie in opposite directions |
| default posture ← existing sessions | Turning an opt-in gate on by default changes behaviour for every session that never asked for it; that blast radius is Sean's to accept, not an executor's |
| crate scope ← requirement wording | `wcore-permissions` states it deliberately has no inheritance; adding it to satisfy a requirement's phrasing is an architecture change wearing a patch's clothes |
| repair claim ← real hardware | Only Hetzner and SEANDESKTOP can prove a repair; a Mac-side or source-inspection claim is not evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03-01 | Tampering | A corpus case is edited to pass instead of the defect being fixed, converting a measured red into a green that means nothing and shipping the defect with a certificate | critical | mitigate | Editing a case is permitted ONLY under a checkpoint-recorded DISPROVE verdict with its evidence, and is forbidden in every other circumstance; the RED-over-GREEN rule is stated verbatim and names this specific forgery; the ignored-test count and the lint-suppression count are both gate-checked against HEAD |
| T-21-03-02 | Elevation of Privilege | A behaviour change — turning the opt-in tool-authority gate on by default — is shipped as a routine patch, changing enforcement for every existing session without anyone accepting that blast radius | critical | mitigate | Any FIX that is a behaviour change or an architecture change must be FLAGGED BY NAME in the triage and cannot be authorized implicitly; the checkpoint requires the blast radius to be stated and accepted in Sean's own words; the two known candidates are named in the plan so neither can be slipped through as routine |
| T-21-03-03 | Spoofing | Production is changed to satisfy a case that asked a question production cannot be asked, shipping a real behaviour change to fix an imaginary defect | high | mitigate | DISPROVE is a first-class disposition with equal standing to FIX; the triage must state the evidence for invalidity and what the corrected case asserts; the checkpoint offers corpus correction as its own authorized outcome |
| T-21-03-04 | Denial of Service | Unbounded repair — each fix reveals the next, the gate never comes clean, and the plan runs indefinitely as Phase 20's did | high | mitigate | Two repair iterations across the whole ordered gate, stated as a hard cap with an explicit definition of an iteration; after the second the plan stops and escalates with every remaining failure named; the termination criterion forbids a third |
| T-21-03-05 | Tampering | A repair lands at a caller or is duplicated at a second layer instead of at the seam, so the seam stays widenable through every other route and two answers begin to drift | high | mitigate | Every repair must land at the census-assigned seam; AGENTS.md centralization is restated as a hard rule; every changed file under `crates/` is gate-checked to appear in the authorized repair set |
| T-21-03-06 | Tampering | A budget repair calls the snapshot-restore intersection helpers because their names match the requirement's wording, changing a mechanism that is not on the child-spawn path at all | high | mitigate | The plan states the caller counts and line numbers for both helpers and names the restore path explicitly; the repair is directed to the ancestor-rollup mechanism that actually enforces, and the `limit_for` versus `minimum_remaining` distinction is spelled out |
| T-21-03-07 | Spoofing | A repair is claimed green from source inspection or from the Mac, or from a Windows run whose clippy failed so the tests never executed | high | mitigate | Every behavioural claim comes from Hetzner or SEANDESKTOP at one asserted SHA; Windows clippy with `-D warnings` runs BEFORE the tests and its cleanliness gates the result; deltas are stated against the counts `21-02-CORPUS-RESULTS.md` actually measured, never against a number in this plan |
| T-21-03-08 | Repudiation | A new regression introduced by the repair is annotated away or attributed to a pre-existing red, so the change ships with damage attached to someone else's name | high | mitigate | A residual failure absent from the 21-02 measurement is defined as a NEW regression that must be diagnosed and fixed or escalated with a severity; the Hetzner aggregate proves the workspace beyond the corpus; annotating it away is forbidden |
| T-21-03-09 | Denial of Service | A MEDIUM finding is repaired because it looks quick, and the bounded set stops being bounded one convenience at a time | medium | mitigate | The amended rules give MEDIUM and below exactly one disposition, BACKLOG; the plan states there is no third option for them; the scope fence names it and the changed-file gate catches it |
| T-21-03-10 | Information Disclosure | A repair widens a debug or error surface, leaking authority or credential detail into a payload while fixing an authority defect | medium | mitigate | Public error types use `thiserror` and carry no credential material; the repair set's diff is reviewed for payload content before commit; `ChildPolicySnapshot`'s existing redacted-evidence contract is preserved rather than extended |
| T-21-03-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency is added, removed or updated and no `Cargo.toml` change is expected; if an authorized repair genuinely requires an unreachable symbol, that is raised at the decision checkpoint rather than added quietly, and no install task exists in this plan |
</threat_model>

<verification>
Local gates (Mac, source level): `cargo fmt --all -- --check` clean; `21-03-REPAIR-SET.md` exists, carries a disposition per finding, states the set against the two-iteration cap, and records the delta after the repair; `.planning/BACKLOG.md` carries every MEDIUM-and-below finding, or is correctly unchanged when there were none; `crates/` is unmodified before the checkpoint; after the repair, every changed file under `crates/` appears in the authorized repair set, the ignored-test count is unchanged from HEAD, and the lint-suppression count did not increase.

Authoritative gates, at one exact SHA asserted on each host before any build step. Hetzner, in this order: `cargo build --locked --workspace --all-features`, then the corpus suite, then `cargo clippy --workspace --all-targets -- -D warnings`, then the full `cargo nextest run --profile ci --no-fail-fast` aggregate proving no regression elsewhere. SEANDESKTOP: clippy with `-D warnings` on the touched crates FIRST — because on that platform a lint failure means the tests never execute — then the corpus suite, from a single quiet run.

Known unknowns to record, not to resolve here: whether a repair that closes a widening on the standalone surface also closes it on the host-protocol surface, which only the re-run's equivalence assertion can answer and which must be stated rather than assumed; whether an authorized behaviour change has downstream effects on the Desktop consumer that CTRL-02/D1's conformance harness would catch but this phase cannot; and whether any finding declined at the checkpoint is reachable by a real adversary or only by a cooperating test, which the repair set should state where the evidence allows and mark unknown where it does not.
</verification>

<success_criteria>
- Every corpus red carries exactly one disposition drawn from FIX, DISPROVE and BACKLOG, with FIX and DISPROVE reserved to CRITICAL and HIGH and BACKLOG the only disposition available to MEDIUM and below (F21-01, F21-02).
- Every FIX proposal named the census seam, the exact file and symbol, and the concrete change, so the checkpoint decided about something real.
- Every behaviour-change and architecture-change FIX was flagged by name in the triage and could not be authorized implicitly — specifically the opt-in tool-authority gate and the deliberate absence of inheritance in `wcore-permissions`.
- One authorization is recorded verbatim with a per-finding outcome, and any accepted blast radius is named in Sean's own words.
- Only authorized findings were changed. Every changed file under `crates/` appears in the authorized repair set, and each repair landed at the census-assigned seam rather than at a caller or duplicated at a second layer.
- No budget repair called the snapshot-restore intersection helpers to satisfy a child-spawn finding, and no repair changed `limit_for`'s reporting contract to fix a consumer that should have been asking the ancestor-walking question.
- A corpus case was edited only under a recorded DISPROVE verdict carrying its evidence and reason.
- The corpus re-ran on Hetzner and on SEANDESKTOP at the exact repaired SHA in ALL FOUR combinations, Windows clippy clean first and the Windows run driven by a base64 UTF-16LE `-EncodedCommand` script with cargo invoked by absolute path, with every case outcome stated as a delta against what `21-02-CORPUS-RESULTS.md` actually measured.
- EVERY REPAIR WAS CLOSED ON A LIVE RESULT, not an in-process one: the repaired `wayland-core` binary was built, proved to execute, and observed refusing the actual widening at the surface a user touches — `--json-stream` for host protocol, `-p`/`--no-tui` and the bare binary on a PTY for standalone — with the exact invocation, the asserted mode, the observable evidence and the platform recorded for each (F21-01, F21-02).
- Any repair whose in-process case went green while its LIVE case stayed red is recorded NOT CLOSED with the divergence named, because a fix that the product does not reach is not a fix — this is the exact failure shape this codebase shipped once as orphan permission code.
- The Windows TUI limitation is carried forward as a named limitation, and no dimension it leaves unproved is reported as repaired on that platform.
- Every residual failure is named and diagnosed; every new regression is fixed or escalated with a severity; nothing was annotated away; no test was newly ignored and lint suppressions did not increase.
- Repair stayed within TWO iterations, and every MEDIUM-and-below finding is in `.planning/BACKLOG.md` marked explicitly non-blocking.
- If the checkpoint declined a CRITICAL or HIGH, that finding is recorded OPEN with its evidence as a decision on the record rather than a silent gap.
</success_criteria>

## Artifacts this phase produces
- `.planning/phases/21-child-authority-and-budget-inheritance/21-03-REPAIR-SET.md` — the triage, the recorded authorization with a per-finding outcome, and the post-repair delta.
- `.planning/BACKLOG.md` — every MEDIUM-and-below finding, non-blocking, with pointers back to its corpus case.
- The authorized production repairs at the census-named seams.
- `21-03-SUMMARY.md` recording the triage, the authorization, the repairs, the measured delta and which termination state the plan ended in.

<output>
Create `.planning/phases/21-child-authority-and-budget-inheritance/21-03-SUMMARY.md` using the standard Ferrox summary template. Record: the repair commit SHA and tree; the full triage table with every finding's severity and disposition; the authorization verbatim with its per-finding outcome and any accepted behaviour-change or architecture-change blast radius; every repair with the seam it landed at and why that seam; every DISPROVE with its evidence and the corrected case; the corpus re-run results on both platforms stated as a delta against what `21-02-CORPUS-RESULTS.md` measured, with every residual named and diagnosed; the Hetzner aggregate and workspace clippy proving no regression elsewhere; how many repair iterations were used out of the two permitted; every MEDIUM-and-below finding and where it was logged; every finding left OPEN by decision, with the decision recorded; the recorded unknowns; and which of the four termination states the plan ended in. Mark no requirement complete — closure is claimed by the phase verdict in 21-04.
</output>

hook: PostToolUse
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk rg -n \"allowed_tools|toolsets|ForkOverrides|SubAgentConfig \\{\" ../../../../crates/wcore-agent/src/orchestration ../../../../crates/wcore-agent/src/workflow_synth.rs ../../../../crates/wcore-agent/src/engine.rs | head -300" in /Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
 succeeded in 0ms:
../../../../crates/wcore-agent/src/engine.rs:110:    fn child(name: &str) -> SubAgentConfig {
../../../../crates/wcore-agent/src/engine.rs:111:        SubAgentConfig {
../../../../crates/wcore-agent/src/engine.rs:13376:            for tool_name in &modifier.allowed_tools {
../../../../crates/wcore-agent/src/engine.rs:17081:    fn tc_6_23_allowed_tools_no_duplicates() {
../../../../crates/wcore-agent/src/engine.rs:17084:            allowed_tools: vec!["Bash".to_string(), "Read".to_string()],
../../../../crates/wcore-agent/src/engine.rs:17117:            allowed_tools: vec!["Bash".to_string()],
../../../../crates/wcore-agent/src/engine.rs:17130:                allowed_tools: vec!["Bash".to_string()],
../../../../crates/wcore-agent/src/engine.rs:17135:                allowed_tools: vec!["Read".to_string()],
../../../../crates/wcore-agent/src/orchestration/workflow/pipeline.rs:224:                SubAgentConfig {
../../../../crates/wcore-agent/src/workflow_synth.rs:192:            SubAgentConfig {
../../../../crates/wcore-agent/src/orchestration/council/aggregator.rs:93:                SubAgentConfig {
../../../../crates/wcore-agent/src/orchestration/council/driver.rs:227:                SubAgentConfig {
../../../../crates/wcore-agent/src/orchestration/council/run.rs:244:            let cfg = SubAgentConfig {
../../../../crates/wcore-agent/src/orchestration/workflow/runner.rs:641:                        SubAgentConfig {
../../../../crates/wcore-agent/src/orchestration/workflow/runner.rs:1448:        let config = SubAgentConfig {
../../../../crates/wcore-agent/src/orchestration/workflow/runner.rs:1503:                SubAgentConfig {
../../../../crates/wcore-agent/src/orchestration/workflow/runner.rs:2006:                let cfg = SubAgentConfig {
../../../../crates/wcore-agent/src/orchestration/anvil/forge.rs:42:use wcore_types::spawner::{ChildId, ChildOrigin, ForkOverrides, Spawner, SubAgentConfig};
../../../../crates/wcore-agent/src/orchestration/anvil/forge.rs:396:        let sub = SubAgentConfig {
../../../../crates/wcore-agent/src/orchestration/anvil/forge.rs:411:        let overrides = ForkOverrides {
../../../../crates/wcore-agent/src/orchestration/anvil/forge.rs:414:            allowed_tools: BUILDER_TOOLS.iter().map(|s| (*s).to_string()).collect(),
../../../../crates/wcore-agent/src/orchestration/anvil/forge.rs:505:/// turn. Empty `allowed_tools` = the spawner's read-only set (Read/Grep/Glob).
../../../../crates/wcore-agent/src/orchestration/anvil/forge.rs:544:        let sub = SubAgentConfig {
../../../../crates/wcore-agent/src/orchestration/anvil/forge.rs:554:        let overrides = ForkOverrides {
../../../../crates/wcore-agent/src/orchestration/anvil/forge.rs:557:            allowed_tools: Vec::new(), // read-only (Read/Grep/Glob)

hook: PostToolUse
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk sed -n '610,675p' ../../../../crates/wcore-agent/src/orchestration/workflow/runner.rs
rtk sed -n '190,250p' ../../../../crates/wcore-agent/src/orchestration/workflow/pipeline.rs
rtk sed -n '170,215p' ../../../../crates/wcore-agent/src/workflow_synth.rs
rtk sed -n '380,430p' ../../../../crates/wcore-agent/src/orchestration/anvil/forge.rs
rtk sed -n '13280,13420p' ../../../../crates/wcore-agent/src/engine.rs" in /Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
 succeeded in 0ms:
                if attempt == MAX_SCHEMA_RETRIES {
                    return Err(StageSchemaErr::Dropped {
                        message: err.to_string(),
                        turns: result.turns,
                    });
                }
                // FIX 1 — a schema retry is a dispatch; charge it before
                // re-dispatching so per-item retries cannot escape the run budget.
                if let Err(attempted) = dispatch.budget.try_charge() {
                    return Err(StageSchemaErr::BudgetExceeded {
                        attempted,
                        turns: result.turns,
                    });
                }
                let correction = format!(
                    "Your previous output did not match the required schema: {err}. \
                     Return only valid JSON matching the schema."
                );
                let prompt = format!("{}\n\n{correction}", build_prompt(&stage.prompt, input));
                // Every schema-retry dispatch runs under the pipeline's
                // concurrency regime too, so acquire+hold a permit for it —
                // otherwise retries would escape the cap. A closed semaphore
                // (never closed by the runner) drops the item.
                let Ok(permit) = sem.clone().acquire_owned().await else {
                    return Err(StageSchemaErr::Dropped {
                        message: "pipeline semaphore closed".to_string(),
                        turns: result.turns,
                    });
                };
                result = spawner
                    .spawn_one_with_origin(
                        SubAgentConfig {
                            name: format!("{pipeline_id}[{item_index}]:{}", stage.id),
                            prompt,
                            max_turns: dispatch.max_turns,
                            max_tokens: dispatch.max_tokens,
                            system_prompt: None,
                            // The pipeline stage IS an AgentSpec, so its provider /
                            // model pin is read directly (no node_providers lookup).
                            provider: stage.provider.clone(),
                            model: stage.model.clone(),
                            temperature: None,
                        },
                        ChildOrigin::Pipeline,
                    )
                    .await;
                drop(permit);
                if result.is_error {
                    return Err(StageSchemaErr::Dropped {
                        message: result.text.clone(),
                        turns: result.turns,
                    });
                }
            }
        }
    }
    Err(StageSchemaErr::Dropped {
        message: "schema validation exhausted retries".to_string(),
        turns: result.turns,
    })
}

/// Result of executing a workflow graph to completion.
#[derive(Debug, Clone)]
pub struct WorkflowRunResult {
    /// The final merged state object after the walk.
                super::super::graph::InputMapper::Select { path: path.clone() }.apply(&current)
            }
            None => current.clone(),
        };
        let prompt = build_prompt(&stage.prompt, &input);

        // FIX 1 — charge the per-item-per-stage dispatch against the run budget
        // BEFORE acquiring a permit/dispatching. A breach aborts the whole run
        // (propagated up via `budget_breached`), not just this item.
        if let Err(attempted) = dispatch.budget.try_charge() {
            return ItemOutcome {
                index,
                value: Value::Null,
                stages: stage_records,
                budget_breached: Some(attempted),
            };
        }

        // Acquire a permit so total in-flight stage agents stay under the cap.
        // Held only across this one stage's dispatch, then dropped so another
        // item's stage can take it — this is what lets items interleave.
        // `acquire_owned()` only errors if the semaphore is closed; the runner
        // never closes the pipeline semaphore, so treat closure as "drop this
        // item" rather than silently dispatching capless.
        let Ok(permit) = sem.clone().acquire_owned().await else {
            return ItemOutcome {
                index,
                value: Value::Null,
                stages: stage_records,
                budget_breached: None,
            };
        };
        let result = spawner
            .spawn_one_with_origin(
                SubAgentConfig {
                    name: format!("{pipeline_id}[{index}]:{}", stage.id),
                    prompt,
                    max_turns: dispatch.max_turns,
                    max_tokens: dispatch.max_tokens,
                    system_prompt: None,
                    provider: None,
                    model: None,
                    temperature: None,
                },
                ChildOrigin::Pipeline,
            )
            .await;
        drop(permit);

        if result.is_error {
            // Drop this item to null; skip its remaining stages. The run as a
            // whole continues — other items are unaffected.
            stage_records.push(StageResult {
                node_id: format!("{pipeline_id}[{index}]:{}", stage.id),
                text: result.text,
                is_error: true,
                turns: result.turns,
            });
            return ItemOutcome {
                index,
                value: Value::Null,
        },
    })
}

/// Which failure the most recent attempt hit — used only to pick the typed
/// error in the (logically unreachable) post-loop guard.
enum SynthFailure {
    /// `extract_ron` returned `None`.
    Missing,
    /// The block extracted but `WorkflowPlan::parse` rejected it.
    Parse,
}

/// One-shot LLM dispatch through the spawner, mapping an agent-layer error to a
/// typed [`SynthError::AgentError`].
async fn dispatch(
    spawner: &AgentSpawner,
    name: &str,
    prompt: String,
) -> Result<String, SynthError> {
    let result = spawner
        .spawn_one_with_origin(
            SubAgentConfig {
                name: name.to_string(),
                prompt,
                max_turns: SYNTH_MAX_TURNS,
                max_tokens: SYNTH_MAX_TOKENS,
                // Pin the compiler framing so the model emits RON in its single
                // turn instead of exploring the codebase with its read-only tools.
                system_prompt: Some(SYNTH_SYSTEM_PROMPT.to_string()),
                provider: None,
                model: None,
                temperature: None,
            },
            ChildOrigin::Synthesis,
        )
        .await;
    if result.is_error {
        return Err(SynthError::AgentError(result.text));
    }
    Ok(result.text)
}

/// The RON grammar reference embedded in every synthesis prompt. Kept in lock-
/// step with the `Workflow`/`Phase`/`Step`/`AgentSpec` shape documented in
/// `orchestration/workflow/dsl.rs` so the model emits RON the real parser
#[async_trait]
impl Builder for SpawnBuilder<'_> {
    async fn build(
        &self,
        task: &str,
        feedback: Option<&BuildFeedback>,
    ) -> Result<BuiltCandidate, EngineError> {
        let n = {
            let mut c = self.counter.lock();
            let v = *c;
            *c += 1;
            v
        };
        let id = format!("{}cand-{n}", self.id_prefix);

        let prompt = build_prompt(task, feedback);
        let sub = SubAgentConfig {
            name: id.clone(),
            prompt,
            max_turns: 12,
            max_tokens: 16_384,
            system_prompt: Some(FORGE_SYSTEM_PROMPT.to_string()),
            provider: None,
            model: None,
            temperature: None,
        };
        // BUILDER_TOOLS carries Write/Edit, so the request classifies as an
        // isolated mutation: the seam allocates one transaction-owned standalone
        // checkout and runs the child bound to it (no process CWD, no second
        // checkout). A shared read-only classification would be refused by the
        // seam, so a writing builder can never run in the parent checkout.
        let overrides = ForkOverrides {
            model: None,
            effort: None,
            allowed_tools: BUILDER_TOOLS.iter().map(|s| (*s).to_string()).collect(),
        };

        // Wall-clock bound on ONE builder fork: keep a single in-flight await from
        // outliving the climb governor (which only checks between steps). On
        // timeout the seam future is dropped, terminalizing any checkout it had
        // begun allocating (RAII) — nothing leaks.
        let (result, guard) = tokio::time::timeout(
            BUILDER_TIMEOUT,
            self.spawner
                .spawn_builder_into_retained_checkout(sub, overrides, ChildOrigin::Anvil),
        )
        .await
        .map_err(|_| {
            EngineError::Builder(format!(
                "builder fork exceeded {}s wall budget",
                BUILDER_TIMEOUT.as_secs()
        use wcore_memory::v2_types::{AccessToken, Partition, Query, Tier};
        // Clone the Arc so the search awaits don't hold a borrow of `self`
        // across the `self.messages.push` below.
        let memory_api = self.memory_api.clone();
        let mut previews: Vec<String> = Vec::new();
        let mut seen: std::collections::HashSet<String> = std::collections::HashSet::new();
        for tier in [Tier::Project, Tier::Global] {
            let q = Query {
                text: query.to_string(),
                tier,
                partition: None,
                entities: None,
                limit_per_modality: 5,
                kg_depth: 1,
                token_budget: None,
            };
            match memory_api.search(q, AccessToken::MainAgent).await {
                Ok(hits) => {
                    for h in hits {
                        // Only durable facts are worth pre-injecting; episodic
                        // previews are noisier and the model can still reach
                        // them via `session_search` on demand.
                        if h.partition == Partition::Semantic && seen.insert(h.preview.clone()) {
                            previews.push(h.preview);
                        }
                    }
                }
                Err(e) => tracing::debug!(
                    target: "wcore_agent::memory",
                    error = %e,
                    tier = %tier.as_str(),
                    "session-start fact recall search failed; continuing without injection"
                ),
            }
        }
        if previews.is_empty() {
            return;
        }
        // Cap to keep the injection tight; top hits are first (search returns
        // facts ranked by embedding similarity to the query).
        previews.truncate(6);
        // HIGH-3 / F1: recalled previews are untrusted memory content. Defang any
        // host trust-tag delimiters so a stored fact can't forge or escape this
        // <system-reminder> block. Same helper as the plugin-hook envelope (DRY).
        let body = previews
            .iter()
            .map(|p| format!("- {}", wcore_config::hooks::neutralize_trust_delimiters(p)))
            .collect::<Vec<_>>()
            .join("\n");
        let block = format!(
            "<system-reminder>\nRecalled from your durable cross-session memory \
             (facts you stored in earlier sessions), potentially relevant to the \
             user's message:\n{body}\nUse these if they answer the user's question; \
             ignore any that are irrelevant.\n</system-reminder>"
        );
        tracing::debug!(
            target: "wcore_agent::memory",
            facts = previews.len(),
            "session-start recall: injected durable facts into first turn"
        );
        self.messages.push(Message::now(
            Role::User,
            vec![ContentBlock::Text { text: block }],
        ));
    }

    /// Run stop hooks when the agent session ends
    pub async fn run_stop_hooks(&self) {
        if let Some(hook_engine) = &self.hooks {
            let mut outcome = hook_engine.run_stop().await;
            // v0.9.1.2 F10: hook lifecycle telemetry to tracing only — never
            // eprintln! (leaks into the TUI alt-screen).
            for msg in outcome.hook_trace.drain(..) {
                tracing::debug!(target: "wcore_agent::hooks", "{msg}");
            }
            for msg in outcome.log_lines.drain(..) {
                if is_hook_lifecycle_line(&msg) {
                    tracing::debug!(target: "wcore_agent::hooks", "{msg}");
                } else {
                    eprintln!("{}", msg);
                }
            }
        }
    }

    /// Apply context modifiers collected from skill tool executions.
    fn apply_context_modifiers(&mut self, modifiers: &[Option<ContextModifier>]) {
        for modifier in modifiers.iter().flatten() {
            if let Some(ref model) = modifier.model {
                // D014: a skill context modifier's model is a hook/skill
                // switch — it must not override an explicit user `/model` pin.
                self.apply_switch_model(model.clone());
            }
            if let Some(effort) = modifier.effort {
                self.current_reasoning_effort = Some(effort_to_string(effort));
            }
            for tool_name in &modifier.allowed_tools {
                if !self.allow_list.contains(tool_name) {
                    self.allow_list.push(tool_name.clone());
                }
                // SAFETY: ToolConfirmer critical section cannot
                // panic, so the std::sync Mutex is never poisoned.
                self.confirmer.lock().unwrap().add_to_allow_list(tool_name);
            }

            // Handle plan mode transitions
            if let Some(ref transition) = modifier.plan_mode_transition {
                match transition {
                    PlanModeTransition::Enter => {
                        self.plan_state.pre_plan_allow_list = self.allow_list.clone();
                        self.plan_state.is_active = true;
                        if let Some(ref flag) = self.plan_active_flag {
                            flag.store(true, Ordering::Release);
                        }
                    }
                    PlanModeTransition::Exit { plan_content } => {
                        self.plan_state.is_active = false;
                        self.allow_list = self.plan_state.pre_plan_allow_list.clone();
                        if let Some(ref flag) = self.plan_active_flag {
                            flag.store(false, Ordering::Release);
                        }
                        // W6 F16: persist the plan text on Exit. Source is
                        // the `plan_content` field on the transition (already
                        // surfaced by ExitPlanModeTool); failures are logged,
                        // not propagated — plan-mode exit must always succeed.
                        if let (Some(session), Some(text)) =
                            (self.current_session.as_ref(), plan_content.as_deref())
                            && !text.is_empty()
                            && let Err(e) =
                                crate::plan::persist::save_plan_json(&session.id, text, None)
                        {
                            self.output
                                .emit_info(&format!("[F16] failed to persist plan: {e}"));
                        }
                    }
                }
            }
        }
    }

    /// Apply a `HookOutcome` produced before a turn begins (the `on_turn_start`

hook: PostToolUse
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk rg -n \"register\\(Box::new\\((ReadTool|WriteTool|EditTool|BashTool|GrepTool|GlobTool)|ToolRegistry::new|tool_names\\(\\)|disabled_tools|allowed_tools\" ../../../../crates/wcore-agent/src/bootstrap.rs ../../../../crates/wcore-cli/src ../../../../crates/wcore-config/src | head -260" in /Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
 succeeded in 0ms:
../../../../crates/wcore-agent/src/bootstrap.rs:321:    /// declared `allowed_tools`. `None` (every pre-existing caller) leaves the
../../../../crates/wcore-agent/src/bootstrap.rs:440:    /// `allowed_tools` (from its `AgentManifest`). An EMPTY list is treated as
../../../../crates/wcore-agent/src/bootstrap.rs:658:        let mut registry = wcore_tools::registry::ToolRegistry::new();
../../../../crates/wcore-agent/src/bootstrap.rs:1295:        let builtin_names: Vec<String> = registry.tool_names();
../../../../crates/wcore-agent/src/bootstrap.rs:2537:        // declared `allowed_tools`. Same `retain` mechanism as the channel
../../../../crates/wcore-agent/src/bootstrap.rs:3399:        let mut registry = wcore_tools::registry::ToolRegistry::new();
../../../../crates/wcore-agent/src/bootstrap.rs:3475:    let mut reg = wcore_tools::registry::ToolRegistry::new();
../../../../crates/wcore-agent/src/bootstrap.rs:3925:            allowed_tools: Vec::new(),
../../../../crates/wcore-cli/src/acp_roster.rs:35://!     `allowed_tools`, `max_turns`. [`AgentInfo`] carries ONLY `id` + `label` +
../../../../crates/wcore-cli/src/acp_roster.rs:92:    /// overlay (system_prompt/model/allowed_tools) SERVER-SIDE. Storing both
../../../../crates/wcore-cli/src/acp_roster.rs:172:    /// (system_prompt / model / allowed_tools / max_turns).
../../../../crates/wcore-cli/src/acp_roster.rs:204:    /// `allowed_tools`, and `max_turns`. Only the opaque id, a display label,
../../../../crates/wcore-cli/src/acp_roster.rs:340:    /// persona's system_prompt/model/allowed_tools must never reach a client.
../../../../crates/wcore-cli/src/acp_roster.rs:348:            allowed_tools: vec!["bash".into()],
../../../../crates/wcore-cli/src/acp_roster.rs:364:            "allowed_tools",
../../../../crates/wcore-cli/src/runtime_diagnostics.rs:731:        let snapshot = state.snapshot(&McpLifecycleCatalog::new(), &[], &ToolRegistry::new());
../../../../crates/wcore-cli/src/runtime_diagnostics.rs:760:        let snapshot = state.snapshot(&McpLifecycleCatalog::new(), &[], &ToolRegistry::new());
../../../../crates/wcore-cli/src/runtime_diagnostics.rs:828:        let snapshot = state.snapshot(&lifecycle, &[manager], &ToolRegistry::new());
../../../../crates/wcore-cli/src/runtime_diagnostics.rs:900:        let mut registry = ToolRegistry::new();
../../../../crates/wcore-cli/src/runtime_diagnostics.rs:940:        let registry = ToolRegistry::new();
../../../../crates/wcore-cli/src/runtime_diagnostics.rs:955:        let snapshot = state.snapshot(&McpLifecycleCatalog::new(), &[], &ToolRegistry::new());
../../../../crates/wcore-cli/src/runtime_diagnostics.rs:1122:            let snapshot = state.snapshot(&McpLifecycleCatalog::new(), &[], &ToolRegistry::new());
../../../../crates/wcore-cli/src/runtime_diagnostics.rs:1188:            &ToolRegistry::new(),
../../../../crates/wcore-cli/src/runtime_diagnostics.rs:1220:        let snapshot = state.snapshot(&McpLifecycleCatalog::new(), &[], &ToolRegistry::new());
../../../../crates/wcore-cli/src/runtime_diagnostics.rs:1265:        let snapshot = state.snapshot(&McpLifecycleCatalog::new(), &[], &ToolRegistry::new());
../../../../crates/wcore-cli/src/acp_engine.rs:657:        let tool_names = engine.tools().tool_names();
../../../../crates/wcore-cli/src/acp_engine.rs:799:    /// (system_prompt / model / max_turns / allowed_tools).
../../../../crates/wcore-cli/src/acp_engine.rs:881:        (config, manifest.allowed_tools.clone())
../../../../crates/wcore-cli/src/acp_engine.rs:1152:            Ok(session) => session.tool_names(),
../../../../crates/wcore-cli/src/acp_engine.rs:1767:        let engine = AgentEngine::new(placeholder_config(), ToolRegistry::new(), output);
../../../../crates/wcore-cli/src/acp_engine.rs:1975:                "name: {name}\ndescription: d\nsystem_prompt: \"{prompt}\"\nmodel: {model}\nallowed_tools:\n{tools_yaml}"
../../../../crates/wcore-cli/src/main.rs:1150:                let mut registry = wcore_tools::registry::ToolRegistry::new();
../../../../crates/wcore-cli/src/main.rs:3097:    let builtin_names = engine.tool_names();
../../../../crates/wcore-cli/src/main.rs:4614:                        let builtin_names = engine.tool_names();
../../../../crates/wcore-cli/src/main.rs:5968:            wcore_tools::registry::ToolRegistry::new(),
../../../../crates/wcore-cli/src/main.rs:6027:            wcore_tools::registry::ToolRegistry::new(),
../../../../crates/wcore-cli/src/main.rs:6500:            allowed_tools: Vec::new(),
../../../../crates/wcore-cli/src/main.rs:6717:        let mut registry = wcore_tools::registry::ToolRegistry::new();
../../../../crates/wcore-cli/src/main.rs:6747:        let registry = wcore_tools::registry::ToolRegistry::new();
../../../../crates/wcore-cli/src/main.rs:6766:        let registry = wcore_tools::registry::ToolRegistry::new();
../../../../crates/wcore-cli/src/main.rs:6837:        let before = engine.tool_names().len();
../../../../crates/wcore-cli/src/main.rs:6874:            engine.tool_names().len() > before
../../../../crates/wcore-cli/src/main.rs:6875:                && engine.tool_names().iter().any(|n| n.contains("quick_echo")),
../../../../crates/wcore-cli/src/main.rs:6877:            engine.tool_names()
../../../../crates/wcore-cli/src/main.rs:6900:                .tool_names()
../../../../crates/wcore-cli/src/main.rs:6969:        let names = registry.tool_names();
../../../../crates/wcore-cli/src/main.rs:7194:            !engine.tool_names().iter().any(|n| n.contains("quick_echo")),
../../../../crates/wcore-cli/src/agent_cmd.rs:135:        extra_allowed_tools: tool,
../../../../crates/wcore-cli/src/workflow.rs:520:            allowed_tools: vec!["Write".into(), "Edit".into()],
../../../../crates/wcore-cli/src/tui/engine_bridge.rs:2580:        let builtin_names = guard.tool_names();
../../../../crates/wcore-cli/src/crucible.rs:769:            allowed_tools: vec!["Write".into(), "Edit".into()],
../../../../crates/wcore-cli/src/tui/surfaces/mod.rs:3449:            wcore_tools::registry::ToolRegistry::new(),
../../../../crates/wcore-cli/src/tui/surfaces/mod.rs:4768:            wcore_tools::registry::ToolRegistry::new(),
../../../../crates/wcore-cli/src/tui/surfaces/mod.rs:4841:            wcore_tools::registry::ToolRegistry::new(),
../../../../crates/wcore-cli/src/tui/surfaces/mod.rs:4936:            wcore_tools::registry::ToolRegistry::new(),

hook: PostToolUse
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk sed -n '620,690p' ../../../../crates/wcore-agent/src/bootstrap.rs
rtk sed -n '1270,1315p' ../../../../crates/wcore-agent/src/bootstrap.rs
rtk sed -n '2510,2560p' ../../../../crates/wcore-agent/src/bootstrap.rs
rtk sed -n '3000,3125p' ../../../../crates/wcore-cli/src/main.rs" in /Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
 succeeded in 0ms:
        // `LlmProvider`. We materialize `primary_provider` after the
        // plugin runner has settled (see post-init block below). The
        // original ResilientProvider wrap also moves after that point.

        let memory_dir = self
            .config
            .memory
            .enabled
            .then(|| wcore_memory::paths::auto_memory_dir(cwd_path))
            .flatten();

        let file_cache = if self.config.file_cache.enabled {
            let cache = Arc::new(std::sync::RwLock::new(
                wcore_tools::file_cache::FileStateCache::new(&self.config.file_cache),
            ));
            // Token-opt: enable OUTPUT-dedup (identical-tool-result backrefs)
            // only on client-optimized routes; router-optimized routes defer
            // output/wire dedup to the server. Set once here from the resolved
            // compat (config is moved into the engine below, so it must happen
            // before construction). NOTE (#182): this no longer gates the read
            // diff-resend — that is a context reduction the server can't do for
            // us, so it runs on every route (see wcore-tools `read.rs`).
            if let Ok(mut c) = cache.write() {
                c.set_optimize_reads(self.config.compat.input_optimization() == "client");
            }
            Some(cache)
        } else {
            None
        };

        // F13 (Task 8): keep a clone of file_cache so the Script dispatcher
        // can mint a parallel Read/Write/Edit set that shares the same cache.
        let file_cache_for_script = file_cache.clone();
        // Token-opt (diff-resend): keep a clone for the engine so it can bump the
        // cache's compaction generation after each compaction pass (the original
        // `file_cache` is moved into EditTool below).
        let file_cache_for_engine = file_cache.clone();

        let mut registry = wcore_tools::registry::ToolRegistry::new();

        // W2.5: plugin discovery + initialization. PluginsConfig is
        // intentionally empty this wave; full ~/.wayland-core/plugins.toml
        // load lands in W4 alongside the permission-grant UX. Built-in
        // plugins discovered via inventory work today regardless. Per
        // design spec §5.17: one bad plugin must not crash session boot —
        // every plugin error logs via tracing::warn and continues.
        let plugins_config = wcore_config::plugins_config::PluginsConfig::default();
        let mut plugin_loader = crate::plugins::PluginLoader::discover(&plugins_config);
        let captured = plugin_loader.validate_all().unwrap_or_else(|e| {
            tracing::warn!(error = %e, "plugin validation failed; continuing without plugins");
            Vec::new()
        });
        // FleetDispatcher-class fix (audit 2026-05-24): without flipping
        // `computer_use_advertised`, every captured `CuaToolSpec` errors
        // `CapabilityDisabled` at reify-time and no CUA tool ever reaches
        // the registry. Per-OS reification still self-gates restricted
        // platforms (e.g. wlroots-only Wayland compositors via
        // `compositor_allows_background_input`), so unconditionally `true`
        // here is safe — the runtime check refuses on unsupported hosts.
        let mut plugin_runner =
            crate::plugins::PluginRunner::new().with_computer_use_advertised(true);
        let mut plugin_outcome = match plugin_runner.initialize_all(&captured).await {
            Ok(o) => o,
            Err(e) => {
                tracing::warn!(error = %e, "plugin initialization failed; continuing without plugins");
                crate::plugins::InitializeOutcome::default()
            }
        };
        for (name, err) in &plugin_outcome.errors {
            tracing::warn!(plugin = %name, error = %err, "plugin initialization error (continuing)");
        }
        ));
        // v0.9.0 W1 B6 — cronjob: wire WaylandCronScheduler over FileCronStore.
        // Adapter constructs its own FileCronStore over the default path; the
        // runner at bootstrap.rs:~1900 owns a separate FileCronStore over the
        // same path. Both writers serialise inside the store's internal mutex;
        // reads + tempfile+rename writes are atomic so the two-instance
        // pattern is safe (see tool_backends/cron.rs module doc).
        if let Some(b) = crate::tool_backends::cron::build_cron_backend() {
            registry.register(Box::new(wcore_tools::cronjob_tools::CronJobTool::new(b)));
        }

        // W3→W4 hand-off (Task 9.5): register RepoMap when enabled. Default
        // on per RepoMapToolConfig::default() — read-only and shape-bounded.
        if self.config.builtin_tools.repomap.enabled {
            registry.register(Box::new(wcore_tools::repomap::RepoMapTool::new(
                cwd_path.to_path_buf(),
            )));
        }

        // Reserve ToolSearch in the live registry before plugin and MCP tool
        // delivery. The final refresh below replaces this early snapshot after
        // every boot-time tool is present. Seeding it here makes collision
        // handling deterministic for any extension exporting `ToolSearch` and
        // prevents the final replacement from silently deleting that tool.
        registry.refresh_tool_search_catalog(&self.config.builtin_tools.defer_cold);
        let builtin_names: Vec<String> = registry.tool_names();

        // v0.6.4 Task 1.7 — deliver every captured plugin capability.
        //
        // ORDERING (§6 of the Phase 1 design notes):
        //   - tools: `apply_initialize_outcome` registers plugin tools into
        //     `registry` HERE — *after* the builtin block — so a plugin tool
        //     whose name collides with a builtin is logged + skipped
        //     (builtins win). It runs before the MCP pass so the
        //     `builtin_names` snapshot above is the pure-builtin set.
        //   - agents: returned in `applied.agent_registry`, threaded into
        //     `SpawnTool` + the engine after construction.
        //   - skills: `applied.plugin_skills` appended to the session-local
        //     bundled catalog BEFORE `load_catalog_with_bundled` (below).
        //   - rules: `applied.plugin_rules` passed to `build_system_prompt`.
        //   - hooks: `applied.plugin_hooks` handed to the engine setter
        //     after construction.
        //   - mcp: `applied.plugin_mcp_servers` connected via the
        //     `connect_plugin_mcp_servers` second pass below.
        //   - user-models: `applied.plugin_user_models` is a carrier only at
        //     v0.6.4 Task 2.2. v0.6.4 Task 2.3 will reify each
        };

        // Channel tool posture — for a per-session channel engine, reduce
        // (and, in `Workspace`, jail) the toolset BEFORE it moves into the
        // engine, so a remote sender cannot reach host filesystem/shell
        // tools. No-op for the local CLI/TUI/json-stream engines (posture
        // `None`); for `Full` channel/remote it drops only the unconfined-search
        // tools (Grep/Glob, #667 residual) and keeps the rest. Runs after all
        // built-in registration; MCP tools survive every posture, so
        // post-construction MCP wiring is unaffected.
        if let Some(scope) = self.channel_tool_posture.as_ref() {
            // Task 8 — bootstrap UX gate. Probe whether the platform's
            // sandbox backend enforces secret-read-deny; if not, suppress
            // Bash from the Workspace schema so the LLM isn't offered a
            // tool that would always refuse at exec time. The exec-time
            // gate in bash.rs remains the authoritative boundary.
            let enforces = registry.sandbox_runtime().enforces_read_deny();
            crate::channel_tools::apply_posture(&mut registry, scope, enforces);
            tracing::info!(
                target: "wcore_agent::bootstrap",
                posture = ?scope.posture,
                sandbox_enforces_read_deny = enforces,
                "channel engine tool posture applied"
            );
        }

        // persona-profiles PR-4' — narrow the toolset to the selected persona's
        // declared `allowed_tools`. Same `retain` mechanism as the channel
        // posture above, and applied AFTER it so a persona composes with (and can
        // only further narrow) any posture already in force — it can never
        // re-widen a restricted channel engine.
        if let Some(allowed) = &self.persona_tool_allowlist {
            registry.retain(|t| allowed.iter().any(|a| a == t.name()));
            tracing::info!(
                target: "wcore_agent::bootstrap",
                allowed = ?allowed,
                "persona tool allowlist applied"
            );
        }

        // Every session gets a workspace policy so BashTool's OS sandbox is
        // rooted at the workspace. Only a fingerprint-trusted, non-Managed,
        // genuinely-local session receives the capability-aware Trusted
        // profile. Untrusted repositories and every remote session stay in
        // the Contained profile; repository content cannot select this branch.
        let is_channel_remote = self.channel_tool_posture.is_some();
        if registry.workspace_policy().is_none() {
            let strict_workspace = is_channel_remote
                || self.config.execution_policy.is_managed()
                || !self.config.workspace_trust.is_trusted();
            let workspace = std::path::PathBuf::from(&self.workspace);
            cleanup_outcome,
            removed_tools,
            removal_ledger,
            writer,
        );
        return;
    }
    dynamic_managers.retain(|manager| {
        !(manager.hosts_server(&command.name) || manager.health().contains_key(&command.name))
    });
    runtime_diagnostics.remove_runtime_declaration(&command.name);
    let _ = lifecycle.complete_stopping(&command.name);

    emit_mcp_removal_receipt(
        &command,
        McpRemovalOutcome::Removed,
        removed_tools,
        removal_ledger,
        writer,
    );
}

/// wayland#551 — integrate a background-connected config-MCP manager into
/// the LIVE engine. Registers the manager's tools (same collision handling
/// as boot via [`wcore_mcp::tool_proxy::register_mcp_tools`]), emits one
/// `McpReady` per connected server and one `McpFailed` per failed server,
/// and parks the manager in `dynamic_managers` so it stays alive.
///
/// wayland#551 — absorb a settled background config-MCP connect: on
/// success, integrate into the live engine (returning the parked pair when
/// the registry is momentarily borrowed so the caller can retry between
/// turns); on failure, surface the error with bootstrap's inline-connect
/// wording. Shared by the loop-top non-blocking poll and the select arm.
fn note_deferred_mcp_connect(
    result: DeferredMcpConnectResult,
    runtime_diagnostics: Option<&mut RuntimeDiagnosticsState>,
    engine: &mut wcore_agent::engine::AgentEngine,
    writer: &ProtocolWriter,
    output: &Arc<dyn OutputSink>,
    dynamic_managers: &mut Vec<Arc<McpManager>>,
) -> Option<PendingDeferredMcp> {
    let DeferredMcpConnectResult {
        outcome,
        resolved,
        mut reservations,
    } = result;
    match outcome {
        Ok(mgr) => {
            if let Some(state) = runtime_diagnostics {
                for (name, evidence) in mgr.executable_readiness() {
                    state.record_executable_readiness(
                        wcore_protocol::diagnostics::McpDeclarationOrigin::EffectiveConfig,
                        name,
                        *evidence,
                    );
                }
            }
            let mgr = Arc::new(mgr);
            if integrate_deferred_mcp(
                engine,
                mgr.clone(),
                &resolved,
                &mut reservations,
                writer,
                dynamic_managers,
            ) {
                None
            } else {
                Some(PendingDeferredMcp {
                    manager: mgr,
                    resolved,
                    reservations,
                })
            }
        }
        Err(e) => {
            let reason = format!("MCP initialization error: {e}");
            for (_, reservation) in reservations {
                reservation.complete_failed(reason.clone());
            }
            output.emit_error(&reason, false);
            None
        }
    }
}

/// Returns `false` when the registry Arc is currently borrowed (a turn is
/// mid-flight) — the caller parks the manager and retries at the next
/// between-turns boundary.
fn integrate_deferred_mcp(
    engine: &mut wcore_agent::engine::AgentEngine,
    mgr: Arc<McpManager>,
    resolved_servers: &HashMap<String, McpServerConfig>,
    reservations: &mut HashMap<String, McpConnectionReservation>,
    writer: &ProtocolWriter,
    dynamic_managers: &mut Vec<Arc<McpManager>>,
) -> bool {
    let builtin_names = engine.tool_names();
    let defer_cold = engine.defer_cold_config();
    let Some(reg) = engine.registry_mut() else {
        return false;
    };
    wcore_mcp::tool_proxy::register_mcp_tools(reg, &mgr, &builtin_names, resolved_servers);
    reg.refresh_tool_search_catalog(&defer_cold);
    for (name, reservation) in reservations.drain() {
        match mgr.health().get(&name).and_then(mcp_server_failure_reason) {
            Some(reason) => {
                reservation.complete_failed(reason);
            }
            None if mgr.health().contains_key(&name) => {
                reservation.complete_ready();
            }
            None => {
                reservation.complete_failed("connect outcome missing from MCP health report");
            }
        }
    }
    for event in mcp_ready_events_for(&mgr, reg) {
        let _ = writer.emit(&event);
    }
    for event in mcp_failed_events_for(&mgr) {
        let _ = writer.emit(&event);
    }
    dynamic_managers.push(mgr);
    true
}

hook: PostToolUse
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk rg -n \"AgentSpawner::new\\(\" ../../../../crates --glob '*.rs' | head -240" in /Users/seandonahoe/dev/waylandcore-ferrox/.planning/phases/21-child-authority-and-budget-inheritance/21-03-t2-panel
 succeeded in 0ms:
../../../../crates/wcore-agent/src/engine.rs:250:        let workflow_spawner = engine.govern_transient_spawner(AgentSpawner::new(
../../../../crates/wcore-agent/src/engine.rs:254:        let crucible_spawner = engine.govern_transient_spawner(AgentSpawner::new(
../../../../crates/wcore-agent/src/engine.rs:12061:        let spawner = crate::spawner::AgentSpawner::new(
../../../../crates/wcore-agent/src/engine.rs:12398:        let spawner = crate::spawner::AgentSpawner::new(
../../../../crates/wcore-agent/tests/pipeline_test.rs:184:    let (spawner, _session_root) = bound_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/pipeline_test.rs:215:    let (spawner, _session_root) = bound_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/pipeline_test.rs:258:    let (spawner, _session_root) = bound_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/pipeline_test.rs:339:    let (spawner, _session_root) = bound_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/pipeline_test.rs:381:    let (spawner, _session_root) = bound_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/pipeline_test.rs:421:    let (spawner, _session_root) = bound_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_runner_test.rs:314:    let (spawner, _session_root) = bind_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_runner_test.rs:362:    let (spawner, _session_root) = bind_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_runner_test.rs:403:    let (spawner, _session_root) = bind_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_runner_test.rs:446:    let (spawner, _session_root) = bind_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_runner_test.rs:515:    let (spawner, _session_root) = bind_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_runner_test.rs:581:    let (spawner, _session_root) = bind_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_runner_test.rs:636:    let (spawner, _session_root) = bind_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_runner_test.rs:753:        bind_spawner(AgentSpawner::new(provider, test_config()).with_bus(Arc::clone(&bus)));
../../../../crates/wcore-agent/tests/workflow_runner_test.rs:848:        bind_spawner(AgentSpawner::new(provider, test_config()).with_bus(Arc::clone(&bus)));
../../../../crates/wcore-agent/tests/workflow_runner_test.rs:925:    let (spawner, _session_root) = bind_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_runner_test.rs:999:        let (spawner, _session_root) = bind_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_limits_test.rs:191:    let (spawner, _session_root) = bound_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_limits_test.rs:251:    let (spawner, _session_root) = bound_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_limits_test.rs:282:    let (spawner, _session_root) = bound_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_limits_test.rs:350:        bound_test_spawner(AgentSpawner::new(Arc::new(OrderProvider), test_config()));
../../../../crates/wcore-agent/tests/crucible_scenarios.rs:132:    let spawner = AgentSpawner::new(Arc::new(NeverProvider), test_config())
../../../../crates/wcore-agent/src/spawner.rs:2629:        let spawner = AgentSpawner::new(provider.clone(), config)
../../../../crates/wcore-agent/src/spawner.rs:2665:        let spawner = AgentSpawner::new(provider.clone(), config)
../../../../crates/wcore-agent/src/spawner.rs:2731:        let spawner = AgentSpawner::new(provider.clone(), config)
../../../../crates/wcore-agent/src/spawner.rs:2779:        let spawner = AgentSpawner::new(provider.clone(), Config::default())
../../../../crates/wcore-agent/src/spawner.rs:2804:        let parent = AgentSpawner::new(Arc::clone(&provider), Config::default())
../../../../crates/wcore-agent/src/spawner.rs:2813:            AgentSpawner::new(provider, Config::default()).with_budget_governance(governance);
../../../../crates/wcore-agent/src/spawner.rs:2882:        let spawner = AgentSpawner::new(provider, Config::default()).with_budget_governance(
../../../../crates/wcore-agent/src/spawner.rs:2923:        let spawner = AgentSpawner::new(provider.clone(), config)
../../../../crates/wcore-agent/src/spawner.rs:3005:        let spawner = AgentSpawner::new(Arc::new(StubProvider), Config::default())
../../../../crates/wcore-agent/src/spawner.rs:3070:        let spawner = AgentSpawner::new(parent.clone(), Config::default());
../../../../crates/wcore-agent/src/spawner.rs:3079:        let spawner = AgentSpawner::new(parent.clone(), Config::default())
../../../../crates/wcore-agent/src/spawner.rs:3091:        let spawner = AgentSpawner::new(parent, Config::default());
../../../../crates/wcore-agent/src/spawner.rs:3104:        let spawner = AgentSpawner::new(parent, Config::default())
../../../../crates/wcore-agent/src/spawner.rs:3119:        let spawner = AgentSpawner::new(parent, Config::default())
../../../../crates/wcore-agent/src/spawner.rs:3136:            AgentSpawner::new(parent, Config::default()).with_egress_policy(policy.clone());
../../../../crates/wcore-agent/src/spawner.rs:3146:        let spawner = AgentSpawner::new(parent, Config::default());
../../../../crates/wcore-agent/src/spawner.rs:3171:        let spawner = AgentSpawner::new(parent, base);
../../../../crates/wcore-agent/src/spawner.rs:3185:        let s = AgentSpawner::new(parent, Config::default()).with_budget_tracker(tracker.clone());
../../../../crates/wcore-agent/src/spawner.rs:3204:        let spawner = AgentSpawner::new(parent, Config::default())
../../../../crates/wcore-agent/src/spawner.rs:3269:        let spawner = AgentSpawner::new(parent, Config::default())
../../../../crates/wcore-agent/src/spawner.rs:3431:        let spawner = AgentSpawner::new(parent, Config::default())
../../../../crates/wcore-agent/src/spawner.rs:3606:        AgentSpawner::new(provider, config)
../../../../crates/wcore-agent/src/spawner.rs:3628:        AgentSpawner::new(provider, config)
../../../../crates/wcore-agent/src/spawner.rs:3708:        let spawner = AgentSpawner::new(provider_dyn, Config::default())
../../../../crates/wcore-agent/src/spawner.rs:4442:        let spawner = AgentSpawner::new(provider, Config::default())
../../../../crates/wcore-agent/src/spawner.rs:4567:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), parent);
../../../../crates/wcore-agent/src/spawner.rs:4587:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), parent);
../../../../crates/wcore-agent/src/spawner.rs:4603:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), parent)
../../../../crates/wcore-agent/src/spawner.rs:4653:        let template = AgentSpawner::new(Arc::new(NeverProvider), authority.clone());
../../../../crates/wcore-agent/src/spawner.rs:4684:        let standalone = AgentSpawner::new(Arc::new(NeverProvider), routed)
../../../../crates/wcore-agent/src/spawner.rs:4713:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), parent);
../../../../crates/wcore-agent/src/spawner.rs:4739:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), parent);
../../../../crates/wcore-agent/src/spawner.rs:4771:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), Config::default())
../../../../crates/wcore-agent/src/spawner.rs:4808:        let spawner = AgentSpawner::new(provider.clone(), config)
../../../../crates/wcore-agent/tests/workflow_live_gate_test.rs:704:    let spawner = AgentSpawner::new(quiet_provider(), test_config())
../../../../crates/wcore-agent/tests/workflow_live_gate_test.rs:748:    let spawner = AgentSpawner::new(quiet_provider(), test_config());
../../../../crates/wcore-agent/tests/workflow_live_gate_test.rs:773:    let spawner = AgentSpawner::new(quiet_provider(), test_config());
../../../../crates/wcore-agent/tests/workflow_live_gate_test.rs:805:        match AgentSpawner::new(quiet_provider(), test_config()).with_parent_workspace(&missing) {
../../../../crates/wcore-agent/tests/workflow_live_gate_test.rs:816:    let err = match AgentSpawner::new(quiet_provider(), test_config()).with_parent_workspace(&file)
../../../../crates/wcore-agent/tests/workflow_live_gate_test.rs:841:    let spawner = AgentSpawner::new(quiet_provider(), test_config())
../../../../crates/wcore-agent/tests/workflow_live_gate_test.rs:938:    let spawner = AgentSpawner::new(quiet_provider(), test_config())
../../../../crates/wcore-agent/tests/crucible_assembler.rs:65:    let spawner = AgentSpawner::new(Arc::new(NeverProvider), test_config())
../../../../crates/wcore-agent/src/orchestration/anvil/seat.rs:91:        AgentSpawner::new(resolved.provider, resolved.config),
../../../../crates/wcore-agent/tests/crucible_council.rs:143:        AgentSpawner::new(Arc::new(NeverProvider), test_config())
../../../../crates/wcore-agent/tests/crucible_council.rs:580:        bind_spawner(AgentSpawner::new(provider.clone(), test_config())),
../../../../crates/wcore-agent/tests/crucible_council.rs:600:        bind_spawner(AgentSpawner::new(provider.clone(), test_config())),
../../../../crates/wcore-agent/tests/crucible_council.rs:720:        bind_spawner(AgentSpawner::new(provider.clone(), test_config())),
../../../../crates/wcore-agent/tests/crucible_budget.rs:135:    let spawner = AgentSpawner::new(Arc::new(NeverProvider), test_config())
../../../../crates/wcore-agent/tests/fleet_dispatcher_wired_test.rs:84:        AgentSpawner::new(provider, test_config()).with_bus(Arc::clone(&bus)),
../../../../crates/wcore-agent/tests/fleet_dispatcher_wired_test.rs:172:        AgentSpawner::new(provider, test_config()).with_bus(Arc::clone(&bus)),
../../../../crates/wcore-agent/tests/workflow_e2e.rs:203:    let (spawner, _session_root) = bound_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/crucible_provider_diversity_test.rs:92:    let spawner = AgentSpawner::new(parent, test_config()).with_provider_resolver(resolver);
../../../../crates/wcore-agent/tests/crucible_provider_diversity_test.rs:105:    let spawner = AgentSpawner::new(parent, test_config()).with_provider_resolver(resolver);
../../../../crates/wcore-agent/tests/crucible_provider_diversity_test.rs:117:    let spawner = AgentSpawner::new(parent, test_config()).with_provider_resolver(resolver);
../../../../crates/wcore-agent/tests/spawn_test.rs:36:        common::bind_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/spawn_test.rs:75:        common::bind_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/spawn_test.rs:143:        common::bind_test_spawner(AgentSpawner::new(Arc::clone(&provider_dyn), test_config()));
../../../../crates/wcore-agent/tests/spawn_test.rs:168:        common::bind_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/src/orchestration/council/run.rs:556:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), Config::default());
../../../../crates/wcore-agent/src/orchestration/council/run.rs:590:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), Config::default())
../../../../crates/wcore-agent/src/orchestration/council/run.rs:610:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), Config::default())
../../../../crates/wcore-agent/src/orchestration/council/run.rs:635:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), Config::default())
../../../../crates/wcore-agent/src/orchestration/council/run.rs:712:        let spawner = AgentSpawner::new(counting.clone(), config)
../../../../crates/wcore-agent/src/orchestration/council/run.rs:754:        let spawner = AgentSpawner::new(counting.clone(), config)
../../../../crates/wcore-agent/tests/spawn_description_test.rs:17:    let spawner = Arc::new(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/src/orchestration/council/aggregator.rs:181:        let spawner = AgentSpawner::new(Arc::new(NeverProvider), Config::default());
../../../../crates/wcore-agent/src/spawn_tool.rs:628:            AgentSpawner::new(Arc::new(ImmediateProvider), config)
../../../../crates/wcore-agent/src/spawn_tool.rs:761:            AgentSpawner::new(Arc::new(PartialFailProvider), config)
../../../../crates/wcore-agent/tests/spawn_relay_test.rs:76:        bound_test_spawner_arc(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/spawn_relay_test.rs:186:        bound_test_spawner_arc(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/spawn_relay_test.rs:219:        bound_test_spawner_arc(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/spawn_relay_test.rs:289:        bound_test_spawner_arc(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_tool_test.rs:126:        bound_test_spawner_arc(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_tool_test.rs:172:        bound_test_spawner_arc(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_tool_test.rs:197:        bound_test_spawner_arc(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_tool_test.rs:253:        bound_test_spawner_arc(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_tool_test.rs:286:        bound_test_spawner_arc(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/anvil_forge_transaction.rs:627:        let spawner = AgentSpawner::new(provider, config)
../../../../crates/wcore-agent/tests/workflow_synth_test.rs:122:        common::bind_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_synth_test.rs:164:        common::bind_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_synth_test.rs:195:        common::bind_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_synth_test.rs:217:        common::bind_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_synth_test.rs:244:        common::bind_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_synth_test.rs:279:        common::bind_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/workflow_synth_test.rs:308:        common::bind_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/spawn_bus_lifecycle_test.rs:90:    let spawner = AgentSpawner::new(provider, test_config()).with_bus(Arc::clone(&bus));
../../../../crates/wcore-agent/tests/spawn_bus_lifecycle_test.rs:161:    let spawner = AgentSpawner::new(provider, test_config()).with_bus(Arc::clone(&bus));
../../../../crates/wcore-agent/tests/spawn_bus_lifecycle_test.rs:187:    let spawner = AgentSpawner::new(provider, test_config());
../../../../crates/wcore-agent/tests/spawn_bus_lifecycle_test.rs:205:    let spawner = AgentSpawner::new(provider, test_config()).with_bus(Arc::clone(&bus));
../../../../crates/wcore-agent/tests/spawn_bus_lifecycle_test.rs:260:    let spawner = AgentSpawner::new(provider, test_config()).with_bus(Arc::clone(&bus));
../../../../crates/wcore-agent/tests/spawn_bus_lifecycle_test.rs:283:    let spawner = AgentSpawner::new(provider, test_config()).with_bus(Arc::clone(&bus));
../../../../crates/wcore-agent/tests/workflow_runner_spike.rs:108:    let (spawner, _session_root) = bound_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/src/bootstrap.rs:2195:                crate::spawner::AgentSpawner::new(provider.clone(), self.config.clone()),
../../../../crates/wcore-agent/tests/forgeflows_live_relay_test.rs:113:    let (spawner, _session_root) = bound_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-agent/tests/forgeflows_live_relay_test.rs:163:    let (spawner, _session_root) = bound_test_spawner(AgentSpawner::new(provider, test_config()));
../../../../crates/wcore-cli/src/workflow.rs:173:        AgentSpawner::new(provider, config.clone()),
../../../../crates/wcore-cli/src/crucible.rs:36:        AgentSpawner::new(provider, config.clone()),
../../../../crates/wcore-cli/tests/child_authority_corpus/surfaces.rs:339:    let spawner = AgentSpawner::new(provider, config)

hook: PostToolUse
hook: PostToolUse Completed
codex
Steelman: `authorize-partial` captures contained security gains without forcing the unused PolicyGate architecture into production.

Assumptions:

1. F21-02-01’s bootstrap wiring covers every production spawner.
2. Provenance-only contract changes are handled by the established regeneration procedure.

Counter-arguments:

1. Assumption 1 is false. Production creates additional spawners in `engine.rs`, standalone workflow/crucible paths, and Anvil. With `parent_tool_authority: None` remaining fail-open, the proposed F21-02-01 change does not close the seam—and its live case still refuses for the old workspace reason. Decline F21-02-01 as honestly open.
2. F21-02-02 is contained: it ratchets only `PolicySource::Child`, changed no functional test, and moved only `source_inputs_digest`, not schema. Authorize it, including contract regeneration/check and provenance re-pin.
3. Reject `authorize-full`: F21-02-03 denies ordinary tool dispatch across 22 functional tests and requires the inheritance architecture explicitly excluded from this phase. Decline it. Reject `decline-all` because it would discard the independently contained approval repair. Reject `disprove-and-correct` because none of the three confirmed findings was triaged as an invalid corpus question.

Verdict: F21-02-02 is repaired; none are disproved; F21-02-01 and F21-02-03 are declined and remain explicitly open. Phase 21 cannot claim Success Criterion 1 unconditionally.

BUNDLE=2d5a3d550dcd1e117b03249f6a5e20b66360f64b
PANEL_POSITION=authorize-partial
hook: Stop
hook: Stop Completed
tokens used
123,118
Steelman: `authorize-partial` captures contained security gains without forcing the unused PolicyGate architecture into production.

Assumptions:

1. F21-02-01’s bootstrap wiring covers every production spawner.
2. Provenance-only contract changes are handled by the established regeneration procedure.

Counter-arguments:

1. Assumption 1 is false. Production creates additional spawners in `engine.rs`, standalone workflow/crucible paths, and Anvil. With `parent_tool_authority: None` remaining fail-open, the proposed F21-02-01 change does not close the seam—and its live case still refuses for the old workspace reason. Decline F21-02-01 as honestly open.
2. F21-02-02 is contained: it ratchets only `PolicySource::Child`, changed no functional test, and moved only `source_inputs_digest`, not schema. Authorize it, including contract regeneration/check and provenance re-pin.
3. Reject `authorize-full`: F21-02-03 denies ordinary tool dispatch across 22 functional tests and requires the inheritance architecture explicitly excluded from this phase. Decline it. Reject `decline-all` because it would discard the independently contained approval repair. Reject `disprove-and-correct` because none of the three confirmed findings was triaged as an invalid corpus question.

Verdict: F21-02-02 is repaired; none are disproved; F21-02-01 and F21-02-03 are declined and remain explicitly open. Phase 21 cannot claim Success Criterion 1 unconditionally.

BUNDLE=2d5a3d550dcd1e117b03249f6a5e20b66360f64b
PANEL_POSITION=authorize-partial

---
phase: 22-supervision-durable-goals-fleet-loops
plan: "04"
type: execute
wave: 4
depends_on:
  - "22-01"
  - "22-02"
  - "22-03"
files_modified:
  - .planning/phases/22-supervision-durable-goals-fleet-loops/22-04-SURFACE-PARITY.md
  - crates/wcore-types/src/goal.rs
  - crates/wcore-agent/src/goal/loop_policy.rs
  - crates/wcore-agent/src/goal/mod.rs
  - crates/wcore-agent/src/slash/goal.rs
  - crates/wcore-agent/src/slash/mod.rs
  - crates/wcore-protocol/src/commands.rs
  - crates/wcore-protocol/src/events.rs
  - crates/wcore-protocol/src/contract/generate.rs
  - crates/wcore-protocol/contracts/desktop/v1/manifest.json
  - crates/wcore-cli/src/goal_cmd.rs
  - crates/wcore-cli/src/lib.rs
  - crates/wcore-cli/src/main.rs
  - crates/wcore-cli/src/tui/surfaces/goals.rs
  - crates/wcore-cli/src/tui/surfaces/mod.rs
  - crates/wcore-eval-scenarios/src/pty_capture.rs
  - crates/wcore-protocol/tests/desktop_contract_corpus.rs
  - crates/wcore-agent/tests/goal_loop_bounds_test.rs
  - crates/wcore-cli/tests/goal_surface_parity_live_test.rs
  - .planning/phases/22-supervision-durable-goals-fleet-loops/22-04-EVIDENCE/
autonomous: true
requirements:
  - F22-01
  - F22-05
  - F22-07
must_haves:
  truths:
    - "THREE SURFACES OBSERVING IDENTICAL STATE IS A CONSTRUCTION PROBLEM, NOT A TESTING PROBLEM. If the CLI, the TUI and the host protocol each render Goal state from their own read of the world, they will agree in testing and drift in production, and the drift will be invisible until a user sees two different answers. The only construction that proves Success Criterion 1 is ONE typed Core command and event set, with all three surfaces as thin adapters over it that cannot add, reinterpret or supplement state. The canonical architecture says this in as many words: slash commands and Desktop controls are thin adapters over typed Core commands. Parity then becomes a property of the construction, not a promise in a summary."
    - "THE PROTOCOL CRATE CANNOT DEPEND ON THE AGENT CRATE, WHICH IS WHY THE GOAL VOCABULARY WENT INTO THE TYPES CRATE IN 22-01. `crates/wcore-protocol/Cargo.toml` declares exactly one internal dependency and it is the types crate. The layering rule in AGENTS.md puts the protocol crate below the agent crate and forbids upward references. Any design that needs the protocol to name an agent-crate type is wrong at the layer level, and the fix is the vocabulary already placed in the types crate — not a duplicate definition on the wire side, which would guarantee the two drift."
    - "THE D2 FIXTURE CORPUS ALREADY EXISTS AND THIS PLAN EXTENDS IT RATHER THAN INVENTING ONE. `crates/wcore-protocol/contracts/desktop/v1/` already carries a manifest, a schema directory, types, commands, events, a compatibility tree and an adversarial tree, generated by `crates/wcore-protocol/src/contract/generate.rs` under an explicit major, minor and generator version, and checked by `check_contract` and the corpus and adversarial test suites. Success Criterion 1's clause about emitting canonical serialized producer fixtures consumed later at D2 means adding Goal, task, wait, log and cursor fixtures to THAT corpus under a minor bump — not producing a second fixture set beside it."
    - "THE MANIFEST IS A DECLARED SERIALIZATION SEAM AND IT CONFLICTS DETERMINISTICALLY. The 20A handoff names `contracts/desktop/v1/manifest.json` explicitly as a byte-exact artifact whose concurrent regeneration conflicts every time, alongside the contract generator's version constants. This plan is the only Phase 22 plan authorized to touch either. Regenerate rather than hand-edit, and bump the minor version once — a hand-edited manifest that disagrees with what the generator would produce is a contract that passes its own check and lies to its consumer."
    - "PERSISTENT SCHEDULING IS AN EXPLICIT NON-CLAIM AND THE NON-CLAIM MUST BE ENFORCED, NOT MERELY DOCUMENTED. F22-05 says session-local loops are bounded and that persistent scheduled routines remain an explicit non-claim until the Phase 24 runtime closes them; Success Criterion 4 repeats it. The cron crate already exists with a job model, a schedule, a store and a runner, and the canonical architecture states that persistent routines use the Phase 24 service and the existing cron crate only AFTER its headless dispatch and cryptographic integrity boundaries are corrected. So the risk is not that someone forgets to document the boundary — it is that a loop quietly acquires a durable trigger by being wired to that store. The boundary needs a test that fails if a session loop can survive its session, not a sentence in a manual."
    - "A LOOP THAT OVERLAPS ITSELF OR BURSTS AFTER A GAP IS THE ENTIRE FAILURE MODE. The bounds named in the requirement are expiry, iteration count, no-progress, concurrency and cumulative authority and resource limits, and the canonical architecture adds idle-only dispatch, jitter and no catch-up storms. A machine that sleeps for an hour must not wake to sixty queued iterations, and a slow iteration must not have a second one start beside it. Both are trivially reproducible with a real process — suspend it, resume it, and count — and neither is provable from a unit test that advances a fake clock, because a fake clock cannot reproduce the thing that actually happens to a laptop lid."
    - "RECONNECT, STALE COMMANDS AND DUPLICATE ACKNOWLEDGEMENTS ARE ALREADY SOLVED SHAPES IN THIS CODEBASE AND MUST BE REUSED. `crates/wcore-protocol/src/commands.rs` already carries a versioned resynchronization command with a closed payload, an unknown-field refusal, a request identifier and an optional cursor, plus a resume-turn command and an interrupted-approval resolution, and there is an operator-resolution authority validator with explicit unsupported-version, malformed and stale variants. `crates/wcore-protocol/src/events.rs` already defines the recovery cursor as a journal sequence plus a digest, with the absent sequence documented as the unambiguous genesis position. F22-07's requirements are these mechanisms extended to Goal state — not a second reconnect protocol."
    - "THE TUI CANNOT BE VERIFIED BY ASSERTION ALONE AND THIS PROJECT ALREADY HAS THE INSTRUMENT. `crates/wcore-eval-scenarios/src/pty_capture.rs` and `crates/wcore-eval-scenarios/tests/pty_tui_smoke.rs` already boot the real binary under a pseudo-terminal, parse the byte stream into a screen grid and assert on rendered chrome, with the binary discovered from an environment override and a clean skip when it is absent. That is the instrument for the TUI half of Success Criterion 1, and the reason it exists is exactly the failure this phase must not repeat: Phase 20A drove acceptance to green and nobody ever launched the binary. Its capture module is gated to one platform family, which is a known limit to be handled explicitly rather than silently."
    - "A REPORTED RED IS WORTH FAR MORE THAN AN ENGINEERED GREEN. Never weaken an assertion, `#[ignore]`, `#[allow]`, re-gate, delete a test, or raise a timeout to reach a gate. The specific forgeries here are asserting parity by comparing two surfaces that share a code path while the third is excluded, and lengthening a loop's interval until the overlap stops reproducing."
  artifacts:
    - path: crates/wcore-cli/src/goal_cmd.rs
      provides: "The CLI adapter: start, list, inspect, log, steer, pause, cancel, resume, retry and acknowledge, each a thin call onto the typed Core command with no surface-local state"
    - path: crates/wcore-cli/src/tui/surfaces/goals.rs
      provides: "The TUI adapter rendering the same typed state, with no independent read of the world"
    - path: crates/wcore-protocol/src/commands.rs
      provides: "The typed host commands for the same operations, closed payloads with unknown-field refusal, following the existing versioned recovery command idiom"
    - path: crates/wcore-agent/src/goal/loop_policy.rs
      provides: "The bounded session loop: expiry, iteration, no-progress, concurrency and cumulative limits, idle-only dispatch, jitter, no catch-up burst, and the enforced non-claim that a session loop cannot acquire a durable trigger"
    - path: crates/wcore-protocol/contracts/desktop/v1/manifest.json
      provides: "The regenerated D2 producer corpus including Goal, task, wait, log and cursor fixtures under one minor bump"
    - path: crates/wcore-cli/tests/goal_surface_parity_live_test.rs
      provides: "The assertion side of the live three-surface exercise — it compares the CLI's rendering, the TUI's rendered screen and the host stream's events for one real Goal driven by the real binary"
  key_links:
    - from: crates/wcore-types/src/goal.rs
      to: crates/wcore-protocol/src/commands.rs
      via: "the shared vocabulary placed in the types crate in 22-01, so the wire and the engine cannot define Goal state twice"
      pattern: "single-definition"
    - from: crates/wcore-protocol/src/contract/generate.rs
      to: crates/wcore-protocol/contracts/desktop/v1/manifest.json
      via: "regeneration under one minor bump — never a hand edit, because a hand-edited manifest passes its own check and lies to its consumer"
      pattern: "generated-not-authored"
    - from: crates/wcore-agent/src/goal/loop_policy.rs
      to: crates/wcore-cron/src/store.rs
      via: "the enforced non-claim: a session-local loop must not be able to acquire a durable trigger before the Phase 24 runtime closes that boundary"
      pattern: "enforced-boundary"
---

<objective>
Give Goal, child, task, wait, log, cursor and terminal state one typed Core command and event surface that the CLI, the TUI and the host protocol all adapt without reinterpreting; make session-local loops bounded and unable to outlive their session; and emit the canonical serialized producer fixtures the later D2 consumer gate replays.

Purpose: This closes Success Criterion 1 and Success Criterion 4. Criterion 1 explicitly names three paths observing and controlling IDENTICAL state, which no test suite can establish — three surfaces that each pass their own tests can still disagree, and the only proof is driving one real Goal and reading it back from all three at once. Criterion 4's loop bounds are equally unprovable from a fake clock: a machine that sleeps and wakes is the thing that breaks interval scheduling, and the only way to observe it is to suspend a real process and count what happens on resume.
Output: One typed command and event set with three thin adapters; a bounded loop policy with an enforced non-claim against durable triggers; the D2 producer corpus regenerated under one minor bump; and live exercises that drive the real binary and prove the three surfaces agree, that a suspended-then-resumed loop does not burst, and that a reconnect after a gap resumes at the right cursor.
</objective>

<execution_context>
@$HOME/.claude/ferrox-core/workflows/execute-plan.md
@$HOME/.claude/ferrox-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/HANDOFF-2026-07-26-phase20-20A-complete.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/22-supervision-durable-goals-fleet-loops/22-01-JOURNAL-COMPAT.md
@.planning/phases/22-supervision-durable-goals-fleet-loops/22-02-LOOP-OWNER-CENSUS.md
@.planning/phases/22-supervision-durable-goals-fleet-loops/22-03-CLAIM-MODEL.md
@crates/wcore-types/src/goal.rs
@crates/wcore-protocol/src/commands.rs
@crates/wcore-protocol/src/events.rs
@crates/wcore-protocol/src/contract/generate.rs
@crates/wcore-agent/src/slash/mod.rs
@crates/wcore-cli/src/main.rs
@crates/wcore-eval-scenarios/src/pty_capture.rs
@crates/wcore-eval-scenarios/tests/pty_tui_smoke.rs
</context>

<execution_rules>

**THE TWO AMENDED PHASE RULES — stated verbatim, and they bound this plan.**

- Findings at CRITICAL or HIGH must be fixed or disproved. MEDIUM and below are logged to BACKLOG and DO NOT BLOCK execution.
- Execution begins when no CRITICAL or HIGH finding is open, or after 2 review rounds, whichever comes first. A third round is NOT permitted; it escalates to Sean.

**TERMINATION CRITERION FOR THIS PLAN (hard — the plan STOPS and escalates rather than spawning more work).** This plan builds once, generates once, and proves once. It terminates in exactly one of four states, and in all four it writes its SUMMARY and stops:
1. **Complete** — three surfaces adapt one typed command set, the live parity exercise shows them agreeing on one real Goal, loops are bounded and cannot acquire a durable trigger, the suspend-and-resume exercise shows no burst, and the D2 corpus is regenerated under one minor bump with its checks green.
2. **Complete with a recorded platform limit** — the TUI drive could not be made to work on one platform within the bounded attempt. The limit is stated exactly, the other two surfaces are still proven live on that platform, and the gap is recorded as an open item with the reason. This is honest; silently omitting the platform is not.
3. **Escalated on layering** — the surface set cannot be expressed without the protocol crate depending on the agent crate, or without duplicating the Goal vocabulary on the wire. STOP. Both are architecture violations and neither is fixable from inside this plan.
4. **Escalated on the non-claim** — a session-local loop cannot be prevented from acquiring a durable trigger without changing the cron crate's integrity boundary. STOP and escalate; that boundary belongs to Phase 24.
Under no circumstances does this plan spawn additional plans or extend its own task list. Implementation is bounded at TWO edit-build-run iterations per platform; the TUI drive on the harder platform is bounded at TWO attempts before termination state 2 applies.

**SCOPE BOUNDARY (hard).** This plan owns the operator surfaces, the loop policy and the D2 fixture corpus. It does NOT change the Goal record shape or terminal taxonomy (22-01), does NOT change strategy subordination (22-02), and does NOT change the claim model (22-03). It is the ONLY Phase 22 plan authorized to touch the contract generator, the contract version constants or the desktop manifest — those are declared serialization seams that conflict deterministically.

**PERSISTENT SCHEDULING IS OUT OF SCOPE AND THE BOUNDARY IS ENFORCED.** No durable trigger, no cron store wiring, no service registration. The non-claim gets a test that fails if a session loop can survive its session, not a paragraph.

**NO NEW DEPENDENCIES.** This plan adds no crates.io dependency. The pseudo-terminal and screen-grid crates it needs are ALREADY in the lockfile and in two crates' manifests, with a recorded pin reason; reuse those exact versions rather than adding or upgrading. If a genuinely new dependency becomes necessary, that is a BLOCKING checkpoint requiring a package legitimacy audit before any install; the default disposition of an unaudited package is refusal.

**NON-NEGOTIABLE.** A REPORTED RED IS WORTH FAR MORE THAN AN ENGINEERED GREEN. Never weaken an assertion, `#[ignore]`, `#[allow]`, re-gate, delete a test, or raise a timeout to reach a gate. The specific forgeries here are asserting parity between two surfaces that share a code path while excluding the third, and lengthening a loop interval until an overlap stops reproducing.

**ENVIRONMENT.**
- Linux: `ssh -o BatchMode=yes hetzner-dsm`, `/root/wayland`. Authoritative Cargo proof, the contract regeneration check, and the headless and host-protocol live legs.
- macOS (this Mac): the pseudo-terminal capture module is gated to this platform family, so the live TUI drive runs here against a PREBUILT artifact. Do not run Cargo here as a matter of workflow convention; fetch or copy the built binary. `cargo fmt --all` works here and is the formatting gate.
- Windows: `ssh -o BatchMode=yes SeanD@seandesktop`, `C:\ferrox-win`, cargo at `C:\Users\seand\.cargo\bin\cargo.exe`. Clippy gates the test job there; run it first. `cargo fmt --all` fails there. The CLI and host-protocol live legs run here unconditionally; the TUI leg is the bounded attempt described in Task 4.
- Both hosts' fetch refspecs are pinned to an unrelated branch; always fetch this branch by name.
- Always `/usr/bin/grep` and `/usr/bin/git` on the Mac; the proxied versions silently drop lines and append noise.
- NO push to main, merge, PR, tag, release, or issue closure without Sean.

**AGENTS.md discipline.** Surgical diffs only. Respect the layering: the protocol crate depends on the types crate and never upward. No hardcoded provider quirks. Platform differences go through the centralized helpers. Files stay under 1000 lines; `main.rs` and `config.rs` are both far over and must not grow beyond the minimum registration a new subcommand needs.

**Git hygiene.** Stage the exact paths in `files_modified`, never `-A`, never `.`. No `Co-Authored-By` trailers.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One typed command and event set, three thin adapters, and the bounded loop policy</name>
  <files>crates/wcore-types/src/goal.rs, crates/wcore-agent/src/goal/loop_policy.rs, crates/wcore-agent/src/goal/mod.rs, crates/wcore-agent/src/slash/goal.rs, crates/wcore-agent/src/slash/mod.rs, crates/wcore-protocol/src/commands.rs, crates/wcore-protocol/src/events.rs, crates/wcore-cli/src/goal_cmd.rs, crates/wcore-cli/src/lib.rs, crates/wcore-cli/src/main.rs, crates/wcore-cli/src/tui/surfaces/goals.rs, crates/wcore-cli/src/tui/surfaces/mod.rs, crates/wcore-agent/tests/goal_loop_bounds_test.rs</files>
  <read_first>crates/wcore-protocol/src/commands.rs (the versioned resynchronization command, the resume-turn command and the interrupted-approval resolution — their closed-payload shape, their unknown-field refusal, their request identifier and their optional cursor are the idiom the Goal commands must follow, and the operator-resolution authority validator already names unsupported-version, malformed and stale as distinct rejections), crates/wcore-protocol/src/events.rs (the recovery cursor as journal sequence plus digest, and the documented meaning of an absent sequence as genesis — this is the cursor the Goal surface hands a reconnecting host, not a new one), crates/wcore-agent/src/slash/mod.rs (the parser and dispatcher, its error vocabulary, and the existing per-command modules whose shape the goal module must match), crates/wcore-cli/src/main.rs (the existing top-level subcommand set and how a subcommand's argument type is declared in the library and dispatched from the binary — follow that pattern exactly rather than inventing one), crates/wcore-cli/src/tui/surfaces/mod.rs and one existing surface such as the workflows surface (the surface registration idiom, the state it is allowed to hold, and how it obtains data — the goal surface must obtain data the same way and hold no independent read), crates/wcore-agent/src/goal/kernel.rs and ledger.rs (the state the surfaces render and the commands mutate), crates/wcore-cron/src/store.rs and job.rs (the durable trigger surface the session loop must be structurally unable to reach)</read_first>
  <behavior>
    - Test 1: every one of the ten named operations — start, list, inspect, log, steer, pause, cancel, resume, retry, acknowledge — exists as exactly one typed Core command, and each of the three adapters dispatches to it without transforming the result.
    - Test 2: an adapter cannot introduce state the Core command did not return. A surface that renders a field with no source in the typed response does not compile.
    - Test 3: host command payloads are closed and refuse unknown fields, following the existing versioned recovery idiom, so a host cannot believe an authority-bearing extension was honored when Core ignored it.
    - Test 4: a stale command — one carrying a cursor or version behind the current state — is refused distinguishably from a malformed one and from an unsupported-version one, matching the existing validator's three-way vocabulary.
    - Test 5: a duplicate acknowledgement is idempotent; the second one changes nothing and reports that it changed nothing.
    - Test 6: a loop is bounded by every named limit independently — expiry, iteration count, no-progress, concurrency and cumulative authority and resource limits — and each bound has its own case, because a loop that stops for the wrong reason is not bounded.
    - Test 7: a loop iteration cannot start while the previous one is still running.
    - Test 8: a loop that missed several intervals dispatches AT MOST ONE catch-up iteration, never a burst proportional to the gap.
    - Test 9: an event-driven wait consumes no model turns while waiting.
    - Test 10: a session-local loop cannot acquire a durable trigger. The negative case is load-bearing: the code path from the loop policy to the cron store must not exist, and this must be enforced structurally rather than by a comment.
    - Test 11: resuming a paused loop preserves its cumulative limits rather than resetting them.
  </behavior>
  <action>Put the command and event definitions where the layering allows them. The protocol crate depends on the types crate and never on the agent crate, so the Goal vocabulary placed in the types crate by 22-01 is what both sides name. Do not define a second Goal shape on the wire side; two definitions of one concept will drift and the drift is the exact failure Criterion 1 forbids.

Define ONE typed Core command per operation and make the three adapters thin over it. Thin means each adapter translates its own input syntax into the typed command, dispatches, and renders the typed response — it does not compute, cache, supplement or reinterpret. The strongest construction is for the typed response to be the only thing a surface can render from, so a field with no source in that response is not expressible. Follow the existing closed-payload idiom for the host commands: an explicit version, a request identifier, an unknown-field refusal, and an optional cursor. Reuse the existing recovery cursor rather than defining a Goal-specific one; a reconnecting host must be able to hand back exactly what it was given.

Add the CLI subcommand following the existing pattern — the argument type in the library crate, the dispatch arm in the binary — and add no more to the binary's already-oversized module than that registration requires. Add the slash command as a module beside the existing ones, matching their shape and their error vocabulary. Add the TUI surface following the existing surface registration idiom and give it no independent read of the world.

Build the loop policy as its own module. Every bound is separately enforced and separately observable, because a loop that stopped for the wrong reason is not a bounded loop even though it stopped. Dispatch only when idle; never start an iteration while the previous one runs; add jitter so a fleet of loops does not synchronize; and on resume after a gap, dispatch at most one catch-up iteration regardless of how many intervals were missed. Cumulative authority and resource limits are carried on the Goal record from 22-01, not recomputed here — a loop that resets its own limits on resume has no limits.

Enforce the persistent-scheduling non-claim structurally. The loop policy module must have no path to the cron store, and the way to prove that is a test that fails if such a path appears — not a comment asking future contributors not to add one. If the boundary cannot be made structural without changing the cron crate's integrity boundary, stop and escalate; that boundary is Phase 24's.

Write the tests first, from the behavior list, and watch them fail. Tests 2, 8 and 10 are the structural ones. Assert INVARIANTS, never error strings or numeric statuses.

Prove on Linux with the full aggregate, and on Windows with clippy first then the goal and loop suites. Do not modify, rename, re-gate or delete any existing test. Advances F22-01, F22-05 and F22-07; those complete only when Tasks 3 and 4 are green.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; git fetch gh plan/f20-unified-audit-repair &amp;&amp; git rev-parse HEAD &amp;&amp; cargo clippy --workspace --all-targets --all-features -- -D warnings'</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; cargo nextest run --profile ci --no-fail-fast' > /tmp/f22-04-t1-aggregate-linux.log 2>&amp;1; rc=$?; tail -20 /tmp/f22-04-t1-aggregate-linux.log; exit $rc</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; cargo nextest run -p wcore-agent --test goal_loop_bounds_test --no-fail-fast' > /tmp/f22-04-t1-loopbounds-linux.log 2>&amp;1; rc=$?; tail -20 /tmp/f22-04-t1-loopbounds-linux.log; exit $rc</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; ! /usr/bin/grep -rn "wcore_cron" crates/wcore-agent/src/goal/'</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -ciE goal crates/wcore-protocol/src/commands.rs)" -ge 1 || { echo "no Goal command exists on the wire yet (measured base for this plan: 0 occurrences of goal in commands.rs), so this gate has nothing to guard"; exit 1; }; echo "layering guard (a REGRESSION guard, not coverage): wcore-protocol must still declare no wcore-agent dependency"; ! /usr/bin/grep -qF 'wcore-agent' crates/wcore-protocol/Cargo.toml</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo clippy -p wcore-cli -p wcore-agent -p wcore-protocol --all-targets -- -D warnings 2>&amp;1"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; cmd /c "cargo nextest run -p wcore-agent --test goal_loop_bounds_test --no-fail-fast 2>&amp;1"; exit $LASTEXITCODE }' > /tmp/f22-04-t1-win.log 2>&amp;1; rc=$?; /usr/bin/grep -v CLIXML /tmp/f22-04-t1-win.log > /tmp/f22-04-t1-win.1; /usr/bin/grep -v '^&lt;Objs' /tmp/f22-04-t1-win.1 > /tmp/f22-04-t1-win.txt; tail -40 /tmp/f22-04-t1-win.txt; exit $rc</automated>
  </verify>
  <done>All ten operations exist as exactly one typed Core command each, with three thin adapters that cannot introduce state the command did not return. Host payloads are closed and refuse unknown fields; stale, malformed and unsupported-version rejections stay distinguishable; duplicate acknowledgement is idempotent. Every loop bound is separately enforced and separately observable; a loop cannot overlap itself; a resumed loop dispatches at most one catch-up regardless of gap size and preserves its cumulative limits; an event-driven wait consumes no model turns. The session loop has no path to the cron store and a test fails if one appears. The protocol crate still declares no dependency on the agent crate. The full Linux aggregate is green and the Windows suites are green with clippy clean there.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the D2 producer corpus with Goal, task, wait, log and cursor fixtures under one minor bump</name>
  <files>crates/wcore-protocol/src/contract/generate.rs, crates/wcore-protocol/contracts/desktop/v1/manifest.json, crates/wcore-protocol/tests/desktop_contract_corpus.rs</files>
  <read_first>crates/wcore-protocol/src/bin/wcore-contract.rs (the generate, check and digest entry points — this is the only legal way to change the corpus, and its digest mode is how idempotence is demonstrated), crates/wcore-protocol/src/contract/generate.rs (the major, minor and generator version constants, and how the manifest and the command, event and type fixtures are produced — this is a generator, so the deliverable is a generator change plus its regenerated output, never a hand edit), crates/wcore-protocol/src/contract/check.rs (the check the corpus must satisfy), crates/wcore-protocol/tests/desktop_contract_corpus.rs and desktop_contract_adversarial.rs (the existing corpus and hostile-input suites, so the new fixtures join them rather than forming a second corpus), crates/wcore-protocol/contracts/desktop/v1/ (the existing directory layout including the compatibility and adversarial trees, so new fixtures land in the right places), crates/wcore-protocol/tests/recovery_protocol.rs (the existing reconnect and cursor coverage the Goal fixtures extend)</read_first>
  <behavior>
    - The new fixtures are GENERATED by the existing generator, not authored by hand, and regenerating twice produces a byte-identical manifest.
    - The minor version is bumped exactly once for this plan's additions, and the generator version is updated with it in the existing style.
    - Fixture coverage includes each Goal lifecycle command, each Goal, task, wait, log and cursor event, and the terminal transition for every category in the 22-01 taxonomy — a category with no fixture is a category D2 cannot replay.
    - The adversarial tree gains the hostile cases the new surface introduces: a stale cursor, a duplicate acknowledgement, an unknown field on a closed Goal payload, and a command referencing a Goal that does not exist.
    - The compatibility tree proves an older consumer's view of the previous minor still decodes, so the bump is additive rather than breaking.
    - The existing contract check and both existing suites stay green with the additions in place.
  </behavior>
  <action>Change the GENERATOR and regenerate. A hand-edited manifest passes its own check and lies to its consumer, and the recorded seam warning about this artifact says it conflicts deterministically for exactly that reason — so treat regeneration as the only legal way to change it, and prove idempotence by regenerating twice and comparing.

Bump the minor version once and update the generator version alongside it in the existing style. Do not bump the major: this is additive, and the compatibility tree exists to prove that. If the additions turn out NOT to be additive for an existing consumer, that is a finding — record it with its severity rather than bumping the major from inside this plan, because the major is a shared contract decision beyond this phase.

Emit a fixture for each Goal lifecycle command, for each Goal, task, wait, log and cursor event, and for the terminal transition of EVERY category in the 22-01 taxonomy including the unpriced and partially-checked ones. Success Criterion 1 says these fixtures are consumed later at D2; a category with no fixture is a category the Desktop consumer can never be shown to handle, and the categories that will be missed are precisely the unusual ones.

Extend the adversarial tree with the hostile cases this surface introduces — a cursor behind the current state, a repeated acknowledgement, an unknown field on a closed payload, and a command naming a Goal that does not exist — and assert the refusal is the right one of the three distinguishable kinds rather than merely that something failed.

Keep the compatibility tree honest: prove that a consumer pinned to the previous minor still decodes what this producer emits for the shapes it knew about. That is the actual meaning of an additive bump and it is cheap to assert here and expensive to discover at D2.

Run the contract check and both existing suites. Do not modify, rename, re-gate or delete any existing fixture — the corpus is a record and rewriting it retroactively destroys its value. Advances F22-01 and F22-07.</action>
  <verify>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; cargo nextest run -p wcore-protocol --no-fail-fast' > /tmp/f22-04-t2-protocol-linux.log 2>&amp;1; rc=$?; tail -20 /tmp/f22-04-t2-protocol-linux.log; exit $rc</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; cargo run -p wcore-protocol --bin wcore-contract -- check'</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; cargo run -p wcore-protocol --bin wcore-contract -- generate &amp;&amp; git diff --exit-code -- crates/wcore-protocol/contracts/desktop/v1/manifest.json'</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; cargo run -p wcore-protocol --bin wcore-contract -- digest'</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; G=crates/wcore-protocol/src/contract/generate.rs; M=$(/usr/bin/sed -n 's/^pub const CONTRACT_MINOR: u64 = \([0-9]*\);$/\1/p' "$G"); V=$(/usr/bin/sed -n 's/^pub const GENERATOR_VERSION: &amp;str = "\(.*\)";$/\1/p' "$G"); echo "measured base for this plan: CONTRACT_MINOR=8 GENERATOR_VERSION=wcore-desktop-contract-gen/11 ; now: minor=$M generator=$V"; test -n "$M" -a -n "$V" || { echo "could not parse the contract version constants"; exit 1; }; test "$M" -gt 8 || { echo "CONTRACT_MINOR is still $M against the measured base of 8; this plan bumps the minor once"; exit 1; }; test "$V" != "wcore-desktop-contract-gen/11" || { echo "GENERATOR_VERSION did not move alongside the minor"; exit 1; }; /usr/bin/grep -qF "\"minor\":$M" crates/wcore-protocol/contracts/desktop/v1/manifest.json || { echo "the manifest does not carry minor=$M, so it was hand-edited or not regenerated and it disagrees with the generator"; exit 1; }</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/bin/ls crates/wcore-protocol/contracts/desktop/v1/events | /usr/bin/grep -c goal)" -ge 1</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
  </verify>
  <done>The manifest and fixtures were produced by the generator and regenerating twice is byte-identical. The minor version and the generator version were bumped exactly once. Every Goal lifecycle command, every Goal, task, wait, log and cursor event, and every terminal category in the 22-01 taxonomy including the unpriced and partially-checked ones has a fixture. The adversarial tree covers stale cursor, duplicate acknowledgement, unknown field on a closed payload and unknown Goal, each asserting the right refusal kind. The compatibility tree proves the bump is additive. The contract check and both existing suites are green and no existing fixture was modified or deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Drive one real Goal and prove the CLI, the TUI and the host stream show identical state</name>
  <files>crates/wcore-cli/tests/goal_surface_parity_live_test.rs, crates/wcore-eval-scenarios/src/pty_capture.rs, .planning/phases/22-supervision-durable-goals-fleet-loops/22-04-SURFACE-PARITY.md</files>
  <read_first>crates/wcore-eval-scenarios/src/pty_capture.rs (the existing capture: how it spawns the real binary under a pseudo-terminal, how it points the home directories at a throwaway location, how it parses the byte stream into a screen grid, how it waits for chrome with a bounded budget and dumps the last screen on timeout, and which platform family it is gated to), crates/wcore-eval-scenarios/tests/pty_tui_smoke.rs (the live-test idiom: the binary discovered from an environment override, the clean skip when it is absent, the ignore attribute so the cheap floor does not boot a terminal, and the exact invocation documented in its header), crates/wcore-cli/tests/support/pty.rs (the second existing pseudo-terminal helper, so this task extends rather than adds a third), crates/wcore-cli/src/goal_cmd.rs and crates/wcore-cli/src/tui/surfaces/goals.rs (the two surfaces being compared)</read_first>
  <behavior>
    - ONE real Goal is created by the real binary and then observed from all three surfaces — not three Goals, one each.
    - The TUI observation is a rendered-screen capture from the real binary under a pseudo-terminal, parsed into a screen grid, with the captured screen retained as evidence.
    - The host observation is the real stream from the real binary in host mode, including its cursor.
    - The three observations are compared on the state Criterion 1 names: Goal, child, task, wait, log and cursor, and terminal state.
    - Disagreement is a failure with the three renderings dumped, not a soft warning.
    - A control case proves the comparison is not vacuous: a Goal advanced between two observations must make the comparison notice.
    - The exercise runs on Linux and macOS in full; on Windows the CLI and host legs run unconditionally and the TUI leg is a bounded attempt.
  </behavior>
  <action>This is the LIVE PRODUCT EXERCISE for Success Criterion 1, and the criterion cannot be closed any other way. Build the release binary at the delivered commit; point the home and Wayland home environment at a throwaway directory so nothing touches a real profile.

LIVE EXERCISE 1 — CLI and host stream, on `hetzner-dsm:/root/wayland`. Start one long-enough-lived Goal through the new CLI subcommand and capture its identifier. While it is live, observe it two ways at once: through the CLI's inspect and log operations, and through a second process running the binary in host mode reading the typed event stream with its cursor. OBSERVABLE OUTCOME that distinguishes working from broken: both observations name the same Goal identifier, the same current state, the same task set, the same wait state, the same log tail and the same cursor position; and when the Goal advances, both advance to the same next state. Record both raw renderings verbatim in the parity record — a comparison whose inputs are not retained cannot be re-audited.

LIVE EXERCISE 2 — TUI, on this Mac against a PREBUILT artifact. Extend the existing pseudo-terminal capture rather than writing a third one: it already spawns the real binary, redirects the home directories, parses the byte stream into a screen grid, waits for chrome with a bounded budget and dumps the last screen on timeout. Drive it to the goal surface by sending the keystrokes a user would send, and capture the rendered screen. OBSERVABLE OUTCOME: the rendered screen contains the same Goal identifier, the same state and the same task count as the CLI and host observations of the SAME Goal, and the screen is retained as a text dump in the parity record. Then send a steer or pause keystroke and capture again: the rendered state must change to what the CLI and host also report. Also assert the screen is not garbled — no partial paint, no stray escape sequences in the parsed text — because a TUI that renders the right data illegibly is still broken and this is exactly the class of defect that survives a green test suite.

LIVE EXERCISE 3 — Windows, on `SeanD@seandesktop`. Run the CLI and host-stream legs unconditionally and record the same outcomes. For the TUI leg, the capture module is gated to one platform family; make a BOUNDED attempt at two iterations to drive the Windows terminal through the same capture path using the pseudo-terminal crate's Windows console backend, which is already in the lockfile. If it works, record the same rendered-screen outcome. If it does not work within two attempts, STOP: record the exact reason, keep the two proven surfaces for this platform, and record the TUI gap as an open item with its cause. That is termination state 2 and it is honest. Do not fabricate a manual checklist in its place and do not silently drop the platform.

Prove the comparison is not vacuous with a control: take two observations across a real state change and confirm the comparison NOTICES the difference. A parity assertion that passes against two snapshots of different states proves nothing at all.

Commit the assertion side as a test that takes the three captured renderings from paths given to it, so the comparison re-runs against any future capture. Record every invocation, every raw rendering and every outcome in the parity record. Advances F22-01 and F22-07.</action>
  <verify>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; git rev-parse HEAD &amp;&amp; cargo build --release --locked -p wcore-cli &amp;&amp; ls -l target/release/wayland-core' > /tmp/f22-04-t3-build-linux.log 2>&amp;1; rc=$?; tail -8 /tmp/f22-04-t3-build-linux.log; exit $rc</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; cargo nextest run -p wcore-cli --test goal_surface_parity_live_test --no-fail-fast' > /tmp/f22-04-t3-parity-linux.log 2>&amp;1; rc=$?; tail -20 /tmp/f22-04-t3-parity-linux.log; exit $rc</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; P=.planning/phases/22-supervision-durable-goals-fleet-loops; E=$P/22-04-EVIDENCE/parity; I=$E/INDEX.txt; test -f "$I" || { echo "GATE INVALID: no machine-parseable parity index at $I"; exit 1; }; ID=$(/usr/bin/sed -n 's/^GOAL-ID: //p' "$I"); test -n "$ID" || { echo "the parity index names no GOAL-ID, so nothing establishes that the three surfaces observed ONE Goal rather than three"; exit 1; }; BAD=0; for F in cli-inspect.txt tui-screen.txt host-stream.jsonl; do if test ! -s "$E/$F"; then echo "MISSING the captured $F - a parity comparison whose inputs are not retained cannot be re-audited"; BAD=$((BAD+1)); continue; fi; N=$(/usr/bin/grep -c . "$E/$F"); test "$N" -ge 5 || { echo "$F has only $N non-empty lines, which is not a retained rendering"; BAD=$((BAD+1)); }; /usr/bin/grep -qF "$ID" "$E/$F" || { echo "$F does not mention goal $ID, so the three surfaces did not observe ONE Goal"; BAD=$((BAD+1)); }; done; echo "parity_captures bad=$BAD"; test "$BAD" -eq 0</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; P=.planning/phases/22-supervision-durable-goals-fleet-loops; I=$P/22-04-EVIDENCE/parity/INDEX.txt; test -f "$I" || { echo "GATE INVALID: no machine-parseable parity index at $I"; exit 1; }; MISS=0; for K in linux-cli linux-host macos-tui windows-cli windows-host windows-tui; do /usr/bin/grep -qE "^PARITY-LEG: $K (RAN evidence=[^ ]+|NOT RUN - .{20,})$" "$I" || { echo "no conforming marker for the $K leg"; MISS=$((MISS+1)); }; done; for K in linux-cli linux-host macos-tui windows-cli windows-host; do /usr/bin/grep -qE "^PARITY-LEG: $K RAN evidence=[^ ]+$" "$I" || { echo "the $K leg is not the bounded-attempt one and is not recorded as RAN"; MISS=$((MISS+1)); }; done; BAD=0; for EV in $(/usr/bin/sed -n 's/^PARITY-LEG:.*evidence=\(.*\)$/\1/p' "$I"); do test -s "$P/$EV" || { echo "a parity leg names evidence=$EV which the gate cannot stat"; BAD=$((BAD+1)); }; done; echo "parity_legs_missing=$MISS unresolvable_evidence=$BAD"; test "$MISS" -eq 0 -a "$BAD" -eq 0</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
  </verify>
  <done>ONE real Goal was created by the real binary and observed from the CLI, from a pseudo-terminal capture of the real TUI, and from the real host event stream, with all three raw renderings retained verbatim in the parity record. The three agree on Goal, child, task, wait, log, cursor and terminal state, and they agree again after the Goal advances. The captured TUI screen is retained as text and asserted not garbled. A control case proves the comparison notices a real state difference. Linux and macOS are fully covered; Windows has its CLI and host legs proven, with the TUI leg either proven or its bounded-attempt failure recorded with its exact cause as an open item.</done>
</task>

<task type="auto">
  <name>Task 4: Suspend, resume, preempt and reconnect a real bounded loop</name>
  <files>crates/wcore-cli/tests/goal_surface_parity_live_test.rs, .planning/phases/22-supervision-durable-goals-fleet-loops/22-04-SURFACE-PARITY.md</files>
  <read_first>crates/wcore-agent/src/goal/loop_policy.rs (the bounds this exercise drives to their edges), crates/wcore-protocol/src/commands.rs and events.rs (the resynchronization command and the recovery cursor the reconnect leg uses), .planning/phases/22-supervision-durable-goals-fleet-loops/22-04-SURFACE-PARITY.md (the record this task appends to)</read_first>
  <behavior>
    - A real loop runs in a real process and its iterations are individually countable from outside the process.
    - The process is suspended past several intervals and resumed, and the catch-up behavior is counted rather than reasoned about.
    - A host disconnects mid-loop and reconnects with the cursor it was last given, and the resumed stream is contiguous with no gap and no replay of already-acknowledged events.
    - A user preemption during a live iteration is observed to stop it, and the loop's cumulative limits after preemption reflect the work actually done.
    - Every named bound is driven to its edge in a live process, not only in a unit test, because a bound that only holds against a fake clock does not hold against a laptop lid.
    - The persistent-scheduling non-claim is observed live: the loop does not survive its session.
  </behavior>
  <action>This is the LIVE PRODUCT EXERCISE for Success Criterion 4. It runs on Linux on `hetzner-dsm:/root/wayland` and on Windows on `SeanD@seandesktop`, against the real release binary and a throwaway home directory.

Instrument the loop so each iteration is countable from outside the process — the simplest honest instrument is an iteration appending a unique line to a file. A counter inside the process proves nothing once the process is suspended.

MISSED INTERVALS. Start a loop with a short interval. Let it run several iterations, then SUSPEND the process at the operating-system level — on Linux with the stop signal, on Windows by suspending the process — for a duration spanning at least five intervals. Resume it. OBSERVABLE OUTCOME that distinguishes working from broken: after resume, the iteration file grows by AT MOST ONE catch-up entry before returning to the normal cadence. Five entries appearing at once is the catch-up storm the requirement forbids, and it is only observable this way.

OVERLAP. Force an iteration to take longer than the interval and confirm from the iteration file that a second iteration never begins while the first is still open. Record the timestamps rather than asserting a boolean.

RECONNECT. With a loop live, run the binary in host mode, consume events, record the cursor, disconnect abruptly, wait past at least one interval, then reconnect presenting that exact cursor through the existing resynchronization command. OBSERVABLE OUTCOME: the resumed stream is contiguous from the presented cursor, contains the events that occurred during the gap, and does NOT replay events already acknowledged. Then present a cursor deliberately behind the current state and confirm the stale refusal is the stale one rather than the malformed or unsupported-version one.

PREEMPTION. While an iteration is live, issue the pause and then the cancel operation through the CLI. OBSERVABLE OUTCOME: the running iteration stops, no further iteration entries appear, and inspecting the Goal shows cumulative limits reflecting the work actually done rather than a reset. Then resume a paused loop and confirm its cumulative limits carried over rather than restarting.

BOUNDS. Drive each named bound to its edge in the live process — expiry, iteration count, no-progress and concurrency — and record which bound stopped the loop each time. A loop that stops for the wrong reason is not bounded, and the record must name the reason each time rather than only the fact that it stopped.

NON-CLAIM. End the session and confirm from outside the process that the loop does not run again — no durable trigger was created, nothing survived. Record what you checked, not just the conclusion.

Repeat the missed-interval, overlap and preemption legs on Windows; the suspend mechanism differs there and that is precisely why it is measured rather than ported. Append every command, count and outcome to the parity record. Completes F22-01, F22-05 and F22-07 when this task and Task 3 are both green.</action>
  <verify>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'cd /root/wayland &amp;&amp; cargo nextest run -p wcore-cli --test goal_surface_parity_live_test --no-fail-fast' > /tmp/f22-04-t4-loops-linux.log 2>&amp;1; rc=$?; tail -20 /tmp/f22-04-t4-loops-linux.log; exit $rc</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-cli --test goal_surface_parity_live_test --no-fail-fast 2>&amp;1"; exit $LASTEXITCODE }' > /tmp/f22-04-t4-loops-win.log 2>&amp;1; rc=$?; /usr/bin/grep -v CLIXML /tmp/f22-04-t4-loops-win.log > /tmp/f22-04-t4-loops-win.1; /usr/bin/grep -v '^&lt;Objs' /tmp/f22-04-t4-loops-win.1 > /tmp/f22-04-t4-loops-win.txt; tail -40 /tmp/f22-04-t4-loops-win.txt; exit $rc</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; P=.planning/phases/22-supervision-durable-goals-fleet-loops; I=$P/22-04-EVIDENCE/loops/INDEX.txt; test -f "$I" || { echo "GATE INVALID: no machine-parseable live-loop index at $I"; exit 1; }; MISS=0; for K in MISSED OVERLAP RECONNECT PREEMPTION BOUNDS NON-CLAIM; do /usr/bin/grep -qE "^LOOP-LEG: linux $K [^ ]+ evidence=[^ ]+$" "$I" || { echo "no conforming linux marker with a counted outcome for the $K leg"; MISS=$((MISS+1)); }; done; for K in MISSED OVERLAP PREEMPTION; do /usr/bin/grep -qE "^LOOP-LEG: windows $K [^ ]+ evidence=[^ ]+$" "$I" || { echo "no conforming windows marker with a counted outcome for the $K leg"; MISS=$((MISS+1)); }; done; test "$(/usr/bin/grep -cE '^LOOP-LEG: (linux|windows) MISSED catchup=1 evidence=[^ ]+$' "$I")" -eq 2 || { echo "both suspend-and-resume legs must record catchup=1; any other count is the catch-up storm the requirement forbids"; MISS=$((MISS+1)); }; BAD=0; for EV in $(/usr/bin/sed -n 's/^LOOP-LEG:.*evidence=\(.*\)$/\1/p' "$I"); do test -s "$P/$EV" || { echo "a loop leg names evidence=$EV which the gate cannot stat"; BAD=$((BAD+1)); }; done; echo "loop_legs_missing=$MISS unresolvable_evidence=$BAD"; test "$MISS" -eq 0 -a "$BAD" -eq 0</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
  </verify>
  <done>A real loop ran in a real process with externally countable iterations. Suspending past five intervals and resuming produced at most one catch-up entry, measured from the iteration file, on both platforms. No iteration ever began while another was open, recorded by timestamp. A host disconnected and reconnected with its exact cursor and received a contiguous non-replaying stream, and a deliberately stale cursor was refused as stale rather than malformed. Preemption stopped a live iteration and left cumulative limits reflecting real work, and a resumed loop carried its limits over. Every named bound was driven to its edge with the stopping reason recorded each time. Ending the session left no durable trigger, and what was checked is recorded rather than only concluded.</done>
</task>

<task type="auto">
  <name>Task 5 (CROSS-AUDITED REVIEW): Judge the captured TUI screens and the three-surface renderings on a four-way panel against this plan's own criteria</name>
  <files>.planning/phases/22-supervision-durable-goals-fleet-loops/22-04-SURFACE-PARITY.md, .planning/phases/22-supervision-durable-goals-fleet-loops/22-04-EVIDENCE/review/</files>
  <decision>Do the captured artifacts satisfy the five review criteria this plan states, and is the evidence therefore accepted?</decision>
  <context>
Everything under review is ALREADY CAPTURED by Tasks 3 and 4 under `22-04-EVIDENCE/`. This task runs nothing new against the product; it judges what the product already produced. It exists because Success Criterion 1 requires CLI, TUI and host-protocol to observe IDENTICAL state, so the artifact under review is a three-way comparison — not a screenshot to admire — and because a rendering can carry the right data and still be unusable.

WHAT IS RESERVED AND WHAT IS NOT. Nothing here is reserved. Accepting evidence against stated criteria is a judgment about artifacts this repository already owns, and it is made here, on the evidence, in this session. Escalation exists for a genuine deadlock, and for the case where a second capture still fails the criteria after the plan's bounded repair attempt.

WHAT THE MACHINE CHECKS AND WHAT THE PANEL CHECKS, KEPT SEPARATE ON PURPOSE. Three of the five criteria are mechanically decidable and are therefore MEASURED here, not opined on: whether a screen leaked escape sequences, whether an identifier was truncated, whether the rows are ragged, whether the three renderings actually name one Goal, and whether the catch-up count is one. The panel's job is the RESIDUE — whether the screen is genuinely readable, whether the three surfaces describe the same thing in the SAME TERMS rather than merely consistently, whether something important is visible on one surface and missing from the others, whether each bound stopped the loop for its own reason, and whether the Windows TUI outcome is stated plainly or glossed. Putting a machine-checkable question to a panel wastes the panel; putting a judgment question to a regex fabricates a verdict.
  </context>
  <options>
    <option id="accept">
      <name>Accept the captured evidence — all five criteria are met</name>
      <pros>The renderings are legible and correct, the three surfaces describe one Goal in the same terms, the catch-up count is one on both platforms, every bound stopped the loop for its own reason, and the Windows TUI outcome is stated plainly</pros>
      <cons>Nothing further is repaired, so any defect the panel missed ships with the phase</cons>
    </option>
    <option id="accept-with-recorded-gap">
      <name>Accept with a named gap — the criteria are met except for a specific, stated shortfall that is recorded as an open item rather than repaired here</name>
      <pros>Honest about a real limit, most obviously the bounded-attempt Windows TUI leg, which the plan already provides for as termination state 2; keeps the gap visible in the record instead of absorbed into a summary</pros>
      <cons>Success Criterion 1 is closed with an asterisk, and an asterisk that is not carried into the SUMMARY becomes invisible by the next phase</cons>
    </option>
    <option id="repair-required">
      <name>Repair required — a named rendering or capture is wrong and must be fixed and re-captured</name>
      <pros>Refuses to accept a surface that renders the right data illegibly, which is exactly the defect class that survives a green test suite</pros>
      <cons>Costs one bounded repair-and-recapture cycle; a second failure after it is a genuine escalation rather than a third attempt</cons>
    </option>
  </options>
  <action>THE HALT IS GONE AND WHAT REPLACES IT IS HEAVIER, NOT LIGHTER. Do not hand Sean a checklist and wait. Run the six steps below, reach a verdict, record it, and finish the plan in the same session. The five review criteria are exactly the five this task already frames and are NOT re-scoped; what changes is that they are measured where measurable and adjudicated where not.

Everything this task writes lands under `.planning/phases/22-supervision-durable-goals-fleet-loops/22-04-EVIDENCE/review/`, referred to below as the review directory. The captures under review stay exactly as Tasks 3 and 4 wrote them, and they stay COMMITTED as evidence: this task never edits, trims, prettifies or regenerates a capture in order to make it pass. A capture altered by its reviewer is not evidence.

STEP 1 — MEASURE THE THREE MECHANICALLY DECIDABLE CRITERIA. Write `render-measurements.txt` in the review directory. For every retained TUI screen dump from Task 3, and for the CLI rendering and host stream of the same Goal, record one `RENDER:` line per capture carrying the capture name, `escape_seqs=` the count of raw terminal control sequences that survived into the parsed text (a parsed screen that still contains them is a garbled screen), `goal_id_full=yes` or `no` recording whether the Goal identifier appears in full rather than truncated or ellipsised, `ragged_rows=` the number of rows whose width differs from the screen width, and `mentions_goal=yes` or `no`. Then record one `THREE-WAY:` line naming the single Goal identifier and confirming it appears in all three renderings, and one `CATCHUP:` line per platform carrying the counted catch-up entries from Task 4's missed-interval leg. These are facts, and recording them here means the panel spends its judgment on what a machine cannot settle.

STEP 2 — RE-RUN THE COMPARISON THE CAPTURES WERE SUPPOSED TO PASS. Task 3 committed the assertion side as a test that takes the three captured renderings from paths given to it. Run it now, against the retained captures, and put its output in the review directory as `parity-recheck.txt`. This is not ceremony: it proves the artifacts under review are the same artifacts that passed, rather than a set that drifted between capture and review. If it fails here, that is the finding and the panel judges a failing comparison rather than a passing one.

STEP 3 — BUILD THE ONE EVIDENCE BUNDLE. Write `panel-prompt.txt` in the review directory containing, in this order: the question exactly as this task's `decision` element states it; all three options with their ids, names, pros and cons copied VERBATIM from this task's `options` element, with nothing added, removed, softened or re-scoped, in a ROTATED order; the five review criteria stated exactly as this plan states them; the full text of every retained TUI screen dump; the full CLI rendering and the full host event stream of the SAME Goal; the missed-interval iteration-file excerpts for both platforms; the bounds table with the stopping reason recorded for each bound; the Windows TUI outcome as recorded, whether a screen or a stated reason; and the whole of `render-measurements.txt` and `parity-recheck.txt`. Every panel member receives this file and nothing else. Instruct each member to answer per criterion and to end its reply with one line reading `PANEL_POSITION=` followed by exactly one of the three option ids, plus, whenever it is not `accept`, one line reading `PANEL_FINDING=` followed by the specific capture or rendering it says is wrong. A verdict of repair-required with no named artifact is not actionable and the gate refuses it.

STEP 4 — PUT IT TO ALL FOUR PANEL MEMBERS AND CAPTURE THEM VERBATIM. Each capture file's FIRST line is `PANEL-CMD: ` followed by the exact command that produced it, referencing `panel-prompt.txt`; everything after line 1 is the response byte for byte, never a paraphrase.
  - `codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check "$(cat panel-prompt.txt)" &lt; /dev/null` captured to `panel-codex.txt`. THE STDIN REDIRECT IS NOT OPTIONAL: measured at planning time, without it this command blocks reading additional input from stdin and is still blocked when a four-minute timeout kills it, dropping the vote for a reason unrelated to the question. Measured also: codex REPEATS its final block, so extraction takes the LAST position line.
  - `gemini -p "$(cat panel-prompt.txt)" -m gemini-3.1-pro-preview -o text --skip-trust` captured to `panel-gemini.txt`, stderr to a sidecar rather than merged. THE SKIP-TRUST FLAG IS NOT OPTIONAL: measured at planning time, without it this command exits fifty-five with ZERO bytes on stdout and a refusal about an untrusted directory. Measured also: it writes hundreds of lines of registry noise and an API-key-selection notice to stderr; that is why stderr goes to a sidecar, and the notice is a notice, not a failure, and is never grounds for recording the member NOT-RUN.
  - `/Users/seandonahoe/.kimi-code/bin/kimi -p "$(cat panel-prompt.txt)" --output-format text` captured to `panel-kimi.txt`, stderr to a sidecar. The ABSOLUTE path is required. Measured also, and this is the trap that quietly turns a four-way panel into a three-way one: this member BULLET-PREFIXES and INDENTS its output, so an anchored position regex matches it zero times. Extraction here is UNANCHORED.
  - Your own adversarial pass captured to `panel-claude-adversarial.txt`, first line in the same form. Adversarial here means you look for the defect the other three would excuse: read the screens for something visible on one surface and absent from the others, and read the bounds table for a bound that stopped the loop for a NEIGHBOURING bound's reason. Engage at least two option ids by name.
Before relying on any of those three extraction rules, RE-MEASURE the member's real behavior yourself. If an external CLI genuinely fails to answer, record it NOT-RUN with its exact stderr and command.

STEP 5 — REACH THE VERDICT, BOUND BY THE MEASUREMENTS. Count the four recorded positions and commit to the majority, or to a minority position carrying stronger evidence with a measurement naming it. Record the basis as exactly one of `majority`, `plurality-with-livetest` or `minority-stronger-evidence`, and expect the gate to check that arithmetic. TWO BINDINGS ARE ENFORCED BY GATE RATHER THAN BY INTENTION, and both exist because a review is the easiest place in this phase to wave something through. First, `accept` is unavailable when Step 1's own measurements contradict it: a screen with a surviving escape sequence, a truncated Goal identifier, a rendering that does not mention the Goal all three observed, or a catch-up count other than one on either platform each make `accept` impossible regardless of what any member argued, because the criterion those measurements test is not a matter of opinion. Second, `accept-with-recorded-gap` and `repair-required` each require a named artifact: the ledger must carry the specific capture or rendering at issue, and the gate refuses a verdict that names none. On `repair-required`, DO NOT STOP — repair the named artifact, RE-CAPTURE it through the same live exercise that produced it, and put the same bundle to the same four members ONCE more. That is the plan's bounded repair cycle. If the second panel still returns `repair-required`, that is a genuine escalation: record it carrying the split and the named artifact, and stop.

STEP 6 — PRESERVE THE DISSENT, WRITE THE LEDGER, AND WRITE THE RECORD. `panel-dissent.txt` carries, for every option that lost, the strongest argument any member made FOR it and the specific reason it did not carry, plus every argument made against the committed verdict, plus every per-criterion objection whether or not it changed the outcome. It must name at least two option ids. `review-ledger.txt` carries the machine-parseable verdicts: one `F22-REVIEW-PANEL-CODEX:`, `F22-REVIEW-PANEL-GEMINI:`, `F22-REVIEW-PANEL-KIMI:` and `F22-REVIEW-PANEL-CLAUDE-ADVERSARIAL:` line, each RECOMMENDS-ing one option id and naming its capture; one `F22-REVIEW-MEASURE-RENDER:` and one `F22-REVIEW-MEASURE-PARITY-RECHECK:` line, each OK or NOT-RUN with a reason, naming its capture; one `F22-REVIEW-CRITERION-&lt;n&gt;:` line for each of the five criteria carrying MET, GAP or FAILED and the artifact it was judged against; one `F22-REVIEW-ROUND:` line reading 1 or 2; and exactly ONE `F22-REVIEW-VERDICT:` line carrying the committed option, its basis and its `artifact=` where the option requires one, or the escalation with its split, naming the dissent and rationale files in both cases. Then record in `22-04-SURFACE-PARITY.md` the human-readable form: the five criteria with their per-criterion verdicts, the four verbatim responses, the measurements, the committed verdict, the basis, the named artifact if any, and the dissent. The captures under review stay committed exactly as they were produced.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; D=.planning/phases/22-supervision-durable-goals-fleet-loops/22-04-EVIDENCE/review; P="$D/panel-prompt.txt"; test -f "$P" || { echo "GATE INVALID: no evidence bundle at $P, so there is no panel to judge"; exit 1; }; test "$(/usr/bin/grep -c . "$P")" -ge 25 || { echo "the bundle is too short to carry the question, three options, five criteria, three renderings, two iteration excerpts and the bounds table"; exit 1; }; BAD=0; for O in accept accept-with-recorded-gap repair-required; do /usr/bin/grep -qF "$O" "$P" || { echo "the bundle put to the panel dropped an option this task frames: $O"; BAD=$((BAD+1)); }; done; for K in render-measurements parity-recheck; do /usr/bin/grep -qF "$K" "$P" || { echo "the bundle does not carry the $K measurement, so the panel was asked to opine on questions a machine had already settled"; BAD=$((BAD+1)); }; done; echo "bundle_problems=$BAD"; test "$BAD" -eq 0</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; D=.planning/phases/22-supervision-durable-goals-fleet-loops/22-04-EVIDENCE/review; L="$D/review-ledger.txt"; test -f "$L" || { echo "GATE INVALID: no review ledger at $L"; exit 1; }; OPT='accept|accept-with-recorded-gap|repair-required'; BAD=0; for M in CODEX GEMINI KIMI CLAUDE-ADVERSARIAL; do LN=$(/usr/bin/grep -E "^F22-REVIEW-PANEL-$M: RECOMMENDS ($OPT) evidence=[^ ]+$" "$L"); if test -z "$LN"; then echo "no machine-parseable position for panel member $M"; BAD=$((BAD+1)); continue; fi; EV=$(printf '%s' "$LN" | /usr/bin/sed -e 's/.*evidence=//'); PICK=$(printf '%s' "$LN" | /usr/bin/sed -e 's/.*RECOMMENDS //' -e 's/ .*//'); if test ! -f "$D/$EV"; then echo "$M names a capture the gate cannot stat: $EV"; BAD=$((BAD+1)); continue; fi; test "$(/usr/bin/tail -n +2 "$D/$EV" | /usr/bin/wc -c)" -ge 300 || { echo "the capture for $M is too small below its PANEL-CMD line to be a per-criterion review"; BAD=$((BAD+1)); }; /usr/bin/sed -n '1p' "$D/$EV" | /usr/bin/grep -qF 'panel-prompt.txt' || { echo "the capture for $M does not record on its first line that it was fed the one shared bundle"; BAD=$((BAD+1)); }; LAST=$(/usr/bin/grep -o 'PANEL_POSITION=[a-z0-9-]*' "$D/$EV" | /usr/bin/tail -1 | /usr/bin/sed -e 's/PANEL_POSITION=//'); test "$LAST" = "$PICK" || { echo "$M is recorded as recommending $PICK but the LAST PANEL_POSITION line in its own transcript reads '$LAST'"; BAD=$((BAD+1)); }; test "$PICK" = accept || /usr/bin/grep -q 'PANEL_FINDING=' "$D/$EV" || { echo "$M returned $PICK but named no artifact in its own transcript, which is not an actionable finding"; BAD=$((BAD+1)); }; done; echo "panel_capture_problems=$BAD"; test "$BAD" -eq 0</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; D=.planning/phases/22-supervision-durable-goals-fleet-loops/22-04-EVIDENCE/review; L="$D/review-ledger.txt"; test -f "$L" || { echo "GATE INVALID: no review ledger at $L"; exit 1; }; BAD=0; for K in RENDER PARITY-RECHECK; do LN=$(/usr/bin/grep -E "^F22-REVIEW-MEASURE-$K: (OK|NOT-RUN) " "$L"); if test -z "$LN"; then echo "measurement $K has no machine-parseable verdict"; BAD=$((BAD+1)); continue; fi; case "$LN" in *": NOT-RUN "*) /usr/bin/grep -qE "^F22-REVIEW-MEASURE-$K: NOT-RUN .*reason=.{15,}" "$L" || { echo "$K is NOT-RUN without a reason"; BAD=$((BAD+1)); };; *) EV=$(printf '%s' "$LN" | /usr/bin/sed -e 's/.*evidence=//' -e 's/ .*//'); if test ! -f "$D/$EV"; then echo "$K names a capture the gate cannot stat: $EV"; BAD=$((BAD+1)); else test "$(/usr/bin/grep -c . "$D/$EV")" -ge 4 || { echo "the capture for $K is too small to hold a measurement: $EV"; BAD=$((BAD+1)); }; fi;; esac; done; R="$D/render-measurements.txt"; if test -f "$R"; then test "$(/usr/bin/grep -cE '^RENDER: [^ ]+ escape_seqs=[0-9]+ goal_id_full=(yes|no) ragged_rows=[0-9]+ mentions_goal=(yes|no)$' "$R")" -ge 3 || { echo "fewer than three renderings were measured, so the three-surface comparison was reviewed without its own measurements"; BAD=$((BAD+1)); }; /usr/bin/grep -qE '^THREE-WAY: goal_id=[^ ]+ present_in_all_three=(yes|no)$' "$R" || { echo "no THREE-WAY line establishes that the three surfaces observed ONE Goal"; BAD=$((BAD+1)); }; test "$(/usr/bin/grep -cE '^CATCHUP: (linux|windows) entries=[0-9]+$' "$R")" -eq 2 || { echo "the catch-up count is not recorded for both platforms"; BAD=$((BAD+1)); }; else echo "MISSING $R"; BAD=$((BAD+1)); fi; test "$(/usr/bin/grep -cE '^F22-REVIEW-CRITERION-[1-5]: (MET|GAP|FAILED) artifact=[^ ]+$' "$L")" -eq 5 || { echo "the five stated review criteria do not each carry their own recorded verdict against a named artifact"; BAD=$((BAD+1)); }; echo "measurement_problems=$BAD"; test "$BAD" -eq 0</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; D=.planning/phases/22-supervision-durable-goals-fleet-loops/22-04-EVIDENCE/review; L="$D/review-ledger.txt"; R="$D/render-measurements.txt"; test -f "$L" || { echo "GATE INVALID: no review ledger at $L"; exit 1; }; test -f "$R" || { echo "GATE INVALID: no render measurements at $R, so an accept verdict cannot be bound to anything"; exit 1; }; V=$(/usr/bin/sed -n 's/^F22-REVIEW-VERDICT: \([a-z0-9-]*\).*/\1/p' "$L" | /usr/bin/tail -1); test -n "$V" || { echo "no parseable review verdict"; exit 1; }; if test "$V" = accept; then /usr/bin/grep -qE '^RENDER: [^ ]+ escape_seqs=[1-9]' "$R" &amp;&amp; { echo "accept was committed but a measured rendering still carries surviving terminal escape sequences, which is the garbled-screen criterion failing as a fact rather than as an opinion"; exit 1; }; /usr/bin/grep -qE '^RENDER: .* goal_id_full=no ' "$R" &amp;&amp; { echo "accept was committed but a measured rendering truncates the Goal identifier"; exit 1; }; /usr/bin/grep -qE '^RENDER: .* mentions_goal=no$' "$R" &amp;&amp; { echo "accept was committed but a measured rendering does not mention the Goal the other surfaces observed"; exit 1; }; /usr/bin/grep -qE '^THREE-WAY: goal_id=[^ ]+ present_in_all_three=yes$' "$R" || { echo "accept was committed but no measurement establishes that all three surfaces observed ONE Goal"; exit 1; }; test "$(/usr/bin/grep -cE '^CATCHUP: (linux|windows) entries=1$' "$R")" -eq 2 || { echo "accept was committed but the counted catch-up entries are not one on both platforms, which is the catch-up storm the requirement forbids"; exit 1; }; /usr/bin/grep -qE '^F22-REVIEW-CRITERION-[1-5]: (GAP|FAILED) ' "$L" &amp;&amp; { echo "accept was committed while a per-criterion verdict records a GAP or a FAILURE"; exit 1; }; else /usr/bin/grep -qE "^F22-REVIEW-VERDICT: $V .*artifact=[^ ]+" "$L" || { echo "$V was committed but names no specific capture or rendering, which is not an actionable verdict"; exit 1; }; fi; RD=$(/usr/bin/sed -n 's/^F22-REVIEW-ROUND: \([12]\)$/\1/p' "$L" | /usr/bin/tail -1); test -n "$RD" || { echo "no review round is recorded, so a bounded repair cycle cannot be distinguished from an unbounded one"; exit 1; }; if test "$V" = repair-required -a "$RD" = 2; then /usr/bin/grep -qE '^F22-REVIEW-ESCALATION: SEAN artifact=[^ ]+ split=[0-9-]+ evidence=[^ ]+$' "$L" || { echo "the second round still returned repair-required, which is a genuine escalation, but none was recorded carrying the artifact and the split"; exit 1; }; fi; echo "verdict=$V round=$RD — the verdict survives the measurements it cannot override"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; D=.planning/phases/22-supervision-durable-goals-fleet-loops/22-04-EVIDENCE/review; L="$D/review-ledger.txt"; test -f "$L" || { echo "GATE INVALID: no review ledger at $L"; exit 1; }; test "$(/usr/bin/grep -cE '^F22-REVIEW-VERDICT: ' "$L")" -eq 1 || { echo "expected exactly one F22-REVIEW-VERDICT line"; exit 1; }; LN=$(/usr/bin/grep -E '^F22-REVIEW-VERDICT: (accept|accept-with-recorded-gap|repair-required) basis=(majority|plurality-with-livetest|minority-stronger-evidence)( artifact=[^ ]+)? dissent=[^ ]+ rationale=[^ ]+$' "$L"); test -n "$LN" || { echo "the F22-REVIEW-VERDICT line does not conform"; exit 1; }; DIS=$(printf '%s' "$LN" | /usr/bin/sed -e 's/.*dissent=//' -e 's/ .*//'); test -f "$D/$DIS" || { echo "the verdict names a dissent file the gate cannot stat: $DIS"; exit 1; }; test "$(/usr/bin/grep -c . "$D/$DIS")" -ge 4 || { echo "the dissent record is too short to hold the losing arguments and the per-criterion objections"; exit 1; }; DN=0; for O in accept accept-with-recorded-gap repair-required; do /usr/bin/grep -qF "$O" "$D/$DIS" &amp;&amp; DN=$((DN+1)); done; test "$DN" -ge 2 || { echo "the dissent record names fewer than two options, so the losing arguments were deleted"; exit 1; }; CH=$(printf '%s' "$LN" | /usr/bin/sed -e 's/^F22-REVIEW-VERDICT: //' -e 's/ .*//'); BA=$(printf '%s' "$LN" | /usr/bin/sed -e 's/.*basis=//' -e 's/ .*//'); N=$(/usr/bin/grep -cE "^F22-REVIEW-PANEL-[A-Z-]+: RECOMMENDS $CH " "$L"); case "$BA" in majority) test "$N" -ge 3 || { echo "the basis is majority but only $N of four panel members recommended $CH"; exit 1; };; plurality-with-livetest) test "$N" -eq 2 || { echo "the basis is plurality but $N of four recommended $CH"; exit 1; };; minority-stronger-evidence) test "$N" -le 1 || { echo "the basis is minority but $N of four recommended $CH, which is not a minority"; exit 1; };; esac; test "$BA" = majority || /usr/bin/grep -qE "^F22-REVIEW-MEASURE-[A-Z-]+: OK .*option=$CH" "$L" || { echo "$BA overrides the panel count but no measurement naming $CH backs it"; exit 1; }; echo "exactly one verdict is recorded, its basis survives arithmetic against the four captured positions, and the dissent names at least two options"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; P=.planning/phases/22-supervision-durable-goals-fleet-loops; D=$P/22-04-EVIDENCE/review; test -s "$D/review-ledger.txt" || { echo "GATE INVALID: no review ledger"; exit 1; }; echo "CAPTURE-INTEGRITY GATE: the artifacts under review must still be the artifacts Tasks 3 and 4 produced; a capture edited by its own reviewer is not evidence"; E=$P/22-04-EVIDENCE/parity; BAD=0; for F in cli-inspect.txt tui-screen.txt host-stream.jsonl; do test -s "$E/$F" || { echo "the review ran but the Task 3 capture $F is missing or empty"; BAD=$((BAD+1)); }; done; test -s "$P/22-04-EVIDENCE/loops/INDEX.txt" || { echo "the review ran but Task 4's live-loop index is missing"; BAD=$((BAD+1)); }; test -z "$(/usr/bin/git ls-files --others --exclude-standard -- "$E")" || { echo "Task 3's parity captures are untracked, so they were not committed as evidence"; BAD=$((BAD+1)); }; echo "capture_integrity_problems=$BAD"; test "$BAD" -eq 0</automated>
  </verify>
  <done>The three mechanically decidable review criteria were MEASURED rather than opined on — surviving escape sequences, identifier truncation and ragged rows counted per rendering, the single Goal identifier confirmed present in all three, and the catch-up entries counted for both platforms — and Task 3's committed comparison was re-run against the retained captures to prove they had not drifted between capture and review. The five criteria this plan states each carry their own recorded verdict against a named artifact. The question and the three options went to all four panel members as ONE shared bundle carrying every retained screen, the CLI rendering, the host stream, both iteration excerpts, the bounds table, the Windows TUI outcome and both measurements; each response is captured verbatim to its own file whose first line names that bundle and whose LAST position line matches the pick recorded for it, and any non-accept position names a specific artifact in its own transcript. Exactly one verdict is recorded; its basis survives arithmetic against the four captured positions; `accept` was structurally unavailable against any measurement contradicting it, enforced by gate rather than by intention. A `repair-required` verdict drove one bounded repair-and-recapture and a second panel round; a second `repair-required` was escalated carrying the named artifact and the split rather than attempted a third time. The dissent survives naming at least two options and preserving every per-criterion objection. Every capture under review is committed, unedited, exactly as the live exercises produced it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host client → Goal command surface | An untrusted host issues lifecycle commands; payloads must be closed and must not carry unhonored authority-bearing extensions |
| host client → event cursor | A host presents a cursor for resynchronization; a forged or stale cursor must not yield state it should not see or replay acknowledged effects |
| TUI keystroke → Goal command | Terminal input reaches a lifecycle command; a rendered surface must not be able to invoke an operation the typed command set does not expose |
| session loop → durable trigger | A session-local loop must be structurally unable to acquire persistence before the Phase 24 runtime closes that boundary |
| contract generator → manifest | The manifest is authoritative for a downstream consumer; a hand edit that disagrees with the generator is a contract that lies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-21 | Spoofing | host Goal lifecycle command | high | mitigate | Closed payloads with unknown-field refusal following the existing versioned recovery idiom; Test 3 asserts the refusal so a host cannot believe an ignored extension was honored |
| T-22-22 | Tampering | stale or forged event cursor | high | mitigate | The existing recovery cursor (journal sequence plus digest) is reused rather than redefined; stale, malformed and unsupported-version rejections stay distinguishable (Test 4) and the stale case is exercised live in Task 4 |
| T-22-23 | Elevation of Privilege | session loop acquiring a durable trigger | critical | mitigate | No code path from the loop policy to the cron store exists; Test 10 fails if one appears; the live non-claim leg confirms nothing survives the session |
| T-22-24 | Tampering | hand-edited desktop manifest | high | mitigate | The manifest is generated only; regenerating twice must be byte-identical and the verify step diffs it after a regeneration |
| T-22-25 | Denial of Service | catch-up burst after a suspend | high | mitigate | At most one catch-up iteration regardless of gap; asserted in Test 8 and measured live on both platforms by counting an externally observable iteration file |
| T-22-26 | Denial of Service | overlapping loop iterations | high | mitigate | Idle-only dispatch with a structural refusal to start while an iteration is open; measured live by timestamp rather than asserted |
| T-22-27 | Information Disclosure | Goal log tail rendered across three surfaces | medium | mitigate | All three render the same typed response, which carries the redaction already applied at the source; no surface computes or supplements a field (Test 2) |
| T-22-28 | Repudiation | duplicate acknowledgement double-counted | medium | mitigate | Acknowledgement is idempotent and reports that it changed nothing (Test 5); an adversarial fixture covers the repeat |
| T-22-SC | Tampering | crates.io installs | high | mitigate | This plan adds no dependency; the pseudo-terminal and screen-grid crates are already in the lockfile at recorded pins and are reused at those versions. Any genuinely new package is a blocking human checkpoint with a legitimacy audit; unaudited packages are refused |
</threat_model>

<verification>
- All ten operations exist as exactly one typed Core command each, with three adapters that cannot introduce state the command did not return.
- The protocol crate still declares no dependency on the agent crate, and the Goal vocabulary is defined once, in the types crate.
- The desktop manifest is generator-produced and regenerating twice is byte-identical; the minor and generator versions were bumped exactly once.
- Every terminal category from the 22-01 taxonomy has a D2 fixture, including the unpriced and partially-checked ones.
- The full Linux aggregate is green at the delivered commit; the Windows suites are green with clippy clean there.
- One real Goal was observed live from all three surfaces with all three raw renderings retained, and the comparison was proven non-vacuous by a control.
- The live loop exercise measured at most one catch-up after a five-interval suspend, no overlap, a contiguous non-replaying reconnect, a stale-cursor refusal of the correct kind, a real preemption, every bound driven to its own edge, and nothing surviving the session.
- The Windows TUI leg is either proven or its bounded-attempt failure is recorded with its exact cause.
- No existing test or fixture was modified, renamed, re-gated or deleted; no crates.io dependency was added.
</verification>

<success_criteria>
Phase 22 Success Criterion 1 is closed: CLI, TUI and host-protocol paths observe and control identical Goal, child, task, wait, log, cursor and terminal state — proven by driving one real Goal through the real product and reading it back from all three at once — and the canonical serialized producer fixtures for the later D2 consumer gate exist in the established corpus under one additive minor bump. Success Criterion 4 is closed: session-local fixed, dynamic, event-driven and manual loops stay bounded across reconnect, preemption, missed intervals and resume, measured on a real suspended-and-resumed process on two platforms, and persistent scheduling remains an explicitly enforced non-claim. F22-01, F22-05 and F22-07 are complete.
</success_criteria>

<output>
Create `.planning/phases/22-supervision-durable-goals-fleet-loops/22-04-SUMMARY.md` when done, recording which of the four termination states was reached, the exact invocations and raw renderings for the three-surface exercise, the counted outcomes for every live loop leg on both platforms, the Windows TUI outcome stated plainly as proven or gapped with its cause, the contract minor and generator versions before and after, and any finding recorded rather than repaired.
</output>


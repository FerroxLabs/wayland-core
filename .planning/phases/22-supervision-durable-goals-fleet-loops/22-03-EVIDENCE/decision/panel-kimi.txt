PANEL-CMD: /Users/seandonahoe/.kimi-code/bin/kimi -p "$(cat panel-prompt.txt)" --output-format text
• The question has two halves and the evidence makes clear they are separable: revocation (who decides a claim is dead) and fencing (what refuses the late write). Heartbeat-liveness and os-process-liveness both answer only the first half. The prompt states it directly and the option windows confirm it numerically: both have `refuses_late_write=nothing`, which means their `dup_window_ms` figures (3000, 1000) measure duplicate *execution*, not duplicate *effect*. The requirement as stated is about surviving kill/restart/reassignment without duplicate execution **or lost completion** — and a revocation-only scheme cannot meet it, because the Windows kill leg is unmeasured, so the one platform where "the kill reaps the tree" was actually observed (Linux) is the only platform where a revocation-only scheme might suffice. Betting the ledger's correctness on descendants always dying is a Linux-only bet, and Measurement 2 (pid+start-time binding sound on both hosts) fixes os-process-liveness's soundness problem without giving it a fencing half. Heartbeat-liveness additionally has the suspend-storm failure mode, which Measurement 3 makes concrete: 12/12 beats missed through a 12-second OS stop, so any threshold below 12 mass-reassigns a healthy-but-suspended fleet. The same measurement shows why the lease half of lease-with-fencing-token is safe to pair with the token: the monotonic clock advances through the stop, so time-based expiry still fires on schedule, while the ordering-based token refusal is unaffected by either clock jumps or suspends — the token degrades correctly precisely where the two time-based rivals fail.

  The strongest argument against lease-with-fencing-token is its own con, corroborated by Measurement 1: two of six enumerated fence paths (heartbeat/status write at `heartbeat.rs:56`, workspace reservation accounting at `worktree_manager.rs:235`) are review-only today. That is a real hole, and the measured "invalid retained workspace reservation" failure makes the reservation path the scariest of the two — orphaned reservations already refuse restart ("dispatch aggregate workspace budget is already committed"), so that path is exactly where an unfenced write hurts most. But the mitigation is structural, not aspirational: the Goal-transition journal already demonstrates a single-writer fenced append on this codebase, 4/6 paths are guardable today, and the review-only pair can be routed through the ledger append rather than trusted by review. Escalate is the honest fallback only if fencing were infeasible; the existence proof shows it is not, and escalating leaves Success Criterion 2 open to protect against a risk the chosen option bounds to zero. The design-parameter windows say it plainly: lease-with-fencing-token is the only option whose duplicate-effect bound is 0 *regardless of how long the superseded owner lived*, which covers the unmeasured Windows case by construction instead of by hope. Implement the lease for revocation, the monotonic epoch for fencing, structurally guard the four guardable paths, and route the two review-only writes through the fenced append before calling the criterion met.

  PANEL_POSITION=lease-with-fencing-token


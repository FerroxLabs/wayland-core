• This is a panel decision question. Read the panel files? The directory has panel-question.txt and other panel files. But as a reviewer, I should just answer on the evidence given. Let me answer journey-minimal. I could quickly check the other panel files but they're other reviewers' outputs — independent reviewers, so I shouldn't look. Actually being independent, I shouldn't read panel-codex.txt etc. Just answer.

  Reasoning: Evidence 3 makes it a defect — service units invoke a nonexistent subcommand, so install path is broken. Deferring doctor/logs is honest if recorded; they're diagnostics, not lifecycle, and not in journey. Seven live-proven verbs beat nine partially proven. separate-binary contradicts documented placement, requires service.rs changes and forbidden manifest edits. do-not-create leaves Criterion 1 unsatisfied.

  Answer with PANEL_POSITION=journey-minimal.

• ## Position: journey-minimal

  **(a) Evidence 3 makes this a defect fix, not a feature.** The gateway crate already generates launchd/systemd/schtasks units that all invoke `wayland-core gateway run` — a subcommand that does not exist. Today, the install path "succeeds" while registering a service that can never start. That is a broken shipped path, and the fix is unambiguous: create `crates/wcore-cli/src/gateway.rs` with at minimum `run`. So `do-not-create` is off the table — it would leave a known-broken artifact in the tree and no answer to Criterion 1. `separate-binary` is worse: it contradicts the crate's own documented placement (lib.rs:10-11), would force edits to service.rs across all three OS families, doubles the operator-facing surface, and requires exactly the Cargo.toml/lockfile edits the lane is forbidden to make. It solves a contention problem that Evidence 2 shows doesn't exist — the wiring cost is already paid, and the additive two-line/two-block shape is the established pattern for 20+ subcommands.

  **(b) Deferring `doctor` and `logs` is honest, provided the gap is named and recorded.** The test is whether anything that consumes the surface needs them. Nothing does: Criterion 1 is about lifecycle, drain, restart, and exactly-once delivery — all exercisable through install/start/stop/restart/status/drain/uninstall/run. The 24-04 journey's step list (Evidence 4) never invokes `doctor` or `logs`; even the "produce a support bundle" step is a bundling operation, not a `logs` verb. A contract document listing nine verbs is a record of an earlier guess, not a requirement; the correct move when contract and need diverge is to update the contract with an explicit deferred-items note, not to build unexercised code to match stale text. Unrecorded scope-shaving would be dishonest; a recorded deferral is just accurate planning.

  **(c) Seven live-proven verbs beat nine partially-proven ones, decisively.** The phase's terminal gate is a *live* journey on three platforms, and the lane's budget is finite and shared with 24-03 and 24-04. Every verb in the minimal set is a verb the journey exercises, which means 100% of what ships gets proved end-to-end — including the hard-kill/platform-restart recovery that is the riskiest claim in the whole phase. Budget spent on `doctor` and `logs` is budget taken directly from the kill/recover/exactly-once evidence that the phase actually stands or falls on. Shipping seven proven verbs plus a recorded gap is a stronger deliverable than nine verbs where two are asserted-but-untested.

  PANEL_POSITION=journey-minimal

To resume this session: kimi -r session_8fec787a-1173-499a-8f7c-bdbb755a1ee3

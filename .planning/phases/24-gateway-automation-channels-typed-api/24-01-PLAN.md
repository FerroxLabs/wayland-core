---
phase: 24-gateway-automation-channels-typed-api
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/wcore-gateway/Cargo.toml
  - crates/wcore-gateway/src/lib.rs
  - crates/wcore-gateway/src/lifecycle.rs
  - crates/wcore-gateway/src/service/mod.rs
  - crates/wcore-gateway/src/service/launchd.rs
  - crates/wcore-gateway/src/service/systemd.rs
  - crates/wcore-gateway/src/service/windows.rs
  - crates/wcore-gateway/src/pidlock.rs
  - crates/wcore-gateway/src/drain.rs
  - crates/wcore-gateway/src/ledger.rs
  - crates/wcore-gateway/tests/lifecycle_contract.rs
  - crates/wcore-gateway/tests/ledger_exactly_once.rs
  - crates/wcore-gateway/tests/pidlock_hostile.rs
  - crates/wcore-cli/src/gateway.rs
  - crates/wcore-cli/src/lib.rs
  - crates/wcore-cli/src/main.rs
  - crates/wcore-cli/Cargo.toml
  - crates/wcore-cli/tests/gateway_cli_surface.rs
  - crates/wcore-eval-scenarios/tests/pty_gateway_surface.rs
  - crates/wcore-config/src/config.rs
  - crates/wcore-protocol/src/events.rs
  - crates/wcore-protocol/src/commands.rs
  - crates/wcore-protocol/src/contract/generate.rs
  - crates/wcore-protocol/contracts/desktop/v1/manifest.json
  - templates/gateway/launchd.plist
  - templates/gateway/systemd.service
  - templates/gateway/windows-service.md
  - .config/nextest.toml
  - .planning/phases/24-gateway-automation-channels-typed-api/24-01-GATEWAY-CONTRACT.md
autonomous: false
requirements:
  - F24-01
must_haves:
  truths:
    - "AN OPERATOR CAN INSTALL, START, INSPECT, DRAIN, RESTART, UPGRADE AND ROLL BACK ONE PERSISTENT CORE RUNTIME FROM THE SHIPPED BINARY, and every one of those verbs is observable from the product's own output rather than from a test harness."
    - "DRAIN IS A REAL STATE, NOT A SLEEP. After drain is requested the gateway admits no new work, reports how many turns are still active, finishes or explicitly abandons them within the stated budget, and exits with a status that distinguishes clean drain from forced termination."
    - "DELIVERY IS EXACTLY ONCE ACROSS A LIFECYCLE EVENT. A delivery accepted before drain is delivered exactly once after restart, upgrade or rollback — never lost, never duplicated. This is the criterion's whole content and it is measured by counting, not asserted by inspection."
    - "PROFILE ISOLATION IS PRESERVED EXACTLY AS THE SUPERVISOR ALREADY DEFINES IT. `crates/wcore-cli/src/profile_router.rs` establishes one child equals one profile equals one identity, cleared child environment, per-child key, fail-closed on an unknown profile, bounded concurrency, signal-safe reaping. The gateway hosts that supervisor; it does not invent a second topology and it does not multiplex profiles into one address space."
    - "THE EXISTING DAEMON MACHINERY IS THE STARTING POINT, NOT A COMPETITOR. `crates/wcore-cli/src/cron.rs` already carries the pid-file, log-file, re-exec detach, liveness probe and signal shutdown. The gateway absorbs that machinery; it must not leave two independent detach implementations that can both claim the same home directory."
    - "THE WINDOWS DETACH PATH IS PREDICTED BROKEN AND MUST BE MEASURED BEFORE IT IS TRUSTED. The existing non-Unix spawn branch sets no creation flags, so the spawned process stays attached to the caller's console and process group. On Unix the sibling branch calls into a new session. A background service that dies when its console closes is not a persistent runtime, and this is exactly the class of defect the phase-20A handoff names as gated-away platform behaviour."
    - "WINDOWS HANDLE AND LOCK SEMANTICS ARE HOSTILE AND ARE ALREADY DOCUMENTED. Byte-range locks on Windows are mandatory, so a whole-file lock on the pid file breaks the crate's own readers — a one-byte sentinel is the measured remedy. A handle carrying delete access blocks a directory change into that directory. Canonicalised paths come back in verbatim form that other tooling cannot parse, so normalise at the comparison boundary on both operands, never only at storage."
    - "THIS PLAN OWNS EVERY PHASE-24 SHARED-SEAM EDIT. Protocol events and commands, the contract generator version, the regenerated desktop manifest, the configuration schema keys, the workspace member list and the lockfile are all landed once, here, in wave 1. Plans 24-02 and 24-03 consume those surfaces and do not edit them, so the two wave-2 plans can run concurrently without a deterministic conflict."
    - "A REPORTED RED IS WORTH FAR MORE THAN AN ENGINEERED GREEN. Never weaken an assertion, mark a test ignored, silence a lint, raise a timeout, or delete an inconvenient test to reach a gate. If the honest outcome is that a lifecycle verb cannot be delivered on a platform, record that with its evidence and escalate."
    - "EVERY GATE MUST HAVE A WAY TO GO RED, AND THAT WAY WAS MEASURED. Three shapes are banned because each was caught passing on failure. A remote command piped into `grep -v CLIXML`/`tail` reports the filter's status, not the command's — measured on the Windows box 2026-07-26: unpiped a failing command returns 1, piped it returns 0. A PowerShell `Set-Location` into a missing checkout does not stop the command after it, so the gate passes from the wrong directory; `cmd /c \"cd /d <dir> && ...\"` returns 1 instead. And a bare `git status --porcelain` exits zero whether or not it printed anything, so a shared-seam check written that way asserts nothing — wrap it in `test -z \"$(...)\"`. Ask of every gate written here: what makes this go red? If the answer is nothing, it is not a verification."
  artifacts:
    - path: crates/wcore-gateway/src/lifecycle.rs
      provides: "The gateway lifecycle state machine — installed, stopped, starting, running, draining, drained, failed — with the transitions each CLI verb drives and the status projection the operator reads"
    - path: crates/wcore-gateway/src/ledger.rs
      provides: "The durable outbound delivery ledger that makes delivery exactly-once across drain, restart, upgrade and rollback"
    - path: crates/wcore-gateway/src/service/mod.rs
      provides: "The native service-management abstraction with one implementation per OS family behind a single trait, so no call site branches on platform"
    - path: crates/wcore-cli/src/gateway.rs
      provides: "The operator-facing verb surface on the shipped binary: install, start, stop, restart, status, doctor, logs, drain, uninstall"
    - path: .planning/phases/24-gateway-automation-channels-typed-api/24-01-GATEWAY-CONTRACT.md
      provides: "The recorded lifecycle and delivery contract, the Windows detach measurement, the authorized Windows service mechanism, and the live delivery-count evidence"
  key_links:
    - from: crates/wcore-cli/src/gateway.rs
      to: crates/wcore-gateway/src/lifecycle.rs
      via: "every operator verb drives one declared lifecycle transition and reads back the same status projection"
      pattern: "cli-over-state-machine"
    - from: crates/wcore-gateway/src/drain.rs
      to: crates/wcore-gateway/src/ledger.rs
      via: "drain closes admission and flushes the ledger to a durable point before the process is allowed to exit"
      pattern: "drain-then-fsync"
    - from: crates/wcore-gateway/src/lifecycle.rs
      to: crates/wcore-cli/src/profile_router.rs
      via: "the gateway hosts the existing per-profile child supervisor rather than replacing it"
      pattern: "host-existing-supervisor"
    - from: crates/wcore-protocol/src/events.rs
      to: crates/wcore-protocol/contracts/desktop/v1/manifest.json
      via: "the regenerated contract manifest, checked by the repository's own contract check"
      pattern: "generated-contract"
---

<objective>
Deliver one persistent Core gateway runtime that an operator installs, starts, inspects, drains, restarts, upgrades and rolls back from the shipped binary — and prove that no delivery is lost or duplicated across any of those lifecycle events.

Purpose: Phase 24's first Success Criterion is the phase's spine. Every other criterion attaches to a runtime that is actually persistent: the automation plane needs a single long-lived scheduling owner, the channel framework needs a supervised process that survives a reconnect, and the typed client needs something to connect to that is still there after a restart. Today the pieces exist but are not one runtime — the cron daemon detaches itself in one place, the protocol server supervises profile children in another, and nothing owns drain, upgrade or delivery continuity.
Output: A `wcore-gateway` crate carrying the lifecycle state machine, native service management per OS family, a Windows-safe pid lock, drain, and a durable delivery ledger; the operator verb surface on `wayland-core`; every Phase-24 shared-seam edit landed once; and a recorded contract document holding the Windows detach measurement, the authorized Windows service mechanism, and the live delivery-count evidence from Linux and macOS.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/HANDOFF-2026-07-26-phase20-20A-complete.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@crates/wcore-cli/src/cron.rs
@crates/wcore-cli/src/profile_router.rs
@crates/wcore-cli/src/acp.rs
@crates/wcore-channels/src/dispatch/dedupe.rs
@crates/wcore-channels/src/manager.rs
@crates/wcore-cron/src/runner.rs
@templates/cron-daemon/systemd.service
@templates/cron-daemon/launchd.plist
</context>

<execution_rules>

**THE TWO AMENDED PHASE RULES — verbatim, and they bound this plan.**

- Findings at CRITICAL or HIGH must be fixed or disproved. MEDIUM and below are logged to BACKLOG and DO NOT BLOCK execution.
- Execution begins when no CRITICAL or HIGH finding is open, or after 2 review rounds, whichever comes first. A third round is NOT permitted; it escalates to Sean.

**TERMINATION CRITERION FOR THIS PLAN (hard).** This plan builds the runtime once, decides the Windows service mechanism once, and proves delivery continuity once. It terminates in exactly one of four states, and in all four it writes its SUMMARY and stops:
1. **Complete** — the lifecycle verbs work from the shipped binary, delivery is proven exactly-once across drain, restart, upgrade and rollback on Linux and macOS, and the Windows service mechanism is authorized and implemented (its native proof belongs to 24-04).
2. **Complete with a named platform gap** — every verb works on Linux and macOS, the Windows detach measurement is recorded, and one named Windows verb cannot be delivered inside this plan's declared files. Record it with severity and hand it to 24-04 rather than widening scope here.
3. **Blocked on the decision** — Sean declines every Windows service mechanism at the checkpoint. Ship the Unix families, record the open decision, and stop.
4. **Escalated** — delivery cannot be made exactly-once without changing a surface outside this plan's declared files. Record the finding with its evidence and stop.
Under no circumstances does this plan spawn additional plans or start a second build cycle.

**SCOPE BOUNDARY (hard).** This plan owns the gateway runtime, its verb surface, the delivery ledger, and every Phase-24 shared-seam edit. Scheduling behaviour belongs to 24-02. Channel adapters and the typed client belong to 24-03. Native tri-platform journeys belong to 24-04. If the work leads toward those surfaces, record the connection and stop rather than following it.

**SEAM DISCIPLINE (hard — this is why the plan is wave 1 alone).** The phase-20A handoff names the surfaces that conflict deterministically when two agents touch them concurrently: the workspace manifest and lockfile, the configuration schema file, the protocol contract generator, and the generated desktop manifest. Every Phase-24 edit to those files happens in THIS plan, in one pass, including keys and events that only 24-02 or 24-03 will read. Land them even where this plan does not consume them, and say so in the source comment for each, so the wave-2 plans are additive-only against those files.

**CRATE PLACEMENT.** The portable runtime lives in a new mid-layer crate `wcore-gateway` whose dependencies are `wcore-types`, `wcore-config`, `wcore-protocol`, `wcore-cron` and `wcore-channels`. It must NOT depend on `wcore-agent`, which is a top-layer crate — that would invert the dependency graph. The operator verb surface lives in `crates/wcore-cli/src/gateway.rs`. Verify the resulting graph with `cargo metadata` per AGENTS.md before the plan closes, and run the workspace-hack regeneration and verification targets after adding the member.

**ENVIRONMENT.**
- Linux proof: `ssh -o BatchMode=yes hetzner-dsm`, `/root/wayland`. This is the authoritative Cargo host. **`vx` and `cargo` are NOT on the non-interactive PATH there** — measured 2026-07-26: `ssh hetzner-dsm 'command -v vx'` fails while `bash -lc 'command -v vx'` returns `/root/.local/bin/vx`. Every remote command opens with `export PATH="/root/.local/bin:/root/.cargo/bin:$PATH";`. There is no standalone `just` on that host either: the recipe runner is reached as `vx just <target>`.
- Windows measurement: `ssh -o BatchMode=yes SeanD@seandesktop` (Tailscale), checkout `C:\ferrox-win`. Invocation shape: `ssh -o BatchMode=yes SeanD@seandesktop 'cmd /c "cd /d C:\ferrox-win && <cmd> 2>&1"'`. Wrap git in `cmd /c`. `cargo fmt --all` fails there with os error 206 and the justfile already skips it.
- **TWO WINDOWS GATE SHAPES ARE BANNED, BOTH MEASURED ON THE BOX 2026-07-26.** Appending `| grep -v CLIXML | grep -v "^<Objs"` (or `| tail -N`) to a gate replaces the command's exit status with the filter's: the unpiped form of a failing remote command returns 1, and the same command with those filters appended returns 0. Those filters are for READING output, never for gating. And a PowerShell `Set-Location C:\ferrox-win; cmd /c "..."` does not stop when the directory does not exist — the gate then passes from the home directory; the guarded `cmd /c "cd /d C:\ferrox-win && ..."` form returns 1 for a bad checkout path. The gate shape is therefore: redirect combined output to a run log, capture `rc=$?` on the very next line, filter the log afterwards for human reading, and end on `test $rc -eq 0`.
- macOS: this Mac. NEVER run Cargo here. Use a prebuilt artifact fetched from the Linux or Windows build hosts, or the registered ephemeral macOS runner. `cargo fmt --all -- --check` is the one Cargo command permitted here.
- Always `/usr/bin/grep` on the Mac — the ambient grep is proxied and silently drops lines. Use `-F` for literals. Same caution for `ls`.
- In `cmd`, the unquoted set-variable form appends a trailing space that Rust silently ignores. Use the quoted form or the PowerShell environment form, and prove the value took effect before trusting any measurement that depends on it.
- Fetch explicitly: `git fetch origin plan/f20-unified-audit-repair`. Both hosts' refspecs are pinned elsewhere and a fetch-all silently misses this branch.
- NO push to main, merge, PR, tag, release, or issue closure without Sean.

**AGENTS.md discipline.** Surgical diffs only; every changed line traces to this plan. No drive-by refactor of the channel manager, the cron runner, or the profile supervisor. Clippy-clean at deny-warnings, because the Windows CI job runs the lint before the tests and a lint failure means the tests never execute at all.

**Git hygiene.** Use `/usr/bin/git` on the Mac. Stage the exact paths in `files_modified`, never `-A`, never `.`. Never stage `AGENTS.md` or planning-tool churn. No co-author trailers.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land the gateway crate, the lifecycle state machine, the Windows-safe pid lock, and every Phase-24 shared-seam edit in one pass</name>
  <files>Cargo.toml, Cargo.lock, crates/wcore-gateway/Cargo.toml, crates/wcore-gateway/src/lib.rs, crates/wcore-gateway/src/lifecycle.rs, crates/wcore-gateway/src/pidlock.rs, crates/wcore-gateway/tests/lifecycle_contract.rs, crates/wcore-gateway/tests/pidlock_hostile.rs, crates/wcore-config/src/config.rs, crates/wcore-protocol/src/events.rs, crates/wcore-protocol/src/commands.rs, crates/wcore-protocol/src/contract/generate.rs, crates/wcore-protocol/contracts/desktop/v1/manifest.json, .config/nextest.toml, .planning/phases/24-gateway-automation-channels-typed-api/24-01-GATEWAY-CONTRACT.md</files>
  <read_first>crates/wcore-cli/src/cron.rs (the whole daemon section: the home-directory resolution, the pid-file staleness check, the liveness probe with its Unix and Windows arms, the re-exec detach with its two spawn branches, and the signal-driven shutdown loop — this is the machinery being absorbed), crates/wcore-cron/src/runner.rs (the tick loop, its missed-tick behaviour, and the shutdown watch channel the engine already uses), crates/wcore-protocol/src/contract/generate.rs (the generator version and contract minor constants, and how the manifest is produced), crates/wcore-protocol/contracts/desktop/v1/manifest.json (the shape the generator must reproduce byte-exactly), .planning/HANDOFF-2026-07-26-phase20-20A-complete.md section 5 (the four Windows defect classes and the measured remedy for each)</read_first>
  <behavior>
    - The lifecycle state machine accepts exactly the transitions the operator verbs drive and rejects every other transition with a named error rather than a silent no-op. Starting an already-running gateway, stopping a stopped one, and draining a stopped one each produce a distinct, testable outcome.
    - The status projection is stable and machine-readable: state, process identity, uptime, active profile, count of turns currently in flight, count of pending deliveries, and the identity of the binary the running process was launched from.
    - The pid lock survives the hostile cases the handoff names. A second gateway launch against the same home is refused while the first is alive. A pid file whose process is gone is treated as stale and reclaimed. The lock is a one-byte sentinel so the crate's own readers are not blocked by the mandatory Windows byte-range lock. A path stored in one representation and compared in another still compares equal, because normalisation happens at the comparison boundary on both operands.
    - The liveness probe is honest on both families and has a test that proves the running process reads alive and an impossible identifier reads dead — the same pair of assertions the existing probe already carries, preserved through the move.
    - Every Phase-24 protocol addition lands here: gateway lifecycle transitions, drain progress, active-turn count, and the delivery outcome record, plus the command shapes the typed client in 24-03 will issue. The contract generator's version constants are bumped once and the desktop manifest is regenerated so the repository's own contract check passes.
    - Every Phase-24 configuration key lands here in one pass, including the scheduling keys 24-02 reads and the channel and client keys 24-03 reads. Each carries a source comment naming the plan that consumes it.
    - Adding the workspace member leaves the workspace-hack verification target green.
  </behavior>
  <action>Write the failing tests first: a lifecycle contract test that drives every legal transition and asserts every illegal one is refused by name, and a hostile pid-lock test covering double-launch refusal, stale reclamation, sentinel-scoped locking that leaves readers unblocked, and representation-insensitive path comparison. Both must fail before any implementation exists.

Create the crate as a workspace member in the mid layer. Its dependency set is the shared types, configuration, protocol, scheduling and channel-runtime crates only. It must not reach the top-layer agent crate; check the resulting graph with the workspace metadata command and record the check in the contract document. Regenerate and verify the workspace-hack crate after the member is added, because the repository gates on that verification.

Move the daemon machinery out of the scheduling command module and into the crate: home-directory resolution, pid-file handling, log-file handling, the liveness probe with both platform arms, and the signal-driven shutdown. Leave the scheduling command module calling into the new crate so there is exactly one detach implementation in the workspace. Do not change what the probe or the shutdown loop mean; this is a move plus the lock hardening, not a redesign.

Harden the lock for Windows against the four documented defect classes. Take the lock on a single sentinel byte rather than the whole file, so a mandatory byte-range lock does not exclude the crate's own status reader. Do not hold a delete-bearing handle on the home directory while any code path needs to change into it. Normalise the stored and the compared path at the comparison site, both operands, rather than normalising once at storage — the simplification helper is documented as conditional and no-ops on several real inputs, so a store-side-only normalisation is a fail-open.

Land the shared-seam edits in one pass, in this order: protocol events and commands, then the generator version constants, then the regenerated manifest, then the configuration keys, then the nextest profile entry for the new crate's tests. State in each source comment which plan consumes the addition. This is the whole reason this plan runs alone in wave 1; nothing in wave 2 may need to reopen these files.

Open the contract document and record: the crate placement with its dependency-graph evidence, the lifecycle transition table, the status projection shape, and the pid-lock hardening with the defect class each measure answers. Records evidence for F24-01; marks no requirement complete.</action>
  <verify>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'export PATH="/root/.local/bin:/root/.cargo/bin:$PATH"; cd /root/wayland &amp;&amp; git fetch origin plan/f20-unified-audit-repair &amp;&amp; git checkout FETCH_HEAD -- . &amp;&amp; vx cargo nextest run -p wcore-gateway --profile ci --no-fail-fast'</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'export PATH="/root/.local/bin:/root/.cargo/bin:$PATH"; cd /root/wayland &amp;&amp; vx cargo clippy -p wcore-gateway --all-targets -- -D warnings'</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'export PATH="/root/.local/bin:/root/.cargo/bin:$PATH"; cd /root/wayland &amp;&amp; vx just desktop-contract-check'</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'export PATH="/root/.local/bin:/root/.cargo/bin:$PATH"; cd /root/wayland &amp;&amp; vx just hakari-verify'</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'export PATH="/root/.local/bin:/root/.cargo/bin:$PATH"; cd /root/wayland &amp;&amp; vx cargo metadata --format-version 1 --no-deps &gt;/dev/null &amp;&amp; vx cargo tree -p wcore-gateway -e normal --depth 1 > /tmp/f24-01-tree.txt &amp;&amp; cat /tmp/f24-01-tree.txt &amp;&amp; ! grep -q wcore-agent /tmp/f24-01-tree.txt'</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f .planning/phases/24-gateway-automation-channels-typed-api/24-01-GATEWAY-CONTRACT.md</automated>
  </verify>
  <done>The gateway crate exists as a mid-layer workspace member whose direct dependency list excludes the top-layer agent crate, proven by the dependency-tree output. The lifecycle contract test and the hostile pid-lock test both existed and failed before the implementation and now pass on Linux. Exactly one detach implementation remains in the workspace. Every Phase-24 protocol, configuration, generator and manifest edit is landed, the repository's own contract check passes, and the workspace-hack verification is green. The contract document records the crate placement evidence, the transition table, the status projection, and the pid-lock hardening against each named Windows defect class.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build drain and the durable delivery ledger so no delivery is lost or duplicated across a lifecycle event</name>
  <files>crates/wcore-gateway/src/drain.rs, crates/wcore-gateway/src/ledger.rs, crates/wcore-gateway/src/lifecycle.rs, crates/wcore-gateway/tests/ledger_exactly_once.rs, .planning/phases/24-gateway-automation-channels-typed-api/24-01-GATEWAY-CONTRACT.md</files>
  <read_first>crates/wcore-channels/src/dispatch/dedupe.rs (the inbound duplicate-suppression cache — its key identity, its expiry anchored at first sight rather than sliding on a hit, and its eviction bound; the outbound ledger is the durable twin of this and should not contradict its vocabulary), crates/wcore-channels/src/outgoing.rs (the outbound message shape and its strict unknown-field rejection, which makes any added field a wire-compatibility question), crates/wcore-channels/src/event.rs (the send receipt shape that carries the platform-assigned identity used for correlation), crates/wcore-cron/src/job.rs (the fire-outcome vocabulary, including the outcome that records a fire with no live dispatcher, because that is the existing name for a delivery that was accepted but not performed)</read_first>
  <behavior>
    - A delivery is accepted, recorded durably, attempted, and settled, and each of those is a distinct persisted state. A crash at any point between them leaves a record that a restart can classify without guessing.
    - Restart after a crash mid-attempt performs the delivery exactly once. The ledger distinguishes an attempt that provably completed from one whose outcome is unknown, and only the unknown case is retried — with a stable idempotency key so a retry that reaches an already-served endpoint is suppressed rather than duplicated.
    - Drain closes admission first, then reports the number of turns still active and the number of deliveries still pending, then waits within a stated budget, then flushes the ledger to a durable point, and only then permits exit. A drain that exceeds its budget reports which work it abandoned, by identity, and exits with a status distinguishable from a clean drain.
    - The ledger is bounded. It has a retention rule and a compaction step, and a test proves it does not grow without limit under sustained delivery.
    - Adding the idempotency key to the outbound shape does not break an existing reader, or the compatibility break is explicit and recorded. The strict unknown-field rejection on that struct means this must be decided, not assumed.
    - The ledger's durable file is safe under the Windows handle rules: written through an atomic replace that does not fail against an open destination, and never renamed through a call form the handoff records as rejected on that platform.
  </behavior>
  <action>Write the exactly-once test first and prove it fails. It must drive accept, crash before settle, restart, and then assert the delivery count is exactly the accepted count with zero duplicates — counted from an independent sink, not from the ledger's own view of itself, because a ledger that grades its own homework proves nothing. Include the reverse case: a delivery that provably completed before the crash is not re-performed.

Implement the ledger as an append-then-settle durable record with a stable per-delivery identity. Follow the vocabulary the inbound duplicate cache already establishes so the two halves read as one system. Anchor retention explicitly and add compaction with a bound the test enforces.

Implement drain as a state, not a sleep. The order is fixed and each step is observable: stop admitting, publish the in-flight counts, wait within the budget, flush to a durable point, exit with a status that names clean or forced. Publish the counts through the protocol events landed in Task 1 so a host and the operator verb read the same numbers.

Decide the outbound idempotency-key question explicitly. The outbound message struct rejects unknown fields, so adding a field changes what an older reader accepts. Either keep the key out of the serialized message and carry it alongside in the ledger, or add it and record the compatibility consequence in the contract document. Choose one, state the cost, and do not leave it implicit.

Use an atomic replace for the durable file that is valid when the destination is open, and avoid the rename call form the handoff records as rejected on Windows when a handle is supplied as the root. Record which form was used and why in the contract document. Records evidence for F24-01; marks no requirement complete.</action>
  <verify>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'export PATH="/root/.local/bin:/root/.cargo/bin:$PATH"; cd /root/wayland &amp;&amp; vx cargo nextest run -p wcore-gateway --profile ci --no-fail-fast -E "test(ledger) + test(drain)"'</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'export PATH="/root/.local/bin:/root/.cargo/bin:$PATH"; cd /root/wayland &amp;&amp; vx cargo nextest run -p wcore-channels -p wcore-cron --profile ci --no-fail-fast'</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'export PATH="/root/.local/bin:/root/.cargo/bin:$PATH"; cd /root/wayland &amp;&amp; vx cargo clippy -p wcore-gateway -p wcore-channels --all-targets -- -D warnings'</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'cmd /c "cd /d C:\ferrox-win &amp;&amp; cargo nextest run -p wcore-gateway --profile ci --no-fail-fast 2>&amp;1"' > /tmp/f24-01-win-gateway.log 2>&amp;1; rc=$?; /usr/bin/grep -v CLIXML /tmp/f24-01-win-gateway.log | /usr/bin/grep -v "^&lt;Objs" | tail -60; test $rc -eq 0</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
  </verify>
  <done>The exactly-once test existed and failed before the ledger did, and now passes on Linux and on real Windows. Delivery counts are taken from an independent sink. Drain is an observable ordered state with published in-flight counts and a status that distinguishes clean from forced. The ledger is bounded by an enforced retention and compaction test. The outbound idempotency-key compatibility decision is recorded with its cost, as is the atomic-replace form chosen for the Windows handle rules. No existing channel or scheduling test regressed.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3 (BLOCKING DECISION): Authorize the Windows persistent-service mechanism, after measuring what the current detach path actually does</name>
  <files>crates/wcore-gateway/src/service/windows.rs, templates/gateway/windows-service.md, .planning/phases/24-gateway-automation-channels-typed-api/24-01-GATEWAY-CONTRACT.md</files>
  <action>Before presenting options, MEASURE. On the Windows box, in a scratch checkout and never in the phase's measurement checkout, start the existing detached background scheduler from a console, then close that console, and record whether the spawned process survives. Then repeat with the console interrupt. Change one variable at a time and record each observation with its exact command and output. The prediction is that the process does not survive because the non-Unix spawn branch sets no creation flags while the Unix branch starts a new session — but the prediction is not the measurement, and phase 20A's third plan is the standing example of a confidently stated mechanism that the repository contradicted. Record the measurement in the contract document before the checkpoint runs.

Then present the measurement and the options below to Sean and obtain ONE authorization. Do not write the Windows service implementation before it is given. Record the authorized option and the cost accepted with it verbatim in the contract document. If Sean selects deferral, Task 4 runs Linux and macOS only and the Windows gap is handed to 24-04 with its severity recorded.</action>
  <decision>Which mechanism makes the Wayland Core gateway a persistent, operator-managed service on Windows?</decision>
  <context>
On macOS and Linux the answer is settled by the existing templates: a per-user launch agent and a per-user systemd unit, both already written for the background scheduler and both directly reusable for the gateway. Windows has no equivalent already in the repository and the current detach path is predicted broken. The choice is a genuine product decision with different operator ergonomics, different privilege requirements, and different failure modes, and the phase-20A handoff is explicit that gated-away platform behaviour is how a dead path stays invisible for months. Read the measurement recorded in the contract document before choosing.
  </context>
  <options>
    <option id="win-service-scm">
      <name>A real Windows service registered with the service control manager, with the gateway answering the service control handshake in-process</name>
      <pros>It is the platform's own answer to a persistent background runtime: it starts before login, survives console close by construction, restarts under the platform's own recovery policy, and is inspectable with the tools a Windows operator already uses. It is the closest structural match to the launch agent and the systemd unit, so the three families stay one contract with one implementation each.</pros>
      <cons>Registration needs elevation, which puts an administrator prompt in the install path. The process must answer the control handshake within the platform's timeout or the platform kills it, so the drain budget and the service stop timeout must be reconciled explicitly. It adds a platform service dependency to a crate that currently has none.</cons>
    </option>
    <option id="win-scheduled-task">
      <name>A scheduled task registered to run at logon, with the gateway staying an ordinary detached process</name>
      <pros>No elevation in the ordinary case, so the install path stays a single non-privileged command. It is the closest analogue to the per-user launch agent and per-user systemd unit already in the repository, which keeps the three families symmetric in privilege as well as in shape. Restart-on-failure is expressible in the task definition.</pros>
      <cons>It does not start before login, so a headless or unattended box behaves differently from the Unix families and that difference must be documented rather than discovered. Task registration and query go through an external command-line tool, which is a weaker contract than a typed platform interface and needs its output parsed carefully.</cons>
    </option>
    <option id="win-detached-only">
      <name>Fix only the detach flags and ship no Windows service integration this phase</name>
      <pros>Smallest change: correct the missing creation flags so the background process genuinely leaves the console and process group, which is a real defect fix that stands on its own regardless of which mechanism is chosen later. It unblocks every other Windows behaviour in this phase without committing to a registration model.</pros>
      <cons>The first Success Criterion says native service lifecycle, and a process that must be started by hand after every logon is not that. It would leave Windows visibly behind the other two families and push the gap into a later phase where it competes with certification work.</cons>
    </option>
    <option id="win-defer">
      <name>Defer the Windows mechanism entirely and record the criterion as not closed on that platform</name>
      <pros>Nothing is built on an unauthorized mechanism, and the gap is visible in the record rather than absorbed into an implementation detail.</pros>
      <cons>The phase cannot honestly claim its first or fifth Success Criterion, since both name Windows explicitly. The decision only moves, it does not go away.</cons>
    </option>
  </options>
  <resume-signal>Select: win-service-scm, win-scheduled-task, win-detached-only, or win-defer. If selecting win-detached-only or win-defer, confirm explicitly that Phase 24 will not claim native Windows service lifecycle under Success Criterion 1 without a later plan closing it.</resume-signal>
  <verify>
    <human-check>The Windows detach measurement is recorded in the contract document with its exact commands and outputs, single-variable, taken in a scratch checkout that was removed afterwards. The authorized option and the cost accepted with it are recorded verbatim. The selected option is one the measurement actually left available.</human-check>
  </verify>
  <done>The current Windows detach behaviour is measured and recorded rather than predicted. One service mechanism is authorized with its accepted cost recorded verbatim — or deferral was selected and the Windows gap is handed to 24-04 with a severity.</done>
</task>

<task type="auto">
  <name>Task 4: LIVE — install, start, drain mid-flight, restart, upgrade and roll back the real gateway on Linux and macOS, and count every delivery</name>
  <files>crates/wcore-cli/src/gateway.rs, crates/wcore-cli/src/lib.rs, crates/wcore-cli/src/main.rs, crates/wcore-cli/Cargo.toml, crates/wcore-cli/tests/gateway_cli_surface.rs, crates/wcore-eval-scenarios/tests/pty_gateway_surface.rs, crates/wcore-gateway/src/service/mod.rs, crates/wcore-gateway/src/service/launchd.rs, crates/wcore-gateway/src/service/systemd.rs, crates/wcore-gateway/src/service/windows.rs, templates/gateway/launchd.plist, templates/gateway/systemd.service, templates/gateway/windows-service.md, .planning/phases/24-gateway-automation-channels-typed-api/24-01-GATEWAY-CONTRACT.md</files>
  <read_first>templates/cron-daemon/systemd.service and templates/cron-daemon/launchd.plist (the installation, verification, uninstall and log-routing sections already written for the background scheduler — the gateway templates are these, retargeted, and the operator instructions should not diverge in style), crates/wcore-cli/src/main.rs (the top-level subcommand enumeration and how an existing subcommand module is wired in, so the gateway verbs match the established shape), crates/wcore-cli/tests/harness_cli_surface.rs (the existing pattern for driving the compiled binary's non-interactive surface, which runs on Windows too), crates/wcore-eval-scenarios/src/pty_capture.rs (the pseudo-terminal driver: how it seeds a throwaway home, spawns the real binary with no mode flags so the full-screen interface actually runs, parses the rendered stream, and asserts on human-visible anchors), crates/wcore-cli/src/tui/surfaces/diagnostics.rs (the diagnostics surface reached by the slash command, and the section rendering that the gateway state must appear in)</read_first>
  <behavior>
    - The shipped binary exposes install, start, stop, restart, status, doctor, logs, drain and uninstall as gateway verbs, and each one prints an operator-legible result and returns an exit status that distinguishes success from every named failure.
    - The service abstraction has one implementation per OS family behind a single trait. No call site outside that module branches on platform.
    - On Linux and on macOS, a full lifecycle runs end to end from the shipped binary: install the user-level service, start it, confirm status reports running with a live process identity, submit a known count of deliveries, request drain while deliveries are still in flight, observe the reported in-flight counts fall to zero, restart, and then count what actually arrived at an independent sink.
    - The count is exact: delivered equals submitted, duplicates are zero, losses are zero. The same count survives an upgrade — replace the binary with a newer build and restart through the service — and a rollback back to the prior binary.
    - The running gateway is visible in the product's own interactive surface, not only in a subcommand's stdout. Driving the real full-screen interface under a pseudo-terminal and opening the diagnostics surface shows the gateway state.
    - The captured evidence is retained: the exact commands, the status output at each step, the delivery tally, and the rendered diagnostics screen text.
  </behavior>
  <action>Add the verb surface to the shipped binary following the established subcommand shape. Add the service abstraction with one implementation per family behind a single trait, and write the launch-agent and systemd unit templates by retargeting the existing background-scheduler templates — same installation, verification, uninstall and log sections, pointed at the gateway. Write the Windows implementation and template only as Task 3 authorized; if deferral was selected, leave the Windows arm returning a named unsupported error and record that in the contract document.

Then run the live exercise. On the Linux host, from a release build of the shipped binary, drive the sequence in this exact order and capture every command and its output verbatim into the contract document:

Install the user-level service, then start it, then read status and confirm it reports the running state with a live process identity and a zero in-flight count. Submit a known count of deliveries — use two hundred, which is large enough that a duplicate or a loss is unambiguous and small enough to finish inside a normal drain budget — addressed at an independent sink the gateway does not own, so the tally is not the ledger grading itself. While deliveries are still in flight, request drain, and record the in-flight counts the drain reports as they fall. Confirm the drain returns the clean status rather than the forced one. Restart through the service. Read the logs verb and confirm the lifecycle transitions appear. Then tally the sink: the delivered count must equal two hundred, unique deliveries must equal two hundred, duplicates zero, losses zero.

Repeat the tally across an upgrade: build a second binary with a distinguishable version, swap it in the installed location, restart through the service, and confirm the tally is unchanged and the status verb reports the new binary identity. Then roll back to the prior binary and confirm the tally is still unchanged.

Repeat the whole sequence on this Mac against a prebuilt artifact — never Cargo here — using the launch agent, and record it separately.

Finally, drive the real interactive interface under a pseudo-terminal on both Unix families with the gateway running: boot the binary with no mode flags so the full-screen interface actually runs, open the diagnostics surface through the palette slash command, wait for the surface to render, and capture the rendered screen text showing the gateway state. Store that captured text in the contract document. If the surface does not show gateway state, that is a real gap in operator visibility for this criterion — fix it here rather than reporting the verb output as sufficient.

Write that interface exercise as its OWN test, `crates/wcore-eval-scenarios/tests/pty_gateway_surface.rs`, rather than gating on the existing `pty_tui_smoke`. Two reasons, both measured in the source: that test asserts only the boot chrome wordmark and the Workspace tab, so it can pass with the diagnostics surface entirely broken and would gate nothing this task claims; and it returns early with a SKIP message when no binary is discovered, so it passes when nothing was launched at all. The new test uses the same pseudo-terminal driver — spawn, wait for the workspace anchor, open the palette, type the diagnostics surface's slash command, submit, then wait on a gateway-state anchor and assert on the rendered screen — and it FAILS rather than skips when the binary is absent, because a live gate with nothing to launch is operator error, not a pass. Name the anchor it waits for after text a Windows or Linux operator actually sees on that surface, and if the anchor is not there yet, that is the operator-visibility gap to fix, not a reason to weaken the anchor.

Windows lifecycle proof is NOT this task's; it belongs to 24-04 with the other native journeys. Records evidence for F24-01; marks no requirement complete — the terminal plan 24-04 owns requirement completion for this phase.</action>
  <verify>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'export PATH="/root/.local/bin:/root/.cargo/bin:$PATH"; cd /root/wayland &amp;&amp; vx cargo build --release -p wcore-cli --bin wayland-core &amp;&amp; ./target/release/wayland-core gateway --help'</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'export PATH="/root/.local/bin:/root/.cargo/bin:$PATH"; cd /root/wayland &amp;&amp; vx cargo nextest run -p wcore-cli --profile ci --no-fail-fast -E "test(gateway)"'</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'export PATH="/root/.local/bin:/root/.cargo/bin:$PATH"; cd /root/wayland &amp;&amp; ./target/release/wayland-core gateway status --json'</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'export PATH="/root/.local/bin:/root/.cargo/bin:$PATH"; cd /root/wayland &amp;&amp; test -x target/release/wayland-core &amp;&amp; WCORE_EVAL_BIN=$PWD/target/release/wayland-core vx cargo test -p wcore-eval-scenarios --test pty_gateway_surface -- --ignored --nocapture'</automated>
    <automated>ssh -o BatchMode=yes hetzner-dsm 'export PATH="/root/.local/bin:/root/.cargo/bin:$PATH"; cd /root/wayland &amp;&amp; test -x target/release/wayland-core &amp;&amp; WCORE_EVAL_BIN=$PWD/target/release/wayland-core vx cargo test -p wcore-eval-scenarios --test pty_tui_smoke -- --ignored --nocapture'</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -cF 'duplicates=0' .planning/phases/24-gateway-automation-channels-typed-api/24-01-GATEWAY-CONTRACT.md)" -ge "3"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -cF 'RENDERED-DIAGNOSTICS-SCREEN' .planning/phases/24-gateway-automation-channels-typed-api/24-01-GATEWAY-CONTRACT.md)" -ge "2"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
  </verify>
  <done>All nine gateway verbs exist on the shipped binary with distinguishable exit statuses, and the service abstraction has one implementation per family with no platform branching at any outside call site. On Linux and on macOS the full sequence ran from the shipped binary and the contract document holds the verbatim commands and outputs for install, start, status, two hundred submitted deliveries, mid-flight drain with falling in-flight counts, clean drain status, restart, upgrade and rollback — with a tally of two hundred delivered, two hundred unique, zero duplicates and zero losses at each of the three tally points, taken from an independent sink. The real full-screen interface was driven under a pseudo-terminal on both Unix families by a dedicated test that opens the diagnostics surface and asserts a gateway-state anchor — not by the boot-chrome smoke test, which asserts neither — and that test fails rather than skips when no binary is present. The captured diagnostics screen text showing gateway state is stored in the contract document. Windows lifecycle proof is explicitly deferred to 24-04 and named there.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → gateway verbs | An operator-supplied profile name, home path and service target cross into service registration and process spawn |
| network/channel → gateway ledger | Platform-supplied delivery identities and receipts cross into a durable file that drives retry decisions |
| gateway → per-profile child | The supervisor spawns credential-bearing children whose identity must come solely from their own home |
| prior process → restarting process | A pid file, a lock and a ledger written by a previous process are read by a new one and may be stale, truncated or adversarial |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01-01 | Elevation of Privilege | service registration path | high | mitigate | Service registration accepts only a validated profile name and an absolute binary path resolved from the running executable; no operator string is interpolated into a service command line. Elevation, if the authorized mechanism needs it, is requested explicitly and named in the install output. |
| T-24-01-02 | Spoofing | pid file and lock | high | mitigate | Liveness is proven by an owned handle on the sentinel byte, not by the pid value alone, so an unrelated process that reuses a recycled identifier cannot masquerade as the gateway. Stale reclamation requires the lock to be genuinely acquirable. |
| T-24-01-03 | Tampering | durable delivery ledger | high | mitigate | The ledger is written through an atomic replace and validated on load; a truncated or unparsable tail is quarantined and reported rather than silently discarded, because a silently dropped tail is a lost delivery. |
| T-24-01-04 | Repudiation | drain and upgrade transitions | medium | mitigate | Every lifecycle transition is emitted as a protocol event and written to the gateway log with the binary identity it occurred under, so an upgrade or rollback is attributable. |
| T-24-01-05 | Information Disclosure | logs verb and status projection | high | mitigate | The status projection and the logs verb carry no credential material; the profile supervisor's cleared-environment and per-child-key invariants are preserved unchanged and asserted by test. |
| T-24-01-06 | Denial of Service | drain budget | medium | accept | A drain budget that is exceeded reports the abandoned work by identity and exits forced. Unbounded waiting is the worse failure; the bounded forced exit is accepted and made visible. |
| T-24-01-07 | Tampering | npm/pip/cargo installs | high | mitigate | This plan adds no new package-manager dependency beyond what the authorized Windows mechanism requires. If the authorized option needs a new crate, it goes through a blocking human legitimacy check before the install. |
</threat_model>

<verification>
- The gateway crate's tests pass on Linux and on real Windows, and were written to fail first.
- The repository's own desktop-contract check passes after the protocol and manifest edits.
- The workspace-hack verification target is green after the new member is added.
- Clippy at deny-warnings passes for every touched crate, checked before the tests on the Windows path because that job order means a lint failure suppresses the tests entirely.
- The live delivery tally is exact at three points — after restart, after upgrade, after rollback — on Linux and on macOS, taken from an independent sink.
- The real full-screen interface renders gateway state under a pseudo-terminal on both Unix families, with the screen text captured.
</verification>

<success_criteria>
- Phase 24 Success Criterion 1 is closed for Linux and macOS by live exercise of the shipped binary, and the Windows portion is either implemented under the authorized mechanism or handed to 24-04 with a recorded severity.
- Requirement F24-01 has recorded evidence; requirement completion belongs to terminal plan 24-04.
- Every Phase-24 shared-seam file is edited exactly once, in this plan, so 24-02 and 24-03 can run concurrently in wave 2.
</success_criteria>

<output>
Create `.planning/phases/24-gateway-automation-channels-typed-api/24-01-SUMMARY.md` when done.
</output>

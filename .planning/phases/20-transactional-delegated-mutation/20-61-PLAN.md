---
phase: 20-transactional-delegated-mutation
plan: "61"
type: execute
wave: 56
depends_on:
  - "20-60"
files_modified: []
autonomous: true
requirements:
  - REQ-native-r10
  - REQ-native-r4
must_haves:
  truths:
    - "Unlike the 20-44 seal (which resynced `Cargo.lock` for a new wcore-swarm→dunce edge), the 20-60 delta touches only ONE `#![cfg(windows)]` file (the containment test `hard_process_containment_windows.rs`, hardening all three host-side reap-observer PowerShell scripts to fail closed — `$ErrorActionPreference='Stop'` + `-ErrorAction Stop` on every `Get-CimInstance` + a leading `trap { exit 1 }`) and changes NO manifest, so `Cargo.lock` is UNCHANGED. This plan CONFIRMS the zero lock delta: a `--locked --workspace --all-features` Hetzner build against a detached checkout of the exact 20-60 HEAD succeeds WITHOUT reporting the lock stale and without regenerating it (REQ-native-r10). If a `--locked` build unexpectedly reports the lock stale or a genuinely NEW crate, STOP and surface it before any downstream gate binds."
    - "This plan records the ONE exact further-repaired-successor candidate SHA/tree that every subsequent gate (four-way pre-native cross-audit 20-62, native re-dispatch 20-53, native-inclusive review 20-54, prep 20-55, terminal 20-56) binds to; after this seal, NO source, test, or workflow change follows (REQ-native-r12 anchoring)."
    - "The sealed successor builds `--locked --workspace --all-features` on Hetzner (proving lock consistency) and passes the aggregate Linux proof `nextest --profile ci --no-fail-fast` at 11509/0/48 — the 20-60 edit is Windows-only (`#![cfg(windows)]` test code), so there is no Linux compiled-output change and no Linux regression (REQ-native-r4)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-61-SUMMARY.md
      provides: "The sealed further-repaired-successor candidate SHA/tree with a confirmed-zero-delta lockfile and green --locked Hetzner build + aggregate 11509/0/48 receipts — the exact SHA all downstream gates bind to"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-60-SUMMARY.md
      to: .planning/phases/20-transactional-delegated-mutation/20-61-SUMMARY.md
      via: "the 20-60 fix-complete HEAD is sealed as the exact further-repaired-successor candidate SHA"
      pattern: "source_sha"
---

<objective>
Re-seal the ONE exact further-repaired-successor candidate SHA. Because the 20-60 fix changed NO manifest (one Windows-only test file only — the fail-closed PowerShell-layer hardening of all three reap-observer scripts), this is a no-op-lock seal: confirm `Cargo.lock` is unchanged by proving a `--locked --workspace --all-features` Hetzner build against a detached checkout of the exact 20-60 HEAD succeeds without updating the lock, then prove the aggregate Linux suite is unregressed at 11509/0/48. Record the sealed SHA/tree that anchors 20-62…20-56.

Purpose: Downstream gates cannot bind to an unsealed or lock-inconsistent candidate (REQ-native-r10), and the Linux no-regression gate (REQ-native-r4) must pass before the scarce Sean-gated native run is re-spent (rebound 20-53). This seal anchors 20-62…20-56 to one exact successor SHA.
Output: The recorded sealed successor SHA/tree, a confirmed-zero-delta `Cargo.lock`, a `--locked` Hetzner build receipt, and a `nextest --profile ci` = 11509/0/48 aggregate receipt. No native or requirement claim.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-60-SUMMARY.md
@.planning/codebase/TESTING.md
</context>

<execution_rules>
- Capture the plan base before any edit: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-61)"`.
- NO Cargo on the Mac. All build/aggregate runs go through the committed-HEAD Hetzner harness against a detached checkout of the exact 20-60 fix-complete HEAD.
- The 20-60 delta did NOT change any manifest (no `Cargo.toml` edit — the one file is the `#![cfg(windows)]` containment test with the three reap-observer PowerShell scripts hardened to fail closed), so — unlike the 20-44 seal — NO lock delta is expected. Confirm the lock is unchanged: the `--locked` build must succeed WITHOUT the lock being stale and WITHOUT regeneration. If the `--locked` build reports the lock stale or reveals a genuinely NEW crate, STOP and surface it for a package-legitimacy checkpoint before committing anything.
- Prove against a detached checkout of the EXACT 20-60 fix-complete HEAD so the remote-cargo tree-hash gate verifies the precise sealed candidate tree.
- After this seal, NO source, test, or workflow change for the remainder of the further-repaired-successor sequence; the sealed SHA is the exact candidate all downstream gates bind to.
- This plan writes only planning artifacts (the summary); it modifies no repository source, test, workflow, or lock file.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Confirm zero Cargo.lock delta and record the sealed successor SHA</name>
  <files>None; this task records the sealed SHA and produces a Hetzner --locked build receipt. No repository file is modified.</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-60-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md, Cargo.lock</read_first>
  <behavior>
    - The 20-60 fix added no dependency and changed no `Cargo.toml` (it hardened the three host-side reap-observer PowerShell scripts in the `#![cfg(windows)]` containment test to fail closed), so a `--locked --workspace --all-features` build against the exact 20-60 HEAD is expected to succeed with the lock UNCHANGED (no stale-lock report, no regeneration). This is the no-op-lock counterpart of the 20-44 dunce-edge resync.
    - Confirm `git diff 8a1d2d84 -- Cargo.lock` (and vs the branch tip) shows no lock change introduced by the 20-60 delta; the sealed successor SHA is simply the 20-60 fix-complete HEAD.
    - If the `--locked` build reports the lock stale or a genuinely NEW crate appears, STOP and surface it for a package-legitimacy checkpoint before recording any seal.
  </behavior>
  <action>Through the committed-HEAD Hetzner harness, run a `--locked --workspace --all-features` build against a detached checkout of the exact 20-60 fix-complete HEAD and confirm it succeeds WITHOUT reporting the lock stale and WITHOUT regenerating `Cargo.lock` (the 20-60 delta changed no manifest — only the one `#![cfg(windows)]` containment test — so zero lock delta is expected). Confirm no lock change was introduced by the 20-60 delta (`git diff 8a1d2d84 -- Cargo.lock` empty) and record the 20-60 fix-complete HEAD SHA/tree as the sealed further-repaired successor. If the `--locked` build reports the lock stale or a genuinely NEW crate, STOP for a package-legitimacy checkpoint and do not seal. Do not modify any source, test, workflow, or lock file. Implements REQ-native-r10.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-61)"</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-61-build "$(pwd -P)" build --workspace --all-features --locked</automated>
  </verify>
  <acceptance_criteria>The `--locked` Hetzner build against the exact 20-60 HEAD succeeds with `Cargo.lock` unchanged (no stale-lock report, no regeneration, no new crate); the sealed successor SHA/tree is recorded as the 20-60 fix-complete HEAD; the scope gate confirms this plan modified no repository file.</acceptance_criteria>
  <done>The sealed further-repaired successor SHA is fixed and lock-consistent with zero lock delta.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the sealed successor builds --locked and the aggregate Linux suite is unregressed</name>
  <files>None; this task produces Hetzner receipts against the sealed successor.</files>
  <read_first>.planning/codebase/TESTING.md, .planning/phases/20-transactional-delegated-mutation/20-60-SUMMARY.md</read_first>
  <behavior>
    - `build --workspace --all-features --locked` succeeds on Hetzner against a detached checkout of the sealed successor (the lock is consistent and unchanged).
    - `nextest run --workspace --profile ci --no-fail-fast` on Hetzner reports 11509 passed / 0 failed / 48 skipped — the 20-60 edit is Windows-only (`#![cfg(windows)]` test code), so the Linux count is unchanged.
    - A different aggregate count is a regression from the 20-60 edit and blocks the sequence; it is recorded and investigated, not rationalized (honesty gate).
  </behavior>
  <action>Run the two committed-HEAD Hetzner gates against a detached checkout of the sealed successor: the `--locked` full-workspace all-features build, and the aggregate `nextest --profile ci --no-fail-fast`. Confirm the aggregate is 11509 passed / 0 failed / 48 skipped — any deviation is a Linux regression from the 20-60 edit and stops the sequence for repair. Record both receipts with exact command, exit status, counts, nextest run ID, and remote receipt identity. Implements REQ-native-r4.</action>
  <verify>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-61-build "$(pwd -P)" build --workspace --all-features --locked</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-61-aggregate "$(pwd -P)" nextest run --workspace --profile ci --no-fail-fast</automated>
  </verify>
  <acceptance_criteria>The sealed successor builds `--locked` all-features and the aggregate Linux suite is 11509/0/48 with no regression. Any other count blocks the sequence.</acceptance_criteria>
  <done>The sealed successor has a green `--locked` Hetzner build + aggregate receipt and is ready for the four-way pre-native cross-audit (20-62).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate identity ← lockfile | The sealed SHA must carry a consistent lock; a stale/incoherent lock produces a build that does not match the reviewed source |
| dependency set ← Cargo.toml | The seal must confirm NO manifest/lock change slipped in with the 20-60 fix; an unexpected new crate STOPS the plan for a legitimacy checkpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-61-01 | Tampering | unexpected Cargo.lock delta | high | mitigate | The seal confirms the `--locked` build succeeds with the lock UNCHANGED; any stale-lock report or genuinely NEW crate STOPS the plan for a package-legitimacy checkpoint rather than being sealed silently. |
| T-20-61-02 | Denial of Service | aggregate regression | medium | mitigate | The `nextest --profile ci` gate requires the exact 11509/0/48 baseline; a deviation blocks the sequence rather than being rationalized. |
</threat_model>

<verification>
The two Hetzner receipts (`--locked` build with the lock unchanged + aggregate `nextest --profile ci`), run against a detached checkout of the exact sealed tree, determine acceptance. Record the sealed `source_sha`, source tree, both commands, exit statuses, counts, nextest run ID, remote receipt identity, and the explicit confirmation of ZERO lock delta (no manifest change in the 20-60 fix) in `20-61-SUMMARY.md`. The summary states the candidate is sealed, no source/test/workflow changes follow, and native proof/requirement completion remain deferred (rebound 20-53/20-56). No Cargo runs on the Mac.
</verification>

<success_criteria>
- `Cargo.lock` is confirmed unchanged (no manifest change in the 20-60 fix), and a `--locked` Hetzner build against the exact sealed tree succeeds without updating the lock or pulling a new crate (r10).
- The sealed successor builds `--locked` all-features on Hetzner and the aggregate suite is 11509/0/48 with no Linux regression (r4).
- The sealed SHA is recorded as the single candidate all downstream gates bind to.
</success_criteria>

## Artifacts this phase produces
- The sealed further-repaired-successor candidate SHA with a confirmed-zero-delta lockfile and green `--locked` Hetzner build + aggregate receipts.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-61-SUMMARY.md` using the standard GSD summary template. Record the sealed `source_sha`/tree, the zero-lock-delta confirmation (no manifest change in the 20-60 fix), both Hetzner receipts, and the explicit statement that no source/test/workflow changes follow and native/requirement completion is deferred. Complete no Phase 20 requirement.
</output>
</content>

---
phase: 20-transactional-delegated-mutation
plan: "08"
type: execute
wave: 14
depends_on:
  - "20-01"
  - "20-02"
  - "20-03"
  - "20-04"
  - "20-05"
  - "20-07"
  - "20-14"
files_modified:
  - AGENTS.md
  - crates/wcore-agent/src/child_transaction.rs
  - crates/wcore-agent/src/durable_spawner.rs
  - crates/wcore-agent/src/engine.rs
  - crates/wcore-agent/src/spawner.rs
  - crates/wcore-agent/src/orchestration/anvil/tool.rs
  - crates/wcore-agent/tests/transactional_delegated_mutation_test.rs
  - .github/workflows/nightly-windows-soak.yml
  - scripts/f20-native-windows-proof.ps1
  - scripts/f20-native-macos-proof.sh
  - scripts/f20-native-uat-proof.mjs
  - scripts/f20-native-uat-proof.test.mjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Authoritative snapshot acquisition, durable transaction opening, revalidation, workspace allocation, child launch, gates, coherent parent landing, rollback, cleanup, and terminal receipt form one lifecycle."
    - "Every success, failure, conflict, cancellation, and restart path leaves attributable durable state and performs only owned cleanup."
    - "Multi-candidate Anvil climbs own one transaction per candidate; only the selected winner traverses the parent gate and landing lifecycle, while every loser is terminalized and cleaned."
    - "The exact construction successor passes focused committed-head Hetzner proof and records its immutable source identity for fresh independent review; no native or phase-completion claim is made here."
  artifacts:
    - path: crates/wcore-agent/src/child_transaction.rs
      provides: "Coherent orchestration of the complete child transaction lifecycle"
    - path: crates/wcore-agent/tests/transactional_delegated_mutation_test.rs
      provides: "Hostile end-to-end lifecycle, restart, rollback, and cleanup proof"
    - path: crates/wcore-agent/src/orchestration/anvil/tool.rs
      provides: "Forge terminal routing through authoritative transactional landing without manual merge bypass"
    - path: scripts/f20-native-windows-proof.ps1
      provides: "Final exact-candidate native Windows identity and lifecycle UAT"
    - path: scripts/f20-native-macos-proof.sh
      provides: "Final exact-candidate native macOS retained-authority, Docker Desktop, public-dispatch, and lifecycle UAT"
    - path: scripts/f20-native-uat-proof.mjs
      provides: "No-follow exact-byte UAT publication, state, run, and native-log verifier"
  key_links:
    - from: crates/wcore-agent/src/durable_spawner.rs
      to: crates/wcore-agent/src/child_transaction.rs
      via: "authoritative opening, pre-effect revalidation, launch, and terminal transitions"
      pattern: "ChildTransaction"
    - from: crates/wcore-agent/src/child_transaction.rs
      to: crates/wcore-agent/src/child_transaction/parent.rs
      via: "gate-qualified landing and rollback"
      pattern: "parent"
    - from: scripts/f20-native-windows-proof.ps1
      to: crates/wcore-sandbox/tests/live_fs_acl.rs
      via: "exact-candidate native Windows test"
      pattern: "live_fs_acl"
    - from: .github/workflows/nightly-windows-soak.yml
      to: scripts/f20-native-windows-proof.ps1
      via: "workflow invokes the same exact-candidate native proof entrypoint"
      pattern: "f20-native-windows-proof"
    - from: .github/workflows/nightly-windows-soak.yml
      to: scripts/f20-native-macos-proof.sh
      via: "candidate-only macOS job on the externally provisioned f20-native-macos runner"
      pattern: "f20-native-macos-proof"
    - from: scripts/f20-native-uat-proof.mjs
      to: .github/workflows/nightly-windows-soak.yml
      via: "authorized exact-SHA UAT publication, one dispatch, immutable run capture, and per-target log verification"
      pattern: "F20_NATIVE_TARGET"
---

<objective>
Construct Phase 20 as one production transaction lifecycle and prove its focused behavior on the exact committed source.

Purpose: Individual components do not satisfy F20 unless their success, failure, restart, integration, rollback, and cleanup transitions compose without authority gaps.
Output: Final lifecycle wiring, E2E tests, reconciled native-UAT helpers, and an exact source candidate ready for independent plan 20-16 review.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
@/Users/seandonahoe/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-02-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-05-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-06-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-14-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-07-SUMMARY.md
@crates/wcore-agent/src/child_transaction.rs
@crates/wcore-agent/src/durable_spawner.rs
@crates/wcore-agent/src/child_transaction/gates.rs
@crates/wcore-agent/src/child_transaction/parent.rs
</context>

<salvage>
None. Compose and prove the final lifecycle from accepted predecessor outputs only.
</salvage>

<execution_rules>
- Require a clean isolated checkout and run `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-08)"` before Task 1 edits.
- Every task scope check uses the complete plan-base union through that task, including committed, staged, unstaged, untracked, deleted, renamed, and copied paths.
- Do not run Cargo on macOS; use exact-file `vx rustfmt` for Rust construction and the Hetzner/native harnesses for authoritative proof.
- Native Windows and macOS proof requires a candidate-specific remote branch and one workflow dispatch. Those external mutations are deferred to terminal plan 20-17 and remain blocked on Sean's exact-candidate authorization; this plan only constructs and focused-proves the source candidate on Hetzner.
- No Cargo runs on the local Mac. macOS Cargo proof runs only on one externally provisioned ephemeral runner selected by `[self-hosted, f20-native-macos, f20-ephemeral, f20-no-ambient-secrets, f20-image-&lt;sha256&gt;]`. It must be idle before dispatch, expose no ambient secrets, use no persisted authority-sensitive cache, and deregister after its single job. One explicitly authorized candidate push and one candidate-mode workflow dispatch are the only external mutations.
- Candidate authorization and run identity persist in strict Git-private files returned by `git rev-parse --git-path`; shell variables or agent memory never carry authority across the checkpoint continuation.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Compose the complete child transaction lifecycle</name>
  <files>crates/wcore-agent/src/child_transaction.rs, crates/wcore-agent/src/durable_spawner.rs, crates/wcore-agent/src/engine.rs, crates/wcore-agent/src/spawner.rs, crates/wcore-agent/src/orchestration/anvil/tool.rs</files>
  <read_first>crates/wcore-agent/src/child_transaction.rs, crates/wcore-agent/src/child_transaction/gates.rs, crates/wcore-agent/src/child_transaction/parent.rs, crates/wcore-agent/src/durable_spawner.rs, crates/wcore-agent/src/engine.rs, crates/wcore-agent/src/spawner.rs, crates/wcore-agent/src/orchestration/anvil/tool.rs</read_first>
  <behavior>
    - Mutation follows acquire authoritative snapshot -> journal transaction opening -> revalidate -> allocate owned workspace -> revalidate -> launch -> terminal child result -> parent-owned observed gates -> prepare/lock/revalidate parent -> ref CAS -> coherent index/worktree projection -> landed -> owned cleanup.
    - A workspace, child process, provider, or tool is never invoked without the opaque bound transaction snapshot; startup reconciliation completes before new transactional dispatch.
    - The snapshot token remains bound through declaration, every receipt revision, gates, landing, rollback, and cleanup.
    - Failure, conflict, cancellation, timeout, and restart resume or terminate deterministically without parent mutation or leaked owned state.
    - Rollback and cleanup are idempotent, identity-bound, and refuse foreign state.
    - Forge hands exactly one opaque selected-candidate authority, or none, into the parent lifecycle. Anvil evaluation evidence remains diagnostic/advisory, and all non-selected candidate transactions are durably terminal and cleaned before parent preparation begins.
    - Forge never instructs the user to merge or cherry-pick an unlanded candidate. Its terminal candidate is passed into this lifecycle, and the tool reports only the authoritative landed, conflict, refused, rollback-required, or recovery-required result returned by parent authority.
  </behavior>
  <action>Connect the previously built authoritative transaction-opening/store, standalone-checkout-aware spawner, parent-owned gate executor/state, quarantined object import, and recoverable parent landing state machine into one lifecycle API used by production engine paths. Acquire and durably bind the retained journal snapshot before allocating a workspace; revalidate before allocation and again before execution. The lifecycle—not a caller or child—selects the gate plan, invokes the executor, receives opaque observed results, and advances acceptance. Route exactly one opaque ForgeTool selected-candidate authority, or none, into this lifecycle; retain Anvil evaluation evidence only as diagnostics, refuse parent preparation until all non-selected candidate transactions are durably terminal and cleaned, delete the manual merge/cherry-pick instruction, and render only the authoritative parent result without letting candidate/model prose claim integration. Reconcile incomplete parent projection/rollback before dispatch, preserve structured terminal diagnostics, and clean only the transaction's owned snapshot/workspace/lease/quarantine/lock state. Do not add a second orchestrator or evidence framework.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-08)" crates/wcore-agent/src/child_transaction.rs crates/wcore-agent/src/durable_spawner.rs crates/wcore-agent/src/engine.rs crates/wcore-agent/src/spawner.rs crates/wcore-agent/src/orchestration/anvil/tool.rs</automated>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-agent/src/child_transaction.rs crates/wcore-agent/src/durable_spawner.rs crates/wcore-agent/src/engine.rs crates/wcore-agent/src/spawner.rs crates/wcore-agent/src/orchestration/anvil/tool.rs</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, and every changed path is declared by this task. This does not assert compilation or behavioral acceptance; those are established only by the plan-level clean-HEAD Hetzner gates.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

<task type="auto">
  <name>Task 2: Prove hostile end-to-end mutation, restart, and recovery</name>
  <files>crates/wcore-agent/tests/transactional_delegated_mutation_test.rs</files>
  <read_first>crates/wcore-agent/src/child_transaction.rs, crates/wcore-agent/tests/child_transaction_store_test.rs, crates/wcore-agent/tests/child_transaction_gate_test.rs, crates/wcore-agent/tests/child_transaction_parent_cas_test.rs</read_first>
  <behavior>
    - A successful child mutates only its standalone checkout; the parent runs the exact authorized gates and derives observed receipts; the candidate imports through parent-owned quarantine, lands once, and can roll back exactly.
    - Snapshot substitution, stale generation, session rebind, or conflicting opening before launch leaves workspace/provider/tool/child invocation counters at zero.
    - Parent edits racing the child create an explicit conflict and never disappear; restart at every ref/HEAD/index/worktree landing or rollback boundary converges to exactly old or new coherent state, never a silent mixture.
    - Failed/spoofed/substituted/unrun gates, malformed evidence, process interruption, timeout, cancellation, duplicate terminal events, restart, dirty user worktrees, and cleanup failure remain fail-closed and diagnosable.
    - The accepted happy path executes through a live qualifying hard-containment backend on the proof host; scripted runners cover injected failures only and cannot establish acceptance.
    - A multi-candidate Anvil climb lands at most the selected winner, while rejected, superseded, failed, and losing candidate transactions roll back or clean up exactly and cannot reach parent CAS.
    - Forge terminal output cannot bypass landing: an accepted candidate either traverses parent CAS and reports its authoritative outcome or remains explicitly unlanded; the legacy manual merge/cherry-pick instruction is absent.
  </behavior>
  <action>Create a black-box independent-repository test that exercises authoritative opening, the production spawner, Forge terminal handoff, object import, coherent parent landing, rollback, and cleanup rather than isolated helpers. Make the accepted happy path run through an actually live and semantically probed qualifying hard-containment backend; scripted runners may inject failures but may never prove acceptance. Cover snapshot substitution/stale generation/rebind and assert zero external effects; inject crashes at every parent lock/prepared/ref-CAS/index/worktree/landed and rollback boundary; keep arbitrary dirty user worktrees present; run a multi-candidate Anvil keep-best climb and prove only the winner enters the parent-owned 20-06 gate/20-07 CAS while every loser is terminalized and identity-cleaned; exercise Forge landed/conflict/refused/recovery-required outcomes and assert no manual-merge escape text; and assert target ref, symbolic HEAD, index tree, worktree bytes, journal outcome, snapshot token, standalone checkout, lease, quarantine, and cleanup identity at every path. Include all severity findings discovered during implementation rather than deferring them.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-08)" crates/wcore-agent/src/child_transaction.rs crates/wcore-agent/src/durable_spawner.rs crates/wcore-agent/src/engine.rs crates/wcore-agent/src/spawner.rs crates/wcore-agent/src/orchestration/anvil/tool.rs crates/wcore-agent/tests/transactional_delegated_mutation_test.rs</automated>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-agent/tests/transactional_delegated_mutation_test.rs</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, every changed path is declared, and the owned test source names every hostile lifecycle case described above. No test, platform, review, or all-severity acceptance claim is made before the applicable post-commit and phase-level gates.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile final native Windows and macOS proof</name>
  <files>AGENTS.md, scripts/f20-native-windows-proof.ps1, scripts/f20-native-macos-proof.sh, scripts/f20-native-uat-proof.mjs, scripts/f20-native-uat-proof.test.mjs, .github/workflows/nightly-windows-soak.yml</files>
  <read_first>scripts/f20-native-windows-proof.ps1, .github/workflows/nightly-windows-soak.yml, crates/wcore-sandbox/src/directory_authority_windows_tests.rs, crates/wcore-sandbox/src/backends/process_tree.rs, crates/wcore-sandbox/src/backends/docker_tests.rs, crates/wcore-sandbox/tests/live_fs_acl.rs, crates/wcore-sandbox/tests/hard_process_containment.rs, crates/wcore-swarm/tests/dispatch_smoke.rs, crates/wcore-swarm/tests/worker_runtime_limits.rs, crates/wcore-swarm/tests/workspace_authority.rs, crates/wcore-agent/tests/transactional_delegated_mutation_test.rs</read_first>
  <behavior>
    - Both helpers fail unless repository root, clean state, exact expected commit, and exact expected tree match before any test runs.
    - The Windows helper runs the exact retained-handle, AppContainer ACL, Job Object teardown, public-dispatch Bash containment, hard-process-containment, and complete F20 lifecycle targets.
    - The macOS helper requires Darwin plus a live Docker Desktop daemon and runs the exact retained-directory, process-tree teardown, retained Docker transport, public-dispatch Bash containment, Docker cancellation/budget, and complete F20 lifecycle targets.
    - The existing workflow gains candidate mode. One dispatch runs hosted `windows-2022` plus a dynamically pinned `[self-hosted, f20-native-macos, f20-ephemeral, f20-no-ambient-secrets, f20-image-&lt;sha256&gt;]` runner against the same `github.sha` and emits commit/tree-bearing platform markers. Candidate mode grants only `contents: read`, receives no secrets, disables persisted caches and generic issue automation, binds GitHub job IDs plus runner ID/name/image label to evidence, and requires the macOS runner to deregister after its one job.
    - Each helper emits exactly one candidate-bound target marker immediately after each required target succeeds, then exactly one final platform marker. Any absent, duplicate, reordered, skipped, retried, or foreign marker fails closed.
    - The UAT proof helper reads every authority/state object exactly once through a no-follow file descriptor, validates the opened object, retains those exact bytes in memory, and passes only those bytes across later authority checks. It creates log/evidence files with exclusive no-follow descriptors, streams command output to the already-open descriptor, syncs it, and verifies the exact captured bytes rather than reopening a pathname.
    - Any mismatch, mutable-path substitution, FIFO, missing runner/backend, ignored or absent test, nonzero command, incomplete log, or source change fails closed; no native PASS is fabricated.
  </behavior>
  <action>Make both helpers consume argv-safe expected-commit/tree inputs, validate the exact checkout, and run every named target with `--no-tests=fail` and `--run-ignored all` only for deliberately ignored native-acceptance tests. After each successful selector, Windows emits exactly one `F20_NATIVE_TARGET=PASS platform=windows target=&lt;id&gt; commit=&lt;commit&gt; tree=&lt;tree&gt;` marker for `windows-retained-handle`, `windows-appcontainer-acl`, `windows-job-object`, `windows-public-dispatch`, `windows-hard-process-containment`, and `windows-f20-lifecycle`; macOS emits the corresponding `platform=macos` marker for `macos-retained-directory`, `macos-process-tree`, `macos-docker-reject-path-replacement`, `macos-docker-roundtrip-delete`, `macos-public-dispatch`, `macos-docker-cancellation`, `macos-docker-budget`, and `macos-f20-lifecycle`. Only after all target markers may the helper emit its single candidate-bound platform acceptance marker. The macOS helper accepts `--expected-commit` and `--expected-tree`, uses strict lowercase 40-or-64-hex parsing, requires `WAYLAND_F20_NATIVE_ACCEPTANCE=1`, `uname -s` equal to `Darwin`, a clean exact checkout, and successful `docker info`. It runs the eight exact selectors already declared by this plan separately. Add boolean `workflow_dispatch.inputs.f20_candidate` defaulting false plus required candidate-mode `f20_macos_runner_label`; use that externally pinned image label in the macOS `runs-on` array. In candidate mode run only the exact F20 jobs, grant only `contents: read`, pass no secrets, use no persisted authority-sensitive cache, record job/runner identity, and disable normal soak and failure-to-issue automation; scheduled nightly behavior remains unchanged. Implement `f20-native-uat-proof.mjs` as the sole terminal-plan publication/state/log authority: open each Git-private state input once with no-follow semantics; validate the opened regular-file identity, mode, exact newline grammar, commit/tree/ref/runner/run tuple, and current candidate; retain exact bytes; publish only the authorized full SHA to the exact UAT ref with hooks disabled and no Cargo/format/commit action; execute `gh` with argv arrays; capture logs through an exclusively created, already-open mode-0600 descriptor; sync and validate those same bytes; bind GitHub job IDs and hosted/ephemeral runner identity; require the pinned macOS runner ID to disappear after its single job; and count every target/final marker exactly once in the correct job log. Its tests must replace paths and use FIFOs between checks, precreate/symlink output paths, mutate state after open, duplicate/omit/reorder markers, substitute runner/job/image identity, retain the supposedly ephemeral runner, and prove fail-closed behavior without executing a production push. Update `AGENTS.md` with the narrow rule that only the human-authorized Phase 20 terminal UAT helper may publish the already-focused-proofed and independently reviewed exact full SHA directly to its exact temporary UAT ref without `just push`; no other ref or push bypass is authorized. Do not push or dispatch in this task.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-08)" crates/wcore-agent/src/child_transaction.rs crates/wcore-agent/src/durable_spawner.rs crates/wcore-agent/src/engine.rs crates/wcore-agent/src/spawner.rs crates/wcore-agent/src/orchestration/anvil/tool.rs crates/wcore-agent/tests/transactional_delegated_mutation_test.rs AGENTS.md scripts/f20-native-windows-proof.ps1 scripts/f20-native-macos-proof.sh scripts/f20-native-uat-proof.mjs scripts/f20-native-uat-proof.test.mjs .github/workflows/nightly-windows-soak.yml</automated>
    <automated>bash -n scripts/f20-native-macos-proof.sh</automated>
    <automated>git diff --check</automated>
    <automated>node --test scripts/f20-native-uat-proof.test.mjs</automated>
    <automated>bash -lc 'set -euo pipefail; rg -F "f20_candidate" .github/workflows/nightly-windows-soak.yml; rg -F "f20-native-macos" .github/workflows/nightly-windows-soak.yml; rg -F "scripts/f20-native-macos-proof.sh" .github/workflows/nightly-windows-soak.yml; rg -F "F20_NATIVE_WINDOWS_ACCEPTANCE=PASS" scripts/f20-native-windows-proof.ps1; rg -F "F20_NATIVE_MACOS_ACCEPTANCE=PASS" scripts/f20-native-macos-proof.sh'</automated>
  </verify>
  <acceptance_criteria>Construction only: both helpers bind the exact candidate and emit one marker per required native identity; the no-follow UAT helper rejects mutable state/log paths and incomplete or duplicated target evidence; candidate-mode workflow targets hosted Windows and the explicit external macOS runner without issue-tracker side effects. No native PASS is claimed yet.</acceptance_criteria>
  <done>Exact-candidate dual-platform UAT is constructed as a provisional task commit.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>Caller snapshot bytes, child process, filesystem, Git ref/HEAD/index/worktree surfaces, journal, gate output, parent integration, cleanup, restart, and native runner identities meet in one lifecycle.</trust_boundaries>
<threats>Effects before authoritative opening, snapshot substitution/rebind, authority gaps, caller/child gate-result forgery, unrun gates, post-terminal mutation, lost diagnostics, ref/index/worktree divergence, cleanup of foreign state, restart double-landing, stale native proof, helper argument injection, and claiming readiness from focused tests alone.</threats>
<mitigations>Opaque journal-owned snapshot token, durable opening before effects, two pre-effect revalidations, parent-owned gates, exact identities at every transition, locked recoverable landing state machine, idempotent owned cleanup/rollback, hostile E2E tests, and commit/tree-pinned standard UAT.</mitigations>
</threat_model>

<verification>
Each task first passes its pre-commit construction gate on the Mac and becomes a provisional GSD task commit. After every task commit exists and the checkout is clean, the following exact committed-HEAD Hetzner gates determine construction acceptance. A construction commit is not behavioral evidence:
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-08-e2e "$(pwd -P)" test -p wcore-agent --test transactional_delegated_mutation_test</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-08-clippy "$(pwd -P)" clippy -p wcore-agent --all-targets --all-features -- -D warnings</automated>
Record the exact committed `source_sha`, source tree, commands, exit status, and remote receipt identity in `20-08-SUMMARY.md`. The summary must state that native platform proof, aggregate proof, requirement completion, and Phase 20 completion are deferred. Plan 20-16 independently reviews this exact source before plan 20-17 may request native UAT. Any source repair creates a new source candidate and invalidates the focused proof and review chain.
</verification>

<success_criteria>
- The complete lifecycle is coherent, attributable, reversible, and restart-safe.
- Conflicts and all failed gates stop before merge while preserving diagnostics.
- The exact construction successor passes both focused committed-HEAD Hetzner gates.
- The summary records exact source identity and explicitly awaits the fresh all-severity review in plan 20-16; it makes no native, aggregate, requirement-completion, or phase-completion claim.
</success_criteria>

## Artifacts this phase produces

- Final production child-transaction lifecycle wiring.
- Hostile end-to-end transactional mutation test.
- Reconciled exact-candidate native Windows and macOS UAT helpers plus one candidate-mode workflow route.

<output>
After construction completion, create `.planning/phases/20-transactional-delegated-mutation/20-08-SUMMARY.md` using the standard GSD summary template. Record exact `source_sha`, source tree, focused Hetzner commands/results, changed paths, and the explicit `awaiting 20-16 independent review` disposition. Do not mark any Phase 20 requirement complete and do not claim native, aggregate, packaging, deployment, release, or phase acceptance.
</output>

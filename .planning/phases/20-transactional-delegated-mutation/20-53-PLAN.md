---
phase: 20-transactional-delegated-mutation
plan: "53"
type: execute
wave: 55
depends_on:
  - "20-59"
files_modified: []
autonomous: false
requirements:
  - REQ-native-r14
  - REQ-native-r11
  - REQ-native-r5
  - REQ-native-r7
  - REQ-native-r6
  - REQ-native-r2
  - REQ-native-r9
  - REQ-native-r3
  - REQ-native-r4
must_haves:
  truths:
    - "One FRESH Sean-authorized native-proof dispatch runs the exact re-sealed further-repaired-successor (the SHA/tree recorded by 20-58) on real hardware: Windows on the AppContainer-capable self-hosted msvc runner (client SKU) and macOS on a FRESHLY re-registered LOCAL ephemeral Apple-silicon runner; native proof is never claimed from cross-compilation or source inspection (CONTEXT D6/D7, REQ-native-r11). ALL prior native-proof-dispatch authorizations (95c81ec6, 17412cf2, daf27337, and any tied to the sealed-but-RED 3f839309 or the 20-52-blocked f0dd5b6d) are spent/void; this digest requires its own fresh authorization on a fresh dispatch branch with single-attempt discipline (REQ-native-r14)."
    - "The native run re-proves on hardware the 20-50 finding fixes with no onion-peel: FINDING 1 — windows-hard-process-containment's `active_process_cap_is_enforced` captures a NON-EMPTY set of fan-out `choice` PIDs while the tagged parent is alive and, after job close, confirms every captured ProcessId is gone (the reap check is genuinely non-vacuous on hardware, and a concurrent `live_fs_acl` `choice.exe` does not pollute it); every other containment assertion (positive peak, cap ceiling, below-attempts bound, clean exit) and the sibling `job_close_reaps_detached_descendant_with_no_residue` pass. FINDING 2 — windows-appcontainer-acl + windows-retained-handle read the granted file (exit 0, not ERROR_INVALID_NAME), confirming the tightened `resolved_program_is_cmd` still routes real `cmd.exe` `/c`/`/k` payloads through the cmd quoter; the optional inline classifier unit test (if added) compiles and passes on msvc (REQ-native-r5/r7/r2)."
    - "The two 20-43 PRODUCTION root causes still hold across ALL 6 Windows targets: #A — windows-f20-lifecycle + windows-public-dispatch pass the DispatchAdmission capacity probe (de-verbatimized swarm_root + WCORE_SWARM_PROBE_ROOT env transport, real budget intact); #B — windows-job-object + windows-hard-process-containment run their fixed cmd payloads and containment counting. The Kimi watch-items are EXERCISED as next-layer checks: git operations run against the de-verbatimized swarm_root; `tagged_cmd_count` is CIM-visible under NetworkService; the bash worker under AppContainer takes the no-Windows-Docker-fallback path (dispatch.rs); the worker test-exe loads its DLLs under the Low-IL restricted token; and `dispatch.rs:604` `canonicalize(program_path)` producing a verbatim device-prefixed path in `fs_read_allow` is watched so a windows-public-dispatch surprise is not misread as a #A regression. Any watch-item surfacing as a new-layer failure is recorded honestly, not masked (REQ-native-r14/r6)."
    - "macOS is re-run for the sealed tuple (already 8/8-proven, re-confirmed on the ephemeral runner: alpine:3.19 present, all 8 targets genuine + green, macos-retained-directory green, no E0599) and the lib-test compile debt stays cleared on msvc; the wcore-agent/wcore-sandbox surfaces compile on x86_64-pc-windows-msvc (REQ-native-r9/r3/r4). Both platforms green with ordered per-target and final commit/tree/nonce markers bound to the exact re-sealed successor SHA; the UAT evidence ref is retained (deletion not authorized)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-53-SUMMARY.md
      provides: "Retained dual-OS native evidence bound to the exact re-sealed further-repaired-successor for the fresh native-inclusive review (20-54)"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-58-SUMMARY.md
      to: .planning/phases/20-transactional-delegated-mutation/20-53-SUMMARY.md
      via: "the re-sealed successor SHA is published to a temporary UAT ref + fresh dispatch branch and dispatched to native runners"
      pattern: "source_sha"
---

<objective>
Produce real native evidence for the re-sealed further-repaired-successor: one FRESH Sean-authorized native-proof dispatch that runs Windows (AppContainer-capable self-hosted msvc) and macOS (freshly re-registered local ephemeral) acceptance on hardware, re-proving the two 20-50 finding fixes (Finding 1 non-vacuous captured-PID reap on windows-hard-process-containment; Finding 2 granted-read exit-0 on windows-appcontainer-acl + windows-retained-handle confirming the tightened cmd classifier) AND the two 20-43 PRODUCTION root causes across ALL 6 Windows targets, exercising the Kimi watch-items as the next-layer checks, plus re-confirming macOS 8/8 for the sealed tuple, so the fresh 20-54 review can review native evidence rather than defer it.

Purpose: The audit requires the fresh review NOT to defer native (R12/R13); that is only possible if native evidence exists at review time. This is the re-dispatch of the native-proof gate for the further repaired successor — Sean-only (D6), with a fresh digest-specific authorization (the 95c81ec6, 17412cf2, daf27337, and any 3f839309-tied or f0dd5b6d-tied authorizations are spent/void), distinct from the terminal exact-tuple authorization at 20-56.
Output: Retained, candidate-bound dual-OS native evidence, or an explicit incomplete disposition; no requirement completion here.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md
@.planning/phases/20-transactional-delegated-mutation/20-50-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-58-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-59-SUMMARY.md
</context>

<execution_rules>
- Sean-only gate (CONTEXT D6): this plan dispatches a scarce native run. ALL prior native-proof-dispatch authorizations (95c81ec6, 17412cf2, daf27337, and any tied to the sealed-but-RED 3f839309 or the 20-52-blocked f0dd5b6d) are spent/void; a FRESH digest-specific authorization is required, bound to the exact re-sealed further-repaired-successor SHA.
- Dispatch discipline: fresh digest auth; a freshly re-registered LOCAL ephemeral Apple-silicon macOS runner; a fresh dispatch branch; single-attempt (no silent retries). The Windows leg runs on an AppContainer-capable self-hosted msvc client-SKU runner (REQ-native-r11), NOT a hosted runner.
- Before the run is spent, re-confirm the sanctioned macOS compile gate `cargo check -p wcore-sandbox --features live-docker --tests` is green on THIS Mac (fail-safe: blocks if it cannot run). This gate CANNOT compile the `#![cfg(windows)]` containment test or the `windows_impl` cmd classifier — their compile AND behavior are proven by THIS re-dispatch (CONTEXT D5/D7).
- Native proof is real-hardware only (CONTEXT D7); never claim it from cross-compilation or source inspection.
- The run MUST exercise ALL 6 Windows targets AND the 20-50 fix-proof items AND the Kimi watch-items:
  - Finding 1 (fix-proof): on windows-hard-process-containment, `active_process_cap_is_enforced` must observe a non-empty captured `choice`-PID set before job close and confirm every captured ProcessId gone after close (the reap check is genuinely non-vacuous on hardware and immune to concurrent `live_fs_acl` `choice.exe`); the sibling `job_close_reaps_detached_descendant_with_no_residue` and the other containment assertions pass.
  - Finding 2 (fix-proof): windows-appcontainer-acl + windows-retained-handle read the granted file (exit 0, not ERROR_INVALID_NAME), confirming the tightened `resolved_program_is_cmd` still routes real `cmd.exe` payloads through the cmd quoter; the optional inline classifier unit test (if added at 20-50) compiles + passes on msvc.
  - Kimi watch-items: git operations run against the de-verbatimized swarm_root; `tagged_cmd_count` CIM visibility under NetworkService; the bash worker under AppContainer + no-Windows-Docker-fallback in dispatch.rs; the worker test-exe DLL-load under the Low-IL restricted token; and `dispatch.rs:604` `canonicalize(program_path)` producing a `\\?\` path in `fs_read_allow` (audit-rated likely-benign but explicitly watched so a windows-public-dispatch surprise is not misread as a #A regression).
  macOS is already 8/8-proven but re-run for the sealed tuple. This plan writes no source; it dispatches and records evidence.
</execution_rules>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The re-sealed further-repaired-successor (20-58) with the two 20-45-panel finding fixes (Finding 1 non-vacuous captured-PID reap in the containment test; Finding 2 exact-final-component cmd classifier in command.rs), the 20-57 reap-observer fail-closed hardening, atop the two 20-43 PRODUCTION root-cause fixes and the counting-scope/compile-debt work, pre-native-cross-audited zero-finding by the full four-way panel (20-59). It is ready for the FRESH Sean-authorized dual-OS native-proof dispatch.</what-built>
  <how-to-verify>
    1. Confirm the sanctioned macOS compile gate is green on this Mac: `cargo check -p wcore-sandbox --features live-docker --tests` (blocks if it cannot run — no false green).
    2. Confirm the 20-59 cross-audit is a zero-finding PASS from ALL FOUR reviewers (Codex Sol + Gemini Pro + Kimi K3 + internal Claude adversarial), each with a schema-validated artifact, native deferred, the two 20-45 findings and the 20-52 reap-observer finding confirmed closed, and the Kimi watch-items carried.
    3. Authorize the FRESH native-proof dispatch bound to the EXACT re-sealed successor SHA/tree (all prior digest authorizations are spent/void). Provide fresh digest auth, a freshly re-registered LOCAL ephemeral Apple-silicon macOS runner, and the AppContainer-capable self-hosted msvc runner; single-attempt on a fresh dispatch branch.
    4. The run must exercise ALL 6 Windows targets, the 20-50 fix-proof items (Finding 1 non-vacuous captured-PID reap; Finding 2 granted-read exit-0), and the Kimi watch-items (incl. dispatch.rs:604), and re-run all 8 macOS targets for the sealed tuple.
  </how-to-verify>
  <resume-signal>Type "authorized: &lt;re-sealed-sha&gt;" to dispatch the fresh native run bound to that exact SHA, or describe why it is withheld.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Dispatch the fresh dual-OS native run and retain candidate-bound evidence</name>
  <files>None; this task dispatches to native runners and records retained evidence.</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-58-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-59-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-50-SUMMARY.md, .planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md</read_first>
  <behavior>
    - Under the fresh Sean authorization, publish the exact re-sealed successor SHA/tree to a temporary UAT ref + fresh dispatch branch and dispatch the Windows leg to the AppContainer-capable self-hosted msvc runner and the macOS leg to the freshly re-registered LOCAL ephemeral Apple-silicon runner (single attempt).
    - Windows leg re-proves the 20-50 finding fixes and the 20-43 root causes across ALL 6 targets with no onion-peel: Finding 1 — windows-hard-process-containment's `active_process_cap_is_enforced` captures a non-empty `choice`-PID set before job close and confirms every captured ProcessId gone after close (non-vacuous, concurrency-immune); Finding 2 — windows-appcontainer-acl + windows-retained-handle read the granted file exit 0 (not ERROR_INVALID_NAME); #A — windows-f20-lifecycle + windows-public-dispatch pass DispatchAdmission (de-verbatimized swarm_root + env-transport probe); #B — windows-job-object + windows-hard-process-containment run their fixed cmd payloads with tagged-tree containment counting. It also EXERCISES the Kimi watch-items (git vs de-verbatimized swarm_root; `tagged_cmd_count` CIM visibility under NetworkService; bash worker under AppContainer + no-Windows-Docker-fallback in dispatch.rs; worker test-exe DLL-load under Low-IL; dispatch.rs:604 canonicalize) and confirms the lib-test compile debt stays cleared on msvc (wcore-agent/wcore-sandbox compile on x86_64-pc-windows-msvc).
    - macOS leg re-runs all 8 targets for the sealed tuple: alpine:3.19 present, every docker target genuine + green, macos-retained-directory green, no E0599.
    - Any watch-item or fix-proof item that surfaces as a genuine next-layer failure is recorded honestly as an incomplete disposition (routing to a further repaired successor), never masked. On full green, retain the UAT evidence ref (deletion not authorized) with ordered per-target and final commit/tree/nonce markers bound to the exact re-sealed successor SHA.
  </behavior>
  <action>Under the fresh Sean authorization from the checkpoint, publish the exact re-sealed successor SHA/tree to a temporary UAT ref + fresh dispatch branch and dispatch the Windows leg (self-hosted msvc, AppContainer-capable) and macOS leg (freshly re-registered local ephemeral Apple-silicon), single attempt. Confirm ALL 6 Windows targets pass with no onion-peel — including the 20-50 fix-proof items (Finding 1: active_process_cap captures a non-empty choice-PID set before close and confirms every captured ProcessId gone after close; Finding 2: windows-appcontainer-acl + windows-retained-handle granted-read exit 0) — the #A/#B root-cause fixes hold, the Kimi watch-items (incl. dispatch.rs:604) are exercised, the msvc lib-test compile is clean, and all 8 macOS targets are green for the sealed tuple. Retain the UAT evidence ref with ordered per-target + final commit/tree/nonce markers bound to the exact SHA; record any watch-item or fix-proof next-layer failure honestly as an incomplete disposition. Implements REQ-native-r14/r11/r5/r7/r6/r2/r9/r3/r4; no requirement completion here.</action>
  <verify>
    <human-check>Windows: all 6 targets green on the self-hosted msvc runner with markers bound to the exact re-sealed SHA; the 20-50 fix-proof items (non-vacuous captured-PID reap; granted-read exit 0) hold; the Kimi watch-items (incl. dispatch.rs:604) are exercised. macOS: all 8 targets green on the local ephemeral runner. The UAT evidence ref is retained. Any next-layer failure is recorded as an explicit incomplete disposition.</human-check>
  </verify>
  <done>Retained dual-OS native evidence bound to the exact re-sealed further-repaired-successor exists for the 20-54 fresh native-inclusive review, or an explicit incomplete disposition routes to a further repaired successor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native authorization ← Sean digest gate | A scarce native run is dispatched only under a fresh, digest-specific Sean authorization bound to the exact re-sealed SHA; all prior authorizations are spent/void |
| native evidence integrity ← real hardware | Native proof must come from real Windows/macOS hardware, never cross-compilation or source inspection |
| finding-fix reality ← on-hardware exercise | Finding 1's non-vacuous reap and Finding 2's granted-read exit-0 must be exercised on real hardware, not asserted from source |
| candidate binding ← ordered markers | The retained evidence must carry ordered per-target + final commit/tree/nonce markers bound to the exact re-sealed SHA |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-53-01 | Spoofing | native proof from non-hardware source | high | mitigate | Proof is real-hardware only (CONTEXT D7); markers bind to the exact re-sealed SHA/tree, and the run is single-attempt on a fresh dispatch branch. |
| T-20-53-02 | Elevation of Privilege | stale/reused native authorization | high | mitigate | All prior digest authorizations (95c81ec6/17412cf2/daf27337 and any 3f839309-tied or f0dd5b6d-tied) are spent/void; a fresh digest-specific Sean authorization bound to the exact re-sealed SHA is required at the blocking-human checkpoint. |
| T-20-53-03 | Tampering | onion-peel / masked watch-item or fix-proof failure | medium | mitigate | The run must exercise ALL 6 Windows targets, the 20-50 fix-proof items, AND the Kimi watch-items; any next-layer failure is recorded honestly as an incomplete disposition routing to a further repaired successor, never rationalized. |
| T-20-53-04 | Spoofing | Finding 1 reap re-passing vacuously on hardware | high | mitigate | The native check requires the captured `choice`-PID set to be NON-EMPTY before close and every captured ProcessId confirmed gone after close, so a structurally-empty (vacuous) reap cannot register as a hardware PASS. |
</threat_model>

<verification>
The dual-OS native run under the fresh Sean authorization determines acceptance: all 6 Windows targets green on the self-hosted msvc runner (with the 20-50 fix-proof items holding, the Kimi watch-items exercised, and the msvc lib-test compile clean) and all 8 macOS targets green on the local ephemeral runner, with ordered per-target + final commit/tree/nonce markers bound to the exact re-sealed successor SHA and the UAT evidence ref retained. Record the re-sealed `source_sha`/tree, the fresh authorization identity, both legs' per-target results, the two fix-proof outcomes, the Kimi watch-item outcomes, the retained ref identity, and — on any next-layer failure — an explicit incomplete disposition, in `20-53-SUMMARY.md`. No source/test/workflow change; no requirement completion.
</verification>

<success_criteria>
- One fresh Sean-authorized dual-OS native run on real hardware (self-hosted msvc + local ephemeral macOS), bound to the exact re-sealed SHA, with all prior authorizations spent/void (r14/r11).
- All 6 Windows targets green — the 20-50 fix-proof items (Finding 1 non-vacuous captured-PID reap; Finding 2 granted-read exit-0) hold, the #A admission probe and #B containment fixes hold with no onion-peel, the Kimi watch-items (incl. dispatch.rs:604) are exercised, the msvc lib-test compile is clean (r5/r7/r6/r2/r3); all 8 macOS targets green for the sealed tuple (r9/r4).
- Retained candidate-bound native evidence exists for the 20-54 fresh native-inclusive review, or an explicit incomplete disposition routes to a further repaired successor.
</success_criteria>

## Artifacts this phase produces
- Retained dual-OS native evidence (real hardware) bound to the exact re-sealed further-repaired-successor SHA, or an explicit incomplete disposition.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-53-SUMMARY.md` using the standard GSD summary template. Record the re-sealed `source_sha`/tree, the fresh Sean authorization identity, both legs' per-target results, the two 20-50 fix-proof outcomes, the Kimi watch-item outcomes, the retained UAT ref identity, and the disposition (dual-OS green admits 20-54; any next-layer failure is an explicit incomplete routing to a further repaired successor). Complete no Phase 20 requirement.
</output>
</content>

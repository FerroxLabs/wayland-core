---
phase: 20-transactional-delegated-mutation
plan: "11"
type: execute
wave: 8
depends_on:
  - "20-10"
files_modified:
  - .planning/phases/20-transactional-delegated-mutation/20-06B-INDEPENDENT-REVIEW.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "A fresh executor that did not author 20-10 reviews the exact clean 06B source SHA."
    - "A linear stock-GSD metadata-only chain separates the source SHA from the review base; the review commit is its sole-parent child, changes only the review record, and preserves every reviewed source blob."
    - "Every severity is dispositioned and only zero findings qualifies 06B."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-06B-INDEPENDENT-REVIEW.md
      provides: "Exact-SHA non-author 06B interface review"
---

<objective>
Independently review the exact 06B containment packet before gate execution work starts.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-10-PLAN.md
@.planning/phases/20-transactional-delegated-mutation/20-10-SUMMARY.md
@crates/wcore-sandbox/src/lib.rs
@crates/wcore-sandbox/src/manifest.rs
@crates/wcore-sandbox/src/backends/mod.rs
@crates/wcore-sandbox/src/backends/appcontainer.rs
@crates/wcore-sandbox/src/backends/bwrap.rs
@crates/wcore-sandbox/src/backends/docker.rs
@crates/wcore-sandbox/src/backends/process_tree.rs
</context>

<admission>
- Require a clean isolated checkout at the end of the ordinary stock-GSD 20-10 metadata chain. Starting at the earlier `source_sha` recorded in `20-10-SUMMARY.md`, every intermediate commit must be sole-parent, merge-free, and change only `20-10-SUMMARY.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, or `.planning/REQUIREMENTS.md`; the summary must change exactly once.
- Before editing, capture the current commit/tree separately with `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-11)"` and `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-review-base-20-11)"`. Derive the exact source SHA from the summary, verify its tree, then exclusively capture both with `node .planning/scripts/task-base-authority.mjs capture "$(git rev-parse --git-path gsd-reviewed-source-20-11)" "$source" "$(git rev-parse "$source^{tree}")"`.
- Require a fresh executor identity distinct from the 20-10 executor; otherwise fail closed.
</admission>

<tasks>

<task type="auto">
  <name>06B-R: Prosecute containment authority and policy sufficiency</name>
  <files>.planning/phases/20-transactional-delegated-mutation/20-06B-INDEPENDENT-REVIEW.md</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-10-PLAN.md, .planning/phases/20-transactional-delegated-mutation/20-10-SUMMARY.md, crates/wcore-sandbox/src/lib.rs, crates/wcore-sandbox/src/manifest.rs, crates/wcore-sandbox/src/backends/mod.rs, crates/wcore-sandbox/src/backends/appcontainer.rs, crates/wcore-sandbox/src/backends/bwrap.rs, crates/wcore-sandbox/src/backends/docker.rs, crates/wcore-sandbox/src/backends/process_tree.rs, crates/wcore-config/src/shell.rs</read_first>
  <behavior>
    - Review consumer sufficiency, one-use opacity, live-probe/spawn identity binding, read-only/private-write-root policy, platform refusal semantics, descendant ownership, and complete failure closure.
    - Record all findings at every severity and never edit production source.
    - Write one schema-validated `wayland-core.phase20-independent-review.v1` JSON object with distinct source-executor/reviewer identities, exact source SHA/tree, PASS for the `f20-11` checks, zero findings at every severity, and executed command evidence; never self-reference the review commit SHA.
  </behavior>
  <action>Audit the exact 06B tree and its owned tests across BLOCKER, CRITICAL, HIGH, MEDIUM, and LOW. Write the 06B review record as schema-validated JSON matching `.planning/scripts/verify-review-result.mjs` profile `f20-11`. PASS only with zero findings; otherwise record FAIL and stop for a new 20-10 source successor.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-11)" .planning/phases/20-transactional-delegated-mutation/20-06B-INDEPENDENT-REVIEW.md</automated>
    <automated>base="$(node .planning/scripts/task-base-authority.mjs read "$(git rev-parse --git-path gsd-task-base-20-11)" | sed -n '1p')"; git diff --check "$base"..HEAD && git diff --cached --check && git diff --check</automated>
  </verify>
  <acceptance_criteria>The review is non-author, exact-source, all-severity, evidence-backed, and review-file-only. PASS requires zero findings.</acceptance_criteria>
  <done>The review-only commit exists for post-task exact-pair verification.</done>
</task>

</tasks>

<verification>
Before summary creation, derive `source` and `review_base` from the first lines returned by one no-follow read of their respective Git-private two-line authority objects, and derive `review=$(git rev-parse HEAD)`, then run `bash .planning/scripts/verify-review-pair.sh "$source" "$review_base" "$review" .planning/phases/20-transactional-delegated-mutation/20-10-SUMMARY.md .planning/phases/20-transactional-delegated-mutation/20-06B-INDEPENDENT-REVIEW.md crates/wcore-sandbox/src/lib.rs crates/wcore-sandbox/src/manifest.rs crates/wcore-sandbox/src/backends/mod.rs crates/wcore-sandbox/src/backends/appcontainer.rs crates/wcore-sandbox/src/backends/bwrap.rs crates/wcore-sandbox/src/backends/docker.rs crates/wcore-sandbox/src/backends/process_tree.rs` followed by `node .planning/scripts/verify-review-result.mjs "$review" .planning/phases/20-transactional-delegated-mutation/20-06B-INDEPENDENT-REVIEW.md "$source" "$(git rev-parse "${source}^{tree}")" f20-11`. Require both to pass and record `(source_sha, source_tree, review_base_sha, review_sha)` in `20-11-SUMMARY.md`; its later summary commit remains outside the tuple. No Cargo runs on macOS.
</verification>

<success_criteria>
- A distinct non-author reviewed the exact 06B source SHA.
- Sole-parent, review-only, and byte-identical-source proof passes.
- Only a zero-finding PASS admits 20-12.
</success_criteria>

<output>
Create `20-11-SUMMARY.md` with executor identity, source/review-base/review SHAs, exact path proof, disposition, and admission or rejection of 20-12.
</output>

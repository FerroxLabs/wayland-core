---
phase: 20-transactional-delegated-mutation
plan: "06"
type: execute
wave: 3
depends_on:
  - "20-01"
  - "20-02"
  - "20-03"
  - "20-04"
files_modified:
  - crates/wcore-swarm/src/worktree.rs
  - crates/wcore-swarm/src/worktree/candidate.rs
  - crates/wcore-swarm/src/worktree_tests.rs
autonomous: true
requirements:
  - F20-GATE-02
must_haves:
  truths:
    - "Only the accepted 20-03 live standalone-checkout capability can mint a non-serializable, non-cloneable CandidateSeal."
    - "The seal binds transaction, repository/object identity, base, head, tree, source manifest, clean state, and physical isolation and is revalidated before and after gates."
    - "This source packet makes no receipt, containment, acceptance, landing, or lifecycle claim."
  artifacts:
    - path: crates/wcore-swarm/src/worktree/candidate.rs
      provides: "Opaque live CandidateSeal and exact revalidation"
  key_links:
    - from: crates/wcore-swarm/src/worktree/candidate.rs
      to: crates/wcore-swarm/src/worktree.rs
      via: "seal minting remains owned by the live standalone checkout capability"
      pattern: "CandidateSeal"
---

<objective>
Build only the 20-06A candidate-seal source packet on the accepted 20-04 integration successor, using the standalone-checkout capability established by 20-03.

Purpose: establish an opaque, live, exactly revalidated candidate identity before containment or receipt work begins.
Output: one clean GSD source-plan completion HEAD for a separate non-author review plan.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-transactional-delegated-mutation/20-01-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-02-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-03-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-04-SUMMARY.md
@crates/wcore-swarm/src/worktree.rs
@crates/wcore-swarm/src/worktree_security.rs
@crates/wcore-swarm/src/worktree_tests.rs
@crates/wcore-types/src/child_transaction.rs
</context>

<admission>
- Do not execute until 20-03 and the replayed 20-04 successor are accepted, integrated, clean, and named by exact commit in their summaries.
- Require a clean isolated worker checkout. Before reading or editing source, record `git rev-parse HEAD` at `$(git rev-parse --git-path gsd-task-base-20-06)`.
- This plan has exactly one source task. The source task commit SHA, captured before the ordinary GSD summary commit, is the exact source SHA reviewed by 20-09. The summary is a separately permitted metadata-only successor and never defines or rewrites its own source identity.
</admission>

<tasks>

<task type="auto">
  <name>06A: Bind a CandidateSeal to the live standalone checkout</name>
  <files>crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/src/worktree/candidate.rs, crates/wcore-swarm/src/worktree_tests.rs</files>
  <read_first>crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/src/worktree_security.rs, crates/wcore-swarm/src/worktree_tests.rs, crates/wcore-types/src/child_transaction.rs, .planning/phases/20-transactional-delegated-mutation/20-03-SUMMARY.md</read_first>
  <behavior>
    - `CandidateSeal` is opaque, non-serializable, non-cloneable, and non-reconstructible from paths, serialized fields, or caller-provided hashes.
    - Only the accepted 20-03 live standalone-checkout capability can mint it.
    - It binds transaction/opening identity, canonical checkout identity, independent Git common directory and physical object storage, base, head, tree, source/path manifest digest, clean state, and isolation invariants.
    - Creation and revalidation use fixed parent-owned Git plumbing with inherited/system/global configuration, replace objects, hooks, filters, child argv, refspec, environment, executable, and pathname authority disabled.
    - Before/after revalidation rejects dirty or drifting source, ambiguous or foreign repository state, shared metadata or objects, aliases, alternates, replace refs, parent remotes, linked-worktree metadata, disallowed Git configuration, hooks/filters, and symlink escape.
    - Failure injection remains crate-private and covers inspection failure, source drift, repository substitution, aliasing, poisoning, cleanup, and seal lifetime.
  </behavior>
  <action>First require a clean checkout and record the exact starting HEAD in `$(git rev-parse --git-path gsd-task-base-20-06)`. Extend the accepted 20-03 standalone-checkout owner with the minimum opaque CandidateSeal and before/after revalidation operations. Reuse accepted 20-03 protected Git and physical-isolation primitives. Do not create signing, generic evidence, receipt, containment, acceptance, CAS, or landing infrastructure.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-06)" crates/wcore-swarm/src/worktree.rs crates/wcore-swarm/src/worktree/candidate.rs crates/wcore-swarm/src/worktree_tests.rs</automated>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-swarm/src/worktree.rs crates/wcore-swarm/src/worktree/candidate.rs crates/wcore-swarm/src/worktree_tests.rs</automated>
    <automated>base="$(cat "$(git rev-parse --git-path gsd-task-base-20-06)")"; git diff --check "$base"..HEAD && git diff --cached --check && git diff --check</automated>
  </verify>
  <acceptance_criteria>The complete committed/staged/unstaged/untracked/deleted/renamed/copied scope union equals the three declared source paths. No public/caller constructor, serde implementation, path-only reconstruction, or clone path exists. Owned tests name minting, exact revalidation, inspection failures, drift, substitution, aliasing, poisoning, cleanup, and lifetime cases. No compilation or runtime claim is made on macOS.</acceptance_criteria>
  <done>One exact clean source packet is committed; the plan summary records the already-existing source task commit SHA for independent review by 20-09.</done>
</task>

</tasks>

<verification>
Before writing the summary, derive `base=$(cat "$(git rev-parse --git-path gsd-task-base-20-06)")` and `source=$(git rev-parse HEAD)`. Require a clean tree; require `source` to be the source task commit and descend from `base`; rerun `.planning/scripts/verify-task-scope.sh` against the same exact three paths so the committed `base..source` view is included; and record both existing SHAs in `20-06-SUMMARY.md`. The later summary commit may change only `20-06-SUMMARY.md` and is not part of source identity. No Cargo command runs on macOS.
</verification>

<success_criteria>
- The source packet is exact, clean, ownership-complete, and ready for a distinct non-author review.
- The plan does not mark 06A construction-qualified or behaviorally accepted.
</success_criteria>

<output>
After completion, create `.planning/phases/20-transactional-delegated-mutation/20-06-SUMMARY.md` containing `task_base`, the already-existing source task commit as `source_sha`, exact changed paths, checks run, explicit non-claims, and readiness for 20-09 only. Never name the summary commit as `source_sha`.
</output>

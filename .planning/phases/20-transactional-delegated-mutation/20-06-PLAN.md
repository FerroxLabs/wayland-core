---
phase: 20-transactional-delegated-mutation
plan: "06"
type: execute
wave: 3
depends_on:
  - "20-01"
  - "20-02"
  - "20-03"
  - "20-04"
files_modified:
  - crates/wcore-agent/src/child_transaction.rs
  - crates/wcore-agent/src/child_transaction/gate_executor.rs
  - crates/wcore-agent/src/child_transaction/gates.rs
  - crates/wcore-sandbox/src/lib.rs
  - crates/wcore-sandbox/src/backends/mod.rs
  - crates/wcore-sandbox/src/backends/appcontainer.rs
  - crates/wcore-sandbox/src/backends/bwrap.rs
  - crates/wcore-sandbox/src/backends/docker.rs
  - crates/wcore-swarm/src/worktree.rs
  - crates/wcore-swarm/src/worktree/candidate.rs
  - crates/wcore-sandbox/tests/hard_process_containment.rs
  - crates/wcore-agent/tests/child_transaction_gate_test.rs
  - crates/wcore-agent/tests/child_transaction_gate_execution_test.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Gate acceptance is bound to the exact transaction, standalone checkout, base, head, tree, and source bytes that were tested."
    - "Only a parent-owned executor can run pre-authorized gate closures and derive gate receipts from observed process results; child or caller claims cannot mint acceptance."
    - "This parent-owned gate is the sole authority that can make a selected candidate eligible for landing; Anvil evaluation/stability gates and model/candidate claims remain advisory and cannot mint that state."
    - "A failed, stale, incomplete, malformed, or mismatched gate can never make a candidate mergeable."
    - "Every denial preserves structured diagnostics in the durable transaction lifecycle."
  artifacts:
    - path: crates/wcore-swarm/src/worktree/candidate.rs
      provides: "Exact standalone-checkout candidate identity and drift checks"
    - path: crates/wcore-agent/src/child_transaction/gates.rs
      provides: "Focused-gate state machine and acceptance validation"
    - path: crates/wcore-agent/src/child_transaction/gate_executor.rs
      provides: "Parent-owned authorized gate execution and observed-result receipt construction"
    - path: crates/wcore-agent/tests/child_transaction_gate_test.rs
      provides: "Hostile stale, malformed, failed, and mismatched gate tests"
    - path: crates/wcore-agent/tests/child_transaction_gate_execution_test.rs
      provides: "Spoofed-result, trivial-plan, command, environment, and candidate-drift execution tests"
  key_links:
    - from: crates/wcore-agent/src/child_transaction/gates.rs
      to: crates/wcore-swarm/src/worktree/candidate.rs
      via: "candidate identity validation before gate acceptance"
      pattern: "Candidate"
    - from: crates/wcore-agent/src/child_transaction/gates.rs
      to: crates/wcore-agent/src/child_transaction.rs
      via: "durable receipt commit and diagnostics"
      pattern: "ChildTransactionStore"
    - from: crates/wcore-agent/src/child_transaction/gate_executor.rs
      to: crates/wcore-agent/src/child_transaction/gates.rs
      via: "opaque observed gate result is the only acceptance input"
      pattern: "Observed"
    - from: crates/wcore-agent/src/child_transaction/gate_executor.rs
      to: crates/wcore-sandbox/src/lib.rs
      via: "hard-containment-required sandbox execution for every authorized gate"
      pattern: "hard_process_containment"
---

<objective>
Define and enforce the exact candidate and gate state that permits a child transaction to approach integration.

Purpose: A green command is not authority unless it ran against the exact isolated candidate and satisfies the declared gate set.
Output: Candidate identity, parent-owned gate executor, gate validator/state machine, and hostile gate tests.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-01-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-02-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-03-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-04-SUMMARY.md
@crates/wcore-agent/src/child_transaction.rs
@crates/wcore-swarm/src/worktree.rs
@crates/wcore-sandbox/src/lib.rs
@crates/wcore-sandbox/src/backends/mod.rs
@crates/wcore-sandbox/src/backends/process_tree.rs
@crates/wcore-sandbox/tests/backend_integration.rs
</context>

<salvage>
None. Build this plan fresh from the accepted predecessor outputs; no historical commit may be cherry-picked as gate authority.
</salvage>

<tasks>

<task type="auto">
  <name>Task 1: Derive exact candidate identity from the owned standalone checkout</name>
  <files>crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/src/worktree/candidate.rs</files>
  <read_first>crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/src/worktree_tests.rs, crates/wcore-types/src/child_transaction.rs</read_first>
  <behavior>
    - Candidate identity is derived from Git and filesystem state, never accepted from child prose.
    - Transaction ID, standalone repository path, independent Git common directory and physical object storage, base commit, head commit, tree, and clean-state checks all agree.
    - Before and after every gate, validation re-proves the full isolation invariant: no common-directory/object hardlink alias, alternates, replace refs, parent remote, linked-worktree metadata, disallowed Git configuration, hook execution surface, symlink escape, or foreign checkout.
    - Object and source inspection uses fixed parent-owned Git plumbing with inherited/system/global configuration disabled, replace-object lookup disabled, hooks and filters disabled, and no child-supplied argv, refspec, environment, or executable.
    - Any post-test source drift, dirty state, ambiguous repository, poisoned metadata, shared parent metadata/object identity, or foreign checkout fails validation.
  </behavior>
  <action>Add a candidate module beneath the existing checkout owner. Build identity only from the open owned standalone repository through parent-owned filesystem inspection and fixed safe-argv Git plumbing. Parse and validate repository metadata as hostile data; reject object hardlink aliases, alternates, replace refs, parent remotes, linked-worktree metadata, disallowed local configuration, and hook/filter execution surfaces. Run Git with a scrubbed environment and explicit replace/config/hook/filter disabling, then re-prove the complete invariant before and after every gate. Do not add receipt signing or a parallel evidence framework.</action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, and every changed path is declared by this task. This does not assert compilation or behavioral acceptance; those are established only by the plan-level clean-HEAD Hetzner gates.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

<task type="auto">
  <name>Task 2: Execute authorized gates and accept only observed results</name>
  <files>crates/wcore-agent/src/child_transaction.rs, crates/wcore-agent/src/child_transaction/gate_executor.rs, crates/wcore-agent/src/child_transaction/gates.rs, crates/wcore-sandbox/src/lib.rs, crates/wcore-sandbox/src/backends/mod.rs, crates/wcore-sandbox/src/backends/appcontainer.rs, crates/wcore-sandbox/src/backends/bwrap.rs, crates/wcore-sandbox/src/backends/docker.rs</files>
  <read_first>crates/wcore-agent/src/child_transaction.rs, crates/wcore-agent/src/session_journal/model.rs, crates/wcore-swarm/src/worktree/candidate.rs, crates/wcore-types/src/child_transaction.rs, crates/wcore-sandbox/src/lib.rs, crates/wcore-sandbox/src/manifest.rs, crates/wcore-sandbox/src/backends/mod.rs, crates/wcore-sandbox/src/backends/process_tree.rs, crates/wcore-config/src/shell.rs</read_first>
  <behavior>
    - A mutating transaction requires a nonempty, pre-authorized `ChildGatePlan`; each closure digest resolves only through a parent-owned registry to fixed argv, timeout, sanitized environment policy, exact standalone CWD semantics, and pinned transitive-input identity.
    - The executor revalidates the exact candidate immediately before and after each gate, runs only safe argv without a shell through a containment-required `SandboxRegistry`, bounds stdout bytes, stderr bytes, and elapsed time, owns and reaps the complete descendant process tree on success and every failure path, and derives outcome, exit code, evidence digest, and diagnostics exclusively from the observed child process.
    - The gate sandbox grants writes only to the exact standalone checkout and transaction-private gate scratch, grants only the pinned read-only toolchain closure, denies network and inherited credentials/configuration, and cannot honor Dangerous/no-sandbox authority.
    - The selected backend must explicitly advertise hard descendant containment and pass a semantic live containment probe bound to the exact executable/runtime identity used for the gate. Bubblewrap PID namespaces, Docker containers, and Windows AppContainer Job Objects may qualify only when that probe succeeds; PATH presence, socket presence, caller claims, macOS sandbox-exec, no-sandbox, fail-closed stubs, and ordinary Unix process groups do not qualify. Identity drift between probe and spawn refuses execution.
    - Timeout, cancellation, output overflow, capture/read/wait failure, and containment-establishment failure terminate the entire gate process tree and fail closed; no descendant residue may survive.
    - Only an opaque observed-result value constructible inside the executor module may enter the acceptance state machine. Serialized `ChildGateReceipt`, child text, caller-supplied status, injected success, missing commands, unknown closures, and trivial/empty plans cannot mint acceptance.
    - Anvil `EvaluationGateReport`, `ClimbOutcome`, verified stamps, journals, receipts, loser identities, and model/candidate claims are caller-supplied advisory data, never acceptance inputs. The parent independently resolves and reruns its authorized nonempty gate plan against only the opaque selected-candidate identity.
    - All declared required gates must finish successfully against the same exact candidate.
    - Missing, duplicate-conflicting, out-of-order, post-terminal, or candidate-mismatched results fail closed.
    - Missing proof-host capability, live-probe failure, identity drift, or any gate failure records explicit backend/command/status diagnostics and cannot transition the transaction to mergeable; an unavailable hard backend is a loud refusal, never a skip.
  </behavior>
  <action>Create a parent-owned gate executor plus a small acceptance state machine tied to the durable child transaction. Preserve the unique AppContainer profile, ACL lease, and crash-recovery semantics established by 20-02 while adding an object-safe hard-descendant-containment capability to `SandboxBackend`; default it to false, expose it through `SandboxRegistry`, and opt in only the implementations that actually provide a kernel/container boundary: Bubblewrap PID namespace, Docker container, and Windows AppContainer Job Object. A qualifying backend must run a semantic live containment probe, derive the executable/runtime identity from parent-observed state, bind that identity to the gate attempt, and revalidate it at spawn; a PATH entry, socket, configured capability, fabricated probe response, or replaced backend is insufficient. Gate execution must reject unavailable, bypassing, identity-drifted, or non-hard backends before candidate code starts. Resolve each sealed closure by digest from trusted runtime configuration, build fixed argv with `wcore_config::shell::shell_command_argv`, construct a network-denied and credential-denied `SandboxManifest` bound to the exact checkout plus transaction-private scratch and pinned read-only toolchain inputs, and execute through the containment-required registry. Keep output and elapsed-time bounds and cancellation cleanup at this boundary; do not call the trusted-utility `capture_bounded_process` path for candidate-controlled code. Introduce a crate-private `GateProcessRunner` seam whose production implementation is the hard sandbox adapter. Keep its scripted deterministic failure tests inside the crate's `#[cfg(test)]` unit-test module so no external integration test depends on crate-private API; external tests exercise only the public transaction behavior and the real hard-sandbox fixture. Treat spawn, probe, identity, containment, read, wait, cancellation, timeout, and output-limit errors as terminal fail-closed cleanup. Construct receipt/evidence internally from observed results, accept only the executor's module-private opaque result, validate gate IDs/order and exact candidate identity at every transition, persist terminal diagnostics, and expose `accepted_candidate` only after the complete nonempty plan succeeds.</action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, every changed path is declared, the backend capability defaults false, only the three hard-containment backends opt in, and the owned executor requires the hard sandbox adapter rather than trusted non-sandboxed process capture. Compilation and behavior remain plan-level claims.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

<task type="auto">
  <name>Task 3: Attack gate execution, ordering, identity, and diagnostics</name>
  <files>crates/wcore-agent/tests/child_transaction_gate_execution_test.rs, crates/wcore-agent/tests/child_transaction_gate_test.rs, crates/wcore-sandbox/tests/hard_process_containment.rs</files>
  <read_first>crates/wcore-agent/src/child_transaction/gate_executor.rs, crates/wcore-agent/src/child_transaction/gates.rs, crates/wcore-swarm/src/worktree/candidate.rs, crates/wcore-agent/tests/child_transaction_store_test.rs, crates/wcore-sandbox/src/backends/process_tree.rs, crates/wcore-sandbox/tests/backend_integration.rs</read_first>
  <behavior>
    - Tests reject empty/trivial plans, unknown or mismatched closure digests, shell-string injection, inherited hostile environment, wrong CWD, forged serialized receipts, child-spoofed success, timeout/cancellation, output overflow, capture failure, containment failure, and candidate drift during execution.
    - Success, nonzero exit, timeout, cancellation, output overflow, capture/read/wait failure, and external interruption each leave no live descendant process or owned process-capture residue.
    - On Unix, a fixture that calls `setsid`/`setpgid` and spawns a sentinel descendant is either killed by a qualifying hard sandbox or refused before spawn; a plain process-group backend can never produce acceptance.
    - Fabricated backend capability or probe output, PATH-only Bubblewrap availability, socket-only Docker availability, backend executable replacement after probing, Dangerous/no-sandbox selection, macOS sandbox-exec selection, network access, host/global-temp writes, credential/config reads, and scratch substitution fail closed.
    - Tests reject stale head/tree, dirty source, foreign transaction, newly introduced alternates/replace refs/remotes/config/hooks/object aliases, missing gate, conflicting duplicate, reorder, and post-terminal result.
    - Forged Anvil verified stamps, green evaluation reports, journals, receipts, loser identities, and unselected candidate identities cannot advance acceptance; even the selected candidate must pass the independently rerun parent gate plan.
    - A failed gate leaves the parent workspace unchanged and preserves the exact failure diagnostic.
    - Only a complete, ordered gate set for the current candidate becomes accepted.
  </behavior>
  <action>Write black-box integration tests for every hostile execution and transition plus one successful multi-gate acceptance. Put deterministic scripted `GateProcessRunner` coverage for spawn, probe, identity, containment, read, wait, cancellation, timeout, and output-limit results in the crate's internal unit-test module; keep the external integration tests limited to public transaction behavior and the real hard-sandbox descendant-residue fixture. Make the Unix fixture escape its original process group through `setsid`/`setpgid`, spawn a sentinel, and prove a qualifying backend removes it; where no hard backend is available, prove refusal occurs before the fixture starts. Attempt capability spoofing, fabricated probe success, PATH-only Bubblewrap and socket-only Docker availability, executable replacement between probe and spawn, Dangerous/no-sandbox and sandbox-exec use, network/credential/global-temp access, scratch substitution, passing-receipt injection without execution, harmless-command substitution, output-spoofed success, shell metacharacters, hostile inherited environment, and candidate drift. Separately poison Git config, alternates, replace refs, hooks, remote URLs, common-directory metadata, and object identity between checks. Prove only observed registered commands can advance state, failures preserve durable diagnostics, and parent bytes/refs/objects remain unchanged.</action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, every changed path is declared, and the owned tests name every hostile gate and descendant-residue case described above. Test and Clippy results remain plan-level claims.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>Child-controlled source and test output cross into the authoritative transaction and future integration decision.</trust_boundaries>
<threats>Stale-green reuse, head/tree substitution, dirty-checkout drift, shared parent Git metadata or object aliases, alternates/replace-ref/config/hook/filter poisoning, child/caller receipt forgery, arbitrary or substituted green commands, shell/environment/CWD injection, empty plans, missing gates, conflicting duplicates, reordered events, and lost failure diagnostics.</threats>
<mitigations>Parent-owned closure registry and executor, fixed argv and sanitized environment, hard-containment capability default-deny, qualifying sandbox-only execution, network/credential/global-temp denial, transaction-private scratch, observed-result-only receipt construction, module-private acceptance input, bounded capture, parent-owned sanitized Git inspection, physical object and metadata independence checks, complete before/after isolation revalidation, nonempty declared gate sets, fail-closed state transitions, durable diagnostics, deterministic failure injection, and escaped-session residue tests.</mitigations>
</threat_model>

<verification>
Each task first passes its pre-commit construction gate on the Mac and becomes a provisional GSD task commit. After every task commit exists and the checkout is clean, the following exact committed-HEAD Hetzner gates determine plan acceptance. A construction commit is not behavioral evidence:
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-candidate "$(pwd -P)" test -p wcore-swarm --lib worktree</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-gates "$(pwd -P)" test -p wcore-agent --test child_transaction_gate_execution_test --test child_transaction_gate_test</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-containment "$(pwd -P)" test -p wcore-sandbox --test hard_process_containment</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-clippy "$(pwd -P)" clippy -p wcore-agent -p wcore-swarm -p wcore-sandbox --all-targets --all-features -- -D warnings</automated>
This plan produces product gate state; it does not introduce a custom proof or receipt system.
</verification>

<success_criteria>
- Only parent-observed execution of one nonempty authorized gate plan over one exact unchanged candidate can pass.
- The selected proof host demonstrates a live qualifying hard backend or the plan fails loudly without accepting or skipping the candidate.
- Advisory Anvil gates, child output, and serialized receipts cannot create parent landing eligibility.
- Every failed or malformed transition remains non-mergeable and diagnosable.
- Parent state remains unchanged.
- Focused tests and strict Clippy pass on Hetzner.
</success_criteria>

## Artifacts this phase produces

- Git-derived transaction candidate identity.
- Parent-owned authorized gate executor and observed-result receipt construction.
- Fail-closed focused gate state machine.
- Hostile gate lifecycle integration tests.

<output>
After completion, create `.planning/phases/20-transactional-delegated-mutation/20-06-SUMMARY.md` using the standard GSD summary template with exact commits, test commands, and results.
</output>

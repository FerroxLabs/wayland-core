---
phase: 20-transactional-delegated-mutation
plan: "06"
type: execute
wave: 3
depends_on:
  - "20-01"
  - "20-02"
  - "20-03"
  - "20-04"
files_modified:
  - .planning/phases/20-transactional-delegated-mutation/20-06-INTERFACE-REVIEWS.md
  - crates/wcore-swarm/src/worktree.rs
  - crates/wcore-swarm/src/worktree/candidate.rs
  - crates/wcore-swarm/src/worktree_tests.rs
  - crates/wcore-sandbox/src/lib.rs
  - crates/wcore-sandbox/src/manifest.rs
  - crates/wcore-sandbox/src/backends/mod.rs
  - crates/wcore-sandbox/src/backends/appcontainer.rs
  - crates/wcore-sandbox/src/backends/bwrap.rs
  - crates/wcore-sandbox/src/backends/docker.rs
  - crates/wcore-sandbox/src/backends/process_tree.rs
  - crates/wcore-agent/src/child_transaction.rs
  - crates/wcore-agent/src/child_transaction/gate_executor.rs
  - crates/wcore-agent/src/child_transaction/gates.rs
  - crates/wcore-agent/src/spawner.rs
  - crates/wcore-agent/src/spawner/durable_launch.rs
  - crates/wcore-agent/src/spawner/mutation_workspace.rs
  - crates/wcore-agent/src/session_journal/model.rs
  - crates/wcore-agent/src/session_journal/reducer.rs
  - crates/wcore-sandbox/tests/hard_process_containment.rs
  - crates/wcore-agent/tests/child_transaction_gate_test.rs
  - crates/wcore-agent/tests/child_transaction_gate_execution_test.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Gate acceptance is bound to an opaque seal derived from the live owned standalone checkout and its exact transaction, repository/object identity, base, head, tree, source bytes, and clean state; serialized or caller-supplied identity cannot substitute for that seal."
    - "Only a parent-owned executor holding live hard-containment authority can run a nonempty pre-authorized gate plan and derive results from observed process execution."
    - "The exact raw canonical receipt bytes are durably appended and replayed under transaction authority before an opaque AcceptedCandidate can exist; a caller-built receipt, model claim, or advisory Anvil result cannot mint acceptance."
    - "AcceptedCandidate owns the still-armed MutationAttemptGuard. Drop, cancellation, failure, or any incomplete handoff durably terminalizes the transaction and cleans only its owned workspace."
    - "Gate execution sees the sealed candidate checkout read-only and receives only transaction-private writable scratch/build/cache paths; hard containment, network denial, and credential denial cannot be bypassed."
    - "A failed, stale, incomplete, malformed, reordered, duplicated, post-terminal, or mismatched gate remains non-landing and durably diagnosable."
  artifacts:
    - path: crates/wcore-swarm/src/worktree/candidate.rs
      provides: "Opaque live CandidateSeal plus exact standalone-checkout revalidation"
    - path: crates/wcore-sandbox/src/lib.rs
      provides: "Opaque live HardContainmentAuthority minted and consumed by SandboxRegistry"
    - path: crates/wcore-sandbox/src/manifest.rs
      provides: "Read-only candidate and transaction-private writable gate policy"
    - path: crates/wcore-agent/src/child_transaction/gates.rs
      provides: "Guard-owned AcceptedCandidate and fail-closed gate acceptance state machine"
    - path: crates/wcore-agent/src/child_transaction/gate_executor.rs
      provides: "Parent-owned execution, raw receipt construction, commit, replay, and acceptance closure"
    - path: crates/wcore-agent/tests/child_transaction_gate_execution_test.rs
      provides: "Hostile authority, raw-receipt, containment, checkout-write, and cleanup tests"
    - path: .planning/phases/20-transactional-delegated-mutation/20-06-INTERFACE-REVIEWS.md
      provides: "Fresh independent exact-SHA interface-review checklists and dispositions for construction qualification"
  key_links:
    - from: crates/wcore-agent/src/child_transaction/gate_executor.rs
      to: crates/wcore-swarm/src/worktree/candidate.rs
      via: "consume and revalidate the live CandidateSeal before and after every gate"
      pattern: "CandidateSeal"
    - from: crates/wcore-agent/src/child_transaction/gate_executor.rs
      to: crates/wcore-sandbox/src/lib.rs
      via: "consume live HardContainmentAuthority at the exact gate spawn"
      pattern: "HardContainmentAuthority"
    - from: crates/wcore-agent/src/child_transaction/gate_executor.rs
      to: crates/wcore-agent/src/child_transaction.rs
      via: "append exact internally constructed receipt bytes and reopen/reduce them before acceptance"
      pattern: "ChildTransactionStore"
    - from: crates/wcore-agent/src/child_transaction/gates.rs
      to: crates/wcore-agent/src/spawner/mutation_workspace.rs
      via: "AcceptedCandidate owns the original still-armed MutationAttemptGuard"
      pattern: "MutationAttemptGuard"
---

<objective>
Make one exact delegated-mutation candidate eligible for Phase 20-07 landing only after parent-observed gates run under live hard containment and the exact authoritative receipt is durably committed and replayed.

Purpose: Close the authority gaps between standalone checkout ownership, sandbox execution, durable receipt truth, and terminal cleanup without introducing another evidence framework.
Output: Opaque candidate seal, opaque hard-containment authority, read-only gate policy, parent-owned gate executor, guard-owned AcceptedCandidate, durable acceptance closure, and hostile tests.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-01-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-02-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-03-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-04-SUMMARY.md
@crates/wcore-agent/src/child_transaction.rs
@crates/wcore-agent/src/spawner.rs
@crates/wcore-agent/src/spawner/durable_launch.rs
@crates/wcore-agent/src/spawner/mutation_workspace.rs
@crates/wcore-agent/src/session_journal/model.rs
@crates/wcore-agent/src/session_journal/reducer.rs
@crates/wcore-swarm/src/worktree.rs
@crates/wcore-sandbox/src/lib.rs
@crates/wcore-sandbox/src/manifest.rs
@crates/wcore-sandbox/src/backends/mod.rs
</context>

<admission_and_execution_order>
- Do not start 06A or 06B until the repaired 20-03 successor is accepted, integrated, clean, and recorded by exact commit in `20-03-SUMMARY.md`. Current or historical path-only checkout APIs are not an admissible base.
- Replay and independently accept the sealed 20-04 candidate `a0d27dd4bd7ddf02baf16aebb45dbe1239ec4dc3` against that stable 20-03 successor. Do not treat its current sealed/unaccepted status as dependency satisfaction.
- Stock GSD executes this plan serially. Start 06A only after predecessor and formatting admission; start 06B only after 06A is `CONSTRUCTION-QUALIFIED`; start 06C only after 06B is `CONSTRUCTION-QUALIFIED`. No sibling task in this plan is advertised or executed as parallel work.
- 06A owns `wcore-swarm`; 06B owns `wcore-sandbox`; both append only their own exact-SHA review section to `20-06-INTERFACE-REVIEWS.md`. Neither may edit any `Cargo.toml`, `Cargo.lock`, shared schema/configuration, codegen output, or toolchain file. A newly discovered shared seam requires a plan revision and serial ownership before work continues.
- `CONSTRUCTION-QUALIFIED` means the packet's changed-file formatting, diff hygiene, declared ownership, source-level negative checks, and independent interface review are green against its exact commit. It does not mean the code compiles, runs, contains processes, commits receipts, cleans resources, or is accepted behavior.
- `CONSTRUCTION-QUALIFIED` additionally requires a fresh independent reviewer who did not author that packet to review its exact source SHA, complete the packet checklist, assign a PASS/FAIL disposition with all findings, and append the reviewer identity, exact packet source SHA, checklist, findings, disposition, and separate review-record commit SHA to `.planning/phases/20-transactional-delegated-mutation/20-06-INTERFACE-REVIEWS.md`. The builder first commits the source packet and stops; the reviewer then records the review in a distinct review-only descendant commit so recording evidence cannot change the SHA under review. Qualification is the pair `(packet source SHA, review-record commit SHA)`. A stale, author-written, SHA-mismatched, source-mixed, missing, or incomplete review cannot qualify the packet.
- 06C starts only after sequentially qualified 06A then 06B and behaviorally accepted replayed 20-04. It consumes the exact predecessor interfaces; it must not recreate transaction/workspace authority from paths.
- 06D starts only after the exact 06C commit passes its mandatory private hostile library gate described in 06C. 06D owns black-box tests only. Integration remains serial in dependency order.
- Only one exact clean integrated 06A+06B+06C+06D HEAD can become `BEHAVIORALLY-ACCEPTED`, and only after every remote gate, the repository-wide formatting gate, strict Clippy check, Windows GNU check, and independent all-severity audit below passes for that exact HEAD.
</admission_and_execution_order>

<known_baseline>
Commit `94f014d039b8babf3f5926385a3bbc5cb5cf3c41` historically demonstrates that repository-wide formatting was red, but historical failure is not an admissible exception or comparison baseline. Immediately after the exact 20-03/20-04 predecessor is frozen and before 06A starts, run the following committed-HEAD Hetzner gate:
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-fmt-admission "$(pwd -P)" fmt --all -- --check</automated>
The command must exit zero. Any nonzero result makes 20-06 admission RED. Formatting cleanup must occur as a separately scoped and authorized predecessor change, be reviewed and integrated, and then the admission command must be rerun against its exact successor before 06A begins. No 20-06 task may absorb unrelated formatting cleanup, and no raw nonzero formatting result is an acceptance gate or accepted follow-up.
</known_baseline>

<proof_host_admission>
Before behavioral proof, the exact Hetzner proof host must pass the root integration test `qualified_hard_containment_backend_preflight` in the exact `wcore-sandbox` package, integration-test kind, and `hard_process_containment` binary. The test semantically probes the exact Bubblewrap or Docker executable/runtime and policy path used by the positive test; PATH/socket/config presence is insufficient. Its body must contain production-derived assertions that a qualifying backend was observed, its executable/runtime/policy identity matched the subsequent spawn authority, and the expected refusal controls remained active. It must not convert missing qualification, an early return, or a conditional no-op into success. Refusal tests remain required but cannot replace this positive proof.
</proof_host_admission>

<targeted_test_evidence>
Every targeted nextest gate below is a two-receipt proof against one exact committed HEAD. The first receipt is `nextest list --message-format json` and must contain exactly one selected, nonignored test with the declared exact package, kind, binary, and fully qualified test identity, and no second selected test. The second receipt uses the identical equality filter with `--ignore-default-filter --run-ignored default --no-tests fail --retries 0`; it must exit zero and report exactly that one test as PASS. Missing, ignored, skipped, filtered-out, retried, additional, or differently named results are RED. `test(=...)`, `package(=...)`, `kind(=...)`, and `binary(=...)` are mandatory; contains matchers such as `test(name)` are forbidden.

The selection flags prove only which nonignored test executed. They cannot prove that the test body is meaningful and cannot detect a body that returns early. The independent audit must therefore inspect the exact test source SHA and confirm that each named test reaches and asserts its production-derived hostile or positive outcome with no environment-dependent success return. The two positive host tests must assert the actual qualifying-backend identity and every named `CandidateSeal`-to-`AcceptedCandidate` transition respectively. A list/run receipt without those source assertions is not acceptance evidence.
</targeted_test_evidence>

<salvage>
The 20-04 candidate at `a0d27dd4bd7ddf02baf16aebb45dbe1239ec4dc3` is input for replay and reconciliation only. Preserve its useful `MutationAttemptGuard` and `ExecutedDurableLaunch::into_mutation_handoff()` design if they survive accepted-predecessor rebase and audit. Do not cherry-pick it blindly, claim it accepted, reconstruct its opaque authority from paths, or salvage any historical gate-acceptance implementation.
</salvage>

<tasks>

<task type="auto">
  <name>06A: Seal the exact live standalone candidate</name>
  <files>crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/src/worktree/candidate.rs, crates/wcore-swarm/src/worktree_tests.rs, .planning/phases/20-transactional-delegated-mutation/20-06-INTERFACE-REVIEWS.md</files>
  <read_first>crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/src/worktree_security.rs, crates/wcore-swarm/src/worktree_tests.rs, crates/wcore-types/src/child_transaction.rs, .planning/phases/20-transactional-delegated-mutation/20-03-SUMMARY.md</read_first>
  <behavior>
    - `CandidateSeal` is opaque, non-serializable, non-cloneable, and non-reconstructible from paths or caller-provided hashes. Only the accepted 20-03 live standalone-checkout capability can mint it.
    - The seal binds transaction/opening identity, canonical checkout identity, independent Git common directory and physical object storage, base commit, head commit, tree, source/path manifest digest, clean state, and all isolation invariants.
    - Seal creation and every revalidation use fixed parent-owned Git plumbing with inherited/system/global configuration disabled, replace-object lookup disabled, hooks and filters disabled, and no child-supplied argv, refspec, environment, executable, or pathname authority.
    - Before and after every gate, revalidation rejects dirty or drifting source, ambiguous/foreign repository state, shared metadata or objects, hardlink/reflink aliasing, alternates, replace refs, parent remotes, linked-worktree metadata, disallowed Git configuration, hook/filter execution surfaces, and symlink escape.
    - The seal exposes only the minimum identity and capability operations needed by 06C and later Phase 20-07 landing; it does not expose constructors or mutable checkout authority.
    - Crate-private Git/filesystem inspection and failure-injection seams are tested inside the owned `wcore-swarm` source modules for read/config/object/path failures, before/after drift, and cleanup without exposing test-only public authority.
  </behavior>
  <action>Extend the accepted 20-03 standalone-checkout owner with an opaque `CandidateSeal`. Derive it from the live owned capability after full Git/filesystem revalidation and keep the capability relationship alive rather than copying a pathname plus hashes. Provide crate/API methods for before/after-gate revalidation and the minimum read-only identity needed downstream. Reuse accepted 20-03 protected Git and physical-isolation primitives. Do not create signing, receipt, CAS, or generic evidence infrastructure. The builder commits the source packet and stops. A fresh non-author reviewer then reviews that exact source SHA and records the checklist and disposition in a separate review-only descendant commit; the builder does not author or amend its own review record.</action>
  <verify>
    <automated>changed="$(git diff --name-only --diff-filter=ACMR -- '*.rs')"; test -z "$changed" || printf '%s\n' "$changed" | xargs vx rustfmt --edition 2021 --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>`CONSTRUCTION-QUALIFIED` only: changed-file formatting and diff hygiene pass; only the three declared `wcore-swarm` paths plus this packet's review-log section change; source contains no public/caller constructor, serde implementation, path-only reconstruction, or clone path for `CandidateSeal`; owned source-module tests name minting, exact revalidation, injected inspection failures, source drift, repository substitution, aliasing, metadata poisoning, cleanup, and seal-lifetime cases. A fresh non-author reviewer records the exact 06A SHA and PASS/FAIL checklist covering accepted-20-03 producer compatibility, 06C consumer sufficiency, opacity/non-reconstructibility, ownership/scope, failure semantics, and every finding in `20-06-INTERFACE-REVIEWS.md`; only PASS qualifies. This makes no compilation, runtime, containment, or behavioral claim.</acceptance_criteria>
  <done>One exact 06A source commit plus its separate exact review-record commit is `CONSTRUCTION-QUALIFIED` on the stable 20-03 base; behavioral acceptance remains impossible until the integrated 06A-06D candidate passes the plan-level proof.</done>
</task>

<task type="auto">
  <name>06B: Require live hard containment and a read-only candidate policy</name>
  <files>crates/wcore-sandbox/src/lib.rs, crates/wcore-sandbox/src/manifest.rs, crates/wcore-sandbox/src/backends/mod.rs, crates/wcore-sandbox/src/backends/appcontainer.rs, crates/wcore-sandbox/src/backends/bwrap.rs, crates/wcore-sandbox/src/backends/docker.rs, crates/wcore-sandbox/src/backends/process_tree.rs, .planning/phases/20-transactional-delegated-mutation/20-06-INTERFACE-REVIEWS.md</files>
  <read_first>crates/wcore-sandbox/src/lib.rs, crates/wcore-sandbox/src/manifest.rs, crates/wcore-sandbox/src/backends/mod.rs, crates/wcore-sandbox/src/backends/appcontainer.rs, crates/wcore-sandbox/src/backends/bwrap.rs, crates/wcore-sandbox/src/backends/docker.rs, crates/wcore-sandbox/src/backends/process_tree.rs, crates/wcore-sandbox/tests/backend_integration.rs, crates/wcore-config/src/shell.rs, .planning/phases/20-transactional-delegated-mutation/20-02-SUMMARY.md</read_first>
  <behavior>
    - A boolean, enum, configured backend name, PATH entry, socket, caller claim, or serialized value cannot assert hard containment. `HardContainmentAuthority` is opaque, non-serializable, non-cloneable, and minted only by `SandboxRegistry` from a successful semantic live probe.
    - The authority binds the selected backend, exact executable/runtime identity, probe result, process-tree mechanism, policy identity, and the exact spawn it authorizes. Identity or policy drift between probe and spawn refuses execution.
    - Only Bubblewrap with the required PID namespace semantics, Docker with the required container semantics, and Windows AppContainer plus Job Object may qualify, and only when the live probe succeeds. macOS sandbox-exec, no-sandbox/Dangerous modes, fail-closed stubs, and ordinary Unix process groups never qualify.
    - The candidate checkout is mounted/exposed read-only for every gate. Writes are limited to transaction-private scratch/build/cache paths created and owned by the parent; host/global temp, home/config/credential paths, and network remain denied.
    - SandboxRegistry consumes the authority at the exact spawn. It cannot be replayed for another process, backend, manifest, executable, or candidate.
    - Spawn, probe, identity, containment, cancellation, timeout, output overflow, capture/read/wait failure, or descendant cleanup failure terminates the complete process tree and fails closed with structured diagnostics.
    - Crate-private backend/probe/spawn/process-tree failure injection is tested inside the owned `wcore-sandbox` source modules, including probe success followed by executable, runtime, manifest, policy, or backend drift, without exposing test-only public authority.
  </behavior>
  <action>Replace any spoofable hard-containment capability flag at the authoritative gate boundary with an opaque one-use `HardContainmentAuthority`. Mint it only after a semantic live probe of the same backend/executable/runtime and exact normalized manifest that will be used for spawn, then consume it inside `SandboxRegistry` while creating the process. Define a gate manifest with the candidate checkout read-only and only transaction-private scratch/build/cache writable; deny network, inherited credentials/configuration, global temp, and bypass modes. Preserve the accepted 20-02 AppContainer profile/ACL/crash-recovery authority. Keep ordinary process-tree helpers for cleanup, but never let them qualify as the hard boundary. Do not execute candidate-controlled code during an unsuccessful probe/admission path. The builder commits the source packet and stops. A fresh non-author reviewer then reviews that exact source SHA and records the checklist and disposition in a separate review-only descendant commit; the builder does not author or amend its own review record.</action>
  <verify>
    <automated>changed="$(git diff --name-only --diff-filter=ACMR -- '*.rs')"; test -z "$changed" || printf '%s\n' "$changed" | xargs vx rustfmt --edition 2021 --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>`CONSTRUCTION-QUALIFIED` only: changed-file formatting and diff hygiene pass; only the declared `wcore-sandbox` source paths plus this packet's review-log section change; no public constructor, serde implementation, clone path, boolean shortcut, or bypass mode can mint/retain `HardContainmentAuthority`; the manifest source makes candidate read-only and transaction-private scratch/build/cache the only writable roots; owned source-module tests name every crate-private probe/spawn/process-tree failure and identity/policy drift case. A fresh non-author reviewer records the exact 06B SHA and PASS/FAIL checklist covering 06C consumer sufficiency, one-use opacity, backend/probe/spawn binding, read-only/write-root policy, platform refusal semantics, ownership/scope, and every finding in `20-06-INTERFACE-REVIEWS.md`; only PASS qualifies. This makes no compilation, runtime, containment, or behavioral claim.</acceptance_criteria>
  <done>One exact 06B source commit plus its separate exact review-record commit is `CONSTRUCTION-QUALIFIED` on the stable 20-03 base; behavioral acceptance remains impossible until the integrated 06A-06D candidate passes the plan-level proof.</done>
</task>

<task type="auto">
  <name>06C: Close durable receipt authority and create a guard-owned AcceptedCandidate</name>
  <files>crates/wcore-agent/src/child_transaction.rs, crates/wcore-agent/src/child_transaction/gate_executor.rs, crates/wcore-agent/src/child_transaction/gates.rs, crates/wcore-agent/src/spawner.rs, crates/wcore-agent/src/spawner/durable_launch.rs, crates/wcore-agent/src/spawner/mutation_workspace.rs, crates/wcore-agent/src/session_journal/model.rs, crates/wcore-agent/src/session_journal/reducer.rs</files>
  <read_first>crates/wcore-agent/src/child_transaction.rs, crates/wcore-agent/src/spawner.rs, crates/wcore-agent/src/spawner/durable_launch.rs, crates/wcore-agent/src/spawner/mutation_workspace.rs, crates/wcore-agent/src/session_journal/model.rs, crates/wcore-agent/src/session_journal/reducer.rs, crates/wcore-types/src/child_transaction.rs, crates/wcore-swarm/src/worktree/candidate.rs, crates/wcore-sandbox/src/lib.rs, .planning/phases/20-transactional-delegated-mutation/20-04-SUMMARY.md</read_first>
  <behavior>
    - Mutating execution reaches gates only through the exact original `MutationAttemptGuard` returned by accepted 20-04 durable launch. Gate code cannot recreate transaction, workspace, or cleanup authority from a transaction ID or pathname.
    - The guard remains armed through every gate and durable acceptance step. Every error, denial, cancellation, panic unwind, or dropped incomplete handoff durably records a terminal non-landing disposition and rolls back/cleans only the owned workspace.
    - The crate-private parent-owned `AuthorizedGateClosureRegistry` in `crates/wcore-agent/src/child_transaction/gate_executor.rs` resolves every nonempty `ChildGatePlan` requirement by exact digest to one sealed `AuthorizedGateClosure` containing fixed argv, timeout, sanitized environment, `GateCwd::SealedCandidateRoot`, pinned read-only toolchain/input identities, and transaction-private writable scratch/build/cache policy. The digest commits the CWD policy; `CandidateSeal` supplies and binds the only exact resolved path, so no caller path is accepted.
    - `AuthorizedGateClosureRegistry` recomputes each canonical closure digest from all execution-affecting fields at registration and immediately before spawn. Unknown digest, command substitution, configuration/environment/CWD/toolchain drift, duplicate-conflicting registration, or registry mutation fails before candidate code runs.
    - The executor revalidates the live `CandidateSeal` immediately before and after each gate, obtains and consumes live `HardContainmentAuthority`, bounds output and time, owns/reaps descendants, and derives status/evidence/diagnostics only from observed execution.
    - The acceptance state machine accepts only module-private observed results for the same seal in declared order. Missing, conflicting duplicate, reordered, post-terminal, stale, malformed, or mismatched results fail closed.
    - Gate receipt fields and canonical raw bytes are assembled inside transaction authority. A caller-built or deserialized `ChildTransactionReceipt` cannot invoke the acceptance commit path.
    - The exact raw bytes are conditionally appended to the journal, reopened from durable storage, checksum/sequence validated, reduced, and matched back to transaction opening, gate plan, candidate seal, observed-result digests, and receipt digest before acceptance exists.
    - `AcceptedCandidate` is opaque, non-serializable, non-cloneable, and constructible only by that successful append/reopen/reduce closure. It owns the still-armed `MutationAttemptGuard` and `CandidateSeal` for the Phase 20-07 landing handoff.
    - Until Phase 20-07 consumes the typed handoff, dropping `AcceptedCandidate` terminalizes non-landing state and performs owned cleanup. Serialized receipts, child output, injected statuses, Anvil reports/stamps/journals/receipts, and model/candidate claims cannot mint or retain it.
    - Crate-private registry, executor, receipt-append/reopen/reduce, and guard-terminalization failure seams are tested inside the owned `wcore-agent` source modules; no test-only public constructor or authority is introduced.
  </behavior>
  <action>Integrate the `CONSTRUCTION-QUALIFIED` 06A/06B interfaces with the replayed-and-behaviorally-accepted 20-04 lifecycle. Preserve the exact `MutationAttemptGuard` returned by `ExecutedDurableLaunch::into_mutation_handoff()` and move it into a new opaque `AcceptedCandidate`; do not reopen a `WorktreeManager` from paths. Define crate-private `AuthorizedGateClosureRegistry`, `AuthorizedGateClosure`, and `GateCwd::SealedCandidateRoot` in `crates/wcore-agent/src/child_transaction/gate_executor.rs`. At trusted parent registration, seal every execution-affecting field into the closure digest; at lookup and immediately before spawn, recompute it and require exact match to the `ChildGatePlan`. Resolve only fixed argv, timeout, sanitized environment, the sealed candidate-root CWD policy, pinned toolchain/input identities, and private writable-root policy; then resolve the CWD exclusively from the live `CandidateSeal` and bind that exact path in the observed gate subject and receipt. Execute with safe argv and the read-only gate manifest, consume live hard-containment authority, and construct observed results internally. Add a crate-private transaction-store operation that constructs the authoritative receipt and canonical bytes from live state, appends conditionally, then reopens and reduces the durable bytes before returning acceptance authority. Put deterministic injection for unknown/substituted/drifted registry entries, execution stages, receipt persistence/replay, and terminal cleanup in `#[cfg(test)]` modules of the owned source files. Do not widen the public wire schema unless an executable test proves the existing schema cannot express the required state. Keep all terminal paths guard-owned and idempotent. Expose only the typed crate/API handoff Phase 20-07 needs; do not add receipt signing or a parallel evidence system.</action>
  <verify>
    <automated>changed="$(git diff --name-only --diff-filter=ACMR -- '*.rs')"; test -z "$changed" || printf '%s\n' "$changed" | xargs vx rustfmt --edition 2021 --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-06c-private-all "$(pwd -P)" test -p wcore-agent --lib</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-06c-unknown "$(pwd -P)" nextest list -p wcore-agent --lib --ignore-default-filter --run-ignored default -E 'package(=wcore-agent) and kind(=lib) and binary(=wcore_agent) and test(=child_transaction::gate_executor::tests::rejects_unknown_closure_digest)' --message-format json</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-06c-unknown "$(pwd -P)" nextest run -p wcore-agent --lib --ignore-default-filter --run-ignored default -E 'package(=wcore-agent) and kind(=lib) and binary(=wcore_agent) and test(=child_transaction::gate_executor::tests::rejects_unknown_closure_digest)' --no-tests fail --retries 0 --status-level pass --final-status-level pass</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-06c-substituted "$(pwd -P)" nextest list -p wcore-agent --lib --ignore-default-filter --run-ignored default -E 'package(=wcore-agent) and kind(=lib) and binary(=wcore_agent) and test(=child_transaction::gate_executor::tests::rejects_substituted_closure)' --message-format json</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-06c-substituted "$(pwd -P)" nextest run -p wcore-agent --lib --ignore-default-filter --run-ignored default -E 'package(=wcore-agent) and kind(=lib) and binary(=wcore_agent) and test(=child_transaction::gate_executor::tests::rejects_substituted_closure)' --no-tests fail --retries 0 --status-level pass --final-status-level pass</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-06c-drift "$(pwd -P)" nextest list -p wcore-agent --lib --ignore-default-filter --run-ignored default -E 'package(=wcore-agent) and kind(=lib) and binary(=wcore_agent) and test(=child_transaction::gate_executor::tests::rejects_closure_config_drift)' --message-format json</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-06c-drift "$(pwd -P)" nextest run -p wcore-agent --lib --ignore-default-filter --run-ignored default -E 'package(=wcore-agent) and kind(=lib) and binary(=wcore_agent) and test(=child_transaction::gate_executor::tests::rejects_closure_config_drift)' --no-tests fail --retries 0 --status-level pass --final-status-level pass</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-06c-stage "$(pwd -P)" nextest list -p wcore-agent --lib --ignore-default-filter --run-ignored default -E 'package(=wcore-agent) and kind(=lib) and binary(=wcore_agent) and test(=child_transaction::gate_executor::tests::fails_closed_at_each_gate_execution_stage)' --message-format json</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-06c-stage "$(pwd -P)" nextest run -p wcore-agent --lib --ignore-default-filter --run-ignored default -E 'package(=wcore-agent) and kind(=lib) and binary(=wcore_agent) and test(=child_transaction::gate_executor::tests::fails_closed_at_each_gate_execution_stage)' --no-tests fail --retries 0 --status-level pass --final-status-level pass</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-06c-replay "$(pwd -P)" nextest list -p wcore-agent --lib --ignore-default-filter --run-ignored default -E 'package(=wcore-agent) and kind(=lib) and binary(=wcore_agent) and test(=child_transaction::tests::rejects_append_reopen_reduce_corruption)' --message-format json</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-06c-replay "$(pwd -P)" nextest run -p wcore-agent --lib --ignore-default-filter --run-ignored default -E 'package(=wcore-agent) and kind(=lib) and binary(=wcore_agent) and test(=child_transaction::tests::rejects_append_reopen_reduce_corruption)' --no-tests fail --retries 0 --status-level pass --final-status-level pass</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-06c-guard "$(pwd -P)" nextest list -p wcore-agent --lib --ignore-default-filter --run-ignored default -E 'package(=wcore-agent) and kind(=lib) and binary(=wcore_agent) and test(=child_transaction::gates::tests::guard_drop_terminalizes_and_cleans)' --message-format json</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-06c-guard "$(pwd -P)" nextest run -p wcore-agent --lib --ignore-default-filter --run-ignored default -E 'package(=wcore-agent) and kind(=lib) and binary(=wcore_agent) and test(=child_transaction::gates::tests::guard_drop_terminalizes_and_cleans)' --no-tests fail --retries 0 --status-level pass --final-status-level pass</automated>
  </verify>
  <acceptance_criteria>06D admission gate only: formatting, diff hygiene, and declared ownership pass, and each exact fully qualified 06C hostile test above has its paired one-test JSON list receipt and non-retried PASS receipt against the same exact 06C commit. Missing, additional, differently named, filtered-out, ignored, skipped, vacuous/early-returning, retried, or failing results block 06D. The independent reviewer confirms each test's production-derived assertion rather than inferring meaning from selection flags. No public/caller receipt path can create acceptance; `AcceptedCandidate` owns the original guard and has no public constructor, serde implementation, or clone path. This scoped gate makes no overall behavioral or landing claim.</acceptance_criteria>
  <done>One exact 06C successor passes its mandatory private hostile library gate after sequentially `CONSTRUCTION-QUALIFIED` 06A/06B and behaviorally accepted replayed 20-04; only then may 06D start.</done>
</task>

<task type="auto">
  <name>06D: Attack exact acceptance, containment, receipt bytes, and terminal cleanup</name>
  <files>crates/wcore-sandbox/tests/hard_process_containment.rs, crates/wcore-agent/tests/child_transaction_gate_test.rs, crates/wcore-agent/tests/child_transaction_gate_execution_test.rs</files>
  <read_first>crates/wcore-agent/src/child_transaction.rs, crates/wcore-agent/src/child_transaction/gate_executor.rs, crates/wcore-agent/src/child_transaction/gates.rs, crates/wcore-agent/src/spawner/mutation_workspace.rs, crates/wcore-swarm/src/worktree/candidate.rs, crates/wcore-sandbox/src/lib.rs, crates/wcore-sandbox/src/manifest.rs, crates/wcore-agent/tests/child_transaction_store_test.rs, crates/wcore-sandbox/tests/backend_integration.rs</read_first>
  <behavior>
    - Tests cannot construct or deserialize `CandidateSeal`, `HardContainmentAuthority`, observed gate results, or `AcceptedCandidate`; only production authority paths can produce them.
    - Tests reject candidate/path/head/tree/source substitution, dirty or drifting source, foreign transaction/workspace, repository/object aliasing, poisoned Git metadata, and seal reuse.
    - Tests reject empty/trivial plans, unknown/substituted closure digests, registered-command substitution, closure-registry configuration drift, environment/CWD/toolchain/input drift, shell-string injection, hostile inherited environment, arbitrary green commands, child-spoofed success, missing gates, conflicting duplicates, reorder, post-terminal events, and stale replay.
    - Tests reject fabricated hard-containment flags/probe results, PATH-only Bubblewrap, socket-only Docker, backend/executable replacement, authority replay, Dangerous/no-sandbox, macOS sandbox-exec, ordinary process groups, network/credential/global-temp access, and scratch substitution.
    - Candidate checkout writes fail while transaction-private scratch/build/cache writes succeed. Gate-produced source drift cannot be accepted.
    - Forged/caller-built/serialized receipts, changed raw bytes, replaced or truncated journal records, mismatched checksum/sequence/digest, and durable replay mismatch cannot create `AcceptedCandidate`.
    - Success, nonzero exit, timeout, cancellation, output overflow, spawn/probe/identity/containment/capture/read/wait failure, panic, guard drop, and accepted-handoff drop each leave a durable terminal result and no descendant, workspace, lease, or capture residue outside explicitly retained authority.
    - A Unix fixture that escapes its original process group with `setsid`/`setpgid` is killed by a qualifying hard backend or refused before spawn; ordinary process-group cleanup never produces acceptance.
    - One complete ordered multi-gate execution against an unchanged sealed candidate commits and replays the exact receipt and returns one guard-owned `AcceptedCandidate`; it does not land or mutate the parent.
    - The positive test `qualified_host_candidate_to_accepted_candidate` is nonignored and nonskippable and, on the exact admitted Hetzner host, exercises the complete production chain: live standalone capability to `CandidateSeal`, semantic qualifying-backend probe, one-use `HardContainmentAuthority`, real gate process, before/after candidate revalidation, durable append/reopen/reduce, and `AcceptedCandidate`. Missing qualification is failure, not skip or refusal-only success.
  </behavior>
  <action>Add black-box tests only, exercising public transaction behavior and the real hard-sandbox fixture. Do not own, add, or relocate crate-private failure-injection seams; those obligations belong to 06A, 06B, and 06C source modules. Through public behavior, include candidate/seal substitution, unknown/substituted/config-drifted closure registry entries, raw-receipt and journal-byte substitution, read-only-checkout/writeable-scratch behavior, escaped-session descendants, authority replay, every terminal/drop/cancellation boundary, and forged Anvil/model/caller evidence. Every substantiated BLOCKER, HIGH, MEDIUM, and LOW finding is fixed and re-proved or rejected with executable counter-evidence before behavioral acceptance.</action>
  <verify>
    <automated>changed="$(git diff --name-only --diff-filter=ACMR -- '*.rs')"; test -z "$changed" || printf '%s\n' "$changed" | xargs vx rustfmt --edition 2021 --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass; only the three declared integration-test paths change; test names cover every hostile class above and explicitly distinguish refusal from live hard-containment success. Final behavioral and Clippy acceptance requires the exact clean committed HEAD remote gates below.</acceptance_criteria>
  <done>Black-box hostile coverage is committed after 06C and the exact clean integrated 06A-06D candidate is ready to seek, but has not yet earned, behavioral acceptance.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>Child-controlled source, repository metadata, gate commands, process output, serialized receipts, and advisory evaluator claims cross toward durable transaction and later parent-landing authority.</trust_boundaries>
<threats>Path/hash reconstruction of authority, candidate or checkout substitution, stale-green reuse, source drift, shared parent Git metadata/object aliases, Git poisoning, spoofed hard-containment capability, probe/spawn identity drift, writable candidate source, network/credential leakage, descendant escape, caller-built receipt acceptance, raw journal-byte substitution, incomplete terminalization, cleanup of foreign state, and advisory Anvil/model claims treated as authoritative.</threats>
<mitigations>Opaque live CandidateSeal and HardContainmentAuthority, exact before/after revalidation, one-use authority consumption, read-only candidate policy, transaction-private writable roots, fixed argv and sanitized environment, semantic backend probe, complete descendant ownership, internally constructed canonical receipt bytes, conditional append plus reopen/reduce verification, guard-owned AcceptedCandidate, fail-closed terminal cleanup, and hostile black-box tests.</mitigations>
</threat_model>

<verification>
Each task first passes its Mac-safe construction checks and becomes a provisional task commit; only 06A and 06B may receive the narrowly defined `CONSTRUCTION-QUALIFIED` status before integration. No Cargo command runs on the Mac. After 06A, 06B, 06C, and 06D are serially integrated into one clean committed candidate, the following committed-HEAD Hetzner gates determine whether that exact candidate may become `BEHAVIORALLY-ACCEPTED`:
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-candidate "$(pwd -P)" test -p wcore-swarm --lib worktree</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-agent-lib "$(pwd -P)" test -p wcore-agent --lib</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-sandbox-lib "$(pwd -P)" test -p wcore-sandbox --lib</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-proof-host-preflight "$(pwd -P)" nextest list -p wcore-sandbox --test hard_process_containment --ignore-default-filter --run-ignored default -E 'package(=wcore-sandbox) and kind(=test) and binary(=hard_process_containment) and test(=qualified_hard_containment_backend_preflight)' --message-format json</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-proof-host-preflight "$(pwd -P)" nextest run -p wcore-sandbox --test hard_process_containment --ignore-default-filter --run-ignored default -E 'package(=wcore-sandbox) and kind(=test) and binary(=hard_process_containment) and test(=qualified_hard_containment_backend_preflight)' --no-tests fail --retries 0 --status-level pass --final-status-level pass</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-positive-chain "$(pwd -P)" nextest list -p wcore-agent --test child_transaction_gate_execution_test --ignore-default-filter --run-ignored default -E 'package(=wcore-agent) and kind(=test) and binary(=child_transaction_gate_execution_test) and test(=qualified_host_candidate_to_accepted_candidate)' --message-format json</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-positive-chain "$(pwd -P)" nextest run -p wcore-agent --test child_transaction_gate_execution_test --ignore-default-filter --run-ignored default -E 'package(=wcore-agent) and kind(=test) and binary(=child_transaction_gate_execution_test) and test(=qualified_host_candidate_to_accepted_candidate)' --no-tests fail --retries 0 --status-level pass --final-status-level pass</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-gates "$(pwd -P)" test -p wcore-agent --test child_transaction_gate_execution_test --test child_transaction_gate_test</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-containment "$(pwd -P)" test -p wcore-sandbox --test hard_process_containment</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-lifecycle "$(pwd -P)" test -p wcore-agent --test child_transaction_store_test --test durable_spawn_test --test child_transaction_gate_execution_test --test child_transaction_gate_test</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-clippy "$(pwd -P)" clippy -p wcore-agent -p wcore-swarm -p wcore-sandbox --all-targets --all-features -- -D warnings</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-windows-gnu "$(pwd -P)" check --target x86_64-pc-windows-gnu --workspace --all-targets --all-features</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-06-global-fmt "$(pwd -P)" fmt --all -- --check</automated>
Then run one independent all-severity review against that exact HEAD. Fix and re-prove every substantiated finding or reject it with executable counter-evidence. Any source repair creates a new successor and invalidates all prior 20-06 behavioral acceptance: rerun the affected source-module unit tests and focused test, both agent/sandbox library gates, proof-host preflight, positive production-chain test, every cross-packet and lifecycle gate listed above, global formatting, strict Clippy, Windows GNU check, and the independent all-severity audit against the new exact HEAD. Native Windows AppContainer/Job Object containment remains explicitly unaccepted until Phase 20-08 native UAT exercises it. On macOS, no currently qualifying live backend is claimed; gate execution must fail closed rather than report success. No merge to main, push, release, deployment, or issue closure is authorized by this plan.
</verification>

<success_criteria>
- Only the live accepted 20-03 standalone-checkout capability can mint a CandidateSeal, and it remains exact through every gate.
- Candidate source is read-only; only parent-owned transaction-private scratch/build/cache is writable.
- Only a live semantically probed hard backend can execute candidate gates, and its authority is bound to and consumed by the exact spawn.
- Only internally observed ordered gate results whose exact canonical receipt bytes were durably appended, reopened, reduced, and matched can create AcceptedCandidate.
- AcceptedCandidate owns the original armed MutationAttemptGuard; every unsuccessful or abandoned path durably terminalizes and cleans only owned state.
- Advisory Anvil data, child/model claims, serialized receipts, and caller-supplied status cannot create parent landing eligibility.
- Focused, source-module, lifecycle, strict Clippy, repository-wide formatting, and Windows GNU compile checks pass on the exact clean integrated HEAD on Hetzner, followed by zero unresolved findings at any severity.
- 20-06 makes no native Windows containment claim before 20-08 UAT and no macOS hard-containment success claim without a live qualifying backend; both platforms fail closed when the required authority is unavailable.
</success_criteria>

## Artifacts this phase produces

- Opaque live exact-candidate seal.
- Opaque one-use hard-containment spawn authority.
- Read-only-candidate gate sandbox policy.
- Parent-owned gate executor and raw durable receipt authority closure.
- Guard-owned AcceptedCandidate handoff for Phase 20-07.
- Hostile containment, receipt, authority, and terminal-cleanup regression tests.
- Fresh independent exact-source-SHA interface review records for 06A and 06B, each committed separately from the source packet it qualifies.

<output>
After completion, create `.planning/phases/20-transactional-delegated-mutation/20-06-SUMMARY.md` using the standard GSD summary template. Record the accepted 20-03 SHA, accepted replay successor of 20-04 commit `a0d27dd4bd7ddf02baf16aebb45dbe1239ec4dc3`, exact 06A/06B source and review-record commit pairs, exact 06C/06D commits, integrated candidate SHA, proof-host identity and preflight result, remote commands/results, independent review disposition, and explicit platform limitations.
</output>

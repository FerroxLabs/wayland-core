---
phase: 20-transactional-delegated-mutation
plan: "25"
type: execute
wave: 24
depends_on:
  - "20-24"
files_modified: []
autonomous: false
requirements:
  - REQ-native-r14
  - REQ-native-r4
  - REQ-native-r7
  - REQ-native-r9
  - REQ-native-r11
must_haves:
  truths:
    - "One Sean-authorized native-proof dispatch runs the exact sealed candidate on real hardware: Windows on the AppContainer-capable self-hosted msvc runner and macOS on the ephemeral Scaleway Apple-silicon runner; native proof is never claimed from cross-compilation or source inspection (CONTEXT D6/D7, REQ-native-r11)."
    - "The native run re-proves BOTH the 2026-07-23 AppContainer findings (is_available true; read/grant/revoke green; genuine DENY blocks; normal-SID-only denied; real Job-Object containment) AND the ACTUAL recorded 2026-07-22 20-18 failures — the wcore-sandbox Windows COMPILE fix and the macOS proof-harness test-mapping fix — on hardware (REQ-native-r14/r7/r9)."
    - "All Windows native targets are green on the self-hosted msvc runner and all macOS native targets are green on the ephemeral runner, with ordered per-target and final commit/tree/nonce markers bound to the exact sealed SHA; the UAT evidence ref is retained (deletion not authorized) (REQ-native-r4)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-25-SUMMARY.md
      provides: "Retained dual-OS native evidence bound to the exact sealed candidate for the fresh review"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-23-SUMMARY.md
      to: .planning/phases/20-transactional-delegated-mutation/20-25-SUMMARY.md
      via: "the sealed candidate SHA is published to a temporary UAT ref and dispatched to native runners"
      pattern: "source_sha"
---

<objective>
Produce the first real native evidence for the sealed candidate: one Sean-authorized native-proof dispatch that runs Windows (self-hosted msvc) and macOS (ephemeral) acceptance on hardware, so the fresh 20-16-role review (20-26) can review native evidence rather than defer it.

Purpose: The audit requires the fresh review NOT to defer native (R13/R12); that is only possible if native evidence exists at review time. This is the native-proof dispatch gate — Sean-only (D6), distinct from the terminal exact-tuple authorization at 20-28. It also re-proves the actual 07-22 compile/mapping failures on hardware (R14), not only the 07-23 AppContainer findings.
Output: Retained, candidate-bound dual-OS native evidence, or an explicit incomplete disposition; no requirement completion here.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
@/Users/seandonahoe/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/inbox/native-uat-repair-BRIEF.md
@.planning/phases/20-transactional-delegated-mutation/20-18-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-23-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-24-SUMMARY.md
@scripts/f20-native-windows-proof.ps1
@scripts/f20-native-macos-proof.sh
@scripts/f20-native-uat-proof.mjs
@.github/workflows/nightly-windows-soak.yml
</context>

<admission>
- Require the exact sealed candidate SHA/tree from `20-23-SUMMARY.md` and the zero-finding cross-audit from `20-24-SUMMARY.md`; any drift, non-PASS cross-audit, or unclean checkout stops before the checkpoint.
- The native-proof dispatch is Sean-only. No prior general approval, silence, or approval for another candidate is authority. Only Sean's explicit native-proof-dispatch authorization for THIS sealed SHA may run Task 2.
- Re-entry after authorization uses the same durable dispatch/run state: it may capture an already-dispatched exact attempt but must never dispatch or retry a second run.
- The Windows leg MUST land on `[self-hosted, Windows, X64, msvc]` (AppContainer-capable); the macOS leg on the pinned ephemeral runner with `DOCKER_HOST` exported. A hosted `windows-2022` landing is a failure, not a fallback.
</admission>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Sean authorizes the native-proof dispatch for the sealed candidate</name>
  <files>None; this checkpoint runs no command and changes no file or external state.</files>
  <decision>Authorize one native-proof dispatch of the exact sealed candidate to its temporary UAT ref and one native-proof workflow run (Windows self-hosted msvc + macOS ephemeral), or provide no authorization and leave the native path unproven.</decision>
  <context>20-23 sealed the candidate (Linux 11509/0 + all-features build) and 20-24 cross-audited it CLEAN with native deferred. This gate produces the FIRST real native evidence so the fresh review (20-26) can review it. This is the native-proof dispatch gate, separate from the terminal exact-tuple authorization at 20-28.</context>
  <options>
    <option id="authorize"><name>Authorize sealed-candidate native-proof dispatch</name><pros>Produces real Windows+macOS evidence so the fresh review need not defer native.</pros><cons>Publishes one temporary UAT ref and consumes the self-hosted msvc + ephemeral macOS runners.</cons></option>
  </options>
  <action>Run no command. Display the exact sealed candidate commit/tree, the target runner labels (self-hosted msvc + ephemeral macOS), and the request nonce/digest, then stop. Continue only when Sean explicitly authorizes THIS sealed SHA's native-proof dispatch using the displayed digest.</action>
  <verify>The response is Sean's explicit native-proof-dispatch authorization matching the displayed sealed-candidate digest. Bare approval, silence, prior approval, or an approval for a different candidate fails this checkpoint and ends execution.</verify>
  <done>The explicit native-proof-dispatch authorization exists; otherwise the plan remains incomplete and Task 2 does not run.</done>
  <resume-signal>Authorize the native-proof dispatch for the exact displayed sealed candidate digest. Any other response leaves the native path unproven.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Dispatch, capture, and verify dual-OS native evidence</name>
  <files>None; this task produces candidate-bound external evidence and Git-private authority objects.</files>
  <read_first>scripts/f20-native-uat-proof.mjs, scripts/f20-native-windows-proof.ps1, scripts/f20-native-macos-proof.sh, .github/workflows/nightly-windows-soak.yml, .planning/phases/20-transactional-delegated-mutation/20-18-SUMMARY.md</read_first>
  <behavior>
    - Publication and dispatch use the durable one-attempt state machines in `f20-native-uat-proof.mjs`: publish the exact sealed SHA to its temporary UAT ref only if absent (reuse an exact ref; conflicting ref fails); persist+sync dispatch intent (digest, full SHA/tree/ref/workflow, nonce, API boundary, pre-existing run set) before the single dispatch; after intent, reconcile exactly one post-boundary run carrying the nonce and never dispatch again.
    - `capture-verify` validates the Windows job ran on the self-hosted msvc runner and the macOS job on the ephemeral runner (which deregisters after its one job), with ordered per-target and final commit/tree/nonce markers for every Windows and macOS target — including the Job-Object containment targets and the re-validated macOS targets.
    - The run re-proves the ACTUAL 07-22 failure modes on hardware: the wcore-sandbox Windows lib COMPILES (no E0425/E0423 module-path debt) and the macOS proof maps to real macOS tests (no `#![cfg(windows)]`->0-tests). A repeat of either is a native failure.
    - The UAT evidence ref is retained; its remote SHA is rechecked after capture and recorded `retained` / `deletion_not_authorized`.
  </behavior>
  <action>Persist the native-proof-dispatch authorization for the exact sealed digest. Publish the sealed SHA to its temporary UAT ref (first-attempt-only; reconcile-only after intent), then dispatch the native-proof workflow once (Windows self-hosted msvc + macOS ephemeral). Capture and verify: bind the Windows and macOS job/runner identities, require the self-hosted msvc runner for Windows and the ephemeral runner for macOS, validate ordered per-target + final markers for every target, and confirm on hardware both the AppContainer read/grant/revoke/isolation + Job-Object containment AND the wcore-sandbox Windows compile + macOS test-mapping fixes. Recheck and retain the exact evidence ref. Do NOT complete any requirement, run the aggregate (that is 20-28), retry, repair source, or delete the ref. Any missing/skipped/wrong-runner/incomplete-marker evidence fails closed; a native RED requires a repaired successor and fresh authorization.</action>
  <verify>
    <automated>node scripts/f20-native-uat-proof.mjs verify-publication --git-dir "$(git rev-parse --absolute-git-dir)" --repo FerroxLabs/wayland-core</automated>
    <automated>node scripts/f20-native-uat-proof.mjs verify-evidence --git-dir "$(git rev-parse --absolute-git-dir)" --repo FerroxLabs/wayland-core --workflow nightly-windows-soak.yml</automated>
  </verify>
  <acceptance_criteria>Both native platforms are green on the correct runners against the exact sealed source, with complete ordered markers and a retained evidence ref. Any other state is incomplete and blocks the fresh review.</acceptance_criteria>
  <done>The sealed candidate has retained, candidate-bound dual-OS native evidence for 20-26 to review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native evidence ← runner host | Only a real AppContainer-capable Windows host and a real macOS host may produce native proof; cross-compilation/source cannot |
| dispatch authority ← Sean | The native-proof dispatch is a Sean-only external mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-25-01 | Spoofing | native PASS provenance | critical | mitigate | Markers bind exact SHA/tree/nonce; the Windows job must run on self-hosted msvc and macOS on the ephemeral runner; a hosted-image landing or missing marker fails closed. Native is never claimed from cross-compilation (D7). |
| T-20-25-02 | Elevation of Privilege | unauthorized dispatch | high | mitigate | Blocking-human checkpoint; only Sean's explicit dispatch authorization for THIS sealed digest runs Task 2; re-entry is reconcile-only, never a second dispatch. |
| T-20-25-SC | Tampering | UAT evidence ref | high | mitigate | One-attempt publish/dispatch state machines; ref retained and rechecked; deletion not authorized. |
</threat_model>

<verification>
Authenticate the sealed source/tree, the single native attempt bound by nonce, the correct runner identities, complete ordered markers, and the retained ref. Record the retained UAT ref, run id, per-target results, and both platforms' green/red disposition in `20-25-SUMMARY.md`. No Cargo runs on the Mac; no requirement completes here.
</verification>

<success_criteria>
- One Sean-authorized native-proof dispatch ran the exact sealed candidate on the self-hosted msvc runner (Windows) and the ephemeral runner (macOS) (r11).
- Both platforms are green, re-proving the 07-23 AppContainer findings AND the actual 07-22 compile/mapping failures on hardware (r14/r7/r9/r4).
- The evidence ref is retained (deletion not authorized) and bound to the exact sealed SHA for the fresh review.
</success_criteria>

## Artifacts this phase produces
- Retained candidate-bound dual-OS native evidence and Git-private dispatch authority objects.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-25-SUMMARY.md` using the standard GSD summary template. Record the sealed source/tree, dispatch/run identity, per-target native results for both platforms, the retained UAT ref, and the explicit non-claim of requirement completion. If either platform is RED, record INCOMPLETE and route to a repaired successor.
</output>

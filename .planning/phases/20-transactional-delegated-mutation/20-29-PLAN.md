---
phase: 20-transactional-delegated-mutation
plan: "29"
type: execute
wave: 28
depends_on:
  - "20-25"
files_modified:
  - crates/wcore-sandbox/src/directory_authority.rs
  - crates/wcore-sandbox/src/directory_authority_file.rs
  - .github/workflows/nightly-windows-soak.yml
autonomous: true
requirements:
  - REQ-native-r9
  - REQ-native-r4
  - REQ-native-r11
must_haves:
  truths:
    - "Before any edit the three touched files' pre-edit blobs equal the sealed candidate 95c81ec6 tree blobs (no source drift), so the new candidate is a clean successor of 95c81ec6 that adds ONLY the rename_into unix fix and the Windows safe.directory workflow guard — the two exact 20-25 RED causes and nothing else (REQ-native-r9)."
    - "DirectoryAuthority::rename_into (and the RegularFileAuthority variant) resolve on unix: each becomes one method with internal cfg branches — windows delegates to the existing windows::rename_directory_into / rename_file_into, and not(windows) performs a dirfd-relative renameat-family rename reusing the same handle-relative machinery as atomic_write_child — so the wcore-sandbox lib tests compile on macOS and the macos-retained-directory acceptance target no longer fails E0599 at directory_authority_tests.rs:149 (REQ-native-r9)."
    - "The unix rename preserves the Windows isolation semantics rather than merely compiling: source and destination child names are validated single components resolved ONLY relative to the retained destination-parent directory handle (never the ambient display-path pathname namespace), the held source object's identity is re-proven against the destination-parent child before the rename, and replace=false cannot clobber an existing target; the decoy-planted macOS acceptance test proves the rename lands on the retained object and never the decoy planted at the original path (REQ-native-r9)."
    - "The f20-windows-candidate job in nightly-windows-soak.yml runs git config --global --add safe.directory before its git rev-parse EXPECTED_COMMIT tree-resolve step, so the self-hosted SEANDESKTOP msvc runner no longer aborts with detected dubious ownership and the Windows native proof script can start (REQ-native-r11)."
    - "Linux is unregressed: Hetzner clippy -D warnings for wcore-sandbox is clean (the now-genuinely-used unix RegularFileAuthority::rename_into is not dead code) and no Linux behavior changes because the macOS acceptance test is cfg-excluded on Linux and the atomic_write_child publish unification is byte-for-byte behavior-preserving (REQ-native-r4)."
  artifacts:
    - path: crates/wcore-sandbox/src/directory_authority.rs
      provides: "cfg(not(windows)) DirectoryAuthority::rename_into reusing the renameat handle-relative machinery, plus the unix atomic_write_child publish routed through the unified file rename_into"
    - path: crates/wcore-sandbox/src/directory_authority_file.rs
      provides: "cfg(not(windows)) RegularFileAuthority::rename_into that reuses the renameat machinery and is exercised by the unix atomic_write_child path (not dead code)"
    - path: .github/workflows/nightly-windows-soak.yml
      provides: "safe.directory guard on the candidate native job(s) before the tree-resolve, unblocking the self-hosted msvc runner"
  key_links:
    - from: crates/wcore-sandbox/src/directory_authority.rs
      to: crates/wcore-sandbox/src/directory_authority_tests.rs
      via: "the cfg(all(test, unix)) macOS acceptance test calls rename_into on the retained directory authority"
      pattern: "rename_into"
    - from: .github/workflows/nightly-windows-soak.yml
      to: scripts/f20-native-windows-proof.ps1
      via: "the safe.directory guard lets the candidate job resolve the tree and reach the proof script on the self-hosted msvc runner"
      pattern: "safe.directory"
---

<objective>
Repair the two exact 20-25 RED causes on the sealed candidate `95c81ec6` and nothing else: (1) the macOS `wcore-sandbox` lib-test compile error — `DirectoryAuthority::rename_into` and its file variant are `#[cfg(windows)]`-only while the `#[cfg(all(test, unix))]` macOS acceptance test calls `rename_into` — and (2) the Windows self-hosted-runner `dubious ownership` misconfiguration that killed the candidate job in setup before the proof ran.

Purpose: The 20-25 native-proof dispatch came back RED on BOTH platforms — macOS on a genuine sealed-tree compile defect (E0599 at `directory_authority_tests.rs:149`), Windows on a runner git `safe.directory` guard, not a candidate defect. This plan writes the real fixes so a NEW candidate can be re-sealed (20-30), re-cross-audited (20-31), and re-dispatched (20-32) with a fresh Sean authorization (REQ-native-r9 macOS-acceptance / REQ-native-r11 self-hosted-runner). The macOS fix touches a TOCTOU-sensitive sandbox boundary op, so it must preserve the "resolves neither pathname" isolation property, not merely compile.
Output: One clean successor delta touching exactly the two sandbox source files plus the candidate workflow; the macOS compile is proven green on THIS Mac (the one sanctioned targeted Mac cargo invocation) and Hetzner clippy `-D warnings` is clean; no native, aggregate, or requirement claim.
</objective>

<redispatch_decision>
Re-dispatch structure — AUTHOR NEW PLANS 20-32…20-35; do NOT re-bind 20-25/20-26/20-27/20-28.

Choice: the repaired successor is the fully additive serial chain 20-29 (fix) -> 20-30 (re-seal) -> 20-31 (pre-native cross-audit) -> 20-32 (native re-dispatch) -> 20-33 (fresh review, native NOT deferred) -> 20-34 (re-prep tuple) -> 20-35 (terminal). This mirrors 20-19 -> 20-23 -> 20-24 -> 20-25 -> 20-26 -> 20-27 -> 20-28 (the fix is collapsed to one plan because the repair is two small targeted fixes, not the four-plan 20-19…20-22 construction).

Rationale (why new, not re-bind):
- 20-25 is already EXECUTED with a retained RED disposition and a retained UAT evidence ref (`refs/f20-native-uat/95c81ec6…`, deletion not authorized). Re-binding/re-executing 20-25 would overwrite that permanent RED record and its retained ref — forbidden by CONTEXT D1 (additive only; never re-execute) and by the Sean-only retained-ref-deletion gate. A fresh native digest also requires FRESH Sean authorization (D6: the prior digest is spent/void), so the dispatch must be a new checkpoint bound to the NEW sealed SHA — cleaner as a new plan than mutating 20-25's spent checkpoint.
- 20-26/20-27/20-28 are authored but UNEXECUTED and hardcoded (depends_on, `@…20-25-SUMMARY.md` context refs, key_links) to 20-25's now-RED native evidence. Editing them in place to point at the new dispatch would mutate already-authored plans and risk stale/ambiguous candidate binding — the error-prone path. As authored, 20-26's admission already fails closed on a RED/missing native evidence and routes to "a repaired successor," so they are self-consistently superseded by 20-33/20-34/20-35 without any mutation.
- Net: 20-25 stays as retained RED evidence; 20-26/20-27/20-28 are superseded (dead — their upstream 20-25 is terminal RED) and are neither executed nor edited by this chain; the NEW sealed candidate flows through 20-30…20-35.
</redispatch_decision>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-NATIVE-REPAIR-PRD.md
@.planning/phases/20-transactional-delegated-mutation/20-25-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-19-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-23-SUMMARY.md
@crates/wcore-sandbox/src/directory_authority.rs
@crates/wcore-sandbox/src/directory_authority_file.rs
@crates/wcore-sandbox/src/directory_authority_tests.rs
@crates/wcore-sandbox/src/directory_authority_windows.rs
@.github/workflows/nightly-windows-soak.yml
@scripts/f20-native-windows-proof.ps1
</context>

<execution_rules>
- Require a clean isolated standalone checkout (`.git` is a directory) on branch `plan/f20-unified-audit-repair`. Do NOT execute in the sibling `/Users/seandonahoe/dev/waylandcore` primary checkout.
- The new candidate is a SUCCESSOR of the sealed candidate `95c81ec6a351ec22125497333739fa7c93a0cd8b` (tree `784f498002b9944856aedee6cb3db347b55c1dcc`). Only planning-doc commits sit between the seal and the current branch tip, so the three touched files are byte-identical to the sealed tree; confirm that (no source drift) before editing, then author the fixes on the branch tip.
- Capture the plan base before any edit: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-29)"`.
- NO Cargo/clippy/nextest on the Mac, with EXACTLY ONE sanctioned exception (CONTEXT D5 carve-out for this repair): the targeted macOS compile gate `cargo check -p wcore-sandbox --features live-docker --tests` in Task 1's verify — it compiles the `#[cfg(all(test, unix))]` acceptance module and catches the E0599 locally so a compile error never burns another scarce Sean-authorized native run. It is a compile/type-check only (no test execution, no Docker runtime needed), not a workspace build. Everything else is `vx rustfmt` (fmt) + the committed-HEAD Hetzner harness (`/Users/seandonahoe/.ratchet/harness/remote-cargo.sh`) + node tooling.
- Touch ONLY the three declared files. No drive-by refactor of `directory_authority.rs`, the AppContainer implementation, or any other module beyond what r9/r11 require. The atomic_write_child publish unification is in-scope ONLY because it is what makes the brief-required unix file `rename_into` genuinely used (not dead code under `-D warnings`); it MUST be byte-for-byte behavior-preserving.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Expose cfg(not(windows)) rename_into on both authorities, isolation preserved</name>
  <files>crates/wcore-sandbox/src/directory_authority.rs, crates/wcore-sandbox/src/directory_authority_file.rs</files>
  <read_first>crates/wcore-sandbox/src/directory_authority.rs, crates/wcore-sandbox/src/directory_authority_file.rs, crates/wcore-sandbox/src/directory_authority_tests.rs, crates/wcore-sandbox/src/directory_authority_windows.rs</read_first>
  <behavior>
    - `DirectoryAuthority::rename_into(destination_parent, child_name, replace)` compiles and runs on unix: it is one method (no longer `#[cfg(windows)]`-gated away) with internal cfg branches — `#[cfg(windows)]` delegates to `windows::rename_directory_into` exactly as today; `#[cfg(not(windows))]` renames the held directory into the retained destination parent using the dirfd-relative `libc::renameat` family already used by `atomic_write_child`.
    - `RegularFileAuthority::rename_into(target_parent, name, replace)` gets the identical cfg-branch treatment and its `#[cfg(not(windows))]` body reuses the same `renameat` machinery. It is made genuinely used on unix (not dead code under `-D warnings`) by routing the unix branch of `DirectoryAuthority::atomic_write_child`'s file publish through it, replacing the inline `renameat` block with the same call shape the `#[cfg(windows)]` branch already uses (`temporary_authority.rename_into(self, name, true)` with its existing cleanup-on-error wrapper). This is byte-for-byte behavior-preserving: same source/destination child names, same replace semantics, same failure cleanup.
    - Isolation is preserved, not merely satisfied: both unix renames validate `child_name` (and the derived current source name) as single safe components (`validate_child_name`), resolve BOTH names relative ONLY to the retained destination/target parent directory handle (dirfd) — never the ambient `display_path` pathname namespace — and re-prove, through the retained parent handle, that the source child still resolves to the exact held source object's identity before issuing the rename (mirroring the existing `open_child_directory` + identity-token check pattern). A `replace=false` rename must NOT clobber an existing target: use the OS no-replace primitive (`renameat2` with `RENAME_NOREPLACE` on Linux, `renameatx_np` with `RENAME_EXCL` on macos/Apple) so non-replace fails closed if the target exists; `replace=true` keeps plain `renameat` overwrite semantics (matching the file publish).
    - After the fix the `#[cfg(all(test, unix))]` module `directory_authority_tests.rs` compiles on macOS; `macos_retained_parent_rename_delete_enumeration_and_cwd_stay_handle_relative` (line 121) — which swaps `root`->`retained`, plants a decoy at `root`, then calls `mover.rename_into(&authority, "landed", false)` — resolves against the retained object so `retained.join("landed").is_dir()` and `!root.join("landed").exists()` hold. The decoy path stays empty.
  </behavior>
  <action>On unix, expose `rename_into` on both `DirectoryAuthority` and `RegularFileAuthority` by converting each from a `#[cfg(windows)]`-only method into one method with an internal `#[cfg(windows)]` (delegate to the existing `windows::rename_*_into`) and `#[cfg(not(windows))]` branch that performs a dirfd-relative rename reusing the `libc::renameat`-family machinery already present in `atomic_write_child`. Preserve the Windows "resolves neither pathname" isolation semantics: validate single-component names, resolve source and destination names relative only to the retained destination/target-parent handle (never `display_path` through the ambient namespace), re-prove the source child's identity against the held source object before renaming, and honor `replace` faithfully (no-replace via `renameat2 RENAME_NOREPLACE` on Linux / `renameatx_np RENAME_EXCL` on macos; overwrite via plain `renameat` for `replace=true`). Make the unix file variant genuinely used by routing `atomic_write_child`'s unix file publish through `temporary_authority.rename_into(self, name, true)` (same cleanup wrapper as the windows branch), replacing the inline `renameat` — a behavior-preserving unification, not a semantic change. Do NOT touch the AppContainer implementation, the windows-only rename helpers, or any file outside the two declared here. Windows/macOS runtime behavior is proven only on hardware at the native-proof gate (20-32); this task's runtime proof is the sanctioned macOS compile check plus the Linux clippy gate.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-29)" crates/wcore-sandbox/src/directory_authority.rs crates/wcore-sandbox/src/directory_authority_file.rs</automated>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-sandbox/src/directory_authority.rs crates/wcore-sandbox/src/directory_authority_file.rs</automated>
    <automated>cargo check -p wcore-sandbox --features live-docker --tests</automated>
  </verify>
  <acceptance_criteria>Formatting and diff-scope pass, both authorities expose a `#[cfg(not(windows))]` `rename_into`, and the ONE sanctioned macOS compile gate (`cargo check -p wcore-sandbox --features live-docker --tests`) is green on THIS Mac — the `cfg(all(test, unix))` acceptance module (including `directory_authority_tests.rs:149`) compiles, closing the E0599. The isolation property is asserted by source review and by the macOS acceptance test on hardware at 20-32.</acceptance_criteria>
  <done>Both unix `rename_into` variants exist as a provisional task commit; the sealed-tree E0599 is closed locally; hardware behavior is deferred to 20-32.</done>
</task>

<task type="auto">
  <name>Task 2: Add the safe.directory guard to the candidate native job(s) before tree-resolve</name>
  <files>.github/workflows/nightly-windows-soak.yml</files>
  <read_first>.github/workflows/nightly-windows-soak.yml, .planning/phases/20-transactional-delegated-mutation/20-25-SUMMARY.md</read_first>
  <behavior>
    - The `f20-windows-candidate` job runs `git config --global --add safe.directory '*'` as a dedicated step immediately after `actions/checkout@v4` and BEFORE the "Run exact-candidate F20 native Windows proof" step whose `git rev-parse "$env:EXPECTED_COMMIT^{tree}"` (currently line 240) aborted with `detected dubious ownership … BUILTIN\Administrators vs NT AUTHORITY\NETWORK SERVICE` on the self-hosted `SEANDESKTOP` runner. A global git config set once at job start covers both the workflow-step `git rev-parse` and any git the proof script runs.
    - The `f20-macos-candidate` job gets the same guard defensively (shell: bash), because a FRESH ephemeral macOS runner is re-registered for the re-dispatch and its checkout-dir ownership is unproven; the guard is a harmless no-op if not needed and prevents burning another scarce native run on an identical ownership abort. (The 20-25 macOS run did NOT hit ownership — it reached the proof and failed on E0599 — so this is belt-and-suspenders, not the proven RED.)
    - Value `'*'` is chosen over an exact path because the self-hosted runners are single-tenant Sean-owned, the candidate jobs grant only `contents: read` with no secrets, and a Windows backslash-vs-forward-slash `safe.directory` path mismatch would silently fail to match and burn another native run. `'*'` is the documented remedy for exactly this `dubious ownership` error. No other job (`windows-soak`) is modified.
    - The workflow remains structurally valid: the candidate `if: github.event.inputs.f20_candidate == 'true'` gating, the self-hosted `runs-on` label set, the least-privilege permissions, and the existing proof steps are unchanged.
  </behavior>
  <action>In `.github/workflows/nightly-windows-soak.yml`, add a `git config --global --add safe.directory '*'` step to the `f20-windows-candidate` job (required — the proven Windows RED cause) immediately after its checkout and before the proof step's tree-resolve, and add the equivalent step to the `f20-macos-candidate` job (defensive — the freshly re-registered ephemeral runner's checkout ownership is unproven). Use `shell: pwsh` for the Windows step and `shell: bash` for the macOS step; value `'*'`. Change nothing else — not the `windows-soak` job, not the candidate gating, labels, permissions, env-var landing, or the proof invocations. Runtime proof that the Windows leg now reaches the proof script on the self-hosted msvc runner is deferred to the native re-dispatch (20-32).</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-29)" .github/workflows/nightly-windows-soak.yml</automated>
    <automated>test "$(grep -c 'add safe.directory' .github/workflows/nightly-windows-soak.yml)" -ge 2</automated>
    <automated>grep -nE "runs-on:|self-hosted|f20_candidate == 'true'|EXPECTED_COMMIT.\{tree\}|safe.directory" .github/workflows/nightly-windows-soak.yml</automated>
  </verify>
  <acceptance_criteria>The scope check passes, the `safe.directory` guard appears in BOTH candidate jobs (>=2 occurrences), and the candidate gating / self-hosted `runs-on` / tree-resolve steps remain present and intact. Runtime proof that setup now clears is deferred to 20-32.</acceptance_criteria>
  <done>The candidate workflow guards are added as a provisional task commit; the Windows RED cause is structurally closed; hardware proof is deferred to 20-32.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sandbox rename ← retained parent handle | The unix `rename_into` must resolve source/destination ONLY through the retained parent dirfd; resolving `display_path` through the ambient namespace would let a same-path decoy be renamed instead of the held object (TOCTOU) |
| candidate job git ← self-hosted runner ownership | The `safe.directory` guard changes which repo dirs git will operate on for the runner user; overly broad scope on a shared runner would let git trust foreign checkouts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-29-01 | Tampering | unix directory/file `rename_into` | critical | mitigate | Resolve source and destination names relative ONLY to the retained destination-parent dirfd (reusing the `atomic_write_child` `renameat` pattern), validate single-component names, and re-prove the source child's identity against the held object before the rename. The decoy-planted macOS acceptance test (`macos_retained_parent_rename_delete_enumeration_and_cwd_stay_handle_relative`) falsifies a namespace-resolved rename on hardware at 20-32. |
| T-20-29-02 | Elevation of Privilege | `replace=false` clobber | high | mitigate | Non-replace uses the OS no-replace primitive (`renameat2 RENAME_NOREPLACE` / `renameatx_np RENAME_EXCL`) so a `replace=false` rename cannot silently overwrite an existing target, preserving the Windows variant's semantics. |
| T-20-29-03 | Spoofing | `safe.directory '*'` scope | low | accept | The candidate jobs run only on single-tenant Sean-owned self-hosted runners, grant only `contents: read`, receive no secrets, and never run untrusted checkouts; `'*'` is the documented remedy and avoids a path-format mismatch burning another scarce native run. Rationale for acceptance recorded here. |
</threat_model>

<verification>
Each task first passes its Mac construction gate (scope + `vx rustfmt --check`, plus the one sanctioned `cargo check -p wcore-sandbox --features live-docker --tests` for Task 1) as a provisional GSD task commit. After both task commits exist and the checkout is clean, the committed-HEAD Hetzner gate determines Linux construction acceptance (a construction commit is not behavioral evidence):
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-29-clippy "$(pwd -P)" clippy -p wcore-sandbox --all-targets --all-features -- -D warnings</automated>
Record the exact committed `source_sha`, source tree, the macOS compile-check receipt, the Hetzner clippy receipt, changed paths, and the explicit deferral of all Windows/macOS runtime behavior (rename isolation on hardware, Windows setup clearing on the self-hosted runner) to the native-proof gate (20-32) in `20-29-SUMMARY.md`. Note that `fix1_dispatch_budget_aborts_with_partial_result` is a pre-existing default-profile flake (out of scope here — do not fix). No aggregate, requirement, or phase claim is made.
</verification>

<success_criteria>
- Both unix `rename_into` variants exist with the Windows isolation property preserved; the macOS `cargo check … --tests` gate is green on THIS Mac, closing the E0599 (r9).
- The candidate workflow carries the `safe.directory` guard before the tree-resolve on the Windows (required) and macOS (defensive) candidate jobs (r11).
- Hetzner clippy `-D warnings` for `wcore-sandbox` is clean — the newly-used unix file variant is not dead code and no Linux regression is introduced (r4).
- The summary records the exact new candidate `source_sha`/tree and defers every hardware runtime claim to 20-32.
</success_criteria>

## Artifacts this phase produces
- The two unix `rename_into` fixes plus the candidate-workflow `safe.directory` guard as one reviewable successor delta over the sealed candidate.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-29-SUMMARY.md` using the standard GSD summary template. Record the exact new candidate `source_sha`/tree, the macOS compile-check receipt, the Hetzner clippy receipt, changed paths, the re-dispatch structure decision (new 20-32…20-35, not re-bind), and the explicit deferral of all Windows/macOS runtime proof to 20-32. Complete no Phase 20 requirement.
</output>

---
phase: 20-transactional-delegated-mutation
plan: "47"
type: execute
wave: 46
depends_on:
  - "20-46"
files_modified:
  - .planning/phases/20-transactional-delegated-mutation/20-47-INDEPENDENT-REVIEW.md
autonomous: true
requirements:
  - REQ-native-r13
  - REQ-native-r12
must_haves:
  truths:
    - "A fresh non-author review — with Kimi K3 as the independent reviewer (`/Users/seandonahoe/.kimi-code/bin/kimi -p \"…\" --output-format text`) — prosecutes the exact sealed third-repaired-successor at every severity across code review, phase validation, and ASVS Level 2 security, AND reviews the 20-46 native evidence: native_macos and native_windows are PASS checks, NOT deferred (the review the audit demanded) (REQ-native-r13/r12)."
    - "The review is recorded as a schema-validated `wayland-core.phase20-independent-review.v1` artifact (deferred empty — native included); every claimed reviewer (Kimi K3 + a ferrox non-author reviewer for the schema) emits its own schema-validated artifact — no prose-only review counts toward the PASS (REQ-native-r13)."
    - "The review confirms on the retained native evidence that all 6 Windows targets and all 8 macOS targets are green for the exact sealed SHA, the two PRODUCTION isolation guardrails held on hardware (#A admission budget real; #B boundary primitives intact), and the Kimi watch-items were exercised, not masked. Only a zero-finding PASS with native NOT deferred admits terminal preparation (20-48); any finding at any severity stops the sequence for a further repaired successor and fresh review (REQ-native-r13/r12)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-47-INDEPENDENT-REVIEW.md
      provides: "Fresh non-author all-severity review (Kimi K3 + ferrox) of the sealed third-repaired-successor WITH native evidence, native not deferred, schema-validated per reviewer"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-47-INDEPENDENT-REVIEW.md
      to: .planning/phases/20-transactional-delegated-mutation/20-46-SUMMARY.md
      via: "the review consumes the retained dual-OS native evidence as the native_macos/native_windows PASS basis"
      pattern: "native_windows"
---

<objective>
Perform the fresh 20-16-role independent review the audit required, now against the third repaired successor with Kimi K3 as the independent reviewer: prosecute the exact sealed successor at every severity AND review the real native evidence from 20-46 — native NOT deferred — recording a schema-validated artifact per claimed reviewer.

Purpose: The original 20-16 passed CLEAN but with native deferred by design, and the prior native re-dispatches (20-25, 20-32, and the daf27337 diagnostic run 30064412019) went RED before a native-inclusive review could run. This review closes the gap on the third repaired successor: it reviews the actual Windows+macOS native evidence — including the two fixed PRODUCTION root causes (#A capacity-probe admission across all Windows dispatch/lifecycle targets; #B granted-read exit-0 and fixed containment) and the exercised Kimi watch-items — not a promise, and every claimed reviewer leaves an on-disk schema-validated artifact.
Output: One (or more, per claimed reviewer) schema-validated review artifact over the sealed successor SHA with native as PASS checks; a zero-finding PASS admits 20-48.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md
@.planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-46-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md
</context>

<execution_rules>
- The independent reviewer is Kimi K3, invoked at its ABSOLUTE path `/Users/seandonahoe/.kimi-code/bin/kimi -p "<prompt>" --output-format text` (the audit demanded the fresh review NOT defer native, so Kimi K3 reviews the actual 20-46 native evidence). A ferrox non-author reviewer emits the second schema-validated artifact for the schema. Neither reviewer is the plan author.
- Every claimed reviewer MUST leave its own on-disk schema-validated `wayland-core.phase20-independent-review.v1` artifact; a prose-only review does not count toward PASS (REQ-native-r13).
- native_macos and native_windows are PASS checks here, NOT deferred — they are prosecuted against the retained 20-46 native evidence bound to the exact sealed SHA.
- FAIL-CLOSED: if Kimi K3 is unreachable or its output cannot be schema-validated, record an explicit incomplete disposition and BLOCK — do not synthesize a PASS (CONTEXT D5 fail-safe).
- Review the EXACT sealed successor SHA/tree recorded by 20-44 with the retained native evidence from 20-46; this plan writes only the review artifact.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Fresh native-inclusive review (Kimi K3 + ferrox), native NOT deferred, fail-closed</name>
  <files>.planning/phases/20-transactional-delegated-mutation/20-47-INDEPENDENT-REVIEW.md</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-46-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md, .planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md</read_first>
  <behavior>
    - Invoke Kimi K3 at its absolute path as the independent non-author reviewer of the exact sealed third-repaired-successor SHA/tree, prosecuting at every severity across code review, phase validation, and ASVS Level 2 security, AND reviewing the retained 20-46 native evidence with native_macos/native_windows as PASS checks (NOT deferred).
    - The review confirms on the retained evidence: all 6 Windows targets + all 8 macOS targets green for the exact sealed SHA; the two PRODUCTION isolation guardrails held on hardware (#A the DispatchAdmission budget is real, not defaulted-to-unlimited; #B the cmd payload quoting stayed a quoting-layer change with is_unc_or_device_path, the Low-IL token, the Job-Object limits, and the ACL lease intact); and the Kimi watch-items were exercised, not masked.
    - Record the review as a schema-validated `wayland-core.phase20-independent-review.v1` artifact (deferred empty, native included) for Kimi K3, and have a ferrox non-author reviewer emit its own schema-validated artifact over the same SHA.
    - Fail-closed: if Kimi K3 is unreachable or its output fails schema validation, record an explicit incomplete disposition and block — no false-green PASS.
  </behavior>
  <action>Run Kimi K3 (`/Users/seandonahoe/.kimi-code/bin/kimi -p "<prompt>" --output-format text`) as the independent non-author reviewer over the exact sealed successor SHA/tree with the retained 20-46 native evidence, prosecuting every severity across code review, phase validation, and ASVS Level 2 — with native_macos/native_windows as PASS checks, NOT deferred. Confirm all 6 Windows + all 8 macOS targets green for the exact SHA, both PRODUCTION isolation guardrails held on hardware, and the Kimi watch-items were exercised. Record Kimi K3's result as a schema-validated `wayland-core.phase20-independent-review.v1` artifact in `20-47-INDEPENDENT-REVIEW.md` and have a ferrox non-author reviewer emit its own schema-validated artifact over the same SHA. If Kimi K3 is unreachable or schema-invalid, record an explicit incomplete disposition and BLOCK. Implements REQ-native-r13/r12; write only the review artifact.</action>
  <verify>
    <automated>test -f .planning/phases/20-transactional-delegated-mutation/20-47-INDEPENDENT-REVIEW.md</automated>
    <automated>grep -nE "phase20-independent-review|native_windows|native_macos|source_sha|Kimi K3|PASS|deferred" .planning/phases/20-transactional-delegated-mutation/20-47-INDEPENDENT-REVIEW.md | head</automated>
  </verify>
  <acceptance_criteria>The review artifact exists and records a Kimi K3 schema-validated `wayland-core.phase20-independent-review.v1` result AND a ferrox schema-validated result over the exact sealed SHA, with native_macos/native_windows as PASS checks (NOT deferred) confirmed on the retained 20-46 evidence, both isolation guardrails held on hardware, and the watch-items exercised. A zero-finding PASS admits 20-48; any finding, or an unreachable/schema-invalid Kimi K3, blocks with an explicit incomplete disposition.</acceptance_criteria>
  <done>The sealed third-repaired successor has a fresh native-inclusive per-reviewer schema-validated review (Kimi K3 + ferrox), native not deferred, or an explicit fail-closed block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal preparation ← native-inclusive review PASS | Terminal prep (20-48) is admitted only by a zero-finding review with native NOT deferred |
| attestation integrity ← per-reviewer artifact | A PASS must be backed by a schema-validated artifact per claimed reviewer, not prose |
| native verdict ← retained 20-46 evidence | native_macos/native_windows PASS must derive from the retained hardware evidence bound to the exact sealed SHA |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-47-01 | Spoofing | deferred-native or prose-only PASS | high | mitigate | native_macos/native_windows are PASS checks bound to the retained 20-46 evidence (not deferred); every claimed reviewer emits its own schema-validated artifact; prose does not count. |
| T-20-47-02 | Repudiation | Kimi K3 unreachable false-green | high | mitigate | Fail-closed: an unreachable or schema-invalid Kimi K3 output records an explicit incomplete disposition and blocks; no PASS is synthesized (CONTEXT D5). |
| T-20-47-03 | Tampering | isolation guardrail regression unseen on hardware | high | mitigate | The review confirms on the retained evidence that #A (admission budget real) and #B (boundary primitives intact) held on hardware and the watch-items were exercised, so a hardware-level regression cannot pass silently. |
</threat_model>

<verification>
The review artifact `20-47-INDEPENDENT-REVIEW.md` determines acceptance: a Kimi K3 schema-validated `wayland-core.phase20-independent-review.v1` result AND a ferrox schema-validated result over the exact sealed successor SHA, both zero-finding, with native_macos/native_windows as PASS checks (NOT deferred), the two isolation guardrails confirmed on hardware, and the watch-items exercised. Record the sealed `source_sha`/tree, the two reviewer identities and schema-validation status, the native verdicts, and the disposition (PASS admits 20-48; any finding or an unreachable/invalid Kimi K3 blocks) in `20-47-SUMMARY.md`. No source/test/workflow change; no requirement completion.
</verification>

<success_criteria>
- Kimi K3 prosecutes the exact sealed successor at every severity AND reviews the 20-46 native evidence with native NOT deferred; a ferrox non-author reviewer emits its own schema-validated artifact (r13/r12).
- The review confirms all 6 Windows + all 8 macOS targets green for the exact SHA, both PRODUCTION isolation guardrails held on hardware, and the Kimi watch-items were exercised.
- A zero-finding PASS with native not deferred admits 20-48; any finding, or an unreachable/schema-invalid Kimi K3, blocks with an explicit incomplete disposition.
</success_criteria>

## Artifacts this phase produces
- The fresh native-inclusive per-reviewer schema-validated review (Kimi K3 + ferrox) of the sealed third-repaired-successor.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-47-SUMMARY.md` using the standard GSD summary template. Record the sealed `source_sha`/tree, the Kimi K3 + ferrox reviewer identities and schema-validation status, the native_macos/native_windows PASS verdicts on the retained evidence, the two hardware-confirmed isolation guardrails, and the disposition (PASS or fail-closed incomplete). Complete no Phase 20 requirement.
</output>
</content>

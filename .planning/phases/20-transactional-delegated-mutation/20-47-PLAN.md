---
phase: 20-transactional-delegated-mutation
plan: "47"
type: execute
wave: 46
depends_on:
  - "20-46"
files_modified:
  - .planning/phases/20-transactional-delegated-mutation/20-47-INDEPENDENT-REVIEW.md
autonomous: true
requirements:
  - REQ-native-r13
  - REQ-native-r12
must_haves:
  truths:
    - "A fresh non-author FOUR-WAY review panel — Codex 5.6 Sol (`codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check \"<prompt>\"`), Gemini 3.1 Pro (`gemini -p \"<prompt>\" -m gemini-3.1-pro-preview -o text --approval-mode plan`), and Kimi K3 (`/Users/seandonahoe/.kimi-code/bin/kimi -p \"<prompt>\" --output-format text`, absolute path) run in parallel, plus an internal Claude non-author adversarial reviewer prompted to REFUTE (default-refuted-if-uncertain) — prosecutes the exact SEALED third-repaired-successor `source_sha 3f839309` (tree `3092475b`) at every severity across code review, phase validation, and ASVS Level 2 security, AND reviews the retained 20-46 native evidence: native_macos and native_windows are PASS checks, NOT deferred (the review the audit demanded) (REQ-native-r13/r12)."
    - "Each of the four reviewers emits its OWN on-disk schema-validated artifact (deferred empty — native included): Codex Sol → `f20-native-crossaudit.codex-sol`, Gemini Pro → `f20-native-crossaudit.gemini-pro`, Kimi K3 → `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial reviewer → `wayland-core.phase20-independent-review.v1` — no prose-only review counts toward the PASS (REQ-native-r13)."
    - "The panel confirms on the retained native evidence that all 6 Windows targets and all 8 macOS targets are green for the exact sealed SHA, the two PRODUCTION isolation guardrails held on hardware (#A admission budget real; #B boundary primitives intact), and the Kimi watch-items were exercised, not masked. Only a zero-finding PASS from ALL FOUR reviewers with native NOT deferred admits terminal preparation (20-48); any single finding at any severity from any auditor stops the sequence for a further repaired successor and fresh review (REQ-native-r13/r12)."
    - "Fail-closed: if ANY external reviewer (Codex Sol / Gemini Pro / Kimi K3) is unreachable or its output cannot be schema-validated, the gate records an explicit incomplete disposition and BLOCKS rather than emitting a false-green PASS from a partial panel; the internal Claude adversarial pass is always reachable but is one of the four required votes, never a substitute for a missing external reviewer (mirrors CONTEXT D5 fail-safe) (REQ-native-r13)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-47-INDEPENDENT-REVIEW.md
      provides: "Fresh non-author four-way all-severity review (Codex Sol + Gemini Pro + Kimi K3 + internal Claude adversarial) of the sealed third-repaired-successor (3f839309) WITH native evidence, native not deferred, schema-validated per reviewer"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-47-INDEPENDENT-REVIEW.md
      to: .planning/phases/20-transactional-delegated-mutation/20-46-SUMMARY.md
      via: "the four-way panel consumes the retained dual-OS native evidence as the native_macos/native_windows PASS basis"
      pattern: "native_windows"
---

<objective>
Perform the fresh 20-16-role independent review the audit required, now against the third repaired successor with a FOUR-WAY panel — Codex 5.6 Sol, Gemini 3.1 Pro, and Kimi K3 (three external CLIs run in parallel), plus an internal Claude non-author adversarial reviewer: prosecute the exact sealed successor (`source_sha 3f839309`, tree `3092475b`) at every severity AND review the real native evidence from 20-46 — native NOT deferred — with each reviewer recording its OWN schema-validated artifact. ALL FOUR MUST PASS.

Purpose: The original 20-16 passed CLEAN but with native deferred by design, and the prior native re-dispatches (20-25, 20-32, and the daf27337 diagnostic run 30064412019) went RED before a native-inclusive review could run. This review closes the gap on the third repaired successor: the four-independent-auditor panel reviews the actual Windows+macOS native evidence — including the two fixed PRODUCTION root causes (#A capacity-probe admission across all Windows dispatch/lifecycle targets; #B granted-read exit-0 and fixed containment) and the exercised Kimi watch-items — not a promise, and every reviewer leaves an on-disk schema-validated artifact. Four independent auditors, all-must-pass, close the shared-blind-spot risk that a single reviewer could carry into terminal preparation.
Output: Four schema-validated review artifacts over the sealed successor SHA with native as PASS checks — Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1`. A zero-finding PASS from ALL FOUR admits 20-48; if any external reviewer is unreachable or schema-invalid, an explicit incomplete disposition (no false green from a partial panel).
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md
@.planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-46-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md
</context>

<execution_rules>
- The review is a FOUR-WAY panel of independent (non-author) reviewers; NONE is the plan author. Three are external CLIs, invoked verbatim and run in PARALLEL:
  1. Codex 5.6 Sol — `codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check "<prompt>"`
  2. Gemini 3.1 Pro — `gemini -p "<prompt>" -m gemini-3.1-pro-preview -o text` (add `--approval-mode plan` for a read-only lock)
  3. Kimi K3 — `/Users/seandonahoe/.kimi-code/bin/kimi -p "<prompt>" --output-format text` (ABSOLUTE path — the Bash shell PATH omits it)
  The fourth is an internal Claude non-author adversarial subagent, prompted to REFUTE the candidate (default-refuted-if-uncertain). The audit demanded the fresh review NOT defer native, so all four review the actual retained 20-46 native evidence.
- Every one of the four reviewers MUST leave its OWN on-disk schema-validated artifact. The three external legs emit `f20-native-crossaudit` variants keyed by reviewer id — Codex Sol → `f20-native-crossaudit.codex-sol`, Gemini Pro → `f20-native-crossaudit.gemini-pro`, Kimi K3 → `f20-native-crossaudit.kimi-k3`; the internal Claude adversarial leg emits `wayland-core.phase20-independent-review.v1`. A prose-only review does not count toward PASS (REQ-native-r13).
- native_macos and native_windows are PASS checks here, NOT deferred — they are prosecuted by every reviewer against the retained 20-46 native evidence bound to the exact sealed SHA `3f839309` (tree `3092475b`).
- DISPOSITION — ALL FOUR MUST PASS: only a zero-finding PASS from all four reviewers (native NOT deferred) admits 20-48. Any single finding from any one reviewer at any severity BLOCKS and routes to a further repaired successor and fresh review.
- FAIL-CLOSED: if ANY external reviewer (Codex Sol / Gemini Pro / Kimi K3) is unreachable or its output cannot be schema-validated, record an explicit incomplete disposition and BLOCK — do not synthesize a PASS from the remaining reviewers (CONTEXT D5 fail-safe). The internal Claude adversarial pass is always reachable but is one of the four required votes, not a substitute for a missing external reviewer.
- Review the EXACT sealed successor SHA/tree recorded by 20-44 — `source_sha 3f839309` (tree `3092475b`) — with the retained native evidence from 20-46; this plan writes only the review artifact.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Fresh four-way native-inclusive review (Codex Sol + Gemini Pro + Kimi K3 + Claude adversarial), native NOT deferred, all-must-pass, fail-closed</name>
  <files>.planning/phases/20-transactional-delegated-mutation/20-47-INDEPENDENT-REVIEW.md</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-46-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md, .planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md</read_first>
  <behavior>
    - Dispatch the FOUR-WAY independent panel over the exact sealed third-repaired-successor SHA/tree (`source_sha 3f839309`, tree `3092475b`). Run the three external CLIs in parallel — Codex 5.6 Sol (`codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check`), Gemini 3.1 Pro (`gemini -m gemini-3.1-pro-preview -o text --approval-mode plan`), Kimi K3 (`/Users/seandonahoe/.kimi-code/bin/kimi --output-format text`, absolute path) — plus an internal Claude non-author adversarial subagent prompted to REFUTE (default-refuted-if-uncertain). Every reviewer prosecutes at every severity across code review, phase validation, and ASVS Level 2 security, AND reviews the retained 20-46 native evidence with native_macos/native_windows as PASS checks (NOT deferred).
    - Each reviewer confirms on the retained evidence: all 6 Windows targets + all 8 macOS targets green for the exact sealed SHA; the two PRODUCTION isolation guardrails held on hardware (#A the DispatchAdmission budget is real, not defaulted-to-unlimited; #B the cmd payload quoting stayed a quoting-layer change with is_unc_or_device_path, the Low-IL token, the Job-Object limits, and the ACL lease intact); and the Kimi watch-items were exercised, not masked.
    - Record each reviewer's result as its OWN schema-validated artifact (deferred empty, native included): Codex Sol → `f20-native-crossaudit.codex-sol`, Gemini Pro → `f20-native-crossaudit.gemini-pro`, Kimi K3 → `f20-native-crossaudit.kimi-k3`, internal Claude adversarial → `wayland-core.phase20-independent-review.v1`.
    - Disposition — ALL FOUR MUST PASS: only a zero-finding PASS from all four (native NOT deferred) admits 20-48; any single finding from any reviewer blocks and routes to a further repaired successor.
    - Fail-closed: if ANY external reviewer (Codex Sol / Gemini Pro / Kimi K3) is unreachable or its output fails schema validation, record an explicit incomplete disposition and block — no false-green PASS from a partial panel.
  </behavior>
  <action>Dispatch the four-way independent panel over the exact sealed successor SHA/tree recorded by 20-44 (`source_sha 3f839309`, tree `3092475b`) with the retained 20-46 native evidence: run the three external CLIs in parallel — Codex 5.6 Sol (`codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check "<prompt>"`), Gemini 3.1 Pro (`gemini -p "<prompt>" -m gemini-3.1-pro-preview -o text --approval-mode plan`), and Kimi K3 (`/Users/seandonahoe/.kimi-code/bin/kimi -p "<prompt>" --output-format text`) — plus an internal Claude non-author adversarial subagent prompted to REFUTE (default-refuted-if-uncertain), each prosecuting every severity across code review, phase validation, and ASVS Level 2, with native_macos/native_windows as PASS checks (NOT deferred). Confirm all 6 Windows + all 8 macOS targets green for the exact SHA, both PRODUCTION isolation guardrails held on hardware, and the Kimi watch-items were exercised. Record each reviewer's result as its own schema-validated artifact in `20-47-INDEPENDENT-REVIEW.md` — `f20-native-crossaudit.codex-sol`, `f20-native-crossaudit.gemini-pro`, `f20-native-crossaudit.kimi-k3`, and the internal Claude `wayland-core.phase20-independent-review.v1`. ALL FOUR must PASS zero-finding to admit 20-48; any single finding blocks. If ANY external reviewer is unreachable or schema-invalid, record an explicit incomplete disposition and BLOCK. Implements REQ-native-r13/r12; write only the review artifact.</action>
  <verify>
    <automated>test -f .planning/phases/20-transactional-delegated-mutation/20-47-INDEPENDENT-REVIEW.md</automated>
    <automated>grep -nE "f20-native-crossaudit\.codex-sol|f20-native-crossaudit\.gemini-pro|f20-native-crossaudit\.kimi-k3|phase20-independent-review|Codex 5\.6 Sol|Gemini 3\.1 Pro|Kimi K3|Claude adversarial|native_windows|native_macos|3f839309|source_sha|PASS|deferred" .planning/phases/20-transactional-delegated-mutation/20-47-INDEPENDENT-REVIEW.md | head -40</automated>
  </verify>
  <acceptance_criteria>The review artifact exists and records FOUR per-reviewer schema-validated results over the exact sealed SHA (3f839309) — Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1` — with native_macos/native_windows as PASS checks (NOT deferred) confirmed on the retained 20-46 evidence, both isolation guardrails held on hardware, and the watch-items exercised. A zero-finding PASS from ALL FOUR admits 20-48; any single finding from any auditor, or any external reviewer unreachable/schema-invalid, blocks with an explicit incomplete disposition.</acceptance_criteria>
  <done>The sealed third-repaired successor has a fresh native-inclusive four-way per-reviewer schema-validated review (Codex Sol + Gemini Pro + Kimi K3 + Claude adversarial), native not deferred, or an explicit fail-closed block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal preparation ← four-way native-inclusive review PASS | Terminal prep (20-48) is admitted only by a zero-finding review from ALL FOUR panel reviewers with native NOT deferred |
| attestation integrity ← per-reviewer artifact | A PASS must be backed by a schema-validated artifact per each of the four reviewers, not prose |
| native verdict ← retained 20-46 evidence | native_macos/native_windows PASS must derive from the retained hardware evidence bound to the exact sealed SHA |
| panel independence ← external-reviewer reachability | If ANY external reviewer (Codex Sol / Gemini Pro / Kimi K3) is down, the review must fail closed rather than pass on a partial panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-47-01 | Spoofing | deferred-native or prose-only/partial-panel PASS | high | mitigate | native_macos/native_windows are PASS checks bound to the retained 20-46 evidence (not deferred); each of the four reviewers emits its own schema-validated artifact and PASS requires ALL FOUR; prose does not count. |
| T-20-47-02 | Repudiation | external reviewer unreachable false-green | high | mitigate | Fail-closed: any unreachable or schema-invalid external-reviewer output records an explicit incomplete disposition and blocks; no PASS is synthesized from the remaining reviewers (CONTEXT D5). |
| T-20-47-03 | Tampering | isolation guardrail regression unseen on hardware | high | mitigate | All four reviewers confirm on the retained evidence that #A (admission budget real) and #B (boundary primitives intact) held on hardware and the watch-items were exercised, so a hardware-level regression cannot pass a four-independent-auditor panel silently. |
</threat_model>

<verification>
The review artifact `20-47-INDEPENDENT-REVIEW.md` determines acceptance: FOUR per-reviewer schema-validated results over the exact sealed successor SHA `3f839309` (tree `3092475b`) — Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1` — ALL zero-finding, with native_macos/native_windows as PASS checks (NOT deferred), the two isolation guardrails confirmed on hardware, and the watch-items exercised. Record the sealed `source_sha`/tree, the four reviewer identities and each artifact's schema-validation status, the three external CLI invocations, the native verdicts, and the disposition (ALL FOUR PASS admits 20-48; any single finding or any external reviewer unreachable/invalid blocks) in `20-47-SUMMARY.md`. No source/test/workflow change; no requirement completion.
</verification>

<success_criteria>
- The four-way panel (Codex 5.6 Sol + Gemini 3.1 Pro + Kimi K3 external, run in parallel, plus the internal Claude adversarial reviewer) prosecutes the exact sealed successor at every severity AND reviews the 20-46 native evidence with native NOT deferred; each reviewer emits its own schema-validated artifact — `f20-native-crossaudit.codex-sol` / `.gemini-pro` / `.kimi-k3` and `wayland-core.phase20-independent-review.v1` (r13/r12).
- The panel confirms all 6 Windows + all 8 macOS targets green for the exact SHA, both PRODUCTION isolation guardrails held on hardware, and the Kimi watch-items were exercised.
- A zero-finding PASS from ALL FOUR reviewers with native not deferred admits 20-48; any single finding from any auditor, or any external reviewer unreachable/schema-invalid, blocks with an explicit incomplete disposition (no false green from a partial panel).
</success_criteria>

## Artifacts this phase produces
- The fresh native-inclusive four-way per-reviewer schema-validated review of the sealed third-repaired-successor (`3f839309`): Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1`.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-47-SUMMARY.md` using the standard GSD summary template. Record the sealed `source_sha`/tree (`3f839309` / `3092475b`), the four reviewer identities (Codex Sol, Gemini Pro, Kimi K3, internal Claude adversarial) and each artifact's schema-validation status, the native_macos/native_windows PASS verdicts on the retained evidence, the two hardware-confirmed isolation guardrails, and the disposition (ALL-FOUR PASS or fail-closed incomplete). Complete no Phase 20 requirement.
</output>
</content>

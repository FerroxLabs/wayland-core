---
phase: 20-transactional-delegated-mutation
plan: "17"
type: execute
wave: 16
depends_on:
  - "20-16"
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "The exact source candidate qualified with zero findings by 20-16 is reauthenticated before any native-proof request exists."
    - "Existing focused receipts are reused only after exact source/tree, command, environment, exit-status, and log-digest authentication."
    - "One strict pending request records one eligible source/ref/runner/image tuple in a deterministic source-keyed proof checkout without authorizing, publishing, or dispatching it."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-17-SUMMARY.md
      provides: "Durable exact pending native-proof tuple for terminal authorization"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-16-SUMMARY.md
      to: .planning/phases/20-transactional-delegated-mutation/20-17-SUMMARY.md
      via: "reauthenticated source/review tuple and pending-request identity"
      pattern: "source_sha"
---

<objective>
Prepare one durable native-proof request for the exact independently reviewed Phase 20 candidate.

Purpose: finish every safe, read-only or Git-private preparation step before the separate human authorization plan begins.
Output: one standard GSD summary containing the immutable pending tuple; no publication, dispatch, or requirement completion.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-08-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-08-INDEPENDENT-REVIEW.md
@.planning/phases/20-transactional-delegated-mutation/20-16-SUMMARY.md
@.planning/scripts/verify-review-pair.sh
@.planning/scripts/verify-review-result.mjs
@scripts/f20-native-uat-proof.mjs
@scripts/f20-native-uat-proof.test.mjs
@.github/workflows/nightly-windows-soak.yml
</context>

<admission>
- Read the exact `(source_sha, source_tree, review_base_sha, review_sha)` tuple from `20-16-SUMMARY.md`; rerun `verify-review-pair.sh` over the complete 20-16 source set and run `verify-review-result.mjs` profile `f20-16` against the exact review commit blob. Any mismatch, failed evidence command, nonzero finding, or non-PASS disposition stops.
- Derive one deterministic proof checkout location from the absolute primary Git directory plus the exact source SHA. Before creating or opening it, atomically persist a no-follow mode-0600 locator/state object under that primary Git directory. Re-entry must reopen the same locator and checkout; an unexpected existing path, different source, malformed state, symlink, FIFO, or ambiguous checkout fails closed.
- Materialize that one read-only evidence checkout at the exact reviewed source SHA. Do not edit, format, commit, merge, rebase, or run Cargo on the Mac. Verify its HEAD/tree and source blobs match the admitted tuple. It is not a second GSD or source-mutation authority.
- Git-private pending-request state is the only durable authority. The request operation must be exact-tuple idempotent: an identical pending request is reused; any conflicting existing state fails closed.
</admission>

<tasks>

<task type="auto">
  <name>Task 1: Revalidate the reviewed source and persist one pending native-proof request</name>
  <files>None; this task writes only strict Git-private proof-checkout authority objects and the standard GSD summary.</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-16-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-08-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-08-INDEPENDENT-REVIEW.md, .planning/scripts/verify-review-result.mjs, scripts/f20-native-uat-proof.mjs, scripts/f20-native-uat-proof.test.mjs</read_first>
  <behavior>
    - Reauthenticate the exact 20-16 source/review tuple and the retained `f20-08-e2e` and `f20-08-clippy` receipts; never rerun unchanged focused proof.
    - Read-only runner preflight admits exactly one online idle runner carrying `f20-native-macos`, `f20-ephemeral`, `f20-no-ambient-secrets`, and exactly one `f20-image-&lt;64 lowercase hex&gt;` label.
    - `f20-native-uat-proof.mjs request` exclusively persists the candidate commit/tree/ref and runner ID/name/image label with state `pending` in a strict no-follow mode-0600 Git-private object.
    - Re-execution first follows the primary-Git-dir locator and returns the existing identical pending object without a second checkout or request; a different tuple, malformed object, non-pending state, or ambiguous runner fails closed.
    - This plan does not authorize, publish, dispatch, retry, merge, release, deploy, tag, close an issue, delete a ref, or complete a requirement.
  </behavior>
  <action>Authenticate the exact review pair and schema-validated `f20-16` result; atomically create or reopen the primary-Git-dir locator for the deterministic source-SHA-keyed immutable proof checkout; validate that checkout; authenticate the retained focused receipts; switch GitHub auth to `FerroxLabs` immediately before runner inspection; run only the helper's read-only runner preflight; and create or reuse the identical strict pending request. Record the locator/state digest, checkout identity, candidate/ref/runner/image tuple, and pending-object digest in `20-17-SUMMARY.md`. Stop on any invalid or conflicting state.</action>
  <verify>The exact reviewed source has authenticated focused receipts and exactly one durable pending request; no authorization, publication, dispatch, or external source mutation exists.</verify>
  <acceptance_criteria>Preparation is crash-safe and exact-tuple idempotent. Re-entry cannot repeat expensive proof or create a second pending request. All Phase 20 requirements remain incomplete.</acceptance_criteria>
  <done>One standard GSD summary durably hands the immutable pending tuple to plan 20-18.</done>
</task>

</tasks>

<verification>
Reauthenticate the exact 20-16 review tuple, focused receipt identities, clean proof-checkout commit/tree, and strict pending-request bytes. No Cargo runs on the local Mac and no external mutation occurs.
</verification>

<success_criteria>
- Preparation is complete and durably summarized before the human decision boundary; that verified summary alone admits plan 20-18.
- An interrupted or repeated execution reuses only the identical pending tuple and cannot repeat focused proof or create conflicting authority.
- Native UAT, aggregate proof, requirement completion, and phase completion remain pending for 20-18.
</success_criteria>

## Artifacts this phase produces

- `.planning/phases/20-transactional-delegated-mutation/20-17-SUMMARY.md` — immutable pending-request handoff with exact candidate and runner tuple.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-17-SUMMARY.md` using the standard GSD summary template. Record exact source/review authority, authenticated focused receipt identities, proof-checkout identity, pending-request digest, runner tuple, and explicit non-claims. Do not mark any Phase 20 requirement complete.
</output>

---
phase: 20-transactional-delegated-mutation
plan: "17"
type: execute
wave: 16
depends_on:
  - "20-16"
files_modified: []
autonomous: false
requirements:
  - F20-01
  - F20-02
  - F20-03
  - F20-04
  - F20-05
  - F20-06
  - F20-GATE-01
  - F20-GATE-02
must_haves:
  truths:
    - "The exact source candidate qualified with zero findings by 20-16 is the only candidate eligible for terminal proof."
    - "One explicitly authorized candidate-specific workflow run proves native Windows and native macOS behavior against that unchanged source commit/tree; missing infrastructure, skipped targets, incomplete logs, retry, or source repair fails closed."
    - "The same exact source passes aggregate workspace test and build on Hetzner before any requirement or phase-completion claim."
    - "Implementation, independent review, native UAT, and aggregate proof remain separately identified factual states."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-16-SUMMARY.md
      provides: "Exact reviewed source/review authority tuple and zero-finding admission"
    - path: scripts/f20-native-uat-proof.mjs
      provides: "Candidate-specific publication, dispatch, log capture, and evidence verification"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-16-SUMMARY.md
      to: scripts/f20-native-uat-proof.mjs
      via: "exact reviewed source SHA/tree authorizes only the displayed UAT candidate"
      pattern: "source_sha"
---

<objective>
Run terminal native and aggregate acceptance against the exact independently reviewed Phase 20 source candidate.

Purpose: keep external UAT and requirement completion strictly downstream of a fresh zero-finding source review.
Output: candidate-bound native Windows/macOS and aggregate Hetzner evidence, or an explicit incomplete result.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
@/Users/seandonahoe/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-08-PLAN.md
@.planning/phases/20-transactional-delegated-mutation/20-08-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-08-INDEPENDENT-REVIEW.md
@.planning/phases/20-transactional-delegated-mutation/20-16-SUMMARY.md
@scripts/f20-native-windows-proof.ps1
@scripts/f20-native-macos-proof.sh
@scripts/f20-native-uat-proof.mjs
@scripts/f20-native-uat-proof.test.mjs
@.github/workflows/nightly-windows-soak.yml
</context>

<admission>
- Read the exact `(source_sha, source_tree, review_base_sha, review_sha)` tuple from `20-16-SUMMARY.md` and require a zero-finding PASS. Re-run `verify-review-pair.sh` with the same source set declared by 20-16 before any external request.
- Materialize one clean disposable standalone proof checkout at the exact reviewed source SHA. The checkout is execution input only: do not edit, format, commit, merge, rebase, or run Cargo on the Mac. Verify its HEAD/tree and source blobs match the admitted tuple.
- Candidate authorization and run identity persist only in strict Git-private files belonging to that proof checkout. Shell variables, conversation memory, previous general authorization, or mutable path reopens never carry authority across the checkpoint.
</admission>

<tasks>

<task type="auto">
  <name>Task 1: Revalidate reviewed source and prepare one native-proof request</name>
  <files>None; this task writes only strict Git-private proof-checkout authority objects and external proof receipts.</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-16-SUMMARY.md, scripts/f20-native-uat-proof.mjs, scripts/f20-native-uat-proof.test.mjs</read_first>
  <behavior>
    - The exact 20-16-qualified source/review tuple is reauthenticated before preparation begins.
    - Focused `f20-08-e2e` and `f20-08-clippy` proof passes on Hetzner against a clean disposable standalone checkout of that exact source SHA/tree.
    - Read-only runner preflight admits exactly one online idle runner carrying `f20-native-macos`, `f20-ephemeral`, `f20-no-ambient-secrets`, and exactly one `f20-image-&lt;64 lowercase hex&gt;` label.
    - `f20-native-uat-proof.mjs request` exclusively persists the displayed candidate commit/tree/ref and runner ID/name/image label with state `pending` in a strict no-follow mode-0600 Git-private object.
    - This task does not authorize, publish, dispatch, retry, merge, release, deploy, tag, close an issue, or delete a ref.
  </behavior>
  <action>Read and authenticate the exact `(source_sha, source_tree, review_base_sha, review_sha)` tuple from `20-16-SUMMARY.md`; require a zero-finding PASS; and re-run `verify-review-pair.sh` over the source set declared by 20-16. Materialize one clean disposable standalone proof checkout at the exact reviewed source SHA and verify its HEAD, tree, and source blobs. Run the exact focused Hetzner `f20-08-e2e` and `f20-08-clippy` commands against it. Switch GitHub auth to `FerroxLabs`, run only the helper's read-only runner preflight, then invoke `f20-native-uat-proof.mjs request` to persist the pending exact tuple. Print the exact tuple required by Task 2. Any mismatch, failed focused gate, missing or ambiguous runner, stale object, or unavailable infrastructure fails closed before the checkpoint.</action>
  <verify>The exact reviewed source has green focused Hetzner receipts and the strict pending request names exactly one eligible candidate/ref/runner/image tuple. No publication, dispatch, or authorization exists.</verify>
  <done>One immutable pending request tuple is prepared and displayed for a human decision; all Phase 20 requirements remain incomplete.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Authorize or hold the prepared exact-candidate native proof</name>
  <files>None; this checkpoint performs no command and changes no file or external state.</files>
  <decision>After the exact 20-16-qualified source passes focused Hetzner revalidation and exactly one idle, clean-image, no-ambient-secret, ephemeral Docker-Desktop-capable macOS runner is observed, authorize one publication of the displayed exact candidate to `refs/heads/uat/f20-&lt;candidate-short-sha&gt;` and one `nightly-windows-soak.yml` dispatch, or hold with every Phase 20 requirement incomplete.</decision>
  <context>Task 1 already completed all automation and displayed the exact pending candidate commit/tree/ref plus runner ID/name/image tuple. This checkpoint asks only for a decision about those immutable prepared bytes. Approval must name the unchanged displayed tuple and authorizes no retry or successor. Prior general authorization and silence are not authorization.</context>
  <options>
    <option id="authorize"><name>Authorize exact-candidate native proof</name><pros>Enables native Windows and macOS acceptance against the independently reviewed source.</pros><cons>Publishes one temporary UAT branch and consumes GitHub Actions plus the external macOS runner.</cons></option>
    <option id="hold"><name>Hold locally</name><pros>Performs no external mutation.</pros><cons>All Phase 20 requirements remain incomplete.</cons></option>
  </options>
  <action>Do not run a command. Display the exact pending tuple produced by Task 1 and stop until the resume signal supplies a tuple-specific decision.</action>
  <verify>The human response is exactly `authorize` for the complete displayed tuple or `hold`; no automated command, prior approval, or inferred intent substitutes for that response.</verify>
  <done>Task 3 remains blocked unless the recorded response is `authorize` for the exact displayed tuple. A `hold` response ends this plan incomplete without external mutation.</done>
  <resume-signal>Reply `authorize` for the displayed exact candidate tuple, or `hold`. Silence and prior general authorization mean hold.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Persist authorization, execute native acceptance, and aggregate proof</name>
  <files>None; this task produces candidate-bound execution evidence consumed by the plan summary.</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-16-SUMMARY.md, scripts/f20-native-windows-proof.ps1, scripts/f20-native-macos-proof.sh, scripts/f20-native-uat-proof.mjs, scripts/f20-native-uat-proof.test.mjs, .github/workflows/nightly-windows-soak.yml</read_first>
  <behavior>
    - `authorize` first revalidates the candidate, exact pending request bytes, runner identity, and tuple-specific human response before exclusively persisting authorization.
    - `publish-dispatch` publishes only the authorized full source SHA to its exact temporary UAT ref with hooks disabled, confirms the remote ref, dispatches exactly once, and binds the new attempt-1 run to the retained authorization bytes.
    - `capture-verify` validates the recorded run, exact Windows and macOS job/runner identities, all ordered per-target and final commit/tree markers, exact captured log bytes, and macOS runner deregistration.
    - After evidence capture, the exact UAT ref is retained as evidence. Deletion is a separate exact-ref mutation requiring new Sean authorization and must fail closed if the remote ref no longer names the proved source SHA.
    - No retry, reused run, source repair, candidate substitution, skipped/duplicate/reordered marker, mutable-state/log replacement, remaining ephemeral runner, or incomplete/nonzero job can pass.
    - Only after native proof is green do the aggregate workspace test and build run on Hetzner against the same exact source SHA/tree.
  </behavior>
  <action>On continuation after the exact tuple-specific `authorize` response, run `f20-native-uat-proof.mjs authorize` to revalidate and exclusively persist that authorization. Then run `publish-dispatch` once and `capture-verify` once from the immutable proof checkout. Verify publication and evidence with the helper. Record the UAT evidence ref as retained; do not delete it without a later exact-ref Sean authorization and an unchanged-remote-SHA check. Then run the aggregate workspace test and build through the remote Hetzner harness against that exact checkout. Do not retry, repair source, merge, release, deploy, tag, close an issue, or delete the UAT ref. A repaired successor returns to 20-08 construction and requires a fresh 20-16 review plus new tuple-specific authorization.</action>
  <verify>
    <automated>node scripts/f20-native-uat-proof.mjs verify-publication --git-dir "$(git rev-parse --absolute-git-dir)" --repo FerroxLabs/wayland-core</automated>
    <automated>node scripts/f20-native-uat-proof.mjs verify-evidence --git-dir "$(git rev-parse --absolute-git-dir)" --repo FerroxLabs/wayland-core --workflow nightly-windows-soak.yml</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-aggregate-test "$(pwd -P)" test --workspace --all-features</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-aggregate-build "$(pwd -P)" build --workspace --all-features</automated>
  </verify>
  <acceptance_criteria>Both native platforms and both aggregate gates pass against the unchanged independently reviewed source. Any other state is incomplete, never skipped or accepted.</acceptance_criteria>
  <done>The exact reviewed candidate has complete candidate-bound native and aggregate evidence and is eligible for factual requirement completion.</done>
</task>

</tasks>

<verification>
Authenticate the exact 20-16 review tuple, the focused committed-head receipts, the one newly dispatched native Windows/macOS run, and both aggregate Hetzner receipts. All evidence must bind the same exact source SHA/tree. The later stock GSD phase verifier may authenticate these receipts but does not replace or reinterpret a missing gate. No Cargo runs on the local Mac.
</verification>

<success_criteria>
- The exact source candidate passed a fresh independent all-severity review before any external UAT mutation.
- Native Windows and macOS retained filesystem authority, hard descendant containment, Docker Desktop transport, public dispatch, and complete lifecycle tests pass once against that unchanged source after explicit tuple-specific authorization.
- Aggregate workspace test and build pass on Hetzner against the same exact source.
- Every Phase 20 requirement is completed only when all preceding evidence is green and source identity remains unchanged; otherwise all remain incomplete.
</success_criteria>

## Artifacts this phase produces

- Candidate-bound native Windows and macOS workflow evidence.
- Exact-source aggregate Hetzner test/build receipts.
- Terminal Phase 20 completion or explicit incomplete disposition.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-17-SUMMARY.md` using the standard GSD summary template. Record the 20-16 source/review tuple, focused proof, the single-run native Windows/macOS results, aggregate test/build receipts, exact requirement disposition, and every limitation as distinct factual states. Do not create a completion summary or mark any Phase 20 requirement complete when review, focused proof, either native platform, or either aggregate proof is missing, skipped, reused, or red.
</output>

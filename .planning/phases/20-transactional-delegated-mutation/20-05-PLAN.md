---
phase: 20-transactional-delegated-mutation
plan: "05"
type: execute
wave: 11
depends_on:
  - "20-03"
  - "20-04"
  - "20-14"
files_modified:
  - crates/wcore-agent/src/orchestration/anvil/engine.rs
  - crates/wcore-agent/src/orchestration/anvil/forge.rs
  - crates/wcore-agent/src/orchestration/anvil/seat.rs
  - crates/wcore-agent/src/orchestration/council/run.rs
  - crates/wcore-agent/tests/common/mod.rs
  - crates/wcore-agent/tests/crucible_council.rs
  - crates/wcore-agent/tests/anvil_forge_transaction.rs
  - crates/wcore-cli/src/anvil.rs
  - crates/wcore-cli/src/crucible.rs
  - crates/wcore-cli/src/workflow.rs
autonomous: true
requirements:
  - F20-01
  - F20-02
must_haves:
  truths:
    - "Anvil, Council, Crucible, and workflow CLI delegation all use the same workspace-aware production spawner."
    - "No orchestration caller can silently bypass classification or launch mutating work in the parent checkout."
    - "Existing read-only orchestration behavior remains compatible while mutation fails closed without isolation."
  artifacts:
    - path: crates/wcore-agent/src/orchestration/anvil/engine.rs
      provides: "Each BuiltCandidate bound to its own opaque transaction/candidate identity and winner-only selection"
    - path: crates/wcore-agent/src/orchestration/anvil/forge.rs
      provides: "Anvil Forge use of one transaction-owned checkout per candidate with loser cleanup"
    - path: crates/wcore-agent/src/orchestration/anvil/seat.rs
      provides: "Anvil seat propagation of workspace authority"
    - path: crates/wcore-agent/src/orchestration/council/run.rs
      provides: "Council participant propagation of workspace authority"
    - path: crates/wcore-agent/tests/crucible_council.rs
      provides: "Cross-orchestrator isolation and denial coverage"
    - path: crates/wcore-agent/tests/anvil_forge_transaction.rs
      provides: "Forge candidate-identity and no-process-CWD-mutation regression coverage"
    - path: crates/wcore-cli/src/workflow.rs
      provides: "CLI workflow construction using the production spawner contract"
  key_links:
    - from: crates/wcore-agent/src/orchestration/anvil/forge.rs
      to: crates/wcore-agent/src/orchestration/anvil/engine.rs
      via: "each BuiltCandidate preserves its own production-spawner transaction identity through selection"
      pattern: "BuiltCandidate"
    - from: crates/wcore-agent/src/orchestration/anvil/forge.rs
      to: crates/wcore-agent/src/durable_spawner.rs
      via: "each Forge build receives its own transaction-owned standalone checkout instead of creating another worktree"
      pattern: "workspace"
    - from: crates/wcore-agent/src/orchestration/anvil/seat.rs
      to: crates/wcore-agent/src/spawner.rs
      via: "seat dispatch request"
      pattern: "spawn"
    - from: crates/wcore-cli/src/workflow.rs
      to: crates/wcore-agent/src/bootstrap.rs
      via: "production engine/bootstrap construction"
      pattern: "bootstrap"
---

<objective>
Close every known orchestration caller over the workspace-aware spawner contract.

Purpose: Prevent Anvil, Council, Crucible, or CLI workflows from remaining legacy escape hatches around transactional delegation.
Output: Caller wiring and cross-orchestrator regression tests.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-04-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-06-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-14-SUMMARY.md
@crates/wcore-swarm/src/worktree/candidate.rs
@crates/wcore-agent/src/spawner.rs
@crates/wcore-agent/src/orchestration/anvil/engine.rs
@crates/wcore-agent/src/orchestration/anvil/forge.rs
@crates/wcore-agent/src/orchestration/anvil/seat.rs
@crates/wcore-agent/src/orchestration/council/run.rs
@crates/wcore-cli/src/workflow.rs
</context>

<salvage>
Adapt only owned seat, Council, common fixtures, Crucible/Council tests, and CLI material from `e200c0a178feb698af350312a80a33d5b04fc699`. Construct Forge explicit-root transaction behavior fresh. Reject every second-checkout or process-global-CWD behavior. Record path-level adopted/adapted/rejected dispositions in the SUMMARY; do not cherry-pick.
</salvage>

<execution_rules>
- Require a clean isolated checkout and record its exact starting HEAD at `$(git rev-parse --git-path gsd-task-base-20-05)` before Task 1 edits.
- Every task scope check uses the complete plan-base union through that task, including committed, staged, unstaged, untracked, deleted, renamed, and copied paths.
- Do not run Cargo on macOS; use exact-file `vx rustfmt` for construction and the Hetzner harness for authoritative Cargo proof.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Route Anvil Forge, seats, and Council through transactional delegation</name>
  <files>crates/wcore-agent/src/orchestration/anvil/engine.rs, crates/wcore-agent/src/orchestration/anvil/forge.rs, crates/wcore-agent/src/orchestration/anvil/seat.rs, crates/wcore-agent/src/orchestration/council/run.rs</files>
  <read_first>crates/wcore-swarm/src/worktree/candidate.rs, crates/wcore-agent/src/child_transaction/gates.rs, crates/wcore-agent/src/orchestration/anvil/engine.rs, crates/wcore-agent/src/orchestration/anvil/forge.rs, crates/wcore-agent/src/orchestration/anvil/seat.rs, crates/wcore-agent/src/orchestration/council/run.rs, crates/wcore-agent/src/spawner.rs, crates/wcore-agent/src/durable_spawner.rs</read_first>
  <behavior>
    - Anvil Forge and seat work declare workspace intent before dispatch.
    - Every `builder.build` candidate receives a distinct transaction opening and transaction-owned standalone checkout allocated by the production spawner; Forge never creates a second checkout for that candidate, changes process-global CWD, or derives an identity from a bare path.
    - Forge prompts, child tools, terminal result, advisory evaluation gates, stability reruns, and each returned `BuiltCandidate` carry that candidate's same predecessor-defined opaque transaction/checkout/base/head/tree identity. `ClimbOutcome` identifies the selected winner without collapsing or reusing identities across candidates.
    - Rejected, failed, superseded, and losing candidates transition their own transactions to terminal non-landing states and complete identity-bound rollback/cleanup. Only the selected winner may be handed to the parent-owned Phase 20 gate and landing lifecycle.
    - The Anvil-local surface is explicitly named `EvaluationGateExecutor`; its reports, stamps, journals, receipts, and `SandboxGate` results are advisory evaluation and selection evidence only. They can reject or score a candidate but cannot construct the 20-06 opaque observed result, mint `accepted_candidate`, or authorize parent integration.
    - `EvaluationGateExecutor::run` and `SandboxGate::run` accept the exact candidate identity, never a bare path; path substitution, stale head/tree, sibling checkout, and identity/path disagreement fail closed.
    - Council participants receive transaction/workspace authority through the production spawner.
    - Mutating dispatch without a valid isolated workspace returns a structured error and never invokes the child.
  </behavior>
  <action>Remove Forge's direct `WorktreeManager::create_worker_tree` and `std::env::set_current_dir` choreography. Adapt Forge, seat, and Council call sites to the Phase 20 spawner request without creating alternate policy. Open one durable child transaction and allocate one standalone checkout for every `SpawnBuilder::build` candidate; never reuse a transaction or checkout across climb candidates. Replace `BuiltCandidate`'s independently stored bare worktree path with the predecessor-defined opaque exact candidate identity and owned lifecycle handle. Rename the Anvil-local `GateExecutor` contract to `EvaluationGateExecutor`; make `EvaluationGateExecutor::run`, `gate_and_record`, `stability_holds`, `check_verified`, `ClimbOutcome`, `SandboxGate::run`, and `SpawnBuilder::build` preserve the per-candidate identity without reconstructing it from a path. Its reports, stamps, journals, and receipts remain advisory and cannot create the 20-06 module-private acceptance input or invoke parent landing. On builder failure, evaluation failure, rejection, timeout, cancellation, no-winner termination, or replacement of the current best, durably record the disposition and rollback/clean every losing candidate. Persist the replacement winner before cleaning the displaced best, preserve only the selected candidate until the parent lifecycle consumes it, and pass no candidate if selection or cleanup is ambiguous. Update inline `SeqBuilder`, `ScriptGate`, `NoGate`, `RecordingBuilder`, production `SpawnBuilder`, and SandboxGate fixtures. Carry caller/correlation identity into diagnostics and preserve existing result ordering and Anvil/Council semantics.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-05)" crates/wcore-agent/src/orchestration/anvil/engine.rs crates/wcore-agent/src/orchestration/anvil/forge.rs crates/wcore-agent/src/orchestration/anvil/seat.rs crates/wcore-agent/src/orchestration/council/run.rs</automated>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-agent/src/orchestration/anvil/engine.rs crates/wcore-agent/src/orchestration/anvil/forge.rs crates/wcore-agent/src/orchestration/anvil/seat.rs crates/wcore-agent/src/orchestration/council/run.rs</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, every changed path is declared, one predecessor-defined identity is carried end to end within each distinct candidate transaction, `EvaluationGateExecutor` cannot accept a bare path or mint parent acceptance, and the owned Forge source contains no second-checkout or process-global-CWD choreography. Compilation and behavior remain plan-level claims.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

<task type="auto">
  <name>Task 2: Prove Forge, Crucible, and Council caller behavior</name>
  <files>crates/wcore-agent/tests/common/mod.rs, crates/wcore-agent/tests/anvil_forge_transaction.rs, crates/wcore-agent/tests/crucible_council.rs</files>
  <read_first>crates/wcore-agent/tests/common/mod.rs, crates/wcore-agent/tests/crucible_council.rs, crates/wcore-agent/src/orchestration/anvil/engine.rs, crates/wcore-agent/src/orchestration/anvil/forge.rs, crates/wcore-agent/src/orchestration/anvil/seat.rs, crates/wcore-agent/src/orchestration/council/run.rs</read_first>
  <behavior>
    - Mutating multi-agent fixtures write only inside their individual isolated roots.
    - A participant cannot substitute the parent path or another participant's worktree.
    - Failure to allocate isolation prevents dispatch and preserves caller/correlation diagnostics.
    - For every climb candidate, Forge's prompt root, child execution root, returned candidate, and advisory gate subject are that candidate's exact same transaction-owned checkout; concurrent and sequential candidates have distinct identities and never change another task's process CWD.
    - Keep-best returns at most one winner to the parent gate lifecycle; every loser, rejected candidate, and failed build is terminalized and cleaned without mutating the parent, and advisory Anvil gates cannot directly land anything.
  </behavior>
  <action>Extend shared test fixtures with explicit workspace intent and exact identities. Add a production-composition Forge regression with at least three build candidates: an initial best, an accepted replacement, and a rejected candidate. Prove every prompt, Write/Edit/Bash activity, returned candidate, advisory gate invocation, stability rerun, and terminal outcome retains that candidate's identical authority object without global CWD changes or a second checkout; all candidates have distinct transaction and checkout identities; the displaced and rejected candidates are durably rolled back and cleaned; and only the selected candidate remains for the parent gate lifecycle. Add a no-winner case that cleans every candidate, interruption during loser cleanup, forged advisory success that cannot mint acceptance, bare-path substitution, stale head/tree, sibling-checkout substitution, cross-candidate transaction reuse, identity/path disagreement, cross-seat/participant substitution, missing isolation, and parent mutation attempts alongside existing successful Crucible/Council flows.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-05)" crates/wcore-agent/src/orchestration/anvil/engine.rs crates/wcore-agent/src/orchestration/anvil/forge.rs crates/wcore-agent/src/orchestration/anvil/seat.rs crates/wcore-agent/src/orchestration/council/run.rs crates/wcore-agent/tests/common/mod.rs crates/wcore-agent/tests/anvil_forge_transaction.rs crates/wcore-agent/tests/crucible_council.rs</automated>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-agent/tests/common/mod.rs crates/wcore-agent/tests/anvil_forge_transaction.rs crates/wcore-agent/tests/crucible_council.rs</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, every changed path is declared, and the owned tests contain production-composition identity-continuity, substitution, missing-authority, concurrent-CWD, and unchanged-parent cases. Test behavior remains a plan-level claim.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

<task type="auto">
  <name>Task 3: Close CLI Anvil, Crucible, and workflow entrypoints</name>
  <files>crates/wcore-cli/src/anvil.rs, crates/wcore-cli/src/crucible.rs, crates/wcore-cli/src/workflow.rs</files>
  <read_first>crates/wcore-cli/src/anvil.rs, crates/wcore-cli/src/crucible.rs, crates/wcore-cli/src/workflow.rs, crates/wcore-agent/src/bootstrap.rs, crates/wcore-agent/src/spawner.rs</read_first>
  <behavior>
    - Each CLI entrypoint constructs the same workspace-aware production engine/spawner.
    - Missing repository or isolation prerequisites produce actionable errors before orchestration begins.
    - CLI options cannot downgrade mutating work to shared workspace access.
  </behavior>
  <action>Update CLI construction and request mapping to supply the Phase 20 authority contract. Add inline authority and denial tests in all three owned modules, including Anvil, so the full CLI library gate executes every entrypoint. Keep UI and command syntax compatible; make denial messages name the missing prerequisite without exposing secrets or claiming a child ran.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-05)" crates/wcore-agent/src/orchestration/anvil/engine.rs crates/wcore-agent/src/orchestration/anvil/forge.rs crates/wcore-agent/src/orchestration/anvil/seat.rs crates/wcore-agent/src/orchestration/council/run.rs crates/wcore-agent/tests/common/mod.rs crates/wcore-agent/tests/anvil_forge_transaction.rs crates/wcore-agent/tests/crucible_council.rs crates/wcore-cli/src/anvil.rs crates/wcore-cli/src/crucible.rs crates/wcore-cli/src/workflow.rs</automated>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-cli/src/anvil.rs crates/wcore-cli/src/crucible.rs crates/wcore-cli/src/workflow.rs</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, every changed path is declared, and all three CLI sources route through the shared authority constructor with denial cases present. Tests and Clippy remain plan-level claims.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>CLI and orchestration-specific requests cross into the shared production spawner.</trust_boundaries>
<threats>Legacy caller bypass, Forge double-checkout/candidate divergence, transaction reuse across climb candidates, losing candidate landing or leakage, advisory evaluation results confused with parent acceptance, process-global CWD races, policy downgrade through CLI flags, participant checkout substitution, missing correlation diagnostics, and tests that exercise mocks rather than production composition.</threats>
<mitigations>One transaction-owned checkout per candidate, winner-only handoff into the sole parent gate/landing authority, loser terminalization and cleanup, explicit-root Forge execution, caller-specific adapters only, production-construction tests, exact root/candidate validation, and denial-before-dispatch assertions.</mitigations>
</threat_model>

<verification>
Each task first passes its pre-commit construction gate on the Mac and becomes a provisional GSD task commit. After every task commit exists and the checkout is clean, the following exact committed-HEAD Hetzner gates determine plan acceptance. A construction commit is not behavioral evidence:
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-05-agent "$(pwd -P)" test -p wcore-agent --test anvil_forge_transaction --test crucible_council</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-05-cli "$(pwd -P)" test -p wcore-cli --lib</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-05-clippy "$(pwd -P)" clippy -p wcore-agent -p wcore-cli --all-targets --all-features -- -D warnings</automated>
</verification>

<success_criteria>
- All named orchestration and CLI callers use the transactional spawner; every Forge candidate has one distinct transaction-owned checkout and no process-CWD authority.
- Keep-best passes only the selected winner to the sole parent-owned acceptance/landing lifecycle; all other candidate transactions are terminal and cleaned.
- Hostile caller substitutions and missing isolation stop before dispatch.
- Existing successful read-only behavior remains compatible.
- Focused tests and strict Clippy pass remotely.
</success_criteria>

## Artifacts this phase produces

- Workspace-aware Anvil Forge/seat and Council delegation.
- Crucible/Council isolation regression fixtures.
- Closed CLI entrypoints for Anvil, Crucible, and workflows.

<output>
After completion, create `.planning/phases/20-transactional-delegated-mutation/20-05-SUMMARY.md` using the standard GSD summary template with exact commits and remote results.
</output>

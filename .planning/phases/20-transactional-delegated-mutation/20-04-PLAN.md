---
phase: 20-transactional-delegated-mutation
plan: "04"
type: execute
wave: 2
depends_on:
  - "20-01"
  - "20-03"
files_modified:
  - crates/wcore-agent/src/bootstrap.rs
  - crates/wcore-agent/src/durable_spawner.rs
  - crates/wcore-agent/src/engine.rs
  - crates/wcore-agent/src/spawn_tool.rs
  - crates/wcore-agent/src/spawner.rs
  - crates/wcore-agent/tests/workflow_live_gate_test.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Delegation intent and isolated workspace authority propagate from bootstrap through every production spawner path."
    - "A mutating child starts inside its owned standalone checkout; missing, stale, or repository-metadata-sharing authority stops before provider or tool execution."
    - "Read-only children retain shared access without accidentally inheriting mutation authority."
  artifacts:
    - path: crates/wcore-agent/src/spawner.rs
      provides: "Spawner contract carrying workspace intent and transaction authority"
    - path: crates/wcore-agent/src/durable_spawner.rs
      provides: "Durable child launch bound to exact isolated workspace"
    - path: crates/wcore-agent/tests/workflow_live_gate_test.rs
      provides: "Production-path propagation and fail-closed tests"
  key_links:
    - from: crates/wcore-agent/src/bootstrap.rs
      to: crates/wcore-agent/src/spawner.rs
      via: "production spawner construction"
      pattern: "Spawner"
    - from: crates/wcore-agent/src/spawn_tool.rs
      to: crates/wcore-swarm/src/worktree.rs
      via: "mutating child standalone-checkout allocation"
      pattern: "worktree"
    - from: crates/wcore-agent/src/spawn_tool.rs
      to: crates/wcore-types/src/spawner.rs
      via: "consume ForkOverrides::requested_workspace before workspace allocation"
      pattern: "RequestedChildWorkspace"
---

<objective>
Propagate the Phase 20 workspace decision through the real agent bootstrap, engine, Spawn tool, and durable spawner paths.

Purpose: The isolation substrate is useless if production delegation bypasses it.
Output: One spawner contract, production wiring, and live workflow regression coverage.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-01-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-03-SUMMARY.md
@crates/wcore-types/src/child_transaction.rs
@crates/wcore-types/src/spawner.rs
@crates/wcore-agent/src/child_transaction.rs
@crates/wcore-agent/src/spawner.rs
@crates/wcore-agent/src/durable_spawner.rs
@crates/wcore-agent/src/spawn_tool.rs
@crates/wcore-swarm/src/worktree.rs
</context>

<salvage>
Adapt only owned bootstrap, durable-spawner, engine, Spawn-tool, and workflow-test material from `e200c0a178feb698af350312a80a33d5b04fc699`; use `ccf824b9de2ff7efd64a2ea7cf720047f9add85d` for overlapping `spawner.rs` successor material. Reject any linked-worktree, shared-object, or global-CWD assumption. Record path-level adopted/adapted/rejected dispositions in the SUMMARY; do not cherry-pick.
</salvage>

<tasks>

<task type="auto">
  <name>Task 1: Extend the spawner contract with explicit workspace authority</name>
  <files>crates/wcore-agent/src/spawner.rs, crates/wcore-agent/src/durable_spawner.rs</files>
  <read_first>crates/wcore-agent/src/child_transaction.rs, crates/wcore-types/src/child_transaction.rs, crates/wcore-types/src/spawner.rs, crates/wcore-agent/src/spawner.rs, crates/wcore-agent/src/durable_spawner.rs, crates/wcore-swarm/src/worktree.rs, crates/wcore-tools/src/workspace_policy.rs</read_first>
  <behavior>
    - Every spawn request carries a read-only/shared or mutating/isolated decision.
    - The classification decision is recorded once before allocation with caller/correlation identity and remains traceable through durable opening, allocation, launch, and terminal result; no production path may synthesize a default after dispatch begins.
    - Every mutating spawn attempt, including every Anvil `Builder::build` call, opens a distinct durable child transaction with exactly one distinct standalone checkout and private scratch root; transaction-opening and workspace authority are never reused across candidate attempts.
    - Mutating requests carry the opaque durable transaction-opening authority from 20-01 and require a standalone-checkout identity bound to that same authority.
    - Returned launch authority owns an identity-bound lifecycle handle terminalized exactly once. Failure before a candidate is returned durably records failure, rolls back, and cleans that attempt before returning.
    - Stale, missing, parent-equal, shared-common-directory, or transaction-mismatched identities fail before launch.
  </behavior>
  <action>Harden the existing authority-bearing spawner interfaces and apply them in the durable spawner. Reuse the opaque transaction-opening authority created by 20-01 together with `RequestedChildWorkspace`, `RequestedChildWorkspace::permits`, `ForkOverrides::requested_workspace`, `ResolvedChildLaunch`, and `resolve_durable_launch_in_workspace`; do not introduce another transaction token, intent enum, allowlist, classifier, or workspace abstraction. Persist and carry the one pre-allocation classification decision plus caller/correlation identity through durable opening, allocation, launch, and terminal diagnostics. Open a new transaction/workspace authority for every mutating spawn attempt and return an identity-bound lifecycle handle that must terminalize exactly once; on any pre-return failure, durably record failure and rollback/clean the just-opened transaction before propagating the error. Preserve the fail-closed rule that `IsolatedMutation` can never be realized as `ChildWorkspaceMode::SharedReadOnly`, require allocation and launch identities to match the same durable opening, and keep existing provider-neutral spawn semantics intact.</action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, every changed path is declared by this task, and the owned source contains the authority-bearing contract and structured validation branches described above. Compilation and behavior remain plan-level claims.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire bootstrap, engine, and Spawn tool to the authority contract</name>
  <files>crates/wcore-agent/src/bootstrap.rs, crates/wcore-agent/src/engine.rs, crates/wcore-agent/src/spawn_tool.rs</files>
  <read_first>crates/wcore-agent/src/child_transaction.rs, crates/wcore-types/src/child_transaction.rs, crates/wcore-types/src/spawner.rs, crates/wcore-agent/src/bootstrap.rs, crates/wcore-agent/src/engine.rs, crates/wcore-agent/src/spawn_tool.rs, crates/wcore-agent/src/spawner.rs, crates/wcore-tools/src/workspace_policy.rs</read_first>
  <behavior>
    - Bootstrap installs one configured spawner that knows the parent repository identity.
    - `SharedReadOnly` maps only to a shared workspace with no mutation-capable tools; `IsolatedMutation` allocates a standalone checkout bound to the exact 20-01 transaction opening before launch.
    - The engine never rewrites a mutating child's current directory back to the parent workspace.
  </behavior>
  <action>Thread the 20-01 opaque transaction-opening authority and canonical `ForkOverrides::requested_workspace` result through normal engine construction and the Spawn tool. Allocate and validate the transaction-owned standalone checkout and private scratch roots against that same opening before mutating dispatch, then install both roots through the transaction-specific contained `WorkspacePolicy` constructor produced by 20-03, registry VFS, and sandbox mechanisms. Never fall back to the general constructor's global temporary-directory grant. Preserve shared read-only compatibility only when `RequestedChildWorkspace::permits` authorizes the tool set, and return actionable structured errors for unavailable, stale, rebound, or mismatched authority. Do not reclassify tool or shell text.</action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, every changed path is declared by this task, production call sites carry the canonical authority, and no owned source path contains a legacy parent-CWD or mutation-fallback branch. Compilation and behavior remain plan-level claims.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

<task type="auto">
  <name>Task 3: Prove production-path propagation and denials</name>
  <files>crates/wcore-agent/tests/workflow_live_gate_test.rs</files>
  <read_first>crates/wcore-agent/src/child_transaction.rs, crates/wcore-types/src/child_transaction.rs, crates/wcore-agent/tests/workflow_live_gate_test.rs, crates/wcore-agent/src/bootstrap.rs, crates/wcore-agent/src/spawn_tool.rs, crates/wcore-agent/src/durable_spawner.rs</read_first>
  <behavior>
    - A mutating workflow child observes its standalone checkout and cannot modify the parent workspace, Git metadata, Wayland configuration/memory/credentials, or protected-state fixtures.
    - Read-only delegation stays shared and cannot upgrade itself to mutation.
    - Missing, stale, or mismatched workspace identities stop with preserved diagnostics.
    - A checkout bound to another transaction opening, session generation, or parent snapshot is refused before provider or tool execution.
    - Tests trace the same classification record and correlation identity through bootstrap, durable opening, allocation, provider/tool denial or launch, and terminal diagnostics.
  </behavior>
  <action>Exercise the actual workflow/spawner composition rather than a mock-only adapter. Add hostile fixtures for stale or substituted transaction opening, session-generation or parent-snapshot rebind, missing checkout, shared Git common-directory/alternates, parent-root substitution, classification downgrade, inherited Wayland config/memory/credential paths, and unavailable platform confinement. Assert denial before provider/tool execution and absence of parent workspace, ref, config, hook, object, and protected-state mutation.</action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, the owned test source contains production-composition cases for every listed denial and unchanged-parent assertion, and no test or Clippy PASS is claimed until plan-level verification.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>Planner/tool intent crosses into spawner authority; the parent repository identity crosses into a child process working directory.</trust_boundaries>
<threats>Unclassified dispatch, stale checkout identity, shared parent Git metadata, parent-root substitution, read-only privilege upgrade, mock-only proof, and fallback to legacy unisolated spawn paths.</threats>
<mitigations>One explicit spawner contract, pre-dispatch common-directory and identity validation, standalone checkout identity, production-path tests, and no mutation fallback.</mitigations>
</threat_model>

<verification>
Each task first passes its pre-commit construction gate on the Mac and becomes a provisional GSD task commit. After every task commit exists and the checkout is clean, the following exact committed-HEAD Hetzner gates determine plan acceptance. A construction commit is not behavioral evidence:
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-04-spawner "$(pwd -P)" test -p wcore-agent --lib spawner</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-04-live "$(pwd -P)" test -p wcore-agent --test workflow_live_gate_test</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-04-clippy "$(pwd -P)" clippy -p wcore-agent --all-targets --all-features -- -D warnings</automated>
</verification>

<success_criteria>
- Every production spawn is classified.
- Mutating children launch only in transaction-owned standalone checkouts with independent Git metadata.
- Invalid authority stops before execution and preserves diagnostics.
- The real workflow gate and strict Clippy pass remotely.
</success_criteria>

## Artifacts this phase produces

- Workspace-aware production spawner contract.
- Bootstrap, engine, and Spawn-tool propagation.
- Live workflow isolation regression tests.

<output>
After completion, create `.planning/phases/20-transactional-delegated-mutation/20-04-SUMMARY.md` using the standard GSD summary template with exact remote commands and results.
</output>

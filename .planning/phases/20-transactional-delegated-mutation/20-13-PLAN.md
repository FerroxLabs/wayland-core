---
phase: 20-transactional-delegated-mutation
plan: "13"
type: execute
wave: 10
depends_on:
  - "20-12"
files_modified:
  - crates/wcore-sandbox/tests/hard_process_containment.rs
  - crates/wcore-agent/tests/child_transaction_gate_test.rs
  - crates/wcore-agent/tests/child_transaction_gate_execution_test.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "The final black-box proof uses a live qualifying hard-containment backend for acceptance; scripted runners prove failure injection only."
    - "Unknown, substituted, drifted, skipped, retried, filtered, or unexecuted gate evidence cannot create AcceptedCandidate."
    - "Any repair invalidates the complete affected proof set and requires exact-candidate re-execution before independent review."
  artifacts:
    - path: crates/wcore-sandbox/tests/hard_process_containment.rs
      provides: "Live backend qualification and descendant-containment proof"
    - path: crates/wcore-agent/tests/child_transaction_gate_test.rs
      provides: "Hostile gate-evidence and durable-replay proof"
    - path: crates/wcore-agent/tests/child_transaction_gate_execution_test.rs
      provides: "Positive host-candidate to AcceptedCandidate execution proof"
  key_links:
    - from: crates/wcore-agent/tests/child_transaction_gate_execution_test.rs
      to: crates/wcore-agent/src/child_transaction/gate_executor.rs
      via: "public parent-owned gate execution surface"
      pattern: "AcceptedCandidate"
    - from: crates/wcore-sandbox/tests/hard_process_containment.rs
      to: crates/wcore-sandbox/src/backends
      via: "live production backend qualification"
      pattern: "qualified"
---

<objective>
Add the final black-box gate and hard-containment proof, then run the complete 06C/06D exact-candidate Hetzner gate set.

Purpose: Convert the private source-level gate construction into falsifiable behavior without widening production authority.
Output: One exact 06D test source commit and complete committed-HEAD proof ready for a fresh independent 20-14 audit.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-12-SUMMARY.md
@crates/wcore-agent/src/child_transaction/gate_executor.rs
@crates/wcore-agent/src/child_transaction/gates.rs
@crates/wcore-sandbox/src/lib.rs
</context>

<execution_rules>
- Do not run Cargo on macOS. Every compile, test, nextest, Clippy, target-check, and Cargo-format claim runs through the Hetzner remote harness against the exact committed candidate.
- Require the 20-09 and 20-11 source/review tuples to re-pass `verify-review-pair.sh` before editing.
- Begin from a clean isolated checkout. Run `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-13)"` before any edit.
- This plan owns exactly the three declared black-box test paths and no production path.
- The source task commit, captured before the ordinary GSD `20-13-SUMMARY.md` commit, is the candidate reviewed by 20-14. The summary is metadata only.
- A list command must select exactly one nonignored test. A run must execute that same one test once, with no retry, skip, filter, or early-return false green.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Prove the live containment and authoritative gate boundary</name>
  <files>crates/wcore-sandbox/tests/hard_process_containment.rs, crates/wcore-agent/tests/child_transaction_gate_test.rs, crates/wcore-agent/tests/child_transaction_gate_execution_test.rs</files>
  <read_first>crates/wcore-sandbox/tests/hard_process_containment.rs, crates/wcore-agent/tests/child_transaction_gate_test.rs, crates/wcore-agent/tests/child_transaction_gate_execution_test.rs, crates/wcore-agent/src/child_transaction/gate_executor.rs, crates/wcore-agent/src/child_transaction/gates.rs</read_first>
  <behavior>
    - A live semantically probed qualifying backend owns the accepted process tree and proves no residue after every terminal path.
    - Malformed, unknown-critical, substituted, stale, drifted, duplicate, out-of-order, missing, skipped, filtered, retried, or model-claimed evidence fails closed.
    - The positive path reaches AcceptedCandidate only through the parent-owned executor after durable append, reopen, and reduce of every exact observed result.
    - Cancellation, timeout, process death, dropped guard, and restart terminalize and clean exactly once without parent mutation.
  </behavior>
  <action>Add only the minimum hostile and positive black-box cases needed to prove the declared boundary through public production surfaces. Do not add a second executor, evidence framework, receipt authority, landing behavior, or lifecycle implementation. Record the starting task base, commit the exact three-path source packet, and retain that source SHA for the later summary.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-13)" crates/wcore-sandbox/tests/hard_process_containment.rs crates/wcore-agent/tests/child_transaction_gate_test.rs crates/wcore-agent/tests/child_transaction_gate_execution_test.rs</automated>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-sandbox/tests/hard_process_containment.rs crates/wcore-agent/tests/child_transaction_gate_test.rs crates/wcore-agent/tests/child_transaction_gate_execution_test.rs</automated>
    <automated>base="$(node .planning/scripts/task-base-authority.mjs read "$(git rev-parse --git-path gsd-task-base-20-13)" | sed -n '1p')"; git diff --check "$base"..HEAD && git diff --cached --check && git diff --check</automated>
  </verify>
  <acceptance_criteria>The complete committed/staged/unstaged/untracked/deleted/rename/copy scope union equals the three declared paths. Test source names every required hostile and positive case, but no behavioral claim is made until the plan-level remote gates pass.</acceptance_criteria>
  <done>One clean exact 06D source task commit exists and is ready for committed-HEAD Hetzner proof.</done>
</task>

</tasks>

<verification>
After the source task commit exists and the checkout is clean, run every command below against that same exact HEAD. Before any run, re-prove the six 20-12 list/run pairs; any source repair reruns those six pairs plus both final pairs and every aggregate command below.

<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-13-swarm-lib "$(pwd -P)" test -p wcore-swarm --lib</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-13-agent-lib "$(pwd -P)" test -p wcore-agent --lib</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-13-sandbox-lib "$(pwd -P)" test -p wcore-sandbox --lib</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-13-preflight "$(pwd -P)" nextest list -p wcore-sandbox --test hard_process_containment --ignore-default-filter --run-ignored default -E 'package(=wcore-sandbox) and kind(=test) and binary(=hard_process_containment) and test(=qualified_hard_containment_backend_preflight)' --message-format json</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-13-preflight "$(pwd -P)" nextest run -p wcore-sandbox --test hard_process_containment --ignore-default-filter --run-ignored default -E 'package(=wcore-sandbox) and kind(=test) and binary(=hard_process_containment) and test(=qualified_hard_containment_backend_preflight)' --no-tests fail --retries 0 --status-level pass --final-status-level pass</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-13-positive "$(pwd -P)" nextest list -p wcore-agent --test child_transaction_gate_execution_test --ignore-default-filter --run-ignored default -E 'package(=wcore-agent) and kind(=test) and binary(=child_transaction_gate_execution_test) and test(=qualified_host_candidate_to_accepted_candidate)' --message-format json</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-13-positive "$(pwd -P)" nextest run -p wcore-agent --test child_transaction_gate_execution_test --ignore-default-filter --run-ignored default -E 'package(=wcore-agent) and kind(=test) and binary(=child_transaction_gate_execution_test) and test(=qualified_host_candidate_to_accepted_candidate)' --no-tests fail --retries 0 --status-level pass --final-status-level pass</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-13-gate-test "$(pwd -P)" test -p wcore-agent --test child_transaction_gate_test</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-13-gate-execution "$(pwd -P)" test -p wcore-agent --test child_transaction_gate_execution_test</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-13-hard-containment "$(pwd -P)" test -p wcore-sandbox --test hard_process_containment</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-13-clippy "$(pwd -P)" clippy -p wcore-swarm -p wcore-sandbox -p wcore-agent --all-targets --all-features -- -D warnings</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-13-windows-gnu "$(pwd -P)" check -p wcore-sandbox --target x86_64-pc-windows-gnu</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-13-fmt "$(pwd -P)" fmt --all -- --check</automated>

Before writing the summary, rerun `verify-task-scope.sh`, record the exact clean source task SHA, and record each command, result, and explicit platform limitation. The later summary commit must include `20-13-SUMMARY.md` and may additionally change only `.planning/STATE.md`, `.planning/ROADMAP.md`, and `.planning/REQUIREMENTS.md`; it is outside source identity.
</verification>

<success_criteria>
- The exact source task commit has complete scope proof and all eight exact list/run qualification pairs are green with one selected, unretried PASS each.
- All declared focused, integration, containment, Clippy, Windows cross-target, and format gates pass on the same exact committed candidate.
- No native Windows runtime, parent landing, full lifecycle, release, or production claim is made.
- 20-14 is admitted for independent all-severity audit of the unchanged integrated gate candidate.
</success_criteria>

<output>
Create `20-13-SUMMARY.md` with task base, exact pre-summary `source_sha`, exact tree, scope result, every remote command/result, six inherited and two final qualification-pair results, repair invalidation history, limitations, and readiness for 20-14 only. Never record the summary commit as `source_sha`.
</output>

---
phase: 20-transactional-delegated-mutation
plan: "07"
type: execute
wave: 12
depends_on:
  - "20-01"
  - "20-03"
  - "20-05"
  - "20-14"
files_modified:
  - crates/wcore-agent/src/child_transaction.rs
  - crates/wcore-agent/src/child_transaction/parent.rs
  - crates/wcore-agent/src/session_journal/model.rs
  - crates/wcore-agent/src/session_journal/reducer.rs
  - crates/wcore-swarm/src/worktree.rs
  - crates/wcore-swarm/src/worktree_security.rs
  - crates/wcore-swarm/src/worktree/parent.rs
  - crates/wcore-agent/tests/child_transaction_parent_cas_test.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Only an accepted exact child candidate may attempt parent integration."
    - "Trusted parent authority imports a standalone candidate through a quarantined object-transfer path, revalidates the imported commit/tree/source graph, then performs compare-and-swap against the expected parent identity."
    - "A failed or interrupted landing reconciles the target ref, symbolic HEAD, index, owned worktree, and durable journal to one old or new coherent state; foreign state is never overwritten."
  artifacts:
    - path: crates/wcore-swarm/src/worktree/parent.rs
      provides: "Quarantined candidate import plus recoverable ref/HEAD/index/worktree compare-and-swap state machine"
    - path: crates/wcore-agent/src/child_transaction/parent.rs
      provides: "Transaction-state authorization of prepare, land, and rollback"
    - path: crates/wcore-agent/src/session_journal/model.rs
      provides: "Versioned landing, projection, recovery, and rollback lifecycle events"
    - path: crates/wcore-agent/src/session_journal/reducer.rs
      provides: "Deterministic reduced recovery state and invalid-transition rejection"
    - path: crates/wcore-agent/tests/child_transaction_parent_cas_test.rs
      provides: "Concurrent parent drift, conflict, interruption, and rollback tests"
  key_links:
    - from: crates/wcore-agent/src/child_transaction/parent.rs
      to: crates/wcore-agent/src/child_transaction/gates.rs
      via: "accepted candidate prerequisite"
      pattern: "accepted_candidate"
    - from: crates/wcore-agent/src/child_transaction/parent.rs
      to: crates/wcore-swarm/src/worktree/parent.rs
      via: "authorized atomic landing operation"
      pattern: "compare"
    - from: crates/wcore-agent/src/child_transaction/parent.rs
      to: crates/wcore-agent/src/session_journal/reducer.rs
      via: "conditional lifecycle append and reducer-derived restart recovery"
      pattern: "append_conditionally"
---

<objective>
Import and integrate an accepted standalone child candidate into the parent only through parent-owned quarantine, exact revalidation, compare-and-swap, and explicit rollback behavior.

Purpose: Make child mutation affect the parent only after gates and only when the parent is still the expected state.
Output: Parent landing primitives, transaction authorization, and hostile CAS tests.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-06-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-14-SUMMARY.md
@crates/wcore-agent/src/child_transaction.rs
@crates/wcore-agent/src/child_transaction/gates.rs
@crates/wcore-agent/src/session_journal/model.rs
@crates/wcore-agent/src/session_journal/reducer.rs
@crates/wcore-swarm/src/worktree.rs
@crates/wcore-swarm/src/worktree/candidate.rs
</context>

<salvage>
None. Build the parent import, coherent landing, recovery, and rollback state machine fresh from accepted predecessor outputs.
</salvage>

<execution_rules>
- Require a clean isolated checkout and run `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-07)"` before Task 1 edits.
- Every task scope check uses the complete plan-base union through that task, including committed, staged, unstaged, untracked, deleted, renamed, and copied paths.
- Do not run Cargo on macOS; use exact-file `vx rustfmt` for construction and the Hetzner harness for authoritative Cargo proof.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Import the accepted object graph and land by exact CAS</name>
  <files>crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/src/worktree_security.rs, crates/wcore-swarm/src/worktree/parent.rs</files>
  <read_first>crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/src/worktree_security.rs, crates/wcore-swarm/src/worktree/candidate.rs, crates/wcore-swarm/src/worktree_tests.rs</read_first>
  <behavior>
    - Only trusted parent code may transfer objects from the standalone child; child commands, remotes, hooks, filters, replace refs, configuration, environment, executables, and caller-supplied refspecs cannot drive import.
    - Immediately before transfer and again from quarantined bytes, parent authority re-proves independent common/object storage, no hardlink/reflink aliases, no alternates/replace refs/parent remotes/linked metadata, and sanitized Git behavior.
    - Candidate objects first enter a parent-owned temporary quarantine object directory. Object hash/type/reachability checks and exact accepted head/tree/base/source revalidation complete there before any parent ref or working tree may move.
    - Only an explicitly leased Wayland-owned integration checkout may be mutated. Arbitrary or dirty user worktrees, detached or ambiguous HEAD, and a target branch checked out by an unowned worktree are refused.
    - A durable parent preimage binds canonical common-Git-dir and checkout identity, full target ref, symbolic HEAD, expected commit/tree, index tree/digest, clean worktree digest/status, and cross-process lock/lease identity.
    - The primitive imports and validates the reachable graph, binds it to a transaction-scoped quarantine ref, revalidates under one parent-owned repository/ref lock, advances the full target ref through `git update-ref <ref> <new> <old>`, then coherently projects and verifies symbolic HEAD, index, and the owned worktree.
    - It returns typed prepared, ref-advanced, projected, completed, conflict, and recovery-required outcomes carrying the exact preimage/successor identity; it never reads or writes `SessionJournal` and cannot mint durable lifecycle authority.
    - Parent drift, dirty staged/unstaged/untracked state, lock contention, missing/corrupt/substituted/foreign objects, non-descendant candidates, and Git conflicts stop before target mutation.
    - Interruption before promotion deletes only the owned temporary object directory; interruption after promotion retains a recoverable transaction quarantine ref. Cleanup removes that ref but never directly deletes possibly shared objects; normal Git reachability/GC owns later reclamation.
    - Restart distinguishes prepared-old/old, prepared-new/old, prepared-new/new, partial/dirty, and foreign states; it retries, finishes projection, records landing, performs exact reverse-CAS rollback, or enters `RecoveryRequired` without overwriting foreign bytes.
    - Successful landing returns the complete preimage/successor identities and a rollback handle; rollback holds the same lock and proceeds only when ref, HEAD, index, and worktree still equal the landed successor.
  </behavior>
  <action>Add a pure parent-owned import/landing primitive beneath the existing checkout owner. Reuse its canonicalization, cleanliness, protected argv, and stripped-environment primitives. Re-prove standalone isolation, stage the reachable graph in parent-owned no-follow quarantine, recompute commit/tree/base/source solely from quarantined bytes, and verify connectivity and physical independence. Acquire an exclusive cross-process parent lease, accept an exact durable-authority preimage supplied by the upper layer, revalidate everything under the lease, use exact full-ref `git update-ref` CAS as the logical commit point, coherently project index/worktree, verify all surfaces, and return typed identity-bound outcomes sufficient for the upper layer to journal and recover every boundary. Do not depend on `wcore-agent`, read or write `SessionJournal`, target an arbitrary user checkout, resolve conflicts automatically, or let the child write parent Git state.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-07)" crates/wcore-swarm/src/worktree.rs crates/wcore-swarm/src/worktree_security.rs crates/wcore-swarm/src/worktree/parent.rs</automated>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-swarm/src/worktree.rs crates/wcore-swarm/src/worktree_security.rs crates/wcore-swarm/src/worktree/parent.rs</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, and every changed path is declared by this task. This does not assert compilation or behavioral acceptance; those are established only by the plan-level clean-HEAD Hetzner gates.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

<task type="auto">
  <name>Task 2: Authorize landing and rollback from durable transaction state</name>
  <files>crates/wcore-agent/src/child_transaction.rs, crates/wcore-agent/src/child_transaction/parent.rs, crates/wcore-agent/src/session_journal/model.rs, crates/wcore-agent/src/session_journal/reducer.rs</files>
  <read_first>crates/wcore-agent/src/child_transaction.rs, crates/wcore-agent/src/child_transaction/gates.rs, crates/wcore-swarm/src/worktree/parent.rs, crates/wcore-agent/src/session_journal.rs, crates/wcore-agent/src/session_journal/model.rs, crates/wcore-agent/src/session_journal/reducer.rs</read_first>
  <behavior>
    - Only a durably gate-accepted transaction may import, prepare, and land.
    - The upper layer conditionally appends `LandingPrepared` with the exact expected parent/candidate/preimage before invoking the pure lower-layer CAS primitive, then appends each returned ref-advanced/projection outcome and `Landed` only after verified coherent projection.
    - Import and landing events record expected/observed parent, child, quarantine, object graph, target ref, symbolic HEAD, index, worktree, lock/lease, preimage, successor, and typed primitive outcome before becoming terminal.
    - Startup reconciliation derives every importing, quarantined, prepared, ref-advanced, partially projected, interrupted, and rollback state solely from deterministic journal replay before another transactional dispatch may start.
    - Rollback succeeds only while ref, HEAD, index, and worktree all equal that transaction's landed successor; later foreign changes stop rollback in `RecoveryRequired` for explicit resolution.
  </behavior>
  <action>Connect durable transaction state to the pure parent primitive. Add versioned `SessionEvent` and `ReducedSessionState` representation for importing, quarantined, landing-prepared, ref-advanced, projecting, landed, rollback-prepared, conflict, failed, interrupted, recovery-required, recovered, and rolled-back transitions. Append `LandingPrepared` with `SessionJournal::append_conditionally` before invoking the primitive; map every typed primitive outcome to an identity-matched conditional append, and append `Landed` only after coherent projection is independently verified. Reduce/replay deterministically, reject conflicting duplicates, reorder, gaps, post-terminal transitions, and unknown critical fields, and derive startup recovery solely from reduced journal state. Implement the explicit old/new/foreign ref and projection recovery matrix, preserve diagnostics, make retries identity-bound and idempotent, and prevent the lower-layer primitive, child, or model from minting import, landing, recovery, or rollback authority.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-07)" crates/wcore-swarm/src/worktree.rs crates/wcore-swarm/src/worktree_security.rs crates/wcore-swarm/src/worktree/parent.rs crates/wcore-agent/src/child_transaction.rs crates/wcore-agent/src/child_transaction/parent.rs crates/wcore-agent/src/session_journal/model.rs crates/wcore-agent/src/session_journal/reducer.rs</automated>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-agent/src/child_transaction.rs crates/wcore-agent/src/child_transaction/parent.rs crates/wcore-agent/src/session_journal/model.rs crates/wcore-agent/src/session_journal/reducer.rs</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, every changed path is declared, and the owned journal model/reducer source represents every named recovery state and invalid-transition branch. Replay behavior remains a plan-level claim.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

<task type="auto">
  <name>Task 3: Attack concurrent landing, interruption, and rollback</name>
  <files>crates/wcore-agent/tests/child_transaction_parent_cas_test.rs</files>
  <read_first>crates/wcore-agent/src/child_transaction/parent.rs, crates/wcore-agent/src/session_journal/model.rs, crates/wcore-agent/src/session_journal/reducer.rs, crates/wcore-swarm/src/worktree/parent.rs, crates/wcore-agent/tests/child_transaction_gate_test.rs, crates/wcore-agent/tests/session_journal_test.rs</read_first>
  <behavior>
    - Two candidates racing for one expected parent cannot both land.
    - Missing, corrupt, substituted, foreign, stale, physically aliased, or metadata-poisoned object graphs fail before target-ref movement.
    - Conflicts and injected interruption before/after parent lock, prepared journal append, object import, ref CAS, index update, worktree projection, final landed append, and every rollback boundary converge to exactly old or new coherent state or explicit `RecoveryRequired`.
    - Two landers, branch switch/deletion, detached HEAD, index-lock contention, dirty staged/unstaged/untracked state, an unowned worktree holding the branch, partial projection, and foreign edits are refused without changing user bytes or unrelated refs.
    - Exact rollback restores the prior ref, symbolic HEAD, index, worktree, and journal identity; rollback after any foreign movement stops with diagnostics.
  </behavior>
  <action>Create real independent temporary Git repositories and exercise standalone-candidate import and landing through the public transaction API. Cover object absence/corruption/substitution/aliasing, foreign ancestry, source/tree mismatch, metadata poisoning, malicious Git behavior, concurrent landers, textual conflict, branch switch/deletion, detached HEAD, index lock, dirty staged/unstaged/untracked parent state, another worktree holding the target branch, every import/lock/prepared/CAS/index/projection/landed and rollback interruption boundary, exact retry/recovery, partial projection, quarantine cleanup, successful rollback, and rollback refusal after foreign change. Add journal crash-matrix fixtures for conflicting duplicates, reorder, gaps, post-terminal events, unknown critical fields, and replay of every intermediate recovery state. Assert exact ref, symbolic HEAD, index tree, worktree bytes, reduced journal state, user bytes, unrelated refs, object bytes, and `git fsck` on every path.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-07)" crates/wcore-swarm/src/worktree.rs crates/wcore-swarm/src/worktree_security.rs crates/wcore-swarm/src/worktree/parent.rs crates/wcore-agent/src/child_transaction.rs crates/wcore-agent/src/child_transaction/parent.rs crates/wcore-agent/src/session_journal/model.rs crates/wcore-agent/src/session_journal/reducer.rs crates/wcore-agent/tests/child_transaction_parent_cas_test.rs</automated>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-agent/tests/child_transaction_parent_cas_test.rs</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, every changed path is declared, and the owned tests name every CAS, interruption, rollback, and journal crash-matrix case described above. Test and Clippy results remain plan-level claims.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>An accepted standalone repository object graph crosses into parent Git authority; rollback crosses from recorded history back into a mutable current parent.</trust_boundaries>
<threats>Missing or substituted objects, hardlink/reflink aliases, malicious Git behavior, graph/source mismatch, quarantine escape, import interruption, TOCTOU drift, concurrent double landing, arbitrary or dirty checkout mutation, ref/HEAD/index/worktree divergence, forged acceptance, conflict auto-resolution, replayed landing, and rollback over foreign changes.</threats>
<mitigations>Complete isolation revalidation, parent-owned quarantine, fixed plumbing argv and scrubbed environment, object connectivity and graph revalidation, transaction-scoped refs, Wayland-owned checkout lease, full parent preimage, cross-process lock, prepared-before-CAS journal ordering, exact full-ref CAS, coherent projection verification, durable recovery matrix, idempotency, explicit diagnostics, and exact reverse CAS.</mitigations>
</threat_model>

<verification>
Each task first passes its pre-commit construction gate on the Mac and becomes a provisional GSD task commit. After every task commit exists and the checkout is clean, the following exact committed-HEAD Hetzner gates determine plan acceptance. A construction commit is not behavioral evidence:
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-07-worktree "$(pwd -P)" test -p wcore-swarm --lib worktree</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-07-parent "$(pwd -P)" test -p wcore-agent --test child_transaction_parent_cas_test</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-07-clippy "$(pwd -P)" clippy -p wcore-agent -p wcore-swarm --all-targets --all-features -- -D warnings</automated>
No merge to main, push, release, deployment, or issue closure is authorized by this product-level landing mechanism.
</verification>

<success_criteria>
- Only a durably accepted exact standalone candidate whose object graph was imported and revalidated by parent authority can mutate its parent.
- Parent drift and conflicts stop without silent changes.
- Every crash boundary reconciles ref, symbolic HEAD, index, worktree, and journal to one coherent state or explicit refusal.
- Rollback is exact and refuses to erase later foreign work.
- Hostile CAS tests and strict Clippy pass remotely.
</success_criteria>

## Artifacts this phase produces

- Parent-owned quarantined object import and exact compare-and-swap integration primitive.
- Durable prepare/land/conflict/rollback transaction state.
- Concurrent and interrupted landing regression tests.

<output>
After completion, create `.planning/phases/20-transactional-delegated-mutation/20-07-SUMMARY.md` using the standard GSD summary template with exact commits and remote results.
</output>

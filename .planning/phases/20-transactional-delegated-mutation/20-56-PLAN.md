---
phase: 20-transactional-delegated-mutation
plan: "56"
type: execute
wave: 61
depends_on:
  - "20-55"
files_modified:
  - scripts/f20-native-uat-proof.mjs
autonomous: false
requirements:
  - F20-01
  - F20-02
  - F20-03
  - F20-04
  - F20-05
  - F20-06
  - F20-GATE-01
  - F20-GATE-02
  - REQ-native-r12
  - REQ-native-r4
must_haves:
  truths:
    - "Only the exact durable pending terminal tuple produced by 20-55 may receive Sean's fresh candidate-specific exact-tuple authorization; ALL prior digests (cb6f06bd/6ed9642c and the spent 95c81ec6 / 17412cf2 / daf27337 native-proof-dispatch authorizations, plus any tied to the sealed-but-RED 3f839309, the 20-52-blocked f0dd5b6d, or the 20-59-blocked 8a1d2d84) are spent/void, and any RED or new candidate SHA requires fresh authorization (CONTEXT D6)."
    - "Under that authorization, the retained 20-53 dual-OS native evidence is re-verified bound to the exact re-sealed successor (unchanged remote SHA, all 6 Windows + all 8 macOS targets green with complete markers, the two 20-50 finding fixes held on hardware), and the exact re-sealed source passes aggregate workspace test AND `--locked` build on Hetzner, before any requirement-completion claim (REQ-native-r12/r4)."
    - "Only when the fresh native-inclusive four-way review (20-54, native NOT deferred), the retained 20-53 native evidence, and the aggregate proof all belong to the one exact re-sealed successor SHA and are green do the Phase 20 requirements F20-01..F20-06 / F20-GATE-01 / F20-GATE-02 complete; otherwise all remain incomplete (Phase-20 Success Criterion #3 native clause closed)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-55-SUMMARY.md
      provides: "Immutable pending terminal candidate/ref/runner/image + native-evidence tuple"
    - path: scripts/f20-native-uat-proof.mjs
      provides: "Exact-tuple authorization, retained-evidence re-verification, and proof-checkout authority for the aggregate gate"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-55-SUMMARY.md
      to: scripts/f20-native-uat-proof.mjs
      via: "exact pending bytes and tuple-specific human authorization"
      pattern: "pending"
---

<objective>
Authorize once (exact-tuple), confirm the exact review-qualified re-sealed further-repaired-successor is dual-platform native green from the retained 20-53 evidence, run the aggregate Hetzner proof against the exact source, and complete the Phase 20 requirements — or record an explicit incomplete disposition.

Purpose: This is the terminal exact-tuple authorization gate (Sean gate #2, distinct from the native-proof dispatch at 20-53) for the further repaired successor. It keeps requirement completion behind a durable preparation boundary and one exact Sean decision, closing the native clause of Phase-20 Success Criterion #3.
Output: Requirement completion bound to the one exact re-sealed successor SHA, or an explicit incomplete disposition.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-53-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-54-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-55-SUMMARY.md
@scripts/f20-native-uat-proof.mjs
</context>

<execution_rules>
- Sean gate #2 (CONTEXT D6): only the exact durable pending terminal tuple from 20-55 may receive a fresh candidate-specific exact-tuple authorization. ALL prior digests (cb6f06bd/6ed9642c and the spent 95c81ec6/17412cf2/daf27337 native-proof-dispatch authorizations, plus any tied to the sealed-but-RED 3f839309, the 20-52-blocked f0dd5b6d, or the 20-59-blocked 8a1d2d84) are spent/void; any RED or new candidate SHA requires fresh authorization.
- Under authorization, re-verify the retained 20-53 native evidence is bound to the exact re-sealed SHA (unchanged remote SHA, all 6 Windows + all 8 macOS targets green with complete markers, the two 20-50 finding fixes held on hardware) and run the aggregate Hetzner proof (`--locked` build + `nextest --profile ci` = 11509/0/48) against the exact re-sealed source BEFORE any requirement-completion claim.
- Requirement completion is claimed ONLY when the 20-54 four-way review (native NOT deferred), the retained 20-53 native evidence, and the aggregate proof all belong to the one exact re-sealed successor SHA and are green; otherwise all Phase 20 requirements remain incomplete and an explicit incomplete disposition is recorded.
- No Cargo on the Mac; the aggregate proof runs on the committed-HEAD Hetzner harness.
</execution_rules>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The exact re-sealed further-repaired-successor (20-61), qualified by a fresh native-inclusive four-way review (20-54, Codex Sol + Gemini Pro + Kimi K3 + internal Claude adversarial, native NOT deferred) over retained dual-OS native evidence (20-53), with a durable pending terminal tuple (20-55). This is the terminal exact-tuple authorization gate.</what-built>
  <how-to-verify>
    1. Confirm the 20-55 pending tuple binds the exact re-sealed successor SHA, the runner/image tuple, and the retained native-evidence identity.
    2. Confirm the 20-54 review is a zero-finding PASS from ALL FOUR reviewers with native NOT deferred, and the retained 20-53 evidence is all 6 Windows + all 8 macOS targets green for the exact SHA with the two 20-50 finding fixes held on hardware.
    3. Authorize the terminal completion for EXACTLY this tuple (all prior digests spent/void). This authorizes requirement completion, not a new native run.
  </how-to-verify>
  <resume-signal>Type "authorized-terminal: &lt;re-sealed-sha&gt;" to authorize completion for that exact tuple, or describe why it is withheld.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Exact-tuple re-verify + aggregate proof, then complete Phase 20 requirements or record incomplete</name>
  <files>scripts/f20-native-uat-proof.mjs</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-55-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-53-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-54-SUMMARY.md, scripts/f20-native-uat-proof.mjs</read_first>
  <behavior>
    - Under the exact-tuple authorization, re-verify the retained 20-53 native evidence is bound to the exact re-sealed SHA (unchanged remote SHA, all 6 Windows + all 8 macOS targets green with complete ordered markers, the two 20-50 finding fixes held on hardware) and that the 20-54 four-way review is a zero-finding PASS with native NOT deferred (four schema-validated artifacts present).
    - Run the aggregate Hetzner proof against the exact re-sealed source: `--locked --workspace --all-features` build AND `nextest --profile ci --no-fail-fast` = 11509/0/48.
    - Only when review (native NOT deferred), retained native evidence, and aggregate proof ALL belong to the one exact re-sealed successor SHA and are green, complete F20-01..F20-06 / F20-GATE-01 / F20-GATE-02 (Phase-20 Success Criterion #3 native clause closed). Otherwise record an explicit incomplete disposition and leave every requirement incomplete.
    - `scripts/f20-native-uat-proof.mjs` performs the exact-tuple authorization, retained-evidence re-verification, and proof-checkout authority; any edit to it is confined to closing the terminal gate (no scope creep).
  </behavior>
  <action>Under the exact-tuple authorization from the checkpoint, use `scripts/f20-native-uat-proof.mjs` to re-verify the retained 20-53 native evidence is bound to the exact re-sealed SHA (all 6 Windows + all 8 macOS targets green, complete ordered markers, unchanged remote SHA, the two 20-50 finding fixes held on hardware) and the 20-54 four-way review is a zero-finding PASS with native NOT deferred, then run the aggregate Hetzner proof against the exact re-sealed source (`--locked` build + `nextest --profile ci --no-fail-fast` = 11509/0/48). If review, retained native evidence, and aggregate proof all belong to the one exact re-sealed SHA and are green, complete F20-01..F20-06 / F20-GATE-01 / F20-GATE-02; otherwise record an explicit incomplete disposition and leave all requirements incomplete. Any edit to `scripts/f20-native-uat-proof.mjs` is confined to closing the terminal gate (no scope creep). Implements REQ-native-r12/r4 and closes the Phase-20 Success Criterion #3 native clause.</action>
  <verify>
    <human-check>The retained 20-53 native evidence re-verifies bound to the exact re-sealed SHA (all 6 Windows + all 8 macOS targets green, complete markers, the two 20-50 finding fixes held on hardware); the 20-54 four-way review is zero-finding with native not deferred; the aggregate Hetzner proof is 11509/0/48 against the exact source. On all-green, F20-01..F20-06/F20-GATE-01/F20-GATE-02 are completed; otherwise an explicit incomplete disposition is recorded.</human-check>
  </verify>
  <done>Phase 20 requirements are completed bound to the one exact re-sealed further-repaired-successor SHA, or an explicit incomplete disposition is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirement completion ← exact-tuple Sean decision | Completion is gated by one exact-tuple Sean authorization; all prior digests are spent/void |
| completion validity ← exact-SHA re-verification | Review, retained native evidence, and aggregate proof must all belong to the one exact re-sealed SHA before completion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-56-01 | Elevation of Privilege | reused/stale terminal authorization | high | mitigate | Only the exact 20-55 pending tuple receives a fresh candidate-specific exact-tuple authorization; all prior digests (cb6f06bd/6ed9642c/95c81ec6/17412cf2/daf27337 and any 3f839309-tied, f0dd5b6d-tied, or 8a1d2d84-tied) are spent/void; any RED or new SHA requires fresh authorization (CONTEXT D6). |
| T-20-56-02 | Spoofing | completion on mismatched evidence | high | mitigate | Completion requires the 20-54 review (native NOT deferred), the retained 20-53 native evidence, and the aggregate `--locked`/nextest proof to all belong to the one exact re-sealed SHA and be green; any mismatch records an explicit incomplete disposition. |
| T-20-56-03 | Denial of Service | aggregate regression at terminal | medium | mitigate | The terminal aggregate proof requires 11509/0/48 against the exact re-sealed source; a deviation blocks completion rather than being rationalized. |
</threat_model>

<verification>
The exact-tuple authorization, the retained-evidence re-verification (all 6 Windows + all 8 macOS targets green, complete markers, unchanged remote SHA bound to the exact re-sealed SHA, the two 20-50 finding fixes held on hardware), and the aggregate Hetzner proof (`--locked` build + `nextest --profile ci` = 11509/0/48) determine acceptance. Only an all-green exact-SHA tuple completes F20-01..F20-06 / F20-GATE-01 / F20-GATE-02; any mismatch records an explicit incomplete disposition with every requirement left incomplete. Record the authorized `source_sha`/tree, the retained native-evidence re-verification, the aggregate receipts, and the completion-or-incomplete disposition in `20-56-SUMMARY.md`.
</verification>

<success_criteria>
- One fresh exact-tuple Sean authorization for the exact 20-55 pending tuple, all prior digests spent/void (r12).
- The retained 20-53 native evidence re-verifies bound to the exact re-sealed SHA (all 6 Windows + all 8 macOS green, the two 20-50 finding fixes held on hardware), the 20-54 four-way review is zero-finding with native not deferred, and the aggregate Hetzner proof is 11509/0/48 against the exact source (r12/r4).
- On all-green, F20-01..F20-06 / F20-GATE-01 / F20-GATE-02 complete and the Phase-20 Success Criterion #3 native clause closes; otherwise an explicit incomplete disposition leaves every requirement incomplete.
</success_criteria>

## Artifacts this phase produces
- Phase 20 requirement completion bound to the one exact re-sealed further-repaired-successor SHA, or an explicit incomplete disposition.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-56-SUMMARY.md` using the standard GSD summary template. Record the authorized `source_sha`/tree, the retained 20-53 native-evidence re-verification, the aggregate Hetzner receipts, and the completion-or-incomplete disposition for F20-01..F20-06 / F20-GATE-01 / F20-GATE-02.
</output>
</content>

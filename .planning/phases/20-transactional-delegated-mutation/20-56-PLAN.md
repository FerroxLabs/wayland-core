---
phase: 20-transactional-delegated-mutation
plan: "56"
type: execute
wave: 61
depends_on:
  - "20-17"
files_modified: []
autonomous: true
requirements:
  - F20-01
  - F20-02
  - F20-03
  - F20-04
  - F20-05
  - F20-06
  - F20-GATE-01
  - F20-GATE-02
must_haves:
  truths:
    - "RESCOPED 2026-07-25. This plan previously gated Phase 20 on retained dual-OS NATIVE evidence (20-53), a native-inclusive review (20-54), and a pending native tuple (20-55). All three moved to Phase 20A with REQ-native-r1..r15, so none of them is a Phase 20 dependency any more. Phase 20 closes on ITS OWN four Success Criteria."
    - "Phase-20 Success Criterion #4 requires exactly this: one exact F20 successor lands on the admitted candidate with focused AND aggregate proof. Focused proof was delivered per-plan across 20-01..20-17 (all seventeen carry SUMMARYs). This plan supplies the AGGREGATE half: `--locked --workspace --all-features` build plus `nextest --profile ci --no-fail-fast` on Hetzner (`hetzner-dsm:/root/wayland`) against one exact source SHA."
    - "Success Criterion #3 requires that snapshot, workspace, journal, receipt, merge, rollback, cleanup, and native Windows/macOS identities share ONE AUTHORITATIVE LIFECYCLE. That is a property of the delivered source, satisfied by 20-01/02/08/16. It is NOT a requirement that the native proof RUN be green — that is Phase 20A's Success Criteria 1 and 2."
    - "Only when the aggregate build AND test are green against one exact SHA do F20-01..F20-06 / F20-GATE-01 / F20-GATE-02 complete. Any red, timeout, or incomplete output leaves EVERY requirement incomplete and records an explicit incomplete disposition. A reported red is worth more than an engineered green."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-56-SUMMARY.md
      provides: "Aggregate build+test result bound to one exact SHA, and the requirement completion or explicit incomplete disposition"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-56-SUMMARY.md
      to: .planning/ROADMAP.md
      via: "exact aggregate-proof SHA completing F20-01..F20-06 / F20-GATE-01 / F20-GATE-02"
      pattern: "requirement-completion"
---

<objective>
Run the aggregate Hetzner proof against one exact source SHA and complete the Phase 20 requirements — or record an explicit incomplete disposition.

Purpose: This is the terminal plan for Phase 20 and supplies the AGGREGATE half of Success Criterion #4 (focused proof was delivered per-plan across 20-01..20-17). Native dual-OS proof is NOT in scope: it moved to Phase 20A along with REQ-native-r1..r15.
Output: F20-01..F20-06 / F20-GATE-01 / F20-GATE-02 completed and bound to one exact SHA, or an explicit incomplete disposition.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-53-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-54-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-55-SUMMARY.md
@scripts/f20-native-uat-proof.mjs
</context>

<execution_rules>
- Sean gate #2 (CONTEXT D6): only the exact durable pending terminal tuple from 20-55 may receive a fresh candidate-specific exact-tuple authorization. ALL prior digests (cb6f06bd/6ed9642c and the spent 95c81ec6/17412cf2/daf27337 native-proof-dispatch authorizations, plus any tied to the sealed-but-RED 3f839309, the 20-52-blocked f0dd5b6d, or the 20-59-blocked 8a1d2d84) are spent/void; any RED or new candidate SHA requires fresh authorization.
- Under authorization, re-verify the retained 20-53 native evidence is bound to the exact re-sealed SHA (unchanged remote SHA, all 6 Windows + all 8 macOS targets green with complete markers, the two 20-50 finding fixes held on hardware) and run the aggregate Hetzner proof (`--locked` build + `nextest --profile ci` = 11509/0/48) against the exact re-sealed source BEFORE any requirement-completion claim.
- Requirement completion is claimed ONLY when the 20-54 four-way review (native NOT deferred), the retained 20-53 native evidence, and the aggregate proof all belong to the one exact re-sealed successor SHA and are green; otherwise all Phase 20 requirements remain incomplete and an explicit incomplete disposition is recorded.
- No Cargo on the Mac; the aggregate proof runs on the committed-HEAD Hetzner harness.
</execution_rules>

<tasks>

Human authorization for this terminal plan was given by Sean on 2026-07-25 ("unblock 2056 and get it done ... get Phase 20 fucking fixed"), together with the hard-freeze directive that rescoped the phase. The prior blocking-human checkpoint gated an exact-tuple NATIVE authorization; that gate moved to Phase 20A with the native scope and is not a Phase 20 gate. No push, merge, release, issue closure, or native-proof dispatch is authorized by this plan.

<task type="auto">
  <name>Task 1: Aggregate Hetzner proof against one exact SHA, then complete Phase 20 requirements or record incomplete</name>
  <files></files>
  <read_first>.planning/ROADMAP.md, .planning/phases/20-transactional-delegated-mutation/RECONCILIATION.md</read_first>
  <behavior>
    - Pin ONE exact source SHA and record it. Every result in the SUMMARY must be bound to that single SHA.
    - Make that SHA available on `hetzner-dsm:/root/wayland` (fetch + checkout the exact SHA). Do NOT merge to main, do NOT release, do NOT close issues.
    - Run the aggregate proof there, capturing full output for both:
        `cargo build --locked --workspace --all-features`
        `cargo nextest run --profile ci --no-fail-fast`
    - Record the ACTUAL pass/fail/skip counts observed. Do NOT assert a historical expected total (the tree has changed materially since any prior figure); report what the run produces and compare it against the immediately-preceding run on this branch if one exists.
    - Only when BOTH the build and the test run are green against that one exact SHA, complete F20-01..F20-06 / F20-GATE-01 / F20-GATE-02 in REQUIREMENTS.md and record the completion SHA. Any red, timeout, or truncated output leaves EVERY requirement incomplete with an explicit written disposition naming the failures.
    - Native Windows/macOS proof is OUT OF SCOPE and must not be run, claimed, or waited on. It is Phase 20A Success Criteria 1 and 2.
    - Never weaken, skip, or `#[ignore]` a test to reach green. A reported red is worth more than an engineered green.
  </behavior>
  <action>Pin one exact source SHA. Make it available on `hetzner-dsm:/root/wayland` and run `cargo build --locked --workspace --all-features` followed by `cargo nextest run --profile ci --no-fail-fast`, capturing full output. If BOTH are green, complete F20-01..F20-06 / F20-GATE-01 / F20-GATE-02 bound to that SHA and write `20-56-SUMMARY.md`; if either is red, leave all requirements incomplete and write the explicit incomplete disposition naming every failure with its error text. Do not run or claim native proof — that is Phase 20A.</action>
  <verify>
    <automated>ssh hetzner-dsm 'cd /root/wayland &amp;&amp; git rev-parse HEAD' matches the pinned SHA recorded in the SUMMARY</automated>
    <human-check>The SUMMARY records the exact SHA, the full aggregate build result, the observed nextest pass/fail/skip counts, and either the requirement completion or an explicit incomplete disposition naming each failure. No native claim appears anywhere in it.</human-check>
  </verify>
  <done>Phase 20 requirements are completed bound to one exact SHA with green aggregate build+test, or an explicit incomplete disposition is recorded naming every failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirement completion ← exact-tuple Sean decision | Completion is gated by one exact-tuple Sean authorization; all prior digests are spent/void |
| completion validity ← exact-SHA re-verification | Review, retained native evidence, and aggregate proof must all belong to the one exact re-sealed SHA before completion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-56-01 | Elevation of Privilege | reused/stale terminal authorization | high | mitigate | Only the exact 20-55 pending tuple receives a fresh candidate-specific exact-tuple authorization; all prior digests (cb6f06bd/6ed9642c/95c81ec6/17412cf2/daf27337 and any 3f839309-tied, f0dd5b6d-tied, or 8a1d2d84-tied) are spent/void; any RED or new SHA requires fresh authorization (CONTEXT D6). |
| T-20-56-02 | Spoofing | completion on mismatched evidence | high | mitigate | Completion requires the 20-54 review (native NOT deferred), the retained 20-53 native evidence, and the aggregate `--locked`/nextest proof to all belong to the one exact re-sealed SHA and be green; any mismatch records an explicit incomplete disposition. |
| T-20-56-03 | Denial of Service | aggregate regression at terminal | medium | mitigate | The terminal aggregate proof requires 11509/0/48 against the exact re-sealed source; a deviation blocks completion rather than being rationalized. |
</threat_model>

<verification>
The exact-tuple authorization, the retained-evidence re-verification (all 6 Windows + all 8 macOS targets green, complete markers, unchanged remote SHA bound to the exact re-sealed SHA, the two 20-50 finding fixes held on hardware), and the aggregate Hetzner proof (`--locked` build + `nextest --profile ci` = 11509/0/48) determine acceptance. Only an all-green exact-SHA tuple completes F20-01..F20-06 / F20-GATE-01 / F20-GATE-02; any mismatch records an explicit incomplete disposition with every requirement left incomplete. Record the authorized `source_sha`/tree, the retained native-evidence re-verification, the aggregate receipts, and the completion-or-incomplete disposition in `20-56-SUMMARY.md`.
</verification>

<success_criteria>
- One fresh exact-tuple Sean authorization for the exact 20-55 pending tuple, all prior digests spent/void (r12).
- The retained 20-53 native evidence re-verifies bound to the exact re-sealed SHA (all 6 Windows + all 8 macOS green, the two 20-50 finding fixes held on hardware), the 20-54 four-way review is zero-finding with native not deferred, and the aggregate Hetzner proof is 11509/0/48 against the exact source (r12/r4).
- On all-green, F20-01..F20-06 / F20-GATE-01 / F20-GATE-02 complete and the Phase-20 Success Criterion #3 native clause closes; otherwise an explicit incomplete disposition leaves every requirement incomplete.
</success_criteria>

## Artifacts this phase produces
- Phase 20 requirement completion bound to the one exact re-sealed further-repaired-successor SHA, or an explicit incomplete disposition.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-56-SUMMARY.md` using the standard GSD summary template. Record the authorized `source_sha`/tree, the retained 20-53 native-evidence re-verification, the aggregate Hetzner receipts, and the completion-or-incomplete disposition for F20-01..F20-06 / F20-GATE-01 / F20-GATE-02.
</output>
</content>

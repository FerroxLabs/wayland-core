---
phase: 20-transactional-delegated-mutation
plan: "10"
type: execute
wave: 6
depends_on:
  - "20-02"
  - "20-09"
files_modified:
  - crates/wcore-sandbox/src/lib.rs
  - crates/wcore-sandbox/src/manifest.rs
  - crates/wcore-sandbox/src/backends/mod.rs
  - crates/wcore-sandbox/src/backends/appcontainer.rs
  - crates/wcore-sandbox/src/backends/bwrap.rs
  - crates/wcore-sandbox/src/backends/docker.rs
  - crates/wcore-sandbox/src/backends/process_tree.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Hard containment authority is opaque, one-use, and minted only from a successful semantic live probe of the exact backend and policy used for spawn."
    - "The candidate is read-only, only transaction-private scratch/build/cache is writable, and network plus inherited credentials/configuration are denied."
    - "No boolean, configured backend name, process group, caller claim, serialized value, or bypass mode can qualify as hard containment."
    - "This packet makes no gate-result, receipt, candidate-acceptance, landing, or complete lifecycle claim."
  artifacts:
    - path: crates/wcore-sandbox/src/lib.rs
      provides: "Opaque one-use HardContainmentAuthority"
    - path: crates/wcore-sandbox/src/manifest.rs
      provides: "Read-only candidate and private writable-root policy"
---

<objective>
Build only the 20-06B hard-containment source packet after the exact 06A qualification pair is proven.

Purpose: create a live, one-use process-containment authority suitable for later gate execution.
Output: one clean GSD source-plan completion HEAD for a separate non-author review plan.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-02-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-06-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-09-SUMMARY.md
@crates/wcore-sandbox/src/lib.rs
@crates/wcore-sandbox/src/manifest.rs
@crates/wcore-sandbox/src/backends/mod.rs
@crates/wcore-sandbox/src/backends/appcontainer.rs
@crates/wcore-sandbox/src/backends/bwrap.rs
@crates/wcore-sandbox/src/backends/docker.rs
@crates/wcore-sandbox/src/backends/process_tree.rs
@crates/wcore-sandbox/tests/backend_integration.rs
@crates/wcore-config/src/shell.rs
</context>

<admission>
- Require `20-09-SUMMARY.md` to contain a verified exact `(source_sha, review_base_sha, review_sha)` tuple and zero-finding PASS. Re-run `.planning/scripts/verify-review-pair.sh` with that tuple, `20-06-SUMMARY.md`, the review record, and all three 06A source paths before editing.
- Require a clean isolated checkout. Before source work, record `git rev-parse HEAD` at `$(git rev-parse --git-path gsd-task-base-20-10)`.
- The 06B source task commit captured before the ordinary 20-10 summary commit is the exact source SHA reviewed by 20-11.
</admission>

<tasks>

<task type="auto">
  <name>06B: Require live hard containment and a read-only candidate policy</name>
  <files>crates/wcore-sandbox/src/lib.rs, crates/wcore-sandbox/src/manifest.rs, crates/wcore-sandbox/src/backends/mod.rs, crates/wcore-sandbox/src/backends/appcontainer.rs, crates/wcore-sandbox/src/backends/bwrap.rs, crates/wcore-sandbox/src/backends/docker.rs, crates/wcore-sandbox/src/backends/process_tree.rs</files>
  <read_first>crates/wcore-sandbox/src/lib.rs, crates/wcore-sandbox/src/manifest.rs, crates/wcore-sandbox/src/backends/mod.rs, crates/wcore-sandbox/src/backends/appcontainer.rs, crates/wcore-sandbox/src/backends/bwrap.rs, crates/wcore-sandbox/src/backends/docker.rs, crates/wcore-sandbox/src/backends/process_tree.rs, crates/wcore-sandbox/tests/backend_integration.rs, crates/wcore-config/src/shell.rs, .planning/phases/20-transactional-delegated-mutation/20-02-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-09-SUMMARY.md</read_first>
  <behavior>
    - `HardContainmentAuthority` is opaque, non-serializable, non-cloneable, one-use, and minted by `SandboxRegistry` only after a semantic live probe.
    - It binds backend, executable/runtime identity, probe result, process-tree mechanism, normalized policy identity, and exact spawn. Any drift refuses execution.
    - Only qualifying Bubblewrap PID-namespace, Docker container, or Windows AppContainer plus Job Object semantics may qualify after a successful live probe. macOS sandbox-exec, no-sandbox/Dangerous, stubs, and ordinary process groups never qualify.
    - Candidate source is read-only. Only parent-created transaction-private scratch/build/cache roots are writable; global temp, home/config/credentials, network, and bypass modes are denied.
    - Spawn, probe, identity, containment, cancellation, timeout, overflow, capture/read/wait, or descendant-cleanup failure kills the complete owned process tree and fails closed with diagnostics.
    - Crate-private failure injection covers probe-success followed by executable/runtime/manifest/policy/backend drift and all process-tree failure stages without public test authority.
  </behavior>
  <action>First record the clean exact task base. Replace any spoofable capability flag at the authoritative gate boundary with an opaque one-use authority. Mint only from the semantic live probe of the exact normalized backend/policy later consumed at spawn. Preserve accepted 20-02 AppContainer profile, ACL, and recovery authority. Keep ordinary process-tree helpers for cleanup but never let them qualify as the hard boundary. Do not execute candidate-controlled code on failed admission.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-10)" crates/wcore-sandbox/src/lib.rs crates/wcore-sandbox/src/manifest.rs crates/wcore-sandbox/src/backends/mod.rs crates/wcore-sandbox/src/backends/appcontainer.rs crates/wcore-sandbox/src/backends/bwrap.rs crates/wcore-sandbox/src/backends/docker.rs crates/wcore-sandbox/src/backends/process_tree.rs</automated>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-sandbox/src/lib.rs crates/wcore-sandbox/src/manifest.rs crates/wcore-sandbox/src/backends/mod.rs crates/wcore-sandbox/src/backends/appcontainer.rs crates/wcore-sandbox/src/backends/bwrap.rs crates/wcore-sandbox/src/backends/docker.rs crates/wcore-sandbox/src/backends/process_tree.rs</automated>
    <automated>base="$(cat "$(git rev-parse --git-path gsd-task-base-20-10)")"; git diff --check "$base"..HEAD && git diff --cached --check && git diff --check</automated>
  </verify>
  <acceptance_criteria>The complete TASK_BASE union equals exactly the seven declared source paths. No public constructor, serde, clone, boolean shortcut, process-group shortcut, or bypass mode can mint or retain authority. The manifest is read-only/private-write-root only. Owned source tests name every probe/spawn/process-tree failure and drift case. No runtime-containment claim is made until remote proof.</acceptance_criteria>
  <done>One exact clean 06B source task commit exists; the summary records that already-existing SHA for 20-11 review.</done>
</task>

</tasks>

<verification>
Before the summary, derive the recorded task base and current source task commit, require a clean tree, rerun `verify-task-scope.sh` so committed changes are included, and record both existing SHAs in `20-10-SUMMARY.md`. The later summary commit must include `20-10-SUMMARY.md` and may additionally change only `.planning/STATE.md`, `.planning/ROADMAP.md`, and `.planning/REQUIREMENTS.md`; it cannot become or alter source identity. No Cargo command runs on macOS.
</verification>

<success_criteria>
- The exact 06A qualification tuple remains valid and byte-identical.
- 06B is source-complete and scope-exact but neither construction-qualified nor behaviorally accepted.
</success_criteria>

<output>
Create `20-10-SUMMARY.md` with task base, exact final source SHA, exact paths, checks, non-claims, and readiness for 20-11 only.
</output>

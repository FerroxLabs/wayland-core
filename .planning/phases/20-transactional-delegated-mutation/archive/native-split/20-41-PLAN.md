---
phase: 20-transactional-delegated-mutation
plan: "41"
type: execute
wave: 40
depends_on:
  - "20-40"
files_modified: []
autonomous: true
requirements:
  - REQ-native-r12
must_haves:
  truths:
    - "The exact sealed second-repaired-successor qualified with zero findings by the fresh native-inclusive review (20-40) is reauthenticated before any terminal request exists — its source/tree, the review pair, and the retained 20-39 native evidence are all re-verified (REQ-native-r12)."
    - "One strict pending TERMINAL request records the exact successor source/tree/ref plus the runner/image tuple and the retained native-evidence identity in a deterministic source-keyed proof checkout, without authorizing, publishing, dispatching, or completing any requirement."
    - "Preparation is crash-safe and exact-tuple idempotent: re-entry reuses the identical pending object; a different tuple, malformed object, or non-PASS review fails closed."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-41-SUMMARY.md
      provides: "Durable exact pending terminal tuple for Sean's exact-tuple authorization at 20-42"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-40-SUMMARY.md
      to: .planning/phases/20-transactional-delegated-mutation/20-41-SUMMARY.md
      via: "reauthenticated native-inclusive source/review tuple and pending-request identity"
      pattern: "source_sha"
---

<objective>
Prepare one durable terminal request for the exact sealed second-repaired-successor now qualified by a native-inclusive fresh review — finishing every safe, read-only or Git-private step before Sean's exact-tuple authorization at 20-42.

Purpose: Keep the terminal external mutation and requirement completion behind a durable preparation boundary and an exact tuple-specific Sean decision (mirrors the 20-17/20-27/20-34 role for the second repaired successor). All prior digests are spent/void; this is a fresh pending tuple for the exact sealed successor SHA.
Output: One standard GSD summary containing the immutable pending terminal tuple; no publication, dispatch, or requirement completion.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-37-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-39-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-40-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-40-INDEPENDENT-REVIEW.md
@.planning/scripts/verify-review-pair.sh
@.planning/scripts/verify-review-result.mjs
@scripts/f20-native-uat-proof.mjs
@.github/workflows/nightly-windows-soak.yml
</context>

<admission>
- Read the exact `(source_sha, source_tree, review_base_sha, review_sha)` tuple from `20-40-SUMMARY.md`; rerun `verify-review-pair.sh` over the sealed source set and `verify-review-result.mjs` profile `f20-native-16` against the exact review commit. Any mismatch, failed evidence command, nonzero finding, non-PASS disposition, or deferred-native result stops.
- Reauthenticate the retained 20-39 native evidence: the UAT ref's remote SHA still equals the sealed successor and both platforms' markers are intact. Any drift stops.
- Derive one deterministic proof checkout from the absolute primary Git directory plus the exact sealed successor SHA; persist a no-follow mode-0600 locator/state object; re-entry reopens the same locator. An unexpected path, different source, malformed state, symlink, or FIFO fails closed.
- Git-private pending-request state is the only durable authority; the request operation is exact-tuple idempotent (identical pending reused; conflicting state fails closed).
</admission>

<tasks>

<task type="auto">
  <name>Task 1: Reauthenticate the native-inclusive review and persist one pending terminal request</name>
  <files>None; this task writes only strict Git-private proof-checkout authority objects and the standard GSD summary.</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-40-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-40-INDEPENDENT-REVIEW.md, .planning/phases/20-transactional-delegated-mutation/20-39-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-37-SUMMARY.md, .planning/scripts/verify-review-result.mjs, scripts/f20-native-uat-proof.mjs</read_first>
  <behavior>
    - Reauthenticate the exact native-inclusive review tuple (profile `f20-native-16`, native PASS, deferred empty) and the retained 20-39 native evidence + the 20-37 aggregate/build receipts; never rerun unchanged proof.
    - Read-only runner preflight admits exactly one online idle self-hosted msvc runner and one eligible ephemeral macOS runner tuple.
    - `f20-native-uat-proof.mjs request` exclusively persists the exact sealed successor commit/tree/ref, runner id/name/image label, and retained-native-evidence identity with state pending in a strict no-follow mode-0600 Git-private object.
    - Re-execution follows the primary-Git-dir locator and returns the existing identical pending object; a different tuple, malformed object, non-pending state, non-PASS review, or ambiguous runner fails closed.
    - This plan does not authorize, publish, dispatch, retry, merge, release, deploy, tag, close an issue, delete a ref, or complete a requirement.
  </behavior>
  <action>Authenticate the exact native-inclusive review pair and schema-validated `f20-native-16` result, and the retained 20-39 native evidence. Atomically create or reopen the primary-Git-dir locator for the deterministic source-SHA-keyed immutable proof checkout; validate that checkout at the exact sealed successor SHA/tree. Switch GitHub auth to `FerroxLabs` immediately before runner inspection; run only the helper's read-only runner preflight; and create or reuse the identical strict pending terminal request binding the sealed successor, runner tuple, and retained native-evidence identity. Record the locator/state digest, checkout identity, candidate/ref/runner/image tuple, native-evidence identity, and pending-object digest in `20-41-SUMMARY.md`. Stop on any invalid or conflicting state.</action>
  <verify>The exact review-qualified sealed successor has authenticated review + native + aggregate receipts and exactly one durable pending terminal request; no authorization, publication, dispatch, or external source mutation exists.
    <automated>Re-run `node scripts/f20-native-uat-proof.mjs request` a second time and assert it returns a BYTE-IDENTICAL pending object (same nonce/tuple digest — idempotent reuse, no second request created); assert the persisted Git-private request/locator objects are mode-0600 and were written no-follow (stat the mode; a symlink/FIFO/dir at the authority path fails closed).</automated></verify>
  <acceptance_criteria>Preparation is crash-safe and exact-tuple idempotent. Re-entry cannot repeat expensive proof or create a second pending request. All Phase 20 requirements remain incomplete.</acceptance_criteria>
  <done>One standard GSD summary durably hands the immutable pending terminal tuple to plan 20-42.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal authorization ← pending request | The terminal (20-42) authenticates the pending tuple this plan writes; a forged/stale tuple could misauthorize |
| pending tuple ← review + native evidence | The tuple must bind only the exact native-inclusive-reviewed sealed successor and its retained native evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-41-01 | Tampering | pending terminal request | high | mitigate | No-follow mode-0600 Git-private object; exact-tuple idempotent; conflicting/malformed/non-PASS state fails closed. |
| T-20-41-02 | Spoofing | reused stale review/evidence | medium | mitigate | Reauthenticate the `f20-native-16` review pair AND the retained native evidence remote SHA before writing the tuple; drift stops. |
</threat_model>

<verification>
Reauthenticate the exact `f20-native-16` review tuple, the retained 20-39 native evidence, the clean proof-checkout commit/tree, and the strict pending-request bytes. No Cargo runs on the Mac and no external mutation occurs.
</verification>

<success_criteria>
- Preparation is complete and durably summarized before the human decision boundary; that verified summary alone admits plan 20-42.
- An interrupted or repeated execution reuses only the identical pending tuple.
- Terminal native re-verification, aggregate proof, and requirement completion remain pending for 20-42.
</success_criteria>

## Artifacts this phase produces
- `.planning/phases/20-transactional-delegated-mutation/20-41-SUMMARY.md` — immutable pending terminal-request handoff with exact candidate, runner tuple, and native-evidence identity.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-41-SUMMARY.md` using the standard GSD summary template. Record exact source/review authority, authenticated review + native + aggregate receipt identities, proof-checkout identity, pending-request digest, runner tuple, and explicit non-claims. Complete no Phase 20 requirement.
</output>

---
phase: 20-transactional-delegated-mutation
plan: "22"
type: execute
wave: 21
depends_on:
  - "20-21"
files_modified:
  - scripts/f20-native-macos-proof.sh
  - scripts/f20-native-uat-proof.mjs
  - .github/workflows/nightly-windows-soak.yml
  - .planning/scripts/verify-review-result.mjs
autonomous: true
requirements:
  - REQ-native-r9
  - REQ-native-r11
  - REQ-native-r13
must_haves:
  truths:
    - "The macOS proof harness is re-validated against real macOS: all 8 macOS targets map to tests that actually exist for macOS and are green, and any aspirational mapping is corrected the same way the Windows harness was (no macOS target may point at a Windows-only or nonexistent test) (REQ-native-r9)."
    - "The Windows native proof leg runs on an AppContainer-capable self-hosted msvc runner (labels self-hosted, Windows, X64, msvc) — not the hosted windows-2022 image that reports is_available() false — and the workflow runs-on changes accordingly (REQ-native-r11)."
    - "f20-native-uat-proof.mjs no longer has a verifier-only-vs-writer gap: the request-writing path durably persists the pending request it later verifies, so preparation (20-27) can produce exactly what the terminal (20-28) authenticates (writer/verifier parity)."
    - "verify-review-result.mjs gains a native-inclusive review profile whose checks include native_macos and native_windows as PASS checks with an empty deferred set, so the fresh 20-16-role review (20-26) can be schema-validated with native NOT deferred, per claimed reviewer (REQ-native-r13)."
  artifacts:
    - path: scripts/f20-native-macos-proof.sh
      provides: "Re-validated macOS target->test map with the wrong-OS anti-drift guard applied"
    - path: .github/workflows/nightly-windows-soak.yml
      provides: "Windows native leg pinned to the self-hosted msvc runner labels"
    - path: scripts/f20-native-uat-proof.mjs
      provides: "Writer/verifier parity for the durable pending native-proof request"
    - path: .planning/scripts/verify-review-result.mjs
      provides: "Native-inclusive (deferred-empty) review profile plus a cross-audit profile for the gate sequence"
  key_links:
    - from: .github/workflows/nightly-windows-soak.yml
      to: scripts/f20-native-windows-proof.ps1
      via: "the self-hosted msvc job invokes the Windows proof entrypoint"
      pattern: "self-hosted"
    - from: .planning/scripts/verify-review-result.mjs
      to: .planning/phases/20-transactional-delegated-mutation/20-26-INDEPENDENT-REVIEW.md
      via: "the fresh native-inclusive review is validated against the new profile"
      pattern: "deferred"
---

<objective>
Re-validate the macOS proof harness against real macOS (do NOT assume it green — the same aspirational-machinery pattern that broke the Windows harness previously bit macOS), pin the Windows native leg to the self-hosted msvc runner, close the UAT-proof writer/verifier gap, and add the native-inclusive review profile the fresh 20-16-role review needs.

Purpose: These are the harness + CI + verifier plumbing items (REQ-native-r9/r11 + the STATE writer-gap blocker + the R13 attestation requirement). Without the native-inclusive profile the fresh review cannot be schema-validated with native NOT deferred; without the self-hosted runner the Windows leg runs where AppContainer is unavailable.
Output: Re-validated macOS mappings, self-hosted `runs-on`, writer/verifier parity, and two new review profiles — construction-proven on the Mac and node-self-tested; macOS/Windows green proven on hardware at 20-25.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/inbox/native-uat-repair-BRIEF.md
@.planning/intel/inbox-2026-07-23/AUDIT-2026-07-23.md
@.planning/phases/20-transactional-delegated-mutation/20-21-SUMMARY.md
@scripts/f20-native-macos-proof.sh
@scripts/f20-native-uat-proof.mjs
@scripts/f20-native-uat-proof.test.mjs
@.github/workflows/nightly-windows-soak.yml
@.planning/scripts/verify-review-result.mjs
@.planning/codebase/TESTING.md
</context>

<execution_rules>
- Capture the plan base before edits: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-22)"`.
- NO Cargo/clippy/nextest on the Mac. macOS `run_target` selectors compile/run only on the ephemeral Scaleway Apple-silicon runner at 20-25; node self-tests run on the Mac.
- Do NOT dispatch, push, or trigger any workflow here. This plan edits harness/CI/verifier source only.
- The self-hosted runner facts (BRIEF §8): AppContainer-capable Windows 11 client with online self-hosted msvc runners labeled `self-hosted, Windows, X64, msvc`; the macOS leg is an ephemeral Scaleway Apple-silicon runner that REQUIRES `DOCKER_HOST` exported into the runner process env (colima socket).
- The macOS re-validation must apply the same wrong-OS anti-drift guard authored in 20-21: no macOS target may select a `#![cfg(windows)]` or nonexistent test (the recorded 20-18 macOS failure was exactly `live_integrity.rs` being `#![cfg(windows)]` -> 0 tests).
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Re-validate the macOS proof harness and apply the anti-drift guard</name>
  <files>scripts/f20-native-macos-proof.sh</files>
  <read_first>scripts/f20-native-macos-proof.sh, scripts/f20-native-uat-proof.mjs, .planning/phases/20-transactional-delegated-mutation/20-18-SUMMARY.md</read_first>
  <behavior>
    - Every one of the 8 macOS targets selects a test that exists and is macOS-runnable (`#![cfg(target_os = "macos")]` or `#[cfg(target_os = "macos")]`), never a Windows-only or absent test.
    - The macOS-side wrong-OS anti-drift guard (from 20-21) is applied: before running a target the harness asserts the selected test's source is macOS-gated; a mismatch fails closed.
    - The harness keeps its existing fail-closed preflight (repo root, clean tree, exact commit+tree, acceptance env, Darwin, live `docker info`), the per-target `--run-ignored all --no-tests=fail` discipline, and the ordered per-target + single final markers.
    - Any target whose macOS acceptance test is genuinely unwritten is either authored as a real macOS test or its target is corrected — no aspirational mapping remains.
  </behavior>
  <action>Audit each of the 8 `run_target` selectors in `f20-native-macos-proof.sh` against the actual macOS test surface: confirm the selected `--test`/`-E` names resolve to macOS-gated tests that exist. Apply the 20-21 anti-drift guard on the macOS side so a target pointing at a non-macOS or missing test fails closed (the recorded 07-22 macOS failure was `live_integrity.rs` being Windows-only). Where a target maps to an unwritten or wrong-OS test, correct the mapping to the real macOS acceptance test (authoring one only if genuinely missing, matching the falsifiable `#![cfg(target_os = "macos")]` + `#[ignore]` + `WAYLAND_SANDBOX_LIVE_MACOS`/`docker info` pattern the reviewed macOS tests already use). Preserve the marker grammar and the `DOCKER_HOST`/colima dependency note. Do not weaken any preflight.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-22)" scripts/f20-native-macos-proof.sh</automated>
    <automated>bash -n scripts/f20-native-macos-proof.sh</automated>
    <automated>! git -c grep.lineNumber=false grep -q 'live_integrity' -- scripts/f20-native-macos-proof.sh</automated>
  </verify>
  <acceptance_criteria>All 8 macOS targets select existing macOS-gated tests; the anti-drift guard is applied; no target points at a Windows-only or absent test; preflights intact. macOS green is proven at 20-25.</acceptance_criteria>
  <done>The re-validated macOS harness exists as a provisional task commit.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the self-hosted msvc runner, close the writer gap, and add the review profiles</name>
  <files>.github/workflows/nightly-windows-soak.yml, scripts/f20-native-uat-proof.mjs, .planning/scripts/verify-review-result.mjs</files>
  <read_first>.github/workflows/nightly-windows-soak.yml, scripts/f20-native-uat-proof.mjs, scripts/f20-native-uat-proof.test.mjs, .planning/scripts/verify-review-result.mjs</read_first>
  <behavior>
    - The candidate-mode Windows native job runs on `[self-hosted, Windows, X64, msvc]` (an AppContainer-capable client SKU), not hosted `windows-2022`; scheduled non-candidate behavior is unchanged; the macOS leg still targets the pinned ephemeral runner with `DOCKER_HOST` exported.
    - `f20-native-uat-proof.mjs` has writer/verifier parity: the `request` path exclusively and durably persists the pending native-proof tuple (candidate commit/tree/ref + runner id/name/image label, state pending) that `verify-*`/`proof-checkout` later read — no verifier expects an object the writer never persists, and re-`request` is exact-tuple idempotent.
    - `verify-review-result.mjs` gains two profiles: a native-inclusive review profile (checks include `native_macos` and `native_windows` as PASS, `deferred: []`) for the fresh 20-16-role review, and a cross-audit profile (all_severity + integration/evidence checks, native still deferred) for the pre-native cross-audit gate; existing profiles are untouched.
  </behavior>
  <action>In `nightly-windows-soak.yml`, change the candidate-mode Windows native job `runs-on` to the self-hosted msvc labels `[self-hosted, Windows, X64, msvc]`, keeping candidate-mode's `contents: read`-only grant, no-secrets, disabled caches, job/runner identity capture, and the unchanged scheduled soak; leave the macOS job's ephemeral pin and `DOCKER_HOST` export intact. In `f20-native-uat-proof.mjs`, close the writer/verifier gap: ensure the `request` subcommand durably writes (no-follow, mode-0600, Git-private) the exact pending tuple the verifier/`proof-checkout` paths consume, make repeat `request` return the identical pending object, and fail closed on a conflicting or malformed one — so 20-27 preparation persists exactly what 20-28 authenticates. In `verify-review-result.mjs`, add a native-inclusive profile (e.g. `f20-native-16`) with `checks: [all_severity, asvs_level_2, code_review, native_macos, native_windows, phase_validation]` and `deferred: []`, and a cross-audit profile (e.g. `f20-native-crossaudit`) with `checks: [all_severity, evidence_integrity, integration_authority]` and `deferred: [native_macos, native_windows]`; do not alter f20-09/11/14/15/16. Trigger no workflow.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-22)" scripts/f20-native-macos-proof.sh .github/workflows/nightly-windows-soak.yml scripts/f20-native-uat-proof.mjs .planning/scripts/verify-review-result.mjs</automated>
    <automated>node --check scripts/f20-native-uat-proof.mjs && node --check .planning/scripts/verify-review-result.mjs</automated>
    <automated>git -c grep.lineNumber=false grep -q 'msvc' -- .github/workflows/nightly-windows-soak.yml && for p in f20-native-16 f20-native-crossaudit; do git -c grep.lineNumber=false grep -q "$p" -- .planning/scripts/verify-review-result.mjs || exit 1; done</automated>
    <automated>node --test scripts/f20-native-uat-proof.test.mjs</automated>
  </verify>
  <acceptance_criteria>The Windows leg is pinned to self-hosted msvc, the UAT-proof writer/verifier parity holds and its node self-tests pass, and both new review profiles exist without disturbing the existing ones. Construction only.</acceptance_criteria>
  <done>The runner pin, writer-gap fix, and review profiles exist as a provisional task commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native leg ← runner host | The Windows leg must run where AppContainer exists; a hosted image without it produces a false "unavailable" |
| terminal auth ← pending request | The terminal (20-28) authenticates a pending request the prep (20-27) wrote; a writer/verifier mismatch could strand or forge authorization |
| review PASS ← profile | A review's schema-validated PASS depends on the profile's exact checks/deferred sets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-22-01 | Spoofing | macOS proof target mapping | high | mitigate | Re-validate all 8 targets + apply the anti-drift guard so a wrong-OS/absent macOS test fails closed (the exact 07-22 macOS failure mode). |
| T-20-22-02 | Tampering | UAT-proof pending request | high | mitigate | Writer/verifier parity: `request` durably persists the exact tuple the verifier reads; re-request is idempotent; conflicting/malformed state fails closed. |
| T-20-22-03 | Elevation of Privilege | runner selection | medium | mitigate | Candidate-mode self-hosted msvc job keeps `contents: read`-only, no secrets, disabled caches, and records runner identity into evidence. |
</threat_model>

<verification>
After both task commits exist and the checkout is clean, the committed-HEAD Hetzner gate confirms no Rust regression from any authored macOS test, and the node self-tests confirm the harness/verifier edits:
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-22-clippy "$(pwd -P)" clippy -p wcore-sandbox -p wcore-swarm --all-targets --all-features -- -D warnings</automated>
Record exact `source_sha`/tree, receipts, and changed paths in `20-22-SUMMARY.md`. The summary states macOS/Windows native green is proven ONLY at 20-25 and completes no requirement.
</verification>

<success_criteria>
- The macOS harness is re-validated with no aspirational mappings and the anti-drift guard applied (r9).
- The Windows native leg is pinned to the self-hosted msvc runner (r11).
- The UAT-proof writer/verifier gap is closed and its node self-tests pass.
- The native-inclusive and cross-audit review profiles exist, enabling schema-validated native-not-deferred review (r13); Hetzner clippy clean.
</success_criteria>

## Artifacts this phase produces
- Re-validated macOS harness, self-hosted runner pin, writer/verifier parity, and the two new review profiles.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-22-SUMMARY.md` using the standard GSD summary template. Record exact source identity, receipts, changed paths, and the deferral of macOS/Windows green to 20-25. Complete no Phase 20 requirement.
</output>

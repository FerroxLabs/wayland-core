---
phase: 20-transactional-delegated-mutation
plan: "44"
subsystem: infra
tags: [native-repair, lockfile-seal, cargo-lock, dunce, wcore-swarm, resync, hetzner-proof, remote-cargo, locked-build, aggregate-nextest, third-repaired-successor, no-linux-regression, candidate-seal]

# Dependency graph
requires:
  - phase: "20-43"
    provides: "Third-repaired successor source_sha 92cac8bb0a950082b92de9698c15ccde4eab9e44 (tree 0fd3368b1ad08888409cf08560d95347d9d5b851) carrying the #A/#B production fixes + Job-Object counting-scope + lib-test compile-debt; branch tip 5f433dd1 (docs-only over the source-complete HEAD). Cargo.lock left at the base (dunce edge deliberately reverted for the harness clean-tree gate); the authoritative resync deferred to this plan."
provides:
  - "SEALED third-repaired-successor candidate: source_sha 3f839309574d6741eed416cd3820f56447f74eba (tree 3092475bb4102d010b6ff5f6c9d8080cb4f51928) — the ONE exact SHA every downstream gate (20-45 Kimi cross-audit, 20-46 native re-dispatch, 20-47 native-inclusive review, 20-48 prep, 20-49 terminal) binds to."
  - "Cargo.lock resynced for the wcore-swarm->dunce direct-dependency edge: Hetzner-regenerated (cargo fetch, non-locked), retrieved and committed on the Mac verbatim (never hand-edited). Delta is exactly one line (the edge over the already-resolved dunce 1.0.5 stanza); NO new crate; no package-legitimacy checkpoint triggered."
  - "Green Hetzner receipts against the sealed tree: `build --workspace --all-features --locked` exit 0 (proves the resynced lock is consistent) + aggregate `nextest --profile ci --no-fail-fast` = 11509 passed / 0 failed / 48 skipped (no Linux regression from the 20-43 edits)."
  - "The 20-43 aggregate's sole failure (spawner concurrent_near_cap_admits_exactly_one_retained_workspace) is now PASS on first try at 363 GB free — the environmental disk-pressure diagnosis is confirmed complete, not a genuine unresolved failure."
affects: ["20-45", "20-46", "20-47", "20-48", "20-49"]

# Tech tracking
tech-stack:
  added: []   # No new crate. dunce 1.0.5 was already resolved in the lock (transitive via aws-lc-sys/chromiumoxide); this plan only records the wcore-swarm direct edge.
  patterns:
    - "Lockfile resync performed on the committed-HEAD Hetzner harness (cargo fetch, non-locked) and the exact regenerated bytes retrieved and committed on the Mac — no Cargo runs on the Mac, no hand-edit of the lock."
    - "Confirm-then-seal: a `--locked` build first PROVES the base lock stale (exit 101, `cannot update the lock file ... --locked`), the resync adds only the missing edge, then a second `--locked` build against the sealed tree PROVES consistency."

key-files:
  created:
    - .planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md
  modified:
    - Cargo.lock

key-decisions:
  - "The resync used `cargo fetch` (a clean non-locked resolve) rather than `cargo generate-lockfile`. generate-lockfile rebuilds the lock from scratch and can bump every dependency to its latest allowed version; cargo fetch preserves the existing lock and adds only the missing edge — the minimal, auditable delta the plan requires. The result matches the 20-43 macOS `cargo check` observation exactly (single `+ \"dunce\"` line)."
  - "The regenerated Cargo.lock was retrieved from the harness slot (/root/cargo-slots/f20-44-aggregate/Cargo.lock) via `ssh cat` and copied byte-for-byte onto the Mac's Cargo.lock (cp of the exact bytes) — Hetzner-generated, Mac-committed, never hand-edited. `git diff --cached` confirmed a single-line insertion before the commit."
  - "No package-legitimacy checkpoint (T-20-44-01): the delta added the wcore-swarm->dunce edge over the pre-existing dunce 1.0.5 [[package]] stanza (checksum 92773504...); no new [[package]] block, no version bump, no new crate."
  - "The 20-43 spawner failure was environmental (Hetzner at 98% disk / ~42 GB free; the near-cap durable-launch admission reserves 8 GB + 512 MB per launch and fails closed on `df`). After the box was reclaimed to 79% used / 363 GB free, the identical test PASSES on first try (0.391s) and the aggregate is a clean 11509/0/48 — the diagnosis was complete, not a masked code failure."

patterns-established:
  - "A source-frozen lockfile-only seal: after this commit NO source, test, or workflow file changes for the remainder of the third-repaired-successor sequence; the sealed SHA is the exact candidate all downstream gates bind to."

requirements-completed: []  # NO Phase-20 requirement is claimed here (that is 20-49's job). This plan implements REQ-native-r10 (lock seal) + REQ-native-r4 (Linux no-regression) as gates, not as requirement completion.

# Metrics
duration: ~15min
completed: 2026-07-24
status: complete
---

# Phase 20 Plan 44: Seal the third-repaired successor — Cargo.lock resync for the wcore-swarm->dunce edge + green `--locked` build + aggregate 11509/0/48 Summary

**Re-seals the ONE exact third-repaired-successor candidate at `3f839309` (tree `3092475b`): resyncs `Cargo.lock` for the `dunce` direct edge that 20-43 added to `wcore-swarm/Cargo.toml` (Hetzner-regenerated via `cargo fetch`, Mac-committed verbatim, single-line delta over the already-resolved dunce 1.0.5 — no new crate), then proves on Hetzner that the sealed tree builds `--locked --workspace --all-features` (exit 0) and passes the aggregate Linux suite unchanged at 11509 passed / 0 failed / 48 skipped. The 20-43 spawner failure — diagnosed as environmental disk pressure — now PASSES on first try at 363 GB free, confirming the diagnosis was complete.**

## Candidate identity (SEALED)

- **sealed source_sha:** `3f839309574d6741eed416cd3820f56447f74eba`
- **sealed tree:** `3092475bb4102d010b6ff5f6c9d8080cb4f51928`
- **branch:** `plan/f20-unified-audit-repair` (isolated standalone checkout `/Users/seandonahoe/dev/waylandcore-ferrox`; `.git` is a directory; all git ops via `/usr/bin/git`)
- **parent:** `5f433dd1` (20-43 docs tip) over source-complete HEAD `92cac8bb` (tree `0fd3368b`). The seal commit adds ONLY `Cargo.lock` (the dunce edge); the buildable source is byte-identical to 20-43's `92cac8bb` source.

## The lock resync (REQ-native-r10)

| Step | Command (all Cargo on Hetzner — none on the Mac) | Result |
|---|---|---|
| Confirm stale | `remote-cargo.sh f20-44-aggregate <wt> build --workspace --all-features --locked` (against base HEAD `5f433dd1`) | **exit 101** — `error: cannot update the lock file /root/cargo-slots/f20-44-aggregate/Cargo.lock because --locked was passed to prevent this`. Confirms the base lock lacks the wcore-swarm->dunce edge that `wcore-swarm/Cargo.toml` (`dunce = "1"`) now requires. |
| Resync | `remote-cargo.sh f20-44-aggregate <wt> fetch` (non-locked) | **exit 0** — writes the resynced Cargo.lock into the slot (all crates already cached; no bump). |
| Retrieve + commit | `ssh cat /root/cargo-slots/f20-44-aggregate/Cargo.lock` → `cp` onto the Mac's `Cargo.lock` → `/usr/bin/git commit` | **committed** as `3f839309`. |

**Exact lock delta (confirmed, dunce-edge-only):**
```diff
 name = "wcore-swarm"
 version = "0.12.25"
 dependencies = [
+ "dunce",
  "fd-lock",
  "futures",
```
- `git diff --cached --stat` = **`Cargo.lock | 1 +` (1 insertion)**.
- The `dunce` 1.0.5 `[[package]]` stanza (checksum `92773504d58c093f6de2459af4af33faa518c13451eb8f2b5698ed3d36e7c813`) already existed (previously depended on by two packages via aws-lc-sys/chromiumoxide). **NO new `[[package]]` block, no version bump, no new crate → no package-legitimacy checkpoint (T-20-44-01 satisfied).**
- Scope gate: `verify-task-scope.sh <base> Cargo.lock` = **`scope-ok base=5f433dd1 ... paths=1`** — the union of changes is exactly `Cargo.lock`.

## Hetzner receipts against the SEALED tree (`3f839309` / tree `3092475b`)

Remote receipt identity: box `root@95.216.244.213:16666`, slot `f20-44-aggregate`, `WAYLAND_BUILD_SOURCE_SHA=3f839309...`, remote tree-hash gate verified `3092475b` == local HEAD tree. Box at **79% used / 363 GB free** (reclaimed from the 98%/42 GB state that environmentally blocked 20-43); the slot's debug target was cleared, so this was a COLD rebuild.

| Gate | Command | Result |
|---|---|---|
| `--locked` build (r10 consistency) | `remote-cargo.sh f20-44-aggregate <wt> build --workspace --all-features --locked` | **exit 0** — `Finished dev profile [unoptimized + debuginfo] in 1m 28s`; 0 errors. The resynced lock is `--locked`-consistent (cargo compiled without needing to touch the lock). Lone note: `imap-proto v0.10.2` future-incompat warning — pre-existing, outside the delta. |
| Aggregate `nextest --profile ci` (r4 no-regression) | `remote-cargo.sh f20-44-aggregate <wt> nextest run --workspace --profile ci --no-fail-fast` | **exit 0** — **Nextest run ID `32f1b4ba-ed6d-4d14-853f-f831d7798731`**, profile `ci`. `Starting 11509 tests across 469 binaries (48 tests skipped)` → **`Summary [72.986s] 11509 tests run: 11509 passed (2 flaky), 48 skipped`** = **11509 passed / 0 failed / 48 skipped**. |

**Honesty gate — met exactly at 11509/0/48.**
- The 20-43 sole failure `wcore-agent spawner::production_durable_spawn_tests::concurrent_near_cap_admits_exactly_one_retained_workspace` is now **`PASS [0.391s]` on first try** (no retry). This is the definitive confirmation that the 20-43 diagnosis (environmental disk pressure, not a code regression) was complete: with adequate free disk the near-cap admission succeeds and exactly one launch is retained.
- The 2 "flaky" tests (`wcore-swarm worktree::tests::linux::status_output_cap_kills_git_descendant`, `wcore-cli::deterministic_openai_loop packaged_core_cancels_an_active_stream`) each retried **3/3 → PASS**; nextest counts them as passed. Zero real failures — `nextest` returns non-zero on any test still failing after retries, and it exited 0.

## HONESTY — what this seal does and does NOT prove

- **Proven (Linux):** the resynced lock is consistent; the full workspace builds `--locked --all-features`; the aggregate Linux suite is unregressed at 11509/0/48. The only Linux-compiled 20-43 change (`wcore-swarm` `dunce::simplified`) is a unix no-op — all other 20-43 edits are `#[cfg(windows)]` and compile to empty on Linux, so the Linux count is provably untouched.
- **NOT proven here (deferred):** every Windows compile + real-hardware behavior (#A env-var probe transport, #B cmd.exe payload quoting, containment counting scope, lib-test compile) is proven ONLY at the **20-46** msvc native re-dispatch. **No Phase 20 requirement is completed** (that is 20-49's job).
- **Source-frozen:** after this lock seal, NO source, test, or workflow change follows for the remainder of the third-repaired-successor sequence. `3f839309` is the exact candidate 20-45…20-49 bind to.

## Deviations from Plan

**None material.** The plan permitted regeneration "via `generate-lockfile` or capture from a clean non-locked build"; `cargo fetch` (a clean non-locked resolve) was chosen over `generate-lockfile` specifically to guarantee the minimal single-edge delta rather than a from-scratch re-resolution that could bump unrelated pins. No source/test/workflow files were touched; the only committed change is the one-line Cargo.lock edge.

## Issues Encountered
None. The box was reachable throughout; the cold `--locked` build (1m 28s) and cold nextest compile (1m 54s) both completed clean on the reclaimed-disk box.

## User Setup Required
None.

## Next Phase Readiness
- The sealed third-repaired successor `3f839309` (tree `3092475b`) has a consistent dunce-edge lockfile and green `--locked` build + 11509/0/48 aggregate receipts. Ready for the **20-45** pre-native Kimi K3 cross-audit.
- All Windows compile + real-hardware behavior remains deferred to the **20-46** msvc re-dispatch; requirement completion remains deferred to **20-49**.

## Self-Check: PASSED
- `20-44-SUMMARY.md` present.
- Seal commit `3f839309` present in history; `Cargo.lock` diff vs base is exactly the single wcore-swarm->dunce edge (`paths=1`, `1 insertion`).
- `--locked` Hetzner build exit 0; aggregate nextest exit 0 at 11509/0/48 (run ID `32f1b4ba-ed6d-4d14-853f-f831d7798731`); the 20-43 spawner failure now PASS on first try.

---
*Phase: 20-transactional-delegated-mutation*
*Completed: 2026-07-24*

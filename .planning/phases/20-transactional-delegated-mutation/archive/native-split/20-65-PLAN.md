---
phase: 20-transactional-delegated-mutation
plan: "65"
type: execute
wave: 62
depends_on:
  - "20-53"
files_modified:
  - crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs
autonomous: true
gap_closure: true
requirements:
  - REQ-native-r2
  - REQ-native-r4
must_haves:
  truths:
    - "CLASS B (REAL production security defect, hardware-proven at 20-53 SEANDESKTOP): `fs_read_deny` is no longer implemented as a package-SID DENY ace. Windows' AppContainer lowbox access check does NOT honor a DENY ace against the AppContainer's OWN package SID (proven on a minimal AppContainer: a canonically ordered DENY→ALLOW DACL was read straight through, secret disclosed, exit 0), so the DENY-ace `fs_read_deny` was INERT and provided NO protection for the deny-a-subpath-under-a-granted-tree case. For `IntentKind::Deny`, the lease now enforces denial by REMOVAL: it sets `PROTECTED_DACL_SECURITY_INFORMATION` (0x80000000) on the target and strips every package-SID (`S-1-15-2-…`) ALLOW ace — including inherited ones — leaving only normal SIDs, which AppContainer ignores for granting → the child is denied by ABSENCE of a package grant (proven on hardware: exit 1, 'Access is denied.'). This makes `fs_read_deny` a REAL control and is CONSISTENT with the passing `normal_sid_only_grant_is_denied` (AppContainer denial always comes from withholding the package grant, never from a package DENY) (REQ-native-r2)."
    - "The fix touches `apply_intents` (521-537: route `IntentKind::Deny` to the protected-DACL removal path instead of `DENY_ACCESS`), `explicit_access_for_sid` (839-852) and `apply_explicit_access` (854-883: the package-SID-ALLOW-stripping DACL rebuild writes back with `DACL_SECURITY_INFORMATION | PROTECTED_DACL_SECURITY_INFORMATION`), and imports `PROTECTED_DACL_SECURITY_INFORMATION` (and `UNPROTECTED_DACL_SECURITY_INFORMATION` for revoke). A directory deny target is protected per-object. The `IntentKind::Deny if intent.mask == ACL_READ_MASK` validation (line 464) and the Allow (GRANT_ACCESS) path are unchanged (REQ-native-r2)."
    - "Cleanup/revoke is symmetric and complete: `cleanup_locked` (540) / `remove_and_verify_exact_sid` (687) clear `PROTECTED_DACL_SECURITY_INFORMATION` (write back with `UNPROTECTED_DACL_SECURITY_INFORMATION` to restore inheritance) and restore the original package-SID ALLOW state on revoke of a deny, so a deny leaves NO permanent DACL alteration on the host filesystem — preserving the Phase-20 invariant that the sandbox leaves no residual grant OR residual protection after a run. The removal path must NOT reintroduce the deny-only-SID bug the 20-02/native fixes closed (dropping deny-only SidsToDisable restored reads); denial here comes from package-ALLOW removal + PROTECTED, never from re-enabling deny-only SIDs (REQ-native-r4)."
    - "This is SECURITY-SENSITIVE production code: it does NOT land toward native until the 20-66 FOUR-WAY cross-audit panel (Codex 5.6 Sol + Gemini 3.1 Pro + Kimi K3 + internal Claude adversarial, ALL-FOUR-MUST-PASS, fail-closed) returns zero findings over the exact committed delta. Local proof is limited to `cargo fmt`, the sanctioned Mac compile gate, and the committed-HEAD Hetzner clippy/aggregate harness (no Linux/macOS/workspace regression); the Windows compile AND the on-hardware deny/allow/revoke behavior are proven at 20-67 on SEANDESKTOP, never on the Mac (CONTEXT D5/D7)."
  artifacts:
    - path: crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs
      provides: "A real fs_read_deny control — protected-DACL package-ALLOW removal that AppContainer actually honors — with symmetric revoke that restores inheritance and leaves no residual protection"
  key_links:
    - from: crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs
      to: .planning/phases/20-transactional-delegated-mutation/20-53-SUMMARY.md
      via: "closes the CLASS B production security defect (inert fs_read_deny) hardware-enumerated by the 20-53 SEANDESKTOP native run in 20-VERIFICATION.md"
      pattern: "gap_closure"
---

<objective>
Close CLASS B — the ONE real production security defect from the 20-53 SEANDESKTOP native run (20-VERIFICATION.md): `fs_read_deny` was implemented as a package-SID DENY ace that Windows AppContainer ignores against its own package SID, so it was INERT and gave no protection for a denied subpath under a granted tree. Reimplement deny as enforced REMOVAL — set `PROTECTED_DACL_SECURITY_INFORMATION` and strip every package-SID ALLOW (including inherited), leaving only normal SIDs AppContainer ignores for granting — so the child is denied by absence of a package grant, with symmetric revoke that restores inheritance.

Purpose: This is the delicate one — it is production security code, and the fix must (a) make `fs_read_deny` a real control (hardware-proven: exit 1, "Access is denied."), (b) NOT reintroduce the deny-only-SID bug the earlier native fix closed (which had made the sandbox read no file at all), and (c) leave no residual DACL protection after revoke. Because it is security-sensitive, it is gated behind the 20-66 four-way cross-audit before it advances to the SEANDESKTOP verify.
Output: The reimplemented deny path + symmetric revoke in `acl_lease.rs`, committed as the exact delta the 20-66 panel audits; Windows compile + behavior proven at 20-67.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-VERIFICATION.md
@crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs
</context>

<execution_rules>
- Production code, SECURITY-SENSITIVE. Touch ONLY `crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs`. No test file, no other production file.
- This delta does NOT advance toward native until 20-66 (the four-way cross-audit) returns a zero-finding ALL-FOUR PASS over the exact committed SHA. Fail-closed: any finding blocks and routes back here.
- NO cargo/clippy/nextest on this Mac except the sanctioned compile gate (CONTEXT D5). Permitted local proof: `cargo fmt`; `cargo check -p wcore-sandbox --features live-docker --tests` (compile/type-check only — note this is a Mac target, so it does NOT compile the `#[cfg(windows)]` acl_lease.rs body; it only proves no macOS-side breakage and blocks on a false green); and the committed-HEAD Hetzner harness `/Users/seandonahoe/.ratchet/harness/remote-cargo.sh` for clippy + the workspace aggregate (Linux — proves no Linux/workspace regression). The Windows compile of the `#[cfg(windows)]` acl_lease.rs body AND the on-hardware deny/allow/revoke behavior are proven at 20-67 on SEANDESKTOP, never here.
- Denial MUST come from package-SID-ALLOW removal + `PROTECTED_DACL_SECURITY_INFORMATION`, NEVER from re-enabling deny-only SidsToDisable (that path caused the "sandbox can read no file" regression the earlier native fix closed).
- Revoke MUST be symmetric: clear `PROTECTED` (restore inheritance via `UNPROTECTED_DACL_SECURITY_INFORMATION`) and restore the original package-ALLOW state, leaving no residual protection or grant on the host filesystem.
- Use `/usr/bin/git`; never stage the AGENTS.md ijfw churn.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Reimplement IntentKind::Deny as protected-DACL package-ALLOW removal</name>
  <files>crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs</files>
  <read_first>crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs, .planning/phases/20-transactional-delegated-mutation/20-VERIFICATION.md</read_first>
  <behavior>
    - Applying a lease that carries a `fs_read_deny` intent on a subpath under a granted tree results, on hardware, in the sandboxed child being denied that subpath (exit 1, "Access is denied.") while still reading the granted siblings.
    - A denied FILE target has `PROTECTED_DACL_SECURITY_INFORMATION` set and carries no package-SID (`S-1-15-2-…`) ALLOW ace, inherited or explicit; only normal SIDs remain, which AppContainer ignores for granting.
    - A denied DIRECTORY target is protected per-object with the same package-ALLOW stripping.
    - The Allow (GRANT_ACCESS) path and the `IntentKind::Deny if intent.mask == ACL_READ_MASK` validation (line 464) are unchanged.
  </behavior>
  <action>In `apply_intents` (518-538), split the `IntentKind::Deny` branch off the current `explicit_access_for_sid(sid, mask, DENY_ACCESS)` + `apply_explicit_access` merge (which produces an inert package DENY ace) and route it to a new protected-removal path. That path: reads the target DACL (reuse `read_dacl`), rebuilds a DACL that OMITS every package-SID ALLOW ace whose SID begins with the AppContainer package prefix `S-1-15-2-` (both explicit and inherited — capture inherited entries explicitly so PROTECT does not silently re-grant), keeps all normal-SID entries, and writes it back with `SetNamedSecurityInfoW` using `DACL_SECURITY_INFORMATION | PROTECTED_DACL_SECURITY_INFORMATION` so no inherited package ALLOW can re-apply. Import `PROTECTED_DACL_SECURITY_INFORMATION` (and `UNPROTECTED_DACL_SECURITY_INFORMATION`, used by Task 2) from the appropriate `windows::Win32::Security` path alongside the existing `DACL_SECURITY_INFORMATION` import (line 42 group). Keep `explicit_access_for_sid`/`apply_explicit_access` for the Allow path; if the Deny path no longer uses `DENY_ACCESS`, remove now-dead Deny wiring only as an orphan of this change (no drive-by edits). A directory target is protected per-object (apply to the directory itself, not via a wildcard). Denial comes ONLY from package-ALLOW removal + PROTECTED — do NOT re-enable any deny-only SID. Per 20-VERIFICATION.md CLASS B; repro artifacts C:\aclrepro (box) / scratchpad/aclrepro (local) proved the exit-1 outcome.</action>
  <verify>
    <automated>cargo fmt --all --check</automated>
    <automated>grep -nE "PROTECTED_DACL_SECURITY_INFORMATION|S-1-15-2-|IntentKind::Deny" crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs | head</automated>
    <automated>cargo check -p wcore-sandbox --features live-docker --tests</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-65-clippy "$(pwd -P)" clippy -p wcore-sandbox --all-targets --all-features -- -D warnings</automated>
  </verify>
  <done>`IntentKind::Deny` applies via a protected-DACL rebuild that strips every `S-1-15-2-` ALLOW and sets `PROTECTED_DACL_SECURITY_INFORMATION`; no package DENY ace is added; Allow path unchanged; fmt clean; Mac compile gate green; Hetzner clippy clean. On-hardware deny proven at 20-67.</done>
</task>

<task type="auto">
  <name>Task 2: Symmetric revoke — clear PROTECTED, restore inheritance, leave no residual protection</name>
  <files>crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs</files>
  <read_first>crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs</read_first>
  <behavior>
    - After a lease with a `fs_read_deny` intent is cleaned up, the denied target's DACL carries no `PROTECTED_DACL_SECURITY_INFORMATION` flag and its original inheritance is restored — the deny leaves no permanent alteration.
    - `unrelated_acl_survives_exact_sid_cleanup`-style invariants hold: cleanup touches only what this lease altered.
    - Revoke of a deny never re-enables a deny-only SID.
  </behavior>
  <action>Extend the cleanup path so revoking a `fs_read_deny` intent is symmetric with Task 1's apply. In `cleanup_locked` (540) / `remove_and_verify_exact_sid` (687), for a target that was denied via the protected-removal path, write the DACL back with `UNPROTECTED_DACL_SECURITY_INFORMATION` (clearing `PROTECTED_DACL_SECURITY_INFORMATION`) so inheritance is re-enabled, and restore the original package-ALLOW state that Task 1 stripped where the enclosing grant is still active (so a still-granted parent tree is not left silently denied after the deny lease ends). A directory target is un-protected per-object, matching the per-object protection in Task 1. Keep the existing exact-SID ALLOW cleanup for the Allow path intact. The net invariant: after cleanup no denied target carries `PROTECTED_DACL_SECURITY_INFORMATION` and no residual grant or protection remains from this lease. Per 20-VERIFICATION.md CLASS B and the Phase-20 no-residual invariant.</action>
  <verify>
    <automated>cargo fmt --all --check</automated>
    <automated>grep -nE "UNPROTECTED_DACL_SECURITY_INFORMATION|fn cleanup_locked|fn remove_and_verify_exact_sid" crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs | head</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-65-aggregate "$(pwd -P)" nextest run --workspace --profile ci --no-fail-fast</automated>
  </verify>
  <done>Revoke of a deny clears `PROTECTED_DACL_SECURITY_INFORMATION`, restores inheritance, and restores the original package-ALLOW state for a still-granted parent; no deny-only SID is re-enabled; Hetzner workspace aggregate green (no Linux regression). No-residual-protection proven at 20-67.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sandboxed child → denied subpath | The AppContainer child must be genuinely unable to read an `fs_read_deny` subpath under a granted tree; the prior DENY ace was inert against the package SID |
| deny enforcement ← package-ALLOW removal + PROTECTED | Denial must come from removing the package grant and protecting the DACL, the only mechanism AppContainer honors — never from a package DENY ace or a re-enabled deny-only SID |
| host filesystem ← revoke completeness | After the run the target must carry no residual protection or grant; a left-behind PROTECTED flag or stripped-ALLOW is a permanent host alteration |
| native advance ← 20-66 cross-audit | This security-sensitive delta advances toward hardware only after a zero-finding four-way audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-65-01 | Information Disclosure | inert deny discloses a denied subpath to the sandbox | high | mitigate | Deny reimplemented as `PROTECTED_DACL_SECURITY_INFORMATION` + package-SID-ALLOW removal (the mechanism AppContainer honors); hardware-proven exit 1 "Access is denied."; re-prosecuted by the 20-66 four-way panel and 20-67 hardware run. |
| T-20-65-02 | Denial of Service | reintroducing the deny-only-SID bug makes the sandbox read no file | high | mitigate | Denial comes ONLY from package-ALLOW removal + PROTECTED; no deny-only SidsToDisable is re-enabled; the still-passing `normal_sid_only_grant_is_denied` and the granted-read tests are re-run at 20-67. |
| T-20-65-03 | Tampering | revoke leaves residual PROTECTED / stripped-ALLOW on the host | high | mitigate | Symmetric revoke clears PROTECTED via `UNPROTECTED_DACL_SECURITY_INFORMATION`, restores inheritance and the original package-ALLOW for a still-granted parent; the no-residual invariant is verified on hardware at 20-67. |
| T-20-65-04 | Elevation of Privilege | inherited package ALLOW silently re-grants after PROTECT | medium | mitigate | The DACL rebuild captures and omits inherited package ALLOW entries explicitly before setting PROTECTED, so no inherited grant survives the protection flip. |
| T-20-65-SC | Tampering | unaudited security-sensitive production change lands | high | mitigate | The delta is gated behind the 20-66 four-way cross-audit (ALL-FOUR-MUST-PASS, fail-closed) before it advances to SEANDESKTOP; it is never auto-advanced. |
</threat_model>

<verification>
Local gates prove no Linux/macOS/workspace regression and clean form only: `cargo fmt --all --check`; `cargo check -p wcore-sandbox --features live-docker --tests` (Mac, blocks on false green); Hetzner `remote-cargo.sh` clippy (`-p wcore-sandbox --all-targets --all-features -- -D warnings`) and workspace aggregate (`nextest run --workspace --profile ci --no-fail-fast`) green. The Windows compile of the `#[cfg(windows)]` acl_lease.rs body and the on-hardware deny/allow/revoke + no-residual behavior are proven at 20-67 on SEANDESKTOP, and this security-sensitive delta advances there ONLY after the 20-66 four-way cross-audit returns a zero-finding ALL-FOUR PASS over the exact committed SHA. Record the deny reimplementation, the imported PROTECTED/UNPROTECTED constants, the symmetric revoke, the Hetzner evidence, and the committed SHA in 20-65-SUMMARY.md. REQ-native-r2/r4 are not marked complete here (completion is claimed only after 20-67 hardware green through the fresh review).
</verification>

<success_criteria>
- `IntentKind::Deny` is enforced by `PROTECTED_DACL_SECURITY_INFORMATION` + package-SID-ALLOW removal (a real control AppContainer honors), never by an inert package DENY ace or a re-enabled deny-only SID (REQ-native-r2).
- Revoke is symmetric: clears PROTECTED, restores inheritance and the original package-ALLOW for a still-granted parent, leaves no residual protection or grant (REQ-native-r2/r4).
- fmt clean; Mac compile gate green; Hetzner clippy + workspace aggregate green (no Linux/macOS/workspace regression); committed as the exact delta for the 20-66 four-way cross-audit, then 20-67 hardware proof (REQ-native-r4).
</success_criteria>

## Artifacts this phase produces
- Protected-DACL deny + symmetric revoke in `crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs`, committed as the exact delta the 20-66 panel audits.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-65-SUMMARY.md` using the standard GSD summary template. Record the deny reimplementation (PROTECTED + package-ALLOW removal, inherited entries captured), the imported PROTECTED/UNPROTECTED constants, the symmetric revoke, the directory-per-object handling, the Mac compile gate + Hetzner clippy/aggregate evidence, and the committed SHA to be audited at 20-66. Complete no Phase 20 requirement (completion claimed only after 20-67 hardware green).
</output>
</content>

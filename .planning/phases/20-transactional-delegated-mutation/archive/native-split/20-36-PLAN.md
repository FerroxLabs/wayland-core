---
phase: 20-transactional-delegated-mutation
plan: "36"
type: execute
wave: 35
depends_on:
  - "20-32"
files_modified:
  - crates/wcore-sandbox/tests/live_fs_acl.rs
  - crates/wcore-sandbox/tests/hard_process_containment_windows.rs
  - scripts/f20-native-macos-proof.sh
autonomous: true
requirements:
  - REQ-native-r5
  - REQ-native-r7
  - REQ-native-r9
  - REQ-native-r4
must_haves:
  truths:
    - "Before any edit the three touched files' pre-edit blobs equal the sealed candidate 17412cf2 tree blobs (no source drift), so the new candidate is a clean successor of 17412cf2 that adds ONLY the Windows exit-code-fidelity fix and the macOS docker-image provisioning — the two exact NEW 20-32 RED causes and nothing else (REQ-native-r5/r9)."
    - "Every Windows AppContainer helper that must yield a granted-read exit 0 (type_and_hold, echo_temp_and_hold) exits 0 on the SUCCESS path via a primitive that deterministically SETS the process exit code to 0 (not `ver`, which prints the version and does NOT reset ERRORLEVEL — so the residual `choice.exe` selection index survived as the cmd /c exit code on real hardware), while the `type \"<file>\" && (...)` short-circuit still surfaces a DENIED read's nonzero exit and the stdin-free `choice` hold still keeps the process alive so the grant ACE is observable present-during; `live_fs_acl.rs:382` (one_execution_grant_never_leaks_to_another_identity) and granted_path_is_readable_then_revoked can pass on real Windows (REQ-native-r5)."
    - "The SAME `ver`-does-not-reset-ERRORLEVEL exit-code defect is fixed at EVERY sibling site so the proof cannot peel to the next onion layer: the Job-Object containment scripts (job_close_reaps_detached_descendant_with_no_residue, active_process_cap_is_enforced, breakaway_is_denied, and the preflight's second detached-descendant exec in qualified_hard_containment_backend_preflight) exit the parent 0 deterministically without changing the parent-exit → KILL_ON_JOB_CLOSE containment trigger; contained_detached_child_exit already uses `& exit {code}` (deterministic) and is audit-confirmed unchanged; the 'any' targets (windows-public-dispatch, windows-f20-lifecycle) are Linux-covered and audited for Windows-only exit/rename semantics (REQ-native-r7)."
    - "f20-native-macos-proof.sh ensures its own docker test images are present: after the `docker info` liveness gate and BEFORE the first docker target runs, it pulls the exact image(s) docker_smoke.rs uses (alpine:3.19) and fails CLOSED if a pull fails, so a fresh ephemeral macOS runner can never RED on `404: No such image` at macos-docker-roundtrip-delete and all 8 macOS targets run genuinely rather than one silently skipping (REQ-native-r9)."
    - "Linux and macOS are unregressed: the edits touch only `#![cfg(windows)]` test helpers (compiled to nothing off Windows) and a bash proof script, so no Linux-visible compiled output changes, Hetzner clippy `-D warnings` for wcore-sandbox stays clean, the aggregate stays 11509/0/48, and the sanctioned macOS `cargo check -p wcore-sandbox --features live-docker --tests` stays green (REQ-native-r4)."
  artifacts:
    - path: crates/wcore-sandbox/tests/live_fs_acl.rs
      provides: "type_and_hold / echo_temp_and_hold exit-0 normalization via a deterministic ERRORLEVEL-setting primitive (not `ver`), preserving the denied-read short-circuit and the stdin-free choice hold"
    - path: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      provides: "Job-Object containment scripts exit the parent 0 deterministically at every `ver`-tail site, preserving the parent-exit job-close containment trigger; contained_detached_child_exit audit-confirmed unchanged"
    - path: scripts/f20-native-macos-proof.sh
      provides: "Pre-pull of the docker_smoke.rs test image(s) (alpine:3.19) after the docker-info gate and before the docker targets, fail-closed on pull error, making the docker targets reproducible on a fresh ephemeral runner"
  key_links:
    - from: crates/wcore-sandbox/tests/live_fs_acl.rs
      to: scripts/f20-native-windows-proof.ps1
      via: "the windows-retained-handle / windows-appcontainer-acl targets select the exit-0-asserting live_fs_acl tests whose helper now exits 0 deterministically"
      pattern: "windows-retained-handle"
    - from: scripts/f20-native-macos-proof.sh
      to: crates/wcore-sandbox/tests/docker_smoke.rs
      via: "the harness pulls exactly the alpine:3.19 image docker_smoke.rs hardcodes before the macos-docker-* targets run it"
      pattern: "alpine:3.19"
---

<objective>
Repair the two exact NEW 20-32 RED causes on the sealed candidate `17412cf2` and nothing else: (1) the Windows granted-read exit-code defect — `type_and_hold`/`echo_temp_and_hold` (and the identical pattern in the Job-Object containment scripts) append `& ver >nul` to force exit 0, but `ver` does not reset ERRORLEVEL, so on real hardware the residual `choice.exe` selection index survived as the `cmd /c` exit code and `live_fs_acl.rs:382` failed; and (2) the macOS docker-image gap — `f20-native-macos-proof.sh` never pulls the `alpine:3.19` image `docker_smoke.rs` uses, so a fresh ephemeral runner RED'd on `404: No such image` at `macos-docker-roundtrip-delete`.

Purpose: The 20-32 native-proof dispatch of `17412cf2` came back RED on BOTH platforms on NEW causes (the two 20-25/20-29 fixes HELD — macOS compiled with no E0599 and `macos-retained-directory` passed; Windows setup cleared `safe.directory` and reached the proof). Windows is a genuine candidate defect (`live_fs_acl` exit-0 handling does not hold on hardware); macOS is a runner-environment gap. This plan writes the real fixes so a NEW candidate can be re-sealed (20-37), re-cross-audited (20-38), and re-dispatched (20-39) with a fresh Sean authorization. The Windows fix is genuine batch/exit-code semantics under AppContainer child execution and must be root-caused, not guessed; and it must be applied at EVERY sibling site so the proof does not RED again one target later.
Output: One clean successor delta touching exactly the two Windows test-helper files plus the macOS proof script; fmt-clean, scope-clean, the sanctioned macOS `cargo check` green, and Hetzner clippy `-D warnings` clean; NO native, aggregate, or requirement claim. The Windows helpers are `#![cfg(windows)]` so their compile + exit-code behavior are proven ONLY on the self-hosted msvc runner at the 20-39 re-dispatch.
</objective>

<redispatch_decision>
Re-dispatch structure — AUTHOR NEW PLANS 20-36…20-42; do NOT re-bind the retained-RED 20-32 or the superseded 20-33/20-34/20-35.

Choice: the second repaired successor is the fully additive serial chain 20-36 (fix) -> 20-37 (re-seal) -> 20-38 (pre-native cross-audit) -> 20-39 (native re-dispatch) -> 20-40 (fresh review, native NOT deferred) -> 20-41 (re-prep tuple) -> 20-42 (terminal). This mirrors 20-29 -> 20-30 -> 20-31 -> 20-32 -> 20-33 -> 20-34 -> 20-35 (the fix is collapsed to one plan because the repair is two small targeted fixes, not a multi-plan construction).

Rationale (why new, not re-bind):
- 20-32 is already EXECUTED with a retained RED disposition and a retained UAT evidence ref (`refs/f20-native-uat/17412cf2…`, deletion not authorized) plus its dispatch branch. Re-binding/re-executing 20-32 would overwrite that permanent RED record and its retained ref — forbidden by CONTEXT D1 (additive only; never re-execute) and by the Sean-only retained-ref-deletion gate. A fresh native digest also requires FRESH Sean authorization (D6: the prior 17412cf2 native-proof-dispatch authorization is spent/void), so the dispatch must be a new checkpoint bound to the NEW sealed SHA — cleaner as a new plan than mutating 20-32's spent checkpoint.
- 20-33/20-34/20-35 are authored but UNEXECUTED and hardcoded (`depends_on`, `@…20-32-SUMMARY.md` context refs, key_links) to 20-32's now-RED native evidence; 20-33's admission already fails closed on RED/missing native evidence and routes to "a further repaired successor," so they are self-consistently superseded (their upstream 20-32 is terminal RED) WITHOUT any mutation. Editing them in place would mutate already-authored plans and risk stale/ambiguous candidate binding — the error-prone path.
- Net: 20-32 stays as retained RED evidence; 20-33/20-34/20-35 are superseded (dead — their upstream 20-32 is terminal RED) and are neither executed nor edited by this chain; the NEW sealed candidate flows through 20-37…20-42.
</redispatch_decision>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-NATIVE-REPAIR-PRD.md
@.planning/phases/20-transactional-delegated-mutation/20-32-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-20-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-30-SUMMARY.md
@crates/wcore-sandbox/tests/live_fs_acl.rs
@crates/wcore-sandbox/tests/hard_process_containment_windows.rs
@crates/wcore-sandbox/tests/docker_smoke.rs
@scripts/f20-native-windows-proof.ps1
@scripts/f20-native-macos-proof.sh
</context>

<execution_rules>
- Require a clean isolated standalone checkout (`.git` is a directory) on branch `plan/f20-unified-audit-repair`. Do NOT execute in the sibling `/Users/seandonahoe/dev/waylandcore` primary checkout.
- The new candidate is a SUCCESSOR of the sealed candidate `17412cf2f6a8be9d2ec7272f6693f998db4ba2e5` (tree `00e41519ac6782b05e610fcf7fafc772d5040a5d`). Only planning-doc commits sit between the seal and the current branch tip, so the three touched files are byte-identical to the sealed tree; confirm that (no source drift) before editing, then author the fixes on the branch tip.
- Capture the plan base before any edit: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-36)"`. Advance the base between tasks with `--start-fresh` so each task's disjoint per-task scope gate passes as written.
- NO Cargo/clippy/nextest on the Mac, with EXACTLY ONE sanctioned exception (CONTEXT D5 carve-out): the targeted macOS compile gate `cargo check -p wcore-sandbox --features live-docker --tests` in the verification section — a compile/type-check only (no test execution, no Docker runtime). It re-confirms the macOS-compiled surface (lib + docker_smoke + macOS-gated tests) still compiles; it does NOT compile the `#![cfg(windows)]` helpers this plan edits. Everything else is `vx rustfmt` (fmt) + `bash -n` (shell syntax) + the committed-HEAD Hetzner harness (`/Users/seandonahoe/.ratchet/harness/remote-cargo.sh`) + node tooling.
- The Windows fix cannot be compile- or behavior-proven off Windows: `live_fs_acl.rs` and `hard_process_containment_windows.rs` are `#![cfg(windows)]`, so neither this Mac nor Hetzner Linux compiles a single line of them. Their compile AND real-hardware exit-code behavior are proven ONLY on the self-hosted msvc AppContainer runner at the 20-39 re-dispatch. The plan is explicit about this; do NOT claim a Windows fix "verified" from any pre-dispatch gate.
- Touch ONLY the three declared files. No drive-by refactor of the AppContainer implementation, the ACL test bodies' assertions, the proof harness marker grammar, `docker_smoke.rs`, or any other module. Preserve every existing assertion, the stdin-free `choice` hold, the `type && (...)` denied-read short-circuit, and the parent-exit job-close containment trigger — change ONLY the exit-code-normalization tail and add ONLY the docker pre-pull.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Windows granted-read exit-0 fidelity across live_fs_acl + hard_process_containment_windows (full sibling/target audit)</name>
  <files>crates/wcore-sandbox/tests/live_fs_acl.rs, crates/wcore-sandbox/tests/hard_process_containment_windows.rs</files>
  <read_first>crates/wcore-sandbox/tests/live_fs_acl.rs, crates/wcore-sandbox/tests/hard_process_containment_windows.rs, scripts/f20-native-windows-proof.ps1, .planning/phases/20-transactional-delegated-mutation/20-32-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-20-SUMMARY.md</read_first>
  <behavior>
    - Root cause (confirm against source and the 20-32 RED, do not restate blindly): the helper scripts end the success path with `& ver >nul` to force the script's exit code to 0. `choice.exe /T N /D Y` sets ERRORLEVEL to its 1-based selection index (default `Y` => 1, never 0). `ver` prints the OS version and does NOT modify ERRORLEVEL; `cmd /d /s /c` returns ERRORLEVEL as it stands at exit, so the residual `choice` index survived as the process exit code on real Windows hardware — the 20-20 assumption that `ver` "always exits 0" is false for exit-code purposes. On Linux this was never observed because the files are `#![cfg(windows)]` (zero compiled tests).
    - The fix normalizes the SUCCESS path to a deterministic exit 0 using a primitive that actually SETS the process exit code to 0 — recommended `exit /b 0` (sets ERRORLEVEL 0 and returns; when not inside a batch file it exits the cmd process with 0), equivalently `cmd /c exit 0` or `(call )` followed by exit — chosen by the executor's real-hardware reasoning, NOT by copying `ver`. It MUST preserve: (a) the `type \"<file>\" && ( … )` short-circuit so a DENIED `type` still surfaces its nonzero exit (an exit-0 assertion must keep genuinely gating on the granted read); (b) the stdin-free `choice` hold that keeps the process alive across the delay so the grant ACE is observable present-during (do NOT drop or replace the `choice` delay — `timeout.exe` fails under the sandbox's null stdin, per 20-20); (c) `format!` placeholder/argument counts (e.g. `type_and_hold`'s single positional `{}` for `file.display()` and the inline `{seconds}`).
    - `type_and_hold` and `echo_temp_and_hold` in `live_fs_acl.rs` are both fixed (they carry the identical tail). This one edit fixes BOTH exit-0-asserting proof targets that select `live_fs_acl` tests: `windows-retained-handle` (`one_execution_grant_never_leaks_to_another_identity`, the observed RED at :382) and `windows-appcontainer-acl` (`granted_path_is_readable_then_revoked`, which would have RED'd next for the identical reason since the proof stops at the first failing target).
    - The SAME defect is present in `hard_process_containment_windows.rs` and MUST be fixed there too, or the proof simply peels to the next onion layer: `job_close_reaps_detached_descendant_with_no_residue`, `active_process_cap_is_enforced`, and `breakaway_is_denied` each end their parent script with `& {choice parent-hold} & ver >nul` and assert `out.exit_code == 0`; the second detached-descendant exec inside `qualified_hard_containment_backend_preflight` does the same. Replace each `ver`-tail with the SAME deterministic exit-0 primitive, changing ONLY the exit code — the parent must still exit (its exit is the KILL_ON_JOB_CLOSE trigger that reaps the detached descendants), the `start "" /b` detached idlers and the `choice` holds are unchanged, and the wall-clock/no-residue assertions are untouched.
    - Audit-confirm the two structurally-safe sites and record the reasoning: `contained_detached_child_exit` ends with `& exit {code}` (an explicit deterministic exit code, values 0 and 7) — already correct, DO NOT change it; the FIRST exec of `qualified_hard_containment_backend_preflight` runs a bare `cmd /c "ver >nul"` with no preceding `choice`, so ERRORLEVEL starts 0 at a fresh cmd and `ver` leaves it 0 — audit whether to normalize it too for consistency, but it is not a proven defect.
    - Audit the two 'any' Windows targets and record the finding: `windows-public-dispatch` (`dispatch_smoke`) and `windows-f20-lifecycle` (`transactional_delegated_mutation_test`) are cross-platform and Linux-covered by the aggregate; reason through the 20-20 `dispatch_smoke` parent-container rename for any Windows-only exit/rename semantics that are asserted-not-observed. If a genuine Windows-only defect is found, fix it in scope and record it; if none, make NO change and record the audit conclusion.
  </behavior>
  <action>In `crates/wcore-sandbox/tests/live_fs_acl.rs`, replace the `& ver >nul` exit-normalization tail in `type_and_hold` and `echo_temp_and_hold` with a primitive that deterministically sets the process exit code to 0 on the success path (recommended `exit /b 0`; the executor confirms the mechanism from cmd.exe ERRORLEVEL semantics, not from `ver`), keeping the `type && ( … )` denied-read short-circuit, the stdin-free `choice` hold, and the exact `format!` arguments. In `crates/wcore-sandbox/tests/hard_process_containment_windows.rs`, apply the SAME deterministic exit-0 primitive at every `& ver >nul` parent-exit site (`job_close_reaps_detached_descendant_with_no_residue`, `active_process_cap_is_enforced`, `breakaway_is_denied`, and the second exec of `qualified_hard_containment_backend_preflight`) so the parent exits 0 without changing the parent-exit → job-close containment trigger or any `start "" /b` / `choice` hold. Leave `contained_detached_child_exit` (already `& exit {code}`) and the bare-`ver` preflight probe untouched unless the executor's audit finds a concrete defect. Audit the two 'any' targets (`dispatch_smoke`, `transactional_delegated_mutation_test`) for Windows-only exit/rename semantics and fix any genuine defect in scope, else record no change. Do NOT weaken or remove any existing assertion, the `choice` hold, or the denied-read gate; do NOT touch production source, `docker_smoke.rs`, or the harness scripts. Compile and real-hardware exit-code behavior are proven ONLY on the msvc runner at 20-39; this task's Mac gates are scope + fmt only.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-36)" crates/wcore-sandbox/tests/live_fs_acl.rs crates/wcore-sandbox/tests/hard_process_containment_windows.rs</automated>
    <automated>vx rustfmt --edition 2024 --check crates/wcore-sandbox/tests/live_fs_acl.rs crates/wcore-sandbox/tests/hard_process_containment_windows.rs</automated>
    <automated>grep -nE "type_and_hold|echo_temp_and_hold|choice.exe|start .. /b|ver >nul|exit /b 0|exit 0" crates/wcore-sandbox/tests/live_fs_acl.rs crates/wcore-sandbox/tests/hard_process_containment_windows.rs</automated>
  </verify>
  <acceptance_criteria>Diff-scope is exactly the two declared Windows test files and formatting passes. Every exit-0-asserting granted-read/parent-exit script normalizes to a deterministic exit-0 primitive (not `ver`), the `type && (...)` denied-read short-circuit and the stdin-free `choice` holds are intact, `contained_detached_child_exit`'s `& exit {code}` is unchanged, and the sibling/target audit conclusions are recorded. Windows compile + real-hardware exit-code behavior are proven only at 20-39.</acceptance_criteria>
  <done>Both Windows test helpers and every Job-Object parent-exit site exit 0 deterministically as a provisional task commit; the observed RED cause and its onion-peel siblings are structurally closed; hardware proof is deferred to 20-39.</done>
</task>

<task type="auto">
  <name>Task 2: Make the macOS proof harness pull its own docker test image before the docker targets</name>
  <files>scripts/f20-native-macos-proof.sh</files>
  <read_first>scripts/f20-native-macos-proof.sh, crates/wcore-sandbox/tests/docker_smoke.rs, .planning/phases/20-transactional-delegated-mutation/20-32-SUMMARY.md</read_first>
  <behavior>
    - `docker_smoke.rs` hardcodes `image: "alpine:3.19"` in its three daemon-touching tests; `docker_runs_hello_world` (`.await.unwrap()`) HARD-fails on a missing image, which RED'd `macos-docker-roundtrip-delete` on the fresh ephemeral runner at 20-32; `docker_returns_enforced_resource_limits` would otherwise SILENTLY skip on the same 404 (soft-pass). Pulling the image up front makes ALL docker targets run genuinely and honestly.
    - `f20-native-macos-proof.sh` already gates on a live daemon (`docker info`) but never pulls the image. After that liveness gate and BEFORE the first `run_target` (the first docker target is `macos-docker-reject-path-replacement`, and the hard-failing one is `macos-docker-roundtrip-delete`), pull the exact image(s) `docker_smoke.rs` uses and FAIL CLOSED (nonzero exit, clear message) if a pull fails — a scarce native run must not proceed past a provisioning failure into a mid-proof 404.
    - The image is `alpine:3.19` (the single image `docker_smoke.rs` references). Encode it as the source of truth with a comment pointer to `crates/wcore-sandbox/tests/docker_smoke.rs`, mirroring the existing `SANDBOX_ACTIVE_PROCESS_LIMIT` duplicate-with-pointer convention — do NOT fragile-parse Rust. The pull runs under `set -euo pipefail`; a failed `docker pull` aborts the script before any docker target.
    - Everything else in the harness is unchanged: the repo-root/clean-checkout/exact-commit/exact-tree gates, the `WAYLAND_F20_NATIVE_ACCEPTANCE`/Darwin/`docker info` gates, the wrong-OS anti-drift guard, the eight `run_target` invocations and their selectors, and the ordered per-target + single final acceptance marker grammar consumed by `f20-native-uat-proof.mjs`. The 8 targets, their order, and their markers do not change — the docker targets simply now find their image.
  </behavior>
  <action>In `scripts/f20-native-macos-proof.sh`, add a docker-image pre-pull step after the `docker info` liveness gate and before the first `run_target`: pull `alpine:3.19` (the image `crates/wcore-sandbox/tests/docker_smoke.rs` hardcodes; reference it as a source-of-truth constant/list with a pointer comment, not by parsing Rust) and fail closed with a clear message if the pull fails, so a fresh ephemeral runner reliably has the image before `macos-docker-roundtrip-delete`. Change nothing else — not the exact-checkout gates, the env/Darwin/daemon gates, the anti-drift guard, the eight target selectors/order, or the marker grammar. This is a candidate change (the exact committed script runs on the ephemeral runner), so it is re-sealed at 20-37. macOS runtime proof that all 8 targets now run green is the 20-39 re-dispatch.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-36)" scripts/f20-native-macos-proof.sh</automated>
    <automated>bash -n scripts/f20-native-macos-proof.sh</automated>
    <automated>grep -nE "docker info|docker pull|alpine:3.19|run_target \"macos-docker" scripts/f20-native-macos-proof.sh</automated>
  </verify>
  <acceptance_criteria>Diff-scope is exactly the macOS proof script; `bash -n` parses; a fail-closed `docker pull alpine:3.19` step appears after the `docker info` gate and before the first `run_target`; the eight target selectors/order and the marker grammar are unchanged. macOS runtime green for all 8 targets is proven at 20-39.</acceptance_criteria>
  <done>The macOS proof harness ensures its own docker image as a provisional task commit; the 20-32 macOS env gap is closed for reproducibility on a fresh ephemeral runner; hardware proof is deferred to 20-39.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| granted-read PASS ← script exit code | The exit-0 assertion must genuinely reflect the granted READ succeeding; a normalizer that unconditionally forces 0 could mask a denied read and give a false PASS |
| job-close reap ← parent exit | The Job-Object containment proof triggers on the parent's exit; changing the exit-code tail must not change WHETHER/WHEN the parent exits |
| native proof ← runner image provisioning | The docker targets must find their image on a fresh ephemeral runner; a mid-proof 404 or a silent skip corrupts the native evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-36-01 | Spoofing | granted-read exit-0 normalizer | high | mitigate | The exit-0 primitive is placed ONLY on the success branch behind the `type \"<file>\" && ( … )` short-circuit, so a DENIED read still surfaces its nonzero exit; the exit-0 assertion keeps gating on the granted read plus the MARKER-in-stdout assertion. Hardware falsifies a masking normalizer at 20-39. |
| T-20-36-02 | Tampering | Job-Object containment trigger | high | mitigate | Only the exit-code tail changes; the parent still exits (KILL_ON_JOB_CLOSE trigger unchanged), the `start "" /b` detached idlers, `choice` holds, and the wall-clock/no-residue assertions are untouched, so the containment property is preserved not merely satisfied. |
| T-20-36-03 | Denial of Service | mid-proof docker 404 / silent skip | medium | mitigate | The harness pulls `alpine:3.19` and fails closed under `set -euo pipefail` BEFORE any docker target, so a provisioning failure aborts cleanly rather than burning a scarce run on a 404 or passing via a silent skip. |
| T-20-36-04 | Tampering | undetectable Windows compile break | medium | accept | The `#![cfg(windows)]` helpers cannot be compiled off Windows; risk is bounded to `format!` string-content edits (placeholder/argument counts preserved, fmt-checked) and is proven at the 20-39 msvc re-dispatch. Accepted rationale: no pre-dispatch environment compiles these files; the compile+behavior gate is the re-dispatch itself (CONTEXT D5/D7). |
</threat_model>

<verification>
Each task first passes its Mac construction gate (scope + `vx rustfmt --edition 2024 --check`, plus `bash -n` for Task 2) as a provisional GSD task commit. After both task commits exist and the checkout is clean, run the sanctioned pre-dispatch macOS compile re-confirmation and the committed-HEAD Hetzner no-regression gates (a construction commit is not behavioral evidence):
<automated>cargo check -p wcore-sandbox --features live-docker --tests</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-36-clippy "$(pwd -P)" clippy -p wcore-sandbox --all-targets --all-features -- -D warnings</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-36-nextest "$(pwd -P)" nextest run -p wcore-sandbox</automated>
The macOS `cargo check` re-confirms the macOS-compiled surface (lib + docker_smoke + macOS-gated tests) still compiles — it does NOT compile the edited `#![cfg(windows)]` helpers. The Hetzner gates confirm NO Linux regression (the edited files are `#![cfg(windows)]`/bash, so the Linux-compiled output is unchanged; wcore-sandbox stays clippy-clean and nextest-green). Record the exact committed `source_sha`, source tree, the macOS compile receipt, the Hetzner clippy + nextest receipts, changed paths, the full Windows sibling/target audit conclusions (which sites fixed, which audit-confirmed unchanged and why), and the explicit deferral of ALL Windows compile/exit-code behavior and macOS docker-target green to the native-proof gate (20-39) in `20-36-SUMMARY.md`. No aggregate, requirement, or phase claim is made.
</verification>

<success_criteria>
- `type_and_hold`/`echo_temp_and_hold` and every Job-Object parent-exit site normalize the success path to a deterministic exit-0 primitive (not `ver`), preserving the denied-read short-circuit, the stdin-free `choice` holds, and the parent-exit job-close trigger (r5/r7).
- The full Windows sibling/target audit is recorded: the two live_fs_acl targets and the two Job-Object targets are fixed by the same class of edit; `contained_detached_child_exit` (`& exit {code}`) is audit-confirmed unchanged; the two 'any' targets are audited for Windows-only semantics (r7).
- `f20-native-macos-proof.sh` pulls `alpine:3.19` fail-closed after the `docker info` gate and before the docker targets, so a fresh ephemeral runner runs all 8 targets genuinely (r9).
- The sanctioned macOS `cargo check … --tests` is green and Hetzner clippy `-D warnings` + `nextest -p wcore-sandbox` show no Linux regression (r4); the summary records the exact new candidate `source_sha`/tree and defers every hardware runtime claim to 20-39.
</success_criteria>

## Artifacts this phase produces
- The Windows granted-read/parent-exit exit-0 fidelity fix (two `#![cfg(windows)]` test files) plus the macOS docker-image pre-pull (proof script) as one reviewable successor delta over the sealed candidate `17412cf2`.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-36-SUMMARY.md` using the standard GSD summary template. Record the exact new candidate `source_sha`/tree, the macOS compile-check receipt, the Hetzner clippy + nextest receipts, changed paths, the full Windows sibling/target audit ledger, the re-dispatch structure decision (new 20-37…20-42, not re-bind), and the explicit deferral of all Windows compile/exit-code behavior and macOS docker-target green to 20-39. Complete no Phase 20 requirement.
</output>

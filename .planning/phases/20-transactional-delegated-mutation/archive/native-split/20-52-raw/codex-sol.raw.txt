Reading additional input from stdin...
OpenAI Codex v0.144.4
--------
workdir: /Users/seandonahoe/dev/waylandcore-ferrox
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: high
reasoning summaries: none
session id: 019f9310-7773-7e33-adc2-3c84aedff51a
--------
user
F20 PRE-NATIVE FOUR-WAY CROSS-AUDIT (plan 20-52) — RE-SEALED FURTHER-REPAIRED SUCCESSOR

You are an INDEPENDENT (non-author) adversarial code auditor. Prosecute the candidate at EVERY
severity. Default to REFUTE: if you are uncertain a fix is correct, treat it as a FINDING, not a pass.
Your job is to find any reason this candidate should NOT be admitted to the scarce native-hardware run.

== CANDIDATE UNDER AUDIT (exact, re-sealed) ==
- source_sha (SEALED):  f0dd5b6d312af616f268f96f34c3bc9fc962c4d2
- source_tree (SEALED): ac76c87b318ee4ba8c34927dea23e40e63fd0776
- predecessor (sealed-but-RED, BLOCKED by the 20-45 panel): 3f839309574d6741eed416cd3820f56447f74eba
- repo: wayland-core (multi-provider AI agent CLI, Rust workspace). Branch plan/f20-unified-audit-repair.
- The ONLY deferred checks are native_macos / native_windows (proven at 20-53). EVERYTHING else —
  static code review of the delta, all-severity findings, evidence integrity, integration authority —
  is prosecuted NOW.

== WHAT THIS DELTA IS ==
The 20-45 four-way pre-native panel returned BLOCK on predecessor 3f839309 with TWO real,
statically-provable findings. The 20-50 fix (this delta, 3f839309 -> f0dd5b6d) closes both, touching
ONLY the two Windows-only files the findings name — NO Cargo.toml, NO other source, NO workflow.

--- FINDING 1 (was Codex HIGH / Claude MEDIUM): vacuous post-close reap assertion ---
File: crates/wcore-sandbox/tests/hard_process_containment_windows.rs, test active_process_cap_is_enforced.
The OLD post-close check was `wait_until(|| tagged_choice_descendant_count(&tag) == 0, ...)`. That helper
scoped choice.exe by whose ParentProcessId is a LIVE tagged cmd.exe. After run.await returns, the
AppContainer Job Object has closed and the tagged parent cmd is dead, so the parent-scoped CIM query is
structurally empty and the count is 0 whether or not a choice.exe leaked — VACUOUS.

The 20-50 fix ("threads the needle"):
  * tagged_choice_descendant_count(-> usize) becomes tagged_choice_descendant_pids(-> Vec<u32>): the SAME
    ParentProcessId-scoped CIM query, but returns the choice.exe children's ProcessId LIST.
  * NEW surviving_captured_choice_pids(pids: &[u32]) -> usize: counts choice.exe whose ProcessId is in the
    captured set ($pids -contains $_.ProcessId); empty slice short-circuits to 0.
  * The peak-sampling loop now captures the peak PID set (captured_pids) alongside peak.
  * NEW belt-and-braces assert!(!captured_pids.is_empty(), ...) guards against silent re-vacuity.
  * The post-close reap check is now surviving_captured_choice_pids(&captured_pids) == 0 — fixed PID.
Claim: non-vacuous (a leaked/orphaned captured choice, same PID, still choice.exe, is counted) AND NOT
host-wide-flaky (a concurrent live_fs_acl choice.exe carries a different, non-captured PID -> excluded);
does NOT revert to the host-wide image_count("choice.exe") baseline that the 20-43 Task-3 flake fix removed.
The other containment assertions (peak>0, peak<=SANDBOX_ACTIVE_PROCESS_LIMIT=512, peak<attempts,
exit_code==0) are preserved VERBATIM, as is the sibling test
job_close_reaps_detached_descendant_with_no_residue (whose grandchild carries `rem {tag}` on its OWN
cmdline so tagged_cmd_count survives parent death).

--- FINDING 2 (was Codex MEDIUM / Claude LOW): loose cmd classifier ---
File: crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs, fn resolved_program_is_cmd.
OLD: `.to_ascii_lowercase().ends_with("cmd.exe")` — suffix-matches notcmd.exe/foocmd.exe, which would then
route their /c|/k payload through the cmd-specific quote_cmd_payload instead of the general quote_arg.
NEW: decode + lowercase, then `std::path::Path::new(&decoded).file_name().and_then(to_str)
.is_some_and(|name| name == "cmd.exe")` — exact FINAL PATH COMPONENT match (Windows Path treats both
`\` and `/` as separators). Claim: quote_cmd_payload / quote_arg / classify_bare_shell / resolve_program /
is_unc_or_device_path are byte-identical; ONLY the cmd/non-cmd classification is tightened.

== YOUR ADVERSARIAL MANDATE (prosecute all, refute-by-default) ==
1. Finding 1 non-vacuous WITHOUT the host-wide flake: Does the post-close check catch a leaked/orphaned
   captured choice survivor by fixed PID? Does it avoid the host-wide-image-count flake (match ONLY the
   test's own captured PIDs)? Is !captured_pids.is_empty() a real guard against re-vacuity? Are peak>0,
   peak<=512, peak<attempts, exit_code==0 and the sibling reap test ALL preserved verbatim? Any NEW defect
   introduced by the two-phase rewrite (PID-reuse false-fail, malformed PowerShell filter, race)?
2. Finding 2 exact-final-component: Does file_name()==cmd.exe correctly reject notcmd.exe/foocmd.exe and
   accept System32\cmd.exe? Is the #B quoting behavior (quote_cmd_payload/quote_arg) otherwise identical?
   Any regression where a real cmd.exe is now MISCLASSIFIED as non-cmd (would break isolation quoting)?
3. Isolation guardrails still hold: #A DispatchAdmission budget preserved (no catch-to-unlimited — but note
   this delta does NOT touch worktree_manager.rs at all); #B quoting-layer ONLY — is_unc_or_device_path,
   Low-IL restricted token, Job-Object limits, ACL lease untouched; argv discipline kept; 20-36 `exit /b 0`
   normalization intact.
4. Any NEW defect the fix introduces at any severity.

== OUTPUT CONTRACT (MANDATORY — emit EXACTLY this JSON, nothing else) ==
Emit a single JSON object and NOTHING else (no prose, no markdown fences):
{
  "schema": "f20-native-crossaudit.codex-sol",
  "source_sha": "f0dd5b6d312af616f268f96f34c3bc9fc962c4d2",
  "source_tree": "ac76c87b318ee4ba8c34927dea23e40e63fd0776",
  "reviewer_id": "codex-5.6-sol",
  "reviewer_kind": "external-cli",
  "checks": { "all_severity": "PASS|FAIL", "evidence_integrity": "PASS|FAIL", "integration_authority": "PASS|FAIL" },
  "deferred": ["native_macos", "native_windows"],
  "findings": { "blocker": 0, "critical": 0, "high": 0, "medium": 0, "low": 0 },
  "finding_1_reap_nonvacuous": "PASS|FAIL",
  "finding_2_cmd_exact_component": "PASS|FAIL",
  "guardrail_A_admission_budget": "PASS|FAIL",
  "guardrail_B_quoting_layer_only": "PASS|FAIL",
  "evidence": [ { "claim": "<what you checked>", "severity": "high|medium|low|none", "result": "PASS|FAIL" } ],
  "disposition": "PASS|BLOCK"
}
PASS disposition ONLY if you find ZERO findings at any non-deferred severity. Any real finding => BLOCK.

== THE EXACT REPAIR DELTA (git diff 3f839309..f0dd5b6d, the two source files) ==
diff --git a/crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs b/crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
index afb9e356..7c162fe0 100644
--- a/crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
+++ b/crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
@@ -135,7 +135,9 @@ pub(super) fn quote_cmd_payload(payload: &str) -> String {
 }
 
 /// True when a resolved `lpApplicationName` (NUL-terminated UTF-16, as
-/// returned by [`resolve_program`]) names `cmd.exe`. Used to decide whether the
+/// returned by [`resolve_program`]) has `cmd.exe` as its exact FINAL PATH
+/// COMPONENT (case-insensitive) — NOT a bare suffix, so a name like
+/// `notcmd.exe`/`foocmd.exe` does not match. Used to decide whether the
 /// `/c`/`/k` payload needs the cmd-aware quoting above rather than the general
 /// CRT [`quote_arg`] rules. Matching the RESOLVED program (not the raw
 /// `argv[0]`) is deliberate: bare `cmd`/`cmd.exe` is pinned to
@@ -146,9 +148,15 @@ pub(super) fn resolved_program_is_cmd(app_name_w: &[u16]) -> bool {
         .iter()
         .position(|&c| c == 0)
         .unwrap_or(app_name_w.len());
-    String::from_utf16_lossy(&app_name_w[..end])
-        .to_ascii_lowercase()
-        .ends_with("cmd.exe")
+    // Windows `Path` treats both `\` and `/` as separators, so `file_name`
+    // isolates the true final component of the `System32\cmd.exe` resolution or
+    // any absolute path the caller passed; equality (not a suffix) rejects
+    // `notcmd.exe`.
+    let decoded = String::from_utf16_lossy(&app_name_w[..end]).to_ascii_lowercase();
+    std::path::Path::new(&decoded)
+        .file_name()
+        .and_then(|name| name.to_str())
+        .is_some_and(|name| name == "cmd.exe")
 }
 
 /// Classification of a bare (non-absolute) `argv[0]`. Only `cmd` is
diff --git a/crates/wcore-sandbox/tests/hard_process_containment_windows.rs b/crates/wcore-sandbox/tests/hard_process_containment_windows.rs
index c8a59997..1455271d 100644
--- a/crates/wcore-sandbox/tests/hard_process_containment_windows.rs
+++ b/crates/wcore-sandbox/tests/hard_process_containment_windows.rs
@@ -125,19 +125,28 @@ fn tagged_cmd_count(tag: &str) -> usize {
         .unwrap_or(0)
 }
 
-/// Count the `choice.exe` idlers spawned by THIS test's fan-out, scoped to the
-/// test's own tagged process tree rather than host-wide by image name.
+/// Return the ProcessIds of the `choice.exe` idlers spawned by THIS test's
+/// fan-out, scoped to the test's own tagged process tree rather than host-wide
+/// by image name.
 ///
 /// A bare `choice.exe` idler cannot carry an injected tag on its own command
 /// line (choice rejects the extra argument), but the top-level sandbox `cmd`
 /// that runs the fan-out script DOES carry a unique `rem {tag}` — and every
 /// `start "" /b choice` idler is a direct child of that cmd, so its
-/// `ParentProcessId` is the tagged cmd's `ProcessId`. Counting only choice
+/// `ParentProcessId` is the tagged cmd's `ProcessId`. Selecting only choice
 /// processes whose parent is this test's tagged cmd means a concurrent
 /// `choice`-spawning target on the same runner (e.g. `live_fs_acl`) cannot
-/// pollute the count — closing the host-wide-image-count flake (watch-item d2)
-/// without weakening any containment assertion.
-fn tagged_choice_descendant_count(tag: &str) -> usize {
+/// pollute the capture.
+///
+/// This is the ALIVE-phase half of a two-phase reap check: the returned PIDs
+/// are captured WHILE the tagged parent cmd is still alive (during the
+/// peak-sampling window), and the `ParentProcessId` scope is what makes that
+/// capture immune to a concurrent `choice`-spawning target. Once the job closes
+/// the tagged parent is dead, so this parent-scoped query would go structurally
+/// empty regardless of a leaked survivor — the post-close survivor check is
+/// therefore done by fixed ProcessId via [`surviving_captured_choice_pids`],
+/// NOT by re-running this parent-scoped query.
+fn tagged_choice_descendant_pids(tag: &str) -> Vec<u32> {
     let out = Command::new("powershell")
         .args([
             "-NoProfile",
@@ -148,11 +157,52 @@ fn tagged_choice_descendant_count(tag: &str) -> usize {
                  Where-Object {{ $_.CommandLine -like '*{tag}*' }} | \
                  Select-Object -ExpandProperty ProcessId); \
                  @(Get-CimInstance Win32_Process -Filter \"Name='choice.exe'\" | \
-                 Where-Object {{ $parents -contains $_.ParentProcessId }}).Count"
+                 Where-Object {{ $parents -contains $_.ParentProcessId }} | \
+                 Select-Object -ExpandProperty ProcessId)"
+            ),
+        ])
+        .output()
+        .expect("query this test's tagged choice descendant PIDs via CIM");
+    String::from_utf8_lossy(&out.stdout)
+        .split_whitespace()
+        .filter_map(|s| s.parse::<u32>().ok())
+        .collect()
+}
+
+/// Count how many of the `pids` fan-out `choice.exe` ProcessIds are STILL alive,
+/// matched by fixed `ProcessId` intersected with image `choice.exe`.
+///
+/// This is the POST-CLOSE half of the two-phase reap check. Because it filters
+/// on the exact PIDs captured while the tagged parent was alive — not on the
+/// now-dead tagged parent, and not host-wide by image name — it is:
+///   * non-vacuous — a leaked/orphaned captured `choice` (same PID, still
+///     `choice.exe`) is counted, so a survivor stays detectable; and
+///   * not host-wide-flaky — a concurrent target's `choice.exe` carries a
+///     different, non-captured PID and is excluded.
+///
+/// An empty `pids` slice yields 0 without issuing a malformed filter.
+fn surviving_captured_choice_pids(pids: &[u32]) -> usize {
+    if pids.is_empty() {
+        return 0;
+    }
+    let pid_list = pids
+        .iter()
+        .map(u32::to_string)
+        .collect::<Vec<_>>()
+        .join(",");
+    let out = Command::new("powershell")
+        .args([
+            "-NoProfile",
+            "-NonInteractive",
+            "-Command",
+            &format!(
+                "$pids=@({pid_list}); \
+                 @(Get-CimInstance Win32_Process -Filter \"Name='choice.exe'\" | \
+                 Where-Object {{ $pids -contains $_.ProcessId }}).Count"
             ),
         ])
         .output()
-        .expect("query this test's tagged choice descendants via CIM");
+        .expect("query survival of captured choice PIDs via CIM");
     String::from_utf8_lossy(&out.stdout)
         .trim()
         .parse()
@@ -321,18 +371,29 @@ async fn active_process_cap_is_enforced() {
             .await
     });
 
-    // Sample the live count (already scoped to this test's tagged tree) while
-    // the fan-out is held, tracking its peak.
+    // Sample the live PID set (already scoped to this test's tagged tree) while
+    // the fan-out is held, tracking its peak and capturing the peak PID set so
+    // the post-close reap check can re-verify those exact ProcessIds by fixed
+    // PID — the parent-scoped query goes structurally empty once the tagged
+    // parent dies at job close.
     let mut peak = 0usize;
+    let mut captured_pids: Vec<u32> = Vec::new();
     let watch_deadline = Instant::now() + Duration::from_secs(30);
     while Instant::now() < watch_deadline {
-        let delta = tagged_choice_descendant_count(&tag);
-        peak = peak.max(delta);
+        let pids = tagged_choice_descendant_pids(&tag);
+        if pids.len() > peak {
+            peak = pids.len();
+            captured_pids = pids.clone();
+        }
         // Once we have clearly observed a large admitted set we can stop early.
-        if delta >= SANDBOX_ACTIVE_PROCESS_LIMIT / 2 {
+        if pids.len() >= SANDBOX_ACTIVE_PROCESS_LIMIT / 2 {
             // keep sampling briefly to catch any overshoot past the cap
             std::thread::sleep(Duration::from_millis(500));
-            peak = peak.max(tagged_choice_descendant_count(&tag));
+            let overshoot = tagged_choice_descendant_pids(&tag);
+            if overshoot.len() > peak {
+                peak = overshoot.len();
+                captured_pids = overshoot;
+            }
             break;
         }
         std::thread::sleep(Duration::from_millis(250));
@@ -361,13 +422,22 @@ async fn active_process_cap_is_enforced() {
         "fan-out was not capped: observed {peak} of {attempts} attempted — the limit \
          admitted every spawn"
     );
+    assert!(
+        !captured_pids.is_empty(),
+        "peak PID set was not captured — the post-close reap check would be vacuous"
+    );
 
-    // Everything this test spawned is reaped once the job closes (the count is
-    // already scoped to this test's tagged tree, so "back to baseline" is zero).
+    // After the job closes, re-check the EXACT fan-out `choice` PIDs captured
+    // while the tagged parent was alive, by fixed ProcessId intersected with
+    // image `choice.exe`. This is non-vacuous (a leaked/orphaned captured
+    // survivor is still counted) and not host-wide-flaky (a concurrent target's
+    // `choice.exe` carries a different, non-captured PID). The parent-scoped
+    // query cannot be reused — the tagged parent is dead once `run.await`
+    // returned.
     wait_until(
-        || tagged_choice_descendant_count(&tag) == 0,
+        || surviving_captured_choice_pids(&captured_pids) == 0,
         30,
-        "fan-out descendants reaped after job close",
+        "fan-out descendants reaped after job close (by captured PID)",
     );
     reap_stray_choice();
 }

<stdin>
F20 PRE-NATIVE FOUR-WAY CROSS-AUDIT (plan 20-52) — RE-SEALED FURTHER-REPAIRED SUCCESSOR

You are an INDEPENDENT (non-author) adversarial code auditor. Prosecute the candidate at EVERY
severity. Default to REFUTE: if you are uncertain a fix is correct, treat it as a FINDING, not a pass.
Your job is to find any reason this candidate should NOT be admitted to the scarce native-hardware run.

== CANDIDATE UNDER AUDIT (exact, re-sealed) ==
- source_sha (SEALED):  f0dd5b6d312af616f268f96f34c3bc9fc962c4d2
- source_tree (SEALED): ac76c87b318ee4ba8c34927dea23e40e63fd0776
- predecessor (sealed-but-RED, BLOCKED by the 20-45 panel): 3f839309574d6741eed416cd3820f56447f74eba
- repo: wayland-core (multi-provider AI agent CLI, Rust workspace). Branch plan/f20-unified-audit-repair.
- The ONLY deferred checks are native_macos / native_windows (proven at 20-53). EVERYTHING else —
  static code review of the delta, all-severity findings, evidence integrity, integration authority —
  is prosecuted NOW.

== WHAT THIS DELTA IS ==
The 20-45 four-way pre-native panel returned BLOCK on predecessor 3f839309 with TWO real,
statically-provable findings. The 20-50 fix (this delta, 3f839309 -> f0dd5b6d) closes both, touching
ONLY the two Windows-only files the findings name — NO Cargo.toml, NO other source, NO workflow.

--- FINDING 1 (was Codex HIGH / Claude MEDIUM): vacuous post-close reap assertion ---
File: crates/wcore-sandbox/tests/hard_process_containment_windows.rs, test active_process_cap_is_enforced.
The OLD post-close check was `wait_until(|| tagged_choice_descendant_count(&tag) == 0, ...)`. That helper
scoped choice.exe by whose ParentProcessId is a LIVE tagged cmd.exe. After run.await returns, the
AppContainer Job Object has closed and the tagged parent cmd is dead, so the parent-scoped CIM query is
structurally empty and the count is 0 whether or not a choice.exe leaked — VACUOUS.

The 20-50 fix ("threads the needle"):
  * tagged_choice_descendant_count(-> usize) becomes tagged_choice_descendant_pids(-> Vec<u32>): the SAME
    ParentProcessId-scoped CIM query, but returns the choice.exe children's ProcessId LIST.
  * NEW surviving_captured_choice_pids(pids: &[u32]) -> usize: counts choice.exe whose ProcessId is in the
    captured set ($pids -contains $_.ProcessId); empty slice short-circuits to 0.
  * The peak-sampling loop now captures the peak PID set (captured_pids) alongside peak.
  * NEW belt-and-braces assert!(!captured_pids.is_empty(), ...) guards against silent re-vacuity.
  * The post-close reap check is now surviving_captured_choice_pids(&captured_pids) == 0 — fixed PID.
Claim: non-vacuous (a leaked/orphaned captured choice, same PID, still choice.exe, is counted) AND NOT
host-wide-flaky (a concurrent live_fs_acl choice.exe carries a different, non-captured PID -> excluded);
does NOT revert to the host-wide image_count("choice.exe") baseline that the 20-43 Task-3 flake fix removed.
The other containment assertions (peak>0, peak<=SANDBOX_ACTIVE_PROCESS_LIMIT=512, peak<attempts,
exit_code==0) are preserved VERBATIM, as is the sibling test
job_close_reaps_detached_descendant_with_no_residue (whose grandchild carries `rem {tag}` on its OWN
cmdline so tagged_cmd_count survives parent death).

--- FINDING 2 (was Codex MEDIUM / Claude LOW): loose cmd classifier ---
File: crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs, fn resolved_program_is_cmd.
OLD: `.to_ascii_lowercase().ends_with("cmd.exe")` — suffix-matches notcmd.exe/foocmd.exe, which would then
route their /c|/k payload through the cmd-specific quote_cmd_payload instead of the general quote_arg.
NEW: decode + lowercase, then `std::path::Path::new(&decoded).file_name().and_then(to_str)
.is_some_and(|name| name == "cmd.exe")` — exact FINAL PATH COMPONENT match (Windows Path treats both
`\` and `/` as separators). Claim: quote_cmd_payload / quote_arg / classify_bare_shell / resolve_program /
is_unc_or_device_path are byte-identical; ONLY the cmd/non-cmd classification is tightened.

== YOUR ADVERSARIAL MANDATE (prosecute all, refute-by-default) ==
1. Finding 1 non-vacuous WITHOUT the host-wide flake: Does the post-close check catch a leaked/orphaned
   captured choice survivor by fixed PID? Does it avoid the host-wide-image-count flake (match ONLY the
   test's own captured PIDs)? Is !captured_pids.is_empty() a real guard against re-vacuity? Are peak>0,
   peak<=512, peak<attempts, exit_code==0 and the sibling reap test ALL preserved verbatim? Any NEW defect
   introduced by the two-phase rewrite (PID-reuse false-fail, malformed PowerShell filter, race)?
2. Finding 2 exact-final-component: Does file_name()==cmd.exe correctly reject notcmd.exe/foocmd.exe and
   accept System32\cmd.exe? Is the #B quoting behavior (quote_cmd_payload/quote_arg) otherwise identical?
   Any regression where a real cmd.exe is now MISCLASSIFIED as non-cmd (would break isolation quoting)?
3. Isolation guardrails still hold: #A DispatchAdmission budget preserved (no catch-to-unlimited — but note
   this delta does NOT touch worktree_manager.rs at all); #B quoting-layer ONLY — is_unc_or_device_path,
   Low-IL restricted token, Job-Object limits, ACL lease untouched; argv discipline kept; 20-36 `exit /b 0`
   normalization intact.
4. Any NEW defect the fix introduces at any severity.

== OUTPUT CONTRACT (MANDATORY — emit EXACTLY this JSON, nothing else) ==
Emit a single JSON object and NOTHING else (no prose, no markdown fences):
{
  "schema": "f20-native-crossaudit.codex-sol",
  "source_sha": "f0dd5b6d312af616f268f96f34c3bc9fc962c4d2",
  "source_tree": "ac76c87b318ee4ba8c34927dea23e40e63fd0776",
  "reviewer_id": "codex-5.6-sol",
  "reviewer_kind": "external-cli",
  "checks": { "all_severity": "PASS|FAIL", "evidence_integrity": "PASS|FAIL", "integration_authority": "PASS|FAIL" },
  "deferred": ["native_macos", "native_windows"],
  "findings": { "blocker": 0, "critical": 0, "high": 0, "medium": 0, "low": 0 },
  "finding_1_reap_nonvacuous": "PASS|FAIL",
  "finding_2_cmd_exact_component": "PASS|FAIL",
  "guardrail_A_admission_budget": "PASS|FAIL",
  "guardrail_B_quoting_layer_only": "PASS|FAIL",
  "evidence": [ { "claim": "<what you checked>", "severity": "high|medium|low|none", "result": "PASS|FAIL" } ],
  "disposition": "PASS|BLOCK"
}
PASS disposition ONLY if you find ZERO findings at any non-deferred severity. Any real finding => BLOCK.

== THE EXACT REPAIR DELTA (git diff 3f839309..f0dd5b6d, the two source files) ==
diff --git a/crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs b/crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
index afb9e356..7c162fe0 100644
--- a/crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
+++ b/crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
@@ -135,7 +135,9 @@ pub(super) fn quote_cmd_payload(payload: &str) -> String {
 }
 
 /// True when a resolved `lpApplicationName` (NUL-terminated UTF-16, as
-/// returned by [`resolve_program`]) names `cmd.exe`. Used to decide whether the
+/// returned by [`resolve_program`]) has `cmd.exe` as its exact FINAL PATH
+/// COMPONENT (case-insensitive) — NOT a bare suffix, so a name like
+/// `notcmd.exe`/`foocmd.exe` does not match. Used to decide whether the
 /// `/c`/`/k` payload needs the cmd-aware quoting above rather than the general
 /// CRT [`quote_arg`] rules. Matching the RESOLVED program (not the raw
 /// `argv[0]`) is deliberate: bare `cmd`/`cmd.exe` is pinned to
@@ -146,9 +148,15 @@ pub(super) fn resolved_program_is_cmd(app_name_w: &[u16]) -> bool {
         .iter()
         .position(|&c| c == 0)
         .unwrap_or(app_name_w.len());
-    String::from_utf16_lossy(&app_name_w[..end])
-        .to_ascii_lowercase()
-        .ends_with("cmd.exe")
+    // Windows `Path` treats both `\` and `/` as separators, so `file_name`
+    // isolates the true final component of the `System32\cmd.exe` resolution or
+    // any absolute path the caller passed; equality (not a suffix) rejects
+    // `notcmd.exe`.
+    let decoded = String::from_utf16_lossy(&app_name_w[..end]).to_ascii_lowercase();
+    std::path::Path::new(&decoded)
+        .file_name()
+        .and_then(|name| name.to_str())
+        .is_some_and(|name| name == "cmd.exe")
 }
 
 /// Classification of a bare (non-absolute) `argv[0]`. Only `cmd` is
diff --git a/crates/wcore-sandbox/tests/hard_process_containment_windows.rs b/crates/wcore-sandbox/tests/hard_process_containment_windows.rs
index c8a59997..1455271d 100644
--- a/crates/wcore-sandbox/tests/hard_process_containment_windows.rs
+++ b/crates/wcore-sandbox/tests/hard_process_containment_windows.rs
@@ -125,19 +125,28 @@ fn tagged_cmd_count(tag: &str) -> usize {
         .unwrap_or(0)
 }
 
-/// Count the `choice.exe` idlers spawned by THIS test's fan-out, scoped to the
-/// test's own tagged process tree rather than host-wide by image name.
+/// Return the ProcessIds of the `choice.exe` idlers spawned by THIS test's
+/// fan-out, scoped to the test's own tagged process tree rather than host-wide
+/// by image name.
 ///
 /// A bare `choice.exe` idler cannot carry an injected tag on its own command
 /// line (choice rejects the extra argument), but the top-level sandbox `cmd`
 /// that runs the fan-out script DOES carry a unique `rem {tag}` — and every
 /// `start "" /b choice` idler is a direct child of that cmd, so its
-/// `ParentProcessId` is the tagged cmd's `ProcessId`. Counting only choice
+/// `ParentProcessId` is the tagged cmd's `ProcessId`. Selecting only choice
 /// processes whose parent is this test's tagged cmd means a concurrent
 /// `choice`-spawning target on the same runner (e.g. `live_fs_acl`) cannot
-/// pollute the count — closing the host-wide-image-count flake (watch-item d2)
-/// without weakening any containment assertion.
-fn tagged_choice_descendant_count(tag: &str) -> usize {
+/// pollute the capture.
+///
+/// This is the ALIVE-phase half of a two-phase reap check: the returned PIDs
+/// are captured WHILE the tagged parent cmd is still alive (during the
+/// peak-sampling window), and the `ParentProcessId` scope is what makes that
+/// capture immune to a concurrent `choice`-spawning target. Once the job closes
+/// the tagged parent is dead, so this parent-scoped query would go structurally
+/// empty regardless of a leaked survivor — the post-close survivor check is
+/// therefore done by fixed ProcessId via [`surviving_captured_choice_pids`],
+/// NOT by re-running this parent-scoped query.
+fn tagged_choice_descendant_pids(tag: &str) -> Vec<u32> {
     let out = Command::new("powershell")
         .args([
             "-NoProfile",
@@ -148,11 +157,52 @@ fn tagged_choice_descendant_count(tag: &str) -> usize {
                  Where-Object {{ $_.CommandLine -like '*{tag}*' }} | \
                  Select-Object -ExpandProperty ProcessId); \
                  @(Get-CimInstance Win32_Process -Filter \"Name='choice.exe'\" | \
-                 Where-Object {{ $parents -contains $_.ParentProcessId }}).Count"
+                 Where-Object {{ $parents -contains $_.ParentProcessId }} | \
+                 Select-Object -ExpandProperty ProcessId)"
+            ),
+        ])
+        .output()
+        .expect("query this test's tagged choice descendant PIDs via CIM");
+    String::from_utf8_lossy(&out.stdout)
+        .split_whitespace()
+        .filter_map(|s| s.parse::<u32>().ok())
+        .collect()
+}
+
+/// Count how many of the `pids` fan-out `choice.exe` ProcessIds are STILL alive,
+/// matched by fixed `ProcessId` intersected with image `choice.exe`.
+///
+/// This is the POST-CLOSE half of the two-phase reap check. Because it filters
+/// on the exact PIDs captured while the tagged parent was alive — not on the
+/// now-dead tagged parent, and not host-wide by image name — it is:
+///   * non-vacuous — a leaked/orphaned captured `choice` (same PID, still
+///     `choice.exe`) is counted, so a survivor stays detectable; and
+///   * not host-wide-flaky — a concurrent target's `choice.exe` carries a
+///     different, non-captured PID and is excluded.
+///
+/// An empty `pids` slice yields 0 without issuing a malformed filter.
+fn surviving_captured_choice_pids(pids: &[u32]) -> usize {
+    if pids.is_empty() {
+        return 0;
+    }
+    let pid_list = pids
+        .iter()
+        .map(u32::to_string)
+        .collect::<Vec<_>>()
+        .join(",");
+    let out = Command::new("powershell")
+        .args([
+            "-NoProfile",
+            "-NonInteractive",
+            "-Command",
+            &format!(
+                "$pids=@({pid_list}); \
+                 @(Get-CimInstance Win32_Process -Filter \"Name='choice.exe'\" | \
+                 Where-Object {{ $pids -contains $_.ProcessId }}).Count"
             ),
         ])
         .output()
-        .expect("query this test's tagged choice descendants via CIM");
+        .expect("query survival of captured choice PIDs via CIM");
     String::from_utf8_lossy(&out.stdout)
         .trim()
         .parse()
@@ -321,18 +371,29 @@ async fn active_process_cap_is_enforced() {
             .await
     });
 
-    // Sample the live count (already scoped to this test's tagged tree) while
-    // the fan-out is held, tracking its peak.
+    // Sample the live PID set (already scoped to this test's tagged tree) while
+    // the fan-out is held, tracking its peak and capturing the peak PID set so
+    // the post-close reap check can re-verify those exact ProcessIds by fixed
+    // PID — the parent-scoped query goes structurally empty once the tagged
+    // parent dies at job close.
     let mut peak = 0usize;
+    let mut captured_pids: Vec<u32> = Vec::new();
     let watch_deadline = Instant::now() + Duration::from_secs(30);
     while Instant::now() < watch_deadline {
-        let delta = tagged_choice_descendant_count(&tag);
-        peak = peak.max(delta);
+        let pids = tagged_choice_descendant_pids(&tag);
+        if pids.len() > peak {
+            peak = pids.len();
+            captured_pids = pids.clone();
+        }
         // Once we have clearly observed a large admitted set we can stop early.
-        if delta >= SANDBOX_ACTIVE_PROCESS_LIMIT / 2 {
+        if pids.len() >= SANDBOX_ACTIVE_PROCESS_LIMIT / 2 {
             // keep sampling briefly to catch any overshoot past the cap
             std::thread::sleep(Duration::from_millis(500));
-            peak = peak.max(tagged_choice_descendant_count(&tag));
+            let overshoot = tagged_choice_descendant_pids(&tag);
+            if overshoot.len() > peak {
+                peak = overshoot.len();
+                captured_pids = overshoot;
+            }
             break;
         }
         std::thread::sleep(Duration::from_millis(250));
@@ -361,13 +422,22 @@ async fn active_process_cap_is_enforced() {
         "fan-out was not capped: observed {peak} of {attempts} attempted — the limit \
          admitted every spawn"
     );
+    assert!(
+        !captured_pids.is_empty(),
+        "peak PID set was not captured — the post-close reap check would be vacuous"
+    );
 
-    // Everything this test spawned is reaped once the job closes (the count is
-    // already scoped to this test's tagged tree, so "back to baseline" is zero).
+    // After the job closes, re-check the EXACT fan-out `choice` PIDs captured
+    // while the tagged parent was alive, by fixed ProcessId intersected with
+    // image `choice.exe`. This is non-vacuous (a leaked/orphaned captured
+    // survivor is still counted) and not host-wide-flaky (a concurrent target's
+    // `choice.exe` carries a different, non-captured PID). The parent-scoped
+    // query cannot be reused — the tagged parent is dead once `run.await`
+    // returned.
     wait_until(
-        || tagged_choice_descendant_count(&tag) == 0,
+        || surviving_captured_choice_pids(&captured_pids) == 0,
         30,
-        "fan-out descendants reaped after job close",
+        "fan-out descendants reaped after job close (by captured PID)",
     );
     reap_stray_choice();
 }
</stdin>
deprecated: `[features].codex_hooks` is deprecated. Use `[features].hooks` instead.
Enable it with `--enable hooks` or `[features].hooks` in config.toml. See https://developers.openai.com/codex/config-basic#feature-flags for details.
2026-07-24T07:39:21.339541Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when AuthRequired(AuthRequiredError { www_authenticate_header: "Bearer realm=\"OAuth\", resource_metadata=\"https://mcp.canva.com/.well-known/oauth-protected-resource/mcp\", error=\"invalid_token\", error_description=\"Missing or invalid access token\"" })
2026-07-24T07:39:22.243292Z ERROR rmcp::transport::worker: worker quit with fatal: Transport channel closed, when Auth(AuthorizationRequired)
hook: SessionStart
hook: SessionStart Completed
hook: UserPromptSubmit
hook: UserPromptSubmit Completed
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "sed -n '1,240p' /Users/seandonahoe/.codex/RTK.md && sed -n '1,260p' /Users/seandonahoe/.codex/skills/ijfw-cross-audit/SKILL.md" in /Users/seandonahoe/dev/waylandcore-ferrox
 succeeded in 0ms:
# RTK - Rust Token Killer (Codex CLI)

**Usage**: Token-optimized CLI proxy for shell commands.

## Rule

Always prefix shell commands with `rtk`.

Examples:

```bash
rtk git status
rtk cargo test
rtk npm run build
rtk pytest -q
```

## Meta Commands

```bash
rtk gain            # Token savings analytics
rtk gain --history  # Recent command savings history
rtk proxy <cmd>     # Run raw command without filtering
```

## Verification

```bash
rtk --version
rtk gain
which rtk
```
---
name: ijfw-cross-audit
description: "Generate a cross-platform multi-model audit (Trident) on a diff, brief, or artifact. Trigger: 'cross audit', 'Trident', 'second opinion', 'check with other models', 'check with other AIs', 'cross-check this', 'get another perspective', /cross-audit"
---

## Execution

1. **Detect artifact.** Accept: diff, file path, brief text, or `HEAD~1..HEAD`.
   If none provided, ask once: `What should I audit? (diff, file, or paste text)`

2. **Detect auditors.** Check PATH for `codex` and `gemini`.
   Default: one OpenAI-family + one Google-family, excluding caller's family.
   Cap at 4 auditors total.

3. **Dispatch in parallel via background bash** -- never hand off to user.
   Prompt each: security findings, logic issues, reliability concerns, test gaps.
   ```bash
   codex "Review for security, logic, reliability, test gaps: <artifact>" &
   gemini "Review for security, logic, reliability, test gaps: <artifact>" &
   wait
   ```

4. **Reconcile.** Deduplicate. Classify:
   - CONSENSUS -- flagged by 2+ auditors
   - CONTESTED -- flagged by 1 auditor only
   - PASS -- no issues

5. **Emit report** using the format below.

## Report format rule (all cross-audit outputs)

Any reconciliation report presenting multiple paths MUST use this structure:

```
VERDICT
  <one-line converged recommendation>

OPTIONS
  A -- <short-name>: <one-line what it is>
  B -- <short-name>: <one-line what it is>

REVIEWER CONVERGENCE
  <reviewer>:  <letter>  <score>  "<their call>"  => Option <letter> -- <name>

RECOMMENDATION
  Option <X> -- <name> because <one-sentence why>.

NEXT ACTION
  <exact command or step>
```

Never write "Option A" or "Option B" without "-- name" immediately after it on the same line.

## Output contract

Emit a `gate-result` block as the **LAST** content of your output. Nothing
after it. Use `gate="cross-audit"`. Statuses: `PASS | CONDITIONAL | WARN | FLAG | FAIL`.

Format:

```gate-result
{
  "schema_version": "1.0",
  "gate": "cross-audit",
  "status": "<STATUS>",
  "project_type": "<from project-type-detector>",
  "lenses": [],
  "affected_artifacts": [],
  "accounting": {"duration_ms": 0, "lenses_invoked": 0, "cost_usd": null},
  "remediation": [],
  "receipts_ref": null,
  "supersedes": null,
  "gate_id": "<gate-with-colons-replaced-by-dashes>-<ts>-<rand4>",
  "emitted_at": "<ISO-8601>"
}
```

hook: PostToolUse
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse Completed
mcp: ijfw-memory/ijfw_memory_prelude started
mcp: ijfw-memory/ijfw_memory_prelude (completed)
hook: PostToolUse
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc 'rtk rg -n "F20|20-45|native|hard_process_containment_windows|resolved_program_is_cmd|waylandcore" /Users/seandonahoe/.codex/memories/MEMORY.md | head -80
export WL_LANE=core; rtk wl queue
rtk which codex
rtk which gemini
rtk git status --short --branch
rtk git rev-parse HEAD HEAD''^{tree} f0dd5b6d312af616f268f96f34c3bc9fc962c4d2''^{tree} 3f839309574d6741eed416cd3820f56447f74eba''^{tree}' in /Users/seandonahoe/dev/waylandcore-ferrox
hook: PostToolUse
hook: PostToolUse Completed
 succeeded in 8947ms:
error connecting to api.github.com
check your internet connection or https://githubstatus.com
/opt/homebrew/bin/codex
/Users/seandonahoe/.nvm/versions/node/v22.23.1/bin/gemini
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-2dcjfCmQ' (errno=Operation not permitted)
2026-07-24 14:39:52.121 xcodebuild[79899:161620893]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:39:52.525 xcodebuild[79899:161620846] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-vwXp6Imf' (errno=Operation not permitted)
2026-07-24 14:39:53.470 xcodebuild[80273:161621679]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:39:53.910 xcodebuild[80273:161621669] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
## plan/f20-unified-audit-repair...origin/plan/f20-unified-audit-repair [ahead 119]
 M AGENTS.md
?? .planning/phases/20-transactional-delegated-mutation/20-52-raw/git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-HR6nh6PQ' (errno=Operation not permitted)
2026-07-24 14:39:56.320 xcodebuild[80712:161622724]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:39:56.712 xcodebuild[80712:161622717] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-wWuqt5KY' (errno=Operation not permitted)
2026-07-24 14:39:57.657 xcodebuild[80757:161622938]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:39:58.085 xcodebuild[80757:161622937] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
eff0b3be09adb82e69c1145f21a68a29ce601743
1d943baf6abfadf43146dec06cb29c1a8740afce
ac76c87b318ee4ba8c34927dea23e40e63fd0776
3092475bb4102d010b6ff5f6c9d8080cb4f51928

hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk git cat-file -t f0dd5b6d312af616f268f96f34c3bc9fc962c4d2
rtk git show -s --format='%H%n%T%n%P%n%an%n%ad%n%s' --date=iso-strict f0dd5b6d312af616f268f96f34c3bc9fc962c4d2
rtk git merge-base --is-ancestor 3f839309574d6741eed416cd3820f56447f74eba f0dd5b6d312af616f268f96f34c3bc9fc962c4d2; rtk git diff --name-status 3f839309574d6741eed416cd3820f56447f74eba..f0dd5b6d312af616f268f96f34c3bc9fc962c4d2
rtk git diff --stat 3f839309574d6741eed416cd3820f56447f74eba..f0dd5b6d312af616f268f96f34c3bc9fc962c4d2
rtk git diff --check 3f839309574d6741eed416cd3820f56447f74eba..f0dd5b6d312af616f268f96f34c3bc9fc962c4d2
rtk git diff --exit-code 3f839309574d6741eed416cd3820f56447f74eba..f0dd5b6d312af616f268f96f34c3bc9fc962c4d2 -- Cargo.toml '*/Cargo.toml' '.github/workflows/**' 'crates/wcore-agent/**/worktree_manager.rs' ; true" in /Users/seandonahoe/dev/waylandcore-ferrox
 succeeded in 24600ms:
2026-07-24 14:40:20.515 xcodebuild[81885:161627722]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:40:20.894 xcodebuild[81885:161627709] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-vn1kXqA7' (errno=Operation not permitted)
2026-07-24 14:40:21.862 xcodebuild[81938:161628038]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:40:22.212 xcodebuild[81938:161627991] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
commit
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2
ac76c87b318ee4ba8c34927dea23e40e63fd0776
472d7c032282d51f2e0a23231298075fb5f15b41
ci
2026-07-24T14:11:31+07:00
fix(20-50): match cmd.exe as exact final path component
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-itVi4tTP' (errno=Operation not permitted)
2026-07-24 14:40:26.509 xcodebuild[85009:161631516]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:40:26.718 xcodebuild[85009:161631494] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-qdhF34Ey' (errno=Operation not permitted)
2026-07-24 14:40:27.414 xcodebuild[85906:161632529]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:40:27.624 xcodebuild[85906:161632497] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
A	.planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md
A	.planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md
M	.planning/phases/20-transactional-delegated-mutation/20-45-PLAN.md
A	.planning/phases/20-transactional-delegated-mutation/20-45-SUMMARY.md
A	.planning/phases/20-transactional-delegated-mutation/20-45-raw/audit-context.shared.txt
A	.planning/phases/20-transactional-delegated-mutation/20-45-raw/codex-sol.raw.txt
A	.planning/phases/20-transactional-delegated-mutation/20-45-raw/gemini-pro.raw.txt
A	.planning/phases/20-transactional-delegated-mutation/20-45-raw/kimi-k3.raw.txt
M	.planning/phases/20-transactional-delegated-mutation/20-47-PLAN.md
A	.planning/phases/20-transactional-delegated-mutation/20-50-PLAN.md
A	.planning/phases/20-transactional-delegated-mutation/20-51-PLAN.md
A	.planning/phases/20-transactional-delegated-mutation/20-52-PLAN.md
A	.planning/phases/20-transactional-delegated-mutation/20-53-PLAN.md
A	.planning/phases/20-transactional-delegated-mutation/20-54-PLAN.md
A	.planning/phases/20-transactional-delegated-mutation/20-55-PLAN.md
A	.planning/phases/20-transactional-delegated-mutation/20-56-PLAN.md
M	crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
M	crates/wcore-sandbox/tests/hard_process_containment_windows.rs

--- Changes ---

.../20-44-SUMMARY.md                               | 120 +++++
 .../20-45-CROSS-AUDIT.md                           | 280 +++++++++++
 .../20-45-PLAN.md                                  |  76 +--
 .../20-45-SUMMARY.md                               | 117 +++++
 .../20-45-raw/audit-context.shared.txt             | 541 +++++++++++++++++++++
 .../20-45-raw/codex-sol.raw.txt                    |   9 +
 .../20-45-raw/gemini-pro.raw.txt                   |  22 +
 .../20-45-raw/kimi-k3.raw.txt                      |  25 +
 .../20-47-PLAN.md                                  |  74 +--
 .../20-50-PLAN.md                                  | 177 +++++++
 .../20-51-PLAN.md                                  | 129 +++++
 .../20-52-PLAN.md                                  | 123 +++++
 .../20-53-PLAN.md                                  | 137 ++++++
 .../20-54-PLAN.md                                  | 123 +++++
 .../20-55-PLAN.md                                  | 109 +++++
 .../20-56-PLAN.md                                  | 131 +++++
 .../backends/appcontainer/windows_impl/command.rs  |  16 +-
 .../tests/hard_process_containment_windows.rs      | 106 +++-
 18 files changed, 2225 insertions(+), 90 deletions(-)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-QJMiNXrC' (errno=Operation not permitted)
2026-07-24 14:40:37.000 xcodebuild[89949:161637698]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:40:37.166 xcodebuild[89949:161637697] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-vizKKREr' (errno=Operation not permitted)
2026-07-24 14:40:37.698 xcodebuild[90174:161637994]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:40:37.906 xcodebuild[90174:161637970] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.


hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk codex exec --help | sed -n '1,160p'
rtk gemini --help | sed -n '1,180p'" in /Users/seandonahoe/dev/waylandcore-ferrox
hook: PostToolUse
 succeeded in 1435ms:
Usage: gemini [options] [command]

Gemini CLI - Defaults to interactive mode. Use -p/--prompt for non-interactive (headless) mode.

Commands:
  gemini mcp                   Manage MCP servers
  gemini extensions <command>  Manage Gemini CLI extensions.  [aliases: extension]
  gemini skills <command>      Manage agent skills.  [aliases: skill]
  gemini hooks <command>       Manage Gemini CLI hooks.  [aliases: hook]
  gemini gemma                 Manage local Gemma model routing
  gemini [query..]             Launch Gemini CLI  [default]

Positionals:
  query  Initial prompt. Runs in interactive mode by default; use -p/--prompt for non-interactive.

Options:
  -d, --debug                     Run in debug mode (open debug console with F12)  [boolean] [default: false]
  -m, --model                     Model  [string]
  -p, --prompt                    Run in non-interactive (headless) mode with the given prompt. Appended to input on stdin (if any).  [string]
  -i, --prompt-interactive        Execute the provided prompt and continue in interactive mode  [string]
      --skip-trust                Trust the current workspace for this session.  [boolean] [default: false]
  -w, --worktree                  Start Gemini in a new git worktree. If no name is provided, one is generated automatically.  [string]
  -s, --sandbox                   Run in sandbox?  [boolean]
  -y, --yolo                      Automatically accept all actions (aka YOLO mode, see https://www.youtube.com/watch?v=xvFZjo5PgG0 for more details)?  [boolean] [default: false]
      --approval-mode             Set the approval mode: default (prompt for approval), auto_edit (auto-approve edit tools), yolo (auto-approve all tools), plan (read-only mode)  [string] [choices: "default", "auto_edit", "yolo", "plan"]
      --policy                    Additional policy files or directories to load (comma-separated or multiple --policy)  [array]
      --admin-policy              Additional admin policy files or directories to load (comma-separated or multiple --admin-policy)  [array]
      --acp                       Starts the agent in ACP mode  [boolean]
      --experimental-acp          Starts the agent in ACP mode (deprecated, use --acp instead)  [boolean]
      --allowed-mcp-server-names  Allowed MCP server names  [array]
      --allowed-tools             [DEPRECATED: Use Policy Engine instead See https://geminicli.com/docs/core/policy-engine] Tools that are allowed to run without confirmation  [array]
  -e, --extensions                A list of extensions to use. If not provided, all extensions are used.  [array]
  -l, --list-extensions           List all available extensions and exit.  [boolean]
  -r, --resume                    Resume a previous session. Use "latest" for most recent or index number (e.g. --resume 5)  [string]
      --session-file              Load a session from a JSON file  [string]
      --session-id                Start a new session with a manually provided UUID.  [string]
      --list-sessions             List available sessions for the current project and exit.  [boolean]
      --delete-session            Delete a session by index number (use --list-sessions to see available sessions).  [string]
      --include-directories       Additional directories to include in the workspace (comma-separated or multiple --include-directories)  [array]
      --screen-reader             Enable screen reader mode for accessibility.  [boolean]
  -o, --output-format             The format of the CLI output.  [string] [choices: "text", "json", "stream-json"]
      --raw-output                Disable sanitization of model output (e.g. allow ANSI escape sequences). WARNING: This can be a security risk if the model output is untrusted.  [boolean]
      --accept-raw-output-risk    Suppress the security warning when using --raw-output.  [boolean]
  -v, --version                   Show version number  [boolean]
  -h, --help                      Show help  [boolean]

hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk git show f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs | sed -n '1,360p'
rtk git show f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/tests/hard_process_containment_windows.rs | sed -n '1,520p'" in /Users/seandonahoe/dev/waylandcore-ferrox
hook: PostToolUse
 succeeded in 8449ms:
//! AppContainer command-line, program-resolution and environment helpers (F20-03 Task 1A split of `windows_impl`).
#![allow(unused_imports)]

use super::super::super::SandboxBackend;
use super::super::appcontainer_acl_lease::ExecutionIdentity;
use super::super::{NEGATIVE_PROBE_TTL, ProbeCache};
use crate::error::{Result, SandboxError};
use crate::manifest::{NetworkPolicy, SandboxManifest};
use crate::{ResourceLimitEnforcement, SandboxCommand, SandboxOutput};
use async_trait::async_trait;
use std::collections::BTreeMap;
use std::ffi::OsStr;
use std::mem;
use std::os::windows::ffi::{OsStrExt, OsStringExt};
use std::ptr;
use std::sync::atomic::AtomicUsize;
use std::sync::mpsc;
use std::sync::{Arc, Mutex, OnceLock};
use std::time::Duration;
use windows_sys::Win32::Foundation::{
    CloseHandle, GetLastError, HANDLE, INVALID_HANDLE_VALUE, WAIT_OBJECT_0, WAIT_TIMEOUT,
};
use windows_sys::Win32::Security::{
    AllocateAndInitializeSid, CreateRestrictedToken, DISABLE_MAX_PRIVILEGE, FreeSid, GetLengthSid,
    GetSidSubAuthority, GetSidSubAuthorityCount, GetTokenInformation, SECURITY_ATTRIBUTES,
    SECURITY_CAPABILITIES, SID_AND_ATTRIBUTES, SID_IDENTIFIER_AUTHORITY, SetTokenInformation,
    TOKEN_ADJUST_DEFAULT, TOKEN_ASSIGN_PRIMARY, TOKEN_DUPLICATE, TOKEN_MANDATORY_LABEL,
    TOKEN_QUERY, TokenIntegrityLevel,
};

use super::handles::*;
use super::process::*;
use super::*;
use windows_sys::Win32::Storage::FileSystem::ReadFile;
use windows_sys::Win32::System::JobObjects::{
    AssignProcessToJobObject, CreateJobObjectW, JOB_OBJECT_LIMIT_ACTIVE_PROCESS,
    JOB_OBJECT_LIMIT_BREAKAWAY_OK, JOB_OBJECT_LIMIT_DIE_ON_UNHANDLED_EXCEPTION,
    JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE, JOB_OBJECT_LIMIT_PRIORITY_CLASS,
    JOB_OBJECT_LIMIT_PROCESS_MEMORY, JOB_OBJECT_LIMIT_PROCESS_TIME,
    JOB_OBJECT_LIMIT_SILENT_BREAKAWAY_OK, JOB_OBJECT_UILIMIT_DESKTOP,
    JOB_OBJECT_UILIMIT_DISPLAYSETTINGS, JOB_OBJECT_UILIMIT_EXITWINDOWS,
    JOB_OBJECT_UILIMIT_GLOBALATOMS, JOB_OBJECT_UILIMIT_HANDLES, JOB_OBJECT_UILIMIT_READCLIPBOARD,
    JOB_OBJECT_UILIMIT_SYSTEMPARAMETERS, JOB_OBJECT_UILIMIT_WRITECLIPBOARD,
    JOBOBJECT_BASIC_UI_RESTRICTIONS, JOBOBJECT_EXTENDED_LIMIT_INFORMATION,
    JobObjectBasicUIRestrictions, JobObjectExtendedLimitInformation, SetInformationJobObject,
    TerminateJobObject,
};
use windows_sys::Win32::System::Pipes::CreatePipe;
use windows_sys::Win32::System::SystemInformation::GetSystemDirectoryW;
use windows_sys::Win32::System::Threading::{
    BELOW_NORMAL_PRIORITY_CLASS, CREATE_SUSPENDED, CreateProcessAsUserW,
    DeleteProcThreadAttributeList, EXTENDED_STARTUPINFO_PRESENT, GetCurrentProcess,
    GetExitCodeProcess, INFINITE, InitializeProcThreadAttributeList, LPPROC_THREAD_ATTRIBUTE_LIST,
    OpenProcessToken, PROC_THREAD_ATTRIBUTE_HANDLE_LIST,
    PROC_THREAD_ATTRIBUTE_SECURITY_CAPABILITIES, PROCESS_INFORMATION, ResumeThread,
    STARTF_USESTDHANDLES, STARTUPINFOEXW, TerminateProcess, UpdateProcThreadAttribute,
    WaitForSingleObject,
};

pub(super) fn widen(s: &str) -> Vec<u16> {
    OsStr::new(s)
        .encode_wide()
        .chain(std::iter::once(0))
        .collect()
}

pub(super) fn widen_os(s: &OsStr) -> Vec<u16> {
    s.encode_wide().chain(std::iter::once(0)).collect()
}

/// Windows cmdline-quoting per the MSVC C runtime / `CommandLineToArgvW`
/// round-trip rules (Daniel Colascione's algorithm). Quotes are only
/// added when needed (whitespace, embedded `"`, newline, or empty
/// string); backslashes are doubled when followed by `"` or by the
/// closing quote.
pub(super) fn quote_arg(arg: &str) -> String {
    let needs_quote = arg.is_empty()
        || arg
            .chars()
            .any(|c| matches!(c, ' ' | '\t' | '"' | '\n' | '\x0b'));
    if !needs_quote {
        return arg.to_string();
    }
    let mut out = String::with_capacity(arg.len() + 2);
    out.push('"');
    let mut backslashes = 0usize;
    for c in arg.chars() {
        match c {
            '\\' => backslashes += 1,
            '"' => {
                for _ in 0..(backslashes * 2 + 1) {
                    out.push('\\');
                }
                out.push('"');
                backslashes = 0;
            }
            other => {
                for _ in 0..backslashes {
                    out.push('\\');
                }
                out.push(other);
                backslashes = 0;
            }
        }
    }
    for _ in 0..(backslashes * 2) {
        out.push('\\');
    }
    out.push('"');
    out
}

/// Quote the `/c`/`/k` payload of a `cmd.exe` invocation for the RAW command
/// line `cmd.exe` re-reads.
///
/// `cmd.exe /s /c` (and the bare `/c` the availability probe uses) does NOT
/// consume the MSVC-CRT-parsed argv for its payload: with `/s` it strips only
/// the FIRST and LAST quote of the raw remainder after `/c` and executes the
/// rest VERBATIM. So [`quote_arg`]'s CRT escaping (`"` -> `\"`) leaks the
/// backslash-escaped quotes into the executed text — `type "path"` becomes
/// `type \"path\"` and fails `ERROR_INVALID_NAME`, and every `start "" /b …`
/// containment payload mangles identically. The correct spelling wraps the
/// payload in a single outer double-quote pair and passes its inner quotes
/// through untouched: `/s` strips exactly the outer pair, leaving the script's
/// embedded quotes intact. This is a quoting-layer fix only — argv discipline
/// is preserved (the payload is still one argv entry the caller supplied, not a
/// `format!`-interpolated shell string), and the sandbox token / Job Object /
/// AppContainer ACL boundary is untouched.
pub(super) fn quote_cmd_payload(payload: &str) -> String {
    let mut out = String::with_capacity(payload.len() + 2);
    out.push('"');
    out.push_str(payload);
    out.push('"');
    out
}

/// True when a resolved `lpApplicationName` (NUL-terminated UTF-16, as
/// returned by [`resolve_program`]) has `cmd.exe` as its exact FINAL PATH
/// COMPONENT (case-insensitive) — NOT a bare suffix, so a name like
/// `notcmd.exe`/`foocmd.exe` does not match. Used to decide whether the
/// `/c`/`/k` payload needs the cmd-aware quoting above rather than the general
/// CRT [`quote_arg`] rules. Matching the RESOLVED program (not the raw
/// `argv[0]`) is deliberate: bare `cmd`/`cmd.exe` is pinned to
/// `System32\cmd.exe` and an absolute path is validated as-is, so this reflects
/// the actual image that will parse the command line.
pub(super) fn resolved_program_is_cmd(app_name_w: &[u16]) -> bool {
    let end = app_name_w
        .iter()
        .position(|&c| c == 0)
        .unwrap_or(app_name_w.len());
    // Windows `Path` treats both `\` and `/` as separators, so `file_name`
    // isolates the true final component of the `System32\cmd.exe` resolution or
    // any absolute path the caller passed; equality (not a suffix) rejects
    // `notcmd.exe`.
    let decoded = String::from_utf16_lossy(&app_name_w[..end]).to_ascii_lowercase();
    std::path::Path::new(&decoded)
        .file_name()
        .and_then(|name| name.to_str())
        .is_some_and(|name| name == "cmd.exe")
}

/// Classification of a bare (non-absolute) `argv[0]`. Only `cmd` is
/// runnable under the Low-integrity restricted-token AppContainer; every
/// other shell is recognized solely so the resolver can return a clear,
/// actionable error instead of a cryptic `CreateProcessAsUserW 0x2`
/// (file-not-found, #323) or `0xC0000135` (DLL-not-found, #324) at spawn
/// time.
#[derive(PartialEq, Eq, Debug)]
pub(super) enum BareShell {
    /// `cmd` / `cmd.exe` — lives in `System32`, imports only the minimal
    /// `System32` DLL set, and is the one shell that loads under this
    /// sandbox token.
    Cmd,
    /// `powershell` / `pwsh` — NOT in `System32` (Windows PowerShell is in
    /// `System32\WindowsPowerShell\v1.0\`, pwsh in `Program Files`), and
    /// requires .NET / GAC assemblies that do not load under the Low-IL
    /// restricted token (#324).
    PowerShell,
    /// `bash` / `sh` — git-bash needs `msys-2.0.dll` from `Program Files`,
    /// and even static busybox-w32 links network/auth/UI DLLs the Low-IL
    /// token cannot load (#324). Not resolvable from `System32` either.
    Unsupported,
}

/// Classify a bare executable name against the canonical Windows shells.
/// Returns `None` for anything not recognized as a shell at all (those are
/// rejected with the generic "pass an absolute path" message). Resolution
/// of `cmd` goes through `GetSystemDirectoryW` (always `C:\Windows\System32`,
/// excluding CWD/PATH) — never `SearchPathW` — so a caller can never pull
/// something from `PATH` whose resolution is operator- or LLM-influenceable.
///
/// Both `cmd` and `cmd.exe` map to `Cmd` because Windows callers
/// conventionally omit `.exe`; the resolver appends it when concatenating
/// against `System32\`.
pub(super) fn classify_bare_shell(name: &str) -> Option<BareShell> {
    match name.to_ascii_lowercase().as_str() {
        "cmd" | "cmd.exe" => Some(BareShell::Cmd),
        "powershell" | "powershell.exe" | "pwsh" | "pwsh.exe" => Some(BareShell::PowerShell),
        "bash" | "bash.exe" | "sh" | "sh.exe" => Some(BareShell::Unsupported),
        _ => None,
    }
}

/// Returns true for any UNC / device path: `\\server\share\…`, `\\?\…`,
/// `\\.\…`, plus the forward-slash variants Rust's `Path` also accepts on
/// Windows. These are rejected outright because `Path::is_absolute()`
/// returns true for them, and naively passing them to
/// `CreateProcessAsUserW`'s image-load path triggers SMB / device-driver
/// access in the PARENT's security context — an NTLM-relay vector that
/// happens BEFORE the AppContainer token's network policy applies.
pub(super) fn is_unc_or_device_path(p: &str) -> bool {
    p.starts_with("\\\\") || p.starts_with("//")
}

/// True only for the Windows VERBATIM DISK form `\\?\X:\...` — an
/// extended-length spelling of an ordinary local drive-letter path.
/// `std::fs::canonicalize` returns this form for EVERY local path on
/// Windows, so the fs-allowlist guard must treat it as local, not as a
/// UNC/device path. Genuine UNC (`\\?\UNC\...`), device (`\\.\...`),
/// and other verbatim (`\\?\...`) prefixes are NOT VerbatimDisk and stay
/// rejected. The OS path parser handles slash/case variants for us.
#[cfg(test)]
pub(super) fn is_verbatim_disk_path(path: &std::path::Path) -> bool {
    use std::path::{Component, Prefix};
    matches!(
        path.components().next(),
        Some(Component::Prefix(p)) if matches!(p.kind(), Prefix::VerbatimDisk(_))
    )
}

/// Resolve a program reference into an absolute UTF-16 path suitable for
/// `lpApplicationName`. Hard-fails on any failure — the caller must
/// propagate the error rather than fall back to a NULL `lpApplicationName`
/// (the original 0xcb regression we already fixed once).
///
/// Rejection rules (each surfaces a distinct error message):
///   1. Empty.
///   2. UNC / device path (`\\server\…`, `\\?\…`, `\\.\…`) — NTLM-relay
///      vector.
///   3. Bare name not in the shell allowlist — pass an absolute path.
///   4. Absolute path that doesn't exist OR is unreadable.
///   5. Absolute path that is a directory, not a file.
///
/// Resolution rules:
///   * Absolute file → validated via `try_exists()` + `metadata()`,
///     returned widened.
///   * Bare `cmd` / `cmd.exe` → pinned to `C:\Windows\System32\cmd.exe`,
///     whose existence is then validated (the bare-shell branch used to
///     skip the existence check the absolute branch performs, so an
///     unresolvable shell surfaced only as a cryptic spawn-time `0x2` —
///     #323).
///   * Bare `powershell` / `pwsh` → rejected with a clear message: these
///     do NOT live in `System32` (the old code pinned them there, yielding
///     `0x2`/file-not-found, #323) and cannot load their .NET/GAC
///     dependencies under the Low-IL restricted-token AppContainer
///     (`0xC0000135`, #324). The message names the real install locations
///     and the supported alternative.
///   * Bare `bash` / `sh` → rejected with a clear message: git-bash and
///     busybox link DLLs the Low-IL token cannot load (#324), and they are
///     not in `System32` to begin with.
pub(super) fn resolve_program(program: &str) -> Result<Vec<u16>> {
    if program.is_empty() {
        return Err(SandboxError::ExecFailed("argv[0] is empty".into()));
    }
    if is_unc_or_device_path(program) {
        return Err(SandboxError::ExecFailed(format!(
            "argv[0] {program:?} is a UNC or device path; rejected to prevent \
             NTLM relay / SMB credential disclosure during image load"
        )));
    }
    let p = std::path::Path::new(program);
    if p.is_absolute() {
        match p.try_exists() {
            Ok(true) => {
                let md = p.metadata().map_err(|e| {
                    SandboxError::ExecFailed(format!(
                        "argv[0] {program:?} metadata read failed: {e}"
                    ))
                })?;
                if md.file_type().is_dir() {
                    return Err(SandboxError::ExecFailed(format!(
                        "argv[0] {program:?} is a directory, not an executable"
                    )));
                }
                return Ok(widen_os(p.as_os_str()));
            }
            Ok(false) => {
                return Err(SandboxError::ExecFailed(format!(
                    "argv[0] {program:?} does not exist"
                )));
            }
            Err(e) => {
                return Err(SandboxError::ExecFailed(format!(
                    "argv[0] {program:?} is unreadable: {e}"
                )));
            }
        }
    }
    match classify_bare_shell(program) {
        Some(BareShell::Cmd) => {}
        Some(BareShell::PowerShell) => {
            return Err(SandboxError::ExecFailed(format!(
                "argv[0] {program:?}: PowerShell is not supported under the Windows \
                 AppContainer sandbox. powershell.exe lives in \
                 C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\ and pwsh.exe in \
                 C:\\Program Files\\PowerShell\\7\\ (neither is in System32), and both \
                 require .NET / GAC assemblies that cannot load under the sandbox's \
                 Low-integrity restricted token (they fail with STATUS_DLL_NOT_FOUND \
                 0xC0000135). Use cmd as the sandbox shell, or pass an absolute path to \
                 a sandbox-compatible executable."
            )));
        }
        Some(BareShell::Unsupported) => {
            return Err(SandboxError::ExecFailed(format!(
                "argv[0] {program:?}: this shell is not supported under the Windows \
                 AppContainer sandbox. git-bash requires msys-2.0.dll from \
                 C:\\Program Files\\Git, and even static busybox-w32 links \
                 network/auth/UI DLLs (Secur32, WS2_32, bcrypt, USER32) that cannot \
                 load under the sandbox's Low-integrity restricted token \
                 (STATUS_DLL_NOT_FOUND 0xC0000135). Use cmd as the sandbox shell, or \
                 pass an absolute path to a sandbox-compatible executable."
            )));
        }
        None => {
            return Err(SandboxError::ExecFailed(format!(
                "argv[0] {program:?} is not an absolute path and is not a recognized \
                 sandbox shell. The only bare shell the AppContainer sandbox can run is \
                 cmd (cmd.exe). Pass the absolute path to the executable."
            )));
        }
    }
    // Bare `cmd` / `cmd.exe`: pin to System32\cmd.exe and validate it
    // exists, mirroring the absolute-path branch's existence check so an
    // unresolvable shell yields a descriptive error naming the path rather
    // than a cryptic CreateProcessAsUserW 0x2 at spawn time (#323).
    let sysdir = system_directory()?;
    let mut buf = sysdir;
    if !buf.ends_with(&[b'\\' as u16]) {
        buf.push(b'\\' as u16);
    }
    for u in OsStr::new(program).encode_wide() {
        buf.push(u);
    }
    if !program.to_ascii_lowercase().ends_with(".exe") {
        for u in OsStr::new(".exe").encode_wide() {
            buf.push(u);
        }
    }
    // Validate existence on the widened (NUL-free) path before returning.
    let resolved = std::path::PathBuf::from(std::ffi::OsString::from_wide(&buf));
    match resolved.try_exists() {
        Ok(true) => {}
        Ok(false) => {
            return Err(SandboxError::ExecFailed(format!(
                "argv[0] {program:?} resolved to {} which does not exist",
                resolved.display()
            )));
        }
        Err(e) => {
            return Err(SandboxError::ExecFailed(format!(
//! Live Windows Job-Object hard-containment acceptance — the real Windows
//! counterpart to the Linux-only Bubblewrap `hard_process_containment.rs`.
//!
//! These tests exercise the ACTUAL mechanism the Windows AppContainer sandbox
//! sets up in `windows_impl/process.rs` — a Job Object with
//! `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`, an `ActiveProcessLimit` cap, the
//! breakaway-allow bits cleared (`BREAKAWAY_OK`/`SILENT_BREAKAWAY_OK`), and a
//! `TerminateJobObject` reap of the whole tree on exit/timeout — through the
//! `wcore-sandbox` PUBLIC surface only (`AppContainerBackend::execute` +
//! `SandboxManifest`/`SandboxCommand`). No crate internals and no production
//! test-seam are touched.
//!
//! Every test is `#![cfg(windows)]` + `#[ignore]` and self-qualifies (rather
//! than skips) on `WAYLAND_SANDBOX_LIVE_WINDOWS=1` + `is_available()`, exactly
//! like the native ACL tests in `live_fs_acl.rs`. Off Windows the file compiles
//! to nothing; on non-live hosts the `#[ignore]` keeps them out of the default
//! run. Their empirical green is proven ONLY on the self-hosted AppContainer
//! msvc runner at the 20-25 native-proof gate — this plan authors them
//! construction-only.
//!
//! Falsifiability model (mirrors the bwrap descendant-reaping intent via Job
//! Objects): a detached descendant inherits the child's stdout pipe. If the
//! backend did NOT own and reap the descendant tree, tearing down the direct
//! child would leave that descendant alive holding the pipe, so `execute` would
//! block (drain never reaches EOF) until the manifest timeout, and a
//! host-side liveness query would still find the descendant running. Because
//! the Job Object closes with `KILL_ON_JOB_CLOSE` + `TerminateJobObject`, the
//! whole tree dies promptly and the liveness query finds nothing — so both the
//! wall-clock bound and the explicit "no residue" query are genuine
//! containment assertions, not parent-exit tautologies.

#![cfg(windows)]

use std::process::Command;
use std::sync::atomic::{AtomicU64, Ordering};
use std::time::{Duration, Instant};

use wcore_sandbox::backends::SandboxBackend;
use wcore_sandbox::backends::appcontainer::AppContainerBackend;
use wcore_sandbox::{SandboxCommand, SandboxManifest};

/// The number of authored Job-Object containment acceptance cases. Kept in
/// lockstep with the `#[ignore]`d tests below so a silently-dropped case fails
/// the zero-execution guard rather than shrinking the proof unnoticed.
const NATIVE_CONTAINMENT_CASES: usize = 5;

/// The active-process cap the Windows backend installs on the Job Object,
/// mirrored from `windows_impl/command.rs::SANDBOX_ACTIVE_PROCESS_LIMIT`. It is
/// `pub(super)` (crate-internal), so an integration test cannot import it; the
/// value is duplicated here with this pointer to its source of truth. The cap
/// test asserts a fan-out beyond it is bounded, which fails closed if the two
/// ever drift apart on hardware.
const SANDBOX_ACTIVE_PROCESS_LIMIT: usize = 512;

fn require_live_windows() {
    assert_eq!(
        std::env::var("WAYLAND_SANDBOX_LIVE_WINDOWS").as_deref(),
        Ok("1"),
        "native Job-Object containment acceptance requires WAYLAND_SANDBOX_LIVE_WINDOWS=1"
    );
    assert!(
        AppContainerBackend::new().is_available(),
        "explicit native containment acceptance requires an available AppContainer backend"
    );
}

fn manifest(timeout_secs: u64) -> SandboxManifest {
    SandboxManifest {
        timeout: Some(Duration::from_secs(timeout_secs)),
        ..Default::default()
    }
}

/// `cmd.exe /d /s /c <script>` — the same shell shape the ACL tests use, so the
/// Job Object wraps the identical execution pipeline production drives.
fn cmd_script(script: String) -> SandboxCommand {
    SandboxCommand {
        argv: vec![
            "cmd.exe".into(),
            "/d".into(),
            "/s".into(),
            "/c".into(),
            script,
        ],
        cwd: None,
    }
}

/// A per-run unique token, embedded in a detached descendant's command line so
/// a host-side liveness query can identify EXACTLY this test's tree and nothing
/// else, even under a shared-process runner.
fn unique_tag(label: &str) -> String {
    static COUNTER: AtomicU64 = AtomicU64::new(0);
    let seq = COUNTER.fetch_add(1, Ordering::Relaxed);
    let nanos = std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .map(|d| d.as_nanos())
        .unwrap_or(0);
    format!("wcore_r7_{label}_{}_{seq}_{nanos}", std::process::id())
}

/// Count host processes (UNSANDBOXED — this runs on the host, not in the
/// AppContainer) that are `cmd.exe` AND whose command line carries `tag`.
///
/// The CIM `-Filter "Name='cmd.exe'"` runs at the provider, so the querying
/// `powershell.exe` — whose own command line contains `tag` — is NOT itself a
/// match (its image is `powershell.exe`, not `cmd.exe`). That structurally
/// avoids a self-match without needing to know the query's own PID.
fn tagged_cmd_count(tag: &str) -> usize {
    let out = Command::new("powershell")
        .args([
            "-NoProfile",
            "-NonInteractive",
            "-Command",
            &format!(
                "@(Get-CimInstance Win32_Process -Filter \"Name='cmd.exe'\" | \
                 Where-Object {{ $_.CommandLine -like '*{tag}*' }}).Count"
            ),
        ])
        .output()
        .expect("query host processes via CIM");
    String::from_utf8_lossy(&out.stdout)
        .trim()
        .parse()
        .unwrap_or(0)
}

/// Return the ProcessIds of the `choice.exe` idlers spawned by THIS test's
/// fan-out, scoped to the test's own tagged process tree rather than host-wide
/// by image name.
///
/// A bare `choice.exe` idler cannot carry an injected tag on its own command
/// line (choice rejects the extra argument), but the top-level sandbox `cmd`
/// that runs the fan-out script DOES carry a unique `rem {tag}` — and every
/// `start "" /b choice` idler is a direct child of that cmd, so its
/// `ParentProcessId` is the tagged cmd's `ProcessId`. Selecting only choice
/// processes whose parent is this test's tagged cmd means a concurrent
/// `choice`-spawning target on the same runner (e.g. `live_fs_acl`) cannot
/// pollute the capture.
///
/// This is the ALIVE-phase half of a two-phase reap check: the returned PIDs
/// are captured WHILE the tagged parent cmd is still alive (during the
/// peak-sampling window), and the `ParentProcessId` scope is what makes that
/// capture immune to a concurrent `choice`-spawning target. Once the job closes
/// the tagged parent is dead, so this parent-scoped query would go structurally
/// empty regardless of a leaked survivor — the post-close survivor check is
/// therefore done by fixed ProcessId via [`surviving_captured_choice_pids`],
/// NOT by re-running this parent-scoped query.
fn tagged_choice_descendant_pids(tag: &str) -> Vec<u32> {
    let out = Command::new("powershell")
        .args([
            "-NoProfile",
            "-NonInteractive",
            "-Command",
            &format!(
                "$parents=@(Get-CimInstance Win32_Process -Filter \"Name='cmd.exe'\" | \
                 Where-Object {{ $_.CommandLine -like '*{tag}*' }} | \
                 Select-Object -ExpandProperty ProcessId); \
                 @(Get-CimInstance Win32_Process -Filter \"Name='choice.exe'\" | \
                 Where-Object {{ $parents -contains $_.ParentProcessId }} | \
                 Select-Object -ExpandProperty ProcessId)"
            ),
        ])
        .output()
        .expect("query this test's tagged choice descendant PIDs via CIM");
    String::from_utf8_lossy(&out.stdout)
        .split_whitespace()
        .filter_map(|s| s.parse::<u32>().ok())
        .collect()
}

/// Count how many of the `pids` fan-out `choice.exe` ProcessIds are STILL alive,
/// matched by fixed `ProcessId` intersected with image `choice.exe`.
///
/// This is the POST-CLOSE half of the two-phase reap check. Because it filters
/// on the exact PIDs captured while the tagged parent was alive — not on the
/// now-dead tagged parent, and not host-wide by image name — it is:
///   * non-vacuous — a leaked/orphaned captured `choice` (same PID, still
///     `choice.exe`) is counted, so a survivor stays detectable; and
///   * not host-wide-flaky — a concurrent target's `choice.exe` carries a
///     different, non-captured PID and is excluded.
///
/// An empty `pids` slice yields 0 without issuing a malformed filter.
fn surviving_captured_choice_pids(pids: &[u32]) -> usize {
    if pids.is_empty() {
        return 0;
    }
    let pid_list = pids
        .iter()
        .map(u32::to_string)
        .collect::<Vec<_>>()
        .join(",");
    let out = Command::new("powershell")
        .args([
            "-NoProfile",
            "-NonInteractive",
            "-Command",
            &format!(
                "$pids=@({pid_list}); \
                 @(Get-CimInstance Win32_Process -Filter \"Name='choice.exe'\" | \
                 Where-Object {{ $pids -contains $_.ProcessId }}).Count"
            ),
        ])
        .output()
        .expect("query survival of captured choice PIDs via CIM");
    String::from_utf8_lossy(&out.stdout)
        .trim()
        .parse()
        .unwrap_or(0)
}

/// Poll `predicate` up to `deadline_secs`, panicking with `message` on timeout.
/// Mirrors the `wait_until` helper in `live_fs_acl.rs`.
fn wait_until(mut predicate: impl FnMut() -> bool, deadline_secs: u64, message: &str) {
    let deadline = Instant::now() + Duration::from_secs(deadline_secs);
    while Instant::now() < deadline {
        if predicate() {
            return;
        }
        std::thread::sleep(Duration::from_millis(100));
    }
    panic!("timed out waiting for {message}");
}

/// A stdin-free hold via `choice` (tolerates the sandbox's null stdin, unlike
/// `timeout.exe`) for `seconds`, embeddable in a larger script.
fn choice_hold(seconds: u32) -> String {
    format!("%SystemRoot%\\System32\\choice.exe /T {seconds} /D Y >nul")
}

/// Best-effort host-side cleanup of any residual `choice.exe` this test's
/// fan-out spawned, so a failed assertion cannot leak idlers into later runs.
/// Runs unsandboxed; ignores errors.
fn reap_stray_choice() {
    let _ = Command::new("taskkill")
        .args(["/F", "/IM", "choice.exe", "/T"])
        .output();
}

#[test]
#[ignore = "zero-execution guard for explicit native Windows containment acceptance"]
fn native_containment_gate_marker() {
    require_live_windows();
    assert_eq!(NATIVE_CONTAINMENT_CASES, 5);
}

/// Exit-code fidelity through the Job-Object-wrapped execution on BOTH terminal
/// paths, plus a falsifiable descendant-reaping wall-clock bound.
///
/// The script detaches a 45s `choice` idler (which inherits the child's stdout
/// pipe) and then exits with the declared code. On a backend that owns the
/// descendant tree, the direct child's exit triggers `TerminateJobObject`,
/// which kills the detached idler, EOFs the pipe, and lets `execute` return
/// promptly with the EXACT declared exit code. A non-owning backend would leave
/// the idler holding the pipe, so the drain would block ~45s (well past the 20s
/// bound) or hit the 60s manifest timeout — either way this test FAILS.
#[tokio::test(flavor = "current_thread")]
#[ignore = "explicit native Windows Job-Object containment acceptance"]
async fn contained_detached_child_exit() {
    require_live_windows();
    let backend = AppContainerBackend::new();
    let bound = Duration::from_secs(20);

    for code in [0u8, 7u8] {
        let script = format!("start \"\" /b {} & exit {code}", choice_hold(45));
        let started = Instant::now();
        let out = backend
            .execute(&manifest(60), cmd_script(script))
            .await
            .expect("contained execution must return an exit status, not block or error");
        let elapsed = started.elapsed();
        assert_eq!(
            out.exit_code, code as i32,
            "Job-Object-wrapped execution must report the exact terminal exit code"
        );
        assert!(
            elapsed < bound,
            "exit-{code} path leaked a detached descendant: execute took {elapsed:?} (>= {bound:?})"
        );
    }
    reap_stray_choice();
}

/// KILL_ON_JOB_CLOSE: a detached descendant is reaped with NO residue when the
/// Job Object closes — asserted by an explicit host-side liveness query, not
/// merely by the parent's own exit.
///
/// The parent detaches a tagged `cmd` grandchild that idles 60s, then holds
/// itself alive ~8s so the grandchild can be observed RUNNING mid-flight. When
/// the parent exits, `execute` returns and the Job Object closes: the grandchild
/// — despite its 60s idle still having ~50s to run — must be terminated. If the
/// job did not own it, the grandchild would survive and the post-close query
/// would keep finding it, so `wait_until(count == 0)` would time out and FAIL.
#[tokio::test(flavor = "multi_thread", worker_threads = 2)]
#[ignore = "explicit native Windows Job-Object containment acceptance"]
async fn job_close_reaps_detached_descendant_with_no_residue() {
    require_live_windows();
    let tag = unique_tag("residue");
    // Detached grandchild `cmd` carries `tag` (via a no-op `rem`) then idles 60s;
    // the parent holds ~8s so the grandchild is observable before job close.
    let script = format!(
        "start \"\" /b %ComSpec% /c \"rem {tag} & {}\" & {} & exit /b 0",
        choice_hold(60),
        choice_hold(8),
    );

    let run = tokio::spawn(async move {
        AppContainerBackend::new()
            .execute(&manifest(60), cmd_script(script))
            .await
    });

    // The detached grandchild (and the still-alive parent) carry the tag.
    let observed_tag = tag.clone();
    wait_until(
        || tagged_cmd_count(&observed_tag) >= 1,
        20,
        "detached descendant running before job close",
    );

    let out = run
        .await
        .expect("join contained execution")
        .expect("contained execution returns");
    assert_eq!(out.exit_code, 0, "parent must exit cleanly (exit /b 0)");

    // After the Job Object closes, the detached 60s grandchild must be gone.
    let residue_tag = tag.clone();
    wait_until(
        || tagged_cmd_count(&residue_tag) == 0,
        30,
        "detached descendant reaped with no residue after job close",
    );
    reap_stray_choice();
}

/// ActiveProcessLimit: a fan-out beyond the Job Object's active-process cap is
/// bounded — the job refuses to admit more than the cap of concurrently-live
/// processes, so a runaway fork cannot exceed it.
///
/// The parent attempts to detach `cap + margin` bare `choice` idlers, then holds
/// itself alive so the admitted set is concurrently observable. The host-side
/// image count (baseline-subtracted) must plateau at or below the cap and STAY
/// BELOW the attempted count — proving excess spawns were rejected by the limit,
/// not merely slow to start. An unbounded job would let the delta approach the
/// attempted count and this test would FAIL. After the job closes every idler is
/// reaped back to the baseline.
#[tokio::test(flavor = "multi_thread", worker_threads = 2)]
#[ignore = "explicit native Windows Job-Object containment acceptance"]
async fn active_process_cap_is_enforced() {
    require_live_windows();
    reap_stray_choice();
    let tag = unique_tag("cap");
    let attempts = SANDBOX_ACTIVE_PROCESS_LIMIT + 32;
    // Prefix the script with a `rem {tag}` so the top-level sandbox cmd — the
    // direct parent of every `start "" /b` choice idler — carries this test's
    // unique tag on its command line. The host-side count is then scoped to
    // choice.exe whose ParentProcessId is THIS test's tagged cmd (no host-wide
    // image baseline), so a concurrent choice-spawning target cannot pollute it.
    // Fan out `attempts` detached long idlers, then hold the parent ~25s so the
    // admitted set is concurrently alive and observable, then exit 0 (`exit /b 0`).
    let script = format!(
        "rem {tag} & for /L %i in (1,1,{attempts}) do @start \"\" /b {} & {} & exit /b 0",
        choice_hold(90),
        choice_hold(25),
    );

    let run = tokio::spawn(async move {
        AppContainerBackend::new()
            .execute(&manifest(120), cmd_script(script))
            .await
    });

    // Sample the live PID set (already scoped to this test's tagged tree) while
    // the fan-out is held, tracking its peak and capturing the peak PID set so
    // the post-close reap check can re-verify those exact ProcessIds by fixed
    // PID — the parent-scoped query goes structurally empty once the tagged
    // parent dies at job close.
    let mut peak = 0usize;
    let mut captured_pids: Vec<u32> = Vec::new();
    let watch_deadline = Instant::now() + Duration::from_secs(30);
    while Instant::now() < watch_deadline {
        let pids = tagged_choice_descendant_pids(&tag);
        if pids.len() > peak {
            peak = pids.len();
            captured_pids = pids.clone();
        }
        // Once we have clearly observed a large admitted set we can stop early.
        if pids.len() >= SANDBOX_ACTIVE_PROCESS_LIMIT / 2 {
            // keep sampling briefly to catch any overshoot past the cap
            std::thread::sleep(Duration::from_millis(500));
            let overshoot = tagged_choice_descendant_pids(&tag);
            if overshoot.len() > peak {
                peak = overshoot.len();
                captured_pids = overshoot;
            }
            break;
        }
        std::thread::sleep(Duration::from_millis(250));
    }

    let out = run
        .await
        .expect("join fan-out execution")
        .expect("fan-out execution returns");
    assert_eq!(
        out.exit_code, 0,
        "parent must exit cleanly after the fan-out"
    );

    assert!(
        peak > 0,
        "fan-out never admitted any descendant — the test did not exercise the cap"
    );
    assert!(
        peak <= SANDBOX_ACTIVE_PROCESS_LIMIT,
        "active-process cap breached: observed {peak} concurrent descendants > cap \
         {SANDBOX_ACTIVE_PROCESS_LIMIT}"
    );
    assert!(
        peak < attempts,
        "fan-out was not capped: observed {peak} of {attempts} attempted — the limit \
         admitted every spawn"
    );
    assert!(
        !captured_pids.is_empty(),
        "peak PID set was not captured — the post-close reap check would be vacuous"
    );

    // After the job closes, re-check the EXACT fan-out `choice` PIDs captured
    // while the tagged parent was alive, by fixed ProcessId intersected with
    // image `choice.exe`. This is non-vacuous (a leaked/orphaned captured
    // survivor is still counted) and not host-wide-flaky (a concurrent target's
    // `choice.exe` carries a different, non-captured PID). The parent-scoped
    // query cannot be reused — the tagged parent is dead once `run.await`
    // returned.
    wait_until(
        || surviving_captured_choice_pids(&captured_pids) == 0,
        30,
        "fan-out descendants reaped after job close (by captured PID)",
    );
    reap_stray_choice();
}

/// Breakaway denial: with `BREAKAWAY_OK`/`SILENT_BREAKAWAY_OK` cleared, a
/// detached descendant CANNOT escape the Job Object — it is reaped on job close
/// rather than surviving independently of the parent.
///
/// The parent detaches two tagged `cmd` idlers (each idling 60s) — the shape a
/// process would use to outlive its parent — then holds ~8s so both are
/// observed alive. On job close both must die: if breakaway were permitted, a
/// detached idler would survive the ~52s remainder of its idle and the
/// post-close query would still find it, failing `wait_until(count == 0)`.
#[tokio::test(flavor = "multi_thread", worker_threads = 2)]
#[ignore = "explicit native Windows Job-Object containment acceptance"]
async fn breakaway_is_denied() {
    require_live_windows();
    let tag = unique_tag("breakaway");
    let script = format!(
        "start \"\" /b %ComSpec% /c \"rem {tag} & {hold}\" & \
         start \"\" /b %ComSpec% /c \"rem {tag} & {hold}\" & {parent} & exit /b 0",
        tag = tag,
        hold = choice_hold(60),
        parent = choice_hold(8),
    );

    let run = tokio::spawn(async move {
        AppContainerBackend::new()
            .execute(&manifest(60), cmd_script(script))
            .await
    });

    let observed_tag = tag.clone();
    wait_until(
        || tagged_cmd_count(&observed_tag) >= 2,
        20,
        "both detached breakaway candidates running before job close",
    );

    let out = run
        .await
        .expect("join contained execution")
        .expect("contained execution returns");
    assert_eq!(out.exit_code, 0, "parent must exit cleanly (exit /b 0)");

    // No detached child broke away: the job reaped both on close.
    let residue_tag = tag.clone();
    wait_until(
        || tagged_cmd_count(&residue_tag) == 0,
        30,
        "no detached child broke away from the Job Object",
    );
    reap_stray_choice();
}

/// Hard-containment preflight: the Windows AppContainer backend self-reports
/// hard descendant containment (Job Object ownership), so the qualification the
/// other native containment targets rely on is REAL on Windows — and a live
/// benign contained execution actually runs.
///
/// This is the Windows analogue of the bwrap `qualified_hard_containment_backend_preflight`:
/// it asserts the backend's admission properties through the public trait
/// (`owns_descendants_hard` / `enforces_read_deny` / `blocks_powershell`) and
/// then drives one benign contained command to confirm the Job-Object pipeline
/// is live end to end.
#[tokio::test(flavor = "current_thread")]
#[ignore = "explicit native Windows Job-Object containment acceptance"]
async fn qualified_hard_containment_backend_preflight() {
    require_live_windows();
    let backend = AppContainerBackend::new();

    // Admission properties: only a backend that owns the descendant tree (via the
    // Job Object) may back delegated hard-containment execution on Windows.
    assert!(
        backend.owns_descendants_hard(),
        "the Windows AppContainer backend must own the descendant process tree (Job Object)"
    );
    assert!(
        backend.enforces_read_deny(),
        "the Windows AppContainer backend must enforce fs_read_deny at the OS layer"

hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk git show f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs | sed -n '300,760p'
rtk git grep -n \"resolved_program_is_cmd\\|quote_cmd_payload\\|exit /b 0\\|DispatchAdmission\" f0dd5b6d312af616f268f96f34c3bc9fc962c4d2 -- crates | head -160" in /Users/seandonahoe/dev/waylandcore-ferrox
hook: PostToolUse
hook: PostToolUse Completed
 succeeded in 2961ms:
        Some(BareShell::Cmd) => {}
        Some(BareShell::PowerShell) => {
            return Err(SandboxError::ExecFailed(format!(
                "argv[0] {program:?}: PowerShell is not supported under the Windows \
                 AppContainer sandbox. powershell.exe lives in \
                 C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\ and pwsh.exe in \
                 C:\\Program Files\\PowerShell\\7\\ (neither is in System32), and both \
                 require .NET / GAC assemblies that cannot load under the sandbox's \
                 Low-integrity restricted token (they fail with STATUS_DLL_NOT_FOUND \
                 0xC0000135). Use cmd as the sandbox shell, or pass an absolute path to \
                 a sandbox-compatible executable."
            )));
        }
        Some(BareShell::Unsupported) => {
            return Err(SandboxError::ExecFailed(format!(
                "argv[0] {program:?}: this shell is not supported under the Windows \
                 AppContainer sandbox. git-bash requires msys-2.0.dll from \
                 C:\\Program Files\\Git, and even static busybox-w32 links \
                 network/auth/UI DLLs (Secur32, WS2_32, bcrypt, USER32) that cannot \
                 load under the sandbox's Low-integrity restricted token \
                 (STATUS_DLL_NOT_FOUND 0xC0000135). Use cmd as the sandbox shell, or \
                 pass an absolute path to a sandbox-compatible executable."
            )));
        }
        None => {
            return Err(SandboxError::ExecFailed(format!(
                "argv[0] {program:?} is not an absolute path and is not a recognized \
                 sandbox shell. The only bare shell the AppContainer sandbox can run is \
                 cmd (cmd.exe). Pass the absolute path to the executable."
            )));
        }
    }
    // Bare `cmd` / `cmd.exe`: pin to System32\cmd.exe and validate it
    // exists, mirroring the absolute-path branch's existence check so an
    // unresolvable shell yields a descriptive error naming the path rather
    // than a cryptic CreateProcessAsUserW 0x2 at spawn time (#323).
    let sysdir = system_directory()?;
    let mut buf = sysdir;
    if !buf.ends_with(&[b'\\' as u16]) {
        buf.push(b'\\' as u16);
    }
    for u in OsStr::new(program).encode_wide() {
        buf.push(u);
    }
    if !program.to_ascii_lowercase().ends_with(".exe") {
        for u in OsStr::new(".exe").encode_wide() {
            buf.push(u);
        }
    }
    // Validate existence on the widened (NUL-free) path before returning.
    let resolved = std::path::PathBuf::from(std::ffi::OsString::from_wide(&buf));
    match resolved.try_exists() {
        Ok(true) => {}
        Ok(false) => {
            return Err(SandboxError::ExecFailed(format!(
                "argv[0] {program:?} resolved to {} which does not exist",
                resolved.display()
            )));
        }
        Err(e) => {
            return Err(SandboxError::ExecFailed(format!(
                "argv[0] {program:?} resolved to {} which is unreadable: {e}",
                resolved.display()
            )));
        }
    }
    buf.push(0);
    Ok(buf)
}

/// Returns `C:\Windows\System32` as UTF-16 without the trailing NUL.
/// Query a child process's token integrity level (RID of the last
/// sub-authority on the `TokenIntegrityLevel` SID). Returns
/// `SECURITY_MANDATORY_LOW_RID = 0x1000` for a properly-pinned
/// AppContainer child. Used as a runtime invariant check
/// post-spawn — OS-layer proof that the kernel honored our
/// explicit `SetTokenInformation(IntegrityLevel=Low)` call before
/// image load.
pub(super) unsafe fn query_process_integrity_rid(process_handle: HANDLE) -> Result<u32> {
    let mut token: HANDLE = ptr::null_mut();
    if unsafe { OpenProcessToken(process_handle, TOKEN_QUERY, &mut token) } == 0 {
        return Err(SandboxError::ExecFailed(format!(
            "OpenProcessToken(child, TOKEN_QUERY): {:#x}",
            unsafe { GetLastError() }
        )));
    }
    let _token_guard = OwnedHandle::new(token);

    let mut needed: u32 = 0;
    // Sizing probe (ignored return — we look at `needed`).
    let _ =
        unsafe { GetTokenInformation(token, TokenIntegrityLevel, ptr::null_mut(), 0, &mut needed) };
    if needed == 0 {
        return Err(SandboxError::ExecFailed(format!(
            "GetTokenInformation sizing: {:#x}",
            unsafe { GetLastError() }
        )));
    }
    let mut buf: Vec<u8> = vec![0u8; needed as usize];
    if unsafe {
        GetTokenInformation(
            token,
            TokenIntegrityLevel,
            buf.as_mut_ptr() as _,
            needed,
            &mut needed,
        )
    } == 0
    {
        return Err(SandboxError::ExecFailed(format!(
            "GetTokenInformation: {:#x}",
            unsafe { GetLastError() }
        )));
    }
    let label = unsafe { &*(buf.as_ptr() as *const TOKEN_MANDATORY_LABEL) };
    let sid = label.Label.Sid;
    if sid.is_null() {
        return Err(SandboxError::ExecFailed("integrity SID is null".into()));
    }
    let count_ptr = unsafe { GetSidSubAuthorityCount(sid as _) };
    if count_ptr.is_null() {
        return Err(SandboxError::ExecFailed(
            "GetSidSubAuthorityCount returned null".into(),
        ));
    }
    let count = unsafe { *count_ptr };
    if count == 0 {
        return Err(SandboxError::ExecFailed(
            "integrity SID has no sub-authorities".into(),
        ));
    }
    let rid_ptr = unsafe { GetSidSubAuthority(sid as _, (count - 1) as u32) };
    if rid_ptr.is_null() {
        return Err(SandboxError::ExecFailed(
            "GetSidSubAuthority returned null".into(),
        ));
    }
    Ok(unsafe { *rid_ptr })
}

/// `SECURITY_MANDATORY_LOW_RID` from the SDK — the SubAuthority of
/// the Low Integrity Level SID (`S-1-16-4096`).
pub(super) const SECURITY_MANDATORY_LOW_RID: u32 = 0x1000;

/// Job Object `ActiveProcessLimit` — the maximum number of concurrently
/// active processes in the sandbox job (#321, #322). High enough for a
/// shell plus a parallel build's worker processes, low enough to bound a
/// runaway fork. This is NOT the primary fork-bomb guard: `KILL_ON_JOB_CLOSE`
/// plus the optional per-process memory cap are. It is a defense-in-depth
/// ceiling.
///
/// The previous value of 1 was the root cause of BOTH #322 (the cap itself)
/// AND #321 (reported as "AppContainer cannot spawn child processes — Bash
/// runs only cmd builtins"). cmd.exe is process #1 in the job and is
/// assigned to the job before `ResumeThread`; with a cap of 1 every child
/// it tries to launch is process #2, which the kernel denies before the
/// child's image runs. cmd builtins (`echo`, `dir`) work because they
/// execute IN cmd's process; external programs (`git`, `node`, even
/// `cmd /c <abs cmd.exe> /c exit 42`) fail at the fork. #321's restricted-
/// token/CSRSS theory does not hold: the spawn is rejected by the job cap
/// before the child token is ever used, and the restricted token is left
/// untouched here so the `live_integrity.rs` boundary assertions still hold.
pub(super) const SANDBOX_ACTIVE_PROCESS_LIMIT: u32 = 512;

pub(super) fn system_directory() -> Result<Vec<u16>> {
    let needed = unsafe { GetSystemDirectoryW(ptr::null_mut(), 0) };
    if needed == 0 {
        return Err(SandboxError::ExecFailed(format!(
            "GetSystemDirectoryW probe: {:#x}",
            unsafe { GetLastError() }
        )));
    }
    let mut buf: Vec<u16> = vec![0u16; needed as usize];
    let written = unsafe { GetSystemDirectoryW(buf.as_mut_ptr(), buf.len() as u32) };
    if written == 0 || written as usize >= buf.len() {
        return Err(SandboxError::ExecFailed(format!(
            "GetSystemDirectoryW: written={} buf={} last_err={:#x}",
            written,
            buf.len(),
            unsafe { GetLastError() }
        )));
    }
    buf.truncate(written as usize);
    Ok(buf)
}

/// Build a double-null-terminated UTF-16 env block from `(K, V)` pairs.
///
/// Per the `CREATE_UNICODE_ENVIRONMENT` contract, the block must be
/// sorted alphabetically by key (case-insensitively on Windows, since
/// the OS treats env keys as case-insensitive). Duplicate keys are
/// collapsed last-wins so manifest-supplied vars override the parent's
/// seeded values; **the retained key casing is the LAST insert's casing**,
/// which on Windows is harmless (case-insensitive lookups) but operators
/// reading the trace logs will see whatever case the manifest used.
///
/// Validation rejects:
///   * Empty key.
///   * `=` or any ASCII control char (`< 0x20`) or NUL in keys — these
///     break the `K=V\0` framing AND open log-injection via `tracing::trace!`
///     emission of the key.
///   * NUL in values (kernel framing).
///   * Newline / CR / TAB in values of security-relevant keys (PATH,
///     COMSPEC, PATHEXT, SYSTEMROOT, WINDIR) — downstream parsers (cmd.exe
///     `set` output, `[Environment]::GetEnvironmentVariables()`) split on
///     LF and would treat injected content as additional entries.
pub(super) fn build_env_block(pairs: &[(String, String)]) -> Result<Vec<u16>> {
    let mut map: BTreeMap<String, (String, String)> = BTreeMap::new();
    for (k, v) in pairs {
        if k.is_empty() {
            return Err(SandboxError::ExecFailed("env key is empty".into()));
        }
        if k.chars()
            .any(|c| c == '=' || c == '\0' || (c as u32) < 0x20)
        {
            return Err(SandboxError::ExecFailed(format!(
                "env key {k:?} contains '=' or a control character or NUL"
            )));
        }
        if v.contains('\0') {
            return Err(SandboxError::ExecFailed(format!(
                "env value for {k:?} contains NUL"
            )));
        }
        let upper_k = k.to_ascii_uppercase();
        if matches!(
            upper_k.as_str(),
            "PATH" | "COMSPEC" | "PATHEXT" | "SYSTEMROOT" | "WINDIR"
        ) && v.chars().any(|c| matches!(c, '\n' | '\r' | '\t'))
        {
            return Err(SandboxError::ExecFailed(format!(
                "env value for security-relevant key {k:?} contains a newline or tab"
            )));
        }
        map.insert(upper_k, (k.clone(), v.clone()));
    }
    let mut block: Vec<u16> = Vec::with_capacity(pairs.len() * 32);
    for (k, v) in map.values() {
        for u in OsStr::new(k).encode_wide() {
            block.push(u);
        }
        block.push(b'=' as u16);
        for u in OsStr::new(v).encode_wide() {
            block.push(u);
        }
        block.push(0);
    }
    block.push(0);
    if block.len() == 1 {
        block.push(0);
    }
    Ok(block)
}

/// Vars whose VALUES are safe to print in trace logs. Everything outside
/// this list — especially anything caller-supplied via `manifest.env` —
/// gets its value redacted as `<{len} bytes redacted>` because the
/// manifest is the project's explicit secret-bearing surface (e.g.
/// `AWS_SECRET_ACCESS_KEY`, `*_TOKEN`, `*_KEY`).
pub(super) fn is_trace_safe_env_key(k: &str) -> bool {
    matches!(
        k.to_ascii_uppercase().as_str(),
        "ALLUSERSPROFILE"
            | "APPDATA"
            | "COMMONPROGRAMFILES"
            | "COMMONPROGRAMFILES(X86)"
            | "COMMONPROGRAMW6432"
            | "COMSPEC"
            | "HOMEDRIVE"
            | "HOMEPATH"
            | "LOCALAPPDATA"
            | "NUMBER_OF_PROCESSORS"
            | "PATH"
            | "PATHEXT"
            | "PROCESSOR_ARCHITECTURE"
            | "PROCESSOR_ARCHITEW6432"
            | "PROGRAMDATA"
            | "PROGRAMFILES"
            | "PROGRAMFILES(X86)"
            | "PROGRAMW6432"
            | "PUBLIC"
            | "SYSTEMDRIVE"
            | "SYSTEMROOT"
            | "TEMP"
            | "TMP"
            | "USERDOMAIN"
            | "USERNAME"
            | "USERPROFILE"
            | "WINDIR"
    )
}
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-fRi2ZNfC' (errno=Operation not permitted)
2026-07-24 14:41:29.283 xcodebuild[96028:161658696]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:41:29.381 xcodebuild[96028:161658691] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-69ZVXFie' (errno=Operation not permitted)
2026-07-24 14:41:29.747 xcodebuild[96036:161658738]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:41:29.862 xcodebuild[96036:161658731] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-config/src/shell/executable_readiness_tests.rs:710:        std::fs::write(temp.path().join(program), "@echo off\r\nexit /b 0\r\n").unwrap();
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs:129:pub(super) fn quote_cmd_payload(payload: &str) -> String {
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs:146:pub(super) fn resolved_program_is_cmd(app_name_w: &[u16]) -> bool {
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs:620:            let cmd_payload_idx = if resolved_program_is_cmd(&app_name_w) {
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs:637:                        quote_cmd_payload(a)
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/tests/hard_process_containment_windows.rs:302:        "start \"\" /b %ComSpec% /c \"rem {tag} & {}\" & {} & exit /b 0",
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/tests/hard_process_containment_windows.rs:325:    assert_eq!(out.exit_code, 0, "parent must exit cleanly (exit /b 0)");
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/tests/hard_process_containment_windows.rs:361:    // admitted set is concurrently alive and observable, then exit 0 (`exit /b 0`).
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/tests/hard_process_containment_windows.rs:363:        "rem {tag} & for /L %i in (1,1,{attempts}) do @start \"\" /b {} & {} & exit /b 0",
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/tests/hard_process_containment_windows.rs:461:         start \"\" /b %ComSpec% /c \"rem {tag} & {hold}\" & {parent} & exit /b 0",
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/tests/hard_process_containment_windows.rs:484:    assert_eq!(out.exit_code, 0, "parent must exit cleanly (exit /b 0)");
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/tests/hard_process_containment_windows.rs:542:        "start \"\" /b %ComSpec% /c \"rem {tag} & {}\" & {} & exit /b 0",
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/tests/live_fs_acl.rs:122:    // stdin), then force a deterministic exit 0 with `exit /b 0`. NOT `ver`:
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/tests/live_fs_acl.rs:126:    // 20-32. `exit /b 0` actually SETS the process exit code to 0, so the
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/tests/live_fs_acl.rs:132:        "type \"{}\" && (%SystemRoot%\\System32\\choice.exe /T {seconds} /D Y >nul & exit /b 0)",
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/tests/live_fs_acl.rs:138:    // `echo %TEMP%` then the stdin-free `choice` hold, then `exit /b 0` to force a
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/tests/live_fs_acl.rs:143:        "echo %TEMP% & %SystemRoot%\\System32\\choice.exe /T {seconds} /D Y >nul & exit /b 0"
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/tests/live_fs_acl.rs:384:    // `type && … & exit /b 0`), not `choice`'s 1-based selection index. Gate on
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/error.rs:14:    DispatchAdmission(String),
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/lib.rs:119:            SwarmError::DispatchAdmission("repository dispatch registry is unavailable".into())
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/lib.rs:140:            .ok_or_else(|| SwarmError::DispatchAdmission("worker count overflowed".into()))?;
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/lib.rs:142:            return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/lib.rs:147:            return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/lib.rs:156:                SwarmError::DispatchAdmission("worker stream count overflowed".into())
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/lib.rs:161:            return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/lib.rs:275:    /// [`SwarmError::DispatchAdmission`] if this request would exceed the
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/lib.rs:293:            SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/lib.rs:342:            SwarmError::DispatchAdmission("terminal heartbeat registry is unavailable".into())
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/lib.rs:443:            Err(SwarmError::DispatchAdmission(_))
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/lib.rs:447:            Err(SwarmError::DispatchAdmission(_))
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/lib.rs:451:            Err(SwarmError::DispatchAdmission(_))
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/lib.rs:463:        assert!(matches!(error, SwarmError::DispatchAdmission(_)));
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/lib.rs:476:        assert!(matches!(error, SwarmError::DispatchAdmission(_)));
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/lib.rs:488:        assert!(matches!(error, SwarmError::DispatchAdmission(_)));
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree.rs:462:        .map_err(|error| SwarmError::DispatchAdmission(error.to_string()))?;
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree.rs:465:        .map_err(|error| SwarmError::DispatchAdmission(error.to_string()))?;
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree.rs:468:        .map_err(|error| SwarmError::DispatchAdmission(error.to_string()))?;
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree.rs:470:        SwarmError::DispatchAdmission(format!("invalid workspace reservation: {}", path.display()))
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree.rs:473:        return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree.rs:488:        .map_err(|error| SwarmError::DispatchAdmission(error.to_string()))?;
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree.rs:498:        .map_err(|error| SwarmError::DispatchAdmission(error.to_string()))?;
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree.rs:500:        SwarmError::DispatchAdmission("invalid retained workspace reservation".to_owned())
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree.rs:503:        return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree.rs:525:            return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree.rs:533:            return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree.rs:539:            .map_err(|error| SwarmError::DispatchAdmission(error.to_string()))?
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree.rs:547:                        SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree.rs:553:                            SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree.rs:565:                    return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree.rs:611:            return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:142:            return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:155:                return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:164:            return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:176:            return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:184:            return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:198:            return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:208:            return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:217:            return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:229:            return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:262:            return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:284:                        return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:293:                        return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:308:    SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:314:    SwarmError::DispatchAdmission(error.to_string())
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:355:        SwarmError::DispatchAdmission("isolated checkout is missing HEAD".to_owned())
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:360:            SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:450:                SwarmError::DispatchAdmission("source manifest entry count overflowed".to_owned())
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:453:                return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:478:                        SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:522:        return SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:636:    SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree/candidate.rs:642:    SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_cleanup.rs:84:                    SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_cleanup.rs:98:                        return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:94:            .map_err(|error| SwarmError::DispatchAdmission(error.to_string()))?;
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:129:            .map_err(|error| SwarmError::DispatchAdmission(error.to_string()))?;
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:217:                SwarmError::DispatchAdmission("retained worktree count overflowed".into())
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:314:            return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:327:            SwarmError::DispatchAdmission("active worker count exceeds u64".to_owned())
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:338:                SwarmError::DispatchAdmission("dispatch workspace capacity overflowed".to_owned())
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:341:            return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:349:            return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:374:            return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:393:            return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:402:                SwarmError::DispatchAdmission("workspace capacity probe returned no data".into())
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:409:                SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:415:                SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:420:            SwarmError::DispatchAdmission("workspace capacity probe overflowed".into())
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:444:            return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:453:                SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:461:        Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:616:            return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:631:                    return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:639:                    return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:656:                            .map_err(|error| SwarmError::DispatchAdmission(error.to_string()))?,
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:852:            return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:923:            return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:938:                    return Err(SwarmError::DispatchAdmission(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:946:                    return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:963:                            .map_err(|error| SwarmError::DispatchAdmission(error.to_string()))?,
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_manager.rs:1185:            return Err(SwarmError::DispatchAdmission(format!(
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_security.rs:33:            .map_err(|error| SwarmError::DispatchAdmission(error.to_string()))
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_security.rs:39:            .map_err(|error| SwarmError::DispatchAdmission(error.to_string()))
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-swarm/src/worktree_security.rs:54:            .map_err(|error| SwarmError::DispatchAdmission(error.to_string()))

hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk git show f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs | sed -n '560,680p'
rtk git grep -n -A18 -B8 \"resolved_program_is_cmd\" f0dd5b6d312af616f268f96f34c3bc9fc962c4d2 -- crates/wcore-sandbox/src | tail -220
rtk git diff --word-diff=porcelain 3f839309574d6741eed416cd3820f56447f74eba..f0dd5b6d312af616f268f96f34c3bc9fc962c4d2 -- crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs crates/wcore-sandbox/tests/hard_process_containment_windows.rs | sed -n '1,360p'" in /Users/seandonahoe/dev/waylandcore-ferrox
hook: PostToolUse
hook: PostToolUse Completed
 succeeded in 5708ms:
                ));
            }
            let mut attr_buf: Vec<u8> = vec![0u8; attr_size];
            let attr_list: LPPROC_THREAD_ATTRIBUTE_LIST = attr_buf.as_mut_ptr() as _;
            if InitializeProcThreadAttributeList(attr_list, 2, 0, &mut attr_size) == 0 {
                return Err(SandboxError::ExecFailed(format!(
                    "InitializeProcThreadAttributeList: {:#x}",
                    GetLastError()
                )));
            }
            let _attr_guard = AttrListGuard { list: attr_list };

            if UpdateProcThreadAttribute(
                attr_list,
                0,
                PROC_THREAD_ATTRIBUTE_SECURITY_CAPABILITIES as usize,
                &mut sec_caps as *mut _ as _,
                mem::size_of::<SECURITY_CAPABILITIES>(),
                ptr::null_mut(),
                ptr::null(),
            ) == 0
            {
                return Err(SandboxError::ExecFailed(format!(
                    "UpdateProcThreadAttribute(SECURITY_CAPABILITIES): {:#x}",
                    GetLastError()
                )));
            }
            if UpdateProcThreadAttribute(
                attr_list,
                0,
                PROC_THREAD_ATTRIBUTE_HANDLE_LIST as usize,
                handle_list.as_mut_ptr() as *mut _,
                mem::size_of::<HANDLE>() * handle_list.len(),
                ptr::null_mut(),
                ptr::null(),
            ) == 0
            {
                return Err(SandboxError::ExecFailed(format!(
                    "UpdateProcThreadAttribute(HANDLE_LIST): {:#x}",
                    GetLastError()
                )));
            }

            // ---- 7. STARTUPINFOEXW ----
            let mut sinfo: STARTUPINFOEXW = mem::zeroed();
            sinfo.StartupInfo.cb = mem::size_of::<STARTUPINFOEXW>() as u32;
            sinfo.StartupInfo.dwFlags = STARTF_USESTDHANDLES;
            sinfo.StartupInfo.hStdInput = std::ptr::null_mut();
            sinfo.StartupInfo.hStdOutput = stdout_w.as_raw();
            sinfo.StartupInfo.hStdError = stderr_w.as_raw();
            sinfo.lpAttributeList = attr_list;

            // ---- 8. Command line + env block ----
            // When the resolved program is cmd.exe, its `/c`/`/k` payload must be
            // quoted for cmd's RAW-command-line re-read (single outer pair, inner
            // quotes verbatim), NOT with the MSVC CRT `\"` escaping quote_arg
            // emits — cmd /s strips only the outer pair and would otherwise run
            // the backslash-escaped quotes literally (`type \"path\"` ->
            // ERROR_INVALID_NAME). Every other argv entry keeps the CRT quoting.
            // Quoting-layer only: the token / Job / ACL boundary is unchanged.
            let cmd_payload_idx = if resolved_program_is_cmd(&app_name_w) {
                cmd.argv
                    .iter()
                    .position(|a| {
                        let flag = a.to_ascii_lowercase();
                        flag == "/c" || flag == "/k"
                    })
                    .map(|flag_idx| flag_idx + 1)
            } else {
                None
            };
            let cmdline: String = cmd
                .argv
                .iter()
                .enumerate()
                .map(|(idx, a)| {
                    if Some(idx) == cmd_payload_idx {
                        quote_cmd_payload(a)
                    } else {
                        quote_arg(a)
                    }
                })
                .collect::<Vec<_>>()
                .join(" ");
            let mut cmdline_w: Vec<u16> = widen(&cmdline);

            let mut env_pairs: Vec<(String, String)> = Vec::new();
            for key in [
                "SYSTEMROOT",
                "WINDIR",
                "COMSPEC",
                "PATH",
                "PATHEXT",
                "PROCESSOR_ARCHITECTURE",
                "USERPROFILE",
                "APPDATA",
                "LOCALAPPDATA",
                "TEMP",
                "TMP",
                "USERNAME",
                "USERDOMAIN",
                "HOMEDRIVE",
                "HOMEPATH",
                "PROCESSOR_ARCHITEW6432",
                "NUMBER_OF_PROCESSORS",
                "ALLUSERSPROFILE",
                "PROGRAMDATA",
                "PROGRAMFILES",
                "PROGRAMFILES(X86)",
                "PROGRAMW6432",
                "COMMONPROGRAMFILES",
                "COMMONPROGRAMFILES(X86)",
                "COMMONPROGRAMW6432",
                "PUBLIC",
                "SYSTEMDRIVE",
            ] {
                if let Ok(val) = std::env::var(key) {
                    env_pairs.push((key.to_string(), val));
                }
            }
            // Remap TEMP/TMP to AppContainer-writable storage. If
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-aIOAvL8f' (errno=Operation not permitted)
2026-07-24 14:41:41.000 xcodebuild[96645:161660265]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:41:41.102 xcodebuild[96645:161660264] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-NWjJbFgH' (errno=Operation not permitted)
2026-07-24 14:41:41.474 xcodebuild[96651:161660286]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:41:41.575 xcodebuild[96651:161660285] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-138-/// returned by [`resolve_program`]) has `cmd.exe` as its exact FINAL PATH
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-139-/// COMPONENT (case-insensitive) — NOT a bare suffix, so a name like
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-140-/// `notcmd.exe`/`foocmd.exe` does not match. Used to decide whether the
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-141-/// `/c`/`/k` payload needs the cmd-aware quoting above rather than the general
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-142-/// CRT [`quote_arg`] rules. Matching the RESOLVED program (not the raw
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-143-/// `argv[0]`) is deliberate: bare `cmd`/`cmd.exe` is pinned to
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-144-/// `System32\cmd.exe` and an absolute path is validated as-is, so this reflects
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-145-/// the actual image that will parse the command line.
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs:146:pub(super) fn resolved_program_is_cmd(app_name_w: &[u16]) -> bool {
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-147-    let end = app_name_w
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-148-        .iter()
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-149-        .position(|&c| c == 0)
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-150-        .unwrap_or(app_name_w.len());
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-151-    // Windows `Path` treats both `\` and `/` as separators, so `file_name`
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-152-    // isolates the true final component of the `System32\cmd.exe` resolution or
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-153-    // any absolute path the caller passed; equality (not a suffix) rejects
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-154-    // `notcmd.exe`.
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-155-    let decoded = String::from_utf16_lossy(&app_name_w[..end]).to_ascii_lowercase();
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-156-    std::path::Path::new(&decoded)
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-157-        .file_name()
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-158-        .and_then(|name| name.to_str())
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-159-        .is_some_and(|name| name == "cmd.exe")
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-160-}
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-161-
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-162-/// Classification of a bare (non-absolute) `argv[0]`. Only `cmd` is
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-163-/// runnable under the Low-integrity restricted-token AppContainer; every
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs-164-/// other shell is recognized solely so the resolver can return a clear,
--
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-612-            // ---- 8. Command line + env block ----
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-613-            // When the resolved program is cmd.exe, its `/c`/`/k` payload must be
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-614-            // quoted for cmd's RAW-command-line re-read (single outer pair, inner
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-615-            // quotes verbatim), NOT with the MSVC CRT `\"` escaping quote_arg
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-616-            // emits — cmd /s strips only the outer pair and would otherwise run
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-617-            // the backslash-escaped quotes literally (`type \"path\"` ->
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-618-            // ERROR_INVALID_NAME). Every other argv entry keeps the CRT quoting.
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-619-            // Quoting-layer only: the token / Job / ACL boundary is unchanged.
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs:620:            let cmd_payload_idx = if resolved_program_is_cmd(&app_name_w) {
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-621-                cmd.argv
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-622-                    .iter()
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-623-                    .position(|a| {
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-624-                        let flag = a.to_ascii_lowercase();
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-625-                        flag == "/c" || flag == "/k"
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-626-                    })
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-627-                    .map(|flag_idx| flag_idx + 1)
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-628-            } else {
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-629-                None
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-630-            };
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-631-            let cmdline: String = cmd
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-632-                .argv
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-633-                .iter()
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-634-                .enumerate()
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-635-                .map(|(idx, a)| {
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-636-                    if Some(idx) == cmd_payload_idx {
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-637-                        quote_cmd_payload(a)
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs-638-                    } else {
.../backends/appcontainer/windows_impl/command.rs  |  16 +++-
 .../tests/hard_process_containment_windows.rs      | 106 +++++++++++++++++----
 2 files changed, 100 insertions(+), 22 deletions(-)

--- Changes ---

crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
  @@ -135,7 +135,9 @@ pub(super) fn quote_cmd_payload(payload: &str) -> String {
  -names `cmd.exe`.
  +has `cmd.exe` as its exact FINAL PATH
  ~
  +/// COMPONENT (case-insensitive) — NOT a bare suffix, so a name like
  ~
  +/// `notcmd.exe`/`foocmd.exe` does not match.
    Used to decide whether the
  ~
   /// `/c`/`/k` payload needs the cmd-aware quoting above rather than the general
  ~
   /// CRT [`quote_arg`] rules. Matching the RESOLVED program (not the raw
  ~
   /// `argv[0]`) is deliberate: bare `cmd`/`cmd.exe` is pinned to
  ~
  @@ -146,9 +148,15 @@ pub(super) fn resolved_program_is_cmd(app_name_w: &[u16]) -> bool {
  -String::from_utf16_lossy(&app_name_w[..end])
  ~
  -        .to_ascii_lowercase()
  ~
  -        .ends_with("cmd.exe")
  +// Windows `Path` treats both `\` and `/` as separators, so `file_name`
  ~
  +    // isolates the true final component of the `System32\cmd.exe` resolution or
  ~
  +    // any absolute path the caller passed; equality (not a suffix) rejects
  ~
  +    // `notcmd.exe`.
  ~
  +    let decoded = String::from_utf16_lossy(&app_name_w[..end]).to_ascii_lowercase();
  ~
  +    std::path::Path::new(&decoded)
  ~
  +        .file_name()
  ~
  +        .and_then(|name| name.to_str())
  ~
  +        .is_some_and(|name| name == "cmd.exe")
  ~
   }
  ~
   
  ~
   /// Classification of a bare (non-absolute) `argv[0]`. Only `cmd` is
  ~
  +12 -4

crates/wcore-sandbox/tests/hard_process_containment_windows.rs
  @@ -125,19 +125,28 @@ fn tagged_cmd_count(tag: &str) -> usize {
  -Count
  +Return the ProcessIds of
    the `choice.exe` idlers spawned by THIS test's
  ~
  +///
    fan-out, scoped to the
  -///
    test's own tagged process tree rather than host-wide
  ~
  +///
    by image name.
  ~
   ///
  ~
   /// A bare `choice.exe` idler cannot carry an injected tag on its own command
  ~
   /// line (choice rejects the extra argument), but the top-level sandbox `cmd`
  ~
   /// that runs the fan-out script DOES carry a unique `rem {tag}` — and every
  ~
   /// `start "" /b choice` idler is a direct child of that cmd, so its
  ~
   /// `ParentProcessId` is the tagged cmd's `ProcessId`. 
  -Counting
  +Selecting
    only choice
  ~
   /// processes whose parent is this test's tagged cmd means a concurrent
  ~
   /// `choice`-spawning target on the same runner (e.g. `live_fs_acl`) cannot
  ~
   /// pollute the 
  -count
  +capture.
  ~
  +///
  ~
  +/// This is the ALIVE-phase half of a two-phase reap check: the returned PIDs
  ~
  +/// are captured WHILE the tagged parent cmd is still alive (during the
  ~
  +/// peak-sampling window), and the `ParentProcessId` scope is what makes that
  ~
  +/// capture immune to a concurrent `choice`-spawning target. Once the job closes
  ~
  +/// the tagged parent is dead, so this parent-scoped query would go structurally
  ~
  +/// empty regardless of a leaked survivor
    — 
  -closing the host-wide-image-count flake (watch-item d2)
  ~
  -/// without weakening any containment assertion.
  +the post-close survivor check is
  ~
  +/// therefore done by fixed ProcessId via [`surviving_captured_choice_pids`],
  ~
  +/// NOT by re-running this parent-scoped query.
  ~
   fn 
  -tagged_choice_descendant_count(tag:
  +tagged_choice_descendant_pids(tag:
    &str) -> 
  -usize
  +Vec<u32>
    {
  ~
       let out = Command::new("powershell")
  ~
           .args([
  ~
               "-NoProfile",
  ~
  @@ -148,11 +157,52 @@ fn tagged_choice_descendant_count(tag: &str) -> usize {
  +}} | \
  ~
  +                 Select-Object -ExpandProperty ProcessId)"
  ~
  +            ),
  ~
  +        ])
  ~
  +        .output()
  ~
  +        .expect("query this test's tagged choice descendant PIDs via CIM");
  ~
  +    String::from_utf8_lossy(&out.stdout)
  ~
  +        .split_whitespace()
  ~
  +        .filter_map(|s| s.parse::<u32>().ok())
  ~
  +        .collect()
  ~
  +}
  ~
  ~
  +/// Count how many of the `pids` fan-out `choice.exe` ProcessIds are STILL alive,
  ~
  +/// matched by fixed `ProcessId` intersected with image `choice.exe`.
  ~
  +///
  ~
  +/// This is the POST-CLOSE half of the two-phase reap check. Because it filters
  ~
  +/// on the exact PIDs captured while the tagged parent was alive — not on the
  ~
  +/// now-dead tagged parent, and not host-wide by image name — it is:
  ~
  +///   * non-vacuous — a leaked/orphaned captured `choice` (same PID, still
  ~
  +///     `choice.exe`) is counted, so a survivor stays detectable; and
  ~
  +///   * not host-wide-flaky — a concurrent target's `choice.exe` carries a
  ~
  +///     different, non-captured PID and is excluded.
  ~
  +///
  ~
  +/// An empty `pids` slice yields 0 without issuing a malformed filter.
  ~
  +fn surviving_captured_choice_pids(pids: &[u32]) -> usize {
  ~
  +    if pids.is_empty() {
  ~
  +        return 0;
  ~
  +    }
  ~
  +    let pid_list = pids
  ~
  +        .iter()
  ~
  +        .map(u32::to_string)
  ~
  +        .collect::<Vec<_>>()
  ~
  +        .join(",");
  ~
  +    let out = Command::new("powershell")
  ~
  +        .args([
  ~
  +            "-NoProfile",
  ~
  +            "-NonInteractive",
  ~
  +            "-Command",
  ~
  +            &format!(
  ~
  +                "$pids=@({pid_list}); \
  ~
  +                 @(Get-CimInstance Win32_Process -Filter \"Name='choice.exe'\" | \
  ~
  +                 Where-Object {{ $pids -contains $_.ProcessId
    }}).Count"
  ~
               ),
  ~
           ])
  ~
           .output()
  ~
           .expect("query 
  -this test's tagged
  +survival of captured
    choice 
  -descendants
  +PIDs
    via CIM");
  ~
       String::from_utf8_lossy(&out.stdout)
  ~
  @@ -321,18 +371,29 @@ async fn active_process_cap_is_enforced() {
  -count
  +PID set
    (already scoped to this test's tagged tree) while
  ~
       // the fan-out is held, tracking its 
  -peak.
  +peak and capturing the peak PID set so
  ~
  +    // the post-close reap check can re-verify those exact ProcessIds by fixed
  ~
  +    // PID — the parent-scoped query goes structurally empty once the tagged
  ~
  +    // parent dies at job close.
  ~
       let mut peak = 0usize;
  ~
       
  +let mut captured_pids: Vec<u32> = Vec::new();
  ~
       let watch_deadline = Instant::now() + Duration::from_secs(30);
  ~
       while Instant::now() < watch_deadline {
  ~
           let 
  -delta
  +pids
    = 
  -tagged_choice_descendant_count(&tag);
  +tagged_choice_descendant_pids(&tag);
  ~
  +        if pids.len() >
    peak 
  +{
  ~
  +            peak = pids.len();
  ~
  +            captured_pids
    = 
  -peak.max(delta);
  +pids.clone();
  ~
  +        }
  ~
           // Once we have clearly observed a large admitted set we can stop early.
  ~
           if 
  -delta
  +pids.len()
    >= SANDBOX_ACTIVE_PROCESS_LIMIT / 2 {
  ~
               // keep sampling briefly to catch any overshoot past the cap
  ~
               std::thread::sleep(Duration::from_millis(500));
  ~
               
  +let overshoot = tagged_choice_descendant_pids(&tag);
  ~
  +            if overshoot.len() > peak {
  ~
                   peak = 
  -peak.max(tagged_choice_descendant_count(&tag));
  +overshoot.len();
  ~
  +                captured_pids = overshoot;
  ~
  +            }
  ~
               break;
  ~
           }
  ~
           std::thread::sleep(Duration::from_millis(250));
  ~
  @@ -361,13 +422,22 @@ async fn active_process_cap_is_enforced() {
  +assert!(
  ~
  +        !captured_pids.is_empty(),
  ~
  +        "peak PID set was not captured — the post-close reap check would be vacuous"
  ~
  +    );
  ~
   
  ~
       // 
  -Everything this test spawned is reaped once
  +After
    the job 
  -closes (the count
  +closes, re-check the EXACT fan-out `choice` PIDs captured
  ~
  +    // while the tagged parent was alive, by fixed ProcessId intersected with
  ~
  +    // image `choice.exe`. This
    is 
  +non-vacuous (a leaked/orphaned captured
  ~
       // 
  -already scoped to this test's
  +survivor is still counted) and not host-wide-flaky (a concurrent target's
  ~
  +    // `choice.exe` carries a different, non-captured PID). The parent-scoped
  ~
  +    // query cannot be reused — the
    tagged 
  -tree, so "back to baseline"
  +parent
    is 
  -zero).
  +dead once `run.await`
  ~
  +    // returned.
  ~
       wait_until(
  ~
           || 
  -tagged_choice_descendant_count(&tag)
  +surviving_captured_choice_pids(&captured_pids)
    == 0,
  ~
           30,
  ~
           "fan-out descendants reaped after job 
  -close",
  +close (by captured PID)",
  ~
       );
  ~
       reap_stray_choice();
  ~

hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk git grep -n -i \"notcmd\\|foocmd\\|exact final\\|cmd classifier\\|resolved program\" f0dd5b6d312af616f268f96f34c3bc9fc962c4d2 -- crates/wcore-sandbox | head -120
rtk git diff --name-only 3f839309574d6741eed416cd3820f56447f74eba..f0dd5b6d312af616f268f96f34c3bc9fc962c4d2 -- ':(exclude).planning/**'
rtk git diff --name-only 3f839309574d6741eed416cd3820f56447f74eba..f0dd5b6d312af616f268f96f34c3bc9fc962c4d2 -- '.github/workflows/**' '**/Cargo.toml' 'Cargo.toml' 'crates/wcore-swarm/src/worktree_manager.rs' 'crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs' 'crates/wcore-sandbox/src/backends/appcontainer/appcontainer_acl_lease.rs'
rtk git diff --exit-code 3f839309574d6741eed416cd3820f56447f74eba..f0dd5b6d312af616f268f96f34c3bc9fc962c4d2 -- crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs crates/wcore-swarm/src/worktree_manager.rs crates/wcore-sandbox/tests/live_fs_acl.rs; true" in /Users/seandonahoe/dev/waylandcore-ferrox
hook: PostToolUse
hook: PostToolUse Completed
 succeeded in 9876ms:
2026-07-24 14:41:56.590 xcodebuild[98283:161664280]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:41:56.699 xcodebuild[98283:161664254] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-sThD7BaM' (errno=Operation not permitted)
2026-07-24 14:41:57.103 xcodebuild[98447:161664580]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:41:57.203 xcodebuild[98447:161664574] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs:138:/// returned by [`resolve_program`]) has `cmd.exe` as its exact FINAL PATH
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs:140:/// `notcmd.exe`/`foocmd.exe` does not match. Used to decide whether the
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs:142:/// CRT [`quote_arg`] rules. Matching the RESOLVED program (not the raw
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs:154:    // `notcmd.exe`.
f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs:613:            // When the resolved program is cmd.exe, its `/c`/`/k` payload must be
crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
crates/wcore-sandbox/tests/hard_process_containment_windows.rs

--- Changes ---




hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk git show f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:.planning/phases/20-transactional-delegated-mutation/20-52-PLAN.md | sed -n '1,260p'
rtk git show f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:.planning/phases/20-transactional-delegated-mutation/20-50-PLAN.md | sed -n '1,300p'
rtk git log --oneline --decorate --reverse 3f839309574d6741eed416cd3820f56447f74eba..f0dd5b6d312af616f268f96f34c3bc9fc962c4d2" in /Users/seandonahoe/dev/waylandcore-ferrox
hook: PostToolUse
hook: PostToolUse Completed
 succeeded in 4282ms:
---
phase: 20-transactional-delegated-mutation
plan: "52"
type: execute
wave: 51
depends_on:
  - "20-51"
files_modified:
  - .planning/phases/20-transactional-delegated-mutation/20-52-CROSS-AUDIT.md
autonomous: true
requirements:
  - REQ-native-r12
  - REQ-native-r13
must_haves:
  truths:
    - "A FOUR-WAY independent cross-audit panel — Codex 5.6 Sol (`codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check \"<prompt>\"`), Gemini 3.1 Pro (`gemini -p \"<prompt>\" -m gemini-3.1-pro-preview -o text --approval-mode plan`, with `GEMINI_CLI_TRUST_WORKSPACE=true --skip-trust` for the untrusted checkout), and Kimi K3 (`/Users/seandonahoe/.kimi-code/bin/kimi -p \"<prompt>\" --output-format text`, ABSOLUTE path, passed via -p not stdin) run in PARALLEL, PLUS an internal Claude non-author adversarial reviewer prompted to REFUTE (default-refuted-if-uncertain) — cross-audits the exact RE-SEALED further-repaired-successor SHA/tree recorded by 20-51 at every severity across the 20-50 repair delta (Finding 1 non-vacuous captured-PID reap in the containment test; Finding 2 exact-final-component cmd classifier in command.rs) BEFORE any Sean-gated native run is re-spent (REQ-native-r12 pre-native gate)."
    - "Each of the four auditors emits its OWN on-disk schema-validated artifact: Codex Sol → `f20-native-crossaudit.codex-sol`, Gemini Pro → `f20-native-crossaudit.gemini-pro`, Kimi K3 → `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial reviewer → `wayland-core.phase20-independent-review.v1`; no prose-only vote counts toward the PASS — sustaining the 20-24/20-31/20-38/20-45 attestation discipline (REQ-native-r13). Raw outputs are preserved on disk alongside the parsed artifacts."
    - "The panel adversarially prosecutes that the two 20-45 findings are ACTUALLY CLOSED without regression: (1) the `active_process_cap_is_enforced` post-close reap check is non-vacuous — it captures the fan-out `choice` PIDs while the tagged parent is alive and re-checks those exact ProcessIds after close, AND it did NOT reintroduce the host-wide-image-count flake (it matches only the test's own captured PIDs); every other containment assertion and the sibling `job_close_reaps_detached_descendant_with_no_residue` are preserved verbatim. (2) `resolved_program_is_cmd` matches `cmd.exe` as the exact final path component (case-insensitive), not a bare suffix, with the #B `quote_cmd_payload`/`quote_arg` behavior otherwise identical. The two isolation guardrails from the 20-43 delta still hold (#A DispatchAdmission budget preserved; #B quoting-layer-only, boundary primitives untouched), and the Kimi watch-items are carried into the 20-53 re-dispatch (REQ-native-r12/r13)."
    - "native_macos/native_windows are the ONLY deferred checks (proven at 20-53); code review of the 20-50 delta, all-severity findings, evidence integrity, and integration authority are prosecuted here. ALL FOUR auditors MUST PASS with zero findings to admit 20-53; any single finding from any one auditor (any severity except the deferred native checks) stops the sequence for a further repaired successor. Fail-closed: if ANY external auditor (Codex Sol / Gemini Pro / Kimi K3) is unreachable or its output cannot be schema-validated, the gate records an explicit incomplete disposition and BLOCKS rather than emitting a false-green PASS from a partial panel; the internal Claude adversarial pass is one of the four required votes, never a substitute for a missing external auditor (CONTEXT D5 fail-safe) (REQ-native-r13)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-52-CROSS-AUDIT.md
      provides: "Four per-reviewer schema-validated pre-native cross-audits of the re-sealed further-repaired-successor's 20-50 repair delta: the Codex Sol / Gemini Pro / Kimi K3 f20-native-crossaudit artifacts plus the internal Claude adversarial wayland-core.phase20-independent-review.v1 artifact, with raw outputs preserved"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-52-CROSS-AUDIT.md
      to: .planning/phases/20-transactional-delegated-mutation/20-51-SUMMARY.md
      via: "source/review-base/review authority tuple over the re-sealed further-repaired-successor SHA/tree recorded by 20-51"
      pattern: "source_sha"
---

<objective>
Cross-audit the exact re-sealed further-repaired-successor (the SHA/tree recorded by 20-51) before re-spending a Sean-gated native run: a FOUR-WAY independent panel — Codex 5.6 Sol, Gemini 3.1 Pro, and Kimi K3 (three external CLIs run in parallel), plus an internal Claude non-author adversarial reviewer — prosecutes the 20-50 repair delta at every severity, and each auditor records its OWN schema-validated artifact. ALL FOUR MUST PASS; native explicitly deferred to 20-53.

Purpose: The gate sequence puts an adversarial cross-audit before the native proof so a broken candidate does not consume the scarce native run; and R13 requires per-reviewer schema-validated artifacts, not prose. This is the pre-native attestation gate for the further-repaired successor. The 20-50 delta closes the two real, statically-provable findings the 20-45 panel returned BLOCK on, so the panel focuses adversarially on whether Finding 1's fix is genuinely non-vacuous (captured-PID reap) WITHOUT reintroducing the host-wide-image-count flake, whether Finding 2 is an exact-final-component match, and whether the 20-43 isolation guardrails (#A admission budget; #B quoting-layer only) still hold. A four-independent-auditor panel raises the cost of a shared blind spot: exactly the class of miss (Gemini + Kimi both missed the reap-assertion degradation at 20-45) a four-way panel exists to catch.
Output: Four schema-validated artifacts over the re-sealed successor SHA — Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1`. Zero findings across ALL FOUR admits the native-proof gate. If ANY external auditor is unreachable or its output cannot be schema-validated, an explicit incomplete disposition (no false green from a partial panel).
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md
@.planning/phases/20-transactional-delegated-mutation/20-50-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-51-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-45-PLAN.md
</context>

<execution_rules>
- The cross-audit is a FOUR-WAY panel of independent (non-author) reviewers; NONE is the plan author. Three are external CLIs, invoked verbatim and run in PARALLEL:
  1. Codex 5.6 Sol — `codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check "<prompt>"` (brief on stdin is acceptable)
  2. Gemini 3.1 Pro — `gemini -p "<prompt>" -m gemini-3.1-pro-preview -o text --approval-mode plan` (set `GEMINI_CLI_TRUST_WORKSPACE=true` and add `--skip-trust` to run in the untrusted checkout dir; 20-45 hit exit 55 without them)
  3. Kimi K3 — `/Users/seandonahoe/.kimi-code/bin/kimi -p "<prompt>" --output-format text` (ABSOLUTE path — the Bash shell PATH omits it; Kimi's -p mode does NOT ingest piped stdin, so pass the full brief as the -p argument; see the [[cross-auditor-kimi-k3]] project memory)
  The fourth is an internal Claude non-author adversarial subagent, prompted to REFUTE the candidate (default-refuted-if-uncertain).
- Every one of the four auditors MUST leave its OWN on-disk schema-validated artifact. The three external legs emit `f20-native-crossaudit` variants keyed by auditor id — Codex Sol → `f20-native-crossaudit.codex-sol`, Gemini Pro → `f20-native-crossaudit.gemini-pro`, Kimi K3 → `f20-native-crossaudit.kimi-k3`; the internal Claude adversarial leg emits `wayland-core.phase20-independent-review.v1`. A prose-only vote does not count toward PASS (REQ-native-r13). Preserve each auditor's raw output on disk (a `20-52-raw/` sidecar directory) alongside the parsed artifacts.
- Scope every prompt to the EXACT re-sealed successor SHA/tree recorded by 20-51 (do not review the branch tip if it has advanced) and the 20-50 repair delta (Finding 1 containment-test reap; Finding 2 command.rs cmd classifier). The review base is that tuple.
- native_macos and native_windows are the ONLY checks that may be deferred (they are proven at 20-53). All other checks — code review of the 20-50 delta, all-severity findings, evidence integrity, integration authority — are prosecuted here by every auditor.
- DISPOSITION — ALL FOUR MUST PASS: only a zero-finding PASS from all four auditors admits 20-53. Any single finding from any one auditor (any severity except the deferred native_macos/native_windows) BLOCKS and routes to a further repaired successor.
- FAIL-CLOSED: if ANY external auditor (Codex Sol / Gemini Pro / Kimi K3) cannot be reached or its output cannot be schema-validated, record an explicit incomplete disposition and BLOCK — do not synthesize a PASS from the remaining reviewers or from prose (CONTEXT D5 fail-safe). The internal Claude adversarial pass is always reachable but is one of the four required votes, not a substitute for a missing external auditor. Do not modify any source/test/workflow file; this plan writes only the cross-audit artifact.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Four-way pre-native cross-audit panel of the re-sealed successor (Codex Sol + Gemini Pro + Kimi K3 + Claude adversarial), all-must-pass, fail-closed</name>
  <files>.planning/phases/20-transactional-delegated-mutation/20-52-CROSS-AUDIT.md</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-51-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-50-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md, .planning/phases/20-transactional-delegated-mutation/20-45-PLAN.md</read_first>
  <behavior>
    - Dispatch the FOUR-WAY independent panel over the exact re-sealed successor SHA/tree recorded by 20-51. Run the three external CLIs in parallel — Codex 5.6 Sol (`codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check`), Gemini 3.1 Pro (`gemini -m gemini-3.1-pro-preview -o text --approval-mode plan`, with `GEMINI_CLI_TRUST_WORKSPACE=true --skip-trust`), Kimi K3 (`/Users/seandonahoe/.kimi-code/bin/kimi --output-format text`, absolute path, brief in the -p argument) — plus an internal Claude non-author adversarial subagent prompted to REFUTE (default-refuted-if-uncertain). Scope every prompt to the 20-50 repair delta and its predecessor findings: Finding 1 (containment reap non-vacuous) and Finding 2 (cmd classifier exact component).
    - Every auditor prosecutes adversarially: (1) Is `active_process_cap_is_enforced`'s post-close reap check now non-vacuous — does it capture the fan-out `choice` PIDs while the tagged parent is alive and re-check those exact ProcessIds after close (intersected with image `choice.exe`), and did it AVOID reintroducing the host-wide-image-count flake (matching only the test's own captured PIDs)? Are every OTHER containment assertion (positive peak, cap ceiling, below-attempts bound, clean exit) and the sibling `job_close_reaps_detached_descendant_with_no_residue` preserved verbatim? (2) Does `resolved_program_is_cmd` match `cmd.exe` as the exact final path component (case-insensitive) rather than a bare suffix, with `quote_cmd_payload`/`quote_arg` otherwise byte-identical? (3) Do the 20-43 isolation guardrails still hold (#A DispatchAdmission budget preserved — no catch-to-unlimited; #B quoting-layer only — `is_unc_or_device_path`, Low-IL token, Job-Object limits, ACL lease untouched; argv discipline kept)? (4) Are the Kimi watch-items carried as explicit 20-53 re-dispatch checks?
    - Capture each auditor's findings as its OWN schema-validated artifact: Codex Sol → `f20-native-crossaudit.codex-sol`, Gemini Pro → `f20-native-crossaudit.gemini-pro`, Kimi K3 → `f20-native-crossaudit.kimi-k3`, internal Claude adversarial → `wayland-core.phase20-independent-review.v1`. native_macos/native_windows are deferred, proven at 20-53. Preserve the raw outputs in a `20-52-raw/` sidecar.
    - Disposition — ALL FOUR MUST PASS: only a zero-finding PASS from all four admits 20-53; any single finding from any auditor blocks and routes to a further repaired successor.
    - Fail-closed: if ANY external auditor (Codex Sol / Gemini Pro / Kimi K3) is unreachable or its output fails schema validation, record an explicit incomplete disposition and block — no false-green PASS from a partial panel.
  </behavior>
  <action>Dispatch the four-way independent panel over the exact re-sealed successor SHA/tree recorded by 20-51: run the three external CLIs in parallel — Codex 5.6 Sol (`codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check "<prompt>"`), Gemini 3.1 Pro (`GEMINI_CLI_TRUST_WORKSPACE=true gemini -p "<prompt>" -m gemini-3.1-pro-preview -o text --approval-mode plan --skip-trust`), and Kimi K3 (`/Users/seandonahoe/.kimi-code/bin/kimi -p "<prompt>" --output-format text`) — plus an internal Claude non-author adversarial subagent prompted to REFUTE (default-refuted-if-uncertain), scoping each to the 20-50 repair delta: Finding 1 (containment reap non-vacuous captured-PID check without host-wide flake; other assertions + sibling reap test preserved) and Finding 2 (cmd classifier exact-final-component match; #B quoting otherwise identical), plus the still-holding #A/#B isolation guardrails. Record each auditor's findings as its own schema-validated artifact in `20-52-CROSS-AUDIT.md` — `f20-native-crossaudit.codex-sol`, `f20-native-crossaudit.gemini-pro`, `f20-native-crossaudit.kimi-k3`, and the internal Claude `wayland-core.phase20-independent-review.v1` (native_macos/native_windows deferred) — preserving raw outputs under `20-52-raw/`. Verify the Kimi watch-items are recorded as explicit 20-53 re-dispatch checks. ALL FOUR must PASS zero-finding to admit 20-53; any single finding blocks. If ANY external auditor is unreachable or its output fails schema validation, record an explicit incomplete disposition and BLOCK — do not synthesize a PASS. Implements REQ-native-r12/r13; write only the cross-audit artifact.</action>
  <verify>
    <automated>test -f .planning/phases/20-transactional-delegated-mutation/20-52-CROSS-AUDIT.md</automated>
    <automated>grep -nE "f20-native-crossaudit\.codex-sol|f20-native-crossaudit\.gemini-pro|f20-native-crossaudit\.kimi-k3|phase20-independent-review|Codex 5\.6 Sol|Gemini 3\.1 Pro|Kimi K3|Claude adversarial|source_sha|deferred|Finding 1|Finding 2" .planning/phases/20-transactional-delegated-mutation/20-52-CROSS-AUDIT.md | head -40</automated>
  </verify>
  <acceptance_criteria>The cross-audit artifact exists and records FOUR per-reviewer results over the exact re-sealed successor SHA: Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1`, with native_macos/native_windows deferred, the two 20-45 findings confirmed closed without regression, and the Kimi watch-items carried as 20-53 checks. A zero-finding PASS from ALL FOUR reviewers admits 20-53; any single finding from any auditor, or any external auditor unreachable/schema-invalid, blocks with an explicit incomplete disposition.</acceptance_criteria>
  <done>The re-sealed further-repaired successor has a four-way per-reviewer schema-validated pre-native cross-audit (Codex Sol + Gemini Pro + Kimi K3 + Claude adversarial), or an explicit fail-closed block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native run authorization ← four-way cross-audit PASS | The scarce Sean-gated native run is only re-spent after ALL FOUR panel auditors pass; a false-green here wastes the run |
| attestation integrity ← per-reviewer artifact | A PASS must be backed by an on-disk schema-validated artifact per each of the four auditors, not prose |
| finding closure ← adversarial re-prosecution | The two 20-45 findings must be confirmed genuinely closed (non-vacuous reap; exact cmd component) without a regression such as the reintroduced host-wide flake |
| panel independence ← external-auditor reachability | If ANY external auditor (Codex Sol / Gemini Pro / Kimi K3) is down, the audit must fail closed rather than pass on a partial panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-52-01 | Spoofing | prose-only or partial-panel PASS | high | mitigate | Each of the four auditors emits its own schema-validated artifact; a prose-only or missing artifact does not count, and PASS requires ALL FOUR; raw outputs are preserved on disk. |
| T-20-52-02 | Repudiation | external auditor unreachable false-green | high | mitigate | Fail-closed: any unreachable or schema-invalid external-auditor output records an explicit incomplete disposition and blocks; no PASS is synthesized from the remaining reviewers (CONTEXT D5 fail-safe). |
| T-20-52-03 | Tampering | a finding-fix regression slipping through | high | mitigate | All four auditors adversarially re-prosecute that Finding 1's reap is non-vacuous AND did not reintroduce the host-wide flake, that Finding 2 is an exact-component match, and that the #A/#B guardrails still hold — so a fix-regression cannot pass a four-independent-auditor panel silently. |
</threat_model>

<verification>
The cross-audit artifact `20-52-CROSS-AUDIT.md` determines acceptance: it must record FOUR per-reviewer results over the exact re-sealed successor SHA/tree — Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1` — ALL zero-finding, with native_macos/native_windows deferred, the two 20-45 findings confirmed closed without regression, and the Kimi watch-items carried as explicit 20-53 re-dispatch checks. Record the re-sealed `source_sha`/tree, the four reviewer identities, the three external CLI invocations, each artifact's schema-validation status, the raw-output sidecar identity, and the disposition (ALL FOUR PASS admits 20-53; any single finding or any external auditor unreachable/invalid blocks with an explicit incomplete disposition) in `20-52-SUMMARY.md`. No source/test/workflow change; no native or requirement claim.
</verification>

<success_criteria>
- The four-way panel (Codex 5.6 Sol + Gemini 3.1 Pro + Kimi K3 external, run in parallel, plus the internal Claude adversarial reviewer) cross-audits the exact re-sealed successor at every severity across the 20-50 delta, and each auditor records its own schema-validated artifact — `f20-native-crossaudit.codex-sol` / `.gemini-pro` / `.kimi-k3` and `wayland-core.phase20-independent-review.v1` (r12/r13).
- The two 20-45 findings are confirmed closed without regression (non-vacuous captured-PID reap that avoids the host-wide flake; exact-final-component cmd classifier), the #A/#B isolation guardrails still hold, and the Kimi watch-items are carried as 20-53 checks; native_macos/native_windows are the only deferred checks.
- A zero-finding PASS from ALL FOUR reviewers admits 20-53; any single finding from any auditor, or any external auditor unreachable/schema-invalid, blocks with an explicit incomplete disposition (no false green from a partial panel).
</success_criteria>

## Artifacts this phase produces
- The four pre-native cross-audits of the re-sealed further-repaired-successor's 20-50 repair delta: Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1`, with raw outputs preserved.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-52-SUMMARY.md` using the standard GSD summary template. Record the re-sealed `source_sha`/tree, the four reviewer identities (Codex Sol, Gemini Pro, Kimi K3, internal Claude adversarial) and each artifact's schema-validation status, the two-finding-closure verdicts, the two isolation-guardrail verdicts, the carried 20-53 watch-items, the raw-output sidecar identity, and the disposition (ALL-FOUR PASS or fail-closed incomplete). Complete no Phase 20 requirement.
</output>
</content>
---
phase: 20-transactional-delegated-mutation
plan: "50"
type: execute
wave: 49
depends_on:
  - "20-45"
files_modified:
  - crates/wcore-sandbox/tests/hard_process_containment_windows.rs
  - crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
autonomous: true
requirements:
  - REQ-native-r2
  - REQ-native-r5
  - REQ-native-r7
  - REQ-native-r4
must_haves:
  truths:
    - "Finding 1 (20-45 panel, Codex HIGH / Claude MEDIUM) CLOSED: in `crates/wcore-sandbox/tests/hard_process_containment_windows.rs`, `active_process_cap_is_enforced`'s post-close reap check is no longer vacuous. The 20-43 scoping (`tagged_choice_descendant_count`, scoped by the tagged parent cmd's `ParentProcessId` via CIM) goes structurally empty once the tagged parent cmd dies at job close, so the old `wait_until(... == 0)` passed on the first poll regardless of a leaked survivor. The fix captures the fan-out `choice.exe` PIDs WHILE the tagged parent is still alive (during the peak-sampling window) and, AFTER job close, re-checks those exact PIDs by fixed ProcessId — a leaked/orphaned bare-`choice` idler is therefore still detectable (REQ-native-r7)."
    - "The fix threads the needle: it does NOT reintroduce the host-wide-image-count flake that 20-43 Task 3 legitimately removed (a concurrent `live_fs_acl` `choice.exe` polluting a host-wide baseline). The post-close survivor check matches ONLY the test's own captured PIDs (intersected with image `choice.exe`), so a concurrent target's `choice.exe` — which carries a different, non-captured PID — cannot inflate the count. Non-vacuous AND not host-wide-flaky (REQ-native-r7)."
    - "Every OTHER containment assertion in `active_process_cap_is_enforced` is preserved verbatim: the positive peak (`peak > 0`), the cap ceiling (`peak <= SANDBOX_ACTIVE_PROCESS_LIMIT`), the below-attempts bound (`peak < attempts`), the clean parent exit (`exit_code == 0`), and the `reap_stray_choice()` cleanup. The sibling `job_close_reaps_detached_descendant_with_no_residue` (whose grandchild carries `rem {tag}` on its OWN command line, so `tagged_cmd_count` survives parent death) is the robust reap-property cover and is NOT touched or weakened (REQ-native-r7)."
    - "Finding 2 (20-45 panel, Codex MEDIUM / Claude LOW) CLOSED: in `crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs`, `resolved_program_is_cmd` no longer uses `.ends_with(\"cmd.exe\")` (which suffix-matches a hypothetical `notcmd.exe`/`foocmd.exe` and would route its `/c`/`/k` payload through the cmd-specific `quote_cmd_payload` instead of `quote_arg`). It matches `cmd.exe` as the exact FINAL path component (case-insensitive), so only an image whose filename IS `cmd.exe` is classified as cmd. The #B `quote_cmd_payload` / `quote_arg` behavior is otherwise byte-for-byte identical (REQ-native-r2/r5)."
    - "Isolation guardrails HELD (unchanged from the 20-43 delta): #B stays a quoting-and-classification-layer change only — `is_unc_or_device_path`, the Low-IL restricted token, the Job-Object limits, and the AppContainer ACL lease are untouched; argv discipline is kept (no shell-string interpolation of caller data); the 20-36 `exit /b 0` normalization in the containment scripts is preserved, not reverted (REQ-native-r5/r7)."
    - "Linux is unregressed and HONESTLY DEFERRED: both edited files are `#![cfg(windows)]` / part of `windows_impl` (`#[cfg(windows)]`), so zero Linux compiled output changes — the Hetzner aggregate stays 11509/0/48 and the sanctioned macOS `cargo check -p wcore-sandbox --features live-docker --tests` stays green. Neither the Mac nor Hetzner Linux compiles a line of the two fixes; their compile AND real-hardware behavior are proven ONLY at the 20-53 native msvc re-dispatch (REQ-native-r4)."
  artifacts:
    - path: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      provides: "Finding 1 fix: capture the fan-out choice PIDs before job close and re-check by fixed ProcessId after close; every other containment assertion preserved; sibling reap test untouched"
    - path: crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
      provides: "Finding 2 fix: resolved_program_is_cmd matches cmd.exe as the exact final path component (case-insensitive) rather than a bare suffix; #B quoting behavior otherwise identical"
  key_links:
    - from: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      to: crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
      via: "both are the two 20-45-panel findings on the sealed-but-RED 3f839309; the containment test exercises the cmd payload the classifier routes"
      pattern: "cmd.exe"
---

<objective>
Close the two real, statically-provable findings the 20-45 FOUR-WAY pre-native panel (Codex 5.6 Sol + Gemini 3.1 Pro + Kimi K3 + internal Claude adversarial, ALL-FOUR-MUST-PASS) returned BLOCK on against the sealed-but-RED third-repaired-successor `3f839309` (tree `3092475b`), as a clean further-repaired successor over that exact sealed tree, touching ONLY the two files the findings name:

- Finding 1 (Codex HIGH / Claude MEDIUM) — vacuous post-close reap assertion. `active_process_cap_is_enforced` fans out BARE `choice.exe` idlers (choice rejects an injected tag), so the 20-43 `tagged_choice_descendant_count` scopes them by the tagged parent cmd's `ParentProcessId`. By the post-close check `run.await` has returned → the AppContainer job closed → the tagged parent cmd is DEAD → the CIM `cmd.exe`-with-tag query is empty → `$parents` is empty → the count is structurally 0 whether or not a `choice` leaked. Fix: capture the specific fan-out `choice` PIDs WHILE the tagged parent is still alive (during the peak-sampling window), then AFTER close assert each of those exact PIDs is gone — checked by fixed ProcessId (intersected with image `choice.exe`), NOT by the now-dead tagged parent and NOT host-wide by image name. Non-vacuous (catches a leaked/orphaned survivor) AND not host-wide-flaky (only the test's own captured PIDs count).
- Finding 2 (Codex MEDIUM / Claude LOW) — loose cmd detection. `resolved_program_is_cmd` uses `.ends_with("cmd.exe")`, which suffix-matches `notcmd.exe`. Fix: match `cmd.exe` as the exact final PATH COMPONENT (case-insensitive), not a bare suffix.

Purpose: This supersedes the now-RED-`3f839309`-bound plans 20-46…20-49 (authored, unexecuted — their upstream candidate is terminal RED per the 20-45 BLOCK). It writes the real fixes so a NEW further-repaired candidate can be re-sealed (20-51), re-cross-audited by the full four-way panel (20-52), and re-dispatched natively (20-53) under a fresh Sean authorization.
Output: One clean successor delta scoped to the two finding files. Both fixes are `#![cfg(windows)]` / `windows_impl` — UNCOMPILABLE on Mac/Hetzner Linux; their compile AND real-hardware behavior are proven ONLY on the self-hosted msvc runner at 20-53 (honest per CONTEXT D5/D7). No source file outside the two named files is touched; no requirement is completed.
</objective>

<redispatch_decision>
Re-dispatch structure — AUTHOR NEW PLANS 20-50…20-56; do NOT re-bind or edit the executed/sealed 20-43/20-44/20-45 records, and do NOT edit the superseded 20-46…20-49.

Choice: the further-repaired successor is the fully additive serial chain 20-50 (fix) → 20-51 (re-seal, no-op lock confirm) → 20-52 (four-way pre-native cross-audit) → 20-53 (native re-dispatch) → 20-54 (native-inclusive four-way review) → 20-55 (re-prep tuple) → 20-56 (terminal). This mirrors the 20-43…20-49 discipline that superseded 20-38…20-42.

Rationale (why new, not re-bind):
- 20-43/20-44/20-45 are EXECUTED and sealed (SUMMARYs on disk). 20-44 sealed `3f839309`; 20-45's four-way panel found that exact candidate RED (BLOCK) with two real, statically-provable findings. Re-binding a new fix onto those would overwrite a sealed record; a fresh delta belongs on a new fix plan.
- 20-46…20-49 are authored but UNEXECUTED and hardcoded (`depends_on`, context refs, `source_sha 3f839309`) to the now-RED sealed candidate. Their upstream candidate is terminal RED, so they are self-consistently SUPERSEDED (dead) WITHOUT any mutation; editing them in place would mutate authored plans and risk stale candidate binding.
- Net: 20-43/20-44 stay as the sealed record; 20-45 stays as the BLOCK gate; 20-46…20-49 are superseded (dead — their bound candidate is comprehensively RED) and are neither executed nor edited by this chain; the NEW further-repaired candidate flows through 20-51…20-56.
</redispatch_decision>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md
@.planning/phases/20-transactional-delegated-mutation/20-45-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-43-SUMMARY.md
@crates/wcore-sandbox/tests/hard_process_containment_windows.rs
@crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
</context>

<execution_rules>
- Require a clean isolated standalone checkout (`.git` is a directory) on branch `plan/f20-unified-audit-repair`, in `/Users/seandonahoe/dev/waylandcore-ferrox`. Do NOT execute in the sibling `/Users/seandonahoe/dev/waylandcore` primary checkout. All git ops via `/usr/bin/git`.
- The new candidate is a SUCCESSOR of the sealed-but-RED `3f839309` (tree `3092475b`). Only planning-doc commits sit between that seal and the branch tip; confirm the TWO touched files are byte-identical to the sealed tree (no source drift) before editing — `git diff 3f839309 -- crates/wcore-sandbox/tests/hard_process_containment_windows.rs crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs` must be empty — then author the fixes on the branch tip.
- Capture the plan base before any edit: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-50)"`. Advance the base between tasks with `--start-fresh` so each task's disjoint per-task scope gate passes.
- NO Cargo/clippy/nextest on the Mac, with EXACTLY ONE sanctioned exception (CONTEXT D5 carve-out): the targeted macOS compile gate `cargo check -p wcore-sandbox --features live-docker --tests` (compile/type-check only, no test run, no Docker). Fail-safe: if that check cannot run on the Mac, the gate BLOCKS rather than emitting a false green. Everything else is `vx rustfmt --edition 2024 --check` (fmt) + grep + the committed-HEAD Hetzner harness (`/Users/seandonahoe/.ratchet/harness/remote-cargo.sh`) + node tooling.
- HONESTY (CONTEXT D5/D7): both `hard_process_containment_windows.rs` (`#![cfg(windows)]`) and `command.rs` (part of `windows_impl`, `#[cfg(windows)]`) — neither this Mac nor Hetzner Linux compiles a line of the two fixes. Their compile AND real-hardware behavior are proven ONLY on the self-hosted msvc runner at 20-53. Do NOT claim a Windows fix "verified" from any pre-dispatch gate. Because the two files are Windows-only, the sanctioned macOS compile gate and the Hetzner aggregate/clippy are a NO-REGRESSION floor (they must stay green + 11509/0/48), not a proof of the fixes.
- Touch ONLY the two declared files. No drive-by refactor of the AppContainer implementation, the other containment assertions, the sibling reap test, `quote_arg`/`quote_cmd_payload`/`classify_bare_shell`/`resolve_program`, or any other module. Preserve every OTHER containment assertion (positive peak, cap ceiling, below-attempts bound, clean parent exit), the sibling `job_close_reaps_detached_descendant_with_no_residue`, `is_unc_or_device_path`, the Low-IL restricted token, the Job-Object limits, the AppContainer ACL lease, and the 20-36 `exit /b 0` normalization.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Finding 1 — non-vacuous post-close reap by captured ProcessId (thread the needle: not host-wide, not tag-scoped-once-parent-dies)</name>
  <files>crates/wcore-sandbox/tests/hard_process_containment_windows.rs</files>
  <read_first>crates/wcore-sandbox/tests/hard_process_containment_windows.rs, .planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md</read_first>
  <behavior>
    - The vacuous assertion is the FINAL `wait_until(|| tagged_choice_descendant_count(&tag) == 0, 30, "fan-out descendants reaped after job close")` in `active_process_cap_is_enforced`. `tagged_choice_descendant_count` scopes `choice.exe` by whose `ParentProcessId` is a live tagged `cmd.exe` (`$parents = ProcessId of cmd.exe whose CommandLine matches the tag`). After `run.await` returns the job has closed and the tagged parent cmd is dead, so the `cmd.exe`-with-tag query returns nothing → `$parents` is empty → the `choice.exe` count is structurally 0 whether or not a `choice` leaked. The prior host-wide `image_count("choice.exe") <= baseline` would have caught a leaked survivor; this scoping cannot.
    - Fix: replace the count helper with a PID-list helper, capture the peak PID set while the tagged parent is ALIVE, then re-check those exact PIDs after close by fixed ProcessId.
      1. Replace `tagged_choice_descendant_count(tag) -> usize` with `tagged_choice_descendant_pids(tag) -> Vec<u32>`: the same `ParentProcessId`-scoped CIM query, but `Select-Object -ExpandProperty ProcessId` (returning the ProcessId list) instead of `.Count`; parse whitespace/newline-separated PIDs into a `Vec<u32>`. Keep the `ParentProcessId` scoping — it is what makes the ALIVE-phase capture immune to a concurrent `choice`-spawning target. Update the helper's doc to describe the two-phase (alive-capture + post-close by-PID) design and DROP the inaccurate "without weakening any containment assertion" claim the 20-45 panel flagged.
      2. Add `surviving_captured_choice_pids(pids: &[u32]) -> usize`: a CIM query over `Name='choice.exe'` filtered to `ProcessId` values IN the captured set (`$pids -contains $_.ProcessId`), returning the `.Count`. This matches ONLY the test's own captured PIDs intersected with image `choice.exe`, so a concurrent target's `choice.exe` (different PID) is excluded (no host-wide flake) and a leaked/orphaned captured `choice` (same PID, still `choice.exe`) is counted (non-vacuous). An empty `pids` slice must yield 0 without querying a malformed filter.
      3. In the peak-sampling loop, track the peak PID SET alongside `peak`: sample `let pids = tagged_choice_descendant_pids(&tag);`, and whenever `pids.len()` exceeds the running peak, set `peak = pids.len()` and store the captured set (`captured_pids`). Preserve the existing early-break-with-500ms-overshoot-resample shape (also updating `captured_pids` on the overshoot resample if larger).
      4. Keep the three peak assertions verbatim (`peak > 0`; `peak <= SANDBOX_ACTIVE_PROCESS_LIMIT`; `peak < attempts`) and the `exit_code == 0` assertion. After them, assert `!captured_pids.is_empty()` (belt-and-braces: the reap check must not silently go vacuous again), then replace the final vacuous `wait_until` with `wait_until(|| surviving_captured_choice_pids(&captured_pids) == 0, 30, "fan-out descendants reaped after job close (by captured PID)")`. Keep the trailing `reap_stray_choice()`.
    - Do NOT touch the other tests. In particular the sibling `job_close_reaps_detached_descendant_with_no_residue` is the robust reap-property cover (its grandchild carries `rem {tag}` on its OWN command line, so `tagged_cmd_count` survives parent death) — leave it, `tagged_cmd_count`, and every other assertion unchanged. `tagged_choice_descendant_count` is used only inside `active_process_cap_is_enforced` (three call sites), so replacing it introduces no dead code elsewhere.
    - `#![cfg(windows)]`: uncompilable on Mac/Hetzner. Compile + behavior are proven ONLY at the 20-53 msvc re-dispatch; this task's Mac gates are scope + fmt + grep.
  </behavior>
  <action>In `crates/wcore-sandbox/tests/hard_process_containment_windows.rs`, close Finding 1 by making `active_process_cap_is_enforced`'s post-close reap check non-vacuous WITHOUT reintroducing the host-wide-image-count flake. Replace `tagged_choice_descendant_count(&tag) -> usize` with `tagged_choice_descendant_pids(&tag) -> Vec<u32>` (same `ParentProcessId`-scoped CIM query, but selecting/parsing the `ProcessId` list instead of `.Count`), and update its doc to the two-phase design (dropping the inaccurate "without weakening any containment assertion" line). Add `surviving_captured_choice_pids(pids: &[u32]) -> usize` that counts `choice.exe` whose `ProcessId` is in the captured set (fixed-PID intersected with image, empty-set yields 0). In the peak-sampling loop, capture the peak PID set into `captured_pids` alongside `peak` (preserving the early-break/overshoot-resample shape). Keep the three peak assertions (`peak > 0`, `peak <= SANDBOX_ACTIVE_PROCESS_LIMIT`, `peak < attempts`) and the clean-exit assertion verbatim; add `assert!(!captured_pids.is_empty(), ...)`; then replace the final vacuous `wait_until(... tagged_choice_descendant_count ... == 0 ...)` with `wait_until(|| surviving_captured_choice_pids(&captured_pids) == 0, 30, "fan-out descendants reaped after job close (by captured PID)")`, keeping the trailing `reap_stray_choice()`. Do NOT touch `job_close_reaps_detached_descendant_with_no_residue`, `tagged_cmd_count`, or any other test/assertion. Implements REQ-native-r7; Windows compile + behavior proven at 20-53.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-50)" crates/wcore-sandbox/tests/hard_process_containment_windows.rs</automated>
    <automated>vx rustfmt --edition 2024 --check crates/wcore-sandbox/tests/hard_process_containment_windows.rs</automated>
    <automated>grep -nE "tagged_choice_descendant_pids|surviving_captured_choice_pids|captured_pids" crates/wcore-sandbox/tests/hard_process_containment_windows.rs | head</automated>
    <automated>grep -nE "ExpandProperty ProcessId|-contains .*ProcessId" crates/wcore-sandbox/tests/hard_process_containment_windows.rs | head</automated>
    <automated>grep -nE "peak > 0|peak <= SANDBOX_ACTIVE_PROCESS_LIMIT|peak < attempts" crates/wcore-sandbox/tests/hard_process_containment_windows.rs | head</automated>
  </verify>
  <acceptance_criteria>Diff-scope is exactly the containment test and fmt passes. The count helper is replaced by a `ProcessId`-list helper, the post-close reap uses `surviving_captured_choice_pids` over a `captured_pids` set matched by fixed `ProcessId` (grep-confirmed), the three peak assertions and the clean-exit assertion are preserved verbatim, and `job_close_reaps_detached_descendant_with_no_residue` / `tagged_cmd_count` are untouched. Windows behavior (non-vacuous reap; no host-wide flake) proven at 20-53.</acceptance_criteria>
  <done>The active-process-cap post-close reap check catches a leaked/orphaned captured-`choice` survivor by fixed ProcessId without host-wide flakiness, as a provisional task commit.</done>
</task>

<task type="auto">
  <name>Task 2: Finding 2 — resolved_program_is_cmd matches cmd.exe as the exact final path component (not a suffix)</name>
  <files>crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs</files>
  <read_first>crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs, .planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md</read_first>
  <behavior>
    - `resolved_program_is_cmd(app_name_w: &[u16]) -> bool` decodes the NUL-terminated UTF-16 resolved `lpApplicationName`, lowercases it, and currently tests a bare suffix, which also matches `notcmd.exe`/`foocmd.exe`. When that classifier returns true for such a name, `process.rs` routes the `/c`/`/k` payload through the cmd-specific `quote_cmd_payload` instead of the general `quote_arg` — a misclassification (Finding 2). Reachability is bounded (the resolver pins bare `cmd` to `System32\cmd.exe` and only `cmd` runs under the Low-IL token), so it is not an isolation regression, but the classifier is imprecise.
    - Fix: keep the UTF-16 decode + lowercasing, but match `cmd.exe` as the exact FINAL PATH COMPONENT. Extract the final component via `std::path::Path::new(&decoded).file_name()` (on Windows `Path` treats both `\` and `/` as separators, covering the `System32\cmd.exe` resolution and any absolute path the caller passed) and compare it case-insensitively to `cmd.exe`; only a filename that IS `cmd.exe` classifies as cmd. Update the doc comment to say "final path component == cmd.exe" rather than a suffix.
    - Keep everything else byte-identical: `quote_cmd_payload`, `quote_arg`, `classify_bare_shell`, `resolve_program`, `is_unc_or_device_path`, and every other item. The #B quoting behavior is unchanged; only the cmd/non-cmd classification is tightened.
    - OPTIONAL (executor judgment, belt-and-braces, in-scope since command.rs is the declared file): add a small inline `#[cfg(test)]` unit assertion that `resolved_program_is_cmd` returns true for a widened `...\\System32\\cmd.exe` and false for a widened `...\\notcmd.exe`, to make the tightening a msvc-compiled regression check at 20-53. If added, it must be `#[cfg(windows)]`+`#[cfg(test)]`, touch no non-test code, and change no other behavior.
    - `windows_impl` (`#[cfg(windows)]`): uncompilable on Mac/Hetzner. Compile + behavior are proven ONLY at the 20-53 msvc re-dispatch; this task's Mac gates are scope + fmt + grep.
  </behavior>
  <action>In `crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs`, tighten `resolved_program_is_cmd` (Finding 2): keep the NUL-terminated UTF-16 decode and lowercasing, but extract the final path component with `std::path::Path::new(...).file_name()` (Windows `Path` handles both `\` and `/`) and compare it case-insensitively for equality with `cmd.exe`, replacing the bare-suffix test so `notcmd.exe`/`foocmd.exe` no longer classify as cmd. Update the doc comment to state the exact-final-component match. Leave `quote_cmd_payload`, `quote_arg`, `classify_bare_shell`, `resolve_program`, `is_unc_or_device_path`, and every other item byte-identical (the #B quoting behavior is unchanged). Optionally, at your judgment, add an inline `#[cfg(windows)]`+`#[cfg(test)]` assertion that a `...\\System32\\cmd.exe` widened path classifies as cmd and a `...\\notcmd.exe` widened path does not (regression cover compiled at 20-53) — touching no non-test code. Implements REQ-native-r2/r5; Windows compile + behavior proven at 20-53.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-50)" crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs</automated>
    <automated>vx rustfmt --edition 2024 --check crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs</automated>
    <automated>grep -nE "fn resolved_program_is_cmd" crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs</automated>
    <automated>grep -nE "file_name" crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs</automated>
    <automated>grep -nE "is_unc_or_device_path|SANDBOX_ACTIVE_PROCESS_LIMIT|quote_cmd_payload" crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs | head</automated>
  </verify>
  <acceptance_criteria>Diff-scope is exactly command.rs and fmt passes. `resolved_program_is_cmd` extracts the final path component (`file_name`, grep-confirmed) and compares it case-insensitively to `cmd.exe`, so a suffix like `notcmd.exe` no longer matches; `quote_cmd_payload`/`quote_arg`/`classify_bare_shell`/`resolve_program`/`is_unc_or_device_path` are grep-confirmed present and unchanged. Windows compile + behavior proven at 20-53.</acceptance_criteria>
  <done>The cmd classifier matches only an exact `cmd.exe` filename component, with the #B quoting behavior and the sandbox boundary intact, as a provisional task commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| containment proof ← post-close reap check | The active-process-cap reap assertion must remain a genuine detector of a leaked/orphaned descendant; a query that goes structurally empty once the tagged parent dies is a false-green containment proof |
| containment proof ← process counting scope | The reap check must not revert to a host-wide image count that a concurrent runner corrupts; it must count only this test's own processes |
| cmd/non-cmd classification ← command-line quoting | The classifier decides whether the `/c`/`/k` payload is cmd-quoted; a suffix match misclassifies non-cmd images and routes their payload through the wrong quoter |
| candidate identity ← Windows-only source | The two fixes cannot be compiled off Windows; a compile break is invisible until the msvc re-dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-50-01 | Spoofing | vacuous post-close reap assertion (Finding 1) | high | mitigate | The reap check captures the fan-out `choice` PIDs while the tagged parent is alive and re-checks those exact ProcessIds after close (intersected with image `choice.exe`), so a leaked/orphaned survivor is still detected — it can no longer pass on a structurally-empty query. |
| T-20-50-02 | Tampering | host-wide-count flake reintroduction | medium | mitigate | The post-close survivor check matches ONLY the test's own captured PIDs, so a concurrent `live_fs_acl` `choice.exe` (different PID) cannot inflate the count; the 20-43 Task 3 flake fix is preserved, not reverted. |
| T-20-50-03 | Tampering | weakening a sibling containment assertion | medium | mitigate | Only the vacuous assertion is replaced; the positive peak, cap ceiling, below-attempts bound, clean exit, and the robust sibling `job_close_reaps_detached_descendant_with_no_residue` are preserved verbatim (grep-confirmed for the peak assertions). |
| T-20-50-04 | Elevation of Privilege | cmd misclassification routing (Finding 2) | low | mitigate | `resolved_program_is_cmd` matches `cmd.exe` as the exact final path component (case-insensitive), so a `notcmd.exe`-style image is no longer routed through the cmd-specific quoter; the Low-IL token / Job-Object limits / ACL lease / `is_unc_or_device_path` are untouched. |
| T-20-50-05 | Tampering | undetectable Windows-only compile break | medium | accept | Both files are `#![cfg(windows)]` / `windows_impl` and cannot be compiled off Windows; risk is bounded by scope + fmt + grep gates and is proven at the 20-53 msvc re-dispatch (CONTEXT D5/D7). No pre-dispatch environment compiles the two fixes. |
</threat_model>

<verification>
Each task first passes its Mac construction gate (scope + `vx rustfmt --edition 2024 --check` + grep) as a provisional task commit. After both task commits exist and the checkout is clean, run the sanctioned pre-dispatch macOS compile re-confirmation and the committed-HEAD Hetzner no-regression gates (a construction commit is not behavioral evidence):
<automated>cargo check -p wcore-sandbox --features live-docker --tests</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-50-clippy "$(pwd -P)" clippy -p wcore-sandbox --all-targets --all-features -- -D warnings</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-50-aggregate "$(pwd -P)" nextest run --workspace --profile ci --no-fail-fast</automated>
The macOS `cargo check` re-confirms the macOS-compiled wcore-sandbox surface still compiles — it does NOT compile the `#![cfg(windows)]` containment test or the `windows_impl` classifier. The Hetzner clippy + aggregate are a NO-REGRESSION floor: because both fixes are Windows-only, the Linux aggregate `nextest --profile ci --no-fail-fast` MUST be exactly 11509/0/48 (a different count is a Linux regression that blocks the sequence — the honesty gate) and this aggregate runs WITH `--locked` because 20-50 changes no manifest (the lock is unchanged; 20-51 confirms zero lock delta). Record the exact committed `source_sha`/tree, the byte-identity check of the two files vs `3f839309`, the macOS compile receipt, the Hetzner clippy + aggregate receipts, changed paths, and the explicit deferral of ALL Windows compile/behavior (the non-vacuous reap check and the tightened cmd classifier) to the 20-53 msvc re-dispatch in `20-50-SUMMARY.md`. No native, aggregate-seal, or requirement claim is made.
</verification>

<success_criteria>
- Finding 1: `active_process_cap_is_enforced`'s post-close reap check captures the fan-out `choice` PIDs while the tagged parent is alive and re-checks those exact ProcessIds after close (intersected with image `choice.exe`), so it is non-vacuous AND not host-wide-flaky; every other containment assertion and the sibling reap test are preserved verbatim (r7).
- Finding 2: `resolved_program_is_cmd` matches `cmd.exe` as the exact final path component (case-insensitive), so a `notcmd.exe`-style suffix no longer classifies as cmd; the #B `quote_cmd_payload`/`quote_arg` behavior and the sandbox boundary are unchanged (r2/r5).
- Sanctioned macOS `cargo check --tests` is green, Hetzner clippy `-D warnings` is clean, and the aggregate is 11509/0/48 with no Linux regression (r4); the summary records the exact new candidate `source_sha`/tree and defers every hardware runtime claim to 20-53.
</success_criteria>

## Artifacts this phase produces
- The two 20-45-panel finding fixes (Finding 1 non-vacuous captured-PID reap in the containment test; Finding 2 exact-final-component cmd classifier in command.rs) as one reviewable further-repaired successor delta over the sealed-but-RED `3f839309`.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-50-SUMMARY.md` using the standard GSD summary template. Record the exact new candidate `source_sha`/tree, the byte-identity check of the two touched files vs `3f839309`, the macOS compile-check receipt, the Hetzner clippy + aggregate receipts, changed paths, the re-dispatch structure decision (new 20-50…20-56, superseding the RED-`3f839309`-bound 20-46…20-49), the confirmation that Finding 1's fix threads the needle (non-vacuous captured-PID reap without reintroducing the host-wide flake) and Finding 2 is an exact-component match, and the explicit deferral of all Windows compile/behavior to 20-53. Complete no Phase 20 requirement.
</output>
</content>
</invoke>
d45efe4f docs(20-44): seal third-repaired successor SUMMARY (Cargo.lock dunce...
f1dcf73b docs(20-45,20-47): cross-audit gate → 4-way panel (Codex Sol + Gemin...
8aeb7c6a docs(20-45): four-way pre-native cross-audit of sealed successor 3f8...
db16d18c docs(phase20): further-repaired successor 20-50..56 — fix vacuous re...
472d7c03 test(20-50): non-vacuous post-close reap by captured choice PID
f0dd5b6d fix(20-50): match cmd.exe as exact final path component

hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rtk git show f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:.planning/phases/20-transactional-delegated-mutation/20-45-raw/codex-sol.raw.txt
rtk git show f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:.planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md | sed -n '1,320p'
rtk git show f0dd5b6d312af616f268f96f34c3bc9fc962c4d2:.planning/phases/20-transactional-delegated-mutation/20-50-SUMMARY.md | sed -n '1,320p'" in /Users/seandonahoe/dev/waylandcore-ferrox
hook: PostToolUse
hook: PostToolUse Completed
 succeeded in 4568ms:
BLOCK. The sealed SHA/tree match, but three non-deferred defects refute the candidate:

- HIGH — `crates/wcore-sandbox/tests/hard_process_containment_windows.rs:140-160,365-371`: the post-close check re-discovers live tagged parent PIDs. Once the parent exits, `$parents` is empty and the helper returns zero even if detached `choice.exe` descendants leaked. This creates a false-green containment assertion.
- MEDIUM — `crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs:144-151,263-277` and `process.rs:620-640`: `ends_with("cmd.exe")` misclassifies absolute executables such as `notcmd.exe`. Their `/c` or `/k` payload receives cmd-specific raw quoting instead of CRT quoting, violating the claimed argv discipline.
- MEDIUM — `Cargo.lock:10183` and `.planning/phases/20-transactional-delegated-mutation/20-43-SUMMARY.md:67-70,93-121`: the alleged verbatim range omits `Cargo.lock` and 13 planning/evidence files. The sealed tree’s available repair summary binds to another SHA and reports 11508/1/48, not the claimed candidate-bound 11509/0/48 receipt. Evidence integrity is therefore unproven.

Guardrail A passes: probe and parse failures propagate as `DispatchAdmission`; no unlimited fallback exists. Kimi watch-items are explicitly carried in `20-46-PLAN.md:23-24,61,83`.

{"checks":{"all_severity":"FAIL","evidence_integrity":"FAIL","integration_authority":"FAIL"},"deferred":["native_macos","native_windows"],"findings":{"blocker":0,"critical":0,"high":1,"medium":2,"low":0},"guardrail_A_admission_budget":"PASS","guardrail_B_quoting_layer_only":"FAIL","kimi_watchitems_carried":"PASS","disposition":"BLOCK"}
# 20-45 Pre-native FOUR-WAY cross-audit — sealed third-repaired-successor `3f839309`

Gate: pre-native attestation before the scarce Sean-gated native run (20-46) is re-spent.
Panel: **Codex 5.6 Sol + Gemini 3.1 Pro + Kimi K3** (three external CLIs, run in parallel)
**+ internal Claude non-author adversarial reviewer** (prompted to REFUTE, default-refuted-if-uncertain).

- **sealed source_sha:** `3f839309574d6741eed416cd3820f56447f74eba`
- **sealed source_tree:** `3092475bb4102d010b6ff5f6c9d8080cb4f51928` (verified `3f839309^{tree}` == this)
- **review base tuple:** `source_sha` over `20-44-SUMMARY.md`; delta prosecuted = 20-43 repair over `daf27337`
- **branch:** `plan/f20-unified-audit-repair` (isolated checkout `/Users/seandonahoe/dev/waylandcore-ferrox`; `.git` is a directory; all git ops via `/usr/bin/git`)
- **deferred (only):** `native_macos`, `native_windows` (proven at 20-46)
- **raw outputs preserved:** `.planning/phases/20-transactional-delegated-mutation/20-45-raw/{codex-sol,gemini-pro,kimi-k3}.raw.txt` + shared `audit-context.shared.txt`

## External CLI invocations (verbatim)

| Auditor | Invocation | Exit | Reachable | Schema-valid |
|---|---|---|---|---|
| Codex 5.6 Sol | `codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check "<prompt>"` (brief on stdin) | 0 | YES | YES |
| Gemini 3.1 Pro | `gemini --skip-trust -m gemini-3.1-pro-preview -o text -p "<brief>"` (`GEMINI_CLI_TRUST_WORKSPACE=true`) | 0 | YES | YES |
| Kimi K3 | `/Users/seandonahoe/.kimi-code/bin/kimi -p "<brief>" --output-format text` (absolute path) | 0 | YES | YES |

All three external auditors were reachable and returned schema-validatable JSON, so the fail-closed
"external auditor unreachable/invalid" path was NOT triggered. This is a **finding-based BLOCK**, not an
incomplete-panel BLOCK.

## Per-auditor disposition

| # | Auditor id | Disposition | blocker/critical/high/medium/low | Guardrail A (admission budget) | Guardrail B (quoting-layer only) | Kimi watch-items carried |
|---|---|---|---|---|---|---|
| 1 | Codex 5.6 Sol (`f20-native-crossaudit.codex-sol`) | **BLOCK** | 0/0/1/2/0 | PASS | FAIL (ends_with imprecision) | PASS |
| 2 | Gemini 3.1 Pro (`f20-native-crossaudit.gemini-pro`) | PASS | 0/0/0/0/0 | PASS | PASS | PASS |
| 3 | Kimi K3 (`f20-native-crossaudit.kimi-k3`) | PASS | 0/0/0/0/0 | PASS | PASS | PASS |
| 4 | Claude adversarial (`wayland-core.phase20-independent-review.v1`) | **BLOCK** | 0/0/0/1/1 | PASS | PASS (isolation) / imprecise (classifier) | PASS |

## GATE DISPOSITION: **BLOCK — does NOT admit 20-46**

Two of four auditors (Codex 5.6 Sol + internal Claude adversarial) raise **real, non-deferred findings**.
Under the plan's ALL-FOUR-MUST-PASS rule, any single non-deferred finding from any one auditor stops the
sequence and routes to a **further repaired successor**. Gemini and Kimi returned zero-finding PASS but both
**missed** the post-close reap-assertion degradation — the exact class of shared blind spot a four-independent
panel exists to catch ("one auditor's miss is caught by the others").

**The findings are NOT rationalized away.** The blocking defect is a statically-provable code fact (not a
Windows-runtime question, not a hallucination):

1. **[MEDIUM — real, impact-mitigated] `active_process_cap_is_enforced` post-close reap assertion is vacuous.**
   `crates/wcore-sandbox/tests/hard_process_containment_windows.rs` — the fan-out idlers are **bare** `choice.exe`
   (choice rejects an injected tag), so `tagged_choice_descendant_count(&tag)` scopes them by the tagged parent
   cmd's `ParentProcessId`. By the time the post-close check runs, `run.await` has returned → the AppContainer job
   closed → the tagged parent cmd is dead → the CIM query for `cmd.exe` with the tag returns empty → `$parents`
   is empty → the count is **structurally 0** regardless of any leaked/orphaned `choice.exe`. The final
   `wait_until(|| tagged_choice_descendant_count(&tag) == 0, ...)` is therefore satisfied vacuously on the first
   poll. The prior host-wide `image_count("choice.exe") <= baseline` would have detected a leaked survivor; the new
   scoping cannot. The helper's own doc ("without weakening any containment assertion") and 20-43-SUMMARY's "Every
   containment assertion is preserved verbatim" are inaccurate for this assertion. **Impact-mitigated** because the
   reap-after-close *property* is robustly proven by the sibling `job_close_reaps_detached_descendant_with_no_residue`
   (whose grandchild carries `rem {tag}` on its OWN command line, so `tagged_cmd_count` survives parent death) —
   but the specific active-process-cap reap check is a degraded near-tautology. Flagged HIGH by Codex; honestly
   rated MEDIUM here given the sibling coverage.
   **Fix for the further-repaired successor:** capture the fan-out `choice.exe` PIDs (or the parent cmd `ProcessId`)
   BEFORE job close and re-check them by fixed PID after close, or retain a tag-independent host-wide safety net
   for the reap assertion.

2. **[LOW — real, low reachability] `resolved_program_is_cmd` uses `ends_with("cmd.exe")`.**
   `crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs` — `to_ascii_lowercase().ends_with("cmd.exe")`
   suffix-matches a hypothetical `notcmd.exe`/`foocmd.exe`, which would route its `/c`|`/k` payload through the
   cmd-specific `quote_cmd_payload` instead of `quote_arg`. Reachability is bounded (the resolver pins bare `cmd`
   to `System32\cmd.exe` and only `cmd` runs under the Low-IL token), so this is not an isolation-boundary
   regression — but it is an imprecise classifier. Flagged MEDIUM by Codex; honestly rated LOW here.
   **Fix:** compare the final path component `== "cmd.exe"` rather than a suffix match.

**Codex's third finding (evidence-integrity FAIL) is NOT carried as a real defect.** Codex read only
`20-43-SUMMARY.md` (the environmentally-disk-pressured `11508/1/48` run bound to `92cac8bb`) and did not reconcile
it with `20-44-SUMMARY.md`, which re-ran clean at **11509/0/48** (nextest run `32f1b4ba-ed6d-4d14-853f-f831d7798731`)
against the sealed tree `3092475b`, proved `--locked --workspace --all-features` exit 0, and sealed the candidate at
`3f839309` (adding exactly the one-line `Cargo.lock` dunce edge). The seal chain is correct; the `Cargo.lock` is part
of `daf27337..3f839309`. It is recorded here as an auditor observation, not a candidate defect.

**Guardrail verdicts (all four legs concur):**
- **Guardrail A (DispatchAdmission budget preserved):** PASS (4/4). Probe returns `Result<u64>`; on spawn/capture
  failure, non-zero status, or parse failure it returns `SwarmError::DispatchAdmission` (fail-closed). No
  `u64::MAX`, no `unwrap_or`, no catch-to-unlimited. `WCORE_SWARM_PROBE_ROOT` env transport changes delivery only,
  not the free-space computation. `dunce::simplified` is no-op on unix / genuine UNC.
- **Guardrail B (quoting-layer only, boundary primitives untouched):** PASS on isolation (4/4). The delta touches
  no `is_unc_or_device_path`, no Low-IL restricted token, no Job-Object limits, no ACL lease; argv discipline kept
  (payload is one caller-supplied argv entry, not `format!`-interpolated); the 20-36 `exit /b 0` normalization is
  intact. The only sub-finding is the LOW classifier imprecision above (Codex records it under Guardrail B).
- **Kimi watch-items carried into 20-46:** PASS (4/4). `20-46-PLAN.md` lines 24, 61, 83 carry git-ops-over-
  de-verbatimized-swarm_root, `tagged_cmd_count` CIM visibility under NetworkService, bash-worker-under-AppContainer
  + no-Windows-Docker-fallback, and worker test-exe DLL-load under Low-IL as explicit next-layer 20-46 checks
  (plus the extra `dispatch.rs:604` canonicalize watch-item).

---

## Artifact 1 — `f20-native-crossaudit.codex-sol` (Codex 5.6 Sol, external)

```json
{
  "schema": "f20-native-crossaudit.codex-sol",
  "source_sha": "3f839309574d6741eed416cd3820f56447f74eba",
  "source_tree": "3092475bb4102d010b6ff5f6c9d8080cb4f51928",
  "reviewer_id": "codex-5.6-sol",
  "reviewer_kind": "external-cli",
  "invocation": "codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check (brief on stdin)",
  "reachable": true,
  "schema_validated": true,
  "checks": {
    "all_severity": "FAIL",
    "evidence_integrity": "FAIL",
    "integration_authority": "FAIL"
  },
  "deferred": ["native_macos", "native_windows"],
  "findings": { "blocker": 0, "critical": 0, "high": 1, "medium": 2, "low": 0 },
  "guardrail_A_admission_budget": "PASS",
  "guardrail_B_quoting_layer_only": "FAIL",
  "kimi_watchitems_carried": "PASS",
  "evidence": [
    {
      "command": "hard_process_containment_windows.rs active_process_cap post-close reap: after the tagged parent exits, $parents is empty and tagged_choice_descendant_count returns 0 even if detached choice.exe descendants leaked — false-green containment assertion",
      "severity": "high",
      "result": "FAIL"
    },
    {
      "command": "command.rs resolved_program_is_cmd + process.rs cmd-payload routing: ends_with(\"cmd.exe\") misclassifies absolute executables such as notcmd.exe, whose /c|/k payload then receives cmd-specific raw quoting instead of CRT quoting",
      "severity": "medium",
      "result": "FAIL"
    },
    {
      "command": "Cargo.lock + 20-43-SUMMARY: alleged verbatim range omits Cargo.lock and planning/evidence files; the summary Codex read binds to another SHA and reports 11508/1/48 not 11509/0/48 — evidence integrity unproven from that summary alone [EXECUTOR NOTE: refuted — the clean 11509/0/48 seal is in 20-44-SUMMARY against tree 3092475b; not carried as a real candidate defect]",
      "severity": "medium",
      "result": "FAIL"
    }
  ],
  "disposition": "BLOCK"
}
```

## Artifact 2 — `f20-native-crossaudit.gemini-pro` (Gemini 3.1 Pro, external)

```json
{
  "schema": "f20-native-crossaudit.gemini-pro",
  "source_sha": "3f839309574d6741eed416cd3820f56447f74eba",
  "source_tree": "3092475bb4102d010b6ff5f6c9d8080cb4f51928",
  "reviewer_id": "gemini-3.1-pro-preview",
  "reviewer_kind": "external-cli",
  "invocation": "gemini --skip-trust -m gemini-3.1-pro-preview -o text -p <brief> (GEMINI_CLI_TRUST_WORKSPACE=true)",
  "reachable": true,
  "schema_validated": true,
  "checks": {
    "all_severity": "PASS",
    "evidence_integrity": "PASS",
    "integration_authority": "PASS"
  },
  "deferred": ["native_macos", "native_windows"],
  "findings": { "blocker": 0, "critical": 0, "high": 0, "medium": 0, "low": 0 },
  "guardrail_A_admission_budget": "PASS",
  "guardrail_B_quoting_layer_only": "PASS",
  "kimi_watchitems_carried": "PASS",
  "evidence": [
    {
      "command": "Gemini returned a zero-finding PASS across all three checks and both guardrails; it did NOT surface the active_process_cap post-close reap-assertion degradation that Codex + the internal Claude leg caught (panel blind-spot coverage)",
      "result": "PASS"
    }
  ],
  "disposition": "PASS"
}
```

## Artifact 3 — `f20-native-crossaudit.kimi-k3` (Kimi K3, external)

```json
{
  "schema": "f20-native-crossaudit.kimi-k3",
  "source_sha": "3f839309574d6741eed416cd3820f56447f74eba",
  "source_tree": "3092475bb4102d010b6ff5f6c9d8080cb4f51928",
  "reviewer_id": "kimi-k3",
  "reviewer_kind": "external-cli",
  "invocation": "/Users/seandonahoe/.kimi-code/bin/kimi -p <brief> --output-format text",
  "reachable": true,
  "schema_validated": true,
  "checks": {
    "all_severity": "PASS",
    "evidence_integrity": "PASS",
    "integration_authority": "PASS"
  },
  "deferred": ["native_macos", "native_windows"],
  "findings": { "blocker": 0, "critical": 0, "high": 0, "medium": 0, "low": 0 },
  "guardrail_A_admission_budget": "PASS",
  "guardrail_B_quoting_layer_only": "PASS",
  "kimi_watchitems_carried": "PASS",
  "evidence": [
    {
      "command": "Guardrail A: traced workspace_capacity -> available_workspace_bytes().await?; three fail-closed exits (capture_error/non-zero-status/parse -> DispatchAdmission), no unwrap_or/u64::MAX/catch-to-unlimited; dunce::simplified round-trips or returns the verbatim original so it cannot manufacture a wrong root",
      "result": "PASS"
    },
    {
      "command": "Guardrail B: argv discipline kept (one entry -> one token, not format!-interpolated); quote_cmd_payload wraps the existing entry in one outer pair (inner verbatim) as cmd /s requires; scope narrow (resolved cmd.exe + first exact /c|/k, flag_idx+1==len yields never-matching Some); no change to is_unc_or_device_path/token/Job/ACL; exit /b 0 present verbatim",
      "result": "PASS"
    },
    {
      "command": "r7/r3/nextest/evidence-integrity/watch-items: tag test-generated (safe -like interpolation); reap assertion judged preserved and CIM-visibility treated as Windows-runtime deferred; nextest cfg(windows) override inert on Linux (11509/0/48 consistent); watch-items ride the native_windows deferral into 20-46 without being closed or weakened [NOTE: Kimi treated the active_process_cap post-close reap check as belt-and-braces/runtime-deferred rather than a static code defect]",
      "result": "PASS"
    }
  ],
  "disposition": "PASS"
}
```

## Artifact 4 — `wayland-core.phase20-independent-review.v1` (internal Claude non-author adversarial)

```json
{
  "schema": "wayland-core.phase20-independent-review.v1",
  "source_sha": "3f839309574d6741eed416cd3820f56447f74eba",
  "source_tree": "3092475bb4102d010b6ff5f6c9d8080cb4f51928",
  "source_executor_id": "wayland-f20-native-repair-builder",
  "reviewer_id": "wayland-f20-45-claude-adversarial",
  "reviewer_kind": "internal-claude-adversarial",
  "checks": {
    "all_severity": "FAIL",
    "evidence_integrity": "PASS",
    "integration_authority": "PASS"
  },
  "deferred": ["native_macos", "native_windows"],
  "findings": { "blocker": 0, "critical": 0, "high": 0, "medium": 1, "low": 1 },
  "guardrail_A_admission_budget": "PASS",
  "guardrail_B_quoting_layer_only": "PASS",
  "kimi_watchitems_carried": "PASS",
  "evidence": [
    {
      "command": "SHA/tree bind: git rev-parse 3f839309^{tree} == 3092475bb4102d010b6ff5f6c9d8080cb4f51928 (verified); 3f839309 show --stat = Cargo.lock | 1 + (the dunce edge seal); the 20-43 source delta daf27337..3f839309 is exactly the 8 declared files + Cargo.lock + nextest.toml",
      "exit_code": 0,
      "result": "PASS"
    },
    {
      "command": "MEDIUM (non-deferred, real, impact-mitigated): hard_process_containment_windows.rs active_process_cap_is_enforced post-close reap assertion wait_until(tagged_choice_descendant_count(&tag)==0) is vacuously satisfied. The fan-out idlers are BARE choice.exe (choice rejects an injected tag) so the helper scopes by the tagged parent cmd ParentProcessId; by the post-close check run.await has returned -> job closed -> tagged parent cmd dead -> CIM cmd.exe-with-tag empty -> $parents empty -> count structurally 0 regardless of a leaked/orphaned choice. The prior host-wide image_count(choice.exe)<=baseline would have caught a leaked survivor. Contrast job_close_reaps_detached_descendant_with_no_residue whose grandchild carries rem {tag} on its OWN cmdline, so tagged_cmd_count survives parent death (robust). The helper doc 'without weakening any containment assertion' + 20-43-SUMMARY 'Every containment assertion is preserved verbatim' are inaccurate for this assertion. Static code fact, not a Windows-runtime question -> non-deferred. Impact-mitigated: reap-after-close property robustly proven by the sibling test",
      "exit_code": 0,
      "result": "FAIL"
    },
    {
      "command": "LOW (non-deferred, real, low reachability): command.rs resolved_program_is_cmd uses to_ascii_lowercase().ends_with(\"cmd.exe\") which suffix-matches notcmd.exe/foocmd.exe -> would route its /c|/k payload through quote_cmd_payload instead of quote_arg. Bounded reachability (resolver pins bare cmd to System32\\cmd.exe; only cmd runs under Low-IL) so NOT an isolation-boundary regression, but an imprecise classifier; should compare the final path component == cmd.exe",
      "exit_code": 0,
      "result": "FAIL"
    },
    {
      "command": "Guardrail A PASS: worktree_manager.rs workspace_capacity does available_workspace_bytes().await?; #[cfg(windows)] probe returns SwarmError::DispatchAdmission on spawn/capture failure, non-zero status, and parse failure; required = MAX_TRANSACTION_WORKSPACE_BYTES.min(...).checked_add(WORKSPACE_SAFETY_MARGIN_BYTES); grep u64::MAX|unwrap_or|unlimited in the probe path = none. env-var transport (command.env WCORE_SWARM_PROBE_ROOT) changes delivery only; dunce::simplified no-op on unix. No catch-to-unlimited",
      "exit_code": 0,
      "result": "PASS"
    },
    {
      "command": "Guardrail B PASS (isolation): git diff daf27337..3f839309 -- crates/wcore-sandbox grep is_unc_or_device_path|JOB_OBJECT_LIMIT|SECURITY_MANDATORY_LOW|restricted token|SetNamedSecurityInfo|icacls|acl_lease|CreateAppContainer = no boundary primitive touched (only a comment line matches 'Low-integrity'). quote_cmd_payload is applied only to the entry after the first /c|/k when resolved lpApplicationName ends_with cmd.exe; every other argv keeps quote_arg; payload stays one caller-supplied argv entry (argv discipline). 20-36 exit /b 0 present verbatim in all containment scripts",
      "exit_code": 0,
      "result": "PASS"
    },
    {
      "command": "Kimi watch-items carried: 20-46-PLAN.md lines 24/61/83 carry git-ops-over-de-verbatimized-swarm_root, tagged_cmd_count CIM visibility under NetworkService, bash-worker-under-AppContainer + no-Windows-Docker-fallback (dispatch.rs), worker test-exe DLL-load under Low-IL as explicit next-layer 20-46 checks (plus the extra dispatch.rs:604 canonicalize watch-item). Not silently dropped",
      "exit_code": 0,
      "result": "PASS"
    },
    {
      "command": "Evidence integrity PASS (candidate): 20-44-SUMMARY records --locked --workspace --all-features exit 0 + aggregate nextest run 32f1b4ba-ed6d-4d14-853f-f831d7798731 = 11509 passed / 0 failed / 48 skipped against tree 3092475b, and the seal adds exactly the one-line Cargo.lock dunce edge at 3f839309. Codex's evidence-integrity FAIL derives from reading only 20-43-SUMMARY (env-blocked 11508/1/48, source_sha 92cac8bb) and is refuted; not carried as a real candidate defect",
      "exit_code": 0,
      "result": "PASS"
    }
  ],
  "disposition": "BLOCK"
}
```

---

## Routing

BLOCK → a **further repaired successor** must (1) fix the `active_process_cap_is_enforced` post-close reap
assertion so a leaked/orphaned bare-`choice` idler is still detectable (capture the fan-out PIDs / parent
`ProcessId` before job close and re-check by fixed PID, or keep a tag-independent host-wide safety net), and
(2) tighten `resolved_program_is_cmd` to match the exact `cmd.exe` filename component rather than a suffix.
Then re-seal (Cargo.lock + `--locked` build + aggregate) and re-run this four-way pre-native cross-audit to a
zero-finding all-four PASS before 20-46 is authorized. All prior native-proof authorizations remain spent/void.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-HRu4ceZ0' (errno=Operation not permitted)
2026-07-24 14:42:41.091 xcodebuild[2386:161673002]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:42:41.201 xcodebuild[2386:161673001] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-46k5zKOw' (errno=Operation not permitted)
2026-07-24 14:42:41.577 xcodebuild[2394:161673028]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-24 14:42:41.678 xcodebuild[2394:161673027] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
fatal: path '.planning/phases/20-transactional-delegated-mutation/20-50-SUMMARY.md' exists on disk, but not in 'f0dd5b6d312af616f268f96f34c3bc9fc962c4d2'


hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "sed -n '1,300p' .planning/phases/20-transactional-delegated-mutation/20-50-SUMMARY.md
sed -n '1,300p' .planning/phases/20-transactional-delegated-mutation/20-51-SUMMARY.md
rtk git log --oneline --decorate f0dd5b6d312af616f268f96f34c3bc9fc962c4d2..HEAD | head -40
rtk git diff --name-status f0dd5b6d312af616f268f96f34c3bc9fc962c4d2..HEAD | head -120" in /Users/seandonahoe/dev/waylandcore-ferrox
hook: PostToolUse
hook: PostToolUse Completed
 succeeded in 4617ms:
eff0b3be (HEAD -> plan/f20-unified-audit-repair) docs(20-51): seal further-re...
b8a1c9d4 docs(20-50): summarize further-repaired successor closing 20-45 find...
A	.planning/phases/20-transactional-delegated-mutation/20-50-SUMMARY.md
A	.planning/phases/20-transactional-delegated-mutation/20-51-SUMMARY.md

--- Changes ---


codex
{"schema":"f20-native-crossaudit.codex-sol","source_sha":"f0dd5b6d312af616f268f96f34c3bc9fc962c4d2","source_tree":"ac76c87b318ee4ba8c34927dea23e40e63fd0776","reviewer_id":"codex-5.6-sol","reviewer_kind":"external-cli","checks":{"all_severity":"FAIL","evidence_integrity":"FAIL","integration_authority":"FAIL"},"deferred":["native_macos","native_windows"],"findings":{"blocker":0,"critical":0,"high":0,"medium":1,"low":1},"finding_1_reap_nonvacuous":"FAIL","finding_2_cmd_exact_component":"PASS","guardrail_A_admission_budget":"PASS","guardrail_B_quoting_layer_only":"PASS","evidence":[{"claim":"The sealed commit resolves to tree ac76c87b318ee4ba8c34927dea23e40e63fd0776; the non-planning delta against 3f839309 is exactly the two declared Windows-only files, with no Cargo.toml, workflow, worktree_manager.rs, process.rs, ACL-lease, or live_fs_acl change.","severity":"none","result":"PASS"},{"claim":"The captured-PID survivor query is syntactically well formed, scopes by the nonempty captured PID set intersected with choice.exe, and therefore detects a captured same-PID choice.exe survivor without counting ordinary concurrent choice.exe processes having different PIDs.","severity":"none","result":"PASS"},{"claim":"surviving_captured_choice_pids ignores PowerShell exit status and stderr, then converts empty or malformed stdout to zero with parse().unwrap_or(0); any CIM or PowerShell query failure therefore makes the post-close assertion pass immediately, preserving a statically provable false-green path. The nonempty captured_pids assertion does not guard the post-close observer.","severity":"medium","result":"FAIL"},{"claim":"Fixed PID plus image name is not a stable process identity after job close: Windows may reuse one of the captured PIDs for a concurrent choice.exe before the first survivor query, producing the host-concurrency false failure the fix claims to exclude. Capturing creation identity or retaining a process handle is required to eliminate this race.","severity":"low","result":"FAIL"},{"claim":"The peak-positive, peak-at-or-below-512, peak-below-attempts, and exit-code-zero assertions remain; job_close_reaps_detached_descendant_with_no_residue and its independently tagged descendant check are unchanged.","severity":"none","result":"PASS"},{"claim":"resolved_program_is_cmd now compares the lowercased exact Windows final path component to cmd.exe, accepting System32\\cmd.exe and rejecting notcmd.exe or foocmd.exe; quote_cmd_payload, quote_arg, classify_bare_shell, resolve_program, and is_unc_or_device_path are unchanged.","severity":"none","result":"PASS"},{"claim":"DispatchAdmission budget code is outside and byte-unchanged by this delta; the quoting change remains classifier-only, while Low-IL token setup, Job-Object limits, ACL leasing, argv construction, and exit /b 0 normalization are untouched.","severity":"none","result":"PASS"}],"disposition":"BLOCK"}
hook: Stop
hook: Stop Completed
tokens used
124,530
{"schema":"f20-native-crossaudit.codex-sol","source_sha":"f0dd5b6d312af616f268f96f34c3bc9fc962c4d2","source_tree":"ac76c87b318ee4ba8c34927dea23e40e63fd0776","reviewer_id":"codex-5.6-sol","reviewer_kind":"external-cli","checks":{"all_severity":"FAIL","evidence_integrity":"FAIL","integration_authority":"FAIL"},"deferred":["native_macos","native_windows"],"findings":{"blocker":0,"critical":0,"high":0,"medium":1,"low":1},"finding_1_reap_nonvacuous":"FAIL","finding_2_cmd_exact_component":"PASS","guardrail_A_admission_budget":"PASS","guardrail_B_quoting_layer_only":"PASS","evidence":[{"claim":"The sealed commit resolves to tree ac76c87b318ee4ba8c34927dea23e40e63fd0776; the non-planning delta against 3f839309 is exactly the two declared Windows-only files, with no Cargo.toml, workflow, worktree_manager.rs, process.rs, ACL-lease, or live_fs_acl change.","severity":"none","result":"PASS"},{"claim":"The captured-PID survivor query is syntactically well formed, scopes by the nonempty captured PID set intersected with choice.exe, and therefore detects a captured same-PID choice.exe survivor without counting ordinary concurrent choice.exe processes having different PIDs.","severity":"none","result":"PASS"},{"claim":"surviving_captured_choice_pids ignores PowerShell exit status and stderr, then converts empty or malformed stdout to zero with parse().unwrap_or(0); any CIM or PowerShell query failure therefore makes the post-close assertion pass immediately, preserving a statically provable false-green path. The nonempty captured_pids assertion does not guard the post-close observer.","severity":"medium","result":"FAIL"},{"claim":"Fixed PID plus image name is not a stable process identity after job close: Windows may reuse one of the captured PIDs for a concurrent choice.exe before the first survivor query, producing the host-concurrency false failure the fix claims to exclude. Capturing creation identity or retaining a process handle is required to eliminate this race.","severity":"low","result":"FAIL"},{"claim":"The peak-positive, peak-at-or-below-512, peak-below-attempts, and exit-code-zero assertions remain; job_close_reaps_detached_descendant_with_no_residue and its independently tagged descendant check are unchanged.","severity":"none","result":"PASS"},{"claim":"resolved_program_is_cmd now compares the lowercased exact Windows final path component to cmd.exe, accepting System32\\cmd.exe and rejecting notcmd.exe or foocmd.exe; quote_cmd_payload, quote_arg, classify_bare_shell, resolve_program, and is_unc_or_device_path are unchanged.","severity":"none","result":"PASS"},{"claim":"DispatchAdmission budget code is outside and byte-unchanged by this delta; the quoting change remains classifier-only, while Low-IL token setup, Job-Object limits, ACL leasing, argv construction, and exit /b 0 normalization are untouched.","severity":"none","result":"PASS"}],"disposition":"BLOCK"}

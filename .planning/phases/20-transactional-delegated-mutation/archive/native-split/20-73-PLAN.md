---
phase: 20-transactional-delegated-mutation
plan: "73"
type: execute
wave: 69
depends_on:
  - "20-72"
files_modified:
  - crates/wcore-sandbox/src/directory_authority.rs
  - crates/wcore-sandbox/src/directory_authority_windows.rs
  - crates/wcore-swarm/src/worktree_security.rs
  - crates/wcore-swarm/src/worktree.rs
  - crates/wcore-swarm/src/worktree_manager.rs
  - crates/wcore-swarm/src/worktree_cleanup.rs
  - crates/wcore-swarm/src/worktree_tests.rs
  - crates/wcore-swarm/tests/dispatch_smoke.rs
autonomous: true
gap_closure: true
requirements:
  - REQ-native-r6
  - REQ-native-r4
  - REQ-native-r12
must_haves:
  truths:
    - "PART A ROOT CAUSE (hardware-proven on SEANDESKTOP, Win11 10.0.26200.8875, NTFS — DO NOT re-litigate, do not re-derive): Windows byte-range locking is UNDEFINED on directory objects. `LockFileEx(<directory HANDLE>, LOCKFILE_EXCLUSIVE_LOCK, 0, 1, 0, &OVERLAPPED{0})` returns FALSE with `GetLastError() == 87 (ERROR_INVALID_PARAMETER)` for ANY directory handle regardless of access mode or share mode, and Rust maps 87 to `ErrorKind::InvalidInput`. Measured truth table: DIR access=GR|GW|DELETE share=R|W|D -> FAIL 87; DIR access=GR|GW share=R|W|D -> FAIL 87; DIR access=GR share=R|W|D -> FAIL 87; FILE access=GR|GW share=R|W|D -> OK. Refuted alternatives, all of which SUCCEED on a directory handle on this box and are therefore NOT the cause: `FlushFileBuffers`; `SetFileInformationByHandle(FileDispositionInfoEx)`; `SetFileInformationByHandle(FileDispositionInfo)`. The DELETE access bit is IRRELEVANT here — that is the separate defect 20-72 owns. `fd-lock 4.0.4` `src/sys/windows/rw_lock.rs::write` is exactly that `LockFileEx` call."
    - "SCOPE OF THE PART A DEFECT — THREE sites in `crates/wcore-swarm/src/worktree.rs`, ALL unconditionally broken on Windows since inception, therefore Windows mutual exclusion across every transaction path has NEVER worked (a correctness/concurrency defect, not a cosmetic one): (1) `with_directory_lock` clones the authority's DIRECTORY handle and blocks on `lock.write()` — callers `worktree.rs` `TransactionCleanup::release`, `worktree_manager.rs` isolated-checkout registration, `worktree_manager.rs` integration-checkout registration, ALL THREE passing `(&self.swarm_root, &self.swarm_authority)`; (2) `ActiveLease::acquire` takes `lock.write()` on a `DirectoryHandleLoan` — callers `worktree_cleanup.rs::cleanup_all` (swarm-root authority) and the two checkout registration closures (transaction-root authority); (3) `transaction_is_active` takes `lock.try_write()` on a directory handle — callers `worktree_cleanup.rs::reserved_workspace_bytes` and `worktree_cleanup.rs::cleanup_all`. Every one of the eight call sites fails closed with `InvalidInput` on Windows today."
    - "THE PART A FIX — take the advisory lock on a REGULAR FILE, never a directory, and resolve that file HANDLE-RELATIVE to the retained directory so the anti-swap property is preserved. The crate already locks a real file correctly at `worktree/parent.rs::open_landing_lock_file` (a plain `OpenOptions` create-or-open handed to `fd_lock::RwLock` by `land_candidate` and `rollback_landing_inner`), so this is the established in-repo pattern (AGENTS.md: match existing patterns). What changes is ONLY the lock target: the `DirectoryAuthority` is retained unchanged for IDENTITY, and `with_directory_lock` keeps its before-and-after `validate_path` calls verbatim. The sentinel is resolved through the retained handle rather than by re-resolving a pathname, so a swap of the directory between validation and lock acquisition cannot redirect the lock."
    - "THE LOCK TARGETS, and why each is where it is: the transaction lease is `<transaction_root>/.wayland-active-lease` — the file the code ALREADY creates and then immediately drops without ever locking it, so this fix simply makes the code do what its own `LEASE_FILE` constant already claims, and `transaction_is_active` gains the meaning its name already implies. The swarm sentinel is `<swarm_root>/.wayland-control/.wayland-swarm-lock`. Placing it INSIDE `CONTROL_DIR` is REQUIRED, not merely tidy: four production loops enumerate `read_dir(&self.swarm_root)` and skip only `CONTROL_DIR` — `worktree_cleanup.rs::reserved_workspace_bytes`, `worktree_cleanup.rs::cleanup_all`, `worktree_manager.rs::retained_worker_count`, `worktree_manager.rs::sandbox_read_denies` — and the first three hard-`Err` through `is_real_directory_entry` on any non-directory direct child. A sentinel placed directly under `swarm_root` would therefore break admission accounting, cleanup and the residual sweep. Inside `CONTROL_DIR` the sentinel needs ZERO enumeration changes and is already covered by the unconditional `control_root` entry in `sandbox_read_denies`."
    - "ONE SENTINEL PER SEMANTIC LOCK, because the interlock set must not be split: all three `with_directory_lock` callers AND `cleanup_all`'s lease currently take an exclusive lock on the SAME swarm-root directory object, so admission critical sections and full cleanup serialize against each other today. They must all move to the SAME swarm sentinel or that serialization silently disappears. Likewise the per-transaction lease and the `transaction_is_active` probes must share `LEASE_FILE`. The plan therefore introduces exactly one `swarm_lock_handle` and one `transaction_lease_handle` derivation and routes every site through them (AGENTS.md: no duplicate code)."
    - "LOAN ACCOUNTING IS PRESERVED EXACTLY, not merely 'not broken': today `ActiveLease` holds a `DirectoryHandleLoan` on the transaction root, and `remove_open_dir_all` REFUSES while `has_outstanding_handle_loans()` is true — that refusal is load-bearing (`TransactionCleanup::release` closes the lease before the cleanup block for exactly this reason). The new sandbox primitive therefore returns a `DirectoryHandleLoan` whose `loans` counter is the PARENT DIRECTORY's counter and whose `handle` is the child lock file, so the delete-refusal invariant is byte-for-byte unchanged. `DirectoryHandleLoan` already implements `Deref<Target = File>`, `AsFd` and `AsHandle` (`directory_authority.rs:36-56`), so it drops straight into `fd_lock::RwLock` and `ActiveLease::acquire`'s signature does not change at all."
    - "THE UNIX DECISION (made, not left open): UNIX MOVES TO THE LOCK FILE TOO — one code path, no `#[cfg]` in `wcore-swarm`. Reasoning: a platform split would put TWO different mutual-exclusion mechanisms inside the crate's most safety-critical primitive across three functions, and would make the Windows mechanism unexercisable by any test that runs on Linux CI. That is precisely the condition that hid this defect for the project's entire life, and reintroducing it would guarantee the next Windows lock regression is equally invisible. One mechanism means one regression test proving one code path everywhere. MIGRATION CONSEQUENCE, accepted and recorded: the on-disk lock artifact changes, so during a version-skew window a unix process running an older build (locking the directory) and one running this build (locking the sentinel) will NOT interlock. Blast radius is bounded to admission serialization and aggregate capacity accounting; it weakens NO authority proof, because `validate_swarm_root`, `validate_repo_authority`, every `DirectoryAuthority` identity check and the reservation receipts are all lock-independent and still fail closed. Windows has NO compatibility surface to preserve — the lock never worked there at all — so the skew concern is unix-only and one-directional."
    - "PART B — the empirically established Windows rename truth table (probe, NTFS), which every Part B correction is derived from: RULE 1, a handle on an object NEVER blocks renaming that object provided the share mode includes FILE_SHARE_DELETE, and desired access is irrelevant; RULE 2, without FILE_SHARE_DELETE the rename is refused with ERROR_SHARING_VIOLATION (32), which is NOT `PermissionDenied`; RULE 3, ANY open handle to a descendant (file or directory, any share mode) blocks renaming ANY ancestor with ERROR_ACCESS_DENIED (5), which Rust maps to `PermissionDenied`. Every existing Windows arm in these tests attributes its outcome to share-mode reasoning; all of them are actually rule 1 or rule 3."
    - "B1 — `worktree_tests.rs::transaction_cleanup_never_deletes_swap_after_validation` has a WRONG TEST and NO production hole. Its Windows arm asserts the rename of the handle-held transaction root is refused; by rule 1 that rename actually SUCCEEDS, and its comments state the inverse of real Windows semantics. Direct probe of the production mechanism proved the defense sound: `remove_open_dir_all` (`directory_authority_windows.rs:442-475`) enumerates via `NtQueryDirectoryFile` on the retained handle, opens children handle-relative via `NtCreateFile` with `RootDirectory = authority.handle`, and disposes via `SetFileInformationByHandle` — every operation handle-bound, nothing re-resolves the pathname. After a swap the handle follows the OBJECT: the substituted directory at the original path survives untouched with its replacement receipt readable, and the moved original is deleted. FIX: delete the platform split and run the unix body on BOTH platforms. This fixture is the one that CAN construct the swap on Windows, because it opens NO descendant handle inside `root` — only `DirectoryAuthority::open(&root)` on the object itself — so rule 1 permits the rename. The test becomes STRONGER: it exercises the real handle-bound defense instead of asserting a false OS guarantee."
    - "B2 — `worktree_tests.rs::transaction_cleanup_preserves_same_path_replacement` PASSES for a reason unrelated to its stated rationale, and its fixture is what makes it pass. `test_transaction_cleanup` writes `RESERVATION_FILE` into `root` and retains a `RegularFileAuthority` on it, and that open DESCENDANT is what blocks the rename by rule 3 — not the share-mode story the comment tells. Remove the reservation file from the fixture and the assertion silently flips to failing. UNLIKE B1 the swap is NOT constructible here: `TransactionCleanup` structurally REQUIRES a `reservation_authority` field which `release()` validates, so the descendant handle cannot be dropped without dropping the transaction. FIX (decided after empirical confirmation): keep the platform split, correct the rationale to cite the descendant rule explicitly, and record the fixture coupling in-source so the fragility is visible. The same-path-replacement software defense DOES gain real Windows coverage — from B1's now-unified test, whose fixture holds no descendant handle."
    - "B3 — `worktree_tests.rs::failed_transaction_cleanup_remains_retryable` has a SOUND assertion (renaming `swarm_root` while its `worker-retry` descendant handles are open is rule 3) and a WRONG explanation. Only the clause attributing the refusal to share-mode semantics is corrected; no assertion, gate or polarity changes."
    - "B4 — COVERAGE REGRESSION introduced by `334f264d` in `crates/wcore-swarm/tests/dispatch_smoke.rs`. `assert_ancestor_rename_os_refused` is non-vacuous and TRUE and both live tests pass on the box, but (i) its doc comments attribute the refusal to the share-delete flag when the real cause is rule 3, and (ii) the claim that the substitution is physically impossible while handles are held is OVERCLAIMED — it is impossible via the ANCESTOR construction only. This is a TEST-COVERAGE regression, NOT a security hole: `validate_repo_authority` -> `DirectoryAuthority::validate_path` is platform-independent, reached from 11 sites, and would catch the substitution — but after `334f264d` Windows no longer exercises it, asserting an OS property instead of the software defense."
    - "B4 PREMISE CORRECTION (verified in source; the brief's proposed construction is very likely REFUSED): the reasoning that an attacker could rename the held `repo` directory ITSELF depends on the swarm holding NO descendant handle inside `repo`. That holds for `new_with_workspace_authority`, but these tests use `Swarm::new` (`lib.rs:251`), which calls `WorktreeManager::new`, which sets `swarm_root = <repo>/.swarm-worktrees` and RETAINS `swarm_authority` on it — a live DESCENDANT handle inside `repo`. By rule 3 the rename of `repo` itself is therefore refused with ERROR_ACCESS_DENIED, exactly like the ancestor rename. The executor must confirm this empirically before acting on it, and the plan supplies the construction that DOES restore software-defense coverage on both platforms: a `WorktreeManager::new_with_workspace_root` topology whose swarm root lives OUTSIDE the repository, leaving `repo_authority` on `repo` itself as the only handle inside it, so rule 1 permits the rename and `validate_repo_authority` is genuinely exercised. 20-72 makes `repo_authority` observational; per rule 1 access mode is irrelevant to renaming the object itself, so that change does not affect this analysis."
    - "HARDWARE GATE (REQ-native-r6/r4/r12): the Mac CANNOT compile this workspace — only `cargo fmt --all` works there. Every build, test, clippy and measurement runs on SEANDESKTOP (`ssh -o BatchMode=yes SeanD@seandesktop`, checkout `C:\\ferrox-win`). 20-72 lands FIRST and its stated post-state is `cargo nextest run -p wcore-swarm --no-fail-fast` at 84 run / 81 passed / 3 failed / 6 skipped, where the 3 residual failures are exactly the Part A and Part B tests this plan fixes. That number is a PREDICTION, not a measurement: the executor MUST re-measure the actual baseline on the real post-20-72 tree before claiming any delta. Target after this plan: 0 failures, plus 9/9 on `wcore-agent --test transactional_delegated_mutation_test`, all of `wcore-swarm --test dispatch_smoke` green including the rewritten replacement tests, and clippy plus fmt clean."
  artifacts:
    - path: crates/wcore-sandbox/src/directory_authority_windows.rs
      provides: "The Windows handle-relative lock-file open — two new `RelativeIntent` arms granting `GENERIC_READ | GENERIC_WRITE | SYNCHRONIZE` (no DELETE) with `FILE_OPEN` / `FILE_OPEN_IF` dispositions — documented with the LockFileEx-on-directory finding so nobody retargets a lock at a directory again"
    - path: crates/wcore-sandbox/src/directory_authority.rs
      provides: "`DirectoryAuthority::open_child_lock_file` and `open_or_create_child_lock_file` — cross-platform, handle-relative, returning a parent-accounted `DirectoryHandleLoan` so the delete-refusal invariant is unchanged"
    - path: crates/wcore-swarm/src/worktree_security.rs
      provides: "The two lock-file constructors on the crate-private wrapper, so `wcore-swarm` carries no `#[cfg]` for this"
    - path: crates/wcore-swarm/src/worktree.rs
      provides: "`SWARM_LOCK_FILE`, the single `swarm_lock_handle` and `transaction_lease_handle` derivations, and the three repaired lock sites (`with_directory_lock`, `ActiveLease::acquire` callers, `transaction_is_active`)"
    - path: crates/wcore-swarm/src/worktree_manager.rs
      provides: "Both checkout registration closures acquiring the lease on the lease FILE they already create, with the redundant create-and-drop retired"
    - path: crates/wcore-swarm/src/worktree_cleanup.rs
      provides: "`cleanup_all` taking the swarm sentinel, so full cleanup still serializes against admission"
    - path: crates/wcore-swarm/src/worktree_tests.rs
      provides: "The mutual-exclusion invariant tests for both lock targets, B1's unified swap test, and the corrected B2/B3 rationales"
    - path: crates/wcore-swarm/tests/dispatch_smoke.rs
      provides: "Rule-accurate rationales for the Windows arms and the new cross-platform test that restores real `validate_repo_authority` coverage"
    - path: .planning/phases/20-transactional-delegated-mutation/20-73-SUMMARY.md
      provides: "Record of the LockFileEx-on-directory root cause, the unix unification decision and its migration consequence, the Part B rule attributions, the B2 and B4 empirical determinations, and the SEANDESKTOP counts"
  key_links:
    - from: crates/wcore-swarm/src/worktree.rs
      to: crates/wcore-sandbox/src/directory_authority.rs
      via: "Every advisory lock is taken on a regular-file child resolved through the retained directory handle, so `LockFileEx` receives a file object (defined) instead of a directory object (undefined, error 87), while the `DirectoryAuthority` continues to supply identity via `validate_path`"
      pattern: "native_windows"
    - from: crates/wcore-swarm/src/worktree.rs
      to: crates/wcore-swarm/src/worktree_cleanup.rs
      via: "`with_directory_lock` and `cleanup_all` resolve the SAME `<swarm_root>/.wayland-control/.wayland-swarm-lock` sentinel through one shared derivation, preserving the admission-versus-cleanup serialization that the shared directory handle provided today"
      pattern: "native_windows"
    - from: crates/wcore-swarm/tests/dispatch_smoke.rs
      to: crates/wcore-swarm/src/worktree_manager.rs
      via: "A swarm root placed OUTSIDE the repository leaves `repo_authority` as the only handle inside `repo`, so rule 1 permits renaming it and `validate_repo_authority` is exercised as the software defense on Windows as well as unix"
      pattern: "native_windows"
---

<objective>
Close a production concurrency defect and a test-coverage regression that share one root: nobody ever proved what these Windows tests claim to prove. PART A retargets every swarm advisory lock from a DIRECTORY handle — on which `LockFileEx` is undefined and returns error 87 unconditionally — to a REGULAR FILE resolved handle-relative from the retained directory, so Windows mutual exclusion across the transaction paths works for the first time. PART B corrects four tests whose Windows arms assert OS properties that are either false or true-for-the-wrong-reason, and restores the software-defense coverage that commit `334f264d` removed.

Purpose: `fd-lock 4.0.4` calls `LockFileEx` directly, and Windows byte-range locking is undefined on directory objects — measured on SEANDESKTOP as FALSE with `ERROR_INVALID_PARAMETER` for every access mode and share mode tried, while the same call on a regular file succeeds. All three lock helpers in `worktree.rs` pass a directory handle, so all eight of their call sites have failed closed on Windows since inception: admission critical sections, transaction leases, full cleanup and the active-transaction probe have never mutually excluded anything on that platform. The crate already knows how to do this correctly — `worktree/parent.rs::open_landing_lock_file` locks a real file — so the repair follows an in-repo pattern rather than inventing one. Separately, four Windows test arms explain their outcomes with share-mode reasoning that the rename truth table refutes: one asserts a refusal that does not happen, one passes only because of an incidental fixture handle, one is right for the wrong reason, and one traded the platform-independent software defense for an OS-behaviour assertion.
Output: A handle-relative lock-file primitive in `wcore-sandbox`; one sentinel per semantic lock in `wcore-swarm` with no platform conditional; mutual-exclusion invariant tests that fail if anyone retargets a lock at a directory; four rule-accurate test corrections; restored `validate_repo_authority` coverage on both platforms; and SEANDESKTOP-green counts re-measured against the real post-20-72 tree.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-NATIVE-REPAIR-PRD.md
@crates/wcore-sandbox/src/directory_authority.rs
@crates/wcore-sandbox/src/directory_authority_windows.rs
@crates/wcore-swarm/src/worktree.rs
@crates/wcore-swarm/src/worktree_security.rs
@crates/wcore-swarm/src/worktree_manager.rs
@crates/wcore-swarm/src/worktree_cleanup.rs
@crates/wcore-swarm/src/worktree/parent.rs
@crates/wcore-swarm/src/worktree_tests.rs
@crates/wcore-swarm/tests/dispatch_smoke.rs
</context>

<execution_rules>
- SEQUENCING (hard): plan 20-72 lands FIRST and edits `crates/wcore-sandbox/src/directory_authority.rs`, `crates/wcore-sandbox/src/directory_authority_windows.rs`, `crates/wcore-swarm/src/worktree_manager.rs`, and adds a test to `crates/wcore-swarm/src/worktree_tests.rs`. Do NOT duplicate, revert or second-guess its work: it converts the repo-root authority to a read-only observational open to fix a DELETE-access-bit/chdir defect, which is a DIFFERENT defect from this plan's. EVERY line number quoted in this plan is a pre-20-72 landmark and WILL have drifted — some already have. Locate every edit by SYMBOL NAME and surrounding code, never by line number, and rebase all expectations (including test counts and gate counts) on the actual file contents at execution time.
- COMPILE DISCIPLINE (hard): the Mac CANNOT compile this workspace. Do NOT run `cargo build`, `cargo check`, `cargo clippy`, `cargo test` or `cargo nextest` on the Mac. `cargo fmt --all` DOES work on the Mac and MUST be run before every commit. All compilation, clippy, tests and measurements run on SEANDESKTOP: `ssh -o BatchMode=yes SeanD@seandesktop`, checkout `C:\ferrox-win`.
- REMOTE SHELL FORM (hard): every remote invocation uses the script-block form `ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "<cmd> 2>&1" }'`. Git on the box MUST be wrapped as `cmd /c "git ..."` — PowerShell's Stop preference treats git's stderr chatter as fatal and aborts otherwise. Wrapping cargo the same way keeps cargo's progress chatter off stderr-as-fatal.
- Because the Mac cannot compile, Tasks 1-5 verify at SOURCE level only (`cargo fmt --all -- --check` plus targeted greps). No behavioural claim is made before Task 6.
- GREP HYGIENE (hard, and MEASURED — do not "simplify" these gates back to bare `grep`): the local `grep` is rtk-proxied to a different engine that both treats `(` and `|` as metacharacters AND silently mis-filters inverted matches. Measured on this tree: `grep -v '^[[:space:]]*//' crates/wcore-swarm/src/worktree.rs | wc -l` returns 32 lines where `/usr/bin/grep -v` on the same 704-line file returns 674 — the proxy drops 642 lines without erroring, which would make any comment-filtered gate pass or fail for reasons unrelated to the code. EVERY gate in this plan therefore invokes `/usr/bin/grep` explicitly, exactly as it invokes `/usr/bin/git`, and every literal gate uses `-F` (fixed strings). Where a gate must exclude comment lines it filters first with `/usr/bin/grep -v '^[[:space:]]*//'` so a doc comment can never satisfy or break a code gate. Do NOT add count-based gates over whole files that 20-72 also edits — use presence and absence gates on literals this plan alone introduces or removes.
- Use `/usr/bin/git` on the Mac. NEVER stage the AGENTS.md or `.ijfw` churn: `/usr/bin/git add` the exact source paths listed in `files_modified`, never `-A`, never `.`. No `Co-Authored-By` trailers.
- AGENTS.md discipline: surgical diffs only (every changed line traces to Part A or to a numbered Part B item), no drive-by refactors, no reformatting of adjacent code, `thiserror` for public error types / `anyhow` internally (here: the existing `SandboxError` and `SwarmError` plus the crates' `Result` aliases), clippy-clean, platform differences centralized in `wcore-sandbox` so `wcore-swarm` gains no `#[cfg]`.
- No new crate, no new dependency, no `Cargo.toml` or `Cargo.lock` change: `fd-lock` 4.0.4 and `windows-sys` are already resolved, and every symbol used here is already reachable.
- Do NOT modify or revisit the runner-proven commits `00f453f2`, `aaeeb379`, `0e867008`, the path-representation repair `ece09681..919629ee`, or whatever 20-72 lands. Do NOT touch `crates/wcore-sandbox/tests/live_fs_acl.rs` or `crates/wcore-sandbox/tests/hard_process_containment_windows.rs` — those are other plans' surfaces.
- The Part A root cause and the Part B rename truth table are PROVEN. Do not re-derive them, do not add diagnostic probes to production code, and do not re-measure them except where a task explicitly demands an empirical determination (Task 4's B2 and Task 5's B4).
- NEVER assert an error code or `ErrorKind` that encodes the DEFECT (`InvalidInput`, `ERROR_INVALID_PARAMETER`, 87) anywhere in a test. Tests assert the INVARIANT — that mutual exclusion holds — not the shape of today's failure.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Add the handle-relative lock-file primitive to wcore-sandbox (Part A foundation)</name>
  <files>crates/wcore-sandbox/src/directory_authority_windows.rs, crates/wcore-sandbox/src/directory_authority.rs</files>
  <read_first>crates/wcore-sandbox/src/directory_authority.rs (the `DirectoryHandleLoan` struct and its `Deref`/`AsFd`/`AsHandle`/`Drop` impls near the top; `try_clone_handle`; `open_child_file`; `create_child_file`; `open_or_create_child_directory`), crates/wcore-sandbox/src/directory_authority_windows.rs (the `RelativeKind`/`RelativeIntent` enums, `open_relative` in full including its `desired_access` match and its disposition expression, and `open_child_file`), crates/wcore-swarm/src/worktree/parent.rs (`open_landing_lock_file` — the in-repo pattern being followed)</read_first>
  <behavior>
    - A caller can obtain a lockable regular-file child of a retained directory WITHOUT re-resolving any pathname: the child is opened through the retained directory handle, so a swap of that directory cannot redirect the lock.
    - The returned handle is accepted by `fd_lock::RwLock` on both platforms and an exclusive lock on it SUCCEEDS on Windows, because the lock target is a file object rather than a directory object.
    - Two variants exist, mirroring the crate's established `open_child_*` / `open_or_create_child_*` naming: an open-only variant that surfaces a not-found error, and an open-or-create variant.
    - The returned value is loan-accounted against the PARENT directory's counter, so a directory whose lock file is currently held still refuses destructive removal exactly as it does today.
    - The Windows handle carries read and write access but NOT delete access — the lock needs neither the ability to destroy the file nor the access bit that 20-72 proved blocks a chdir.
    - Unix behaviour is a plain relative open on a private-mode file; no directory handle is ever handed to the locking layer on any platform.
  </behavior>
  <action>In `crates/wcore-sandbox/src/directory_authority_windows.rs`, extend the private `RelativeIntent` enum with two variants for the lock-target open — one that opens an existing child and one that opens-or-creates it. Wire them into `open_relative` in exactly two places and no others: (a) the `desired_access` match gains an arm for `RelativeKind::File` paired with each new intent, granting `GENERIC_READ | GENERIC_WRITE | SYNCHRONIZE`; (b) the disposition expression, which today is a two-way choice between the create disposition and the open disposition, becomes a three-way selection so the open-or-create intent maps to `FILE_OPEN_IF` (import it alongside the existing disposition constants from `windows_sys::Wdk::Storage::FileSystem`) while the open-only intent keeps the plain open disposition. Pair each new intent with `RelativeKind::Directory` and `RelativeKind::Any` by returning the same kind of explicit `SandboxError::ExecFailed` refusal the existing enum already uses for an unsupported combination — a lock target is a regular file by construction, and silently accepting a directory here is the exact defect being closed. Do NOT alter any existing arm, do NOT alter the share mode, and do NOT alter `FILE_OPEN_REPARSE_POINT | FILE_SYNCHRONOUS_IO_NONALERT` in the create-options word.

Deliberately withhold the delete access right from both new arms. Record why in a comment at the `desired_access` arm: the lock handle never destroys the sentinel (removal happens through the parent's normal descendant cleanup, which opens its own handle), and 20-72 proved on this hardware that a retained delete right is a real functional hazard on Windows directories — so least privilege here is a decision, not an oversight.

Add `pub(super) fn open_child_lock_file(parent: &DirectoryAuthority, name: &str, create: bool) -> Result<File>` (or the equivalent shape that keeps the two public wrappers thin) delegating to `open_relative` with `RelativeKind::File` and the matching new intent. Give it a doc comment that makes the finding un-erasable. It must record: (a) the measured fact that `LockFileEx` on a DIRECTORY handle returns FALSE with `ERROR_INVALID_PARAMETER` for every access mode and share mode tried on this hardware, while the same call on a regular-file handle succeeds — Windows byte-range locking is undefined on directory objects and no access-mode change can fix it; (b) that `fd-lock` calls `LockFileEx` directly, so any advisory lock in this workspace MUST target a regular file; (c) that the child is opened through the parent's retained handle so the lock cannot be redirected by a pathname swap; (d) that this is the reason the function exists and that retargeting any caller at a directory handle silently disables Windows mutual exclusion rather than failing loudly at review time.

In `crates/wcore-sandbox/src/directory_authority.rs`, add two public methods to `DirectoryAuthority`, placed beside the existing child-file accessors so the API reads as one family: `pub fn open_child_lock_file(&self, name: &str) -> Result<DirectoryHandleLoan>` and `pub fn open_or_create_child_lock_file(&self, name: &str) -> Result<DirectoryHandleLoan>`. Both begin with the existing `validate_child_name` guard, exactly as every sibling child accessor does. Both construct the returned loan by incrementing the SAME `handle_loans` counter `try_clone_handle` increments and building the same `DirectoryHandleLoan` shape with the child file as its handle — that is what preserves the destructive-removal refusal unchanged, and it must be stated in the doc comment rather than left to be rediscovered. Implement the platform split inside this crate only, following the per-`cfg` shape the neighbouring child accessors already use: on Windows delegate to the new Windows helper; on unix perform a relative open through the retained parent descriptor with read-write access, no-follow and close-on-exec, adding the create flag and a private `0o600` mode for the open-or-create variant. On any other platform return the same `SandboxError::PolicyNotSupported` refusal the sibling accessors return.

Document on both public methods that the returned handle is intended solely as an advisory-lock target, that the caller wraps it in `fd_lock::RwLock`, and that the open-only variant surfaces a not-found error so a caller probing for an absent lock file can treat that as "unheld" without mutating the directory it is merely observing.

Records evidence for REQ-native-r6 and REQ-native-r4; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'pub fn open_child_lock_file' crates/wcore-sandbox/src/directory_authority.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'pub fn open_or_create_child_lock_file' crates/wcore-sandbox/src/directory_authority.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'fn open_child_lock_file' crates/wcore-sandbox/src/directory_authority_windows.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'FILE_OPEN_IF' crates/wcore-sandbox/src/directory_authority_windows.rs)" -ge "2"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -v '^[[:space:]]*//' crates/wcore-sandbox/src/directory_authority_windows.rs | /usr/bin/grep -cF 'GENERIC_READ | GENERIC_WRITE | SYNCHRONIZE')" -ge "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && /usr/bin/git diff --stat -- crates/wcore-sandbox/src/</automated>
  </verify>
  <done>`directory_authority_windows.rs` gains exactly one lock-file open helper plus two `RelativeIntent` arms granting read-write-without-delete and selecting the open / open-or-create dispositions, documented with the LockFileEx-on-directory finding; no existing arm, share mode or create-options word changed. `directory_authority.rs` gains exactly one `open_child_lock_file` and one `open_or_create_child_lock_file`, both name-validated, both returning a parent-accounted `DirectoryHandleLoan`, with the loan-accounting rationale stated in-source. `cargo fmt --all -- --check` is clean. No behavioural claim — the Mac cannot compile.</done>
</task>

<task type="auto">
  <name>Task 2: Retarget all three swarm lock helpers from directory handles to file sentinels, with one derivation per semantic lock</name>
  <files>crates/wcore-swarm/src/worktree_security.rs, crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/src/worktree_manager.rs, crates/wcore-swarm/src/worktree_cleanup.rs</files>
  <read_first>crates/wcore-swarm/src/worktree_security.rs (the crate-private `DirectoryAuthority` wrapper in full), crates/wcore-swarm/src/worktree.rs (the file-scope constants; `ActiveLease` in full; `TransactionCleanup::release`; `with_directory_lock`; `transaction_is_active`), crates/wcore-swarm/src/worktree_manager.rs (both constructors' control-directory handling; both checkout registration closures around the lease-file creation and lease acquisition), crates/wcore-swarm/src/worktree_cleanup.rs (`reserved_workspace_bytes`, `cleanup_all` including its lease acquisition and enumeration loops, `remove_owned_transaction_root`)</read_first>
  <behavior>
    - An exclusive advisory lock is acquired successfully on Windows at every one of the eight call sites; none of them hands a directory handle to the locking layer any more.
    - The swarm-root lock and the full-cleanup lock still exclude each other, because both resolve the identical sentinel: admission critical sections and `cleanup_all` serialize exactly as they do today.
    - The per-transaction lease and the active-transaction probe still refer to the same lock, so a live transaction is reported active and a released one is reported inactive.
    - `with_directory_lock` keeps its before-and-after identity proofs verbatim, so a swap of the swarm root around the critical section still fails closed.
    - A transaction root whose lease is currently held still refuses destructive removal, because the lease handle remains accounted against that root's loan counter.
    - The active-transaction probe performs no mutation: probing a transaction root that has no lease file reports "not active" instead of creating one.
    - `wcore-swarm` contains no platform conditional for any of this.
  </behavior>
  <action>In `crates/wcore-swarm/src/worktree_security.rs`, add two thin `pub(super)` methods to the crate-private wrapper beside the existing `try_clone_handle`, forwarding to the two new sandbox constructors and mapping their errors through the same conversion `try_clone_handle` already uses. Give each a one-line doc comment pointing at the sandbox definition as the single home of the rationale.

In `crates/wcore-swarm/src/worktree.rs`, add one constant beside the existing `LEASE_FILE` / `CONTROL_DIR` / `RESERVATION_FILE` group: `const SWARM_LOCK_FILE: &str = ".wayland-swarm-lock";`. Then add exactly two crate-private derivations, placed immediately above `with_directory_lock`, and route every lock site through them so each semantic lock has ONE definition:

  - `fn swarm_lock_handle(authority: &DirectoryAuthority) -> Result<DirectoryHandleLoan>` — opens (or creates) `CONTROL_DIR` beneath the swarm authority using the wrapper's existing open-or-create child-directory method, then opens-or-creates `SWARM_LOCK_FILE` beneath that, and returns the loan. Use the open-or-create form for the control directory deliberately: `TransactionCleanup::release` reaches this helper through a `swarm_authority` that in unit-test fixtures is opened on a bare temporary directory with no control directory present, so an open-only form would make cleanup fail for a reason unrelated to locking. Record in a comment WHY the sentinel lives inside the control directory rather than directly under the swarm root: four production loops enumerate the swarm root's direct children and skip only the control directory, and three of them hard-error on any non-directory child, so a sentinel placed directly under the swarm root would break admission accounting, cleanup and the residual sweep. That placement is therefore load-bearing, not cosmetic, and moving it requires changing those four loops.
  - `fn transaction_lease_handle(authority: &DirectoryAuthority) -> Result<DirectoryHandleLoan>` — opens-or-creates `LEASE_FILE` beneath the transaction-root authority and returns the loan. Note in a comment that this is the file the code already created and then discarded without locking, so the constant's name is now accurate.

Change `with_directory_lock` to obtain its lock target from `swarm_lock_handle(authority)` instead of cloning the authority's own directory handle. Change NOTHING else about it: the leading `validate_path`, the interrupted-retry loop shape, the trailing `validate_path` and the `action()` call all stay exactly as written. Add a short comment at the lock acquisition recording that the target must be a regular file because Windows byte-range locking is undefined on directory objects, and that the sentinel is resolved through the retained handle so the identity proofs bracketing it remain meaningful.

Change `transaction_is_active` to probe `LEASE_FILE` through the OPEN-ONLY sandbox constructor and treat a not-found error as `Ok(false)`. Keep `authority.validate_path(path)?` first, keep the would-block arm returning `Ok(true)`, and keep the interrupted-retry recursion. State in a comment that the probe is deliberately non-mutating — it runs over foreign and legacy transaction roots during capacity accounting and cleanup, so creating a lease file inside a directory it is merely observing would be a side effect — and that "no lease file" correctly means "nobody holds the lease", which is exactly the disposition the surrounding code already assumes on the not-found path.

`ActiveLease::acquire` keeps its signature unchanged (it already takes the loan type the new constructors return); only its three call sites change. In `crates/wcore-swarm/src/worktree_cleanup.rs`, `cleanup_all` acquires its lease through `swarm_lock_handle(&self.swarm_authority)` so it still excludes admission. In `crates/wcore-swarm/src/worktree_manager.rs`, both checkout registration closures acquire their lease through `transaction_lease_handle(&root_authority)`; delete the now-redundant separate creation-and-immediate-drop of the lease file in each closure, since the lease handle creates it, and leave the private-mode creation of every OTHER guard file and directory in those closures untouched.

Do NOT change any enumeration loop, any `sandbox_read_denies` entry, any reservation handling, any identity check, or any error message. Do NOT introduce a `#[cfg]` anywhere in `wcore-swarm`. Before changing the `cleanup_all` site, confirm by reading that its lease is taken on the SWARM authority and the two manager sites take theirs on the TRANSACTION-ROOT authority — the two must not be collapsed onto one sentinel, or full cleanup would deadlock against the transaction it is trying to remove.

Implements REQ-native-r6; records evidence for REQ-native-r4; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'fn swarm_lock_handle' crates/wcore-swarm/src/worktree.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'fn transaction_lease_handle' crates/wcore-swarm/src/worktree.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'SWARM_LOCK_FILE' crates/wcore-swarm/src/worktree.rs)" -ge "2"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -v '^[[:space:]]*//' crates/wcore-swarm/src/worktree.rs | /usr/bin/grep -cF 'authority.try_clone_handle()?;')" = "0"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -v '^[[:space:]]*//' crates/wcore-swarm/src/worktree_cleanup.rs | /usr/bin/grep -cF 'swarm_authority.try_clone_handle()')" = "0"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -v '^[[:space:]]*//' crates/wcore-swarm/src/worktree_manager.rs | /usr/bin/grep -cF 'root_authority.try_clone_handle()')" = "0"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'transaction_lease_handle(&root_authority)' crates/wcore-swarm/src/worktree_manager.rs)" = "2"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'cfg(windows)' crates/wcore-swarm/src/worktree.rs)" = "$(/usr/bin/git show HEAD:crates/wcore-swarm/src/worktree.rs | /usr/bin/grep -cF 'cfg(windows)')"</automated>
  </verify>
  <done>`worktree.rs` defines `SWARM_LOCK_FILE` plus exactly one `swarm_lock_handle` and one `transaction_lease_handle`; all three lock helpers take their target from a file sentinel and no non-comment line in `worktree.rs`, `worktree_cleanup.rs` or `worktree_manager.rs` still clones a directory handle for locking; both manager lease sites use the transaction derivation (2 hits) and the cleanup site uses the swarm derivation; `with_directory_lock` keeps both identity proofs and `transaction_is_active` treats an absent lease file as unheld; the `cfg(windows)` count in `worktree.rs` is unchanged from the pre-task tree, so no platform conditional leaked into `wcore-swarm`; `cargo fmt --all -- --check` is clean. No behavioural claim — the Mac cannot compile.</done>
</task>

<task type="auto">
  <name>Task 3: Prove mutual exclusion actually holds — invariant tests for both lock targets</name>
  <files>crates/wcore-swarm/src/worktree_tests.rs</files>
  <read_first>crates/wcore-swarm/src/worktree_tests.rs (the file header and `use super::*`; `test_transaction_cleanup`; the existing non-git unit tests near the top for fixture idiom; whatever test 20-72 added, so nothing is duplicated or displaced), crates/wcore-swarm/src/worktree.rs (`ActiveLease` in full, `swarm_lock_handle`, `transaction_lease_handle`, `with_directory_lock`, `transaction_is_active` as written by Task 2)</read_first>
  <behavior>
    - `transaction_lease_is_mutually_exclusive`: with a lease held on a transaction root, an INDEPENDENT probe of that same root reports the transaction active; once the lease is closed, the same probe reports it inactive. It asserts the invariant — that exclusion holds and then releases — never an error kind or an OS code.
    - `swarm_lock_is_mutually_exclusive`: while a critical section holds the swarm sentinel, an independent attempt to take that sentinel is observed as contended, and it succeeds once the critical section ends.
    - Both tests run on every platform with no `#[cfg]` gate, because Task 2 made the mechanism identical everywhere. Both fail before this plan on Windows (acquisition itself errors out) and pass after it. Both fail again on every platform if anyone retargets a lock at a directory handle, because a directory lock either errors on Windows or, on unix, silently locks a different object than the probe inspects.
    - Neither test can pass vacuously: the probe uses a separately opened authority, so it holds a genuinely independent open file description on unix and a genuinely independent handle on Windows. A test that probed through the same handle would observe its own lock and prove nothing.
    - Neither test can hang the suite: any wait for the other side is bounded and fails the test on expiry rather than blocking the runner.
  </behavior>
  <action>Add exactly two tests to `crates/wcore-swarm/src/worktree_tests.rs`, placed among the non-git unit tests near the top of the file so they sit beside the fixtures they use and away from the git-backed section. Both are plain `#[test]` functions (no async runtime is needed — every function under test is synchronous) and neither carries a platform gate.

`transaction_lease_is_mutually_exclusive`: create a temporary directory to stand in for a transaction root, open a `DirectoryAuthority` on it, acquire an `ActiveLease` through `transaction_lease_handle`, then open a SECOND, INDEPENDENT `DirectoryAuthority` on the same path and assert `transaction_is_active` against it returns true. Close the lease, then assert the same independent probe returns false. Add a doc comment stating that the second authority is what makes the assertion non-vacuous, that the invariant is exclusion-then-release rather than any particular failure shape, and that this test is the regression guard against retargeting the lock at a directory handle. Assert with messages that name the invariant, not any error text.

`swarm_lock_is_mutually_exclusive`: create a temporary directory to stand in for a swarm root and open a `DirectoryAuthority` on it. Spawn a scoped or joined thread that enters `with_directory_lock` on that root, signals readiness over a channel, then waits on a second channel before returning — so the critical section is provably held while the main thread observes it. From the main thread, after receiving the readiness signal, open an independent authority on the same path, take the swarm sentinel through `swarm_lock_handle`, and assert that a non-blocking exclusive attempt on it reports contention. Then release the critical section, join the thread, and assert the same non-blocking attempt now succeeds. Every channel wait uses a bounded timeout whose expiry fails the test with a message naming which side did not arrive; no unbounded blocking wait may appear in either test.

Do NOT assert `ErrorKind::InvalidInput`, `ERROR_INVALID_PARAMETER`, the number 87, or any other encoding of today's Windows failure — that would enshrine the defect. The only error condition either test may inspect is a would-block contention signal, and only as evidence that exclusion held.

Do not modify, rename, re-gate or delete any existing test or helper, including the one 20-72 added. Implements REQ-native-r6; records evidence for REQ-native-r4; marks no requirement complete — behaviour is proven at Tasks 6 and 7.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'fn transaction_lease_is_mutually_exclusive' crates/wcore-swarm/src/worktree_tests.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'fn swarm_lock_is_mutually_exclusive' crates/wcore-swarm/src/worktree_tests.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'InvalidInput' crates/wcore-swarm/src/worktree_tests.rs)" = "0"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'recv_timeout' crates/wcore-swarm/src/worktree_tests.rs)" -ge "2"</automated>
  </verify>
  <done>Exactly two new ungated tests exist, one per lock target, each probing through an independently opened authority and each asserting exclusion-then-release rather than any failure shape; no bounded-wait is unbounded and no assertion names the defect's error kind; no existing test or helper was touched; `cargo fmt --all -- --check` is clean. Behaviour proven at Task 6.</done>
</task>

<task type="auto">
  <name>Task 4: Correct the three worktree_tests.rs Windows arms (B1 unify, B2 re-attribute, B3 comment-only)</name>
  <files>crates/wcore-swarm/src/worktree_tests.rs</files>
  <read_first>crates/wcore-swarm/src/worktree_tests.rs (`test_transaction_cleanup`; `failed_transaction_cleanup_remains_retryable`; `transaction_cleanup_preserves_same_path_replacement`; `transaction_cleanup_never_deletes_swap_after_validation` — all four in full, at their post-20-72 positions), crates/wcore-sandbox/src/directory_authority_windows.rs (`remove_open_dir_all`, `remove_descendants`, `open_relative`, `delete_open_object` — the handle-bound cleanup mechanism these tests exercise)</read_first>
  <behavior>
    - B1: `transaction_cleanup_never_deletes_swap_after_validation` runs ONE body on both platforms. On Windows it now actually constructs the mid-validation swap, and the handle-bound cleanup satisfies the previously-unix-only assertions verbatim: the substituted directory at the original pathname survives with its replacement receipt readable, the moved original is deleted, and the control directory is left empty.
    - B2: `transaction_cleanup_preserves_same_path_replacement` keeps its Windows refusal assertion but attributes it to the correct cause — the fixture's open descendant reservation handle — and records in-source that the assertion is coupled to that fixture, so a future change that drops the reservation file cannot silently flip the test into passing for no reason.
    - B3: `failed_transaction_cleanup_remains_retryable` keeps every assertion, gate and polarity byte-identical; only the clause misattributing the refusal is corrected.
    - No test is deleted, weakened, re-gated or made conditional to buy a green suite; B1 gains coverage rather than losing it.
  </behavior>
  <action>Work these three in order and treat the middle one as an empirical determination, not a foregone conclusion.

B1 — `transaction_cleanup_never_deletes_swap_after_validation`. Delete the platform split entirely and keep the unix body as the single body for both platforms. Concretely: the race hook performs the rename of the transaction root out of the way, recreates a fresh directory at the original pathname, and writes the replacement receipt; the post-cleanup block asserts the receipt is still readable at the original pathname, that the moved original no longer exists, and that the control directory is empty. Remove the boolean that existed only to prove the Windows arm's hook fired, and remove the platform branch in the post-cleanup block. This works on Windows for a specific, checkable reason that MUST be stated in the test's doc comment: this fixture opens NO descendant handle inside the transaction root — only an authority on the root object itself — so by the rename truth table a handle on an object never blocks renaming that object when the share mode admits delete. Also record in that comment why the defense holds afterwards: destructive cleanup enumerates and opens children through the RETAINED handle and disposes them by handle, so it follows the directory OBJECT wherever the pathname now points, and the substituted directory at the original pathname is never touched. Delete the existing comments asserting the opposite; do not leave a corrected and an uncorrected explanation side by side.

B2 — `transaction_cleanup_preserves_same_path_replacement`. FIRST establish empirically what is constructible, at Task 6 time on the box, rather than assuming: the source-level prediction is that this test's fixture retains a `RegularFileAuthority` on the reservation file inside the transaction root, that this open descendant is what refuses the rename by the ancestor rule, and that the descendant handle CANNOT be dropped without dropping the transaction because the cleanup struct requires that authority as a field and validates it during release. If the box confirms that prediction, keep the platform split and make these corrections: rewrite the Windows arm's rationale to attribute the refusal to the open descendant handle on the reservation file rather than to share-mode semantics, and add an explicit note that the assertion is coupled to the fixture retaining that handle, so removing the reservation file from the fixture would flip this test into vacuous passing. Also note that the same-path-replacement defense IS exercised on Windows by the now-unified B1 test, so this arm's narrowness is a fixture limitation and not a coverage hole. If the box instead shows the rename succeeds — meaning the prediction is wrong and no descendant handle is live — then collapse the split exactly as B1 does and run the unix body on both platforms. Record which of the two outcomes was measured, with the evidence, in the SUMMARY.

B3 — `failed_transaction_cleanup_remains_retryable`. Comment-only change. The assertion is sound: the fixture holds authorities on the swarm root AND on its transaction-root descendant, and renaming an ancestor while a descendant handle is open is refused with an access-denied error. Replace only the clause that attributes the refusal to what the share-delete flag does or does not authorize, and state the ancestor rule instead. Change no assertion, no gate, no expected error kind and no polarity. Its release-path failure is a Part A symptom and is fixed by Tasks 1-2, not here.

Across all three: do not introduce a `#[cfg]` where a runtime platform branch exists today or vice versa, do not change any fixture other than as B1 requires, and do not touch the test 20-72 added. Implements REQ-native-r4; records evidence for REQ-native-r6; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'Windows must refuse renaming the handle-held transaction root' crates/wcore-swarm/src/worktree_tests.rs)" = "0"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'swap_refused' crates/wcore-swarm/src/worktree_tests.rs)" = "0"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'preserve-race' crates/wcore-swarm/src/worktree_tests.rs)" -ge "2"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'FILE_SHARE_DELETE only authorizes' crates/wcore-swarm/src/worktree_tests.rs)" = "0"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'fn failed_transaction_cleanup_remains_retryable' crates/wcore-swarm/src/worktree_tests.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && /usr/bin/git diff --stat -- crates/wcore-swarm/src/worktree_tests.rs</automated>
  </verify>
  <done>B1 has one body for both platforms, its inverted-premise refusal assertion and its Windows-only hook-fired flag are gone, and the replacement-receipt assertions now run everywhere with the rule-1 and handle-bound-cleanup rationale recorded in-source. B2 is corrected per the measured outcome, with the fixture coupling stated if the split was kept. B3 changed comments only and still exists with every assertion intact. No clause anywhere in the file still attributes a rename outcome to share-delete authorization. `cargo fmt --all -- --check` is clean. Behaviour proven at Task 6.</done>
</task>

<task type="auto">
  <name>Task 5: Restore real software-defense coverage in dispatch_smoke.rs and correct its Windows rationales (B4)</name>
  <files>crates/wcore-swarm/tests/dispatch_smoke.rs</files>
  <read_first>crates/wcore-swarm/tests/dispatch_smoke.rs (`dispatch_rejects_same_head_repository_replacement`; `dispatch_rejects_different_head_repository_replacement`; `replace_repo_container`; the ancestor-rename refusal helper; `assert_repository_replacement_rejected`; `init_repo` and the file's imports), crates/wcore-swarm/src/lib.rs (`Swarm::new`), crates/wcore-swarm/src/worktree_manager.rs (`new`, `new_with_workspace_root`, `new_with_workspace_authority`, `validate_repo_authority`, `retained_worker_count`), crates/wcore-sandbox/src/directory_authority.rs (`validate_path`)</read_first>
  <behavior>
    - Windows exercises the platform-independent software defense again: a repository replaced at the same pathname by a different directory object is refused by `validate_repo_authority`, not merely declared unconstructible by the OS.
    - The retained OS-refusal assertions keep their polarity and their non-vacuity, but explain themselves by the ancestor rule — an open descendant handle blocks renaming an ancestor — instead of by share-mode semantics, and no comment claims the substitution is impossible in general when it is impossible only via that construction.
    - The new coverage runs on BOTH platforms from one body, so unix and Windows prove the same defense through the same code path.
    - No existing dispatch test loses its assertion, and nothing is re-gated to make a suite pass.
  </behavior>
  <action>FIRST, verify empirically before relying on anything. The brief's proposed construction — renaming the held `repo` directory itself — rests on the swarm holding no descendant handle inside `repo`. That premise is very likely FALSE for these tests: `Swarm::new` builds its manager through the constructor that places the swarm root at `.swarm-worktrees` INSIDE the repository and RETAINS an authority on it, which is a live descendant handle, so by the ancestor rule renaming `repo` itself should be refused with an access-denied error exactly as the ancestor rename is. Confirm this on the box at Task 6 time with a direct attempt before either relying on it or discarding it, and record the measured outcome in the SUMMARY. A single overlooked descendant handle is the difference between the two branches below.

If the rename of `repo` itself is REFUSED (the predicted case): keep both existing Windows arms as OS-refusal assertions but correct everything they claim. Rename the ancestor-named refusal helper to a name that describes the actual mechanism — refusal caused by an open descendant — and rewrite its doc comment to state the ancestor rule as measured: any open handle to a descendant, of any kind and any share mode, blocks renaming any ancestor with an access-denied error, and desired access and share mode are irrelevant to that outcome. Delete the claim that the share-delete flag authorizes renaming the held object but not an ancestor; the truth is that a handle on an object never blocks renaming that object at all when the share mode admits delete. Delete the overclaim that the substitution is physically impossible while handles are held, and replace it with the precise statement: it is impossible via this ancestor construction for this topology, because the swarm retains a handle on a directory inside the repository. Apply the same corrections to the unix-only helper's doc comment, which carries the same misattribution.

If the rename of `repo` itself SUCCEEDS: additionally convert the two Windows arms to construct the substitution by renaming `repo` itself and then running the existing software-defense assertion, so both platforms reach the same refusal, and record that the descendant-handle premise did not hold.

In EITHER case, add one new test that restores genuine software-defense coverage on both platforms, because that is the coverage `334f264d` removed and neither branch above recovers it for Windows on its own. Name it for what it proves — that a repository replaced at the same pathname is refused by the retained repository authority. Build it with the manager constructor that takes an explicit workspace root, giving it the repository directory and a swarm root under a SEPARATE temporary directory outside the repository. That topology is the whole point: with the swarm root outside the repository the only handle the manager holds inside it is the repository authority on the repository object itself, so by rule 1 renaming that object is permitted on Windows and the substitution becomes constructible. Rename the repository directory aside, create a fresh directory at the original pathname, then call a public manager entry point that begins by validating the repository authority — the retained-worker count is the cheapest such entry point and needs no git — and assert it returns an error naming the directory identity change. Assert on the refusal the authority actually emits, not on a substring a different error would also satisfy. Do not gate the test to one platform: the assertion is a universal invariant and it must run on unix too, where it stands as the permanent guard.

Record in that test's doc comment: which handles are and are not held inside the repository under this constructor and why that is what makes the substitution constructible; that 20-72 converts the repository authority to a read-only observational open, which does not affect this analysis because access mode is irrelevant to renaming the object itself; and that this test is the replacement for the software-defense coverage that the Windows arms of the two dispatch tests lost.

Do not change `assert_repository_replacement_rejected`, `init_repo`, the transaction-entry counter, or any unrelated test in the file. Implements REQ-native-r4; records evidence for REQ-native-r6 and REQ-native-r12; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'physically impossible while handles are held' crates/wcore-swarm/tests/dispatch_smoke.rs)" = "0"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'FILE_SHARE_DELETE authorizes renaming the' crates/wcore-swarm/tests/dispatch_smoke.rs)" = "0"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'new_with_workspace_root' crates/wcore-swarm/tests/dispatch_smoke.rs)" -ge "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'directory identity changed' crates/wcore-swarm/tests/dispatch_smoke.rs)" -ge "2"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'fn dispatch_rejects_same_head_repository_replacement' crates/wcore-swarm/tests/dispatch_smoke.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'fn dispatch_rejects_different_head_repository_replacement' crates/wcore-swarm/tests/dispatch_smoke.rs)" = "1"</automated>
  </verify>
  <done>Both existing dispatch replacement tests still exist with their assertions intact and their Windows rationales rewritten to the ancestor rule, with the impossibility overclaim and the share-delete misattribution gone from both helpers. Exactly one new ungated test exercises `validate_repo_authority` against a same-pathname repository substitution built on an out-of-repository swarm root, asserting the authority's own refusal text. `cargo fmt --all -- --check` is clean. Behaviour proven at Task 6.</done>
</task>

<task type="auto">
  <name>Task 6: Commit, sync SEANDESKTOP, RE-MEASURE the real post-20-72 baseline, and settle the two empirical determinations</name>
  <files>None (measurement and conditional repair): commits Tasks 1-5, measures on SEANDESKTOP, and only then finalizes the B2 and B4 branches.</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md, .planning/phases/20-transactional-delegated-mutation/20-NATIVE-REPAIR-PRD.md, .planning/phases/20-transactional-delegated-mutation/20-72-PLAN.md (its Task 5 gate, for the post-20-72 expectation this task must verify rather than assume)</read_first>
  <behavior>
    - The tree under test is a committed SHA the box checks out; nothing is measured from a Mac cross-compile or from source inspection.
    - The ACTUAL post-20-72 baseline is measured on the real tree before any delta is claimed. The predicted 84 run / 81 passed / 3 failed / 6 skipped is treated as a hypothesis to confirm or correct, never as a given.
    - `cargo check -p wcore-sandbox -p wcore-swarm --all-targets` compiles on Windows — the first genuine type-check of the new FFI-adjacent lock-file code.
    - The B2 constructibility question and the B4 descendant-handle question are answered by direct attempt on the box, and the corresponding task branches are finalized from the measurement.
    - Every residual failure is attributed individually — either to a named cause with evidence, or diagnosed — and none is waved through.
    - No guard is widened, no assertion deleted and no test re-gated to make a suite go green.
  </behavior>
  <action>Run `cargo fmt --all` on the Mac. Commit Tasks 1-5 with `/usr/bin/git`, staging ONLY the eight paths in `files_modified` — never `-A`, never `.`, never `AGENTS.md` or any `.ijfw` churn, and with no `Co-Authored-By` trailer. Push the branch `plan/f20-unified-audit-repair` so the box can fetch it.

BEFORE anything else on the box, re-measure the baseline. Check out the commit 20-72 landed (the parent of this plan's commit) and run `cargo nextest run -p wcore-swarm --no-fail-fast` there, recording the ACTUAL run/passed/failed/skipped counts and the exact names of every failing test. Do not assume the 84 / 81 / 3 / 6 prediction: 20-72 lands first, its own second-layer decision may have changed the tree, and every count and line number in this plan predates it. If the residual failures are not exactly the Part A and Part B tests this plan targets, stop and report the discrepancy with the failing names before proceeding — an unexplained extra failure means something else is in play and this plan's delta would be unattributable.

Then check out this plan's commit, confirm the SHA the box prints matches the Mac's `HEAD`, and run, in order, `cargo check -p wcore-sandbox -p wcore-swarm --all-targets` and `cargo nextest run -p wcore-swarm --no-fail-fast`. Repair any compile error before running tests — a suite result against a tree that does not compile is meaningless. Every remote invocation uses the script-block form with the cargo or git command wrapped in `cmd /c "... 2>&1"`.

Now settle the two empirical determinations that Tasks 4 and 5 deferred, using a short throwaway probe on the box (never a diagnostic added to production code, and never committed):

  - B2: with a fixture matching `test_transaction_cleanup` — a transaction root containing the reservation file, with an authority retained on the root AND a file authority retained on the reservation — attempt to rename the transaction root. Confirm whether it is refused with an access-denied error. If refused, keep Task 4's B2 split with the corrected descendant-rule rationale. If it succeeds, collapse the split and run the unix body on both platforms, then amend the commit.
  - B4: with a live swarm constructed the way the two dispatch replacement tests construct it, attempt to rename the repository directory ITSELF. Confirm whether it is refused. If refused, keep Task 5's corrected OS-refusal arms. If it succeeds, convert those arms to construct the substitution and run the software-defense assertion, then amend the commit. Either way the new out-of-repository-workspace test stays, because it is what restores the lost coverage regardless of which branch holds.

Attribute every residual `wcore-swarm` failure by name. A failure that this plan did not target and that was not in the re-measured baseline is a NEW regression and must be diagnosed and fixed, not annotated. If any Part A test still fails, the most likely causes in order are: a lock target that resolved to a directory after all, a sentinel that two call sites derive differently so their interlock split, or a probe that created a lease file where it should have reported unheld — check those three before hypothesizing anything new. Amend or add a commit for any change made, push it, re-sync the box, and re-run `cargo check` plus the `wcore-swarm` suite before advancing to Task 7.

Records evidence for REQ-native-r6, REQ-native-r4 and REQ-native-r12; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && cargo fmt --all -- --check</automated>
    <automated>BASE=$(cd /Users/seandonahoe/dev/waylandcore-ferrox && /usr/bin/git rev-parse HEAD~1); ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "git fetch --all --prune 2>&1"; cmd /c "git checkout --detach '"$BASE"' 2>&1"; cmd /c "git rev-parse HEAD"; cmd /c "cargo nextest run -p wcore-swarm --no-fail-fast 2>&1" }'</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox && /usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "git fetch --all --prune 2>&1"; cmd /c "git checkout --detach '"$SHA"' 2>&1"; cmd /c "git rev-parse HEAD" }'</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo check -p wcore-sandbox -p wcore-swarm --all-targets 2>&1" }'</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-swarm --no-fail-fast 2>&1" }'</automated>
  </verify>
  <done>The real post-20-72 baseline was MEASURED on the box (counts and failing test names recorded) rather than assumed from the 84 / 81 / 3 / 6 prediction, and any discrepancy was reported before proceeding. The box is detached on the exact fix SHA and `cargo check -p wcore-sandbox -p wcore-swarm --all-targets` compiles on Windows. The `wcore-swarm` suite has been run and every residual failure is attributed by name. The B2 and B4 constructibility questions were answered by direct attempt on the box, the corresponding branches were finalized from the measurement, and any resulting change was committed, pushed and re-measured. No probe was added to production code, no guard was widened, no assertion deleted, no test re-gated.</done>
</task>

<task type="auto">
  <name>Task 7: Hardware verification gate on SEANDESKTOP — the only place a pass may be claimed</name>
  <files>None (verification only): runs the full ordered gate on the final SHA.</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md, .planning/phases/20-transactional-delegated-mutation/20-NATIVE-REPAIR-PRD.md</read_first>
  <behavior>
    - Every gate runs on SEANDESKTOP against the FINAL commit — including anything Task 6 amended — never against a Mac cross-compile and never against source inspection (PRD D5/D7).
    - `cargo check -p wcore-sandbox -p wcore-swarm --all-targets` compiles.
    - `cargo nextest run -p wcore-swarm --no-fail-fast` reaches ZERO failures against the re-measured baseline, with the Part A and Part B tests this plan targets all passing and the two new Part A invariant tests plus the new Part B coverage test all green.
    - `cargo nextest run -p wcore-agent --test transactional_delegated_mutation_test --no-fail-fast` reaches 9/9.
    - `cargo nextest run -p wcore-swarm --test dispatch_smoke --no-fail-fast` passes entirely, including both rewritten replacement tests and the new software-defense test.
    - `cargo clippy --workspace --all-targets` and `cargo fmt --all --check` are clean.
    - Every count, the exact SHA and the wall clock are recorded; any residual failure is enumerated with its cause and never waved through.
  </behavior>
  <action>Run the gate on SEANDESKTOP in this exact order, stopping at the first failure and repairing before advancing — a later suite's green is meaningless if an earlier one is red. Re-sync the box first so the tree is the FINAL commit, including anything Task 6 amended, and confirm the printed SHA against the Mac's `HEAD`. Use the script-block form for every remote invocation, with cargo and git wrapped in `cmd /c "... 2>&1"`.

Order: (1) `cargo check -p wcore-sandbox -p wcore-swarm --all-targets`. (2) `cargo nextest run -p wcore-swarm --no-fail-fast` — require ZERO failures. Compare against the baseline Task 6 MEASURED, not against any number written in this plan: the prediction was that the residual failures at the post-20-72 baseline are exactly the Part A and Part B tests this plan fixes, and the delta claimed here must be stated against what was actually measured. The run count rises by the tests Tasks 3 and 5 add. (3) `cargo nextest run -p wcore-agent --test transactional_delegated_mutation_test --no-fail-fast` — require 9/9. (4) `cargo nextest run -p wcore-swarm --test dispatch_smoke --no-fail-fast` — require all green, including both rewritten replacement tests and the new coverage test. (5) `cargo clippy --workspace --all-targets` and `cargo fmt --all --check` — require both clean.

If step 2 shows a Part A test still failing, do not relax the target and do not re-gate the test: return to Task 6's diagnosis list. If step 4 shows a replacement test failing, the most likely cause is that the B4 empirical determination was applied to the wrong branch — re-run the direct attempt before changing any assertion.

Record every count, the exact SHA, the measured baseline it is a delta against, and the wall clock in the SUMMARY. Re-dispatching the full 6-target native proof is NOT a task here: it happens only after this plan and the rest of the phase's native repair set have landed, and the dispatch itself remains a Sean gate under the phase's existing native-proof-dispatch flow (PRD D6). Records evidence for REQ-native-r6, REQ-native-r4 and REQ-native-r12; marks no requirement complete — closure is claimed by the downstream native proof that consumes this tree.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && cargo fmt --all -- --check</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox && /usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "git fetch --all --prune 2>&1"; cmd /c "git checkout --detach '"$SHA"' 2>&1"; cmd /c "git rev-parse HEAD" }'</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo check -p wcore-sandbox -p wcore-swarm --all-targets 2>&1" }'</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-swarm --no-fail-fast 2>&1" }'</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-agent --test transactional_delegated_mutation_test --no-fail-fast 2>&1" }'</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-swarm --test dispatch_smoke --no-fail-fast 2>&1" }'</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo clippy --workspace --all-targets 2>&1"; cmd /c "cargo fmt --all --check 2>&1" }'</automated>
  </verify>
  <done>SEANDESKTOP is on the exact final SHA. `cargo check -p wcore-sandbox -p wcore-swarm --all-targets` compiles. `cargo nextest run -p wcore-swarm --no-fail-fast` has ZERO failures, stated as a delta against the baseline Task 6 measured rather than against any number predicted here, with both Part A invariant tests, the unified B1 test and the new Part B coverage test green. `cargo nextest run -p wcore-agent --test transactional_delegated_mutation_test --no-fail-fast` is 9/9. `cargo nextest run -p wcore-swarm --test dispatch_smoke --no-fail-fast` is entirely green. `cargo clippy --workspace --all-targets` and `cargo fmt --all --check` are clean. Every count, the SHA, the measured baseline and the wall clock are recorded, and any residual failure is enumerated with its cause.</done>
</task>

</tasks>

## What this plan does NOT change (scope fence)

- **The DELETE-access-bit / chdir defect — 20-72 owns it, entirely.** That plan converts the repository-root authority to a read-only observational open. This plan does not touch that conversion, does not revisit its reasoning, and does not extend the observational open to any further authority. The two defects are independent: the DELETE bit is irrelevant to the lock failure (the measured probe shows a directory handle fails `LockFileEx` with error 87 at read-only access too), and the lock target is irrelevant to the chdir failure.
- **`DirectoryAuthority` identity semantics — untouched.** No struct field is added, no construction site changes, `validate_path` and its before/held/after agreement check are unchanged, and every authority retains exactly the handle it retains today. Only the ADVISORY LOCK TARGET changes; the authority remains the identity witness and `with_directory_lock` keeps both of its identity proofs bracketing the critical section.
- **The shared mutating `open_directory` and every existing `RelativeIntent` arm — untouched.** The new lock arms are additive; no existing access mask, share mode, disposition or create-options word is modified, so `remove_open_dir_all`, relative create/rename/delete and the durability boundary all behave identically.
- **The four swarm-root enumeration loops — untouched.** Placing the sentinel inside the control directory is precisely what avoids changing `reserved_workspace_bytes`, `cleanup_all`, `retained_worker_count` and `sandbox_read_denies`. No skip list grows, no `is_real_directory_entry` refusal is relaxed, and no entry is newly excluded from accounting.
- **`worktree/parent.rs::open_landing_lock_file` and the parent landing lock — untouched.** It already locks a real file and is the pattern this plan follows, not a surface it modifies. The landing critical section, its contention behaviour and its rollback path are unchanged.
- **The path-representation repair and the runner-proven commits — not revisited.** `ece09681..919629ee`, `00f453f2`, `aaeeb379` and `0e867008` are hardware-verified and stay as they are; `crates/wcore-sandbox/tests/live_fs_acl.rs` and `crates/wcore-sandbox/tests/hard_process_containment_windows.rs` are other plans' surfaces and are not touched.
- **No dependency, `Cargo.toml` or `Cargo.lock` change**, no new crate, no new module, and no change to `fd-lock`'s version or usage pattern — only what is handed to it.
- **No test is deleted, weakened, re-gated or made conditional to buy a green suite.** B1 gains platform coverage; B3 changes only a comment; B2 and B4 keep every existing assertion and add coverage rather than trading it away. No refusal message, error polarity or fail-closed disposition changes anywhere in this plan.
- **No `#[cfg]` is added to `wcore-swarm`.** The platform difference lives entirely in `wcore-sandbox` per AGENTS.md, and the `cfg(windows)` count in `worktree.rs` is gate-checked against the pre-task tree.
- **No behaviour change on Linux or macOS beyond the deliberate, recorded lock-artifact migration.** The lock semantics, ordering and blocking behaviour are identical; only the object the advisory lock is taken on moves from a directory to a regular file, which is the unification decision argued in the truths above.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent swarm workers ← swarm-root advisory lock | Admission accounting, aggregate capacity reservation and full cleanup are serialized ONLY by this lock; if it never engages, two workers can each observe stale aggregate state and both admit |
| cross-process CLI instances ← on-disk lock artifact | The lock is the sole cross-process interlock for a shared swarm root; its on-disk identity is therefore a compatibility surface, not an implementation detail |
| transaction lifetime ← lease and its probe | A live transaction is distinguished from an abandoned one solely by whether its lease is held; a probe that cannot observe the lease will report a live transaction as collectable |
| retained directory ← lock target resolution | If the lock target is re-resolved by pathname rather than through the retained handle, a swap between validation and acquisition redirects the lock to a directory the authority never proved |
| test assertion ← OS behaviour vs software defense | A test that asserts an OS property in place of the software defense removes coverage of the defense on that platform without failing |
| host repair claim ← real hardware | The Mac cannot compile this workspace; only SEANDESKTOP can prove or disprove the repair |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-73-01 | Tampering | Windows mutual exclusion has NEVER held across the transaction paths: `LockFileEx` on a directory handle returns error 87 unconditionally, so all eight call sites of `with_directory_lock`, `ActiveLease::acquire` and `transaction_is_active` fail closed. Concurrent admission, concurrent cleanup and a cleanup racing an admission are all unserialized on that platform | critical | mitigate | Retarget every lock at a regular-file sentinel resolved handle-relative (Tasks 1-2); prove exclusion actually holds with invariant tests that fail if a directory handle returns (Task 3); confirm on hardware (Tasks 6-7) |
| T-20-73-02 | Tampering | The interlock set is split by the repair itself — the three admission critical sections and full cleanup stop excluding each other because two call sites derive different sentinels | high | mitigate | Exactly one `swarm_lock_handle` and one `transaction_lease_handle` derivation exist and every site routes through them; the swarm sentinel is gate-checked for a single definition; the swarm-lock invariant test observes contention across an independently opened authority, which fails if the derivations diverge |
| T-20-73-03 | Denial of Service | The sentinel is placed directly under the swarm root, where four enumeration loops treat any non-directory child as a hard error — breaking admission accounting, cleanup and the residual sweep | high | mitigate | The sentinel lives inside `CONTROL_DIR`, which every one of those loops already skips and which `sandbox_read_denies` already denies unconditionally; the placement rationale is recorded in-source as load-bearing so it is not "tidied" later |
| T-20-73-04 | Elevation of Privilege | The destructive-removal refusal is silently lost: today a transaction root whose lease is held refuses `remove_open_dir_all` because the lease holds a loan on that root | high | mitigate | The new primitive returns a `DirectoryHandleLoan` accounted against the PARENT directory's counter, so the refusal is byte-for-byte unchanged; the rationale is stated at the API definition rather than left implicit |
| T-20-73-05 | Spoofing | The lock target is resolved by pathname, so a swap of the directory between `validate_path` and lock acquisition redirects the lock to an unproven object | high | mitigate | Both sentinel derivations open through the retained parent handle via the sandbox relative-open path — no pathname is re-resolved — and `with_directory_lock` keeps its before-and-after identity proofs verbatim |
| T-20-73-06 | Tampering | Cross-version interlock loss: the on-disk lock artifact changes, so a unix process on an older build (locking the directory) and one on this build (locking the sentinel) do not exclude each other during a version-skew window | medium | accept | Accepted as the stated cost of the unification decision. Blast radius is bounded to admission serialization and aggregate accounting; NO authority proof weakens, because `validate_swarm_root`, `validate_repo_authority`, every identity check and the reservation receipts are lock-independent and still fail closed. Windows has no compatibility surface to preserve — the lock never worked there. The consequence is recorded in-source at the constant and in the SUMMARY |
| T-20-73-07 | Repudiation | The non-mutating probe change turns a legacy or foreign ACTIVE transaction with no lease file into a collectable one, so cleanup removes a live workspace | medium | mitigate | The probe's not-found disposition matches what the surrounding code already assumes on that path, and removal still runs through `remove_owned_transaction_root`, whose handle-bound cleanup and identity re-proof fail closed independently of the lock. The residual cross-version window is the same one accepted in T-20-73-06 and is recorded with it |
| T-20-73-08 | Spoofing | B4 — Windows no longer exercises `validate_repo_authority` against a same-pathname repository substitution, having traded the platform-independent software defense for an OS-behaviour assertion; a future change that weakened `validate_path` would not fail on that platform | high | mitigate | A new ungated test builds an out-of-repository swarm root so the substitution is constructible, then asserts the authority's own refusal — restoring the defense's coverage on both platforms regardless of which B4 branch the measurement selects (Task 5) |
| T-20-73-09 | Repudiation | A test asserts an OS guarantee that is false (B1) or true for an unstated incidental reason (B2, B3, B4), so it passes while proving nothing and misleads the next reader into believing a defense exists | high | mitigate | Every corrected arm is re-attributed to the measured rename rule; B1's split is deleted so the real handle-bound defense is exercised; B2's fixture coupling is recorded in-source so a future fixture change cannot silently flip it; no corrected and uncorrected explanation is left side by side |
| T-20-73-10 | Tampering | Silent gate-weakening — a suite is made green by widening a guard, deleting an assertion or re-gating a test instead of fixing the defect | high | mitigate | Forbidden explicitly in the execution rules and in Tasks 6-7; the invariant tests assert exclusion-then-release rather than any error shape; tests may not assert the defect's error kind or OS code; the `cfg(windows)` count in `worktree.rs` is diffed against the pre-task tree |
| T-20-73-11 | Spoofing | A false green claimed from Mac inspection, from cross-compilation (which is impossible here), or from a baseline number predicted in this plan rather than measured on the real post-20-72 tree | high | mitigate | PRD D5/D7: Tasks 1-5 verify at source level only and claim nothing behavioural; Task 6 RE-MEASURES the actual post-20-72 baseline and reports any discrepancy before proceeding; Task 7 states its delta against that measurement, never against a predicted count |
| T-20-73-12 | Repudiation | The B2 and B4 constructibility questions are answered by reasoning rather than by attempt, and a single overlooked descendant handle inverts the conclusion | medium | mitigate | Both are explicitly marked unproven and are settled by direct attempt on the box at Task 6, with both branches specified in advance and the measured outcome recorded in the SUMMARY |
| T-20-73-SC | Tampering | Package-manager supply chain | low | accept | No dependency is added, removed or updated; no `Cargo.toml` or `Cargo.lock` change; no install task exists in this plan |
</threat_model>

<verification>
Local gates (Mac, source level only — the Mac cannot compile this workspace): `cargo fmt --all -- --check` clean; exactly one `open_child_lock_file` and one `open_or_create_child_lock_file` on `DirectoryAuthority` and one lock-file helper in the Windows module, with the read-write-without-delete access mask and both dispositions present; exactly one `swarm_lock_handle` and one `transaction_lease_handle` in `worktree.rs` with `SWARM_LOCK_FILE` defined there; no non-comment line in `worktree.rs`, `worktree_cleanup.rs` or `worktree_manager.rs` still clones a directory handle for locking, and both manager lease sites use the transaction derivation; the `cfg(windows)` count in `worktree.rs` is unchanged from the pre-task tree; the two Part A invariant tests exist and no test names the defect's error kind; B1's inverted-premise refusal assertion and its Windows-only hook flag are gone while its replacement-receipt assertions remain; no clause in `worktree_tests.rs` or `dispatch_smoke.rs` attributes a rename outcome to share-delete authorization, and the impossibility overclaim is gone; the new out-of-repository-workspace coverage test exists in `dispatch_smoke.rs` and both existing replacement tests survive.

Authoritative gate (SEANDESKTOP, real hardware, PRD D5/D7), in order, on the exact final SHA: the post-20-72 baseline is RE-MEASURED with counts and failing test names recorded, and any discrepancy from the 84 / 81 / 3 / 6 prediction is reported before proceeding; `cargo check -p wcore-sandbox -p wcore-swarm --all-targets` compiles; `cargo nextest run -p wcore-swarm --no-fail-fast` has ZERO failures stated as a delta against that measured baseline; `cargo nextest run -p wcore-agent --test transactional_delegated_mutation_test --no-fail-fast` is 9/9; `cargo nextest run -p wcore-swarm --test dispatch_smoke --no-fail-fast` is entirely green including both rewritten replacement tests and the new coverage test; `cargo clippy --workspace --all-targets` clean; `cargo fmt --all --check` clean.

Known unknowns to record, not to resolve here: all probes behind this plan ran on NTFS under `%TEMP%` on C:. `FILE_DISPOSITION_INFO_EX` succeeded there, so the disposition fallback in `directory_authority_windows.rs` is UNTESTED on this box and may still matter on ReFS, FAT or SMB. The `LockFileEx`-on-directory failure is an object-manager property and does NOT depend on the filesystem, so the Part A finding generalizes even though the rename-rule probes do not necessarily.

Re-dispatching the full 6-target native proof is a DEPENDENCY, not a task here: it happens after this plan and the rest of the phase's native repair set have landed, and the dispatch itself remains a Sean gate under the phase's existing native-proof-dispatch flow (PRD D6).
</verification>

<success_criteria>
- Every advisory lock in `wcore-swarm` is taken on a REGULAR FILE resolved handle-relative from the retained directory, at all three helpers and all eight call sites, so Windows mutual exclusion across the transaction paths works for the first time (REQ-native-r6).
- The lock-file primitive is a documented, cross-platform `wcore-sandbox` API whose doc comment records the measured `LockFileEx`-on-directory finding, so a future reader cannot retarget a lock at a directory without deleting the reason (AGENTS.md "Centralize Platform Differences").
- Exactly one derivation exists per semantic lock, so admission critical sections and full cleanup still serialize against each other and the lease and its probe still refer to the same lock; the sentinel's placement inside the control directory keeps all four swarm-root enumeration loops unchanged.
- The destructive-removal refusal is preserved exactly: a transaction root whose lease is held still refuses `remove_open_dir_all`, because the lease handle is accounted against that root's loan counter.
- The unix decision is MADE and JUSTIFIED, not deferred: unix moves to the lock-file target for one code path and one testable mechanism, and the lock-artifact migration consequence is recorded in-source and in the SUMMARY with its bounded blast radius (REQ-native-r4).
- Two invariant tests prove mutual exclusion actually holds and releases, through independently opened authorities, with bounded waits and without naming the defect's error kind — so they fail if any lock is ever retargeted at a directory handle again.
- B1's platform split is gone and the unix body runs on both platforms, exercising the real handle-bound cleanup defense instead of asserting a false OS guarantee; B2's rationale cites the descendant rule with its fixture coupling recorded; B3 is a comment-only correction with every assertion intact (REQ-native-r4).
- B4's coverage regression is closed: `validate_repo_authority` is exercised against a same-pathname repository substitution on BOTH platforms via an out-of-repository swarm root, and the surviving OS-refusal arms explain themselves by the ancestor rule with the impossibility overclaim removed.
- The two explicitly-unproven premises (B2 constructibility, B4 descendant handles inside the repository) were settled by direct attempt on the box, both branches having been specified in advance, and the measured outcomes are recorded.
- SEANDESKTOP on the exact final SHA: `cargo check` compiles; `wcore-swarm` has zero failures against a RE-MEASURED post-20-72 baseline; `transactional_delegated_mutation_test` is 9/9; `dispatch_smoke` is entirely green; clippy and fmt are clean (REQ-native-r12).
- No scope creep and no gate weakening: 20-72's DELETE-bit repair, the path-representation repair, the runner-proven commits, the parent landing lock, the other plans' test surfaces and every enumeration loop are untouched; no guard widened, no assertion deleted, no test re-gated, no dependency changed (REQ-native-r4).
</success_criteria>

## Artifacts this phase produces
- `crates/wcore-sandbox/src/directory_authority_windows.rs` — the documented handle-relative lock-file open with its two new relative-intent arms.
- `crates/wcore-sandbox/src/directory_authority.rs` — `open_child_lock_file` and `open_or_create_child_lock_file`, loan-accounted against the parent directory.
- `crates/wcore-swarm/src/worktree_security.rs`, `worktree.rs`, `worktree_manager.rs`, `worktree_cleanup.rs` — the two sentinel derivations and the eight retargeted lock sites, with no platform conditional.
- `crates/wcore-swarm/src/worktree_tests.rs` — the two mutual-exclusion invariant tests plus the B1/B2/B3 corrections.
- `crates/wcore-swarm/tests/dispatch_smoke.rs` — the rule-accurate Windows rationales and the new software-defense coverage test.
- 20-73-SUMMARY.md recording the root cause, the unification decision and its migration consequence, the Part B rule attributions, the two empirical determinations, and the SEANDESKTOP counts.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-73-SUMMARY.md` using the standard GSD summary template. Record: the fix commit `source_sha`/tree; the `LockFileEx`-on-directory root cause with its measured truth table and the refuted alternatives, and the explicit statement that Windows mutual exclusion across the transaction paths had never worked; the three broken helpers and all eight call sites, with what each now locks; the sentinel placement decision and why locating it inside the control directory is load-bearing rather than tidy; the loan-accounting preservation argument; the UNIX UNIFICATION DECISION with its full reasoning and its migration consequence (mixed-version unix fleets sharing a swarm root do not interlock during a skew window; blast radius bounded to admission serialization and aggregate accounting; no authority proof weakened; Windows had no compatibility surface); the Part B corrections with each one's rule attribution (B1 rule 1 plus the handle-bound cleanup mechanism, B2 rule 3 via the fixture's reservation handle, B3 rule 3 via the descendant, B4 rule 3 via the retained swarm-root handle inside the repository); the two empirical determinations with the measured outcome and which branch was taken; the RE-MEASURED post-20-72 baseline counts and failing test names, and the final per-suite SEANDESKTOP counts stated as a delta against that measurement, with clippy/fmt status and wall clock; the recorded unknowns (the disposition fallback untested on this box; probes limited to NTFS under `%TEMP%` on C:; the object-manager finding being filesystem-independent while the rename-rule probes are not); and any residual failure with its cause. Mark no Phase 20 requirement complete — closure is claimed by the downstream native proof, and the full 6-target native re-dispatch waits on the rest of the phase's native repair set under the existing Sean gate.
</output>

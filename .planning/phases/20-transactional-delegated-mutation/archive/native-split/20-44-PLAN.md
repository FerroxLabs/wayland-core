---
phase: 20-transactional-delegated-mutation
plan: "44"
type: execute
wave: 43
depends_on:
  - "20-43"
files_modified:
  - Cargo.lock
autonomous: true
requirements:
  - REQ-native-r10
  - REQ-native-r4
must_haves:
  truths:
    - "The 20-43 delta added `dunce` as a DIRECT dependency of `wcore-swarm` (`Cargo.toml`), so `Cargo.lock` must be resynced to record the new wcore-swarm→dunce edge. `dunce` is already resolved in the lock tree (transitive via aws-lc-sys/chromiumoxide), so the resync adds an edge, NOT a new crate; the resync is performed on Hetzner (never on the Mac) and committed on the Mac (never hand-edited). If the resync pulls a genuinely NEW crate, STOP for a package-legitimacy checkpoint before committing (REQ-native-r10)."
    - "This plan records the ONE exact third-repaired-successor candidate SHA/tree that every subsequent gate (Kimi K3 cross-audit 20-45, native re-dispatch 20-46, fresh native-inclusive review 20-47, prep 20-48, terminal 20-49) binds to; after the lock seal, no source, test, or workflow change follows (REQ-native-r12 anchoring)."
    - "The sealed successor builds `--locked --workspace --all-features` on Hetzner (proving the resynced lock is consistent) and passes the aggregate Linux proof `nextest --profile ci --no-fail-fast` at 11509/0/48 — the wcore-swarm dunce::simplified change is a no-op on unix and the other 20-43 edits are `#![cfg(windows)]`, so there is no Linux regression from the fixes (REQ-native-r4)."
  artifacts:
    - path: Cargo.lock
      provides: "Resynced workspace lockfile recording the wcore-swarm→dunce direct-dependency edge on the sealed third-repaired-successor candidate (regenerated on Hetzner, committed on the Mac)"
  key_links:
    - from: Cargo.lock
      to: crates/wcore-swarm/Cargo.toml
      via: "the lock resolves the new wcore-swarm direct dependency on dunce (already in the lock tree; no new crate)"
      pattern: "dunce"
---

<objective>
Re-seal the ONE exact third-repaired-successor candidate SHA: resync `Cargo.lock` for the `dunce` direct-dependency edge that 20-43 added to `wcore-swarm/Cargo.toml` (Hetzner-regenerated, Mac-committed, no hand-edit), confirm the resync adds an edge and NOT a new crate, then prove on Hetzner that the sealed successor builds `--locked --workspace --all-features` and passes the aggregate Linux suite unchanged at 11509/0/48.

Purpose: Downstream gates cannot bind to an unsealed or lock-inconsistent candidate (REQ-native-r10), and the Linux no-regression gate (REQ-native-r4) must pass before the scarce Sean-gated native run is re-spent (20-46). This seal anchors 20-45…20-49 to one exact successor SHA.
Output: The recorded sealed successor SHA/tree, a resynced-and-committed `Cargo.lock`, a `--locked` Hetzner build receipt, and a `nextest --profile ci` = 11509/0/48 aggregate receipt. No native or requirement claim.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-37-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-43-SUMMARY.md
@.planning/codebase/TESTING.md
</context>

<execution_rules>
- Capture the plan base before any edit: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-44)"`.
- NO Cargo on the Mac. `Cargo.lock` is regenerated through the committed-HEAD Hetzner harness, retrieved, and committed on the Mac; the lock is NOT hand-edited.
- The 20-43 delta DID change a manifest (added `dunce` to `wcore-swarm/Cargo.toml`), so — unlike the 20-37 no-op seal — a genuine lock delta IS expected: the wcore-swarm package entry gains a `dunce` dependency line. `dunce` is already in the lock tree, so this must add ONLY that edge. If the `--locked` build or the resync reveals a genuinely NEW external crate, STOP and surface it for a package-legitimacy checkpoint before committing.
- Prove against a detached checkout of the EXACT 20-43 source-complete HEAD so the remote-cargo tree-hash gate verifies the precise sealed candidate tree.
- After this seal, NO source, test, or workflow change for the remainder of the third-repaired-successor sequence; the sealed SHA is the exact candidate all downstream gates bind to.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Resync Cargo.lock for the dunce edge and record the sealed successor SHA</name>
  <files>Cargo.lock</files>
  <read_first>crates/wcore-swarm/Cargo.toml, Cargo.lock, .planning/phases/20-transactional-delegated-mutation/20-43-SUMMARY.md</read_first>
  <behavior>
    - The 20-43 fix added `dunce` to `wcore-swarm/Cargo.toml [dependencies]`. A `--locked` build against the 20-43 HEAD will report the lock is stale (the wcore-swarm→dunce edge is missing). Regenerate the lock via the Hetzner harness (`generate-lockfile` or capture from a clean non-locked build), confirm the delta is limited to the wcore-swarm `dunce` edge over an already-resolved crate (surface any genuinely NEW crate for a legitimacy checkpoint), commit the resynced lock on the Mac, and record that lock-sealing commit as the sealed successor SHA.
    - No new package is legitimacy-relevant here: `dunce` is already in Cargo.lock (transitive via aws-lc-sys/chromiumoxide, confirmed in the Kimi audit). Promoting it to a direct dep of wcore-swarm adds no new source.
  </behavior>
  <action>Through the committed-HEAD Hetzner harness, run a `--locked --workspace --all-features` build against a detached checkout of the exact 20-43 HEAD; it is expected to report the lock stale (missing wcore-swarm→dunce edge). Regenerate `Cargo.lock` via the harness, confirm the only delta is the wcore-swarm `dunce` edge over an already-resolved crate (surface any NEW crate for a legitimacy checkpoint and STOP), commit the resynced lock on the Mac (never hand-edit), and record the lock-sealing commit SHA/tree as the sealed third-repaired successor. Do not touch any source, test, or workflow file. Implements REQ-native-r10.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-44)" Cargo.lock</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-44-aggregate "$(pwd -P)" build --workspace --all-features --locked</automated>
  </verify>
  <acceptance_criteria>The resynced `Cargo.lock` is committed on the Mac; the sealed successor SHA/tree is recorded; the delta is limited to the wcore-swarm `dunce` edge (no new crate); a `--locked` Hetzner build against the exact sealed tree then succeeds without further updating the lock.</acceptance_criteria>
  <done>The sealed third-repaired successor SHA is fixed and lock-consistent with the dunce direct dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the sealed successor builds --locked and the aggregate Linux suite is unregressed</name>
  <files>None; this task produces Hetzner receipts against the sealed successor.</files>
  <read_first>.planning/codebase/TESTING.md, .planning/phases/20-transactional-delegated-mutation/20-43-SUMMARY.md</read_first>
  <behavior>
    - `build --workspace --all-features --locked` succeeds on Hetzner against a detached checkout of the sealed successor (the resynced lock is consistent).
    - `nextest run --workspace --profile ci --no-fail-fast` on Hetzner reports 11509 passed / 0 failed / 48 skipped — the wcore-swarm dunce::simplified change is a no-op on unix and the other 20-43 edits are `#![cfg(windows)]`, so the Linux count is unchanged.
    - A different aggregate count is a regression from the 20-43 fixes and blocks the sequence; it is recorded and investigated, not rationalized (honesty gate).
  </behavior>
  <action>Run the two committed-HEAD Hetzner gates against a detached checkout of the sealed successor: the `--locked` full-workspace all-features build, and the aggregate `nextest --profile ci --no-fail-fast`. Confirm the aggregate is 11509 passed / 0 failed / 48 skipped — any deviation is a Linux regression from the 20-43 edits and stops the sequence for repair. Record both receipts with exact command, exit status, counts, nextest run ID, and remote receipt identity. Implements REQ-native-r4.</action>
  <verify>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-44-aggregate "$(pwd -P)" build --workspace --all-features --locked</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-44-aggregate "$(pwd -P)" nextest run --workspace --profile ci --no-fail-fast</automated>
  </verify>
  <acceptance_criteria>The sealed successor builds `--locked` all-features and the aggregate Linux suite is 11509/0/48 with no regression. Any other count blocks the sequence.</acceptance_criteria>
  <done>The sealed successor has a green `--locked` Hetzner build + aggregate receipt and is ready for the pre-native Kimi K3 cross-audit (20-45).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate identity ← lockfile | The sealed SHA must carry a consistent lock; a stale/incoherent lock produces a build that does not match the reviewed source |
| dependency set ← Cargo.toml | The resync must add only the wcore-swarm→dunce edge and must not silently introduce an unaudited new crate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-44-01 | Tampering | Cargo.lock resync (dunce edge) | high | mitigate | The lock is Hetzner-regenerated and Mac-committed (never hand-edited); the delta is confirmed limited to the wcore-swarm→dunce edge over an already-resolved crate; any genuinely NEW crate STOPS the plan for a package-legitimacy checkpoint. |
| T-20-44-02 | Denial of Service | aggregate regression | medium | mitigate | The `nextest --profile ci` gate requires the exact 11509/0/48 baseline; a deviation blocks the sequence rather than being rationalized. |
</threat_model>

<verification>
The two Hetzner receipts (`--locked` build + aggregate `nextest --profile ci`), run against a detached checkout of the exact sealed tree, determine acceptance. Record the sealed `source_sha`, source tree, both commands, exit statuses, counts, nextest run ID, remote receipt identity, and the lock delta (the wcore-swarm→dunce edge; no new crate) in `20-44-SUMMARY.md`. The summary states the candidate is sealed, no source/test/workflow changes follow, and native proof/requirement completion remain deferred (20-46/20-49). No Cargo runs on the Mac.
</verification>

<success_criteria>
- `Cargo.lock` is resynced for the wcore-swarm→dunce edge (Hetzner-regenerated, Mac-committed), the delta adds no new crate, and a `--locked` Hetzner build against the exact sealed tree succeeds (r10).
- The sealed successor builds `--locked` all-features on Hetzner and the aggregate suite is 11509/0/48 with no Linux regression (r4).
- The sealed SHA is recorded as the single candidate all downstream gates bind to.
</success_criteria>

## Artifacts this phase produces
- The sealed third-repaired-successor candidate SHA with a resynced dunce-edge lockfile and green `--locked` Hetzner build + aggregate receipts.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md` using the standard GSD summary template. Record the sealed `source_sha`/tree, the lock resync result (the wcore-swarm→dunce edge; no new crate), both Hetzner receipts, and the explicit statement that no source/test/workflow changes follow and native/requirement completion is deferred. Complete no Phase 20 requirement.
</output>
</content>

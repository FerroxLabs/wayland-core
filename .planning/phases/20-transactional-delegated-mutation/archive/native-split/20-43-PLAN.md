---
phase: 20-transactional-delegated-mutation
plan: "43"
type: execute
wave: 42
depends_on:
  - "20-37"
files_modified:
  - crates/wcore-swarm/src/worktree_manager.rs
  - crates/wcore-swarm/Cargo.toml
  - crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs
  - crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
  - crates/wcore-sandbox/tests/hard_process_containment_windows.rs
  - crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs
  - crates/wcore-sandbox/src/directory_authority_windows_tests.rs
  - .config/nextest.toml
autonomous: true
requirements:
  - REQ-native-r2
  - REQ-native-r3
  - REQ-native-r4
  - REQ-native-r5
  - REQ-native-r6
  - REQ-native-r7
must_haves:
  truths:
    - "Root cause #A (PRODUCTION): `crates/wcore-swarm/src/worktree_manager.rs:9` and `:13` canonicalize `repo_root`/`swarm_root` with `std::fs::canonicalize`, which on Windows always returns a `\\?\C:\...` verbatim path; the PowerShell capacity probe (`:418-451`, SCRIPT const `:419`, trailing arg after `-Command` at `:427`) then hands that verbatim path to `powershell.exe -Command`, which re-parses it as script text and dies with UnexpectedToken. The stored `swarm_root` (and `repo_root`) are de-verbatimized at the canonicalize SITE via `dunce::simplified`, so the probe — and every downstream git invocation that inherits the root (latent watch-item d1) — receives a normal drive-letter path. `dunce::simplified` is a no-op on unix, so the Linux aggregate is unaffected and stays 11509/0/48; windows-f20-lifecycle + windows-public-dispatch stop failing at admission (REQ-native-r6)."
    - "Root cause #A isolation guardrail HELD: the probe is NOT 'fixed' by catching the parse error and defaulting capacity to unlimited — that would silently defeat the DispatchAdmission resource-governance budget. The fix normalizes the path only; the probe still computes real free-space and admission still enforces its budget. A belt-and-braces env-var transport (`WCORE_SWARM_PROBE_ROOT` set with `.env(...)` on the tokio Command from `shell_command_argv`, script reads `$env:WCORE_SWARM_PROBE_ROOT`) removes the trailing-arg reparse class entirely without weakening admission (REQ-native-r6)."
    - "Root cause #B (PRODUCTION): `quote_arg` (`command.rs:76-111`) CRT-escapes embedded quotes to `\\\"` when `process.rs:616` builds the child command line; but `cmd.exe /d /s /c` strips only the OUTER quote pair of the RAW command line and executes the remainder verbatim, so `type \"path\"` runs as a malformed path and fails ERROR_INVALID_NAME, and every containment `start \"\" /b ...` payload is mangled. In the BACKEND, when the resolved program is cmd.exe and the argument follows `/c`/`/k`, the `/c` payload is wrapped in a single outer double-quote pair with inner quotes passed through untouched (NOT CRT-escaped), so cmd `/s` strips the outer pair and executes the script verbatim; windows-appcontainer-acl + windows-retained-handle + windows-job-object stop failing on mangled payloads (REQ-native-r2/r5/r7)."
    - "Root cause #B isolation guardrail HELD: the change is a QUOTING-layer fix only. argv discipline is kept (NO switch to shell-string interpolation of caller data, per AGENTS.md); `is_unc_or_device_path`, the Low-IL restricted token, the Job-Object limits, and the AppContainer ACL lease are untouched. The 20-36 `exit /b 0` exit-code normalization is COMPATIBLE and is preserved, not reverted (REQ-native-r5/r7)."
    - "Job-Object flake closed: `image_count`/`tagged_cmd_count` (`hard_process_containment_windows.rs:109`,`:132`) counted `choice.exe`/`cmd.exe` SYSTEM-WIDE, so a concurrent native target (live_fs_acl also spawns `choice`) polluted the baseline. Counting is scoped to the test's own tagged process tree (by PID/tag, not host-wide image) and/or the native containment targets are serialized via a `.config/nextest.toml` test-group; every containment assertion (active_process_cap, breakaway, job_close reap) is preserved (REQ-native-r7)."
    - "Lib-test compile debt closed: `windows_impl/tests.rs` and `directory_authority_windows_tests.rs` gain the missing imports (`Arc`, `SandboxError`, `SandboxManifest`, `SandboxCommand`, `NetworkPolicy`) and the bad `windows_sys::...::FILE_ID_BOTH_DIR_INFORMATION` import is corrected, so the `#[cfg(test)]` modules compile on msvc (REQ-native-r3)."
    - "Linux is unregressed: the `dunce::simplified` calls are no-ops on unix, `process.rs`/`command.rs`/`windows_impl/tests.rs`/`directory_authority_windows_tests.rs`/`hard_process_containment_windows.rs` are `#[cfg(windows)]`/`#![cfg(windows)]` (zero Linux compiled output), the sanctioned macOS `cargo check -p wcore-sandbox --features live-docker --tests` stays green, Hetzner clippy `-D warnings` stays clean, and the aggregate `nextest --profile ci` stays 11509/0/48 (REQ-native-r4)."
  artifacts:
    - path: crates/wcore-swarm/src/worktree_manager.rs
      provides: "#A: swarm_root/repo_root de-verbatimized via dunce::simplified at the canonicalize site + belt-and-braces WCORE_SWARM_PROBE_ROOT env transport on the capacity-probe Command; DispatchAdmission budget preserved"
    - path: crates/wcore-swarm/Cargo.toml
      provides: "#A: dunce promoted from transitive to a direct dependency of wcore-swarm (already resolved in Cargo.lock; no NEW crate pulled)"
    - path: crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs
      provides: "#B: cmd.exe /c|/k payload wrapped in a single outer quote pair (inner quotes untouched) instead of CRT-escaped, so cmd /s executes the script verbatim; sandbox boundary untouched"
    - path: crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
      provides: "#B: quote_arg gains a cmd-payload-aware path (or a sibling helper) that does not CRT-escape the /c|/k script; general argv quoting unchanged"
    - path: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      provides: "Job-Object counting scoped to the tagged process tree (PID/tag), containment assertions preserved"
    - path: crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs
      provides: "Compile-debt imports restored so the #[cfg(test)] module builds on msvc"
    - path: crates/wcore-sandbox/src/directory_authority_windows_tests.rs
      provides: "Compile-debt imports restored + corrected windows_sys FILE_ID_BOTH_DIR_INFORMATION import"
    - path: .config/nextest.toml
      provides: "Optional test-group serializing the native containment targets on one runner (belt-and-braces for the counting-scope fix)"
  key_links:
    - from: crates/wcore-swarm/src/worktree_manager.rs
      to: crates/wcore-swarm/Cargo.toml
      via: "worktree_manager uses dunce::simplified; wcore-swarm declares dunce as a direct dep (already in Cargo.lock, no new crate)"
      pattern: "dunce"
    - from: crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs
      to: crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
      via: "process.rs builds the cmd child command line and uses the cmd-payload-aware quoting for the /c|/k script"
      pattern: "quote_arg"
---

<objective>
Apply the two PRODUCTION-code root causes the Windows `--no-fail-fast` diagnostic (run 30064412019) plus the independent Kimi K3 cross-audit collapsed the 5 failing targets into, on a clean successor of the sealed `daf27337` candidate, and close the two remaining test/compile defects in the same delta:

- Root cause #A (production): `worktree_manager.rs:9,13` canonicalize `repo_root`/`swarm_root` into `\\?\`-verbatim Windows paths; the PowerShell capacity probe (`:418-451`) hands the verbatim path to `powershell.exe -Command`, which re-parses it as script text → UnexpectedToken → DispatchAdmission fails → windows-f20-lifecycle + windows-public-dispatch RED. Fix: de-verbatimize with `dunce::simplified` AT the canonicalize site (also defuses latent git-with-verbatim-paths, watch-item d1), add `dunce` as a direct dep of wcore-swarm, and belt-and-braces pass the probe path via the `WCORE_SWARM_PROBE_ROOT` env var.
- Root cause #B (production): `quote_arg` (`command.rs:76-111`) CRT-escapes embedded quotes to `\"` when `process.rs:616` builds the cmd child command line, but `cmd.exe /d /s /c` strips only the outer quote pair of the RAW command line and runs the rest verbatim → `type \"path\"` fails ERROR_INVALID_NAME and every `start \"\" /b` containment payload mangles → windows-appcontainer-acl + windows-retained-handle + windows-job-object RED. Fix in the BACKEND: for a cmd.exe `/c`/`/k` payload, wrap in a single outer quote pair and pass inner quotes through untouched (no CRT escaping).
- Job-Object flake: scope `image_count`/`tagged_cmd_count` to the test's own tagged process tree (and/or serialize the native containment targets via a nextest test-group), preserving every containment assertion.
- Lib-test compile debt: restore the missing imports in `windows_impl/tests.rs` + `directory_authority_windows_tests.rs` and fix the bad `windows_sys` FILE_ID import so the `#[cfg(test)]` modules compile on msvc.

Purpose: This supersedes the comprehensively-RED `daf27337`-bound plans 20-38…20-42 (authored, unexecuted). It writes the real production fixes so a NEW candidate can be re-sealed (20-44), Kimi-K3-cross-audited (20-45), and re-dispatched natively (20-46) under a fresh Sean authorization.
Output: One clean successor delta scoped to production `#A`/`#B` code plus the two test/compile defects. The `#![cfg(windows)]` process.rs/command.rs/containment/compile-debt changes are UNCOMPILABLE on Mac/Hetzner Linux — their compile AND real-hardware behavior are proven ONLY on the self-hosted msvc runner at 20-46 (honest per CONTEXT D5/D7). The wcore-swarm `dunce` change IS Linux-compiled and Linux-exercised — prove no regression on Hetzner.
</objective>

<redispatch_decision>
Re-dispatch structure — AUTHOR NEW PLANS 20-43…20-49; do NOT re-bind or edit the superseded 20-38…20-42.

Choice: the third repaired successor is the fully additive serial chain 20-43 (fix) -> 20-44 (re-seal) -> 20-45 (pre-native Kimi K3 cross-audit) -> 20-46 (native re-dispatch) -> 20-47 (fresh review, native NOT deferred, Kimi K3) -> 20-48 (re-prep tuple) -> 20-49 (terminal). This mirrors the 20-36…20-42 discipline.

Rationale (why new, not re-bind):
- 20-37 is EXECUTED and sealed `daf27337` as the second-repaired successor; the full `--no-fail-fast` diagnostic (run 30064412019) on real windows-msvc hardware plus the independent Kimi K3 cross-audit found that candidate comprehensively RED with two PRODUCTION-code root causes (#A verbatim-path probe reparse, #B cmd.exe quote-mangling). Re-binding a new fix onto 20-37 would overwrite that sealed record; a fresh production delta belongs on a new fix plan.
- 20-38…20-42 are authored but UNEXECUTED and hardcoded (`depends_on`, `@…20-37-SUMMARY.md` context refs, source_sha tuples) to the now-RED `daf27337`. Their upstream candidate is terminal RED, so they are self-consistently superseded (dead) WITHOUT any mutation; editing them in place would mutate authored plans and risk stale candidate binding.
- Net: 20-37 stays as the sealed-but-RED record; 20-38…20-42 are superseded (dead — their bound candidate is comprehensively RED) and are neither executed nor edited by this chain; the NEW third-repaired candidate flows through 20-44…20-49.
</redispatch_decision>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md
@.planning/phases/20-transactional-delegated-mutation/20-36-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-37-SUMMARY.md
@crates/wcore-swarm/src/worktree_manager.rs
@crates/wcore-swarm/Cargo.toml
@crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs
@crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
@crates/wcore-sandbox/tests/hard_process_containment_windows.rs
@crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs
@crates/wcore-sandbox/src/directory_authority_windows_tests.rs
</context>

<execution_rules>
- Require a clean isolated standalone checkout (`.git` is a directory) on branch `plan/f20-unified-audit-repair`. Do NOT execute in the sibling `/Users/seandonahoe/dev/waylandcore` primary checkout.
- The new candidate is a SUCCESSOR of the sealed-but-RED `daf27337` candidate. Only planning-doc commits sit between that seal and the branch tip; confirm the seven touched files are byte-identical to the sealed tree (no source drift) before editing, then author the fixes on the branch tip.
- Capture the plan base before any edit: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-43)"`. Advance the base between tasks with `--start-fresh` so each task's disjoint per-task scope gate passes.
- NO Cargo/clippy/nextest on the Mac, with EXACTLY ONE sanctioned exception (CONTEXT D5 carve-out): the targeted macOS compile gate `cargo check -p wcore-sandbox --features live-docker --tests` (compile/type-check only, no test run, no Docker). Fail-safe: if that check cannot run on the Mac, the gate BLOCKS rather than emitting a false green. Everything else is `vx rustfmt` (fmt) + toml/`bash -n` syntax + the committed-HEAD Hetzner harness (`/Users/seandonahoe/.ratchet/harness/remote-cargo.sh`) + node tooling.
- HONESTY (CONTEXT D5/D7): `process.rs`, `command.rs`, `hard_process_containment_windows.rs`, `windows_impl/tests.rs`, and `directory_authority_windows_tests.rs` are `#[cfg(windows)]`/`#![cfg(windows)]` — neither this Mac nor Hetzner Linux compiles a line of them. Their compile AND real-hardware behavior are proven ONLY on the self-hosted msvc runner at 20-46. Do NOT claim a Windows fix "verified" from any pre-dispatch gate. The wcore-swarm `worktree_manager.rs`/`Cargo.toml` change IS Linux-compiled and Linux-exercised (dunce::simplified is a no-op on unix) — prove no Linux regression on Hetzner.
- Touch ONLY the declared files. No drive-by refactor of the AppContainer implementation, the ACL/job-object test assertions, the probe grammar, or any other module. Preserve the DispatchAdmission budget, the 20-36 `exit /b 0` normalization, every containment assertion, `is_unc_or_device_path`, the Low-IL restricted token, the Job-Object limits, and the AppContainer ACL lease.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Root cause #A — de-verbatimize swarm_root/repo_root (dunce::simplified) + env-var probe transport, DispatchAdmission budget preserved</name>
  <files>crates/wcore-swarm/src/worktree_manager.rs, crates/wcore-swarm/Cargo.toml</files>
  <read_first>crates/wcore-swarm/src/worktree_manager.rs, crates/wcore-swarm/Cargo.toml, .planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md</read_first>
  <behavior>
    - `std::fs::canonicalize` at `worktree_manager.rs:9` (repo_root) and `:13` (swarm_root) always returns a `\\?\C:\...` verbatim path on Windows. The Windows capacity probe (`available_workspace_bytes`, `:418-451`; SCRIPT const `:419` uses `[IO.Path]::GetPathRoot($args[0])` then `[IO.DriveInfo]::new($root)`; the root is passed as a trailing argv entry after `-Command SCRIPT` at `:427`) hands that verbatim path to `powershell.exe -Command`, which re-parses trailing text as PowerShell script → UnexpectedToken at the `\\?\...` token → the probe errors → DispatchAdmission fails closed → windows-f20-lifecycle + windows-public-dispatch RED at admission (never reaching the acceptance body). Note also: even if merely quoted, `[IO.DriveInfo]::new` on a `\\?\C:\` root throws — so the prefix MUST be stripped regardless of transport.
    - Fix: wrap both canonicalize results with `dunce::simplified(...)` (which strips the `\\?\` verbatim prefix for real drive-letter paths and is a no-op on unix and for genuine UNC/device paths) so the stored `swarm_root`/`repo_root` are ordinary drive-letter paths. Doing it at the canonicalize SITE also de-verbatimizes every downstream git invocation that inherits the root (latent watch-item d1) in one place. `dunce` is currently only a transitive dep (aws-lc-sys, chromiumoxide in Cargo.lock) — add it as a DIRECT dependency of `wcore-swarm` in `Cargo.toml [dependencies]`; it is already resolved in Cargo.lock, so NO new crate is pulled (the lock resync is 20-44's concern).
    - Belt-and-braces: also transport the probe root out-of-band. Set it with `.env("WCORE_SWARM_PROBE_ROOT", <de-verbatimized root>)` on the tokio Command returned by `shell_command_argv` at the probe call site, and change the SCRIPT const to read `$env:WCORE_SWARM_PROBE_ROOT` instead of `$args[0]`. This removes the `-Command` trailing-arg reparse class for ALL weird path characters, not just `\\?\`.
    - ISOLATION GUARDRAIL: do NOT catch the PowerShell parse error and default capacity to unlimited/`u64::MAX` — that silently defeats the DispatchAdmission resource-governance budget. The probe must still compute real free space; admission must still enforce its budget. The unix `df` branch (`:376-415`) is unaffected (argv mode, no reparse) — leave it unchanged.
    - This code is cross-platform (`dunce::simplified` compiles and runs everywhere; it is a no-op on unix), so it IS exercised by the Hetzner Linux aggregate — the aggregate MUST stay 11509/0/48 (no behavior change on Linux). The Windows path behavior is proven at 20-46.
  </behavior>
  <action>In `crates/wcore-swarm/src/worktree_manager.rs`, wrap the `std::fs::canonicalize` results for `repo_root` (`:9`) and `swarm_root` (`:13`) with `dunce::simplified(...)` so the stored roots are de-verbatimized (per Kimi #A). At the Windows capacity-probe call site (`available_workspace_bytes`, `:418-451`), set `WCORE_SWARM_PROBE_ROOT` to the de-verbatimized root via `.env(...)` on the `shell_command_argv` tokio Command and change the SCRIPT const (`:419`) to read `$env:WCORE_SWARM_PROBE_ROOT` in place of `$args[0]`, dropping the trailing root argument after `-Command`. Do NOT add any error-swallowing fallback that defaults capacity to unlimited — keep the real free-space computation and the DispatchAdmission budget intact (isolation guardrail). Add `dunce` to `crates/wcore-swarm/Cargo.toml` `[dependencies]` as a direct dep (already in Cargo.lock; no new crate — if a `--locked` build later reveals a genuinely new crate, that is a 20-44 package-legitimacy checkpoint). Leave the unix `df` branch and every other canonicalize site not on the probe path unchanged. Implements REQ-native-r6; no Linux behavior change (REQ-native-r4).</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-43)" crates/wcore-swarm/src/worktree_manager.rs crates/wcore-swarm/Cargo.toml</automated>
    <automated>vx rustfmt --edition 2024 --check crates/wcore-swarm/src/worktree_manager.rs</automated>
    <automated>grep -nE "dunce::simplified" crates/wcore-swarm/src/worktree_manager.rs | grep -v '^#'</automated>
    <automated>grep -nE "WCORE_SWARM_PROBE_ROOT" crates/wcore-swarm/src/worktree_manager.rs | grep -c -v '^#'</automated>
    <automated>grep -nE "^dunce" crates/wcore-swarm/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>Diff-scope is exactly the two wcore-swarm files and fmt passes. `dunce::simplified` de-verbatimizes both canonicalize sites, the probe reads `WCORE_SWARM_PROBE_ROOT` (no trailing `-Command` arg), no capacity-to-unlimited fallback exists (grep-confirmed absent), `dunce` is a direct dep, and the unix `df` branch is unchanged. Linux no-regression proven on Hetzner in the verification section; Windows probe behavior proven at 20-46.</acceptance_criteria>
  <done>swarm_root/repo_root are de-verbatimized and the probe uses env transport with the admission budget intact, as a provisional task commit.</done>
</task>

<task type="auto">
  <name>Task 2: Root cause #B — backend cmd.exe /c|/k payload quoting (no CRT-escape), argv discipline + sandbox boundary preserved</name>
  <files>crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs, crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs</files>
  <read_first>crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs, crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs, crates/wcore-sandbox/tests/live_fs_acl.rs, .planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md</read_first>
  <behavior>
    - `command.rs:76-111` `quote_arg` CRT-escapes embedded quotes to `\"` (the Colascione/MSVC rule) when `process.rs:616` maps each argv entry through `quote_arg` to build the child command line. But `cmd.exe /d /s /c` does NOT consume the CRT-parsed argv: with `/s` it strips only the FIRST and LAST quote of the RAW command line and executes the remainder verbatim. So a script like `type "<file>" && (...)` is emitted as `"type \"<file>\" && (...)"`, cmd strips the outer pair, and executes `type \"<file>\"` → ERROR_INVALID_NAME (the observed windows-appcontainer-acl `:217` / windows-retained-handle `:387` stderr), and every containment `start "" /b ...` payload mangles identically. Proven by the audit: the ACE-presence wait passed before the read failed, so the grant works and only the read command was malformed.
    - Fix in the BACKEND (not the tests, because any production caller issuing `cmd /c` with embedded quotes hits the same landmine): when the resolved program is cmd.exe (argv[0] resolves to `cmd.exe`, `:218`) and the payload argument follows `/c` or `/k`, do NOT CRT-escape that payload. Wrap the script in a single outer double-quote pair and pass inner quotes through untouched, so cmd `/s` strips exactly the outer pair and runs the script verbatim. General argv entries and non-cmd programs keep the existing `quote_arg` behavior unchanged. Implement as a cmd-payload-aware branch in `process.rs` (detecting the resolved-cmd + `/c`/`/k` position) delegating to a dedicated helper in `command.rs`, leaving `quote_arg` itself intact for all other callers.
    - ISOLATION GUARDRAIL (Kimi): quoting layer ONLY. Keep argv discipline — do NOT switch to shell-string interpolation of caller data (AGENTS.md forbids `format!`-interpolating supplied data into a shell string). Do NOT relax `is_unc_or_device_path`, the Low-IL restricted token, the Job-Object limits, or the AppContainer ACL lease. The 20-36 `exit /b 0` exit-code normalization in the tests is COMPATIBLE with this fix — do NOT revert it.
    - `#![cfg(windows)]`: uncompilable on Mac/Hetzner. Compile + behavior are proven ONLY at the 20-46 msvc re-dispatch; this task's Mac gates are scope + fmt.
  </behavior>
  <action>In `crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs`, change the child-command-line construction (the `quote_arg` map at `:616`) so that when the resolved program is cmd.exe (`:218`) and an argument is the payload following `/c` or `/k`, that payload is wrapped in a single outer double-quote pair with its inner quotes passed through untouched (cmd `/s` strips the outer pair and executes verbatim) rather than CRT-escaped — per Kimi root cause #B. Add the cmd-payload quoting as a dedicated helper in `crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs`, leaving `quote_arg` unchanged for all non-cmd-payload argv entries. Do NOT switch to shell-string interpolation; do NOT relax `is_unc_or_device_path`, the Low-IL token, the Job-Object limits, or the ACL lease (isolation guardrail); do NOT revert the 20-36 `exit /b 0` normalization. Implements REQ-native-r2/r5/r7; Windows compile + behavior proven at 20-46.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-43)" crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs</automated>
    <automated>vx rustfmt --edition 2024 --check crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs</automated>
    <automated>grep -nE "is_unc_or_device_path|DISABLE_MAX_PRIVILEGE|JOB_OBJECT" crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs | grep -v '^#' | head</automated>
  </verify>
  <acceptance_criteria>Diff-scope is exactly process.rs + command.rs and fmt passes. A cmd `/c`/`/k` payload is emitted wrapped in a single outer quote pair without CRT-escaping inner quotes; `quote_arg` is unchanged for other argv; argv discipline holds (no shell-string interpolation); `is_unc_or_device_path`/Low-IL token/Job-Object limits/ACL lease are grep-confirmed present and unchanged; the 20-36 `exit /b 0` normalization is not reverted. Windows compile + behavior proven at 20-46.</acceptance_criteria>
  <done>The cmd.exe payload quoting no longer mangles embedded quotes, with the sandbox boundary intact, as a provisional task commit.</done>
</task>

<task type="auto">
  <name>Task 3: Job-Object flake — scope process counting to the test's tagged tree (containment assertions preserved)</name>
  <files>crates/wcore-sandbox/tests/hard_process_containment_windows.rs, .config/nextest.toml</files>
  <read_first>crates/wcore-sandbox/tests/hard_process_containment_windows.rs, .config/nextest.toml, .planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md</read_first>
  <behavior>
    - `image_count` (`:132`, `tasklist /IM <image>`) and `tagged_cmd_count` (`:109`, `Get-CimInstance Win32_Process Name='cmd.exe'`) count SYSTEM-WIDE by image name; `active_process_cap_is_enforced` baselines with `image_count("choice.exe")` (`:288`). `live_fs_acl` also spawns `choice.exe`, so a concurrent native target on the same runner pollutes the baseline/delta → windows-job-object flake (watch-item d2 + the earlier static audit).
    - Fix (primary, robust): scope the counting to the test's OWN tagged process tree — count only descendants of the test's spawned job/parent PID (walk the process tree by ParentProcessId / job membership) or by the test's unique tag, NOT host-wide image name. Preserve every containment assertion exactly: active_process_cap still asserts a positive peak and the cap ceiling, breakaway still asserts detached breakaway is denied, and job_close still asserts KILL_ON_JOB_CLOSE reaps the tagged descendants with no residue.
    - Fix (belt-and-braces, optional, executor's judgment): additionally add a `.config/nextest.toml` test-group that serializes the native containment targets on one runner so two `choice`-spawning targets never overlap. If added, it must not change which tests run or their selectors — only their concurrency.
    - `#![cfg(windows)]`: the counting change is uncompilable off Windows and proven at 20-46. The `.config/nextest.toml` change (if made) is a Linux-visible config file — validate it parses.
  </behavior>
  <action>In `crates/wcore-sandbox/tests/hard_process_containment_windows.rs`, rescope `image_count`/`tagged_cmd_count` (and the `active_process_cap_is_enforced` baseline at `:288`) to count only the test's own tagged process tree — descendants of the test's spawned parent/job by ParentProcessId or job membership rather than host-wide image name — so a concurrent `choice`-spawning target cannot pollute the count (per Kimi watch-item d2). Preserve every containment assertion (positive peak + cap ceiling, breakaway denied, job-close reap with no residue) verbatim. Optionally, at the executor's judgment, add a `.config/nextest.toml` test-group serializing the native containment targets as belt-and-braces (changing concurrency only, never selectors). Implements REQ-native-r7; Windows behavior proven at 20-46.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-43)" crates/wcore-sandbox/tests/hard_process_containment_windows.rs .config/nextest.toml</automated>
    <automated>vx rustfmt --edition 2024 --check crates/wcore-sandbox/tests/hard_process_containment_windows.rs</automated>
    <automated>grep -nE "ParentProcessId|ProcessId|job|tag" crates/wcore-sandbox/tests/hard_process_containment_windows.rs | grep -v '^#' | head</automated>
  </verify>
  <acceptance_criteria>Diff-scope is the containment test (and optionally `.config/nextest.toml`, which must parse) and fmt passes. Counting is scoped to the test's tagged tree (grep-confirmed PID/tag/job basis, not host-wide image); every containment assertion is preserved. Windows behavior proven at 20-46.</acceptance_criteria>
  <done>Job-Object counting no longer flakes under concurrent runners; containment properties preserved, as a provisional task commit.</done>
</task>

<task type="auto">
  <name>Task 4: Lib-test compile debt — restore missing imports + fix windows_sys FILE_ID import (msvc compiles)</name>
  <files>crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs, crates/wcore-sandbox/src/directory_authority_windows_tests.rs</files>
  <read_first>crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs, crates/wcore-sandbox/src/directory_authority_windows_tests.rs, .planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md</read_first>
  <behavior>
    - The Windows `--no-fail-fast` diagnostic surfaced a lib-test COMPILE-FAIL (real debt, not a proof-target blocker — proof builds only integration `--test` binaries): `windows_impl/tests.rs` and `directory_authority_windows_tests.rs` reference `Arc`, `SandboxError`, `SandboxManifest`, `SandboxCommand`, `NetworkPolicy` without importing them, and `directory_authority_windows_tests.rs` has a bad `windows_sys::...::FILE_ID_BOTH_DIR_INFORMATION` import path.
    - Fix: add the missing `use` imports (`std::sync::Arc` and the crate's `SandboxError`/`SandboxManifest`/`SandboxCommand`/`NetworkPolicy` types at their real module paths) and correct the `windows_sys` FILE_ID import to its actual path in the pinned `windows_sys` version, so both `#[cfg(test)]` modules compile on msvc. No behavioral test change — imports only.
    - `#[cfg(windows)]` + `#[cfg(test)]`: these modules are not compiled on Mac or Hetzner Linux, so the compile is proven ONLY at the 20-46 msvc re-dispatch (the diagnostic's lib-test build). The Mac floor is scope + fmt + an import-presence grep.
  </behavior>
  <action>In `crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs` and `crates/wcore-sandbox/src/directory_authority_windows_tests.rs`, add the missing `use` imports (`std::sync::Arc`, and `SandboxError`/`SandboxManifest`/`SandboxCommand`/`NetworkPolicy` from their real crate module paths) and correct the invalid `windows_sys::...::FILE_ID_BOTH_DIR_INFORMATION` import to its actual path in the pinned `windows_sys` version, so both `#[cfg(test)]` modules compile on msvc (per Kimi's compile-debt item). Imports only — do NOT change any test body, assertion, or non-test code. Implements REQ-native-r3; the msvc compile is proven at 20-46.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-43)" crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs crates/wcore-sandbox/src/directory_authority_windows_tests.rs</automated>
    <automated>vx rustfmt --edition 2024 --check crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs crates/wcore-sandbox/src/directory_authority_windows_tests.rs</automated>
    <automated>grep -nE "use std::sync::Arc|SandboxManifest|SandboxCommand|NetworkPolicy|FILE_ID_BOTH_DIR" crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs crates/wcore-sandbox/src/directory_authority_windows_tests.rs | grep -v '^#' | head</automated>
  </verify>
  <acceptance_criteria>Diff-scope is exactly the two lib-test files and fmt passes. The missing imports are present and the `windows_sys` FILE_ID import path is corrected (grep-confirmed); no test body changed. msvc compile proven at 20-46.</acceptance_criteria>
  <done>The two `#[cfg(test)]` modules have their imports restored so they compile on msvc, as a provisional task commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatch admission ← capacity probe | The probe computes the free-space budget that DispatchAdmission enforces; a path-normalization fix must not turn the probe into an unbounded/uncomputed capacity |
| sandbox boundary ← cmd command-line quoting | The child command line crosses into the AppContainer/Low-IL sandbox; a quoting fix must not become a shell-injection or boundary-relaxing change |
| containment proof ← process counting | The Job-Object containment assertions depend on an accurate count of the test's own descendants; a system-wide count is corrupted by concurrent runners |
| candidate identity ← Windows-only source | The `#[cfg(windows)]` fixes cannot be compiled off Windows; a compile break is invisible until the msvc re-dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-43-01 | Denial of Service | DispatchAdmission budget via capacity probe (#A) | high | mitigate | The probe path is de-verbatimized (dunce::simplified) and transported via env var, but the real free-space computation and the admission budget are preserved; the fix explicitly forbids the error-swallow-to-unlimited anti-pattern (Kimi #A isolation guardrail), grep-confirmed absent. |
| T-20-43-02 | Elevation of Privilege | cmd.exe payload quoting (#B) | high | mitigate | The change is a quoting-layer fix confined to the cmd `/c`/`/k` payload; argv discipline is kept (no shell-string interpolation of caller data), and `is_unc_or_device_path`, the Low-IL restricted token, the Job-Object limits, and the ACL lease are untouched (Kimi #B isolation guardrail). |
| T-20-43-03 | Tampering | Job-Object containment proof (#4) | medium | mitigate | Counting is rescoped to the test's own tagged tree so a concurrent runner cannot inflate/deflate it; every containment assertion (peak, cap ceiling, breakaway denial, job-close reap) is preserved verbatim, optionally reinforced by a nextest serialization group. |
| T-20-43-04 | Tampering | undetectable Windows-only compile break | medium | accept | The `#[cfg(windows)]`/`#![cfg(windows)]` files cannot be compiled off Windows; risk is bounded by scope + fmt + import/identifier grep gates and is proven at the 20-46 msvc re-dispatch (CONTEXT D5/D7). No pre-dispatch environment compiles these files. |
| T-20-43-05 | Tampering | supply-chain (dunce direct dep) | low | mitigate | `dunce` is already resolved in Cargo.lock (transitive via aws-lc-sys/chromiumoxide); promoting it to a direct dep pulls NO new crate. If 20-44's `--locked` resync reveals a genuinely new crate, STOP for a package-legitimacy checkpoint before committing the lock. |
</threat_model>

<verification>
Each task first passes its Mac construction gate (scope + `vx rustfmt --edition 2024 --check`, plus a toml/grep check) as a provisional task commit. After all four task commits exist and the checkout is clean, run the sanctioned pre-dispatch macOS compile re-confirmation and the committed-HEAD Hetzner no-regression gates (a construction commit is not behavioral evidence):
<automated>cargo check -p wcore-sandbox --features live-docker --tests</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-43-clippy "$(pwd -P)" clippy -p wcore-swarm -p wcore-sandbox --all-targets --all-features -- -D warnings</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-43-aggregate "$(pwd -P)" nextest run --workspace --profile ci --no-fail-fast</automated>
The macOS `cargo check` re-confirms the macOS-compiled wcore-sandbox surface (lib + docker_smoke + macOS-gated tests) still compiles — it does NOT compile the `#![cfg(windows)]` process.rs/command.rs/containment/compile-debt code. The Hetzner clippy gate proves the Linux-compiled wcore-swarm change (dunce::simplified, no-op on unix) is clean; the aggregate `nextest --profile ci --no-fail-fast` MUST be 11509/0/48 — the wcore-swarm change IS Linux-exercised, so a different count is a Linux regression that blocks the sequence (honesty gate). Note: this aggregate runs WITHOUT `--locked` because Task 1 added `dunce` to wcore-swarm/Cargo.toml and the lock is resynced authoritatively at 20-44. Record the exact committed `source_sha`/tree, the macOS compile receipt, the Hetzner clippy + aggregate receipts, changed paths, and the explicit deferral of ALL Windows compile/behavior (probe env transport, cmd payload quoting, containment counting, lib-test compile) to the 20-46 msvc re-dispatch in `20-43-SUMMARY.md`. No native, aggregate-seal, or requirement claim is made.
</verification>

<success_criteria>
- #A: `worktree_manager.rs:9,13` are de-verbatimized via `dunce::simplified`, the probe reads `WCORE_SWARM_PROBE_ROOT` with the DispatchAdmission budget intact (no unlimited fallback), and `dunce` is a direct dep (no new crate) (r6/r4).
- #B: the cmd.exe `/c`/`/k` payload is wrapped in a single outer quote pair without CRT-escaping inner quotes, `quote_arg` is otherwise unchanged, argv discipline and the sandbox boundary (is_unc_or_device_path, Low-IL token, Job-Object limits, ACL lease) are preserved, and 20-36 `exit /b 0` is not reverted (r2/r5/r7).
- Job-Object counting is scoped to the test's tagged tree with every containment assertion preserved (r7); the two lib-test modules have their imports restored to compile on msvc (r3).
- Sanctioned macOS `cargo check --tests` is green, Hetzner clippy `-D warnings` is clean, and the aggregate is 11509/0/48 with no Linux regression (r4); the summary records the exact new candidate `source_sha`/tree and defers every hardware runtime claim to 20-46.
</success_criteria>

## Artifacts this phase produces
- The two PRODUCTION-code root-cause fixes (#A verbatim-path probe in wcore-swarm; #B cmd.exe payload quoting in wcore-sandbox) plus the Job-Object counting-scope fix and the two lib-test compile-debt imports, as one reviewable third-repaired successor delta over the sealed-but-RED `daf27337`.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-43-SUMMARY.md` using the standard GSD summary template. Record the exact new candidate `source_sha`/tree, the macOS compile-check receipt, the Hetzner clippy + aggregate receipts, changed paths, the re-dispatch structure decision (new 20-43…20-49, not re-bind of superseded 20-38…20-42), the confirmation that #A and #B are scoped to production code with the isolation boundary preserved, and the explicit deferral of all Windows compile/behavior to 20-46. Complete no Phase 20 requirement.
</output>
</content>
</invoke>

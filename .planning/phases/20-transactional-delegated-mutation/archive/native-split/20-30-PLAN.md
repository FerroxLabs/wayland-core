---
phase: 20-transactional-delegated-mutation
plan: "30"
type: execute
wave: 29
depends_on:
  - "20-29"
files_modified:
  - Cargo.lock
autonomous: true
requirements:
  - REQ-native-r10
  - REQ-native-r4
must_haves:
  truths:
    - "The 20-29 repaired successor delta introduces NO new external crate (the fix is source-only: two unix rename_into methods reusing existing libc/renameat machinery plus a workflow YAML guard), so Cargo.lock stays consistent with Cargo.toml; a --locked Hetzner build succeeds without updating the lock, and a lock commit is created ONLY if a genuine delta is found (none expected) (REQ-native-r10)."
    - "This plan records the ONE exact repaired-successor candidate SHA/tree that every subsequent gate (cross-audit 20-31, native proof 20-32, fresh review 20-33, prep 20-34, terminal 20-35) binds to; no source, test, or workflow change follows it (REQ-native-r12 anchoring)."
    - "The sealed successor builds on Hetzner (build --workspace --all-features) and passes the aggregate Linux proof (nextest --profile ci) at the same 11509/0 count as the prior seal — the macOS acceptance test is cfg-excluded on Linux and the atomic_write_child publish unification is behavior-preserving, so no Linux count changes and there is no regression from the 20-29 fixes (REQ-native-r4)."
  artifacts:
    - path: Cargo.lock
      provides: "Confirmed-consistent workspace lockfile on the sealed repaired-successor candidate (regenerated and committed only if a delta exists)"
  key_links:
    - from: Cargo.lock
      to: crates/wcore-sandbox/Cargo.toml
      via: "the lock resolves the unchanged wcore-sandbox dependency surface; the 20-29 fix added no dependency"
      pattern: "wcore-sandbox"
---

<objective>
Seal the ONE exact repaired-successor candidate SHA: confirm `Cargo.lock` is still consistent (the 20-29 fix added no dependency), regenerate+commit it ONLY if a genuine delta exists, then prove on Hetzner that the sealed successor builds all-features and passes the aggregate Linux suite unchanged at 11509/0 — no regression from the 20-29 rename_into and workflow fixes.

Purpose: Downstream gates cannot bind to an unsealed or lock-inconsistent candidate (REQ-native-r10), and the Linux no-regression gate (REQ-native-r4) must pass before any scarce Sean-gated native run is re-spent. This seal anchors 20-31…20-35 to one exact successor SHA.
Output: The recorded sealed successor SHA/tree, a `--locked` Hetzner build receipt, and a `nextest --profile ci` = 11509/0 aggregate receipt. No native or requirement claim.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-23-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-29-SUMMARY.md
@.planning/codebase/TESTING.md
</context>

<execution_rules>
- Capture the plan base before any edit: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-30)"`.
- NO Cargo on the Mac. `Cargo.lock` (if a delta exists) is regenerated through the committed-HEAD Hetzner harness, retrieved, and committed on the Mac; the lock is NOT hand-edited.
- No new package is selected here: the 20-29 fix declared no new dependency, so the lock only needs a consistency confirmation. If a `--locked` build reveals a genuinely NEW external crate, STOP and surface it for a package-legitimacy checkpoint before committing.
- After this seal, NO source, test, or workflow change for the remainder of the repaired-successor sequence; the sealed SHA is the exact candidate all downstream gates bind to.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Confirm Cargo.lock consistency and record the sealed successor SHA</name>
  <files>Cargo.lock</files>
  <read_first>crates/wcore-sandbox/Cargo.toml, Cargo.lock, .planning/phases/20-transactional-delegated-mutation/20-29-SUMMARY.md</read_first>
  <behavior>
    - A `--locked` Hetzner build against the 20-29 successor HEAD succeeds without attempting to update the lock, proving `Cargo.lock` is consistent with the current `Cargo.toml` surface.
    - No new external crate is introduced beyond what the manifests already declare (the 20-29 fix reused existing `libc`/`renameat` machinery and touched no `Cargo.toml`); if a delta is found it is surfaced for a legitimacy checkpoint rather than silently committed.
    - The exact sealed successor SHA/tree is recorded: either the 20-29 source-complete HEAD (if the lock is already consistent — expected) or the new lock-sealing commit (if a genuine delta required regeneration).
  </behavior>
  <action>Through the committed-HEAD Hetzner harness, run a `--locked --workspace --all-features` build against the 20-29 successor HEAD to prove `Cargo.lock` is consistent. If it succeeds without updating the lock (expected — the fix added no dependency), record the 20-29 source-complete HEAD SHA/tree as the sealed successor candidate and make no lock commit. If the build reports the lock is stale, regenerate it via the harness (`generate-lockfile` or capture from a clean build), confirm the delta is limited to already-declared dependencies (surface any NEW crate for a legitimacy checkpoint), commit the lock as the sealing commit, and record that commit as the sealed SHA. Do not hand-edit the lock; do not touch any source, test, or workflow file.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-30)" Cargo.lock</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-30-locked-build "$(pwd -P)" build --workspace --all-features --locked</automated>
  </verify>
  <acceptance_criteria>A `--locked` Hetzner build succeeds without updating the lock; the sealed successor SHA/tree is recorded; any lock delta is limited to already-declared dependencies (no new crate). No source/test/workflow change.</acceptance_criteria>
  <done>The sealed successor SHA is fixed and lock-consistent; it is the exact repaired candidate.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the sealed successor builds and the aggregate Linux suite is unregressed</name>
  <files>None; this task produces Hetzner receipts against the sealed successor.</files>
  <read_first>.planning/codebase/TESTING.md, .planning/phases/20-transactional-delegated-mutation/20-23-SUMMARY.md</read_first>
  <behavior>
    - `build --workspace --all-features` succeeds on Hetzner against the sealed successor.
    - `nextest run --workspace --profile ci --no-fail-fast` on Hetzner reports 11509 passed / 0 failed / 48 skipped — the exact prior seal baseline; the macOS acceptance test is `#[cfg(target_os = "macos")]` and adds nothing to the Linux count, and the atomic_write_child publish unification is behavior-preserving over the existing atomic-write tests.
    - A different aggregate count is a regression from the 20-29 fixes and blocks the sequence; it is recorded and investigated, not rationalized.
  </behavior>
  <action>Run the two committed-HEAD Hetzner gates against the sealed successor: the full-workspace all-features build, and the aggregate `nextest --profile ci --no-fail-fast`. Confirm the aggregate is 11509 passed / 0 failed / 48 skipped — any deviation is a Linux regression from the rename_into unification and stops the sequence for repair. Record both receipts with exact command, exit status, counts, and remote receipt identity.</action>
  <verify>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-30-build "$(pwd -P)" build --workspace --all-features</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-30-aggregate "$(pwd -P)" nextest run --workspace --profile ci --no-fail-fast</automated>
  </verify>
  <acceptance_criteria>The sealed successor builds all-features and the aggregate Linux suite is 11509/0/48 with no regression. Any other count blocks the sequence.</acceptance_criteria>
  <done>The sealed successor has a green Hetzner build + aggregate receipt and is ready for the pre-native cross-audit (20-31).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate identity ← lockfile | The sealed SHA must carry a consistent lock; a stale/incoherent lock produces a build that does not match the reviewed source |
| dependency set ← Cargo.toml | The lock must not silently introduce an unaudited new crate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-30-01 | Tampering | Cargo.lock consistency | high | mitigate | A `--locked` Hetzner build proves consistency (never hand-edit); any NEW external crate is surfaced for a legitimacy checkpoint, not silently committed. |
| T-20-30-02 | Denial of Service | aggregate regression | medium | mitigate | The `nextest --profile ci` gate requires the exact 11509/0/48 baseline; a deviation blocks the sequence rather than being rationalized. |
</threat_model>

<verification>
The two Hetzner receipts (locked build + aggregate `nextest --profile ci`) determine acceptance. Record the sealed `source_sha`, source tree, both commands, exit statuses, counts, and remote receipt identity in `20-30-SUMMARY.md`. The summary states the candidate is sealed, no source/test/workflow changes follow, and native proof/requirement completion remain deferred (20-32/20-35). No Cargo runs on the Mac.
</verification>

<success_criteria>
- `Cargo.lock` is confirmed consistent (a `--locked` Hetzner build succeeds without updating it); a lock commit exists only if a genuine delta was found (r10).
- The sealed successor builds all-features on Hetzner and the aggregate suite is 11509/0/48 with no Linux regression (r4).
- The sealed SHA is recorded as the single candidate all downstream gates bind to.
</success_criteria>

## Artifacts this phase produces
- The sealed repaired-successor candidate SHA with a confirmed-consistent lockfile and green Hetzner build + aggregate receipts.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-30-SUMMARY.md` using the standard GSD summary template. Record the sealed `source_sha`/tree, the lock consistency result (delta or none), both Hetzner receipts, and the explicit statement that no source/test/workflow changes follow and native/requirement completion is deferred. Complete no Phase 20 requirement.
</output>

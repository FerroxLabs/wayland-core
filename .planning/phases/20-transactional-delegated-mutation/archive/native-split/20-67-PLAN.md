---
phase: 20-transactional-delegated-mutation
plan: "67"
type: execute
wave: 64
depends_on:
  - "20-63"
  - "20-64"
  - "20-66"
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - REQ-native-r2
  - REQ-native-r5
  - REQ-native-r7
  - REQ-native-r12
  - REQ-native-r4
must_haves:
  truths:
    - "The COMBINED further-repaired-successor tree — CLASS A + CLASS C (20-63, `live_fs_acl.rs`), CLASS D (20-64, `hard_process_containment_windows.rs`), and the 20-66-cross-audit-PASSED CLASS B production delta (20-65, `acl_lease.rs`) — is verified on real Windows hardware (SEANDESKTOP: Win11 build 26200 client SKU, AppContainer-capable) BEFORE any CI dispatch. The authoritative command `cargo nextest run -p wcore-sandbox --test live_fs_acl --test hard_process_containment_windows --run-ignored all --no-fail-fast` (with `WAYLAND_SANDBOX_LIVE_WINDOWS=1`) is ALL-GREEN — the full `--no-fail-fast` enumeration that first exposed all four failure classes at once at 20-53 now passes end to end (REQ-native-r12/r7/r5/r2)."
    - "Each class fix is proven on hardware, not by inference: CLASS A — the three revoke/absent tests (`granted_path_is_readable_then_revoked`, `one_exit_does_not_remove_another_execution_grant`, `unrelated_acl_survives_exact_sid_cleanup`) observe a genuinely cleared DACL (the SID-only detector no longer collides with the seed-dir name). CLASS C — the `waitfor` hold actually holds for the present-during-run window (`type_and_hold`/`echo_temp_and_hold` no longer collapse under NULL stdin). CLASS D — `job_close_reaps_detached_descendant_with_no_residue`, `breakaway_is_denied`, and `active_process_cap_is_enforced` pass with a NON-VACUOUS captured-PID reap via the CommandLine-free handshake. CLASS B — a genuine `fs_read_deny` subpath under a granted tree is DENIED (exit 1, 'Access is denied.') while granted siblings still read, the passing `normal_sid_only_grant_is_denied` and granted-read invariants still hold, and after the run the target carries NO residual `PROTECTED_DACL_SECURITY_INFORMATION` or grant (REQ-native-r2/r5/r7)."
    - "The run is on real hardware only (CONTEXT D7) — never claimed from cross-compilation or source inspection — and it is the Windows compile-of-record for the `#[cfg(windows)]` `acl_lease.rs` deny reimplementation that no Mac/Hetzner gate could compile. It is a Sean-authorized action (`ssh SeanD@seandesktop`, checkout `C:\\ferrox-win`); the tree under test is the exact combined-fix HEAD, and its SHA/tree is recorded so 20-68 dispatches that same tree. Any RED test is recorded honestly as an incomplete disposition routing back to the owning class plan, never masked (REQ-native-r4)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-67-SUMMARY.md
      provides: "Retained SEANDESKTOP native evidence that the combined four-class repair is all-green, bound to the exact combined-fix SHA, gating the CI dispatch at 20-68"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-67-SUMMARY.md
      to: .planning/phases/20-transactional-delegated-mutation/20-VERIFICATION.md
      via: "re-runs the exact --no-fail-fast enumeration that produced the 20-53 gap list and proves all four classes closed on hardware"
      pattern: "native_windows"
---

<objective>
Verify the COMBINED four-class repair on real Windows hardware (SEANDESKTOP) BEFORE any CI dispatch. Run the exact `--no-fail-fast` enumeration that first exposed all four failure classes at 20-53 and prove it is now all-green: CLASS A + C (20-63) and CLASS D (20-64) test fixes plus the 20-66-cross-audit-PASSED CLASS B production deny fix (20-65), together.

Purpose: The native proof at 20-53 died at target #2 and never reached the containment tests, so `--no-fail-fast` was the first time every failure was seen at once. This gate re-runs that same enumeration on the combined-fix tree so the closure is proven as a whole (not one-at-a-time), and it is the ONLY place the `#[cfg(windows)]` `acl_lease.rs` deny reimplementation actually compiles and its deny/allow/revoke behavior runs. It must be green here before the scarce full 6-target CI proof is dispatched at 20-68.
Output: Retained SEANDESKTOP native evidence bound to the exact combined-fix SHA, or an explicit incomplete disposition routing back to the owning class plan.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-VERIFICATION.md
@.planning/phases/20-transactional-delegated-mutation/20-63-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-64-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-65-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-66-SUMMARY.md
</context>

<execution_rules>
- Real-hardware only (CONTEXT D7): native proof is never claimed from cross-compilation or source inspection. This is a Sean-authorized action on SEANDESKTOP (`ssh SeanD@seandesktop`, checkout `C:\ferrox-win`).
- Gate precondition: 20-66 is a zero-finding ALL-FOUR PASS (the CLASS B security delta is audited). If 20-66 blocked, this plan does not run.
- The tree under test is the exact combined-fix HEAD: CLASS A/C (20-63) + CLASS D (20-64) + the audited CLASS B delta (20-65). Sync `C:\ferrox-win` to that exact SHA before running; record the SHA/tree.
- The pass bar is the FULL enumeration green: `cargo nextest run -p wcore-sandbox --test live_fs_acl --test hard_process_containment_windows --run-ignored all --no-fail-fast` under `WAYLAND_SANDBOX_LIVE_WINDOWS=1`, all-green (no fail-fast — every test runs so a residual failure in any class is seen). Do NOT dispatch CI (20-68) until this is green.
- Any RED test is recorded honestly as an incomplete disposition routing back to the owning class plan (A/C → 20-63, D → 20-64, B → 20-65 then re-audit at 20-66); never mask or rationalize a failure.
- Use `/usr/bin/git`; never stage the AGENTS.md ijfw churn.
</execution_rules>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The combined four-class repair on one tree: CLASS A SID-only detector + CLASS C `waitfor` stdin-free hold (20-63, `live_fs_acl.rs`); CLASS D CommandLine-free PID-handshake containment observers (20-64, `hard_process_containment_windows.rs`); and the CLASS B `fs_read_deny` protected-DACL production reimplementation (20-65, `acl_lease.rs`), pre-native cross-audited zero-finding by the four-way panel at 20-66. It is ready for the SEANDESKTOP native verification that must be all-green before any CI dispatch.</what-built>
  <how-to-verify>
    1. Confirm 20-66 is a zero-finding ALL-FOUR PASS (Codex Sol + Gemini Pro + Kimi K3 + internal Claude adversarial), each with a schema-validated artifact, over the exact CLASS B delta SHA.
    2. On SEANDESKTOP (`ssh SeanD@seandesktop`), sync `C:\ferrox-win` to the exact combined-fix HEAD (CLASS A/C + CLASS D + audited CLASS B), and record the SHA/tree.
    3. Run: `set WAYLAND_SANDBOX_LIVE_WINDOWS=1` then `cargo nextest run -p wcore-sandbox --test live_fs_acl --test hard_process_containment_windows --run-ignored all --no-fail-fast`. Every test must pass.
    4. Confirm each class on hardware: CLASS A — the three revoke/absent tests observe a cleared DACL; CLASS C — `type_and_hold`/`echo_temp_and_hold` actually hold; CLASS D — the three containment tests pass with a non-vacuous captured-PID reap; CLASS B — a genuine `fs_read_deny` subpath is denied (exit 1) while granted siblings read, `normal_sid_only_grant_is_denied` still passes, and no residual `PROTECTED_DACL_SECURITY_INFORMATION`/grant remains after the run.
    5. If any test is RED, record it as an incomplete disposition routing back to the owning class plan; do NOT proceed to 20-68.
  </how-to-verify>
  <resume-signal>Type "seandesktop-green: &lt;combined-fix-sha&gt;" once the full enumeration is all-green on hardware bound to that exact SHA, or describe the RED test(s) and the owning class plan to route back to.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Record the retained SEANDESKTOP native evidence bound to the exact combined-fix SHA</name>
  <files>None; this task records retained hardware evidence in 20-67-SUMMARY.md.</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-63-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-64-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-65-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-66-SUMMARY.md</read_first>
  <behavior>
    - The retained evidence names the exact combined-fix SHA/tree and the SEANDESKTOP host, and records the full `--no-fail-fast` run as all-green with the per-class confirmations (A cleared-DACL, C real hold, D non-vacuous reap, B genuine deny + no residual protection).
    - If the run was RED, the evidence records the specific failing test(s), the class, and the routing-back disposition.
  </behavior>
  <action>From the SEANDESKTOP run authorized at the checkpoint, record the retained native evidence in 20-67-SUMMARY.md: the exact combined-fix SHA/tree, the host (SEANDESKTOP, Win11 26200 client SKU), the exact command and `WAYLAND_SANDBOX_LIVE_WINDOWS=1` env, and the all-green `--no-fail-fast` result with the four per-class hardware confirmations (CLASS A cleared-DACL on the three revoke/absent tests; CLASS C real `waitfor` hold; CLASS D non-vacuous captured-PID reap on the three containment tests; CLASS B genuine `fs_read_deny` denial (exit 1) with granted siblings still readable, `normal_sid_only_grant_is_denied` still passing, and no residual `PROTECTED_DACL_SECURITY_INFORMATION`/grant). On any RED test, record the failing test, its class, and the incomplete disposition routing back to the owning class plan (A/C → 20-63, D → 20-64, B → 20-65+20-66). This is the compile-of-record for the `#[cfg(windows)]` `acl_lease.rs` deny reimplementation. Implements REQ-native-r2/r5/r7/r12/r4; gates the CI dispatch at 20-68.</action>
  <verify>
    <human-check>The 20-67-SUMMARY records the exact combined-fix SHA, the SEANDESKTOP host, the exact `--no-fail-fast` command + env, an all-green result (or the specific RED test(s) + routing-back disposition), and the four per-class hardware confirmations.</human-check>
  </verify>
  <done>Retained SEANDESKTOP native evidence bound to the exact combined-fix SHA shows the full enumeration all-green (all four classes closed on hardware), gating 20-68; or an explicit incomplete disposition routes back to the owning class plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI dispatch ← SEANDESKTOP green | The scarce full 6-target CI proof (20-68) is dispatched only after the combined-fix tree is all-green on real Windows hardware |
| native evidence integrity ← real hardware | The proof must come from a real Win11 AppContainer host, never cross-compilation or source inspection |
| combined-fix binding ← exact SHA | The evidence and the 20-68 dispatch must bind to the exact same combined-fix SHA/tree |
| CLASS B compile-of-record ← SEANDESKTOP | The `#[cfg(windows)]` deny reimplementation compiles nowhere the Mac/Hetzner gates run; SEANDESKTOP is its only compile+behavior proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-67-01 | Spoofing | native green claimed from non-hardware source | high | mitigate | Proof is real-hardware only (CONTEXT D7) on SEANDESKTOP; the evidence binds to the exact combined-fix SHA and the host identity. |
| T-20-67-02 | Tampering | a masked RED test lets a broken class through to CI | high | mitigate | `--no-fail-fast` runs every test so no residual class failure is hidden by an early exit; any RED is recorded honestly as an incomplete disposition routing back to the owning class plan. |
| T-20-67-03 | Information Disclosure | CLASS B deny still inert on hardware but passed on inference | high | mitigate | The gate requires an on-hardware genuine `fs_read_deny` denial (exit 1) with granted siblings readable and no residual PROTECT, not a source-inferred pass. |
| T-20-67-04 | Elevation of Privilege | dispatching CI before the hardware gate is green | medium | mitigate | 20-68 depends_on 20-67 and the blocking-human checkpoint; CI is not dispatched until SEANDESKTOP is all-green bound to the exact SHA. |
</threat_model>

<verification>
The SEANDESKTOP run under Sean's authorization determines acceptance: `cargo nextest run -p wcore-sandbox --test live_fs_acl --test hard_process_containment_windows --run-ignored all --no-fail-fast` all-green under `WAYLAND_SANDBOX_LIVE_WINDOWS=1`, on the exact combined-fix HEAD, with the four per-class hardware confirmations. Record the SHA/tree, the host, the command+env, the all-green result (or the specific RED test(s) + routing-back disposition), and the per-class confirmations in 20-67-SUMMARY.md. No source/test change here; requirements are not marked complete until the fresh native-inclusive review consumes this evidence — this gate produces the evidence and admits 20-68.
</verification>

<success_criteria>
- The full `--no-fail-fast` enumeration that first exposed all four classes at 20-53 is all-green on SEANDESKTOP for the combined-fix tree (REQ-native-r12/r7/r5/r2).
- Each class is confirmed on hardware: A cleared-DACL, C real hold, D non-vacuous reap, B genuine deny + no residual protection with the granted/normal-SID invariants preserved (REQ-native-r2/r5/r7).
- Retained evidence binds to the exact combined-fix SHA and gates the CI dispatch at 20-68; any RED test routes back to the owning class plan (REQ-native-r4).
</success_criteria>

## Artifacts this phase produces
- Retained SEANDESKTOP native evidence (real hardware) that the combined four-class repair is all-green, bound to the exact combined-fix SHA.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-67-SUMMARY.md` using the standard GSD summary template. Record the combined-fix `source_sha`/tree, the SEANDESKTOP host, the exact `--no-fail-fast` command + `WAYLAND_SANDBOX_LIVE_WINDOWS=1` env, the all-green (or RED + routing) result, and the four per-class hardware confirmations. Complete no Phase 20 requirement (completion is claimed after the fresh native-inclusive review consumes this evidence); this gate admits 20-68.
</output>
</content>

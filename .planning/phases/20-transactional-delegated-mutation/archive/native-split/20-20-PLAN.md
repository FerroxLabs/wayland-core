---
phase: 20-transactional-delegated-mutation
plan: "20"
type: execute
wave: 19
depends_on:
  - "20-19"
files_modified:
  - crates/wcore-sandbox/tests/live_fs_acl.rs
  - crates/wcore-swarm/tests/dispatch_smoke.rs
autonomous: true
requirements:
  - REQ-native-r2
  - REQ-native-r5
  - REQ-native-r6
must_haves:
  truths:
    - "The one-execution-grant native test holds its process alive with a stdin-free, exit-0 delay primitive and asserts on the granted read's success (exit 0), not on choice.exe's 1-based selection index, so a genuine granted read is what the test proves (REQ-native-r5)."
    - "The AppContainer isolation acceptance tests prove all four boundary facts on real hardware: a fs_read_allow-granted file is readable, an ungranted file is denied, the grant is revoked after the run, AND a genuine DENY ace still blocks while a file granted only to a normal SID (e.g. Everyone) is still denied — so dropping the deny-only SIDs restored reads without weakening isolation (REQ-native-r2)."
    - "dispatch_smoke's different-head-replacement test is Windows-portable: it drops open handles to the repo directory before renaming (or is otherwise restructured) so it no longer panics with Os code 5 PermissionDenied on Windows (REQ-native-r6)."
  artifacts:
    - path: crates/wcore-sandbox/tests/live_fs_acl.rs
      provides: "Stdin-free exit-0 hold primitive and the read/grant/revoke plus genuine-DENY and normal-SID-only-denied isolation proofs"
    - path: crates/wcore-swarm/tests/dispatch_smoke.rs
      provides: "Windows-portable different-head-replacement rejection test"
  key_links:
    - from: crates/wcore-sandbox/tests/live_fs_acl.rs
      to: crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs
      via: "native ACL tests exercise the restricted AppContainer child token from 20-19"
      pattern: "granted_path_is_readable_then_revoked"
    - from: crates/wcore-sandbox/tests/live_fs_acl.rs
      to: scripts/f20-native-windows-proof.ps1
      via: "windows-retained-handle / windows-appcontainer-acl proof targets select these tests"
      pattern: "one_execution_grant_never_leaks_to_another_identity"
---

<objective>
Fix the two Windows test bugs the hardware run exposed (a wrong exit-code assumption and a non-portable directory rename) and author the AppContainer isolation acceptance tests that prove the 20-19 read-boundary fix restored reads WITHOUT weakening the sandbox.

Purpose: The 20-19 fix drops the deny-only SIDs; that change touches a security boundary, so it must be proven — not asserted — that a genuine DENY still blocks and a normal-SID-only grant is still denied (REQ-native-r2 / CONTEXT D4). The two test bugs (REQ-native-r5/r6) otherwise fail the Windows proof for reasons unrelated to the sandbox.
Output: Corrected and newly-authored native `#[cfg(windows)]`/`#[ignore]` acceptance tests, construction-proven on the Mac and clippy-clean on Hetzner; real green is proven on hardware at 20-25.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/inbox/native-uat-repair-BRIEF.md
@.planning/phases/20-transactional-delegated-mutation/20-19-SUMMARY.md
@crates/wcore-sandbox/tests/live_fs_acl.rs
@crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs
@crates/wcore-swarm/tests/dispatch_smoke.rs
@.planning/codebase/TESTING.md
</context>

<execution_rules>
- Capture the plan base before edits: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-20)"`.
- NO Cargo/clippy/nextest on the Mac. Construction is `vx rustfmt`; native `#[cfg(windows)]` tests are compiled and run only on the self-hosted msvc runner at the 20-25 gate. Hetzner clippy checks the crates' Linux-visible surface.
- Native acceptance tests are `#[cfg(target_os = "windows")]` + `#[ignore]` + gated behind `WAYLAND_SANDBOX_LIVE_WINDOWS` and `AppContainerBackend::is_available()`, matching the existing `live_fs_acl.rs` cases so `--run-ignored all` on the real host runs them and other hosts skip.
- Isolation tests must be genuinely falsifiable: they must be able to FAIL if the boundary regresses (a normal-SID-only grant becoming readable, or a DENY ace being bypassed). No happy-path-only assertions (AGENTS.md / TESTING.md).
- Touch only the two declared test files. No production source change here (that was 20-19).
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Fix the exit-code and directory-rename test bugs</name>
  <files>crates/wcore-sandbox/tests/live_fs_acl.rs, crates/wcore-swarm/tests/dispatch_smoke.rs</files>
  <read_first>crates/wcore-sandbox/tests/live_fs_acl.rs, crates/wcore-swarm/tests/dispatch_smoke.rs</read_first>
  <behavior>
    - `type_and_hold` (live_fs_acl.rs) holds the sandboxed process alive with a stdin-free primitive whose exit code reflects the granted read's success (exit 0) — replacing the `choice.exe /T N /D Y` hold, which returns the 1-based selected-option index (default Y -> exit 1) and can never assert exit 0. The `one_execution_grant_never_leaks_to_another_identity` test then asserts the granting identity read exit 0 while the other identity was denied.
    - The delay must not require interactive stdin (bare `timeout.exe` fails under redirected stdin, which is why `choice` was originally chosen); the replacement keeps a no-interactive-stdin hold while yielding exit 0 on the granted read.
    - `dispatch_smoke::dispatch_rejects_different_head_repository_replacement` no longer renames a directory that still has open handles; it drops handles first (or restructures the replacement) so Windows does not return Os code 5 PermissionDenied, while still proving a different-HEAD repository replacement is rejected.
  </behavior>
  <action>In `live_fs_acl.rs`, replace the `choice.exe`-based hold in `type_and_hold` with a stdin-free primitive whose success maps to exit 0 (per the BRIEF §7.2 candidates: a cmd busy-wait / a `ping -n` loopback delay that needs no network grant / a tiny purpose-built sleeper), and change the `one_execution_grant_never_leaks_to_another_identity` assertion to gate on the granted read's exit 0 rather than the hold command's index; keep the "other identity denied" assertion. In `dispatch_smoke.rs` around the different-head-replacement case, ensure every handle to the repository directory is dropped before `fs::rename` (or restructure to avoid renaming an open directory), preserving the rejection assertion. Do not change production source or any unrelated test.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-20)" crates/wcore-sandbox/tests/live_fs_acl.rs crates/wcore-swarm/tests/dispatch_smoke.rs</automated>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-sandbox/tests/live_fs_acl.rs crates/wcore-swarm/tests/dispatch_smoke.rs</automated>
  </verify>
  <acceptance_criteria>Construction gate: both files scope-clean and fmt-clean; the hold primitive is stdin-free and exit-0-on-success; the rename no longer occurs on an open directory. No native run is claimed here.</acceptance_criteria>
  <done>The two test-bug fixes exist as a provisional task commit.</done>
</task>

<task type="auto">
  <name>Task 2: Author the AppContainer isolation acceptance proofs</name>
  <files>crates/wcore-sandbox/tests/live_fs_acl.rs</files>
  <read_first>crates/wcore-sandbox/tests/live_fs_acl.rs, crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs, crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs</read_first>
  <behavior>
    - `granted_path_is_readable_then_revoked` proves the positive path on hardware: a fs_read_allow-granted file is readable by the sandboxed child (exit 0), an ungranted file is denied, and the grant's ACL entry is removed after the run.
    - A new `deny_ace_still_blocks_granted_read` proves that an explicit DENY ace on the target file still blocks the sandboxed child even when a matching package-SID grant is also present — the DENY wins. This can FAIL if the boundary weakens.
    - A new `normal_sid_only_grant_is_denied` proves that a file granted ONLY to a normal SID (e.g. Everyone/Users), with no AppContainer package-SID grant, is STILL denied to the sandboxed child after the deny-only SIDs were dropped — confirming isolation is intrinsic to the AppContainer model and the deny-only marking was redundant, not load-bearing.
    - All three are `#[cfg(target_os = "windows")]` + `#[ignore]`, gated on `WAYLAND_SANDBOX_LIVE_WINDOWS` and `is_available()`, and self-qualify (not skip) on a real AppContainer host.
  </behavior>
  <action>Extend `live_fs_acl.rs` with the two new isolation proofs `deny_ace_still_blocks_granted_read` and `normal_sid_only_grant_is_denied`, and confirm `granted_path_is_readable_then_revoked` asserts read-then-revoke fully (grant ACE present during run, absent after). Construct each so the child runs under the 20-19 restricted AppContainer token via the same helper path the existing native cases use; write files whose DACLs encode exactly the case under test (package-SID grant; package-SID grant PLUS explicit DENY; normal-SID-only grant with no package grant); assert exit 0 only where a read must succeed and denial everywhere isolation must hold. Make the negative proofs genuinely falsifiable — if a normal-SID-only grant became readable or a DENY were bypassed, the test must fail. Match the existing native-test gating and helpers; add no production source.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-20)" crates/wcore-sandbox/tests/live_fs_acl.rs crates/wcore-swarm/tests/dispatch_smoke.rs</automated>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-sandbox/tests/live_fs_acl.rs</automated>
    <automated>git -c grep.lineNumber=false grep -q 'normal_sid_only_grant_is_denied' -- crates/wcore-sandbox/tests/live_fs_acl.rs && git -c grep.lineNumber=false grep -q 'deny_ace_still_blocks_granted_read' -- crates/wcore-sandbox/tests/live_fs_acl.rs</automated>
  </verify>
  <acceptance_criteria>The isolation proofs exist as `#[cfg(windows)]`/`#[ignore]` native tests that name the granted-read, revoke, genuine-DENY, and normal-SID-only-denied cases and are falsifiable. Construction only; hardware green is proven at 20-25.</acceptance_criteria>
  <done>The isolation acceptance tests exist as a provisional task commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sandboxed child ← file DACL | The child may read only what the AppContainer package SID is granted; DENY aces and normal-SID grants must not be reachable |
| test assertion ← hold primitive | The hold command's exit code must not mask or forge the granted read's real result |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-20-01 | Elevation of Privilege | dropped deny-only SIDs (20-19) | critical | mitigate | `normal_sid_only_grant_is_denied` + `deny_ace_still_blocks_granted_read` prove isolation held after the drop; both are falsifiable and run on hardware at 20-25. |
| T-20-20-02 | Repudiation | choice.exe exit-index assertion | high | mitigate | Replace with a stdin-free exit-0-on-success hold so the test can only pass on a genuine granted read, never on an unrelated hold-command index. |
| T-20-20-03 | Tampering | non-portable directory rename | low | accept-then-fix | Windows rename-with-open-handle is an OS constraint; drop handles before rename so the rejection assertion is what actually runs. |
</threat_model>

<verification>
After both task commits exist and the checkout is clean, the committed-HEAD Hetzner gate confirms the crates still build/lint on Linux (the native tests are cfg-excluded there):
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-20-clippy "$(pwd -P)" clippy -p wcore-sandbox -p wcore-swarm --all-targets --all-features -- -D warnings</automated>
Record exact `source_sha`/tree, the receipt, and changed paths in `20-20-SUMMARY.md`. The summary states that the native isolation and test-bug fixes are proven ONLY on hardware at 20-25 and makes no requirement claim.
</verification>

<success_criteria>
- The exit-code and directory-rename test bugs are fixed (r5, r6) and construction-clean.
- The read/grant/revoke plus genuine-DENY and normal-SID-only-denied isolation proofs exist and are falsifiable (r2 proof / D4).
- Hetzner clippy is clean for `wcore-sandbox` and `wcore-swarm`.
- The summary defers all hardware green to 20-25 and completes no requirement.
</success_criteria>

## Artifacts this phase produces
- Corrected native ACL test hold primitive and portable dispatch test, plus the AppContainer isolation acceptance proofs.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-20-SUMMARY.md` using the standard GSD summary template. Record exact source identity, the Hetzner clippy receipt, changed paths, and the explicit deferral of native green to 20-25. Complete no Phase 20 requirement.
</output>

---
phase: 20-transactional-delegated-mutation
plan: "60"
type: execute
wave: 55
depends_on:
  - "20-59"
files_modified:
  - crates/wcore-sandbox/tests/hard_process_containment_windows.rs
autonomous: true
requirements:
  - REQ-native-r7
  - REQ-native-r4
must_haves:
  truths:
    - "The 20-59 BLOCK finding (Codex MEDIUM / Kimi LOW / Claude LOW, real, non-refutable, three of four auditors converged) is CLOSED: in `crates/wcore-sandbox/tests/hard_process_containment_windows.rs`, EVERY host-side reap observer's PowerShell `-Command` script now FAILS CLOSED on a CIM/PowerShell query error. The three observer scripts (`tagged_cmd_count`, `tagged_choice_descendant_pids`, `surviving_captured_choice_pids`) each escalate a non-terminating `Get-CimInstance` error to a TERMINATING error that exits `powershell.exe` non-zero — so the already-present Rust-layer `assert!(out.status.success())` fires and the query can no longer read as a passing `@(...).Count == '0'` (or an empty PID token stream). A post-close CIM failure that exits 0-with-error-record can therefore NO LONGER satisfy `wait_until(observer == 0)` and report the reap property proven WITHOUT evidence — the exact 20-52 fail-open class the 20-57 fix moved one layer down (Rust → PowerShell) is now closed at the PowerShell layer too (REQ-native-r7)."
    - "The hardening is BELT-AND-BRACES / defense-in-depth: ALL THREE mechanisms are applied to EACH of the three observer scripts (not a single flag) — (1) `$ErrorActionPreference='Stop'` prepended to the `-Command` script string; (2) `-ErrorAction Stop` on EVERY `Get-CimInstance` invocation (two of them in `tagged_choice_descendant_pids`, one each in the other two — four total across the file); (3) a leading `trap { exit 1 }` so any terminating error deterministically exits non-zero even if a future edit reintroduces a non-Stop path. The empty-vs-error ambiguity in `tagged_choice_descendant_pids` is thereby resolved: with Stop a CIM error TERMINATES (exit 1 → the Rust status assert panics), so an empty stdout can ONLY mean a genuine success-with-no-descendants (still a valid empty Vec) (REQ-native-r7)."
    - "Fix vs scope-change distinguished: ONLY the three `-Command` script STRINGS (and their doc comments) change. The 20-57 Rust-layer hardening the 20-59 panel confirmed sound is PRESERVED VERBATIM: `out.status.success()` ×3, the panicking parse (`unwrap_or_else(|err| panic!(...))`) ×3, no `parse().unwrap_or(0) => 0`, no `filter_map(...ok())` swallow. The captured-PID two-phase reap logic, `assert!(!captured_pids.is_empty())`, the `if pids.is_empty() { return 0; }` empty-set short-circuit, the peak/cap/below-attempts/clean-exit assertions, and the sibling `job_close_reaps_detached_descendant_with_no_residue` / `breakaway_is_denied` / `qualified_hard_containment_backend_preflight` tests are unchanged — the 20-59 panel confirmed every one of these sound. This is 'make the query fail closed', NOT 'change what is asserted' (REQ-native-r7)."
    - "OUT OF SCOPE and untouched (the 20-59 panel confirmed sound by absence of change): the Finding-2 `command.rs` cmd exact-final-component classifier (`resolved_program_is_cmd`), both isolation guardrails (#A DispatchAdmission budget in `worktree_manager.rs`; #B quoting-layer-only — `is_unc_or_device_path`, Low-IL token, Job-Object limits, ACL lease, argv discipline). This delta touches ONE file only; the production sandbox boundary is not modified. The benign `unique_tag` SystemTime-nanos `unwrap_or(0)` fallback and the `reap_stray_choice` best-effort `let _ = ...output()` cleanup stay untouched (REQ-native-r7)."
    - "Linux unregressed and HONESTLY DEFERRED: the file is `#![cfg(windows)]`, so zero Linux compiled-output change — the Hetzner aggregate stays 11509/0/48 and the sanctioned macOS `cargo check -p wcore-sandbox --features live-docker --tests` stays green; NEITHER the Mac nor Hetzner Linux compiles a line of the PowerShell-layer fix (it is Windows-only test code). Its compile AND real-hardware behavior are proven ONLY at the rebound 20-53 native msvc re-dispatch. The Codex post-close PID-reuse-race LOW is carried as a fail-closed 20-53 native watch-item (it can only false-FAIL, never false-green), NOT expanded in scope here (REQ-native-r4)."
  artifacts:
    - path: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      provides: "All three host-side reap-observer PowerShell scripts hardened to fail CLOSED on a CIM/PowerShell error ($ErrorActionPreference='Stop' + -ErrorAction Stop on every Get-CimInstance + a leading trap { exit 1 }), so a non-terminating CIM error becomes a terminating error that exits powershell.exe non-zero and is caught by the existing assert!(out.status.success()); the 20-57 Rust-layer hardening, every assertion body, and every sibling test preserved verbatim"
  key_links:
    - from: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      to: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      via: "a failed CIM query now escalates to a terminating PowerShell error → powershell.exe exits non-zero → the existing assert!(out.status.success()) fires, so the reap wait_until(... == 0) checks can no longer pass on a query error that exits 0-with-error-record"
      pattern: "$ErrorActionPreference='Stop'"
---

<objective>
Close the ONE real, statically-provable, non-refutable finding the 20-59 FOUR-WAY pre-native panel (Codex 5.6 Sol MEDIUM + Kimi K3 LOW + internal Claude adversarial LOW, all three concurring; Gemini missed it) returned BLOCK on against the re-sealed successor `8a1d2d84` (tree `c1fe79fe`): the residual PowerShell-LAYER fail-open. The 20-57 fix correctly closed the RUST-layer fail-open (each observer now `assert!(out.status.success())` after `.output()` and parses with a panicking fallback), and the 20-59 panel confirmed that half sound — but the three `-Command` CIM scripts set NO `$ErrorActionPreference='Stop'`, NO `-ErrorAction Stop`, NO `trap`. Under Windows PowerShell a non-terminating `Get-CimInstance` error leaves `powershell.exe` exiting 0 with `@(...).Count == '0'` (or an empty PID token stream), so `out.status.success()` is TRUE, the panicking parse succeeds on `'0'`/empty, and `wait_until(observer == 0)` is satisfied WITHOUT evidence — the exact 20-52 fail-open class moved one layer down (Rust → PowerShell).

This is a COMPREHENSIVE, belt-and-braces, one-file fix that closes the PowerShell layer under the (preserved) Rust layer: harden ALL THREE `-Command` scripts in `crates/wcore-sandbox/tests/hard_process_containment_windows.rs` so a CIM/PowerShell error is TERMINATING and exits non-zero, applying all three mechanisms to each script for defense-in-depth — `$ErrorActionPreference='Stop'`, `-ErrorAction Stop` on every `Get-CimInstance`, and a leading `trap { exit 1 }` — so a re-audit cannot surface a residual exit-0-with-error-record path.

Purpose: The 20-59 gate BLOCKED and routed to a further-repaired successor. This delta writes the PowerShell-layer observer-integrity fix so a NEW candidate can be re-sealed (20-61), re-cross-audited by the full four-way panel (20-62), and — on a zero-finding ALL-FOUR PASS — admit the rebound native re-dispatch (20-53 onto the 20-61 seal).
Output: One clean successor delta scoped to the single finding file, changing ONLY the three `-Command` script strings and their doc comments. The change is `#![cfg(windows)]` — UNCOMPILABLE on Mac/Hetzner Linux; its compile AND real-hardware behavior are proven ONLY on the self-hosted msvc runner at the rebound 20-53 (honest per CONTEXT D5/D7). No file outside the one named file is touched; no requirement is completed.
</objective>

<redispatch_decision>
Chain structure — AUTHOR DELTA plans 20-60 (this fix) → 20-61 (re-seal) → 20-62 (four-way pre-native cross-audit), then REBIND the existing pending (unexecuted) 20-53…20-56 onto the 20-61 seal (depends_on chains from 20-62; symbolic seal reference 20-58 → 20-61; pre-native cross-audit reference 20-59 → 20-62; add the 20-59-blocked `8a1d2d84` to the spent/void authorization enumerations; bump waves for DAG consistency). Do NOT mutate the executed/sealed records 20-43/20-44/20-45/20-50/20-51/20-52/20-57/20-58/20-59.

Rationale:
- 20-59 is EXECUTED and sealed (its four-way BLOCK is on disk); a fresh PowerShell-layer fix belongs on a new plan (20-60), never a re-bind of the sealed BLOCK.
- 20-53…20-56 are authored but UNEXECUTED. Rewiring their upstream (depends_on) and their symbolic seal/cross-audit references and spent-auth enumerations is a pure pending-plan rebind — it mutates no sealed record and is the efficient alternative to superseding + re-authoring the native/review/prep/terminal chain each cycle.
- Net: 20-43/44/45/50/51/52/57/58/59 stay as the sealed record; the further-repaired candidate flows 20-60 → 20-61 → 20-62 → (rebound) 20-53 → 20-54 → 20-55 → 20-56.
</redispatch_decision>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-59-CROSS-AUDIT.md
@.planning/phases/20-transactional-delegated-mutation/20-59-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-57-SUMMARY.md
@crates/wcore-sandbox/tests/hard_process_containment_windows.rs
</context>

<execution_rules>
- Require a clean isolated standalone checkout (`.git` is a directory) on branch `plan/f20-unified-audit-repair`, in `/Users/seandonahoe/dev/waylandcore-ferrox`. Do NOT execute in the sibling `/Users/seandonahoe/dev/waylandcore` primary checkout. All git ops via `/usr/bin/git`.
- The new candidate is a SUCCESSOR of the 20-59-blocked `8a1d2d84` (tree `c1fe79fe`). Only planning-doc commits sit between that seal and the branch tip; confirm the ONE touched file is byte-identical to the sealed candidate (no source drift) before editing — `git diff 8a1d2d84 -- crates/wcore-sandbox/tests/hard_process_containment_windows.rs` must be empty — then author the fix on the branch tip.
- Capture the plan base before any edit: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-60)"`.
- NO Cargo/clippy/nextest on the Mac, with EXACTLY ONE sanctioned exception (CONTEXT D5 carve-out): the targeted macOS compile gate `cargo check -p wcore-sandbox --features live-docker --tests` (compile/type-check only, no test run, no Docker). Fail-safe: if that check cannot run on the Mac, the gate BLOCKS rather than emitting a false green. Everything else is `vx rustfmt --edition 2024 --check` (fmt) + grep + the committed-HEAD Hetzner harness (`/Users/seandonahoe/.ratchet/harness/remote-cargo.sh`) + node tooling.
- HONESTY (CONTEXT D5/D7): `hard_process_containment_windows.rs` is `#![cfg(windows)]` — neither this Mac nor Hetzner Linux compiles a line of the PowerShell-layer fix. Its compile AND real-hardware behavior (a non-terminating CIM error now exits non-zero) are proven ONLY on the self-hosted msvc runner at the rebound 20-53. Do NOT claim a Windows fix "verified" from any pre-dispatch gate. Because the file is Windows-only, the sanctioned macOS compile gate and the Hetzner aggregate/clippy are a NO-REGRESSION floor (they must stay green + 11509/0/48), not a proof of the fix.
- Touch ONLY the one declared file, and within it ONLY the three `-Command` script STRINGS (and their doc comments). No drive-by change to the Rust-layer hardening (the `assert!(out.status.success())` and panicking parse stay VERBATIM), the `command.rs` cmd classifier, the two isolation guardrails (#A/#B), the AppContainer implementation, the containment assertions, the captured-PID two-phase reap logic, the `if pids.is_empty() { return 0; }` short-circuit, the `reap_stray_choice` best-effort cleanup, the `unique_tag` nanos fallback, or any test body. Distinguish "make the query fail closed" (the fix) from "changing WHAT is asserted" (NOT done): every assertion is preserved verbatim.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Harden ALL THREE reap-observer PowerShell scripts to fail CLOSED on a CIM error (ErrorActionPreference Stop + -ErrorAction Stop on every Get-CimInstance + a leading trap), preserving the Rust layer and every assertion verbatim</name>
  <files>crates/wcore-sandbox/tests/hard_process_containment_windows.rs</files>
  <read_first>crates/wcore-sandbox/tests/hard_process_containment_windows.rs, .planning/phases/20-transactional-delegated-mutation/20-59-CROSS-AUDIT.md, .planning/phases/20-transactional-delegated-mutation/20-59-SUMMARY.md</read_first>
  <behavior>
    - Three host-side reap observers shell out to `powershell -Command` with a CIM query script. The 20-57 fix hardened the RUST layer around each (`assert!(out.status.success())` + panicking parse), which the 20-59 panel confirmed sound and which MUST be preserved. But the `-Command` script bodies escalate no CIM error, so under Windows PowerShell a non-terminating `Get-CimInstance` failure leaves `powershell.exe` exiting 0 with a passing-looking result — the residual PowerShell-LAYER fail-open the 20-59 panel blocked on:
      1. `tagged_cmd_count(tag: &str) -> usize` — one `Get-CimInstance Win32_Process` piped to `Where-Object` under `@(...).Count`. On a non-terminating CIM error `@(<failed>).Count` prints `'0'` and `powershell.exe` exits 0, so the Rust status/parse checks both pass and the `wait_until(tagged_cmd_count(...) == 0, ...)` reap checks (in `job_close_reaps_detached_descendant_with_no_residue`, `breakaway_is_denied`, `qualified_hard_containment_backend_preflight`) are satisfied without evidence.
      2. `tagged_choice_descendant_pids(tag: &str) -> Vec<u32>` — TWO `Get-CimInstance Win32_Process` invocations (a `$parents` cmd-tree query, then a `choice.exe` parent-scoped query). On a non-terminating CIM error the second query yields no tokens and `powershell.exe` exits 0, so a query error is indistinguishable from a legitimate empty result.
      3. `surviving_captured_choice_pids(pids: &[u32]) -> usize` (the named finding) — one `Get-CimInstance Win32_Process` filtered by captured `$pids` under `@(...).Count`. Same shape as (1): a CIM error prints `'0'` at exit 0, satisfying `wait_until(|| surviving_captured_choice_pids(&captured_pids) == 0, 30, ...)` on the first poll.
    - Fix (COMPREHENSIVE, belt-and-braces — harden all three `-Command` script STRINGS; touch ONLY those strings and their doc comments). Apply ALL THREE mechanisms to EACH script (do NOT rely on a single flag):
      1. Prepend `$ErrorActionPreference='Stop'; ` to each `-Command` script string, so a non-terminating CIM error becomes a terminating error.
      2. Add `-ErrorAction Stop` to EVERY `Get-CimInstance` invocation — one in `tagged_cmd_count`, TWO in `tagged_choice_descendant_pids`, one in `surviving_captured_choice_pids` (four total across the file), so the error escalation holds at each query even if the preference is later weakened.
      3. Add a leading `trap { exit 1 } ` to each script so any terminating error deterministically exits `powershell.exe` non-zero even if a future edit reintroduces a non-Stop path.
      CRITICAL Rust-syntax note: each `-Command` body is a `format!` literal (it interpolates `{tag}` / `{pid_list}`), so every literal PowerShell brace is DOUBLED in the Rust source — the file already writes `Where-Object {{ ... }}`. Write the trap's braces DOUBLED as `trap {{ exit 1 }}` (and keep any other literal braces doubled); a single-brace `trap { exit 1 }` would break the `format!` and fail compilation — a break that is INVISIBLE off Windows (`#![cfg(windows)]`) and would not surface until the native msvc run. The `$ErrorActionPreference='Stop';` prefix and `-ErrorAction Stop` flags contain no braces and are written verbatim.
      With any of these in force, a CIM error TERMINATES → `powershell.exe` exits non-zero → the already-present Rust `assert!(out.status.success())` fires (fail-closed). The empty-vs-error ambiguity in `tagged_choice_descendant_pids` is resolved: a CIM error now TERMINATES rather than yielding empty-at-exit-0, so an empty stdout can ONLY mean a genuine success-with-no-descendants — still a valid empty `Vec`.
    - Update each observer's doc comment to state the PowerShell layer now fails closed: a CIM/PowerShell query error is terminating and exits non-zero, so a failed query can never read as a passing count or an empty PID list; note for `tagged_choice_descendant_pids` that with the error escalation an empty stdout can only mean a genuine success-with-no-descendants. Do NOT weaken the 20-57 Rust-layer doc-comment claims; extend them to name the PowerShell layer.
    - Full-file sweep — confirm the fix is complete and no other observer/`wait_until`/assertion can report a containment or reap property satisfied without evidence after this change: (a) all three `-Command` scripts carry the three mechanisms; (b) the 20-57 Rust-layer hardening is intact (`out.status.success()` ×3, panicking parse ×3, `filter_map` absent, the sole `.unwrap_or(0)` is the benign `unique_tag` nanos fallback); (c) `wait_until` still panics on timeout (fail-closed) — leave it; (d) `reap_stray_choice`'s best-effort `let _ = ...taskkill...output()` stays intentional cleanup — leave it. Do NOT touch (b)'s `unique_tag` fallback or (d).
    - PRESERVE VERBATIM (the 20-59 panel confirmed sound): the Rust-layer hardening (status-assert + panicking parse), the captured-PID two-phase reap logic (peak-set alive-phase capture + fixed-PID post-close re-check), `assert!(!captured_pids.is_empty())`, `peak > 0`, `peak <= SANDBOX_ACTIVE_PROCESS_LIMIT`, `peak < attempts`, `exit_code == 0`, the `if pids.is_empty() { return 0; }` short-circuit, the `reap_stray_choice()` trailers, and the sibling tests. Only the three `-Command` script strings and their doc comments change; no assertion, no Rust-layer check, and no test body is altered.
    - `#![cfg(windows)]`: uncompilable on Mac/Hetzner. Compile + behavior (a non-terminating CIM error now exits non-zero) are proven ONLY at the rebound 20-53 msvc re-dispatch; this task's Mac gates are scope + fmt + grep.
  </behavior>
  <action>In `crates/wcore-sandbox/tests/hard_process_containment_windows.rs`, harden ALL THREE reap-observer PowerShell `-Command` scripts to FAIL CLOSED on a CIM/PowerShell error, applying all three mechanisms to each for defense-in-depth: (1) prepend `$ErrorActionPreference='Stop'; ` to each `-Command` script string; (2) add `-ErrorAction Stop` to EVERY `Get-CimInstance` invocation — one in `tagged_cmd_count`, two in `tagged_choice_descendant_pids`, one in `surviving_captured_choice_pids`, four total; (3) add a leading `trap { exit 1 } ` to each script so any terminating error deterministically exits non-zero. Because each `-Command` body is a `format!` literal, write the trap's braces DOUBLED in the Rust source as `trap {{ exit 1 }}` (mirroring the existing `Where-Object {{ ... }}`) so the `format!` stays valid — a single-brace form breaks compilation invisibly off Windows. With any mechanism in force a non-terminating CIM error becomes terminating → `powershell.exe` exits non-zero → the already-present Rust-layer `assert!(out.status.success())` fires, so a failed query can no longer read as `@(...).Count == '0'` or an empty PID stream and satisfy a reap `wait_until(... == 0)` without evidence. Update each observer's doc comment to state the PowerShell layer now fails closed (query errors are terminating and exit non-zero; a failed query can never read as a passing count/empty list), and note that in `tagged_choice_descendant_pids` an empty stdout can now only mean a genuine success-with-no-descendants (a still-valid empty Vec). PRESERVE VERBATIM the 20-57 Rust-layer hardening (`out.status.success()` ×3 + panicking parse ×3; no `parse().unwrap_or(0) => 0`; no `filter_map(...ok())`), the captured-PID two-phase reap logic, `!captured_pids.is_empty()`, the `if pids.is_empty() { return 0; }` short-circuit, the peak/cap/below-attempts/clean-exit assertions, and the sibling `job_close_reaps_detached_descendant_with_no_residue` / `breakaway_is_denied` / `qualified_hard_containment_backend_preflight` tests. Do a full-file sweep and confirm the only remaining `.unwrap_or(0)` is the benign `unique_tag` SystemTime-nanos fallback (leave it), `reap_stray_choice` stays best-effort cleanup (leave it), and `wait_until` still panics on timeout (leave it) — no other observer, predicate, or assertion may report a property satisfied without evidence. Change ONLY the three `-Command` script strings and their doc comments; do NOT touch `command.rs`, the #A/#B guardrails, the Rust-layer checks, or any file outside this one. Implements REQ-native-r7; Windows compile + behavior proven at the rebound 20-53.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-60)" crates/wcore-sandbox/tests/hard_process_containment_windows.rs</automated>
    <automated>vx rustfmt --edition 2024 --check crates/wcore-sandbox/tests/hard_process_containment_windows.rs</automated>
    <automated>test "$(grep -c "ErrorActionPreference='Stop'" crates/wcore-sandbox/tests/hard_process_containment_windows.rs)" -ge 3</automated>
    <automated>test "$(grep -o -- "-ErrorAction Stop" crates/wcore-sandbox/tests/hard_process_containment_windows.rs | wc -l | tr -d ' ')" -ge 4</automated>
    <automated>test "$(grep -c "trap {{" crates/wcore-sandbox/tests/hard_process_containment_windows.rs)" -ge 3</automated>
    <automated>test "$(grep -o "Get-CimInstance" crates/wcore-sandbox/tests/hard_process_containment_windows.rs | wc -l | tr -d ' ')" -eq 4</automated>
    <automated>test "$(grep -c "out.status.success()" crates/wcore-sandbox/tests/hard_process_containment_windows.rs)" -eq 3</automated>
    <automated>grep -nE "unwrap_or_else\(\|err\| panic!|unwrap_or_else\(\|" crates/wcore-sandbox/tests/hard_process_containment_windows.rs | head</automated>
    <automated>grep -nE "fn tagged_cmd_count|fn tagged_choice_descendant_pids|fn surviving_captured_choice_pids" crates/wcore-sandbox/tests/hard_process_containment_windows.rs</automated>
    <automated>grep -nE "if pids\.is_empty\(\)|!captured_pids\.is_empty\(\)|peak > 0|peak <= SANDBOX_ACTIVE_PROCESS_LIMIT|peak < attempts" crates/wcore-sandbox/tests/hard_process_containment_windows.rs | head</automated>
  </verify>
  <acceptance_criteria>Diff-scope is exactly the containment test and fmt passes. All three `-Command` scripts carry `$ErrorActionPreference='Stop'` (grep >= 3), a leading `trap {{ exit 1 }}` (doubled braces in the `format!` source; grep `trap {{` >= 3), and `-ErrorAction Stop` on every `Get-CimInstance` (>= 4 occurrences over exactly 4 `Get-CimInstance`), so a CIM error becomes terminating and exits non-zero. The 20-57 Rust-layer hardening is preserved (grep: `out.status.success()` exactly 3, panicking `unwrap_or_else`/`panic!` present); the `if pids.is_empty()` short-circuit, `!captured_pids.is_empty()`, and the three peak assertions are grep-confirmed preserved; `command.rs`, the #A/#B guardrails, and the sibling test bodies are untouched; the only surviving `.unwrap_or(0)` is the benign `unique_tag` nanos fallback. Windows behavior (a non-terminating CIM error now exits non-zero → the reap `== 0` checks can no longer pass on a query error) proven at the rebound 20-53.</acceptance_criteria>
  <done>Every host-side reap-observer PowerShell script fails closed on a CIM/PowerShell error — a non-terminating CIM error is now terminating and exits `powershell.exe` non-zero, caught by the preserved Rust-layer `assert!(out.status.success())`, so a post-close query error can no longer satisfy a reap assertion without evidence — with the 20-57 Rust-layer hardening and every panel-confirmed-sound assertion preserved verbatim, as a task commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| containment proof ← reap-observer PowerShell layer | The reap assertion is only genuine if BOTH layers fail closed; hardening only the Rust layer (20-57) leaves the query script able to exit 0 on a non-terminating CIM error, so `@(...).Count == '0'` / an empty PID stream passes the Rust checks and satisfies `wait_until(... == 0)` without evidence — a statically-provable false-green one layer below the Rust hardening |
| reap-property integrity ← every observer script | The PowerShell-layer fail-open shape is shared by all three `-Command` scripts; hardening fewer than all three, or with a single flag, leaves a residual exit-0-with-error-record path a re-audit can surface |
| assertion semantics ← script hardening | Hardening the query script must change only HOW a CIM error is handled (escalate to a terminating non-zero exit), never WHAT the test asserts, and must not weaken the preserved 20-57 Rust layer |
| candidate identity ← Windows-only source | The fix cannot be compiled off Windows; a compile break or a behavior gap is invisible until the msvc re-dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-60-01 | Spoofing | residual PowerShell-layer reap-observer false-green (the 20-59 finding) | high | mitigate | Each of the three `-Command` scripts escalates a CIM error to a terminating non-zero exit via `$ErrorActionPreference='Stop'` + `-ErrorAction Stop` on every `Get-CimInstance` + a leading `trap { exit 1 }`, so a non-terminating CIM error can no longer exit 0 with `@(...).Count == '0'` / an empty PID stream — the preserved Rust `assert!(out.status.success())` fires and the reap `wait_until(... == 0)` cannot pass on a query error. |
| T-20-60-02 | Tampering | a residual exit-0-with-error-record path in any one script | medium | mitigate | Belt-and-braces defense-in-depth: all THREE mechanisms are applied to all THREE scripts (not a single flag on a subset); a full-file sweep confirms four `-ErrorAction Stop` over four `Get-CimInstance`, three `$ErrorActionPreference='Stop'`, three `trap { exit 1 }`, so no observer of the class is left with an exit-0-on-error path. |
| T-20-60-03 | Tampering | weakening the preserved 20-57 Rust layer or changing WHAT is asserted under cover of "hardening" | high | mitigate | Only the three `-Command` script strings and their doc comments change; the Rust-layer `out.status.success()` ×3 + panicking parse, the captured-PID reap logic, `!captured_pids.is_empty()`, the peak/cap/below-attempts/clean-exit assertions, the `if pids.is_empty()` short-circuit, and the sibling tests are preserved verbatim (grep-confirmed), so the fix cannot silently weaken a containment check. |
| T-20-60-04 | Denial of Service | Codex post-close PID-reuse race | low | accept | Recycling a captured PID to a concurrent `choice.exe` within the 30s window can only make the test FAIL (fail-closed), never false-green; it is carried as a 20-53-observable native watch-item and NOT expanded in scope here. |
| T-20-60-05 | Tampering | undetectable Windows-only compile break / behavior gap | medium | accept | The file is `#![cfg(windows)]` and cannot be compiled off Windows; risk is bounded by scope + fmt + grep and the fail-closed behavior (a non-terminating CIM error now exits non-zero) is proven at the rebound 20-53 msvc re-dispatch (CONTEXT D5/D7). No pre-dispatch environment compiles the fix. |
</threat_model>

<verification>
The task first passes its Mac construction gate (scope + `vx rustfmt --edition 2024 --check` + grep) as a provisional task commit. After the task commit exists and the checkout is clean, run the sanctioned pre-dispatch macOS compile re-confirmation and the committed-HEAD Hetzner no-regression gates (a construction commit is not behavioral evidence):
<automated>cargo check -p wcore-sandbox --features live-docker --tests</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-60-clippy "$(pwd -P)" clippy -p wcore-sandbox --all-targets --all-features -- -D warnings</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-60-aggregate "$(pwd -P)" nextest run --workspace --profile ci --no-fail-fast</automated>
The macOS `cargo check` re-confirms the macOS-compiled wcore-sandbox surface still compiles — it does NOT compile the `#![cfg(windows)]` containment test. The Hetzner clippy + aggregate are a NO-REGRESSION floor: because the fix is Windows-only test code, the Linux aggregate `nextest --profile ci --no-fail-fast` MUST be exactly 11509/0/48 (a different count is a Linux regression that blocks the sequence — the honesty gate) and this aggregate runs WITH `--locked` because 20-60 changes no manifest (the lock is unchanged; 20-61 confirms zero lock delta). Record the exact committed `source_sha`/tree, the byte-identity check of the file vs `8a1d2d84`, the macOS compile receipt, the Hetzner clippy + aggregate receipts, changed paths, and the explicit deferral of ALL Windows compile/behavior (the fail-closed PowerShell scripts) to the rebound 20-53 msvc re-dispatch in `20-60-SUMMARY.md`. No native, aggregate-seal, or requirement claim is made.
</verification>

<success_criteria>
- All three host-side reap-observer PowerShell scripts fail CLOSED on a CIM/PowerShell error: each carries `$ErrorActionPreference='Stop'`, `-ErrorAction Stop` on every `Get-CimInstance` (four total), and a leading `trap { exit 1 }`, so a non-terminating CIM error becomes terminating → `powershell.exe` exits non-zero → the preserved Rust-layer `assert!(out.status.success())` fires and a post-close query failure can no longer satisfy a reap `wait_until(... == 0)` without evidence (r7).
- The fix is comprehensive and belt-and-braces (all three mechanisms on all three scripts; full-file sweep leaves no residual exit-0-on-error path), the 20-57 Rust-layer hardening and every panel-confirmed-sound assertion are preserved verbatim — the captured-PID reap logic, `!captured_pids.is_empty()`, the peak/cap/below-attempts/clean-exit assertions, the empty-set short-circuit, and the sibling tests; `command.rs` and the #A/#B guardrails are untouched (r7).
- Sanctioned macOS `cargo check --tests` is green, Hetzner clippy `-D warnings` is clean, and the aggregate is 11509/0/48 with no Linux regression (r4); the summary records the exact new candidate `source_sha`/tree and defers every hardware runtime claim (and the fail-closed PID-reuse watch-item) to the rebound 20-53.
</success_criteria>

## Artifacts this phase produces
- The comprehensive PowerShell-layer observer-hardening fix (all three `-Command` scripts fail closed on a CIM error) as one reviewable further-repaired successor delta over the 20-59-blocked `8a1d2d84`, scoped to the single finding file.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-60-SUMMARY.md` using the standard GSD summary template. Record the exact new candidate `source_sha`/tree, the byte-identity check of the touched file vs `8a1d2d84`, the macOS compile-check receipt, the Hetzner clippy + aggregate receipts, changed paths, the chain decision (author 20-60…20-62, rebind pending 20-53…20-56 onto the 20-61 seal, do not mutate sealed 20-43/44/45/50/51/52/57/58/59), the confirmation that ALL THREE PowerShell scripts now fail closed (ErrorActionPreference Stop + -ErrorAction Stop on every Get-CimInstance + trap) while the 20-57 Rust-layer hardening and every panel-confirmed-sound assertion are preserved verbatim, and the explicit deferral of all Windows compile/behavior (and the fail-closed PID-reuse watch-item) to the rebound 20-53. Complete no Phase 20 requirement.
</output>
</content>

---
phase: 20-transactional-delegated-mutation
plan: "63"
type: execute
wave: 62
depends_on:
  - "20-53"
files_modified:
  - crates/wcore-sandbox/tests/live_fs_acl.rs
autonomous: true
gap_closure: true
requirements:
  - REQ-native-r5
  - REQ-native-r2
  - REQ-native-r4
must_haves:
  truths:
    - "CLASS A (test-detector, hardware-proven at 20-53 SEANDESKTOP): `has_appcontainer_ace` (live_fs_acl.rs:67-69) matches ONLY the raw AppContainer package SID `s-1-15-2-`; the `|| acl.contains(\"wcore-\")` disjunct is removed. That substring was ALWAYS true because `icacls <dir>` echoes the seed-dir path `wcore-r61-<pid>-<tag>` (seed_file line 51) in every output line, so every ABSENT/REVOKE check built on the detector could never observe a cleared DACL. The detector now mirrors the belt-and-braces present/absent pattern already correct at lines 240/297/613 (`!acl.contains(\"S-1-15-2-\") && !acl_lower.contains(\"wcoresandbox\")`). Present-checks still pass: while a grant is live, icacls renders the ephemeral package SID as raw `S-1-15-2-…` (PROFILE_PREFIX=\"WCore\", acl_lease.rs:62). Fixes `granted_path_is_readable_then_revoked` (revoke-wait line 229-230), `one_exit_does_not_remove_another_execution_grant` (revoke-wait line 341-342), and `unrelated_acl_survives_exact_sid_cleanup` (assert line 562) (REQ-native-r2)."
    - "CLASS C (test-hold artifact, hardware-proven at 20-53 SEANDESKTOP): the delay primitive in `type_and_hold` (line 118, hold at 131-133) and `echo_temp_and_hold` (line 137, hold at 142-143) is `waitfor.exe /t {seconds} wlhold >nul 2>&1 & exit /b 0`, NOT `choice.exe /T {seconds} /D Y`. `choice.exe` exits instantly (~26ms) under the sandbox's NULL stdin, so the intended present-during-run hold never held; `waitfor` is stdin/console/network-independent and verified present on the box. It is NOT `ping 127.0.0.1` (the AppContainer is built with zero network capability, process.rs:544-545, so loopback is unreachable). The now-false in-source comments at lines 119-130 and 138-141 (claiming `choice` tolerates the sandbox null stdin) are corrected to describe the `waitfor` hold and the null-stdin reality (REQ-native-r5)."
    - "The two fixes are test-only, confined to `crates/wcore-sandbox/tests/live_fs_acl.rs`; no production source changes and no Linux/macOS regression (both fixes are inside a `#![cfg(windows)]` module, so they are inert off Windows). Behavior is proven on real hardware at 20-67 (SEANDESKTOP direct nextest of the whole `live_fs_acl` suite), never on the Mac (REQ-native-r4)."
  artifacts:
    - path: crates/wcore-sandbox/tests/live_fs_acl.rs
      provides: "Class A SID-only detector + Class C waitfor stdin-free hold, so the live_fs_acl grant/revoke acceptance suite can correctly observe a working AppContainer sandbox on hardware"
  key_links:
    - from: crates/wcore-sandbox/tests/live_fs_acl.rs
      to: .planning/phases/20-transactional-delegated-mutation/20-53-SUMMARY.md
      via: "closes the CLASS A + CLASS C hardware-enumerated failures from the 20-53 SEANDESKTOP native run recorded in 20-VERIFICATION.md"
      pattern: "gap_closure"
---

<objective>
Close CLASS A (test-detector substring collision) and CLASS C (test-hold artifact) from the 20-53 SEANDESKTOP native run (20-VERIFICATION.md). Both are hardware-proven TEST bugs — the tests could not correctly observe a WORKING sandbox — confined to `crates/wcore-sandbox/tests/live_fs_acl.rs`.

Purpose: Class A made every absent/revoke assertion in the live ACL suite unfalsifiable (the `wcore-` substring the detector matched is echoed by `icacls` from the seed-dir name), and Class C made every "present during the run" hold vacuous (`choice.exe` exits instantly under NULL stdin). Neither is a Phase-20 regression and neither touches production; production revoke works (7 leftover seed dirs on the box carried ZERO `S-1-15-2-` ACEs, raw grant/revoke measured ~20ms). Fixing both lets the suite genuinely gate on the AppContainer ACL boundary.
Output: A test-only, `#![cfg(windows)]`-confined repair of the detector and the hold primitive, committed to the repair branch; real behavior proven at 20-67 on SEANDESKTOP.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-VERIFICATION.md
@crates/wcore-sandbox/tests/live_fs_acl.rs
</context>

<execution_rules>
- Test-only. Touch ONLY `crates/wcore-sandbox/tests/live_fs_acl.rs`. No production source, no other test file (Class D lives in `hard_process_containment_windows.rs` and is a DIFFERENT plan; do not conflate — that file's own detached `choice` idlers hold correctly and must NOT be changed here).
- NO cargo/clippy/nextest on this Mac (CONTEXT D5). `cargo fmt` is permitted. The file is `#![cfg(windows)]`, so a Mac compile check compiles it to nothing — its real compile AND behavior are proven at 20-67 on SEANDESKTOP, never here.
- Use `/usr/bin/git` for any staging/commit; never stage the AGENTS.md ijfw churn.
- Preserve the belt-and-braces present/absent assertions already correct at lines 240/297/613; the detector fix must make `has_appcontainer_ace` consistent with them, not weaken them.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: CLASS A — make has_appcontainer_ace match only the raw package SID</name>
  <files>crates/wcore-sandbox/tests/live_fs_acl.rs</files>
  <read_first>crates/wcore-sandbox/tests/live_fs_acl.rs, .planning/phases/20-transactional-delegated-mutation/20-VERIFICATION.md</read_first>
  <action>In `has_appcontainer_ace` (lines 67-69), change the detector so it returns true only when the lowercased `icacls` output contains the raw AppContainer package-SID prefix `s-1-15-2-`. Remove the second disjunct that also returns true on the profile-name substring, because `icacls <path>` echoes the seed-dir path (`wcore-r61-<pid>-<tag>`, produced by `seed_file` at line 51) in every line of its output, making that disjunct unconditionally true and every absent/revoke check unfalsifiable. Keep the existing `to_ascii_lowercase()` normalization so the raw-SID compare stays case-insensitive. This aligns the detector with the belt-and-braces present/absent assertions already correct at lines 240, 297, and 613 (which negate both `S-1-15-2-` and the resolved `wcoresandbox` profile rendering). Do NOT alter those three assertion sites. Per 20-VERIFICATION.md CLASS A; while a grant is live the ephemeral package SID renders as raw `S-1-15-2-…` (PROFILE_PREFIX=\"WCore\", acl_lease.rs:62), so the present-checks at lines 206-207 and 318-319 and 366 still pass.</action>
  <verify>
    <automated>cargo fmt --all --check</automated>
    <automated>awk '/fn has_appcontainer_ace/{f=1} f{print} /^}/{if(f)exit}' crates/wcore-sandbox/tests/live_fs_acl.rs | grep -c 's-1-15-2-'</automated>
    <automated>test "$(awk '/fn has_appcontainer_ace/{f=1} f{print} /^}/{if(f)exit}' crates/wcore-sandbox/tests/live_fs_acl.rs | grep -c 'contains')" = "1"</automated>
  </verify>
  <done>`has_appcontainer_ace` contains exactly one `contains(...)` call, matching only `s-1-15-2-`; the profile-name fallback disjunct is gone; fmt is clean. Real revoke-detection behavior proven at 20-67.</done>
</task>

<task type="auto">
  <name>Task 2: CLASS C — replace the choice hold with a stdin-free waitfor hold and correct the comments</name>
  <files>crates/wcore-sandbox/tests/live_fs_acl.rs</files>
  <read_first>crates/wcore-sandbox/tests/live_fs_acl.rs, .planning/phases/20-transactional-delegated-mutation/20-VERIFICATION.md</read_first>
  <behavior>
    - `type_and_hold(file, seconds)` still proves the granted read first (`type "<file>"`), and ONLY on its success holds the process alive for ~`seconds`, then forces a deterministic exit 0 — but the hold no longer collapses instantly under the sandbox's NULL stdin.
    - `echo_temp_and_hold(seconds)` echoes `%TEMP%` then holds ~`seconds` and exits 0.
    - On real hardware the held child is observable as alive during the run window (what the callers' present-during-run waits depend on), instead of finishing in ~26ms as `choice.exe` did.
  </behavior>
  <action>In `type_and_hold` (line 118) and `echo_temp_and_hold` (line 137), replace the `%SystemRoot%\\System32\\choice.exe /T {seconds} /D Y >nul` delay with a `waitfor.exe`-based hold of the form `waitfor.exe /t {seconds} wlhold >nul 2>&1 & exit /b 0` — `waitfor` waits for a signal that never arrives and times out after `{seconds}`, is independent of stdin/console/network, and is present on the box. Keep the surrounding structure intact: in `type_and_hold` the hold must remain gated behind the granted read's success (`&&`) so an exit-0 assertion still genuinely gates on the read; in both helpers keep the trailing `exit /b 0` so the script's exit code reflects the granted read, not the hold primitive's residual ERRORLEVEL. Do NOT use `ping 127.0.0.1` — the AppContainer is built with zero network capability (process.rs:544-545) so loopback is unreachable. Rewrite the now-false comments at lines 119-130 and 138-141: state that `choice.exe` exits instantly under the sandbox's NULL stdin (so it never held), that `waitfor` is the stdin/console/network-independent hold, and that `exit /b 0` sets the deterministic success exit code. Per 20-VERIFICATION.md CLASS C. Do NOT touch `hard_process_containment_windows.rs` — its own detached `choice` idlers inherit a pipe and hold correctly (that is CLASS D, a separate plan).</action>
  <verify>
    <automated>cargo fmt --all --check</automated>
    <automated>test "$(grep -c 'waitfor.exe /t' crates/wcore-sandbox/tests/live_fs_acl.rs)" = "2"</automated>
    <automated>grep -nE 'waitfor.exe /t [0-9{].*wlhold' crates/wcore-sandbox/tests/live_fs_acl.rs</automated>
  </verify>
  <done>Both hold helpers use `waitfor.exe /t {seconds} wlhold` and force `exit /b 0`; no `choice.exe` remains in either helper; the comments describe the null-stdin reality and the waitfor hold; fmt clean. Present-during-run behavior proven at 20-67.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test observation ← DACL detector | The absent/revoke assertions must observe the real DACL state; a substring that is always-true (Class A) makes the boundary check unfalsifiable |
| present-during-run assertion ← hold primitive | The "grant is live while the child runs" checks depend on a child that actually stays alive; a hold that exits instantly (Class C) makes those checks vacuous |
| test-only change ← production untouched | The repair must not alter the AppContainer ACL production code, only the observing test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-63-01 | Spoofing | always-true detector reports a cleared DACL | high | mitigate | Detector matches only the raw package SID `s-1-15-2-`, mirroring the correct belt-and-braces assertions at 240/297/613; the profile-name substring that `icacls` echoes is removed. |
| T-20-63-02 | Repudiation | vacuous hold passes a present-during-run check without a live child | medium | mitigate | `waitfor` provides a real stdin/console/network-independent hold; behavior is proven on hardware at 20-67, not asserted from source. |
| T-20-63-03 | Tampering | a test-file edit silently changing production ACL behavior | low | accept | Change is confined to `tests/live_fs_acl.rs` inside `#![cfg(windows)]`; no production path is touched; verified by files_modified scope. |
</threat_model>

<verification>
Local gates: `cargo fmt --all --check` clean; `has_appcontainer_ace` has exactly one `contains` call matching `s-1-15-2-`; both hold helpers contain `waitfor.exe /t` and no `choice.exe`. The authoritative behavioral proof (the three Class A tests and the Class C-dependent present-during-run tests green) is deferred to 20-67 on SEANDESKTOP (`cargo nextest run -p wcore-sandbox --test live_fs_acl --run-ignored all`), never on the Mac. Record the exact detector and hold edits, the corrected-comment lines, and the committed SHA in 20-63-SUMMARY.md. No production change; no requirement completed here (closure is claimed only after 20-67 hardware green).
</verification>

<success_criteria>
- `has_appcontainer_ace` matches only `s-1-15-2-` (Class A closed at the source; three revoke/absent tests can now observe a cleared DACL) (REQ-native-r2).
- `type_and_hold` and `echo_temp_and_hold` use a stdin-free `waitfor` hold with a deterministic `exit /b 0`, comments corrected (Class C closed at the source) (REQ-native-r5).
- Change is test-only and `#![cfg(windows)]`-confined; no Linux/macOS regression; fmt clean; committed to the repair branch for the SEANDESKTOP verify at 20-67 (REQ-native-r4).
</success_criteria>

## Artifacts this phase produces
- Class A SID-only detector + Class C waitfor hold in `crates/wcore-sandbox/tests/live_fs_acl.rs`, committed to the repair branch.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-63-SUMMARY.md` using the standard GSD summary template. Record the detector edit (matches only `s-1-15-2-`), the hold edit (`waitfor.exe /t {seconds} wlhold` + `exit /b 0` in both helpers), the corrected comment ranges, the committed SHA, and that behavioral proof is deferred to 20-67 (SEANDESKTOP). Complete no Phase 20 requirement (closure claimed only after 20-67 hardware green).
</output>
</content>

---
phase: 20-transactional-delegated-mutation
plan: "33"
type: execute
wave: 32
depends_on:
  - "20-32"
files_modified:
  - .planning/phases/20-transactional-delegated-mutation/20-33-INDEPENDENT-REVIEW.md
autonomous: true
requirements:
  - REQ-native-r13
  - REQ-native-r12
must_haves:
  truths:
    - "A fresh non-author reviewer prosecutes the exact sealed repaired-successor at every severity across code review, phase validation, and ASVS Level 2 security, AND reviews the 20-32 native evidence — native_macos and native_windows are PASS checks, NOT deferred (the review the audit demanded: no deferred native) (REQ-native-r13/r12)."
    - "The review is recorded as a schema-validated wayland-core.phase20-independent-review.v1 artifact under profile f20-native-16 (deferred empty); every claimed reviewer emits its own schema-validated artifact — no prose-only review counts toward the PASS (REQ-native-r13)."
    - "Only a zero-finding PASS with native NOT deferred admits the terminal preparation (20-34); any finding at any severity stops the sequence for a further repaired successor and fresh review."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-33-INDEPENDENT-REVIEW.md
      provides: "Fresh non-author all-severity review of the sealed successor WITH native evidence, native not deferred, schema-validated per reviewer"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-33-INDEPENDENT-REVIEW.md
      to: .planning/phases/20-transactional-delegated-mutation/20-32-SUMMARY.md
      via: "the review consumes the retained dual-OS native evidence as the native_macos/native_windows PASS basis"
      pattern: "native_windows"
---

<objective>
Perform the fresh 20-16-role independent review the audit required, now against the repaired successor: a non-author reviewer prosecutes the exact sealed successor at every severity AND reviews the real native evidence from 20-32 — native NOT deferred — recording a schema-validated artifact per claimed reviewer.

Purpose: The original 20-16 passed CLEAN but with native deferred by design, and 20-26 could not run because 20-25 went RED. This review closes the gap on the repaired successor: it reviews the actual Windows+macOS native evidence (including the two fixed RED causes), not a promise, and every claimed reviewer leaves an on-disk schema-validated artifact.
Output: One (or more, per claimed reviewer) schema-validated review artifact over the sealed successor SHA with native as PASS checks; a zero-finding PASS admits 20-34.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
@/Users/seandonahoe/.agents/skills/gsd-validate-phase/SKILL.md
@/Users/seandonahoe/.agents/skills/gsd-secure-phase/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-30-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-31-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-32-SUMMARY.md
@.planning/scripts/verify-review-result.mjs
@.planning/scripts/verify-review-pair.sh
</context>

<admission>
- Require a clean isolated checkout at the exact sealed successor SHA (from `20-30-SUMMARY.md`); verify its tree. The 20-32 native evidence must be present and green for BOTH platforms; if native is missing or RED, do NOT run a deferred-native fallback — fail closed and route to a further repaired successor.
- Capture the source authority: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-33)"`, `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-review-base-20-33)"`, and `node .planning/scripts/task-base-authority.mjs capture "$(git rev-parse --git-path gsd-reviewed-source-20-33)" "$source" "$(git rev-parse "$source^{tree}")"`.
- The executor MUST be a fresh non-author executor distinct from the 20-29 author and from the 20-31 cross-auditor. Each claimed reviewer records its OWN schema-validated artifact.
</admission>

<tasks>

<task type="auto">
  <name>Task 1: Fresh non-author all-severity review with native NOT deferred</name>
  <files>.planning/phases/20-transactional-delegated-mutation/20-33-INDEPENDENT-REVIEW.md</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-30-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-31-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-32-SUMMARY.md, crates/wcore-sandbox/src/directory_authority.rs, crates/wcore-sandbox/src/directory_authority_file.rs, crates/wcore-sandbox/src/directory_authority_tests.rs, crates/wcore-sandbox/src/directory_authority_windows.rs, .github/workflows/nightly-windows-soak.yml, scripts/f20-native-windows-proof.ps1, scripts/f20-native-macos-proof.sh, scripts/f20-native-uat-proof.mjs</read_first>
  <behavior>
    - Prosecute the exact sealed successor at every severity: unix rename_into isolation (handle-relative, source identity re-proven, no-replace fidelity, decoy-immune per the macOS acceptance test), the behavior-preserving atomic_write_child unification, the file variant non-dead / directory variant pub reachability, unchanged Windows delegation, and the candidate-workflow safe.directory guard scoped to the candidate jobs only.
    - Review the 20-32 native evidence directly: confirm Windows ran on self-hosted msvc and macOS on the freshly-registered ephemeral runner, both green, with complete ordered markers bound to the exact sealed successor SHA, re-proving the AppContainer read/isolation + Job-Object containment AND the two fixed 20-25 RED causes (macOS lib compiles + rename acceptance green; Windows setup clears). Record `native_macos` and `native_windows` as PASS — not deferred.
    - Execute `gsd-validate-phase` against every Phase 20 must-have and `gsd-secure-phase` ASVS Level 2 with `security_block_on: low`.
    - Every BLOCKER/CRITICAL/HIGH/MEDIUM/LOW finding is disqualifying; do not repair source.
    - Record one schema-validated JSON object matching `wayland-core.phase20-independent-review.v1`, profile `f20-native-16` (checks all_severity/asvs_level_2/code_review/native_macos/native_windows/phase_validation all PASS; deferred empty). Distinct stable source-executor/reviewer identities; never embed the review commit's own SHA.
  </behavior>
  <action>Inspect the complete sealed successor source, tests, harness, and the 20-32 native evidence without changing source. Apply code-review + `gsd-validate-phase` + `gsd-secure-phase` (ASVS Level 2) as one blocking exact-source review, and additionally verify the native evidence is genuine, exact-SHA-bound, on the correct runners, and green for both platforms. Write one combined schema-validated JSON artifact matching `.planning/scripts/verify-review-result.mjs` profile `f20-native-16` with native as PASS checks and deferred empty. If additional reviewers are claimed, EACH emits its own schema-validated artifact (a sibling `20-33-INDEPENDENT-REVIEW-<reviewer>.md`); a claimed reviewer without an artifact does not count. PASS only when every truth (including native) has credible evidence and zero findings remain; otherwise record FAIL and stop the sequence.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-33)" .planning/phases/20-transactional-delegated-mutation/20-33-INDEPENDENT-REVIEW.md</automated>
    <automated>base="$(node .planning/scripts/task-base-authority.mjs read "$(git rev-parse --git-path gsd-task-base-20-33)" | sed -n '1p')"; git diff --check "$base"..HEAD && git diff --cached --check && git diff --check</automated>
    <automated>source="$(node .planning/scripts/task-base-authority.mjs read "$(git rev-parse --git-path gsd-reviewed-source-20-33)" | sed -n '1p')"; review="$(git rev-parse HEAD)"; node .planning/scripts/verify-review-result.mjs "$review" .planning/phases/20-transactional-delegated-mutation/20-33-INDEPENDENT-REVIEW.md "$source" "$(git rev-parse "${source}^{tree}")" f20-native-16</automated>
  </verify>
  <acceptance_criteria>The review is fresh, non-author, exact-sealed-SHA, complete at every severity, schema-validated per claimed reviewer, with native as PASS checks (NOT deferred). PASS requires zero findings; FAIL creates no qualification and does not admit 20-34.</acceptance_criteria>
  <done>The schema-validated review artifact(s) with native-not-deferred exist for post-task pair verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review PASS ← native evidence | The review must consume real native evidence; a deferred-native "PASS" would repeat the audited gap |
| PASS ← reviewer artifact | Only a schema-validated per-reviewer artifact counts (no prose-only reviews) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-33-01 | Repudiation | deferred-native review | high | mitigate | Profile `f20-native-16` requires native_macos/native_windows PASS with deferred empty; a review that cannot substantiate native from the 20-32 evidence fails closed. |
| T-20-33-02 | Spoofing | native evidence review | high | mitigate | The reviewer independently confirms the native evidence is exact-SHA-bound, on the correct runners, green — not accepting the summary's word. |
</threat_model>

<verification>
Derive `source`, `review_base`, `review`; run `verify-review-pair.sh` over the sealed source set and `verify-review-result.mjs` profile `f20-native-16`. Require a zero-finding PASS with native NOT deferred. Record the `(source_sha, source_tree, review_base_sha, review_sha)` tuple and each reviewer artifact identity in `20-33-SUMMARY.md`. No Cargo runs on the Mac.
</verification>

<success_criteria>
- A distinct non-author reviewer reviewed the exact sealed successor AND the real native evidence at every severity with zero findings; native is PASS, NOT deferred (r13/r12).
- Every claimed reviewer emitted a schema-validated artifact; no prose-only review counted (r13).
- Only a zero-finding native-inclusive PASS admits the terminal preparation (20-34).
</success_criteria>

## Artifacts this phase produces
- `.planning/phases/20-transactional-delegated-mutation/20-33-INDEPENDENT-REVIEW.md` (+ any per-reviewer siblings) — fresh native-inclusive schema-validated review.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-33-SUMMARY.md` with executor identity, sealed source SHA/tree, review-base/review SHAs, per-reviewer artifact identities, all-severity + native disposition, and explicit admission or rejection of the terminal preparation 20-34. It does not directly admit the terminal 20-35.
</output>

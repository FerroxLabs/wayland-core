---
phase: 20-transactional-delegated-mutation
plan: "45"
type: execute
wave: 44
depends_on:
  - "20-44"
files_modified:
  - .planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md
autonomous: true
requirements:
  - REQ-native-r12
  - REQ-native-r13
must_haves:
  truths:
    - "A FOUR-WAY independent cross-audit panel — Codex 5.6 Sol (`codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check \"<prompt>\"`), Gemini 3.1 Pro (`gemini -p \"<prompt>\" -m gemini-3.1-pro-preview -o text --approval-mode plan`), and Kimi K3 (`/Users/seandonahoe/.kimi-code/bin/kimi -p \"<prompt>\" --output-format text`, absolute path) run in parallel, PLUS an internal Claude non-author adversarial reviewer prompted to REFUTE (default-refuted-if-uncertain) — cross-audits the exact SEALED third-repaired-successor `source_sha 3f839309` (tree `3092475b`, recorded by 20-44) at every severity across the 20-43 repair delta: the PRODUCTION #A verbatim-path/env-transport probe fix in wcore-swarm, the PRODUCTION #B cmd.exe /c|/k payload-quoting fix in wcore-sandbox, the Job-Object counting-scope fix, and the lib-test compile-debt imports — BEFORE any Sean-gated native run is re-spent (REQ-native-r12 pre-native gate)."
    - "Each of the four auditors emits its OWN on-disk schema-validated artifact: Codex Sol → `f20-native-crossaudit.codex-sol`, Gemini Pro → `f20-native-crossaudit.gemini-pro`, Kimi K3 → `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial reviewer → `wayland-core.phase20-independent-review.v1`; no prose-only vote counts toward the PASS — sustaining the 20-24/20-31/20-38 attestation discipline (REQ-native-r13)."
    - "The panel adversarially prosecutes the two isolation guardrails: (a) #A did NOT silently defeat the DispatchAdmission budget (no catch-and-default-to-unlimited), and (b) #B is a quoting-layer change only (argv discipline kept; is_unc_or_device_path, the Low-IL token, the Job-Object limits, and the ACL lease untouched; no shell-string interpolation). It also verifies the Kimi watch-items are baked into the 20-46 re-dispatch checks. native_macos/native_windows are the ONLY deferred checks (proven at 20-46); code review, all-severity, evidence integrity, and integration authority are prosecuted here. ALL FOUR auditors MUST PASS with zero findings to admit 20-46; any single finding from any one auditor (any severity except the deferred native_macos/native_windows) stops the sequence for a further repaired successor (REQ-native-r12/r13)."
    - "Fail-closed: if ANY external auditor (Codex Sol / Gemini Pro / Kimi K3) is unreachable or its output cannot be schema-validated, the gate records an explicit incomplete disposition and BLOCKS rather than emitting a false-green PASS from a partial panel. The internal Claude adversarial pass is always reachable but is one of the four required votes, never a substitute for a missing external auditor (mirrors CONTEXT D5 fail-safe)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md
      provides: "Four per-reviewer schema-validated cross-audits of the sealed third-repaired-successor (3f839309, tree 3092475b) repair delta: the Codex Sol / Gemini Pro / Kimi K3 f20-native-crossaudit artifacts plus the internal Claude adversarial wayland-core.phase20-independent-review.v1 artifact"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md
      to: .planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md
      via: "source/review-base/review authority tuple over the sealed third-repaired-successor SHA 3f839309 (tree 3092475b)"
      pattern: "source_sha"
---

<objective>
Cross-audit the exact sealed third-repaired-successor (`source_sha 3f839309`, tree `3092475b`) before re-spending a Sean-gated native run: a FOUR-WAY independent panel — Codex 5.6 Sol, Gemini 3.1 Pro, and Kimi K3 (three external CLIs run in parallel), plus an internal Claude non-author adversarial reviewer — prosecutes the 20-43 repair delta at every severity, and each auditor records its OWN schema-validated artifact. ALL FOUR MUST PASS; native explicitly deferred to 20-46.

Purpose: The gate sequence puts an adversarial cross-audit before the native proof so a broken candidate does not consume the scarce native run; and R13 requires per-reviewer schema-validated artifacts, not prose. This is the pre-native attestation gate for the third repaired successor. The 20-43 delta changes PRODUCTION code in two security-relevant places (the DispatchAdmission capacity probe in wcore-swarm, and the AppContainer cmd.exe command-line construction in wcore-sandbox), so the panel focuses adversarially on whether #A preserved the admission budget (no catch-to-unlimited) and whether #B stayed a pure quoting-layer change (argv discipline; no relaxation of is_unc_or_device_path, the Low-IL token, the Job-Object limits, or the ACL lease). A four-independent-auditor panel raises the cost of a shared blind spot: one auditor's miss is caught by the other three, and no single reviewer can green the gate alone.
Output: Four schema-validated artifacts over the sealed successor SHA — Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1`. Zero findings across ALL FOUR admits the native-proof gate. If ANY external auditor is unreachable or its output cannot be schema-validated, an explicit incomplete disposition (no false green from a partial panel).
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md
@.planning/phases/20-transactional-delegated-mutation/20-43-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-31-CROSS-AUDIT.md
</context>

<execution_rules>
- The cross-audit is a FOUR-WAY panel of independent (non-author) reviewers; NONE is the plan author. Three are external CLIs, invoked verbatim and run in PARALLEL:
  1. Codex 5.6 Sol — `codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check "<prompt>"`
  2. Gemini 3.1 Pro — `gemini -p "<prompt>" -m gemini-3.1-pro-preview -o text` (add `--approval-mode plan` for a read-only lock)
  3. Kimi K3 — `/Users/seandonahoe/.kimi-code/bin/kimi -p "<prompt>" --output-format text` (ABSOLUTE path — the Bash shell PATH omits it; see the [[cross-auditor-kimi-k3]] project memory)
  The fourth is an internal Claude non-author adversarial subagent, prompted to REFUTE the candidate (default-refuted-if-uncertain).
- Every one of the four auditors MUST leave its OWN on-disk schema-validated artifact. The three external legs emit `f20-native-crossaudit` variants keyed by auditor id — Codex Sol → `f20-native-crossaudit.codex-sol`, Gemini Pro → `f20-native-crossaudit.gemini-pro`, Kimi K3 → `f20-native-crossaudit.kimi-k3`; the internal Claude adversarial leg emits `wayland-core.phase20-independent-review.v1`. A prose-only vote does not count toward PASS (REQ-native-r13).
- Scope every prompt to the EXACT sealed successor SHA/tree recorded by 20-44 — `source_sha 3f839309` (tree `3092475b`) — and the 20-43 repair delta (the review base is that tuple; do not review the branch tip if it has advanced).
- native_macos and native_windows are the ONLY checks that may be deferred (they are proven at 20-46). All other checks — code review of the #A/#B production changes, all-severity findings, evidence integrity, integration authority — are prosecuted here by every auditor.
- DISPOSITION — ALL FOUR MUST PASS: only a zero-finding PASS from all four auditors admits 20-46. Any single finding from any one auditor (any severity except the deferred native_macos/native_windows) BLOCKS and routes to a further repaired successor.
- FAIL-CLOSED: if ANY external auditor (Codex Sol / Gemini Pro / Kimi K3) cannot be reached or its output cannot be schema-validated, record an explicit incomplete disposition and BLOCK — do not synthesize a PASS from the remaining reviewers or from prose (CONTEXT D5 fail-safe). The internal Claude adversarial pass is always reachable but is one of the four required votes, not a substitute for a missing external auditor. Do not modify any source/test/workflow file; this plan writes only the cross-audit artifact.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Four-way pre-native cross-audit panel of the sealed successor (Codex Sol + Gemini Pro + Kimi K3 + Claude adversarial), all-must-pass, fail-closed</name>
  <files>.planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-43-SUMMARY.md, .planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md, .planning/phases/20-transactional-delegated-mutation/20-31-CROSS-AUDIT.md</read_first>
  <behavior>
    - Dispatch the FOUR-WAY independent panel over the exact sealed third-repaired-successor SHA/tree (`source_sha 3f839309`, tree `3092475b`). Run the three external CLIs in parallel — Codex 5.6 Sol (`codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check`), Gemini 3.1 Pro (`gemini -m gemini-3.1-pro-preview -o text --approval-mode plan`), Kimi K3 (`/Users/seandonahoe/.kimi-code/bin/kimi --output-format text`, absolute path) — plus an internal Claude non-author adversarial subagent prompted to REFUTE (default-refuted-if-uncertain). Scope every prompt to the 20-43 repair delta: #A (wcore-swarm dunce::simplified de-verbatimization + WCORE_SWARM_PROBE_ROOT env transport, DispatchAdmission budget), #B (wcore-sandbox cmd.exe /c|/k payload quoting), the Job-Object counting-scope fix, and the lib-test compile-debt imports.
    - Every auditor prosecutes adversarially: does #A preserve the admission budget (NO error-swallow-to-unlimited)? Is #B a quoting-layer change only (argv discipline kept; is_unc_or_device_path, Low-IL token, Job-Object limits, ACL lease untouched; no shell-string interpolation)? Does the counting-scope fix keep every containment assertion? Are the Kimi watch-items (git ops with de-verbatimized swarm_root; tagged_cmd_count CIM visibility under NetworkService; the bash worker under AppContainer + no-Windows-Docker-fallback; worker test-exe DLL-load under Low-IL) explicitly carried as 20-46 re-dispatch checks rather than silently dropped?
    - Capture each auditor's findings as its OWN schema-validated artifact: Codex Sol → `f20-native-crossaudit.codex-sol`, Gemini Pro → `f20-native-crossaudit.gemini-pro`, Kimi K3 → `f20-native-crossaudit.kimi-k3`, internal Claude adversarial → `wayland-core.phase20-independent-review.v1`. native_macos/native_windows are deferred (empty/deferred), proven at 20-46.
    - Disposition — ALL FOUR MUST PASS: only a zero-finding PASS from all four admits 20-46; any single finding from any auditor blocks and routes to a further repaired successor.
    - Fail-closed: if ANY external auditor (Codex Sol / Gemini Pro / Kimi K3) is unreachable or its output fails schema validation, record an explicit incomplete disposition and block — no false-green PASS from a partial panel.
  </behavior>
  <action>Dispatch the four-way independent panel over the exact sealed successor SHA/tree recorded by 20-44 (`source_sha 3f839309`, tree `3092475b`): run the three external CLIs in parallel — Codex 5.6 Sol (`codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check "<prompt>"`), Gemini 3.1 Pro (`gemini -p "<prompt>" -m gemini-3.1-pro-preview -o text --approval-mode plan`), and Kimi K3 (`/Users/seandonahoe/.kimi-code/bin/kimi -p "<prompt>" --output-format text`) — plus an internal Claude non-author adversarial subagent prompted to REFUTE (default-refuted-if-uncertain), scoping each to the 20-43 repair delta and the two isolation guardrails (#A admission budget preserved; #B quoting-layer only). Record each auditor's findings as its own schema-validated artifact in `20-45-CROSS-AUDIT.md` — `f20-native-crossaudit.codex-sol`, `f20-native-crossaudit.gemini-pro`, `f20-native-crossaudit.kimi-k3`, and the internal Claude `wayland-core.phase20-independent-review.v1` (native_macos/native_windows deferred). Verify the Kimi watch-items are recorded as explicit 20-46 re-dispatch checks. ALL FOUR must PASS zero-finding to admit 20-46; any single finding blocks. If ANY external auditor is unreachable or its output fails schema validation, record an explicit incomplete disposition and BLOCK — do not synthesize a PASS. Implements REQ-native-r12/r13; write only the cross-audit artifact.</action>
  <verify>
    <automated>test -f .planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md</automated>
    <automated>grep -nE "f20-native-crossaudit\.codex-sol|f20-native-crossaudit\.gemini-pro|f20-native-crossaudit\.kimi-k3|phase20-independent-review|Codex 5\.6 Sol|Gemini 3\.1 Pro|Kimi K3|Claude adversarial|3f839309|source_sha|deferred" .planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md | head -40</automated>
  </verify>
  <acceptance_criteria>The cross-audit artifact exists and records FOUR per-reviewer results over the exact sealed SHA (3f839309): Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1`, with native_macos/native_windows deferred and the Kimi watch-items carried as 20-46 checks. A zero-finding PASS from ALL FOUR reviewers admits 20-46; any single finding from any auditor, or any external auditor unreachable/schema-invalid, blocks with an explicit incomplete disposition.</acceptance_criteria>
  <done>The sealed third-repaired successor has a four-way per-reviewer schema-validated pre-native cross-audit (Codex Sol + Gemini Pro + Kimi K3 + Claude adversarial), or an explicit fail-closed block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native run authorization ← four-way cross-audit PASS | The scarce Sean-gated native run is only re-spent after ALL FOUR panel auditors pass; a false-green here wastes the run |
| attestation integrity ← per-reviewer artifact | A PASS must be backed by an on-disk schema-validated artifact per each of the four auditors, not prose |
| panel independence ← external-auditor reachability | If ANY external auditor (Codex Sol / Gemini Pro / Kimi K3) is down, the audit must fail closed rather than pass on a partial panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-45-01 | Spoofing | prose-only or partial-panel PASS | high | mitigate | Each of the four auditors emits its own schema-validated artifact (Codex Sol `f20-native-crossaudit.codex-sol` + Gemini Pro `f20-native-crossaudit.gemini-pro` + Kimi K3 `f20-native-crossaudit.kimi-k3` + Claude adversarial `wayland-core.phase20-independent-review.v1`); a prose-only or missing artifact does not count, and PASS requires ALL FOUR. |
| T-20-45-02 | Repudiation | external auditor unreachable false-green | high | mitigate | Fail-closed: any unreachable or schema-invalid external-auditor output records an explicit incomplete disposition and blocks; no PASS is synthesized from the remaining reviewers (CONTEXT D5 fail-safe). |
| T-20-45-03 | Tampering | isolation-guardrail regression slipping through | high | mitigate | All four auditors adversarially prosecute #A (admission budget preserved) and #B (quoting-layer only; boundary primitives untouched) and require the Kimi watch-items be carried as 20-46 checks, so a boundary-weakening change cannot pass a four-independent-auditor panel silently. |
</threat_model>

<verification>
The cross-audit artifact `20-45-CROSS-AUDIT.md` determines acceptance: it must record FOUR per-reviewer results over the exact sealed successor SHA `3f839309` (tree `3092475b`) — Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1` — ALL zero-finding, with native_macos/native_windows deferred and the Kimi watch-items carried as explicit 20-46 re-dispatch checks. Record the sealed `source_sha`/tree, the four reviewer identities, the three external CLI invocations, each artifact's schema-validation status, and the disposition (ALL FOUR PASS admits 20-46; any single finding or any external auditor unreachable/invalid blocks with an explicit incomplete disposition) in `20-45-SUMMARY.md`. No source/test/workflow change; no native or requirement claim.
</verification>

<success_criteria>
- The four-way panel (Codex 5.6 Sol + Gemini 3.1 Pro + Kimi K3 external, run in parallel, plus the internal Claude adversarial reviewer) cross-audits the exact sealed successor at every severity across the 20-43 delta, and each auditor records its own schema-validated artifact — `f20-native-crossaudit.codex-sol` / `.gemini-pro` / `.kimi-k3` and `wayland-core.phase20-independent-review.v1` (r12/r13).
- The two isolation guardrails (#A admission budget; #B quoting-layer only) are adversarially prosecuted and the Kimi watch-items are carried as 20-46 checks; native_macos/native_windows are the only deferred checks.
- A zero-finding PASS from ALL FOUR reviewers admits 20-46; any single finding from any auditor, or any external auditor unreachable/schema-invalid, blocks with an explicit incomplete disposition (no false green from a partial panel).
</success_criteria>

## Artifacts this phase produces
- The four pre-native cross-audits of the sealed third-repaired-successor (`3f839309`) repair delta: Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1`.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-45-SUMMARY.md` using the standard GSD summary template. Record the sealed `source_sha`/tree (`3f839309` / `3092475b`), the four reviewer identities (Codex Sol, Gemini Pro, Kimi K3, internal Claude adversarial) and each artifact's schema-validation status, the two isolation-guardrail verdicts, the carried 20-46 watch-items, and the disposition (ALL-FOUR PASS or fail-closed incomplete). Complete no Phase 20 requirement.
</output>
</content>

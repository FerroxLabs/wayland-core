---
phase: 20-transactional-delegated-mutation
plan: "74"
type: execute
wave: 70
depends_on:
  - "20-73"
files_modified:
  - .config/nextest.toml
  - crates/wcore-cli/src/main.rs
  - crates/wcore-sandbox/src/directory_authority.rs
  - crates/wcore-sandbox/src/directory_authority_windows.rs
  - crates/wcore-sandbox/src/directory_authority_windows_tests.rs
  - crates/wcore-sandbox/src/directory_authority_archive.rs
  - crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs
  - crates/wcore-sandbox/tests/hard_process_containment_windows.rs
  - crates/wcore-swarm/src/worktree_manager.rs
  - crates/wcore-swarm/src/worktree_security.rs
  - crates/wcore-swarm/src/worktree_cleanup.rs
  - crates/wcore-swarm/src/worktree_tests.rs
  - crates/wcore-swarm/tests/worker_runtime_limits.rs
autonomous: true
gap_closure: true
requirements:
  - REQ-native-r6
  - REQ-native-r4
  - REQ-native-r12
must_haves:
  truths:
    - "THIS IS THE PHASE-20 NATIVE WINDOWS CLOSEOUT. It must take the suite to ZERO failures, not reduce it. MEASURED BASELINE at `a1af7707` (executor RE-MEASURES; these are the previous run's numbers, not a licence to skip measuring): `cargo nextest run -p wcore-swarm --no-fail-fast` = 87 run / 78 passed / 9 failed / 6 skipped; `cargo nextest run -p wcore-agent --test transactional_delegated_mutation_test --run-ignored all` = 9 / 3 passed / 6 failed; `cargo nextest run -p wcore-swarm --test dispatch_smoke --no-fail-fast` = 7 / 3 passed / 4 failed / 3 skipped. The 9 swarm failures decompose as 5 stack-overflow aborts (workstream A) plus 4 `Access is denied. (os error 5)` failures (workstream B); the 6 `wcore-agent` failures are the checkout-authority chdir class (workstream C). Every one is PRE-EXISTING and previously masked by the err-87 defect that 20-73 closed. No test regressed."
    - "CRITICAL MEASUREMENT TRAP (this already produced ONE false conclusion in this phase — it is recorded here so it cannot produce a second): in `cmd`, `set VAR=value && cmd2` assigns `\"value \"` WITH A TRAILING SPACE, because `cmd` takes everything between `=` and `&&` literally. Rust's `std::thread::min_stack()` reads `env::var_os(\"RUST_MIN_STACK\").and_then(|s| s.to_str().and_then(|s| s.parse().ok()))`; `\"8388608 \".parse::<usize>()` returns `Err`, so the value is DISCARDED and std SILENTLY falls back to the 2 MiB default. A measurement taken that way reports 'the larger stack did not help' when the larger stack was never applied. Any env-var-driven measurement in this plan MUST use a form that appends no whitespace (PowerShell `$env:VAR='x'`, or `cmd`'s quoted form `set \"VAR=x\"`), and MUST be proven to have taken effect by observing the value the program actually parsed — never by assuming the assignment worked."
    - "WORKSTREAM A ROOT CAUSE (diagnosed, NOT unbounded recursion — do not re-derive, do re-measure the outcome): 5 tests abort with `0xc00000fd` (STATUS_STACK_OVERFLOW): `dispatches_4_noop_workers_in_parallel`, `swarm_reports_failed_worker_status_and_succeeding_workers_complete`, `timeout_releases_workspace_and_capacity_before_return`, `multi_worker_output_exhaustion_fails_without_retaining_buffers`, `required_live_windows_public_dispatch_bash_confines_parent_and_descendants`. A stack-size sweep over the full suite measured: unset (2 MiB) -> 4 aborts; 2359296 (2.25 MiB) -> 0 aborts; 4194304 -> 0; 33554432 -> 0. Unbounded recursion would overflow at EVERY size; the requirement is bounded and at most ~256 KiB above the 2 MiB default. The overflowing thread is the libtest-spawned TEST thread (its name is the test name), not `main` and not `tokio-runtime-worker`. All five are plain `#[tokio::test]` (source-verified: `dispatch_smoke.rs:11` and `:140`, `swarm_worker_failure_reporting_e2e.rs:16`, `worker_runtime_limits.rs:14` and `:56`), so the default current-thread flavor drives the future on the calling thread via `block_on` and `RUST_MIN_STACK` correctly governs it."
    - "WORKSTREAM A MECHANISM (a bounded chain, not a cycle): `Swarm::dispatch_with_cancel` -> `futures::stream::iter(..).buffer_unordered(MAX_CONCURRENT_WORKERS)` over a vector of `Box::pin(dispatch::run_worker(..))` (`lib.rs`, the `futs` loop) -> `dispatch::run_worker` (`dispatch.rs`, `tokio::pin!(create_tree)`) -> `WorktreeManager::create_isolated_checkout`, whose whole body is `Box::pin(self.create_isolated_checkout_inner(..)).await`. At `opt-level=0` there is NO placement-new: `Box::pin(f(..))` first materializes the full-size future as a STACK temporary and then memcpys it to the heap, so existing heap-pinning still pays a transient stack cost at every hop. The codebase already documents this hazard in three places and its comments must not be contradicted: `lib.rs` above the `futs.push(Box::pin(..))` ('Keep each worker state machine on the heap ... can overflow a small Tokio test/runtime stack'), `worktree_manager.rs` above `create_isolated_checkout_inner` ('inlining all of them in a caller's async state machine can exhaust the default test-thread stack'), and `worktree_tests/linux.rs` which already runs one scenario on an explicit `.stack_size(16 * 1024 * 1024)` thread."
    - "WORKSTREAM A REQUIRED FIX IS TWO-PART, BOTH REQUIRED. (1) TEST SIDE: `.config/nextest.toml` gains a top-level `[env]` table setting `RUST_MIN_STACK` to `8388608`. That file currently has NO `[env]` table, so this is additive. Coverage assessment that must be stated: this does NOT cover bare `cargo test`. That is ACCEPTABLE because every gate path routes through nextest — `justfile:22-23` `test:` runs `vx cargo nextest run --workspace --profile default`, `justfile:34-35` `test-ci:` runs `vx cargo nextest run --workspace --profile ci --no-fail-fast`, `justfile:126` `push` depends on `test`, and `ci.yml` runs `vx just test-ci` — so bare `cargo test` is a developer convenience, not a gate. The executor MUST re-verify that justfile claim against the file at execution time rather than trusting this sentence. The `.cargo/config.toml` `[env]` alternative was considered and REJECTED: it would set the variable for every cargo-driven process in the workspace (build scripts, `cargo run`, the CLI binary itself), a far broader blast radius than the test harness, to buy coverage of a non-gate path."
    - "WORKSTREAM A PART 2 (production side) AND ITS HONESTY REQUIREMENT: `crates/wcore-cli/src/main.rs` already fixes this class for the MAIN thread — `const ENTRY_STACK_SIZE: usize = 32 * 1024 * 1024` on a named `wcore-main` `std::thread::Builder`, with a comment naming Windows' 1 MiB main-thread default and `0xC00000FD`. But the runtime built INSIDE that thread is `tokio::runtime::Builder::new_multi_thread().enable_all().build()` with NO `.thread_stack_size(..)`, so every `tokio::spawn`ed task runs on a 2 MiB worker thread — and `create_isolated_checkout` IS reached in production, from `crates/wcore-agent/src/spawner.rs`, `crates/wcore-agent/src/spawner/durable_launch.rs`, `crates/wcore-agent/src/orchestration/anvil/forge.rs` and `crates/wcore-agent/src/child_transaction/gates.rs` (grep-verified). `.thread_stack_size(8 * 1024 * 1024)` is added there. THE HONEST FRAMING, which must appear in the comment and the SUMMARY: the production path was NOT measured to overflow. What WAS measured is that the nearest path's headroom is under 256 KiB over the same 2 MiB default. This is defense-in-depth against a measured-narrow margin, NOT a reproduced production crash, and it must never be written up as one. The cost is virtual RESERVE, not commit — negligible on 64-bit."
    - "WORKSTREAM A STACK VALUE RATIONALE (must be argued, not asserted): 8 MiB is chosen because it matches the unix main-thread default (which is why this class only bites on Windows), it is ~4x the 2.25 MiB that measurably sufficed, and it leaves the guard meaningful — a genuinely unbounded recursion still overflows at 8 MiB, so raising the stack cannot mask a runaway. Raising it to 32 MiB or higher WOULD start masking runaways and is refused for that reason."
    - "WORKSTREAM A RE-MEASUREMENT IS MANDATORY: at `185d9612` these same 5 tests degraded into err-87 failures once the stack was raised. err-87 is now fixed by 20-73, so after the stack fix they may pass outright — or may reveal yet another layer. Do NOT assume either. Measure, and classify every non-passing test by name and cause."
    - "WORKSTREAM B ROOT CAUSE (a production defect in security-sensitive code, isolated by bisecting probe — `PROBE[empty root]: SUCCEEDED`, `PROBE[one file child]: SUCCEEDED`, `PROBE[one EMPTY DIRECTORY child]: FAILED Io(Os { code: 5, PermissionDenied })`): in `crates/wcore-sandbox/src/directory_authority_windows.rs`, `remove_descendants` opens every child as `open_relative(authority, &name, RelativeKind::Any, RelativeIntent::Mutate)`. The access arm pairing the File and Any kinds with the Mutate intent grants `FILE_GENERIC_READ | DELETE | SYNCHRONIZE` — with NO `FILE_GENERIC_WRITE`. When the child turns out to be a DIRECTORY, that write-less handle is wrapped as a `DirectoryAuthority` and the recursion ends in `authority.handle.sync_all()` (`FlushFileBuffers`), which requires `GENERIC_WRITE` and returns `ERROR_ACCESS_DENIED`. The `(RelativeKind::Directory, RelativeIntent::Create | RelativeIntent::Mutate)` arm DOES carry `FILE_GENERIC_WRITE` for exactly this reason; the Any pairing never did. Affected: `transaction_workspace_paths_share_manager_representation`, `independent_cli_processes_cannot_overbook_shared_capacity`, `malformed_heartbeat_fails_closed_and_preserves_bounded_diagnostic`, `public_dispatch_owns_git_authority_and_preserves_parent_and_sibling_state`."
    - "WORKSTREAM B FIX, DECIDED AND JUSTIFIED (least privilege, stated as a full rights table so nothing is granted by accident): SPLIT the arm that currently pairs the File and Any kinds with Mutate. `(RelativeKind::File, RelativeIntent::Mutate)` KEEPS `FILE_GENERIC_READ | DELETE | SYNCHRONIZE` unchanged (it has no caller today — grep-verified, the only Mutate call site is the Any one in `remove_descendants` — so keeping it narrow costs nothing and prevents a future caller inheriting a widened grant). `(RelativeKind::Any, RelativeIntent::Mutate)` becomes `FILE_GENERIC_READ | FILE_GENERIC_WRITE | DELETE | SYNCHRONIZE`, byte-identical to the Directory/Mutate arm. THE ARGUMENT: `Any` means the object's kind is not knowable until the handle exists, so the grant must cover every kind the handle can turn out to be; the union over {File, Directory} of the Mutate rights IS the Directory arm's set. `Any` therefore receives exactly the union and nothing beyond it, and no other (kind, intent) pair changes. Resulting table, unchanged except the one marked pair: (File, ReadOnly) read; (Directory, ReadOnly) read; (Any, ReadOnly) read; (File, Create) generic read+write+delete; (Directory, Create|Mutate) read+write+delete; (File, Mutate) read+delete; **(Any, Mutate) read+write+delete — CHANGED**; (Any, Create) refused; (File, LockOpen|LockOpenOrCreate) generic read+write, no delete; (Directory|Any, LockOpen|LockOpenOrCreate) refused."
    - "WORKSTREAM B ACCEPTED CONSEQUENCE AND REJECTED ALTERNATIVES, both of which must be recorded rather than discovered later. CONSEQUENCE: a FILE child opened through the Any path now carries `FILE_GENERIC_WRITE` it never exercises, and cleanup of such a file now additionally requires its other openers to have granted `FILE_SHARE_WRITE` — so a file held elsewhere with only share-read plus share-delete, which was previously deletable, will now fail with a sharing violation. Bounded, and the executor must watch for it as a possible new failure signature. REJECTED (i): re-open the child by NAME with the exact kind once metadata is read — that inserts a second by-name resolution between the metadata read and the delete, which is precisely the pathname re-resolution the retained-handle design exists to forbid, and would need an identity re-proof to stay fail-closed. REJECTED (ii): drop the trailing `sync_all()` so a write-less handle suffices — that deletes the documented Windows durability boundary to satisfy an access check, which is weakening a guarantee to make a test pass. DEFERRED, and named as the correct long-term answer: carry the directory-ness out of the enumeration (`parse_directory_entries` already walks `FILE_ID_BOTH_DIR_INFORMATION`, which carries `FileAttributes`) and pass a precise `RelativeKind`, eliminating `Any` from this path entirely — deferred because it changes the raw-pointer directory parser in the same commit that first COMPILES that parser's tests (workstream D), which is an unacceptable stacking of risk."
    - "WORKSTREAM C (the SECOND LAYER 20-72 predicted and deliberately deferred; it has now materialized): 6 `wcore-agent` tests fail with `fatal: cannot change to '\\\\?\\C:\\...\\checkout': Permission denied`. The mechanism 20-72 PROVED on this hardware is that a retained directory handle carrying the `DELETE` access right makes `SetCurrentDirectory` — and therefore MSYS/Cygwin in-process `chdir()` — fail with a sharing violation, while `CreateProcess` with `lpCurrentDirectory` set to the same directory SUCCEEDS. `git -C <checkout>` chdirs in-process. The invocation sites are `worktree_cleanup.rs::checkout_git_command` (`self.git_command(&[\"-C\", checkout, ..])`, reached from `run_checkout_git` and `checkout_git_stdout`) and `worktree/parent.rs::landing_git_command` (same `-C` shape plus quarantine object-dir env). The DELETE-bearing handle is the checkout authority minted at `worktree_manager.rs` by `DirectoryAuthority::open(&checkout)` in BOTH `create_isolated_checkout` and `create_integration_checkout`, stored in `TransactionWorkspaceAuthorities.checkout` and cloned into `TransactionCleanup::bind_checkout_authority`; `RelativeIntent::Create`/`Mutate` and the plain `open_directory` all grant DELETE."
    - "WORKSTREAM C SOURCE DETERMINATION (grep-verified now, to be CONFIRMED not assumed): the checkout authority's complete consumer set is — `dispatch.rs` x4 via `heartbeat::read_status_authorized(&checkout_authority)`, which calls `read_child_bounded(STATUS_FILE, MAX_STATUS_BYTES)` (a relative child READ); `worktree.rs::logical_tree_bytes(self.checkout_authority(), ..)` (recursive read-only enumeration); `worktree.rs::validate_execution_authority` via `self.authorities.checkout.validate_path(&self.checkout)` (identity only); `worktree.rs::seal_candidate` via `CandidateSeal::mint(&self.authorities.checkout, ..)` (manifest read); `worktree.rs::TransactionCleanup::release` via `checkout.has_outstanding_loans()` (loan accounting, no I/O); `worktree/parent.rs` x4 via `.display_path()` (path metadata only). NOTHING deletes, renames, creates a child, or calls `sync_all` THROUGH the checkout authority. The transaction root's destruction runs through `root_authority` -> `remove_transaction_root` -> the ROOT's handle, and reaches the checkout as an ordinary descendant of that recursion with its own freshly-opened handle. The 20-72 decision rule — if a handle genuinely NEEDS DELETE for cleanup, scope WHEN it is held rather than drop a bit it needs — therefore points at branch (i), because on this evidence the checkout authority does not need DELETE at all."
    - "WORKSTREAM C IDENTIFIED FALSIFIER, which the executor must test before committing to branch (i): 20-72's `open_observational` opens with `access_mode(GENERIC_READ)`, and `FILE_GENERIC_READ` includes `FILE_LIST_DIRECTORY` (the same bit as `FILE_READ_DATA`) but NOT `FILE_TRAVERSE`. Unlike `repo_authority` — which 20-72 converted and which is used ONLY as a `validate_path` witness, never as a `RootDirectory` — the checkout authority IS used as the `RootDirectory` for relative child opens and for `NtQueryDirectoryFile` enumeration. Traverse checking is normally bypassed because SeChangeNotifyPrivilege is granted to Everyone by default, but that is an assumption, not a proof, and it is exactly the kind of assumption a restricted or AppContainer token invalidates. So 20-72 supplies NO evidence for this conversion and the executor must obtain its own."
    - "WORKSTREAM C BRANCHES, all three specified in advance so the choice is a measurement and not an improvisation. (i) OBSERVATIONAL OPEN — convert both `DirectoryAuthority::open(&checkout)` sites to the existing `open_observational`. Preferred if the falsifier clears, because it removes a capability the authority never exercises while the handle stays CONTINUOUSLY held, so the anti-swap property is byte-for-byte what it is today — identical in form to 20-72's argument. (ii) LIFETIME SCOPING — release the authority around chdir-ing git calls and re-acquire with a fresh identity proof. Chosen ONLY if measurement shows the authority genuinely needs DELETE, because it converts a 'no window exists' guarantee into a 'window exists but is detected' guarantee, which is a real reduction in the anti-swap property and must be argued as such if taken. (iii) HYBRID — keep a continuously held handle but define a third access profile that grants read, list and traverse WITHOUT delete. Chosen if branch (i) fails only on the traverse/relative-open falsifier; this preserves the continuous-hold property that makes (i) correct while restoring whatever access the relative opens actually need. WHICHEVER BRANCH IS TAKEN, the anti-swap and identity guarantees the retained handle provides MUST NOT weaken; if any handle is released and re-acquired, identity MUST be re-validated on re-acquisition and the re-validation must fail closed."
    - "WORKSTREAM C PATH-REPRESENTATION QUESTION, to be settled by a decisive probe and NOT by reasoning: the error text carries a verbatim `\\\\?\\C:\\...` path. `worktree_paths.rs::normalized_root` is the crate's ONE representation decision (`std::fs::canonicalize` then `dunce::simplified`) and its own module doc records that the de-verbatimizing strip is CONDITIONAL — refused when any component exceeds 255 chars, when the path is not valid UTF-8, when a component is a reserved DOS name, or when a component ends in '.' or a space — and that MSYS behind git-for-Windows argv cannot parse the verbatim form. So the verbatim prefix could be causal rather than incidental. THE PROBE: on the box, with NO handle held, run the same git subcommand with `-C` given the verbatim path and then the plain path against the same repository. If verbatim fails and plain succeeds with no handle held, representation is causal or co-causal and the repair belongs in the path derivation. If both succeed, representation is incidental and the failure is the DELETE bit. Whatever the answer, 20-71 established representation-agnostic identity and `normalized_root` as the ONLY site allowed to name the de-verbatimizing call — do NOT re-open that, and do NOT 'fix' this by adding a second de-verbatimizing call somewhere else."
    - "WORKSTREAM D — 19 PRE-EXISTING `wcore-sandbox` COMPILE ERRORS ON WINDOWS, verified NEVER to have compiled since introduction (`8738b24e`, 2026-07-20; the partial repair `92cac8bb` missed them; `git log -S \"pub(super) fn validate_windows_child_name\"` is empty). All 19 sit behind Windows cfg gates, so Linux is unaffected and no Linux result changes. FOUR GROUPS. GROUP A (9 errors, E0422/E0425 in `src/directory_authority_windows_tests.rs`): that file is mounted as a SIBLING of the `windows` module (`directory_authority.rs`, `#[cfg(all(test, windows))] #[path = \"directory_authority_windows_tests.rs\"] mod tests;`) and reaches internals through `use super::windows::*;`, but `DirectoryCaseSensitiveInfo` — AND its `flags: u32` FIELD, whose omission yields a follow-on E0451 — plus `checked_information_length`, `parse_directory_entries` and `validate_windows_child_name` are all still PRIVATE, and a glob import silently skips private items. The file's own header documents the intended contract ('bumped to `pub(super)`') and the neighbouring `pub(super) const FILE_CS_FLAG_CASE_SENSITIVE_DIR` shows the pattern. FIX: add `pub(super)` to those four items plus the struct field. GROUP B (1 error, E0599 `as_raw_handle` on `&std::fs::File`): add `use std::os::windows::io::AsRawHandle;`. GROUP C (8 errors in `src/backends/appcontainer/windows_impl/tests.rs`): that file already carries a 'Compile-debt restore' import block but omits three — add `use std::time::Duration;`, `use crate::ResourceLimitEnforcement;`, and `use crate::backends::SandboxBackend;` (the last is trait-in-scope: `execute` and `is_available` are `SandboxBackend` methods). GROUP D (1 error, E0609 in `src/directory_authority_archive.rs`): the `BY_HANDLE_FILE_INFORMATION` field access uses the non-Hungarian spelling; windows-sys spells it with the leading `n`, as the workspace already does correctly at `crates/wcore-agent/src/session_journal/lease.rs:468` and `crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs:499`."
    - "WORKSTREAM D IS A REAL PRODUCTION DEFECT, NOT TEST DEBT, and must be written up as one. Group D sits inside `#[cfg(windows)]` PRODUCTION logic that rejects hard-linked files during archive export, in a module declared `#[cfg(any(feature = \"live-docker\", test))] pub(crate) mod archive;` (verified at `directory_authority.rs`). Therefore `--features live-docker` on Windows DOES NOT BUILD AT ALL, and the Windows half of that hard-link guard has never existed in a compiled binary; the Unix branch immediately above it is correct. Because the same gate includes `test`, the wcore-sandbox test targets have never compiled on Windows either, so the archive tests have never run there. COVERAGE CONSEQUENCE OF GROUP A, equally substantive: the Windows retained-handle SECURITY proofs have NEVER EXECUTED on Windows — `parse_directory_entries` (a raw-pointer directory-entry parser walking `FILE_ID_BOTH_DIR_INFORMATION` offsets), `checked_information_length` (its overflow guard), and `validate_windows_child_name` (the child-name refusal that every handle-relative open funnels through). Making them compile may surface REAL failures in security-relevant code. The executor must budget for that and MUST report any test that then fails, root-cause it, and either fix it or explicitly escalate it — never paper over it, never re-gate it, never mark it ignored."
    - "WORKSTREAM E — the workspace lint gate becomes REACHABLE once D lands. `just lint` is `vx cargo clippy --workspace --all-targets -- -D warnings` (`justfile:75-76`) and `ci.yml` runs it as 'Clippy (warnings = errors)' on the self-hosted Windows runner (labels `self-hosted, Windows, X64, msvc`). Known remaining warnings, measured after the D fix and to be RE-MEASURED not trusted: an unused `FILE_RENAME_INFO` import in `directory_authority_windows_tests.rs`; two unreachable expressions in the `directory_authority.rs` LIB (pre-existing); four `unsafe_op_in_unsafe_fn` inside the nested `unsafe fn spawn_suspended` in `tests/hard_process_containment_windows.rs`; two never-used methods in `directory_authority_archive.rs`; dead-code `rename_into` (`worktree_security.rs`) and `set_ambient_git_env` (`worktree_cleanup.rs`); an unused `Arc` import in `tests/worker_runtime_limits.rs`; an `is_multiple_of` lint; and a collapsible-`if` in the bwrap backend. EACH is fixed PROPERLY — genuinely dead code removed, `unsafe {}` blocks added with SAFETY comments, and so on. A blanket suppression attribute is FORBIDDEN as a silencing device; a narrowly scoped one is permitted ONLY where the lint is a genuine platform-conditional false positive, and every one added must be listed with its justification in the SUMMARY. If any warning reveals a real bug, it is treated as a bug and reported."
    - "WORKSTREAM E CI-STATUS FACT TO ESTABLISH, NOT GUESS: `justfile:96-98` carries a `[windows]` override for `fmt-check` but `lint` has NO windows override, so either the self-hosted Windows runner has not run these commits or the Windows CI job is currently RED. The executor checks the ACTUAL status with `gh` (`gh auth switch --user FerroxLabs` before every gh operation; issues live on `FerroxLabs/wayland`, PRs on `FerroxLabs/wayland-core`) and REPORTS what it finds. Do not assert either possibility without the check."
    - "ORDERING (D -> B -> C -> A -> E -> gate), chosen to minimize masking and justified per step: D FIRST because it unblocks `--all-targets` compilation for `wcore-sandbox`, which is a hard prerequisite for both the workspace lint gate and for running that crate's tests at all, and because it may surface new failures in never-executed security code that are far cheaper to find now than at the final gate. B SECOND because it is a single-arm production fix that unmasks 4 named tests and is independent of C and A. C THIRD because with the os-5 class gone any residual access-denied text is unambiguously the chdir class, which makes attribution trivial. A FOURTH because the stack fix CHANGES HOW the dispatch tests fail, so running it after every other layer is resolved means a residual abort is attributable to the stack and nothing else. E FIFTH because lint is only reachable once everything compiles and because D/B/C/A themselves add code that must be lint-clean. Gate LAST. Any reordering must be justified in the SUMMARY, never taken silently."
    - "HARDWARE GATE (REQ-native-r6/r4/r12, PRD D5/D7): the Mac CANNOT compile this workspace — only `cargo fmt --all` works there. Every build, check, clippy, test and measurement runs on SEANDESKTOP (`ssh -o BatchMode=yes SeanD@seandesktop`, checkout `C:\\ferrox-win`, currently detached at `a1af7707`). No native claim may be made from cross-compilation or source inspection. TARGET: zero failures across `wcore-swarm`, 9/9 on `wcore-agent --test transactional_delegated_mutation_test --run-ignored all`, `dispatch_smoke` entirely green, a REPORTED `wcore-sandbox` suite result (that crate's tests have never run on Windows, so failures there are expected to be newly surfaced and must be root-caused, not hidden), and `cargo clippy --workspace --all-targets -- -D warnings` plus `cargo fmt --all --check` clean."
  artifacts:
    - path: .config/nextest.toml
      provides: "A top-level `[env]` table setting `RUST_MIN_STACK` to 8 MiB, with the coverage assessment and the trailing-space trap recorded in-file so the value cannot be trimmed or mis-set later"
    - path: crates/wcore-cli/src/main.rs
      provides: "`.thread_stack_size(8 * 1024 * 1024)` on the multi-thread runtime built inside the 32 MiB entry thread, documented explicitly as defense-in-depth against a measured-narrow margin and NOT as a reproduced production crash"
    - path: crates/wcore-sandbox/src/directory_authority_windows.rs
      provides: "The split Mutate access arms — File keeps read+delete, Any receives the union read+write+delete — with the `sync_all`/`FlushFileBuffers` requirement recorded in-source so the write bit cannot be trimmed back"
    - path: crates/wcore-sandbox/src/directory_authority_windows_tests.rs
      provides: "`pub(super)` visibility restored on the four private items plus the struct field (Group A), the unused import removed, and the Windows regression test proving destructive removal succeeds through a directory child"
    - path: crates/wcore-sandbox/src/directory_authority_archive.rs
      provides: "The corrected windows-sys hard-link field access — the first time the Windows half of that guard has ever compiled — plus the resolution of its two never-used methods"
    - path: crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs
      provides: "The three missing imports that complete the existing compile-debt restore block (Group C)"
    - path: crates/wcore-swarm/src/worktree_manager.rs
      provides: "Both checkout-authority acquisition sites repaired under the chosen workstream-C branch, with the branch, its measurement and its anti-swap argument recorded at the acquisition"
    - path: crates/wcore-swarm/src/worktree_tests.rs
      provides: "The workstream-C regression test — a chdir-ing git subcommand still succeeds inside a checkout while the workspace authorities are alive"
    - path: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      provides: "Explicit `unsafe {}` blocks with SAFETY comments inside the nested unsafe fn, closing four `unsafe_op_in_unsafe_fn` findings without a suppression attribute"
    - path: .planning/phases/20-transactional-delegated-mutation/20-74-SUMMARY.md
      provides: "The closeout record: re-measured baseline, per-workstream root cause and fix, the workstream-C branch taken with its evidence, the newly surfaced `wcore-sandbox` results, every warning fixed or escalated, the CI status found, and the final zero-failure counts with the exact SHA"
  key_links:
    - from: crates/wcore-sandbox/src/directory_authority_windows.rs
      to: crates/wcore-sandbox/src/directory_authority_windows_tests.rs
      via: "Group A's `pub(super)` restoration is what first compiles the raw-pointer directory parser's proofs on Windows, so the same commit that fixes the access-rights arm also makes the security tests around it executable for the first time"
      pattern: "native_windows"
    - from: crates/wcore-swarm/src/worktree_manager.rs
      to: crates/wcore-swarm/src/worktree_cleanup.rs
      via: "The checkout authority's access profile governs whether `checkout_git_command`'s `git -C <checkout>` can chdir; the authority is held continuously so identity is never re-resolved by pathname"
      pattern: "native_windows"
    - from: .config/nextest.toml
      to: crates/wcore-cli/src/main.rs
      via: "One bounded stack requirement, two surfaces: the test harness raises the libtest thread through `RUST_MIN_STACK`, the binary raises the tokio worker threads through `thread_stack_size`, and neither is large enough to mask an unbounded recursion"
      pattern: "native_windows"
---

<objective>
Take Phase-20 native Windows to ZERO failures. Five distinct defects remain and each has been masking the next: 19 `wcore-sandbox` compile errors that have never once compiled on Windows and that hide both a production hard-link guard and the entire retained-handle security proof set; a missing write bit on the cleanup path's access derivation that makes destructive removal of any directory child fail with access-denied; the checkout-authority DELETE/chdir defect that 20-72 predicted and deferred; five bounded stack overflows in the dispatch state machine; and a workspace lint gate that has been unreachable for as long as the compile errors have existed.

Purpose: 20-73 closed the `LockFileEx`-on-directory defect and, in doing so, unmasked everything behind it — 9 `wcore-swarm` failures, 6 `wcore-agent` failures, and a clippy gate that cannot even be invoked. None of them is a regression; all of them are pre-existing defects that a broken advisory lock had been hiding. This is the closeout plan, and a closeout plan is exactly where the pressure to weaken a gate is highest, so every task here states what it may NOT do as precisely as what it must. Two of the five defects are genuine production defects in security-sensitive code and must be written up as such: the Windows half of the archive hard-link guard has never existed in a compiled binary, and the handle-relative directory parser, its overflow guard and the child-name refusal have never executed a single test on Windows.
Output: A tree that compiles `--all-targets` on Windows for the three affected crates; zero `wcore-swarm` failures; 9/9 on the transactional delegated-mutation suite; a green `dispatch_smoke`; a REPORTED first-ever `wcore-sandbox` Windows suite result with every newly surfaced failure root-caused or escalated; a clean `cargo clippy --workspace --all-targets -- -D warnings`; and a prepared, Sean-gated re-dispatch of the full 6-target native proof.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-NATIVE-REPAIR-PRD.md
@.planning/phases/20-transactional-delegated-mutation/20-73-SUMMARY.md
@crates/wcore-sandbox/src/directory_authority.rs
@crates/wcore-sandbox/src/directory_authority_windows.rs
@crates/wcore-sandbox/src/directory_authority_windows_tests.rs
@crates/wcore-sandbox/src/directory_authority_archive.rs
@crates/wcore-swarm/src/worktree_manager.rs
@crates/wcore-swarm/src/worktree_cleanup.rs
@crates/wcore-swarm/src/worktree/parent.rs
@crates/wcore-swarm/src/worktree_paths.rs
@crates/wcore-cli/src/main.rs
@justfile
@.config/nextest.toml
</context>

<execution_rules>
- COMPILE DISCIPLINE (hard, PRD D5): the Mac CANNOT compile this workspace. Do NOT run `cargo build`, `cargo check`, `cargo clippy`, `cargo test` or `cargo nextest` on the Mac. `cargo fmt --all` DOES work on the Mac and MUST be run before every commit. Every build, check, clippy, test and measurement runs on SEANDESKTOP: `ssh -o BatchMode=yes SeanD@seandesktop`, checkout `C:\ferrox-win`, currently detached at `a1af7707`.
- REMOTE SHELL FORM (hard): every remote invocation uses the script-block form `ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "<cmd> 2>&1" }'`, and its output is piped through `/usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"` to strip PowerShell's serialization noise. Git on the box MUST be wrapped as `cmd /c "git ..."` — PowerShell's Stop preference treats git's stderr chatter as fatal and aborts otherwise. Wrapping cargo the same way keeps cargo's progress chatter off stderr-as-fatal.
- ENV-VAR MEASUREMENT (hard, and this trap has ALREADY produced one false conclusion in this phase): in `cmd`, `set VAR=value && cmd2` assigns the value WITH A TRAILING SPACE. `std::thread::min_stack()` parses `RUST_MIN_STACK` with `.parse().ok()`, which fails on trailing whitespace and silently falls back to 2 MiB. NEVER use that form for any measurement. Use PowerShell's `$env:VAR='x'` or `cmd`'s quoted `set "VAR=x"` form, and PROVE the value took effect by observing what the program actually parsed — never by assuming the assignment worked. Task 1 establishes and records this protocol before any other measurement is taken.
- GREP HYGIENE (hard, and MEASURED — do not "simplify" these gates back to bare `grep`): the local `grep` is rtk-proxied to a different engine that silently drops lines. Measured on this tree: a comment-filtering inverted match returned 32 lines where `/usr/bin/grep -v` on the same 704-line file returned 674 — 642 lines dropped with no error, which would make any filtered gate pass or fail for reasons unrelated to the code. EVERY gate in this plan invokes `/usr/bin/grep` explicitly, exactly as it invokes `/usr/bin/git`, and every literal gate uses `-F` (fixed strings).
- Use `/usr/bin/git` on the Mac. ONE ATOMIC COMMIT PER SOURCE TASK, in the branch's existing message style (`fix(scope): ...`, `test(scope): ...`, `style(scope): ...`, `docs(20-74): ...`). NO `Co-Authored-By` trailers. `/usr/bin/git add` the exact source paths that task touched — never `-A`, never `.`. NEVER stage `AGENTS.md` or any `.ijfw` churn. Measurement-only tasks commit nothing.
- LOCATE EVERY EDIT BY SYMBOL NAME, never by line number. Every line number in this plan is a landmark from an earlier commit and has already drifted or will. Rebase every expectation — including test counts and gate counts — on the actual file contents at execution time.
- Do NOT modify or revisit the runner-proven commits `00f453f2`, `aaeeb379`, `0e867008`, the path-representation repair `ece09681..919629ee`, the observational-open repair `fac01305..185d9612`, or the advisory-lock repair `44190aa6..a1af7707`. Their findings are hardware-proven; consume them, do not re-derive them.
- AGENTS.md discipline: surgical diffs only (every changed line traces to a numbered workstream), no drive-by refactors, no reformatting of adjacent code, `thiserror` for public error types and `anyhow` internally (here: the existing `SandboxError` and `SwarmError` plus the crates' `Result` aliases), clippy-clean, and platform differences centralized in `wcore-sandbox` so `wcore-swarm` gains no new `#[cfg]`.
- No new crate, no new dependency, no `Cargo.toml` or `Cargo.lock` change. Every symbol used here is already reachable.
- NO GATE WEAKENING, and this is the plan where that pressure is highest. Do not widen a guard, delete an assertion, re-gate a test to one platform, add `#[ignore]`, relax a threshold, or add a blanket lint suppression to reach the zero-failure target. If a target cannot be reached honestly, STOP and report with evidence — a reported red is worth more than an engineered green.
- Every residual or newly surfaced failure is attributed BY NAME with a cause. "Pre-existing" is not a cause. "Flaky" is not a cause without a measurement showing it flaky.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Establish ground truth — re-measure the baseline on SEANDESKTOP, prove the env-var measurement protocol, and read the real CI status</name>
  <files>None (measurement only; commits nothing)</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-73-SUMMARY.md (the residual-failure inventory this task must confirm or correct), justfile (lines around `test:`, `test-ci:`, `lint:`, `fmt-check:` — confirm the nextest routing claim rather than trusting this plan), .config/nextest.toml (confirm no `[env]` table exists yet), .github/workflows/ci.yml (the self-hosted Windows job's steps)</read_first>
  <behavior>
    - The box is on the exact `a1af7707` tree, confirmed by the SHA it prints, and every count below is measured there rather than copied from this plan.
    - The three suite results and the exact name of every failing or aborting test are recorded, and each is bucketed into workstream A, B or C. A failure that fits none of the three buckets is a NEW signal and stops the plan until it is explained.
    - The `cmd` trailing-space trap is demonstrated concretely, and the whitespace-safe form is demonstrated to produce a clean value — both recorded verbatim, so no later measurement in this plan can be doubted.
    - The stack value the program ACTUALLY parses is observed under the chosen invocation form, not assumed from the assignment succeeding.
    - The real CI status of the self-hosted Windows job on this branch is read from GitHub and reported, rather than inferred from the absence of a `[windows]` lint override.
    - Nothing is committed and no source file changes.
  </behavior>
  <action>Sync the box and confirm the tree: `cmd /c "git fetch --all --prune"`, `cmd /c "git checkout --detach a1af7707"`, `cmd /c "git rev-parse HEAD"`, and confirm the printed SHA matches. Then run and record, in full, `cargo nextest run -p wcore-swarm --no-fail-fast`, `cargo nextest run -p wcore-agent --test transactional_delegated_mutation_test --run-ignored all`, and `cargo nextest run -p wcore-swarm --test dispatch_smoke --no-fail-fast`. Record run/passed/failed/skipped for each and the EXACT NAME of every failing or aborting test. Bucket each name into workstream A (a `0xc00000fd` abort), B (an `Access is denied` / os error 5 failure) or C (a git chdir refusal). If any name fits none of those buckets, or if the counts differ materially from 87/78/9/6, 9/3/6 and 7/3/4/3, STOP and report the discrepancy with the names before proceeding — an unexplained failure means something else is in play and every delta this plan claims would be unattributable.

Establish the measurement protocol next, and record the raw output of both halves. First demonstrate the trap: run a `cmd` one-liner that assigns a variable with the unquoted `set VAR=value && ...` form and echoes it delimited on both sides (brackets, not bare), so the trailing space is VISIBLE in the transcript. Then run the same thing with the quoted `set "VAR=value"` form and show the delimiter is flush. Then prove the effect on the actual consumer: with `RUST_MIN_STACK` set through the whitespace-safe form, observe the value `std::thread::min_stack()` returns inside a test process — use a throwaway probe (a scratch test that prints or fails with the number, run once and then deleted, never committed, never added to production code) and record the number. A value of 2097152 when you set 8388608 means the assignment did not take effect and every subsequent measurement is void; do not proceed past that until the observed number matches.

Finally read the CI reality. `gh auth switch --user FerroxLabs` first — that is required before EVERY gh operation. Query the workflow runs for `plan/f20-unified-audit-repair` on `FerroxLabs/wayland-core` and report the status of the self-hosted Windows `ci` job: whether it has run on these commits at all, and if it has, whether the `Clippy (warnings = errors)` step is red. Report what you find; do not guess, and do not fix anything here.

Records evidence for REQ-native-r6, REQ-native-r4 and REQ-native-r12; marks no requirement complete; changes no source file and creates no commit.</action>
  <verify>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "git fetch --all --prune 2>&1"; cmd /c "git checkout --detach a1af7707 2>&1"; cmd /c "git rev-parse HEAD" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-swarm --no-fail-fast 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-agent --test transactional_delegated_mutation_test --run-ignored all 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-swarm --test dispatch_smoke --no-fail-fast 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { cmd /c "set TRAPCHK=8388608 && echo [%TRAPCHK%]"; cmd /c "set \"TRAPCHK2=8388608\" && echo [%TRAPCHK2%]" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && /usr/bin/grep -n -A 2 '^test:' justfile && /usr/bin/grep -n -A 2 '^test-ci:' justfile && test "$(/usr/bin/grep -cE '^\[env\]' .config/nextest.toml)" = "0"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && gh auth switch --user FerroxLabs && gh run list -R FerroxLabs/wayland-core --branch plan/f20-unified-audit-repair --limit 10</automated>
  </verify>
  <done>The box is detached on `a1af7707` and all three suites have been run there with counts and the exact names of every failing or aborting test recorded and bucketed into workstream A, B or C, with any unbucketable name reported as a stop condition. The `cmd` trailing-space trap is demonstrated in the transcript with delimited output showing the unquoted form produces a trailing space and the quoted form does not, and the value `std::thread::min_stack()` actually parses under the whitespace-safe form has been OBSERVED and recorded. The justfile's nextest routing is confirmed against the file, and `.config/nextest.toml` is confirmed to have no `[env]` table. The real CI status of the self-hosted Windows job on this branch is reported. No source file changed and no commit was created.</done>
</task>

<task type="auto">
  <name>Task 2: Workstream D — repair the 19 pre-existing wcore-sandbox Windows compile errors, and report what compiling them surfaces</name>
  <files>crates/wcore-sandbox/src/directory_authority_windows.rs, crates/wcore-sandbox/src/directory_authority_windows_tests.rs, crates/wcore-sandbox/src/directory_authority_archive.rs, crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs</files>
  <read_first>crates/wcore-sandbox/src/directory_authority.rs (the `#[cfg(all(test, windows))] #[path = ...] mod tests;` mount and the `#[cfg(any(feature = "live-docker", test))] pub(crate) mod archive;` declaration — both are load-bearing to this task's write-up), crates/wcore-sandbox/src/directory_authority_windows.rs (the module header's `use` block; `FILE_CS_FLAG_CASE_SENSITIVE_DIR` and the struct immediately after it; `directory_is_case_sensitive`; `checked_information_length`; `parse_directory_entries`; `validate_windows_child_name`), crates/wcore-sandbox/src/directory_authority_windows_tests.rs (the whole header comment and import block, and every reference to the four private items), crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs (the existing compile-debt restore import block and the bodies that reference the three missing names), crates/wcore-sandbox/src/directory_authority_archive.rs (the `#[cfg(windows)]` hard-link rejection block and the `#[cfg(unix)]` branch immediately above it), crates/wcore-agent/src/session_journal/lease.rs and crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs (the two sites where the workspace already spells the windows-sys field correctly)</read_first>
  <behavior>
    - `cargo check -p wcore-sandbox --all-targets` compiles on Windows for the first time since the code was introduced, with zero errors.
    - Linux is unaffected: every one of the 19 errors sits behind a Windows cfg gate, so no Linux target's behaviour, warnings or test set changes.
    - The Windows retained-handle security proofs become executable for the first time, and whatever they then report is REPORTED — a newly surfaced failure in `parse_directory_entries`, `checked_information_length` or `validate_windows_child_name` is treated as a possible real defect in security-relevant code, not as noise to be silenced.
    - The Windows half of the archive hard-link guard compiles for the first time, and the write-up says plainly that no Windows binary has ever contained it and that `--features live-docker` has never built on Windows.
    - Visibility is widened by exactly the minimum the sibling test module requires — `pub(super)`, matching the neighbouring constant's existing pattern — and nothing becomes `pub` or `pub(crate)`.
  </behavior>
  <action>Four groups, each located by symbol name.

GROUP A — visibility (9 errors, E0422/E0425 in `directory_authority_windows_tests.rs`). That file is mounted as a SIBLING of the `windows` module and reaches internals through a glob `use super::windows::*;`, which silently skips private items; its own header already documents the intended contract. Add `pub(super)` to exactly four items in `directory_authority_windows.rs` — the case-sensitivity info struct declared immediately after `FILE_CS_FLAG_CASE_SENSITIVE_DIR`, `checked_information_length`, `parse_directory_entries`, and `validate_windows_child_name` — AND to that struct's single `u32` field, because leaving the field private yields a follow-on E0451 at the test's struct-literal construction. Match the neighbouring `pub(super) const FILE_CS_FLAG_CASE_SENSITIVE_DIR` exactly; do NOT reach for `pub` or `pub(crate)`, and do not change any signature, body or attribute of the four items.

GROUP B — one E0599 where `as_raw_handle` is called on a `&std::fs::File` without the extension trait in scope. LOCATE THE SITE FROM THE COMPILER'S OWN E0599 OUTPUT rather than guessing the file: `directory_authority_archive.rs` already imports that trait inside its `#[cfg(windows)]` hard-link block, so the failing call is somewhere else and this plan does not name it. Add the `AsRawHandle` extension-trait import at the correct scope for whatever site the compiler points at. If that site is inside a `#[cfg(windows)]` block with a local `use` group, put the import there rather than at module scope, so it cannot become an unused import on another platform. Add the file to the commit's staged path set and record it in the SUMMARY, since this plan's `files_modified` could not name it in advance.

GROUP C — 8 errors in `backends/appcontainer/windows_impl/tests.rs`. That file already carries a documented "Compile-debt restore" import block; extend that same block, do not create a second one. Add `use std::time::Duration;`, `use crate::ResourceLimitEnforcement;`, and `use crate::backends::SandboxBackend;`. The last one is a trait-in-scope import, not a type import — `execute` and `is_available` are `SandboxBackend` methods and the calls do not resolve without it; say so in a one-line comment so nobody removes it as "unused". While you are in that file, note but do NOT fix the duplicated `use super::*;` — it is pre-existing and outside this task's trace; workstream E owns any lint it produces.

GROUP D — one E0609 in `directory_authority_archive.rs`. The `#[cfg(windows)]` hard-link rejection reads the link count off `BY_HANDLE_FILE_INFORMATION` using the non-Hungarian field spelling; windows-sys spells that field with a leading `n`. Correct it to the spelling the workspace already uses at `wcore-agent/src/session_journal/lease.rs` and `wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs`. Change nothing else in that block — the `GetFileInformationByHandle` call, the SAFETY comment, the `> 1` comparison and the refusal message all stay exactly as written, and the `#[cfg(unix)]` branch above it is already correct and is not touched.

Add a short comment above the corrected Windows hard-link branch recording WHY this is being fixed now and what it means: this module is declared `#[cfg(any(feature = "live-docker", test))]`, so this expression has never compiled on Windows, which means `--features live-docker` has never built on Windows and the Windows half of this guard has never existed in a compiled binary. That is a substantive production fact, not test debt, and it belongs in the source as much as in the SUMMARY.

Commit this task alone, then push and measure on the box. Run `cargo check -p wcore-sandbox --all-targets` and require zero errors. Then run `cargo nextest run -p wcore-sandbox --no-fail-fast` — the FIRST time that crate's tests have ever run on Windows — and REPORT the full result. For every failure: name it, root-cause it, and either fix it in this task if the fix is small and clearly traceable to the same compile debt, or escalate it explicitly with its evidence if it is a distinct defect. Do NOT mark any of them `#[ignore]`, do NOT re-gate any of them to unix, and do NOT weaken any assertion to make them pass. A newly surfaced failure inside the raw-pointer directory parser, the overflow guard or the child-name refusal is exactly the outcome this task exists to expose, and hiding one would be worse than never having compiled the file.

Implements REQ-native-r4; records evidence for REQ-native-r6 and REQ-native-r12; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'pub(super) fn checked_information_length' crates/wcore-sandbox/src/directory_authority_windows.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'pub(super) fn parse_directory_entries' crates/wcore-sandbox/src/directory_authority_windows.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'pub(super) fn validate_windows_child_name' crates/wcore-sandbox/src/directory_authority_windows.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'pub(super) struct DirectoryCaseSensitiveInfo' crates/wcore-sandbox/src/directory_authority_windows.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'pub(super) flags: u32' crates/wcore-sandbox/src/directory_authority_windows.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF '.nNumberOfLinks' crates/wcore-sandbox/src/directory_authority_archive.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'use std::time::Duration;' crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs)" = "1" && test "$(/usr/bin/grep -cF 'use crate::ResourceLimitEnforcement;' crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs)" = "1" && test "$(/usr/bin/grep -cF 'use crate::backends::SandboxBackend;' crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/git diff --name-only a1af7707 -- crates/wcore-sandbox/src/ | /usr/bin/grep -c .)" -ge "4"</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox && /usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "git fetch --all --prune 2>&1"; cmd /c "git checkout --detach '"$SHA"' 2>&1"; cmd /c "git rev-parse HEAD"; cmd /c "cargo check -p wcore-sandbox --all-targets 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-sandbox --no-fail-fast 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
  </verify>
  <done>`cargo check -p wcore-sandbox --all-targets` compiles on Windows with zero errors, for the first time since `8738b24e`. All four Group A items plus the struct field are `pub(super)` and nothing became `pub` or `pub(crate)`; Group B's extension-trait import is in scope at the call site; Group C's three imports extend the existing compile-debt block with the trait-in-scope one annotated; Group D uses the windows-sys field spelling the rest of the workspace already uses, with the never-compiled-guard fact recorded in-source. `cargo nextest run -p wcore-sandbox --no-fail-fast` has been run and its FULL result reported, with every failure named, root-caused, and either fixed here or explicitly escalated — none ignored, re-gated or assertion-weakened. `cargo fmt --all -- --check` is clean and the task is one atomic commit staging only its four source paths.</done>
</task>

<task type="auto">
  <name>Task 3: Workstream B — grant the unknown-kind mutate open the rights its own recursion requires, and prove it with a regression test</name>
  <files>crates/wcore-sandbox/src/directory_authority_windows.rs, crates/wcore-sandbox/src/directory_authority_windows_tests.rs</files>
  <read_first>crates/wcore-sandbox/src/directory_authority_windows.rs (`remove_descendants` in full, including the child open, the reparse refusal, the identity read and the recursion; `remove_open_dir_all`; `delete_open_object`; `open_relative` in full, especially the `(kind, intent)` access match, the type-options match, the attributes match and the disposition match; the `RelativeKind` and `RelativeIntent` enums), crates/wcore-sandbox/src/directory_authority_windows_tests.rs (the fixture idiom and the tests nearest the destructive-removal surface, at their post-Task-2 state)</read_first>
  <behavior>
    - Destructive removal succeeds through a directory child: a retained authority whose directory contains an empty directory child can be removed, where today that fails with an access-denied error.
    - Exactly one (kind, intent) pair changes. Every other pair's access mask is byte-identical, and the share mode, the disposition selection, the create-options word and the type-options selection are all untouched.
    - The four named `wcore-swarm` tests that failed with os error 5 now pass, and any test that changes state in the other direction is reported rather than absorbed.
    - A regression test fails if anyone trims the write bit back off the unknown-kind mutate open.
    - The reason the write bit is required — the recursion terminates in a directory flush that demands it — is recorded at the arm itself, not left to be rediscovered by the next person to read a probe transcript.
  </behavior>
  <action>In `open_relative`'s `(kind, intent)` access match, SPLIT the arm that currently pairs the File kind and the Any kind with the Mutate intent into two arms.

The File-and-Mutate arm keeps `FILE_GENERIC_READ | DELETE | SYNCHRONIZE` exactly as it reads today. It has no caller — grep the crate to confirm before you change anything, since the only Mutate call site is the unknown-kind one in `remove_descendants` — and keeping it narrow costs nothing while preventing a future caller from silently inheriting a widened grant.

The Any-and-Mutate arm becomes `FILE_GENERIC_READ | FILE_GENERIC_WRITE | DELETE | SYNCHRONIZE`, byte-identical to the arm that pairs the Directory kind with Create-or-Mutate. Place the two new arms where the single arm sits today so the match's ordering and readability are unchanged.

Record the argument at the new arm, because it is the whole justification for the grant. It must state: the unknown kind means the object's type is not knowable until the handle exists, so the grant must cover every kind the handle can turn out to be, and the union over the file and directory Mutate rights is exactly the directory arm's set — this receives the union and nothing beyond it. It must also state the mechanical requirement: `remove_descendants` wraps a directory child in an authority and the recursion terminates in `sync_all` on that handle, which is `FlushFileBuffers`, which demands write access — so the write bit here is load-bearing and trimming it back reintroduces an access-denied failure on every directory child. Finally record the accepted cost so it is visible at review time: a file child opened through this path carries write access it never exercises, and cleanup of such a file now additionally requires its other openers to have granted share-write, so a file held elsewhere with only share-read plus share-delete that was previously deletable will now be refused.

Do NOT change the share mode, the file-attributes match, the type-options match, the disposition match, or the create-options word. Do NOT touch the lock-target arms that 20-73 added. Do NOT alter `remove_descendants` itself — no re-open, no second name resolution, no restructuring — and do NOT remove or relocate the trailing `sync_all`: dropping that flush to satisfy an access check would delete the crate's documented Windows durability boundary in order to make a test pass, which is forbidden.

Add ONE Windows regression test in `directory_authority_windows_tests.rs`. It builds a temporary directory, creates an EMPTY DIRECTORY child inside it (that is the minimal reproducer the bisecting probe identified — an empty root and a lone file child both already succeed), opens a `DirectoryAuthority` on the root, and asserts that destructive removal succeeds and the root is gone. Give it a doc comment naming what it guards: that the unknown-kind mutate open must carry write access because the recursion flushes the directory handle, and that this test fails the moment that bit is trimmed. Keep it Windows-only and say why in the comment — unix removal goes through `unlinkat` relative to the descriptor and has no per-handle access mask, so the arm being fixed has no unix analogue and a cross-platform test would prove nothing off Windows. Assert on the invariant (removal succeeds, the directory is gone), never on an OS error code.

Commit this task alone, push, and measure on the box: `cargo check -p wcore-sandbox -p wcore-swarm --all-targets`, then `cargo nextest run -p wcore-sandbox --no-fail-fast`, then `cargo nextest run -p wcore-swarm --no-fail-fast`. The four named os-error-5 tests must pass. Attribute every remaining failure by name. Watch specifically for the accepted-cost signature — a cleanup failing with a sharing violation where it previously succeeded — and if one appears, report it as caused by this change rather than filing it under the pre-existing set.

Implements REQ-native-r4 and REQ-native-r6; records evidence for REQ-native-r12; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF '(RelativeKind::File | RelativeKind::Any, RelativeIntent::Mutate)' crates/wcore-sandbox/src/directory_authority_windows.rs)" = "0"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF '(RelativeKind::Any, RelativeIntent::Mutate)' crates/wcore-sandbox/src/directory_authority_windows.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF '(RelativeKind::File, RelativeIntent::Mutate)' crates/wcore-sandbox/src/directory_authority_windows.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -v '^[[:space:]]*//' crates/wcore-sandbox/src/directory_authority_windows.rs | /usr/bin/grep -cF 'FILE_GENERIC_READ | FILE_GENERIC_WRITE | DELETE | SYNCHRONIZE')" = "2"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'access_mode(GENERIC_READ | GENERIC_WRITE | DELETE)' crates/wcore-sandbox/src/directory_authority_windows.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -v '^[[:space:]]*//' crates/wcore-sandbox/src/directory_authority_windows.rs | /usr/bin/grep -cF 'authority.handle.sync_all()')" -ge "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && /usr/bin/git diff --stat HEAD~1 -- crates/wcore-sandbox/src/</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox && /usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "git fetch --all --prune 2>&1"; cmd /c "git checkout --detach '"$SHA"' 2>&1"; cmd /c "git rev-parse HEAD"; cmd /c "cargo check -p wcore-sandbox -p wcore-swarm --all-targets 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-sandbox --no-fail-fast 2>&1"; cmd /c "cargo nextest run -p wcore-swarm --no-fail-fast 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
  </verify>
  <done>Exactly one (kind, intent) pair changed: the combined file-or-unknown Mutate arm is split, the file pairing keeps its original read-plus-delete rights, and the unknown pairing now carries the same read-write-delete union the directory Mutate arm carries — with the flush requirement, the union argument and the accepted sharing-violation cost all recorded at the arm. The mutating `open_directory` access mode is still the file's only one, the share mode, dispositions, attributes, type options and create-options word are untouched, `remove_descendants` and its trailing flush are unchanged, and the 20-73 lock arms are untouched. One Windows-only regression test asserts destructive removal succeeds through an empty-directory child and states what trimming the write bit would break. On the box the three affected crates check clean and the four named os-error-5 tests pass, with every remaining failure attributed by name and any new sharing-violation signature reported as caused by this change. `cargo fmt --all -- --check` is clean; one atomic commit staging only the two source paths.</done>
</task>

<task type="auto">
  <name>Task 4: Workstream C — settle the checkout-authority chdir defect by measurement, then repair it without weakening anti-swap</name>
  <files>crates/wcore-swarm/src/worktree_manager.rs, crates/wcore-swarm/src/worktree_security.rs, crates/wcore-swarm/src/worktree_tests.rs, crates/wcore-sandbox/src/directory_authority.rs, crates/wcore-sandbox/src/directory_authority_windows.rs</files>
  <read_first>crates/wcore-swarm/src/worktree_manager.rs (`create_isolated_checkout_inner` and `create_integration_checkout` in full — every git invocation, the `normalized_root` calls, and the `TransactionWorkspaceAuthorities` construction where the checkout authority is opened; `validate_repo_authority`), crates/wcore-swarm/src/worktree_cleanup.rs (`git_command`, `checkout_git_command`, `run_checkout_git`, `checkout_git_stdout`), crates/wcore-swarm/src/worktree/parent.rs (`landing_git_command` and every call site that derives its checkout path from `checkout_authority().display_path()`), crates/wcore-swarm/src/worktree.rs (`TransactionCleanup` fields and `bind_checkout_authority`; `release`; `TransactionWorkspace::checkout_authority`, `validate_execution_authority`, `seal_candidate`, `logical_tree_bytes`), crates/wcore-swarm/src/heartbeat.rs (`read_status_authorized`), crates/wcore-swarm/src/worktree_paths.rs (the whole module — the representation contract this task must not re-open), crates/wcore-sandbox/src/directory_authority.rs (`open`, `open_observational`, `open_inner_with`, `read_child_bounded`, `open_child_directory`), crates/wcore-sandbox/src/directory_authority_windows.rs (`open_directory`, `open_directory_observational`, `open_relative`'s use of the parent handle as `RootDirectory`, `child_names`)</read_first>
  <behavior>
    - The failing git invocation is CAPTURED — which call site, which path string, which authorities were alive — rather than inferred from the error text.
    - The path-representation question is answered by a decisive probe with no handle held, so the verbatim prefix in the error message is established as causal or incidental instead of assumed either way.
    - The checkout authority's complete consumer set is re-enumerated from source and each consumer classified as identity, read or mutate, so the branch choice rests on what the authority actually does.
    - The chosen branch is one of the three specified in advance, chosen on evidence, with the evidence recorded.
    - Whatever branch is taken, the anti-swap guarantee does not weaken: the handle stays continuously held, or — if it cannot — identity is re-validated on every re-acquisition and that re-validation fails closed.
    - The 6 `wcore-agent` transactional delegated-mutation tests reach 9/9, and a regression test fails if a chdir-blocking access right ever returns to a directory git must enter.
    - `wcore-swarm` gains no new `#[cfg]`; any platform difference lives in `wcore-sandbox`.
  </behavior>
  <action>MEASURE FIRST. Nothing in this task may be implemented before all four determinations below are recorded.

DETERMINATION 1 — capture the failure. On the box, run the failing `wcore-agent` tests with output captured and identify: the exact git argv, the exact path string handed to `-C`, the call site (`checkout_git_command` versus `landing_git_command`), and which authorities were alive at that moment. Note that in both checkout constructors the checkout authority is opened AFTER the construction-time git calls, so a construction-time failure and a landing-time failure have different held-handle sets and different fixes — establish which one you are looking at.

DETERMINATION 2 — the path-representation probe, which is decisive and cheap. On the box, with NO handle held by our code, run the same git subcommand twice against the same repository: once with `-C` given the verbatim path form and once with the plain drive-letter form. If the verbatim form fails and the plain form succeeds, representation is causal or co-causal and the repair belongs in the path derivation. If both succeed, representation is incidental and the failure is the access bit. Record the raw output of both. Whichever way it falls, `worktree_paths.rs::normalized_root` remains the ONLY site in the crate allowed to name the de-verbatimizing call — 20-71 settled that and it is not re-opened here. If the strip was refused for this path, the fix is to understand WHY `dunce` refused (its refusal conditions are documented in that module's header) and address that, never to sprinkle a second strip.

DETERMINATION 3 — re-enumerate the checkout authority's consumers from source and classify each. The prediction to confirm or refute: the complete set is the four `dispatch.rs` heartbeat reads (a relative child read), the recursive read-only tree walk in `logical_tree_bytes`, the `validate_path` identity check inside `validate_execution_authority`, the manifest read inside `seal_candidate`, the loan-count check inside `TransactionCleanup::release`, and four `display_path()` reads in `parent.rs` — with NOTHING deleting, renaming, creating a child, or flushing through the checkout authority, and with the transaction root's destruction running through the root authority's own recursion. Confirm each by grep; if you find even one mutating consumer, the prediction is refuted and branch (i) is off the table.

DETERMINATION 4 — test the falsifier for branch (i). `open_observational` opens with read-only access, and `FILE_GENERIC_READ` carries list-directory but not traverse. Unlike the repo authority that 20-72 converted — which is only ever a `validate_path` witness — the checkout authority is used as the `RootDirectory` for relative child opens and for directory enumeration. Traverse checking is normally bypassed because the bypass privilege is granted to Everyone by default, but that is an assumption and exactly the kind a restricted token invalidates, so 20-72 supplies no evidence here. Prove it directly: with a checkout authority opened observationally, exercise a relative child read and a directory enumeration through it and confirm both succeed.

THEN CHOOSE, from the three branches specified in advance, and record which and why.

BRANCH (i) — OBSERVATIONAL OPEN. Take this if determination 3 confirms no mutating consumer and determination 4 clears. Convert both `DirectoryAuthority::open(&checkout)` sites — the one in `create_isolated_checkout_inner` and the one in `create_integration_checkout` — to `open_observational`, which already exists on the sandbox type and on the `wcore-swarm` wrapper, so no new API is needed and no `#[cfg]` enters `wcore-swarm`. This is preferred because it removes a capability the authority never exercises while the handle stays CONTINUOUSLY held, so the anti-swap property is byte-for-byte what it is today — the same argument 20-72 made for the repo authority. Record at the acquisition: which consumers justify the access level, that no consumer mutates, that the handle is still retained so identity still survives a rename, and that widening it back reintroduces the chdir refusal for every git invocation scoped to that checkout — including, note explicitly, invocations made by CHILD processes, because the sharing conflict is system-wide and a handle held in the manager process denies a worker process's chdir just as effectively.

BRANCH (ii) — LIFETIME SCOPING. Take this ONLY if determination 3 finds a genuine mutating consumer, i.e. the handle really needs delete. Open late or release before each chdir-ing git invocation, then re-acquire and re-prove identity against the retained token. If you take this branch you MUST argue the cost in-source and in the SUMMARY: it converts a guarantee that no window exists into a guarantee that a window exists but is detected. The re-validation must fail closed and must compare against the identity captured before the release, not against a freshly derived one.

BRANCH (iii) — HYBRID. Take this if determination 3 clears but determination 4 fails, i.e. the relative opens need more than read-only. Keep the continuously held handle and define a third directory access profile in `wcore-sandbox` that grants read, list and traverse but NOT delete, surfaced through the `wcore-swarm` wrapper exactly as `open_observational` is, so `wcore-swarm` still gains no `#[cfg]`. Document it beside `open_directory_observational` with the same discipline: what it grants, what it deliberately withholds, and the measured reason.

In every branch, and regardless of which one you take, annotate BOTH checkout-authority acquisition sites — the one in `create_isolated_checkout_inner` and the one in `create_integration_checkout` — with a comment that begins with the exact marker text `workstream C branch` followed by which branch was taken and the measured evidence for it. Two sites, two markers, so a later reader finds the rationale at the code and a gate can prove both sites were reasoned about rather than one being changed by pattern-matching.

In every branch: do NOT convert the scratch authority, the transaction-root authority, the swarm authority or the quarantine authority — none of them is a git working directory and none is in this defect's trace. Do NOT change `open_directory`'s mutating access mode. Do NOT alter `validate_path`, the identity derivation, or any refusal message.

Add ONE regression test in `crates/wcore-swarm/src/worktree_tests.rs`, named exactly `checkout_git_chdir_succeeds_while_workspace_authorities_are_alive`, that asserts the INVARIANT and not the incidental error string: a chdir-requiring git subcommand scoped to a checkout still succeeds while the workspace's authorities are alive. Model it on the regression test 20-72 added for the repository root — same shape, same reasoning, different directory — so the pair reads as one family. It must fail before this task and pass after, and it must fail again if a chdir-blocking access right ever returns to a directory git must enter.

Commit this task alone, push, and measure: `cargo check -p wcore-sandbox -p wcore-swarm -p wcore-agent --all-targets`, then `cargo nextest run -p wcore-agent --test transactional_delegated_mutation_test --run-ignored all` requiring 9/9, then `cargo nextest run -p wcore-swarm --no-fail-fast`. Attribute every remaining failure by name.

Implements REQ-native-r6 and REQ-native-r4; records evidence for REQ-native-r12; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'dunce::simplified' crates/wcore-swarm/src/worktree_paths.rs)" = "1" && test "$(/usr/bin/grep -rlF 'dunce::simplified' crates/wcore-swarm/src/ | /usr/bin/grep -c .)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'workstream C branch' crates/wcore-swarm/src/worktree_manager.rs)" = "2"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'access_mode(GENERIC_READ | GENERIC_WRITE | DELETE)' crates/wcore-sandbox/src/directory_authority_windows.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'fn checkout_git_chdir_succeeds_while_workspace_authorities_are_alive' crates/wcore-swarm/src/worktree_tests.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'cfg(windows)' crates/wcore-swarm/src/worktree_manager.rs)" = "$(/usr/bin/git show a1af7707:crates/wcore-swarm/src/worktree_manager.rs | /usr/bin/grep -cF 'cfg(windows)')"</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox && /usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "git fetch --all --prune 2>&1"; cmd /c "git checkout --detach '"$SHA"' 2>&1"; cmd /c "git rev-parse HEAD"; cmd /c "cargo check -p wcore-sandbox -p wcore-swarm -p wcore-agent --all-targets 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-agent --test transactional_delegated_mutation_test --run-ignored all 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-swarm --no-fail-fast 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
  </verify>
  <done>All four determinations were MEASURED and recorded before any code changed: the failing invocation captured with its call site, path string and live-authority set; the path-representation question settled by a no-handle probe whose raw output is recorded; the checkout authority's consumer set re-enumerated from source with each consumer classified; and branch (i)'s traverse falsifier tested directly rather than inherited from 20-72. One of the three pre-specified branches was taken, on that evidence, with the choice and its rationale recorded at both acquisition sites (each carrying the branch marker) and in the SUMMARY. The anti-swap guarantee did not weaken: the handle is continuously held, or — if branch (ii) was taken — identity is re-validated against the pre-release token on every re-acquisition and fails closed, and that cost is argued in-source. `worktree_paths.rs` is still the crate's only site naming the de-verbatimizing call. The mutating `open_directory` access mode is unchanged and no other authority was converted. One regression test asserts a chdir-requiring git subcommand still succeeds inside a checkout while the workspace authorities are alive, failing before this task and after any reintroduction of a chdir-blocking right. On the box, the three crates check clean and `transactional_delegated_mutation_test --run-ignored all` is 9/9, with every remaining `wcore-swarm` failure attributed by name. `wcore-swarm`'s `cfg(windows)` count in the touched file is unchanged from `a1af7707`. `cargo fmt --all -- --check` is clean; one atomic commit.</done>
</task>

<task type="auto">
  <name>Task 5: Workstream A — raise the bounded stack on both the test harness and the runtime workers, then re-measure the five aborts and classify what remains</name>
  <files>.config/nextest.toml, crates/wcore-cli/src/main.rs</files>
  <read_first>.config/nextest.toml (the whole file — the profile layout, the existing per-test override blocks and the `[test-groups]` section, so the new table is placed consistently), crates/wcore-cli/src/main.rs (the entry-thread block: the `ENTRY_STACK_SIZE` constant, its comment, the `std::thread::Builder` and the `tokio::runtime::Builder::new_multi_thread()` chain built inside it), justfile (`test:`, `test-ci:`, `push`), crates/wcore-swarm/src/lib.rs (the `futs.push(Box::pin(dispatch::run_worker(..)))` loop and its existing heap-pinning comment), crates/wcore-swarm/src/worktree_manager.rs (`create_isolated_checkout` and the comment above `create_isolated_checkout_inner`), crates/wcore-swarm/src/worktree_tests/linux.rs (the existing explicit `.stack_size(..)` scenario thread — the in-repo precedent)</read_first>
  <behavior>
    - Test processes run with a libtest thread stack large enough for the measured bounded requirement, applied through configuration rather than through a per-invocation environment variable a human must remember to set correctly.
    - The value is proven to have taken effect by observing what the program parses, not by observing that an assignment command succeeded.
    - The chosen value is large enough for the bounded chain and small enough that a genuinely unbounded recursion still overflows, so raising it cannot mask a runaway.
    - Tokio worker threads in the shipped binary get the same headroom the entry thread already documents, and the write-up says plainly that this is defense-in-depth against a measured-narrow margin and not a reproduced production crash.
    - The five aborting tests are re-measured after the change and each is classified: passing, or failing with a named new cause. Neither outcome is assumed in advance.
    - The `cargo test` coverage gap is stated with its justification rather than left implicit.
  </behavior>
  <action>TEST SIDE. Add a top-level `[env]` table to `.config/nextest.toml` setting `RUST_MIN_STACK` to `8388608`. Place it near the top of the file, above the profile definitions, so it reads as a file-wide setting rather than a profile detail — it must apply to `default`, `ci`, `e2e` and `eval` alike. Confirm on the box that the value actually reaches the test process; if the ambient environment shadows the config value, use nextest's forced form instead, and record which form was needed and why. Do NOT take "the config file contains the value" as proof; the proof is the number `std::thread::min_stack()` returns inside a test process, observed through the same throwaway probe Task 1 established and then deleted.

Comment the table so it cannot be trimmed or mis-set later. It must record: that a stack-size sweep on the full suite measured 2 MiB producing aborts and 2.25 MiB producing none, so the requirement is BOUNDED at roughly 256 KiB above the default and this is not unbounded recursion; that the overflowing thread is the libtest-spawned test thread, which takes its size from this variable because the tests are current-thread `#[tokio::test]`s driven by `block_on` on that same thread; that 8 MiB is chosen to match the unix main-thread default and to leave the guard meaningful, since a genuinely unbounded recursion still overflows at 8 MiB and raising this to 32 MiB or beyond would begin masking runaways; and that this does NOT cover bare `cargo test`, which is acceptable because every gate path routes through nextest — verify that last claim against the justfile before writing it, and if the justfile has changed, write what is actually true. Also record why `.cargo/config.toml` was not used instead: its `[env]` table would set the variable for every cargo-driven process in the workspace, including build scripts and `cargo run` and the CLI binary itself, which is a far wider blast radius than the test harness for the sake of covering a non-gate path.

PRODUCTION SIDE. In `crates/wcore-cli/src/main.rs`, add `.thread_stack_size(8 * 1024 * 1024)` to the `tokio::runtime::Builder::new_multi_thread()` chain built inside the entry thread. Leave `ENTRY_STACK_SIZE`, its comment and the entry thread itself exactly as they are.

Write the comment with deliberate honesty, because the temptation here is to overstate. It must say: the entry thread already carries a large explicit stack, but the runtime built inside it spawns worker threads at the platform default, so every `tokio::spawn`ed task runs on a 2 MiB stack on Windows; `create_isolated_checkout` is reached in production from the agent's spawner, its durable-launch path, the anvil forge and the child-transaction gates, and it drives the same chain of large futures that overflows in tests; the production path was NOT observed to overflow, and the measured headroom on the nearest path is under 256 KiB, so this is defense-in-depth against a narrow measured margin and NOT a reproduced production crash; and the cost is virtual address-space RESERVE rather than committed memory, which is negligible on 64-bit. Do not write it as a bug fix and do not write it up as one in the SUMMARY.

While you are here, grep the workspace for other production `new_multi_thread()` builders and check whether any of them is on the `create_isolated_checkout` path. REPORT what you find; do NOT change them in this task. Widening this repair beyond the CLI entry point is out of scope and would be a drive-by.

Commit this task alone, push, and re-measure on the box. Run `cargo nextest run -p wcore-swarm --no-fail-fast` and examine the five named tests individually. Classify each: passing now, or failing with a NEW named cause. At `185d9612` these same five degraded into err-87 failures once the stack was raised, and err-87 is now gone, so passing outright is plausible — but so is another layer, and neither may be assumed. If any still aborts with the stack-overflow status, the fix did not take effect and you must re-check the observed `min_stack()` value before hypothesizing anything about the code.

Implements REQ-native-r4; records evidence for REQ-native-r6 and REQ-native-r12; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cE '^\[env\]' .config/nextest.toml)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cE '^RUST_MIN_STACK' .config/nextest.toml)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF '8388608' .config/nextest.toml)" -ge "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF '.thread_stack_size(8 * 1024 * 1024)' crates/wcore-cli/src/main.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'ENTRY_STACK_SIZE: usize = 32 * 1024 * 1024' crates/wcore-cli/src/main.rs)" = "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF '[env]' .cargo/config.toml)" = "0"</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox && /usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "git fetch --all --prune 2>&1"; cmd /c "git checkout --detach '"$SHA"' 2>&1"; cmd /c "git rev-parse HEAD"; cmd /c "cargo nextest run -p wcore-swarm --no-fail-fast 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-swarm --test dispatch_smoke --no-fail-fast 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
  </verify>
  <done>`.config/nextest.toml` carries one top-level `[env]` table setting `RUST_MIN_STACK` to 8388608, documented with the bounded-requirement measurement, the reason the libtest thread is the one that matters, the value rationale (large enough for the chain, small enough that an unbounded recursion still overflows), the `cargo test` coverage gap with its justification re-verified against the justfile, and the reason `.cargo/config.toml` was rejected. The value is PROVEN to have reached the test process by an observed `min_stack()` reading, not by a successful assignment, and the form that was needed is recorded. `crates/wcore-cli/src/main.rs` adds `.thread_stack_size(8 * 1024 * 1024)` to the multi-thread runtime with the entry thread and its constant untouched, commented honestly as defense-in-depth against a measured-narrow margin rather than a reproduced crash. Other production runtime builders were surveyed and REPORTED, not changed. All five previously aborting tests were re-measured and each classified as passing or as failing with a named new cause; nothing was assumed. `cargo fmt --all -- --check` is clean; one atomic commit staging only the two paths.</done>
</task>

<task type="auto">
  <name>Task 6: Workstream E — restore the workspace lint gate properly, fixing every warning rather than silencing it</name>
  <files>crates/wcore-sandbox/src/directory_authority.rs, crates/wcore-sandbox/src/directory_authority_windows_tests.rs, crates/wcore-sandbox/src/directory_authority_archive.rs, crates/wcore-sandbox/tests/hard_process_containment_windows.rs, crates/wcore-swarm/src/worktree_security.rs, crates/wcore-swarm/src/worktree_cleanup.rs, crates/wcore-swarm/tests/worker_runtime_limits.rs</files>
  <read_first>justfile (`lint:` and `lint-fix:`, and the `[windows]` `fmt-check` override that has no `lint` counterpart), .github/workflows/ci.yml (the self-hosted Windows job's Clippy step), crates/wcore-sandbox/tests/hard_process_containment_windows.rs (the nested `unsafe fn spawn_suspended` and every unsafe operation inside it), crates/wcore-sandbox/src/directory_authority.rs (the two functions clippy reports unreachable expressions in — read the whole cfg cascade around them, not just the reported lines), crates/wcore-sandbox/src/directory_authority_archive.rs (the two never-used methods and their cfg gating), crates/wcore-swarm/src/worktree_security.rs (`rename_into` and every reference to it), crates/wcore-swarm/src/worktree_cleanup.rs (`set_ambient_git_env`, its `#[cfg(test)]` fields and every reference to it)</read_first>
  <behavior>
    - `cargo clippy --workspace --all-targets -- -D warnings` runs to completion on Windows and is clean — the first time it has been reachable since the compile debt was introduced.
    - Every warning is fixed at its cause. Genuinely dead code is removed; unsafe operations get explicit blocks with SAFETY comments; unused imports go; a lint that reveals a real bug is treated as a bug and reported.
    - No blanket suppression is used as a silencing device. A narrowly scoped one is permitted only for a genuine platform-conditional false positive, and every one added is listed with its justification.
    - Dead-code findings are INVESTIGATED before deletion: a symbol that looks dead on Windows because its only caller sits behind a `#[cfg(test)]` or a platform gate is not dead, and deleting it would break the other configuration.
    - The Linux and macOS lint results are unchanged — nothing here may trade a Windows warning for a warning elsewhere.
  </behavior>
  <action>Start by MEASURING, not by working from the list below. Run `cargo clippy --workspace --all-targets -- -D warnings` on the box against the post-Task-5 tree and capture the full warning set. The list this plan carries was measured at a different point and the preceding four tasks have changed the tree; treat it as a starting inventory to reconcile against, and report any warning present in one and absent in the other.

The expected inventory, each to be fixed at its cause: an unused rename-info import in `directory_authority_windows_tests.rs` — check first that Task 2's visibility work did not make it genuinely needed, then remove the import AND update the header comment that exists solely to explain which namespace it comes from, so no stale explanation of a deleted import is left behind; two unreachable expressions in the `directory_authority.rs` LIB — these are pre-existing and in library code, so read the surrounding cfg cascade and establish WHY the expression is unreachable before touching it, because an unreachable expression in a platform-conditional fallback can be the visible symptom of a branch that can never be taken and that is a bug, not a lint; four `unsafe_op_in_unsafe_fn` findings inside the nested `unsafe fn spawn_suspended` in `tests/hard_process_containment_windows.rs` — wrap each operation in an explicit `unsafe {}` block with a SAFETY comment naming the invariant that makes it sound, exactly as the workspace does elsewhere, and do not reach for an allow attribute; two never-used methods in `directory_authority_archive.rs` — determine whether they are used under `--features live-docker` (which, per Task 2, has never built on Windows, so "never used" here may simply mean "never compiled with its caller") before removing anything; dead-code `rename_into` in `worktree_security.rs` and `set_ambient_git_env` in `worktree_cleanup.rs` — the latter is `pub(super)` and its companion state is `#[cfg(test)]`-gated, so establish which configurations reference it before deleting it; an unused `Arc` import in `tests/worker_runtime_limits.rs` — remove it; an `is_multiple_of` lint and a collapsible-`if` in the bwrap backend — apply the idiomatic rewrite clippy suggests, and confirm the rewrite preserves the exact condition semantics rather than trusting the autofix.

RULES that override any convenience here. A blanket suppression is forbidden as a silencing device. If a lint is a genuine platform-conditional false positive, a narrowly scoped attribute with a comment naming why is acceptable — and every single one added must appear in the SUMMARY with its justification, so the count is visible and reviewable. If a warning turns out to describe a real defect, STOP treating it as lint work: report it, and either fix it as a defect with its own reasoning or escalate it. Do not run `just lint-fix` and accept the result unread — `cargo clippy --fix` will happily rewrite code you have not examined, and this crate set includes raw-pointer parsing and FFI-adjacent unsafe code where an automated rewrite is exactly the wrong tool. Read every hunk it would produce, or make the edits by hand.

Note the plan's own scope-fence conflict and resolve it explicitly: 20-73 declared `tests/hard_process_containment_windows.rs` another plan's surface. THIS is that plan, and it touches that file for the four `unsafe_op_in_unsafe_fn` fixes and nothing else — no assertion, no fixture, no test selector and no timing changes there.

Then act on the CI status Task 1 reported. If the self-hosted Windows job has been red on this branch, say so plainly in the SUMMARY with the run id and the failing step. If it has simply never run these commits, say that instead. Either way the statement must be sourced from the `gh` output, not inferred.

Commit this task alone, push, and confirm on the box that `cargo clippy --workspace --all-targets -- -D warnings` exits clean and `cargo fmt --all --check` is clean.

Implements REQ-native-r4 and REQ-native-r12; records evidence for REQ-native-r6; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'use windows_sys::Win32::Storage::FileSystem::FILE_RENAME_INFO;' crates/wcore-sandbox/src/directory_authority_windows_tests.rs)" = "0"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'use std::sync::Arc;' crates/wcore-swarm/tests/worker_runtime_limits.rs)" = "0"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'SAFETY' crates/wcore-sandbox/tests/hard_process_containment_windows.rs)" -ge "4"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'allow(unsafe_op_in_unsafe_fn)' crates/wcore-sandbox/tests/hard_process_containment_windows.rs)" = "0"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && test "$(/usr/bin/grep -cF 'allow(dead_code)' crates/wcore-swarm/src/worktree_security.rs)" = "0" && test "$(/usr/bin/grep -cF 'allow(dead_code)' crates/wcore-swarm/src/worktree_cleanup.rs)" = "0"</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox && /usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "git fetch --all --prune 2>&1"; cmd /c "git checkout --detach '"$SHA"' 2>&1"; cmd /c "git rev-parse HEAD"; cmd /c "cargo clippy --workspace --all-targets -- -D warnings 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo fmt --all --check 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
  </verify>
  <done>`cargo clippy --workspace --all-targets -- -D warnings` exits clean on Windows for the first time since the compile debt was introduced, and `cargo fmt --all --check` is clean there too. The measured warning set was reconciled against this plan's inventory with any difference reported. Every warning was fixed at its cause: unsafe operations carry explicit blocks with SAFETY comments and no suppression attribute; unused imports are gone; each dead-code finding was investigated across configurations before anything was deleted; the unreachable expressions were understood before being touched, and anything that turned out to be a real defect was reported as one. Every narrowly scoped suppression added — if any — is listed in the SUMMARY with its justification. `tests/hard_process_containment_windows.rs` was touched only for the four unsafe-block fixes. The real CI status of the self-hosted Windows job is stated from the `gh` output rather than inferred. One atomic commit.</done>
</task>

<task type="auto">
  <name>Task 7: Final verification gate on SEANDESKTOP, then prepare the Sean-gated 6-target native re-dispatch</name>
  <files>None (verification and dispatch preparation; commits only the SUMMARY)</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-NATIVE-REPAIR-PRD.md (D5, D6, D7 — the hardware rule, the Sean-only dispatch gate, and the no-inspection-claims rule), scripts/f20-native-windows-proof.ps1 (the 6 targets and what each requires), .github/workflows/nightly-windows-soak.yml (the `f20_candidate`, `f20_request_nonce` and `f20_macos_runner_label` dispatch inputs and the jobs they gate)</read_first>
  <behavior>
    - Every gate runs on SEANDESKTOP against the FINAL commit, in a fixed order, stopping at the first failure and repairing before advancing — a later suite's green is meaningless if an earlier one is red.
    - The zero-failure claim is stated as a delta against the baseline Task 1 MEASURED, never against any number written in this plan.
    - The `wcore-sandbox` suite result is REPORTED in full, including any newly surfaced failure, because that crate's tests have never run on Windows and this is the first data point that exists.
    - The 6-target native proof is PREPARED with its exact command, ref and nonce stated, and is not fired without Sean's explicit authorization, per PRD D6.
    - The three already-green targets are named, and the requirement that they must REMAIN green is stated as an acceptance condition, not as an expectation.
  </behavior>
  <action>Re-sync the box to the FINAL commit and confirm the printed SHA against the Mac's `HEAD`. Then run the gate in this exact order, stopping at the first failure and repairing before advancing.

(1) `cargo check -p wcore-sandbox -p wcore-swarm -p wcore-agent --all-targets` — must compile.
(2) `cargo nextest run -p wcore-swarm --no-fail-fast` — must reach ZERO failures. State the result as a delta against the baseline Task 1 measured. The run count rises by the regression tests Tasks 3 and 4 add.
(3) `cargo nextest run -p wcore-agent --test transactional_delegated_mutation_test --run-ignored all` — must reach 9/9.
(4) `cargo nextest run -p wcore-swarm --test dispatch_smoke --no-fail-fast` — must reach zero failures.
(5) `cargo nextest run -p wcore-sandbox --no-fail-fast` — REPORT the result. This crate's tests have never run on Windows; failures here are EXPECTED to be newly surfaced and every one must be reported, root-caused, and either fixed or explicitly escalated. Hiding one, ignoring one, or re-gating one to unix is forbidden and would invalidate the entire closeout claim.
(6) `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --all --check` — both must be clean.

If step 2 or step 4 shows a stack-overflow abort, return to Task 5's diagnosis and re-check the OBSERVED `min_stack()` value before hypothesizing about code — the trailing-space trap is the single most likely cause of a confusing result here and it has already fooled this phase once. If step 3 shows a chdir refusal, return to Task 4's four determinations rather than adjusting an assertion.

ONLY when steps 1-6 all pass, prepare the native re-dispatch. Derive the UAT ref and the request nonce from the phase's existing native-proof-dispatch flow rather than inventing them, and STATE both explicitly before doing anything. The command shape is `gh workflow run nightly-windows-soak.yml --ref <UAT> -f f20_candidate=true -f f20_request_nonce=<sha> -f f20_macos_runner_label=""`, with `gh auth switch --user FerroxLabs` first. Name the three targets that are already green — retained-handle, appcontainer-acl, and job-object — and state as an acceptance condition that they must REMAIN green, so a re-dispatch that turns any of them red is a regression this plan owns rather than a new finding.

PRD D6 makes the native-proof dispatch a SEAN-ONLY gate that is never inferred. Note the tension explicitly: the closeout brief asks for the re-dispatch, and the PRD reserves the trigger. Resolve it the only way that honors both — prepare the exact command, present it with the derived ref and nonce and the evidence that steps 1-6 are green, and WAIT for Sean's explicit authorization before firing. Do not fire it on your own initiative, and do not record the phase as closed on the strength of a dispatch that has not run.

Record in the SUMMARY: every count, the exact final SHA, the baseline it is a delta against, the wall clock, the workstream-C branch taken with its evidence, the full `wcore-sandbox` first-ever Windows result with each newly surfaced failure and its disposition, every narrowly scoped lint suppression added with its justification, the CI status found, and any residual failure with its cause. Mark no Phase 20 requirement complete — closure is claimed by the downstream native proof under the existing Sean gate.

Records evidence for REQ-native-r6, REQ-native-r4 and REQ-native-r12; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && cargo fmt --all -- --check</automated>
    <automated>SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox && /usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "git fetch --all --prune 2>&1"; cmd /c "git checkout --detach '"$SHA"' 2>&1"; cmd /c "git rev-parse HEAD" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo check -p wcore-sandbox -p wcore-swarm -p wcore-agent --all-targets 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-swarm --no-fail-fast 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-agent --test transactional_delegated_mutation_test --run-ignored all 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-swarm --test dispatch_smoke --no-fail-fast 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo nextest run -p wcore-sandbox --no-fail-fast 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
    <automated>ssh -o BatchMode=yes SeanD@seandesktop 'powershell -NoProfile -Command { Set-Location C:\ferrox-win; cmd /c "cargo clippy --workspace --all-targets -- -D warnings 2>&1"; cmd /c "cargo fmt --all --check 2>&1" }' | /usr/bin/grep -v CLIXML | /usr/bin/grep -v "^<Objs"</automated>
  </verify>
  <done>SEANDESKTOP is on the exact final SHA, confirmed against the Mac's HEAD. All six gates ran in order: the three crates check clean with `--all-targets`; `wcore-swarm` has ZERO failures stated as a delta against the baseline Task 1 measured; `transactional_delegated_mutation_test --run-ignored all` is 9/9; `dispatch_smoke` has zero failures; the `wcore-sandbox` suite's first-ever Windows result is reported in full with every newly surfaced failure named, root-caused and either fixed or explicitly escalated; and clippy plus fmt are clean. The 6-target native re-dispatch is PREPARED with its derived ref and nonce stated, the three already-green targets named with the requirement that they remain green, and it awaits Sean's explicit authorization per PRD D6 rather than having been fired. Every count, the SHA, the measured baseline, the wall clock, the workstream-C branch and its evidence, every lint suppression added, and the CI status found are recorded in the SUMMARY. No Phase 20 requirement is marked complete.</done>
</task>

</tasks>

## What this plan does NOT change (scope fence)

- **The 20-73 advisory-lock repair — untouched.** `open_child_lock_file`, `open_or_create_child_lock_file`, the two `LockOpen`/`LockOpenOrCreate` relative-intent arms, `SWARM_LOCK_FILE`, `swarm_lock_handle`, `transaction_lease_handle` and all eight retargeted lock sites stay exactly as they are. Workstream B splits a DIFFERENT arm; it must not perturb the lock arms' access mask, their refusal of directory targets, or their disposition selection.
- **The 20-72 observational repair — consumed, not revisited.** `open_directory_observational`, `open_inner_with`, `open_observational` and the repo-root conversion are hardware-proven and are the mechanism workstream C builds on. This plan does not re-derive the DELETE-bit finding, does not alter the repo-root conversion, and does not change the Windows path-identity probe's routing.
- **The 20-71 path representation — not re-opened.** `worktree_paths.rs::normalized_root` remains the crate's ONE derivation and the ONLY site allowed to name the de-verbatimizing call. Workstream C answers whether the verbatim prefix is causal, but it may not add a second strip anywhere, may not change the conditional-refusal semantics, and may not weaken any comparison that depends on both operands sharing the one derivation.
- **The shared mutating `open_directory` — untouched.** Its `GENERIC_READ | GENERIC_WRITE | DELETE` access mode, its share mode and its durability comment stay verbatim, and the file still contains exactly one such line. `swarm_root`, the transaction roots, the scratch authority, the control directory and the quarantine authority keep the access they have today.
- **`remove_descendants`, `remove_open_dir_all` and `delete_open_object` — structurally unchanged.** Workstream B changes only which rights the child open requests. No re-open, no second name resolution, no restructuring, and above all no removal or relocation of the trailing `sync_all` — dropping that flush to satisfy an access check would delete the crate's documented Windows durability boundary in order to make a test pass.
- **The precise-kind refactor of the cleanup child open — deliberately DEFERRED, not forgotten.** Carrying directory-ness out of `parse_directory_entries` and eliminating the unknown kind from the mutate path is the correct long-term answer, and it is recorded as the follow-up. It is not done here because it would change the raw-pointer directory parser in the same commit that first compiles that parser's tests.
- **Every other `RelativeIntent` arm and every other authority's access profile — untouched.** Exactly one (kind, intent) pair changes in workstream B, and exactly the checkout authority changes in workstream C. Scratch, transaction-root, swarm, control and quarantine authorities are all outside this plan's trace.
- **The entry-thread stack and its comment — untouched.** `ENTRY_STACK_SIZE` stays at 32 MiB on the named entry thread. Workstream A adds a worker-thread size to the runtime built inside it and changes nothing else in that block.
- **Other production runtime builders — surveyed and REPORTED, never modified.** Widening the stack repair beyond the CLI entry point is out of scope.
- **The runner-proven commits — not revisited.** `00f453f2`, `aaeeb379`, `0e867008`, `ece09681..919629ee`, `fac01305..185d9612` and `44190aa6..a1af7707` stay as they are.
- **`crates/wcore-sandbox/tests/live_fs_acl.rs` — not touched.** `tests/hard_process_containment_windows.rs` IS touched, but only for the four `unsafe_op_in_unsafe_fn` fixes in workstream E: no assertion, fixture, selector, timing or containment-count change there.
- **No dependency, `Cargo.toml` or `Cargo.lock` change**, no new crate, no new module, and no change to any nextest profile's retries, timeouts, test-threads or per-test overrides — workstream A adds a table, it does not tune the existing ones.
- **No test is deleted, weakened, re-gated, ignored or made conditional to reach the zero-failure target**, and no lint is silenced with a blanket suppression. A red that cannot be fixed honestly is REPORTED, not engineered away.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retained directory handle ← handle-relative cleanup | `remove_descendants` walks and destroys through the retained handle; the rights it requests determine both what it can destroy and what it can be blocked from destroying |
| raw NT directory buffer ← `parse_directory_entries` | A raw-pointer walk over kernel-filled `FILE_ID_BOTH_DIR_INFORMATION` records with caller-supplied offsets; its overflow guard is the only thing between a malformed length and an out-of-bounds read |
| child name ← `validate_windows_child_name` | Every handle-relative open funnels through this refusal; it is the boundary that keeps a traversal or device name out of the relative-open path |
| archive export ← hard-link guard | Export must refuse a hard-linked file so a tree cannot smuggle a second name for content outside its own bounds |
| checkout directory ← retained authority access rights | The access rights on a retained handle decide whether git — in this process or a child — can make that directory its working directory at all |
| git working directory ← in-process chdir vs process spawn | `CreateProcess` with an explicit current directory succeeds where in-process `SetCurrentDirectory` is refused, so the same held handle breaks one and not the other |
| test-thread stack ← env-var parsing | A single trailing space silently discards the configured stack size and makes every stack measurement report the default |
| repair claim ← real hardware | The Mac cannot compile this workspace; only SEANDESKTOP can prove or disprove any claim in this plan (PRD D5/D7) |
| closeout claim ← the temptation to engineer green | A plan whose stated goal is "zero failures" is the maximum-pressure environment for widening a guard or re-gating a test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-74-01 | Tampering | The Windows retained-handle SECURITY proofs have NEVER EXECUTED — `parse_directory_entries` (raw-pointer parser), `checked_information_length` (its overflow guard) and `validate_windows_child_name` (the refusal every relative open funnels through) have had zero test coverage on Windows since `8738b24e`, because the test module has never compiled there. A defect in any of them is a memory-safety or path-confinement defect in the crate's most privileged code, and nothing would have caught it | critical | mitigate | Task 2 restores `pub(super)` visibility so the module compiles, then RUNS `cargo nextest run -p wcore-sandbox --no-fail-fast` — the first execution that has ever existed — and requires every failure to be named, root-caused and either fixed or explicitly escalated. Ignoring, re-gating or assertion-weakening any of them is forbidden in the task, in the scope fence and in the execution rules. Task 7 re-runs and re-reports the same suite on the final SHA |
| T-20-74-02 | Tampering | The Windows half of the archive hard-link guard has never existed in a compiled binary: the field access does not compile, and the module is gated `#[cfg(any(feature = "live-docker", test))]`, so `--features live-docker` has never built on Windows while the Unix branch immediately above works correctly. Any Windows-side reasoning that relied on that guard rejecting hard-linked files was reasoning about code that was never there | high | mitigate | Task 2 corrects the field spelling to the one the workspace already uses at two other sites and records in-source that this expression has never compiled on Windows, so the gap is documented at the code rather than only in a summary. Task 2 then compiles and runs the crate's tests, which is also the first execution of the archive tests on Windows |
| T-20-74-03 | Elevation of Privilege | Workstream B's widening grants `FILE_GENERIC_WRITE` to a handle on a FILE child that never writes, because the unknown-kind open cannot know the child's type until the handle exists | medium | accept | Accepted with the argument recorded at the arm: the unknown kind must be granted the union over the kinds it can resolve to, and that union is exactly the rights the directory Mutate arm already carries — nothing beyond. The narrower file-and-Mutate pairing is split out and KEPT narrow so no future caller inherits the widening. The alternative that would avoid it (re-opening by name once the kind is known) is refused because it inserts a second pathname resolution into the crate's most security-critical loop |
| T-20-74-04 | Denial of Service | Workstream B's widening narrows what cleanup can delete: a file held elsewhere with only share-read plus share-delete was previously removable and now fails with a sharing violation, so a workspace could become uncleanable in a way it was not before | medium | mitigate | The consequence is stated in the truths, recorded at the arm, and named as a signature the executor must actively watch for in Task 3's measurement. If it appears it is reported as caused by this change rather than filed under the pre-existing set |
| T-20-74-05 | Spoofing | Workstream C weakens the anti-swap guarantee: if the checkout authority is released and re-acquired around git invocations, a window opens in which the directory can be substituted, converting "no window exists" into "a window exists but is detected" | high | mitigate | Branch (i) — a continuously held observational handle — is preferred precisely because it opens no window, and it is chosen unless measurement refutes it. Branch (iii) preserves the continuous hold while restoring whatever access the relative opens need. Branch (ii) is permitted only if the authority genuinely needs delete, and then only with identity re-validated against the PRE-RELEASE token on every re-acquisition, failing closed, and with the cost argued in-source and in the SUMMARY |
| T-20-74-06 | Elevation of Privilege | Workstream C converts an authority to a weaker access profile and thereby breaks a capability it actually needed — the checkout authority is used as a `RootDirectory` for relative child opens and enumeration, unlike the repo authority 20-72 converted, and read-only access does not carry traverse | high | mitigate | The falsifier is named in the truths and tested directly as Determination 4 before any code changes, rather than inherited from 20-72 which supplies no evidence for it. Branch (iii) exists specifically as the pre-specified fallback, so a failed falsifier produces a designed response instead of an improvisation |
| T-20-74-07 | Tampering | Raising the stack masks a genuinely unbounded recursion, turning a loud abort into a silent pass and hiding a real defect | high | mitigate | The value is argued, not chosen for comfort: 8 MiB is ~4x the 2.25 MiB that measurably sufficed and matches the unix main-thread default, and an unbounded recursion still overflows at 8 MiB. Raising it to 32 MiB or beyond is explicitly refused for exactly this reason, and the bounded-versus-unbounded evidence (aborts at 2 MiB, none at 2.25 MiB) is recorded in-file so the reasoning survives |
| T-20-74-08 | Repudiation | A stack measurement is taken with the `cmd` unquoted assignment form, the trailing space silently voids the value, and the plan concludes "the larger stack did not help" — the exact false conclusion this phase already reached once | high | mitigate | The trap is a truth, an execution rule and a Task 1 deliverable: the trailing space is demonstrated in the transcript with delimited output, the whitespace-safe form is demonstrated clean, and the value `std::thread::min_stack()` actually parses is OBSERVED before any measurement is trusted. Task 7 names it as the first thing to re-check if a stack-overflow abort reappears |
| T-20-74-09 | Repudiation | A source gate passes or fails for reasons unrelated to the code because the rtk-proxied `grep` silently drops lines — measured at 32 lines returned where 674 exist | high | mitigate | Every gate in this plan invokes `/usr/bin/grep` explicitly with `-F` for literals, exactly as it invokes `/usr/bin/git`, and the measured 32-versus-674 evidence is carried in the execution rules so the discipline is not "simplified" away later |
| T-20-74-10 | Tampering | The zero-failure target is reached by engineering rather than by repair — a guard widened, an assertion deleted, a test re-gated or marked ignored, a threshold relaxed, or a lint blanket-suppressed | critical | mitigate | Forbidden in the execution rules, restated in the scope fence, and restated in every task that measures. Task 2 forbids ignoring or re-gating newly surfaced sandbox failures by name; Task 6 forbids blanket suppression and requires every narrowly scoped one to be listed with its justification; Task 7 makes hiding a `wcore-sandbox` failure an invalidation of the whole closeout claim. A red that cannot be fixed honestly is REPORTED |
| T-20-74-11 | Spoofing | A native pass is claimed from Mac inspection, from cross-compilation (impossible here), or from a baseline number copied out of this plan rather than measured on the real tree | high | mitigate | PRD D5/D7 are execution rules. Task 1 RE-MEASURES the baseline on the box and stops the plan on any unexplained discrepancy; Task 7 states its delta against that measurement and never against a number written here; every task that makes a behavioural claim carries its own box-run gate |
| T-20-74-12 | Repudiation | The production stack change is written up as a fixed crash when no production crash was ever observed, inflating the repair's provenance and misleading the next reader | medium | mitigate | The honesty requirement is a truth, an action instruction and a done criterion: the comment and the SUMMARY must say the production path was not measured to overflow, that the measured headroom on the nearest path is under 256 KiB, and that this is defense-in-depth against a narrow margin rather than a reproduced crash |
| T-20-74-13 | Information Disclosure | The verbatim-path question is settled by reasoning instead of by attempt, and the wrong layer is repaired — either an access bit is changed when the real fault is path representation, or a second de-verbatimizing strip is added, splitting the comparison lattice that 20-71 unified | high | mitigate | Determination 2 is a decisive no-handle-held probe run before any code changes, with both raw outputs recorded. `worktree_paths.rs` remains the only site allowed to name the de-verbatimizing call, gate-checked, and a `dunce` refusal must be understood at its cause rather than papered over |
| T-20-74-14 | Denial of Service | The workspace lint gate is restored in name only: `cargo clippy --fix` is run unread over raw-pointer parsing and FFI-adjacent unsafe code, and an automated rewrite changes semantics | medium | mitigate | Task 6 forbids accepting an unread autofix over this crate set, requires every hunk to be read or the edit made by hand, requires the collapsible-condition rewrite to be checked for semantic equivalence, and requires any warning that turns out to describe a real defect to be reported as a defect rather than closed as lint work |
| T-20-74-SC | Tampering | Package-manager supply chain | low | accept | No dependency is added, removed or updated; no `Cargo.toml` or `Cargo.lock` change; no install task exists in this plan |
</threat_model>

<verification>
Local gates (Mac, source level only — the Mac cannot compile this workspace): `cargo fmt --all -- --check` clean; the four Group A items plus the struct field carry `pub(super)` and nothing became `pub` or `pub(crate)`; the archive hard-link guard uses the windows-sys field spelling the rest of the workspace uses; the three Group C imports extend the existing compile-debt block; the combined file-or-unknown Mutate arm is gone and exactly one file-and-Mutate and one unknown-and-Mutate arm exist, with exactly two occurrences of the read-write-delete mask in non-comment lines and the mutating `open_directory` access mode still appearing exactly once; the workstream B regression test exists; both checkout-authority acquisition sites carry the branch marker; the workstream C regression test exists; `dunce::simplified` appears in exactly one file; the `cfg(windows)` count in `worktree_manager.rs` is unchanged from `a1af7707`; `.config/nextest.toml` has exactly one `[env]` table naming `RUST_MIN_STACK` at 8388608 while `.cargo/config.toml` has none; `main.rs` carries the worker stack size with the entry constant unchanged; the unused imports are gone, four SAFETY comments exist in the containment test, and no `unsafe_op_in_unsafe_fn` or `dead_code` suppression was added to the named files.

Authoritative gate (SEANDESKTOP, real hardware, PRD D5/D7), in order, on the exact final SHA: the baseline is RE-MEASURED at `a1af7707` with counts and failing test names recorded and bucketed, and any unbucketable name stops the plan; the `cmd` trailing-space trap is demonstrated and the parsed `min_stack()` value OBSERVED; `cargo check -p wcore-sandbox -p wcore-swarm -p wcore-agent --all-targets` compiles; `cargo nextest run -p wcore-swarm --no-fail-fast` has ZERO failures stated as a delta against that measured baseline; `cargo nextest run -p wcore-agent --test transactional_delegated_mutation_test --run-ignored all` is 9/9; `cargo nextest run -p wcore-swarm --test dispatch_smoke --no-fail-fast` has zero failures; `cargo nextest run -p wcore-sandbox --no-fail-fast` is REPORTED in full with every newly surfaced failure named, root-caused and either fixed or explicitly escalated; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo fmt --all --check` clean.

Known unknowns to record, not to resolve here: the `wcore-sandbox` suite has never run on Windows, so its pass rate is genuinely unknown and any number this plan predicted for it would be invented; all prior probes behind this phase ran on NTFS under `%TEMP%` on C:, so the disposition fallback in `directory_authority_windows.rs` remains untested on this box and may still matter on ReFS, FAT or SMB; and whether the traverse-bypass privilege that makes branch (i) viable is present under every token the swarm runs under is established only for the tokens actually measured.

Re-dispatching the full 6-target native proof is PREPARED here but remains a Sean gate under PRD D6. The three already-green targets — retained-handle, appcontainer-acl and job-object — must REMAIN green; a re-dispatch that turns any of them red is a regression this plan owns.
</verification>

<success_criteria>
- `cargo check -p wcore-sandbox -p wcore-swarm -p wcore-agent --all-targets` compiles on Windows, and `wcore-sandbox` compiles `--all-targets` there for the first time since `8738b24e` (REQ-native-r4).
- The Windows retained-handle security proofs — the raw-pointer directory parser, its overflow guard and the child-name refusal — EXECUTE on Windows for the first time, and every failure they surface is named, root-caused and either fixed or explicitly escalated. None is ignored, re-gated or assertion-weakened.
- The Windows half of the archive hard-link guard compiles, and the fact that it has never existed in a compiled binary — and that `--features live-docker` has never built on Windows — is recorded in-source and in the SUMMARY as a production defect rather than as test debt.
- Destructive removal succeeds through a directory child: exactly one (kind, intent) pair changed, the unknown kind receives the union over the kinds it can resolve to and nothing beyond, the flush requirement that makes the write bit load-bearing is recorded at the arm, and a Windows regression test fails if the bit is ever trimmed back (REQ-native-r6).
- The four named `Access is denied` failures pass, and the accepted sharing-violation cost is stated rather than discovered.
- The checkout-authority chdir defect is closed on measured evidence: the failing invocation captured, the verbatim-path question settled by a no-handle probe, the consumer set re-enumerated and classified, the traverse falsifier tested directly, and one of three pre-specified branches taken with its rationale recorded at both acquisition sites (REQ-native-r6).
- The anti-swap guarantee did not weaken. The handle stays continuously held, or identity is re-validated against the pre-release token on every re-acquisition and fails closed, with the cost argued in-source. `worktree_paths.rs` is still the crate's only site naming the de-verbatimizing call, and `wcore-swarm` gained no new `#[cfg]`.
- `cargo nextest run -p wcore-agent --test transactional_delegated_mutation_test --run-ignored all` is 9/9, and a regression test asserts a chdir-requiring git subcommand still succeeds inside a checkout while the workspace authorities are alive.
- The five stack-overflow aborts are resolved on both surfaces — the libtest thread through nextest configuration and the runtime workers through an explicit thread stack size — with the value argued to be large enough for the bounded chain and small enough that an unbounded recursion still overflows, the `cargo test` coverage gap stated with its justification, and the production change written up honestly as defense-in-depth rather than as a reproduced crash (REQ-native-r4).
- The value is PROVEN to have taken effect by an observed `min_stack()` reading, and the `cmd` trailing-space trap is demonstrated in the transcript so no measurement in this plan can be doubted.
- `cargo clippy --workspace --all-targets -- -D warnings` is clean on Windows for the first time, with every warning fixed at its cause, every dead-code finding investigated across configurations before deletion, no blanket suppression used as a silencing device, every narrowly scoped one listed with its justification, and any warning that revealed a real defect reported as one (REQ-native-r12).
- The real CI status of the self-hosted Windows job is stated from `gh` output rather than inferred.
- SEANDESKTOP on the exact final SHA: zero `wcore-swarm` failures against a RE-MEASURED baseline, 9/9 on the transactional suite, a green `dispatch_smoke`, a fully reported first-ever `wcore-sandbox` Windows result, and clean clippy and fmt (REQ-native-r12).
- The 6-target native re-dispatch is prepared with its derived ref and nonce stated and the three already-green targets named as a must-remain-green condition, and it awaits Sean's explicit authorization per PRD D6 rather than having been fired.
- No scope creep and no gate weakening: the 20-71, 20-72 and 20-73 repairs, the runner-proven commits, the mutating `open_directory`, every other relative-intent arm, every other authority's access profile, the entry-thread stack and the other production runtime builders are all untouched; no guard widened, no assertion deleted, no test re-gated or ignored, no dependency changed (REQ-native-r4).
</success_criteria>

## Artifacts this phase produces
- `.config/nextest.toml` — the `[env]` table raising `RUST_MIN_STACK`, documented with the bounded-requirement measurement, the value rationale and the coverage assessment.
- `crates/wcore-cli/src/main.rs` — the runtime worker stack size, documented honestly as defense-in-depth.
- `crates/wcore-sandbox/src/directory_authority_windows.rs` — the split Mutate arms with the flush requirement recorded, plus the Group A visibility restoration.
- `crates/wcore-sandbox/src/directory_authority_windows_tests.rs` — the destructive-removal regression test and the import cleanup, in a module that compiles on Windows for the first time.
- `crates/wcore-sandbox/src/directory_authority_archive.rs` — the corrected windows-sys hard-link field access and its never-compiled-guard note.
- `crates/wcore-sandbox/src/backends/appcontainer/windows_impl/tests.rs` — the three imports completing the compile-debt restore.
- `crates/wcore-swarm/src/worktree_manager.rs` and `worktree_security.rs` — the checkout-authority repair under the measured branch, with the branch marker and rationale at both acquisition sites.
- `crates/wcore-swarm/src/worktree_tests.rs` — the checkout chdir regression test.
- `crates/wcore-sandbox/tests/hard_process_containment_windows.rs` — explicit unsafe blocks with SAFETY comments.
- 20-74-SUMMARY.md recording the re-measured baseline, all five workstreams, the workstream-C branch and its evidence, the first-ever `wcore-sandbox` Windows result, every lint suppression added, the CI status found, and the final zero-failure counts with the exact SHA.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-74-SUMMARY.md` using the standard GSD summary template. Record: the final `source_sha` and the RE-MEASURED `a1af7707` baseline with every failing test name bucketed; the `cmd` trailing-space trap demonstration and the OBSERVED `min_stack()` value, so no measurement in this plan can be doubted later; workstream D with all four groups, the explicit statement that `wcore-sandbox` had never compiled `--all-targets` on Windows, that `--features live-docker` had never built there, that the Windows half of the hard-link guard had never existed in a compiled binary, and that the raw-pointer directory parser, its overflow guard and the child-name refusal had never executed a single test on Windows — plus the FULL first-ever `wcore-sandbox` Windows suite result with each newly surfaced failure named and its disposition; workstream B with the bisecting-probe evidence, the full (kind, intent) rights table before and after, the union argument, the accepted sharing-violation cost, the two rejected alternatives and the deferred precise-kind refactor named as the follow-up; workstream C with all four determinations and their raw evidence, the branch taken and why, the anti-swap argument, and whether the verbatim prefix proved causal or incidental; workstream A with the stack-sweep evidence, the value rationale (bounded chain versus unbounded recursion), the `cargo test` coverage gap and its justification, the rejected `.cargo/config.toml` alternative, the honest framing of the production change as defense-in-depth rather than a reproduced crash, the survey of other runtime builders, and the per-test classification of all five previously aborting tests; workstream E with the measured warning set reconciled against the plan's inventory, every fix at its cause, every narrowly scoped suppression with its justification, anything a warning revealed to be a real defect, and the CI status read from `gh`; the ordering actually executed with a justification for any deviation; the final per-suite SEANDESKTOP counts stated as a delta against the measured baseline, with clippy/fmt status, the exact SHA and the wall clock; the recorded unknowns (the `wcore-sandbox` Windows pass rate had no prior data point; probes limited to NTFS under `%TEMP%` on C:; the traverse-bypass privilege established only for the tokens measured); the prepared but UNFIRED 6-target native re-dispatch with its derived ref and nonce and the three must-remain-green targets; and any residual failure with its cause. Mark no Phase 20 requirement complete — closure is claimed by the downstream native proof under the existing Sean gate.
</output>

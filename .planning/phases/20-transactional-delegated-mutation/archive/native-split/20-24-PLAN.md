---
phase: 20-transactional-delegated-mutation
plan: "24"
type: execute
wave: 23
depends_on:
  - "20-23"
files_modified:
  - .planning/phases/20-transactional-delegated-mutation/20-24-CROSS-AUDIT.md
autonomous: true
requirements:
  - REQ-native-r12
  - REQ-native-r13
must_haves:
  truths:
    - "A fresh non-author reviewer cross-audits the exact sealed candidate SHA (the 20-23 lock-sealing commit) at every severity across the native-repair delta (20-19..20-23), before any Sean-gated native run is spent (REQ-native-r12 pre-native gate)."
    - "Every claimed reviewer emits its OWN schema-validated review artifact (wayland-core.phase20-independent-review.v1); no prose-only 'review' counts toward the PASS — closing the 20-08/20-16 attestation gap where 4 claimed reviews had no on-disk artifact (REQ-native-r13)."
    - "The cross-audit defers only native_macos/native_windows (those are proven at 20-25); code review, all-severity, evidence integrity, and integration authority of the sealed delta are prosecuted here with zero findings, or the sequence stops for a repaired successor."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-24-CROSS-AUDIT.md
      provides: "Fresh non-author schema-validated cross-audit of the sealed candidate native-repair delta"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-24-CROSS-AUDIT.md
      to: .planning/phases/20-transactional-delegated-mutation/20-23-SUMMARY.md
      via: "source/review-base/review authority tuple over the sealed candidate SHA"
      pattern: "source_sha"
---

<objective>
Cross-audit the exact sealed candidate before spending a Sean-gated native run: a fresh non-author reviewer prosecutes the native-repair delta (20-19..20-23) at every severity and records a schema-validated artifact per claimed reviewer, native explicitly deferred to 20-25.

Purpose: The gate sequence puts an adversarial cross-audit before the native proof so a broken candidate does not consume the scarce native run; and the audit addendum (R13) requires per-reviewer schema-validated artifacts, not prose. This is the pre-native attestation gate.
Output: One (or more, per claimed reviewer) schema-validated cross-audit JSON artifact over the sealed SHA; zero findings admits the native-proof gate.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
@/Users/seandonahoe/.agents/skills/gsd-validate-phase/SKILL.md
@/Users/seandonahoe/.agents/skills/gsd-secure-phase/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/intel/inbox-2026-07-23/AUDIT-2026-07-23.md
@.planning/phases/20-transactional-delegated-mutation/20-19-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-20-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-21-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-22-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-23-SUMMARY.md
@.planning/scripts/verify-review-result.mjs
@.planning/scripts/verify-review-pair.sh
</context>

<admission>
- Require a clean isolated checkout at the sealed candidate SHA recorded by `20-23-SUMMARY.md`; verify its tree. Capture the source authority: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-24)"` and `node .planning/scripts/task-base-authority.mjs capture "$(git rev-parse --git-path gsd-reviewed-source-20-24)" "$source" "$(git rev-parse "$source^{tree}")"`.
- The executor MUST be a fresh non-author executor distinct from every 20-19..20-23 source author. If reviewer identity cannot establish that separation, fail closed.
- Each claimed reviewer records its OWN schema-validated artifact. A prose-only or artifact-less claimed review does not count toward the PASS (R13).
</admission>

<tasks>

<task type="auto">
  <name>Task 1: Fresh non-author cross-audit of the sealed native-repair delta</name>
  <files>.planning/phases/20-transactional-delegated-mutation/20-24-CROSS-AUDIT.md</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-19-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-20-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-21-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-22-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-23-SUMMARY.md, crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs, crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs, crates/wcore-agent/src/session_journal/snapshot.rs, crates/wcore-sandbox/tests/live_fs_acl.rs, crates/wcore-sandbox/tests/hard_process_containment_windows.rs, crates/wcore-swarm/tests/dispatch_smoke.rs, scripts/f20-native-windows-proof.ps1, scripts/f20-native-macos-proof.sh, scripts/f20-native-uat-proof.mjs, .github/workflows/nightly-windows-soak.yml</read_first>
  <behavior>
    - Prosecute the exact sealed candidate SHA at every severity: the deny-only SID drop preserves the AppContainer boundary (restricted/low-IL token, restricted-token process creation, no full-primary-token path, no reintroduced diagnostic); the isolation proofs are falsifiable; the Job-Object tests exercise the real mechanism; the harness repoint + anti-drift guard cannot map a native target to a wrong-OS test; the macOS re-validation removed aspirational mappings; the lock is consistent; the runner pin/writer-parity/profiles are sound.
    - Confirm the sealed candidate's Hetzner build + aggregate receipts (from 20-23) belong to the exact SHA and are green (11509/0).
    - Classify native_macos/native_windows as DEFERRED to the 20-25 native-proof gate — not passed from source.
    - Every BLOCKER/CRITICAL/HIGH/MEDIUM/LOW finding is disqualifying; do not repair source here.
    - Record the result as one schema-validated JSON object matching `wayland-core.phase20-independent-review.v1`, profile `f20-native-crossaudit` (checks all_severity/evidence_integrity/integration_authority PASS; deferred native_macos/native_windows). Distinct stable source-executor/reviewer identities; never embed the review commit's own SHA.
  </behavior>
  <action>Inspect the complete sealed candidate delta (20-19 source fixes, 20-20/20-21 tests, 20-22 harness/CI/verifier, 20-23 lock) without changing source. Apply the code-review, `gsd-validate-phase`, and `gsd-secure-phase` (ASVS Level 2) checklists as one blocking exact-source cross-audit, focused adversarially on the security-boundary change (deny-only SID drop) and the harness anti-drift. Write one combined schema-validated JSON artifact matching `.planning/scripts/verify-review-result.mjs` profile `f20-native-crossaudit`. If additional cross-AI reviewers are claimed, EACH emits its own schema-validated artifact (a sibling `20-24-CROSS-AUDIT-<reviewer>.md`); a claimed reviewer without an artifact does not count. PASS only when every pre-native truth has credible source/receipt evidence, native is explicitly deferred, and zero findings remain; otherwise record FAIL and stop the sequence for a repaired successor.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-24)" .planning/phases/20-transactional-delegated-mutation/20-24-CROSS-AUDIT.md</automated>
    <automated>source="$(node .planning/scripts/task-base-authority.mjs read "$(git rev-parse --git-path gsd-reviewed-source-20-24)" | sed -n '1p')"; review="$(git rev-parse HEAD)"; node .planning/scripts/verify-review-result.mjs "$review" .planning/phases/20-transactional-delegated-mutation/20-24-CROSS-AUDIT.md "$source" "$(git rev-parse "${source}^{tree}")" f20-native-crossaudit</automated>
  </verify>
  <acceptance_criteria>The cross-audit is fresh, non-author, exact-sealed-SHA, complete at every severity, schema-validated per claimed reviewer, and native-deferred. PASS requires zero findings; FAIL creates no qualification and does not admit 20-25.</acceptance_criteria>
  <done>The schema-validated cross-audit artifact(s) exist for post-task pair verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native run authorization ← pre-native audit | A scarce Sean-gated native run should not be spent on a candidate a fresh audit would reject |
| PASS ← reviewer artifact | Only a schema-validated per-reviewer artifact may count toward the PASS (no prose-only reviews) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-24-01 | Repudiation | unattested reviews | high | mitigate | Every claimed reviewer emits a schema-validated artifact verified by `verify-review-result.mjs`; prose-only reviews do not count (closes the 20-08/20-16 gap). |
| T-20-24-02 | Elevation of Privilege | deny-only SID drop | critical | mitigate | The cross-audit adversarially re-confirms the boundary change did not widen isolation before the native run is spent. |
</threat_model>

<verification>
Derive `source` and `review` and run the schema verifier (Task 1 `<verify>`). Require a zero-finding PASS under profile `f20-native-crossaudit`. Record the externally derived `(source_sha, source_tree, review_sha)` tuple and each reviewer artifact identity in `20-24-SUMMARY.md`. No Cargo runs on the Mac.
</verification>

<success_criteria>
- A fresh non-author reviewer cross-audited the exact sealed candidate at every severity with zero findings (r12 pre-native gate).
- Every claimed reviewer emitted a schema-validated artifact; no prose-only review counted (r13).
- Native is explicitly deferred to 20-25; only a zero-finding PASS admits the native-proof gate.
</success_criteria>

## Artifacts this phase produces
- `.planning/phases/20-transactional-delegated-mutation/20-24-CROSS-AUDIT.md` (+ any per-reviewer siblings) — schema-validated pre-native cross-audit.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-24-SUMMARY.md` with executor identity, sealed source SHA/tree, review SHA(s), per-reviewer artifact identities, all-severity disposition, and explicit admission or rejection of the 20-25 native-proof gate. It admits no requirement.
</output>

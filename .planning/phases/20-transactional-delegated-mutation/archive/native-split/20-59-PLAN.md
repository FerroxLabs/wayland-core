---
phase: 20-transactional-delegated-mutation
plan: "59"
type: execute
wave: 54
depends_on:
  - "20-58"
files_modified:
  - .planning/phases/20-transactional-delegated-mutation/20-59-CROSS-AUDIT.md
autonomous: true
requirements:
  - REQ-native-r12
  - REQ-native-r13
must_haves:
  truths:
    - "A FOUR-WAY independent cross-audit panel — Codex 5.6 Sol (`codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check \"<prompt>\"`), Gemini 3.1 Pro (`gemini -p \"<prompt>\" -m gemini-3.1-pro-preview -o text --approval-mode plan`, with `GEMINI_CLI_TRUST_WORKSPACE=true --skip-trust` for the untrusted checkout), and Kimi K3 (`/Users/seandonahoe/.kimi-code/bin/kimi -p \"<prompt>\" --output-format text`, ABSOLUTE path, passed via -p not stdin) run in PARALLEL, PLUS an internal Claude non-author adversarial reviewer prompted to REFUTE (default-refuted-if-uncertain) — cross-audits the exact RE-SEALED further-repaired-successor SHA/tree recorded by 20-58 at every severity across the 20-57 repair delta (the observer-hardening: all three host-side query observers in `hard_process_containment_windows.rs` now fail closed) BEFORE any Sean-gated native run is re-spent (REQ-native-r12 pre-native gate)."
    - "Each of the four auditors emits its OWN on-disk schema-validated artifact: Codex Sol → `f20-native-crossaudit.codex-sol`, Gemini Pro → `f20-native-crossaudit.gemini-pro`, Kimi K3 → `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial reviewer → `wayland-core.phase20-independent-review.v1`; no prose-only vote counts toward the PASS — sustaining the 20-24/20-31/20-38/20-45/20-52 attestation discipline (REQ-native-r13). Raw outputs are preserved on disk alongside the parsed artifacts."
    - "The panel adversarially prosecutes that the 20-52 BLOCK finding (the residual reap-OBSERVER false-green) is ACTUALLY CLOSED without regression: ALL THREE host-side query observers — `tagged_cmd_count`, `tagged_choice_descendant_pids`, `surviving_captured_choice_pids` — now FAIL CLOSED. Each asserts `out.status.success()` after `.output()` and panics on a non-success exit; the two `.Count` observers also panic on empty/unparseable stdout (no `parse().unwrap_or(0) => 0`), and the PID-list reader panics on a malformed token (no `filter_map(...ok())` swallow). A post-close CIM/PowerShell query failure can therefore NO LONGER satisfy `wait_until(... == 0)` and report the reap property proven WITHOUT evidence. Every panel-confirmed-sound assertion is preserved VERBATIM: the captured-PID reap logic (alive-phase capture + fixed-PID post-close re-check), `!captured_pids.is_empty()`, the peak/cap/below-attempts/clean-exit assertions, the `if pids.is_empty() { return 0; }` legitimate empty-set short-circuit, and the sibling `job_close_reaps_detached_descendant_with_no_residue` / `breakaway_is_denied` / `qualified_hard_containment_backend_preflight` tests. The only remaining `.unwrap_or(0)` is the benign `unique_tag` SystemTime-nanos fallback (NOT a query observer) and `reap_stray_choice` stays intentional best-effort cleanup. The Finding-2 `command.rs` cmd classifier and both 20-43 isolation guardrails (#A DispatchAdmission budget; #B quoting-layer-only) — confirmed sound at 20-52 and untouched by the one-file 20-57 delta — still hold, and the Kimi watch-items are carried into the 20-53 re-dispatch (REQ-native-r12/r13)."
    - "native_macos/native_windows are the ONLY deferred checks (proven at 20-53); code review of the 20-57 delta, all-severity findings, evidence integrity, and integration authority are prosecuted here. ALL FOUR auditors MUST PASS with zero findings to admit 20-53; any single finding from any one auditor (any severity except the deferred native checks) stops the sequence for a further repaired successor. Fail-closed: if ANY external auditor (Codex Sol / Gemini Pro / Kimi K3) is unreachable or its output cannot be schema-validated, the gate records an explicit incomplete disposition and BLOCKS rather than emitting a false-green PASS from a partial panel; the internal Claude adversarial pass is one of the four required votes, never a substitute for a missing external auditor (CONTEXT D5 fail-safe) (REQ-native-r13)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-59-CROSS-AUDIT.md
      provides: "Four per-reviewer schema-validated pre-native cross-audits of the re-sealed further-repaired-successor's 20-57 observer-hardening repair delta: the Codex Sol / Gemini Pro / Kimi K3 f20-native-crossaudit artifacts plus the internal Claude adversarial wayland-core.phase20-independent-review.v1 artifact, with raw outputs preserved"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-59-CROSS-AUDIT.md
      to: .planning/phases/20-transactional-delegated-mutation/20-58-SUMMARY.md
      via: "source/review-base/review authority tuple over the re-sealed further-repaired-successor SHA/tree recorded by 20-58"
      pattern: "source_sha"
---

<objective>
Cross-audit the exact re-sealed further-repaired-successor (the SHA/tree recorded by 20-58) before re-spending a Sean-gated native run: a FOUR-WAY independent panel — Codex 5.6 Sol, Gemini 3.1 Pro, and Kimi K3 (three external CLIs run in parallel), plus an internal Claude non-author adversarial reviewer — prosecutes the 20-57 observer-hardening repair delta at every severity, and each auditor records its OWN schema-validated artifact. ALL FOUR MUST PASS; native explicitly deferred to 20-53.

Purpose: The gate sequence puts an adversarial cross-audit before the native proof so a broken candidate does not consume the scarce native run; and R13 requires per-reviewer schema-validated artifacts, not prose. This is the re-run pre-native attestation gate for the further-repaired successor: the 20-52 four-way panel BLOCKED the prior candidate on a residual reap-OBSERVER false-green (`surviving_captured_choice_pids` ending in `parse().unwrap_or(0)` with no `out.status.success()` check, plus the same fail-open shape in the sibling `tagged_cmd_count`), which Gemini + Kimi both missed. The 20-57 delta hardens ALL THREE host-side query observers to fail closed in one pass, so the panel focuses adversarially on whether every observer now asserts query success and panics on failure/unparseable output (no path maps a failed query to a passing value), whether every panel-confirmed-sound assertion and sibling test is preserved verbatim, and whether the Finding-2 cmd classifier and the 20-43 #A/#B isolation guardrails still hold. A four-independent-auditor panel raises the cost of a shared blind spot: exactly the class of miss (Gemini + Kimi both missed the observer fail-open at 20-52) a four-way panel exists to catch.
Output: Four schema-validated artifacts over the re-sealed successor SHA — Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1`. Zero findings across ALL FOUR admits the native-proof gate. If ANY external auditor is unreachable or its output cannot be schema-validated, an explicit incomplete disposition (no false green from a partial panel).
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-52-CROSS-AUDIT.md
@.planning/phases/20-transactional-delegated-mutation/20-52-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-57-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-58-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-57-PLAN.md
</context>

<execution_rules>
- The cross-audit is a FOUR-WAY panel of independent (non-author) reviewers; NONE is the plan author. Three are external CLIs, invoked verbatim and run in PARALLEL:
  1. Codex 5.6 Sol — `codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check "<prompt>"` (brief on stdin is acceptable)
  2. Gemini 3.1 Pro — `gemini -p "<prompt>" -m gemini-3.1-pro-preview -o text --approval-mode plan` (set `GEMINI_CLI_TRUST_WORKSPACE=true` and add `--skip-trust` to run in the untrusted checkout dir; 20-45/20-52 hit exit 55 without them)
  3. Kimi K3 — `/Users/seandonahoe/.kimi-code/bin/kimi -p "<prompt>" --output-format text` (ABSOLUTE path — the Bash shell PATH omits it; Kimi's -p mode does NOT ingest piped stdin, so pass the full brief as the -p argument; see the [[cross-auditor-kimi-k3]] project memory)
  The fourth is an internal Claude non-author adversarial subagent, prompted to REFUTE the candidate (default-refuted-if-uncertain).
- Every one of the four auditors MUST leave its OWN on-disk schema-validated artifact. The three external legs emit `f20-native-crossaudit` variants keyed by auditor id — Codex Sol → `f20-native-crossaudit.codex-sol`, Gemini Pro → `f20-native-crossaudit.gemini-pro`, Kimi K3 → `f20-native-crossaudit.kimi-k3`; the internal Claude adversarial leg emits `wayland-core.phase20-independent-review.v1`. A prose-only vote does not count toward PASS (REQ-native-r13). Preserve each auditor's raw output on disk (a `20-59-raw/` sidecar directory) alongside the parsed artifacts.
- Scope every prompt to the EXACT re-sealed successor SHA/tree recorded by 20-58 (do not review the branch tip if it has advanced) and the 20-57 repair delta (the one-file observer-hardening in `hard_process_containment_windows.rs`). The review base is that tuple.
- native_macos and native_windows are the ONLY checks that may be deferred (they are proven at 20-53). All other checks — code review of the 20-57 delta, all-severity findings, evidence integrity, integration authority — are prosecuted here by every auditor.
- DISPOSITION — ALL FOUR MUST PASS: only a zero-finding PASS from all four auditors admits 20-53. Any single finding from any one auditor (any severity except the deferred native_macos/native_windows) BLOCKS and routes to a further repaired successor.
- FAIL-CLOSED: if ANY external auditor (Codex Sol / Gemini Pro / Kimi K3) cannot be reached or its output cannot be schema-validated, record an explicit incomplete disposition and BLOCK — do not synthesize a PASS from the remaining reviewers or from prose (CONTEXT D5 fail-safe). The internal Claude adversarial pass is always reachable but is one of the four required votes, not a substitute for a missing external auditor. Do not modify any source/test/workflow file; this plan writes only the cross-audit artifact.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Four-way pre-native cross-audit panel of the re-sealed successor (Codex Sol + Gemini Pro + Kimi K3 + Claude adversarial), all-must-pass, fail-closed</name>
  <files>.planning/phases/20-transactional-delegated-mutation/20-59-CROSS-AUDIT.md</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-58-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-57-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-52-CROSS-AUDIT.md, .planning/phases/20-transactional-delegated-mutation/20-57-PLAN.md</read_first>
  <behavior>
    - Dispatch the FOUR-WAY independent panel over the exact re-sealed successor SHA/tree recorded by 20-58. Run the three external CLIs in parallel — Codex 5.6 Sol (`codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check`), Gemini 3.1 Pro (`gemini -m gemini-3.1-pro-preview -o text --approval-mode plan`, with `GEMINI_CLI_TRUST_WORKSPACE=true --skip-trust`), Kimi K3 (`/Users/seandonahoe/.kimi-code/bin/kimi --output-format text`, absolute path, brief in the -p argument) — plus an internal Claude non-author adversarial subagent prompted to REFUTE (default-refuted-if-uncertain). Scope every prompt to the 20-57 observer-hardening repair delta and its predecessor finding: the 20-52 BLOCK on the residual reap-OBSERVER false-green.
    - Every auditor prosecutes adversarially: (1) Do ALL THREE host-side query observers (`tagged_cmd_count`, `tagged_choice_descendant_pids`, `surviving_captured_choice_pids`) now FAIL CLOSED — each asserting `out.status.success()` after `.output()` and panicking on a non-success exit, the two `.Count` observers panicking on empty/unparseable stdout (no `parse().unwrap_or(0) => 0`) and the PID-list reader panicking on a malformed token (no `filter_map(...ok())` swallow) — so no `wait_until(... == 0)` reap check can be satisfied by a failed query? (2) Is every panel-confirmed-sound assertion preserved VERBATIM — the captured-PID reap logic, `!captured_pids.is_empty()`, the peak/cap/below-attempts/clean-exit assertions, the `if pids.is_empty() { return 0; }` legitimate empty-set short-circuit, and the sibling `job_close_reaps_detached_descendant_with_no_residue` / `breakaway_is_denied` / `qualified_hard_containment_backend_preflight` tests — with no assertion weakened under cover of "hardening"? (3) Is the full-file sweep clean — the only remaining `.unwrap_or(0)` is the benign `unique_tag` nanos fallback and `reap_stray_choice` stays best-effort cleanup, with no third fail-open observer of the same class? (4) Do the Finding-2 `command.rs` cmd classifier and the 20-43 #A/#B isolation guardrails still hold (untouched by the one-file 20-57 delta)? (5) Are the Kimi watch-items (incl. dispatch.rs:604) carried as explicit 20-53 re-dispatch checks, including the fail-closed post-close PID-reuse race?
    - Capture each auditor's findings as its OWN schema-validated artifact: Codex Sol → `f20-native-crossaudit.codex-sol`, Gemini Pro → `f20-native-crossaudit.gemini-pro`, Kimi K3 → `f20-native-crossaudit.kimi-k3`, internal Claude adversarial → `wayland-core.phase20-independent-review.v1`. native_macos/native_windows are deferred, proven at 20-53. Preserve the raw outputs in a `20-59-raw/` sidecar.
    - Disposition — ALL FOUR MUST PASS: only a zero-finding PASS from all four admits 20-53; any single finding from any auditor blocks and routes to a further repaired successor.
    - Fail-closed: if ANY external auditor (Codex Sol / Gemini Pro / Kimi K3) is unreachable or its output fails schema validation, record an explicit incomplete disposition and block — no false-green PASS from a partial panel.
  </behavior>
  <action>Dispatch the four-way independent panel over the exact re-sealed successor SHA/tree recorded by 20-58: run the three external CLIs in parallel — Codex 5.6 Sol (`codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check "<prompt>"`), Gemini 3.1 Pro (`GEMINI_CLI_TRUST_WORKSPACE=true gemini -p "<prompt>" -m gemini-3.1-pro-preview -o text --approval-mode plan --skip-trust`), and Kimi K3 (`/Users/seandonahoe/.kimi-code/bin/kimi -p "<prompt>" --output-format text`) — plus an internal Claude non-author adversarial subagent prompted to REFUTE (default-refuted-if-uncertain), scoping each to the 20-57 observer-hardening repair delta: all three host-side query observers now fail closed (assert query success + panic on non-success/unparseable output, no map-failure-to-zero), every panel-confirmed-sound assertion and sibling test preserved verbatim, the full-file sweep clean (only the benign `unique_tag` nanos fallback remains), and the still-holding Finding-2 cmd classifier and #A/#B isolation guardrails untouched. Record each auditor's findings as its own schema-validated artifact in `20-59-CROSS-AUDIT.md` — `f20-native-crossaudit.codex-sol`, `f20-native-crossaudit.gemini-pro`, `f20-native-crossaudit.kimi-k3`, and the internal Claude `wayland-core.phase20-independent-review.v1` (native_macos/native_windows deferred) — preserving raw outputs under `20-59-raw/`. Verify the Kimi watch-items (incl. dispatch.rs:604 and the fail-closed post-close PID-reuse race) are recorded as explicit 20-53 re-dispatch checks. ALL FOUR must PASS zero-finding to admit 20-53; any single finding blocks. If ANY external auditor is unreachable or its output fails schema validation, record an explicit incomplete disposition and BLOCK — do not synthesize a PASS. Implements REQ-native-r12/r13; write only the cross-audit artifact.</action>
  <verify>
    <automated>test -f .planning/phases/20-transactional-delegated-mutation/20-59-CROSS-AUDIT.md</automated>
    <automated>grep -nE "f20-native-crossaudit\.codex-sol|f20-native-crossaudit\.gemini-pro|f20-native-crossaudit\.kimi-k3|phase20-independent-review|Codex 5\.6 Sol|Gemini 3\.1 Pro|Kimi K3|Claude adversarial|source_sha|deferred|fail.closed|status.success|observer" .planning/phases/20-transactional-delegated-mutation/20-59-CROSS-AUDIT.md | head -40</automated>
  </verify>
  <acceptance_criteria>The cross-audit artifact exists and records FOUR per-reviewer results over the exact re-sealed successor SHA: Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1`, with native_macos/native_windows deferred, all three query observers confirmed fail-closed, every panel-confirmed-sound assertion and sibling test preserved, the Finding-2 classifier and #A/#B guardrails still holding, and the Kimi watch-items carried as 20-53 checks. A zero-finding PASS from ALL FOUR reviewers admits 20-53; any single finding from any auditor, or any external auditor unreachable/schema-invalid, blocks with an explicit incomplete disposition.</acceptance_criteria>
  <done>The re-sealed further-repaired successor has a four-way per-reviewer schema-validated pre-native cross-audit (Codex Sol + Gemini Pro + Kimi K3 + Claude adversarial), or an explicit fail-closed block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native run authorization ← four-way cross-audit PASS | The scarce Sean-gated native run is only re-spent after ALL FOUR panel auditors pass; a false-green here wastes the run |
| attestation integrity ← per-reviewer artifact | A PASS must be backed by an on-disk schema-validated artifact per each of the four auditors, not prose |
| finding closure ← adversarial re-prosecution | The 20-52 reap-observer finding must be confirmed genuinely closed (all three observers fail closed) without a regression such as a weakened assertion or a third fail-open observer |
| panel independence ← external-auditor reachability | If ANY external auditor (Codex Sol / Gemini Pro / Kimi K3) is down, the audit must fail closed rather than pass on a partial panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-59-01 | Spoofing | prose-only or partial-panel PASS | high | mitigate | Each of the four auditors emits its own schema-validated artifact; a prose-only or missing artifact does not count, and PASS requires ALL FOUR; raw outputs are preserved on disk. |
| T-20-59-02 | Repudiation | external auditor unreachable false-green | high | mitigate | Fail-closed: any unreachable or schema-invalid external-auditor output records an explicit incomplete disposition and blocks; no PASS is synthesized from the remaining reviewers (CONTEXT D5 fail-safe). |
| T-20-59-03 | Tampering | a finding-fix regression slipping through | high | mitigate | All four auditors adversarially re-prosecute that all three observers now fail closed (assert query success + panic, no map-failure-to-zero), that no assertion was weakened under cover of "hardening", that no third fail-open observer of the same class remains, and that the Finding-2 classifier and #A/#B guardrails still hold — so a fix-regression cannot pass a four-independent-auditor panel silently. |
</threat_model>

<verification>
The cross-audit artifact `20-59-CROSS-AUDIT.md` determines acceptance: it must record FOUR per-reviewer results over the exact re-sealed successor SHA/tree — Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1` — ALL zero-finding, with native_macos/native_windows deferred, all three query observers confirmed fail-closed, every panel-confirmed-sound assertion and sibling test preserved, the Finding-2 classifier and #A/#B guardrails still holding, and the Kimi watch-items carried as explicit 20-53 re-dispatch checks. Record the re-sealed `source_sha`/tree, the four reviewer identities, the three external CLI invocations, each artifact's schema-validation status, the raw-output sidecar identity, and the disposition (ALL FOUR PASS admits 20-53; any single finding or any external auditor unreachable/invalid blocks with an explicit incomplete disposition) in `20-59-SUMMARY.md`. No source/test/workflow change; no native or requirement claim.
</verification>

<success_criteria>
- The four-way panel (Codex 5.6 Sol + Gemini 3.1 Pro + Kimi K3 external, run in parallel, plus the internal Claude adversarial reviewer) cross-audits the exact re-sealed successor at every severity across the 20-57 observer-hardening delta, and each auditor records its own schema-validated artifact — `f20-native-crossaudit.codex-sol` / `.gemini-pro` / `.kimi-k3` and `wayland-core.phase20-independent-review.v1` (r12/r13).
- The 20-52 reap-observer finding is confirmed closed without regression (all three query observers fail closed — assert query success + panic, no map-failure-to-zero — with every panel-confirmed-sound assertion and sibling test preserved and no third fail-open observer remaining), the Finding-2 cmd classifier and #A/#B isolation guardrails still hold, and the Kimi watch-items (incl. dispatch.rs:604 and the fail-closed post-close PID-reuse race) are carried as 20-53 checks; native_macos/native_windows are the only deferred checks.
- A zero-finding PASS from ALL FOUR reviewers admits 20-53; any single finding from any auditor, or any external auditor unreachable/schema-invalid, blocks with an explicit incomplete disposition (no false green from a partial panel).
</success_criteria>

## Artifacts this phase produces
- The four pre-native cross-audits of the re-sealed further-repaired-successor's 20-57 observer-hardening repair delta: Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1`, with raw outputs preserved.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-59-SUMMARY.md` using the standard GSD summary template. Record the re-sealed `source_sha`/tree, the four reviewer identities (Codex Sol, Gemini Pro, Kimi K3, internal Claude adversarial) and each artifact's schema-validation status, the observer-hardening closure verdict (all three observers fail closed; assertions preserved), the Finding-2 and #A/#B guardrail verdicts, the carried 20-53 watch-items, the raw-output sidecar identity, and the disposition (ALL-FOUR PASS or fail-closed incomplete). Complete no Phase 20 requirement.
</output>
</content>

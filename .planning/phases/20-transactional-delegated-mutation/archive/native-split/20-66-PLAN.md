---
phase: 20-transactional-delegated-mutation
plan: "66"
type: execute
wave: 63
depends_on:
  - "20-65"
files_modified:
  - .planning/phases/20-transactional-delegated-mutation/20-66-CROSS-AUDIT.md
autonomous: true
gap_closure: true
requirements:
  - REQ-native-r12
  - REQ-native-r13
must_haves:
  truths:
    - "A FOUR-WAY independent cross-audit panel — Codex 5.6 Sol (`codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check \"<prompt>\"`), Gemini 3.1 Pro (`gemini -p \"<prompt>\" -m gemini-3.1-pro-preview -o text --approval-mode plan`, with `GEMINI_CLI_TRUST_WORKSPACE=true --skip-trust` for the untrusted checkout), and Kimi K3 (`/Users/seandonahoe/.kimi-code/bin/kimi -p \"<prompt>\" --output-format text`, ABSOLUTE path, passed via -p not stdin) run in PARALLEL, PLUS an internal Claude non-author adversarial reviewer prompted to REFUTE (default-refuted-if-uncertain) — cross-audits the EXACT committed CLASS B production delta from 20-65 (the `fs_read_deny` reimplementation in `crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs`: protected-DACL package-SID-ALLOW removal + symmetric revoke) at every severity BEFORE the security-sensitive change advances to the scarce SEANDESKTOP native verify (REQ-native-r12 pre-native gate)."
    - "Each of the four auditors emits its OWN on-disk schema-validated artifact: Codex Sol → `f20-native-crossaudit.codex-sol`, Gemini Pro → `f20-native-crossaudit.gemini-pro`, Kimi K3 → `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial reviewer → `wayland-core.phase20-independent-review.v1`; no prose-only vote counts toward the PASS — sustaining the 20-24/20-31/20-38/20-45/20-52/20-59/20-62 attestation discipline (REQ-native-r13). Raw outputs are preserved in a `20-66-raw/` sidecar."
    - "The panel adversarially prosecutes the CLASS B security change specifically: (1) does the deny path enforce denial ONLY by setting `PROTECTED_DACL_SECURITY_INFORMATION` and stripping every package-SID (`S-1-15-2-…`) ALLOW ace — INCLUDING inherited ones (so no inherited grant re-applies after PROTECT) — and NEVER by an inert package DENY ace or a re-enabled deny-only SID (which would resurrect the 'sandbox reads no file' regression)? (2) is revoke symmetric and complete — clears `PROTECTED` via `UNPROTECTED_DACL_SECURITY_INFORMATION`, restores inheritance, restores the original package-ALLOW for a still-granted parent, and leaves NO residual protection or grant on the host (the Phase-20 no-residual invariant)? (3) is a denied DIRECTORY protected per-object? (4) does the change leave the Allow (GRANT_ACCESS) path and the `IntentKind::Deny if intent.mask == ACL_READ_MASK` validation intact, with no regression to the passing `normal_sid_only_grant_is_denied` / granted-read / `unrelated_acl_survives_exact_sid_cleanup` invariants? (5) are there error-path leaks — a partial apply that leaves PROTECTED set or package-ALLOW stripped without a matching rollback in `apply_intents`' failure cleanup? Any single finding at any severity BLOCKS (REQ-native-r12/r13)."
    - "native_macos/native_windows are the ONLY deferred checks (proven at 20-67); code review of the 20-65 CLASS B delta, all-severity findings, evidence integrity, and integration authority are prosecuted here. ALL FOUR auditors MUST PASS with zero findings to admit 20-67; any single finding from any one auditor (any severity except the deferred native checks) stops the sequence and routes back to a repaired 20-65 delta. Fail-closed: if ANY external auditor (Codex Sol / Gemini Pro / Kimi K3) is unreachable or its output cannot be schema-validated, the gate records an explicit incomplete disposition and BLOCKS rather than emitting a false-green PASS from a partial panel; the internal Claude adversarial pass is one of the four required votes, never a substitute for a missing external auditor (CONTEXT D5 fail-safe) (REQ-native-r13)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-66-CROSS-AUDIT.md
      provides: "Four per-reviewer schema-validated pre-native cross-audits of the exact committed CLASS B fs_read_deny protected-DACL production delta, with raw outputs preserved"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-66-CROSS-AUDIT.md
      to: .planning/phases/20-transactional-delegated-mutation/20-65-SUMMARY.md
      via: "source/review-base/review authority tuple over the exact committed CLASS B acl_lease.rs delta recorded by 20-65"
      pattern: "source_sha"
---

<objective>
Cross-audit the exact committed CLASS B production delta (20-65: the `fs_read_deny` protected-DACL reimplementation in `acl_lease.rs`) before it consumes a scarce SEANDESKTOP native run: a FOUR-WAY independent panel — Codex 5.6 Sol, Gemini 3.1 Pro, and Kimi K3 (three external CLIs run in parallel), plus an internal Claude non-author adversarial reviewer — prosecutes the security change at every severity, and each auditor records its OWN schema-validated artifact. ALL FOUR MUST PASS; native explicitly deferred to 20-67.

Purpose: CLASS B is the single real production security defect from the 20-53 hardware run and the only one of the four fixes that touches production; a mistake here (an inert deny, a reintroduced deny-only-SID regression, an inherited-ALLOW that survives PROTECT, or an asymmetric revoke that leaves residual protection) is a real security or correctness fault. The panel puts an adversarial four-independent-auditor gate before the native proof so a broken security delta neither ships nor wastes the Sean-gated run; and R13 requires per-reviewer schema-validated artifacts, not prose.
Output: Four schema-validated artifacts over the exact committed CLASS B delta SHA — Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1`. Zero findings across ALL FOUR admits the SEANDESKTOP gate; if ANY external auditor is unreachable or schema-invalid, an explicit incomplete disposition (no false green from a partial panel).
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-VERIFICATION.md
@.planning/phases/20-transactional-delegated-mutation/20-65-PLAN.md
@.planning/phases/20-transactional-delegated-mutation/20-65-SUMMARY.md
@crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs
</context>

<execution_rules>
- The cross-audit is a FOUR-WAY panel of independent (non-author) reviewers; NONE is the plan author. Three are external CLIs, invoked verbatim and run in PARALLEL:
  1. Codex 5.6 Sol — `codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check "<prompt>"` (brief on stdin is acceptable)
  2. Gemini 3.1 Pro — `gemini -p "<prompt>" -m gemini-3.1-pro-preview -o text --approval-mode plan` (set `GEMINI_CLI_TRUST_WORKSPACE=true` and add `--skip-trust` to run in the untrusted checkout dir; 20-45/20-52/20-59/20-62 hit exit 55 without them)
  3. Kimi K3 — `/Users/seandonahoe/.kimi-code/bin/kimi -p "<prompt>" --output-format text` (ABSOLUTE path — the Bash shell PATH omits it; Kimi's -p mode does NOT ingest piped stdin, so pass the full brief as the -p argument; see the [[cross-auditor-kimi-k3]] project memory)
  The fourth is an internal Claude non-author adversarial subagent, prompted to REFUTE the candidate (default-refuted-if-uncertain).
- Every one of the four auditors MUST leave its OWN on-disk schema-validated artifact — Codex Sol → `f20-native-crossaudit.codex-sol`, Gemini Pro → `f20-native-crossaudit.gemini-pro`, Kimi K3 → `f20-native-crossaudit.kimi-k3`; the internal Claude adversarial leg emits `wayland-core.phase20-independent-review.v1`. A prose-only vote does not count toward PASS (REQ-native-r13). Preserve each auditor's raw output in a `20-66-raw/` sidecar.
- Scope every prompt to the EXACT committed CLASS B delta SHA/tree recorded by 20-65 (the `acl_lease.rs` `fs_read_deny` protected-DACL reimplementation), not the branch tip if it has advanced. The review base is that tuple.
- native_macos and native_windows are the ONLY checks that may be deferred (they are proven at 20-67). All other checks — code review of the CLASS B delta, all-severity findings, evidence integrity, integration authority — are prosecuted here by every auditor.
- DISPOSITION — ALL FOUR MUST PASS: only a zero-finding PASS from all four auditors admits 20-67. Any single finding from any one auditor (any severity except the deferred native checks) BLOCKS and routes back to a repaired 20-65 delta.
- FAIL-CLOSED: if ANY external auditor is unreachable or its output cannot be schema-validated, record an explicit incomplete disposition and BLOCK — do not synthesize a PASS from the remaining reviewers or from prose (CONTEXT D5 fail-safe). Do not modify any source/test file; this plan writes only the cross-audit artifact.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Four-way pre-native cross-audit panel of the CLASS B fs_read_deny protected-DACL delta (Codex Sol + Gemini Pro + Kimi K3 + Claude adversarial), all-must-pass, fail-closed</name>
  <files>.planning/phases/20-transactional-delegated-mutation/20-66-CROSS-AUDIT.md</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-65-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-65-PLAN.md, .planning/phases/20-transactional-delegated-mutation/20-VERIFICATION.md, crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs</read_first>
  <behavior>
    - Dispatch the FOUR-WAY independent panel over the exact committed CLASS B delta SHA/tree recorded by 20-65. Run the three external CLIs in parallel — Codex 5.6 Sol, Gemini 3.1 Pro (with `GEMINI_CLI_TRUST_WORKSPACE=true --skip-trust`), Kimi K3 (absolute path, brief in the -p argument) — plus an internal Claude non-author adversarial subagent prompted to REFUTE (default-refuted-if-uncertain). Scope every prompt to the `acl_lease.rs` `fs_read_deny` reimplementation and its predecessor finding: the hardware-proven inert package DENY ace.
    - Every auditor prosecutes adversarially: (1) deny enforced ONLY by `PROTECTED_DACL_SECURITY_INFORMATION` + package-SID (`S-1-15-2-`) ALLOW removal INCLUDING inherited entries, never by a package DENY ace or a re-enabled deny-only SID; (2) revoke symmetric and complete — clears PROTECTED via `UNPROTECTED_DACL_SECURITY_INFORMATION`, restores inheritance, restores the original package-ALLOW for a still-granted parent, no residual protection/grant; (3) a denied DIRECTORY protected per-object; (4) Allow path + `IntentKind::Deny if intent.mask == ACL_READ_MASK` validation intact, no regression to `normal_sid_only_grant_is_denied` / granted-read / `unrelated_acl_survives_exact_sid_cleanup`; (5) error-path integrity — `apply_intents`' failure cleanup rolls back a partial protect/strip so no half-applied deny leaks; and the concurrency case where an Allow and a Deny identity operate on overlapping trees does not interfere.
    - Capture each auditor's findings as its OWN schema-validated artifact (`f20-native-crossaudit.codex-sol` / `.gemini-pro` / `.kimi-k3`, and `wayland-core.phase20-independent-review.v1`). native_macos/native_windows are deferred, proven at 20-67. Preserve raw outputs in `20-66-raw/`.
    - Disposition — ALL FOUR MUST PASS: only a zero-finding PASS from all four admits 20-67; any single finding from any auditor blocks and routes back to a repaired 20-65 delta.
    - Fail-closed: if ANY external auditor is unreachable or its output fails schema validation, record an explicit incomplete disposition and block — no false-green PASS from a partial panel.
  </behavior>
  <action>Dispatch the four-way independent panel over the exact committed CLASS B delta SHA/tree recorded by 20-65: run the three external CLIs in parallel — Codex 5.6 Sol (`codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check "<prompt>"`), Gemini 3.1 Pro (`GEMINI_CLI_TRUST_WORKSPACE=true gemini -p "<prompt>" -m gemini-3.1-pro-preview -o text --approval-mode plan --skip-trust`), and Kimi K3 (`/Users/seandonahoe/.kimi-code/bin/kimi -p "<prompt>" --output-format text`) — plus an internal Claude non-author adversarial subagent prompted to REFUTE (default-refuted-if-uncertain), scoping each to the `acl_lease.rs` `fs_read_deny` protected-DACL reimplementation: deny enforced only by PROTECTED + package-ALLOW removal including inherited (never a package DENY ace or a re-enabled deny-only SID), symmetric complete revoke (UNPROTECTED + inheritance + original package-ALLOW restored, no residual), directory protected per-object, Allow path and the deny mask validation intact with no regression to the passing invariants, and error-path rollback integrity in `apply_intents`. Record each auditor's findings as its own schema-validated artifact in `20-66-CROSS-AUDIT.md` — `f20-native-crossaudit.codex-sol`, `f20-native-crossaudit.gemini-pro`, `f20-native-crossaudit.kimi-k3`, and the internal Claude `wayland-core.phase20-independent-review.v1` (native_macos/native_windows deferred) — preserving raw outputs under `20-66-raw/`. ALL FOUR must PASS zero-finding to admit 20-67; any single finding blocks and routes back to a repaired 20-65 delta. If ANY external auditor is unreachable or its output fails schema validation, record an explicit incomplete disposition and BLOCK — do not synthesize a PASS. Implements REQ-native-r12/r13; write only the cross-audit artifact.</action>
  <verify>
    <automated>test -f .planning/phases/20-transactional-delegated-mutation/20-66-CROSS-AUDIT.md</automated>
    <automated>grep -nE "f20-native-crossaudit\.codex-sol|f20-native-crossaudit\.gemini-pro|f20-native-crossaudit\.kimi-k3|phase20-independent-review|Codex 5\.6 Sol|Gemini 3\.1 Pro|Kimi K3|Claude adversarial|source_sha|deferred|fail.closed|PROTECTED_DACL|package.*ALLOW|revoke" .planning/phases/20-transactional-delegated-mutation/20-66-CROSS-AUDIT.md | head -40</automated>
  </verify>
  <acceptance_criteria>The cross-audit artifact exists and records FOUR per-reviewer results over the exact committed CLASS B delta SHA: Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1`, with native_macos/native_windows deferred, the deny-by-removal-and-PROTECT design confirmed (inherited ALLOW stripped, no deny-only SID re-enabled), symmetric complete revoke confirmed (no residual protection/grant), directory-per-object confirmed, Allow-path and invariants preserved, and error-path rollback intact. A zero-finding PASS from ALL FOUR admits 20-67; any single finding from any auditor, or any external auditor unreachable/schema-invalid, blocks with an explicit incomplete disposition.</acceptance_criteria>
  <done>The CLASS B production delta has a four-way per-reviewer schema-validated pre-native cross-audit (Codex Sol + Gemini Pro + Kimi K3 + Claude adversarial) confirming the deny-by-removal + symmetric revoke design with no regression, or an explicit fail-closed block routing back to 20-65.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native run authorization ← four-way cross-audit PASS | The scarce SEANDESKTOP run is spent only after ALL FOUR panel auditors pass; a false-green here ships or wastes-the-run on a broken security delta |
| attestation integrity ← per-reviewer artifact | A PASS must be backed by an on-disk schema-validated artifact per each of the four auditors, not prose |
| security-fix correctness ← adversarial prosecution | The deny-by-removal design must be confirmed genuinely sound (inherited ALLOW stripped, symmetric revoke, no deny-only-SID regression), not just plausible |
| panel independence ← external-auditor reachability | If ANY external auditor is down, the audit fails closed rather than passing on a partial panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-66-01 | Spoofing | prose-only or partial-panel PASS | high | mitigate | Each of the four auditors emits its own schema-validated artifact; a prose-only or missing artifact does not count, PASS requires ALL FOUR, raw outputs preserved. |
| T-20-66-02 | Repudiation | external auditor unreachable false-green | high | mitigate | Fail-closed: any unreachable or schema-invalid external-auditor output records an explicit incomplete disposition and blocks; no PASS synthesized from the remaining reviewers (CONTEXT D5 fail-safe). |
| T-20-66-03 | Information Disclosure | an inert or leaky deny passes the audit | high | mitigate | All four auditors prosecute that denial comes only from PROTECTED + package-ALLOW removal (incl. inherited) and that revoke is symmetric and complete — an inert deny, a surviving inherited ALLOW, or a residual PROTECT cannot pass a four-independent-auditor panel silently. |
| T-20-66-04 | Denial of Service | a reintroduced deny-only-SID regression passes | high | mitigate | The panel explicitly checks that no deny-only SID is re-enabled and that `normal_sid_only_grant_is_denied` / granted-read invariants are preserved. |
</threat_model>

<verification>
The cross-audit artifact `20-66-CROSS-AUDIT.md` determines acceptance: it must record FOUR per-reviewer results over the exact committed CLASS B delta SHA — Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1` — ALL zero-finding, with native_macos/native_windows deferred, the deny-by-removal + PROTECT design confirmed (inherited ALLOW stripped, no deny-only SID re-enabled), symmetric complete revoke confirmed, directory-per-object confirmed, Allow-path/invariants preserved, and error-path rollback intact. Record the committed `source_sha`/tree, the four reviewer identities, the three external CLI invocations, each artifact's schema-validation status, the raw-output sidecar identity, and the disposition (ALL FOUR PASS admits 20-67; any single finding or any external auditor unreachable/invalid blocks) in 20-66-SUMMARY.md. No source/test change; no native or requirement claim.
</verification>

<success_criteria>
- The four-way panel (Codex 5.6 Sol + Gemini 3.1 Pro + Kimi K3 external in parallel, plus the internal Claude adversarial reviewer) cross-audits the exact committed CLASS B delta at every severity, and each auditor records its own schema-validated artifact (r12/r13).
- The deny-by-removal + PROTECT design is confirmed sound (inherited package-ALLOW stripped, no deny-only-SID regression, symmetric complete revoke, directory-per-object, Allow-path and invariants preserved, error-path rollback intact); native_macos/native_windows are the only deferred checks.
- A zero-finding PASS from ALL FOUR reviewers admits 20-67; any single finding from any auditor, or any external auditor unreachable/schema-invalid, blocks with an explicit incomplete disposition routing back to 20-65 (no false green from a partial panel).
</success_criteria>

## Artifacts this phase produces
- The four pre-native cross-audits of the CLASS B `fs_read_deny` protected-DACL production delta: Codex Sol `f20-native-crossaudit.codex-sol`, Gemini Pro `f20-native-crossaudit.gemini-pro`, Kimi K3 `f20-native-crossaudit.kimi-k3`, and the internal Claude adversarial `wayland-core.phase20-independent-review.v1`, with raw outputs preserved.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-66-SUMMARY.md` using the standard GSD summary template. Record the committed `source_sha`/tree, the four reviewer identities and each artifact's schema-validation status, the CLASS B design verdict (deny-by-removal + PROTECT sound; symmetric revoke; no regression; error-path rollback), the raw-output sidecar identity, and the disposition (ALL-FOUR PASS admits 20-67, or fail-closed incomplete routing back to 20-65). Complete no Phase 20 requirement.
</output>
</content>

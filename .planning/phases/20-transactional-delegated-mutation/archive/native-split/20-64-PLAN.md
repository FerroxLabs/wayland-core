---
phase: 20-transactional-delegated-mutation
plan: "64"
type: execute
wave: 62
depends_on:
  - "20-53"
files_modified:
  - crates/wcore-sandbox/tests/hard_process_containment_windows.rs
autonomous: true
gap_closure: true
requirements:
  - REQ-native-r7
  - REQ-native-r4
must_haves:
  truths:
    - "CLASS D (test-observer, hardware-proven at 20-53 SEANDESKTOP): the two host-side observers that still identify the test's own sandboxed process tree by WMI `Win32_Process.CommandLine` — `tagged_cmd_count` (line 119, predicate `CommandLine -like '*{tag}*'` line 128) and `tagged_choice_descendant_pids` (line 181, predicate line 190) — no longer key on CommandLine. `Win32_Process.CommandLine` is NULL for the Low-IL AppContainer restricted-token processes the sandbox spawns (proven NULL even from SYSTEM context, so it fails on the CI runner too, not just over SSH), so the `rem {tag}` handshake never matched → 20s poll → panic. They now key on a PID HANDSHAKE: the sandbox fan-out script writes the ProcessId of each descendant idler to a host-readable, AppContainer-writable handshake file (the backend remaps TEMP/TMP into LOCALAPPDATA via command.rs `build_env_block`), and the host reads the captured PID set from that file, then detects liveness/reap by `Win32_Process` `ProcessId` ∩ image `Name` (both WMI-readable) — mirroring `surviving_captured_choice_pids` (line 238), which already works precisely because it filters by fixed ProcessId, never CommandLine (REQ-native-r7)."
    - "Every element the 20-45/20-52/20-59 panels confirmed SOUND is preserved VERBATIM: the two-phase reap logic (alive-phase capture + fixed-PID post-close re-check), `surviving_captured_choice_pids`'s `if pids.is_empty() { return 0; }` short-circuit, `!captured_pids.is_empty()`, the peak/cap/below-attempts/clean-exit assertions, the sibling `job_close_reaps_detached_descendant_with_no_residue` / `breakaway_is_denied` / `qualified_hard_containment_backend_preflight` tests, and the full fail-closed hardening added at 20-57 (Rust layer: `assert!(out.status.success())` + panicking parse, no `unwrap_or(0)`, no `filter_map(...ok())`) and 20-60 (PowerShell layer: `$ErrorActionPreference='Stop'` + `-ErrorAction Stop` on every `Get-CimInstance` + a leading `trap { exit 1 }`). Any residual `Win32_Process` query the handshake still needs (liveness by ProcessId ∩ Name) keeps that same fail-closed shape; the only `.unwrap_or(0)` remains the benign `unique_tag` nanos fallback and `reap_stray_choice` stays best-effort cleanup (REQ-native-r7)."
    - "The change is test-only, confined to `crates/wcore-sandbox/tests/hard_process_containment_windows.rs`, inside `#![cfg(windows)]`; production `AppContainerBackend` containment is PROVEN SOUND on hardware (`contained_detached_child_exit` passes in 0.35s; Job Object KILL_ON_JOB_CLOSE + TerminateJobObject reaps the detached tree) and is not touched. No Linux/macOS regression; behavior proven at 20-67 on SEANDESKTOP (REQ-native-r4)."
  artifacts:
    - path: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      provides: "CommandLine-free PID-handshake observers so the Windows Job-Object containment acceptance suite can observe the working sandbox on hardware without depending on the NULL AppContainer CommandLine"
  key_links:
    - from: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      to: .planning/phases/20-transactional-delegated-mutation/20-53-SUMMARY.md
      via: "closes the CLASS D hardware-enumerated observer failure from the 20-53 SEANDESKTOP native run recorded in 20-VERIFICATION.md"
      pattern: "gap_closure"
---

<objective>
Close CLASS D (containment observer keys on WMI CommandLine, which is NULL for AppContainer processes) from the 20-53 SEANDESKTOP native run (20-VERIFICATION.md). It is a hardware-proven TEST-OBSERVER bug — the containment is genuinely working, but two observers could not see the test's own sandboxed tree.

Purpose: `job_close_reaps_detached_descendant_with_no_residue`, `breakaway_is_denied`, and `active_process_cap_is_enforced` time out because `tagged_cmd_count` and `tagged_choice_descendant_pids` filter `Win32_Process` by `CommandLine -like '*{tag}*'`, and CommandLine is NULL for the Low-IL AppContainer restricted-token processes the sandbox spawns (NULL even from SYSTEM context, so it also fails on the CI runner). Switching those two observers to the same fixed-PID identification `surviving_captured_choice_pids` already uses removes the CommandLine dependency while keeping the reap check non-vacuous and falsifiable. No production change; the sandbox is fine.
Output: A test-only, `#![cfg(windows)]`-confined rework of the two CommandLine observers into a PID handshake, committed to the repair branch; real behavior proven at 20-67 on SEANDESKTOP.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-VERIFICATION.md
@crates/wcore-sandbox/tests/hard_process_containment_windows.rs
@.planning/phases/20-transactional-delegated-mutation/20-57-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-60-SUMMARY.md
</context>

<execution_rules>
- Test-only. Touch ONLY `crates/wcore-sandbox/tests/hard_process_containment_windows.rs`. No production source; no other test file.
- NO cargo/clippy/nextest on this Mac (CONTEXT D5). `cargo fmt` is permitted. The file is `#![cfg(windows)]`; real compile AND behavior are proven at 20-67 on SEANDESKTOP.
- Preserve every panel-confirmed-sound element VERBATIM: the two-phase reap logic, `!captured_pids.is_empty()`, the `if pids.is_empty() { return 0; }` short-circuit, the peak/cap/below-attempts/clean-exit assertions, the three sibling tests, and the 20-57 Rust-layer + 20-60 PowerShell-layer fail-closed hardening. Do NOT weaken any assertion under cover of this rework.
- Keep every remaining host-side `Win32_Process` query fail-closed in the same shape 20-57/20-60 established: assert the querying process exits success and panic on a non-success exit / unparseable output; never map a failed query to a passing count or an empty PID set.
- Do NOT change this file's own detached idler HOLD mechanism on the theory of CLASS C — VERIFICATION.md confirms these idlers inherit a pipe and hold correctly; CLASS C's `choice`→`waitfor` fix belongs only to `live_fs_acl.rs`. If (and only if) the PID handshake requires the descendant to self-report its own ProcessId, the idler image may change; when it does, update the `surviving_captured_choice_pids` image-`Name` filter and the `reap_stray_choice` best-effort cleanup to the same image consistently, and keep the two-phase reap semantics identical.
- Use `/usr/bin/git`; never stage the AGENTS.md ijfw churn.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Emit a descendant-PID handshake from the fan-out script into the AppContainer-writable temp dir</name>
  <files>crates/wcore-sandbox/tests/hard_process_containment_windows.rs</files>
  <read_first>crates/wcore-sandbox/tests/hard_process_containment_windows.rs, .planning/phases/20-transactional-delegated-mutation/20-VERIFICATION.md</read_first>
  <behavior>
    - Each descendant idler the fan-out spawns records its own ProcessId into a per-test handshake file located in the sandbox's writable temp dir (the backend remaps TEMP/TMP into LOCALAPPDATA via command.rs `build_env_block`, so a path under `%TEMP%` is both AppContainer-writable and host-readable).
    - The host resolves the same handshake path (the host-visible LOCALAPPDATA temp mapping) and reads the captured PID set from it, rather than issuing a `Win32_Process.CommandLine` query.
    - The alive-phase capture still happens WHILE the tagged parent is alive; a concurrent `choice`-spawning target on the same runner cannot pollute the capture because the captured set is the exact PIDs this test's own descendants wrote.
  </behavior>
  <action>Rework the fan-out scripts used by `active_process_cap_is_enforced` (script at line 424) and the residue test (script at line 359-363) so each spawned descendant idler writes its own ProcessId to a per-test handshake file under the sandbox's writable temp dir. Because the backend remaps TEMP/TMP into LOCALAPPDATA (command.rs `build_env_block`), a path built from `%TEMP%` (plus a unique per-test filename derived from the existing `unique_tag`) is AppContainer-writable inside the sandbox and readable from the host at the corresponding LOCALAPPDATA lease temp location. A robust self-reporting idler is a PowerShell one-liner that appends `$PID` to the handshake file and then holds stdin-free (`Start-Sleep`), e.g. spawned via `start "" /b powershell -NoProfile -NonInteractive -Command "..."`; the executor may choose any mechanism that (a) writes each descendant's real ProcessId to the handshake file and (b) holds the descendant alive for the peak-sampling window without depending on stdin — provided the descendants remain a bounded fan-out under the Job Object so the active-process-cap assertion still exercises the cap. Add a host-side helper that resolves the handshake path and parses the captured PID set fail-closed (panic on a read/parse error, not a swallow). Per 20-VERIFICATION.md CLASS D. Keep the `rem {tag}` marker only if a sibling still needs it; the CommandLine predicate is removed in Task 2.</action>
  <verify>
    <automated>cargo fmt --all --check</automated>
    <automated>grep -nE "TEMP|Local(AppData|)|handshake|ProcessId|\\$PID" crates/wcore-sandbox/tests/hard_process_containment_windows.rs | head -20</automated>
  </verify>
  <done>The fan-out descendants self-report their ProcessIds into a per-test handshake file in the AppContainer-writable temp dir; a fail-closed host-side reader resolves and parses that file. fmt clean. Behavior proven at 20-67.</done>
</task>

<task type="auto">
  <name>Task 2: Rekey tagged_cmd_count and tagged_choice_descendant_pids off the PID handshake, preserving fail-closed shape and every sound assertion</name>
  <files>crates/wcore-sandbox/tests/hard_process_containment_windows.rs</files>
  <read_first>crates/wcore-sandbox/tests/hard_process_containment_windows.rs</read_first>
  <behavior>
    - `tagged_choice_descendant_pids` returns the captured descendant ProcessIds from the handshake file (the alive-phase set), intersected with liveness by `Win32_Process` `ProcessId` ∩ image `Name` — never by CommandLine.
    - `tagged_cmd_count` reports the count of the test's own live tagged descendants by fixed ProcessId ∩ image Name, so a NULL CommandLine can no longer make the predicate never-match.
    - The post-close survivor check remains `surviving_captured_choice_pids` (already fixed-PID), so the two-phase reap stays: capture-while-alive, then confirm every captured ProcessId gone after job close.
  </behavior>
  <action>Replace the `Win32_Process.CommandLine -like '*{tag}*'` identification in `tagged_cmd_count` (predicate line 128) and `tagged_choice_descendant_pids` (predicate line 190) with the PID handshake from Task 1: read the captured descendant ProcessId set from the handshake file, then determine liveness/count by querying `Win32_Process` filtered by fixed `ProcessId` intersected with the descendant image `Name` (both WMI-readable under the Low-IL token), exactly as `surviving_captured_choice_pids` (line 238) already does. Keep the 20-60 PowerShell-layer fail-closed shape on any retained CIM query (`$ErrorActionPreference='Stop'`, `-ErrorAction Stop` on every `Get-CimInstance`, leading `trap { exit 1 }`) and the 20-57 Rust-layer shape (`assert!(out.status.success())`, panicking parse, no `unwrap_or(0)`, no `filter_map(...ok())`). Preserve VERBATIM the two-phase reap logic, `!captured_pids.is_empty()`, the `if pids.is_empty() { return 0; }` short-circuit, the peak/cap/below-attempts/clean-exit assertions, and the sibling `job_close_reaps_detached_descendant_with_no_residue` / `breakaway_is_denied` / `qualified_hard_containment_backend_preflight` tests. If Task 1 changed the idler image, update the `Name` filter here and in `reap_stray_choice` consistently. The only permitted `.unwrap_or(0)` remains the benign `unique_tag` nanos fallback. Per 20-VERIFICATION.md CLASS D.</action>
  <verify>
    <automated>cargo fmt --all --check</automated>
    <automated>grep -nE "handshake|read_to_string|ProcessId -contains|ParentProcessId" crates/wcore-sandbox/tests/hard_process_containment_windows.rs | head</automated>
    <automated>grep -nE "out.status.success|ErrorActionPreference='Stop'|ProcessId|if pids.is_empty" crates/wcore-sandbox/tests/hard_process_containment_windows.rs | head -30</automated>
    <automated>grep -nE "!captured_pids.is_empty|surviving_captured_choice_pids|job_close_reaps_detached_descendant_with_no_residue|breakaway_is_denied|qualified_hard_containment_backend_preflight" crates/wcore-sandbox/tests/hard_process_containment_windows.rs | head</automated>
  </verify>
  <done>Neither observer keys on `CommandLine`; both identify the test tree by fixed ProcessId ∩ image Name from the handshake; the fail-closed Rust+PowerShell shape and every panel-confirmed-sound assertion/sibling test are preserved. fmt clean. Reap non-vacuity proven at 20-67.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| containment observation ← process identification | The reap/cap observers must be able to identify the test's own sandboxed tree; keying on NULL CommandLine makes them blind and their timeouts masquerade as containment failures |
| reap non-vacuity ← captured-PID set | The post-close reap must confirm a NON-EMPTY captured set is gone; the handshake must supply real descendant PIDs, not an empty set that passes vacuously |
| fail-closed query ← retained CIM calls | Any residual host query must fail the test on error, never map a failed query to a passing count |
| test-only change ← production untouched | Production containment is proven sound; the rework must not touch `AppContainerBackend` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-64-01 | Spoofing | NULL-CommandLine predicate never matches → false containment failure | high | mitigate | Observers rekey off a PID handshake + fixed ProcessId ∩ image Name (WMI-readable under Low-IL), mirroring the already-working `surviving_captured_choice_pids`. |
| T-20-64-02 | Repudiation | vacuous reap (empty captured set) passes without evidence | high | mitigate | The handshake supplies the real descendant PIDs; `!captured_pids.is_empty()` and the two-phase capture/re-check are preserved verbatim; proven non-vacuous on hardware at 20-67. |
| T-20-64-03 | Tampering | a retained CIM query silently reads a failure as count 0 | high | mitigate | The 20-57 Rust-layer (`assert!(out.status.success())` + panicking parse) and 20-60 PowerShell-layer (`$ErrorActionPreference='Stop'` + `-ErrorAction Stop` + `trap`) fail-closed shape is preserved on every retained query. |
| T-20-64-04 | Tampering | assertion weakened under cover of the rework | medium | mitigate | Every panel-confirmed-sound assertion and the three sibling tests are preserved verbatim; the 20-66/20-67 gates re-prosecute; grep verifies their presence. |
</threat_model>

<verification>
Local gates: `cargo fmt --all --check` clean; zero `CommandLine -like` predicates remain; the fail-closed markers (`out.status.success`, `$ErrorActionPreference='Stop'`, `trap { exit 1 }`) and the sound assertions/sibling tests still present by grep. The authoritative behavioral proof (the three containment tests green with a non-vacuous captured-PID reap) is deferred to 20-67 on SEANDESKTOP (`cargo nextest run -p wcore-sandbox --test hard_process_containment_windows --run-ignored all`), never on the Mac. Record the handshake mechanism, the two rekeyed observers, the preserved assertions, and the committed SHA in 20-64-SUMMARY.md. No production change; no requirement completed here (closure claimed only after 20-67 hardware green).
</verification>

<success_criteria>
- `tagged_cmd_count` and `tagged_choice_descendant_pids` no longer key on `Win32_Process.CommandLine`; both identify the test tree by fixed ProcessId ∩ image Name via the handshake, mirroring `surviving_captured_choice_pids` (Class D closed at the source) (REQ-native-r7).
- The two-phase reap, `!captured_pids.is_empty()`, the empty-set short-circuit, the peak/cap/below-attempts/clean-exit assertions, the three sibling tests, and the 20-57/20-60 fail-closed hardening are preserved verbatim (REQ-native-r7).
- Change is test-only and `#![cfg(windows)]`-confined; no Linux/macOS regression; fmt clean; committed for the SEANDESKTOP verify at 20-67 (REQ-native-r4).
</success_criteria>

## Artifacts this phase produces
- CommandLine-free PID-handshake observers in `crates/wcore-sandbox/tests/hard_process_containment_windows.rs`, committed to the repair branch.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-64-SUMMARY.md` using the standard GSD summary template. Record the PID-handshake mechanism (descendant self-report into the remapped-TEMP handshake file; host read + liveness by ProcessId ∩ Name), the two rekeyed observers, the preserved sound assertions/sibling tests and fail-closed shape, whether the idler image changed (and the consistent `Name`/`reap_stray_choice` update if so), and the committed SHA. Complete no Phase 20 requirement (closure claimed only after 20-67 hardware green).
</output>
</content>

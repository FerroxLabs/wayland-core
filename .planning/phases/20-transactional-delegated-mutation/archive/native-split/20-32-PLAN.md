---
phase: 20-transactional-delegated-mutation
plan: "32"
type: execute
wave: 31
depends_on:
  - "20-31"
files_modified: []
autonomous: false
requirements:
  - REQ-native-r14
  - REQ-native-r4
  - REQ-native-r7
  - REQ-native-r9
  - REQ-native-r11
  - REQ-native-r3
must_haves:
  truths:
    - "One FRESH Sean-authorized native-proof dispatch runs the exact sealed repaired-successor on real hardware: Windows on the AppContainer-capable self-hosted msvc runner and macOS on a freshly re-registered ephemeral Apple-silicon runner; native proof is never claimed from cross-compilation or source inspection (CONTEXT D6/D7, REQ-native-r11). The prior 95c81ec6 native-proof-dispatch authorization is spent/void; this digest requires its own fresh authorization."
    - "Before the scarce run is spent, the repaired-successor macOS compile is re-confirmed green on THIS Mac (the one sanctioned targeted cargo check) so a compile error like the 20-25 E0599 can never burn another native run; the Windows leg's safe.directory guard is present so the self-hosted runner clears setup and reaches the proof (REQ-native-r14/r9/r11)."
    - "The native run re-proves on hardware BOTH the 2026-07-23 AppContainer findings (is_available true; read/grant/revoke green; genuine DENY blocks; normal-SID-only denied; real Job-Object containment) AND the two 20-25 RED causes now fixed — the macOS wcore-sandbox lib COMPILES and the macos-retained-directory rename acceptance is green (handle-relative, decoy-immune), and the Windows candidate job runs past setup on the self-hosted msvc runner (REQ-native-r14/r7/r9/r3)."
    - "All Windows native targets are green on the self-hosted msvc runner and all macOS native targets are green on the ephemeral runner, with ordered per-target and final commit/tree/nonce markers bound to the exact sealed successor SHA; the UAT evidence ref is retained (deletion not authorized) (REQ-native-r4)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-32-SUMMARY.md
      provides: "Retained dual-OS native evidence bound to the exact sealed repaired-successor for the fresh review (20-33)"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-30-SUMMARY.md
      to: .planning/phases/20-transactional-delegated-mutation/20-32-SUMMARY.md
      via: "the sealed successor SHA is published to a temporary UAT ref + dispatch branch and dispatched to native runners"
      pattern: "source_sha"
---

<objective>
Produce real native evidence for the sealed repaired-successor: one FRESH Sean-authorized native-proof dispatch that runs Windows (self-hosted msvc) and macOS (freshly re-registered ephemeral) acceptance on hardware, re-proving the two now-fixed 20-25 RED causes plus the AppContainer/Job-Object findings, so the fresh 20-33 review can review native evidence rather than defer it.

Purpose: The audit requires the fresh review NOT to defer native (R13/R12); that is only possible if native evidence exists at review time. This is the re-dispatch of the native-proof gate for the repaired successor — Sean-only (D6), with a fresh digest-specific authorization (the 95c81ec6 authorization is spent/void), distinct from the terminal exact-tuple authorization at 20-35.
Output: Retained, candidate-bound dual-OS native evidence, or an explicit incomplete disposition; no requirement completion here.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
@/Users/seandonahoe/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-25-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-29-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-30-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-31-SUMMARY.md
@scripts/f20-native-windows-proof.ps1
@scripts/f20-native-macos-proof.sh
@scripts/f20-native-uat-proof.mjs
@.github/workflows/nightly-windows-soak.yml
</context>

<admission>
- Require the exact sealed successor SHA/tree from `20-30-SUMMARY.md`, the zero-finding `f20-native-crossaudit` from `20-31-SUMMARY.md`, and the 20-29 macOS compile-check + Hetzner clippy receipts; any drift, non-PASS cross-audit, or unclean checkout stops before the checkpoint.
- Pre-dispatch guard (before the checkpoint): re-run the one sanctioned Mac compile check `cargo check -p wcore-sandbox --features live-docker --tests` against the sealed successor and require it green, and confirm the candidate workflow carries the `safe.directory` guard in the `f20-windows-candidate` job before its tree-resolve. A red compile check or missing guard stops here — a scarce native run is never spent on a candidate that cannot compile or clear Windows setup.
- The native-proof dispatch is Sean-only and digest-specific. No prior general approval, silence, the spent 95c81ec6 authorization, or approval for another candidate is authority. Only Sean's explicit native-proof-dispatch authorization for THIS sealed successor digest may run Task 2.
- Dispatch mechanics (learned at 20-25 — do not rediscover): `workflow_dispatch --ref` resolves only branches/tags, NOT the `refs/f20-native-uat/*` evidence namespace (that returns HTTP 422 "No ref found" and creates no run). Publish the sealed SHA to its `refs/f20-native-uat/<sha>` evidence ref AND create a dispatch BRANCH `f20-native-uat-<full-sha>` at the same sealed SHA; dispatch on the branch so `github.sha == sealed SHA`. Both point at the same immutable commit.
- Re-entry after authorization uses the same durable dispatch/run state: it may capture an already-dispatched exact attempt but must never dispatch or retry a second run.
- The Windows leg MUST land on `[self-hosted, Windows, X64, msvc]` (AppContainer-capable); the macOS leg on the freshly re-registered pinned ephemeral runner. A hosted `windows-2022` landing is a failure, not a fallback.
</admission>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Sean authorizes the FRESH native-proof dispatch for the sealed repaired-successor</name>
  <files>None; this checkpoint runs no command and changes no file or external state.</files>
  <decision>Authorize one native-proof dispatch of the exact sealed repaired-successor to its temporary UAT ref + dispatch branch and one native-proof workflow run (Windows self-hosted msvc + macOS freshly-registered ephemeral), or provide no authorization and leave the native path unproven.</decision>
  <context>20-30 sealed the repaired successor (Linux 11509/0 + all-features build), 20-31 cross-audited it CLEAN with native deferred, and the Mac compile check is green + the Windows safe.directory guard is in place. This gate produces real native evidence so the fresh review (20-33) need not defer native. The prior 95c81ec6 native-proof-dispatch authorization is spent/void; this is a fresh digest-specific authorization, separate from the terminal exact-tuple authorization at 20-35.</context>
  <options>
    <option id="authorize"><name>Authorize repaired-successor native-proof dispatch</name><pros>Produces real Windows+macOS evidence re-proving the two fixed RED causes so the fresh review need not defer native.</pros><cons>Publishes one temporary UAT ref + dispatch branch and consumes the self-hosted msvc + freshly-registered ephemeral macOS runners.</cons></option>
  </options>
  <action>Run no command. Display the exact sealed successor commit/tree, the target runner labels (self-hosted msvc + ephemeral macOS image label), the request nonce/digest, and confirmation that the Mac compile check is green and the Windows safe.directory guard is present, then stop. Continue only when Sean explicitly authorizes THIS sealed successor digest's native-proof dispatch using the displayed digest.</action>
  <verify>The response is Sean's explicit native-proof-dispatch authorization matching the displayed sealed-successor digest. Bare approval, silence, the spent 95c81ec6 authorization, or an approval for a different candidate fails this checkpoint and ends execution.</verify>
  <done>The explicit fresh native-proof-dispatch authorization exists; otherwise the plan remains incomplete and Task 2 does not run.</done>
  <resume-signal>Authorize the native-proof dispatch for the exact displayed sealed successor digest. Any other response leaves the native path unproven.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Dispatch, capture, and verify dual-OS native evidence for the sealed successor</name>
  <files>None; this task produces candidate-bound external evidence and Git-private authority objects.</files>
  <read_first>scripts/f20-native-uat-proof.mjs, scripts/f20-native-windows-proof.ps1, scripts/f20-native-macos-proof.sh, .github/workflows/nightly-windows-soak.yml, .planning/phases/20-transactional-delegated-mutation/20-25-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-29-SUMMARY.md</read_first>
  <behavior>
    - Publication and dispatch use the durable one-attempt state machines in `f20-native-uat-proof.mjs`: publish the exact sealed successor SHA to its temporary `refs/f20-native-uat/<sha>` evidence ref only if absent (reuse an exact ref; conflicting ref fails), AND create the dispatch branch `f20-native-uat-<full-sha>` at the same sealed SHA; persist+sync dispatch intent (digest, full SHA/tree/ref/workflow, nonce, API boundary, pre-existing run set) before the single dispatch; dispatch on the BRANCH (`--ref f20-native-uat-<full-sha>`), never the `refs/f20-native-uat/*` namespace; after intent, reconcile exactly one post-boundary run carrying the nonce and never dispatch again.
    - `capture-verify` validates the Windows job ran on the self-hosted msvc runner and the macOS job on the freshly re-registered ephemeral runner (which deregisters after its one job), with ordered per-target and final commit/tree/nonce markers for every Windows and macOS target — including the Job-Object containment targets and the macOS targets.
    - The run re-proves on hardware the two now-fixed 20-25 RED causes: the macOS `wcore-sandbox` lib COMPILES and `macos-retained-directory` (the rename acceptance) is green (handle-relative, decoy-immune), AND the Windows candidate job clears the git `safe.directory`/dubious-ownership setup on the self-hosted msvc runner and reaches the proof. A repeat of either RED (E0599 on macOS, or a Windows setup abort before the proof) is a native failure.
    - The UAT evidence ref is retained; its remote SHA is rechecked after capture and recorded `retained` / `deletion_not_authorized`. The dispatch branch is left in place (no cleanup authorization).
  </behavior>
  <action>Persist the fresh native-proof-dispatch authorization for the exact sealed successor digest. Publish the sealed SHA to its temporary UAT evidence ref (first-attempt-only; reconcile-only after intent) and create the dispatch branch at the same SHA, then dispatch the native-proof workflow once on the branch (Windows self-hosted msvc + macOS ephemeral). Capture and verify: bind the Windows and macOS job/runner identities, require the self-hosted msvc runner for Windows and the freshly-registered ephemeral runner for macOS, validate ordered per-target + final markers for every target, and confirm on hardware both the AppContainer read/grant/revoke/isolation + Job-Object containment AND the two fixed RED causes (macOS lib compiles + rename acceptance green; Windows setup clears). Recheck and retain the exact evidence ref. Do NOT complete any requirement, run the aggregate (that is 20-35), retry, repair source, or delete the ref/branch. Any missing/skipped/wrong-runner/incomplete-marker evidence fails closed; a native RED requires a further repaired successor and fresh authorization.</action>
  <verify>
    <automated>node scripts/f20-native-uat-proof.mjs verify-publication --git-dir "$(git rev-parse --absolute-git-dir)" --repo FerroxLabs/wayland-core</automated>
    <automated>node scripts/f20-native-uat-proof.mjs verify-evidence --git-dir "$(git rev-parse --absolute-git-dir)" --repo FerroxLabs/wayland-core --workflow nightly-windows-soak.yml</automated>
  </verify>
  <acceptance_criteria>Both native platforms are green on the correct runners against the exact sealed successor source, with complete ordered markers and a retained evidence ref. Any other state is incomplete and blocks the fresh review.</acceptance_criteria>
  <done>The sealed successor has retained, candidate-bound dual-OS native evidence for 20-33 to review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native evidence ← runner host | Only a real AppContainer-capable Windows host and a real macOS host may produce native proof; cross-compilation/source cannot |
| dispatch authority ← Sean | The native-proof dispatch is a Sean-only external mutation; the prior digest's authorization is spent/void |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-32-01 | Spoofing | native PASS provenance | critical | mitigate | Markers bind exact SHA/tree/nonce; Windows must run on self-hosted msvc and macOS on the freshly-registered ephemeral runner; a hosted-image landing or missing marker fails closed. Native is never claimed from cross-compilation (D7). |
| T-20-32-02 | Elevation of Privilege | unauthorized/stale dispatch | high | mitigate | Blocking-human checkpoint; only Sean's fresh digest-specific authorization runs Task 2; the spent 95c81ec6 authorization is void; re-entry is reconcile-only, never a second dispatch. |
| T-20-32-03 | Denial of Service | burned run on non-compiling candidate | high | mitigate | Pre-dispatch guard re-runs the sanctioned Mac compile check and confirms the Windows safe.directory guard before the scarce run is authorized, so a compile error or setup abort cannot burn the run. |
| T-20-32-SC | Tampering | UAT evidence ref | high | mitigate | One-attempt publish/dispatch state machines; ref retained and rechecked; deletion not authorized. |
</threat_model>

<verification>
Authenticate the sealed successor source/tree, the pre-dispatch Mac compile check + Windows guard, the single native attempt bound by nonce, the correct runner identities, complete ordered markers, and the retained ref. Record the retained UAT ref, dispatch branch, run id, per-target results, and both platforms' green/red disposition in `20-32-SUMMARY.md`. No Cargo runs on the Mac except the sanctioned pre-dispatch compile check; no requirement completes here.
</verification>

<success_criteria>
- One FRESH Sean-authorized native-proof dispatch ran the exact sealed successor on the self-hosted msvc runner (Windows) and the freshly-registered ephemeral runner (macOS) (r11).
- Both platforms are green, re-proving the AppContainer/Job-Object findings AND the two fixed 20-25 RED causes on hardware (r14/r7/r9/r3/r4).
- The evidence ref is retained (deletion not authorized) and bound to the exact sealed successor SHA for the fresh review.
</success_criteria>

## Artifacts this phase produces
- Retained candidate-bound dual-OS native evidence and Git-private dispatch authority objects for the repaired successor.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-32-SUMMARY.md` using the standard GSD summary template. Record the sealed source/tree, dispatch branch/ref + run identity, per-target native results for both platforms, the retained UAT ref, and the explicit non-claim of requirement completion. If either platform is RED, record INCOMPLETE and route to a further repaired successor + fresh authorization.
</output>

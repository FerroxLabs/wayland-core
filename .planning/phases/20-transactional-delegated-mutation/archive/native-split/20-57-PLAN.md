---
phase: 20-transactional-delegated-mutation
plan: "57"
type: execute
wave: 52
depends_on:
  - "20-52"
files_modified:
  - crates/wcore-sandbox/tests/hard_process_containment_windows.rs
autonomous: true
requirements:
  - REQ-native-r7
  - REQ-native-r4
must_haves:
  truths:
    - "The 20-52 BLOCK finding (Codex MEDIUM / Claude LOW, non-refutable) is CLOSED: in `crates/wcore-sandbox/tests/hard_process_containment_windows.rs`, EVERY host-side query observer that shells out and parses a count/PID-list now FAILS CLOSED on a query failure. `tagged_cmd_count`, `tagged_choice_descendant_pids`, and `surviving_captured_choice_pids` each assert `out.status.success()` after `.output()`, and treat a non-success exit — and, for the two `.Count` observers, empty/unparseable stdout — as a hard test failure (panic) instead of `parse().unwrap_or(0) => 0` or a silently-empty `Vec`. A post-close CIM/PowerShell query failure can therefore no longer satisfy `wait_until(... == 0)` and report the reap property proven WITHOUT evidence (REQ-native-r7)."
    - "The hardening is COMPREHENSIVE / single-class: ALL THREE query observers in the file are fixed in ONE pass (not only the two the finding named — `surviving_captured_choice_pids` + `tagged_cmd_count`), closing the fail-open class so a re-audit cannot surface a third instance. A full-file sweep confirms the only remaining `.unwrap_or(0)` is the `unique_tag` SystemTime-nanos fallback — NOT a query observer, it merely seeds a tag suffix — and `reap_stray_choice` stays intentionally best-effort cleanup (`let _ = ...output()`); both are explicitly out of scope and unchanged (REQ-native-r7)."
    - "Fix vs scope-change distinguished: only the three shared helper BODIES change (adding the `out.status.success()` check and the panic-on-failure); NO test body and NO assertion is altered. The captured-PID reap logic (alive-phase capture + fixed-PID post-close re-check), `assert!(!captured_pids.is_empty())`, the peak/cap/below-attempts/clean-exit assertions, the `if pids.is_empty() { return 0; }` legitimate empty-set short-circuit, and the sibling `job_close_reaps_detached_descendant_with_no_residue` / `breakaway_is_denied` / `qualified_hard_containment_backend_preflight` tests are preserved VERBATIM — the 20-52 panel confirmed every one of these sound (REQ-native-r7)."
    - "OUT OF SCOPE and untouched (all four 20-52 legs confirmed sound): the Finding-2 `command.rs` cmd classifier (`resolved_program_is_cmd`), both isolation guardrails (#A DispatchAdmission budget in `worktree_manager.rs`; #B quoting-layer-only — `is_unc_or_device_path`, Low-IL token, Job-Object limits, ACL lease, argv discipline, the 20-36 `exit /b 0`). This delta touches ONE file only; the production sandbox boundary is not modified (REQ-native-r7)."
    - "Linux unregressed and HONESTLY DEFERRED: the file is `#![cfg(windows)]`, so zero Linux compiled-output change — the Hetzner aggregate stays 11509/0/48 and the sanctioned macOS `cargo check -p wcore-sandbox --features live-docker --tests` stays green; NEITHER the Mac nor Hetzner Linux compiles a line of the observer fix. Its compile AND real-hardware behavior are proven ONLY at the 20-53 native msvc re-dispatch. The Codex LOW post-close PID-reuse race is carried as a fail-closed 20-53 native watch-item (it can only false-FAIL, never false-green), NOT expanded in scope here (REQ-native-r4)."
  artifacts:
    - path: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      provides: "All three host-side query observers (tagged_cmd_count, tagged_choice_descendant_pids, surviving_captured_choice_pids) hardened to fail CLOSED on a CIM/PowerShell query failure (out.status.success() check + panic on non-success/unparseable), closing the 20-52 residual reap-observer false-green; every assertion body and sibling test preserved verbatim"
  key_links:
    - from: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      to: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      via: "the reap `wait_until(... == 0)` checks can no longer pass on a query error because the observers they call now fail closed (status-check + panic) instead of returning 0"
      pattern: "out.status.success()"
---

<objective>
Close the ONE real, statically-provable, non-refutable finding the 20-52 FOUR-WAY pre-native panel (Codex 5.6 Sol MEDIUM + internal Claude adversarial LOW, both concurring; Gemini + Kimi missed it) returned BLOCK on against the re-sealed successor `f0dd5b6d` (tree `ac76c87b`): the residual reap-OBSERVER false-green. The 20-50 Finding-1 fix closed the STRUCTURAL reap vacuity but the post-close observer still FAILS OPEN — `surviving_captured_choice_pids` ends in `String::from_utf8_lossy(&out.stdout).trim().parse().unwrap_or(0)` with NO `out.status.success()` check, so a post-close CIM/PowerShell query FAILURE returns `0` → satisfies `wait_until(... == 0)` → the reap property is "proven" with no evidence. The same fail-open shape is shared by the sibling `tagged_cmd_count` observer.

This is a COMPREHENSIVE, single-class, one-file fix that breaks the whack-a-mole: harden EVERY host-side query observer in `crates/wcore-sandbox/tests/hard_process_containment_windows.rs` to FAIL CLOSED in one pass — not only the two the finding names, but all three (`tagged_cmd_count`, `tagged_choice_descendant_pids`, `surviving_captured_choice_pids`) — plus a full-file sweep for any other fail-open or vacuous pattern, so a re-audit cannot find a third instance of this class.

Purpose: The 20-52 gate blocked and routed to a further-repaired successor. This delta writes the observer-integrity fix so a NEW candidate can be re-sealed (20-58), re-cross-audited by the full four-way panel (20-59), and — on a zero-finding ALL-FOUR PASS — admit the rebound native re-dispatch (20-53 onto the 20-58 seal).
Output: One clean successor delta scoped to the single finding file. The change is `#![cfg(windows)]` — UNCOMPILABLE on Mac/Hetzner Linux; its compile AND real-hardware behavior are proven ONLY on the self-hosted msvc runner at the rebound 20-53 (honest per CONTEXT D5/D7). No file outside the one named file is touched; no requirement is completed.
</objective>

<redispatch_decision>
Chain structure — AUTHOR DELTA plans 20-57 (this fix) → 20-58 (re-seal) → 20-59 (four-way pre-native cross-audit), then REBIND the existing pending (unexecuted) 20-53…20-56 onto the 20-58 seal (depends_on chains from 20-59; symbolic seal reference 20-51 → 20-58; pre-native cross-audit reference 20-52 → 20-59). Do NOT mutate the executed/sealed records 20-43/20-44/20-45/20-50/20-51/20-52.

Rationale:
- 20-52 is EXECUTED and sealed (its four-way BLOCK is on disk); a fresh observer fix belongs on a new plan (20-57), never a re-bind of the sealed BLOCK.
- 20-53…20-56 are authored but UNEXECUTED. Rewiring their upstream (depends_on) and their symbolic seal/cross-audit references is a pure pending-plan rebind — it mutates no sealed record and is the efficient alternative to superseding + re-authoring the native/review/prep/terminal chain each cycle.
- Net: 20-43/44/45/50/51/52 stay as the sealed record; the further-repaired candidate flows 20-57 → 20-58 → 20-59 → (rebound) 20-53 → 20-54 → 20-55 → 20-56.
</redispatch_decision>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-52-CROSS-AUDIT.md
@.planning/phases/20-transactional-delegated-mutation/20-52-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-50-SUMMARY.md
@crates/wcore-sandbox/tests/hard_process_containment_windows.rs
</context>

<execution_rules>
- Require a clean isolated standalone checkout (`.git` is a directory) on branch `plan/f20-unified-audit-repair`, in `/Users/seandonahoe/dev/waylandcore-ferrox`. Do NOT execute in the sibling `/Users/seandonahoe/dev/waylandcore` primary checkout. All git ops via `/usr/bin/git`.
- The new candidate is a SUCCESSOR of the 20-52-blocked `f0dd5b6d` (tree `ac76c87b`). Only planning-doc commits sit between that seal and the branch tip; confirm the ONE touched file is byte-identical to the sealed candidate (no source drift) before editing — `git diff f0dd5b6d -- crates/wcore-sandbox/tests/hard_process_containment_windows.rs` must be empty — then author the fix on the branch tip.
- Capture the plan base before any edit: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-57)"`.
- NO Cargo/clippy/nextest on the Mac, with EXACTLY ONE sanctioned exception (CONTEXT D5 carve-out): the targeted macOS compile gate `cargo check -p wcore-sandbox --features live-docker --tests` (compile/type-check only, no test run, no Docker). Fail-safe: if that check cannot run on the Mac, the gate BLOCKS rather than emitting a false green. Everything else is `vx rustfmt --edition 2024 --check` (fmt) + grep + the committed-HEAD Hetzner harness (`/Users/seandonahoe/.ratchet/harness/remote-cargo.sh`) + node tooling.
- HONESTY (CONTEXT D5/D7): `hard_process_containment_windows.rs` is `#![cfg(windows)]` — neither this Mac nor Hetzner Linux compiles a line of the observer fix. Its compile AND real-hardware behavior are proven ONLY on the self-hosted msvc runner at the rebound 20-53. Do NOT claim a Windows fix "verified" from any pre-dispatch gate. Because the file is Windows-only, the sanctioned macOS compile gate and the Hetzner aggregate/clippy are a NO-REGRESSION floor (they must stay green + 11509/0/48), not a proof of the fix.
- Touch ONLY the one declared file, and within it ONLY the three query-observer helper bodies. No drive-by change to the `command.rs` cmd classifier, the two isolation guardrails (#A/#B), the AppContainer implementation, the containment assertions, the captured-PID reap logic, the `if pids.is_empty() { return 0; }` short-circuit, the `reap_stray_choice` best-effort cleanup, the `unique_tag` nanos fallback, or any test body. Distinguish "fail closed on observer error" (the fix) from "changing WHAT is asserted" (NOT done): every assertion is preserved verbatim.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Harden ALL THREE host-side query observers to fail CLOSED in one pass (status-check + panic on non-success/unparseable), preserving every assertion verbatim</name>
  <files>crates/wcore-sandbox/tests/hard_process_containment_windows.rs</files>
  <read_first>crates/wcore-sandbox/tests/hard_process_containment_windows.rs, .planning/phases/20-transactional-delegated-mutation/20-52-CROSS-AUDIT.md</read_first>
  <behavior>
    - Three host-side query observers shell out to `powershell` and parse the result; each currently maps a query FAILURE to a passing value (the fail-open class the 20-52 panel blocked on):
      1. `surviving_captured_choice_pids(pids: &[u32]) -> usize` (the named finding) ends in `String::from_utf8_lossy(&out.stdout).trim().parse().unwrap_or(0)` — on empty/malformed stdout returns 0, satisfying `wait_until(|| surviving_captured_choice_pids(&captured_pids) == 0, 30, ...)` on the first poll with no evidence.
      2. `tagged_cmd_count(tag: &str) -> usize` (the sibling, same shape) ends in the identical `.trim().parse().unwrap_or(0)` — it backs the `wait_until(tagged_cmd_count(...) == 0, ...)` reap assertions in `job_close_reaps_detached_descendant_with_no_residue`, `breakaway_is_denied`, and `qualified_hard_containment_backend_preflight`, so the same false-green reaches three more reap checks.
      3. `tagged_choice_descendant_pids(tag: &str) -> Vec<u32>` (the alive-phase capture reader) ends in `.split_whitespace().filter_map(|s| s.parse::<u32>().ok()).collect()`, which silently swallows a non-success exit or a malformed token and returns an empty `Vec`.
    - Fix (COMPREHENSIVE, one pass — harden all three; touch ONLY their bodies):
      1. `tagged_cmd_count`: after `.output().expect(...)`, `assert!(out.status.success(), ...)` (a non-success exit is a hard test failure with a clear message including `out.status.code()` and stderr). Then parse the trimmed stdout with a panicking fallback (`unwrap_or_else(|err| panic!(...))`) instead of `.unwrap_or(0)` — the CIM `@(...).Count` always prints a number on success, so empty/unparseable stdout on a "success" exit is anomalous and must fail the test, never read as 0.
      2. `surviving_captured_choice_pids`: keep the `if pids.is_empty() { return 0; }` legitimate empty-set short-circuit VERBATIM (the 20-52 panel confirmed it sound), then apply the same hardening as `tagged_cmd_count` to the post-query parse (`assert!(out.status.success(), ...)` + panicking parse instead of `.unwrap_or(0)`).
      3. `tagged_choice_descendant_pids`: after `.output().expect(...)`, `assert!(out.status.success(), ...)`; then parse the whitespace-separated PID list with a panicking per-token parse (`.map(|s| s.parse::<u32>().unwrap_or_else(|err| panic!(...)))`) instead of `filter_map(...ok())`, so a malformed token is a hard failure. A LEGITIMATE empty result (no descendants yet during early sampling on a SUCCESS exit) still yields an empty `Vec` — `split_whitespace()` over empty stdout produces no tokens — so this preserves the valid zero-descendants case while failing closed on a query error.
    - Update each observer's doc comment to note the fail-closed behavior (assert query success; a failed query panics rather than reading as zero) WITHOUT writing the literal zero-default token in the comment text.
    - Full-file sweep — confirm NO other fail-open or vacuous pattern remains: (a) the only surviving `.unwrap_or(0)` is the `unique_tag` SystemTime-nanos fallback (`.map(|d| d.as_nanos()).unwrap_or(0)`), which is NOT a query observer (it seeds a tag suffix) and is intentionally left; (b) `reap_stray_choice` (`let _ = Command::new("taskkill")...output();`) is intentionally best-effort cleanup and is left; (c) no `wait_until` predicate can now pass on a query error and no assertion is satisfiable by an error path, because every observer the reap `== 0` checks call now fails closed. Do NOT touch (a) or (b).
    - PRESERVE VERBATIM (the 20-52 panel confirmed sound): the captured-PID reap logic (peak-set alive-phase capture + fixed-PID post-close re-check), `assert!(!captured_pids.is_empty())`, `peak > 0`, `peak <= SANDBOX_ACTIVE_PROCESS_LIMIT`, `peak < attempts`, `exit_code == 0`, the `reap_stray_choice()` trailers, and the sibling tests `job_close_reaps_detached_descendant_with_no_residue` / `breakaway_is_denied` / `qualified_hard_containment_backend_preflight`. Only the three helper bodies change; no assertion and no test body is altered.
    - `#![cfg(windows)]`: uncompilable on Mac/Hetzner. Compile + behavior are proven ONLY at the rebound 20-53 msvc re-dispatch; this task's Mac gates are scope + fmt + grep.
  </behavior>
  <action>In `crates/wcore-sandbox/tests/hard_process_containment_windows.rs`, harden ALL THREE host-side query observers to FAIL CLOSED in one pass. For `tagged_cmd_count` and `surviving_captured_choice_pids` (the two `.Count` observers): after `.output().expect(...)`, add `assert!(out.status.success(), ...)` including `out.status.code()` and stderr in the message, and replace the trailing `.parse().unwrap_or(0)` with a panicking parse (`.unwrap_or_else(|err| panic!(...))`), so a non-success exit or empty/unparseable stdout fails the test instead of reading as 0; keep `surviving_captured_choice_pids`'s `if pids.is_empty() { return 0; }` short-circuit verbatim. For `tagged_choice_descendant_pids` (the PID-list capture reader): add the same `assert!(out.status.success(), ...)` and replace `filter_map(|s| s.parse::<u32>().ok())` with a panicking per-token parse (`.map(|s| s.parse::<u32>().unwrap_or_else(|err| panic!(...)))`), so a malformed token fails closed while a legitimate empty result (no descendants on a success exit) still yields an empty Vec. Update each observer's doc comment to note the fail-closed behavior. Do a full-file sweep and confirm the only remaining `.unwrap_or(0)` is the benign `unique_tag` SystemTime-nanos fallback (leave it) and `reap_stray_choice` stays best-effort cleanup (leave it) — no other observer, `wait_until` predicate, or assertion may pass on a query error. Change ONLY the three helper bodies; preserve every assertion, the captured-PID reap logic, `!captured_pids.is_empty()`, the peak/cap/below-attempts/clean-exit assertions, and the sibling `job_close_reaps_detached_descendant_with_no_residue` / `breakaway_is_denied` / `qualified_hard_containment_backend_preflight` tests verbatim. Do NOT touch `command.rs`, the #A/#B guardrails, or any file outside this one. Implements REQ-native-r7; Windows compile + behavior proven at the rebound 20-53.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-57)" crates/wcore-sandbox/tests/hard_process_containment_windows.rs</automated>
    <automated>vx rustfmt --edition 2024 --check crates/wcore-sandbox/tests/hard_process_containment_windows.rs</automated>
    <automated>grep -nE "out\.status\.success\(\)" crates/wcore-sandbox/tests/hard_process_containment_windows.rs</automated>
    <automated>grep -nE "unwrap_or_else\(\|err\| panic!|unwrap_or_else\(\|" crates/wcore-sandbox/tests/hard_process_containment_windows.rs | head</automated>
    <automated>grep -nE "fn tagged_cmd_count|fn tagged_choice_descendant_pids|fn surviving_captured_choice_pids" crates/wcore-sandbox/tests/hard_process_containment_windows.rs</automated>
    <automated>grep -nE "if pids\.is_empty\(\)|!captured_pids\.is_empty\(\)|peak > 0|peak <= SANDBOX_ACTIVE_PROCESS_LIMIT|peak < attempts" crates/wcore-sandbox/tests/hard_process_containment_windows.rs | head</automated>
  </verify>
  <acceptance_criteria>Diff-scope is exactly the containment test and fmt passes. All three query observers (`tagged_cmd_count`, `tagged_choice_descendant_pids`, `surviving_captured_choice_pids`) now assert `out.status.success()` (grep-confirmed, three occurrences) and panic on non-success/unparseable output (grep-confirmed `unwrap_or_else`/`panic!`), with no query observer left mapping a failure to 0; the `if pids.is_empty()` short-circuit, `!captured_pids.is_empty()`, and the three peak assertions are grep-confirmed preserved; `command.rs`, the #A/#B guardrails, and the sibling test bodies are untouched. The only surviving `.unwrap_or(0)` is the benign `unique_tag` nanos fallback. Windows behavior (fail-closed observers; the reap `== 0` checks can no longer pass on a query error) proven at the rebound 20-53.</acceptance_criteria>
  <done>Every host-side query observer in the containment test fails closed on a CIM/PowerShell query failure — a post-close query error panics the test instead of satisfying a reap assertion without evidence — with every panel-confirmed-sound assertion preserved verbatim, as a task commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| containment proof ← post-close reap OBSERVER | The reap assertion is only genuine if its host-side observer fails CLOSED; an observer that maps a failed CIM/PowerShell query to a passing value (0 survivors / empty list) is a statically-provable false-green even after the structural vacuity is fixed |
| reap-property integrity ← every observer of the class | The fail-open shape is shared by all three query observers; fixing only the two the finding named leaves a third instance a re-audit can surface |
| assertion semantics ← observer hardening | Hardening the observer must change only HOW a query error is handled (fail closed), never WHAT the test asserts |
| candidate identity ← Windows-only source | The fix cannot be compiled off Windows; a compile break is invisible until the msvc re-dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-57-01 | Spoofing | residual reap-observer false-green (the 20-52 finding) | high | mitigate | `surviving_captured_choice_pids` and `tagged_cmd_count` assert `out.status.success()` and panic on non-success/unparseable stdout instead of `parse().unwrap_or(0) => 0`, so a post-close CIM/PowerShell query failure fails the containment test closed rather than satisfying `wait_until(... == 0)` without evidence. |
| T-20-57-02 | Tampering | a third undetected fail-open observer of the same class | medium | mitigate | The fix is single-class and comprehensive: all three query observers (incl. `tagged_choice_descendant_pids`, whose `filter_map(...ok())` silently swallowed a query error) are hardened in one pass, and a full-file sweep confirms the only surviving `.unwrap_or(0)` is the benign `unique_tag` nanos fallback and `reap_stray_choice` is intentional best-effort cleanup — so a re-audit cannot find another instance. |
| T-20-57-03 | Tampering | changing WHAT is asserted under cover of "hardening" | high | mitigate | Only the three shared helper bodies change; every assertion body, the captured-PID reap logic, `!captured_pids.is_empty()`, the peak/cap/below-attempts/clean-exit assertions, the `if pids.is_empty()` short-circuit, and the sibling tests are preserved verbatim (grep-confirmed helper names + assertions), so the fix cannot silently weaken a containment check. |
| T-20-57-04 | Denial of Service | Codex LOW post-close PID-reuse race | low | accept | Recycling a captured PID to a concurrent `choice.exe` within the 30s window can only make the test FAIL (fail-closed), never false-green; it is carried as a 20-53-observable native watch-item and NOT expanded in scope here. |
| T-20-57-05 | Tampering | undetectable Windows-only compile break | medium | accept | The file is `#![cfg(windows)]` and cannot be compiled off Windows; risk is bounded by scope + fmt + grep and is proven at the rebound 20-53 msvc re-dispatch (CONTEXT D5/D7). No pre-dispatch environment compiles the fix. |
</threat_model>

<verification>
The task first passes its Mac construction gate (scope + `vx rustfmt --edition 2024 --check` + grep) as a provisional task commit. After the task commit exists and the checkout is clean, run the sanctioned pre-dispatch macOS compile re-confirmation and the committed-HEAD Hetzner no-regression gates (a construction commit is not behavioral evidence):
<automated>cargo check -p wcore-sandbox --features live-docker --tests</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-57-clippy "$(pwd -P)" clippy -p wcore-sandbox --all-targets --all-features -- -D warnings</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-57-aggregate "$(pwd -P)" nextest run --workspace --profile ci --no-fail-fast</automated>
The macOS `cargo check` re-confirms the macOS-compiled wcore-sandbox surface still compiles — it does NOT compile the `#![cfg(windows)]` containment test. The Hetzner clippy + aggregate are a NO-REGRESSION floor: because the fix is Windows-only, the Linux aggregate `nextest --profile ci --no-fail-fast` MUST be exactly 11509/0/48 (a different count is a Linux regression that blocks the sequence — the honesty gate) and this aggregate runs WITH `--locked` because 20-57 changes no manifest (the lock is unchanged; 20-58 confirms zero lock delta). Record the exact committed `source_sha`/tree, the byte-identity check of the file vs `f0dd5b6d`, the macOS compile receipt, the Hetzner clippy + aggregate receipts, changed paths, and the explicit deferral of ALL Windows compile/behavior (the fail-closed observers) to the rebound 20-53 msvc re-dispatch in `20-57-SUMMARY.md`. No native, aggregate-seal, or requirement claim is made.
</verification>

<success_criteria>
- All three host-side query observers (`tagged_cmd_count`, `tagged_choice_descendant_pids`, `surviving_captured_choice_pids`) fail CLOSED on a CIM/PowerShell query failure: each asserts `out.status.success()` and panics on non-success/unparseable output rather than mapping the failure to 0 or an empty list, so a post-close query failure can no longer satisfy a reap `wait_until(... == 0)` without evidence (r7).
- The hardening is comprehensive and single-class (all three observers in one pass; full-file sweep leaves no other fail-open pattern), and every panel-confirmed-sound assertion is preserved verbatim — the captured-PID reap logic, `!captured_pids.is_empty()`, the peak/cap/below-attempts/clean-exit assertions, the empty-set short-circuit, and the sibling tests; `command.rs` and the #A/#B guardrails are untouched (r7).
- Sanctioned macOS `cargo check --tests` is green, Hetzner clippy `-D warnings` is clean, and the aggregate is 11509/0/48 with no Linux regression (r4); the summary records the exact new candidate `source_sha`/tree and defers every hardware runtime claim (and the fail-closed PID-reuse watch-item) to the rebound 20-53.
</success_criteria>

## Artifacts this phase produces
- The comprehensive observer-hardening fix (all three query observers fail closed) as one reviewable further-repaired successor delta over the 20-52-blocked `f0dd5b6d`, scoped to the single finding file.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-57-SUMMARY.md` using the standard GSD summary template. Record the exact new candidate `source_sha`/tree, the byte-identity check of the touched file vs `f0dd5b6d`, the macOS compile-check receipt, the Hetzner clippy + aggregate receipts, changed paths, the chain decision (author 20-57…20-59, rebind pending 20-53…20-56 onto the 20-58 seal, do not mutate sealed 20-43/44/45/50/51/52), the confirmation that ALL THREE query observers now fail closed (status-check + no zero-default on error) while every panel-confirmed-sound assertion is preserved verbatim, and the explicit deferral of all Windows compile/behavior (and the fail-closed PID-reuse watch-item) to the rebound 20-53. Complete no Phase 20 requirement.
</output>
</content>

---
phase: 20-transactional-delegated-mutation
plan: "19"
type: execute
wave: 18
depends_on:
  - "20-16"
files_modified:
  - crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs
  - crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs
  - crates/wcore-agent/src/session_journal/snapshot.rs
autonomous: true
requirements:
  - REQ-native-r15
  - REQ-native-r1
  - REQ-native-r2
  - REQ-native-r3
must_haves:
  truths:
    - "Before any candidate build, the working tree is reset to pristine be84bd2 so the uncommitted boundary-breaking process.rs diagnostic (full-token current_token swap for process creation, the CreateRestrictedToken privilege-preserving flag, and the env-gated token-group dump) is discarded and the storage.rs spike is re-derived rather than salvaged (REQ-native-r15)."
    - "AppContainerBackend::is_available() can create the ACL-lease probe file on real Windows because the create-new-no-follow OpenOptions chain now requests high-level write access alongside its explicit access_mode (REQ-native-r1)."
    - "The sandbox child token drops the redundant deny-only marking of Administrators/Users/Authenticated Users so an AppContainer child can read a package-SID-granted file, while the AppContainer boundary is unchanged: process creation still uses the restricted, low-integrity, AppContainer-tagged token, never the caller's full primary token (REQ-native-r2)."
    - "wcore-agent compiles on x86_64-pc-windows-msvc because READ_CONTROL and WRITE_DAC are imported from the module that actually exports them in the pinned windows-sys, not from a module where they are absent (REQ-native-r3)."
  artifacts:
    - path: crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs
      provides: "Freshly re-derived create-new-no-follow write-access fix restoring is_available() on real Windows"
    - path: crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs
      provides: "Freshly written drop-deny-only-SIDs fix that restores reads while preserving the restricted/low-IL/AppContainer child token"
    - path: crates/wcore-agent/src/session_journal/snapshot.rs
      provides: "Corrected Windows security-constant import that unblocks the msvc build"
  key_links:
    - from: crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs
      to: crates/wcore-sandbox/src/backends/appcontainer/mod.rs
      via: "create-new-no-follow probe reached by is_available()"
      pattern: "is_available"
    - from: crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs
      to: crates/wcore-sandbox/tests/live_fs_acl.rs
      via: "restricted AppContainer child token exercised by the native ACL read/grant/revoke test"
      pattern: "CreateProcessAsUserW"
---

<objective>
Reset the tainted working tree to pristine `be84bd2`, then write — fresh, never salvaged from the discarded diagnostic — the three real native source fixes that the 2026-07-23 hardware investigation root-caused: the AppContainer availability probe, the sandbox-child read boundary, and the Windows `wcore-agent` import.

Purpose: These are the CORE sandbox correctness defects (REQ-native-r1/r2/r3). Every later native gate is meaningless until the tree is clean (REQ-native-r15) and these fixes exist as reviewable, gated source rather than throwaway hardware spikes.
Output: One clean candidate delta touching exactly the three defect source files; construction-proven on the Mac (fmt + scope) and clippy-clean on Hetzner; no native, aggregate, or requirement claim.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-NATIVE-REPAIR-PRD.md
@.planning/inbox/native-uat-repair-BRIEF.md
@.planning/intel/inbox-2026-07-23/AUDIT-2026-07-23.md
@.planning/intel/inbox-2026-07-23/requirements.md
@crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs
@crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs
@crates/wcore-agent/src/session_journal/snapshot.rs
</context>

<execution_rules>
- Require a clean isolated standalone checkout (`.git` is a directory) on branch `plan/f20-unified-audit-repair`. Do NOT execute in the sibling `/Users/seandonahoe/dev/waylandcore` primary checkout.
- Capture the plan base before any edit: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-19)"`.
- NO Cargo/clippy/nextest on the Mac. Construction uses exact-file `vx rustfmt`; authoritative Rust proof is the committed-HEAD Hetzner harness only. `cargo fmt` and node tooling are the sole Mac exceptions.
- The real `process.rs` fix is written FRESH from the pristine baseline. It is a hard error to reintroduce the discarded diagnostic (full primary-token process creation, the privilege-preserving `CreateRestrictedToken` flag value, or the env-gated token-group dump). Task 1 must complete and be verified before Task 2 begins.
- Touch only the three declared files. No drive-by refactor of the ~4,700-line AppContainer implementation beyond what r1/r2/r3 require.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Hard hygiene reset to pristine be84bd2</name>
  <files>crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs, crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs</files>
  <read_first>crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs, crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs</read_first>
  <behavior>
    - After the reset, the working tree carries zero uncommitted modification to the two spike/diagnostic files; their bytes equal the pristine `be84bd2` blobs (identical to committed HEAD, since only planning-doc commits sit between).
    - The pristine `process.rs` restores the original restricted-token construction: the deny-only Administrators/Users/Authenticated-Users marking is present again, `CreateProcessAsUserW` uses the restricted token, and no env-gated token-group dump exists. (Task 2 will then remove the deny-only marking deliberately, from this clean baseline.)
    - The pristine `storage.rs` create-new-no-follow chain is restored to its committed form (Task 2 re-derives the write-access fix deliberately).
  </behavior>
  <action>Discard the two uncommitted working-tree modifications by restoring both files from the committed HEAD (equivalently pristine `be84bd2`, verified byte-identical for these paths). This deletes the off-plan hardware diagnostic that swapped the child's process-creation token to the caller's full primary token, neutered the restricted-token privilege stripping, and dumped child token groups under an environment gate — that diagnostic BREAKS the AppContainer boundary and must never enter the candidate. Do not stage a partial hunk; restore each file whole. Confirm the tree is pristine for these paths before proceeding to Task 2, which writes the real fixes fresh.</action>
  <verify>
    <automated>test -z "$(git status --porcelain -- crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs)"</automated>
    <automated>git diff --quiet be84bd2 HEAD -- crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs && git diff --quiet -- crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs</automated>
    <automated>test -z "$(git -c grep.lineNumber=false grep -c 'WCORE_DIAG_TOKEN' -- crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs || true)"</automated>
  </verify>
  <acceptance_criteria>Both spike/diagnostic files are byte-identical to the pristine baseline with no working-tree modification; the boundary-breaking diagnostic is absent. No fix has been written yet.</acceptance_criteria>
  <done>The tree is pristine for the two files; the reset is a provisional task commit and Task 2 may write the real fixes fresh.</done>
</task>

<task type="auto">
  <name>Task 2: Write the real sandbox availability, read-boundary, and Windows-import fixes fresh</name>
  <files>crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs, crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs, crates/wcore-agent/src/session_journal/snapshot.rs</files>
  <read_first>crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs, crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs, crates/wcore-agent/src/session_journal/snapshot.rs</read_first>
  <behavior>
    - `create_new_nofollow` requests high-level write access in its `OpenOptions` chain in addition to the explicit `access_mode`, so std's creation-mode validation is satisfied and the ACL-lease probe file is creatable — making `is_available()` return true on a real AppContainer-capable Windows host (REQ-native-r1).
    - `execute_blocking` builds the child token via `CreateRestrictedToken` with NO deny-only SIDs (the `SidsToDisable` count is zero and the pointer is null), and the now-dead Administrators/Users/Authenticated-Users SID-allocation block is removed. Process creation continues to use the restricted, explicitly-low-integrity, AppContainer-tagged token — never the caller's full primary token. Isolation is preserved because the AppContainer access model ignores normal SIDs for granting; the deny-only marking was redundant and was what blocked package-SID-granted reads (REQ-native-r2).
    - `snapshot.rs` imports `READ_CONTROL` and `WRITE_DAC` from `Win32::Storage::FileSystem` (where the pinned windows-sys 0.59 exports them, and whose feature the crate already enables), not from `Win32::Security`, resolving E0432 on the msvc target (REQ-native-r3).
  </behavior>
  <action>Re-derive, from the pristine baseline, the three fixes the hardware investigation proved. (1) In `storage.rs` `create_new_nofollow`, add high-level write access to the create-new-no-follow `OpenOptions` chain while keeping the explicit `access_mode`; document why std requires it for a create-new open. (2) In `process.rs` `execute_blocking`, pass zero/null for the deny-only SID parameters of `CreateRestrictedToken` and delete the dead Administrators/Users/Authenticated-Users allocation; keep the restricted-token privilege-stripping flag value, the explicit low-integrity label, the AppContainer capability set, and restricted-token process creation exactly as the pristine baseline had them so the boundary is preserved; add a comment recording that isolation is intrinsic to the AppContainer package-SID model and the deny-only marking was redundant (the hardware matrix proved reads exit 1 with it ON, exit 0 with it OFF, and a normal-SID-only grant is still denied either way). (3) In `snapshot.rs`, move the `READ_CONTROL`/`WRITE_DAC` import to `Win32::Storage::FileSystem`. Do NOT rewrite the token via CreateAppContainerToken/explicit LowBox, do NOT touch max-privilege/bypass-traverse, and do NOT alter the base token — all four were spike-disproven. Windows and macOS behavior is verified only on real hardware at the native-proof gate; this task is construction only.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-19)" crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs crates/wcore-agent/src/session_journal/snapshot.rs</automated>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs crates/wcore-agent/src/session_journal/snapshot.rs</automated>
    <automated>git -c grep.lineNumber=false grep -q 'Win32::Storage::FileSystem::{' -- crates/wcore-agent/src/session_journal/snapshot.rs</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, every changed path is declared, the three fixes are present as source, and the restricted/low-IL/AppContainer token path is intact. No compilation or behavioral claim is made until the plan-level Hetzner gate and the later native-proof gate.</acceptance_criteria>
  <done>The three fixes exist as a provisional task commit; behavioral proof is deferred to Hetzner clippy and the native-proof gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sandbox child ← parent token | The AppContainer child token is derived from the parent's primary token; any widening of what the child can read or do crosses the containment boundary |
| ACL-lease file ← filesystem | The availability probe creates/opens a lease file that gates whether the sandbox will run at all |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-19-01 | Elevation of Privilege | process.rs child token construction | critical | mitigate | Drop ONLY the redundant deny-only SIDs; keep restricted-token creation, explicit low-IL label, AppContainer capability set, and restricted-token (never full-primary-token) process creation. The genuine-DENY-blocks and normal-SID-only-denied proofs are authored in 20-20 and run on hardware at 20-25. |
| T-20-19-02 | Tampering | uncommitted diagnostic in process.rs | high | mitigate | Hard reset to pristine be84bd2 (Task 1) discards the boundary-breaking full-token diagnostic; the real fix is written fresh and cannot inherit the diagnostic's full-primary-token process creation. |
| T-20-19-03 | Spoofing | is_available() probe | medium | mitigate | The write-access fix only enables lease-file creation; it does not relax any DACL or capability check, so a false "available" cannot arise from a non-AppContainer host. |
</threat_model>

<verification>
Each task first passes its Mac construction gate (scope + `vx rustfmt --check`) as a provisional GSD task commit. After both task commits exist and the checkout is clean, the committed-HEAD Hetzner gate determines construction acceptance (a construction commit is not behavioral evidence):
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-19-clippy "$(pwd -P)" clippy -p wcore-sandbox -p wcore-agent --all-targets --all-features -- -D warnings</automated>
Record the exact committed `source_sha`, source tree, commands, exit status, and remote receipt identity in `20-19-SUMMARY.md`. The summary must state that Windows/macOS runtime behavior (is_available true, read/grant/revoke green, msvc build) is UNPROVEN here and is proven only at the native-proof gate (20-25), and that no aggregate, requirement, or phase claim is made.
</verification>

<success_criteria>
- The working tree was reset to pristine `be84bd2` before any fix was written; the boundary-breaking diagnostic is gone (REQ-native-r15).
- The three defect files carry freshly-written fixes for is_available (r1), the sandbox read boundary with isolation preserved (r2), and the Windows import (r3).
- Hetzner clippy is clean for `wcore-sandbox` and `wcore-agent` at `-D warnings`.
- The summary records exact source identity and defers every Windows/macOS runtime claim to the native-proof gate.
</success_criteria>

## Artifacts this phase produces
- Reset-to-pristine hygiene plus the three real native source fixes as one reviewable candidate delta.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-19-SUMMARY.md` using the standard GSD summary template. Record the reset evidence, exact `source_sha`/tree, the Hetzner clippy receipt, changed paths, and the explicit deferral of all native runtime proof to 20-25. Complete no Phase 20 requirement.
</output>

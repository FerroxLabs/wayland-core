• ## Prosecution

  **Guardrail A — DispatchAdmission budget (#A, worktree_manager.rs).**
  Traced the full sealed path. `workspace_capacity` does `available_workspace_bytes().await?` — any probe failure propagates. The Windows probe itself has exactly three exits: spawn/capture failure → `DispatchAdmission` via `capture_error`; non-zero exit status → `DispatchAdmission` with stderr; stdout parse failure → `DispatchAdmission`. There is no `unwrap_or`, no `u64::MAX` fallback, no catch-to-unlimited anywhere in the sealed context. The downstream arithmetic is `checked_mul`/`checked_add` with overflow → `DispatchAdmission`. The env-var transport (`command.env("WCORE_SWARM_PROBE_ROOT", root)` on the child argv command only) narrows the input surface: it removes the trailing-`-Command`-arg reparse class without changing the failure semantics — a missing/empty/lossy root makes `DriveInfo::new` throw, status non-success, still fail-closed. `dunce::simplified` re-canonicalizes internally and returns the verbatim original if the simplified form doesn't round-trip, so it cannot manufacture a wrong-but-plausible root. **PASS.**

  **Guardrail B — quoting-layer only (#B, command.rs/process.rs).**
  - argv discipline kept: `cmd.argv.iter().enumerate().map(...)` still maps one caller-supplied argv entry to one command-line token; the payload is not `format!`-interpolated into a shell string. `quote_cmd_payload` wraps the *existing* entry in one outer quote pair, inner bytes verbatim — that is precisely what `cmd /s /c` requires (strips exactly the first/last quote of the raw remainder).
  - Scope of the special case is narrow: only when `resolved_program_is_cmd(app_name_w)` (the *resolved* `lpApplicationName`, not raw argv[0]) AND only the single entry immediately after the first exact `/c` or `/k` (case-insensitive, exact token match — `/call` doesn't match). `flag_idx + 1 == argv.len()` yields `Some(len)` which never equals a real index — no panic, no mis-quote.
  - The delta touches nothing else: no change to `is_unc_or_device_path`, token creation, Job Object limits, ACL lease — none of those files appear in the diff. The 20-36 `exit /b 0` normalization is still present verbatim in the `active_process_cap_is_enforced` script (`... & exit /b 0`). **PASS.**
  - One imprecision noted and filed under the deferred umbrella: `ends_with("cmd.exe")` would also suffix-match a hypothetical `notcmd.exe`. Unreachable given the resolver pins bare `cmd` to `System32\cmd.exe` and only `cmd` is runnable under the Low-IL token, and any consequence is Windows-runtime behavior — native_windows, deferred to 20-46. Not a non-deferred finding.

  **r7 (test).** `tagged_choice_descendant_count` scopes by `ParentProcessId ∈ {tagged cmd PIDs}`; the tag is test-generated (`unique_tag`), not attacker-controlled, so the `-like '*{tag}*'` interpolation is safe in this context. The `.parse().unwrap_or(0)` cannot produce a false pass: a stuck-at-zero count fails the downstream peak/admission assertion ("admitted every spawn" requires observing ≥ `SANDBOX_ACTIVE_PROCESS_LIMIT/2`), and the final `== 0` reaped assertion is only meaningful after a non-zero peak was observed. All original containment assertions (cap enforcement, reap-after-close) are preserved. Whether CIM exposes the sandbox cmd's `CommandLine` from the host query is a Windows-runtime question — deferred.

  **r3 (test).** Import restoration only; `FILE_ID_BOTH_DIR_INFORMATION` moved to `windows_sys::Wdk::Storage::FileSystem` matching production. No logic change; compile verification is Windows-only — deferred.

  **nextest.toml.** `[test-groups]` with `max-threads = 1` plus `[[profile.default.overrides]]` with `platform = 'cfg(windows)'` is valid nextest configuration. The platform gate means the Linux `--profile ci` aggregate is untouched, consistent with the claimed 11509/0/48. Concurrency-only; no test added or removed — claim in the comment is accurate.

  **Evidence integrity.** Nothing in the delta contradicts the stated evidence (macOS check, Linux clippy/nextest are the only locally-provable legs, and every changed behavior line is `#[cfg(windows)]` or Windows-target test code). The nextest.toml comment's claim that the override is inert on Linux is correct by construction. No evidence is overstated: Windows compile+behavior is explicitly disclaimed to 20-46 rather than claimed green.

  **Kimi watch-items.** Not silently dropped: the two choice-spawning targets remain `#[ignore]`d native-only; d2's structural fix (tag-scoped counting) is landed with the serialization group as belt-and-braces; git-ops-over-de-verbatimized-root, CIM visibility under NetworkService, bash-worker-under-AppContainer/no-Docker-fallback, and worker test-exe DLL-load under Low-IL all remain Windows-runtime properties with no green claim in this seal — they ride the native_windows deferral to 20-46. The delta neither closes them nor weakens their eventual check.

  No defect found at any non-deferred severity. Findings: zero.

  {"checks":{"all_severity":"PASS","evidence_integrity":"PASS","integration_authority":"PASS"},"deferred":["native_macos","native_windows"],"findings":{"blocker":0,"critical":0,"high":0,"medium":0,"low":0},"guardrail_A_admission_budget":"PASS","guardrail_B_quoting_layer_only":"PASS","kimi_watchitems_carried":"PASS","disposition":"PASS"}


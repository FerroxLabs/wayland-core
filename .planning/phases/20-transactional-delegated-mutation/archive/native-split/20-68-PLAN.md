---
phase: 20-transactional-delegated-mutation
plan: "68"
type: execute
wave: 65
depends_on:
  - "20-67"
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - REQ-native-r11
  - REQ-native-r12
  - REQ-native-r14
must_haves:
  truths:
    - "Only AFTER the SEANDESKTOP full-suite verify (20-67) is all-green on the exact combined-fix tree, ONE fresh Sean-authorized CI dispatch runs the full 6-target Windows native proof via `nightly-windows-soak.yml` with `f20_candidate=true`, bound to that SAME combined-fix SHA/tree. The Windows leg runs on the AppContainer-capable self-hosted msvc runner (client SKU, labels `self-hosted, Windows, X64, msvc`), NOT a hosted `windows-2022` (REQ-native-r11/r12)."
    - "The dispatch is single-attempt on a fresh dispatch branch/UAT ref bound to the exact combined-fix SHA; all prior native-proof-dispatch authorizations are spent/void — this proof requires its own fresh Sean authorization (REQ-native-r14). The full 6-target enumeration must be green with per-target and final commit/tree/nonce markers bound to the exact SHA; the retained evidence is not deleted (REQ-native-r12)."
    - "This dispatch does NOT run until 20-67 is green (SEANDESKTOP direct nextest all-green) — the CI proof is the fleet-wide confirmation, never the first hardware touch. Any RED target is recorded honestly as an incomplete disposition routing back to the owning class plan; core#254 is OUT OF SCOPE (its own separate 4-way BLOCK verdict already recorded) and is not entangled here (REQ-native-r14)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-68-SUMMARY.md
      provides: "Retained full 6-target CI native-proof evidence bound to the exact combined-fix SHA, closing the Phase-20 native Windows acceptance gap"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-68-SUMMARY.md
      to: .planning/phases/20-transactional-delegated-mutation/20-67-SUMMARY.md
      via: "dispatches the exact SEANDESKTOP-green combined-fix SHA to the full 6-target CI proof on the self-hosted msvc runner"
      pattern: "native_windows"
---

<objective>
After the SEANDESKTOP full-suite verify (20-67) is all-green, dispatch the full 6-target Windows native proof via `nightly-windows-soak.yml f20_candidate=true`, bound to the exact same combined-fix SHA, on the AppContainer-capable self-hosted msvc runner. This is the fleet-wide confirmation of the four-class repair.

Purpose: The brief requires the whole native suite verified on SEANDESKTOP BEFORE any CI dispatch, then the full 6-target proof. This plan is that dispatch — Sean-only, single-attempt, fresh authorization, bound to the exact SEANDESKTOP-green SHA — so the closure of the CLASS A/B/C/D gaps is proven across the full target matrix, not just the two focused test targets.
Output: Retained full 6-target CI native-proof evidence bound to the exact combined-fix SHA, or an explicit incomplete disposition routing back to the owning class plan.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-VERIFICATION.md
@.planning/phases/20-transactional-delegated-mutation/20-67-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-53-SUMMARY.md
</context>

<execution_rules>
- Sean-only gate (CONTEXT D6): this plan dispatches a scarce CI native run. All prior native-proof-dispatch authorizations are spent/void; a FRESH digest-specific authorization is required, bound to the exact SEANDESKTOP-green combined-fix SHA (REQ-native-r14).
- Precondition: 20-67 is all-green on SEANDESKTOP for the exact combined-fix tree. This dispatch does NOT run before that — the CI proof is fleet confirmation, never the first hardware touch.
- Dispatch discipline: single-attempt on a fresh dispatch branch/UAT ref bound to the exact combined-fix SHA; the Windows leg runs on the AppContainer-capable self-hosted msvc runner (labels `self-hosted, Windows, X64, msvc`), not a hosted runner (REQ-native-r11).
- Invocation: `nightly-windows-soak.yml` with `f20_candidate=true`, full 6-target enumeration, per-target and final commit/tree/nonce markers bound to the exact SHA; retain the evidence (deletion not authorized).
- Native proof is real-hardware only (CONTEXT D7); never claimed from cross-compilation or source inspection.
- core#254 is OUT OF SCOPE — do not entangle its Relaxed-Mode change (its own 4-way BLOCK verdict is separately recorded). Any RED target is recorded honestly as an incomplete disposition routing back to the owning class plan (A/C → 20-63, D → 20-64, B → 20-65+20-66, then re-verify at 20-67).
- Use `/usr/bin/git`; never stage the AGENTS.md ijfw churn.
</execution_rules>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The combined four-class repair, all-green on SEANDESKTOP at 20-67 (the exact `--no-fail-fast` enumeration that first exposed all four classes at 20-53 now passes), bound to a recorded combined-fix SHA. It is ready for the fresh Sean-authorized full 6-target CI native proof on the self-hosted msvc runner.</what-built>
  <how-to-verify>
    1. Confirm 20-67 is all-green on SEANDESKTOP for the exact combined-fix SHA (the four per-class hardware confirmations recorded).
    2. Authorize the FRESH CI dispatch bound to that EXACT SHA (all prior digest authorizations spent/void); single-attempt on a fresh dispatch branch/UAT ref.
    3. Confirm the Windows leg targets the AppContainer-capable self-hosted msvc runner (labels `self-hosted, Windows, X64, msvc`), not a hosted runner.
    4. Dispatch `nightly-windows-soak.yml` with `f20_candidate=true`; the full 6-target enumeration must be green with per-target + final commit/tree/nonce markers bound to the exact SHA.
    5. If any target is RED, record it as an incomplete disposition routing back to the owning class plan; do NOT claim closure.
  </how-to-verify>
  <resume-signal>Type "authorized: &lt;combined-fix-sha&gt;" to dispatch the fresh full 6-target CI proof bound to that exact SHA, or describe why it is withheld.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Dispatch the full 6-target CI native proof and retain candidate-bound evidence</name>
  <files>None; this task dispatches to CI and records retained evidence.</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-67-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-53-SUMMARY.md</read_first>
  <behavior>
    - Under the fresh Sean authorization, publish the exact combined-fix SHA/tree to a fresh dispatch branch/UAT ref and dispatch `nightly-windows-soak.yml f20_candidate=true` to the AppContainer-capable self-hosted msvc runner (single attempt).
    - The full 6-target enumeration is green, with per-target and final commit/tree/nonce markers bound to the exact combined-fix SHA; the retained evidence is not deleted.
    - Any RED target is recorded honestly as an incomplete disposition routing back to the owning class plan; core#254 stays out of scope.
  </behavior>
  <action>Under the fresh Sean authorization from the checkpoint, publish the exact combined-fix SHA/tree (the SEANDESKTOP-green tree from 20-67) to a fresh dispatch branch/UAT ref and dispatch `nightly-windows-soak.yml` with `f20_candidate=true` to the AppContainer-capable self-hosted msvc runner (client SKU, `self-hosted, Windows, X64, msvc`), single attempt. Confirm the full 6-target enumeration is green with per-target + final commit/tree/nonce markers bound to the exact combined-fix SHA, and retain the evidence (deletion not authorized). Record any RED target honestly as an incomplete disposition routing back to the owning class plan (A/C → 20-63, D → 20-64, B → 20-65+20-66). Keep core#254 out of scope. Implements REQ-native-r11/r12/r14; no requirement is marked complete here — closure is claimed by the fresh native-inclusive review that consumes this evidence.</action>
  <verify>
    <human-check>The full 6-target `nightly-windows-soak.yml f20_candidate=true` run is green on the self-hosted msvc runner with per-target + final commit/tree/nonce markers bound to the exact combined-fix SHA, and the retained evidence exists. Any RED target is recorded as an explicit incomplete disposition routing back to the owning class plan.</human-check>
  </verify>
  <done>Retained full 6-target CI native-proof evidence bound to the exact combined-fix SHA exists (all four classes closed across the target matrix), or an explicit incomplete disposition routes back to the owning class plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI native proof ← fresh Sean authorization | The scarce CI run is dispatched only under a fresh, digest-specific Sean authorization bound to the exact SEANDESKTOP-green SHA; all prior authorizations spent/void |
| runner correctness ← self-hosted msvc | The Windows leg must run on the AppContainer-capable self-hosted msvc runner, not a hosted runner that cannot exercise AppContainer |
| native evidence integrity ← real hardware | The proof must come from real Windows hardware, never cross-compilation or source inspection |
| scope isolation ← core#254 excluded | The dispatch must not entangle core#254's separate Relaxed-Mode concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-68-01 | Elevation of Privilege | stale/reused native authorization | high | mitigate | All prior digest authorizations are spent/void; a fresh digest-specific Sean authorization bound to the exact SEANDESKTOP-green SHA is required at the blocking-human checkpoint. |
| T-20-68-02 | Spoofing | native proof from a hosted runner or non-hardware source | high | mitigate | The Windows leg runs on the AppContainer-capable self-hosted msvc runner (REQ-native-r11); proof is real-hardware only (CONTEXT D7); markers bind to the exact combined-fix SHA. |
| T-20-68-03 | Tampering | dispatching before SEANDESKTOP is green | high | mitigate | 20-68 depends_on 20-67 and the blocking-human checkpoint; CI is not dispatched until SEANDESKTOP is all-green on the exact combined-fix SHA. |
| T-20-68-04 | Tampering | masked RED target or scope creep into core#254 | medium | mitigate | Any RED target is recorded honestly as an incomplete disposition routing back to the owning class plan; core#254 is explicitly out of scope. |
</threat_model>

<verification>
The full 6-target CI run under the fresh Sean authorization determines acceptance: `nightly-windows-soak.yml f20_candidate=true` green across all 6 targets on the self-hosted msvc runner, with per-target + final commit/tree/nonce markers bound to the exact combined-fix SHA and the retained evidence kept. Record the combined-fix `source_sha`/tree, the fresh authorization identity, the runner identity, the per-target results, the retained ref identity, and — on any RED target — an explicit incomplete disposition routing back to the owning class plan, in 20-68-SUMMARY.md. No source/test change; no requirement completed here (closure is claimed by the fresh native-inclusive review that consumes this evidence).
</verification>

<success_criteria>
- One fresh Sean-authorized full 6-target CI native proof on the self-hosted msvc runner, bound to the exact SEANDESKTOP-green combined-fix SHA, single-attempt, all prior authorizations spent/void (REQ-native-r11/r14).
- The full 6-target enumeration is green with per-target + final commit/tree/nonce markers bound to the exact SHA; retained evidence kept (REQ-native-r12).
- Any RED target routes back to the owning class plan; core#254 stays out of scope (REQ-native-r14).
</success_criteria>

## Artifacts this phase produces
- Retained full 6-target CI native-proof evidence (self-hosted msvc, real hardware) bound to the exact combined-fix SHA, or an explicit incomplete disposition.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-68-SUMMARY.md` using the standard GSD summary template. Record the combined-fix `source_sha`/tree, the fresh Sean authorization identity, the self-hosted msvc runner identity, the `nightly-windows-soak.yml f20_candidate=true` per-target results, the retained ref identity, and the disposition (6-target green closes the native Windows acceptance gap for the fresh review; any RED target is an explicit incomplete routing back to the owning class plan). Complete no Phase 20 requirement (closure claimed by the fresh native-inclusive review).
</output>
</content>

---
phase: 20-transactional-delegated-mutation
plan: "23"
type: execute
wave: 22
depends_on:
  - "20-22"
files_modified:
  - Cargo.lock
autonomous: true
requirements:
  - REQ-native-r10
  - REQ-native-r4
must_haves:
  truths:
    - "Cargo.lock is regenerated and committed so it is consistent with the current Cargo.toml surface (the stale be84bd2 lock omitted cap-std/serde_json/tar for wcore-sandbox and fd-lock/libc/sha2/windows-sys for wcore-swarm); a --locked build succeeds against the committed lock (REQ-native-r10)."
    - "This lock-sealing commit is the ONE exact repaired candidate SHA that every subsequent gate (cross-audit, native proof, fresh review, prep, terminal) binds to; no source or test changes after it (REQ-native-r12 anchoring)."
    - "The sealed candidate builds on Hetzner (build --workspace --all-features) and passes the aggregate Linux proof (nextest --profile ci) at the same 11509/0 count, with no Linux regression from any of the 20-19..20-22 native fixes (the new Windows/macOS tests are cfg-excluded on Linux) (REQ-native-r4)."
  artifacts:
    - path: Cargo.lock
      provides: "Regenerated, consistent workspace lockfile committed on the sealed candidate"
  key_links:
    - from: Cargo.lock
      to: crates/wcore-sandbox/Cargo.toml
      via: "the lock resolves the wcore-sandbox dependency surface (cap-std/serde_json/tar)"
      pattern: "wcore-sandbox"
    - from: Cargo.lock
      to: crates/wcore-swarm/Cargo.toml
      via: "the lock resolves the wcore-swarm dependency surface (fd-lock/libc/sha2/windows-sys)"
      pattern: "wcore-swarm"
---

<objective>
Seal the ONE exact repaired candidate SHA: regenerate and commit a consistent `Cargo.lock`, then prove on Hetzner that the sealed candidate builds and passes the aggregate Linux suite unchanged — no regression from the 20-19..20-22 native fixes.

Purpose: The candidate cannot enter cross-audit / native proof / review with a stale lock (REQ-native-r10), and the Linux no-regression gate (REQ-native-r4) must pass before any Sean-gated native run is spent. This lock-sealing commit anchors every downstream gate to one SHA.
Output: The sealed candidate SHA with a committed consistent lockfile, a Hetzner `--locked` build receipt, and a `nextest --profile ci` = 11509/0 aggregate receipt. No native or requirement claim.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/inbox/native-uat-repair-BRIEF.md
@.planning/phases/20-transactional-delegated-mutation/20-19-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-20-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-21-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-22-SUMMARY.md
@.planning/codebase/TESTING.md
</context>

<execution_rules>
- Capture the plan base before edits: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-23)"`.
- NO Cargo on the Mac. `Cargo.lock` is regenerated through the committed-HEAD Hetzner harness, retrieved, and committed on the Mac; the lock is NOT hand-edited.
- No new package is selected here: the lock only synchronizes to the EXISTING `Cargo.toml` dependency surface (the deps already declared at be84bd2 plus any dev-dep the 20-19..20-22 tests introduced). No package-legitimacy checkpoint is required because no new crate is being chosen — only a lockfile resync of already-declared deps. If any authored test introduced a genuinely NEW external crate, STOP and surface it for a package-legitimacy checkpoint before committing.
- After this lock-sealing commit, NO source or test file changes for the remainder of the native-repair sequence; the sealed SHA is the exact candidate all downstream gates bind to.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate and commit a consistent Cargo.lock</name>
  <files>Cargo.lock</files>
  <read_first>crates/wcore-sandbox/Cargo.toml, crates/wcore-swarm/Cargo.toml, Cargo.lock</read_first>
  <behavior>
    - The committed `Cargo.lock` resolves the full current workspace `Cargo.toml` surface with no missing or extraneous entries relative to the declared dependencies.
    - A `--locked` build on Hetzner does not attempt to update the lock (proving consistency).
    - No new external crate is introduced beyond what the existing manifests already declare; if one was, it is surfaced for a legitimacy checkpoint rather than silently committed.
  </behavior>
  <action>Through the committed-HEAD Hetzner harness, regenerate the workspace lockfile (`generate-lockfile`, or capture the lock a clean `--workspace --all-features` build produces), retrieve the regenerated `Cargo.lock`, and commit it. Confirm the delta versus the stale be84bd2 lock is limited to the expected dependency resync (cap-std/serde_json/tar under wcore-sandbox; fd-lock/libc/sha2/windows-sys under wcore-swarm; plus any dev-dep the authored tests require). If the delta introduces a NEW external crate not previously declared in any `Cargo.toml`, stop and surface it. Do not hand-edit the lock; do not touch any source or test file.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-23)" Cargo.lock</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-23-locked-build "$(pwd -P)" build --workspace --all-features --locked</automated>
  </verify>
  <acceptance_criteria>The committed lock is consistent (a `--locked` Hetzner build succeeds without updating it) and reflects only the expected existing-dependency resync. No source/test change.</acceptance_criteria>
  <done>The lock-sealing commit exists; it is the exact repaired candidate SHA.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the sealed candidate builds and the aggregate Linux suite is unregressed</name>
  <files>None; this task produces Hetzner receipts against the sealed candidate.</files>
  <read_first>.planning/codebase/TESTING.md, .planning/phases/20-transactional-delegated-mutation/20-08-INDEPENDENT-REVIEW.md</read_first>
  <behavior>
    - `build --workspace --all-features` succeeds on Hetzner against the sealed candidate.
    - `nextest run --workspace --profile ci --no-fail-fast` on Hetzner reports 11509 passed / 0 failed (the exact prior aggregate; the new Windows/macOS acceptance tests are cfg-excluded on Linux and add nothing to the Linux count), including `transactional_delegated_mutation_test` 9/9 and `anvil_forge_transaction` 5/5.
    - A different aggregate count is a regression and blocks the sequence; it is recorded and investigated, not rationalized.
  </behavior>
  <action>Run the two committed-HEAD Hetzner gates against the sealed candidate: the full-workspace all-features build, and the aggregate `nextest --profile ci --no-fail-fast`. Confirm the aggregate is 11509 passed / 0 failed / 48 skipped (the exact reviewed baseline for `6937ef6`) — any deviation is a Linux regression from the native fixes and stops the sequence for repair. Record both receipts with exact command, exit status, counts, and remote receipt identity.</action>
  <verify>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-23-build "$(pwd -P)" build --workspace --all-features</automated>
    <automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-23-aggregate "$(pwd -P)" nextest run --workspace --profile ci --no-fail-fast</automated>
  </verify>
  <acceptance_criteria>The sealed candidate builds all-features and the aggregate Linux suite is 11509/0 with no regression. Any other count blocks the sequence.</acceptance_criteria>
  <done>The sealed candidate has a green Hetzner build + aggregate receipt and is ready for the cross-audit gate (20-24).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate identity ← lockfile | The sealed SHA must carry a consistent lock; a stale/incoherent lock produces a build that does not match the reviewed source |
| dependency set ← Cargo.toml | The lock must not silently introduce an unaudited new crate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-23-01 | Tampering | Cargo.lock regeneration | high | mitigate | Regenerate via the harness (never hand-edit); a `--locked` Hetzner build proves consistency; any new external crate is surfaced for a legitimacy checkpoint, not silently committed. |
| T-20-23-02 | Denial of Service | aggregate regression | medium | mitigate | The `nextest --profile ci` gate requires the exact 11509/0 baseline; a deviation blocks the sequence rather than being rationalized. |
</threat_model>

<verification>
The two Hetzner receipts (locked build + aggregate `nextest --profile ci`) determine acceptance. Record the sealed `source_sha`, source tree, both commands, exit statuses, counts, and remote receipt identity in `20-23-SUMMARY.md`. The summary states the candidate is sealed, no source/test changes follow, and native proof/requirement completion remain deferred (20-25/20-28). No Cargo runs on the Mac.
</verification>

<success_criteria>
- `Cargo.lock` is regenerated, consistent, and committed as the sealing commit (r10).
- The sealed candidate builds all-features on Hetzner and the aggregate suite is 11509/0 with no Linux regression (r4).
- The sealed SHA is recorded as the single candidate all downstream gates bind to.
</success_criteria>

## Artifacts this phase produces
- The sealed repaired candidate SHA with a consistent committed lockfile and green Hetzner build + aggregate receipts.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-23-SUMMARY.md` using the standard GSD summary template. Record the sealed `source_sha`/tree, the lock delta summary, both Hetzner receipts, and the explicit statement that no source/test changes follow and native/requirement completion is deferred. Complete no Phase 20 requirement.
</output>

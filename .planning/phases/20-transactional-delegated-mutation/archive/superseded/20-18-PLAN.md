---
phase: 20-transactional-delegated-mutation
plan: "18"
type: execute
wave: 17
depends_on:
  - "20-17"
files_modified: []
autonomous: false
requirements:
  - F20-01
  - F20-02
  - F20-03
  - F20-04
  - F20-05
  - F20-06
  - F20-GATE-01
  - F20-GATE-02
must_haves:
  truths:
    - "Only the exact durable pending tuple produced by 20-17 may receive Sean's candidate-specific authorization."
    - "One authorized workflow run proves native Windows and macOS behavior against the unchanged reviewed source; missing, skipped, incomplete, retried, or repaired evidence fails closed."
    - "The same exact source passes aggregate workspace test and build on Hetzner before any requirement-completion claim."
    - "Implementation, review, preparation, authorization, native UAT, aggregate proof, and requirement completion remain separately identified states."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-17-SUMMARY.md
      provides: "Immutable pending candidate/ref/runner/image tuple"
    - path: scripts/f20-native-uat-proof.mjs
      provides: "Candidate-specific authorization, publication, dispatch, capture, and evidence verification"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-17-SUMMARY.md
      to: scripts/f20-native-uat-proof.mjs
      via: "exact pending bytes and tuple-specific human authorization"
      pattern: "pending"
---

<objective>
Authorize once, run terminal native acceptance, and prove the exact independently reviewed Phase 20 source on Hetzner.

Purpose: keep every external mutation and requirement-completion claim behind a durable preparation boundary and an exact tuple-specific Sean decision.
Output: candidate-bound native Windows/macOS and aggregate Hetzner evidence, or an explicit incomplete disposition.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
@/Users/seandonahoe/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-16-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-17-SUMMARY.md
@scripts/f20-native-windows-proof.ps1
@scripts/f20-native-macos-proof.sh
@scripts/f20-native-uat-proof.mjs
@scripts/f20-native-uat-proof.test.mjs
@.github/workflows/nightly-windows-soak.yml
</context>

<admission>
- Reopen the strict pending object once without following links and require its exact digest and candidate/ref/runner/image tuple to match `20-17-SUMMARY.md`. Reauthenticate the 20-16 zero-finding source/review tuple and unchanged focused receipts. Any drift stops before the checkpoint.
- No prior general approval, silence, conversation memory, or approval for another tuple is authority. Only Sean's response `authorize &lt;pending_digest&gt;`, where the digest matches the complete displayed pending tuple, may authorize Task 2.
- Re-entry after authorization must use the same durable authorization/run state. It may capture an already-dispatched exact attempt, but it must never dispatch or retry a second run.
</admission>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Authorize the exact prepared candidate or leave Phase 20 incomplete</name>
  <files>None; this checkpoint performs no command and changes no file or external state.</files>
  <decision>Authorize one publication of the exact displayed candidate to its exact temporary UAT ref and one `nightly-windows-soak.yml` dispatch, or provide no authorization and leave every Phase 20 requirement incomplete.</decision>
  <context>Plan 20-17 completed all safe preparation and durably recorded the exact pending candidate commit/tree/ref plus runner ID/name/image tuple. This checkpoint asks only for a decision about those immutable bytes.</context>
  <options>
    <option id="authorize"><name>Authorize exact-candidate native proof</name><pros>Allows one native Windows/macOS acceptance run against the reviewed source.</pros><cons>Publishes one temporary UAT ref and consumes GitHub Actions plus the ephemeral macOS runner.</cons></option>
  </options>
  <action>Run no command. Display the complete exact pending tuple and its digest, then stop. Continue only when Sean replies `authorize &lt;pending_digest&gt;` using that exact digest.</action>
  <verify>The response is exactly `authorize &lt;pending_digest&gt;` and the digest recomputed from the unchanged pending bytes matches. Bare `authorize`, `hold`, silence, prior approval, or any other response fails this checkpoint and ends execution without continuing.</verify>
  <done>The exact tuple-specific authorization response exists; otherwise the plan remains incomplete and Task 2 does not run.</done>
  <resume-signal>Reply `authorize &lt;pending_digest&gt;` using the digest displayed with the complete exact candidate/ref/runner/image tuple. Any other response leaves Phase 20 incomplete.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Persist authorization, execute native acceptance, and run aggregate proof</name>
  <files>None; this task produces candidate-bound external evidence consumed by the plan summary.</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-16-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-17-SUMMARY.md, scripts/f20-native-windows-proof.ps1, scripts/f20-native-macos-proof.sh, scripts/f20-native-uat-proof.mjs, scripts/f20-native-uat-proof.test.mjs, .github/workflows/nightly-windows-soak.yml</read_first>
  <behavior>
    - `authorize` revalidates the exact pending bytes, candidate, runner identity, and tuple-specific response before exclusively persisting authorization.
    - Before publication, `publish-dispatch` persists and syncs immutable publication intent. On the first uninterrupted attempt it publishes only if the ref is absent; an exact ref is reused, a conflicting ref fails, and recovery after intent only reconciles the remote ref and never invokes a second push. Before the dispatch API call it separately persists and syncs immutable dispatch intent containing the authorization digest, exact full SHA/tree/ref/workflow, request nonce, API time boundary, and complete pre-existing run-ID set.
    - Once dispatch intent exists, every recovery path is reconcile-only and can never issue another dispatch. It identifies exactly one post-boundary, not-pre-existing run carrying the nonce and exact source/ref; zero after the bounded observation window or multiple matches fail closed rather than retrying.
    - `capture-verify` validates exact Windows/macOS job and runner identities, ordered per-target and final commit/tree markers, captured log bytes, and ephemeral runner deregistration.
    - The UAT ref remains retained evidence. Its remote SHA is rechecked after capture and immediately before summary; the summary records `retained`, the exact SHA, and `deletion_not_authorized`. Drift fails closed. Deletion requires separate exact-ref Sean authorization and another unchanged-remote-SHA check.
    - Only after native proof is green do aggregate workspace test and build run on Hetzner against the same exact source SHA/tree.
  </behavior>
  <action>Persist the exact digest-bound authorization. Persist and sync publication intent before the first publication call; after that boundary, reconcile the remote ref only and never issue a second push. Persist and sync dispatch intent, nonce, API boundary, and pre-run set before the only dispatch call; after that boundary, reconcile and bind only the unique matching attempt 1 and never dispatch again. Capture and verify it, recheck and retain the exact evidence ref, authenticate the primary-Git-dir proof-checkout locator, and run aggregate workspace test and build through the remote Hetzner harness against that unchanged source checkout rather than the later GSD checkout. Recheck the remote ref immediately before summary and record that deletion is not authorized. Do not retry, repair source, merge, release, deploy, tag, close an issue, or delete the UAT ref. A repaired successor returns to 20-08 and requires fresh 20-16 review, 20-17 preparation, and authorization.</action>
  <verify>
    <automated>node scripts/f20-native-uat-proof.mjs verify-publication --git-dir "$(git rev-parse --absolute-git-dir)" --repo FerroxLabs/wayland-core</automated>
    <automated>node scripts/f20-native-uat-proof.mjs verify-evidence --git-dir "$(git rev-parse --absolute-git-dir)" --repo FerroxLabs/wayland-core --workflow nightly-windows-soak.yml</automated>
    <automated>set -euo pipefail; proof_json="$(node scripts/f20-native-uat-proof.mjs proof-checkout --git-dir "$(git rev-parse --absolute-git-dir)")"; proof_checkout="$(printf '%s' "$proof_json" | jq -er .path)"; source_sha="$(printf '%s' "$proof_json" | jq -er .source_sha)"; source_tree="$(printf '%s' "$proof_json" | jq -er .source_tree)"; test "$(git -C "$proof_checkout" rev-parse HEAD)" = "$source_sha" && test "$(git -C "$proof_checkout" rev-parse HEAD^{tree})" = "$source_tree" && test -z "$(git -C "$proof_checkout" status --porcelain)" && /Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-aggregate-test "$proof_checkout" nextest run --workspace --profile ci</automated>
    <automated>set -euo pipefail; proof_json="$(node scripts/f20-native-uat-proof.mjs proof-checkout --git-dir "$(git rev-parse --absolute-git-dir)")"; proof_checkout="$(printf '%s' "$proof_json" | jq -er .path)"; source_sha="$(printf '%s' "$proof_json" | jq -er .source_sha)"; source_tree="$(printf '%s' "$proof_json" | jq -er .source_tree)"; test "$(git -C "$proof_checkout" rev-parse HEAD)" = "$source_sha" && test "$(git -C "$proof_checkout" rev-parse HEAD^{tree})" = "$source_tree" && test -z "$(git -C "$proof_checkout" status --porcelain)" && /Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-aggregate-build "$proof_checkout" build --workspace --all-features</automated>
  </verify>
  <acceptance_criteria>Both native platforms and both aggregate gates pass against the unchanged independently reviewed source. Any other state is incomplete.</acceptance_criteria>
  <done>The exact reviewed candidate has complete candidate-bound native and aggregate evidence and is eligible for factual Phase 20 requirement completion.</done>
</task>

</tasks>

<verification>
Authenticate the exact 20-16 review tuple, 20-17 pending tuple, tuple-specific authorization, retained focused receipts, single native attempt, and both aggregate Hetzner receipts. Every item must bind the same source SHA/tree. No Cargo runs on the local Mac.
</verification>

<success_criteria>
- The exact source passed independent zero-finding review before preparation and external mutation.
- One tuple-specific authorization produced exactly one native Windows/macOS attempt against that unchanged source.
- Aggregate workspace test and build passed on Hetzner against the same source.
- Requirements complete only when every preceding evidence state is green; otherwise all remain incomplete.
</success_criteria>

## Artifacts this phase produces

- Candidate-bound native Windows/macOS workflow evidence.
- Exact-source aggregate Hetzner test/build receipts.
- Terminal Phase 20 completion or explicit incomplete disposition.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-18-SUMMARY.md` using the standard GSD summary template. Record the 20-16 source/review tuple, 20-17 pending tuple, tuple-specific authorization, single-run native results, aggregate receipts, exact requirement disposition, and limitations as distinct factual states. Do not mark a requirement complete when any required state is missing, skipped, reused without authentication, or red.
</output>

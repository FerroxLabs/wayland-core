---
phase: 20-transactional-delegated-mutation
plan: "35"
type: execute
wave: 34
depends_on:
  - "20-34"
files_modified: []
autonomous: false
requirements:
  - F20-01
  - F20-02
  - F20-03
  - F20-04
  - F20-05
  - F20-06
  - F20-GATE-01
  - F20-GATE-02
  - REQ-native-r12
  - REQ-native-r4
must_haves:
  truths:
    - "Only the exact durable pending terminal tuple produced by 20-34 may receive Sean's fresh candidate-specific exact-tuple authorization; ALL prior digests (cb6f06bd/6ed9642c and the spent 95c81ec6 native-proof-dispatch authorization) are spent/void, and any RED or new candidate SHA requires fresh authorization (CONTEXT D6)."
    - "Under that authorization, the retained 20-32 dual-OS native evidence is re-verified bound to the exact sealed successor (unchanged remote SHA, both platforms green), and the exact sealed source passes aggregate workspace test AND build on Hetzner, before any requirement-completion claim (REQ-native-r12/r4)."
    - "Only when review (native NOT deferred), retained native evidence, and aggregate proof all belong to the one exact sealed successor SHA and are green do the Phase 20 requirements F20-01..F20-06 / F20-GATE-01 / F20-GATE-02 complete; otherwise all remain incomplete (Phase-20 Success Criterion #3 native clause closed)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-34-SUMMARY.md
      provides: "Immutable pending terminal candidate/ref/runner/image + native-evidence tuple"
    - path: scripts/f20-native-uat-proof.mjs
      provides: "Exact-tuple authorization, retained-evidence re-verification, and proof-checkout authority for the aggregate gate"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-34-SUMMARY.md
      to: scripts/f20-native-uat-proof.mjs
      via: "exact pending bytes and tuple-specific human authorization"
      pattern: "pending"
---

<objective>
Authorize once (exact-tuple), confirm the exact review-qualified sealed successor is dual-platform native green from the retained 20-32 evidence, run the aggregate Hetzner proof against the exact source, and complete the Phase 20 requirements — or record an explicit incomplete disposition.

Purpose: This is the terminal exact-tuple authorization gate (Sean gate #2, distinct from the native-proof dispatch at 20-32) for the repaired successor. It keeps requirement completion behind a durable preparation boundary and one exact Sean decision, closing the native clause of Phase-20 Success Criterion #3.
Output: Requirement completion bound to the one exact sealed successor SHA, or an explicit incomplete disposition.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
@/Users/seandonahoe/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-32-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-33-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-34-SUMMARY.md
@scripts/f20-native-uat-proof.mjs
@scripts/f20-native-windows-proof.ps1
@scripts/f20-native-macos-proof.sh
@.github/workflows/nightly-windows-soak.yml
</context>

<admission>
- Reopen the strict pending terminal object once without following links; require its exact digest and candidate/ref/runner/image + native-evidence tuple to match `20-34-SUMMARY.md`. Reauthenticate the `f20-native-16` zero-finding native-inclusive review (20-33) and the retained 20-32 native evidence (remote SHA unchanged, both platforms green). Any drift stops before the checkpoint.
- No prior general approval, silence, conversation memory, the spent native-proof-dispatch authorizations (20-25 and 20-32), or approval for another tuple is authority. Only Sean's exact-tuple terminal authorization matching the complete displayed pending digest may run Task 2.
- Re-entry after authorization uses the same durable authorization/verification state; it never re-dispatches native and never mutates source.
</admission>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Authorize the exact prepared successor or leave Phase 20 incomplete</name>
  <files>None; this checkpoint performs no command and changes no file or external state.</files>
  <decision>Provide the exact-tuple terminal authorization for the exact displayed sealed successor (re-verify retained dual-OS native evidence + run the aggregate Hetzner proof + complete Phase 20 requirements), or provide no authorization and leave every Phase 20 requirement incomplete.</decision>
  <context>20-34 durably recorded the exact pending terminal tuple (candidate commit/tree/ref + runner/image + retained native-evidence identity) for the successor that 20-33 reviewed CLEAN with native NOT deferred. This checkpoint asks only for a decision about those immutable bytes. This is the terminal exact-tuple authorization, separate from the already-spent native-proof-dispatch authorizations at 20-25 and 20-32.</context>
  <options>
    <option id="authorize"><name>Authorize exact-successor requirement completion</name><pros>Confirms dual-OS native green + aggregate against the reviewed source and completes Phase 20.</pros><cons>Re-verifies retained evidence and runs the aggregate Hetzner proof; requirement completion becomes a durable claim.</cons></option>
  </options>
  <action>Run no command. Display the complete exact pending terminal tuple and its digest, then stop. Continue only when Sean replies with the exact-tuple authorization using that exact digest.</action>
  <verify>The response is Sean's exact-tuple terminal authorization and the digest recomputed from the unchanged pending bytes matches. Bare approval, silence, prior approval, the spent 20-25/20-32 authorizations, or any other response fails this checkpoint and ends execution without continuing.</verify>
  <done>The exact tuple-specific terminal authorization exists; otherwise the plan remains incomplete and Task 2 does not run.</done>
  <resume-signal>Reply with the exact-tuple authorization using the digest displayed with the complete exact candidate/ref/runner/image + native-evidence tuple. Any other response leaves Phase 20 incomplete.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Persist authorization, confirm retained native evidence, run aggregate, and complete requirements</name>
  <files>None; this task consumes candidate-bound evidence and produces the terminal disposition.</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-32-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-33-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-34-SUMMARY.md, scripts/f20-native-uat-proof.mjs, .github/workflows/nightly-windows-soak.yml</read_first>
  <behavior>
    - The exact-tuple authorization revalidates the pending bytes, candidate, runner identity, and native-evidence identity before it is exclusively persisted.
    - Retained-evidence re-verification: the 20-32 UAT ref's remote SHA still equals the sealed successor; the Windows (self-hosted msvc) and macOS (ephemeral) jobs' ordered per-target + final markers are intact and green. This plan does NOT re-dispatch native (the native run was the 20-32 gate); it confirms the retained evidence under terminal authorization. Any drift or non-green fails closed.
    - Aggregate proof runs against the exact sealed successor proof checkout (unchanged HEAD/tree, clean): `nextest --profile ci` (11509/0) AND `build --workspace --all-features` on Hetzner.
    - Requirements complete only when the native-inclusive review, retained dual-OS native evidence, and both aggregate gates all bind the same exact sealed successor SHA and are green; the UAT ref stays retained (`deletion_not_authorized`).
  </behavior>
  <action>Persist the exact digest-bound terminal authorization. Re-verify the retained 20-32 dual-OS native evidence bound to the exact sealed successor SHA (remote ref SHA unchanged, both platforms' markers green) — do not re-dispatch. Authenticate the primary-Git-dir proof-checkout locator and run the aggregate workspace test and build through the Hetzner harness against that unchanged sealed checkout. Recheck the retained UAT ref immediately before summary and record `retained` / `deletion_not_authorized`. Only if the native-inclusive review, retained native evidence, and both aggregate gates are all green on the one exact sealed successor SHA, complete F20-01..F20-06 / F20-GATE-01 / F20-GATE-02 (closing the native clause of Success Criterion #3). Do not retry, repair source, re-dispatch native, merge, release, deploy, tag, close an issue, or delete the UAT ref. Any missing/red/drifted state leaves all requirements incomplete and routes to a further repaired successor + fresh review + prep + authorization.</action>
  <verify>
    <automated>node scripts/f20-native-uat-proof.mjs verify-evidence --git-dir "$(git rev-parse --absolute-git-dir)" --repo FerroxLabs/wayland-core --workflow nightly-windows-soak.yml</automated>
    <automated>set -euo pipefail; proof_json="$(node scripts/f20-native-uat-proof.mjs proof-checkout --git-dir "$(git rev-parse --absolute-git-dir)")"; proof_checkout="$(printf '%s' "$proof_json" | jq -er .path)"; source_sha="$(printf '%s' "$proof_json" | jq -er .source_sha)"; source_tree="$(printf '%s' "$proof_json" | jq -er .source_tree)"; test "$(git -C "$proof_checkout" rev-parse HEAD)" = "$source_sha" && test "$(git -C "$proof_checkout" rev-parse HEAD^{tree})" = "$source_tree" && test -z "$(git -C "$proof_checkout" status --porcelain)" && /Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-35-aggregate-test "$proof_checkout" nextest run --workspace --profile ci</automated>
    <automated>set -euo pipefail; proof_json="$(node scripts/f20-native-uat-proof.mjs proof-checkout --git-dir "$(git rev-parse --absolute-git-dir)")"; proof_checkout="$(printf '%s' "$proof_json" | jq -er .path)"; source_sha="$(printf '%s' "$proof_json" | jq -er .source_sha)"; source_tree="$(printf '%s' "$proof_json" | jq -er .source_tree)"; test "$(git -C "$proof_checkout" rev-parse HEAD)" = "$source_sha" && test "$(git -C "$proof_checkout" rev-parse HEAD^{tree})" = "$source_tree" && test -z "$(git -C "$proof_checkout" status --porcelain)" && /Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-35-aggregate-build "$proof_checkout" build --workspace --all-features</automated>
  </verify>
  <acceptance_criteria>Retained dual-OS native evidence is green and exact-SHA-bound, and both aggregate Hetzner gates pass against the unchanged reviewed source. Only then do the Phase 20 requirements complete; any other state is incomplete.</acceptance_criteria>
  <done>The exact reviewed successor has native-inclusive review, retained dual-OS native evidence, and aggregate proof all bound to one SHA; Phase 20 requirements are factually complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirement completion ← evidence set | Requirements complete only when review + native + aggregate all bind one exact SHA and are green |
| terminal authorization ← Sean | The exact-tuple terminal authorization is a Sean-only decision; all spent digests are void |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-35-01 | Elevation of Privilege | terminal authorization | critical | mitigate | Blocking-human exact-tuple checkpoint; only Sean's fresh exact-digest authorization for THIS successor runs Task 2; the spent 20-25/20-32 authorizations and all prior digests are void. |
| T-20-35-02 | Spoofing | retained native evidence | high | mitigate | Re-verify the UAT ref remote SHA is unchanged and both platforms' markers green before completion; never accept the summary's word or re-dispatch. |
| T-20-35-03 | Tampering | proof checkout for aggregate | high | mitigate | The aggregate runs only against a proof checkout whose HEAD/tree equal the sealed successor SHA and whose status is clean; a mutated checkout fails closed. |
</threat_model>

<verification>
Authenticate the exact `f20-native-16` review tuple, the 20-34 pending terminal tuple, the exact-tuple authorization, the retained single native attempt (both platforms green, remote SHA unchanged), and both aggregate Hetzner receipts. Every item binds the same sealed successor SHA/tree. No Cargo runs on the Mac.
</verification>

<success_criteria>
- The exact sealed successor source passed a native-inclusive zero-finding review before this terminal gate.
- One exact-tuple Sean authorization confirmed retained dual-OS native green and ran the aggregate against the unchanged source.
- Aggregate workspace test (11509/0) and all-features build passed on Hetzner against the same source.
- Phase 20 requirements complete only when every evidence state is green on one SHA; otherwise all remain incomplete.
</success_criteria>

## Artifacts this phase produces
- Terminal Phase 20 requirement completion (or explicit incomplete disposition) bound to the one exact sealed successor SHA.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-35-SUMMARY.md` using the standard GSD summary template. Record the `f20-native-16` source/review tuple, the 20-34 pending tuple, the exact-tuple authorization, retained dual-OS native results, both aggregate receipts, the exact requirement disposition, and limitations as distinct factual states. Do not mark a requirement complete when any required state is missing, skipped, reused without authentication, re-dispatched, or red.
</output>

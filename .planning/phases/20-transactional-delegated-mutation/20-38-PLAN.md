---
phase: 20-transactional-delegated-mutation
plan: "38"
type: execute
wave: 37
depends_on:
  - "20-37"
files_modified:
  - .planning/phases/20-transactional-delegated-mutation/20-38-CROSS-AUDIT.md
autonomous: true
requirements:
  - REQ-native-r12
  - REQ-native-r13
must_haves:
  truths:
    - "A fresh non-author reviewer cross-audits the exact sealed second-repaired-successor SHA (recorded by 20-37) at every severity across the 20-36 repair delta (the Windows exit-0 normalization in live_fs_acl.rs + hard_process_containment_windows.rs and the macOS docker-image pre-pull in f20-native-macos-proof.sh), before any Sean-gated native run is re-spent (REQ-native-r12 pre-native gate)."
    - "Every claimed reviewer emits its OWN schema-validated review artifact (wayland-core.phase20-independent-review.v1); no prose-only review counts toward the PASS — sustaining the 20-24/20-31 attestation discipline (REQ-native-r13)."
    - "The cross-audit defers only native_macos/native_windows (proven at 20-39); code review, all-severity, evidence integrity, and integration authority of the repair delta are prosecuted here with zero findings, or the sequence stops for a further repaired successor."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-38-CROSS-AUDIT.md
      provides: "Fresh non-author schema-validated cross-audit of the sealed second-repaired-successor repair delta"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-38-CROSS-AUDIT.md
      to: .planning/phases/20-transactional-delegated-mutation/20-37-SUMMARY.md
      via: "source/review-base/review authority tuple over the sealed second-repaired-successor SHA"
      pattern: "source_sha"
---

<objective>
Cross-audit the exact sealed second-repaired-successor before re-spending a Sean-gated native run: a fresh non-author reviewer prosecutes the 20-36 repair delta at every severity and records a schema-validated artifact per claimed reviewer, native explicitly deferred to 20-39.

Purpose: The gate sequence puts an adversarial cross-audit before the native proof so a broken candidate does not consume the scarce native run; and R13 requires per-reviewer schema-validated artifacts, not prose. This is the pre-native attestation gate for the second repaired successor. The 20-36 delta changes cmd.exe exit-code semantics in security-relevant AppContainer/Job-Object acceptance tests and adds a docker pre-pull to the proof harness, so the audit focuses adversarially on whether the exit-0 normalization can MASK a denied read or change the job-close containment trigger, and whether the pre-pull is scoped and fail-closed.
Output: One (or more, per claimed reviewer) schema-validated cross-audit JSON artifact over the sealed successor SHA; zero findings admits the native-proof gate.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
@/Users/seandonahoe/.agents/skills/gsd-validate-phase/SKILL.md
@/Users/seandonahoe/.agents/skills/gsd-secure-phase/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-32-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-36-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-37-SUMMARY.md
@.planning/scripts/verify-review-result.mjs
@.planning/scripts/verify-review-pair.sh
</context>

<admission>
- Require a clean isolated checkout at the sealed successor SHA recorded by `20-37-SUMMARY.md`; verify its tree. Capture the source authority: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-38)"` and `node .planning/scripts/task-base-authority.mjs capture "$(git rev-parse --git-path gsd-reviewed-source-20-38)" "$source" "$(git rev-parse "$source^{tree}")"`.
- The executor MUST be a fresh non-author executor distinct from the 20-36 source author. If reviewer identity cannot establish that separation, fail closed.
- Each claimed reviewer records its OWN schema-validated artifact. A prose-only or artifact-less claimed review does not count toward the PASS (R13).
</admission>

<tasks>

<task type="auto">
  <name>Task 1: Fresh non-author cross-audit of the sealed 20-36 repair delta</name>
  <files>.planning/phases/20-transactional-delegated-mutation/20-38-CROSS-AUDIT.md</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-32-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-36-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-37-SUMMARY.md, crates/wcore-sandbox/tests/live_fs_acl.rs, crates/wcore-sandbox/tests/hard_process_containment_windows.rs, crates/wcore-sandbox/tests/docker_smoke.rs, scripts/f20-native-windows-proof.ps1, scripts/f20-native-macos-proof.sh</read_first>
  <behavior>
    - Prosecute the exact sealed successor SHA at every severity, focused on the 20-36 delta: the Windows exit-0 normalization is placed ONLY on the SUCCESS branch behind the `type \"<file>\" && ( … )` short-circuit so a DENIED read still surfaces its nonzero exit (it cannot force a false exit-0 PASS); the stdin-free `choice` hold is preserved so the grant ACE is still observable present-during; the Job-Object parent-exit sites still exit the parent (the KILL_ON_JOB_CLOSE reap trigger) with the `start "" /b` idlers, `choice` holds, and wall-clock/no-residue assertions unchanged; `contained_detached_child_exit` (`& exit {code}`) is unchanged; and the full sibling/target audit ledger from 20-36 is complete (both live_fs_acl targets, both Job-Object targets, the two 'any' targets) with no `ver`-tail exit-0 site left behind.
    - Prosecute the macOS harness change: the `docker pull` is scoped after the `docker info` gate and before the first `run_target`, fails closed under `set -euo pipefail`, pulls exactly the image `docker_smoke.rs` uses (alpine:3.19), and leaves the eight target selectors/order and the ordered per-target + single final marker grammar (consumed by `f20-native-uat-proof.mjs`) unchanged.
    - Confirm the sealed successor's Hetzner build + aggregate receipts (from 20-37) belong to the exact SHA and are green (11509/0/48), and that the macOS `cargo check … --tests` receipt (from 20-36) is green.
    - Classify native_macos/native_windows as DEFERRED to the 20-39 native-proof gate — not passed from source. Explicitly note that the Windows helpers are `#![cfg(windows)]` and their compile + exit-code behavior are provable only on the msvc runner at 20-39, so a source-only PASS here is necessarily native-deferred.
    - Every BLOCKER/CRITICAL/HIGH/MEDIUM/LOW finding is disqualifying; do not repair source here.
    - Record the result as one schema-validated JSON object matching `wayland-core.phase20-independent-review.v1`, profile `f20-native-crossaudit` (checks all_severity/evidence_integrity/integration_authority PASS; deferred native_macos/native_windows). Distinct stable source-executor/reviewer identities; never embed the review commit's own SHA.
  </behavior>
  <action>Inspect the complete sealed successor delta (the Windows exit-0 normalization across live_fs_acl.rs + hard_process_containment_windows.rs and the macOS docker pre-pull) without changing source. Apply the code-review, `gsd-validate-phase`, and `gsd-secure-phase` (ASVS Level 2) checklists as one blocking exact-source cross-audit, focused adversarially on whether the exit-0 normalizer can mask a denied read, whether the job-close containment trigger is preserved, and whether the docker pre-pull is scoped + fail-closed. Write one combined schema-validated JSON artifact matching `.planning/scripts/verify-review-result.mjs` profile `f20-native-crossaudit`. If additional cross-AI reviewers are claimed, EACH emits its own schema-validated artifact (a sibling `20-38-CROSS-AUDIT-<reviewer>.md`); a claimed reviewer without an artifact does not count. PASS only when every pre-native truth has credible source/receipt evidence, native is explicitly deferred, and zero findings remain; otherwise record FAIL and stop the sequence for a further repaired successor.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-38)" .planning/phases/20-transactional-delegated-mutation/20-38-CROSS-AUDIT.md</automated>
    <automated>source="$(node .planning/scripts/task-base-authority.mjs read "$(git rev-parse --git-path gsd-reviewed-source-20-38)" | sed -n '1p')"; review="$(git rev-parse HEAD)"; node .planning/scripts/verify-review-result.mjs "$review" .planning/phases/20-transactional-delegated-mutation/20-38-CROSS-AUDIT.md "$source" "$(git rev-parse "${source}^{tree}")" f20-native-crossaudit</automated>
  </verify>
  <acceptance_criteria>The cross-audit is fresh, non-author, exact-sealed-SHA, complete at every severity, schema-validated per claimed reviewer, and native-deferred. PASS requires zero findings; FAIL creates no qualification and does not admit 20-39.</acceptance_criteria>
  <done>The schema-validated cross-audit artifact(s) exist for post-task pair verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native run authorization ← pre-native audit | A scarce Sean-gated native run should not be re-spent on a candidate a fresh audit would reject |
| PASS ← reviewer artifact | Only a schema-validated per-reviewer artifact may count toward the PASS (no prose-only reviews) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-38-01 | Spoofing | exit-0 normalizer masking a denied read | high | mitigate | The cross-audit adversarially confirms the exit-0 primitive is behind the `type && (...)` short-circuit and the exit-0 assertion still co-asserts MARKER-in-stdout, so a denied read cannot pass, before the native run is re-spent. |
| T-20-38-02 | Tampering | job-close containment trigger | high | mitigate | The audit confirms only the exit-code tail changed and the parent still exits (KILL_ON_JOB_CLOSE trigger + no-residue assertions intact), so the containment property is preserved not merely satisfied. |
| T-20-38-03 | Repudiation | unattested reviews | high | mitigate | Every claimed reviewer emits a schema-validated artifact verified by `verify-review-result.mjs`; prose-only reviews do not count. |
</threat_model>

<verification>
Derive `source` and `review` and run the schema verifier (Task 1 `<verify>`). Require a zero-finding PASS under profile `f20-native-crossaudit`. Record the externally derived `(source_sha, source_tree, review_sha)` tuple and each reviewer artifact identity in `20-38-SUMMARY.md`. No Cargo runs on the Mac.
</verification>

<success_criteria>
- A fresh non-author reviewer cross-audited the exact sealed successor at every severity with zero findings (r12 pre-native gate).
- Every claimed reviewer emitted a schema-validated artifact; no prose-only review counted (r13).
- Native is explicitly deferred to 20-39; only a zero-finding PASS admits the native-proof gate.
</success_criteria>

## Artifacts this phase produces
- `.planning/phases/20-transactional-delegated-mutation/20-38-CROSS-AUDIT.md` (+ any per-reviewer siblings) — schema-validated pre-native cross-audit.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-38-SUMMARY.md` with executor identity, sealed source SHA/tree, review SHA(s), per-reviewer artifact identities, all-severity disposition, and explicit admission or rejection of the 20-39 native-proof gate. It admits no requirement.
</output>

---
phase: 20-transactional-delegated-mutation
plan: "16"
type: execute
wave: 15
depends_on:
  - "20-08"
files_modified:
  - .planning/phases/20-transactional-delegated-mutation/20-08-INDEPENDENT-REVIEW.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "A fresh executor that did not author 20-08 reviews the exact clean product source SHA and tree recorded by 20-08-SUMMARY.md."
    - "The source summary and stock-GSD tracking updates form a linear merge-free metadata-only chain from the reviewed source; the review commit is a sole-parent child of that review base and changes only the review record."
    - "Independent code review, phase-contract validation, and ASVS Level 2 security prosecution all cover the exact source; every BLOCKER, CRITICAL, HIGH, MEDIUM, and LOW finding is recorded, and only a zero-finding PASS admits preparation plan 20-17."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-08-INDEPENDENT-REVIEW.md
      provides: "Exact-source non-author all-severity review of final lifecycle construction and native-UAT machinery"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-08-INDEPENDENT-REVIEW.md
      to: .planning/phases/20-transactional-delegated-mutation/20-08-SUMMARY.md
      via: "source/review-base/review authority tuple"
      pattern: "source_sha"
---

<objective>
Independently review the exact final Phase 20 construction before any native publication, dispatch, aggregate proof, or requirement completion.

Purpose: make final product-source review a dependency-ordered stock-GSD boundary rather than an advisory check after terminal UAT.
Output: one review-only descendant commit and an externally derived source/review-base/review qualification tuple.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
@/Users/seandonahoe/.agents/skills/gsd-validate-phase/SKILL.md
@/Users/seandonahoe/.agents/skills/gsd-secure-phase/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-08-PLAN.md
@.planning/phases/20-transactional-delegated-mutation/20-08-SUMMARY.md
@crates/wcore-agent/src/child_transaction.rs
@crates/wcore-agent/src/durable_spawner.rs
@crates/wcore-agent/src/engine.rs
@crates/wcore-agent/src/spawner.rs
@crates/wcore-agent/src/orchestration/anvil/tool.rs
@crates/wcore-agent/tests/transactional_delegated_mutation_test.rs
@.github/workflows/nightly-windows-soak.yml
@scripts/f20-native-windows-proof.ps1
@scripts/f20-native-macos-proof.sh
@scripts/f20-native-uat-proof.mjs
@scripts/f20-native-uat-proof.test.mjs
</context>

<admission>
- Require a clean isolated checkout at the end of the ordinary stock-GSD 20-08 metadata chain. Starting at the exact `source_sha` and tree recorded in `20-08-SUMMARY.md`, every intermediate commit must be sole-parent, merge-free, and change only `20-08-SUMMARY.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, or `.planning/REQUIREMENTS.md`; the summary must change exactly once.
- Before editing, capture the current commit/tree separately with `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-16)"` and `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-review-base-20-16)"`. Derive the exact source SHA from the summary, verify its recorded tree, then exclusively capture both with `node .planning/scripts/task-base-authority.mjs capture "$(git rev-parse --git-path gsd-reviewed-source-20-16)" "$source" "$(git rev-parse "$source^{tree}")"`.
- The executor must be a fresh stock-GSD executor distinct from the 20-08 executor. If executor identity cannot establish that separation, fail closed.
</admission>

<tasks>

<task type="auto">
  <name>08-R: Prosecute final lifecycle and native-proof authority</name>
  <files>.planning/phases/20-transactional-delegated-mutation/20-08-INDEPENDENT-REVIEW.md</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-08-PLAN.md, .planning/phases/20-transactional-delegated-mutation/20-08-SUMMARY.md, AGENTS.md, crates/wcore-agent/src/child_transaction.rs, crates/wcore-agent/src/durable_spawner.rs, crates/wcore-agent/src/engine.rs, crates/wcore-agent/src/spawner.rs, crates/wcore-agent/src/orchestration/anvil/tool.rs, crates/wcore-agent/tests/transactional_delegated_mutation_test.rs, .github/workflows/nightly-windows-soak.yml, scripts/f20-native-windows-proof.ps1, scripts/f20-native-macos-proof.sh, scripts/f20-native-uat-proof.mjs, scripts/f20-native-uat-proof.test.mjs</read_first>
  <behavior>
    - Review lifecycle composition, retained snapshot authority, effects ordering, gate ownership, candidate selection, parent CAS, rollback, restart convergence, cleanup identity, and Forge terminal reporting.
    - Review native helper and workflow authority, exact candidate binding, no-follow state/log handling, runner identity, one-dispatch semantics, marker completeness/order, and the narrow UAT-only publication exception.
    - Execute the substantive `gsd-validate-phase` contract against every Phase 20 must-have and user-observable outcome that can be proved before external native UAT; classify native-only checks as pending for 20-18 rather than passing them from source.
    - Execute the substantive `gsd-secure-phase` threat-model and ASVS Level 2 review with `security_block_on: low`; no open security finding at any severity may pass.
    - Reconcile every 20-08 must-have, hostile test, and focused committed-head receipt against the exact source bytes. Types, source presence, builder self-checks, or intended future native execution are insufficient.
    - Treat every BLOCKER, CRITICAL, HIGH, MEDIUM, and LOW finding as disqualifying. Do not repair production or test source in this plan.
    - Record the result as one schema-validated JSON object matching `wayland-core.phase20-independent-review.v1`; include distinct stable source-executor/reviewer identities, exact source SHA/tree, PASS for code review, phase validation, ASVS Level 2, and all-severity prosecution, native macOS/Windows as the only deferred checks, zero counts for every severity, and executed command evidence. Never embed the review commit's own SHA in the review record.
  </behavior>
  <action>Inspect the complete exact 20-08 source tree, tests, summary, and committed-head evidence without changing source. Apply the installed code-review, `gsd-validate-phase`, and `gsd-secure-phase` checklists as one blocking exact-source review, including a requirement/must-have evidence matrix and ASVS Level 2 threat review. Write one combined independent record as schema-validated JSON matching `.planning/scripts/verify-review-result.mjs` profile `f20-16`. PASS only when every pre-UAT truth has credible source and hostile-test evidence, every focused receipt belongs to the exact source, native proof machinery fails closed before external mutation, native-only checks remain explicitly pending for 20-18, and zero findings remain; otherwise record FAIL and stop the dependency chain for a repaired 20-08 successor and fresh review.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-16)" .planning/phases/20-transactional-delegated-mutation/20-08-INDEPENDENT-REVIEW.md</automated>
    <automated>base="$(node .planning/scripts/task-base-authority.mjs read "$(git rev-parse --git-path gsd-task-base-20-16)" | sed -n '1p')"; git diff --check "$base"..HEAD && git diff --cached --check && git diff --check</automated>
  </verify>
  <acceptance_criteria>The review is fresh, non-author, exact-source, complete at every severity, evidence-backed, and changes only `20-08-INDEPENDENT-REVIEW.md`. PASS requires zero findings; FAIL creates no qualification and does not admit 20-17.</acceptance_criteria>
  <done>The review-only commit exists for post-task exact-pair verification.</done>
</task>

</tasks>

<verification>
Before summary creation, derive `source` and `review_base` from the first lines returned by one no-follow read of their respective Git-private two-line authority objects, derive `review=$(git rev-parse HEAD)`, and run:

<automated>source="$(node .planning/scripts/task-base-authority.mjs read "$(git rev-parse --git-path gsd-reviewed-source-20-16)" | sed -n '1p')"; review_base="$(node .planning/scripts/task-base-authority.mjs read "$(git rev-parse --git-path gsd-review-base-20-16)" | sed -n '1p')"; review="$(git rev-parse HEAD)"; bash .planning/scripts/verify-review-pair.sh "$source" "$review_base" "$review" .planning/phases/20-transactional-delegated-mutation/20-08-SUMMARY.md .planning/phases/20-transactional-delegated-mutation/20-08-INDEPENDENT-REVIEW.md AGENTS.md crates/wcore-agent/src/child_transaction.rs crates/wcore-agent/src/durable_spawner.rs crates/wcore-agent/src/engine.rs crates/wcore-agent/src/spawner.rs crates/wcore-agent/src/orchestration/anvil/tool.rs crates/wcore-agent/tests/transactional_delegated_mutation_test.rs .github/workflows/nightly-windows-soak.yml scripts/f20-native-windows-proof.ps1 scripts/f20-native-macos-proof.sh scripts/f20-native-uat-proof.mjs scripts/f20-native-uat-proof.test.mjs</automated>
<automated>source="$(node .planning/scripts/task-base-authority.mjs read "$(git rev-parse --git-path gsd-reviewed-source-20-16)" | sed -n '1p')"; review="$(git rev-parse HEAD)"; node .planning/scripts/verify-review-result.mjs "$review" .planning/phases/20-transactional-delegated-mutation/20-08-INDEPENDENT-REVIEW.md "$source" "$(git rev-parse "${source}^{tree}")" f20-16</automated>

Require a zero-finding PASS. Record the externally derived `(source_sha, source_tree, review_base_sha, review_sha)` tuple in `20-16-SUMMARY.md`; the later summary commit is metadata and is not part of that tuple. No Cargo runs on macOS.
</verification>

<success_criteria>
- A distinct non-author executor reviewed, phase-validated, and ASVS Level 2 security-prosecuted the exact 20-08 source SHA/tree and every declared product, test, helper, workflow, and policy path.
- The stock-GSD summary/tracking chain is linear and metadata-only, the review commit is review-file-only, and every reviewed source blob remains unchanged throughout.
- Every finding at every severity is repaired in a new 20-08 successor or rejected with executable counter-evidence and re-reviewed.
- Only a zero-finding PASS in the exact review tuple admits preparation plan 20-17. Plan 20-18 remains blocked until 20-17 durably summarizes the matching pending tuple.
</success_criteria>

## Artifacts this phase produces

- `.planning/phases/20-transactional-delegated-mutation/20-08-INDEPENDENT-REVIEW.md` — exact-source independent all-severity review record.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-16-SUMMARY.md` with executor identity, source SHA/tree, review-base/review SHAs, exact changed-path proof, all-severity disposition, and explicit admission or rejection of preparation plan 20-17. It does not directly admit 20-18.
</output>

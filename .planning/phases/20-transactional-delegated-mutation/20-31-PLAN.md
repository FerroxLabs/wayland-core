---
phase: 20-transactional-delegated-mutation
plan: "31"
type: execute
wave: 30
depends_on:
  - "20-30"
files_modified:
  - .planning/phases/20-transactional-delegated-mutation/20-31-CROSS-AUDIT.md
autonomous: true
requirements:
  - REQ-native-r12
  - REQ-native-r13
must_haves:
  truths:
    - "A fresh non-author reviewer cross-audits the exact sealed repaired-successor SHA (recorded by 20-30) at every severity across the 20-29 repair delta (the two unix rename_into methods, the atomic_write_child publish unification, and the candidate-workflow safe.directory guard), before any Sean-gated native run is re-spent (REQ-native-r12 pre-native gate)."
    - "Every claimed reviewer emits its OWN schema-validated review artifact (wayland-core.phase20-independent-review.v1); no prose-only review counts toward the PASS — sustaining the 20-24 attestation discipline (REQ-native-r13)."
    - "The cross-audit defers only native_macos/native_windows (proven at 20-32); code review, all-severity, evidence integrity, and integration authority of the repair delta are prosecuted here with zero findings, or the sequence stops for a further repaired successor."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-31-CROSS-AUDIT.md
      provides: "Fresh non-author schema-validated cross-audit of the sealed repaired-successor repair delta"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-31-CROSS-AUDIT.md
      to: .planning/phases/20-transactional-delegated-mutation/20-30-SUMMARY.md
      via: "source/review-base/review authority tuple over the sealed repaired-successor SHA"
      pattern: "source_sha"
---

<objective>
Cross-audit the exact sealed repaired-successor before re-spending a Sean-gated native run: a fresh non-author reviewer prosecutes the 20-29 repair delta at every severity and records a schema-validated artifact per claimed reviewer, native explicitly deferred to 20-32.

Purpose: The gate sequence puts an adversarial cross-audit before the native proof so a broken candidate does not consume the scarce native run; and R13 requires per-reviewer schema-validated artifacts, not prose. This is the pre-native attestation gate for the repaired successor. The 20-29 delta touches a TOCTOU-sensitive sandbox boundary op (the unix rename_into), so the audit focuses adversarially on the isolation property, not just compilation.
Output: One (or more, per claimed reviewer) schema-validated cross-audit JSON artifact over the sealed successor SHA; zero findings admits the native-proof gate.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
@/Users/seandonahoe/.agents/skills/gsd-validate-phase/SKILL.md
@/Users/seandonahoe/.agents/skills/gsd-secure-phase/SKILL.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-25-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-29-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-30-SUMMARY.md
@.planning/scripts/verify-review-result.mjs
@.planning/scripts/verify-review-pair.sh
</context>

<admission>
- Require a clean isolated checkout at the sealed successor SHA recorded by `20-30-SUMMARY.md`; verify its tree. Capture the source authority: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-31)"` and `node .planning/scripts/task-base-authority.mjs capture "$(git rev-parse --git-path gsd-reviewed-source-20-31)" "$source" "$(git rev-parse "$source^{tree}")"`.
- The executor MUST be a fresh non-author executor distinct from the 20-29 source author. If reviewer identity cannot establish that separation, fail closed.
- Each claimed reviewer records its OWN schema-validated artifact. A prose-only or artifact-less claimed review does not count toward the PASS (R13).
</admission>

<tasks>

<task type="auto">
  <name>Task 1: Fresh non-author cross-audit of the sealed repair delta</name>
  <files>.planning/phases/20-transactional-delegated-mutation/20-31-CROSS-AUDIT.md</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-25-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-29-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-30-SUMMARY.md, crates/wcore-sandbox/src/directory_authority.rs, crates/wcore-sandbox/src/directory_authority_file.rs, crates/wcore-sandbox/src/directory_authority_tests.rs, crates/wcore-sandbox/src/directory_authority_windows.rs, .github/workflows/nightly-windows-soak.yml, scripts/f20-native-windows-proof.ps1, scripts/f20-native-macos-proof.sh</read_first>
  <behavior>
    - Prosecute the exact sealed successor SHA at every severity: the unix `rename_into` resolves source AND destination names ONLY relative to the retained destination/target-parent dirfd (never `display_path` through the ambient namespace); it re-proves the source child's identity against the held object before renaming; `replace=false` cannot clobber (no-replace primitive); the atomic_write_child publish unification is byte-for-byte behavior-preserving; the file variant is genuinely used (no dead code) and the directory variant is `pub` API-reachable; the Windows `windows::rename_*_into` delegation path is unchanged; the workflow `safe.directory` guard is scoped to the candidate jobs only, before the tree-resolve, and does not weaken the candidate gating/permissions/secrets posture.
    - Confirm the sealed successor's Hetzner build + aggregate receipts (from 20-30) belong to the exact SHA and are green (11509/0/48), and that the macOS compile-check receipt (from 20-29) closed the E0599.
    - Classify native_macos/native_windows as DEFERRED to the 20-32 native-proof gate — not passed from source.
    - Every BLOCKER/CRITICAL/HIGH/MEDIUM/LOW finding is disqualifying; do not repair source here.
    - Record the result as one schema-validated JSON object matching `wayland-core.phase20-independent-review.v1`, profile `f20-native-crossaudit` (checks all_severity/evidence_integrity/integration_authority PASS; deferred native_macos/native_windows). Distinct stable source-executor/reviewer identities; never embed the review commit's own SHA.
  </behavior>
  <action>Inspect the complete sealed successor delta (the 20-29 unix rename_into methods, the atomic_write_child unification, and the candidate-workflow guard) without changing source. Apply the code-review, `gsd-validate-phase`, and `gsd-secure-phase` (ASVS Level 2) checklists as one blocking exact-source cross-audit, focused adversarially on the security-boundary change (the unix handle-relative rename) and the workflow guard scope. Write one combined schema-validated JSON artifact matching `.planning/scripts/verify-review-result.mjs` profile `f20-native-crossaudit`. If additional cross-AI reviewers are claimed, EACH emits its own schema-validated artifact (a sibling `20-31-CROSS-AUDIT-<reviewer>.md`); a claimed reviewer without an artifact does not count. PASS only when every pre-native truth has credible source/receipt evidence, native is explicitly deferred, and zero findings remain; otherwise record FAIL and stop the sequence for a further repaired successor.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-31)" .planning/phases/20-transactional-delegated-mutation/20-31-CROSS-AUDIT.md</automated>
    <automated>source="$(node .planning/scripts/task-base-authority.mjs read "$(git rev-parse --git-path gsd-reviewed-source-20-31)" | sed -n '1p')"; review="$(git rev-parse HEAD)"; node .planning/scripts/verify-review-result.mjs "$review" .planning/phases/20-transactional-delegated-mutation/20-31-CROSS-AUDIT.md "$source" "$(git rev-parse "${source}^{tree}")" f20-native-crossaudit</automated>
  </verify>
  <acceptance_criteria>The cross-audit is fresh, non-author, exact-sealed-SHA, complete at every severity, schema-validated per claimed reviewer, and native-deferred. PASS requires zero findings; FAIL creates no qualification and does not admit 20-32.</acceptance_criteria>
  <done>The schema-validated cross-audit artifact(s) exist for post-task pair verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native run authorization ← pre-native audit | A scarce Sean-gated native run should not be re-spent on a candidate a fresh audit would reject |
| PASS ← reviewer artifact | Only a schema-validated per-reviewer artifact may count toward the PASS (no prose-only reviews) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-31-01 | Tampering | unix rename_into isolation | critical | mitigate | The cross-audit adversarially re-confirms the rename resolves neither pathname through the ambient namespace and re-proves source identity before the native run is re-spent. |
| T-20-31-02 | Repudiation | unattested reviews | high | mitigate | Every claimed reviewer emits a schema-validated artifact verified by `verify-review-result.mjs`; prose-only reviews do not count. |
</threat_model>

<verification>
Derive `source` and `review` and run the schema verifier (Task 1 `<verify>`). Require a zero-finding PASS under profile `f20-native-crossaudit`. Record the externally derived `(source_sha, source_tree, review_sha)` tuple and each reviewer artifact identity in `20-31-SUMMARY.md`. No Cargo runs on the Mac.
</verification>

<success_criteria>
- A fresh non-author reviewer cross-audited the exact sealed successor at every severity with zero findings (r12 pre-native gate).
- Every claimed reviewer emitted a schema-validated artifact; no prose-only review counted (r13).
- Native is explicitly deferred to 20-32; only a zero-finding PASS admits the native-proof gate.
</success_criteria>

## Artifacts this phase produces
- `.planning/phases/20-transactional-delegated-mutation/20-31-CROSS-AUDIT.md` (+ any per-reviewer siblings) — schema-validated pre-native cross-audit.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-31-SUMMARY.md` with executor identity, sealed source SHA/tree, review SHA(s), per-reviewer artifact identities, all-severity disposition, and explicit admission or rejection of the 20-32 native-proof gate. It admits no requirement.
</output>

---
phase: 20-transactional-delegated-mutation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/wcore-sandbox/src/backends/appcontainer.rs
  - crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs
  - crates/wcore-sandbox/src/backends/appcontainer/acl_lease/sha256.rs
  - crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs
  - crates/wcore-sandbox/src/backends/appcontainer/acl_lease/mutation_lock.rs
  - crates/wcore-sandbox/src/backends/appcontainer/acl_lease/tests.rs
  - crates/wcore-sandbox/tests/live_fs_acl.rs
  - .github/workflows/nightly-windows-soak.yml
  - scripts/f20-native-windows-proof.ps1
autonomous: true
requirements: []
must_haves:
  truths:
    - "Each Windows sandbox execution owns a unique AppContainer identity and a durable ACL lease bound to that identity."
    - "Stale, ambiguous, swapped, corrupt, or foreign lease state fails closed before ACL mutation."
    - "Cleanup removes only the exact owned SID grants and exact owned AppContainer profile, never removes another live execution's authority, and durably resumes incomplete cleanup after restart."
  artifacts:
    - path: crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs
      provides: "Per-execution identity and ACL lease lifecycle"
      exports: ["ExecutionIdentity"]
    - path: crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs
      provides: "No-follow, identity-checked, crash-safe lease persistence"
    - path: crates/wcore-sandbox/tests/live_fs_acl.rs
      provides: "Native Windows concurrent ACL isolation and cleanup proof"
    - path: scripts/f20-native-windows-proof.ps1
      provides: "Exact-candidate native Windows UAT entrypoint"
  key_links:
    - from: crates/wcore-sandbox/src/backends/appcontainer.rs
      to: crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs
      via: "execution start and cleanup guard"
      pattern: "ExecutionIdentity"
    - from: .github/workflows/nightly-windows-soak.yml
      to: crates/wcore-sandbox/tests/live_fs_acl.rs
      via: "native Windows test invocation"
      pattern: "live_fs_acl"
---

<objective>
Make Windows AppContainer identity and filesystem authority exact, isolated, crash-recoverable, and natively provable.

Purpose: F20 cannot claim transactional isolation if concurrent Windows children share or leak ACL authority.
Output: ACL lease lifecycle, hardened storage/serialization, concurrency tests, and an exact-candidate native proof route.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@crates/wcore-sandbox/src/backends/appcontainer.rs
@crates/wcore-sandbox/tests/live_fs_acl.rs
@.github/workflows/nightly-windows-soak.yml
</context>

<salvage>
Use cumulative successor `999303eb116640216a4b0b6ad9e50e75461f9cb2` as the only salvage source for owned `wcore-sandbox` paths. Treat `b4c3f5aa7d2767c0d01bb41830d122c136606c66` as superseded provenance wherever paths overlap. Reconcile path by path, never cherry-pick blindly, and record adopted/adapted/rejected dispositions in the SUMMARY.
</salvage>

<tasks>

<task type="auto">
  <name>Task 1: Own a unique AppContainer identity and ACL lease per execution</name>
  <files>crates/wcore-sandbox/src/backends/appcontainer.rs, crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs, crates/wcore-sandbox/src/backends/appcontainer/acl_lease/sha256.rs</files>
  <read_first>crates/wcore-sandbox/src/backends/appcontainer.rs, crates/wcore-sandbox/src/manifest.rs, crates/wcore-sandbox/tests/live_fs_acl.rs</read_first>
  <behavior>
    - Concurrent executions receive distinct profile names and SIDs.
    - Lease content binds the profile, SID, process identity, intents, state, and digest.
    - Unknown, stale, malformed, or digest-mismatched leases are rejected before any grant or revoke operation.
    - After the owned process tree exits and its exact SID grants are revoked, cleanup deletes only that execution's AppContainer profile; incomplete deletion remains durably cleanup-pending and is retried from the lease after restart.
    - A live, foreign, reused, or identity-mismatched profile is never deleted; the lease remains diagnosable and fails closed instead of leaking silent authority.
  </behavior>
  <action>Wire a per-execution `ExecutionIdentity` into AppContainer launch and cleanup. Canonicalize requested filesystem intents, persist an integrity-bound lease, and keep cleanup attached to the execution guard for success, failure, timeout, and cancellation. Model grant-active, process-exited, ACL-revoked, profile-deletion-pending, and cleaned states. Only after whole-tree exit and exact ACL revocation, invoke the native AppContainer profile deletion API for the lease-bound unique profile; verify its identity and absence before marking cleanup terminal. On interruption, recover the exact pending sequence from the durable lease. Preserve unrelated ACL entries and refuse deletion for any live, foreign, reused, or mismatched profile.</action>
  <verify>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-sandbox/src/backends/appcontainer.rs crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs crates/wcore-sandbox/src/backends/appcontainer/acl_lease/sha256.rs</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, and every changed path is declared by this task. This does not assert compilation or behavioral acceptance; those are established only by the plan-level clean-HEAD Hetzner gates.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

<task type="auto">
  <name>Task 2: Make lease storage race-safe and crash-recoverable</name>
  <files>crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs, crates/wcore-sandbox/src/backends/appcontainer/acl_lease/mutation_lock.rs, crates/wcore-sandbox/src/backends/appcontainer/acl_lease/tests.rs</files>
  <read_first>crates/wcore-sandbox/src/backends/appcontainer.rs, crates/wcore-sandbox/src/backends/appcontainer/acl_lease.rs, crates/wcore-sandbox/src/manifest.rs, crates/wcore-sandbox/tests/live_fs_acl.rs</read_first>
  <behavior>
    - Lease writes and rewrites are durable old-or-new operations across injected interruption points.
    - Symlinks, junctions, path replacement, foreign ownership, and opened-file identity swaps fail closed.
    - Windows path identity is derived from opened native handles and canonical comparison, not raw slash form, drive-letter case, UNC spelling, `\\?\` spelling, or caller text; equivalent spellings cannot create two lease authorities and reparse aliases cannot escape the owned root.
    - Cross-process ACL mutation is serialized by an owner-scoped lock that cannot be replaced by untrusted filesystem state.
    - Startup recovery resumes ACL revocation and profile deletion only for the exact lease-bound dead execution and retains nonterminal evidence when safe cleanup cannot be proven.
  </behavior>
  <action>Implement no-follow root and leaf handling, opened-file identity checks, synced atomic replacement, rewrite-temp recovery, and a global user-keyed Windows mutex for mutations. Normalize only through Windows-native opened-handle identity and canonical path comparison; never infer identity from slash replacement or string case alone. Add deterministic recovery for every ACL-revocation and profile-deletion boundary, with live/foreign/reused identity refusal. Add hostile unit tests for substitution, interruption, concurrent mutation, bounded profile cleanup, restart recovery, drive-letter case variants, slash variants, UNC and extended-length spellings, trailing-dot/space edge cases, and junction/reparse aliases.</action>
  <verify>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-sandbox/src/backends/appcontainer/acl_lease/storage.rs crates/wcore-sandbox/src/backends/appcontainer/acl_lease/mutation_lock.rs crates/wcore-sandbox/src/backends/appcontainer/acl_lease/tests.rs</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, and every changed path is declared by this task. This does not assert compilation or behavioral acceptance; those are established only by the plan-level clean-HEAD Hetzner gates.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

<task type="auto">
  <name>Task 3: Pin native Windows acceptance to the exact candidate</name>
  <files>crates/wcore-sandbox/tests/live_fs_acl.rs, .github/workflows/nightly-windows-soak.yml, scripts/f20-native-windows-proof.ps1</files>
  <read_first>crates/wcore-sandbox/tests/live_fs_acl.rs, .github/workflows/nightly-windows-soak.yml, crates/wcore-sandbox/Cargo.toml</read_first>
  <behavior>
    - Native tests prove isolation, denial, exact cleanup, cancellation cleanup, and concurrent uniqueness on Windows.
    - The helper aborts on wrong repository, dirty tree, wrong commit/tree, or missing explicit native-acceptance opt-in.
    - CI and the helper run the same named native test target and preserve the exact result.
  </behavior>
  <action>Expand `live_fs_acl` with concurrent and hostile identity cases. Add a PowerShell helper that accepts expected commit/tree inputs and only runs the native target after verifying them. Update the nightly Windows soak to use the same acceptance marker. Do not dispatch native infrastructure, publish, or make a native PASS claim in this plan; the exact-candidate run remains standard UAT under Sean's later authorization.</action>
  <verify>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-sandbox/tests/live_fs_acl.rs</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, and every changed path is declared by this task. This does not assert compilation or behavioral acceptance; those are established only by the plan-level clean-HEAD Hetzner gates.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>Untrusted workspace paths and concurrent child processes cross into Windows ACL authority and persisted lease state.</trust_boundaries>
<threats>SID or profile reuse, unbounded persistent-profile accumulation, stale process identity, lease substitution, symlink or junction traversal, partial rewrite, cross-process races, overbroad cleanup, and false native-platform claims.</threats>
<mitigations>Unique profiles, canonical intent validation, integrity-bound cleanup state, no-follow opened-handle validation, global mutation locking, exact SID revocation followed by exact profile deletion, restart recovery with foreign/live refusal, and commit/tree-pinned native UAT.</mitigations>
</threat_model>

<verification>
Each task first passes its pre-commit construction gate on the Mac and becomes a provisional GSD task commit. After every task commit exists and the checkout is clean, the following exact committed-HEAD Hetzner gates determine plan acceptance. A construction commit is not behavioral evidence:
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-02-unit "$(pwd -P)" test -p wcore-sandbox --lib</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-02-windows-check "$(pwd -P)" check --target x86_64-pc-windows-gnu -p wcore-sandbox --all-targets --all-features</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-02-clippy "$(pwd -P)" clippy -p wcore-sandbox --all-targets --all-features -- -D warnings</automated>
The cross-target check proves Windows code compiles but not native AppContainer behavior. Native Windows behavior is accepted only after standard phase UAT invokes `scripts/f20-native-windows-proof.ps1` on an explicitly authorized exact candidate.
</verification>

<success_criteria>
- Concurrent Windows children cannot share ACL authority.
- Stale, corrupt, or substituted lease state fails closed.
- ACL and AppContainer-profile cleanup is exact, bounded, and crash recoverable without deleting live or foreign identities.
- Cross-platform checks are green on Hetzner; native Windows remains an explicit later UAT result.
</success_criteria>

## Artifacts this phase produces

- Per-execution AppContainer ACL leases and mutation locking.
- Hostile native Windows ACL lifecycle tests.
- Exact-candidate PowerShell and CI proof entrypoints.

<output>
After completion, create `.planning/phases/20-transactional-delegated-mutation/20-02-SUMMARY.md` using the standard GSD summary template, separating Hetzner construction proof from native Windows UAT status.
</output>

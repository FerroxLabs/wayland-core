---
phase: 20-transactional-delegated-mutation
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.lock
  - crates/wcore-sandbox/Cargo.toml
  - crates/wcore-sandbox/src/backends/appcontainer.rs
  - crates/wcore-sandbox/src/backends/bwrap.rs
  - crates/wcore-sandbox/src/backends/docker.rs
  - crates/wcore-sandbox/src/backends/docker_tests.rs
  - crates/wcore-sandbox/src/backends/mod.rs
  - crates/wcore-sandbox/src/backends/process_tree.rs
  - crates/wcore-sandbox/src/directory_authority.rs
  - crates/wcore-sandbox/src/directory_authority_archive.rs
  - crates/wcore-sandbox/src/directory_authority_file.rs
  - crates/wcore-sandbox/src/directory_authority_tests.rs
  - crates/wcore-sandbox/src/directory_authority_windows.rs
  - crates/wcore-sandbox/src/directory_authority_windows_tests.rs
  - crates/wcore-sandbox/src/lib.rs
  - crates/wcore-swarm/Cargo.toml
  - crates/wcore-swarm/src/dispatch.rs
  - crates/wcore-swarm/src/heartbeat.rs
  - crates/wcore-swarm/src/lib.rs
  - crates/wcore-swarm/src/worktree.rs
  - crates/wcore-swarm/src/worktree_cleanup.rs
  - crates/wcore-swarm/src/worktree_manager.rs
  - crates/wcore-swarm/src/worktree_security.rs
  - crates/wcore-swarm/src/worktree_tests.rs
  - crates/wcore-swarm/tests/dispatch_smoke.rs
  - crates/wcore-swarm/tests/heartbeat_test.rs
  - crates/wcore-swarm/tests/swarm_worker_failure_reporting_e2e.rs
  - crates/wcore-swarm/tests/worker_runtime_limits.rs
  - crates/wcore-swarm/tests/workspace_authority.rs
  - crates/wcore-tools/src/bash.rs
  - crates/wcore-tools/src/workspace_policy.rs
  - crates/wcore-tools/tests/bash_sandbox_routing_test.rs
autonomous: true
requirements:
  - F20-01
  - F20-02
must_haves:
  truths:
    - "Every delegated operation arrives with the canonical RequestedChildWorkspace authority selected before execution; Bash is always mutation-capable and never reclassifies shell text."
    - "The public Swarm dispatch path, not only a private worktree helper, creates and carries the authoritative standalone checkout through worker launch, heartbeat observation, terminal teardown, cleanup, and reservation release."
    - "A mutating child receives a standalone Git checkout with its own Git directory, object store, refs, config, and reflogs; it shares no writable repository metadata with the parent."
    - "Directory and regular-file identities are retained as capabilities across validation and use; descendant reads, writes, renames, enumeration, cleanup, and command working directories remain bound to the retained filesystem objects rather than reopening mutable absolute paths."
    - "A child process and all descendants are hard-owned by the selected enforcing backend; there is no unsandboxed or unowned fallback, and terminal process-tree teardown completes before workspace cleanup or capacity-reservation release."
    - "Docker Desktop never receives the host workspace through an ambient bind mount: retained authority exports a bounded canonical archive into container-owned `/workspace`, and imports a bounded result transaction only after identity, reservation, and budget revalidation."
    - "Bash policy grants write access only to the standalone checkout and a transaction-private scratch directory; parent and sibling reads and writes, global temporary storage, parent Git metadata, inherited Wayland configuration/memory/credentials, symlink/reparse aliases, and other protected state remain denied."
    - "After cleanup, only bounded owner-observed terminal evidence remains available; cleanup cannot delete or disclose a same-path replacement, foreign workspace, parent state, or unbounded worker output."
  artifacts:
    - path: crates/wcore-sandbox/src/directory_authority.rs
      provides: "Platform-neutral retained-directory capability facade"
    - path: crates/wcore-sandbox/src/directory_authority_archive.rs
      provides: "Bounded retained-handle canonical archive export and transactional import"
    - path: crates/wcore-sandbox/src/directory_authority_windows.rs
      provides: "Windows handle-relative filesystem operations and 128-bit identity enforcement"
    - path: crates/wcore-sandbox/src/directory_authority_windows_tests.rs
      provides: "Native Windows retained-handle and reparse-point hostile proof"
    - path: crates/wcore-swarm/src/dispatch.rs
      provides: "Public Swarm lifecycle wiring and terminal ownership ordering"
    - path: crates/wcore-swarm/src/worktree.rs
      provides: "Owned standalone-checkout lifecycle and candidate identity"
    - path: crates/wcore-swarm/tests/dispatch_smoke.rs
      provides: "Public-dispatch retained-authority and bounded terminal-evidence proof"
    - path: crates/wcore-tools/tests/bash_sandbox_routing_test.rs
      provides: "Hostile routing, parent-read/write denial, and live containment coverage"
  key_links:
    - from: crates/wcore-swarm/src/dispatch.rs
      to: crates/wcore-swarm/src/worktree.rs
      via: "public dispatch retains the transaction workspace and its filesystem identities until terminal teardown"
      pattern: "TransactionWorkspace"
    - from: crates/wcore-swarm/src/dispatch.rs
      to: crates/wcore-sandbox/src/backends/process_tree.rs
      via: "owned worker process tree reaches a terminal state before cleanup and reservation release"
      pattern: "ProcessTree"
    - from: crates/wcore-tools/src/bash.rs
      to: crates/wcore-tools/src/workspace_policy.rs
      via: "enforce the already-selected isolated root through WorkspacePolicy and VFS containment"
      pattern: "WorkspacePolicy"
    - from: crates/wcore-sandbox/src/backends/docker.rs
      to: crates/wcore-sandbox/src/directory_authority.rs
      via: "export and import a bounded canonical workspace archive through retained authority rather than a host bind mount"
      pattern: "DirectoryAuthority"
---

<objective>
Build the isolation substrate that enforces already-selected delegated workspace authority and routes public Swarm mutation into an owned standalone checkout.

Purpose: Prevent an unmerged child or any descendant from reading or mutating the parent repository, configuration, metadata, or protected state, while keeping terminal cleanup and bounded evidence reliable.
Output: Retained cross-platform filesystem capabilities, public Swarm lifecycle ownership, independent-metadata checkout lifecycle, Bash enforcement, and hostile lifecycle proof.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@crates/wcore-types/src/spawner.rs
@crates/wcore-sandbox/src/directory_authority.rs
@crates/wcore-sandbox/src/backends/mod.rs
@crates/wcore-swarm/src/dispatch.rs
@crates/wcore-swarm/src/worktree.rs
@crates/wcore-tools/src/bash.rs
@crates/wcore-tools/src/workspace_policy.rs
</context>

<salvage>
Use `e200c0a178feb698af350312a80a33d5b04fc699` only for Bash containment material that consumes supplied authority, and successor `ccf824b9de2ff7efd64a2ea7cf720047f9add85d` only for owned worktree paths. Reject any second intent classifier, shell-text downgrade, linked-worktree, shared-object, hardlink/reflink, path-reopen authority, or process-global-CWD assumption. Reconcile each owned path and record adopted/adapted/rejected dispositions in the SUMMARY; do not cherry-pick either commit.
</salvage>

<execution_order>
This is one stock-GSD plan with strictly serial tasks: Task 1A, then 1B, then 1C, then 1D, then Task 2. A later task may consume only a committed predecessor task. No task runs in parallel, no task is accepted from a stale predecessor, and no alternative orchestration or evidence system is introduced.
</execution_order>

<scope_gate>
Before Task 1A, the executor requires a clean checkout, captures `F20_03_EXECUTION_BASE=$(git rev-parse HEAD)` and `F20_03_EXECUTION_BASE_TREE=$(git rev-parse HEAD^{tree})`, and carries both values unchanged through every serial task. The canonical scope is the ordered 32-entry `files_modified` array returned for plan `20-03` by stock GSD `phase-plan-index`; no handwritten second manifest is authoritative.

Before every task commit, stage every intended construction change, then run the following gate against that exact index snapshot. It requires zero unstaged and zero untracked files, rejects any staged/previously committed path outside the canonical manifest, and rejects every deletion, copy, or rename visible from the execution base through the index. Missing paths are permitted only while later serial tasks have not yet been constructed:

<automated>bash -lc 'set -euo pipefail; test -n "${F20_03_EXECUTION_BASE:?}"; test "$(git merge-base "$F20_03_EXECUTION_BASE" HEAD)" = "$F20_03_EXECUTION_BASE"; test -z "$(git diff --name-only)"; test -z "$(git ls-files --others --exclude-standard)"; canonical=$(node /Users/seandonahoe/.codex/gsd-core/bin/gsd-tools.cjs phase-plan-index 20 --raw | jq -r '\''.plans[] | select(.id == "20-03") | .files_modified[]'\'' | LC_ALL=C sort); actual=$(git diff --cached --name-only "$F20_03_EXECUTION_BASE" | LC_ALL=C sort -u); test -z "$(comm -13 &lt;(printf "%s\n" "$canonical") &lt;(printf "%s\n" "$actual"))"; test -z "$(git diff --cached --name-status -C --find-copies-harder "$F20_03_EXECUTION_BASE" | awk '\''$1 ~ /^[DRC]/ { print }'\'')"'</automated>

After Task 2 is committed, run the final scope gate against the committed delta. It requires a clean checkout, no untracked files, the original base commit/tree, no delete/copy/rename, and byte-for-byte equality after deterministic sorting between the canonical 32-path manifest and `base..HEAD` product-path union:

<automated>bash -lc 'set -euo pipefail; test -n "${F20_03_EXECUTION_BASE:?}"; test -n "${F20_03_EXECUTION_BASE_TREE:?}"; test "$(git rev-parse "$F20_03_EXECUTION_BASE^{tree}")" = "$F20_03_EXECUTION_BASE_TREE"; test -z "$(git status --porcelain)"; test "$(git merge-base "$F20_03_EXECUTION_BASE" HEAD)" = "$F20_03_EXECUTION_BASE"; test -z "$(git diff --name-status -C --find-copies-harder "$F20_03_EXECUTION_BASE..HEAD" | awk '\''$1 ~ /^[DRC]/ { print }'\'')"; canonical=$(node /Users/seandonahoe/.codex/gsd-core/bin/gsd-tools.cjs phase-plan-index 20 --raw | jq -r '\''.plans[] | select(.id == "20-03") | .files_modified[]'\'' | LC_ALL=C sort); actual=$(git diff --name-only "$F20_03_EXECUTION_BASE..HEAD" | LC_ALL=C sort); test "$actual" = "$canonical"; test "$(printf "%s\n" "$actual" | wc -l | tr -d " ")" = 32'</automated>
</scope_gate>

<tasks>

<task type="auto">
  <name>Task 1A: Establish retained filesystem capability substrate</name>
  <files>Cargo.lock, crates/wcore-sandbox/Cargo.toml, crates/wcore-sandbox/src/backends/appcontainer.rs, crates/wcore-sandbox/src/backends/bwrap.rs, crates/wcore-sandbox/src/backends/docker.rs, crates/wcore-sandbox/src/backends/mod.rs, crates/wcore-sandbox/src/backends/process_tree.rs, crates/wcore-sandbox/src/directory_authority.rs, crates/wcore-sandbox/src/directory_authority_file.rs, crates/wcore-sandbox/src/directory_authority_tests.rs, crates/wcore-sandbox/src/directory_authority_windows.rs, crates/wcore-sandbox/src/directory_authority_windows_tests.rs, crates/wcore-sandbox/src/lib.rs</files>
  <read_first>crates/wcore-sandbox/src/lib.rs, crates/wcore-sandbox/src/directory_authority.rs, crates/wcore-sandbox/src/directory_authority_windows.rs, crates/wcore-sandbox/src/directory_authority_windows_tests.rs, crates/wcore-sandbox/src/backends/mod.rs, crates/wcore-sandbox/src/backends/process_tree.rs, crates/wcore-sandbox/src/backends/appcontainer.rs, crates/wcore-sandbox/src/backends/bwrap.rs, crates/wcore-sandbox/src/backends/docker.rs</read_first>
  <behavior>
    - A directory authority opens and validates an exact directory once, retains the OS object identity, and performs descendant open, create, enumerate, atomic publish, rename, and deletion relative to that retained object.
    - Regular-file evidence is identity-checked and size-bounded before and after read; replacement, truncation, same-object rewrite, symlink, mount alias, or Windows reparse substitution fails closed.
    - Atomic child writes use a transaction-owned sibling temporary file, sync it, and publish it relative to the retained parent; global temporary paths and predictable external staging names are forbidden.
    - Unix uses descriptor-relative operations. Windows uses retained handles, 128-bit `FileIdInfo`, root-handle-relative native operations, reparse-point rejection, handle-relative rename/deletion, and tracked handle loans; pathname reopening and 64-bit file-index identity are not authority.
    - Sandbox command CWD binding and process-tree ownership consume the retained capability. A backend may claim hard containment only when its real spawn path owns descendants and cannot bypass that boundary.
    - The exact native test identities `directory_authority::tests::windows_handle_relative_rename_stays_bound_to_target_parent`, `directory_authority::tests::windows_handle_relative_delete_rejects_same_path_replacement`, `directory_authority::tests::windows_handle_relative_enumeration_rejects_reparse_children`, `directory_authority::tests::windows_command_cwd_stays_bound_to_renamed_directory_object`, and `directory_authority::tests::macos_retained_parent_rename_delete_enumeration_and_cwd_stay_handle_relative` exist under the applicable native target and contain no availability skip or foreign-platform substitute; Windows-only proof implementation lives in `directory_authority_windows_tests.rs` while retaining these fully qualified identities.
  </behavior>
  <action>Add the lowest-layer retained filesystem capability in `wcore-sandbox`, centralized behind platform-neutral methods and small Unix/Windows implementation modules. Make every security decision and later operation consume the same retained authority or a tracked derivative. Bind command working-directory selection to that authority, not process-global CWD or a pathname re-open. Preserve AppContainer, Bubblewrap, and Docker hard-containment claims only where their production spawn path proves descendant ownership; the generic process-tree layer must fail closed when hard ownership is unavailable. Keep files under the repository's 1000-line limit and update only the existing dependency/feature surfaces needed by this capability.</action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction only: all Task 1A paths are declared, formatting and diff hygiene pass, and no product behavior is accepted until the committed-head plan-level remote and native-platform gates pass.</acceptance_criteria>
  <done>A provisional Task 1A commit supplies retained filesystem and process-ownership primitives without claiming platform acceptance.</done>
</task>

<task type="auto">
  <name>Task 1B: Wire retained authority through public Swarm production lifecycle</name>
  <files>crates/wcore-swarm/Cargo.toml, crates/wcore-swarm/src/dispatch.rs, crates/wcore-swarm/src/heartbeat.rs, crates/wcore-swarm/src/lib.rs, crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/src/worktree_cleanup.rs, crates/wcore-swarm/src/worktree_manager.rs, crates/wcore-swarm/src/worktree_security.rs</files>
  <read_first>crates/wcore-swarm/src/lib.rs, crates/wcore-swarm/src/dispatch.rs, crates/wcore-swarm/src/heartbeat.rs, crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/src/worktree_cleanup.rs, crates/wcore-sandbox/src/directory_authority.rs, crates/wcore-tools/src/workspace_policy.rs</read_first>
  <behavior>
    - The public `Swarm` dispatch API acquires the exact parent repository authority, creates a unique standalone transaction workspace from its exact base, and passes the retained checkout and scratch authorities through the real worker launch path.
    - The checkout owns independent Git metadata and object bytes, has no alternates, linked-worktree state, writable parent remote, hardlinks, or reflinks, and cannot mutate parent or sibling Git state.
    - Capacity is reserved durably before materialization and released only after the worker and descendants are terminal and owner-bound cleanup has completed or recorded a retryable residual.
    - Heartbeats and terminal evidence are written and read through retained capabilities with bounded sizes. Malformed, substituted, symlinked, rewritten, missing, or unbounded evidence fails closed without disclosing host bytes.
    - `WorkspaceMonitor` is bounded defense-in-depth that may terminate observed growth; it is reactive accounting and never a claimed kernel-enforced hard quota or a substitute for preflight capacity authority.
    - Required live admission, enforcement, and teardown tests fail rather than return early when Bubblewrap, Docker, AppContainer, Job Object ownership, or the required native runtime is unavailable.
  </behavior>
  <action>Change the existing production `Swarm` dispatch and worktree manager so retained parent, workspace, checkout, scratch, reservation, heartbeat, and worker-process authorities are acquired once and carried end to end. Materialize the exact base's reachable object graph through a non-local, non-hardlink transfer into child-owned Git storage, remove aliases and writable parent linkage, and reject identity drift before spawn. On success, timeout, cancellation, output exhaustion, monitor termination, and spawn failure, first terminate and reap the owned worker tree, then perform owner-bound cleanup, then release the reservation. Preserve a bounded terminal receipt or explicit cleanup residual after deletion; never reopen an attacker-replaceable path to decide what to delete or report.</action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction only: the public dispatch path is wired through the Task 1A capability, all Task 1B paths are declared, and no lifecycle, Docker-transport, or quota claim is accepted until Tasks 1C/1D and plan-level proof pass.</acceptance_criteria>
  <done>A provisional Task 1B commit routes the real public Swarm lifecycle through retained ownership and terminal cleanup ordering.</done>
</task>

<task type="auto">
  <name>Task 1C: Prove public lifecycle, hostile identity changes, and bounded cleanup</name>
  <files>crates/wcore-swarm/src/worktree_tests.rs, crates/wcore-swarm/tests/dispatch_smoke.rs, crates/wcore-swarm/tests/heartbeat_test.rs, crates/wcore-swarm/tests/swarm_worker_failure_reporting_e2e.rs, crates/wcore-swarm/tests/worker_runtime_limits.rs, crates/wcore-swarm/tests/workspace_authority.rs</files>
  <read_first>crates/wcore-swarm/src/dispatch.rs, crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/src/worktree_tests.rs, crates/wcore-swarm/tests/dispatch_smoke.rs, crates/wcore-swarm/tests/heartbeat_test.rs, crates/wcore-swarm/tests/swarm_worker_failure_reporting_e2e.rs, crates/wcore-swarm/tests/worker_runtime_limits.rs, crates/wcore-swarm/tests/workspace_authority.rs</read_first>
  <behavior>
    - `Swarm::dispatch` proves parent and sibling source/Git state remain byte-identical while children execute in independent repositories.
    - Repository, checkout, scratch, reservation, heartbeat, and cleanup-entry replacement before and after validation cannot redirect execution, disclosure, evidence, deletion, or reservation release.
    - Timeout, cancellation, worker failure, output exhaustion, and reactive growth termination kill and reap descendants before workspace cleanup and capacity release; a successor is admitted only after that terminal order completes.
    - Interrupted or failed cleanup remains retryable, preserves the exact residual identity, and never deletes a same-path replacement or foreign transaction.
    - Cross-process capacity registration, malformed heartbeats, bounded output/evidence, and post-cleanup terminal reporting fail closed under the public API.
    - Linux exposes the exact non-skipping test identities `backends::bwrap::tests::required_live_bwrap_admission`, `backends::bwrap::tests::required_live_bwrap_retained_cwd_enforcement`, `backends::process_tree::linux_tests::required_live_descendant_teardown_before_workspace_cleanup`, and `backends::docker::tests::required_live_docker_admission_enforcement_and_teardown`.
    - Windows exposes the exact non-skipping ignored native-acceptance identity `backends::appcontainer::windows_impl::tests::required_live_job_teardown_precedes_workspace_cleanup` plus `required_live_windows_public_dispatch_bash_confines_parent_and_descendants` in `dispatch_smoke`; its runner opts in explicitly for the ignored primitive and may not omit the public composition case.
    - macOS exposes exact non-skipping identities `backends::process_tree::macos_tests::required_live_descendant_teardown_before_workspace_cleanup` and `required_live_macos_public_dispatch_bash_confines_parent_and_descendants` in `dispatch_smoke`; Docker Desktop transport proof is owned by serial Task 1D.
  </behavior>
  <action>Add hostile tests through public `Swarm::dispatch`, not only private helpers. Name and prove the production paths for retained-directory swap rejection, public parent/sibling isolation, malformed and symlinked heartbeat denial, repository replacement rejection, multi-process reservation contention, worker descendant cancellation, timeout, output exhaustion, workspace-growth defense-in-depth, cleanup retry, same-path replacement preservation, and bounded post-cleanup evidence. Add the exact platform admission/enforcement/teardown identities declared above, including native Windows and macOS public dispatch through real Bash containment rather than a primitive-only substitute. Every `required_live_*` test must assert the backend/runtime is present and fail on absence or execution failure; it must never print a skip and return success. Tests must assert teardown-before-cleanup-before-release ordering and may not describe `WorkspaceMonitor` as a hard quota.</action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction only: every named public lifecycle test is present in a declared test target and all Task 1C paths are declared; execution evidence comes only from the exact committed-head remote/native gates.</acceptance_criteria>
  <done>A provisional Task 1C commit contains hostile public-lifecycle proof for every terminal path and authority substitution class.</done>
</task>

<task type="auto">
  <name>Task 1D: Preserve retained workspace authority across Docker Desktop</name>
  <files>crates/wcore-sandbox/src/backends/docker.rs, crates/wcore-sandbox/src/backends/docker_tests.rs, crates/wcore-sandbox/src/directory_authority.rs, crates/wcore-sandbox/src/directory_authority_archive.rs, crates/wcore-sandbox/src/directory_authority_file.rs, crates/wcore-swarm/src/dispatch.rs, crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/tests/worker_runtime_limits.rs, crates/wcore-swarm/tests/workspace_authority.rs</files>
  <read_first>crates/wcore-sandbox/src/backends/docker.rs, crates/wcore-sandbox/src/backends/docker_tests.rs, crates/wcore-sandbox/src/directory_authority.rs, crates/wcore-sandbox/src/directory_authority_archive.rs, crates/wcore-sandbox/src/directory_authority_file.rs, crates/wcore-swarm/src/dispatch.rs, crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/tests/worker_runtime_limits.rs, crates/wcore-swarm/tests/workspace_authority.rs</read_first>
  <behavior>
    - Docker Desktop receives no ambient host bind mount and no pathname reopening authority. A bounded canonical archive is exported recursively from the retained checkout into a container-owned fixed `/workspace` only.
    - Export and result archives accept only normalized relative regular-file and directory entries. Symlinks, hardlinks, devices, FIFOs, sockets, absolute paths, empty/ambiguous components, `.`/`..`, duplicate normalized names, case-fold collisions, oversized entries, and aggregate-budget overflow fail closed before materialization or import.
    - The container result is bounded and fully validated before host mutation. Source identity and durable reservation are revalidated immediately before export, after container termination, and immediately before import; any drift, cancellation, timeout, missing ownership, or reservation mismatch rejects the result.
    - Creates, updates, and deletions are imported as one owner-bound transaction through retained authority with atomic publication and rollback/quarantine on partial failure. No host path is reopened and no unvalidated result becomes visible.
    - Capacity is remeasured against authoritative bytes before import and after publication. An over-budget result is rejected, container/scratch residue is cleaned after descendant teardown, and the original retained workspace remains unchanged.
    - Native macOS exposes exact non-skipping identities `backends::docker::tests::required_live_macos_retained_transport_rejects_path_replacement`, `backends::docker::tests::required_live_macos_retained_transport_roundtrips_output_and_deletion`, `required_live_macos_docker_cancellation_cleans_container_workspace` in `worker_runtime_limits`, and `required_live_macos_docker_rejects_over_budget_result` in `workspace_authority`.
  </behavior>
  <action>Replace Docker Desktop host-workspace bind mounting with a capability-preserving transfer layered on the retained authority from Task 1A and the public lifecycle from Task 1B. Put bounded retained-handle canonical export/import in the single-responsibility `directory_authority_archive.rs` module; keep Docker-specific orchestration in `docker.rs` and its tests in the dedicated `docker_tests.rs` module so both files remain below the repository's 1,000-line limit. Produce a deterministic bounded archive and manifest by walking the retained directory recursively, materialize it into container-owned `/workspace`, and export a bounded result transaction after the owned container/process tree terminates. Reject every non-regular or non-canonical entry and every identity, reservation, or budget drift. Import verified create/update/delete effects only through retained relative operations with atomic publication and rollback/quarantine semantics; cancellation or partial import must leave the original workspace authoritative and record only bounded retryable residue. Keep all implementation and tests inside the declared 32-path manifest.</action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction only: no ambient bind-mount path remains in the macOS Docker workspace route, all Task 1D paths are already in the canonical 32-path scope, and the four named native macOS tests exist without skip-on-unavailable behavior.</acceptance_criteria>
  <done>A provisional Task 1D commit carries retained workspace authority across Docker Desktop with bounded export/import and hostile native macOS proof.</done>
</task>

<task type="auto">
  <name>Task 2: Bind Bash containment to supplied isolated workspace authority</name>
  <files>crates/wcore-tools/src/bash.rs, crates/wcore-tools/src/workspace_policy.rs, crates/wcore-tools/tests/bash_sandbox_routing_test.rs, crates/wcore-swarm/tests/dispatch_smoke.rs</files>
  <read_first>crates/wcore-types/src/spawner.rs, crates/wcore-tools/src/bash.rs, crates/wcore-tools/src/workspace_policy.rs, crates/wcore-tools/src/registry.rs, crates/wcore-tools/src/vfs.rs, crates/wcore-tools/tests/bash_sandbox_routing_test.rs, crates/wcore-swarm/src/dispatch.rs, crates/wcore-swarm/tests/dispatch_smoke.rs, crates/wcore-config/src/shell.rs</read_first>
  <behavior>
    - Mutating Bash execution uses only the supplied retained checkout and scratch authorities; it cannot reopen or substitute either path between policy validation and spawn.
    - Its filesystem sandbox grants writes only inside those roots and denies reads and writes to parent/sibling workspaces, parent Git metadata, global temporary storage, inherited configuration/memory/credentials, symlinks, reparse points, aliases, and traversal.
    - `TMPDIR`, `TMP`, `TEMP`, and redirected tool caches resolve under the private scratch root, never host global temporary storage or candidate source.
    - Missing retained isolation, unavailable hard descendant confinement, parent-root aliasing, and identity drift are rejected before spawn; there is no permissive fallback.
    - Bash remains mutation-capable under `ForkOverrides::requested_workspace`; this task does not parse shell text, downgrade Bash to shared read-only, or redefine workspace intent.
    - Linux exposes the exact non-skipping integration identity `delegated_mutation_required_live_sandbox_confines_parent_and_descendants`; it fails if the required hard sandbox is unavailable or bypassed.
    - Native Windows and macOS expose the exact non-skipping `dispatch_smoke` identities `required_live_windows_public_dispatch_bash_confines_parent_and_descendants` and `required_live_macos_public_dispatch_bash_confines_parent_and_descendants`; each enters through public `Swarm::dispatch`, executes real Bash, proves allowed isolated-checkout mutation plus parent/sibling read-write denial, and fails when its native containment backend is unavailable.
  </behavior>
  <action>Consume the already-selected `RequestedChildWorkspace::IsolatedMutation` authority and the exact Task 1A/1B retained capabilities at the Bash execution boundary. Extend `WorkspacePolicy` only as needed for an owner-issued checkout and private scratch pair, bind those capabilities into the production filesystem sandbox and process CWD, and retain `ToolContext::with_workspace`, `ToolRegistry::set_workspace_policy`, `ToolRegistry::set_tool_vfs`, `SandboxedFs`, and `SecretDenyFs` for the remaining policy path. Add live integration cases for parent/global-temp/symlink writes and reads, scratch or checkout substitution, traversal, inherited secrets, unavailable confinement, and ambiguous commands. Add the exact native Windows/macOS public-dispatch composition tests in `dispatch_smoke.rs`; primitives or a direct BashTool call do not satisfy them. Stop before spawn if the selected backend cannot own all descendants or enforce the retained roots.</action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction only: formatting and diff hygiene pass, every Task 2 path is declared, and compilation/behavior remain plan-level committed-head claims.</acceptance_criteria>
  <done>A provisional Task 2 commit binds Bash to the retained transaction capabilities and includes hostile live routing tests.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>LLM-supplied commands and worker behavior cross into filesystem, Git workspace, sandbox-backend, process-lifecycle, heartbeat, cleanup, and reservation authority.</trust_boundaries>
<threats>Duplicate or downgraded intent authority, pathname time-of-check/time-of-use substitution, parent/sibling read or write, symlink/reparse traversal, linked-worktree or object-store aliasing, shared refs/config/objects/hooks, writable parent remotes, checkout collision, stale base identity, child/descendant escape, cleanup-before-reap, same-path replacement deletion, forged or unbounded heartbeat/evidence, and premature capacity release.</threats>
<mitigations>Reuse canonical default-to-mutating `RequestedChildWorkspace`; retain OS filesystem identities across validation and use; use handle/descriptor-relative operations and atomic sibling publication; materialize independent Git metadata and objects; require a hard descendant-owning backend; order terminal teardown before owner-bound cleanup and reservation release; bound every evidence read; and prove the public Swarm and Bash paths with hostile tests.</mitigations>
</threat_model>

<non_claims>
- `WorkspaceMonitor` and workspace-size polling are reactive defense-in-depth accounting, not a kernel-enforced hard quota and not proof that transient growth cannot exceed a threshold.
- Cross-compilation, source review, or `cargo check --target ...` is not Windows-native runtime, filesystem-authority, AppContainer, Job Object, cleanup, or lifecycle proof.
- macOS source review or Linux behavior is not macOS-native sandbox containment proof. Native macOS evidence must execute the real selected macOS backend on macOS.
- A Docker Desktop host bind mount is not retained-authority evidence and is forbidden for the delegated workspace, even when its pathname currently names the retained directory.
- A task commit, focused test, type check, or clean diff is not exact integrated acceptance.
- `20-03-SUMMARY.md` is generated only after the exact successor is serially integrated, the integrated commit is clean, every required gate passes at that exact commit, and independent review has zero unresolved findings at every severity.
</non_claims>

<verification>
Stock GSD must first recognize this plan as executable and incomplete. A stale summary is a hard failure:
<automated>node /Users/seandonahoe/.codex/gsd-core/bin/gsd-tools.cjs phase-plan-index 20 --raw | jq -e '(.plans[] | select(.id == "20-03") | .has_summary == false and .task_count == 5) and (.incomplete | index("20-03") != null)'</automated>

Tasks execute serially under stock GSD and produce provisional commits. After Task 2, the final scope gate above must pass and the checkout must remain at one clean exact committed HEAD/tree for every platform receipt. The Linux admission gate uses the committed-head Hetzner harness with all features and exact test identities. Each required live test asserts availability and execution; `--no-tests=fail` rejects a missing identity, and any absent Bubblewrap/Docker runtime is a test failure rather than a skip:
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-03-linux-sandbox "$(pwd -P)" nextest run --all-features -p wcore-sandbox --lib --no-tests=fail</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-03-linux-swarm-lib "$(pwd -P)" nextest run --all-features -p wcore-swarm --lib --no-tests=fail</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-03-linux-swarm-integrations "$(pwd -P)" nextest run --all-features -p wcore-swarm --test dispatch_smoke --test heartbeat_test --test swarm_worker_failure_reporting_e2e --test worker_runtime_limits --test workspace_authority --no-tests=fail</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-03-linux-bash-complete "$(pwd -P)" nextest run --all-features -p wcore-tools --test bash_sandbox_routing_test --no-tests=fail</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-03-linux-bwrap-admission "$(pwd -P)" nextest run --all-features -p wcore-sandbox --lib --no-tests=fail -E 'test(=backends::bwrap::tests::required_live_bwrap_admission)'</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-03-linux-bwrap-enforcement "$(pwd -P)" nextest run --all-features -p wcore-sandbox --lib --no-tests=fail -E 'test(=backends::bwrap::tests::required_live_bwrap_retained_cwd_enforcement)'</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-03-linux-process-teardown "$(pwd -P)" nextest run --all-features -p wcore-sandbox --lib --no-tests=fail -E 'test(=backends::process_tree::linux_tests::required_live_descendant_teardown_before_workspace_cleanup)'</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-03-linux-docker "$(pwd -P)" nextest run --all-features -p wcore-sandbox --lib --no-tests=fail -E 'test(=backends::docker::tests::required_live_docker_admission_enforcement_and_teardown)'</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-03-linux-public-dispatch "$(pwd -P)" nextest run --all-features -p wcore-swarm --test dispatch_smoke --no-tests=fail -E 'test(=public_dispatch_owns_git_authority_and_preserves_parent_and_sibling_state)'</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-03-linux-terminal-order "$(pwd -P)" nextest run --all-features -p wcore-swarm --test worker_runtime_limits --no-tests=fail -E 'test(=cancellation_kills_worker_descendant_and_releases_owned_workspace)'</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-03-linux-capacity "$(pwd -P)" nextest run --all-features -p wcore-swarm --test workspace_authority --no-tests=fail -E 'test(=independent_cli_processes_cannot_overbook_shared_capacity)'</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-03-linux-bash "$(pwd -P)" nextest run --all-features -p wcore-tools --test bash_sandbox_routing_test --no-tests=fail -E 'test(=delegated_mutation_required_live_sandbox_confines_parent_and_descendants)'</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-03-linux-clippy "$(pwd -P)" clippy -p wcore-sandbox -p wcore-swarm -p wcore-tools --all-targets --all-features -- -D warnings</automated>

The native Windows runner receives `F20_03_EXPECTED_COMMIT` and `F20_03_EXPECTED_TREE` from the sealed candidate. This exact fail-fast PowerShell command checks native OS, clean source, commit, and tree before and after every all-features exact test; explicitly runs ignored native acceptance; executes public `Swarm::dispatch` through real Bash containment; and blocks acceptance on a missing runner, missing AppContainer/Job runtime, missing test, reparse/identity failure, composition bypass, or teardown failure:
<automated>powershell -NoProfile -NonInteractive -Command '$ErrorActionPreference="Stop"; if ($env:OS -ne "Windows_NT") { throw "native Windows runner required" }; if (-not $env:F20_03_EXPECTED_COMMIT -or -not $env:F20_03_EXPECTED_TREE) { throw "sealed commit/tree required" }; function Assert-Candidate { if (@(git status --porcelain).Count -ne 0) { throw "dirty candidate" }; if ((git rev-parse HEAD).Trim() -cne $env:F20_03_EXPECTED_COMMIT) { throw "commit drift" }; if ((git rev-parse "HEAD^{tree}").Trim() -cne $env:F20_03_EXPECTED_TREE) { throw "tree drift" } }; function Run-Sandbox([string]$Name) { Assert-Candidate; cargo nextest run --all-features -p wcore-sandbox --lib --run-ignored all --no-tests=fail -E "test(=$Name)"; if ($LASTEXITCODE -ne 0) { throw "failed sandbox: $Name" }; Assert-Candidate }; function Run-Swarm([string]$Target,[string]$Name) { Assert-Candidate; cargo nextest run --all-features -p wcore-swarm --test $Target --no-tests=fail -E "test(=$Name)"; if ($LASTEXITCODE -ne 0) { throw "failed swarm: ${Target}::${Name}" }; Assert-Candidate }; Run-Sandbox "directory_authority::tests::windows_handle_relative_rename_stays_bound_to_target_parent"; Run-Sandbox "directory_authority::tests::windows_handle_relative_delete_rejects_same_path_replacement"; Run-Sandbox "directory_authority::tests::windows_handle_relative_enumeration_rejects_reparse_children"; Run-Sandbox "directory_authority::tests::windows_command_cwd_stays_bound_to_renamed_directory_object"; Run-Sandbox "backends::appcontainer::windows_impl::tests::required_live_job_teardown_precedes_workspace_cleanup"; Run-Swarm "dispatch_smoke" "required_live_windows_public_dispatch_bash_confines_parent_and_descendants"'</automated>

The native macOS runner receives the same sealed commit/tree values. This exact fail-fast command checks Darwin, clean source, commit, and tree before and after each all-features exact test; executes public `Swarm::dispatch` through real Bash containment; and blocks acceptance when the native runner, Docker runtime, test identity, retained-handle behavior, process-tree teardown, path-replacement defense, output/deletion roundtrip, cancellation cleanup, budget rejection, or composition containment is unavailable. The Docker tests must exercise the public lifecycle with container-owned `/workspace` transfer and reject an ambient host bind mount:
<automated>bash -lc 'set -euo pipefail; test "$(uname -s)" = Darwin; test -n "${F20_03_EXPECTED_COMMIT:?}"; test -n "${F20_03_EXPECTED_TREE:?}"; assert_candidate() { test -z "$(git status --porcelain)"; test "$(git rev-parse HEAD)" = "$F20_03_EXPECTED_COMMIT"; test "$(git rev-parse HEAD^{tree})" = "$F20_03_EXPECTED_TREE"; }; run_sandbox() { assert_candidate; cargo nextest run --all-features -p wcore-sandbox --lib --no-tests=fail -E "test(=$1)"; assert_candidate; }; run_swarm() { assert_candidate; cargo nextest run --all-features -p wcore-swarm --test "$1" --no-tests=fail -E "test(=$2)"; assert_candidate; }; run_sandbox directory_authority::tests::macos_retained_parent_rename_delete_enumeration_and_cwd_stay_handle_relative; run_sandbox backends::process_tree::macos_tests::required_live_descendant_teardown_before_workspace_cleanup; run_sandbox backends::docker::tests::required_live_docker_admission_enforcement_and_teardown; run_sandbox backends::docker::tests::required_live_macos_retained_transport_rejects_path_replacement; run_sandbox backends::docker::tests::required_live_macos_retained_transport_roundtrips_output_and_deletion; run_swarm dispatch_smoke required_live_macos_public_dispatch_bash_confines_parent_and_descendants; run_swarm worker_runtime_limits required_live_macos_docker_cancellation_cleans_container_workspace; run_swarm workspace_authority required_live_macos_docker_rejects_over_budget_result'</automated>

Final repository checks run only after Linux, native Windows, and native macOS gates pass against the same exact commit/tree:
<automated>cargo fmt --all -- --check</automated>
<automated>git diff --check</automated>
<automated>test -z "$(git status --porcelain)" &amp;&amp; git rev-parse HEAD &amp;&amp; git rev-parse HEAD^{tree}</automated>

Cross-compilation, source review, emulation, a foreign-platform unit suite, or an unavailable runner/backend never substitutes for native evidence. Missing native Windows or macOS infrastructure leaves 20-03 and F20-02 incomplete and forbids the SUMMARY.
</verification>

<success_criteria>
- Canonical `RequestedChildWorkspace` selection precedes execution, and Bash cannot redefine or downgrade it.
- Public Swarm dispatch carries retained filesystem, process, workspace, heartbeat, cleanup, and reservation authority end to end.
- Mutating work receives an owned standalone repository whose metadata and object bytes are independent from the parent.
- Docker Desktop carries that retained workspace through bounded canonical export/import into container-owned `/workspace`, never an ambient host bind mount.
- Parent/sibling workspace, Git metadata, configuration, credentials, and protected reads/writes remain denied under hostile identity replacement.
- Every terminal path reaps the worker tree before owner-bound cleanup and reservation release, while retaining only bounded terminal evidence.
- Focused sandbox, Swarm, public lifecycle, Bash routing, strict all-target/all-feature Clippy, and required native-platform gates pass at the exact integrated commit.
- F20-02 alone is traced to this plan and remains incomplete until the exact integrated Linux, native Windows, and native macOS gates all pass.
</success_criteria>

## Artifacts this phase produces

- Retained cross-platform directory and file authority for sandboxed child execution.
- Public Swarm transaction lifecycle with terminal process ownership and owner-bound cleanup.
- Bash enforcement of accepted isolated-workspace authority.
- Hostile public-lifecycle, workspace-authority, heartbeat, cleanup, and containment proof.

<output>
Only after exact serial integration and acceptance, create `.planning/phases/20-transactional-delegated-mutation/20-03-SUMMARY.md` using the standard GSD summary template with exact integrated commit, exact commands, native-platform boundaries, independent-review disposition, and adopted/adapted/rejected salvage evidence.
</output>

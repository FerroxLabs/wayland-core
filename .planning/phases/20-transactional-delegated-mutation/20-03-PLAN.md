---
phase: 20-transactional-delegated-mutation
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/wcore-swarm/src/worktree.rs
  - crates/wcore-swarm/src/worktree_tests.rs
  - crates/wcore-tools/src/bash.rs
  - crates/wcore-tools/src/workspace_policy.rs
  - crates/wcore-tools/tests/bash_sandbox_routing_test.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every delegated operation arrives with the canonical RequestedChildWorkspace authority selected before execution; Bash is always mutation-capable and never reclassifies shell text."
    - "A mutating child receives a standalone Git checkout with its own Git directory, object store, refs, config, and reflogs; it shares no writable repository metadata with the parent."
    - "Bash policy grants write access only to the standalone checkout and a transaction-private scratch directory; global temporary storage, the parent workspace, parent Git metadata, inherited Wayland configuration/memory/credentials, and other protected state remain denied."
  artifacts:
    - path: crates/wcore-swarm/src/worktree.rs
      provides: "Owned standalone-checkout lifecycle and candidate identity"
    - path: crates/wcore-tools/tests/bash_sandbox_routing_test.rs
      provides: "Hostile routing and parent-write denial coverage"
  key_links:
    - from: crates/wcore-tools/src/bash.rs
      to: crates/wcore-tools/src/workspace_policy.rs
      via: "enforce the already-selected isolated root through WorkspacePolicy and VFS containment"
      pattern: "WorkspacePolicy"
---

<objective>
Build the isolation substrate that enforces already-selected delegated workspace authority and routes mutation into an owned standalone checkout.

Purpose: Prevent an unmerged child from mutating the parent repository, configuration, metadata, or protected state.
Output: Independent-metadata checkout lifecycle and Bash enforcement tests consuming the canonical workspace-intent authority.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@crates/wcore-types/src/spawner.rs
@crates/wcore-swarm/src/worktree.rs
@crates/wcore-tools/src/bash.rs
@crates/wcore-tools/src/workspace_policy.rs
</context>

<salvage>
Use `e200c0a178feb698af350312a80a33d5b04fc699` only for Bash containment material that consumes supplied authority, and successor `ccf824b9de2ff7efd64a2ea7cf720047f9add85d` only for owned worktree paths. Reject any second intent classifier, shell-text downgrade, linked-worktree, shared-object, hardlink/reflink, or process-global-CWD assumption. Reconcile each owned path and record adopted/adapted/rejected dispositions in the SUMMARY; do not cherry-pick either commit.
</salvage>

<tasks>

<task type="auto">
  <name>Task 1: Create and own standalone mutation checkouts</name>
  <files>crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/src/worktree_tests.rs</files>
  <read_first>crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/src/worktree_tests.rs, crates/wcore-swarm/src/lib.rs, crates/wcore-tools/src/workspace_policy.rs</read_first>
  <behavior>
    - A mutating transaction materializes a unique standalone repository from an exact parent commit and records path, base, head, and tree identity.
    - The transaction workspace owns two disjoint canonical roots: the standalone Git checkout and a private scratch directory outside the candidate source tree; both are identity-bound and cleaned only by their owning transaction.
    - The child checkout has an independent Git common directory, object store, refs, config, and reflogs; it has no alternates, shared object directory, local-clone hardlinks/reflinks, writable parent remote, or linked-worktree metadata.
    - Materialization copies the exact base object graph into child-owned storage. Replacing or corrupting a child object cannot change parent object bytes, inode/file identity, reachability, or `git fsck` results.
    - Parent authority measures the reachable transfer closure, available disk, per-transaction allocation, and aggregate concurrent-workspace budget before materialization. Oversized or under-capacity requests fail before child launch, and partial transfers are removed only through the owning transaction's cleanup record.
    - Existing paths, dirty or ambiguous repositories, symlink escapes, identity drift, and any parent/common-directory alias fail closed.
    - Dropping or cleaning a workspace never removes the parent checkout or another transaction's checkout.
  </behavior>
  <action>Change the existing worktree manager's mutating path to materialize a standalone repository rather than `git worktree add`. Parent authority transfers only the exact base's reachable closure through a non-local/non-hardlink path (for example an initialized repository plus a content-addressed parent-built bundle/pack followed by child-owned unpack, or an equivalently proven copy) into an independent Git directory before child launch, removes any source remote and alternates, and exposes an identity-bound transaction workspace containing disjoint `checkout` and `scratch` roots. Before writing, measure the transfer closure, reserve it against explicit per-transaction and aggregate workspace budgets, check usable disk with safety margin, and fail closed before launch when capacity cannot be proven; release reservations and identity-bound partial transfers on every failure/restart path. The scratch root must be outside the candidate Git worktree while remaining beneath the transaction-owned workspace, so temporary output cannot dirty or enter candidate identity. Never use a default local clone, hardlink, reflink, or shared object database. Canonicalize repository, checkout, and scratch paths, assert exact ancestry and distinct Git common directories, and add hostile tests for oversized closure, exhausted aggregate budget, disk-shortfall injection, interrupted transfer cleanup, object-identity separation, child object corruption, child `git config`, `git update-ref`, reflog, hook and worktree-metadata writes, and global/foreign/symlink scratch substitution; prove parent object bytes, `git fsck`, metadata, refs, reservations, and foreign temporary paths remain unchanged.</action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, and every changed path is declared by this task. This does not assert compilation or behavioral acceptance; those are established only by the plan-level clean-HEAD Hetzner gates.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

<task type="auto">
  <name>Task 2: Bind Bash containment to supplied isolated workspace authority</name>
  <files>crates/wcore-tools/src/bash.rs, crates/wcore-tools/src/workspace_policy.rs, crates/wcore-tools/tests/bash_sandbox_routing_test.rs</files>
  <read_first>crates/wcore-types/src/spawner.rs, crates/wcore-tools/src/bash.rs, crates/wcore-tools/src/workspace_policy.rs, crates/wcore-tools/src/registry.rs, crates/wcore-tools/src/vfs.rs, crates/wcore-tools/tests/bash_sandbox_routing_test.rs, crates/wcore-config/src/shell.rs</read_first>
  <behavior>
    - Mutating Bash execution uses only the supplied isolated root.
    - Its filesystem sandbox grants writes only inside the checkout root and its identity-bound private scratch root; global OS temporary storage, absolute paths, inherited configuration paths, parent Git common-directory paths, symlinks, aliases, and traversal cannot reach parent or protected state.
    - `TMPDIR`, `TMP`, `TEMP`, and redirected tool caches resolve under the private scratch root, never the host global temporary directory or the candidate source tree.
    - Missing isolation, unavailable write confinement, parent-root aliasing, and path traversal are rejected before spawn.
    - Bash is always mutation-capable under `ForkOverrides::requested_workspace`; this task does not parse shell text, downgrade Bash to shared read-only, or redefine workspace intent.
  </behavior>
  <action>Consume the already-selected `RequestedChildWorkspace::IsolatedMutation` authority and apply it at the Bash execution boundary; never create another intent enum, allowlist, classifier, or shell-text parser. Extend `WorkspacePolicy` with a transaction-specific contained constructor that requires both the canonical checkout and the owner-issued private scratch root instead of calling the general `WorkspacePolicy::contained`, whose global `scratch_dirs()` grant is forbidden for delegated mutation. Bind only those two owned roots into the real filesystem sandbox write allowlist, redirect temporary/cache environment into scratch, and retain `ToolContext::with_workspace`, `ToolRegistry::set_workspace_policy`, `ToolRegistry::set_tool_vfs`, `SandboxedFs`, and `SecretDenyFs` for the remaining policy path. Validate checkout, scratch, working directory, and sandbox grants before spawn. Add integration cases that attempt global-temp writes, scratch substitution, absolute and relative parent writes, parent `.git` ref/config/hook/object writes, inherited Wayland config/memory/credential writes, traversal, symlink aliases, and ambiguous commands. If private scratch identity or the platform write boundary cannot be enforced, stop before spawn.</action>
  <verify>
    <automated>cargo fmt --all -- --check</automated>
    <automated>git diff --check</automated>
    <automated>git diff --name-only --relative</automated>
  </verify>
  <acceptance_criteria>Construction gate only: formatting and diff hygiene pass, and every changed path is declared by this task. This does not assert compilation or behavioral acceptance; those are established only by the plan-level clean-HEAD Hetzner gates.</acceptance_criteria>
  <done>The task implementation and its assigned tests are constructed as a provisional task commit; no compilation or behavioral claim is made until plan-level remote verification passes.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>LLM-supplied command intent crosses into filesystem and Git workspace authority.</trust_boundaries>
<threats>Duplicate or downgraded intent authority, ambiguous shell composition, parent-path aliasing, symlink traversal, linked-worktree common-directory mutation, local-clone hardlink/reflink aliasing, shared refs/config/objects/hooks, writable parent remotes, checkout collision, stale base identity, and cleanup of foreign checkouts.</threats>
<mitigations>Reuse canonical default-to-mutating `RequestedChildWorkspace` authority, never inspect Bash text to downgrade it, use non-local/non-hardlink object materialization, standalone Git metadata with no alternates or writable parent remote, a transaction-private scratch root with no global-temp grant, canonical path/common-directory/object-identity separation checks, exact Git identities, unique ownership, fail-closed routing, and hostile parent workspace/metadata/object-corruption tests.</mitigations>
</threat_model>

<verification>
Each task first passes its pre-commit construction gate on the Mac and becomes a provisional GSD task commit. After every task commit exists and the checkout is clean, the following exact committed-HEAD Hetzner gates determine plan acceptance. A construction commit is not behavioral evidence:
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-03-worktree "$(pwd -P)" test -p wcore-swarm --lib worktree</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-03-routing "$(pwd -P)" test -p wcore-tools --test bash_sandbox_routing_test</automated>
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-03-clippy "$(pwd -P)" clippy -p wcore-swarm -p wcore-tools --all-targets --all-features -- -D warnings</automated>
</verification>

<success_criteria>
- Canonical `RequestedChildWorkspace` selection precedes execution, and Bash cannot redefine or downgrade it.
- Mutating work always receives an owned standalone root with repository metadata independent from the parent.
- Parent workspace, Git metadata, configuration, sibling, and protected paths cannot be mutated through Bash or child Git commands.
- Focused tests and strict Clippy pass on Hetzner.
</success_criteria>

## Artifacts this phase produces

- Bash enforcement of accepted isolated-workspace authority.
- Transaction-bound standalone Git checkout lifecycle.
- Hostile Bash sandbox-routing proof.

<output>
After completion, create `.planning/phases/20-transactional-delegated-mutation/20-03-SUMMARY.md` using the standard GSD summary template with exact commits and remote test results.
</output>

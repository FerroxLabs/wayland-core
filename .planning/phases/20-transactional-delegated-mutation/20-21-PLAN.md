---
phase: 20-transactional-delegated-mutation
plan: "21"
type: execute
wave: 20
depends_on:
  - "20-20"
files_modified:
  - crates/wcore-sandbox/tests/hard_process_containment_windows.rs
  - scripts/f20-native-windows-proof.ps1
  - scripts/f20-native-uat-proof.mjs
autonomous: true
requirements:
  - REQ-native-r7
  - REQ-native-r8
must_haves:
  truths:
    - "The windows-job-object and windows-hard-process-containment proof targets map to REAL Windows Job-Object containment tests that exercise the actual mechanism the Windows sandbox uses (KILL_ON_JOB_CLOSE, active-process cap, breakaway denial, exit-code fidelity on zero and non-zero terminal paths, and descendant reaping with no residue) — never the Linux-only Bubblewrap tests they were wired to (REQ-native-r7)."
    - "A detached descendant of a contained Windows child is reaped when the Job Object closes, leaving no residual process; the test asserts no-residue, not just parent exit (REQ-native-r7)."
    - "A structural guard makes it impossible for a Windows proof target to silently map to a non-Windows test (and vice-versa): each native target's selected test is asserted to be cfg-gated for the target's own OS, or the harness fails closed (REQ-native-r8)."
  artifacts:
    - path: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      provides: "Newly authored real Windows Job-Object hard-containment acceptance tests"
    - path: scripts/f20-native-windows-proof.ps1
      provides: "Windows proof targets repointed to the real Job-Object tests plus the wrong-OS anti-drift guard"
    - path: scripts/f20-native-uat-proof.mjs
      provides: "WINDOWS_TARGETS map kept consistent with the repointed proof targets"
  key_links:
    - from: scripts/f20-native-windows-proof.ps1
      to: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      via: "windows-job-object / windows-hard-process-containment targets select the new Windows containment tests"
      pattern: "hard_process_containment_windows"
    - from: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      to: crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs
      via: "tests exercise the SharedJob / Job-Object lifecycle the child token construction sets up"
      pattern: "KILL_ON_JOB_CLOSE"
---

<objective>
Author the real Windows Job-Object hard-containment acceptance tests (none exist — the two "Windows" containment targets are wired to Linux-only Bubblewrap tests), repoint the two proof targets to them, and add a structural guard so a native proof target can never again map to a wrong-OS test.

Purpose: This is the systemic harness defect (BRIEF §4.F/§4.G). Without real Job-Object tests the Windows containment claim is aspirational; without the anti-drift guard the same class of defect recurs silently (REQ-native-r7/r8).
Output: A new `#[cfg(windows)]` containment test file, repointed proof targets, a consistent UAT-proof target map, and a wrong-OS guard — construction-proven on the Mac; real green on hardware at 20-25.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/inbox/native-uat-repair-BRIEF.md
@.planning/phases/20-transactional-delegated-mutation/20-20-SUMMARY.md
@crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs
@crates/wcore-sandbox/tests/hard_process_containment.rs
@scripts/f20-native-windows-proof.ps1
@scripts/f20-native-uat-proof.mjs
@.planning/codebase/TESTING.md
</context>

<execution_rules>
- Capture the plan base before edits: `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-21)"`.
- NO Cargo/clippy/nextest on the Mac. The new `#[cfg(windows)]` tests compile and run only on the self-hosted msvc runner at 20-25. Hetzner clippy checks Linux-visible surface; do not install `pwsh` here.
- The new tests live in a NEW file `hard_process_containment_windows.rs` so the existing Linux-only `hard_process_containment.rs` (Bubblewrap) is untouched. The Windows targets must select the NEW file's tests.
- The anti-drift guard is authored so each native proof target declares its expected OS and the harness verifies the selected test is cfg-gated for that OS; a mismatch fails the proof closed.
- Keep the ps1 `$targets` ids and the `f20-native-uat-proof.mjs` `WINDOWS_TARGETS` order/ids consistent — a repointed target id or test name in one must match the other.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Author the real Windows Job-Object hard-containment tests</name>
  <files>crates/wcore-sandbox/tests/hard_process_containment_windows.rs</files>
  <read_first>crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs, crates/wcore-sandbox/tests/hard_process_containment.rs, crates/wcore-sandbox/tests/live_fs_acl.rs</read_first>
  <behavior>
    - `contained_detached_child_exit` proves exit-code fidelity: a contained child that exits zero reports zero, and a child that exits non-zero reports that exact non-zero code, through the Job-Object-wrapped execution.
    - `job_close_reaps_detached_descendant_with_no_residue` proves KILL_ON_JOB_CLOSE: a child spawns a detached grandchild, the Job Object closes, and BOTH are terminated — the test asserts no residual process survives (queries the descendant's liveness after job close and requires it dead).
    - `active_process_cap_is_enforced` proves the Job Object's active-process limit rejects fan-out beyond the cap.
    - `breakaway_is_denied` proves a child cannot escape the Job Object via a breakaway attempt (breakaway denied by the job's limit flags).
    - `qualified_hard_containment_backend_preflight` proves the Windows backend self-reports hard descendant containment so the preflight other targets rely on is real on Windows.
    - All tests are `#[cfg(target_os = "windows")]` + `#[ignore]`, gated on `WAYLAND_SANDBOX_LIVE_WINDOWS` and `is_available()`, mirroring the native ACL tests.
  </behavior>
  <action>Create `hard_process_containment_windows.rs` exercising the REAL Windows Job-Object mechanism the sandbox already sets up in `windows_impl/process.rs` (SharedJob, kill-on-job-close, active-process cap, breakaway denial, UI restrictions). Author the five named tests so each drives an actual contained execution and asserts the specific containment property — exit-code fidelity on both zero and non-zero terminal paths, descendant reaping with no residue after job close (mirror the intent of the Linux bwrap descendant-no-residue test but via Job Objects), the active-process cap, breakaway denial, and the hard-containment preflight. Gate them `#[cfg(windows)]`/`#[ignore]`/live-env exactly like the ACL tests so `--run-ignored all` runs them on the real host and other hosts compile them out. Add no production source; if the Job-Object handle or preflight is not reachable from a test-visible seam, expose the minimum test-only accessor consistent with the existing native-test pattern and record it as a scope note.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-21)" crates/wcore-sandbox/tests/hard_process_containment_windows.rs</automated>
    <automated>vx rustfmt --edition 2021 --check crates/wcore-sandbox/tests/hard_process_containment_windows.rs</automated>
    <automated>for t in contained_detached_child_exit job_close_reaps_detached_descendant_with_no_residue active_process_cap_is_enforced breakaway_is_denied qualified_hard_containment_backend_preflight; do git -c grep.lineNumber=false grep -q "$t" -- crates/wcore-sandbox/tests/hard_process_containment_windows.rs || exit 1; done</automated>
  </verify>
  <acceptance_criteria>The five Job-Object containment tests exist as `#[cfg(windows)]`/`#[ignore]` native acceptance tests, each asserting a real containment property. Construction only; hardware green is proven at 20-25.</acceptance_criteria>
  <done>The Windows containment test file exists as a provisional task commit.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the Windows proof targets and add the wrong-OS anti-drift guard</name>
  <files>scripts/f20-native-windows-proof.ps1, scripts/f20-native-uat-proof.mjs</files>
  <read_first>scripts/f20-native-windows-proof.ps1, scripts/f20-native-uat-proof.mjs, crates/wcore-sandbox/tests/hard_process_containment_windows.rs, crates/wcore-sandbox/tests/hard_process_containment.rs</read_first>
  <behavior>
    - The `windows-job-object` target selects `hard_process_containment_windows` (e.g. `contained_detached_child_exit` / `job_close_reaps_detached_descendant_with_no_residue`) and the `windows-hard-process-containment` target selects `hard_process_containment_windows` (`qualified_hard_containment_backend_preflight`), never the Linux-only `hard_process_containment.rs`.
    - Each native proof target carries an expected-OS declaration; before running a target the harness asserts the selected test's source is cfg-gated for that OS (a Windows target's test file/module is `#[cfg(target_os = "windows")]` or `#[cfg(windows)]`; a macOS target's is `macos`). A target whose selected test is gated for a different OS fails the proof closed rather than reporting a false skip/pass.
    - `f20-native-uat-proof.mjs` WINDOWS_TARGETS ids/order stay identical to the ps1 `$targets` ids; the marker grammar is unchanged.
  </behavior>
  <action>In `f20-native-windows-proof.ps1`, repoint the two containment targets from `hard_process_containment.rs` (Bubblewrap) to the new `hard_process_containment_windows` tests, preserving the `--run-ignored all --no-tests=fail` selector discipline and the per-target/final marker emission. Add the anti-drift guard: give each target an expected `os` field and, before invoking cargo for a target, verify the target's test source is cfg-gated for that OS (grep the selected test file for the matching `cfg(target_os = ...)`/`cfg(windows)` attribute) — fail closed on mismatch, absent gate, or a Windows target pointing at a `#![cfg(...)]` non-Windows file. Mirror the same guard shape in `f20-native-macos-proof.sh` semantics via the shared expectation so macOS targets are equally protected (the macOS-side application is completed in 20-22). Update `f20-native-uat-proof.mjs` WINDOWS_TARGETS so its ids/order match the repointed ps1 targets exactly. Change no production source and no test.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-21)" crates/wcore-sandbox/tests/hard_process_containment_windows.rs scripts/f20-native-windows-proof.ps1 scripts/f20-native-uat-proof.mjs</automated>
    <automated>node --check scripts/f20-native-uat-proof.mjs</automated>
    <automated>git -c grep.lineNumber=false grep -q 'hard_process_containment_windows' -- scripts/f20-native-windows-proof.ps1 && ! git -c grep.lineNumber=false grep -nE "test\('?hard_process_containment'?\b" -- scripts/f20-native-windows-proof.ps1</automated>
    <automated>node --test scripts/f20-native-uat-proof.test.mjs</automated>
  </verify>
  <acceptance_criteria>Both containment targets select the Windows Job-Object tests; the anti-drift guard fails closed when a native target maps to a wrong-OS test; the UAT-proof target map is consistent; the existing UAT-proof node tests still pass. Construction only.</acceptance_criteria>
  <done>The repointed targets and anti-drift guard exist as a provisional task commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proof target ← selected test | A native proof target claims a platform property; if it selects a wrong-OS or absent test it can report a false PASS/skip |
| contained child ← Job Object | Descendants must be reaped on job close; breakaway and fan-out must be denied |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-21-01 | Spoofing | windows containment proof targets | critical | mitigate | Repoint to real Job-Object tests; `--no-tests=fail` + the anti-drift guard make a wrong-OS or missing test fail closed, so a false containment PASS cannot be fabricated. |
| T-20-21-02 | Elevation of Privilege | Job Object breakaway / fan-out | high | mitigate | `breakaway_is_denied` and `active_process_cap_is_enforced` prove the job's limit flags hold on hardware at 20-25. |
| T-20-21-03 | Denial of Service | detached descendant residue | high | mitigate | `job_close_reaps_detached_descendant_with_no_residue` asserts no residual process after job close. |
</threat_model>

<verification>
After both task commits exist and the checkout is clean, the committed-HEAD Hetzner gate confirms Linux build/lint (the Windows tests are cfg-excluded there) and the node harness self-tests pass:
<automated>/Users/seandonahoe/.ratchet/harness/remote-cargo.sh f20-21-clippy "$(pwd -P)" clippy -p wcore-sandbox --all-targets --all-features -- -D warnings</automated>
Record exact `source_sha`/tree, receipts, and changed paths in `20-21-SUMMARY.md`. The summary states the Job-Object containment green is proven ONLY on the self-hosted msvc runner at 20-25 and completes no requirement.
</verification>

<success_criteria>
- Real Windows Job-Object containment tests exist and the two containment targets select them (r7).
- The wrong-OS anti-drift guard fails closed on a native target mapped to a wrong-OS test (r8).
- The UAT-proof target map is consistent and its node self-tests pass; Hetzner clippy is clean.
- The summary defers containment green to 20-25 and completes no requirement.
</success_criteria>

## Artifacts this phase produces
- Newly authored Windows Job-Object containment tests, repointed proof targets, and the wrong-OS anti-drift guard.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-21-SUMMARY.md` using the standard GSD summary template. Record exact source identity, receipts, changed paths, and the deferral of containment green to 20-25. Complete no Phase 20 requirement.
</output>

---
phase: 20-transactional-delegated-mutation
plan: "09"
type: execute
wave: 6
depends_on:
  - "20-06"
files_modified:
  - .planning/phases/20-transactional-delegated-mutation/20-06A-INDEPENDENT-REVIEW.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "A fresh executor that did not author 20-06 reviews the exact clean source SHA recorded by 20-06."
    - "The source summary and optional stock-GSD tracking updates form a linear metadata-only chain from the source task commit; the review commit is a sole-parent child of that review base and changes only the review record."
    - "Every finding at every severity is recorded; only a zero-finding PASS qualifies 06A."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-06A-INDEPENDENT-REVIEW.md
      provides: "Exact-SHA non-author 06A interface review"
---

<objective>
Independently review the exact 06A source packet before any containment source work begins.

Purpose: make non-author review a real stock-GSD plan boundary rather than an assertion inside the builder session.
Output: a review-only descendant commit and an externally derived qualification pair.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-06-PLAN.md
@.planning/phases/20-transactional-delegated-mutation/20-06-SUMMARY.md
@crates/wcore-swarm/src/worktree.rs
@crates/wcore-swarm/src/worktree/candidate.rs
@crates/wcore-swarm/src/worktree_tests.rs
</context>

<admission>
- Require a clean isolated checkout whose HEAD is the end of the ordinary stock-GSD 20-06 metadata chain. Starting at the `source_sha` named inside `20-06-SUMMARY.md`, every intermediate commit must be sole-parent, merge-free, and change only `20-06-SUMMARY.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, or `.planning/REQUIREMENTS.md`; the summary must change exactly once.
- Before editing, capture the current commit/tree separately with `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-task-base-20-09)"` and `bash .planning/scripts/verify-task-scope.sh --capture "$(git rev-parse --git-path gsd-review-base-20-09)"`. Derive the exact source SHA from the summary, verify its tree, then exclusively capture both with `node .planning/scripts/task-base-authority.mjs capture "$(git rev-parse --git-path gsd-reviewed-source-20-09)" "$source" "$(git rev-parse "$source^{tree}")"`.
- The executor for this plan must be a fresh stock-GSD executor distinct from the 20-06 executor. If executor identity cannot establish that separation, fail closed.
</admission>

<tasks>

<task type="auto">
  <name>06A-R: Prosecute CandidateSeal authority and interface sufficiency</name>
  <files>.planning/phases/20-transactional-delegated-mutation/20-06A-INDEPENDENT-REVIEW.md</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-06-PLAN.md, .planning/phases/20-transactional-delegated-mutation/20-06-SUMMARY.md, crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/src/worktree/candidate.rs, crates/wcore-swarm/src/worktree_tests.rs, crates/wcore-types/src/child_transaction.rs</read_first>
  <behavior>
    - Review producer compatibility with accepted 20-03, future 06C consumer sufficiency, opacity, non-reconstructibility, exact revalidation, scope, and failure semantics.
    - Treat every BLOCKER, CRITICAL, HIGH, MEDIUM, and LOW finding as disqualifying. Do not repair production source in this plan.
    - Write one schema-validated `wayland-core.phase20-independent-review.v1` JSON object with distinct source-executor/reviewer identities, exact source SHA/tree, PASS for the `f20-09` checks, zero findings at every severity, and executed command evidence. Never embed the review commit's own SHA.
  </behavior>
  <action>Inspect the complete exact source tree and tests without changing source. Write the 06A review record as schema-validated JSON matching `.planning/scripts/verify-review-result.mjs` profile `f20-09`. PASS only with zero findings at every severity; otherwise record FAIL and stop the dependency chain for a new 20-06 source successor.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-09)" .planning/phases/20-transactional-delegated-mutation/20-06A-INDEPENDENT-REVIEW.md</automated>
    <automated>base="$(node .planning/scripts/task-base-authority.mjs read "$(git rev-parse --git-path gsd-task-base-20-09)" | sed -n '1p')"; git diff --check "$base"..HEAD && git diff --cached --check && git diff --check</automated>
  </verify>
  <acceptance_criteria>The review is non-author, exact-source, complete at every severity, evidence-backed, and changes only the review record. PASS requires zero findings; FAIL creates no qualification.</acceptance_criteria>
  <done>The review record is committed as the only changed path, ready for post-task exact-pair verification.</done>
</task>

</tasks>

<verification>
Before writing the summary, derive `source` and `review_base` from the first lines returned by one no-follow read of their respective Git-private two-line authority objects, and derive `review=$(git rev-parse HEAD)`, then run `bash .planning/scripts/verify-review-pair.sh "$source" "$review_base" "$review" .planning/phases/20-transactional-delegated-mutation/20-06-SUMMARY.md .planning/phases/20-transactional-delegated-mutation/20-06A-INDEPENDENT-REVIEW.md crates/wcore-swarm/src/worktree.rs crates/wcore-swarm/src/worktree/candidate.rs crates/wcore-swarm/src/worktree_tests.rs` followed by `node .planning/scripts/verify-review-result.mjs "$review" .planning/phases/20-transactional-delegated-mutation/20-06A-INDEPENDENT-REVIEW.md "$source" "$(git rev-parse "${source}^{tree}")" f20-09`. Require both to pass. Record the externally derived `(source_sha, source_tree, review_base_sha, review_sha)` tuple in `20-09-SUMMARY.md`; the later 20-09 summary commit is metadata and is not part of that tuple. No Cargo runs on macOS.
</verification>

<success_criteria>
- A distinct non-author executor reviewed the exact 06A source SHA.
- The permitted summary metadata and review-only commits form the exact two-step chain from source, and source blobs are unchanged throughout.
- Only a zero-finding PASS marks 06A construction-qualified and admits 20-10.
</success_criteria>

<output>
After completion, create `.planning/phases/20-transactional-delegated-mutation/20-09-SUMMARY.md` containing executor identity, `source_sha`, derived `review_sha`, exact changed path, disposition, findings, and admission or rejection of 20-10.
</output>

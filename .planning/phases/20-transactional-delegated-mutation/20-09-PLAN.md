---
phase: 20-transactional-delegated-mutation
plan: "09"
type: execute
wave: 4
depends_on:
  - "20-06"
files_modified:
  - .planning/phases/20-transactional-delegated-mutation/20-06-INTERFACE-REVIEWS.md
autonomous: true
requirements:
  - F20-GATE-02
must_haves:
  truths:
    - "A fresh executor that did not author 20-06 reviews the exact clean source SHA recorded by 20-06."
    - "The source summary is the sole permitted metadata-only child of the source task commit; the review commit is a sole-parent child of that review base and changes only the review record."
    - "Every finding at every severity is recorded; only a zero-finding PASS qualifies 06A."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-06-INTERFACE-REVIEWS.md
      provides: "Exact-SHA non-author 06A interface review"
---

<objective>
Independently review the exact 06A source packet before any containment source work begins.

Purpose: make non-author review a real stock-GSD plan boundary rather than an assertion inside the builder session.
Output: a review-only descendant commit and an externally derived qualification pair.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-06-PLAN.md
@.planning/phases/20-transactional-delegated-mutation/20-06-SUMMARY.md
@crates/wcore-swarm/src/worktree.rs
@crates/wcore-swarm/src/worktree/candidate.rs
@crates/wcore-swarm/src/worktree_tests.rs
</context>

<admission>
- Require a clean isolated checkout whose HEAD is the ordinary `20-06-SUMMARY.md` metadata commit. The `source_sha` named inside that summary must be its sole parent, and the source-to-HEAD delta must be exactly `20-06-SUMMARY.md`.
- Before editing, write current HEAD to `$(git rev-parse --git-path gsd-task-base-20-09)` and `$(git rev-parse --git-path gsd-review-base-20-09)`, and write the summary's earlier source task SHA to `$(git rev-parse --git-path gsd-reviewed-source-20-09)`.
- The executor for this plan must be a fresh stock-GSD executor distinct from the 20-06 executor. If executor identity cannot establish that separation, fail closed.
</admission>

<tasks>

<task type="auto">
  <name>06A-R: Prosecute CandidateSeal authority and interface sufficiency</name>
  <files>.planning/phases/20-transactional-delegated-mutation/20-06-INTERFACE-REVIEWS.md</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-06-PLAN.md, .planning/phases/20-transactional-delegated-mutation/20-06-SUMMARY.md, crates/wcore-swarm/src/worktree.rs, crates/wcore-swarm/src/worktree/candidate.rs, crates/wcore-swarm/src/worktree_tests.rs, crates/wcore-types/src/child_transaction.rs</read_first>
  <behavior>
    - Review producer compatibility with accepted 20-03, future 06C consumer sufficiency, opacity, non-reconstructibility, exact revalidation, scope, and failure semantics.
    - Treat every BLOCKER, CRITICAL, HIGH, MEDIUM, and LOW finding as disqualifying. Do not repair production source in this plan.
    - Append reviewer identity, exact source SHA, checklist, commands/evidence, findings, and PASS/FAIL only. Never attempt to embed the review commit's own SHA.
  </behavior>
  <action>Inspect the complete exact source tree and tests without changing source. Append one 06A section to the review record. PASS only with zero findings at every severity; otherwise record FAIL and stop the dependency chain for a new 20-06 source successor.</action>
  <verify>
    <automated>bash .planning/scripts/verify-task-scope.sh "$(git rev-parse --git-path gsd-task-base-20-09)" .planning/phases/20-transactional-delegated-mutation/20-06-INTERFACE-REVIEWS.md</automated>
    <automated>base="$(cat "$(git rev-parse --git-path gsd-task-base-20-09)")"; git diff --check "$base"..HEAD && git diff --cached --check && git diff --check</automated>
  </verify>
  <acceptance_criteria>The review is non-author, exact-source, complete at every severity, evidence-backed, and changes only the review record. PASS requires zero findings; FAIL creates no qualification.</acceptance_criteria>
  <done>The review record is committed as the only changed path, ready for post-task exact-pair verification.</done>
</task>

</tasks>

<verification>
Before writing the summary, derive `source`, `review_base`, and `review=$(git rev-parse HEAD)` from the recorded files, then run `bash .planning/scripts/verify-review-pair.sh "$source" "$review_base" "$review" .planning/phases/20-transactional-delegated-mutation/20-06-SUMMARY.md .planning/phases/20-transactional-delegated-mutation/20-06-INTERFACE-REVIEWS.md crates/wcore-swarm/src/worktree.rs crates/wcore-swarm/src/worktree/candidate.rs crates/wcore-swarm/src/worktree_tests.rs`. Require zero-finding PASS. Record the externally derived `(source_sha, review_base_sha, review_sha)` tuple in `20-09-SUMMARY.md`; the later 20-09 summary commit is metadata and is not part of that tuple. No Cargo runs on macOS.
</verification>

<success_criteria>
- A distinct non-author executor reviewed the exact 06A source SHA.
- The permitted summary metadata and review-only commits form the exact two-step chain from source, and source blobs are unchanged throughout.
- Only a zero-finding PASS marks 06A construction-qualified and admits 20-10.
</success_criteria>

<output>
After completion, create `.planning/phases/20-transactional-delegated-mutation/20-09-SUMMARY.md` containing executor identity, `source_sha`, derived `review_sha`, exact changed path, disposition, findings, and admission or rejection of 20-10.
</output>

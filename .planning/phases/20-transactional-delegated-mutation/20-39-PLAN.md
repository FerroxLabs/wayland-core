---
phase: 20-transactional-delegated-mutation
plan: "39"
type: execute
wave: 38
depends_on:
  - "20-38"
files_modified: []
autonomous: false
requirements:
  - REQ-native-r14
  - REQ-native-r5
  - REQ-native-r7
  - REQ-native-r9
  - REQ-native-r11
  - REQ-native-r4
  - REQ-native-r3
must_haves:
  truths:
    - "One FRESH Sean-authorized native-proof dispatch runs the exact sealed second-repaired-successor on real hardware: Windows on the AppContainer-capable self-hosted msvc runner (SEANDESKTOP) and macOS on a FRESHLY re-registered ephemeral Apple-silicon runner; native proof is never claimed from cross-compilation or source inspection (CONTEXT D6/D7, REQ-native-r11). BOTH prior native-proof-dispatch authorizations (95c81ec6 AND 17412cf2) are spent/void; this digest requires its own fresh authorization."
    - "Before the scarce run is spent, the sanctioned macOS compile re-confirmation `cargo check -p wcore-sandbox --features live-docker --tests` is green on THIS Mac and the Windows leg's inherited `safe.directory` guard is still present in the candidate workflow, so setup clears and the proof reaches the acceptance tests (REQ-native-r11/r4). The pre-dispatch guard CANNOT compile the `#![cfg(windows)]` exit-code fix — its compile AND real-hardware exit-code behavior are proven by the re-dispatch itself (CONTEXT D5/D7)."
    - "The native run re-proves on hardware the two now-fixed 20-32 RED causes: EVERY Windows target returns the granted-read/parent-exit exit 0 (windows-retained-handle + windows-appcontainer-acl via the fixed live_fs_acl helper AND windows-job-object + windows-hard-process-containment via the fixed Job-Object parent-exit sites, no onion-peel to a later target), and EVERY macOS docker target finds alpine:3.19 (the harness pre-pull) so all 8 macOS targets run genuinely — while the previously-fixed causes still hold (macOS compiles/no E0599, macos-retained-directory green; Windows safe.directory setup clears) (REQ-native-r14/r5/r7/r9/r3)."
    - "All Windows native targets are green on the self-hosted msvc runner and all macOS native targets are green on the ephemeral runner, with ordered per-target and final commit/tree/nonce markers bound to the exact sealed successor SHA; the UAT evidence ref is retained (deletion not authorized) (REQ-native-r4)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-39-SUMMARY.md
      provides: "Retained dual-OS native evidence bound to the exact sealed second-repaired-successor for the fresh review (20-40)"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-37-SUMMARY.md
      to: .planning/phases/20-transactional-delegated-mutation/20-39-SUMMARY.md
      via: "the sealed successor SHA is published to a temporary UAT ref + dispatch branch and dispatched to native runners"
      pattern: "source_sha"
---

<objective>
Produce real native evidence for the sealed second-repaired-successor: one FRESH Sean-authorized native-proof dispatch that runs Windows (self-hosted msvc) and macOS (freshly re-registered ephemeral) acceptance on hardware, re-proving the two now-fixed 20-32 RED causes (Windows granted-read/parent-exit exit-0 across every Windows target; macOS docker targets finding alpine:3.19) plus the still-holding AppContainer/Job-Object/rename findings, so the fresh 20-40 review can review native evidence rather than defer it.

Purpose: The audit requires the fresh review NOT to defer native (R13/R12); that is only possible if native evidence exists at review time. This is the re-dispatch of the native-proof gate for the second repaired successor — Sean-only (D6), with a fresh digest-specific authorization (BOTH the 95c81ec6 and 17412cf2 authorizations are spent/void), distinct from the terminal exact-tuple authorization at 20-42.
Output: Retained, candidate-bound dual-OS native evidence, or an explicit incomplete disposition; no requirement completion here.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
@/Users/seandonahoe/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/phases/20-transactional-delegated-mutation/20-32-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-36-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-37-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-38-SUMMARY.md
@scripts/f20-native-windows-proof.ps1
@scripts/f20-native-macos-proof.sh
@scripts/f20-native-uat-proof.mjs
@.github/workflows/nightly-windows-soak.yml
</context>

<admission>
- Require the exact sealed successor SHA/tree from `20-37-SUMMARY.md`, the zero-finding `f20-native-crossaudit` from `20-38-SUMMARY.md`, and the 20-36 macOS compile-check + Hetzner clippy/nextest receipts; any drift, non-PASS cross-audit, or unclean checkout stops before the checkpoint.
- Pre-dispatch guard (before the checkpoint): re-run the one sanctioned Mac compile check `cargo check -p wcore-sandbox --features live-docker --tests` against the sealed successor and require it green, and confirm the candidate workflow still carries the `safe.directory` guard in the `f20-windows-candidate` job before its tree-resolve. A red compile check or missing guard stops here. NOTE: this guard does NOT compile the `#![cfg(windows)]` exit-code fix (`live_fs_acl.rs`/`hard_process_containment_windows.rs` compile to nothing off Windows); their compile AND real-hardware exit-code behavior are proven by THIS re-dispatch — the plan is explicit that the Windows fix's hardware proof is the re-dispatch itself (CONTEXT D5/D7).
- The native-proof dispatch is Sean-only and digest-specific. No prior general approval, silence, the spent 95c81ec6 or 17412cf2 authorizations, or approval for another candidate is authority. Only Sean's explicit native-proof-dispatch authorization for THIS sealed successor digest may run Task 2.
- Dispatch mechanics (learned at 20-25/20-32 — do not rediscover): `workflow_dispatch --ref` resolves only branches/tags, NOT the `refs/f20-native-uat/*` evidence namespace (that returns HTTP 422 "No ref found" and creates no run). Publish the sealed SHA to its `refs/f20-native-uat/<sha>` evidence ref AND create a dispatch BRANCH `f20-native-uat-<full-sha>` at the same sealed SHA; dispatch on the branch so `github.sha == sealed SHA`. Both point at the same immutable commit.
- Re-entry after authorization uses the same durable dispatch/run state: it may capture an already-dispatched exact attempt but must never dispatch or retry a second run.
- The Windows leg MUST land on `[self-hosted, Windows, X64, msvc]` (AppContainer-capable); the macOS leg on the FRESHLY re-registered pinned ephemeral runner with a live Docker Desktop daemon (the harness pre-pulls `alpine:3.19`, but the daemon must be up per the `docker info` gate). A hosted `windows-2022` landing is a failure, not a fallback.
- Switch GitHub auth to `FerroxLabs` immediately before any gh/dispatch operation.
</admission>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Sean authorizes the FRESH native-proof dispatch for the sealed second-repaired-successor</name>
  <files>None; this checkpoint runs no command and changes no file or external state.</files>
  <decision>Authorize one native-proof dispatch of the exact sealed second-repaired-successor to its temporary UAT ref + dispatch branch and one native-proof workflow run (Windows self-hosted msvc + macOS freshly-registered ephemeral), or provide no authorization and leave the native path unproven.</decision>
  <context>20-37 sealed the second repaired successor (Linux 11509/0/48 + all-features build), 20-38 cross-audited it CLEAN with native deferred, and the Mac compile check is green + the Windows safe.directory guard is in place. This gate produces real native evidence re-proving the two fixed 20-32 RED causes (Windows exit-0 across every target; macOS docker image present) so the fresh review (20-40) need not defer native. BOTH prior native-proof-dispatch authorizations (95c81ec6 and 17412cf2) are spent/void; this is a fresh digest-specific authorization, separate from the terminal exact-tuple authorization at 20-42.</context>
  <options>
    <option id="authorize"><name>Authorize second-repaired-successor native-proof dispatch</name><pros>Produces real Windows+macOS evidence re-proving the two fixed 20-32 RED causes (and the still-holding earlier fixes) so the fresh review need not defer native.</pros><cons>Publishes one temporary UAT ref + dispatch branch and consumes the self-hosted msvc + freshly-registered ephemeral macOS runners.</cons></option>
  </options>
  <action>Run no command. Display the exact sealed successor commit/tree, the target runner labels (self-hosted msvc + ephemeral macOS image label), the request nonce/digest, and confirmation that the Mac compile check is green and the Windows safe.directory guard is present, then stop. Continue only when Sean explicitly authorizes THIS sealed successor digest's native-proof dispatch using the displayed digest.</action>
  <verify>The response is Sean's explicit native-proof-dispatch authorization matching the displayed sealed-successor digest. Bare approval, silence, the spent 95c81ec6/17412cf2 authorizations, or an approval for a different candidate fails this checkpoint and ends execution.</verify>
  <done>The explicit fresh native-proof-dispatch authorization exists; otherwise the plan remains incomplete and Task 2 does not run.</done>
  <resume-signal>Authorize the native-proof dispatch for the exact displayed sealed successor digest. Any other response leaves the native path unproven.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Dispatch, capture, and verify dual-OS native evidence for the sealed successor</name>
  <files>None; this task produces candidate-bound external evidence and Git-private authority objects.</files>
  <read_first>scripts/f20-native-uat-proof.mjs, scripts/f20-native-windows-proof.ps1, scripts/f20-native-macos-proof.sh, .github/workflows/nightly-windows-soak.yml, .planning/phases/20-transactional-delegated-mutation/20-32-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-36-SUMMARY.md</read_first>
  <behavior>
    - Publication and dispatch use the durable one-attempt state machines in `f20-native-uat-proof.mjs`: publish the exact sealed successor SHA to its temporary `refs/f20-native-uat/<sha>` evidence ref only if absent (reuse an exact ref; conflicting ref fails), AND create the dispatch branch `f20-native-uat-<full-sha>` at the same sealed SHA; persist+sync dispatch intent (digest, full SHA/tree/ref/workflow, nonce, API boundary, pre-existing run set) before the single dispatch; dispatch on the BRANCH (`--ref f20-native-uat-<full-sha>`), never the `refs/f20-native-uat/*` namespace; after intent, reconcile exactly one post-boundary run carrying the nonce and never dispatch again.
    - `capture-verify` validates the Windows job ran on the self-hosted msvc runner and the macOS job on the freshly re-registered ephemeral runner (which deregisters after its one job), with ordered per-target and final commit/tree/nonce markers for EVERY Windows and macOS target — the two live_fs_acl ACL targets, BOTH Job-Object containment targets (windows-job-object + windows-hard-process-containment), the cross-platform dispatch/lifecycle targets, and all eight macOS targets including every docker target.
    - The run re-proves on hardware the two now-fixed 20-32 RED causes: EVERY Windows granted-read/parent-exit target returns exit 0 (windows-retained-handle no longer fails at live_fs_acl.rs:382, windows-appcontainer-acl passes, and the Job-Object targets whose parent scripts had the `ver`-tail now exit 0 with no onion-peel), AND EVERY macOS docker target finds `alpine:3.19` (no `404: No such image`), so both `F20_NATIVE_WINDOWS_ACCEPTANCE` and `F20_NATIVE_MACOS_ACCEPTANCE` final markers appear. The still-holding earlier fixes must also hold (macOS compiles/no E0599, macos-retained-directory green; Windows safe.directory setup clears). A repeat of any RED (a residual `ver`-index exit on any Windows target, a mid-proof docker 404, or a regression of a prior fix) is a native failure.
    - The UAT evidence ref is retained; its remote SHA is rechecked after capture and recorded `retained` / `deletion_not_authorized`. The dispatch branch is left in place (no cleanup authorization).
  </behavior>
  <action>Persist the fresh native-proof-dispatch authorization for the exact sealed successor digest. Switch GitHub auth to `FerroxLabs`. Publish the sealed SHA to its temporary UAT evidence ref (first-attempt-only; reconcile-only after intent) and create the dispatch branch at the same SHA, then dispatch the native-proof workflow once on the branch (Windows self-hosted msvc + macOS ephemeral). Capture and verify: bind the Windows and macOS job/runner identities, require the self-hosted msvc runner for Windows and the freshly-registered ephemeral runner for macOS, validate ordered per-target + final markers for every target, and confirm on hardware both fixed 20-32 causes (every Windows target exits 0 with no onion-peel to a later target; every macOS docker target finds alpine:3.19) AND that the earlier fixes still hold (macOS lib compiles + rename acceptance green; Windows setup clears). Recheck and retain the exact evidence ref. Do NOT complete any requirement, run the aggregate (that is 20-42), retry, repair source, or delete the ref/branch. Any missing/skipped/wrong-runner/incomplete-marker evidence fails closed; a native RED requires a further repaired successor and fresh authorization.</action>
  <verify>
    <automated>node scripts/f20-native-uat-proof.mjs verify-publication --git-dir "$(git rev-parse --absolute-git-dir)" --repo FerroxLabs/wayland-core</automated>
    <automated>node scripts/f20-native-uat-proof.mjs verify-evidence --git-dir "$(git rev-parse --absolute-git-dir)" --repo FerroxLabs/wayland-core --workflow nightly-windows-soak.yml</automated>
  </verify>
  <acceptance_criteria>Both native platforms are green on the correct runners against the exact sealed successor source, with complete ordered markers (every Windows target and all 8 macOS targets), both final acceptance markers present, and a retained evidence ref. Any other state is incomplete and blocks the fresh review.</acceptance_criteria>
  <done>The sealed successor has retained, candidate-bound dual-OS native evidence for 20-40 to review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native evidence ← runner host | Only a real AppContainer-capable Windows host and a real macOS host (with a live Docker daemon) may produce native proof; cross-compilation/source cannot |
| dispatch authority ← Sean | The native-proof dispatch is a Sean-only external mutation; both prior digests' authorizations are spent/void |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-39-01 | Spoofing | native PASS provenance | critical | mitigate | Markers bind exact SHA/tree/nonce; Windows must run on self-hosted msvc and macOS on the freshly-registered ephemeral runner; a hosted-image landing or missing/partial marker (e.g. an onion-peel to windows-job-object, or a missing docker-target marker) fails closed. Native is never claimed from cross-compilation (D7). |
| T-20-39-02 | Elevation of Privilege | unauthorized/stale dispatch | high | mitigate | Blocking-human checkpoint; only Sean's fresh digest-specific authorization runs Task 2; BOTH the spent 95c81ec6 and 17412cf2 authorizations are void; re-entry is reconcile-only, never a second dispatch. |
| T-20-39-03 | Denial of Service | burned run on non-compiling/unprovisioned candidate | high | mitigate | Pre-dispatch guard re-runs the sanctioned Mac compile check and confirms the Windows safe.directory guard; the macOS harness now pre-pulls alpine:3.19 fail-closed so a docker 404 aborts cleanly rather than mid-proof. The `#![cfg(windows)]` exit-code fix is un-pre-compilable and is accepted as proven at the re-dispatch itself. |
| T-20-39-SC | Tampering | UAT evidence ref | high | mitigate | One-attempt publish/dispatch state machines; ref retained and rechecked; deletion not authorized. |
</threat_model>

<verification>
Authenticate the sealed successor source/tree, the pre-dispatch Mac compile check + Windows guard, the single native attempt bound by nonce, the correct runner identities, complete ordered markers for every Windows target and all 8 macOS targets, both final acceptance markers, and the retained ref. Record the retained UAT ref, dispatch branch, run id, per-target results, and both platforms' green/red disposition in `20-39-SUMMARY.md`. No Cargo runs on the Mac except the sanctioned pre-dispatch compile check; no requirement completes here.
</verification>

<success_criteria>
- One FRESH Sean-authorized native-proof dispatch ran the exact sealed successor on the self-hosted msvc runner (Windows) and the freshly-registered ephemeral runner (macOS) (r11).
- Both platforms are green, re-proving the two fixed 20-32 RED causes on hardware — every Windows target exits 0 with no onion-peel; every macOS docker target finds alpine:3.19 — while the earlier fixes still hold (r14/r5/r7/r9/r3/r4).
- The evidence ref is retained (deletion not authorized) and bound to the exact sealed successor SHA for the fresh review.
</success_criteria>

## Artifacts this phase produces
- Retained candidate-bound dual-OS native evidence and Git-private dispatch authority objects for the second repaired successor.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-39-SUMMARY.md` using the standard GSD summary template. Record the sealed source/tree, dispatch branch/ref + run identity, per-target native results for both platforms (every Windows target + all 8 macOS targets), the retained UAT ref, and the explicit non-claim of requirement completion. If either platform is RED, record INCOMPLETE and route to a further repaired successor + fresh authorization.
</output>

---
phase: 20-transactional-delegated-mutation
plan: "69"
type: execute
wave: 66
depends_on: []
files_modified:
  - crates/wcore-sandbox/tests/hard_process_containment_windows.rs
autonomous: true
gap_closure: true
requirements:
  - REQ-native-r7
  - REQ-native-r8
must_haves:
  truths:
    - "On SEANDESKTOP (`ssh SeanD@seandesktop`, `C:\\ferrox-win`), `cargo nextest run -p wcore-sandbox --test hard_process_containment_windows --run-ignored all` is 5/5 green in <40s. The redesigned `active_process_cap_is_enforced` no longer deterministically times out at ~120s (the measured ~2s/spawn Low-IL AppContainer cost made the 544-descendant end-to-end fan-out infeasible) (REQ-native-r7)."
    - "The redesigned `active_process_cap_is_enforced` is NON-VACUOUS: it builds a real Windows Job Object with a SMALL `JOB_OBJECT_LIMIT_ACTIVE_PROCESS` cap (`TEST_JOB_CAP`, e.g. 4), assigns `TEST_JOB_CAP` plain suspended children (all succeed), cross-checks the kernel accounting count equals the cap, then proves the (cap+1)th `AssignProcessToJobObject` is REJECTED with `ERROR_NOT_ENOUGH_QUOTA` — an assertion that FAILS if the cap were absent or unenforced. This is the exact OS primitive `process.rs:454` relies on (REQ-native-r7)."
    - "A source-level static assertion ties the small-scale primitive to production wiring: the test reads `command.rs` and `process.rs` at run time and asserts `command.rs` still declares `SANDBOX_ACTIVE_PROCESS_LIMIT: u32 = 512` AND `process.rs` installs `ActiveProcessLimit = SANDBOX_ACTIVE_PROCESS_LIMIT` under `JOB_OBJECT_LIMIT_ACTIVE_PROCESS`. Drift between test intent and production wiring fails the test CLOSED (REQ-native-r7)."
    - "The test carries explicit, honest in-file documentation stating WHY the end-to-end 512-concurrent fan-out was infeasible (hardware-measured ~2s/spawn under the Low-IL AppContainer; peak concurrent descendants = 1; 544 serial spawns cannot fit `manifest(120)`) and what the redesign proves instead — so the change is auditable, never silent gate-weakening."
    - "The coupled proof harness stays consistent: `scripts/f20-native-windows-proof.ps1` `windows-job-object` selector still names `active_process_cap_is_enforced`, `scripts/f20-native-uat-proof.mjs` `WINDOWS_TARGETS`/`WINDOWS_TARGET_SOURCES` still resolve `windows-job-object` to `hard_process_containment_windows` (os=windows), the source stays `#![cfg(windows)]`-gated with NO foreign-OS cfg literal, and the wrong-OS anti-drift guard passes. The test NAME is preserved, so no selector/map edit is needed — consistency is proven by a fail-closed grep, not assumed (REQ-native-r8)."
    - "`NATIVE_CONTAINMENT_CASES` remains 5 and `native_containment_gate_marker` still asserts 5 — no containment coverage is silently dropped by the redesign."
  artifacts:
    - path: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      provides: "A feasible (<40s), deterministic, non-vacuous `active_process_cap_is_enforced` that proves the Job-Object ActiveProcessLimit primitive at small scale plus a source-grep tie to the production 512 cap, replacing the infeasible 544-descendant end-to-end fan-out"
    - path: .planning/phases/20-transactional-delegated-mutation/20-69-SUMMARY.md
      provides: "Record of the redesign, the measured infeasibility it closes, the SEANDESKTOP 5/5 <40s proof, and the verified harness consistency"
  key_links:
    - from: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      to: crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
      via: "source-grep static assertion that production still declares SANDBOX_ACTIVE_PROCESS_LIMIT: u32 = 512 and process.rs installs ActiveProcessLimit = SANDBOX_ACTIVE_PROCESS_LIMIT under JOB_OBJECT_LIMIT_ACTIVE_PROCESS"
      pattern: "native_windows"
    - from: crates/wcore-sandbox/tests/hard_process_containment_windows.rs
      to: scripts/f20-native-windows-proof.ps1
      via: "preserved test name keeps the windows-job-object selector + the mjs WINDOWS_TARGET_SOURCES map consistent; verified by a fail-closed grep"
      pattern: "native_windows"
---

<objective>
Redesign the ONE broken native-proof test — `crates/wcore-sandbox/tests/hard_process_containment_windows.rs::active_process_cap_is_enforced` (currently line 551) — from an infeasible end-to-end 544-descendant sandboxed fan-out into a FEASIBLE (<40s), DETERMINISTIC, NON-VACUOUS direct Job-Object `ActiveProcessLimit` primitive assertion at small scale, paired with a source-grep static assertion tying it to the production 512 cap. Preserve the test's identity, the containment case count, the `#![cfg(windows)]` gate, and the whole coupled proof harness.

Purpose: The current test fans out `SANDBOX_ACTIVE_PROCESS_LIMIT(512)+32 = 544` detached sandboxed `for /L` descendants to try to observe the Job-Object cap. Hardware-measured on SEANDESKTOP (i9-13900KF): sandboxed process CREATION under the Low-IL AppContainer is ~2s each, so 544 serial spawns cannot complete inside `manifest(120)` — the test deterministically fails at ~120.5s with `Err(Timeout)`, both in isolation and under load, and it is VACUOUS (peak concurrent descendants measured = 1, because spawn latency ≈ hold duration, so descendants never accumulate toward the 512 cap). An end-to-end 512-concurrent cap test THROUGH the sandbox is infeasible on real hardware. This plan replaces it with a direct assertion of the same OS primitive the production sandbox installs, at a small scale that is fast and unambiguous, without weakening the gate.
Output: A redesigned `active_process_cap_is_enforced` proven 5/5 green in <40s on SEANDESKTOP, with the coupled harness verified consistent.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@crates/wcore-sandbox/tests/hard_process_containment_windows.rs
@crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs
@crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs
@scripts/f20-native-windows-proof.ps1
@scripts/f20-native-uat-proof.mjs
</context>

<execution_rules>
- Compile discipline: NO Mac cargo except `cargo check -p wcore-sandbox --features live-docker --tests`. Because the test file is `#![cfg(windows)]`, that Mac check compiles the body to NOTHING — it only proves the rest of the crate/harness still builds and does NOT type-check the new Win32 FFI. The AUTHORITATIVE compile + run is SEANDESKTOP only. `cargo fmt` works on Mac and MUST be run.
- Real-hardware proof only (CONTEXT D7): the pass is claimed from the SEANDESKTOP `nextest` run, never from Mac cross-compilation or source inspection.
- Preserve the test NAME `active_process_cap_is_enforced` and keep the source `#![cfg(windows)]`-gated. This keeps the `windows-job-object` selector (`scripts/f20-native-windows-proof.ps1`) and the shared map (`scripts/f20-native-uat-proof.mjs` `WINDOWS_TARGETS`/`WINDOWS_TARGET_SOURCES`) consistent with ZERO edits. Consistency is proven by a fail-closed grep, not assumed.
- Anti-drift guard safety (REQ-native-r8): the wrong-OS guard reads the test SOURCE and fails closed if it finds a foreign-OS configuration attribute (unix, or any non-windows target_os). The redesigned file — including every comment and the honest documentation — must contain NO foreign-OS configuration attribute literal. Describe the Linux/bwrap counterpart by prose only, never by a config-attribute literal.
- Keep `NATIVE_CONTAINMENT_CASES = 5`; do not drop or rename any of the other four containment tests. Their shared helpers (`live_descendant_pids`, `descendant_hold`, `inline_hold`, `wait_until`, `surviving_captured_descendant_pids`, `reap_stray_descendants`, etc.) remain in use — do not delete them.
- No new dependency: `windows-sys` 0.59 is already a `cfg(windows)` normal dependency of `wcore-sandbox` with the `Win32_System_JobObjects`, `Win32_System_Threading`, and `Win32_Foundation` features enabled — the test uses it directly, no Cargo.toml change.
- Use `/usr/bin/git`; never stage the AGENTS.md ijfw churn.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Redesign active_process_cap_is_enforced as a direct small-scale Job-Object ActiveProcessLimit assertion plus a production source-grep tie</name>
  <files>crates/wcore-sandbox/tests/hard_process_containment_windows.rs</files>
  <read_first>crates/wcore-sandbox/tests/hard_process_containment_windows.rs, crates/wcore-sandbox/src/backends/appcontainer/windows_impl/command.rs, crates/wcore-sandbox/src/backends/appcontainer/windows_impl/process.rs</read_first>
  <behavior>
    - Cap admits up to the small cap: building a Job Object with `JOB_OBJECT_LIMIT_ACTIVE_PROCESS` (plus `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`, mirroring production) and `ActiveProcessLimit = TEST_JOB_CAP` (e.g. 4), then assigning `TEST_JOB_CAP` plain suspended child processes, every `AssignProcessToJobObject` returns success.
    - Kernel accounting confirms the count: `QueryInformationJobObject(JobObjectBasicAccountingInformation)` reports `ActiveProcesses == TEST_JOB_CAP` after the `TEST_JOB_CAP` assignments — proving the suspended children genuinely occupy the cap (removes any "suspended may not count" ambiguity).
    - Overflow is rejected by the cap: creating one more suspended child and calling `AssignProcessToJobObject` returns 0 (failure) AND `GetLastError() == ERROR_NOT_ENOUGH_QUOTA` — the exact job-active-process-limit error. `ActiveProcesses` stays at `TEST_JOB_CAP` (the rejected process never entered). This assertion FAILS if the cap is absent/unenforced (non-vacuous).
    - Production wiring is still real: a source-grep of `command.rs` finds `SANDBOX_ACTIVE_PROCESS_LIMIT: u32 = 512`, and a source-grep of `process.rs` finds both `ActiveProcessLimit = SANDBOX_ACTIVE_PROCESS_LIMIT` and `JOB_OBJECT_LIMIT_ACTIVE_PROCESS`. Missing any of these fails the test (drift fails closed).
    - Feasible and deterministic: the whole test completes in well under 1s (children are created suspended and never resumed — no image executes, no ~2s AppContainer spawn cost), and every handle is cleaned up (terminate the un-assigned overflow child, close all process/thread handles, close the job — `KILL_ON_JOB_CLOSE` reaps the assigned suspended children).
  </behavior>
  <action>Replace the body of `active_process_cap_is_enforced` (currently line 551), preserving its NAME, its `#[ignore = "explicit native Windows Job-Object containment acceptance"]`, and the leading `require_live_windows()` gate. Convert it from an `async` `tokio::test` to a plain synchronous `#[test]` (the redesign is pure Win32 FFI — no tokio needed). Delete the 544-descendant fan-out (`SANDBOX_ACTIVE_PROCESS_LIMIT + 32` attempts, the `%k`/`%i` nested `for /L`, the peak-sampling loop, and the post-close captured-PID reap) entirely.

Implement the direct primitive using `windows-sys` (already a `cfg(windows)` dep): `use windows_sys::Win32::System::JobObjects::{CreateJobObjectW, SetInformationJobObject, AssignProcessToJobObject, QueryInformationJobObject, JOBOBJECT_EXTENDED_LIMIT_INFORMATION, JOBOBJECT_BASIC_ACCOUNTING_INFORMATION, JobObjectExtendedLimitInformation, JobObjectBasicAccountingInformation, JOB_OBJECT_LIMIT_ACTIVE_PROCESS, JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE}`, `windows_sys::Win32::System::Threading::{CreateProcessW, PROCESS_INFORMATION, STARTUPINFOW, CREATE_SUSPENDED, CREATE_NO_WINDOW, TerminateProcess}`, and `windows_sys::Win32::Foundation::{CloseHandle, GetLastError, ERROR_NOT_ENOUGH_QUOTA}`. Add a small module const `const TEST_JOB_CAP: u32 = 4;` above the test.

Steps in the test: (1) `CreateJobObjectW`, then zero a `JOBOBJECT_EXTENDED_LIMIT_INFORMATION`, set `BasicLimitInformation.LimitFlags = JOB_OBJECT_LIMIT_ACTIVE_PROCESS | JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` and `BasicLimitInformation.ActiveProcessLimit = TEST_JOB_CAP`, and `SetInformationJobObject(job, JobObjectExtendedLimitInformation, ...)` — mirroring the exact production pattern in `process.rs:437-454`. (2) Resolve a benign child image path from `ComSpec` env (fallback: `%SystemRoot%\\System32\\cmd.exe`); spawn `TEST_JOB_CAP` children with `CreateProcessW(..., CREATE_SUSPENDED | CREATE_NO_WINDOW, ...)` (never resumed), assigning each to the job and asserting each `AssignProcessToJobObject` returns non-zero. (3) `QueryInformationJobObject(JobObjectBasicAccountingInformation)` and assert `ActiveProcesses == TEST_JOB_CAP`. (4) Spawn one more suspended child, call `AssignProcessToJobObject`, and assert it returns 0 with `GetLastError() == ERROR_NOT_ENOUGH_QUOTA`; re-query accounting and assert `ActiveProcesses` is still `TEST_JOB_CAP`. (5) Cleanup: `TerminateProcess` + `CloseHandle` the overflow child's process/thread handles (it is NOT in the job so `KILL_ON_JOB_CLOSE` will not reap it), `CloseHandle` every assigned child's process/thread handle, then `CloseHandle` the job (reaps the assigned suspended children). Wrap the FFI in a single `unsafe` block with the same error-message style the other tests use; on any Win32 failure, `panic!` with `GetLastError()` (fail closed).

Repurpose the existing module const `SANDBOX_ACTIVE_PROCESS_LIMIT` (keep it, value 512) as the EXPECTED production value for a source-grep static assertion: read `../src/backends/appcontainer/windows_impl/command.rs` and `process.rs` (relative to `env!("CARGO_MANIFEST_DIR")`) and assert `command.rs` contains the production declaration of `SANDBOX_ACTIVE_PROCESS_LIMIT` set to `SANDBOX_ACTIVE_PROCESS_LIMIT` (512), and `process.rs` contains both `ActiveProcessLimit = SANDBOX_ACTIVE_PROCESS_LIMIT` and `JOB_OBJECT_LIMIT_ACTIVE_PROCESS`; panic with a drift message if any is absent. This preserves the original const's fail-closed-on-drift intent (per its doc comment) while dropping the infeasible live 512 fan-out.

Rewrite the test's doc comment to be explicit and honest: state that an end-to-end 512-concurrent cap test THROUGH the sandbox is infeasible on real hardware because sandboxed process creation under the Low-IL AppContainer is hardware-measured at ~2s each (so 544 serial spawns cannot fit `manifest(120)` and peak concurrent descendants was measured = 1, never approaching 512), and that the redesign instead proves the SAME OS primitive `process.rs:454` relies on — a Job Object with `JOB_OBJECT_LIMIT_ACTIVE_PROCESS` rejects the (cap+1)th assignment with `ERROR_NOT_ENOUGH_QUOTA` — at a small, fast, deterministic scale, tied to the real 512 production cap by the source-grep. The doc must NOT contain any foreign-OS configuration-attribute literal (describe the Linux bwrap counterpart in prose only) so the REQ-native-r8 anti-drift guard stays green.

Keep `NATIVE_CONTAINMENT_CASES = 5` and leave the other four tests and all shared helpers untouched. Implements REQ-native-r7 (real Windows Job-Object containment test, now feasible and non-vacuous). No requirement is marked complete here — closure is claimed by the downstream native proof that consumes the SEANDESKTOP-green tree.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox && cargo fmt --all -- --check && cargo check -p wcore-sandbox --features live-docker --tests 2>&1 | tail -5</automated>
  </verify>
  <done>`cargo fmt --all -- --check` is clean and `cargo check -p wcore-sandbox --features live-docker --tests` builds on the Mac (the `#![cfg(windows)]` body is skipped there, so this proves only the crate/harness still builds; the authoritative Windows compile is Task 2). The redesigned `active_process_cap_is_enforced` is a synchronous `#[test]` with no 544-descendant fan-out, uses the direct Job-Object `ActiveProcessLimit` primitive with the `ERROR_NOT_ENOUGH_QUOTA` overflow assertion and the `JobObjectBasicAccountingInformation` cross-check, carries the source-grep static assertion, and has honest documentation with no foreign-OS config literal. `NATIVE_CONTAINMENT_CASES` is still 5.</done>
</task>

<task type="auto">
  <name>Task 2: Prove SEANDESKTOP 5/5 green in under 40s and verify the coupled proof harness stays consistent</name>
  <files>None (verification only): reads scripts/f20-native-windows-proof.ps1 and scripts/f20-native-uat-proof.mjs; runs nextest on SEANDESKTOP.</files>
  <read_first>scripts/f20-native-windows-proof.ps1, scripts/f20-native-uat-proof.mjs, crates/wcore-sandbox/tests/hard_process_containment_windows.rs</read_first>
  <behavior>
    - Harness consistency (local, fail-closed): the `windows-job-object` PS1 selector still names `active_process_cap_is_enforced` and still targets `--test hard_process_containment_windows`; the mjs `WINDOWS_TARGETS` still contains `windows-job-object` and `WINDOWS_TARGET_SOURCES['windows-job-object']` still maps to `{ test: 'hard_process_containment_windows', os: 'windows' }`; the test source still begins `#![cfg(windows)]` and contains no foreign-OS configuration-attribute literal. Because the test name is preserved, no edit is expected — the grep proves consistency rather than assuming it.
    - Authoritative Windows proof (real hardware): on SEANDESKTOP the full ignored native containment suite runs green — all 5 tests, including the redesigned cap test — in under 40s wall clock (the redesigned cap test alone completes in well under 1s; the other four hold ~2s each).
  </behavior>
  <action>First, run the local harness-consistency gate (fail-closed) from the repo root: grep `scripts/f20-native-windows-proof.ps1` to confirm the `windows-job-object` target still selects `--test hard_process_containment_windows` with `active_process_cap_is_enforced` present in its `-E 'test(/.../)'` regex; grep `scripts/f20-native-uat-proof.mjs` to confirm `WINDOWS_TARGETS` contains `windows-job-object` and `WINDOWS_TARGET_SOURCES['windows-job-object']` maps to `hard_process_containment_windows` with `os: 'windows'`; and confirm the test source's first non-doc line is `#![cfg(windows)]` and it contains no unix / non-windows target_os configuration-attribute literal (so the REQ-native-r8 anti-drift guard passes). If any check fails, STOP and fix (the name should have been preserved in Task 1, so a failure means an accidental rename or a foreign-OS literal crept in).

Then commit the Task 1 change with `/usr/bin/git` (never stage AGENTS.md) and run the authoritative Windows proof on SEANDESKTOP: `ssh SeanD@seandesktop` into `C:\ferrox-win`, fetch and check out the exact fix commit, set `WAYLAND_SANDBOX_LIVE_WINDOWS=1`, and run `cargo nextest run -p wcore-sandbox --test hard_process_containment_windows --run-ignored all`. Confirm all 5 tests pass and the total wall clock is under 40s (record it). As downstream confirmation, note the full 6-target native proof path (`scripts/f20-native-windows-proof.ps1` + `scripts/f20-native-uat-proof.mjs`) that consumes this test via the `windows-job-object` target; the scarce full 6-target CI dispatch itself remains gated by the phase's existing Sean-authorized native-proof-dispatch flow (20-25/20-68) and is out of scope for this focused repair. Records evidence for REQ-native-r7 and REQ-native-r8; marks no requirement complete.</action>
  <verify>
    <automated>ssh SeanD@seandesktop "cd C:\ferrox-win; git fetch --quiet; git checkout --quiet $(cd /Users/seandonahoe/dev/waylandcore-ferrox && /usr/bin/git rev-parse HEAD); $env:WAYLAND_SANDBOX_LIVE_WINDOWS='1'; cargo nextest run -p wcore-sandbox --test hard_process_containment_windows --run-ignored all"</automated>
  </verify>
  <done>Local harness-consistency grep passes (PS1 `windows-job-object` selector + mjs `WINDOWS_TARGETS`/`WINDOWS_TARGET_SOURCES` map both intact and pointing at `hard_process_containment_windows` os=windows; source still `#![cfg(windows)]` with no foreign-OS config literal). On SEANDESKTOP `cargo nextest run -p wcore-sandbox --test hard_process_containment_windows --run-ignored all` is 5/5 green in under 40s (the cap test that previously timed out at ~120.5s now passes fast). The recorded wall clock and per-test results are captured in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate integrity ← test redesign | The redesign must not weaken the gate: it must remain an assertion that FAILS if the sandbox did not bound runaway process creation |
| production wiring ← test claim | The small-scale primitive proof is only meaningful if production still installs `ActiveProcessLimit = SANDBOX_ACTIVE_PROCESS_LIMIT`; the source-grep binds them |
| harness consistency ← preserved identity | The `windows-job-object` selector + shared map + anti-drift guard must stay consistent across the redesign |
| native evidence ← real hardware | The green must come from SEANDESKTOP, never the Mac cross-compile (which skips the `#![cfg(windows)]` body) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-69-01 | Tampering | silent gate-weakening — replacing an infeasible test with a vacuous/always-green stub | high | mitigate | The redesign keeps a fail-if-unenforced assertion (the (cap+1)th `AssignProcessToJobObject` must be rejected with `ERROR_NOT_ENOUGH_QUOTA`) plus a `JobObjectBasicAccountingInformation` count cross-check; honest in-file documentation records WHY the fan-out was infeasible; `NATIVE_CONTAINMENT_CASES` stays 5. |
| T-20-69-02 | Repudiation | test drifts from production wiring (cap changed/removed in src, test unaware) | high | mitigate | Source-grep static assertion fails closed unless `command.rs` declares `SANDBOX_ACTIVE_PROCESS_LIMIT: u32 = 512` and `process.rs` installs `ActiveProcessLimit = SANDBOX_ACTIVE_PROCESS_LIMIT` under `JOB_OBJECT_LIMIT_ACTIVE_PROCESS`. |
| T-20-69-03 | Tampering | anti-drift regression — a rename or a foreign-OS cfg literal breaks the wrong-OS guard | high | mitigate | Preserve the test name (zero selector/map edits) and forbid any foreign-OS configuration-attribute literal in the file; a fail-closed grep re-verifies the PS1 selector, the mjs map, and the `#![cfg(windows)]` gate (REQ-native-r8). |
| T-20-69-04 | Spoofing | false green from the Mac cross-compile (which skips the `#![cfg(windows)]` body) | high | mitigate | The authoritative pass is the SEANDESKTOP `nextest` run on real hardware; the Mac `cargo check` is documented as non-authoritative for the Windows body. |
</threat_model>

<verification>
Acceptance is the SEANDESKTOP run: `cargo nextest run -p wcore-sandbox --test hard_process_containment_windows --run-ignored all` is 5/5 green in under 40s, with the redesigned `active_process_cap_is_enforced` proving the Job-Object `ActiveProcessLimit` primitive (small cap admits the cap, the (cap+1)th assignment is rejected with `ERROR_NOT_ENOUGH_QUOTA`, accounting confirms the count) and the source-grep tying it to the production 512 cap. The local fail-closed grep confirms the PS1 `windows-job-object` selector, the mjs `WINDOWS_TARGETS`/`WINDOWS_TARGET_SOURCES` map, and the `#![cfg(windows)]` gate are consistent, so the REQ-native-r8 anti-drift guard passes. Record in 20-69-SUMMARY.md: the fix commit SHA/tree, the measured infeasibility being closed, the per-test results and total wall clock from SEANDESKTOP, and the harness-consistency confirmation. No Phase 20 requirement is marked complete here — closure is claimed by the downstream native proof that consumes the SEANDESKTOP-green tree.
</verification>

<success_criteria>
- `active_process_cap_is_enforced` is redesigned into a synchronous, direct small-scale Job-Object `ActiveProcessLimit` assertion (admit-to-cap, `ERROR_NOT_ENOUGH_QUOTA` overflow rejection, `JobObjectBasicAccountingInformation` count cross-check) plus a production source-grep tie to `SANDBOX_ACTIVE_PROCESS_LIMIT = 512`; the 544-descendant fan-out is gone (REQ-native-r7).
- SEANDESKTOP `cargo nextest run -p wcore-sandbox --test hard_process_containment_windows --run-ignored all` is 5/5 green in under 40s — the deterministic ~120.5s timeout is eliminated (REQ-native-r7).
- The test carries honest documentation of the measured infeasibility and what the redesign proves instead; it is non-vacuous (an assertion FAILS if the cap were unenforced).
- The coupled harness stays consistent with zero edits: PS1 `windows-job-object` selector + mjs map intact, source `#![cfg(windows)]` with no foreign-OS config literal, anti-drift guard green, `NATIVE_CONTAINMENT_CASES = 5` (REQ-native-r8).
</success_criteria>

## Artifacts this phase produces
- A redesigned `crates/wcore-sandbox/tests/hard_process_containment_windows.rs` with a feasible, non-vacuous `active_process_cap_is_enforced`.
- 20-69-SUMMARY.md recording the redesign, the measured infeasibility closed, the SEANDESKTOP 5/5 <40s proof, and the verified harness consistency.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-69-SUMMARY.md` using the standard GSD summary template. Record the fix commit `source_sha`/tree, the measured ~2s/spawn infeasibility being closed, the redesign (direct Job-Object `ActiveProcessLimit` primitive + `ERROR_NOT_ENOUGH_QUOTA` overflow + accounting cross-check + production source-grep), the SEANDESKTOP per-test results and total wall clock (<40s), and the harness-consistency confirmation (PS1 selector + mjs map + `#![cfg(windows)]` + anti-drift guard). Mark no Phase 20 requirement complete (closure claimed by the downstream native proof).
</output>

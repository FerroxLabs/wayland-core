---
phase: 20-transactional-delegated-mutation
plan: "46"
type: execute
wave: 45
depends_on:
  - "20-45"
files_modified: []
autonomous: false
requirements:
  - REQ-native-r14
  - REQ-native-r11
  - REQ-native-r5
  - REQ-native-r7
  - REQ-native-r6
  - REQ-native-r2
  - REQ-native-r9
  - REQ-native-r3
  - REQ-native-r4
must_haves:
  truths:
    - "One FRESH Sean-authorized native-proof dispatch runs the exact sealed third-repaired-successor on real hardware: Windows on the AppContainer-capable self-hosted msvc runner (client SKU) and macOS on a FRESHLY re-registered ephemeral Apple-silicon runner; native proof is never claimed from cross-compilation or source inspection (CONTEXT D6/D7, REQ-native-r11). ALL prior native-proof-dispatch authorizations (95c81ec6, 17412cf2, daf27337) are spent/void; this digest requires its own fresh authorization on a fresh dispatch branch with single-attempt discipline (REQ-native-r14)."
    - "The native run re-proves on hardware the two now-fixed 20-43 PRODUCTION root causes across ALL 6 Windows targets with no onion-peel to a later target: #A — windows-f20-lifecycle + windows-public-dispatch pass the DispatchAdmission capacity probe (the de-verbatimized swarm_root + WCORE_SWARM_PROBE_ROOT env transport, real budget intact); #B — windows-appcontainer-acl + windows-retained-handle read the granted file (exit 0, not ERROR_INVALID_NAME) and windows-job-object + windows-hard-process-containment run their fixed cmd payloads and containment counting (REQ-native-r5/r7/r6/r2)."
    - "The Kimi watch-items are EXERCISED as the next-layer checks: git operations run against the de-verbatimized swarm_root; tagged_cmd_count is CIM-visible under NetworkService; the bash worker under AppContainer takes the no-Windows-Docker-fallback path (dispatch.rs); the worker test-exe loads its DLLs under the Low-IL restricted token. Any watch-item surfacing as a new-layer failure is recorded honestly, not masked (REQ-native-r14)."
    - "macOS is re-run for the sealed tuple (already 8/8-proven, re-confirmed on the ephemeral runner: alpine:3.19 present, all 8 targets genuine + green, macos-retained-directory green, no E0599) and the lib-test compile debt is confirmed cleared on msvc; the wcore-agent/wcore-sandbox surfaces compile on x86_64-pc-windows-msvc (REQ-native-r9/r3/r4). Both platforms green with ordered per-target and final commit/tree/nonce markers bound to the exact sealed successor SHA; the UAT evidence ref is retained (deletion not authorized)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-46-SUMMARY.md
      provides: "Retained dual-OS native evidence bound to the exact sealed third-repaired-successor for the fresh native-inclusive review (20-47)"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md
      to: .planning/phases/20-transactional-delegated-mutation/20-46-SUMMARY.md
      via: "the sealed successor SHA is published to a temporary UAT ref + fresh dispatch branch and dispatched to native runners"
      pattern: "source_sha"
---

<objective>
Produce real native evidence for the sealed third-repaired-successor: one FRESH Sean-authorized native-proof dispatch that runs Windows (AppContainer-capable self-hosted msvc) and macOS (freshly re-registered ephemeral) acceptance on hardware, re-proving the two now-fixed 20-43 PRODUCTION root causes across ALL 6 Windows targets (#A capacity-probe admission on windows-f20-lifecycle + windows-public-dispatch; #B granted-read exit-0 + fixed containment on windows-appcontainer-acl + windows-retained-handle + windows-job-object + windows-hard-process-containment) AND exercising the Kimi watch-items as the next-layer checks, plus re-confirming macOS 8/8 for the sealed tuple, so the fresh 20-47 review can review native evidence rather than defer it.

Purpose: The audit requires the fresh review NOT to defer native (R12/R13); that is only possible if native evidence exists at review time. This is the re-dispatch of the native-proof gate for the third repaired successor — Sean-only (D6), with a fresh digest-specific authorization (the 95c81ec6, 17412cf2, AND daf27337 authorizations are spent/void), distinct from the terminal exact-tuple authorization at 20-49.
Output: Retained, candidate-bound dual-OS native evidence, or an explicit incomplete disposition; no requirement completion here.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md
@.planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-45-SUMMARY.md
</context>

<execution_rules>
- Sean-only gate (CONTEXT D6): this plan dispatches a scarce native run. ALL prior native-proof-dispatch authorizations (95c81ec6, 17412cf2, daf27337) are spent/void; a FRESH digest-specific authorization is required, bound to the exact sealed third-repaired-successor SHA.
- Dispatch discipline: fresh digest auth; fresh ephemeral macOS runner (re-registered); a fresh dispatch branch; single-attempt (no silent retries). The Windows leg runs on an AppContainer-capable self-hosted msvc client-SKU runner (REQ-native-r11), NOT a hosted runner.
- Before the run is spent, re-confirm the sanctioned macOS compile gate `cargo check -p wcore-sandbox --features live-docker --tests` is green on THIS Mac (fail-safe: blocks if it cannot run). This gate CANNOT compile the `#![cfg(windows)]` #A-probe-env/#B-quoting/containment/compile-debt code — their compile AND behavior are proven by THIS re-dispatch (CONTEXT D5/D7).
- Native proof is real-hardware only (CONTEXT D7); never claim it from cross-compilation or source inspection.
- The run MUST exercise ALL 6 Windows targets AND the Kimi watch-items (git-with-de-verbatimized-swarm_root, tagged_cmd_count CIM visibility under NetworkService, bash worker under AppContainer + no-Windows-Docker-fallback in dispatch.rs, worker test-exe DLL-load under Low-IL). macOS is already 8/8-proven but re-run for the sealed tuple. This plan writes no source; it dispatches and records evidence.
</execution_rules>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The sealed third-repaired-successor (20-44) with the two PRODUCTION root-cause fixes (#A verbatim-path/env-transport probe; #B cmd.exe payload quoting), the Job-Object counting-scope fix, and the lib-test compile-debt imports, pre-native-cross-audited by Kimi K3 + ferrox (20-45). It is ready for the FRESH Sean-authorized dual-OS native-proof dispatch.</what-built>
  <how-to-verify>
    1. Confirm the sanctioned macOS compile gate is green on this Mac: `cargo check -p wcore-sandbox --features live-docker --tests` (blocks if it cannot run — no false green).
    2. Confirm the 20-45 cross-audit is a zero-finding PASS with per-reviewer schema-validated artifacts (Kimi K3 `f20-native-crossaudit` + ferrox), native deferred, watch-items carried.
    3. Authorize the FRESH native-proof dispatch bound to the EXACT sealed successor SHA/tree (all prior digest authorizations are spent/void). Provide fresh digest auth, a freshly re-registered ephemeral macOS runner, and the AppContainer-capable self-hosted msvc runner; single-attempt on a fresh dispatch branch.
    4. The run must exercise ALL 6 Windows targets and the Kimi watch-items, and re-run all 8 macOS targets for the sealed tuple.
  </how-to-verify>
  <resume-signal>Type "authorized: &lt;sealed-sha&gt;" to dispatch the fresh native run bound to that exact SHA, or describe why it is withheld.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Dispatch the fresh dual-OS native run and retain candidate-bound evidence</name>
  <files>None; this task dispatches to native runners and records retained evidence.</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-45-SUMMARY.md, .planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md</read_first>
  <behavior>
    - Under the fresh Sean authorization, publish the exact sealed successor SHA/tree to a temporary UAT ref + fresh dispatch branch and dispatch the Windows leg to the AppContainer-capable self-hosted msvc runner and the macOS leg to the freshly re-registered ephemeral runner (single attempt).
    - Windows leg re-proves the two fixed 20-43 root causes across ALL 6 targets with no onion-peel: #A — windows-f20-lifecycle + windows-public-dispatch pass DispatchAdmission (the de-verbatimized swarm_root + env-transport probe compute a real budget); #B — windows-appcontainer-acl + windows-retained-handle read the granted file exit 0 (not ERROR_INVALID_NAME), and windows-job-object + windows-hard-process-containment run their fixed cmd payloads with tagged-tree containment counting. It also EXERCISES the Kimi watch-items (git vs de-verbatimized swarm_root; tagged_cmd_count CIM visibility under NetworkService; bash worker under AppContainer + no-Windows-Docker-fallback in dispatch.rs; worker test-exe DLL-load under Low-IL) and confirms the lib-test compile debt is cleared on msvc (wcore-agent/wcore-sandbox compile on x86_64-pc-windows-msvc).
    - macOS leg re-runs all 8 targets for the sealed tuple: alpine:3.19 present, every docker target genuine + green, macos-retained-directory green, no E0599.
    - Any watch-item that surfaces as a genuine next-layer failure is recorded honestly as an incomplete disposition (routing to a further repaired successor), never masked. On full green, retain the UAT evidence ref (deletion not authorized) with ordered per-target and final commit/tree/nonce markers bound to the exact sealed successor SHA.
  </behavior>
  <action>Under the fresh Sean authorization from the checkpoint, publish the exact sealed successor SHA/tree to a temporary UAT ref + fresh dispatch branch and dispatch the Windows leg (self-hosted msvc, AppContainer-capable) and macOS leg (freshly re-registered ephemeral), single attempt. Confirm ALL 6 Windows targets pass (the #A admission probe and #B granted-read/containment fixes hold with no onion-peel), the Kimi watch-items are exercised, the msvc lib-test compile is clean, and all 8 macOS targets are green for the sealed tuple. Retain the UAT evidence ref with ordered per-target + final commit/tree/nonce markers bound to the exact SHA; record any watch-item next-layer failure honestly as an incomplete disposition. Implements REQ-native-r14/r11/r5/r7/r6/r2/r9/r3/r4; no requirement completion here.</action>
  <verify>
    <human-check>Windows: all 6 targets green on the self-hosted msvc runner with markers bound to the exact sealed SHA; the Kimi watch-items are exercised. macOS: all 8 targets green on the ephemeral runner. The UAT evidence ref is retained. Any next-layer failure is recorded as an explicit incomplete disposition.</human-check>
  </verify>
  <done>Retained dual-OS native evidence bound to the exact sealed third-repaired-successor exists for the 20-47 fresh native-inclusive review, or an explicit incomplete disposition routes to a further repaired successor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native authorization ← Sean digest gate | A scarce native run is dispatched only under a fresh, digest-specific Sean authorization bound to the exact sealed SHA; all prior authorizations are spent/void |
| native evidence integrity ← real hardware | Native proof must come from real Windows/macOS hardware, never cross-compilation or source inspection |
| candidate binding ← ordered markers | The retained evidence must carry ordered per-target + final commit/tree/nonce markers bound to the exact sealed SHA |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-46-01 | Spoofing | native proof from non-hardware source | high | mitigate | Proof is real-hardware only (CONTEXT D7); markers bind to the exact sealed SHA/tree, and the run is single-attempt on a fresh dispatch branch. |
| T-20-46-02 | Elevation of Privilege | stale/reused native authorization | high | mitigate | All prior digest authorizations (95c81ec6/17412cf2/daf27337) are spent/void; a fresh digest-specific Sean authorization bound to the exact sealed SHA is required at the blocking-human checkpoint. |
| T-20-46-03 | Tampering | onion-peel / masked watch-item failure | medium | mitigate | The run must exercise ALL 6 Windows targets AND the Kimi watch-items; any next-layer failure is recorded honestly as an incomplete disposition routing to a further repaired successor, never rationalized. |
</threat_model>

<verification>
The dual-OS native run under the fresh Sean authorization determines acceptance: all 6 Windows targets green on the self-hosted msvc runner (with the Kimi watch-items exercised and the msvc lib-test compile clean) and all 8 macOS targets green on the ephemeral runner, with ordered per-target + final commit/tree/nonce markers bound to the exact sealed successor SHA and the UAT evidence ref retained. Record the sealed `source_sha`/tree, the fresh authorization identity, both legs' per-target results, the watch-item outcomes, the retained ref identity, and — on any next-layer failure — an explicit incomplete disposition, in `20-46-SUMMARY.md`. No source/test/workflow change; no requirement completion.
</verification>

<success_criteria>
- One fresh Sean-authorized dual-OS native run on real hardware (self-hosted msvc + ephemeral macOS), bound to the exact sealed SHA, with all prior authorizations spent/void (r14/r11).
- All 6 Windows targets green (the #A admission probe and #B granted-read/containment fixes hold, no onion-peel), the Kimi watch-items exercised, the msvc lib-test compile clean (r5/r7/r6/r2/r3); all 8 macOS targets green for the sealed tuple (r9/r4).
- Retained candidate-bound native evidence exists for the 20-47 fresh native-inclusive review, or an explicit incomplete disposition routes to a further repaired successor.
</success_criteria>

## Artifacts this phase produces
- Retained dual-OS native evidence (real hardware) bound to the exact sealed third-repaired-successor SHA, or an explicit incomplete disposition.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-46-SUMMARY.md` using the standard GSD summary template. Record the sealed `source_sha`/tree, the fresh Sean authorization identity, both legs' per-target results, the Kimi watch-item outcomes, the retained UAT ref identity, and the disposition (dual-OS green admits 20-47; any next-layer failure is an explicit incomplete routing to a further repaired successor). Complete no Phase 20 requirement.
</output>
</content>

---
context: phase
phase: 20-transactional-delegated-mutation
live_plans: 18
completed_plans: 17
remaining_plans: 1
next_plan: "20-56"
status: paused_awaiting_sean
last_updated: 2026-07-25
---

# Phase 20 — Transactional Delegated Mutation

**Rewritten 2026-07-25 from `RECONCILIATION.md`. Supersedes the prior file, which claimed 18 plans, was 5 days stale, and pointed at task 3 of a plan list that had since grown to 74.**

# BLOCKING CONSTRAINTS — Read Before Anything Else

- The primary checkout `/Users/seandonahoe/dev/waylandcore` is heavily dirty. Do not edit, clean, reset, rebase, or execute Phase 20 there. Phase 20 work happens in `/Users/seandonahoe/dev/waylandcore-ferrox`.
- Cargo never runs on this Mac. `cargo fmt --all` is the only working cargo command here.
- No push, merge, release, issue closure, or native-proof dispatch without Sean's explicit authorization.

## State

| | |
|---|---|
| Live plans | **18** (`20-01`…`20-17`, `20-56`) |
| Completed (have a SUMMARY) | **17** |
| Remaining | **1** |
| Archived (not deleted) | **56** — `archive/native-split/` (51), `archive/superseded/` (5) |
| Plans before reconciliation | 74 |

Phase 20 is done when its four Success Criteria are TRUE — **not** when the plan list is empty.

## Success Criteria (the only completion authority)

1. Parallel children can make conflicting edits without overwriting or silently mutating the parent.
2. Stale identity, failed gates, and conflicts stop before merge while preserving usable evidence.
3. Snapshot, workspace, journal, receipt, merge, rollback, cleanup, and native Windows/macOS identities share one authoritative lifecycle.
4. One exact F20 successor lands on the admitted candidate with focused and aggregate proof.

## The single next executable plan

**`20-56-PLAN.md`** — the terminal plan. It alone carries `F20-01`…`F20-06`, `F20-GATE-01`, `F20-GATE-02`, and takes Sean's exact-tuple authorization. `autonomous: false`.

**Two blockers on it, for Sean to decide — do not resolve these by planning:**

1. `depends_on: 20-55`, which is now in `archive/native-split/`. That dependency belonged to the native repair chain, which is no longer Phase 20.
2. It carries `REQ-native-r12`, which moved to Phase 20A.

`20-56` therefore needs its `depends_on` and `requirements` rebased onto the post-split Phase 20 before it can run. That is a small edit to one existing plan — **not** a new plan.

## What changed and why

Phase 20 grew to 74 plans because two rules could not terminate: every finding at every severity had to be repaired or rejected, and execution was gated on zero findings. Adversarial review always produces findings, so neither rule had a fixed point. Both were amended in `d0837aa7`:

- Findings at CRITICAL or HIGH must be fixed or disproved. MEDIUM and below are logged to BACKLOG and do not block execution.
- Execution begins when no CRITICAL or HIGH finding is open, or after 2 review rounds, whichever comes first. A third round is not permitted; it escalates to Sean.

The 51 archived native plans are seven repeating generations of the same seven steps (fix → re-seal → cross-audit → dispatch → post-native review → re-prep → authorize), each re-planned against a successive candidate SHA. They are **history, not a queue**. Phase 20A replans from its own Success Criteria; it does not resume that chain.

## Do not

- Create `20-76` or any new Phase-20 plan.
- Re-plan, re-architect, or re-scope Phase 20.
- Open a new review, audit, or council round.
- Resume the archived native chain plan-by-plan.
- Fix the two open MEDIUM findings — they are BACKLOG under the amended rule.

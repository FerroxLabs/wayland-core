---
context: phase
phase: 20-transactional-delegated-mutation
live_plans: 18
completed_plans: 18
remaining_plans: 0
next_plan: null
status: complete
completed_at_sha: 01a5b0ae459c9d5088cfd7e41271a5d4ece1b9bb
last_updated: 2026-07-25
---

# Phase 20 — Transactional Delegated Mutation — **COMPLETE**

**Phase 20 is complete.** The terminal plan `20-56` closed green on 2026-07-25 at exact SHA
`01a5b0ae459c9d5088cfd7e41271a5d4ece1b9bb` (tree `4a5247ca804a88c5fc621402d5e55a3dab10e8a5`,
branch `plan/f20-unified-audit-repair`), proved on `hetzner-dsm:/root/wayland` with a clean
working tree:

```
cargo build --locked --workspace --all-features   → exit 0
cargo nextest run --profile ci --no-fail-fast     → exit 0
Summary [ 194.331s] 11519 tests run: 11519 passed (1 slow, 3 flaky), 48 skipped
```

Zero failed, zero timed out. `F20-01`…`F20-06`, `F20-GATE-01`, `F20-GATE-02` are all ticked in
`.planning/REQUIREMENTS.md`, bound to that one SHA. Full evidence, the two repairs, and the
superseded RED disposition are in `20-56-SUMMARY.md` (see its `# Closeout 2026-07-25` section)
with logs in `20-56-evidence/`.

## Scope boundary — what is NOT claimed here

**Native Windows/macOS is Phase 20A and is explicitly out of scope of this completion.** No native
proof was run, claimed, dispatched, or waited on. The twelve `REQ-native-r1 … r12` requirements
remain open and untouched in `REQUIREMENTS.md`. Success Criterion 3's native clause is Phase 20A's
to close.

## Standing constraints (still true)

- The primary checkout `/Users/seandonahoe/dev/waylandcore` is heavily dirty. Phase 20 work
  happened in `/Users/seandonahoe/dev/waylandcore-ferrox`.
- Cargo never runs on this Mac. `cargo fmt --all` is the only working cargo command here; the
  Linux host `hetzner-dsm:/root/wayland` is the only place the workspace compiles.
- No push to `main`, merge, PR, tag, release, or issue closure without Sean's explicit
  authorization. The only remote write made was pushing the work branch to `gh` so the proof host
  could fetch the exact SHA.

## Open, deliberately not chased

Logged so the next phase does not rediscover them; neither blocks Phase 20:

- `packaged_f04_run_is_repeatable_and_content_addressed` burns ~185s of single-core CPU for two
  packaged runs that each finish in under 2s. It passes, and it now has a measured 480s budget,
  but that cost is unexplained and deserves its own investigation.
- The `default` nextest profile still runs that test on the 30s × 2 budget; only `[profile.ci]`
  was given the override.
- Three tests remain flaky-but-passing (`dangerous_expiry_reaches_bootstrapped_spawn_child`,
  `worktree::tests::linux::status_output_cap_kills_git_descendant`,
  `packaged_core_cancels_an_active_stream`), down from five.
- Two MEDIUM findings remain in BACKLOG under the amended termination rule (`d0837aa7`).

## Do not

- Create `20-76` or any new Phase-20 plan.
- Re-plan, re-architect, or re-scope Phase 20.
- Open a new review, audit, or council round on Phase 20.
- Resume the archived native chain plan-by-plan — the 51 plans in `archive/native-split/` are
  history, not a queue. Phase 20A replans from its own Success Criteria.

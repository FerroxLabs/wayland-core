---
phase: 20-transactional-delegated-mutation
plan: "45"
type: execute
wave: 44
depends_on:
  - "20-44"
files_modified:
  - .planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md
autonomous: true
requirements:
  - REQ-native-r12
  - REQ-native-r13
must_haves:
  truths:
    - "Kimi K3 — the designated independent cross-auditor (`/Users/seandonahoe/.kimi-code/bin/kimi -p \"…\" --output-format text`, absolute path) — cross-audits the exact sealed third-repaired-successor SHA (recorded by 20-44) at every severity across the 20-43 repair delta: the PRODUCTION #A verbatim-path/env-transport probe fix in wcore-swarm, the PRODUCTION #B cmd.exe /c|/k payload-quoting fix in wcore-sandbox, the Job-Object counting-scope fix, and the lib-test compile-debt imports — BEFORE any Sean-gated native run is re-spent (REQ-native-r12 pre-native gate)."
    - "Kimi K3's findings are captured as a schema-validated `f20-native-crossaudit` artifact, AND a ferrox non-author reviewer emits its own schema-validated `wayland-core.phase20-independent-review.v1` artifact for the schema; no prose-only review counts toward the PASS — sustaining the 20-24/20-31/20-38 attestation discipline (REQ-native-r13)."
    - "The cross-audit adversarially prosecutes the two isolation guardrails: (a) #A did NOT silently defeat the DispatchAdmission budget (no catch-and-default-to-unlimited), and (b) #B is a quoting-layer change only (argv discipline kept; is_unc_or_device_path, the Low-IL token, the Job-Object limits, and the ACL lease untouched; no shell-string interpolation). It also verifies the Kimi watch-items are baked into the 20-46 re-dispatch checks. native_macos/native_windows are the ONLY deferred checks (proven at 20-46); code review, all-severity, evidence integrity, and integration authority are prosecuted here with zero findings or the sequence stops for a further repaired successor (REQ-native-r12/r13)."
    - "Fail-closed: if Kimi K3 is unreachable (e.g. Moonshot 429 / kimi-code key 401 per the cross-auditor status note), the gate BLOCKS and records an explicit incomplete disposition rather than emitting a false-green PASS from a missing or single-reviewer audit (mirrors CONTEXT D5 fail-safe)."
  artifacts:
    - path: .planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md
      provides: "Kimi K3 schema-validated f20-native-crossaudit of the sealed third-repaired-successor repair delta, plus the ferrox non-author schema-validated review artifact"
  key_links:
    - from: .planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md
      to: .planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md
      via: "source/review-base/review authority tuple over the sealed third-repaired-successor SHA"
      pattern: "source_sha"
---

<objective>
Cross-audit the exact sealed third-repaired-successor before re-spending a Sean-gated native run: Kimi K3 — the designated non-author cross-auditor — prosecutes the 20-43 repair delta at every severity and records a schema-validated `f20-native-crossaudit` artifact, alongside a ferrox non-author schema reviewer, native explicitly deferred to 20-46.

Purpose: The gate sequence puts an adversarial cross-audit before the native proof so a broken candidate does not consume the scarce native run; and R13 requires per-reviewer schema-validated artifacts, not prose. This is the pre-native attestation gate for the third repaired successor. The 20-43 delta changes PRODUCTION code in two security-relevant places (the DispatchAdmission capacity probe in wcore-swarm, and the AppContainer cmd.exe command-line construction in wcore-sandbox), so the audit focuses adversarially on whether #A preserved the admission budget (no catch-to-unlimited) and whether #B stayed a pure quoting-layer change (argv discipline; no relaxation of is_unc_or_device_path, the Low-IL token, the Job-Object limits, or the ACL lease).
Output: One Kimi K3 schema-validated `f20-native-crossaudit` artifact plus one ferrox schema-validated review artifact over the sealed successor SHA; zero findings admits the native-proof gate. If Kimi K3 is unreachable, an explicit incomplete disposition (no false green).
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/20-transactional-delegated-mutation/20-CONTEXT.md
@.planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md
@.planning/phases/20-transactional-delegated-mutation/20-43-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md
@.planning/phases/20-transactional-delegated-mutation/20-31-CROSS-AUDIT.md
</context>

<execution_rules>
- The cross-auditor is Kimi K3, invoked at its ABSOLUTE path `/Users/seandonahoe/.kimi-code/bin/kimi -p "<prompt>" --output-format text` (see the [[cross-auditor-kimi-k3]] project memory). Kimi K3 is the independent (non-author) reviewer of the sealed successor; the ferrox reviewer is a SECOND non-author reviewer, not the plan author.
- Every claimed reviewer MUST leave its OWN on-disk schema-validated artifact. Kimi K3's findings are recorded as the `f20-native-crossaudit` artifact; the ferrox reviewer emits `wayland-core.phase20-independent-review.v1`. A prose-only review does not count toward PASS (REQ-native-r13).
- Review the EXACT sealed successor SHA/tree recorded by 20-44 (the review base is that tuple; do not review the branch tip if it has advanced).
- native_macos and native_windows are the ONLY checks that may be deferred (they are proven at 20-46). All other checks — code review of the #A/#B production changes, all-severity findings, evidence integrity, integration authority — are prosecuted here.
- FAIL-CLOSED: if Kimi K3 cannot be reached or its output cannot be schema-validated, record an explicit incomplete disposition and BLOCK — do not synthesize a PASS from the ferrox reviewer alone or from prose (CONTEXT D5 fail-safe). Do not modify any source/test/workflow file; this plan writes only the cross-audit artifact.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Kimi K3 pre-native cross-audit of the sealed successor (+ ferrox schema reviewer), fail-closed</name>
  <files>.planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md</files>
  <read_first>.planning/phases/20-transactional-delegated-mutation/20-44-SUMMARY.md, .planning/phases/20-transactional-delegated-mutation/20-43-SUMMARY.md, .planning/intel/inbox-2026-07-24/KIMI-K3-CROSS-AUDIT-windows-native.md, .planning/phases/20-transactional-delegated-mutation/20-31-CROSS-AUDIT.md</read_first>
  <behavior>
    - Invoke Kimi K3 at its absolute path as the independent non-author cross-auditor of the exact sealed third-repaired-successor SHA/tree. Scope the prompt to the 20-43 repair delta: #A (wcore-swarm dunce::simplified de-verbatimization + WCORE_SWARM_PROBE_ROOT env transport, DispatchAdmission budget), #B (wcore-sandbox cmd.exe /c|/k payload quoting), the Job-Object counting-scope fix, and the lib-test compile-debt imports.
    - The audit prosecutes adversarially: does #A preserve the admission budget (NO error-swallow-to-unlimited)? Is #B a quoting-layer change only (argv discipline kept; is_unc_or_device_path, Low-IL token, Job-Object limits, ACL lease untouched; no shell-string interpolation)? Does the counting-scope fix keep every containment assertion? Are the Kimi watch-items (git ops with de-verbatimized swarm_root; tagged_cmd_count CIM visibility under NetworkService; the bash worker under AppContainer + no-Windows-Docker-fallback; worker test-exe DLL-load under Low-IL) explicitly carried as 20-46 re-dispatch checks rather than silently dropped?
    - Capture Kimi K3's findings as the schema-validated `f20-native-crossaudit` artifact and have a ferrox non-author reviewer independently emit `wayland-core.phase20-independent-review.v1` over the same sealed SHA. native_macos/native_windows are deferred (empty/deferred), proven at 20-46.
    - Fail-closed: if Kimi K3 is unreachable (429/401 per the cross-auditor status note) or its output fails schema validation, record an explicit incomplete disposition and block — no false-green PASS.
  </behavior>
  <action>Run Kimi K3 (`/Users/seandonahoe/.kimi-code/bin/kimi -p "<prompt>" --output-format text`) as the independent non-author cross-auditor over the exact sealed successor SHA/tree recorded by 20-44, scoping it to the 20-43 repair delta and the two isolation guardrails (#A admission budget preserved; #B quoting-layer only). Record its findings as the schema-validated `f20-native-crossaudit` artifact in `20-45-CROSS-AUDIT.md`, and have a ferrox non-author reviewer independently emit its own schema-validated `wayland-core.phase20-independent-review.v1` over the same SHA (native_macos/native_windows deferred). Verify the Kimi watch-items are recorded as explicit 20-46 re-dispatch checks. If Kimi K3 is unreachable or its output fails schema validation, record an explicit incomplete disposition and BLOCK — do not synthesize a PASS. Implements REQ-native-r12/r13; write only the cross-audit artifact.</action>
  <verify>
    <automated>test -f .planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md</automated>
    <automated>grep -nE "f20-native-crossaudit|phase20-independent-review|source_sha|kimi|Kimi K3|deferred" .planning/phases/20-transactional-delegated-mutation/20-45-CROSS-AUDIT.md | head</automated>
  </verify>
  <acceptance_criteria>The cross-audit artifact exists and records BOTH a Kimi K3 `f20-native-crossaudit` result and a ferrox `wayland-core.phase20-independent-review.v1` result over the exact sealed SHA, with native_macos/native_windows deferred and the Kimi watch-items carried as 20-46 checks. A zero-finding PASS from both reviewers admits 20-46; any finding, or Kimi K3 unreachable/invalid, blocks with an explicit incomplete disposition.</acceptance_criteria>
  <done>The sealed third-repaired successor has a per-reviewer schema-validated pre-native cross-audit (Kimi K3 + ferrox), or an explicit fail-closed block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native run authorization ← cross-audit PASS | The scarce Sean-gated native run is only re-spent after this adversarial cross-audit passes; a false-green here wastes the run |
| attestation integrity ← per-reviewer artifact | A PASS must be backed by an on-disk schema-validated artifact per claimed reviewer, not prose |
| reviewer independence ← Kimi K3 reachability | If Kimi K3 is down, the audit must fail closed rather than pass on a single reviewer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-45-01 | Spoofing | prose-only or single-reviewer PASS | high | mitigate | Every claimed reviewer emits its own schema-validated artifact (Kimi K3 `f20-native-crossaudit` + ferrox `wayland-core.phase20-independent-review.v1`); a prose-only or missing artifact does not count toward PASS. |
| T-20-45-02 | Repudiation | Kimi K3 unreachable false-green | high | mitigate | Fail-closed: an unreachable or schema-invalid Kimi K3 output records an explicit incomplete disposition and blocks; no PASS is synthesized from the ferrox reviewer alone (CONTEXT D5 fail-safe). |
| T-20-45-03 | Tampering | isolation-guardrail regression slipping through | high | mitigate | The audit adversarially prosecutes #A (admission budget preserved) and #B (quoting-layer only; boundary primitives untouched) and requires the Kimi watch-items be carried as 20-46 checks, so a boundary-weakening change cannot pass silently. |
</threat_model>

<verification>
The cross-audit artifact `20-45-CROSS-AUDIT.md` determines acceptance: it must record a Kimi K3 `f20-native-crossaudit` result AND a ferrox `wayland-core.phase20-independent-review.v1` result over the exact sealed successor SHA, both zero-finding, with native_macos/native_windows deferred and the Kimi watch-items carried as explicit 20-46 re-dispatch checks. Record the sealed `source_sha`/tree, the two reviewer identities, the Kimi K3 invocation, both artifacts' schema-validation status, and the disposition (PASS admits 20-46; any finding or an unreachable/invalid Kimi K3 blocks with an explicit incomplete disposition) in `20-45-SUMMARY.md`. No source/test/workflow change; no native or requirement claim.
</verification>

<success_criteria>
- Kimi K3 cross-audits the exact sealed successor at every severity across the 20-43 delta and records a schema-validated `f20-native-crossaudit` artifact; a ferrox non-author reviewer emits its own schema-validated artifact (r12/r13).
- The two isolation guardrails (#A admission budget; #B quoting-layer only) are adversarially prosecuted and the Kimi watch-items are carried as 20-46 checks; native is the only deferred check.
- A zero-finding PASS from both reviewers admits 20-46; any finding, or an unreachable/schema-invalid Kimi K3, blocks with an explicit incomplete disposition (no false green).
</success_criteria>

## Artifacts this phase produces
- The pre-native Kimi K3 `f20-native-crossaudit` plus the ferrox schema-validated review of the sealed third-repaired-successor repair delta.

<output>
Create `.planning/phases/20-transactional-delegated-mutation/20-45-SUMMARY.md` using the standard GSD summary template. Record the sealed `source_sha`/tree, the Kimi K3 + ferrox reviewer identities and their schema-validation status, the two isolation-guardrail verdicts, the carried 20-46 watch-items, and the disposition (PASS or fail-closed incomplete). Complete no Phase 20 requirement.
</output>
</content>

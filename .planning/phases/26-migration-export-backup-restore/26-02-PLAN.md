---
phase: 26-migration-export-backup-restore
plan: "02"
type: execute
wave: 2
depends_on:
  - "26-01"
files_modified:
  - crates/wcore-cli/src/migrate/mod.rs
  - crates/wcore-cli/src/migrate/quarantine.rs
  - crates/wcore-cli/src/migrate/provenance.rs
  - crates/wcore-cli/src/migrate/select.rs
  - crates/wcore-cli/src/profile.rs
  - crates/wcore-config/src/profile.rs
  - crates/wcore-cli/tests/migrate_quarantine.rs
  - crates/wcore-cli/tests/fixtures/portability-exec/README.md
  - scripts/portability-promotion-scale.sh
  - .planning/phases/26-migration-export-backup-restore/panel/26-02-containment-contract/
requirements:
  - F26-02
autonomous: true
must_haves:
  truths:
    - "THE IMPORTABLE SURFACE IS DOMINATED BY EXECUTABLE CONTENT, MEASURED ON REAL STATE. The shipped binary run against Sean's real Hermes home reports **540 skill directories** and **12 `SOUL.md` persona files** as detected-but-not-imported. Making them importable is this plan's subject, and skills are the single largest class — so the quarantine contract is not a corner case bolted onto an import, it IS the import."
    - "A SKILL IS EXECUTABLE BY CONSTRUCTION IN THIS CODEBASE, AND THE MECHANISM IS ALREADY IN SOURCE. `crates/wcore-skills/src/shell.rs` exposes `contains_shell_commands(content, loaded_from) -> bool` and `execute_shell_commands`, and `crates/wcore-skills/src/executor.rs` calls the latter on a skill body. `crates/wcore-skills/src/permissions.rs` keys its permission decision off `contains_shell_commands(&skill.content, skill.loaded_from)`. So a `SKILL.md` copied out of a peer install is not inert data — it is a shell payload that Core already knows how to run, and `LoadedFrom` (`Skills`, `CommandsDeprecated`, `Managed`, `Bundled`, `Mcp`) is the existing axis along which its trust is decided."
    - "CORE ALREADY HAS THE RIGHT PRECEDENT FOR THIS AND IT MUST BE FOLLOWED, NOT REINVENTED. GHSA-8r7g established, in `crates/wcore-config/src/hooks.rs`, that foreign executable content is INERT BY DEFAULT and cannot grant itself trust: `trust_project_hooks` defaults to `false`, a `HookDef.command` is executed as a child process, and — the load-bearing half — only the operator's GLOBAL config value is honored, because `merge_config_files` refuses to let a project config trust its own hooks. `crates/wcore-config/src/workspace_trust.rs` carries the matching enforcement shape: a domain-separated SHA-256 over the executable surface, with hard refusals for a symlinked executable file, an oversized file and an oversized surface. Imported peer content is the same threat with a different delivery route, and it gets the same answer."
    - "AN IMPORTED MCP SERVER IS A COMMAND, NOT A SETTING. The Hermes mapper already carries `command`, `args` and `env` from a peer's MCP definition straight into `McpServerConfig` with `TransportType::Stdio`, and a stdio MCP server is launched as a child process. That path exists TODAY and is reached by the existing apply. It is therefore already an execution surface fed by peer-controlled strings, and this plan brings it under the same quarantine as skills and hooks rather than leaving it as the one unguarded door."
    - "PROVENANCE IS WHAT MAKES A MIGRATION REVERSIBLE AND AUDITABLE. An imported item with no record of where it came from cannot be selectively rolled back, cannot be re-synced without guessing, and cannot be judged when its source is later found to be malicious. Every imported item records the source tool, the source version, the source-relative path, a content digest, and when it was imported — and that record is what 26-03's rollback and 26-04's conflict corpora both read."
    - "THE INERTNESS PROOF MUST HAVE A POSITIVE CONTROL OR IT PROVES NOTHING. Asserting that a quarantined skill's side effect is absent passes vacuously if the skill was never loaded, never discovered, or silently dropped on a parse error. The only way the assertion carries information is to prove the SAME payload DOES produce its side effect once promoted. Absent-without-a-positive-control is the exact self-passing gate shape this phase forbids."
    - "SELECTIVE MEANS THE USER CHOOSES, NOT THAT THE IMPORTER OMITS. Narrowing an import to a chosen set is a user instruction. Quietly dropping an item the user did not exclude — because it was awkward, large, or unfamiliar — is data loss reported as success. Every discovered item ends in exactly one of three states: imported, quarantined, or explicitly excluded by the user, and the three counts must sum to the discovered total."
    - "THE EXISTING COUNT CEILING IS ALREADY SMALLER THAN A REAL INSTALL, AND THE NUMBERS ARE MEASURED. `crates/wcore-config/src/workspace_trust.rs` declares `MAX_EXECUTABLE_FILES: usize = 512`, `MAX_EXECUTABLE_FILE_BYTES: u64 = 4 * 1024 * 1024` and `MAX_EXECUTABLE_TOTAL_BYTES: u64 = 32 * 1024 * 1024`. Sean's real Hermes home carries **540 skill directories**. So mirroring the existing ceilings does not merely constrain a hypothetical import — 540 exceeds 512, and a realistic import is refused by the limit as it stands today. That collision is a FINDING to record with a severity, and raising 512 to admit the import is expressly forbidden: it would widen the executable-surface bound for every workspace-trust consumer to solve a migration problem. The decision leg measures the collision rather than discovering it by eye, and a gate pins all three constants so the limit cannot be quietly moved."
    - "A REPORTED RED IS WORTH FAR MORE THAN AN ENGINEERED GREEN. Never weaken an assertion, `#[ignore]`, `#[allow]`, raise a timeout, re-gate, or delete an inconvenient test to reach a gate."
  artifacts:
    - path: crates/wcore-cli/src/migrate/quarantine.rs
      provides: "The classification and containment of imported executable content — inert on arrival, promotable only by an explicit operator action that the imported content itself cannot perform"
    - path: crates/wcore-cli/src/migrate/provenance.rs
      provides: "The per-item record of source tool, source version, source path, content digest and import time that makes an import auditable and selectively reversible"
    - path: crates/wcore-cli/src/migrate/select.rs
      provides: "Selection by item identity, with the invariant that imported plus quarantined plus excluded equals discovered"
    - path: crates/wcore-cli/tests/fixtures/portability-exec/
      provides: "Executable-content fixtures whose payload writes an observable sentinel, so inertness can be proven by absence AND by a positive control"
    - path: crates/wcore-cli/src/profile.rs
      provides: "The export side of the selective, provenance-preserving portable corpus"
    - path: scripts/portability-promotion-scale.sh
      provides: "The measurement that turns 'would an operator tolerate this at 540 items?' from an opinion into a number — the four counts and their sum, the operator invocations a realistic promotion actually costs, which data kinds were swept into quarantine, which executable kinds escaped it, and whether the existing 512-file executable ceiling refuses a realistic 540-directory import"
  key_links:
    - from: crates/wcore-cli/src/migrate/quarantine.rs
      to: crates/wcore-skills/src/shell.rs
      via: "the existing shell-directive detector classifies an imported skill as executable; quarantine decides whether it may run"
      pattern: "reuse-existing-detector"
    - from: crates/wcore-cli/src/migrate/quarantine.rs
      to: crates/wcore-config/src/hooks.rs
      via: "the GHSA-8r7g contract — inert by default, and the foreign content cannot grant itself trust"
      pattern: "established-precedent"
    - from: crates/wcore-cli/src/migrate/provenance.rs
      to: crates/wcore-cli/src/migrate/select.rs
      via: "item identity is the provenance key, so selection, quarantine and later rollback all address the same item the same way"
      pattern: "shared-identity"
---

<objective>
Make import and export selective, provenance-preserving, and safe against the executable content that dominates a real peer install — proven by a payload that demonstrably CANNOT run while quarantined and demonstrably CAN once promoted.

Purpose: Success Criterion 2 asks for two things that pull against each other. Selective import must move persona, memory, skills, settings, assets, profiles and credentials — and on Sean's real install that is 540 skill directories and 12 personas, the large majority of which are executable payloads Core already knows how to run. Quarantine must make those inert on arrival. Core already solved this exact problem once, under GHSA-8r7g, for project-defined hooks: inert by default, and the foreign thing cannot trust itself. This plan applies that settled contract to imported peer state instead of inventing a second, weaker one.
Output: A classification and containment path for imported executable content; a provenance record per imported item; selection by item identity with a conservation invariant; the export half of the portable corpus; and a live proof on Linux that a real peer skill payload does not execute while quarantined and does execute once promoted.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md
@crates/wcore-cli/src/migrate/mod.rs
@crates/wcore-cli/src/migrate/hermes.rs
@crates/wcore-config/src/hooks.rs
@crates/wcore-config/src/workspace_trust.rs
@crates/wcore-skills/src/shell.rs
@crates/wcore-skills/src/permissions.rs
@crates/wcore-config/src/profile.rs
</context>

<execution_rules>

**THE TWO PHASE RULES — stated verbatim, and they bound this plan.**

- Findings at CRITICAL or HIGH must be fixed or disproved. MEDIUM and below are logged to `.planning/BACKLOG.md` and DO NOT BLOCK execution.
- Execution begins when no CRITICAL or HIGH finding is open, or after 2 review rounds, whichever comes first. A third round is NOT permitted; it escalates to Sean.

**TERMINATION CRITERION FOR THIS PLAN (hard).** This plan classifies ONCE, contains ONCE, and proves ONCE. It terminates in exactly one of three states, and in all three it writes its SUMMARY and stops:
1. **Complete** — imported executable content is inert on arrival and promotable only by explicit operator action, provenance is recorded per item, selection conserves every discovered item, and the paired inertness proof passes with its positive control.
2. **Blocked on an unsafe promotion contract** — no promotion mechanism can be built that the imported content cannot itself influence. STOP, record why, and escalate; shipping import without a safe promotion path is worse than shipping neither.
3. **Escalated** — a CRITICAL or HIGH finding is open that cannot be fixed inside this plan's declared files.
Under no circumstances does this plan spawn additional plans, add backup or restore behavior, or begin the hostile corpora.

**SCOPE BOUNDARY (hard).** This plan owns selective import/export, quarantine and provenance. Discovery and the typed plan model are 26-01's and are CONSUMED here, not modified in shape. Backup, restore, the operation journal and interruption survival are 26-03's — if a rollback need surfaces here, record the requirement for 26-03 and stop rather than building a second journal. Hostile corpora are 26-04's.

**DO NOT WEAKEN THE GHSA-8r7g CONTRACT.** `trust_project_hooks` stays default-false, and the rule that only the operator's GLOBAL config value is honored — so a project config cannot trust its own hooks — is not relaxed, widened, or routed around by this plan. Imported content must not become a new way to reach the execution that GHSA-8r7g closed. If quarantine appears to require loosening it, that is termination state 2.

**NO NEW DEPENDENCY.** `sha2`, `serde`, `serde_json`, `toml`, `tempfile`, `dirs`, `anyhow` are already in the workspace. Adding an external dependency is out of scope; escalate rather than installing.

**SECRETS NEVER LEAVE THE MAC.** Sean's real `~/.hermes` and `~/.openclaw` are read on the Mac and nowhere else. Nothing from either tree is copied to `hetzner-dsm`, to `seandesktop`, into the repository, or into a planning artifact. Use 26-01's committed canary corpora for every automated gate.

**ENVIRONMENT.**
- **Mac (this host, `arm64`)** — CANNOT compile; never run `cargo build`/`test`/`check` here. `cargo fmt --all` works and is the local format gate. Use `/usr/bin/git` and ALWAYS `/usr/bin/grep`; prefer absolute `/usr/bin/` paths for `find`, `sed`, `diff` and `wc` in gates too, because `grep`, `find` and `ls` on this host resolve to an rtk proxy that silently drops or rewrites lines.
- **Linux — `ssh -o BatchMode=yes hetzner-dsm`** — the authoritative build and gate host, and where the live inertness proof runs. This plan works ONLY in its own worktree `/root/wayland-f26-02` and NEVER checks out the shared `/root/wayland`; see the remote gate shape below.
- Push the WORK BRANCH to `gh` so Hetzner can fetch it. NO push to main, merge, PR, tag, release, or issue closure without Sean.

**THE REMOTE GATE SHAPE — the wave-2 race is REAL, and two measured host facts close it.**

**This plan and 26-03 are both `wave: 2` and both depend only on 26-01, so they run CONCURRENTLY.** Each computes its own SHA from its own working-tree HEAD. If both detached the SAME `/root/wayland` tree, each would move it out from under the other's in-flight build, and the resulting green or red would not correspond to the SHA the plan claims — a proof that silently certifies the wrong commit. This is not hypothetical: the 20A handoff records that "concurrent compile load on one shared box corrupted a proof run" and prescribes the fix — "Give each concurrent agent its **own worktree** per host." `ci.yml`'s concurrency comment records the same expectation, that phases 24-27 execute in bounded parallel worktrees, so a THIRD phase can be on that box too.

1. **Per-plan worktree.** `/root/wayland` is used ONLY as the object store to fetch into, under an `flock` so two concurrent fetches cannot race on its refs. All work for this plan happens in `/root/wayland-f26-02`, which 26-03 never touches. Measured: this is already the box's own convention — `git worktree list` there shows `/root/wayland` plus twelve sibling worktrees — and there is 647 GB free with 96 cores and 251 GB RAM, so two concurrent worktree builds are well within the box.
2. **`cargo` is NOT on `PATH` in a non-login `ssh` shell on that host.** Measured: `ssh hetzner-dsm 'cargo --version'` returns `bash: cargo: command not found`; the shell's `PATH` ends at `/snap/bin` with no `/root/.cargo/bin`. The toolchain IS present at `/root/.cargo/bin` (`cargo` 1.95.0 under this tree's pinned toolchain, `cargo-nextest` 0.9.137, `clippy` 0.1.95). Every remote command prepends it. Without that, every Linux gate here is GUARANTEED RED for a reason unrelated to the code — as useless as a gate that cannot go red.
3. **Isolation prevents; the SHA assertion DETECTS.** Every remote gate asserts the worktree is at this plan's SHA immediately before the work and again immediately after it, so anything that moved the tree mid-flight turns the gate RED instead of yielding a green that does not match its claimed SHA.

The shape, used by every Linux gate in this plan:

`SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-02; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2>/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; «WORK»; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA" > «LOG» 2>&1; rc=$?; tail -30 «LOG»; test $rc -eq 0`

`set -e` makes any step's failure the ssh exit status; the status is captured on its own line and tested BEFORE the log is read; no filter ever sits between the command and its status. After this plan's last remote gate passes, remove the worktree from `/root/wayland` with `git worktree remove --force /root/wayland-f26-02 && git worktree prune`. A FAILED gate leaves it for forensics.

**THE MEASUREMENT TRAPS (do not simplify these away).**
1. **Mac `grep` is rtk-proxied and SILENTLY DROPS LINES.** Every gate invokes `/usr/bin/grep` explicitly and uses `-F` for literals.
2. **A pipe replaces the exit status you care about.** Every remote gate redirects output to a file, captures the return code on its own line, and tests it BEFORE reading the file. Never place a filter between a gate command and its status.
3. **A sentinel-absence assertion is vacuous without a positive control.** Every inertness assertion in this plan is paired with a promoted-and-it-fires assertion using the SAME payload.

**AGENTS.md discipline.** Surgical diffs; every changed line traces to this plan. `thiserror` for the public quarantine/provenance error types, `anyhow` for CLI propagation, no `unwrap()` in production paths. Every new file under 1000 lines. `cargo clippy` clean, `cargo fmt` clean.

**Git hygiene.** `/usr/bin/git` on the Mac. Stage the exact paths in `files_modified`, never `-A`, never `.`. No `Co-Authored-By` trailers.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Classify and contain imported executable content, following the GHSA-8r7g contract rather than inventing a second one</name>
  <files>crates/wcore-cli/src/migrate/quarantine.rs, crates/wcore-cli/src/migrate/mod.rs, crates/wcore-cli/tests/fixtures/portability-exec/README.md, crates/wcore-cli/tests/migrate_quarantine.rs</files>
  <read_first>crates/wcore-config/src/hooks.rs (the GHSA-8r7g comment and the `trust_project_hooks` default — note specifically that only the operator's GLOBAL value is honored and why a project cannot self-trust), crates/wcore-config/src/workspace_trust.rs (`fingerprint_workspace`: the domain-separated hash prefix, the symlinked-executable refusal, and the per-file/total/count ceilings — these are the enforcement shapes to mirror), crates/wcore-skills/src/shell.rs (`contains_shell_commands` and its `LoadedFrom` exemption for MCP-loaded content), crates/wcore-skills/src/permissions.rs (how an executable skill's permission decision is currently made), crates/wcore-skills/src/types.rs (the `LoadedFrom` variants), crates/wcore-cli/src/migrate/hermes.rs (how a peer MCP definition's command and args already flow into a stdio server config)</read_first>
  <behavior>
    - Test 1: an imported skill whose body carries a shell directive is classified executable, using the EXISTING detector rather than a second pattern list.
    - Test 2: an imported skill with no directive is classified non-executable and is importable without promotion.
    - Test 3: an imported peer MCP definition carrying a launch command is classified executable, because a stdio server is a child process.
    - Test 4: an imported hook definition carrying a command is classified executable.
    - Test 5: quarantined content lands somewhere the normal discovery path does not load from, and a full agent-facing skill enumeration does not include it.
    - Test 6: promotion requires an explicit operator action; nothing inside the imported content — no frontmatter field, no config key, no manifest entry it carries — can cause its own promotion.
    - Test 7: the enforcement ceilings hold: a symlinked executable file is refused, an oversized file is refused, and an oversized total surface is refused, mirroring the existing workspace-trust refusals rather than inventing new limits.
    - Test 8: quarantining is reported, not silent — the plan and the apply report both name how many items were quarantined and why.
  </behavior>
  <action>Write the tests first, from the behaviors above, and watch them fail before implementing.

Start by reading the settled contract rather than designing a new one. GHSA-8r7g decided this exact question for project-defined hooks: foreign executable content is inert by default, and the foreign artifact cannot grant itself trust because only the operator's global configuration value is consulted. Mirror both halves. The first half is easy to remember and the second is the one that actually holds: whatever promotion mechanism you build, it must be impossible for an imported file to promote itself by carrying a field, a key, a manifest entry, or a filename that the promotion path reads.

Classify using what already exists. An imported skill body is executable exactly when the skills crate's existing shell-directive detector says so — call that function, do not write a second pattern list that will drift from it, and respect its existing exemption for MCP-loaded content. An imported peer MCP definition is executable when it carries a launch command, because the Hermes mapper already routes such a definition into a stdio server config and a stdio server is launched as a child process; that door is open today and this plan closes it under the same policy as skills. An imported hook definition carrying a command is executable for the reason GHSA-8r7g already gives in source. Personas, memory notes, settings and assets are data; classify them as such and let them import without ceremony, because treating everything as dangerous trains an operator to promote everything without reading it.

Contain by placement, not by a flag. Quarantined content must land where the normal load path does not look, so that inertness is a property of where the bytes are rather than of a boolean somebody can flip or forget to check. Prove that with a test that enumerates what the agent would actually load and asserts the quarantined item is absent from it.

Bound the surface the way this codebase already bounds it. The workspace-trust module refuses a symlinked executable file, a file over its size ceiling, and a surface over its total and count ceilings, and it hashes with a domain-separated prefix. Mirror those refusals for imported content and reuse the same ceilings rather than inventing looser ones; if a ceiling genuinely must differ for imports, justify it in source where a maintainer will see it. Note that a real install carries 540 skill directories, so the count ceiling is a real constraint here, not a theoretical one — if the existing ceiling refuses a realistic import, that is a finding to record with its severity, not a limit to quietly raise.

Report what you did. The dry-run plan and the apply report must both state how many items were quarantined and what made each executable, because a containment the user never learns about will be discovered later as missing functionality and worked around.

Records evidence for F26-02; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -s crates/wcore-cli/src/migrate/quarantine.rs &amp;&amp; test "$(/usr/bin/grep -vE '^[[:space:]]*(//|/\*|\*)' crates/wcore-cli/src/migrate/quarantine.rs | /usr/bin/grep -cF 'contains_shell_commands')" -ge "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; H=crates/wcore-config/src/hooks.rs; Q=crates/wcore-cli/tests/migrate_quarantine.rs; FX=crates/wcore-cli/tests/fixtures/portability-exec/README.md; echo "REGRESSION-GUARD LEGS - these assert PRE-EXISTING strings and are NOT completion coverage:"; test "$(/usr/bin/grep -cE '^[[:space:]]*pub trust_project_hooks: bool,' "$H")" -ge "1" || { echo "GUARD: the GHSA-8r7g opt-in field is gone from hooks.rs"; exit 1; }; test "$(/usr/bin/grep -cE '^[[:space:]]*trust_project_hooks: false,' "$H")" -ge "1" || { echo "GUARD: trust_project_hooks no longer DEFAULTS TO FALSE - the load-bearing half of GHSA-8r7g has been weakened"; exit 1; }; test "$(/usr/bin/grep -cE '^[[:space:]]*fn default_dispatch_enabled' "$H")" -ge "1" || { echo "GUARD: default_dispatch_enabled is gone from hooks.rs"; exit 1; }; echo "COMPLETION LEGS - every one of these is FALSE before this task runs:"; test -s "$Q" || { echo "GATE: the quarantine suite $Q does not exist, so none of this task's eight behaviors were written"; exit 1; }; TC=$(/usr/bin/grep -cE '^[[:space:]]*#\[(test|tokio::test)' "$Q"); test "$TC" -ge 8 || { echo "GATE: $Q declares $TC tests - this task states EIGHT behaviors, so fewer than eight means behaviors are unwritten"; exit 1; }; test -s "$FX" || { echo "GATE: the executable-content fixture $FX does not exist, so the classification legs have no realistic payload to classify"; exit 1; }; echo "guard=intact quarantine_tests=$TC"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; for f in crates/wcore-cli/src/migrate/quarantine.rs crates/wcore-cli/src/migrate/mod.rs; do n=$(/usr/bin/wc -l &lt; "$f" | tr -d ' '); test "$n" -lt 1000 || { echo "OVER 1000 LINES: $f ($n)"; exit 1; }; done</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-02; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2&gt;/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; cargo clippy --locked -p wcore-cli --all-targets -- -D warnings; cargo nextest run --locked -p wcore-cli --no-fail-fast; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA" &gt; /tmp/26-02-t1.log 2&gt;&amp;1; rc=$?; tail -30 /tmp/26-02-t1.log; test $rc -eq 0</automated>
  </verify>
  <done>All eight behaviors exist as tests written before the implementation and observed to fail first. Classification calls the skills crate's existing shell-directive detector rather than a second pattern list, and covers skills, peer MCP launch commands and hook commands. Quarantined content is inert by PLACEMENT, proven by an enumeration test that shows it absent from what the agent would load. Promotion cannot be triggered by anything the imported content carries. The existing workspace-trust refusals and ceilings are mirrored rather than loosened, and any ceiling that refuses a realistic 540-directory import is recorded as a finding with its severity rather than raised. Quarantine counts and reasons appear in both the plan and the apply report. GHSA-8r7g's default and its no-self-trust rule are gate-checked intact. Linux clippy at deny-warnings and the crate suite are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record provenance per item and make import and export selective with a conservation invariant</name>
  <files>crates/wcore-cli/src/migrate/provenance.rs, crates/wcore-cli/src/migrate/select.rs, crates/wcore-cli/src/migrate/mod.rs, crates/wcore-cli/src/profile.rs, crates/wcore-config/src/profile.rs, crates/wcore-cli/tests/migrate_quarantine.rs</files>
  <read_first>crates/wcore-config/src/profile.rs (the existing export and import functions, the secret directory and file classification, `copy_tree_filtered`'s symlink refusal, and the existing tests that assert secrets are excluded by default — the export half of this task extends that code and must not weaken those assertions), crates/wcore-cli/src/profile.rs (the existing export and import subcommand surfaces and their flags), .planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md (the typed item vocabulary and item identity that selection and provenance both key off), crates/wcore-config/src/workspace_trust.rs (the domain-separated digest idiom to reuse for the per-item content digest)</read_first>
  <behavior>
    - Test 1: every imported item carries a provenance record naming the source tool, the source version where the source declares one, the source-relative path, a content digest, and the import time.
    - Test 2: the content digest is domain-separated and stable — the same bytes at the same relative path digest identically across runs and across platforms.
    - Test 3: selecting a subset by item identity imports exactly that subset, and the identities are the ones the dry-run plan published, so a user can select from what they previewed.
    - Test 4: excluding a subset imports everything else.
    - Test 5: the conservation invariant holds — imported plus quarantined plus explicitly excluded equals discovered, asserted as a number, for the full committed Hermes corpus and the full committed OpenClaw corpus.
    - Test 6: an item that cannot be imported for a reason other than user exclusion is reported as a named failure, never silently dropped, and the invariant still balances.
    - Test 7: export emits a portable corpus that carries the provenance records, and still excludes secrets by default exactly as the existing tests require.
    - Test 8: export followed by import round-trips the non-secret corpus with provenance preserved, and the round-tripped executable content is still quarantined rather than arriving promoted.
  </behavior>
  <action>Write the tests first, and watch them fail.

Give every item an identity and a provenance record. Identity must be the SAME key the dry-run plan published in 26-01, because a user selects from what they previewed and a later rollback addresses what was imported — three different identity schemes for one item is how selective rollback becomes impossible. The record carries the source tool, the source version where the source declares one, the path relative to the source home, a content digest, and the import time. Digest with the domain-separated idiom the workspace-trust module already uses, so a digest computed here can never be confused with one computed for another purpose, and make it stable across platforms by digesting the relative path in a normalized form rather than the platform's native rendering.

Make selection address items by that identity, supporting both a chosen subset and an excluded subset, and refuse an identity that the plan did not publish rather than silently ignoring it — a typo in a selection that quietly imports nothing is a user telling the tool to do something and being told it succeeded.

Then enforce conservation. Count what was discovered, what was imported, what was quarantined, and what the user explicitly excluded, and assert as a NUMBER that the three outcomes sum to the discovery total. Assert it over the full committed corpora, not a toy fixture, because the whole point is to catch the case where a large real tree loses items in the middle. When an item cannot be imported for a reason other than user exclusion, it is a named failure in the report and it still appears in the arithmetic; an item that vanishes without appearing in any of the four counts is the exact data loss this invariant exists to catch.

Then extend export. The existing profile export already copies a home tree while excluding the secret directory and the credentials files by default, refuses to follow symlinks, and has tests asserting both. Do not weaken any of that. Add the provenance records to the emitted corpus, add selection so an export can be narrowed the same way an import can, and prove a round trip: export a corpus, import it, and assert the provenance survived and that executable content arrived quarantined rather than promoted — because an export/import round trip that launders quarantine is a promotion path the operator never authorized.

Records evidence for F26-02; marks no requirement complete — closure is claimed at the cross-audited decision leg in Task 4.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; M=crates/wcore-cli/src/migrate/mod.rs; test -s crates/wcore-cli/src/migrate/provenance.rs &amp;&amp; test -s crates/wcore-cli/src/migrate/select.rs &amp;&amp; test "$(/usr/bin/grep -cE '^[[:space:]]*(pub[[:space:]]+)?mod[[:space:]]+provenance[[:space:]]*;' "$M")" -ge "1" &amp;&amp; test "$(/usr/bin/grep -cE '^[[:space:]]*(pub[[:space:]]+)?mod[[:space:]]+select[[:space:]]*;' "$M")" -ge "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; P=crates/wcore-config/src/profile.rs; BASE_BLOB=3b77f83ee4a899df120113061af778e370a5f494; echo "REGRESSION-GUARD LEGS - these assert PRE-EXISTING declarations and are NOT completion coverage:"; test "$(/usr/bin/grep -cE '^const SECRET_DIR_NAMES' "$P")" -ge "1" || { echo "GUARD: SECRET_DIR_NAMES declaration gone from profile.rs"; exit 1; }; test "$(/usr/bin/grep -cE '^const SECRET_FILE_PREFIX' "$P")" -ge "1" || { echo "GUARD: SECRET_FILE_PREFIX declaration gone from profile.rs"; exit 1; }; echo "COMPLETION LEG - FALSE before this task runs, because the blob below is the file as measured at plan time:"; NOW=$(/usr/bin/git hash-object "$P"); echo "profile_blob=$NOW base_blob=$BASE_BLOB"; test "$NOW" != "$BASE_BLOB" || { echo "GATE: profile.rs is byte-identical to the pre-task blob measured at plan time - this task declares profile.rs among the files it MODIFIES to carry provenance and selection into export, so an unchanged file means the export half was never done"; exit 1; }</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; Q=crates/wcore-cli/tests/migrate_quarantine.rs; echo "REGRESSION-GUARD LEG - this asserts a PRE-EXISTING test and is NOT completion coverage:"; test "$(/usr/bin/grep -cE '^[[:space:]]*fn export_excludes_secrets_by_default' crates/wcore-config/src/profile.rs)" -ge "1" || { echo "GUARD: the existing default-exclusion test has been renamed or deleted"; exit 1; }; echo "COMPLETION LEG - FALSE before this task runs:"; test -s "$Q" || { echo "GATE: $Q does not exist"; exit 1; }; TC=$(/usr/bin/grep -cE '^[[:space:]]*#\[(test|tokio::test)' "$Q"); test "$TC" -ge 16 || { echo "GATE: $Q declares $TC tests - Task 1 states eight behaviors in this file and Task 2 states eight more, so fewer than sixteen means this task's provenance, selection, conservation and round-trip behaviors are unwritten"; exit 1; }; echo "guard=intact quarantine_tests=$TC"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; for f in crates/wcore-cli/src/migrate/provenance.rs crates/wcore-cli/src/migrate/select.rs crates/wcore-cli/src/profile.rs; do n=$(/usr/bin/wc -l &lt; "$f" | tr -d ' '); test "$n" -lt 1000 || { echo "OVER 1000 LINES: $f ($n)"; exit 1; }; done</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-02; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2&gt;/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; cargo clippy --locked -p wcore-config -p wcore-cli --all-targets -- -D warnings; cargo nextest run --locked -p wcore-config -p wcore-cli --no-fail-fast; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA" &gt; /tmp/26-02-t2.log 2&gt;&amp;1; rc=$?; tail -30 /tmp/26-02-t2.log; test $rc -eq 0</automated>
  </verify>
  <done>All eight behaviors exist as tests written before the implementation. Item identity is shared by the published plan, selection, provenance and any later rollback — one scheme, not three. Provenance records source tool, version, relative path, a domain-separated stable digest and import time. Selection refuses an unpublished identity rather than silently importing nothing. The conservation invariant is asserted as arithmetic over the FULL committed corpora, and a non-exclusion failure is a named report entry that still balances. Export carries provenance, still excludes secrets by default with the existing assertions gate-checked intact, and the export/import round trip preserves provenance while keeping executable content quarantined rather than laundering it into a promotion.</done>
</task>

<task type="auto">
  <name>Task 3: Prove inertness live on Linux with a real payload — absent while quarantined, present once promoted</name>
  <files>crates/wcore-cli/tests/fixtures/portability-exec/README.md, crates/wcore-cli/tests/migrate_quarantine.rs</files>
  <read_first>crates/wcore-cli/tests/smoke_p0.rs (how this codebase drives the REAL `wayland-core` binary in a test — the binary-path idiom and the mock-provider harness it uses to run an actual agent turn without a live provider), crates/wcore-skills/src/executor.rs (what an executing skill body actually does, so the fixture payload is realistic rather than a strawman), crates/wcore-skills/src/discovery.rs (what the agent-facing enumeration actually walks, so the quarantine placement is proven against the real load path rather than an assumed one)</read_first>
  <behavior>
    - The fixture payload is a real skill body carrying a real shell directive whose only effect is to create an observable sentinel file at a path the test controls.
    - Negative leg: after importing the peer corpus containing that payload, driving the REAL binary through an agent turn that would use skills leaves the sentinel ABSENT.
    - Positive control, same payload: after an explicit operator promotion, the SAME driven turn leaves the sentinel PRESENT — proving the negative leg measured containment rather than a payload that never loaded.
    - The negative leg additionally asserts the import actually happened — the item count is non-zero and the item is listed as quarantined — so an import that silently did nothing cannot pass as containment.
    - Both legs run on Linux against the real built binary, not against library functions.
    - The sentinel path is inside a temporary directory unique to the run, so a stale sentinel from an earlier run cannot make either leg lie.
  </behavior>
  <action>Build the fixture as a realistic payload, not a strawman. It is a skill body with the frontmatter a real peer skill carries and a shell directive whose only observable effect is creating a sentinel file at a path the test injects. Realism matters because the detector this plan reuses matches real directive syntax, and a payload that does not look like real content proves nothing about real content.

Run the negative leg against the REAL binary. Import the corpus containing the payload into a temporary home, then drive the binary through an agent turn that would enumerate and use skills, using the same mock-provider harness the existing smoke tests use so the turn is real without needing a live provider. Assert three things together, because any one alone is vacuous: the import reported a non-zero item count, the payload item is reported as quarantined, and the sentinel does NOT exist. The first two are what stop "nothing was imported" and "the item vanished" from reading as containment.

Then run the positive control with the SAME payload and the SAME driven turn, differing only by an explicit operator promotion. Assert the sentinel DOES exist. Without this leg the negative leg carries no information, because absence would be equally consistent with the payload never being loaded, never being parsed, or never being discovered. With it, the pair proves the containment boundary is what changed the outcome.

Make both legs unfalsifiable by accident: put the sentinel inside a temporary directory unique to the run so no stale artifact from a previous run can satisfy the positive leg or contaminate the negative one, and assert the sentinel's absence at the start of each leg before anything runs.

Run both on Linux at this SHA against a freshly built binary. Capture each remote status on its own line and test it before reading any output. State the crate suite counts as a delta against Task 2's and name every residual failure.

Records evidence for F26-02.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -cF 'CARGO_BIN_EXE_wayland-core' crates/wcore-cli/tests/migrate_quarantine.rs)" -ge "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-02; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2&gt;/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; cargo nextest run --locked -p wcore-cli --no-fail-fast -E 'test(/quarantine/)'; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA" &gt; /tmp/26-02-t3-quar.log 2&gt;&amp;1; rc=$?; cat /tmp/26-02-t3-quar.log; test $rc -eq 0 &amp;&amp; test "$(/usr/bin/grep -cE '[1-9][0-9]* (test|tests) run' /tmp/26-02-t3-quar.log)" -ge "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-02; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2&gt;/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; cargo nextest run --locked --profile ci --no-fail-fast; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA" &gt; /tmp/26-02-t3-agg.log 2&gt;&amp;1; rc=$?; tail -30 /tmp/26-02-t3-agg.log; test $rc -eq 0</automated>
  </verify>
  <done>The fixture payload is a realistic peer skill body with a real shell directive whose only effect is an observable sentinel. The negative leg drove the REAL binary through an agent turn and asserted all three of a non-zero import count, the item reported quarantined, and the sentinel absent. The positive control drove the SAME payload through the SAME turn after an explicit promotion and observed the sentinel present, so the negative leg is proven to have measured containment rather than a payload that never loaded. Both sentinels live in a per-run temporary directory and absence is asserted before each leg begins. The quarantine gate is proven non-vacuous by asserting a non-zero test count ran. The aggregate Linux suite is green at this SHA with counts stated as a delta and every residual failure named.</done>
</task>

<task type="auto">
  <name>Task 4 (CROSS-AUDITED AUTONOMOUS DECISION): Measure the containment contract at real 540-item scale, put that measurement to the four-way panel, and commit to accepting or amending it</name>
  <files>scripts/portability-promotion-scale.sh, .planning/phases/26-migration-export-backup-restore/panel/26-02-containment-contract/, .planning/phases/26-migration-export-backup-restore/26-02-SUMMARY.md</files>
  <read_first>crates/wcore-config/src/workspace_trust.rs — lines 14 to 16 specifically, which declare `MAX_EXECUTABLE_FILES: usize = 512;`, `MAX_EXECUTABLE_FILE_BYTES: u64 = 4 * 1024 * 1024;` and `MAX_EXECUTABLE_TOTAL_BYTES: u64 = 32 * 1024 * 1024;` (verified present at those lines at plan time; a gate below pins all three, because 540 exceeds 512 and raising the ceiling to admit the import is the tempting wrong answer), crates/wcore-cli/src/migrate/quarantine.rs (the classification and promotion path as Task 1 built it, because the measurement below counts what it contains and what it lets through), crates/wcore-cli/tests/migrate_quarantine.rs (Task 3's paired inertness legs, because the positive-control verdict below is produced by RE-RUNNING them rather than by transcribing a claim that they fired), scripts/panel-ask.sh and scripts/panel-decision-check.sh (the phase's panel harness, built and self-tested in 26-01 — this task CALLS it and does not rebuild it)</read_first>
  <behavior>
    - "Would an operator tolerate this at 540 items?" is answered by a NUMBER the script itself prints, not by an opinion: the four counts and their sum, the operator invocations a realistic promotion actually costs at two DIFFERENT subset sizes, and a scaling verdict derived from those two points.
    - Over-broad and under-broad classification are each COUNTED — how many data kinds were swept into quarantine, and how many executable kinds escaped it — rather than eyeballed.
    - Whether the existing 512-file executable ceiling refuses a realistic 540-directory import is measured, and the three constants are gate-pinned so the limit cannot be quietly moved to make the import fit.
    - The inertness positive control is re-derived by RE-RUNNING Task 3's paired legs, so "the positive control fired" is a fresh measurement rather than a transcription.
    - All four panel members answer ONE shared evidence bundle carrying that measurement, and each response is captured verbatim to its own file.
    - One position is committed to — the majority, or a minority carrying named evidence — and the dissent survives in its own terms.
    - The panel harness is proven to survive the two extraction shapes the real CLIs actually emit, so a four-way panel is not silently a three-way one.
  </behavior>
  <decision>Is the containment contract this plan built one an operator can actually live with at real 540-item scale — accepted as it stands, or amended, and in which direction?</decision>
  <context>
This decision exists because a containment contract that is technically correct and operationally intolerable gets routed around by the first user who hits it, and then the containment is worse than none. Sean's real install carries **540 skill directories**, so the scale question is not theoretical, and 540 exceeds the existing `MAX_EXECUTABLE_FILES` ceiling of 512 by construction.

The six things being judged are exactly the six this plan already framed, unchanged: that the four counts balance arithmetically over the full corpus; that promotion at 540 items is a path an operator would actually use rather than one they would bypass; that classification is not over-broad, so personas, memory notes, settings and assets import without ceremony; that classification is not under-broad, so peer MCP launch commands and hook commands are contained because they are child processes and not settings; that the inertness proof's positive control demonstrably fired, since a negative leg without one proves nothing; and that the existing workspace-trust ceilings were not raised to admit a realistic import.

What changes is who resolves it. This is an evidence-acceptance and ergonomics judgement about the plan's own artifacts — none of it is a reserved outward action, no credential is involved, and both the corpus and the binary are already on the hosts this plan uses. So it is measured, put to the panel, decided, and recorded here, and the plan continues into its SUMMARY in the same session.

The plan's own standing disposition is preserved and is NOT re-scoped: unacceptable promotion ergonomics are a finding to record now, while it is cheap to change — never a reason to weaken containment.
  </context>
  <options>
    <option id="contract-accept">
      <name>The containment contract stands as built</name>
      <pros>The counts balance, classification is bounded correctly in both directions, the positive control fired, the ceilings were not touched, and the cost of promoting a realistic subset does not grow with the size of that subset.</pros>
      <cons>Nothing is amended, so any ergonomic roughness that the measurement did not capture ships as-is.</cons>
    </option>
    <option id="contract-amend-ergonomics">
      <name>Containment is correct but promotion at real scale is not usable; amend the promotion path and record the amendment</name>
      <pros>Fixes the failure mode that actually destroys containment in the field — an operator who has to promote items one at a time will route around the contract entirely. Amends the promotion path, which is where the defect is.</pros>
      <cons>Costs work inside this plan's declared files, and a promotion path made more convenient is a promotion path that must be re-checked against the rule that imported content cannot promote itself.</cons>
    </option>
    <option id="contract-amend-classification">
      <name>Classification is over-broad or under-broad; amend the breadth and re-measure</name>
      <pros>Over-broad classification trains operators to promote without reading and hollows the contract out; under-broad classification leaves a child-process launch surface uncontained, which is the critical threat this plan exists to close. Either is a real defect with a bounded fix.</pros>
      <cons>Changing breadth changes what the earlier tasks' tests assert, so the classification suite has to be re-run rather than assumed.</cons>
    </option>
    <option id="contract-reject">
      <name>A load-bearing check failed — the counts do not balance, the positive control did not fire, or a ceiling was raised</name>
      <pros>Names the failure honestly instead of absorbing it. Each of these three means a headline claim of this plan is not true as stated.</pros>
      <cons>It is not an outcome that can be recorded and passed: each is a defect this plan must FIX and re-measure before it can close, so choosing it is choosing more work now.</cons>
    </option>
  </options>
  <action>**THE HALT IS GONE AND WHAT REPLACES IT IS HEAVIER, NOT LIGHTER.** Do not put this in front of Sean and wait. Measure it, put the measurement to the panel, commit, record the dissent, and continue into the SUMMARY in the same session. Escalation exists for a genuine deadlock and nothing else — and there is no reserved substance anywhere in this task.

**STEP 1 — TURN THE OPERATOR QUESTION INTO A NUMBER.** Build `scripts/portability-promotion-scale.sh`, taking a binary path, a corpus root and an output directory. Run it on Linux inside this plan's own worktree `/root/wayland-f26-02` at this plan's SHA, against 26-01's committed canary Hermes corpus — the one whose STRUCTURE is cloned from the real 540-directory install — and copy its output back to the panel directory as `promotion-scale.txt`.

The script PRINTS ITS OWN VERDICT LINES; the executor never types them, so each line is tool output rather than an opinion about what was seen. The grammar is fixed and a gate below reads it: `SCALE-DISCOVERED:`, `SCALE-IMPORTED:`, `SCALE-QUARANTINED:` and `SCALE-EXCLUDED:` each followed by a count; `SCALE-BALANCES: yes` or `no`, which the script derives by checking that imported plus quarantined plus excluded equals discovered; at least two `PROMOTE-COST: items=<n> invocations=<n>` lines at DIFFERENT item counts; `PROMOTE-SCALING: bounded` or `linear`; `CLASSIFY-DATA-QUARANTINED:` counting persona, memory-note, settings and asset items that landed in quarantine; `CLASSIFY-EXEC-UNCONTAINED:` counting skill bodies carrying a shell directive, peer MCP launch commands and hook commands that did NOT; `CEILING-REFUSES-REALISTIC: yes` or `no`; `CEILING-CONSTANTS: files=<n> file_bytes=<n> total_bytes=<n>` read out of `workspace_trust.rs` at run time rather than restated from this plan; and `POSITIVE-CONTROL: fired` or `not-fired`.

Two of those lines are the whole point and both must be MEASURED rather than asserted.

The promotion cost is measured at two subset sizes drawn from what the run actually quarantined — take a smaller and a larger subset differing by at least a factor of three, with the larger at least half the quarantined count — and for each the script counts the operator invocations a promotion of that subset genuinely costs. The scaling rule is stated here so it is not the script's whim: `bounded` when the larger subset's invocation count is no more than twice the smaller's, `linear` otherwise. The script prints the rule it applied alongside the verdict. A single measurement point cannot produce a slope, so a gate below requires the two points to name different item counts; this is what converts "if these were your 540 skills, what would you have to do?" from a question of taste into arithmetic.

The positive-control verdict is produced by RE-RUNNING Task 3's paired legs at this SHA and reading their result, never by reading Task 3's own report of them. That distinction is the difference between checking the proof and checking the claim about the proof.

Give the script the same self-red proof the rest of this phase's scripts carry: handed a binary path that does not exist it must exit non-zero and say why, because a measurement script that cannot fail turns every number it prints into decoration.

**STEP 2 — PUT IT TO ALL FOUR MEMBERS, ON ONE BUNDLE.** Follow THE PANEL PROCEDURE that 26-01 established, with the panel directory `.planning/phases/26-migration-export-backup-restore/panel/26-02-containment-contract/`. Write the evidence bundle ONCE to `question.md`: the question exactly as this task's `decision` element states it; all four option ids with their names, pros and cons copied VERBATIM from the `options` element above, nothing added, softened, dropped or re-scoped; the six judgement criteria named in the `context` element above; `promotion-scale.txt` in full; the quarantine and apply report from the Linux run against the full corpus; the classification source; and the GHSA-8r7g contract as it reads in `crates/wcore-config/src/hooks.rs`. Present the four options in a rotated order rather than this plan's order, so the panel is not handed this plan's sequencing as a prior.

Hand that one file to the three external members through `scripts/panel-ask.sh question.md <panel-dir>`, and write your own adversarial pass to `internal.md` in the same two-line-terminated form — argued AGAINST whichever option you walked in believing, engaging at least two option ids by name. A fourth voice that ratifies the other three is not a panel member.

**The bundle carries NO real-home output and NO real secret.** Three of the four members are remote services, the corpus is canary by construction, and a gate below searches this panel directory for every real secret value it can extract from the real peer homes after first proving the search was non-vacuous.

**THREE CLI BEHAVIOURS WERE MEASURED DIRECTLY ON 2026-07-26 AND EACH ONE SILENTLY DROPS A VOTE IF IGNORED.** They are recorded here because a four-way audit that is really three-way is the same defect class as a self-passing gate — a verification that appears to run and does not. First, `gemini` REFUSES without `--skip-trust`: measured exit status 55 with ZERO bytes on stdout and a trusted-directory refusal on stderr, so the member returns nothing at all; with the flag it answers cleanly on stdout and its `GOOGLE_API_KEY` notice goes to stderr, where it is a notice and never grounds for recording the member NOT-RUN. Second, `kimi` prefixes its answer with a bullet and a space and indents the continuation line: measured, a real capture's verdict reads as a bullet followed by the token, so an ANCHORED match at column zero returns 0 and the same unanchored match returns 1 — extraction must be UNANCHORED or kimi's vote vanishes. Third, `codex` repeats its whole final block, so a real capture carries the SAME verdict line twice — extraction must take the LAST occurrence and must TOLERATE a repeated identical verdict, while two DIFFERENT verdict values in one capture remain a genuinely ambiguous capture that must be rejected. 26-01's own Task 4 meets these same three shapes first, in wave 1, against the same three CLIs, so a harness that cannot survive them cannot have gone green there; if the trap gate below is red, that is a 26-01 defect to record and escalate rather than a file for this plan to edit while its wave-2 sibling is running.

**STEP 3 — DECIDE, AND LET THE MEASUREMENT BREAK TIES.** Record the outcome in `DECISION.md` in the grammar 26-01's checker enforces: one `CHOSEN:` naming an option id from this task's own set; `BASIS: majority` or `BASIS: minority-with-evidence`; an `EVIDENCE:` naming a real non-empty artifact whenever the choice is not the majority verdict; a `RATIONALE:` of at least 120 characters; and a `## DISSENT` section naming every captured verdict that differs from the chosen one, with its argument in its own terms. A decision with the losing arguments deleted is not auditable, and the checker derives the dissent set from the CAPTURED verdicts rather than from the prose, so dissent cannot be disposed of by omission.

The measurement outranks the argument, and a gate below enforces the binding rather than trusting it. `contract-accept` is available only when the counts balanced, the positive control fired, the promotion cost was measured `bounded`, no executable kind escaped containment, no data kind was swept into it, and all three ceiling constants stand where they were. A `linear` promotion cost forbids `contract-accept` and requires `contract-amend-ergonomics` with a severity-rated `FINDING:` line — that is this plan's own disposition, said plainly at the moment it is cheap. Any data kind in quarantine or any executable kind outside it requires `contract-amend-classification`. Unbalanced counts, an unfired positive control, or a moved ceiling constant require `contract-reject`.

Because the measurement distinguishes every pair of options here, a genuine deadlock is only possible when the measurement itself failed to run — which is its own red. If one nevertheless occurs, escalate CARRYING the split and all four verbatim captures, not a bare question, and continue with the SUMMARY meanwhile.

**STEP 4 — AMEND IF THE DECISION SAYS SO, THEN RE-MEASURE.** `contract-amend-*` and `contract-reject` are defects this plan must FIX inside its own declared files, not outcomes it may record and pass. After an amendment, re-run the scale script and keep the newer measurement as `promotion-scale.txt`; the final-state gate reads whatever is there at the end, so an amendment that was recorded but not made goes red. Raising `MAX_EXECUTABLE_FILES` to admit the 540-directory import is expressly forbidden and gate-pinned: it would widen the executable-surface bound for every workspace-trust consumer to solve a migration problem. A ceiling that refuses a realistic import is a finding with a severity, recorded in the SUMMARY.

Marks F26-02 complete if every gate below passes.</action>
  <verify>
    <automated>
# SCRIPT-SHAPE AND SELF-RED GATE. The measurement script must exist, parse, be substantive, and PROVE it can fail by exiting non-zero when handed a binary that does not exist. A measurement script that cannot go red turns every number it prints into decoration. Red at base: the script does not exist.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f scripts/portability-promotion-scale.sh &amp;&amp; sh -n scripts/portability-promotion-scale.sh &amp;&amp; test "$(/usr/bin/grep -vc '^[[:space:]]*#' scripts/portability-promotion-scale.sh)" -ge 30 &amp;&amp; { sh scripts/portability-promotion-scale.sh /nonexistent/wayland-core-does-not-exist /nonexistent/corpus /tmp/26-02-scale-selfcheck &gt; /tmp/26-02-scale-selfcheck.log 2&gt;&amp;1; rc=$?; cat /tmp/26-02-scale-selfcheck.log; test $rc -ne 0 || { echo "GATE: the promotion-scale script reported SUCCESS against a binary that does not exist - it cannot go red, so its numbers are not evidence"; exit 1; }; }
</automated>
    <automated>
# SCALE-MEASUREMENT GATE. Reads the CAPTURED measurement the script itself printed on Linux, never prose the executor typed into a document. Requires every line of the fixed grammar, and requires the two promotion-cost points to name DIFFERENT item counts - a scaling verdict derived from one point is an opinion with a number beside it. Red at base: nothing has been measured.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; D=.planning/phases/26-migration-export-backup-restore/panel/26-02-containment-contract; M="$D/promotion-scale.txt"; test -s "$M" || { echo "GATE: no captured promotion-scale measurement at $M"; exit 1; }; for K in SCALE-DISCOVERED SCALE-IMPORTED SCALE-QUARANTINED SCALE-EXCLUDED CLASSIFY-DATA-QUARANTINED CLASSIFY-EXEC-UNCONTAINED; do test "$(/usr/bin/grep -cE "^$K: [0-9]+$" "$M")" -eq 1 || { echo "GATE: $M carries no single well-formed $K line"; exit 1; }; done; for K in "SCALE-BALANCES: (yes|no)" "PROMOTE-SCALING: (bounded|linear)" "POSITIVE-CONTROL: (fired|not-fired)" "CEILING-REFUSES-REALISTIC: (yes|no)" "CEILING-CONSTANTS: files=[0-9]+ file_bytes=[0-9]+ total_bytes=[0-9]+"; do test "$(/usr/bin/grep -cE "^$K$" "$M")" -eq 1 || { echo "GATE: $M carries no single well-formed line matching: $K"; exit 1; }; done; U=$(/usr/bin/grep -oE '^PROMOTE-COST: items=[0-9]+' "$M" | /usr/bin/sort -u | /usr/bin/grep -c .); test "$U" -ge 2 || { echo "GATE: promotion cost was measured at $U distinct item count(s) - two are required, because PROMOTE-SCALING is a slope and a slope needs two points"; exit 1; }; echo "scale measurement well-formed, promotion cost measured at $U distinct item counts"
</automated>
    <automated>
# PANEL GATE. Delegates to the phase's own checker, built and self-tested in 26-01, against THIS task's four option ids. It reads the CAPTURED panel files - four verbatim responses, one verdict each from the supplied set, the same question digest stamped in all three external captures, pairwise-distinct externals, and a dissent set derived from the captured verdicts rather than from prose. Red at base: neither the checker nor the panel directory exists.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; D=.planning/phases/26-migration-export-backup-restore/panel/26-02-containment-contract; test -f scripts/panel-decision-check.sh || { echo "GATE: scripts/panel-decision-check.sh is absent - 26-01 builds it and every converted decision in this phase gates on it"; exit 1; }; test -d "$D" || { echo "GATE: no panel directory $D - the containment question was never put to the panel"; exit 1; }; sh scripts/panel-decision-check.sh "$D" contract-accept contract-amend-ergonomics contract-amend-classification contract-reject &gt; /tmp/26-02-t4-panel.log 2&gt;&amp;1; rc=$?; cat /tmp/26-02-t4-panel.log; test $rc -eq 0 || { echo "GATE: the containment-contract panel record is not well-formed - see above"; exit 1; }
</automated>
    <automated>
# PANEL-EXTRACTION TRAP GATE - the leg that keeps this a four-way panel instead of a three-way one wearing a fourth's name. Measured on the real CLIs on 2026-07-26: kimi prefixes its verdict with a bullet and a space, so an anchored column-zero match returns ZERO for a real kimi capture; and codex repeats its whole final block, so a real codex capture carries the SAME verdict twice. This copies the real panel directory twice and mutates the copies to reproduce exactly those two shapes. The checker must ACCEPT the bullet-prefixed, duplicate-carrying copy - an anchored or duplicate-intolerant extractor goes red here - and must REJECT the copy carrying two DIFFERENT verdict values in one capture, so an extractor that takes whatever it finds also goes red. Both directions are required: a checker that accepts everything and one that rejects everything are equally worthless. Red at base: the panel directory does not exist.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; D=.planning/phases/26-migration-export-backup-restore/panel/26-02-containment-contract; OPTS="contract-accept contract-amend-ergonomics contract-amend-classification contract-reject"; test -d "$D" &amp;&amp; test -f scripts/panel-decision-check.sh || { echo "GATE: no panel directory or no checker to test"; exit 1; }; CH=$(/usr/bin/grep -oE '^CHOSEN: [a-z-]+$' "$D/DECISION.md" | /usr/bin/sed 's/^CHOSEN: //'); OTHER=""; for o in $OPTS; do test "$o" = "$CH" || OTHER="$o"; done; test -n "$CH" &amp;&amp; test -n "$OTHER" || { echo "GATE: could not read a CHOSEN option to build the trap fixtures from"; exit 1; }; A=/tmp/26-02-trap-accept; R=/tmp/26-02-trap-reject; mkdir -p "$A" "$R"; /usr/bin/find "$D" -maxdepth 1 -type f -exec /bin/cp {} "$A"/ \; ; /usr/bin/find "$D" -maxdepth 1 -type f -exec /bin/cp {} "$R"/ \; ; /usr/bin/sed -e 's/^PANEL-VERDICT: /• PANEL-VERDICT: /' -e 's/^PANEL-BASIS: /  PANEL-BASIS: /' "$D/kimi.txt" &gt; "$A/kimi.txt"; { /bin/cat "$D/codex.txt"; /usr/bin/tail -2 "$D/codex.txt"; } &gt; "$A/codex.txt"; { /usr/bin/sed "s/^PANEL-VERDICT: .*/PANEL-VERDICT: $OTHER/" "$D/codex.txt"; /usr/bin/tail -2 "$D/codex.txt"; } &gt; "$R/codex.txt"; sh scripts/panel-decision-check.sh "$A" $OPTS &gt; /tmp/26-02-trap-accept.log 2&gt;&amp;1; ra=$?; sh scripts/panel-decision-check.sh "$R" $OPTS &gt; /tmp/26-02-trap-reject.log 2&gt;&amp;1; rr=$?; echo "trap_accept_rc=$ra trap_reject_rc=$rr"; test $ra -eq 0 || { echo "GATE: the checker REJECTED a panel whose kimi capture is bullet-prefixed and whose codex capture repeats its final block - those are the shapes the real CLIs measurably emit, so this checker silently drops votes and the panel is not four-way"; /bin/cat /tmp/26-02-trap-accept.log; exit 1; }; test $rr -ne 0 || { echo "GATE: the checker ACCEPTED a capture carrying two DIFFERENT verdict values, so it is not extracting a verdict, it is finding one"; /bin/cat /tmp/26-02-trap-reject.log; exit 1; }
</automated>
    <automated>
# MEASUREMENT-BINDING AND FINAL-STATE GATE - the one that makes this a measurement rather than a committee. First it pins the three workspace-trust ceiling constants, because raising 512 to admit a 540-directory import would widen the executable-surface bound for every consumer to solve a migration problem. Then it binds the chosen option to what the run actually measured, and finally it requires the four load-bearing correctness properties to hold in the measurement as it stands at task end - so an amendment that was recorded but not made goes red. Red at base: no measurement exists.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; D=.planning/phases/26-migration-export-backup-restore/panel/26-02-containment-contract; M="$D/promotion-scale.txt"; DEC="$D/DECISION.md"; test -s "$M" &amp;&amp; test -s "$DEC" || { echo "GATE: no measurement or no decision record"; exit 1; }; W=crates/wcore-config/src/workspace_trust.rs; test "$(/usr/bin/grep -cF 'const MAX_EXECUTABLE_FILES: usize = 512;' "$W")" -eq 1 &amp;&amp; test "$(/usr/bin/grep -cF 'const MAX_EXECUTABLE_FILE_BYTES: u64 = 4 * 1024 * 1024;' "$W")" -eq 1 &amp;&amp; test "$(/usr/bin/grep -cF 'const MAX_EXECUTABLE_TOTAL_BYTES: u64 = 32 * 1024 * 1024;' "$W")" -eq 1 || { echo "GATE: a workspace-trust ceiling constant MOVED. Raising it to admit a realistic import is expressly forbidden - the collision is a finding with a severity, not a limit to widen"; exit 1; }; CH=$(/usr/bin/grep -oE '^CHOSEN: [a-z-]+$' "$DEC" | /usr/bin/sed 's/^CHOSEN: //'); BAL=$(/usr/bin/grep -oE '^SCALE-BALANCES: [a-z]+' "$M" | /usr/bin/sed 's/.*: //'); PC=$(/usr/bin/grep -oE '^POSITIVE-CONTROL: [a-z-]+' "$M" | /usr/bin/sed 's/.*: //'); SC=$(/usr/bin/grep -oE '^PROMOTE-SCALING: [a-z]+' "$M" | /usr/bin/sed 's/.*: //'); DQ=$(/usr/bin/grep -oE '^CLASSIFY-DATA-QUARANTINED: [0-9]+' "$M" | /usr/bin/sed 's/.*: //'); EU=$(/usr/bin/grep -oE '^CLASSIFY-EXEC-UNCONTAINED: [0-9]+' "$M" | /usr/bin/sed 's/.*: //'); echo "chosen=$CH balances=$BAL positive_control=$PC scaling=$SC data_quarantined=$DQ exec_uncontained=$EU"; case "$CH" in contract-accept) test "$SC" = bounded || { echo "GATE: contract-accept was chosen but promotion cost measured $SC - a cost that grows with the subset is the contract users bypass, and this plan's own disposition is to record it as a finding under contract-amend-ergonomics rather than accept it"; exit 1; };; contract-amend-ergonomics) test "$SC" = linear || { echo "GATE: an ergonomics amendment was chosen but the measurement says promotion cost is $SC - the panel argued its way to a defect the measurement contradicts"; exit 1; }; /usr/bin/grep -qE '^FINDING: .*(critical|high|medium|low)' "$DEC" || { echo "GATE: an ergonomics amendment carries no severity-rated FINDING line"; exit 1; };; contract-amend-classification) test "$DQ" -gt 0 -o "$EU" -gt 0 || { echo "GATE: a classification amendment was chosen but the measurement found no data kind in quarantine and no executable kind outside it"; exit 1; };; contract-reject) test "$BAL" = no -o "$PC" = not-fired || { echo "GATE: contract-reject was chosen but the counts balance and the positive control fired"; exit 1; };; *) echo "GATE: CHOSEN is '$CH', which is not one of this task's four option ids"; exit 1;; esac; test "$BAL" = yes || { echo "GATE: the four counts do not balance in the FINAL measurement - imported plus quarantined plus excluded must equal discovered, and anything else is data loss reported as success"; exit 1; }; test "$PC" = fired || { echo "GATE: the inertness positive control did NOT fire in the final measurement, so the negative leg proves nothing - absence is equally consistent with a payload that never loaded"; exit 1; }; test "$EU" -eq 0 || { echo "GATE: $EU executable item(s) escaped containment in the final measurement - a peer MCP launch command or hook command outside quarantine is the critical child-process surface this plan exists to close"; exit 1; }; test "$DQ" -eq 0 || { echo "GATE: $DQ plain-data item(s) were swept into quarantine in the final measurement - over-broad classification trains operators to promote without reading"; exit 1; }
</automated>
    <automated>
# PANEL SECRET-HYGIENE GATE. Three of the four members are REMOTE SERVICES, so the bundle is an exfiltration surface - and the case that matters most is precisely the one where redaction had already failed. This extracts the REAL secret values from the REAL peer homes and requires ZERO of them anywhere under this panel directory, after first proving both that it had values to search for and that there was something to search. Red at base: the panel directory does not exist.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; P=.planning/phases/26-migration-export-backup-restore/panel/26-02-containment-contract; test -d "$P" || { echo "GATE INVALID: no panel directory exists, so searching it for leaked secrets would pass vacuously"; exit 1; }; NF=$(/usr/bin/find "$P" -type f 2&gt;/dev/null | /usr/bin/wc -l | tr -d ' '); test "$NF" -ge 5 || { echo "GATE INVALID: panel/ holds $NF files - a completed panel run writes question.md, three captures, an internal pass and a DECISION.md, so fewer than five means there is nothing to search"; exit 1; }; S=$(mktemp); /usr/bin/grep -hoE '[A-Za-z0-9_]*_(API_KEY|TOKEN|SECRET|KEY)=.*' "$HOME"/.hermes/profiles/*/.env 2&gt;/dev/null | /usr/bin/sed 's/^[^=]*=//; s/^["'"'"']//; s/["'"'"']$//' | /usr/bin/grep -vE '^[[:space:]]*$' &gt;&gt; "$S"; /usr/bin/find "$HOME/.openclaw" -maxdepth 2 -type f \( -name '*.json' -o -path '*/credentials/*' \) -print0 2&gt;/dev/null | xargs -0 /usr/bin/grep -hoiE '"[^"]*(key|token|secret|auth|pass|cred)[^"]*"[[:space:]]*:[[:space:]]*"[^"]{16,}"' 2&gt;/dev/null | /usr/bin/sed 's/.*:[[:space:]]*"//; s/"$//' &gt;&gt; "$S"; /usr/bin/sort -u "$S" -o "$S"; T=$(/usr/bin/grep -c . "$S"); test "$T" -ge 4 || { echo "GATE INVALID: only $T real secret values extracted across BOTH peer homes - measured 7 at plan time, so this search is not proving what it claims"; /bin/rm -f "$S"; exit 1; }; HITS=0; while IFS= read -r v; do test ${#v} -lt 8 &amp;&amp; continue; /usr/bin/grep -RqF -- "$v" "$P" 2&gt;/dev/null &amp;&amp; HITS=$((HITS+1)); done &lt; "$S"; /bin/rm -f "$S"; echo "panel_files=$NF searched=$T hits=$HITS"; test "$HITS" -eq 0 || { echo "GATE: a REAL secret value appears under this panel directory - the bundle went to three remote services carrying live credentials. This is a CRITICAL disclosure, not a formatting problem"; exit 1; }
</automated>
  </verify>
  <done>The operator question was answered by a measurement rather than by a judgement: the four counts and their sum, the promotion cost at two DIFFERENT subset sizes with the scaling verdict derived from that slope, the count of data kinds swept into quarantine, the count of executable kinds that escaped it, the ceiling-collision result, and a positive-control verdict produced by RE-RUNNING Task 3's paired legs rather than by transcribing them. The measurement script exists, parses, and was proven able to go red against a binary that does not exist. All four panel members answered ONE shared bundle carrying that measurement, each response captured verbatim, with no real-home output and no real secret in the bundle — proven by a search that first proved it was non-vacuous. The panel harness was proven to survive both shapes the real CLIs measurably emit, so the panel is four-way in fact and not only in name. One option from this task's own set is committed to, its basis is majority or minority-with-evidence naming a real artifact, and the dissent survives in its own terms derived from the captured verdicts. The chosen option is bound to the measurement, so no option could be argued past what the run showed. In the FINAL measurement the counts balance, the positive control fired, no executable kind sits outside containment, no plain data kind sits inside it, and all three workspace-trust ceiling constants stand exactly where they were — a ceiling that refuses a realistic import is recorded as a severity-rated finding, never raised. No wait state occurred; the plan continued into its SUMMARY in the same session.</done>
</task>

</tasks>

## What this plan does NOT change (scope fence)

- **Discovery and the typed plan model — 26-01 owns them.** Consumed here, not reshaped. Item identity in particular is 26-01's and is reused, never redefined.
- **Backup, restore, the operation journal and interruption survival — 26-03 owns them.** A rollback requirement discovered here is recorded for 26-03, not built here as a second journal.
- **Hostile corpora — 26-04 owns them.** The fixtures here are realistic-and-executable, not adversarial.
- **The GHSA-8r7g hook contract — not weakened, and gate-checked.** `trust_project_hooks` stays default-false and a project still cannot trust its own hooks. Imported content does not become a new route to what that advisory closed.
- **The existing secret-exclusion behavior of profile export — not weakened, and gate-checked.** The secret directory and credentials-file classification and its existing tests stay exactly as they are; export gains provenance and selection, not permissiveness.
- **The existing shell-directive detector — reused, not forked.** No second pattern list is introduced that could drift from the one the skills crate actually enforces.
- **`wcore-protocol` — untouched.** No migration or quarantine event is added to the JSON stream protocol; the D1 producer contract and its Desktop consumers are unaffected.
- **No new external dependency.**
- **No test is deleted, weakened, re-gated, `#[ignore]`d or `#[allow]`ed.**

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported skill body → skill executor | A peer `SKILL.md` is a shell payload the executor already knows how to run |
| imported MCP definition → child process launch | A peer MCP entry's command and args already flow into a stdio server config that is spawned |
| imported hook definition → child process launch | The surface GHSA-8r7g closed for project configs, reachable again by a different route |
| quarantine placement → agent load path | Inertness is a property of where the bytes are; anything that makes the load path look there breaks it |
| imported content → its own promotion | The half of GHSA-8r7g that actually holds: the foreign artifact must not be able to trust itself |
| export corpus → re-import | A round trip must not launder quarantine into a promotion the operator never granted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-02-01 | Elevation of Privilege | An imported peer skill executes its shell directive on arrival, giving a hostile peer state directory arbitrary code execution on import | critical | mitigate | Classification via the existing detector plus containment by PLACEMENT outside the load path; proven live against the real binary with a paired positive control that makes the absence assertion meaningful |
| T-26-02-02 | Elevation of Privilege | Imported content promotes ITSELF via a field, key, manifest entry or filename the promotion path reads — the failure GHSA-8r7g explicitly closed for project hooks | critical | mitigate | Promotion consults only an operator action; a dedicated test asserts no content-carried signal can trigger it; the plan terminates in state 2 rather than shipping a promotion path the content can influence |
| T-26-02-03 | Elevation of Privilege | An imported peer MCP definition's launch command spawns a child process, bypassing the quarantine applied to skills — this door is open in the current apply path | critical | mitigate | Peer MCP launch commands are classified executable and contained under the same policy, with a dedicated test; the existing mapper's command/args flow is brought under quarantine rather than left as the one unguarded route |
| T-26-02-04 | Elevation of Privilege | An export/import round trip launders quarantine, so content arrives promoted without any operator grant | high | mitigate | The round-trip test asserts executable content arrives quarantined after export-then-import, not merely that provenance survived |
| T-26-02-05 | Tampering | The inertness gate passes vacuously because the payload was never imported, never parsed, or never discovered — absence proving nothing | high | mitigate | Three assertions together in the negative leg (non-zero import count, item reported quarantined, sentinel absent), plus a positive control with the same payload; the sentinel lives in a per-run temporary directory and its absence is asserted before each leg |
| T-26-02-06 | Information Disclosure | Export carries secrets that the existing default excludes, because provenance or selection work touched the filter | high | mitigate | The existing secret classification constants and the existing default-exclusion test are gate-checked present and unchanged; the round-trip test exercises the default path |
| T-26-02-07 | Denial of Service | Selective import silently drops items on a large real tree, reporting success while losing data | high | mitigate | The conservation invariant is asserted as arithmetic over the FULL committed corpora, and a non-exclusion failure must still appear in the accounting |
| T-26-02-08 | Tampering | A second shell-directive pattern list is written for classification and drifts from the one the executor enforces, so content classified safe still executes | high | mitigate | The existing detector is called directly and its presence in the quarantine module is gate-checked; writing a parallel pattern list is forbidden |
| T-26-02-09 | Elevation of Privilege | The workspace-trust ceilings are raised to admit a 540-directory import, widening the executable surface bound for every consumer | medium | mitigate | Ceilings are mirrored, not loosened; all three constants are gate-pinned byte-for-byte at Task 4, and a ceiling that refuses a realistic import is recorded as a severity-rated finding rather than raised |
| T-26-02-10 | Repudiation | Over-broad classification quarantines plain data too, training operators to promote without reading and hollowing out the contract | medium | mitigate | Personas, memory, settings and assets are explicitly classified as data; Task 4 COUNTS how many data kinds were swept into quarantine and the gate requires that count to be zero, so breadth is measured rather than judged |
| T-26-02-12 | Repudiation | The four-way panel is silently three-way because an extraction shape drops a member's vote — the same defect class as a self-passing gate, a verification that appears to run and does not | high | mitigate | Three CLI behaviours were measured directly rather than read from docs: gemini refuses without `--skip-trust` (exit 55, zero bytes), kimi bullet-prefixes so anchored extraction returns zero, and codex repeats its final block so extraction must take the LAST occurrence and tolerate a repeated identical verdict. Task 4's trap gate mutates copies of the real panel directory into exactly those shapes and requires the checker to ACCEPT the real shapes and REJECT a genuinely ambiguous capture |
| T-26-02-11 | Spoofing | Item identity differs between the published plan, selection and provenance, making selective rollback impossible for 26-03 | medium | mitigate | One identity scheme, established in 26-01 and reused; selection refuses an identity the plan did not publish |
| T-26-02-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency is added, removed or updated; no `Cargo.toml` or `Cargo.lock` change; the execution rules require escalation rather than an install |
</threat_model>

<verification>
Local gates (Mac, source level only): `cargo fmt --all -- --check` clean; the quarantine module demonstrably calls the existing shell-directive detector; the GHSA-8r7g hook constants are gate-checked still present; the profile module's secret classification constants and its default-exclusion test are gate-checked still present; every new or touched file under 1000 lines.

Authoritative gates (`hetzner-dsm`, in this plan's OWN worktree `/root/wayland-f26-02` — never the shared `/root/wayland`, which 26-03 is using concurrently in this same wave — at this plan's SHA asserted before AND after each one, with `/root/.cargo/bin` on `PATH`, each status captured on its own line and tested before any output is read): `cargo clippy --locked -p wcore-config -p wcore-cli --all-targets -- -D warnings`; `cargo nextest run --locked -p wcore-config -p wcore-cli --no-fail-fast`; the quarantine-filtered run, which additionally asserts a NON-ZERO test count actually executed so an empty filter cannot pass as green; and the aggregate `cargo nextest run --locked --profile ci --no-fail-fast`.

Live product gate (Linux, real binary): the negative leg asserts a non-zero import count, the item reported quarantined, and the sentinel absent; the positive control asserts the same payload through the same driven turn produces the sentinel once promoted.

Cross-audited decision gate (Task 4, no human wait state): `scripts/portability-promotion-scale.sh` exists, parses, and is PROVEN able to go red against a binary that does not exist; its captured measurement carries every line of the fixed grammar with the promotion cost measured at two DISTINCT item counts, so the scaling verdict is a slope rather than an opinion; the panel record satisfies 26-01's checker against this task's own four option ids; the checker is proven to survive the two extraction shapes the real CLIs measurably emit and to reject a genuinely ambiguous capture, so the panel is four-way in fact; the chosen option is BOUND to what the run measured; the three workspace-trust ceiling constants are pinned byte-for-byte; the final measurement balances, fires its positive control, leaves no executable kind uncontained and no data kind contained; and no real secret value appears under the panel directory, proven by a search that first proved it was non-vacuous.

Known unknowns to record, not resolve here: whether any of the 540 real skills carries a directive form the existing detector does not match, which would be a pre-existing detector gap rather than a quarantine gap and belongs to the skills crate; whether OpenClaw plugin and flow content needs a distinct classification beyond the four kinds handled here; and how quarantined content behaves on Windows path semantics, which 26-04 owns.
</verification>

<success_criteria>
- Imported executable content is classified using the detector the executor actually enforces, covering skills, peer MCP launch commands and hook commands.
- Quarantined content is inert by PLACEMENT and proven absent from what the agent would really load.
- Nothing the imported content carries can promote it; only an explicit operator action can, and the GHSA-8r7g default and no-self-trust rule are gate-checked intact.
- Every imported item carries provenance: source tool, source version, source-relative path, a domain-separated stable content digest, and import time.
- Import and export are selective by the SAME item identity the dry-run plan published, and an unpublished identity is refused rather than silently ignored.
- Imported plus quarantined plus excluded equals discovered, asserted as arithmetic over the FULL committed corpora.
- Export preserves provenance, still excludes secrets by default with the existing assertions intact, and an export/import round trip does not launder quarantine into promotion.
- Inertness is proven live against the REAL binary with a real payload, with the negative leg carrying three joint assertions and a positive control that demonstrably fires.
- The containment contract is MEASURED at real 540-item scale — the four counts, the promotion cost at two distinct subset sizes and its derived scaling verdict, the over-broad and under-broad classification counts, the ceiling collision, and a re-run positive control — and then accepted or amended by a four-way cross-audited decision whose chosen option is bound to that measurement, with the dissent recorded. No task in this plan waits on a human.
- The panel is four-way in fact and not only in name: the checker is proven to survive kimi's bullet-prefixed verdict and codex's repeated final block, and to reject a capture carrying two different verdict values.
- Every gate can go red, including the quarantine run, which asserts a non-zero test count actually executed.
- Every Linux gate ran in this plan's OWN worktree with the SHA asserted before and after, so the concurrent wave-2 sibling 26-03 cannot have moved the tree under an in-flight proof — and if anything did, the gate went red rather than certifying the wrong commit.
- The GHSA-8r7g tripwire asserts the DEFAULT is still `false`, not merely that the identifier still exists.
</success_criteria>

## Artifacts this plan produces
- `crates/wcore-cli/src/migrate/quarantine.rs` — classification and containment of imported executable content.
- `crates/wcore-cli/src/migrate/provenance.rs` — the per-item source record that makes an import auditable and reversible.
- `crates/wcore-cli/src/migrate/select.rs` — selection by published item identity with the conservation invariant.
- `crates/wcore-cli/tests/fixtures/portability-exec/` — the realistic executable payload and its sentinel harness.
- `crates/wcore-cli/tests/migrate_quarantine.rs` — the paired inertness proof against the real binary.
- `scripts/portability-promotion-scale.sh` — the measurement that turns the 540-item operator question into a number.
- `panel/26-02-containment-contract/` — the four verbatim panel captures, the shared bundle, and the decision record with its dissent.
- `26-02-SUMMARY.md`.

<output>
Create `.planning/phases/26-migration-export-backup-restore/26-02-SUMMARY.md` using the standard summary template. Record: how each of the four executable kinds is classified and which existing detector was called; where quarantined content lands and the enumeration evidence that the real load path does not reach it; the promotion mechanism and precisely why imported content cannot trigger it, mapped against the GHSA-8r7g contract; whether the existing workspace-trust ceilings admitted a realistic 540-directory import and, if not, the finding and its severity; the provenance record's fields and the domain separation used for its digest; the conservation arithmetic for both full corpora with all four counts; the export round-trip result including that quarantine survived it; the live inertness proof with BOTH legs and explicit confirmation the positive control fired; the Linux gate results as a delta with every residual failure named; the Task 4 measurement in full — the four counts, both promotion-cost points and the derived scaling verdict, the over-broad and under-broad counts, the ceiling-collision result, and the re-run positive control — together with the chosen option, its basis, the rule that bound it to the measurement, and the dissent in its own terms; any amendment that was made and its re-measurement; the recorded unknowns; and which of the three termination states the plan ended in. F26-02 is marked complete only if the Task 4 decision leg's gates all passed.
</output>

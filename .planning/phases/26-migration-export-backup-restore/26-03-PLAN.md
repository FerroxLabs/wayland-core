---
phase: 26-migration-export-backup-restore
plan: "03"
type: execute
wave: 2
depends_on:
  - "26-01"
files_modified:
  - crates/wcore-cli/src/backup/mod.rs
  - crates/wcore-cli/src/backup/archive.rs
  - crates/wcore-cli/src/backup/restore.rs
  - crates/wcore-cli/src/backup/journal.rs
  - crates/wcore-cli/src/backup/remap.rs
  - crates/wcore-cli/src/main.rs
  - crates/wcore-cli/src/lib.rs
  - crates/wcore-cli/tests/backup_restore_interrupt.rs
  - scripts/portability-interrupt-proof.sh
  - scripts/portability-interrupt-proof.ps1
  - scripts/portability-remap-capture.sh
  - .planning/phases/26-migration-export-backup-restore/panel/26-03-interruption-nonvacuity/
  - .planning/phases/26-migration-export-backup-restore/panel/26-03-remap-operability/
autonomous: true
requirements:
  - F26-03
  - F26-04
must_haves:
  truths:
    - "THERE IS NO BACKUP, RESTORE OR EXPORT COMMAND TODAY — VERIFIED AGAINST THE SHIPPED BINARY, NOT INFERRED. `wayland-core --help` lists `profile` and `migrate` and nothing else in this family. `profile export` and `profile import` exist and copy a home tree with secrets excluded by default, but there is no archive, no verification, no restore, and no rollback. This plan builds that surface; it does not refactor an existing one."
    - "THE PEER BAR IS PUBLISHED AND SPECIFIC, SO 'GOOD ENOUGH' IS NOT A JUDGEMENT CALL. OpenClaw's `docs/cli/backup.md` documents `backup create`, `backup verify`, a `manifest.json` embedded in the archive, refusal to overwrite an existing archive, rejection of output paths inside the source tree, traversal-path rejection on verify, exclusion of live-mutation paths during archive creation, and a restore that refuses an existing target and never replaces a live database in place. Hermes ships `hermes_cli/backup.py` (1440 lines) with a secret-file set and quick-snapshot restore. Core has none of it. These are the specific behaviors this plan must match, not aspirations."
    - "INTERRUPTION IS THE WHOLE POINT AND IT MUST BE REAL. Success Criterion 3 says these operations survive interruption and restore the EXACT pre-operation state on rollback. A test that calls a rollback function proves the function; only killing the real process mid-flight and then diffing the real tree proves the product. The kill must be uncatchable — a signal or terminate the process cannot trap — because an operation that only survives a graceful shutdown has not survived interruption."
    - "AN INTERRUPTION TEST IS VACUOUS UNLESS THE KILL DEMONSTRABLY LANDED MID-FLIGHT. If the operation finished before the kill arrived, the tree is trivially consistent and the gate goes green having proven nothing. Every interruption leg must independently establish that the operation was genuinely in progress when it died — the journal carries an open record, and the target was in an intermediate state — before its consistency assertion counts for anything."
    - "CORE ALREADY HAS BOTH PRIMITIVES THIS NEEDS AND THEY MUST BE REUSED. `crates/wcore-config/src/atomic_io.rs` provides `atomic_write` built on the sibling-tempfile-plus-rename pattern, with an existing test named for the invariant that a failed write leaves the original untouched. `crates/wcore-cli/src/crash_sentinel.rs` already establishes how this codebase detects an unclean exit: a per-process marker under the home, scanned at next startup, reported only when its owning pid is dead — scoped per process precisely because an unscoped marker made every concurrent engine report a false crash (#181). The operation journal follows that settled shape rather than inventing a third one."
    - "SECRET SOURCE REMAPPING IS A REAL PROBLEM WITH A CONCRETE SURFACE, NOT A SLOGAN. `CredentialsBackend` is `Auto`, `Plaintext`, `Keyring`, or `EncryptedFile { cipher_path, key_params_path }`. A home whose backend is `Keyring` keeps its secrets in the OS keychain, so they are NOT in the backed-up tree at all; a home whose backend is `EncryptedFile` keeps them in files at absolute paths that may not exist on the target. Restoring either one onto another machine without remapping produces a config that points at secrets which are not there — a restore that reports success and yields a broken install. F26-04 requires the remap to be EXPLICIT, which means declared and surfaced, never silently guessed."
    - "WINDOWS IS WHERE RESTORE EXACTNESS ACTUALLY BREAKS, AND PHASE 20A PROVED THAT THE HARD WAY. Windows cannot rename over a file another handle holds open, has different sharing semantics, and enforces path-length limits that a deep restored tree hits. Phase 20A drove Windows and macOS acceptance targets to CI-green while nobody launched the binary, and every real Windows defect it later found was a filesystem-semantics defect. An interruption-and-rollback contract proven only on Linux is a contract proven on the easy platform."
    - "A REPORTED RED IS WORTH FAR MORE THAN AN ENGINEERED GREEN. Never weaken an assertion, `#[ignore]`, `#[allow]`, raise a timeout, re-gate, or delete an inconvenient test to reach a gate. If rollback cannot be made exact on Windows within this plan's files, record that as a finding with its severity — do not narrow the definition of exact until it passes."
  artifacts:
    - path: crates/wcore-cli/src/backup/journal.rs
      provides: "The write-ahead operation journal and its recovery pass — the mechanism by which an interrupted operation is detected and undone to the exact pre-operation state"
    - path: crates/wcore-cli/src/backup/archive.rs
      provides: "Archive creation with an embedded manifest, no-overwrite publication, and refusal of an output path inside the source tree"
    - path: crates/wcore-cli/src/backup/restore.rs
      provides: "Verification-before-restore and a restore that refuses an occupied target rather than replacing state in place"
    - path: crates/wcore-cli/src/backup/remap.rs
      provides: "Explicit secret-source remapping across credential backends, so a restore onto a different machine declares where each secret will come from instead of silently pointing at one that is not there"
    - path: scripts/portability-interrupt-proof.ps1
      provides: "The Windows interruption-and-rollback proof, run on real hardware where restore exactness actually breaks"
    - path: scripts/portability-remap-capture.sh
      provides: "The per-backend capture of what a cross-machine restore actually TELLS an operator and actually DOES — the verbatim message, the exit status, whether the target was written, and whether a source-machine absolute path survived — so remap operability is measured rather than judged by eye"
  key_links:
    - from: crates/wcore-cli/src/backup/journal.rs
      to: crates/wcore-cli/src/crash_sentinel.rs
      via: "the per-process, dead-pid-scanned unclean-exit detection shape this codebase already settled in #181"
      pattern: "established-precedent"
    - from: crates/wcore-cli/src/backup/journal.rs
      to: crates/wcore-config/src/atomic_io.rs
      via: "every journal and target write uses the existing sibling-tempfile-plus-rename atomic write"
      pattern: "reuse-existing-primitive"
    - from: crates/wcore-cli/src/backup/remap.rs
      to: crates/wcore-config/src/credentials.rs
      via: "the four credential backends whose secret location must be declared rather than assumed on restore"
      pattern: "explicit-remap"
---

<objective>
Give Core backup, restore and rollback that survive a real, uncatchable interruption and restore the exact pre-operation state — proven by killing the real process mid-flight on Linux AND on real Windows and diffing the real tree.

Purpose: Success Criterion 3 is the only criterion in this phase that cannot be satisfied by any amount of correct-looking code, because what it asserts is a property of what happens when the code STOPS running. Core ships no backup, restore or rollback at all today, while both peers ship documented ones with specific published behaviors. This plan builds that surface against the peers' published bar and proves the interruption property the way it has to be proven: by interrupting.
Output: Archive creation with an embedded manifest and safe publication; verification before restore; a write-ahead operation journal whose recovery pass undoes an interrupted operation exactly; explicit secret-source remapping across the four credential backends; and interruption proofs on Linux and on real Windows, each of which independently establishes that the kill landed mid-flight before its consistency assertion counts.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md
@.planning/HANDOFF-2026-07-26-phase20-20A-complete.md
@crates/wcore-config/src/atomic_io.rs
@crates/wcore-cli/src/crash_sentinel.rs
@crates/wcore-config/src/credentials.rs
@crates/wcore-config/src/profile.rs
@crates/wcore-cli/src/main.rs
</context>

<execution_rules>

**THE TWO PHASE RULES — stated verbatim, and they bound this plan.**

- Findings at CRITICAL or HIGH must be fixed or disproved. MEDIUM and below are logged to `.planning/BACKLOG.md` and DO NOT BLOCK execution.
- Execution begins when no CRITICAL or HIGH finding is open, or after 2 review rounds, whichever comes first. A third round is NOT permitted; it escalates to Sean.

**TERMINATION CRITERION FOR THIS PLAN (hard).** This plan builds the surface ONCE and proves the interruption property ONCE per platform. It terminates in exactly one of four states, and in all four it writes its SUMMARY and stops:
1. **Complete** — archive, verify, restore, journal and remap exist; both interruption legs pass with their mid-flight landing independently established AND with each script's mid-flight check proven able to fire, by going red against a deliberately undersized fixture. No task in this plan halts for a human: the two questions that used to stop here are decided by measurement and a four-way panel in Task 4.
2. **Complete with a named platform gap** — the Linux leg passes and the Windows leg cannot be made exact within this plan's declared files. Record the exact defect and its severity, do NOT narrow the definition of exactness, and escalate the gap rather than shipping a weaker claim.
3. **Blocked on remap semantics** — no explicit remap can be defined for a backend without either moving a secret Core cannot read or producing a config pointing at a secret that is not there. STOP and escalate; a restore that silently yields a broken install is worse than a restore that refuses.
4. **Escalated** — a CRITICAL or HIGH finding is open that cannot be fixed inside this plan's declared files.
Under no circumstances does this plan spawn additional plans, extend its own task list, or begin the hostile corpora.

**SCOPE BOUNDARY (hard).** This plan owns backup, restore, rollback, the operation journal and secret remapping. Discovery and the typed plan model are 26-01's. Quarantine, provenance and selective import/export are 26-02's — do NOT touch `crates/wcore-cli/src/migrate/`, `crates/wcore-cli/src/profile.rs` or `crates/wcore-config/src/profile.rs`, which 26-02 owns in this same wave; a change there would collide. Hostile corpora and the native matrix are 26-04's; this plan runs Windows for the interruption property specifically, not for a platform matrix.

**NO NEW DEPENDENCY.** `tar`, `flate2`, `zip`, `sha2`, `serde`, `serde_json`, `tempfile`, `libc` and `fd-lock` are already in the workspace. Adding an external dependency is out of scope; escalate rather than installing.

**SECRETS NEVER LEAVE THE MAC.** Sean's real `~/.hermes` and `~/.openclaw` are not involved in this plan and are not copied anywhere. Every fixture home this plan builds is synthetic, created inside a temporary directory, and seeded with canary values only. Never point a backup or restore in this plan at a real home on any host.

**ENVIRONMENT.**
- **Mac (this host, `arm64`)** — CANNOT compile; never run `cargo build`/`test`/`check` here. `cargo fmt --all` works and is the local format gate. Use `/usr/bin/git` and ALWAYS `/usr/bin/grep`; prefer absolute `/usr/bin/` paths for `find`, `sed`, `diff` and `wc` in gates too, because `grep`, `find` and `ls` on this host resolve to an rtk proxy that silently drops or rewrites lines.
- **Linux — `ssh -o BatchMode=yes hetzner-dsm`** — authoritative build and gate host, and the first interruption leg. This plan works ONLY in its own worktree `/root/wayland-f26-03` and NEVER checks out the shared `/root/wayland`; see the remote gate shape below.
- **Windows — `ssh -o BatchMode=yes SeanD@seandesktop`**, checkout `C:\ferrox-win` (verified: a git clone, remote `origin` = `FerroxLabs/wayland-core`, no sibling worktrees), cargo at `C:\Users\seand\.cargo\bin\cargo.exe` (verified: cargo 1.95.0, `cargo-nextest` 0.9.138 — an absolute path, so the Linux `PATH` problem below does not apply there), PowerShell is the default shell. Git on the box MUST be wrapped as a `cmd /c` invocation, because PowerShell's error preference treats git's stderr chatter as fatal and this plan runs a great deal of git. `cargo fmt --all` FAILS there with os error 206 and the justfile already skips fmt-check on Windows.
- Push the WORK BRANCH to `gh` so both hosts can fetch it. NO push to main, merge, PR, tag, release, or issue closure without Sean.

**THE REMOTE GATE SHAPE — the wave-2 race is REAL, and three measured host facts close it.**

**This plan and 26-02 are both `wave: 2` and both depend only on 26-01, so they run CONCURRENTLY.** Each computes its own SHA from its own working-tree HEAD. If both detached the SAME `/root/wayland` tree, each would move it out from under the other's in-flight build, and the resulting green or red would not correspond to the SHA the plan claims — a proof that silently certifies the wrong commit. That is not hypothetical: the 20A handoff records that "concurrent compile load on one shared box corrupted a proof run" and prescribes the fix — "Give each concurrent agent its **own worktree** per host." `ci.yml`'s concurrency comment records the same expectation, that phases 24-27 execute in bounded parallel worktrees, so a THIRD phase can be on that box too.

1. **Per-plan worktree on Linux.** `/root/wayland` is used ONLY as the object store to fetch into, under an `flock` so two concurrent fetches cannot race on its refs. All work for this plan happens in `/root/wayland-f26-03`, which 26-02 never touches. Measured: per-agent worktrees are already that box's own convention — `git worktree list` shows `/root/wayland` plus twelve siblings — with 647 GB free, 96 cores and 251 GB RAM, so two concurrent worktree builds fit comfortably.
2. **`cargo` is NOT on `PATH` in a non-login `ssh` shell on `hetzner-dsm`.** Measured: `ssh hetzner-dsm 'cargo --version'` returns `bash: cargo: command not found`; that shell's `PATH` ends at `/snap/bin` with no `/root/.cargo/bin`. The toolchain IS present at `/root/.cargo/bin` (`cargo` 1.95.0 under this tree's pinned toolchain, `cargo-nextest` 0.9.137, `clippy` 0.1.95). Every remote command prepends it. Without that, every Linux gate here is GUARANTEED RED for a reason unrelated to the code.
3. **Windows keeps the shared `C:\ferrox-win`, and pays for it with an explicit SHA assertion instead.** This plan is the only wave-2 plan that touches Windows and 26-04's Windows work is in wave 3, so there is no intra-phase concurrency there — while a cold cargo target on that box is expensive enough that a throwaway worktree would cost far more than it buys. The bounded-parallel fan-out means another PHASE could still detach that tree, so every Windows gate asserts the checkout is at this plan's SHA immediately before the work and again immediately after, turning any interference into a red rather than a green on the wrong commit.

The Linux shape, used by every Linux gate in this plan:

`SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-03; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2>/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; «WORK»; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA" > «LOG» 2>&1; rc=$?; tail -30 «LOG»; test $rc -eq 0`

`set -e` makes any step's failure the ssh exit status; the status is captured on its own line and tested BEFORE the log is read; no filter ever sits between the command and its status. After this plan's last Linux gate passes, remove the worktree from `/root/wayland` with `git worktree remove --force /root/wayland-f26-03 && git worktree prune`. A FAILED gate leaves it for forensics.

**A FIFTH TRAP — MEASURED ON THE REAL BOX, AND IT MADE EVERY WINDOWS GATE IN AN EARLIER DRAFT OF THIS PLAN INCAPABLE OF GOING RED.** The Windows sshd default shell is PowerShell, so `ssh SeanD@seandesktop "powershell -NoProfile -Command \"…\""` passes through TWO PowerShell layers. The OUTER one expands `$LASTEXITCODE` — which is empty at that point — before the inner one ever sees it, so the inner `exit $LASTEXITCODE` runs with no argument and exits 0. Measured directly: `ssh … "powershell -NoProfile -Command \"cmd /c 'exit 42'; exit \$LASTEXITCODE\""` returns **0**, and an inner `if (\$LASTEXITCODE -ne 0)` arrives at the box as the syntax error `if ( -ne 0)`. Every `-Command` gate written that way reports success no matter what happens.

The two invocation forms need OPPOSITE escaping, and both were measured in both polarities:
- **`-Command` (two PowerShell layers)** — escape with a PowerShell backtick so the OUTER layer passes the variable through literally: written `` \`\$LASTEXITCODE `` in the gate. Measured: fails non-zero on `exit 42`, returns 0 on `exit 0`, and a per-step `if` fires and prints its marker.
- **`-File script.ps1; exit \$LASTEXITCODE` (one layer)** — do NOT backtick it. Here the outer PowerShell's own `$LASTEXITCODE` genuinely IS the child's exit code. Measured: returns 7 when the script exits 7, and 0 when it exits 0. Adding a backtick to this form makes the outer shell try to RUN `$LASTEXITCODE` as a command, which fails every time — a guaranteed red, which is just as useless.

One further measured consequence: the numeric exit code is FLATTENED to 1 by the `-Command` path, so the distinct per-step codes below do not survive as numbers. The `STEP-FAIL: <step>` marker each step prints DOES survive, and that marker — not the number — is what identifies which step failed.

**A SIXTH TRAP, ALSO PRESENT IN AN EARLIER DRAFT OF THIS PLAN.** Chaining several `cmd /c` calls in one PowerShell `-Command` and then reading `$LASTEXITCODE` reports only the status of the LAST one. A draft of the sync gate ran `git fetch`, then `git checkout --detach <SHA>`, then `git rev-parse HEAD`, and exited with the rev-parse's status — so a FAILED checkout still exited 0 while printing the OLD head, and the paired `grep -qF "$SHA"` could still match because a failed checkout's own error message quotes the SHA it could not find. That gate was green on a tree at the wrong commit. Every Windows step therefore checks its OWN status, and the SHA assertion compares the rev-parse OUTPUT ALONE — captured to its own file, isolated from any error text — against the expected value.

**THE MEASUREMENT TRAPS — ALL FOUR ARE LIVE IN THIS PLAN. Do not simplify them away.**
1. **Mac `grep` is rtk-proxied and SILENTLY DROPS LINES.** Every gate invokes `/usr/bin/grep` explicitly, `-F` for literals.
2. **A pipe replaces the exit status you care about.** The shape `ssh host 'cmd' | grep -v CLIXML` yields grep's status, not ssh's, so a failed remote build reports success — and this plan's Windows output is full of CLIXML noise, which is exactly the temptation. Filtering for READING is fine; filtering inside a GATE is forbidden. Use `ssh host 'powershell -NoProfile -File script.ps1; exit $LASTEXITCODE'`, redirect to a file, capture the return code on its own line, test it, and only then read the file.
3. **Reading an exit code from a block that also emits output.** A PowerShell block that tees output and then yields the last exit code returns an ARRAY of every output line plus the code, making a numeric comparison always truthy — this exact bug made an all-PASS Windows soak report failure. Read the status on the line AFTER the pipeline, never from inside a block that also emits.
4. **In `cmd`, an assignment chained with `&&` appends a TRAILING SPACE to the value** and Rust silently ignores it. Use the quoted assignment form or PowerShell's environment syntax, and PROVE the value took effect before trusting any run that depends on it.

**AGENTS.md discipline.** Surgical diffs. `thiserror` for the public backup error types, `anyhow` for CLI propagation, no `unwrap()` in production paths. All platform differences go through `wcore_config::shell` helpers or explicit `cfg!` branches — never a raw shell invocation. Every new file under 1000 lines. `cargo clippy` clean, `cargo fmt` clean.

**Git hygiene.** `/usr/bin/git` on the Mac. Stage the exact paths in `files_modified`, never `-A`, never `.`. No `Co-Authored-By` trailers.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build archive, verify and restore against the peers' published bar, and make secret sources explicitly remapped</name>
  <files>crates/wcore-cli/src/backup/mod.rs, crates/wcore-cli/src/backup/archive.rs, crates/wcore-cli/src/backup/restore.rs, crates/wcore-cli/src/backup/remap.rs, crates/wcore-cli/src/main.rs, crates/wcore-cli/src/lib.rs, crates/wcore-cli/tests/backup_restore_interrupt.rs</files>
  <read_first>crates/wcore-config/src/credentials.rs (the four `CredentialsBackend` variants and where each one actually stores a secret — this is the whole basis of remapping), crates/wcore-config/src/atomic_io.rs (`atomic_write` and the invariant its failure test asserts), crates/wcore-config/src/profile.rs (the home-tree layout, the secret directory and credentials-file classification, and `copy_tree_filtered`'s symlink refusal — the archive must classify the same way rather than inventing a second definition), crates/wcore-cli/src/main.rs (the `TopCmd` enum and how an existing synchronous subcommand such as `Migrate` is dispatched, so the new command matches the established shape), /Users/seandonahoe/dev/resources/openclaw/docs/cli/backup.md (the published peer bar this task is measured against — the embedded manifest, no-overwrite publication, self-inclusion rejection, traversal rejection on verify, and refusal to replace a live target in place)</read_first>
  <behavior>
    - Test 1: creating an archive from a synthetic home embeds a manifest naming every payload and its digest, and verification of that archive passes.
    - Test 2: verification FAILS on a tampered payload, on a traversal-style path inside the archive, and on a manifest that declares a payload the archive does not contain — three distinct rejections, each asserted separately.
    - Test 3: creating an archive never overwrites an existing file at the output path, and refuses an output path that lies inside the tree being archived.
    - Test 4: restore verifies before it writes, and refuses a target that already holds state rather than replacing it in place.
    - Test 5: a home whose credentials backend keeps secrets outside the tree is archived with that fact RECORDED — the archive declares which secrets are not carried, rather than appearing complete.
    - Test 6: restoring such an archive onto a different home surfaces an explicit remap decision for each secret source and refuses to silently emit a config pointing at a secret that is not present.
    - Test 7: restoring a home whose backend stores secrets at absolute paths rewrites those paths for the target home, or refuses — never leaves the source machine's absolute path in the restored config.
    - Test 8: an archive round trip on a home containing no secrets reproduces a byte-identical tree, so the exactness claim is anchored on the simple case before the hard ones.
  </behavior>
  <action>Write the tests first, from the behaviors above, and watch them fail.

Register the new top-level command alongside the existing ones, matching the dispatch shape the migrate command already uses, with subcommands for creating, verifying and restoring. This is the only file in `main.rs` this phase touches, so add the command and nothing else.

Build archive creation against the peer bar rather than against intuition, because that bar is published and specific. Embed a manifest that names every payload and its digest so verification has something to check against. Publish without overwriting: an existing file at the output path is a refusal, not a silent replacement. Refuse an output path inside the tree being archived, because an archive that contains itself grows until the disk does not. Classify secrets using the same constants the profile module already uses rather than writing a second definition that will drift from it.

Build verification so it can actually fail, and prove each rejection separately: a payload whose bytes do not match its declared digest, an archive entry whose path escapes the extraction root, and a manifest entry with no corresponding payload. One combined "verification fails on a bad archive" test hides which of the three is unimplemented.

Build restore so it verifies BEFORE it writes and refuses an occupied target rather than replacing state in place. A restore that half-overwrites a live home and then discovers the archive was corrupt has destroyed the thing the backup existed to protect.

Then do the part that is genuinely hard and is the reason F26-04 exists. Read the four credential backends and establish, for each, WHERE the secret actually lives: in the tree, in the OS keyring, or in files at absolute paths that the config names. A backend that keeps secrets in the OS keyring means the secrets are NOT in the backup at all — so an archive of such a home must RECORD that, and must not present itself as a complete capture. A backend that keeps secrets in files at absolute paths means those paths are machine-specific and cannot be carried verbatim to a different home. On restore, each secret source becomes an explicit decision that is surfaced to the operator and recorded — never guessed. Where no honest remap exists, refuse the restore with a message naming the backend and what the operator must do, because a restore that reports success and yields an install whose credentials resolve to nothing is the worst available outcome. If you find a backend for which no explicit remap can be defined at all, that is termination state 3: stop and escalate rather than inventing one.

Anchor exactness on the easy case before the hard ones: prove a secret-free home round-trips byte-identically, so that when a later test claims exactness for a harder case there is a known-good reference for what exact means.

Records evidence for F26-03 and F26-04; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; for f in crates/wcore-cli/src/backup/archive.rs crates/wcore-cli/src/backup/restore.rs; do test "$(/usr/bin/grep -vE '^[[:space:]]*(//|/\*|\*)' "$f" | /usr/bin/grep -cF 'atomic_write')" -ge "1" || { echo "GATE: $f does not call atomic_write outside comments - a target write is not going through the existing atomic primitive"; exit 1; }; done</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -vE '^[[:space:]]*(//|/\*|\*)' crates/wcore-cli/src/backup/remap.rs | /usr/bin/grep -cF 'CredentialsBackend')" -ge "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; for f in crates/wcore-cli/src/backup/mod.rs crates/wcore-cli/src/backup/archive.rs crates/wcore-cli/src/backup/restore.rs crates/wcore-cli/src/backup/remap.rs; do n=$(/usr/bin/wc -l &lt; "$f" | tr -d ' '); test "$n" -lt 1000 || { echo "OVER 1000 LINES: $f ($n)"; exit 1; }; done</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-03; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2&gt;/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; cargo build --locked --release -p wcore-cli --bin wayland-core; cargo clippy --locked -p wcore-cli --all-targets -- -D warnings; ./target/release/wayland-core backup --help &gt; /tmp/26-03-t1-help.txt 2&gt;&amp;1; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA" &gt; /tmp/26-03-t1.log 2&gt;&amp;1; rc=$?; tail -25 /tmp/26-03-t1.log; test $rc -eq 0 || exit 1; scp -o BatchMode=yes hetzner-dsm:/tmp/26-03-t1-help.txt /tmp/26-03-t1-help.txt &gt;/dev/null 2&gt;&amp;1 || { echo "GATE: could not fetch the isolated help capture"; exit 1; }; cat /tmp/26-03-t1-help.txt; test "$(/usr/bin/grep -c . /tmp/26-03-t1-help.txt)" -ge 3 || { echo "GATE INVALID: help output is trivial or empty"; exit 1; }; /usr/bin/grep -qiE '(^|[[:space:]])restore([[:space:]]|$)' /tmp/26-03-t1-help.txt &amp;&amp; /usr/bin/grep -qiE '(^|[[:space:]])verify([[:space:]]|$)' /tmp/26-03-t1-help.txt &amp;&amp; /usr/bin/grep -qiE '(^|[[:space:]])create([[:space:]]|$)' /tmp/26-03-t1-help.txt</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-03; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2&gt;/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; cargo nextest run --locked -p wcore-cli --no-fail-fast; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA" &gt; /tmp/26-03-t1-test.log 2&gt;&amp;1; rc=$?; tail -30 /tmp/26-03-t1-test.log; test $rc -eq 0</automated>
  </verify>
  <done>All eight behaviors exist as tests written before the implementation. The command is registered and its subcommands appear in the REAL binary's help output on Linux, proven by running the product. Archive creation embeds a manifest, refuses to overwrite, and refuses an output path inside the source tree. Verification's three rejections — tampered payload, traversal path, missing declared payload — are asserted separately. Restore verifies before writing and refuses an occupied target. All four credential backends are analysed for where the secret actually lives; an archive that cannot carry its secrets records that fact; restore surfaces an explicit remap per secret source and refuses rather than emitting a config pointing at an absent secret; absolute source paths are never carried verbatim. A secret-free home round-trips byte-identically. Secret classification reuses the profile module's constants rather than a second definition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the write-ahead operation journal and prove exact rollback by killing the real process mid-flight on Linux</name>
  <files>crates/wcore-cli/src/backup/journal.rs, crates/wcore-cli/src/backup/restore.rs, crates/wcore-cli/src/backup/mod.rs, crates/wcore-cli/tests/backup_restore_interrupt.rs, scripts/portability-interrupt-proof.sh</files>
  <read_first>crates/wcore-cli/src/crash_sentinel.rs (the whole file — the per-process marker under the home, the next-startup scan, the dead-pid check, and specifically WHY #181 made it per-process: an unscoped marker made every concurrent engine report a false crash, and a journal has exactly the same hazard), crates/wcore-config/src/atomic_io.rs (`atomic_write` and its failure invariant — every journal record and every target write goes through it), crates/wcore-cli/tests/smoke_p0.rs (how this codebase spawns and drives the REAL binary from a test, including the process-handling idiom)</read_first>
  <behavior>
    - Test 1: every mutating operation writes an intent record BEFORE touching the target, and the record is durable before the first mutation happens.
    - Test 2: the journal is scoped per operation and per process the way the crash sentinel is, so a concurrent unrelated operation cannot make this one appear interrupted.
    - Test 3: a completed operation leaves no open record.
    - Test 4: the recovery pass detects an open record whose owning process is dead and undoes that operation to the exact pre-operation state, asserted by tree digest equality against a digest taken before the operation began.
    - Test 5: recovery is idempotent — running it twice leaves the same state as running it once.
    - Test 6: recovery does NOT touch an open record whose owning process is still alive, mirroring the crash sentinel's dead-pid rule.
    - Test 7 (the live one): the REAL binary is spawned performing a restore, killed uncatchably mid-flight, and after recovery the tree digests identically to the pre-operation digest — with the mid-flight landing independently established before the digest comparison is allowed to count.
    - Test 8: an operation interrupted by a partial failure rather than a kill — a target that becomes unwritable partway — also rolls back to the exact pre-operation state.
  </behavior>
  <action>Write the tests first, and watch them fail.

Build the journal on the shape this codebase already settled rather than a new one. The crash sentinel writes a marker under the home, scans at next startup, and acts only on markers whose owning process is dead — and it is scoped per process for a reason recorded in source: an unscoped marker made every concurrent engine falsely report a crash. A journal has precisely the same hazard, so scope every record to its operation AND its owning process, and act only on records whose owner is dead. Write every record through the existing atomic write so a torn record cannot itself become the corruption the journal exists to prevent, and make the intent record durable BEFORE the first mutation — a journal written after the fact records history, not intent, and cannot roll anything back.

Record enough to undo. For each target the operation will touch, capture what is needed to restore it exactly: its prior content or a reference to a preserved copy, its prior absence if it did not exist, and its metadata where that is part of exactness. Then the recovery pass reads open records whose owner is dead and restores each target, in reverse order, and closes the record only once the undo is durable.

Then prove it by interrupting, and build the proof so it cannot pass vacuously. Take a digest of the target tree before the operation. Spawn the REAL binary performing a restore large enough that it cannot plausibly complete instantly — the fixture home must be sized so the operation genuinely takes time — then kill it uncatchably, using a signal or terminate mechanism the process cannot trap, because an operation that only survives a graceful shutdown has not survived interruption. Now, BEFORE comparing anything, establish that the kill actually landed mid-flight: the journal must hold an open record, and the target must be observably in an intermediate state. If either check shows the operation had already completed, the leg has proven nothing and must be re-run with a larger fixture — do not let it report success. Only once mid-flight landing is established does the assertion count: run recovery, digest the tree again, and require byte equality with the pre-operation digest.

Then prove the same for a partial failure rather than a kill, because interruption in the field is more often a full disk or a permission change than a signal, and a rollback that handles only signals is half a rollback.

Write the Linux leg as a committed script so the same procedure can be run by hand and by the gate, and so the Windows leg in Task 3 can mirror it step for step rather than diverging. Make the script fail loudly on its own failure modes: a pre-operation digest that could not be taken, a spawn that did not start, a kill that arrived after completion, and a recovery that exited non-zero must each fail it.

Records evidence for F26-03 and F26-04; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -vE '^[[:space:]]*(//|/\*|\*)' crates/wcore-cli/src/backup/journal.rs | /usr/bin/grep -cF 'atomic_write')" -ge "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f scripts/portability-interrupt-proof.sh &amp;&amp; sh -n scripts/portability-interrupt-proof.sh &amp;&amp; test "$(/usr/bin/grep -vc '^[[:space:]]*#' scripts/portability-interrupt-proof.sh)" -ge 30 &amp;&amp; { sh scripts/portability-interrupt-proof.sh /nonexistent/wayland-core-does-not-exist &gt; /tmp/26-03-selfcheck.log 2&gt;&amp;1; rc=$?; cat /tmp/26-03-selfcheck.log; test $rc -ne 0 || { echo "GATE: the interruption proof reported SUCCESS when handed a binary that does not exist - it cannot go red, so its green on Linux would prove nothing"; exit 1; }; }</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; n=$(/usr/bin/wc -l &lt; crates/wcore-cli/src/backup/journal.rs | tr -d ' '); test "$n" -lt 1000</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-03; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2&gt;/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; cargo nextest run --locked -p wcore-cli --no-fail-fast -E 'test(/interrupt/)'; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA" &gt; /tmp/26-03-t2-int.log 2&gt;&amp;1; rc=$?; cat /tmp/26-03-t2-int.log; test $rc -eq 0 &amp;&amp; test "$(/usr/bin/grep -cE '[1-9][0-9]* (test|tests) run' /tmp/26-03-t2-int.log)" -ge "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-03; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2&gt;/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; cargo build --locked --release -p wcore-cli --bin wayland-core; sh scripts/portability-interrupt-proof.sh ./target/release/wayland-core; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA" &gt; /tmp/26-03-t2-live.log 2&gt;&amp;1; rc=$?; cat /tmp/26-03-t2-live.log; test $rc -eq 0</automated>
  </verify>
  <done>All eight behaviors exist as tests written before the implementation. The journal writes durable intent BEFORE the first mutation, through the existing atomic write, scoped per operation and per process with the dead-pid rule mirroring the crash sentinel's #181 reasoning. Recovery is idempotent, leaves live-owner records alone, and restores exactly. The live Linux leg spawned the REAL binary, killed it uncatchably, INDEPENDENTLY established that the kill landed mid-flight via an open journal record and an observably intermediate target before comparing anything, and then proved tree-digest equality with the pre-operation digest. The partial-failure leg proves the same for an unwritable target. The committed script fails loudly on a missing pre-digest, a failed spawn, a late kill, or a non-zero recovery. The interrupt-filtered gate asserts a non-zero test count actually ran.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the same interruption and exact-rollback contract on real Windows, where restore exactness actually breaks</name>
  <files>scripts/portability-interrupt-proof.ps1, crates/wcore-cli/src/backup/restore.rs, crates/wcore-cli/src/backup/journal.rs</files>
  <read_first>scripts/portability-interrupt-proof.sh (the Linux procedure this must mirror step for step, so a difference in result is a platform difference and not a procedure difference), .planning/HANDOFF-2026-07-26-phase20-20A-complete.md section 7 (the environment traps for the Windows box, measured rather than assumed), crates/wcore-cli/src/backup/restore.rs (the write path whose atomicity assumptions Windows will test), crates/wcore-config/src/atomic_io.rs (whether the sibling-tempfile-plus-rename pattern holds when another handle has the target open, which is precisely the Windows question)</read_first>
  <behavior>
    - The Windows script mirrors the Linux procedure step for step, so any divergence in outcome is attributable to the platform rather than to a different test.
    - The real binary is built on the Windows box at this SHA and the interruption is performed against that binary.
    - The kill is uncatchable on Windows, not a graceful request the process could handle.
    - Mid-flight landing is independently established before any consistency assertion counts, exactly as on Linux.
    - After recovery the tree digests identically to the pre-operation digest, with the digest computed identically on both platforms so the comparison is meaningful.
    - A target held open by another handle during restore is exercised, because that is the specific case where the rename-based atomic write behaves differently on Windows than on Linux.
    - Deep and long paths in the restored tree are exercised, because Windows enforces limits Linux does not and a restored tree is exactly where they bite.
    - Every remote invocation returns the real status of the work, never the status of a filter.
  </behavior>
  <action>Mirror the Linux procedure rather than writing a second, different test. A Windows leg that measures something else cannot tell you whether Windows behaves differently — it only tells you two unrelated things passed.

Build the binary on the Windows box at this SHA and run the interruption against that binary. Kill it with a mechanism the process cannot trap; a graceful stop request proves the shutdown path, not the interruption path. Establish mid-flight landing independently before any comparison counts, the same way the Linux leg does, and size the fixture so the operation genuinely takes time on that hardware — the box is not the same speed as Hetzner, so a fixture tuned for Linux may complete before the kill lands and hand you a vacuous green.

Then exercise the two cases where Windows genuinely differs and Linux cannot warn you. First, hold the target open with another handle during the restore: the sibling-tempfile-plus-rename pattern that makes the atomic write atomic on Linux behaves differently when Windows will not rename over a file another handle holds, and the restore's whole exactness claim rests on that write. Second, restore a tree with deep and long paths, because a restored tree reconstructs full paths under a new root and that is exactly where Windows path limits bite — a backup that cannot restore its own deepest file is a backup that does not work.

Compute the digest identically on both platforms — normalize path separators and line-ending representation in the digest input, or the comparison is measuring text encoding rather than content, and you will chase a difference that is not a defect. Task 26-01 established the digest; use it rather than a second one.

Invoke everything so the status you read is the status of the work. Run the script through PowerShell with no profile and have the invocation exit with the script's own status; redirect output to a file; capture the return code on its own line; test that code; and only then read the file. Do not place a filter between the command and its status — the box's output carries serialization noise that makes filtering tempting, and that is the exact shape that reported success for a failed remote build. Do not read a status from a block that also emits output — that returns an array and makes the comparison always truthy, which is the bug that made an all-PASS soak report failure. Set any environment variable with the quoted form and prove it took effect before trusting a run that depends on it.

If the Windows leg cannot be made exact within this plan's declared files, that is termination state 2: record the exact defect and its severity, do NOT narrow the definition of exactness to make it pass, and escalate the gap. Also state the `wcore-cli` counts on the box as a delta against what the Linux run measured, naming every residual failure and attributing anything that belongs to another plan rather than absorbing it.

Records evidence for F26-03 and F26-04.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f scripts/portability-interrupt-proof.ps1 &amp;&amp; test "$(/usr/bin/grep -vc '^[[:space:]]*#' scripts/portability-interrupt-proof.ps1)" -ge "30"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes SeanD@seandesktop "powershell -NoProfile -Command \"Set-Location C:\ferrox-win; cmd /c 'git fetch --all --prune 2>&amp;1'; if (\`\$LASTEXITCODE -ne 0) { Write-Output 'STEP-FAIL: fetch'; exit 11 }; cmd /c 'git checkout --detach $SHA 2>&amp;1'; if (\`\$LASTEXITCODE -ne 0) { Write-Output 'STEP-FAIL: checkout'; exit 12 }; cmd /c 'git rev-parse HEAD &gt; C:\ferrox-win\f26-head.txt'; if (\`\$LASTEXITCODE -ne 0) { Write-Output 'STEP-FAIL: rev-parse'; exit 13 }; exit 0\"" &gt; /tmp/26-03-t3-sync.log 2&gt;&amp;1; rc=$?; cat /tmp/26-03-t3-sync.log; test $rc -eq 0 || exit 1; scp -o BatchMode=yes SeanD@seandesktop:C:/ferrox-win/f26-head.txt /tmp/26-03-t3-head.txt &gt;/dev/null 2&gt;&amp;1 || { echo "GATE: could not fetch the isolated rev-parse capture"; exit 1; }; ACTUAL=$(tr -d ' \r\n' &lt; /tmp/26-03-t3-head.txt); echo "windows_head=[$ACTUAL] expected=[$SHA]"; test "$ACTUAL" = "$SHA"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes SeanD@seandesktop "powershell -NoProfile -Command \"Set-Location C:\ferrox-win; cmd /c 'C:\Users\seand\.cargo\bin\cargo.exe build --locked --release -p wcore-cli --bin wayland-core 2>&amp;1'; if (\`\$LASTEXITCODE -ne 0) { Write-Output 'STEP-FAIL: build'; exit 21 }; cmd /c 'git rev-parse HEAD &gt; C:\ferrox-win\f26-head-after-build.txt'; if (\`\$LASTEXITCODE -ne 0) { Write-Output 'STEP-FAIL: rev-parse'; exit 22 }; exit 0\"" &gt; /tmp/26-03-t3-build.log 2&gt;&amp;1; rc=$?; tail -25 /tmp/26-03-t3-build.log; test $rc -eq 0 || exit 1; scp -o BatchMode=yes SeanD@seandesktop:C:/ferrox-win/f26-head-after-build.txt /tmp/26-03-t3-head2.txt &gt;/dev/null 2&gt;&amp;1 || exit 1; ACTUAL=$(tr -d ' \r\n' &lt; /tmp/26-03-t3-head2.txt); echo "windows_head_after_build=[$ACTUAL] expected=[$SHA]"; test "$ACTUAL" = "$SHA"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; ssh -o BatchMode=yes SeanD@seandesktop "powershell -NoProfile -File C:\ferrox-win\scripts\portability-interrupt-proof.ps1 -Binary C:\nonexistent\wayland-core-does-not-exist.exe; exit \$LASTEXITCODE" &gt; /tmp/26-03-t3-selfcheck.log 2&gt;&amp;1; rc=$?; cat /tmp/26-03-t3-selfcheck.log; test $rc -ne 0 || { echo "GATE: the Windows interruption proof reported SUCCESS when handed a binary that does not exist - it cannot go red, so its green would prove nothing about Windows"; exit 1; }</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes SeanD@seandesktop "powershell -NoProfile -File C:\ferrox-win\scripts\portability-interrupt-proof.ps1 -Binary C:\ferrox-win\target\release\wayland-core.exe; exit \$LASTEXITCODE" &gt; /tmp/26-03-t3-int.log 2&gt;&amp;1; rc=$?; cat /tmp/26-03-t3-int.log; test $rc -eq 0 || exit 1; ssh -o BatchMode=yes SeanD@seandesktop "powershell -NoProfile -Command \"Set-Location C:\ferrox-win; cmd /c 'git rev-parse HEAD &gt; C:\ferrox-win\f26-head-after-int.txt'; if (\`\$LASTEXITCODE -ne 0) { Write-Output 'STEP-FAIL: rev-parse'; exit 23 }; exit 0\"" &gt; /dev/null 2&gt;&amp;1 || exit 1; scp -o BatchMode=yes SeanD@seandesktop:C:/ferrox-win/f26-head-after-int.txt /tmp/26-03-t3-head3.txt &gt;/dev/null 2&gt;&amp;1 || exit 1; ACTUAL=$(tr -d ' \r\n' &lt; /tmp/26-03-t3-head3.txt); echo "windows_head_after_interrupt=[$ACTUAL] expected=[$SHA]"; test "$ACTUAL" = "$SHA"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; ssh -o BatchMode=yes SeanD@seandesktop "powershell -NoProfile -Command \"Set-Location C:\ferrox-win; cmd /c 'C:\Users\seand\.cargo\bin\cargo.exe nextest run --locked -p wcore-cli --no-fail-fast 2>&amp;1'; exit \`\$LASTEXITCODE\"" &gt; /tmp/26-03-t3-test.log 2&gt;&amp;1; rc=$?; tail -30 /tmp/26-03-t3-test.log; test $rc -eq 0</automated>
  </verify>
  <done>The Windows script mirrors the Linux procedure step for step. The binary was built on the box at this SHA, the kill was uncatchable, mid-flight landing was independently established before any comparison counted, and the fixture was sized for that hardware rather than reused blindly from Linux. The open-handle case and the deep-and-long-path case were both exercised, because those are where Windows genuinely differs. Digests were computed identically on both platforms so the comparison measures content. Every remote invocation returned the status of the work — script status propagated through the invocation, redirected to a file, return code captured on its own line and tested before the file was read, no filter between command and status, and no status read from a block that also emits. Counts are stated as a delta against Linux with every residual failure named and attributed. If exactness could not be reached, the defect and severity are recorded and escalated WITHOUT narrowing the definition.</done>
</task>

<task type="auto">
  <name>Task 4 (TWO CROSS-AUDITED AUTONOMOUS DECISIONS): Prove the interruption legs non-vacuous by NEGATIVE control, measure what the remap actually tells an operator, and settle both on the four-way panel</name>
  <files>scripts/portability-interrupt-proof.sh, scripts/portability-interrupt-proof.ps1, scripts/portability-remap-capture.sh, .planning/phases/26-migration-export-backup-restore/panel/26-03-interruption-nonvacuity/, .planning/phases/26-migration-export-backup-restore/panel/26-03-remap-operability/, .planning/phases/26-migration-export-backup-restore/26-03-SUMMARY.md</files>
  <read_first>scripts/portability-interrupt-proof.sh and scripts/portability-interrupt-proof.ps1 as Tasks 2 and 3 built them (specifically the mid-flight landing check, because this task's whole first half is proving that check FIRES rather than trusting that it exists), crates/wcore-config/src/credentials.rs (the `CredentialsBackend` enum — `Auto`, `Plaintext`, `Keyring` and `EncryptedFile { cipher_path, key_params_path }`, verified present at plan time — because the remap capture builds one synthetic home per variant), crates/wcore-cli/src/backup/remap.rs as Task 1 built it (the message and the refusal path being measured), /Users/seandonahoe/dev/resources/openclaw/docs/cli/backup.md and /Users/seandonahoe/dev/resources/hermes-agent/hermes_cli/backup.py (the REAL peer checkouts, verified present at 133 and 1440 lines respectively — the published bar Core's operator message is compared against, so the readability judgement is made against a real product rather than against taste), scripts/panel-ask.sh and scripts/panel-decision-check.sh (the phase's panel harness, built and self-tested in 26-01 — this task CALLS it and does not rebuild it)</read_first>
  <behavior>
    - The interruption proof's mid-flight check is proven to FIRE, by running each platform's script against a deliberately UNDERSIZED fixture where the kill cannot land mid-flight and requiring the script to exit NON-ZERO with a named late-kill reason. A mid-flight check that never fires is decorative, and a decorative check makes the real green worthless.
    - Kill uncatchability is measured rather than asserted: the target installs a handler for the catchable mechanism and the evidence records that the handler did not fire.
    - Whether the Windows fixture was sized for that hardware is a NUMBER — expected operation duration against the moment the kill landed — not a judgement.
    - Cross-platform digest identity is checkable: both captures name the digest algorithm and normalization identifier, and the gate requires those to be byte-equal before it believes a digest comparison.
    - What a cross-machine restore TELLS an operator, and what it DOES, are captured per credential backend: the verbatim message, the exit status, whether the target was written, and whether a source-machine absolute path survived into the restored config.
    - The published peer messaging is captured from the REAL peer checkouts and put in front of the panel, so Core's message is judged against a shipping bar.
    - Both questions go to all four panel members as their own shared bundles, both are committed to, and both dissents survive.
  </behavior>
  <decision>Two questions, decided separately because they turn on different evidence. FIRST: did both interruption legs prove exact rollback from a genuinely mid-flight kill, or is either leg vacuous? SECOND: can an operator act on what a cross-machine restore tells them about secrets the archive could not carry?</decision>
  <context>
Both questions are places where a green gate can be technically true and practically worthless, which is exactly why this plan stopped here — and both are answerable by measurement plus interpretation rather than by waiting.

**On the first.** If the operation completed before the kill arrived, everything downstream is trivially consistent and the gate goes green having proven nothing. This plan already requires each leg to establish mid-flight landing before any comparison counts. What it did not do is prove that requirement can FAIL. That is the gap this task closes with a negative control: run the same script against a fixture too small for the kill to land mid-flight, and require the script to detect the late kill and exit non-zero. This is the exact counterpart of the positive control 26-02 pairs with its inertness proof, applied to interruption, and it is what turns "the mid-flight check exists" into "the mid-flight check works".

**On the second.** A remap that is technically explicit but practically unreadable gets skipped, and a refusal path that warns and continues is not a refusal — it is a restore that reports success and yields an install whose credentials resolve to nothing, which this plan already names as the worst available outcome. Both halves are measurable: whether the message names the backend, names how many credentials will be absent and names an action is machine-checkable against the captured text, and whether a refusal actually refuses is visible in the exit status and in whether the target tree changed at all.

Neither question is reserved substance. No credential is supplied, nothing is merged, pushed, tagged, released or closed, and every fixture home this task builds is synthetic and canary-seeded. So both are measured, put to the panel, decided, recorded with their dissent, and the plan continues into its SUMMARY in the same session.

The plan's own termination states are preserved exactly and are NOT dissolved by either panel. A Windows leg that is genuinely non-vacuous but cannot reach exactness is still termination state 2, recorded as a named defect with a severity and escalated — never reached by narrowing what exact means. A backend for which no explicit remap can be defined at all is still termination state 3. But note precisely what state 3 is not: a REFUSAL with a message naming the backend and the operator's next action is an honest remap outcome this plan already requires, so state 3 cannot be reached merely because a backend has no automatic remap.
  </context>
  <options>
    <option id="both-legs-sound">
      <name>DECISION A — both interruption legs are non-vacuous and exact</name>
      <pros>Each leg independently evidenced an open journal record and an intermediate target before any comparison; each script's mid-flight check was proven to fire against an undersized fixture; both kills were measurably uncatchable; both digests are equal and computed under the same named algorithm and normalization.</pros>
      <cons>None, if it is true — but it is the claim most easily made falsely, because a vacuous leg looks identical to a sound one from the outside.</cons>
    </option>
    <option id="linux-leg-vacuous">
      <name>DECISION A — the Linux leg is vacuous and must be re-run with a larger fixture</name>
      <pros>Names the failure instead of accepting a trivially consistent tree as proof. Cheap to fix: the fixture size is the only variable.</pros>
      <cons>The Linux leg is the one everything else is anchored on, so a vacuous Linux leg puts the whole rollback claim back to zero until it is re-run.</cons>
    </option>
    <option id="windows-leg-vacuous">
      <name>DECISION A — the Windows leg is vacuous and must be re-run with a fixture sized for that hardware</name>
      <pros>This is the specific trap this plan predicted: a fixture tuned on Hetzner can complete before the kill lands on the Windows box, producing a silent vacuous green on the platform that matters most.</pros>
      <cons>Re-running costs a cold build cycle on that box; naming it is still far cheaper than shipping the claim.</cons>
    </option>
    <option id="windows-exactness-gap">
      <name>DECISION A — the Windows leg is non-vacuous but exactness could not be reached: termination state 2</name>
      <pros>The plan's own honest outcome for a real platform defect. The defect is named with a severity and escalated, and the definition of exact is left alone.</pros>
      <cons>F26-03 and F26-04 cannot claim the Windows half; the gap is inherited by name rather than absorbed.</cons>
    </option>
    <option id="remap-actionable">
      <name>DECISION B — the remap is one an operator can act on</name>
      <pros>Every message names the backend, names how many credentials will not be present and names the action; every refusal genuinely refuses, leaving the target untouched; no source-machine absolute path survives into a restored config.</pros>
      <cons>None, if it is true — and it is measurable, so the panel's job here is readability, not existence.</cons>
    </option>
    <option id="remap-opaque">
      <name>DECISION B — technically explicit but not actionable; the message must be rewritten and re-measured</name>
      <pros>A correct-but-opaque notice is one a user clicks past into a broken install, so this is a real defect with a bounded fix inside this plan's own files.</pros>
      <cons>Costs a rewrite and a re-capture; the operator-facing text becomes something later phases must not regress.</cons>
    </option>
    <option id="remap-warns-and-continues">
      <name>DECISION B — the refusal path does not refuse</name>
      <pros>Names the worst available outcome exactly: a restore that reports success and yields an install whose credentials resolve to nothing.</pros>
      <cons>It is a critical defect this plan must FIX, not an outcome it may record — so choosing it is choosing the work.</cons>
    </option>
    <option id="remap-blocked">
      <name>DECISION B — no explicit remap can be defined for some backend at all: termination state 3</name>
      <pros>Stops rather than inventing a semantics that silently yields a broken install.</pros>
      <cons>Reachable only when even a REFUSAL cannot be built for that backend, since a refusal naming the backend and the operator's next action is an honest outcome this plan already requires. It is escalated with the measurement, not asserted.</cons>
    </option>
  </options>
  <action>**BOTH HALTS ARE GONE AND WHAT REPLACES THEM IS HEAVIER, NOT LIGHTER.** Do not put either question in front of Sean and wait. Measure both, run two panels, commit to both, record both dissents, and continue into the SUMMARY in the same session. Escalation exists for a genuine deadlock, for termination state 2, and for termination state 3 — and each of those escalations carries its measurement and its split, never a bare question.

**PART A, STEP 1 — PROVE THE MID-FLIGHT CHECK CAN FIRE.** Add a negative-control mode to both proof scripts — `--undersized` on the shell script, `-Undersized` on the PowerShell one — which runs the IDENTICAL procedure with a fixture deliberately too small for the kill to land mid-flight. In that mode the operation completes before the kill, the script's own mid-flight check must detect it, and the script must exit NON-ZERO naming the late kill. If it exits zero, the mid-flight check never fires and the real green means nothing — that is the finding, and it is fixed here rather than reported around.

Run the negative control on BOTH hosts. On Windows use the single-layer invocation form, passing the script with `-File` and appending the exit propagation WITHOUT a PowerShell backtick, because in that form the outer shell's own last exit code genuinely is the child's — backticking it there makes the shell try to run the variable as a command, which is a guaranteed red and just as useless as a guaranteed green.

**PART A, STEP 2 — MAKE THE EVIDENCE MACHINE-READABLE, PRINTED BY THE SCRIPT.** Both scripts print their own verdict block; the executor never types it. Copy each block back to the interruption panel directory as `interrupt-evidence-linux.txt` and `interrupt-evidence-windows.txt`, and each negative-control run's output as `interrupt-negctl-linux.txt` and `interrupt-negctl-windows.txt`. The grammar is fixed and the gates read it: `INTERRUPT-PLATFORM:` naming `linux` or `windows`; `KILL-MECHANISM:` naming the mechanism used followed by `CATCHABLE: no`; `KILL-HANDLER-PROBE: installed=yes fired=no`, which is the MEASUREMENT of uncatchability rather than the claim — the target installs a handler for the catchable mechanism and the probe records that it did not run; `MIDFLIGHT-JOURNAL-OPEN:` and `MIDFLIGHT-TARGET-INTERMEDIATE:` each `yes` or `no`; `MIDFLIGHT-TIMING: op_expected_ms=<n> kill_at_ms=<n> completed_before_kill=no`, which turns "was the Windows fixture sized for that hardware" into two numbers on the box that ran it; `DIGEST-ALGO:` naming the algorithm and normalization identifier that 26-01 established; `DIGEST-PRE:` and `DIGEST-POST:` each a hex digest; and `DIGEST-EQUAL:` `yes` or `no`. The negative-control captures carry `NEGATIVE-CONTROL: late-kill-detected` or `late-kill-missed` plus `NEGCTL-EXIT:` with the script's own status.

A gate below requires the two platforms' `DIGEST-ALGO` values to be byte-equal before it believes any digest comparison, because a comparison across differently-normalized digests measures encoding rather than content — and that is a difference you would chase for a day.

**PART B, STEP 1 — CAPTURE WHAT THE REMAP ACTUALLY SAYS AND ACTUALLY DOES.** Build `scripts/portability-remap-capture.sh`, taking a binary path and an output directory. For each of the four credential backends it builds a synthetic, canary-seeded source home using that backend, archives it, and restores it onto a DIFFERENT home — the cross-machine case, which is the one the remap exists for. It writes one record per backend and prints them itself: `REMAP-BACKEND:` naming the variant; `REMAP-EXIT:` the restore's status; `REMAP-DISPOSITION:` `remapped` or `refused`; `REMAP-TARGET-WRITTEN:` `yes` or `no`, MEASURED by digesting the target tree before and after rather than inferred from the message, because that is how a warn-and-continue is caught; the verbatim operator-facing message between a `REMAP-MESSAGE-BEGIN` and `REMAP-MESSAGE-END` fence; `REMAP-NAMES-BACKEND:`, `REMAP-NAMES-COUNT:` and `REMAP-NAMES-ACTION:` each `yes` or `no`, derived by the script from the captured text; and `REMAP-CARRIES-SOURCE-ABSOLUTE-PATH:` `yes` or `no`, derived by searching the restored config for the source home's absolute secret paths. Give it the same self-red proof the other scripts carry: handed a binary that does not exist it exits non-zero.

**PART B, STEP 2 — CAPTURE THE PEER BAR FROM THE REAL CHECKOUTS.** Both peers ship this and both are on this host, so operability is compared against a shipping product rather than against taste. Read the real OpenClaw `docs/cli/backup.md` and the real Hermes `hermes_cli/backup.py` at the paths named in `read_first`, extract what each one tells an operator about secrets a backup could not carry and about a restore it refuses, and write those excerpts with their source paths and the checkouts' commit SHAs to `peer-bar.txt` in the remap panel directory. Read the peer sources read-only: never commit into them, never mutate them, never leave a detached checkout behind.

**STEP 3 — TWO PANELS, TWO BUNDLES, ONE PROCEDURE.** Follow THE PANEL PROCEDURE that 26-01 established, twice, with the panel directories `panel/26-03-interruption-nonvacuity/` and `panel/26-03-remap-operability/`. Each gets its own `question.md` written ONCE: the question exactly as this task's `decision` element states it for that half; that half's option ids with their names, pros and cons copied VERBATIM from the `options` element above with nothing added, softened, dropped or re-scoped; and that half's evidence — for A, the four interruption captures plus both negative-control captures; for B, the four per-backend records with their verbatim messages plus `peer-bar.txt`. Present the options in a rotated order rather than this plan's order. Hand each bundle to the three external members through `scripts/panel-ask.sh`, and write your own adversarial pass to `internal.md` in each, argued AGAINST whichever option you walked in believing and engaging at least two option ids of that half by name.

Every fixture home here is synthetic and canary-seeded and no real peer home is involved in this plan at all, so no real secret can reach the bundle — and a gate below proves that rather than asserting it.

**THREE CLI BEHAVIOURS WERE MEASURED DIRECTLY ON 2026-07-26 AND EACH ONE SILENTLY DROPS A VOTE IF IGNORED.** A four-way audit that is really three-way is the same defect class as a self-passing gate: a verification that appears to run and does not. First, `gemini` REFUSES without `--skip-trust` — measured exit status 55 with ZERO bytes on stdout and a trusted-directory refusal on stderr — while with the flag it answers cleanly on stdout and its `GOOGLE_API_KEY` notice goes to stderr, where it is a notice and never grounds for recording the member NOT-RUN. Second, `kimi` prefixes its verdict with a bullet and a space and indents the continuation line, so an ANCHORED column-zero match returns 0 on a real capture where the same unanchored match returns 1; extraction must be UNANCHORED or kimi's vote vanishes. Third, `codex` repeats its whole final block, so a real capture carries the SAME verdict twice; extraction must take the LAST occurrence and TOLERATE a repeated identical verdict, while two DIFFERENT verdict values in one capture remain a genuinely ambiguous capture that must be rejected. 26-01 meets these same three shapes first, in wave 1, so a harness that cannot survive them cannot have gone green there; if the trap gate below is red, that is a 26-01 defect to record and escalate rather than a file for this plan to edit while its wave-2 sibling 26-02 is running.

**STEP 4 — DECIDE BOTH, AND LET THE MEASUREMENT BREAK TIES.** Write a `DECISION.md` in each panel directory in the grammar 26-01's checker enforces: one `CHOSEN:` from that half's own option set; `BASIS: majority` or `BASIS: minority-with-evidence`; an `EVIDENCE:` naming a real non-empty artifact whenever the choice is not the majority verdict; a `RATIONALE:` of at least 120 characters; and a `## DISSENT` section naming every captured verdict that differs from the chosen one, with its argument in its own terms. The checker derives the dissent set from the CAPTURED verdicts rather than from prose, so dissent cannot be disposed of by omission.

The measurement outranks the argument in both halves, and gates below enforce the binding rather than trusting it. `both-legs-sound` is available only when every one of the four interruption captures shows an open journal record, an intermediate target, a handler probe that did not fire, a kill that did not land after completion, equal digests, and matching digest algorithms across the two platforms — AND when BOTH negative controls detected their late kill and exited non-zero. A leg whose `MIDFLIGHT-TIMING` shows the operation completed before the kill forces that platform's vacuous option, whatever any member argues. `windows-exactness-gap` requires the Windows leg to be measurably NON-vacuous and its digests measurably unequal, plus a severity-rated named defect — so it cannot be used to dress a vacuous leg as a platform gap. On the remap half, `remap-actionable` requires every backend record to name the backend, the count and the action, every refusal to have left the target unwritten, and no source absolute path to have survived; a refusal recorded with the target written forces `remap-warns-and-continues`; any missing name forces `remap-opaque`.

**STEP 5 — FIX WHAT IS A DEFECT, RE-MEASURE, AND ESCALATE ONLY WHAT IS RESERVED OR DEADLOCKED.** `remap-opaque` and `remap-warns-and-continues` are defects this plan must FIX inside its declared files and then RE-CAPTURE; the final-state gate reads whatever records exist at task end, so a rewrite that was decided and not made goes red. `linux-leg-vacuous` and `windows-leg-vacuous` are re-runs with a larger fixture, not outcomes. `windows-exactness-gap` and `remap-blocked` are this plan's own termination states 2 and 3: record the exact defect and its severity, do NOT narrow the definition of exactness and do NOT invent a semantics, and escalate CARRYING the measurement and the panel split. A genuine deadlock — a split with no measurement to break it — is escalated the same way, with all four verbatim captures, and the plan continues meanwhile.

Marks F26-03 and F26-04 complete if every gate below passes.</action>
  <verify>
    <automated>
# NEGATIVE-CONTROL GATE, LINUX. Runs the interruption script against a fixture deliberately too small for the kill to land mid-flight and requires it to FAIL. This is the leg that proves the mid-flight check fires at all; without it, a green from the real run is equally consistent with a check that never runs. Red at base: the script does not exist, so the remote run fails.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-03; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2&gt;/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; cargo build --locked --release -p wcore-cli --bin wayland-core; sh scripts/portability-interrupt-proof.sh --undersized ./target/release/wayland-core; echo NEGCTL-UNEXPECTED-SUCCESS" &gt; /tmp/26-03-t4-negctl-linux.log 2&gt;&amp;1; rc=$?; tail -25 /tmp/26-03-t4-negctl-linux.log; test $rc -ne 0 || { echo "GATE: the Linux interruption script reported SUCCESS on an UNDERSIZED fixture, where the operation completes before the kill lands. Its mid-flight check therefore never fires, and its green on the real fixture is equally consistent with a trivially consistent tree - which is exactly the vacuous proof this task exists to rule out"; exit 1; }; /usr/bin/grep -qF 'NEGCTL-UNEXPECTED-SUCCESS' /tmp/26-03-t4-negctl-linux.log &amp;&amp; { echo "GATE: the script exited zero on the undersized fixture and the failure came from something after it"; exit 1; }; echo "linux negative control: the mid-flight check fired and the script went red as required"
</automated>
    <automated>
# NEGATIVE-CONTROL GATE, WINDOWS. The same proof on the box where a Linux-tuned fixture is most likely to complete before the kill lands. Uses the single-layer -File invocation and propagates the script's own status WITHOUT a backtick, because in that form the outer shell's last exit code genuinely is the child's - backticking it there makes the shell try to RUN the variable, a guaranteed red that proves nothing. Red at base: the script does not exist on the box.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; ssh -o BatchMode=yes SeanD@seandesktop "powershell -NoProfile -File C:\ferrox-win\scripts\portability-interrupt-proof.ps1 -Binary C:\ferrox-win\target\release\wayland-core.exe -Undersized; exit \$LASTEXITCODE" &gt; /tmp/26-03-t4-negctl-win.log 2&gt;&amp;1; rc=$?; tail -25 /tmp/26-03-t4-negctl-win.log; test $rc -ne 0 || { echo "GATE: the Windows interruption script reported SUCCESS on an UNDERSIZED fixture. Its mid-flight check never fires on that box, so the real Windows green is vacuous - and this is the precise trap this plan predicted, because a fixture sized on Hetzner can finish before the kill lands on slower or faster hardware"; exit 1; }; echo "windows negative control: the mid-flight check fired and the script went red as required"
</automated>
    <automated>
# INTERRUPTION-EVIDENCE GATE. Reads the four CAPTURED verdict blocks the scripts themselves printed - never prose the executor typed. Requires the full grammar on both platforms, requires the two DIGEST-ALGO values to be byte-equal before any digest comparison is believed, and requires both negative controls to have DETECTED their late kill. Red at base: nothing has been captured.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; D=.planning/phases/26-migration-export-backup-restore/panel/26-03-interruption-nonvacuity; for P in linux windows; do F="$D/interrupt-evidence-$P.txt"; test -s "$F" || { echo "GATE: no captured interruption evidence for $P at $F"; exit 1; }; /usr/bin/grep -qxE "INTERRUPT-PLATFORM: $P" "$F" || { echo "GATE: $F does not declare itself the $P capture"; exit 1; }; for K in "KILL-MECHANISM: .+ CATCHABLE: no" "KILL-HANDLER-PROBE: installed=yes fired=no" "MIDFLIGHT-JOURNAL-OPEN: (yes|no)" "MIDFLIGHT-TARGET-INTERMEDIATE: (yes|no)" "MIDFLIGHT-TIMING: op_expected_ms=[0-9]+ kill_at_ms=[0-9]+ completed_before_kill=(yes|no)" "DIGEST-ALGO: .{4,}" "DIGEST-PRE: [0-9a-f]{16,}" "DIGEST-POST: [0-9a-f]{16,}" "DIGEST-EQUAL: (yes|no)"; do test "$(/usr/bin/grep -cE "^$K$" "$F")" -eq 1 || { echo "GATE: the $P capture carries no single well-formed line matching: $K"; exit 1; }; done; N="$D/interrupt-negctl-$P.txt"; test -s "$N" || { echo "GATE: no captured negative-control evidence for $P at $N"; exit 1; }; /usr/bin/grep -qxE 'NEGATIVE-CONTROL: late-kill-detected' "$N" || { echo "GATE: the $P negative control did not DETECT its late kill, so that platform's mid-flight check is decorative and its real green proves nothing"; exit 1; }; /usr/bin/grep -qE '^NEGCTL-EXIT: [1-9][0-9]*$' "$N" || { echo "GATE: the $P negative control did not exit non-zero"; exit 1; }; done; AL=$(/usr/bin/grep -E '^DIGEST-ALGO: ' "$D/interrupt-evidence-linux.txt"); AW=$(/usr/bin/grep -E '^DIGEST-ALGO: ' "$D/interrupt-evidence-windows.txt"); echo "linux[$AL] windows[$AW]"; test "$AL" = "$AW" || { echo "GATE: the two platforms named DIFFERENT digest algorithms or normalizations, so comparing their digests measures encoding rather than content - and that is a difference you would chase as a defect"; exit 1; }
</automated>
    <automated>
# REMAP-CAPTURE GATE. The remap script must exist, parse, be substantive, and PROVE it can go red against a binary that does not exist - then its captured records must cover all FOUR credential backends in the fixed grammar, each carrying a verbatim fenced operator message of real size. Red at base: the script does not exist.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f scripts/portability-remap-capture.sh &amp;&amp; sh -n scripts/portability-remap-capture.sh &amp;&amp; test "$(/usr/bin/grep -vc '^[[:space:]]*#' scripts/portability-remap-capture.sh)" -ge 30 || { echo "GATE: scripts/portability-remap-capture.sh is absent, unparseable, or too thin to build four backend homes and capture four restores"; exit 1; }; sh scripts/portability-remap-capture.sh /nonexistent/wayland-core-does-not-exist /tmp/26-03-remap-selfcheck &gt; /tmp/26-03-remap-selfcheck.log 2&gt;&amp;1; rc=$?; /bin/cat /tmp/26-03-remap-selfcheck.log; test $rc -ne 0 || { echo "GATE: the remap capture script reported SUCCESS against a binary that does not exist - it cannot go red, so its records are not evidence"; exit 1; }; D=.planning/phases/26-migration-export-backup-restore/panel/26-03-remap-operability; R="$D/remap-records.txt"; test -s "$R" || { echo "GATE: no captured remap records at $R"; exit 1; }; for B in auto plaintext keyring encrypted-file; do /usr/bin/grep -qxE "REMAP-BACKEND: $B" "$R" || { echo "GATE: no captured record for the $B credential backend - all four variants are measured, because where a secret lives differs per variant and that difference IS the remap"; exit 1; }; done; for K in "REMAP-EXIT: [0-9]+" "REMAP-DISPOSITION: (remapped|refused)" "REMAP-TARGET-WRITTEN: (yes|no)" "REMAP-NAMES-BACKEND: (yes|no)" "REMAP-NAMES-COUNT: (yes|no)" "REMAP-NAMES-ACTION: (yes|no)" "REMAP-CARRIES-SOURCE-ABSOLUTE-PATH: (yes|no)"; do test "$(/usr/bin/grep -cE "^$K$" "$R")" -eq 4 || { echo "GATE: expected exactly four lines matching '$K', one per backend"; exit 1; }; done; test "$(/usr/bin/grep -cxF 'REMAP-MESSAGE-BEGIN' "$R")" -eq 4 &amp;&amp; test "$(/usr/bin/grep -cxF 'REMAP-MESSAGE-END' "$R")" -eq 4 || { echo "GATE: the four verbatim operator messages are not all fenced"; exit 1; }; P="$D/peer-bar.txt"; test -s "$P" &amp;&amp; test "$(/usr/bin/grep -c . "$P")" -ge 10 || { echo "GATE: no substantive peer-bar capture at $P - Core's operator message is judged against what the peers actually ship, not against taste"; exit 1; }; test -f /Users/seandonahoe/dev/resources/openclaw/docs/cli/backup.md &amp;&amp; test -f /Users/seandonahoe/dev/resources/hermes-agent/hermes_cli/backup.py || { echo "GATE: a real peer checkout named as the source of the bar is not present on this host"; exit 1; }; /usr/bin/grep -qF 'resources/openclaw/docs/cli/backup.md' "$P" &amp;&amp; /usr/bin/grep -qF 'resources/hermes-agent/hermes_cli/backup.py' "$P" || { echo "GATE: the peer-bar capture does not name both real peer sources it was taken from"; exit 1; }
</automated>
    <automated>
# TWO-PANEL GATE. Delegates to the phase's own checker, built and self-tested in 26-01, once per decision against that decision's OWN option ids. It reads the CAPTURED panel files - four verbatim responses each, one verdict apiece from the supplied set, the same question digest stamped in all three external captures, pairwise-distinct externals, and a dissent set derived from the captured verdicts rather than from prose. Red at base: neither the checker nor either panel directory exists.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; B=.planning/phases/26-migration-export-backup-restore/panel; test -f scripts/panel-decision-check.sh || { echo "GATE: scripts/panel-decision-check.sh is absent - 26-01 builds it and every converted decision in this phase gates on it"; exit 1; }; test -d "$B/26-03-interruption-nonvacuity" &amp;&amp; test -d "$B/26-03-remap-operability" || { echo "GATE: one or both panel directories are missing, so a question this plan asks was never put to the panel"; exit 1; }; sh scripts/panel-decision-check.sh "$B/26-03-interruption-nonvacuity" both-legs-sound linux-leg-vacuous windows-leg-vacuous windows-exactness-gap &gt; /tmp/26-03-t4-panelA.log 2&gt;&amp;1; ra=$?; /bin/cat /tmp/26-03-t4-panelA.log; sh scripts/panel-decision-check.sh "$B/26-03-remap-operability" remap-actionable remap-opaque remap-warns-and-continues remap-blocked &gt; /tmp/26-03-t4-panelB.log 2&gt;&amp;1; rb=$?; /bin/cat /tmp/26-03-t4-panelB.log; echo "panelA_rc=$ra panelB_rc=$rb"; test $ra -eq 0 &amp;&amp; test $rb -eq 0
</automated>
    <automated>
# PANEL-EXTRACTION TRAP GATE - the leg that keeps these four-way panels from being three-way ones wearing a fourth's name. Measured on the real CLIs on 2026-07-26: kimi prefixes its verdict with a bullet and a space, so an anchored column-zero match returns ZERO for a real kimi capture; and codex repeats its whole final block, so a real codex capture carries the SAME verdict twice. This copies the interruption panel directory twice and mutates the copies into exactly those shapes. The checker must ACCEPT the bullet-prefixed, duplicate-carrying copy - an anchored or duplicate-intolerant extractor goes red here - and must REJECT the copy carrying two DIFFERENT verdict values in one capture, so an extractor that takes whatever it finds also goes red. Both directions are required.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; D=.planning/phases/26-migration-export-backup-restore/panel/26-03-interruption-nonvacuity; OPTS="both-legs-sound linux-leg-vacuous windows-leg-vacuous windows-exactness-gap"; test -d "$D" &amp;&amp; test -f scripts/panel-decision-check.sh || { echo "GATE: no panel directory or no checker to test"; exit 1; }; CH=$(/usr/bin/grep -oE '^CHOSEN: [a-z-]+$' "$D/DECISION.md" | /usr/bin/sed 's/^CHOSEN: //'); OTHER=""; for o in $OPTS; do test "$o" = "$CH" || OTHER="$o"; done; test -n "$CH" &amp;&amp; test -n "$OTHER" || { echo "GATE: could not read a CHOSEN option to build the trap fixtures from"; exit 1; }; A=/tmp/26-03-trap-accept; R=/tmp/26-03-trap-reject; mkdir -p "$A" "$R"; /usr/bin/find "$D" -maxdepth 1 -type f -exec /bin/cp {} "$A"/ \; ; /usr/bin/find "$D" -maxdepth 1 -type f -exec /bin/cp {} "$R"/ \; ; /usr/bin/sed -e 's/^PANEL-VERDICT: /• PANEL-VERDICT: /' -e 's/^PANEL-BASIS: /  PANEL-BASIS: /' "$D/kimi.txt" &gt; "$A/kimi.txt"; { /bin/cat "$D/codex.txt"; /usr/bin/tail -2 "$D/codex.txt"; } &gt; "$A/codex.txt"; { /usr/bin/sed "s/^PANEL-VERDICT: .*/PANEL-VERDICT: $OTHER/" "$D/codex.txt"; /usr/bin/tail -2 "$D/codex.txt"; } &gt; "$R/codex.txt"; sh scripts/panel-decision-check.sh "$A" $OPTS &gt; /tmp/26-03-trap-accept.log 2&gt;&amp;1; ra=$?; sh scripts/panel-decision-check.sh "$R" $OPTS &gt; /tmp/26-03-trap-reject.log 2&gt;&amp;1; rr=$?; echo "trap_accept_rc=$ra trap_reject_rc=$rr"; test $ra -eq 0 || { echo "GATE: the checker REJECTED a panel whose kimi capture is bullet-prefixed and whose codex capture repeats its final block - those are the shapes the real CLIs measurably emit, so this checker silently drops votes and neither panel here is four-way"; /bin/cat /tmp/26-03-trap-accept.log; exit 1; }; test $rr -ne 0 || { echo "GATE: the checker ACCEPTED a capture carrying two DIFFERENT verdict values, so it is not extracting a verdict, it is finding one"; /bin/cat /tmp/26-03-trap-reject.log; exit 1; }
</automated>
    <automated>
# MEASUREMENT-BINDING GATE, DECISION A - the one that makes the non-vacuity question a measurement rather than a committee. Binds the chosen option to what the four captures actually show, and closes the specific hole this phase was already corrected for once: an exactness gap cannot be used to dress up a vacuous leg. Red at base: no captures exist.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; D=.planning/phases/26-migration-export-backup-restore/panel/26-03-interruption-nonvacuity; DEC="$D/DECISION.md"; L="$D/interrupt-evidence-linux.txt"; W="$D/interrupt-evidence-windows.txt"; test -s "$DEC" &amp;&amp; test -s "$L" &amp;&amp; test -s "$W" || { echo "GATE: the decision record or an interruption capture is missing"; exit 1; }; CH=$(/usr/bin/grep -oE '^CHOSEN: [a-z-]+$' "$DEC" | /usr/bin/sed 's/^CHOSEN: //'); LV=n; WV=n; /usr/bin/grep -qxE 'MIDFLIGHT-JOURNAL-OPEN: yes' "$L" &amp;&amp; /usr/bin/grep -qxE 'MIDFLIGHT-TARGET-INTERMEDIATE: yes' "$L" &amp;&amp; /usr/bin/grep -qE '^MIDFLIGHT-TIMING: .* completed_before_kill=no$' "$L" &amp;&amp; LV=y; /usr/bin/grep -qxE 'MIDFLIGHT-JOURNAL-OPEN: yes' "$W" &amp;&amp; /usr/bin/grep -qxE 'MIDFLIGHT-TARGET-INTERMEDIATE: yes' "$W" &amp;&amp; /usr/bin/grep -qE '^MIDFLIGHT-TIMING: .* completed_before_kill=no$' "$W" &amp;&amp; WV=y; LE=$(/usr/bin/grep -oE '^DIGEST-EQUAL: [a-z]+' "$L" | /usr/bin/sed 's/.*: //'); WE=$(/usr/bin/grep -oE '^DIGEST-EQUAL: [a-z]+' "$W" | /usr/bin/sed 's/.*: //'); echo "chosen=$CH linux_nonvacuous=$LV windows_nonvacuous=$WV linux_digest_equal=$LE windows_digest_equal=$WE"; case "$CH" in both-legs-sound) test "$LV" = y -a "$WV" = y -a "$LE" = yes -a "$WE" = yes || { echo "GATE: both-legs-sound was chosen, but a leg either did not evidence mid-flight landing or did not produce equal digests. A trivially consistent tree is not a rollback proof, and no panel majority can make it one"; exit 1; };; linux-leg-vacuous) test "$LV" = n || { echo "GATE: the Linux leg was called vacuous but its capture shows an open journal record, an intermediate target and a kill that landed before completion"; exit 1; };; windows-leg-vacuous) test "$WV" = n || { echo "GATE: the Windows leg was called vacuous but its capture shows an open journal record, an intermediate target and a kill that landed before completion"; exit 1; };; windows-exactness-gap) test "$WV" = y || { echo "GATE: an exactness GAP was recorded for a Windows leg that is VACUOUS. A leg whose kill landed after completion proved nothing at all, so it is windows-leg-vacuous and must be re-run with a fixture sized for that hardware - dressing a vacuous leg as a platform gap is precisely the rounding-up this phase was already corrected for once"; exit 1; }; test "$WE" = no || { echo "GATE: an exactness gap was recorded but the Windows digests are EQUAL, so exactness was reached"; exit 1; }; /usr/bin/grep -qE '^FINDING: .*(critical|high|medium|low)' "$DEC" || { echo "GATE: termination state 2 was invoked with no severity-rated FINDING line naming the defect"; exit 1; }; /usr/bin/grep -qE '^ESCALATION: ' "$DEC" || { echo "GATE: termination state 2 was invoked with no ESCALATION record carrying the measurement and the split"; exit 1; };; *) echo "GATE: CHOSEN is '$CH', which is not one of decision A's four option ids"; exit 1;; esac
</automated>
    <automated>
# MEASUREMENT-BINDING AND FINAL-STATE GATE, DECISION B. Binds the chosen option to the four captured backend records, then requires the FINAL records to satisfy the actionable conditions unless the plan honestly stopped at termination state 3 - so a rewrite that was decided and not made goes red, and a refusal that writes the target can never read as a refusal. Red at base: no records exist.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; D=.planning/phases/26-migration-export-backup-restore/panel/26-03-remap-operability; DEC="$D/DECISION.md"; R="$D/remap-records.txt"; test -s "$DEC" &amp;&amp; test -s "$R" || { echo "GATE: the decision record or the remap records are missing"; exit 1; }; CH=$(/usr/bin/grep -oE '^CHOSEN: [a-z-]+$' "$DEC" | /usr/bin/sed 's/^CHOSEN: //'); MISSNAME=$(/usr/bin/grep -cE '^REMAP-NAMES-(BACKEND|COUNT|ACTION): no$' "$R"); ABSPATH=$(/usr/bin/grep -cxE 'REMAP-CARRIES-SOURCE-ABSOLUTE-PATH: yes' "$R"); WROTE=$(/usr/bin/awk '/^REMAP-DISPOSITION: refused$/{f=1} /^REMAP-TARGET-WRITTEN: yes$/{if(f){c++}} /^REMAP-BACKEND: /{f=0} END{print c+0}' "$R"); echo "chosen=$CH unnamed_fields=$MISSNAME source_abspaths=$ABSPATH refusals_that_wrote=$WROTE"; case "$CH" in remap-actionable) test "$MISSNAME" -eq 0 -a "$ABSPATH" -eq 0 -a "$WROTE" -eq 0 || { echo "GATE: remap-actionable was chosen but the captures show $MISSNAME unnamed field(s), $ABSPATH surviving source absolute path(s) and $WROTE refusal(s) that wrote the target anyway"; exit 1; };; remap-opaque) test "$MISSNAME" -gt 0 || { echo "GATE: the message was called opaque but every captured message names the backend, the count and the action"; exit 1; };; remap-warns-and-continues) test "$WROTE" -gt 0 || { echo "GATE: the refusal path was called non-refusing but no captured refusal wrote its target"; exit 1; };; remap-blocked) /usr/bin/grep -qE '^ESCALATION: ' "$DEC" &amp;&amp; /usr/bin/grep -qE '^FINDING: .*(critical|high|medium|low)' "$DEC" || { echo "GATE: termination state 3 was invoked with no ESCALATION record and no severity-rated FINDING. A REFUSAL naming the backend and the operator's next action is an honest remap outcome this plan already requires, so state 3 is reachable only when even that cannot be built - and it must carry the measurement that shows it"; exit 1; };; *) echo "GATE: CHOSEN is '$CH', which is not one of decision B's four option ids"; exit 1;; esac; test "$CH" = remap-blocked || { test "$MISSNAME" -eq 0 -a "$ABSPATH" -eq 0 -a "$WROTE" -eq 0 || { echo "GATE: in the FINAL captured records a message still omits a required name, or a source-machine absolute path still survives into a restored config, or a refusal still writes its target. Deciding to rewrite is not rewriting"; exit 1; }; }
</automated>
    <automated>
# PANEL SECRET-HYGIENE GATE. Three of the four members in each panel are REMOTE SERVICES, so both bundles are an exfiltration surface. Every fixture home in this plan is synthetic and canary-seeded and no real peer home is involved, and this proves that rather than asserting it: it extracts the REAL secret values from the REAL peer homes and requires ZERO of them anywhere under either panel directory, after first proving both that it had values to search for and that there was something to search. Red at base: the panel directories do not exist.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; B=.planning/phases/26-migration-export-backup-restore/panel; for d in 26-03-interruption-nonvacuity 26-03-remap-operability; do test -d "$B/$d" || { echo "GATE INVALID: no panel directory $d, so searching it for leaked secrets would pass vacuously"; exit 1; }; n=$(/usr/bin/find "$B/$d" -type f 2&gt;/dev/null | /usr/bin/wc -l | tr -d ' '); test "$n" -ge 5 || { echo "GATE INVALID: $d holds $n files - a completed panel run writes question.md, three captures, an internal pass and a DECISION.md"; exit 1; }; done; S=$(mktemp); /usr/bin/grep -hoE '[A-Za-z0-9_]*_(API_KEY|TOKEN|SECRET|KEY)=.*' "$HOME"/.hermes/profiles/*/.env 2&gt;/dev/null | /usr/bin/sed 's/^[^=]*=//; s/^["'"'"']//; s/["'"'"']$//' | /usr/bin/grep -vE '^[[:space:]]*$' &gt;&gt; "$S"; /usr/bin/find "$HOME/.openclaw" -maxdepth 2 -type f \( -name '*.json' -o -path '*/credentials/*' \) -print0 2&gt;/dev/null | xargs -0 /usr/bin/grep -hoiE '"[^"]*(key|token|secret|auth|pass|cred)[^"]*"[[:space:]]*:[[:space:]]*"[^"]{16,}"' 2&gt;/dev/null | /usr/bin/sed 's/.*:[[:space:]]*"//; s/"$//' &gt;&gt; "$S"; /usr/bin/sort -u "$S" -o "$S"; T=$(/usr/bin/grep -c . "$S"); test "$T" -ge 4 || { echo "GATE INVALID: only $T real secret values extracted across BOTH peer homes - measured 7 at plan time, so this search is not proving what it claims"; /bin/rm -f "$S"; exit 1; }; HITS=0; while IFS= read -r v; do test ${#v} -lt 8 &amp;&amp; continue; /usr/bin/grep -RqF -- "$v" "$B/26-03-interruption-nonvacuity" "$B/26-03-remap-operability" 2&gt;/dev/null &amp;&amp; HITS=$((HITS+1)); done &lt; "$S"; /bin/rm -f "$S"; echo "searched=$T hits=$HITS"; test "$HITS" -eq 0 || { echo "GATE: a REAL secret value appears under a panel directory - a bundle went to three remote services carrying live credentials. This is a CRITICAL disclosure, not a formatting problem"; exit 1; }
</automated>
  </verify>
  <done>The interruption proof's mid-flight check is PROVEN to fire: both scripts were run against a deliberately undersized fixture on their own hosts, both detected the late kill and both exited non-zero, so the real green is no longer equally consistent with a check that never runs. Kill uncatchability is measured by a handler probe that did not fire rather than asserted. The Windows fixture's sizing is two numbers on the box that ran it, not a judgement. Both platforms named the same digest algorithm and normalization, so the digest comparison measures content. What a cross-machine restore tells an operator and what it does are captured per credential backend — verbatim message, exit status, whether the target was written as measured by a before-and-after digest, and whether any source-machine absolute path survived — and the peers' own published messaging is captured from the REAL checkouts so Core's is judged against a shipping bar. Both questions went to all four panel members as their own shared bundles, every response captured verbatim, and the harness was proven to survive both shapes the real CLIs measurably emit. Both decisions name an option from their own set, with a majority or an evidenced minority basis and a dissent that survives in its own terms. Both are BOUND to their measurements: `both-legs-sound` is unavailable when a leg is vacuous, `windows-exactness-gap` is unavailable when the Windows leg is vacuous — closing the exact hole where a vacuous leg gets dressed as a platform gap — and every remap option is bound to what the captured records show. In the FINAL records every message names the backend, the count and the action, every refusal left its target unwritten, and no source absolute path survived, unless the plan honestly stopped at termination state 3 with an escalation carrying its measurement. No real secret appears under either panel directory, proven by a search that first proved it was non-vacuous. F26-03 and F26-04 are claimed only if every gate here passed; a Windows exactness gap under termination state 2 is named with its severity and escalated rather than absorbed, and the definition of exact is never narrowed.</done>
</task>

</tasks>

## What this plan does NOT change (scope fence)

- **`crates/wcore-cli/src/migrate/`, `crates/wcore-cli/src/profile.rs` and `crates/wcore-config/src/profile.rs` — 26-02 owns them in this same wave.** Touching any of them would collide. The archive reads the profile module's secret classification; it does not modify it.
- **Discovery and the typed plan model — 26-01 owns them.** The tree digest established there is reused, not replaced.
- **Hostile corpora and the native platform matrix — 26-04 owns them.** Windows appears here only for the interruption property, which cannot be proven anywhere else.
- **The crash sentinel — untouched.** Its per-process dead-pid shape is the precedent the journal follows; the sentinel itself is not modified or absorbed.
- **`atomic_write` — reused, not reimplemented.** No second atomic write path is introduced.
- **The credential backends — not changed.** This plan reads where each stores its secrets and remaps on restore; it does not alter any backend or migrate anyone between them.
- **`wcore-protocol` — untouched.** No backup or restore event is added to the JSON stream protocol; the D1 producer contract and its Desktop consumers are unaffected.
- **`main.rs` — one addition only.** The new top-level command and nothing else; this is the only file in `main.rs` the phase touches.
- **No new external dependency.**
- **No test is deleted, weakened, re-gated, `#[ignore]`d or `#[allow]`ed, and no timeout is raised to reach a gate.**

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive file → restore | An archive is an untrusted input by the time it is restored: it may have been tampered with, moved between machines, or crafted |
| archive entry path → extraction root | Every path inside an archive is attacker-controlled and may attempt to escape the root it extracts into |
| operation journal → recovery pass | The journal is what recovery trusts to undo an operation; a torn or forged record misdirects the undo |
| source machine secret location → target machine | Where a secret lives is machine-specific for two of the four backends; carrying it verbatim produces a config pointing at nothing |
| live target home ← in-progress restore | The window in which a home is neither its old self nor its new self, which is exactly what an interruption exposes |
| concurrent operations → each other's journals | The hazard #181 already found for the crash sentinel: an unscoped marker makes unrelated processes act on each other's state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-03-01 | Tampering | An interrupted restore leaves the home in a state that is neither the old nor the new one, destroying the state the backup existed to protect | critical | mitigate | Durable intent written BEFORE the first mutation through the existing atomic write; a recovery pass that undoes to the exact pre-operation state; proven by killing the real binary uncatchably on Linux and on real Windows with tree-digest equality |
| T-26-03-02 | Tampering | An archive entry path escapes the extraction root, letting a crafted archive write outside the target home | critical | mitigate | Traversal rejection asserted as its own separate verification test, mirroring the peer's published verify behavior; restore verifies before it writes |
| T-26-03-03 | Tampering | Restore replaces a live home in place and then discovers the archive was corrupt, destroying the original | critical | mitigate | Verification happens before any write, and restore refuses an occupied target rather than replacing state in place |
| T-26-03-04 | Information Disclosure | An archive presents itself as complete while the source home's secrets lived in the OS keyring and were never captured, so the operator believes they have a recoverable backup and does not | high | mitigate | An archive of such a home RECORDS which secrets are not carried; restore surfaces the gap explicitly and refuses where no honest remap exists; the operator-facing message is CAPTURED verbatim per backend at Task 4, and machine-checked for naming the backend, the count of absent credentials and an action |
| T-26-03-05 | Spoofing | A restored config carries the source machine's absolute secret paths, silently resolving to nothing or — worse — to a different file that happens to exist on the target | high | mitigate | Absolute source paths are never carried verbatim; each secret source becomes an explicit recorded decision, or the restore refuses |
| T-26-03-06 | Tampering | The interruption gate passes vacuously because the operation completed before the kill landed, so consistency is trivial and nothing was proven | high | mitigate | Mid-flight landing is independently established — open journal record plus observably intermediate target — BEFORE any digest comparison counts; the Windows fixture's sizing is recorded as two numbers on the box that ran it; and Task 4 proves the mid-flight check CAN fire by running each script against a deliberately undersized fixture and requiring it to go red |
| T-26-03-07 | Tampering | A remote gate reports the status of a filter rather than of the work, so a failed Windows build or a failed interruption leg reads as success | high | mitigate | Every remote invocation propagates the script's own status, redirects to a file, captures the return code on its own line and tests it before reading; no filter between command and status; no status read from a block that also emits output |
| T-26-03-08 | Denial of Service | A journal record from a concurrent unrelated operation makes recovery undo work that was never interrupted — the #181 hazard, transplanted | high | mitigate | Records are scoped per operation AND per process, and recovery acts only on records whose owning process is dead, mirroring the crash sentinel exactly; a dedicated test covers the live-owner case |
| T-26-03-09 | Tampering | A torn journal record becomes the corruption the journal existed to prevent | high | mitigate | Every record is written through the existing atomic sibling-tempfile-plus-rename write whose failure invariant is already tested |
| T-26-03-10 | Denial of Service | On Windows the rename-based atomic write cannot replace a target another handle holds open, so restore fails or half-completes where Linux succeeds | high | mitigate | The open-handle case is exercised explicitly on real Windows hardware; if exactness cannot be reached, it is recorded as a named defect with severity under termination state 2 rather than being made to pass by narrowing the definition |
| T-26-03-11 | Denial of Service | A restored tree reconstructs paths under a new root that exceed Windows path limits, so the deepest files cannot be restored | medium | mitigate | Deep and long paths are exercised in the Windows leg specifically |
| T-26-03-12 | Repudiation | Digests computed differently per platform make a cross-platform comparison measure encoding rather than content, producing a chased non-defect or a masked real one | medium | mitigate | The digest from 26-01 is reused with normalized path and line-ending representation, and Task 4's gate requires the two platforms' captured DIGEST-ALGO lines to be byte-equal before it believes any digest comparison |
| T-26-03-13 | Denial of Service | An archive whose output path lies inside the tree being archived grows until the disk does | medium | mitigate | Self-inclusion is refused, matching the peer's published behavior; no-overwrite publication is asserted |
| T-26-03-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency is added, removed or updated; no `Cargo.toml` or `Cargo.lock` change; the execution rules require escalation rather than an install |
</threat_model>

<verification>
Local gates (Mac, source level only): `cargo fmt --all -- --check` clean; the journal AND both write paths demonstrably call `atomic_write` outside comments; the remap module demonstrably reads the credential backend enum outside comments; both proof scripts exist, the shell one parses, and each is PROVEN able to go red by being handed a binary path that does not exist and required to exit non-zero — an existence check alone would pass on an empty file and prove nothing about the script that carries this plan's entire live claim; every new file under 1000 lines.

Authoritative Linux gates (`hetzner-dsm`, in this plan's OWN worktree `/root/wayland-f26-03` — never the shared `/root/wayland`, which the concurrent wave-2 sibling 26-02 is using — at this SHA asserted before AND after each one, with `/root/.cargo/bin` on `PATH`, each status captured on its own line and tested before any output is read): release build; `cargo clippy --locked -p wcore-cli --all-targets -- -D warnings`; the REAL binary's `backup --help` captured to its OWN file and fetched back, so the subcommand assertion reads help output rather than a build log that merely mentions the words; `cargo nextest run --locked -p wcore-cli --no-fail-fast`; the interrupt-filtered run asserting a NON-ZERO test count actually executed; and the committed interruption script run against the freshly built release binary.

Authoritative Windows gates (`SeanD@seandesktop`, `C:\ferrox-win`, at this SHA): EVERY step checks its OWN status and fails with a distinct code, because a chain of `cmd /c` calls read through a single trailing `$LASTEXITCODE` reports only the last one — which is how a failed checkout could report success. The SHA is confirmed by capturing `git rev-parse HEAD` ALONE to its own file, fetching it back and comparing the whole value, never by grepping a combined log where a failed checkout's own error message quotes the SHA it could not find. The head is re-asserted after the build and after the interruption run, so a concurrent phase moving that tree turns the gate red. The interruption script is proven able to go red before its real run is believed. The crate suite runs with counts stated as a delta against Linux.

Cross-audited decision gates (Task 4, two decisions, no human wait state): both proof scripts are run against a deliberately UNDERSIZED fixture on their own hosts and required to go RED, which is what proves the mid-flight check fires at all; the four captured interruption blocks carry the full fixed grammar including a handler probe that did not fire and a timing line showing the kill did not land after completion; the two platforms' DIGEST-ALGO lines are byte-equal before any digest comparison is believed; the remap capture covers all four credential backends with verbatim fenced messages and a before-and-after target digest, and names both REAL peer sources it took the bar from; both panel records satisfy 26-01's checker against their own option sets; the checker is proven to survive the two extraction shapes the real CLIs measurably emit and to reject a genuinely ambiguous capture; each chosen option is BOUND to its measurement, so a vacuous leg cannot be recorded as an exactness gap and a decided rewrite that was not made goes red; and no real secret value appears under either panel directory, proven by a search that first proved it was non-vacuous.

Known unknowns to record, not resolve here: whether an archive of a home whose secrets live in the OS keyring can ever be made portable without the operator re-entering credentials, which may be a product decision rather than a defect; whether network or removable target filesystems change the atomicity assumptions on either platform; and how the journal behaves when the home itself is on a filesystem that does not support atomic rename, which 26-04's hostile corpora may surface.
</verification>

<success_criteria>
- Backup, verify and restore exist as real subcommands, proven by the REAL binary's help output on both platforms rather than by reading source.
- Archive creation embeds a manifest, refuses to overwrite an existing output, and refuses an output path inside the source tree.
- Verification fails on a tampered payload, on a traversal path, and on a missing declared payload — three separately asserted rejections.
- Restore verifies before writing and refuses an occupied target rather than replacing state in place.
- Secret sources are EXPLICITLY remapped across all four credential backends: an archive that cannot carry its secrets says so, a restore surfaces the decision, absolute source paths are never carried verbatim, and where no honest remap exists the restore refuses.
- A write-ahead journal records durable intent BEFORE the first mutation, through the existing atomic write, scoped per operation and per process with the crash sentinel's dead-pid rule.
- Recovery is idempotent and leaves live-owner records alone.
- Exact rollback is proven by killing the REAL binary uncatchably mid-flight — on Linux AND on real Windows — with mid-flight landing independently established BEFORE any digest comparison counts, and with pre-operation and post-recovery digests equal.
- The partial-failure path rolls back exactly, not only the signal path.
- The Windows-only cases that Linux cannot warn about — a target held open by another handle, and deep and long restored paths — are both exercised on real hardware.
- Every remote gate returns the status of the work, never of a filter, and no status is read from a block that also emits output. On Windows every step checks its OWN status, and the SHA is proven by comparing an isolated `rev-parse` capture rather than by grepping a combined log.
- Every Linux gate ran in this plan's OWN worktree with the SHA asserted before and after, so the concurrent wave-2 sibling 26-02 cannot have moved the tree under an in-flight proof; on Windows the shared checkout is head-asserted before the build, after the build and after the interruption run.
- Both proof scripts are demonstrated able to go RED — each is handed a nonexistent binary and required to fail — before either one's green is accepted as evidence.
- Each proof script's MID-FLIGHT CHECK is separately demonstrated able to fire, by running the same procedure against a deliberately undersized fixture on its own host and requiring the script to detect the late kill and exit non-zero. Without this, a green on the real fixture is equally consistent with a mid-flight check that never runs.
- Kill uncatchability is MEASURED by a handler probe that did not fire, and the Windows fixture's sizing is two recorded numbers on the box that ran it — neither is asserted.
- The two platforms' digest algorithm and normalization identifiers are byte-equal, so the cross-platform digest comparison measures content rather than encoding.
- What a cross-machine restore tells an operator and what it does are CAPTURED per credential backend — verbatim message, exit status, whether the target was written as measured by a before-and-after digest, and whether a source-machine absolute path survived — and are judged against messaging captured from the two REAL peer checkouts on this host.
- Both questions that used to halt this plan are settled by a four-way cross-audited decision whose chosen option is bound to its measurement, with the dissent recorded — and the binding specifically forbids recording a vacuous Windows leg as an exactness gap.
- If Windows exactness could not be reached, it is a named defect with a severity, escalated — never reached by narrowing what exact means.
</success_criteria>

## Artifacts this plan produces
- `crates/wcore-cli/src/backup/` — archive, verify, restore, the write-ahead journal and its recovery pass, and explicit secret remapping.
- `scripts/portability-interrupt-proof.sh` and `scripts/portability-interrupt-proof.ps1` — the mirrored interruption-and-exact-rollback proofs for Linux and Windows, each carrying the undersized-fixture negative-control mode that proves its own mid-flight check can fire.
- `scripts/portability-remap-capture.sh` — the per-backend capture of what a cross-machine restore tells an operator and what it actually does.
- `crates/wcore-cli/tests/backup_restore_interrupt.rs` — the journal, recovery and interruption tests.
- `panel/26-03-interruption-nonvacuity/` and `panel/26-03-remap-operability/` — the two shared bundles, their four verbatim captures each, and the two decision records with their dissents.
- `26-03-SUMMARY.md`.

<output>
Create `.planning/phases/26-migration-export-backup-restore/26-03-SUMMARY.md` using the standard summary template. Record: the subcommands as they appear in the REAL binary's help on both platforms; the archive manifest shape and the three verification rejections with the evidence for each; the restore's pre-write verification and occupied-target refusal; for EACH of the four credential backends, where its secrets actually live and exactly what the remap does or why it refuses, plus the operator-facing message verbatim; the journal's record shape, its durability ordering relative to the first mutation, and how its per-operation and per-process scoping mirrors the crash sentinel's #181 reasoning; the recovery pass's idempotence and live-owner behavior; for BOTH interruption legs, the fixture size, the kill mechanism, the mid-flight evidence that was established BEFORE any comparison, and the pre-operation versus post-recovery digests; the partial-failure leg; the Windows open-handle and long-path results with any defect named and severity-rated; the Windows crate counts as a delta against Linux with every residual failure attributed; confirmation that every remote gate read the status of the work rather than of a filter; BOTH negative-control runs with the undersized fixture size, the late-kill detection and the non-zero status each script returned; the per-backend remap capture with each verbatim message, its exit status, whether the target was written and whether any source absolute path survived, alongside the peer bar and its two real source paths; both Task 4 decisions with their chosen option, basis, the measurement rule that bound each one, and the dissent in its own terms; the recorded unknowns; and which of the four termination states the plan ended in. F26-03 and F26-04 are marked complete only if every Task 4 gate passed.
</output>

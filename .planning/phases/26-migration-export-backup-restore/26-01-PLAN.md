---
phase: 26-migration-export-backup-restore
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md
  - scripts/portability-corpus-gen.py
  - crates/wcore-cli/tests/fixtures/portability/README.md
  - crates/wcore-config/src/lib.rs
  - crates/wcore-config/src/portability/mod.rs
  - crates/wcore-config/src/portability/redact.rs
  - crates/wcore-config/src/portability/digest.rs
  - crates/wcore-cli/src/migrate/mod.rs
  - crates/wcore-cli/src/migrate/hermes.rs
  - crates/wcore-cli/src/migrate/openclaw.rs
  - crates/wcore-cli/tests/migrate_typed_dryrun.rs
autonomous: false
requirements:
  - F26-01
must_haves:
  truths:
    - "MEASURED LIVE, NOT ASSUMED — this plan's premises were established by running the REAL shipped `wayland-core` binary against Sean's REAL peer installs before any of it was written. `./target/debug/wayland-core migrate hermes --home ~/.hermes --dry-run` exits 0 and reports **12 profiles**, **1 MCP server** (`ijfw-memory`), **540 skill directories**, **12 `SOUL.md` persona files**, and 0 memory notes. Two consecutive runs produced byte-identical stdout, so determinism is a MEASURED baseline this plan must PRESERVE — never a property it may claim to have introduced."
    - "THE FOUR GAPS ARE VERIFIED AGAINST THE SHIPPED BINARY, NOT INFERRED. `wayland-core migrate --help` lists exactly one subcommand, `hermes` — there is NO OpenClaw path. `wayland-core migrate hermes --help` lists exactly `--home`, `--dry-run`, `--yes`, `--include-credentials`, `--overwrite` — there is NO `--json`, so the dry-run output is human prose and is NOT typed. `wayland-core --help` has no `backup`, `restore`, or `export` top-level command. And `hermes::detect_home` (`crates/wcore-cli/src/migrate/hermes.rs`, the `profiles.is_dir()` guard) BAILS unless `<home>/profiles` exists, so a Hermes install that keeps its setup only in the ROOT `config.yaml` + `.env` is unimportable, while a rooted install like Sean's has its root profile silently ignored."
    - "BOTH PEERS MIGRATE FROM EACH OTHER AND CORE IS THE ONLY PARTY WITH NO RECIPROCAL PATH. `intel/COMPETITIVE-LEDGER.md` row `PORT-*` records this as the measured competitive gap, and every peer file it names was confirmed present in the real checkouts: Hermes ships `hermes_cli/migrate.py`, `hermes_cli/backup.py`, `agent/curator_backup.py` and a 3136-line `optional-skills/migration/openclaw-migration/scripts/openclaw_to_hermes.py`; OpenClaw ships `docs/cli/migrate.md`, `docs/cli/backup.md`, `docs/install/migrating-hermes.md` and `extensions/codex/src/migration/{apply,auth}.ts`. Closing the OpenClaw direction is this plan's half of that gap."
    - "THE PEER CHECKOUTS ARE AT HEAD, NOT AT THE PINNED BASELINE, AND THAT DIFFERENCE IS LOAD-BEARING. `/Users/seandonahoe/dev/resources/hermes-agent` is at `d59b79fadd1e9edd7afc5c679cc3b143838e7c01` (0.18.2) but the ledger pins Hermes at `dbe734be` (0.17.0); `/Users/seandonahoe/dev/resources/openclaw` is at `3659c85e534fdb8b8ce6b7505a83d92cc2e4df8e` (2026.7.2) but the ledger pins OpenClaw at `11a0ad10` (2026.6.2). Reading only HEAD would ground the importer in a format the pinned baseline may not have; reading only the baseline would ground it in a format real users no longer run. Both are read, and any drift between them is a recorded compatibility range, not a footnote."
    - "IMPORTED PEER STATE IS HOSTILE INPUT. A peer state directory is attacker-influenced by construction: it can contain symlinks pointing outside itself, `..` path components, absolute paths, names that collide after case-folding or Unicode normalization, files that are enormous, and configuration whose string values are executable commands. Discovery walks that tree. Every read is bounded and every emitted path is validated, and the walk NEVER follows a symlink out of the declared source root."
    - "REDACTION MUST BE STRUCTURAL, NOT COSMETIC. A secret that is redacted when printed but present in the typed value has leaked to every consumer that serializes it — and `--json` creates exactly such a consumer. The secret value must be unrepresentable in the emitted plan type, so that no downstream printer, log line, error message, or serializer can emit it. The existing code already gets the SHAPE of this right for the prose path — it records `credential_env_var` (a name) and only populates `api_key` under `--include-credentials` — and that discipline is what the typed model must carry, not weaken."
    - "A GATE THAT CANNOT FAIL IS WORSE THAN NO GATE. Every negative assertion in this plan is paired with a positive one, because a negative grep over an empty or missing file passes vacuously and manufactures confidence. The redaction gate asserts BOTH that no canary value appears in the output AND that the output is well-formed JSON containing the expected profile count — so an empty output file goes RED rather than green."
    - "A REPORTED RED IS WORTH FAR MORE THAN AN ENGINEERED GREEN. Never weaken an assertion, `#[ignore]`, `#[allow]`, raise a timeout, re-gate, or delete an inconvenient test to reach a gate. If the honest outcome is that a premise here was refuted, record the refutation with its evidence and close on it."
  artifacts:
    - path: .planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md
      provides: "The measured pre-change behavior of the shipped binary against both REAL peer installs, the pinned-versus-HEAD peer format grounding with its drift recorded, and the enumerated source-format contract each importer is written against"
    - path: scripts/portability-corpus-gen.py
      provides: "The generator that clones the STRUCTURE of a real peer state tree into a committed corpus while substituting known canary values for every real secret — so the authoritative Linux gate runs against real-world shape without a real secret ever leaving the Mac"
    - path: crates/wcore-config/src/portability/redact.rs
      provides: "The structural redaction boundary: secret values are unrepresentable in the emitted plan type"
    - path: crates/wcore-config/src/portability/digest.rs
      provides: "The content digest over a directory tree that proves discovery and dry-run are non-mutating"
    - path: crates/wcore-cli/src/migrate/openclaw.rs
      provides: "The reciprocal OpenClaw discovery source that closes the ledger's PORT-* gap in the Core direction"
    - path: crates/wcore-cli/src/migrate/mod.rs
      provides: "The typed, deterministic, machine-readable dry-run plan surfaced as `--json`, alongside the preserved prose rendering"
  key_links:
    - from: crates/wcore-cli/src/migrate/openclaw.rs
      to: crates/wcore-config/src/portability/redact.rs
      via: "every discovered credential is recorded as a source reference, never a value"
      pattern: "structural-redaction"
    - from: crates/wcore-config/src/portability/digest.rs
      to: crates/wcore-cli/tests/migrate_typed_dryrun.rs
      via: "the source tree digest is taken before and after a full dry-run and must be identical"
      pattern: "non-mutation-proof"
    - from: scripts/portability-corpus-gen.py
      to: crates/wcore-cli/tests/fixtures/portability/
      via: "real peer tree structure, canary-substituted, committed as the authoritative Linux gate corpus"
      pattern: "grounded-fixture"
---

<objective>
Make Hermes AND OpenClaw discovery and dry-run typed, deterministic, secret-redacted, and non-mutating — and prove each of those four words with a gate that can actually go red.

Purpose: Success Criterion 1 names four properties. Measured against the shipped binary, Core has one of them today: the Hermes dry-run is deterministic and non-mutating, but its output is untyped prose with no machine-readable form, and there is no OpenClaw path at all. The competitive ledger's `PORT-*` row records that both peers ship migration paths FROM each other while Core is the only party with no reciprocal path; this plan closes the discovery half of that gap and establishes the typed vocabulary that 26-02 and 26-03 build on.
Output: A measured baseline against Sean's real peer installs; a corpus whose structure is cloned from those real trees with canary values substituted for secrets; a typed plan model whose type makes secret values unrepresentable; a `--json` surface; the root-profile discovery gap closed; and an OpenClaw discovery source, all proven live on Linux against the corpus and on macOS against the real trees.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/intel/COMPETITIVE-LEDGER.md
@crates/wcore-cli/src/migrate/mod.rs
@crates/wcore-cli/src/migrate/hermes.rs
@crates/wcore-cli/tests/migrate_hermes.rs
@crates/wcore-config/src/profile.rs
</context>

<execution_rules>

**THE TWO PHASE RULES — stated verbatim, and they bound this plan.**

- Findings at CRITICAL or HIGH must be fixed or disproved. MEDIUM and below are logged to `.planning/BACKLOG.md` and DO NOT BLOCK execution.
- Execution begins when no CRITICAL or HIGH finding is open, or after 2 review rounds, whichever comes first. A third round is NOT permitted; it escalates to Sean.

**TERMINATION CRITERION FOR THIS PLAN (hard).** This plan measures ONCE, builds the typed discovery surface ONCE, and proves it ONCE. It terminates in exactly one of three states, and in all three it writes its SUMMARY and stops:
1. **Complete** — both peer sources discover, both emit a typed deterministic redacted plan, non-mutation is proven by digest, and the live legs pass.
2. **Blocked on a source-format unknown** — a peer's on-disk format cannot be established from the pinned source, the HEAD source, and the real install together. STOP, record exactly which field could not be grounded, and escalate rather than guessing a schema.
3. **Escalated** — a CRITICAL or HIGH finding is open that cannot be fixed inside this plan's declared files. STOP and escalate.
Under no circumstances does this plan spawn additional plans, extend its own task list, or begin implementing import/apply behavior — that is 26-02's, and backup/restore is 26-03's.

**SCOPE BOUNDARY (hard).** This plan changes DISCOVERY and DRY-RUN only. It does not change what `apply` writes, it does not add quarantine or provenance (26-02), it does not add backup/restore (26-03), and it does not add hostile corpora (26-04). If a discovery finding implicates apply behavior, RECORD it with severity and stop rather than following it.

**NO NEW DEPENDENCY.** Every crate this plan needs is already in the workspace: `serde`, `serde_json`, `serde_yaml`, `toml`, `sha2`, `tempfile`, `dirs`, `anyhow`. Adding an external dependency is out of scope; if one appears genuinely necessary, STOP and escalate rather than adding it, because a package install would trigger the legitimacy gate this plan is not scoped to run.

**SECRETS NEVER LEAVE THE MAC.** Sean's real `~/.hermes` and `~/.openclaw` contain live provider credentials. They are read on the Mac and nowhere else. Copying either tree — or any file from it — to `hetzner-dsm`, to `seandesktop`, into the repository, into a planning artifact, or into a commit is FORBIDDEN. The corpus generator exists precisely so the Linux gate can run against real-world SHAPE without a real secret ever crossing a host boundary. Any evidence file that would contain a real secret value is not written at all.

**ENVIRONMENT.**
- **Mac (this host, `arm64`)** — CANNOT compile this workspace; never run `cargo build`/`test`/`check` here. `cargo fmt --all` DOES work and is the local format gate. A previously built `target/debug/wayland-core` exists here and is the binary used for the pre-change baseline measurement ONLY — it is stale with respect to this plan's changes and must never be used to prove new behavior. Use `/usr/bin/git` and ALWAYS `/usr/bin/grep`.
- **Linux — `ssh -o BatchMode=yes hetzner-dsm`, `/root/wayland` (`x86_64`)** — the authoritative build and automated-gate host.
- **Peer sources** — `/Users/seandonahoe/dev/resources/hermes-agent` and `/Users/seandonahoe/dev/resources/openclaw`, both read-only. Never commit into them, never mutate them, never leave a detached checkout behind: read pinned content with `git show <sha>:<path>` rather than checking the tree out.
- Push the WORK BRANCH to `gh` so Hetzner can fetch it. NO push to main, merge, PR, tag, release, or issue closure without Sean.

**THE MEASUREMENT TRAPS (all three measured; do not simplify these away).**
1. **Mac `grep` is rtk-proxied and SILENTLY DROPS LINES** — measured at 32 returned versus 674 for the same inverted match on the same file. Every gate invokes `/usr/bin/grep` explicitly and uses `-F` for literals.
2. **A pipe replaces the exit status you care about.** `ssh host 'cmd' | grep -v noise` yields grep's status, not ssh's, so a failed remote build reports success. Every remote gate in this plan redirects output to a file, captures the return code on its own line, and tests that code BEFORE reading the file. Filtering for READING is fine; filtering inside a GATE is forbidden.
3. **Reading an exit code from a block that also emits output.** A block that both tees output and yields `$LASTEXITCODE` returns an array, making a numeric comparison always truthy. Read the status on the line AFTER the pipeline.

**AGENTS.md discipline.** Surgical diffs only; every changed line traces to this plan. No drive-by refactor of the existing Hermes mapper beyond the root-profile gap. `cargo clippy` clean, `cargo fmt` clean. Errors use `thiserror` for the public portability types and `anyhow` for CLI-level propagation; no `unwrap()` in production paths. Keep every new file under 1000 lines.

**Git hygiene.** Use `/usr/bin/git` on the Mac. Stage the exact paths in `files_modified`, never `-A`, never `.`. Never stage `AGENTS.md`, `.ijfw` churn, or anything under `.planning/` that this plan does not own. No `Co-Authored-By` trailers.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Measure the shipped binary against both REAL peer installs, ground the source formats in the pinned peer sources, and generate the canary corpus</name>
  <files>.planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md, scripts/portability-corpus-gen.py, crates/wcore-cli/tests/fixtures/portability/README.md</files>
  <read_first>crates/wcore-cli/src/migrate/hermes.rs (the whole file — `detect_home`'s directory guard, `build_plan`'s walk and sort, the Hermes YAML schema structs, `parse_dotenv`, `pick_provider_key`, and the deferred-inventory counters), crates/wcore-cli/src/migrate/mod.rs (`MigrateCmd`, `HermesArgs`, `MigrationPlan`, `ProfilePlan`, `Deferred`, `render_plan` and what it chooses to print versus withhold), crates/wcore-cli/tests/migrate_hermes.rs (the existing fixture idiom and env-guard pattern, so nothing is duplicated or displaced), .planning/intel/COMPETITIVE-LEDGER.md (the `PORT-*` row and the pinned baseline table)</read_first>
  <behavior>
    - The pre-change behavior of the REAL shipped binary is recorded against Sean's REAL `~/.hermes` and REAL `~/.openclaw`, with the exact command, the exit status, and the observed counts — not a description of what the code appears to do.
    - Determinism is measured rather than assumed, by running the same invocation twice and comparing the bytes.
    - Non-mutation is measured rather than assumed, by digesting the source tree before and after.
    - Each peer's on-disk source format is grounded in THREE readings that are reconciled against one another: the pinned baseline commit, the checkout HEAD, and the real install on this Mac. Drift between them is recorded as a compatibility range.
    - The generator reproduces a real tree's STRUCTURE — relative paths, file names, directory nesting, file formats, and the identity of every key — while substituting a distinct, recorded canary value for every value it classifies as secret.
    - The canary values live in the corpus as data, never as literals in this plan or in any source file, so that a gate can read them at run time.
    - The generator is deterministic: run twice over the same source it produces byte-identical output.
    - No real secret value is written into the corpus, into the baseline document, or into any file that will be committed.
  </behavior>
  <action>Measure first, and measure the real product. Run the existing binary at `target/debug/wayland-core` against Sean's real Hermes home with the dry-run flag and WITHOUT the credentials flag, and record the exact command, the exit status and the full output. This invocation is safe to run and is already known to be non-mutating and value-free: the dry-run path returns before the apply step, and the planner only records a credential's variable NAME unless credentials are explicitly requested. Do NOT pass the credentials flag against the real home at any point in this plan.

Record the observed counts as the baseline this plan must preserve: the number of profiles, the MCP servers, the skill directories and persona files reported as detected-but-not-imported. Then run the identical invocation a second time and compare the two outputs byte for byte, recording whether determinism holds today. Then take a content digest of the entire real Hermes home before and after a dry-run and record whether they match, which establishes non-mutation as a measured fact rather than a claim about the code.

Establish the four gaps by interrogating the binary, not the source: ask the migrate command for its help and record which sources it offers; ask the Hermes subcommand for its help and record which flags exist; ask the top-level binary for its help and record whether any backup, restore or export command is present. Then read the Hermes home-detection guard and record precisely what it requires to be present before it will proceed, and confirm against the real install whether a root-level configuration exists that the guard causes to be ignored.

Now ground both source formats. For each peer read the format from the PINNED baseline commit named in the competitive ledger using git's object reader rather than checking the tree out, read the same paths at the checkout HEAD, and compare both against the real install on this Mac. For Hermes that means the profile-level configuration and environment files, the persona file, the skills and memories layout, and the root-level configuration the current importer ignores. For OpenClaw that means its primary configuration document and the legacy names it still accepts, the credentials directory, the per-agent tree, the memory and workspace roots, and the backup and last-known-good variants of the configuration — all of which are present in the real install. Do not assume OpenClaw's platform-specific home resolution: DETERMINE it from the peer's own source and record where you found it, because inventing that path would produce an importer that silently finds nothing on one platform.

Reconcile the three readings for each peer and record any drift between the pinned baseline and HEAD as an explicit compatibility range: which fields exist in both, which appeared after the baseline, and which the importer will therefore treat as optional. Where the pinned and HEAD formats disagree in a way that changes how a field must be parsed, that is a finding — record it with a severity.

Then write the generator. It walks a real peer state tree and emits a corpus that preserves the tree's shape exactly — every relative path, file name and file format — while replacing each value it classifies as secret with a distinct canary token, and recording the mapping from corpus location to canary token in a manifest inside the corpus. Classification must be explicit and written down, not implicit: name the rule by which a value is treated as secret. The canary tokens are generated into the corpus as data and are read from the manifest by later gates; never hard-code one into a source file or into this plan. Run the generator over Sean's real Hermes home and real OpenClaw home, commit the resulting corpus, and then prove two things about it: that running the generator twice over the same input yields byte-identical output, and that no real secret value from either source appears anywhere in the committed corpus. Prove the second by extracting the real values on the Mac into a scratch file outside the repository, searching the corpus for each, requiring zero matches, and deleting the scratch file before the task ends.

Write every observation, with its exact command and output, into `26-01-BASELINE.md`. Records evidence for F26-01; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f .planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md &amp;&amp; test "$(/usr/bin/grep -c . .planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md)" -ge "40"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f scripts/portability-corpus-gen.py &amp;&amp; python3 -c "import ast,sys; ast.parse(open('scripts/portability-corpus-gen.py').read())"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -d crates/wcore-cli/tests/fixtures/portability &amp;&amp; test "$(/usr/bin/find crates/wcore-cli/tests/fixtures/portability -type f | wc -l | tr -d ' ')" -ge "8"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; OUT=$(mktemp -d) &amp;&amp; python3 scripts/portability-corpus-gen.py --source "$HOME/.hermes" --kind hermes --out "$OUT/a" &gt;/dev/null 2&gt;&amp;1 &amp;&amp; python3 scripts/portability-corpus-gen.py --source "$HOME/.hermes" --kind hermes --out "$OUT/b" &gt;/dev/null 2&gt;&amp;1 &amp;&amp; /usr/bin/diff -r "$OUT/a" "$OUT/b" &amp;&amp; rm -rf "$OUT"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; S=$(mktemp) &amp;&amp; /usr/bin/grep -hoE '[A-Za-z0-9_]*_API_KEY=.*' "$HOME"/.hermes/profiles/*/.env 2&gt;/dev/null | /usr/bin/sed 's/^[^=]*=//; s/^["'"'"']//; s/["'"'"']$//' | /usr/bin/grep -vE '^\s*$' | sort -u &gt; "$S"; test -s "$S" || { echo "GATE INVALID: no real secret values extracted to search for"; rm -f "$S"; exit 1; }; HITS=0; while IFS= read -r v; do if /usr/bin/grep -RqF -- "$v" crates/wcore-cli/tests/fixtures/portability; then HITS=$((HITS+1)); fi; done &lt; "$S"; rm -f "$S"; test "$HITS" -eq 0</automated>
  </verify>
  <done>The shipped binary's pre-change behavior against BOTH real peer installs is recorded with exact commands, exit statuses and counts. Determinism and non-mutation are measured facts with the comparison recorded, not assertions about the code. All four gaps are established by interrogating the binary's own help output and the home-detection guard. Both peer source formats are grounded in three reconciled readings — pinned baseline, HEAD, and real install — with drift recorded as an explicit compatibility range and OpenClaw's platform home resolution determined from peer source rather than assumed. The generator is committed, is proven deterministic by a second run, and its corpus is proven to contain zero real secret values by a search that first proves it had real values to search for. No real secret was written to any committed file and the scratch extraction was deleted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the Hermes dry-run typed, structurally redacted and machine-readable, and close the root-profile discovery gap</name>
  <files>crates/wcore-config/src/lib.rs, crates/wcore-config/src/portability/mod.rs, crates/wcore-config/src/portability/redact.rs, crates/wcore-config/src/portability/digest.rs, crates/wcore-cli/src/migrate/mod.rs, crates/wcore-cli/src/migrate/hermes.rs, crates/wcore-cli/tests/migrate_typed_dryrun.rs</files>
  <read_first>.planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md (the grounded source-format contract and the measured baseline this task must preserve), crates/wcore-config/src/profile.rs (the existing secret-classification precedent — the secret directory names and the credentials file prefix, and `copy_tree_filtered`'s symlink discipline, because the portability module must classify secrets the SAME way rather than inventing a second definition), crates/wcore-config/src/lib.rs (module declaration order and the crate's public surface conventions), crates/wcore-cli/src/migrate/mod.rs (`render_plan`'s exact prose, which is the measured baseline output and must not change)</read_first>
  <behavior>
    - Test 1: a plan built from the committed Hermes corpus serializes to JSON that parses, and contains the profile count the corpus manifest declares — so an empty or malformed emission fails.
    - Test 2: no canary value from the corpus manifest appears anywhere in the serialized JSON, for a corpus that is proven to contain those canaries at their declared locations.
    - Test 3: serializing the same plan twice yields byte-identical JSON, and JSON built from two independent walks of the same corpus is byte-identical — determinism at the type level, not just at the printer.
    - Test 4: a digest of the corpus tree taken before and after a full discovery plus dry-run is identical — non-mutation.
    - Test 5: a Hermes home with a ROOT-level configuration and no profiles directory produces a plan containing that root setup, where today it errors out.
    - Test 6: a Hermes home with BOTH a root configuration and a profiles directory produces a plan containing the root setup AND every profile, with a stable, documented name for the root entry that cannot silently collide with a real profile name.
    - Test 7: the existing prose rendering is unchanged for the existing fixture — the measured baseline output is preserved byte for byte.
    - Test 8: a source tree containing a symlink that points outside the declared source root is not followed, and the escape is reported as a warning on the plan rather than silently skipped.
    - The emitted plan type has no field capable of carrying a secret value; a credential is represented by its source reference only.
  </behavior>
  <action>Write the tests first, from the behaviors above, and watch them fail before writing the implementation.

Create the portability module in `wcore-config` and declare it from the crate root. It owns three things and nothing else in this plan. First, the typed plan vocabulary that both peer sources produce and that the CLI serializes: a source descriptor naming the peer and the resolved home, an ordered collection of discovered items each carrying its kind, its stable identifier, its source path relative to the home, and its mapped target, plus the detected-but-not-imported inventory and the warnings. Order must be total and derived from the data — sort every collection by a key that exists in the source — so that two walks of the same tree cannot differ. Second, the redaction boundary: a type that represents a discovered credential as its source reference — the variable or key name and the file it came from — with NO variant capable of holding the value. Make this a property of the type rather than of the printer, so that serialization, logging, and error formatting all inherit it without each having to remember. Reuse the secret classification that the profile module already established rather than writing a second, divergent definition of what counts as a secret; if that classification needs to be shared, lift it so both call sites read the same source of truth. Third, the tree digest: a stable content digest over a directory tree, ordering entries deterministically, recording relative paths and content hashes, and refusing to traverse a symlink that resolves outside the root it was given.

Then surface the typed plan on the CLI. Add a machine-readable output flag to the migrate arguments that emits the typed plan as JSON to standard output and suppresses the prose rendering. The prose rendering itself must NOT change: the baseline measured its exact output and Task 1 recorded it, so it stays byte-identical for the existing fixture and any change to it is a regression. Make the flag compose with the existing dry-run flag, and make the machine-readable form of an apply that has not been confirmed emit the plan rather than mutating anything.

Then close the root-profile gap that Task 1 established. The home guard currently refuses any Hermes home without a profiles directory, which makes a root-only install unimportable and makes a rooted install's own top-level setup invisible. Change the guard so a home is acceptable when EITHER a profiles directory exists OR a root-level configuration exists, and extend the walk so the root setup becomes an item in the plan alongside the per-profile items. Give the root entry a stable name and make a collision with a real profile of that name impossible to resolve silently — either the name is one a profile directory cannot have, or a collision is reported as a conflict. Record which choice you made and why, in source, where a maintainer will see it. Do not change how an existing profile is mapped; that mapping is measured-good and is not this plan's subject.

Prove it on Linux at the plan's SHA: build the binary and run the crate suites. State the counts as a delta against what Task 1 measured, and name every residual failure. Records evidence for F26-01; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f crates/wcore-config/src/portability/redact.rs &amp;&amp; test -f crates/wcore-config/src/portability/digest.rs &amp;&amp; test "$(/usr/bin/grep -cF 'portability' crates/wcore-config/src/lib.rs)" -ge "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; for f in crates/wcore-config/src/portability/mod.rs crates/wcore-config/src/portability/redact.rs crates/wcore-config/src/portability/digest.rs crates/wcore-cli/src/migrate/hermes.rs crates/wcore-cli/src/migrate/mod.rs; do n=$(wc -l &lt; "$f" | tr -d ' '); test "$n" -lt 1000 || { echo "OVER 1000 LINES: $f ($n)"; exit 1; }; done</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes hetzner-dsm "cd /root/wayland &amp;&amp; git fetch --all --prune &amp;&amp; git checkout --detach $SHA &amp;&amp; git rev-parse HEAD &amp;&amp; cargo build --locked -p wcore-cli --bin wayland-core &amp;&amp; cargo clippy --locked -p wcore-config -p wcore-cli --all-targets -- -D warnings" &gt; /tmp/26-01-t2-build.log 2&gt;&amp;1; rc=$?; tail -20 /tmp/26-01-t2-build.log; test $rc -eq 0</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes hetzner-dsm "cd /root/wayland &amp;&amp; git checkout --detach $SHA &amp;&amp; cargo nextest run --locked -p wcore-config -p wcore-cli --no-fail-fast" &gt; /tmp/26-01-t2-test.log 2&gt;&amp;1; rc=$?; tail -30 /tmp/26-01-t2-test.log; test $rc -eq 0</automated>
  </verify>
  <done>All eight behaviors exist as tests that were written before the implementation and observed to fail first. The portability module owns the typed vocabulary, the structural redaction boundary and the tree digest, and reuses the existing secret classification rather than duplicating it. The machine-readable flag emits deterministic JSON in which a secret value is unrepresentable by TYPE, and the prose rendering is byte-identical to the measured baseline. The root-profile gap is closed for both a root-only and a rooted-plus-profiles install, with the collision decision recorded in source. Every touched file is under 1000 lines. Linux build, clippy at deny-warnings, and the two crate suites are green at this SHA with counts stated as a delta against Task 1's baseline and every residual failure named.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the reciprocal OpenClaw discovery source and prove all four properties live against real-world state</name>
  <files>crates/wcore-cli/src/migrate/openclaw.rs, crates/wcore-cli/src/migrate/mod.rs, crates/wcore-cli/tests/migrate_typed_dryrun.rs</files>
  <read_first>.planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md (the grounded OpenClaw format contract, its platform home resolution, and the pinned-versus-HEAD compatibility range), crates/wcore-cli/src/migrate/hermes.rs (the mapper idiom the OpenClaw source must match — permissive deserialization that ignores unknown keys, deterministic sort, warnings rather than hard failures on individually bad entries), crates/wcore-config/src/portability/mod.rs (the typed vocabulary this source must produce), crates/wcore-config/src/config.rs (the MCP server config and profile config shapes an OpenClaw entry maps onto)</read_first>
  <behavior>
    - Test 1: an OpenClaw home containing the primary configuration document yields a plan naming the mapped model and provider settings, with the count the corpus manifest declares.
    - Test 2: each legacy configuration name that the peer still accepts is discovered when the primary name is absent, and the primary wins when several are present, with the choice reported.
    - Test 3: backup and last-known-good variants of the configuration that sit beside the primary are NOT treated as additional sources — a real install has several of them and importing them would multiply every item.
    - Test 4: credentials discovered in the credentials directory and in per-agent auth material are represented by source reference only; no canary value from the corpus manifest appears in the serialized plan.
    - Test 5: discovery is deterministic — two independent walks of the same OpenClaw corpus serialize byte-identically.
    - Test 6: a digest of the OpenClaw corpus tree before and after discovery plus dry-run is identical.
    - Test 7: a malformed primary configuration produces a warning-bearing plan or a named error, never a panic and never a silently empty plan that reads as success.
    - Test 8: items whose kind this plan does not import are reported in the detected-but-not-imported inventory rather than dropped, so nothing discovered disappears without being named.
  </behavior>
  <action>Write the tests first against the committed OpenClaw corpus, and watch them fail.

Build the OpenClaw discovery source as a sibling of the Hermes one, matching its idiom exactly: pure reconnaissance that never writes, permissive deserialization that ignores keys it does not know, a deterministic total order, and per-entry warnings rather than a hard failure that discards the whole plan. Resolve the OpenClaw home from an explicit override first and otherwise from the platform resolution Task 1 DETERMINED from peer source — do not re-derive it here and do not assume it. Read the primary configuration document, and when it is absent fall back through the legacy names the peer still honours, reporting which one was used. Treat the backup and last-known-good siblings of the configuration as what they are — prior revisions of the same document, not additional sources — because the real install carries several and counting them would multiply every discovered item.

Map what maps and name what does not. Model and provider settings, and MCP server definitions, map onto the existing configuration shapes exactly as the Hermes source maps its equivalents. Credentials are discovered and represented by source reference only, through the redaction type, so no value can reach the emitted plan. Everything else the real install carries — the per-agent tree, memory, workspace, plugins and plugin skills, flows, tasks, identity, logs and the interface state — goes into the detected-but-not-imported inventory with its count, because a discovered item that is neither imported nor named has been silently dropped, and that is the failure mode this criterion exists to prevent. Register the new source on the migrate command so it appears in help output alongside the existing one.

Then prove all four properties live, on real-world state, with the real binary.

On Linux, build the binary at this SHA and run it against BOTH committed corpora with the machine-readable flag and the dry-run flag. For each: assert the process exited zero; assert the emitted document is well-formed JSON whose item count equals the count the corpus manifest declares — this is the positive assertion that stops an empty output from passing; assert that every canary token listed in the corpus manifest is absent from the emitted document, reading the tokens from the manifest rather than from any literal; assert that a second run of the same invocation emits byte-identical output; and assert that a digest of the corpus tree is unchanged across the run. Capture the return code of each remote invocation on its own line and test it BEFORE reading any output, and never place a filter between the command and its status.

On the Mac, with the real trees, the same run must be made against the REAL `~/.hermes` and REAL `~/.openclaw` — which is where the only real-world-scale input exists, with twelve real profiles, hundreds of real skill directories and real credentials. That leg needs an arm64 binary this host cannot build, so it is executed at the blocking checkpoint in Task 4 rather than asserted here. What this task does provide for it is the check itself: a committed script that takes a binary path, runs both discoveries against the real homes with the machine-readable and dry-run flags, extracts the real secret values from the real sources into a scratch file outside the repository, requires that the extraction found at least one value before it will report anything, searches each emitted document for each real value, requires zero matches, digests both real homes before and after to prove non-mutation, deletes the scratch file, and exits non-zero on any failure. Write it so its own failure modes are loud: an empty extraction, an unparseable document, or a zero item count each fail it.

State the Linux suite counts as a delta against Task 2's. Records evidence for F26-01; marks no requirement complete — closure is claimed at the checkpoint in Task 4.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f crates/wcore-cli/src/migrate/openclaw.rs &amp;&amp; test "$(/usr/bin/grep -cF 'openclaw' crates/wcore-cli/src/migrate/mod.rs)" -ge "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes hetzner-dsm "cd /root/wayland &amp;&amp; git fetch --all --prune &amp;&amp; git checkout --detach $SHA &amp;&amp; cargo build --locked --release -p wcore-cli --bin wayland-core &amp;&amp; ./target/release/wayland-core migrate --help" &gt; /tmp/26-01-t3-help.log 2&gt;&amp;1; rc=$?; cat /tmp/26-01-t3-help.log; test $rc -eq 0 &amp;&amp; /usr/bin/grep -qF 'openclaw' /tmp/26-01-t3-help.log &amp;&amp; /usr/bin/grep -qF 'hermes' /tmp/26-01-t3-help.log</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); ssh -o BatchMode=yes hetzner-dsm "cd /root/wayland &amp;&amp; git checkout --detach $SHA &amp;&amp; cargo nextest run --locked -p wcore-config -p wcore-cli --no-fail-fast" &gt; /tmp/26-01-t3-test.log 2&gt;&amp;1; rc=$?; tail -30 /tmp/26-01-t3-test.log; test $rc -eq 0</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -x scripts/portability-real-state-check.sh || test -f scripts/portability-real-state-check.sh</automated>
  </verify>
  <done>The OpenClaw source exists, is registered on the migrate command and appears in the real binary's help output alongside Hermes, proven by running the built binary on Linux rather than by reading the source. All eight behaviors exist as tests written before the implementation. Legacy configuration names are honoured and backup and last-known-good siblings are excluded, so a real install's several revisions do not multiply the plan. Every discovered item is either mapped or named in the detected-but-not-imported inventory; nothing is dropped unnamed. On Linux both corpora produce deterministic, well-formed, canary-free JSON with the manifest-declared item count, over an unchanged source-tree digest, with every remote status captured on its own line and tested before any output was read. The real-state check script is committed and fails loudly on an empty extraction, an unparseable document or a zero item count.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (BLOCKING): Run the real discovery against Sean's REAL Hermes and OpenClaw installs on macOS and confirm the typed output carries no real secret</name>
  <files>.planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md</files>
  <what-built>A typed, deterministic, structurally redacted, non-mutating discovery and dry-run for BOTH Hermes and OpenClaw, with a machine-readable output flag, the root-profile discovery gap closed, and the reciprocal OpenClaw path that the competitive ledger records Core as lacking. The Linux leg is proven automatically against corpora whose structure was cloned from Sean's real installs with canary values substituted. What remains is the leg that no corpus can substitute for: the real trees, with real credentials, at real scale — twelve real profiles, 540 real skill directories, and a real OpenClaw configuration with live credentials beside it.</what-built>
  <how-to-verify>
This leg needs an `arm64` macOS binary, and this Mac cannot compile the workspace. Obtain one WITHOUT running cargo here — either from the certified macOS runner that Phase 20A proved works (the same path that produced the 8/8 macOS leg of run `30184651330`), or from any Mac that is not this planning host. Do not build here, and do not substitute the stale `target/debug/wayland-core` already on this box: it predates every change in this plan and would prove the old behavior.

1. Put the fresh binary somewhere outside the repository and note its path and its version output.
2. Run `scripts/portability-real-state-check.sh <path-to-binary>`. It runs both discoveries against the REAL `~/.hermes` and REAL `~/.openclaw` with the machine-readable and dry-run flags, extracts the real secret values from the real sources, and searches both emitted documents for each one.
3. Read what it prints. Expect: both invocations exit zero; the Hermes document reports **12 profiles** and **1 MCP server**, matching the pre-change baseline Task 1 measured, so the change did not lose anything; the OpenClaw document reports a non-zero item count; the extraction found at least one real secret value to search for; zero of those values appear in either document; and the digests of both real homes are unchanged before and after.
4. Confirm by eye that the emitted JSON names credentials by their variable or key name only — for example a provider key variable and the file it came from — and never shows a value.
5. If ANY real secret value appears in either document, that is a CRITICAL finding. Stop, do not proceed to 26-02, and report it.

The script deletes its own scratch extraction. Confirm nothing containing a real secret value was left behind, in the repository or elsewhere, and that nothing from either real home was copied to `hetzner-dsm` or `seandesktop` at any point.
  </how-to-verify>
  <resume-signal>Type "approved" with the observed profile count, the observed OpenClaw item count, and confirmation that zero real secret values appeared — or describe exactly what leaked or diverged.</resume-signal>
  <verify>
    <human-check>Both discoveries ran against the REAL homes with a FRESH arm64 binary that was not built on this host; the Hermes item counts match the pre-change baseline; the secret-value search proved it had values to search for and found none in either emitted document; both real-home digests are unchanged; and no real secret value was written to any file or copied to any other host.</human-check>
  </verify>
  <done>The real-state run is recorded in `26-01-BASELINE.md` with the binary's provenance and version, the observed counts for both peers, the confirmation that the secret search was non-vacuous and returned zero hits, and the before-and-after digests of both real homes. F26-01 is claimed complete only if this leg passed; if any real secret appeared, the plan terminates on that CRITICAL finding instead.</done>
</task>

</tasks>

## What this plan does NOT change (scope fence)

- **`apply` behavior — untouched.** What an import actually WRITES is 26-02's. This plan changes discovery and dry-run only. The existing merge-preserving apply path and its four regression tests stay exactly as they are.
- **Quarantine and provenance — 26-02 owns them.** The 540 real skill directories and 12 persona files stay in the detected-but-not-imported inventory here; making them importable safely is the next plan's whole subject.
- **Backup, restore and rollback — 26-03 owns them.** No top-level command is added by this plan.
- **Hostile corpora — 26-04 owns them.** The corpus this plan commits is a STRUCTURE clone of real installs with canary values; it is a realism fixture, not an adversarial one.
- **`wcore-protocol` — not touched, deliberately.** No migration event is added to the JSON stream protocol, so the D1 producer contract, its generated fixtures and every Desktop consumer are untouched. Migration is a CLI surface in this phase. If a protocol event later proves necessary, that is a contract change requiring the D1 checkpoint, and it is out of scope here.
- **The prose rendering — byte-identical.** It is the measured baseline; `--json` is additive. Changing prose output would silently break anyone parsing it.
- **The existing Hermes profile mapping — untouched.** Provider/model prefix stripping, transport inference and the deterministic credential-variable choice are all measured-good; only the home guard and the root-level walk change.
- **No new external dependency.** Everything needed is already in the workspace.
- **No test is deleted, weakened, re-gated, `#[ignore]`d or `#[allow]`ed.**

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer state tree → Core discovery walk | The source tree is attacker-influenced: symlinks, `..` components, absolute paths, case/Unicode collisions and enormous files all cross here |
| peer config values → Core typed plan | Values inside a peer configuration are untrusted strings, several of which are commands that Core may later execute |
| discovered credential → emitted plan | The point at which a real secret either does or does not become representable in an output document |
| real peer install (Mac) → any other host | Real credentials live on this Mac; the corpus generator exists so that nothing needs to cross this boundary |
| plan output → downstream consumer | `--json` creates machine consumers that will serialize whatever the type can hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01-01 | Information Disclosure | A real provider credential reaches the emitted JSON, a log line, or an error message, and `--json` makes that machine-consumable and easy to persist | critical | mitigate | Redaction is structural: the credential type has no variant that can hold a value, so serializers, loggers and error formatters all inherit it. Proven on Linux against manifest-declared canaries and at the blocking checkpoint against REAL secrets extracted from the real installs, with the search first proven non-vacuous |
| T-26-01-02 | Information Disclosure | A real secret leaves the Mac by being copied into the committed corpus, a planning artifact, or onto `hetzner-dsm` | critical | mitigate | The generator substitutes canaries for every classified secret; a gate extracts the real values and requires zero matches in the committed corpus and fails if the extraction was empty; copying any real peer file to another host is forbidden outright by the execution rules |
| T-26-01-03 | Tampering | A symlink or `..` component in a peer tree walks discovery outside the declared source root, letting a hostile state directory make Core read arbitrary files | high | mitigate | The tree walk and the digest both refuse to traverse a link resolving outside the root they were given, and the escape is surfaced as a plan warning rather than silently skipped; a dedicated test covers it |
| T-26-01-04 | Spoofing | OpenClaw's platform home resolution is ASSUMED rather than determined, producing an importer that silently finds nothing on one platform and reports success | high | mitigate | Task 1 must DETERMINE the resolution from the peer's own source and record where it was found; Task 3 is forbidden from re-deriving it; a zero item count fails the real-state check rather than passing |
| T-26-01-05 | Repudiation | The dry-run mutates the source it is previewing, so a user who previews an import has already changed the thing they were deciding about | high | mitigate | Non-mutation is proven by a tree digest taken before and after, on both corpora automatically and on both REAL homes at the checkpoint — measured, never asserted from the code's shape |
| T-26-01-06 | Tampering | A gate passes vacuously: a negative grep over an empty, missing or unparseable output file reports success and manufactures confidence | high | mitigate | Every negative assertion is paired with a positive one — well-formed JSON, manifest-declared item count, non-empty secret extraction — so an empty or malformed emission goes RED; remote statuses are captured on their own line and tested before any output is read, and no filter is ever placed between a gate command and its status |
| T-26-01-07 | Denial of Service | An enormous or deeply nested peer tree makes discovery hang or exhaust memory; the real install already carries 540 skill directories | medium | mitigate | Reads are bounded and the inventory counts rather than loads deferred content; the real-state check at the checkpoint runs against the real 540-directory tree, which is the actual scale test |
| T-26-01-08 | Spoofing | The stale `target/debug/wayland-core` on the Mac is used to prove NEW behavior, so the checkpoint certifies the old product | medium | mitigate | The stale binary is admissible for the pre-change baseline ONLY; the checkpoint requires a fresh binary with its provenance and version recorded, and explicitly forbids the stale one |
| T-26-01-09 | Tampering | Backup and last-known-good configuration siblings are treated as additional sources, multiplying every discovered item on a real install that carries several | medium | mitigate | A dedicated test asserts they are excluded, written against the real install's observed set of such files |
| T-26-01-10 | Repudiation | Peer format is grounded only at HEAD, so the importer targets a format the pinned baseline never had — or only at the baseline, so it misses what real users run | medium | mitigate | Three readings — pinned, HEAD, and real install — are reconciled and their drift recorded as an explicit compatibility range; a parse-changing disagreement is a recorded finding |
| T-26-01-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency is added, removed or updated; no `Cargo.toml` or `Cargo.lock` change; the execution rules require escalation rather than an install, so no legitimacy gate applies |
</threat_model>

<verification>
Local gates (Mac, source level only — this host cannot compile): `cargo fmt --all -- --check` clean; `26-01-BASELINE.md` exists and is substantive; the generator is valid Python and deterministic across two runs over the same real source; the committed corpus contains at least eight files and zero real secret values, proven by a search that first proves it had values to search for; every new or touched source file is under 1000 lines.

Authoritative gates (`hetzner-dsm`, at this plan's SHA, each with its status captured on its own line and tested before any output is read): `cargo build --locked -p wcore-cli --bin wayland-core`; `cargo clippy --locked -p wcore-config -p wcore-cli --all-targets -- -D warnings`; `cargo nextest run --locked -p wcore-config -p wcore-cli --no-fail-fast`; and the built binary's own `migrate --help` listing BOTH sources — proving registration by running the product, not by reading the source. Both corpora must yield well-formed JSON with the manifest-declared item count, byte-identical across two runs, canary-free, over an unchanged tree digest.

Blocking human gate (macOS, real state): a FRESH arm64 binary not built on this host runs both discoveries against the REAL `~/.hermes` and `~/.openclaw`; Hermes reports the 12 profiles and 1 MCP server the pre-change baseline measured; the real-secret search is non-vacuous and returns zero hits; both real homes digest identically before and after.

Known unknowns to record, not resolve here: whether OpenClaw's Windows home resolution differs from what its source declares once observed on real Windows (26-04 owns the native leg); whether any Hermes or OpenClaw install in the wild carries a profile layout neither the pinned baseline, HEAD, nor Sean's install exhibits; and whether the 540-directory skill tree contains individual skills whose formats the deferred inventory miscounts, which 26-02 will discover when it actually imports them.
</verification>

<success_criteria>
- The shipped binary's behavior against BOTH REAL peer installs is MEASURED before anything changes, and determinism and non-mutation are recorded as measured facts rather than claims about the code.
- Both peer source formats are grounded in three reconciled readings — pinned baseline, checkout HEAD, and real install — with drift recorded as an explicit compatibility range, and OpenClaw's platform home resolution determined from peer source rather than assumed.
- Discovery and dry-run are TYPED: a machine-readable plan is emitted for both peers and the prose rendering is preserved byte-identically.
- Discovery and dry-run are DETERMINISTIC: two independent walks of the same source serialize byte-identically, proven on both corpora.
- Discovery and dry-run are SECRET-REDACTED structurally, not cosmetically: the emitted type cannot hold a secret value, proven against manifest canaries on Linux and against REAL credentials on macOS.
- Discovery and dry-run are NON-MUTATING, proven by a tree digest before and after on both corpora and on both real homes.
- The reciprocal OpenClaw path exists and is visible in the real binary's help output, closing the Core direction of the ledger's `PORT-*` gap.
- The root-profile discovery gap is closed for both a root-only and a rooted-plus-profiles install, with the name-collision decision recorded in source.
- Every discovered item is either mapped or named in the detected-but-not-imported inventory — nothing is dropped unnamed.
- Every gate can go red: each negative assertion is paired with a positive one, and no remote gate reads its status through a pipe.
- No real secret value was written to any committed file, and no real peer file crossed a host boundary.
</success_criteria>

## Artifacts this plan produces
- `.planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md` — the measured pre-change behavior against both real installs, the three-way peer format grounding with drift, and the real-state checkpoint record.
- `scripts/portability-corpus-gen.py` and `crates/wcore-cli/tests/fixtures/portability/` — the structure-cloned, canary-substituted corpora and their manifests.
- `scripts/portability-real-state-check.sh` — the real-state, real-secret check used at the blocking checkpoint.
- `crates/wcore-config/src/portability/` — the typed vocabulary, the structural redaction boundary and the tree digest.
- `crates/wcore-cli/src/migrate/openclaw.rs` and the typed `--json` surface on `migrate`.
- `26-01-SUMMARY.md`.

<output>
Create `.planning/phases/26-migration-export-backup-restore/26-01-SUMMARY.md` using the standard summary template. Record: the exact pre-change invocations against both REAL peer installs with their exit statuses and observed counts; the measured determinism and non-mutation results; the four gaps as established from the binary's own help output and the home guard; the three-way peer format grounding with the pinned-versus-HEAD drift and where OpenClaw's platform home resolution was found in peer source; the corpus generator's classification rule and its determinism proof; the typed vocabulary and exactly how the redaction type makes a secret value unrepresentable; the root-profile gap fix and the name-collision decision; the OpenClaw source's mapped versus deferred inventory; the Linux gate results as a delta against the measured baseline with every residual failure named; the real-state checkpoint outcome including the fresh binary's provenance and version and the non-vacuous zero-hit secret search; confirmation that no real secret was committed or copied off the Mac; the recorded unknowns; and which of the three termination states the plan ended in. F26-01 is marked complete only if the blocking checkpoint passed.
</output>

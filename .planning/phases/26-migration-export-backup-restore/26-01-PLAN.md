---
phase: 26-migration-export-backup-restore
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md
  - scripts/portability-corpus-gen.py
  - crates/wcore-cli/tests/fixtures/portability/README.md
  - crates/wcore-config/src/lib.rs
  - crates/wcore-config/src/portability/mod.rs
  - crates/wcore-config/src/portability/redact.rs
  - crates/wcore-config/src/portability/digest.rs
  - crates/wcore-cli/src/migrate/mod.rs
  - crates/wcore-cli/src/migrate/hermes.rs
  - crates/wcore-cli/src/migrate/openclaw.rs
  - crates/wcore-cli/tests/migrate_typed_dryrun.rs
  - scripts/panel-ask.sh
  - scripts/panel-decision-check.sh
  - .planning/phases/26-migration-export-backup-restore/panel/26-01-redaction-contract/
autonomous: true
requirements:
  - F26-01
must_haves:
  truths:
    - "MEASURED LIVE, NOT ASSUMED — this plan's premises were established by running the REAL shipped `wayland-core` binary against Sean's REAL peer installs before any of it was written. `./target/debug/wayland-core migrate hermes --home ~/.hermes --dry-run` exits 0 and reports **12 profiles**, **1 MCP server** (`ijfw-memory`), **540 skill directories**, **12 `SOUL.md` persona files**, and 0 memory notes. Two consecutive runs produced byte-identical stdout, so determinism is a MEASURED baseline this plan must PRESERVE — never a property it may claim to have introduced."
    - "THE FOUR GAPS ARE VERIFIED AGAINST THE SHIPPED BINARY, NOT INFERRED. `wayland-core migrate --help` lists exactly one subcommand, `hermes` — there is NO OpenClaw path. `wayland-core migrate hermes --help` lists exactly `--home`, `--dry-run`, `--yes`, `--include-credentials`, `--overwrite` — there is NO `--json`, so the dry-run output is human prose and is NOT typed. `wayland-core --help` has no `backup`, `restore`, or `export` top-level command. And `hermes::detect_home` (`crates/wcore-cli/src/migrate/hermes.rs`, the `profiles.is_dir()` guard) BAILS unless `<home>/profiles` exists, so a Hermes install that keeps its setup only in the ROOT `config.yaml` + `.env` is unimportable, while a rooted install like Sean's has its root profile silently ignored."
    - "BOTH PEERS MIGRATE FROM EACH OTHER AND CORE IS THE ONLY PARTY WITH NO RECIPROCAL PATH. `intel/COMPETITIVE-LEDGER.md` row `PORT-*` records this as the measured competitive gap, and every peer file it names was confirmed present in the real checkouts: Hermes ships `hermes_cli/migrate.py`, `hermes_cli/backup.py`, `agent/curator_backup.py` and a 3136-line `optional-skills/migration/openclaw-migration/scripts/openclaw_to_hermes.py`; OpenClaw ships `docs/cli/migrate.md`, `docs/cli/backup.md`, `docs/install/migrating-hermes.md` and `extensions/codex/src/migration/{apply,auth}.ts`. Closing the OpenClaw direction is this plan's half of that gap."
    - "THE PEER CHECKOUTS ARE AT HEAD, NOT AT THE PINNED BASELINE, AND THAT DIFFERENCE IS LOAD-BEARING. `/Users/seandonahoe/dev/resources/hermes-agent` is at `d59b79fadd1e9edd7afc5c679cc3b143838e7c01` (0.18.2) but the ledger pins Hermes at `dbe734be` (0.17.0); `/Users/seandonahoe/dev/resources/openclaw` is at `3659c85e534fdb8b8ce6b7505a83d92cc2e4df8e` (2026.7.2) but the ledger pins OpenClaw at `11a0ad10` (2026.6.2). Reading only HEAD would ground the importer in a format the pinned baseline may not have; reading only the baseline would ground it in a format real users no longer run. Both are read, and any drift between them is a recorded compatibility range, not a footnote."
    - "IMPORTED PEER STATE IS HOSTILE INPUT. A peer state directory is attacker-influenced by construction: it can contain symlinks pointing outside itself, `..` path components, absolute paths, names that collide after case-folding or Unicode normalization, files that are enormous, and configuration whose string values are executable commands. Discovery walks that tree. Every read is bounded and every emitted path is validated, and the walk NEVER follows a symlink out of the declared source root."
    - "REDACTION MUST BE STRUCTURAL, NOT COSMETIC. A secret that is redacted when printed but present in the typed value has leaked to every consumer that serializes it — and `--json` creates exactly such a consumer. The secret value must be unrepresentable in the emitted plan type, so that no downstream printer, log line, error message, or serializer can emit it. The existing code already gets the SHAPE of this right for the prose path — it records `credential_env_var` (a name) and only populates `api_key` under `--include-credentials` — and that discipline is what the typed model must carry, not weaken."
    - "A GATE THAT CANNOT FAIL IS WORSE THAN NO GATE. Every negative assertion in this plan is paired with a positive one, because a negative grep over an empty or missing file passes vacuously and manufactures confidence. The redaction gate asserts BOTH that no canary value appears in the output AND that the output is well-formed JSON containing the expected profile count — so an empty output file goes RED rather than green."
    - "A REPORTED RED IS WORTH FAR MORE THAN AN ENGINEERED GREEN. Never weaken an assertion, `#[ignore]`, `#[allow]`, raise a timeout, re-gate, or delete an inconvenient test to reach a gate. If the honest outcome is that a premise here was refuted, record the refutation with its evidence and close on it."
  artifacts:
    - path: .planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md
      provides: "The measured pre-change behavior of the shipped binary against both REAL peer installs, the pinned-versus-HEAD peer format grounding with its drift recorded, and the enumerated source-format contract each importer is written against"
    - path: scripts/portability-corpus-gen.py
      provides: "The generator that clones the STRUCTURE of a real peer state tree into a committed corpus while substituting known canary values for every real secret — so the authoritative Linux gate runs against real-world shape without a real secret ever leaving the Mac"
    - path: crates/wcore-config/src/portability/redact.rs
      provides: "The structural redaction boundary: secret values are unrepresentable in the emitted plan type"
    - path: crates/wcore-config/src/portability/digest.rs
      provides: "The content digest over a directory tree that proves discovery and dry-run are non-mutating"
    - path: crates/wcore-cli/src/migrate/openclaw.rs
      provides: "The reciprocal OpenClaw discovery source that closes the ledger's PORT-* gap in the Core direction"
    - path: crates/wcore-cli/src/migrate/mod.rs
      provides: "The typed, deterministic, machine-readable dry-run plan surfaced as `--json`, alongside the preserved prose rendering"
    - path: scripts/panel-ask.sh
      provides: "The wrapper that hands ONE evidence bundle byte-for-byte to all three external panel members and stamps each capture with that bundle's digest — so 'every panelist saw the same evidence' is a property of the mechanism rather than of the executor's discipline"
    - path: scripts/panel-decision-check.sh
      provides: "The mechanical enforcement of the panel procedure: four verbatim captures, one well-formed verdict each from the plan's own option set, pairwise-distinct external responses, a chosen option that is either the majority or carries named live evidence, and a dissent section derived from the CAPTURED verdicts rather than from prose"
  key_links:
    - from: crates/wcore-cli/src/migrate/openclaw.rs
      to: crates/wcore-config/src/portability/redact.rs
      via: "every discovered credential is recorded as a source reference, never a value"
      pattern: "structural-redaction"
    - from: crates/wcore-config/src/portability/digest.rs
      to: crates/wcore-cli/tests/migrate_typed_dryrun.rs
      via: "the source tree digest is taken before and after a full dry-run and must be identical"
      pattern: "non-mutation-proof"
    - from: scripts/portability-corpus-gen.py
      to: crates/wcore-cli/tests/fixtures/portability/
      via: "real peer tree structure, canary-substituted, committed as the authoritative Linux gate corpus"
      pattern: "grounded-fixture"
---

<objective>
Make Hermes AND OpenClaw discovery and dry-run typed, deterministic, secret-redacted, and non-mutating — and prove each of those four words with a gate that can actually go red.

Purpose: Success Criterion 1 names four properties. Measured against the shipped binary, Core has one of them today: the Hermes dry-run is deterministic and non-mutating, but its output is untyped prose with no machine-readable form, and there is no OpenClaw path at all. The competitive ledger's `PORT-*` row records that both peers ship migration paths FROM each other while Core is the only party with no reciprocal path; this plan closes the discovery half of that gap and establishes the typed vocabulary that 26-02 and 26-03 build on.
Output: A measured baseline against Sean's real peer installs; a corpus whose structure is cloned from those real trees with canary values substituted for secrets; a typed plan model whose type makes secret values unrepresentable; a `--json` surface; the root-profile discovery gap closed; and an OpenClaw discovery source, all proven live on Linux against the corpus and on macOS against the real trees.
</objective>

<execution_context>
@/Users/seandonahoe/.codex/gsd-core/workflows/execute-plan.md
@/Users/seandonahoe/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/intel/COMPETITIVE-LEDGER.md
@crates/wcore-cli/src/migrate/mod.rs
@crates/wcore-cli/src/migrate/hermes.rs
@crates/wcore-cli/tests/migrate_hermes.rs
@crates/wcore-config/src/profile.rs
</context>

<execution_rules>

**THE TWO PHASE RULES — stated verbatim, and they bound this plan.**

- Findings at CRITICAL or HIGH must be fixed or disproved. MEDIUM and below are logged to `.planning/BACKLOG.md` and DO NOT BLOCK execution.
- Execution begins when no CRITICAL or HIGH finding is open, or after 2 review rounds, whichever comes first. A third round is NOT permitted; it escalates to Sean.

**TERMINATION CRITERION FOR THIS PLAN (hard).** This plan measures ONCE, builds the typed discovery surface ONCE, and proves it ONCE. It terminates in exactly one of three states, and in all three it writes its SUMMARY and stops:
1. **Complete** — both peer sources discover, both emit a typed deterministic redacted plan, non-mutation is proven by digest, and the live legs pass.
2. **Blocked on a source-format unknown** — a peer's on-disk format cannot be established from the pinned source, the HEAD source, and the real install together. STOP, record exactly which field could not be grounded, and escalate rather than guessing a schema.
3. **Escalated** — a CRITICAL or HIGH finding is open that cannot be fixed inside this plan's declared files, OR a MANDATORY proof cannot be run at all. The macOS real-secret leg in Task 4 is one such proof: if its binary cannot be obtained from the producer named below, STOP and escalate with the exact failure, leaving F26-01 UNCLAIMED. There is no fourth state in which F26-01 closes without that leg. An earlier revision of this plan invented one, on a measurement that was accurate at the time and is now false; closing a requirement on the evidence that happens to be available is exactly the rounding-up this phase exists to prevent.
Under no circumstances does this plan spawn additional plans, extend its own task list, or begin implementing import/apply behavior — that is 26-02's, and backup/restore is 26-03's.

**SCOPE BOUNDARY (hard).** This plan changes DISCOVERY and DRY-RUN only. It does not change what `apply` writes, it does not add quarantine or provenance (26-02), it does not add backup/restore (26-03), and it does not add hostile corpora (26-04). If a discovery finding implicates apply behavior, RECORD it with severity and stop rather than following it.

**NO NEW DEPENDENCY.** Every crate this plan needs is already in the workspace: `serde`, `serde_json`, `serde_yaml`, `toml`, `sha2`, `tempfile`, `dirs`, `anyhow`. Adding an external dependency is out of scope; if one appears genuinely necessary, STOP and escalate rather than adding it, because a package install would trigger the legitimacy gate this plan is not scoped to run.

**SECRETS NEVER LEAVE THE MAC.** Sean's real `~/.hermes` and `~/.openclaw` contain live provider credentials. They are read on the Mac and nowhere else. Copying either tree — or any file from it — to `hetzner-dsm`, to `seandesktop`, into the repository, into a planning artifact, or into a commit is FORBIDDEN. The corpus generator exists precisely so the Linux gate can run against real-world SHAPE without a real secret ever crossing a host boundary. Any evidence file that would contain a real secret value is not written at all.

**ENVIRONMENT.**
- **Mac (this host, `arm64`)** — CANNOT compile this workspace; never run `cargo build`/`test`/`check` here. `cargo fmt --all` DOES work and is the local format gate. A previously built `target/debug/wayland-core` exists here (confirmed present) and is the binary used for the pre-change baseline measurement ONLY — it is stale with respect to this plan's changes and must never be used to prove new behavior. Use `/usr/bin/git` and ALWAYS `/usr/bin/grep`; prefer absolute `/usr/bin/` paths for `find`, `sed`, `diff` and `wc` in gates too, because `grep`, `find` and `ls` on this host resolve to an rtk proxy that silently drops or rewrites lines.
- **Linux — `ssh -o BatchMode=yes hetzner-dsm`** — the authoritative build and automated-gate host. This plan works in its OWN worktree `/root/wayland-f26-01` and NEVER checks out the shared `/root/wayland`; see the remote gate shape below.
- **Peer sources** — `/Users/seandonahoe/dev/resources/hermes-agent` and `/Users/seandonahoe/dev/resources/openclaw`, both read-only. Never commit into them, never mutate them, never leave a detached checkout behind: read pinned content with `git show <sha>:<path>` rather than checking the tree out.
- Push the WORK BRANCH to `gh` so Hetzner can fetch it. NO push to main, merge, PR, tag, release, or issue closure without Sean.

**THE REMOTE GATE SHAPE — three MEASURED host facts, each of which silently invalidates a gate that ignores it.**

1. **A non-login `ssh` shell on `hetzner-dsm` has no `cargo` on `PATH`.** Measured: `ssh hetzner-dsm 'cargo --version'` returns `bash: cargo: command not found`, and that shell's `PATH` is `/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin` with no `/root/.cargo/bin` in it. The toolchain IS there — `/root/.cargo/bin` holds `cargo` (1.95.0 under this tree's pinned toolchain), `cargo-nextest` 0.9.137 and `clippy` 0.1.95. Every remote command therefore begins by prepending `/root/.cargo/bin` to `PATH`. Without that, every Linux gate in this plan is GUARANTEED RED for a reason that has nothing to do with the code — which is as useless as a gate that cannot go red.
2. **`/root/wayland` is a SHARED working tree and this phase is not its only user.** Measured: `git worktree list` there reports `/root/wayland` plus twelve sibling worktrees (`/root/wayland-f07-*`, `-f076-*`, `-f11-*`, `-frontier-m0`), so a per-agent worktree is already this box's own convention rather than a mechanism this plan is inventing. The 20A handoff prescribes exactly that — "Give each concurrent agent its **own worktree** per host: concurrent compile load on one shared box corrupted a proof run" — and `ci.yml`'s concurrency comment records that phases 24-27 execute in bounded parallel, so another phase can detach `/root/wayland` while this plan is mid-build. `/root/wayland` is used ONLY as the object store to fetch into, under an `flock` so two concurrent fetches cannot race; all work happens in `/root/wayland-f26-01`. Disk headroom was checked: 647 GB free.
3. **Worktree isolation is the prevention; the SHA assertion is the DETECTION.** Every remote gate asserts the worktree is at this plan's SHA immediately BEFORE the work and again immediately AFTER it. If anything moved the tree mid-flight the gate goes RED and its evidence is rejected, instead of yielding a green that does not correspond to the SHA it claims.

The shape, used by every Linux gate in this plan:

`SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-01; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2>/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; «WORK»; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA" > «LOG» 2>&1; rc=$?; tail -30 «LOG»; test $rc -eq 0`

`set -e` makes any step's failure the ssh exit status; the status is captured on its own line and tested BEFORE the log is read; no filter ever sits between the command and its status. When this plan's last remote gate has passed, remove the worktree from `/root/wayland` with `git worktree remove --force /root/wayland-f26-01 && git worktree prune`. A FAILED gate leaves the worktree in place for forensics.

**THE macOS BINARY — the acquisition path, measured, and MANDATORY.**

Task 4 needs a fresh `arm64` macOS binary built from this plan's own SHA, and this plan forbids building one here. There is exactly one admissible producer and it is real: CI's `build` job.

An earlier draft named "the certified macOS runner that Phase 20A proved works" as a source DISTINCT from this host. That is false, and it was checked: the 20A handoff records that runner under the heading "macOS runner (this Mac)" — `~/actions-runner-macos/`, ephemeral, re-registered per dispatch, labels `self-hosted,macOS,ARM64,f20-native-macos,…`. It IS this planning host. Naming it as an alternative was a contradiction and it stays removed.

What was checked in `.github/workflows/ci.yml`, and what each check found:
- The workflow fires on `push` to this working branch — the branch is named explicitly in the `push:` trigger list. The push this plan already performs so Hetzner can fetch is therefore ALSO what produces the binary; no second action starts it.
- Its `build:` job compiles `aarch64-apple-darwin` natively on a GitHub-hosted `macos-latest` runner and UPLOADS the result as the artifact `wayland-core-aarch64-apple-darwin`, with `if-no-files-found: error` so a renamed or missing binary fails the job loudly rather than publishing an empty artifact, and `retention-days: 14`.
- That job declares no `needs:`, so it runs independently and its artifact is retrievable even when other jobs in the same run go red. Measured on this branch, END TO END: run `30195324025` concluded `failure` overall, while all six `Build (<target>)` jobs concluded `success` and the run published a live `wayland-core-aarch64-apple-darwin` artifact of 31,425,283 bytes with `expired=false`. The acquisition path is therefore proven, not merely declared.
- **A QUEUED run IS cancelled by a later push, and this trips the obvious plan.** `concurrency.cancel-in-progress` is `false` for a branch push, which protects a run that has already STARTED — it does NOT protect one still PENDING in the concurrency group, because GitHub holds at most one pending run per group and supersedes it. Measured: run `30194813828` at `62f954da` sat `pending`, never created a single job, and was `cancelled` the moment the push at `84b2cdc2` arrived. So do NOT push again while waiting for the pinned run's build job; if the pinned run was cancelled while queued, re-run it or re-push and re-pin, and never assume the run you are waiting on is still alive without re-reading its status.
- `workflow_dispatch` is still unavailable and is not needed. `ci.yml`'s own comment records why: GitHub exposes that trigger only for workflows already present on the DEFAULT branch, and `ci.yml` on `main` has none. The push IS the trigger.
- **The binary DOES self-report the commit it was built from, and that is the strongest provenance available.** `crates/wcore-cli/build.rs` runs `git rev-parse HEAD` at compile time and embeds the result as `WAYLAND_SOURCE_SHA` (validated to exactly 40 lowercase hex, or the literal `unknown` when git was unavailable); `main.rs` exposes it as the `--build-info` flag, which prints `wayland-core <version> (source <sha>)` and exits. Measured on this host: the stale `target/debug/wayland-core` prints `source a8ed732216bb650a7980f4dcc06e52daef3cc793`, a commit that is NOT HEAD — so the flag distinguishes a stale build from a fresh one by itself. Note that the plain version flag does NOT: `#[command(name = "wayland-core", …, version)]` prints only the crate version, and two different SHAs print identically. It is `--build-info`, not `--version`, that carries provenance.
- Provenance is asserted in LAYERS, because each covers what the others cannot. The embedded SHA reads what the binary was actually compiled from. The run binding — an artifact belongs to the run that produced it, and a run records its head SHA — catches an artifact that came from somewhere other than the run it claims. Feature presence in `migrate --help` catches a binary that is off this plan's line of work entirely. Task 4's gate asserts all of them, and any one failing reddens it.

**The acquisition, concretely.** Push the work branch at this plan's SHA. List the workflow runs for the branch and select the one whose head SHA equals it. Wait for that run's `Build (aarch64-apple-darwin)` job to conclude `success` — not for the run as a whole. Download that run's `wayland-core-aarch64-apple-darwin` artifact into a directory OUTSIDE the repository. Confirm the file is what it claims to be, with `file` reporting a Mach-O `arm64` executable and `lipo -archs` reporting `arm64`, and restore the executable bit if the download did not preserve it. An artifact whose run's head SHA is not a commit carrying this plan's implementation is inadmissible and fails the gate.

**DO NOT build here, and DO NOT degrade the leg.** The 20A handoff records "This Mac CAN compile the workspace. The old 'never compiles on Mac' note is a workflow convention, not a fact," which contradicts this phase's standing environment rule. That contradiction is Sean's to resolve, not an executor's, and it no longer blocks anything — the artifact path does not need it resolved. If the artifact cannot be obtained for some other reason (the run is missing, the build job went red, the artifact expired), the answer is to push again and produce a new run; if that still fails, STOP and escalate under termination state 3 with F26-01 unclaimed. Building here, substituting the stale local binary, and recording the leg as not-run are all forbidden.

**THE MEASUREMENT TRAPS (all three measured; do not simplify these away).**
1. **Mac `grep` is rtk-proxied and SILENTLY DROPS LINES** — measured at 32 returned versus 674 for the same inverted match on the same file. Every gate invokes `/usr/bin/grep` explicitly and uses `-F` for literals.
2. **A pipe replaces the exit status you care about.** `ssh host 'cmd' | grep -v noise` yields grep's status, not ssh's, so a failed remote build reports success. Every remote gate in this plan redirects output to a file, captures the return code on its own line, and tests that code BEFORE reading the file. Filtering for READING is fine; filtering inside a GATE is forbidden.
3. **Reading an exit code from a block that also emits output.** A block that both tees output and yields `$LASTEXITCODE` returns an array, making a numeric comparison always truthy. Read the status on the line AFTER the pipeline.

**THE PANEL PROCEDURE — how a decision in this plan gets made, because no task in it halts for a human.**

A checkpoint is an instruction to DECIDE WELL, not an instruction to stop. Task 4 was a blocking human gate; it is now an autonomous decision leg, and this is the procedure it runs. This plan CREATES the harness the whole phase uses, so build it here even though only one decision in this plan consumes it.

1. **State the question and the options exactly as the plan frames them.** Do not re-scope, do not invent an option, do not quietly drop one.
2. **Write the evidence bundle ONCE** to `<panel-dir>/question.md`: the question, the option ids verbatim, and the evidence itself. It must instruct each panelist to end its answer with exactly two lines — `PANEL-VERDICT: <one option id>` and `PANEL-BASIS: <one sentence>` — because a verdict that has to be inferred from prose is a verdict the executor gets to choose.
3. **Put it to all four members, each given that SAME bundle.** Three are external and are invoked through `scripts/panel-ask.sh <question.md> <panel-dir>`, which reads the bundle once and hands identical bytes to each — that is what makes "same evidence" a property of the mechanism instead of a claim:
   - `codex exec -m gpt-5.6-sol --sandbox read-only --skip-git-repo-check "<question>"` → `codex.txt`
   - `gemini -p "<question>" -m gemini-3.1-pro-preview -o text` → `gemini.txt`. Its `GOOGLE_API_KEY` notice goes to STDERR and is NOT a failure; capture stdout only and do not let stderr chatter abort the wrapper.
   - `/Users/seandonahoe/.kimi-code/bin/kimi -p "<question>" --output-format text` → `kimi.txt`. The ABSOLUTE path is required — a non-interactive shell here predates the PATH entry, so the bare name is not found.
   The fourth member is your own adversarial pass, written to `internal.md` in the same two-line-terminated form, and argued AGAINST the position you expect to win rather than for it.
   Every capture is stored **VERBATIM**. Not a paraphrase, not a summary, not trimmed for length. `panel-ask.sh` fails loudly on a member that errors or returns an empty body rather than writing a stub, because three responses and a placeholder is a three-member panel wearing a fourth's name.
4. **Live-test the decision wherever it is testable, and let the measurement outrank the argument.** Each decision below names its live test. Where a measurement can distinguish the options, the measurement decides and the panel's job is to interpret it; where the measurement is BLIND to the distinction, that blindness is stated explicitly at the decision and the panel is load-bearing there.
5. **Commit to a position and record WHICH and WHY** in `<panel-dir>/DECISION.md`: `CHOSEN: <option id>`; `BASIS: majority` or `BASIS: minority-with-evidence`; `EVIDENCE: <path>` naming a real non-empty artifact, required whenever the chosen option is not the majority verdict; `RATIONALE:` of at least 120 characters; and a `## DISSENT` section naming every verdict that differs from the chosen one, with its argument. The gate derives the dissent set from the CAPTURED verdict lines, so dissent cannot be disposed of by omission — a decision with the losing arguments deleted is not auditable.
6. **Escalate to Sean ONLY** on a genuine deadlock — a split with no evidentiary tiebreak — or when the substance is reserved: merging to main, opening a PR, tagging, releasing, closing an issue, deleting a retained evidence ref, or supplying a real credential or account. An escalation carries the panel's split and the evidence, not just a question. **Nothing in this plan is on that reserved list**; the CI artifact is fetched with the `gh` credentials this host already holds, and both peer installs are already on this host.

`scripts/panel-decision-check.sh <panel-dir> <option-id>...` enforces every clause above mechanically and is what the gates call. It is deliberately NOT satisfied by prose the executor typed: its load-bearing assertions read the captured panel files.

**THE PANEL NEVER RECEIVES A REAL SECRET.** The bundle is a file, and three of the four members are remote services. Feeding a real-home output document to them would exfiltrate the exact thing this plan exists to protect — and would do it *precisely* in the case where redaction had failed, which is the case that matters. Every bundle in this phase is built from canary-corpus material and source code only. A gate asserts that no real secret value appears anywhere under `panel/`, after first proving it had real values to search for.

**AGENTS.md discipline.** Surgical diffs only; every changed line traces to this plan. No drive-by refactor of the existing Hermes mapper beyond the root-profile gap. `cargo clippy` clean, `cargo fmt` clean. Errors use `thiserror` for the public portability types and `anyhow` for CLI-level propagation; no `unwrap()` in production paths. Keep every new file under 1000 lines.

**Git hygiene.** Use `/usr/bin/git` on the Mac. Stage the exact paths in `files_modified`, never `-A`, never `.`. Never stage `AGENTS.md`, `.ijfw` churn, or anything under `.planning/` that this plan does not own. No `Co-Authored-By` trailers.
</execution_rules>

<tasks>

<task type="auto">
  <name>Task 1: Measure the shipped binary against both REAL peer installs, ground the source formats in the pinned peer sources, and generate the canary corpus</name>
  <files>.planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md, scripts/portability-corpus-gen.py, crates/wcore-cli/tests/fixtures/portability/README.md</files>
  <read_first>crates/wcore-cli/src/migrate/hermes.rs (the whole file — `detect_home`'s directory guard, `build_plan`'s walk and sort, the Hermes YAML schema structs, `parse_dotenv`, `pick_provider_key`, and the deferred-inventory counters), crates/wcore-cli/src/migrate/mod.rs (`MigrateCmd`, `HermesArgs`, `MigrationPlan`, `ProfilePlan`, `Deferred`, `render_plan` and what it chooses to print versus withhold), crates/wcore-cli/tests/migrate_hermes.rs (the existing fixture idiom and env-guard pattern, so nothing is duplicated or displaced), .planning/intel/COMPETITIVE-LEDGER.md (the `PORT-*` row and the pinned baseline table)</read_first>
  <behavior>
    - The producer of Task 4's mandatory `arm64` macOS binary is confirmed from the workflow file itself and from a real run's per-job conclusion, before anything is measured or built, so a lost producer surfaces as an escalation at the start rather than as a stuck gate at the end.
    - The pre-change behavior of the REAL shipped binary is recorded against Sean's REAL `~/.hermes` and REAL `~/.openclaw`, with the exact command, the exit status, and the observed counts — not a description of what the code appears to do.
    - Determinism is measured rather than assumed, by running the same invocation twice and comparing the bytes.
    - Non-mutation is measured rather than assumed, by digesting the source tree before and after.
    - Each peer's on-disk source format is grounded in THREE readings that are reconciled against one another: the pinned baseline commit, the checkout HEAD, and the real install on this Mac. Drift between them is recorded as a compatibility range.
    - The generator reproduces a real tree's STRUCTURE — relative paths, file names, directory nesting, file formats, and the identity of every key — while substituting a distinct, recorded canary value for every value it classifies as secret.
    - The canary values live in the corpus as data, never as literals in this plan or in any source file, so that a gate can read them at run time.
    - The generator is deterministic: run twice over the same source it produces byte-identical output.
    - No real secret value is written into the corpus, into the baseline document, or into any file that will be committed.
  </behavior>
  <action>**Confirm the macOS binary's producer FIRST, before any measurement or implementation.** Task 4 is a MANDATORY decision leg that needs a fresh `arm64` macOS binary built from this plan's own SHA, and the plan must know at its START that its producer is intact rather than discover it at the gate. The execution rules name that producer — CI's `build` job and its `wayland-core-aarch64-apple-darwin` artifact — but do NOT take their word for it: `.github/workflows/ci.yml` is shared, other phases are running in this same fan-out, and an assumption about a file someone else can edit is exactly what this phase forbids.

Read `ci.yml` and confirm three things, recording the exact command and the literal result of each: that this working branch is named in the `push:` trigger list, so a push fires the workflow; that the `build:` job matrix includes the `aarch64-apple-darwin` target; and that the job's upload step publishes an artifact named from the target with `if-no-files-found: error`. Then confirm the producer WORKS on this branch rather than merely being declared — ask the workflow history for a recent run on this branch and read the conclusion of its `Build (aarch64-apple-darwin)` job, which is a per-job conclusion and is independent of the run's overall conclusion. Record the marker line `F26-SC1-CI-ARTIFACT: PRESENT — <the artifact name, the trigger line, and the run id and conclusion of the most recent Build (aarch64-apple-darwin) job you observed>` in the baseline.

If any of those is missing, the mandatory leg has lost its producer. STOP and escalate under termination state 3, with F26-01 unclaimed. Do NOT build on this Mac, do NOT substitute the stale binary, and do NOT invent a way to close F26-01 without the leg: it is the only evidence that structural redaction holds against live credentials at real scale, and a plan that ends without it has not proven the criterion it exists to prove. Then continue with the rest of this task exactly as written; everything below is unaffected by this confirmation.

Now measure, and measure the real product. Run the existing binary at `target/debug/wayland-core` against Sean's real Hermes home with the dry-run flag and WITHOUT the credentials flag, and record the exact command, the exit status and the full output. Capture that output to a file as well as into the baseline, because a later gate re-derives it and requires the baseline to contain it verbatim — a baseline that paraphrases the run will go red. This invocation is safe to run and is already known to be non-mutating and value-free: the dry-run path returns before the apply step, and the planner only records a credential's variable NAME unless credentials are explicitly requested. Do NOT pass the credentials flag against the real home at any point in this plan.

Record the observed counts as the baseline this plan must preserve: the number of profiles, the MCP servers, the skill directories and persona files reported as detected-but-not-imported. Then run the identical invocation a second time and compare the two outputs byte for byte, recording whether determinism holds today. Then take a content digest of the entire real Hermes home before and after a dry-run and record whether they match, which establishes non-mutation as a measured fact rather than a claim about the code.

Establish the four gaps by interrogating the binary, not the source: ask the migrate command for its help and record which sources it offers; ask the Hermes subcommand for its help and record which flags exist; ask the top-level binary for its help and record whether any backup, restore or export command is present. Then read the Hermes home-detection guard and record precisely what it requires to be present before it will proceed, and confirm against the real install whether a root-level configuration exists that the guard causes to be ignored.

Now ground both source formats. For each peer read the format from the PINNED baseline commit named in the competitive ledger using git's object reader rather than checking the tree out, read the same paths at the checkout HEAD, and compare both against the real install on this Mac. For Hermes that means the profile-level configuration and environment files, the persona file, the skills and memories layout, and the root-level configuration the current importer ignores. For OpenClaw that means its primary configuration document and the legacy names it still accepts, the credentials directory, the per-agent tree, the memory and workspace roots, and the backup and last-known-good variants of the configuration — all of which are present in the real install. Do not assume OpenClaw's platform-specific home resolution: DETERMINE it from the peer's own source and record where you found it, because inventing that path would produce an importer that silently finds nothing on one platform.

Reconcile the three readings for each peer and record any drift between the pinned baseline and HEAD as an explicit compatibility range: which fields exist in both, which appeared after the baseline, and which the importer will therefore treat as optional. Where the pinned and HEAD formats disagree in a way that changes how a field must be parsed, that is a finding — record it with a severity.

Then write the generator. It walks a real peer state tree and emits a corpus that preserves the tree's shape exactly — every relative path, file name and file format — while replacing each value it classifies as secret with a distinct canary token, and recording the mapping from corpus location to canary token in a manifest inside the corpus. Classification must be explicit and written down, not implicit: name the rule by which a value is treated as secret. The canary tokens are generated into the corpus as data and are read from the manifest by later gates; never hard-code one into a source file or into this plan. Run the generator over Sean's real Hermes home and real OpenClaw home, commit the resulting corpus, and then prove two things about it: that running the generator twice over the same input yields byte-identical output, and that no real secret value from either source appears anywhere in the committed corpus. Prove the second by extracting the real values on the Mac into a scratch file outside the repository, searching the corpus for each, requiring zero matches, and deleting the scratch file before the task ends.

Write every observation, with its exact command and output, into `26-01-BASELINE.md`. Records evidence for F26-01; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; B=.planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md; test -f "$B" || { echo "GATE: baseline missing"; exit 1; }; /usr/bin/grep -qE 'F26-SC1-CI-ARTIFACT: PRESENT — .{20,}' "$B" || { echo "GATE: the macOS binary producer was not confirmed and recorded as PRESENT - Task 4 is mandatory and this is its only admissible source"; exit 1; }; CI=.github/workflows/ci.yml; /usr/bin/grep -qF 'name: wayland-core-${{ matrix.target }}' "$CI" || { echo "GATE: ci.yml no longer uploads the release binary artifact - Task 4 has lost its producer; STOP and escalate"; exit 1; }; /usr/bin/grep -qF 'target: aarch64-apple-darwin' "$CI" || { echo "GATE: ci.yml build matrix no longer includes aarch64-apple-darwin - Task 4 has lost its producer; STOP and escalate"; exit 1; }; /usr/bin/grep -qF -- "- $(/usr/bin/git rev-parse --abbrev-ref HEAD)" "$CI" || { echo "GATE: this working branch is not in ci.yml's push trigger list, so no run - and no artifact - is produced for it; STOP and escalate"; exit 1; }; ./target/debug/wayland-core migrate hermes --home "$HOME/.hermes" --dry-run &gt; /tmp/26-01-prechange.txt 2&gt;&amp;1; rc=$?; test $rc -eq 0 || { echo "GATE: pre-change invocation exited $rc"; exit 1; }; L=$(/usr/bin/grep -c . /tmp/26-01-prechange.txt); test "$L" -ge 40 || { echo "GATE INVALID: pre-change capture only $L non-empty lines - nothing substantive to compare"; exit 1; }; N=0; MISS=0; while IFS= read -r l; do N=$((N+1)); [ -z "$l" ] &amp;&amp; continue; /usr/bin/grep -qF -- "$l" "$B" || { MISS=$((MISS+1)); echo "BASELINE IS MISSING CAPTURED OUTPUT LINE $N"; }; done &lt; /tmp/26-01-prechange.txt; echo "captured=$L missing=$MISS"; test "$MISS" -eq 0</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f scripts/portability-corpus-gen.py &amp;&amp; python3 -c "import ast,sys; ast.parse(open('scripts/portability-corpus-gen.py').read())"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -d crates/wcore-cli/tests/fixtures/portability &amp;&amp; test "$(/usr/bin/find crates/wcore-cli/tests/fixtures/portability -type f | /usr/bin/wc -l | tr -d ' ')" -ge "8"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; OUT=$(mktemp -d) &amp;&amp; python3 scripts/portability-corpus-gen.py --source "$HOME/.hermes" --kind hermes --out "$OUT/a" &gt;/dev/null 2&gt;&amp;1 &amp;&amp; python3 scripts/portability-corpus-gen.py --source "$HOME/.hermes" --kind hermes --out "$OUT/b" &gt;/dev/null 2&gt;&amp;1 &amp;&amp; /usr/bin/diff -r "$OUT/a" "$OUT/b" &amp;&amp; rm -rf "$OUT"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; S=$(mktemp); /usr/bin/grep -hoE '[A-Za-z0-9_]*_(API_KEY|TOKEN|SECRET|KEY)=.*' "$HOME"/.hermes/profiles/*/.env 2&gt;/dev/null | /usr/bin/sed 's/^[^=]*=//; s/^["'"'"']//; s/["'"'"']$//' | /usr/bin/grep -vE '^[[:space:]]*$' &gt;&gt; "$S"; /usr/bin/find "$HOME/.openclaw" -maxdepth 2 -type f \( -name '*.json' -o -path '*/credentials/*' \) -print0 2&gt;/dev/null | xargs -0 /usr/bin/grep -hoiE '"[^"]*(key|token|secret|auth|pass|cred)[^"]*"[[:space:]]*:[[:space:]]*"[^"]{16,}"' 2&gt;/dev/null | /usr/bin/sed 's/.*:[[:space:]]*"//; s/"$//' &gt;&gt; "$S"; /usr/bin/sort -u "$S" -o "$S"; T=$(/usr/bin/grep -c . "$S"); test "$T" -ge 4 || { echo "GATE INVALID: only $T real secret values extracted across BOTH peer homes - measured 7 at plan time, so the search is not proving what it claims"; rm -f "$S"; exit 1; }; PH=.planning/phases/26-migration-export-backup-restore; CORPUS=crates/wcore-cli/tests/fixtures/portability; NF=$(/usr/bin/find "$CORPUS" -type f 2&gt;/dev/null | /usr/bin/wc -l | tr -d ' '); test "$NF" -ge 8 || { echo "GATE INVALID: the corpus holds $NF files - searching it for secrets would pass vacuously because there is nothing to search"; rm -f "$S"; exit 1; }; BL=$(/usr/bin/find "$PH" -maxdepth 1 -type f -name '*BASELINE*' 2&gt;/dev/null | /usr/bin/head -1); test -n "$BL" &amp;&amp; test -s "$BL" || { echo "GATE INVALID: no non-empty baseline artifact was written into the phase directory - it is one of the surfaces this search must cover"; rm -f "$S"; exit 1; }; test -s /tmp/26-01-prechange.txt || { echo "GATE INVALID: the captured pre-change stdout is absent - run the baseline gate first"; rm -f "$S"; exit 1; }; NW=$(/usr/bin/find "$PH" -maxdepth 1 -type f 2&gt;/dev/null | /usr/bin/wc -l | tr -d ' '); echo "corpus_files=$NF phase_artifacts=$NW"; HITS=0; while IFS= read -r v; do [ ${#v} -lt 8 ] &amp;&amp; continue; if /usr/bin/grep -RqF -- "$v" "$CORPUS" "$PH" scripts/portability-corpus-gen.py /tmp/26-01-prechange.txt 2&gt;/dev/null; then HITS=$((HITS+1)); fi; done &lt; "$S"; rm -f "$S"; echo "searched=$T hits=$HITS"; test "$HITS" -eq 0</automated>
  </verify>
  <done>The shipped binary's pre-change behavior against BOTH real peer installs is recorded with exact commands, exit statuses and counts. Determinism and non-mutation are measured facts with the comparison recorded, not assertions about the code. All four gaps are established by interrogating the binary's own help output and the home-detection guard. Both peer source formats are grounded in three reconciled readings — pinned baseline, HEAD, and real install — with drift recorded as an explicit compatibility range and OpenClaw's platform home resolution determined from peer source rather than assumed. The generator is committed, is proven deterministic by a second run, and its corpus is proven to contain zero real secret values by a search that first proves it had real values to search for. No real secret was written to any committed file and the scratch extraction was deleted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the Hermes dry-run typed, structurally redacted and machine-readable, and close the root-profile discovery gap</name>
  <files>crates/wcore-config/src/lib.rs, crates/wcore-config/src/portability/mod.rs, crates/wcore-config/src/portability/redact.rs, crates/wcore-config/src/portability/digest.rs, crates/wcore-cli/src/migrate/mod.rs, crates/wcore-cli/src/migrate/hermes.rs, crates/wcore-cli/tests/migrate_typed_dryrun.rs</files>
  <read_first>.planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md (the grounded source-format contract and the measured baseline this task must preserve), crates/wcore-config/src/profile.rs (the existing secret-classification precedent — the secret directory names and the credentials file prefix, and `copy_tree_filtered`'s symlink discipline, because the portability module must classify secrets the SAME way rather than inventing a second definition), crates/wcore-config/src/lib.rs (module declaration order and the crate's public surface conventions), crates/wcore-cli/src/migrate/mod.rs (`render_plan`'s exact prose, which is the measured baseline output and must not change)</read_first>
  <behavior>
    - Test 1: a plan built from the committed Hermes corpus serializes to JSON that parses, and contains the profile count the corpus manifest declares — so an empty or malformed emission fails.
    - Test 2: no canary value from the corpus manifest appears anywhere in the serialized JSON, for a corpus that is proven to contain those canaries at their declared locations.
    - Test 3: serializing the same plan twice yields byte-identical JSON, and JSON built from two independent walks of the same corpus is byte-identical — determinism at the type level, not just at the printer.
    - Test 4: a digest of the corpus tree taken before and after a full discovery plus dry-run is identical — non-mutation.
    - Test 5: a Hermes home with a ROOT-level configuration and no profiles directory produces a plan containing that root setup, where today it errors out.
    - Test 6: a Hermes home with BOTH a root configuration and a profiles directory produces a plan containing the root setup AND every profile, with a stable, documented name for the root entry that cannot silently collide with a real profile name.
    - Test 7: the existing prose rendering is unchanged for the existing fixture — the measured baseline output is preserved byte for byte.
    - Test 8: a source tree containing a symlink that points outside the declared source root is not followed, and the escape is reported as a warning on the plan rather than silently skipped.
    - The emitted plan type has no field capable of carrying a secret value; a credential is represented by its source reference only.
  </behavior>
  <action>Write the tests first, from the behaviors above, and watch them fail before writing the implementation.

Create the portability module in `wcore-config` and declare it from the crate root. It owns three things and nothing else in this plan. First, the typed plan vocabulary that both peer sources produce and that the CLI serializes: a source descriptor naming the peer and the resolved home, an ordered collection of discovered items each carrying its kind, its stable identifier, its source path relative to the home, and its mapped target, plus the detected-but-not-imported inventory and the warnings. Order must be total and derived from the data — sort every collection by a key that exists in the source — so that two walks of the same tree cannot differ. Second, the redaction boundary: a type that represents a discovered credential as its source reference — the variable or key name and the file it came from — with NO variant capable of holding the value. Make this a property of the type rather than of the printer, so that serialization, logging, and error formatting all inherit it without each having to remember. Reuse the secret classification that the profile module already established rather than writing a second, divergent definition of what counts as a secret; if that classification needs to be shared, lift it so both call sites read the same source of truth. Third, the tree digest: a stable content digest over a directory tree, ordering entries deterministically, recording relative paths and content hashes, and refusing to traverse a symlink that resolves outside the root it was given.

Then surface the typed plan on the CLI. Add a machine-readable output flag to the migrate arguments that emits the typed plan as JSON to standard output and suppresses the prose rendering. The prose rendering itself must NOT change: the baseline measured its exact output and Task 1 recorded it, so it stays byte-identical for the existing fixture and any change to it is a regression. Make the flag compose with the existing dry-run flag, and make the machine-readable form of an apply that has not been confirmed emit the plan rather than mutating anything.

Then close the root-profile gap that Task 1 established. The home guard currently refuses any Hermes home without a profiles directory, which makes a root-only install unimportable and makes a rooted install's own top-level setup invisible. Change the guard so a home is acceptable when EITHER a profiles directory exists OR a root-level configuration exists, and extend the walk so the root setup becomes an item in the plan alongside the per-profile items. Give the root entry a stable name and make a collision with a real profile of that name impossible to resolve silently — either the name is one a profile directory cannot have, or a collision is reported as a conflict. Record which choice you made and why, in source, where a maintainer will see it. Do not change how an existing profile is mapped; that mapping is measured-good and is not this plan's subject.

Prove it on Linux at the plan's SHA: build the binary and run the crate suites. State the counts as a delta against what Task 1 measured, and name every residual failure. Records evidence for F26-01; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -s crates/wcore-config/src/portability/redact.rs &amp;&amp; test -s crates/wcore-config/src/portability/digest.rs &amp;&amp; test "$(/usr/bin/grep -cE '^[[:space:]]*(pub[[:space:]]+)?mod[[:space:]]+portability[[:space:]]*;' crates/wcore-config/src/lib.rs)" -ge "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; for f in crates/wcore-config/src/portability/mod.rs crates/wcore-config/src/portability/redact.rs crates/wcore-config/src/portability/digest.rs crates/wcore-cli/src/migrate/hermes.rs crates/wcore-cli/src/migrate/mod.rs; do n=$(/usr/bin/wc -l &lt; "$f" | tr -d ' '); test "$n" -lt 1000 || { echo "OVER 1000 LINES: $f ($n)"; exit 1; }; done</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-01; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2&gt;/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; cargo build --locked -p wcore-cli --bin wayland-core; cargo clippy --locked -p wcore-config -p wcore-cli --all-targets -- -D warnings; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA" &gt; /tmp/26-01-t2-build.log 2&gt;&amp;1; rc=$?; tail -20 /tmp/26-01-t2-build.log; test $rc -eq 0</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-01; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2&gt;/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; cargo nextest run --locked -p wcore-config -p wcore-cli --no-fail-fast; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA" &gt; /tmp/26-01-t2-test.log 2&gt;&amp;1; rc=$?; tail -30 /tmp/26-01-t2-test.log; test $rc -eq 0</automated>
  </verify>
  <done>All eight behaviors exist as tests that were written before the implementation and observed to fail first. The portability module owns the typed vocabulary, the structural redaction boundary and the tree digest, and reuses the existing secret classification rather than duplicating it. The machine-readable flag emits deterministic JSON in which a secret value is unrepresentable by TYPE, and the prose rendering is byte-identical to the measured baseline. The root-profile gap is closed for both a root-only and a rooted-plus-profiles install, with the collision decision recorded in source. Every touched file is under 1000 lines. Linux build, clippy at deny-warnings, and the two crate suites are green at this SHA with counts stated as a delta against Task 1's baseline and every residual failure named.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the reciprocal OpenClaw discovery source and prove all four properties live against real-world state</name>
  <files>crates/wcore-cli/src/migrate/openclaw.rs, crates/wcore-cli/src/migrate/mod.rs, crates/wcore-cli/tests/migrate_typed_dryrun.rs</files>
  <read_first>.planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md (the grounded OpenClaw format contract, its platform home resolution, and the pinned-versus-HEAD compatibility range), crates/wcore-cli/src/migrate/hermes.rs (the mapper idiom the OpenClaw source must match — permissive deserialization that ignores unknown keys, deterministic sort, warnings rather than hard failures on individually bad entries), crates/wcore-config/src/portability/mod.rs (the typed vocabulary this source must produce), crates/wcore-config/src/config.rs (the MCP server config and profile config shapes an OpenClaw entry maps onto)</read_first>
  <behavior>
    - Test 1: an OpenClaw home containing the primary configuration document yields a plan naming the mapped model and provider settings, with the count the corpus manifest declares.
    - Test 2: each legacy configuration name that the peer still accepts is discovered when the primary name is absent, and the primary wins when several are present, with the choice reported.
    - Test 3: backup and last-known-good variants of the configuration that sit beside the primary are NOT treated as additional sources — a real install has several of them and importing them would multiply every item.
    - Test 4: credentials discovered in the credentials directory and in per-agent auth material are represented by source reference only; no canary value from the corpus manifest appears in the serialized plan.
    - Test 5: discovery is deterministic — two independent walks of the same OpenClaw corpus serialize byte-identically.
    - Test 6: a digest of the OpenClaw corpus tree before and after discovery plus dry-run is identical.
    - Test 7: a malformed primary configuration produces a warning-bearing plan or a named error, never a panic and never a silently empty plan that reads as success.
    - Test 8: items whose kind this plan does not import are reported in the detected-but-not-imported inventory rather than dropped, so nothing discovered disappears without being named.
  </behavior>
  <action>Write the tests first against the committed OpenClaw corpus, and watch them fail.

Build the OpenClaw discovery source as a sibling of the Hermes one, matching its idiom exactly: pure reconnaissance that never writes, permissive deserialization that ignores keys it does not know, a deterministic total order, and per-entry warnings rather than a hard failure that discards the whole plan. Resolve the OpenClaw home from an explicit override first and otherwise from the platform resolution Task 1 DETERMINED from peer source — do not re-derive it here and do not assume it. Read the primary configuration document, and when it is absent fall back through the legacy names the peer still honours, reporting which one was used. Treat the backup and last-known-good siblings of the configuration as what they are — prior revisions of the same document, not additional sources — because the real install carries several and counting them would multiply every discovered item.

Map what maps and name what does not. Model and provider settings, and MCP server definitions, map onto the existing configuration shapes exactly as the Hermes source maps its equivalents. Credentials are discovered and represented by source reference only, through the redaction type, so no value can reach the emitted plan. Everything else the real install carries — the per-agent tree, memory, workspace, plugins and plugin skills, flows, tasks, identity, logs and the interface state — goes into the detected-but-not-imported inventory with its count, because a discovered item that is neither imported nor named has been silently dropped, and that is the failure mode this criterion exists to prevent. Register the new source on the migrate command so it appears in help output alongside the existing one.

Then prove all four properties live, on real-world state, with the real binary.

On Linux, build the binary at this SHA and run it against BOTH committed corpora with the machine-readable flag and the dry-run flag. For each: assert the process exited zero; assert the emitted document is well-formed JSON whose item count equals the count the corpus manifest declares — this is the positive assertion that stops an empty output from passing; assert that every canary token listed in the corpus manifest is absent from the emitted document, reading the tokens from the manifest rather than from any literal; assert that a second run of the same invocation emits byte-identical output; and assert that a digest of the corpus tree is unchanged across the run. Capture the return code of each remote invocation on its own line and test it BEFORE reading any output, and never place a filter between the command and its status.

On the Mac, with the real trees, the same run must be made against the REAL `~/.hermes` and REAL `~/.openclaw` — which is where the only real-world-scale input exists, with twelve real profiles, hundreds of real skill directories and real credentials. That leg needs an arm64 binary this host cannot build, so it is executed by Task 4 rather than asserted here. What this task does provide for it is the check itself: a committed script that takes a binary path, runs both discoveries against the real homes with the machine-readable and dry-run flags, extracts the real secret values from the real sources into a scratch file outside the repository, requires that the extraction found at least one value before it will report anything, searches each emitted document for each real value, requires zero matches, digests both real homes before and after to prove non-mutation, deletes the scratch file, and exits non-zero on any failure. Write it so its own failure modes are loud: an empty extraction, an unparseable document, or a zero item count each fail it.

State the Linux suite counts as a delta against Task 2's. Records evidence for F26-01; marks no requirement complete — closure is claimed by the decision leg in Task 4.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -s crates/wcore-cli/src/migrate/openclaw.rs &amp;&amp; test "$(/usr/bin/grep -cE '^[[:space:]]*(pub[[:space:]]+)?mod[[:space:]]+openclaw[[:space:]]*;' crates/wcore-cli/src/migrate/mod.rs)" -ge "1"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-01; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2&gt;/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; cargo build --locked --release -p wcore-cli --bin wayland-core; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; ./target/release/wayland-core migrate --help &gt; /tmp/26-01-t3-help.txt 2&gt;&amp;1; cat /tmp/26-01-t3-help.txt" &gt; /tmp/26-01-t3-build.log 2&gt;&amp;1; rc=$?; tail -20 /tmp/26-01-t3-build.log; test $rc -eq 0 || exit 1; scp -o BatchMode=yes hetzner-dsm:/tmp/26-01-t3-help.txt /tmp/26-01-t3-help.txt &gt;/dev/null 2&gt;&amp;1 || exit 1; cat /tmp/26-01-t3-help.txt; test "$(/usr/bin/grep -c . /tmp/26-01-t3-help.txt)" -ge 3 &amp;&amp; /usr/bin/grep -qF 'openclaw' /tmp/26-01-t3-help.txt &amp;&amp; /usr/bin/grep -qF 'hermes' /tmp/26-01-t3-help.txt</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-01; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2&gt;/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; cargo nextest run --locked -p wcore-config -p wcore-cli --no-fail-fast; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA" &gt; /tmp/26-01-t3-test.log 2&gt;&amp;1; rc=$?; tail -30 /tmp/26-01-t3-test.log; test $rc -eq 0</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f scripts/portability-real-state-check.sh &amp;&amp; sh -n scripts/portability-real-state-check.sh &amp;&amp; test "$(/usr/bin/grep -vc '^[[:space:]]*#' scripts/portability-real-state-check.sh)" -ge 25 &amp;&amp; { sh scripts/portability-real-state-check.sh /nonexistent/wayland-core-does-not-exist &gt; /tmp/26-01-selfcheck.log 2&gt;&amp;1; rc=$?; cat /tmp/26-01-selfcheck.log; test $rc -ne 0 || { echo "GATE: the real-state check reported SUCCESS when handed a binary that does not exist - it cannot go red and proves nothing at the decision leg"; exit 1; }; }</automated>
  </verify>
  <done>The OpenClaw source exists, is registered on the migrate command and appears in the real binary's help output alongside Hermes, proven by running the built binary on Linux rather than by reading the source. All eight behaviors exist as tests written before the implementation. Legacy configuration names are honoured and backup and last-known-good siblings are excluded, so a real install's several revisions do not multiply the plan. Every discovered item is either mapped or named in the detected-but-not-imported inventory; nothing is dropped unnamed. On Linux both corpora produce deterministic, well-formed, canary-free JSON with the manifest-declared item count, over an unchanged source-tree digest, with every remote status captured on its own line and tested before any output was read. The real-state check script is committed and fails loudly on an empty extraction, an unparseable document or a zero item count.</done>
</task>

<task type="auto">
  <name>Task 4: Run the real discovery against Sean's REAL Hermes and OpenClaw installs, and settle structural-versus-cosmetic redaction by measurement and panel rather than by eye</name>
  <files>.planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md, scripts/panel-ask.sh, scripts/panel-decision-check.sh, crates/wcore-cli/tests/migrate_typed_dryrun.rs, .planning/phases/26-migration-export-backup-restore/panel/26-01-redaction-contract/</files>
  <read_first>crates/wcore-config/src/portability/redact.rs (the redaction type as built, because the question the panel is asked is whether a secret value is unrepresentable in it or merely unprinted by it), crates/wcore-cli/tests/migrate_typed_dryrun.rs (the existing canary assertions, so the multi-emitter probe extends them rather than duplicating them), scripts/portability-real-state-check.sh (the real-state check Task 3 committed, which is the leg this task runs)</read_first>
  <behavior>
    - The mandatory macOS real-state leg is RUN by this task against Sean's REAL `~/.hermes` and REAL `~/.openclaw`, with a fresh `arm64` binary acquired from CI, and its outcome recorded as a marker the gate re-derives from GitHub.
    - The by-eye credential-shape check is replaced by two things that can actually fail: a live multi-emitter probe that distinguishes structural redaction from cosmetic redaction, and a four-member panel that interprets it.
    - The panel harness — `panel-ask.sh` and `panel-decision-check.sh` — exists, is used here, and is proven able to REJECT a malformed panel by its own self-test, because a checker that cannot go red would make every later decision in this phase decorative.
    - No real secret value appears anywhere under `panel/`, proven by a search that first proves it had real values to search for.
  </behavior>
  <action>**This task performs the leg. It no longer waits for anyone.** The evidence it needs is on this host or obtainable from CI with the credentials this host already holds, so there is nothing here to ask about — only work to do and a judgment to make well.

**Part A — build the phase's panel harness.** This plan is wave 1 and every later plan's decision leg calls these two scripts, so build them here and prove they work.

`scripts/panel-ask.sh <question-file> <out-dir>` reads the question file ONCE into memory and hands those identical bytes to each of the three external members named in the panel procedure, writing `codex.txt`, `gemini.txt` and `kimi.txt` into the output directory. It captures stdout only — Gemini's `GOOGLE_API_KEY` notice goes to stderr and is not a failure — and it stamps each capture with a header line recording the member and the SHA-256 of the question file, so "every member saw the same bundle" becomes a checkable fact rather than an assurance. It exits non-zero, loudly, if any member errors or returns an empty body; writing a stub for a member that did not answer would give you a three-member panel wearing a fourth's name.

`scripts/panel-decision-check.sh <panel-dir> <option-id>...` is the enforcement, and it is the reason a converted decision still has a gate that can go red. Against a real panel directory it requires: `question.md` present and substantive; all four captures present, non-trivial, and each carrying exactly ONE `PANEL-VERDICT:` line naming an id from the option set it was handed on the command line; the same question digest stamped in all three external captures, matching the actual digest of `question.md`; the four captures pairwise distinct, so one answer copied into several files is caught; a `DECISION.md` carrying exactly one `CHOSEN:` from that same option set, a `BASIS:` of `majority` or `minority-with-evidence`, an `EVIDENCE:` naming a real non-empty artifact whenever the choice is not the majority verdict, a `RATIONALE:` of at least 120 characters, and a `## DISSENT` section that names every verdict differing from the chosen one — where the set of differing verdicts is computed from the CAPTURED files, not read from the prose, so dissent cannot be disposed of by leaving it out. When all four verdicts agree it requires the dissent section to say so explicitly rather than be empty.

Give it a `--self-test` flag that builds throwaway panel directories in a temporary location and asserts it REJECTS each of: a missing directory; a missing capture; a capture with no verdict line; a verdict naming an id outside the supplied option set; two byte-identical external captures; a mismatched question digest; a `CHOSEN` outside the option set; a minority choice whose `EVIDENCE` names nothing that exists; and a dissenting captured verdict absent from the dissent section — and that it ACCEPTS one well-formed directory. Negative controls alone would pass a checker that rejects everything, so the accepting case is mandatory. The self-test cleans up after itself and exits non-zero if any expectation is violated.

**Part B — acquire the binary. It needs a fresh `arm64` macOS binary built from this plan's own SHA, and CI's `build` job is the one admissible producer.** Do not build here. Do not substitute the stale `target/debug/wayland-core` already on this box: it predates every change in this plan and would certify the old product. And do NOT reach for the Phase 20A macOS runner as though it were a different machine — it is not. The 20A handoff records it under "macOS runner (this Mac)": `~/actions-runner-macos/`, ephemeral, re-registered per dispatch. Using it IS building here.

1. The branch is already pushed at the SHA Task 3's Linux gates asserted — Hetzner fetched it from there — and that push already fired the workflow. The push IS the trigger; `workflow_dispatch` is unavailable here and is not needed.
2. List the workflow runs for this branch and select the run whose head SHA equals that SHA.
3. Wait for that run's `Build (aarch64-apple-darwin)` job to conclude `success`. Do NOT wait on the run as a whole: the `build` job declares no `needs:`, and its artifact is retrievable even when other jobs in the same run are red — measured on run `30195324025`, which concluded `failure` while publishing a live 31 MB `wayland-core-aarch64-apple-darwin`. While waiting, do NOT push anything else to this branch: `cancel-in-progress: false` protects a run that has STARTED, but a run still PENDING in the concurrency group is superseded and cancelled by the next push, which is how run `30194813828` died with zero jobs ever created. Re-read the run's status rather than assuming it is still alive; if it was cancelled while queued, re-run it or re-push and re-pin to the new SHA.
4. Download that run's `wayland-core-aarch64-apple-darwin` artifact into a directory OUTSIDE the repository. Note the run id and the run's head SHA — that pair, not the file, is what carries provenance.
5. Prove the file is what it claims, because a listing entry is not a file: it must be present and non-empty on disk, `file` must report a Mach-O `arm64` executable, `lipo -archs` must report `arm64`, `--build-info` must report the recorded SHA, and `<path> migrate --help` must run and list BOTH `hermes` and `openclaw`. Restore the executable bit if the download did not preserve it. **Leave the binary in place, at the absolute path you record, until the gate has passed** — the gate stats and runs it.
6. If the artifact has expired — retention is 14 days, and an expired artifact still shows up in the run's artifact listing while being undownloadable — re-run that workflow run so a fresh artifact is produced at the SAME head SHA, rather than reaching for any other binary. Re-pushing an unchanged branch fires nothing, so a re-run, not a push, is the mechanism.

**Part C — run the real-state leg.**

7. Run `scripts/portability-real-state-check.sh <path-to-binary>`. It runs both discoveries against the REAL `~/.hermes` and REAL `~/.openclaw` with the machine-readable and dry-run flags, extracts the real secret values from the real sources, and searches both emitted documents for each one.
8. Read what it prints. Expect: both invocations exit zero; the Hermes document reports **12 profiles** and **1 MCP server**, matching the pre-change baseline Task 1 measured, so the change did not lose anything; the OpenClaw document reports a non-zero item count; the extraction found at least one real secret value to search for; zero of those values appear in either document; and the digests of both real homes are unchanged before and after.
9. If ANY real secret value appears in either document, that is a CRITICAL finding. Stop, do not proceed to 26-02, and report it. That outcome is `contract-leaks` below and it is not a decision to be deliberated — the measurement settles it on its own.

The script deletes its own scratch extraction. Confirm nothing containing a real secret value was left behind, in the repository or elsewhere, and that nothing from either real home was copied to `hetzner-dsm` or `seandesktop` at any point.

Record the outcome as the marker line `F26-SC1-MACOS: RAN — run=<run id> sha=<the run's 40-character head SHA> binary=<path> arch=arm64 hermes_profiles=12 openclaw_items=<n> secret_hits=0` in the baseline, beside Task 1's producer record. The gate re-derives that run from GitHub and rejects a marker whose run did not publish that artifact at that SHA, and rejects a SHA whose tree does not contain this plan's implementation — so the line has to be the truth rather than a transcription.

**If the leg cannot be run, that is a STOP, not an outcome.** Escalate under termination state 3 with the exact failure — the run is missing, the build job went red, the artifact expired and a re-run did not restore it, or `gh` cannot reach the repository — and leave F26-01 UNCLAIMED. Recording the leg as not-run, deferring it to 26-04, and closing F26-01 on the Linux evidence alone are all forbidden.

**Part D — the REDACTION-CONTRACT DECISION, settled by measurement first and panel second.**

The question this task used to ask a human to answer by eye: *does the emitted typed plan represent every discovered credential by its source reference only, such that no downstream printer, log line, error message or serializer can emit a value?* The options are the plan's own, drawn from its sixth truth and from step 9 above, and they are not to be re-scoped:

- `contract-holds` — a secret value is unrepresentable in the emitted type; representation is by source reference alone.
- `contract-cosmetic` — values are withheld by the PRINTER rather than by the type, so a downstream serializer, `Debug` rendering or error formatter could still emit one.
- `contract-leaks` — a value is representable and observably present in an emitted document.

**Understand why the obvious measurement is not enough, because this is the whole reason the panel is here.** The zero-hit result from step 8 can REFUTE `contract-holds`, but it can never confirm it: a purely cosmetic redaction that merely declines to print produces exactly the same zero. The real-state check is blind to the distinction the question turns on.

So build the measurement that is not blind. Add a probe test to `crates/wcore-cli/tests/migrate_typed_dryrun.rs` — name it so a filter on `every_emitter` selects it — which builds a plan from the committed canary corpus and drives it through EVERY emitter the type can reach: the JSON serialization, the `Debug` rendering, the `Display` rendering where one exists, and the rendering the error path produces when the plan is reported as part of a failure. Assert that no canary token from the corpus manifest appears in any of them, reading the tokens from the manifest rather than from a literal. A cosmetic redaction survives the JSON assertion and dies on `Debug` or the error path; a structural one survives all of them. That is what makes this decision measured rather than argued, and it runs on Linux at this plan's SHA.

Then run the panel on the interpretation, following THE PANEL PROCEDURE above, with the panel directory `.planning/phases/26-migration-export-backup-restore/panel/26-01-redaction-contract/`. The evidence bundle is `crates/wcore-config/src/portability/redact.rs`, the multi-emitter probe's source and its captured output, the canary-corpus JSON from the Linux leg, and the corpus manifest's canary list. **It contains no real-home output and no real secret** — three of the four members are remote services, and shipping them a real-home document would exfiltrate exactly the thing this plan protects, in precisely the case where redaction had already failed. Ask the panel whether the type makes a value unrepresentable or merely unprinted, and to name the specific field, variant or accessor that decides it.

Weigh it as follows and record which rule applied. Any real-secret hit in step 8, or any canary surfacing in any emitter, settles the question at `contract-leaks` or `contract-cosmetic` regardless of what any panel member says — the measurement outranks the argument. With the probe clean, a majority for `contract-holds` carries. A minority position carries only with `EVIDENCE:` naming the artifact that makes it stronger — a probe log or a source location. A split with a clean probe and no member able to name a field, variant or accessor through which a value could travel is not a deadlock; it is `contract-holds` with the dissent recorded. A split where a member DOES name such a path, and the probe does not cover it, is a real finding: extend the probe to cover it and re-measure rather than voting.

`contract-cosmetic` and `contract-leaks` are defects this plan must FIX, not outcomes it may record and move past — the gate requires `contract-holds`, so an honest cosmetic finding turns this task red until the type is fixed and the probe re-run. That is the intended behavior.

Marks F26-01 complete if every gate below passes.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; B=.planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md; test -f "$B" || { echo "GATE: baseline missing"; exit 1; }; if /usr/bin/grep -qF 'F26-SC1-MACOS: NOT RUN' "$B"; then echo "GATE: the macOS real-secret leg is MANDATORY - NOT RUN is a failure, not an outcome. A fresh arm64 binary IS obtainable: the CI build job publishes wayland-core-aarch64-apple-darwin on every push to this branch. Acquire it and run the leg, or escalate under termination state 3 with F26-01 unclaimed."; exit 1; fi; M=$(/usr/bin/grep -oE 'F26-SC1-MACOS: RAN — run=[0-9]+ sha=[0-9a-f]{40} binary=[^[:space:]]+ arch=arm64 hermes_profiles=12 openclaw_items=[1-9][0-9]* secret_hits=0' "$B" | /usr/bin/head -1); test -n "$M" || { echo "GATE: no well-formed RAN marker. Required exactly: F26-SC1-MACOS: RAN — run=&lt;id&gt; sha=&lt;40-hex&gt; binary=&lt;path&gt; arch=arm64 hermes_profiles=12 openclaw_items=&lt;non-zero&gt; secret_hits=0. hermes_profiles must be 12 - that is the count measured against the real install, and any other number means the change lost or invented profiles"; exit 1; }; echo "$M"; RUN=$(echo "$M" | /usr/bin/sed -E 's/.* run=([0-9]+) .*/\1/'); SHA=$(echo "$M" | /usr/bin/sed -E 's/.* sha=([0-9a-f]{40}) .*/\1/'); BIN=$(echo "$M" | /usr/bin/sed -E 's/.* binary=([^[:space:]]+) .*/\1/'); case "$BIN" in "$PWD"/*) echo "GATE: the recorded binary $BIN sits INSIDE the repository - the artifact must be unpacked outside it, and a path inside is exactly how the stale local build gets substituted"; exit 1;; esac; test -s "$BIN" || { echo "GATE: the recorded binary $BIN is absent or empty - the artifact was never actually downloaded, or it expired, or it was deleted before this gate ran. A listing entry is not a file: leave the downloaded binary in place until this gate passes"; exit 1; }; /usr/bin/file "$BIN" &gt; /tmp/26-01-t4-file.txt 2&gt;&amp;1; /usr/bin/grep -q 'Mach-O' /tmp/26-01-t4-file.txt &amp;&amp; /usr/bin/grep -q 'arm64' /tmp/26-01-t4-file.txt || { echo "GATE: $BIN is not a Mach-O arm64 executable:"; cat /tmp/26-01-t4-file.txt; exit 1; }; "$BIN" --build-info &gt; /tmp/26-01-t4-buildinfo.txt 2&gt;&amp;1; irc=$?; test $irc -eq 0 || { echo "GATE: $BIN --build-info exited $irc - a binary that cannot report its own provenance is not admissible"; cat /tmp/26-01-t4-buildinfo.txt; exit 1; }; cat /tmp/26-01-t4-buildinfo.txt; /usr/bin/grep -qF "(source $SHA)" /tmp/26-01-t4-buildinfo.txt || { echo "GATE: $BIN reports a DIFFERENT embedded source SHA than the recorded $SHA, so it was compiled from another commit. This is the strongest provenance check in this gate: crates/wcore-cli/build.rs embeds 'git rev-parse HEAD' at compile time as WAYLAND_SOURCE_SHA, and it embeds the literal 'unknown' when git was unavailable, so neither a stale build nor a gitless build can satisfy an exact match against the pinned SHA"; exit 1; }; "$BIN" migrate --help &gt; /tmp/26-01-t4-bin-help.txt 2&gt;&amp;1; brc=$?; test $brc -eq 0 || { echo "GATE: $BIN could not run 'migrate --help' (exit $brc) - it is not a runnable arm64 build of this product"; cat /tmp/26-01-t4-bin-help.txt; exit 1; }; /usr/bin/grep -qF 'hermes' /tmp/26-01-t4-bin-help.txt &amp;&amp; /usr/bin/grep -qF 'openclaw' /tmp/26-01-t4-bin-help.txt || { echo "GATE: $BIN does not list the openclaw source in 'migrate --help', so it PREDATES this plan's implementation. wayland-core has no build-info flag and its version string cannot distinguish two SHAs, so this feature-presence check is what proves the downloaded artifact is the post-change build and not the stale local one"; cat /tmp/26-01-t4-bin-help.txt; exit 1; }; /usr/bin/git cat-file -e "$SHA^{commit}" 2&gt;/dev/null || { echo "GATE: recorded sha $SHA is not a commit in this repository"; exit 1; }; /usr/bin/git merge-base --is-ancestor "$SHA" HEAD || { echo "GATE: recorded sha $SHA is not an ancestor of HEAD - that binary was not built from this plan's line of work"; exit 1; }; for p in crates/wcore-cli/src/migrate/openclaw.rs crates/wcore-config/src/portability/redact.rs; do /usr/bin/git cat-file -e "$SHA:$p" 2&gt;/dev/null || { echo "GATE: the tree at $SHA has no $p - that binary predates this plan's implementation and would certify the old product"; exit 1; }; done; command -v gh &gt;/dev/null 2&gt;&amp;1 || { echo "GATE: gh is unavailable, so the artifact's provenance cannot be checked against GitHub and the marker is only a claim"; exit 1; }; HS=$(gh run view "$RUN" -R FerroxLabs/wayland-core --json headSha --jq .headSha 2&gt;/dev/null); test "$HS" = "$SHA" || { echo "GATE: run $RUN reports headSha '$HS', not the recorded $SHA - the binary did not come from the run it claims"; exit 1; }; gh run view "$RUN" -R FerroxLabs/wayland-core --json jobs --jq '.jobs[] | select(.name=="Build (aarch64-apple-darwin)") | .conclusion' 2&gt;/dev/null &gt; /tmp/26-01-t4-job.txt; /usr/bin/grep -qx 'success' /tmp/26-01-t4-job.txt || { echo "GATE: run $RUN has no successful Build (aarch64-apple-darwin) job, so it produced no admissible binary"; exit 1; }; gh api "repos/FerroxLabs/wayland-core/actions/runs/$RUN/artifacts" --jq '.artifacts[] | select(.expired==false and .size_in_bytes&gt;0) | .name' 2&gt;/dev/null &gt; /tmp/26-01-t4-art.txt; /usr/bin/grep -qx 'wayland-core-aarch64-apple-darwin' /tmp/26-01-t4-art.txt || { echo "GATE: run $RUN publishes no LIVE, non-empty wayland-core-aarch64-apple-darwin artifact - it was never produced, or it has passed the 14-day retention. An expired artifact still appears in the listing, so the name alone proves nothing"; exit 1; }; echo "provenance OK: run=$RUN sha=$SHA"</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f scripts/panel-ask.sh || { echo "GATE: scripts/panel-ask.sh is absent - without it there is no mechanism that hands ONE bundle to every member, only an assurance that it happened"; exit 1; }; sh -n scripts/panel-ask.sh || { echo "GATE: scripts/panel-ask.sh is not valid shell"; exit 1; }; test "$(/usr/bin/grep -vc '^[[:space:]]*#' scripts/panel-ask.sh)" -ge 20 || { echo "GATE: scripts/panel-ask.sh is too thin to read a bundle, invoke three members, capture stdout only and stamp a digest"; exit 1; }; test -f scripts/panel-decision-check.sh || { echo "GATE: scripts/panel-decision-check.sh is absent - every converted decision in this phase gates on it"; exit 1; }; sh -n scripts/panel-decision-check.sh || { echo "GATE: scripts/panel-decision-check.sh is not valid shell"; exit 1; }; sh scripts/panel-decision-check.sh --self-test &gt; /tmp/26-01-panel-selftest.log 2&gt;&amp;1; rc=$?; cat /tmp/26-01-panel-selftest.log; test $rc -eq 0 || { echo "GATE: the panel checker's own self-test failed - it must REJECT a missing capture, a verdict outside the option set, duplicated captures, a mismatched question digest, an unevidenced minority choice and an omitted dissent, AND ACCEPT one well-formed directory. A checker that cannot go red makes every decision leg in this phase decorative"; exit 1; }</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; D=.planning/phases/26-migration-export-backup-restore/panel/26-01-redaction-contract; test -d "$D" || { echo "GATE: no panel directory $D - the structural-versus-cosmetic question was never put to the panel"; exit 1; }; sh scripts/panel-decision-check.sh "$D" contract-holds contract-cosmetic contract-leaks &gt; /tmp/26-01-redaction-check.log 2&gt;&amp;1; rc=$?; cat /tmp/26-01-redaction-check.log; test $rc -eq 0 || { echo "GATE: the redaction-contract panel record is not well-formed - see above"; exit 1; }; DEC="$D/DECISION.md"; C=$(/usr/bin/grep -oE '^CHOSEN: [a-z-]+$' "$DEC" | /usr/bin/sed 's/^CHOSEN: //'); echo "redaction-contract chosen=$C"; test "$C" = "contract-holds" || { echo "GATE: the redaction-contract decision settled on '$C'. contract-cosmetic and contract-leaks are DEFECTS this plan must fix, not outcomes it may record and pass - a printer-only redaction leaks through Debug, serde and the error path, and a leak is the CRITICAL stop. Fix the type, re-run the multi-emitter probe, and re-decide"; exit 1; }</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; SHA=$(/usr/bin/git rev-parse HEAD); WT=/root/wayland-f26-01; ssh -o BatchMode=yes hetzner-dsm "set -e; export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland; /usr/bin/flock /tmp/wayland-fetch.lock git fetch --all --prune; git worktree add --detach $WT $SHA 2&gt;/dev/null || git -C $WT checkout --detach $SHA; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA; cd $WT; cargo nextest run --locked -p wcore-cli --no-fail-fast -E 'test(/every_emitter/)'; test \"\$(git -C $WT rev-parse HEAD)\" = $SHA" &gt; /tmp/26-01-t4-emitter.log 2&gt;&amp;1; rc=$?; cat /tmp/26-01-t4-emitter.log; test $rc -eq 0 || { echo "GATE: the multi-emitter probe failed or did not build - this is the ONLY measurement that distinguishes a structural redaction from a cosmetic one, because a printer-only redaction produces the same zero hits on the real-state check"; exit 1; }; test "$(/usr/bin/grep -cE '[1-9][0-9]* (test|tests) run' /tmp/26-01-t4-emitter.log)" -ge "1" || { echo "GATE: the every_emitter filter selected ZERO tests, so the probe never ran and its green means nothing"; exit 1; }</automated>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; S=$(mktemp); /usr/bin/grep -hoE '[A-Za-z0-9_]*_(API_KEY|TOKEN|SECRET|KEY)=.*' "$HOME"/.hermes/profiles/*/.env 2&gt;/dev/null | /usr/bin/sed 's/^[^=]*=//; s/^["'"'"']//; s/["'"'"']$//' | /usr/bin/grep -vE '^[[:space:]]*$' &gt;&gt; "$S"; /usr/bin/find "$HOME/.openclaw" -maxdepth 2 -type f \( -name '*.json' -o -path '*/credentials/*' \) -print0 2&gt;/dev/null | xargs -0 /usr/bin/grep -hoiE '"[^"]*(key|token|secret|auth|pass|cred)[^"]*"[[:space:]]*:[[:space:]]*"[^"]{16,}"' 2&gt;/dev/null | /usr/bin/sed 's/.*:[[:space:]]*"//; s/"$//' &gt;&gt; "$S"; /usr/bin/sort -u "$S" -o "$S"; T=$(/usr/bin/grep -c . "$S"); test "$T" -ge 4 || { echo "GATE INVALID: only $T real secret values extracted across BOTH peer homes - measured 7 at plan time, so this search is not proving what it claims"; rm -f "$S"; exit 1; }; P=.planning/phases/26-migration-export-backup-restore/panel; test -d "$P" || { echo "GATE INVALID: no panel directory exists, so searching it for leaked secrets would pass vacuously"; rm -f "$S"; exit 1; }; NF=$(/usr/bin/find "$P" -type f 2&gt;/dev/null | /usr/bin/wc -l | tr -d ' '); test "$NF" -ge 5 || { echo "GATE INVALID: panel/ holds $NF files - a completed panel run writes question.md, three captures, an internal pass and a DECISION.md, so fewer than five means there is nothing to search"; rm -f "$S"; exit 1; }; HITS=0; while IFS= read -r v; do [ ${#v} -lt 8 ] &amp;&amp; continue; if /usr/bin/grep -RqF -- "$v" "$P" 2&gt;/dev/null; then HITS=$((HITS+1)); fi; done &lt; "$S"; rm -f "$S"; echo "panel_files=$NF searched=$T hits=$HITS"; test "$HITS" -eq 0 || { echo "GATE: a REAL secret value appears under panel/ - the bundle went to three remote services carrying live credentials. This is a CRITICAL disclosure, not a formatting problem"; exit 1; }</automated>
  </verify>
  <done>The real-state run is recorded in `26-01-BASELINE.md` with the CI run id and head SHA the binary came from, its verified `arm64` architecture, the observed counts for both peers, the confirmation that the secret search was non-vacuous and returned zero hits, and the before-and-after digests of both real homes. The panel harness exists and its self-test proves it rejects each malformed panel shape and accepts a well-formed one. The redaction-contract decision is recorded with all four verbatim captures, a chosen option from this plan's own set, the rule that decided it, and the dissent derived from the captured verdicts — and it settled on `contract-holds`, corroborated by a multi-emitter probe that a cosmetic redaction could not have survived. No real secret value appears anywhere under `panel/`. F26-01 is claimed complete only if this leg passed. If the leg could not be run the plan escalates under termination state 3 with F26-01 UNCLAIMED — there is no state in which it closes on the Linux evidence alone. If any real secret appeared, the plan terminates on that CRITICAL finding instead.</done>
</task>

</tasks>

## What this plan does NOT change (scope fence)

- **`apply` behavior — untouched.** What an import actually WRITES is 26-02's. This plan changes discovery and dry-run only. The existing merge-preserving apply path and its four regression tests stay exactly as they are.
- **Quarantine and provenance — 26-02 owns them.** The 540 real skill directories and 12 persona files stay in the detected-but-not-imported inventory here; making them importable safely is the next plan's whole subject.
- **Backup, restore and rollback — 26-03 owns them.** No top-level command is added by this plan.
- **Hostile corpora — 26-04 owns them.** The corpus this plan commits is a STRUCTURE clone of real installs with canary values; it is a realism fixture, not an adversarial one.
- **`wcore-protocol` — not touched, deliberately.** No migration event is added to the JSON stream protocol, so the D1 producer contract, its generated fixtures and every Desktop consumer are untouched. Migration is a CLI surface in this phase. If a protocol event later proves necessary, that is a contract change requiring the D1 checkpoint, and it is out of scope here.
- **The prose rendering — byte-identical.** It is the measured baseline; `--json` is additive. Changing prose output would silently break anyone parsing it.
- **The existing Hermes profile mapping — untouched.** Provider/model prefix stripping, transport inference and the deterministic credential-variable choice are all measured-good; only the home guard and the root-level walk change.
- **No new external dependency.** Everything needed is already in the workspace.
- **No test is deleted, weakened, re-gated, `#[ignore]`d or `#[allow]`ed.**

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer state tree → Core discovery walk | The source tree is attacker-influenced: symlinks, `..` components, absolute paths, case/Unicode collisions and enormous files all cross here |
| peer config values → Core typed plan | Values inside a peer configuration are untrusted strings, several of which are commands that Core may later execute |
| discovered credential → emitted plan | The point at which a real secret either does or does not become representable in an output document |
| real peer install (Mac) → any other host | Real credentials live on this Mac; the corpus generator exists so that nothing needs to cross this boundary |
| plan output → downstream consumer | `--json` creates machine consumers that will serialize whatever the type can hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01-01 | Information Disclosure | A real provider credential reaches the emitted JSON, a log line, or an error message, and `--json` makes that machine-consumable and easy to persist | critical | mitigate | Redaction is structural: the credential type has no variant that can hold a value, so serializers, loggers and error formatters all inherit it. Proven on Linux against manifest-declared canaries and by the Task 4 decision leg against REAL secrets extracted from the real installs, with the search first proven non-vacuous |
| T-26-01-02 | Information Disclosure | A real secret leaves the Mac by being copied into the committed corpus, a planning artifact, or onto `hetzner-dsm` | critical | mitigate | The generator substitutes canaries for every classified secret; a gate extracts the real values and requires zero matches in the committed corpus and fails if the extraction was empty; copying any real peer file to another host is forbidden outright by the execution rules |
| T-26-01-03 | Tampering | A symlink or `..` component in a peer tree walks discovery outside the declared source root, letting a hostile state directory make Core read arbitrary files | high | mitigate | The tree walk and the digest both refuse to traverse a link resolving outside the root they were given, and the escape is surfaced as a plan warning rather than silently skipped; a dedicated test covers it |
| T-26-01-04 | Spoofing | OpenClaw's platform home resolution is ASSUMED rather than determined, producing an importer that silently finds nothing on one platform and reports success | high | mitigate | Task 1 must DETERMINE the resolution from the peer's own source and record where it was found; Task 3 is forbidden from re-deriving it; a zero item count fails the real-state check rather than passing |
| T-26-01-05 | Repudiation | The dry-run mutates the source it is previewing, so a user who previews an import has already changed the thing they were deciding about | high | mitigate | Non-mutation is proven by a tree digest taken before and after, on both corpora automatically and on both REAL homes at the Task 4 decision leg — measured, never asserted from the code's shape |
| T-26-01-06 | Tampering | A gate passes vacuously: a negative grep over an empty, missing or unparseable output file reports success and manufactures confidence | high | mitigate | Every negative assertion is paired with a positive one — well-formed JSON, manifest-declared item count, non-empty secret extraction — so an empty or malformed emission goes RED; remote statuses are captured on their own line and tested before any output is read, and no filter is ever placed between a gate command and its status |
| T-26-01-07 | Denial of Service | An enormous or deeply nested peer tree makes discovery hang or exhaust memory; the real install already carries 540 skill directories | medium | mitigate | Reads are bounded and the inventory counts rather than loads deferred content; the real-state check in Task 4 runs against the real 540-directory tree, which is the actual scale test |
| T-26-01-08 | Spoofing | The stale `target/debug/wayland-core` on the Mac is used to prove NEW behavior, so the Task 4 decision leg certifies the old product | medium | mitigate | The stale binary is admissible for the pre-change baseline ONLY; Task 4 requires a fresh binary with its provenance and version recorded, and explicitly forbids the stale one |
| T-26-01-09 | Tampering | Backup and last-known-good configuration siblings are treated as additional sources, multiplying every discovered item on a real install that carries several | medium | mitigate | A dedicated test asserts they are excluded, written against the real install's observed set of such files |
| T-26-01-10 | Repudiation | Peer format is grounded only at HEAD, so the importer targets a format the pinned baseline never had — or only at the baseline, so it misses what real users run | medium | mitigate | Three readings — pinned, HEAD, and real install — are reconciled and their drift recorded as an explicit compatibility range; a parse-changing disagreement is a recorded finding |
| T-26-01-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency is added, removed or updated; no `Cargo.toml` or `Cargo.lock` change; the execution rules require escalation rather than an install, so no legitimacy gate applies |
</threat_model>

<verification>
Local gates (Mac, source level only — this host cannot compile): `cargo fmt --all -- --check` clean; `26-01-BASELINE.md` exists and is substantive; the generator is valid Python and deterministic across two runs over the same real source; the committed corpus contains at least eight files and zero real secret values, proven by a search that first proves it had values to search for; every new or touched source file is under 1000 lines.

Authoritative gates (`hetzner-dsm`, in this plan's OWN worktree `/root/wayland-f26-01`, at this plan's SHA asserted before AND after each one, with `/root/.cargo/bin` on `PATH` and each status captured on its own line and tested before any output is read): `cargo build --locked -p wcore-cli --bin wayland-core`; `cargo clippy --locked -p wcore-config -p wcore-cli --all-targets -- -D warnings`; `cargo nextest run --locked -p wcore-config -p wcore-cli --no-fail-fast`; and the built binary's own `migrate --help` — captured to its OWN file, fetched back, and asserted non-trivial before the two source names are looked for, so a build log that merely mentions the word cannot satisfy it. Both corpora must yield well-formed JSON with the manifest-declared item count, byte-identical across two runs, canary-free, over an unchanged tree digest.

Autonomous decision leg (macOS, real state) — MANDATORY, and no longer waits on a human, with CI's `build` job as its producer: a FRESH arm64 binary, downloaded as the `wayland-core-aarch64-apple-darwin` artifact from the run whose head SHA carries this plan's implementation, runs both discoveries against the REAL `~/.hermes` and `~/.openclaw`; Hermes reports the 12 profiles and 1 MCP server the pre-change baseline measured; the real-secret search is non-vacuous and returns zero hits; both real homes digest identically before and after. The gate's automated half re-derives that run from GitHub — the run's head SHA, its `Build (aarch64-apple-darwin)` conclusion and its published artifact list — and requires the recorded SHA to be an ancestor of HEAD whose tree already contains `openclaw.rs` and `redact.rs`, so the marker cannot be satisfied by typing it and a pre-change binary cannot be passed off as a fresh one. There is no not-run branch: a `NOT RUN` marker is a hard failure, and a leg that cannot run escalates with F26-01 unclaimed.

Decision gate (the redaction contract): the panel harness's own `--self-test` must pass, proving the checker rejects a missing capture, a verdict outside the option set, duplicated captures, a mismatched question digest, an unevidenced minority choice and an omitted dissent, AND accepts a well-formed directory. The redaction-contract panel directory must satisfy that checker against this plan's own three options, and must have settled on `contract-holds`. The multi-emitter probe must run on Linux with a non-zero selected test count — it is the only measurement that can tell a structural redaction from a cosmetic one, since both produce zero hits on the real-state check. And no real secret value may appear anywhere under `panel/`, proven by a search that first proves it had values to search for.

Known unknowns to record, not resolve here: whether OpenClaw's Windows home resolution differs from what its source declares once observed on real Windows (26-04 owns the native leg); whether any Hermes or OpenClaw install in the wild carries a profile layout neither the pinned baseline, HEAD, nor Sean's install exhibits; and whether the 540-directory skill tree contains individual skills whose formats the deferred inventory miscounts, which 26-02 will discover when it actually imports them.
</verification>

<success_criteria>
- The shipped binary's behavior against BOTH REAL peer installs is MEASURED before anything changes, and determinism and non-mutation are recorded as measured facts rather than claims about the code.
- Both peer source formats are grounded in three reconciled readings — pinned baseline, checkout HEAD, and real install — with drift recorded as an explicit compatibility range, and OpenClaw's platform home resolution determined from peer source rather than assumed.
- Discovery and dry-run are TYPED: a machine-readable plan is emitted for both peers and the prose rendering is preserved byte-identically.
- Discovery and dry-run are DETERMINISTIC: two independent walks of the same source serialize byte-identically, proven on both corpora.
- Discovery and dry-run are SECRET-REDACTED structurally, not cosmetically: the emitted type cannot hold a secret value, proven against manifest canaries on Linux AND against REAL credentials on macOS. Both halves are mandatory; neither substitutes for the other.
- The macOS binary's producer is CONFIRMED from the workflow file and from a real run's per-job conclusion before anything is built, and the binary Task 4 actually runs is bound to the CI run whose head SHA carries this plan's implementation — provenance asserted against GitHub, not against the executor's own prose.
- Discovery and dry-run are NON-MUTATING, proven by a tree digest before and after on both corpora and on both real homes.
- The reciprocal OpenClaw path exists and is visible in the real binary's help output, closing the Core direction of the ledger's `PORT-*` gap.
- The root-profile discovery gap is closed for both a root-only and a rooted-plus-profiles install, with the name-collision decision recorded in source.
- Every discovered item is either mapped or named in the detected-but-not-imported inventory — nothing is dropped unnamed.
- Every gate can go red: each negative assertion is paired with a positive one, and no remote gate reads its status through a pipe.
- The structural-versus-cosmetic question was DECIDED, not deferred: put to all four panel members on one identical evidence bundle, corroborated by a multi-emitter probe that a printer-only redaction could not survive, settled on a named option from this plan's own set, with the dissent recorded and derived from the captured verdicts rather than from prose.
- The panel harness this phase depends on is proven able to go red by its own self-test, which must reject each malformed panel shape AND accept a well-formed one.
- No real secret value was written to any committed file, reached any panel bundle, or crossed a host boundary.
</success_criteria>

## Artifacts this plan produces
- `.planning/phases/26-migration-export-backup-restore/26-01-BASELINE.md` — the measured pre-change behavior against both real installs, the three-way peer format grounding with drift, and the real-state decision-leg record.
- `scripts/portability-corpus-gen.py` and `crates/wcore-cli/tests/fixtures/portability/` — the structure-cloned, canary-substituted corpora and their manifests.
- `scripts/portability-real-state-check.sh` — the real-state, real-secret check Task 4 runs.
- `crates/wcore-config/src/portability/` — the typed vocabulary, the structural redaction boundary and the tree digest.
- `crates/wcore-cli/src/migrate/openclaw.rs` and the typed `--json` surface on `migrate`.
- `scripts/panel-ask.sh` and `scripts/panel-decision-check.sh` — the phase's panel harness, created here and consumed by every later plan's decision leg.
- `.planning/phases/26-migration-export-backup-restore/panel/26-01-redaction-contract/` — the question bundle, the four verbatim captures and the decision record for the structural-versus-cosmetic redaction question.
- `26-01-SUMMARY.md`.

<output>
Create `.planning/phases/26-migration-export-backup-restore/26-01-SUMMARY.md` using the standard summary template. Record: the exact pre-change invocations against both REAL peer installs with their exit statuses and observed counts; the measured determinism and non-mutation results; the four gaps as established from the binary's own help output and the home guard; the three-way peer format grounding with the pinned-versus-HEAD drift and where OpenClaw's platform home resolution was found in peer source; the corpus generator's classification rule and its determinism proof; the typed vocabulary and exactly how the redaction type makes a secret value unrepresentable; the root-profile gap fix and the name-collision decision; the OpenClaw source's mapped versus deferred inventory; the macOS binary producer confirmation with its exact commands and literal results; the Linux gate results as a delta against the measured baseline with every residual failure named; the real-state decision-leg outcome with the CI run id and head SHA the binary came from, its verified `arm64` architecture, the observed counts for both peers and the non-vacuous zero-hit secret search; confirmation that no real secret was committed or copied off the Mac; confirmation that every Linux gate ran in `/root/wayland-f26-01` and asserted the SHA before and after; the redaction-contract decision with each panel member's verdict and basis quoted from its capture, the multi-emitter probe result that corroborated or refuted them, the option chosen, the rule that decided it, and the dissent; confirmation that the panel bundle carried no real-home output and no real secret; the recorded unknowns; and which of the three termination states the plan ended in. F26-01 is marked complete only if the Task 4 decision leg passed.
</output>

---
phase: 30-continuous-scorecard-frontier-review
plan: "04"
type: execute
wave: 4
depends_on:
  - "30-03"
files_modified:
  - crates/wcore-eval-scenarios/src/reserved_authority.rs
  - crates/wcore-eval-scenarios/src/lib.rs
  - crates/wcore-eval-scenarios/bin/wayland-scorecard.rs
  - crates/wcore-eval-scenarios/tests/reserved_authority_contract.rs
  - .planning/phases/30-continuous-scorecard-frontier-review/30-04-AUTHORITY-PROOF.md
  - .planning/phases/30-continuous-scorecard-frontier-review/30-04-POSITIONING-PACKET.md
  - .planning/phases/30-continuous-scorecard-frontier-review/30-PHASE-VERDICT.md
  - .planning/phases/30-continuous-scorecard-frontier-review/evidence/30-04/
  - .planning/SEAM-REQUESTS/30.md
  - .planning/BACKLOG.md
autonomous: true
requirements:
  - F30-05
must_haves:
  truths:
    - "AN AGENT ON THIS PROGRAM INVENTED AN EXTRA TERMINATION STATE TO DODGE AN ARTIFACT IT WRONGLY BELIEVED UNOBTAINABLE, AND THIS IS THE SAME ATTACK POINTED AT SEAN'S AUTHORITY. Criterion 4 cannot live in prose, because prose is exactly what that agent walked around. The reserved actions are a CLOSED enum that refuses an unknown name at DESERIALIZATION, and a named test feeds it the literal string that was actually invented on this program and asserts it fails. That is not a decorative reference: the invented state and an invented reserved action are the identical move — adding an enum member to route around a wall — and the test exists so a future reader finds the reason in the code rather than in a comment."
    - "THE PRINCIPAL ENUM HAS EXACTLY ONE MEMBER, AND IT IS NOT THE AGENT. An approval is a signed record whose principal is Sean and whose signature verifies against a trust root supplied from outside the record. There is no agent principal, no self-approval, no system principal and no catch-all, so an agent recording its own approval is not a policy violation to detect after the fact — it is a value that cannot be deserialized. `an_approval_whose_principal_is_the_agent_fails_to_deserialize` and `a_self_approved_principal_fails_to_deserialize` both assert their exact strings."
    - "THE BUNDLED APPROVAL ROOT IS A PLACEHOLDER THAT FAILS CLOSED, EXACTLY AS THE REPOSITORY ALREADY DOES ELSEWHERE. `crates/wcore-cli/src/plugin/index.rs` ships `INDEX_PUBKEY_HEX` as an all-zeros placeholder and `IndexVerifier::bundled()` refuses it at runtime with an explicit F-021 error rather than trusting it — six occurrences of each at base. The approval trust root follows that proven pattern rather than inventing a new one: the committed root is a placeholder, it refuses every approval, and frontier positioning is therefore STRUCTURALLY UNREACHABLE in this lane. Sean's real key is the one substitution that changes it, and its exact substitution point is named."
    - "EACH RESERVED ACTION HAS ITS OWN SIGNATURE DOMAIN, SO AN APPROVAL FOR ONE IS NOT AN APPROVAL FOR ANOTHER. Approving a documentation push must not be replayable as approving a release, and approving a release must not be replayable as approving frontier positioning. Nine actions, nine domains, and cross-domain replay is a NAMED test rather than a comment — the same discipline `receipt.rs` already applies to its own signature message and that `plugin/index.rs` conspicuously does not."
    - "PROVING THE MECHANISM WORKS AND PROVING NO APPROVAL EXISTS ARE TWO SEPARATE RUNS AND BOTH ARE REQUIRED. A run with a throwaway approval key generated at run time shows the verifier ACCEPTS a valid approval — without it, the corpus is rejection-only and would be passed by a verifier that refuses everything. A run against the committed placeholder root shows positioning is refused. Reporting either alone is worth very little, and reporting only the first is how a lane would accidentally claim it had approval."
    - "THIS LANE MUST PROVE IT TOOK NO RESERVED ACTION, AND THAT PROOF HAS A CEILING WHICH IS STATED RATHER THAN HIDDEN. What is checkable here: no local branch named main or master contains this lane's HEAD; no tag points at it; the retained evidence refs still resolve; and the non-branch ref count did not fall. What is NOT checkable here: the state of the GitHub remote, because reading it needs a credential this lane must never hold. That residual is named in the verdict as a limit of the evidence, not papered over — the authoritative view of the remote is Sean's."
    - "THE VERDICT IS GRADED VERBATIM AGAINST THE ROADMAP'S FOUR SUCCESS CRITERIA AND NOTHING ELSE. Not against the plans, not against what was built, and not against a reworded version that happens to match the evidence. Quote each criterion as written, grade it with the closed enum 30-01 landed, and attach the evidence that settled it. Phase 21 declared its own goal NOT ACHIEVED twice and Phase 27 graded four of five criteria NOT MET, and both are more trustworthy for it. An honest PARTIAL is worth more than a manufactured MET and costs the program far less."
    - "EXPECT NOT MET, AND DO NOT FLINCH FROM IT. This is the last phase in the program and it inherits every open thing before it: Phase 28 states in advance that it expects its own Criterion 2 to fail on Windows on a carried descendant-reap defect; Phase 29 has filed six named requirements back onto 28; Phases 21, 24, 25 and 27 all carry open requirements with named unmet clauses. If 28 and 29 have not landed their verdicts when this runs, the criteria that depend on them are UNPROVEN — never MET — and 30-01's verifier already refuses MET on an unresolved evidence reference, so this is enforced rather than promised."
    - "THIS PLAN STOPS SHORT OF THE POSITIONING DECISION, WHICH IS THE WHOLE POINT OF CRITERION 4. It produces the packet Sean decides from: what may be claimed, what may not, what is unproven, what it would cost to prove, and what the enumerated real-key limits are. It does not recommend a position, does not grade Wayland against a market, and does not contain a sentence a reader could quote as a positioning statement. An agent that ends this phase by saying Wayland is ready has committed the exact violation the phase was built to prevent."
  artifacts:
    - path: crates/wcore-eval-scenarios/src/reserved_authority.rs
      provides: "The closed nine-member reserved-action enum, the single-member principal enum, the per-action signature domains, and the bundled placeholder root that fails closed"
    - path: .planning/phases/30-continuous-scorecard-frontier-review/30-04-AUTHORITY-PROOF.md
      provides: "Both runs — the throwaway-key positive control proving the mechanism accepts a valid approval, and the placeholder-root proof that frontier positioning is unreachable — plus the audit that this lane took no reserved action, with its ceiling stated"
    - path: .planning/phases/30-continuous-scorecard-frontier-review/30-04-POSITIONING-PACKET.md
      provides: "The evidence Sean decides from, containing no recommendation and no positioning sentence: allowed claims, prohibited claims, limitations, unproven legs, real-key limits and what each would cost to close"
    - path: .planning/phases/30-continuous-scorecard-frontier-review/30-PHASE-VERDICT.md
      provides: "The four Success Criteria quoted verbatim and graded with the closed verdict enum against resolving evidence, with NOT MET and UNPROVEN carrying the same evidentiary weight as MET"
  key_links:
    - from: ReservedActionV1
      to: the invented enum member that routes around a wall
      via: "a closed enum refusing an unknown action name at deserialization, with the exact string an agent on this program actually invented asserted in a named test"
      pattern: "closed-enum-anchored-to-a-real-incident"
    - from: frontier positioning
      to: Sean's approval key
      via: "a bundled all-zeros placeholder trust root that fails closed exactly as `IndexVerifier::bundled()` already does, making positioning structurally unreachable in this lane"
      pattern: "structural-unreachability-by-placeholder"
    - from: .planning/phases/30-continuous-scorecard-frontier-review/30-PHASE-VERDICT.md
      to: .planning/ROADMAP.md Phase 30 Success Criteria
      via: "each criterion quoted verbatim and graded by the closed enum against evidence that must resolve, so MET is unreachable on an unresolved reference"
      pattern: "verbatim-grading"
---

<objective>
Make Sean's reserved authority a structure an agent in a hurry cannot collapse — a closed action enum anchored to the exact invention this program has already suffered, a principal enum with no agent member, per-action signature domains, and a bundled placeholder root that fails closed — then prove both that the mechanism works and that positioning is unreachable here, and grade the phase's four Success Criteria verbatim without flinching from NOT MET.

Purpose: Criterion 4 is the reserved-authority criterion and it is the one an agent under deadline pressure is most likely to reinterpret. The move is always the same: add an enum member, or a state, or a principal, that routes around the wall. This program has already watched it happen once. So the defense is not a rule to remember but a type that refuses the invention at deserialization, plus a trust root that this lane cannot satisfy no matter what it decides.
Output: the reserved-authority module with its contract suite; both authority runs captured, the positive control and the refusal; an audit that this lane took no reserved action, with the remote-side ceiling stated rather than hidden; a positioning packet containing no positioning sentence; and the four criteria graded verbatim with every residual filed.
</objective>

<execution_context>
@$HOME/.claude/ferrox-core/workflows/execute-plan.md
@$HOME/.claude/ferrox-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-continuous-scorecard-frontier-review/30-01-LEDGER-REVIEW.md
@.planning/phases/30-continuous-scorecard-frontier-review/30-02-TRIAL-RESULTS.md
@.planning/phases/30-continuous-scorecard-frontier-review/30-03-LIMITATIONS.md
@crates/wcore-eval-scenarios/src/scorecard.rs
@crates/wcore-eval-scenarios/src/receipt.rs
@crates/wcore-cli/src/plugin/index.rs
</context>

<execution_rules>

**THE TWO AMENDED PHASE RULES — verbatim, and they bound this plan.**

- Findings at CRITICAL or HIGH must be fixed or disproved with executable evidence. MEDIUM and below are logged to `.planning/BACKLOG.md` and DO NOT BLOCK execution.
- Execution begins when no CRITICAL or HIGH finding is open, or after 2 review rounds, whichever comes first. A third round is NOT permitted; it escalates to Sean.

**THIS PLAN STOPS SHORT OF THE POSITIONING DECISION. THAT IS CRITERION 4.** It produces the packet Sean decides from. It may not contain a recommendation, a readiness statement, a market comparison, or any sentence that could be quoted as a positioning claim. Ending this phase by declaring Wayland ready for anything is the exact violation the phase exists to prevent, and it would be worse than declaring the phase NOT ACHIEVED.

**THE RESERVED ACTIONS ARE SEAN'S AND THIS LANE PERFORMS NONE OF THEM.** No push to `main`, no merge to `main`, no pull request, no tag, no release, no deployment, no issue closure, no deletion of a retained evidence ref, and no frontier positioning. Committing and pushing this lane's own branch is expected and is not a reserved action. This plan both refrains from them and PROVES it refrained, within the ceiling stated below.

**NO CREDENTIAL, EVER.** Nothing here reads, requests, copies, prints, logs or commits a real key, token or account, and specifically it never obtains, requests or simulates Sean's approval key. The throwaway approval key used for the positive control is generated at run time into a temporary directory, written only to a mode-0600 file, read only from standard input, and dies with the run. No gate may require a secret to pass.

**NEVER `/Users/seandonahoe/dev/waylandcore`.** Verify `git rev-parse --show-toplevel` resolves to the lane worktree before the first write and abort if it does not.

**TERMINATION CRITERION FOR THIS PLAN (hard). There are exactly three states and no others.** The phrase naming a fourth was invented on this program by an agent dodging a wall, and this plan is literally built to refuse it.
1. **Complete** — the authority module lands green, both authority runs are captured, the no-reserved-action audit runs with its ceiling stated, and all four Success Criteria are graded verbatim with evidence.
2. **Complete with criteria NOT MET** — one or more criteria grade NOT MET, PARTIAL or UNPROVEN. This is a complete outcome and, given what this phase inherits, the likely one. Record it plainly, file the residuals, and do not soften the grade.
3. **Escalated** — grading a criterion would require a real credential or an action reserved to Sean. STOP, record it as NOT PROVABLE HERE with its substitution point, and grade that criterion UNPROVEN.

Under no circumstances does this plan spawn additional plans, extend its own task list, invent a fourth termination state, invent a verdict grade, regrade a criterion after seeing that it fails, or perform a reserved action.

**SCOPE BOUNDARY (hard).** 30-01's ledger review and surface inventory, 30-02's frozen protocol and trial results, and 30-03's claims register and published documents are all INPUTS and are gate-checked unmodified. If one of them is wrong, that is a finding in the verdict with a severity — not an edit to make here, because the pass that grades the evidence must not be the pass that repairs it. This plan does NOT touch `crates/wcore-eval-scenarios/src/receipt.rs`, `src/receipt_policy.rs`, `bin/wayland-receipt.rs`, `src/release_integrity.rs`, `src/e5_soak.rs`, `src/process_tree.rs`, `src/fixtures/openai.rs`, `src/frontier_trials.rs`, `src/claims.rs`, `src/scorecard.rs`, `crates/wcore-cli/src/lib.rs`, `crates/wcore-cli/src/main.rs`, `crates/wcore-cli/src/plugin/index.rs` (READ as the fail-closed precedent, never edited), the workspace-root `Cargo.toml`, or `Cargo.lock`.

**THE SHARED EVAL-CRATE FILES ARE AN ADDITIVE-ONLY FENCE.** In `crates/wcore-eval-scenarios/src/lib.rs` add exactly one `pub mod reserved_authority;` line in the existing alphabetical list. In `bin/wayland-scorecard.rs` add the `authority` subcommand as one contiguous additive block alongside `surfaces`, `verify`, `trials` and `claims`.

**NO NEW DEPENDENCY.** `ed25519-dalek`, `rand`, `sha2`, `base64`, `serde`, `serde_json`, `clap`, `tempfile`, `thiserror` and `anyhow` are already dependencies of `wcore-eval-scenarios`.

**DO NOT RUN `wcore-contract generate`.** Nothing here needs it; if that changes, write a fenced seam request into `.planning/SEAM-REQUESTS/30.md` instead.

**NO GATE ENDS IN A PIPE.** Measured on this branch: `ssh hetzner-dsm 'exit 9'` returns 9, `ssh hetzner-dsm 'exit 9' | tail -3` returns 0. Every remote gate captures the remote status into `rc` FIRST. No line containing `ssh` in this plan contains a `|`.

**EVERY GATE MUST BE ABLE TO GO RED, AND ALSO ABLE TO GO GREEN.** Each source-level count gate names the value MEASURED at base `cd5b4e9bf653d115518be5b76cb1a94f535775f4`. Six guaranteed-broken shapes are pre-closed throughout this phase: `grep -c` never gets more than one file; `grep -r` never carries `-c`; a `grep -c` result is never assigned to a variable used as an `&&` chain element, INCLUDING the path-qualified form the gate linter cannot see; `wc -l` always goes through `tr -d ' '`; every digest comparison is preceded by `test -s` on both operands; and a gate proving a REFUSAL never asserts a non-zero remote status locally, because that form passes when ssh is down — the remote side decides and exits zero only on the refusal.

**ENVIRONMENT.**
- Linux (authoritative): `ssh -o BatchMode=yes hetzner-dsm`, `/root/wayland`, `PATH=/root/.cargo/bin:$PATH`, targeted builds only, `df -h /root` before a long run, prune afterwards.
- Mac: `cargo fmt --all -- --check` only. Use `/usr/bin/git`, `/usr/bin/grep`, `/usr/bin/shasum` and `/usr/bin/wc` — the ambient `grep` is rtk-proxied and SILENTLY DROPS LINES.
- Push the lane branch before running any remote gate.
- Commit and push the LANE branch only.

**AGENTS.md discipline.** `thiserror` publicly, `anyhow` internally, no `unwrap()` in production paths without a proven commented invariant, files under 1000 lines, clippy-clean at `-D warnings`, surgical diffs.

**Git hygiene.** `/usr/bin/git` on the Mac. Stage the exact paths in `files_modified`, never `-A`, never `.`. Never `git checkout`, `git reset`, `git stash` or `git rebase`. No `Co-Authored-By` trailers.
</execution_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Land the reserved-authority ledger so that inventing a way around Sean's approval is not a policy violation but a deserialization failure</name>
  <files>crates/wcore-eval-scenarios/src/reserved_authority.rs, crates/wcore-eval-scenarios/src/lib.rs, crates/wcore-eval-scenarios/bin/wayland-scorecard.rs, crates/wcore-eval-scenarios/tests/reserved_authority_contract.rs</files>
  <read_first>crates/wcore-cli/src/plugin/index.rs — `INDEX_PUBKEY_HEX` and `IndexVerifier::bundled()`, the all-zeros placeholder and the explicit F-021 refusal, six occurrences of each at base; this is the proven fail-closed pattern the approval root COPIES rather than improvises on, and it is also the counter-example for signatures because it verifies over a serialised body with NO domain separator; crates/wcore-eval-scenarios/src/receipt.rs — the domain-separated signature message, `AuthorityClaimV1`, `SecretBytes` wipe-on-drop and `decode_secret_32` (the key-handling shapes to reuse); crates/wcore-eval-scenarios/bin/wayland-receipt.rs (a seed read from STDIN and never from argv, wiped after use); crates/wcore-eval-scenarios/src/scorecard.rs (30-01's closed-enum idiom and the evidence resolver); .planning/REQUIREMENTS.md F30-05 and the Out of Scope row naming the Sean-only authorization gate (the exact list of reserved actions); AGENTS.md §11 (the same list stated as a project learning, and the reasoning behind it)</read_first>
  <behavior>
    - Test first, in every case. Each behavior below gets a failing test before the implementation that satisfies it, and the RED is recorded in the summary.
    - The reserved actions are a closed nine-member enum with no catch-all and no default, so an unrecognised action name fails to deserialize rather than mapping to anything.
    - The principal is a closed enum with exactly one member, so an approval attributed to an agent, to itself, or to a system fails to deserialize.
    - Each reserved action has its own signature domain, so an approval minted for one action does not verify for another even when the same key produced it.
    - An approval binds the digest of the subject it approves, so an approval of one artifact cannot be moved onto a different one.
    - Verification requires a trust root supplied independently of the approval; nothing inside the approval establishes its own authority.
    - The bundled approval trust root is a placeholder that refuses every approval with an explicit error rather than trusting itself.
    - Frontier positioning is refused under the bundled placeholder root, and verifies under a throwaway root generated at run time — both proved, so the mechanism is shown to work as well as shown to be closed here.
    - Every acceptance behavior has a matching refusal behavior proved with a mutated input, and every refusal behavior has a pristine control that is accepted first.
  </behavior>
  <action>Add `reserved_authority` as a new module declared with exactly one `pub mod reserved_authority;` line in the existing alphabetical list in `src/lib.rs`, and extend `bin/wayland-scorecard.rs` with an `authority` subcommand as one contiguous additive block. Add no dependency.

Define `ReservedActionV1` as a closed enum over exactly the nine actions the program reserves: source push, main merge, pull request, tag, release, deployment, issue closure, retained-evidence-ref deletion, and frontier positioning. No catch-all, no default, no untagged fallback. Give each action its own signature domain constant naming that action, so nine domains exist and none is shared.

Define `PrincipalV1` as a closed enum with exactly ONE member. There is no agent principal and no self-approval principal, and the point of the single-member enum is that adding one would be a visible, reviewable code change rather than a value an agent can write into a document at three in the morning.

Define `ApprovalRecordV1` binding the action, the principal, the digest of the subject being approved, and an authority claim carrying a key id and a base64 detached signature over the action's own domain-separated message. Every struct crossing the trust boundary rejects unknown fields.

Define the trust root as a document mapping key ids to public keys with a validity window, and give it a `bundled()` constructor that returns an all-zeros placeholder — copy the shape `IndexVerifier::bundled()` already uses in `crates/wcore-cli/src/plugin/index.rs`, including refusing at verification time with an explicit named error rather than silently trusting a zero key. That refusal is what makes frontier positioning structurally unreachable in this lane, and the error message must name the substitution point: Sean's real public key replacing the placeholder is the one change that alters it.

Build the `authority` subcommand with clap following `wayland-receipt`'s shape: initialise a throwaway root into a caller-named directory writing each seed to a mode-0600 file and printing only key ids and public keys; record an approval, with the seed read from standard input and wiped after use; and verify an approval against an independently supplied root. There is no subcommand that prints a seed and none that takes a seed as an argument.

Write the contract suite in `tests/reserved_authority_contract.rs` with these exact names: `an_invented_reserved_action_named_termination_state_4_fails_to_deserialize`, `an_approval_whose_principal_is_the_agent_fails_to_deserialize`, `a_self_approved_principal_fails_to_deserialize`, `an_approval_minted_for_a_source_push_does_not_verify_as_a_release`, `an_approval_moved_onto_a_different_subject_digest_is_refused`, `an_unknown_key_id_is_refused_rather_than_trusted`, `frontier_positioning_is_refused_under_the_bundled_placeholder_root`, and `frontier_positioning_verifies_under_a_throwaway_root_generated_at_run_time`. The first deserializes an action whose name reads `termination_state_4` and asserts it fails — that exact string, because an agent on this program invented exactly that member to route around a wall it wrongly believed impassable, and inventing a reserved action to route around Sean is the identical move. That literal belongs in the test file only and never in the production module. The last is the positive control and is not optional: without it the suite is rejection-only and would be passed by a verifier that refuses everything, which would prove nothing about whether an approval can ever be honoured. Every test carries a pristine control accepted before its mutation.

Prove it on Hetzner: build and test targeted with `-p wcore-eval-scenarios`, and run the `authority` subcommand for real — initialise a throwaway root into a temporary directory, record and verify a frontier-positioning approval under it, then verify the same approval against the bundled placeholder root and capture the refusal — both exit statuses captured. Records evidence for F30-05; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated># MODULE-LANDED GATE. Measured at base: `reserved_authority` appears 0 times in the eval crate's lib.rs and the module file does not exist.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f crates/wcore-eval-scenarios/src/reserved_authority.rs &amp;&amp; test "$(/usr/bin/grep -c 'pub mod reserved_authority' crates/wcore-eval-scenarios/src/lib.rs)" -ge 1</automated>
    <automated># CLOSED-ACTION GATE. Four of the nine reserved actions asserted by name, plus the exhaustive array whose length the compiler checks, plus the requirement that the invented member appears NOWHERE in the production module. All legs red at base because the file does not exist.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; M=crates/wcore-eval-scenarios/src/reserved_authority.rs &amp;&amp; test "$(/usr/bin/grep -cF 'FrontierPositioning' "$M")" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'MainMerge' "$M")" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'IssueClosure' "$M")" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'RetainedEvidenceRefDeletion' "$M")" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'ReservedActionV1; 9]' "$M")" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'termination_state_4' "$M")" -eq 0</automated>
    <automated># SINGLE-PRINCIPAL GATE. The principal enum must exist and neither an agent nor a self-approval member may appear anywhere in the module. Legs 2 and 3 are zero-counts and would pass against a tree where the file is absent, so they are conjoined with leg 1 which is red at base.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; M=crates/wcore-eval-scenarios/src/reserved_authority.rs &amp;&amp; test "$(/usr/bin/grep -cF 'PrincipalV1' "$M")" -ge 1 &amp;&amp; test "$(/usr/bin/grep -ciF 'selfapproved' "$M")" -eq 0 &amp;&amp; test "$(/usr/bin/grep -cE 'Principal(V1)?::Agent' "$M")" -eq 0</automated>
    <automated># NINE-DOMAIN GATE. Each reserved action carries its own signature domain, so an approval for one cannot be replayed as another. Nine distinct domain strings under a common prefix; counting DISTINCT matches so nine repetitions of one domain do not satisfy it. Red at base.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -oE 'wayland\.reserved\.[a-z_]+\.v1' crates/wcore-eval-scenarios/src/reserved_authority.rs | /usr/bin/sort -u | /usr/bin/wc -l | tr -d ' ')" -ge 9</automated>
    <automated># FAIL-CLOSED-PLACEHOLDER GATE. The bundled approval root copies the proven all-zeros-plus-explicit-refusal shape. Leg 1 is red at base; legs 2 and 3 are REGRESSION GUARDS on the precedent itself with baselines measured at `cd5b4e9bf653d115518be5b76cb1a94f535775f4` — `F-021` and `INDEX_PUBKEY_HEX` each appear 6 times in `plugin/index.rs` — so if the precedent this module copies is dissolved while copying it, the line goes red.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -cF 'bundled' crates/wcore-eval-scenarios/src/reserved_authority.rs)" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'F-021' crates/wcore-cli/src/plugin/index.rs)" -ge 6 &amp;&amp; test "$(/usr/bin/grep -cF 'INDEX_PUBKEY_HEX' crates/wcore-cli/src/plugin/index.rs)" -ge 6</automated>
    <automated># ANTI-COLLAPSE GATE. These eight names are the structural defenses against the exact move this program has already suffered. The last is the POSITIVE CONTROL and is listed here deliberately: without it the suite is rejection-only and would be passed by a verifier that refuses everything. Red at base; the remote gate below RUNS them.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; F=crates/wcore-eval-scenarios/tests/reserved_authority_contract.rs &amp;&amp; test "$(/usr/bin/grep -cF 'an_invented_reserved_action_named_termination_state_4_fails_to_deserialize' "$F")" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'an_approval_whose_principal_is_the_agent_fails_to_deserialize' "$F")" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'a_self_approved_principal_fails_to_deserialize' "$F")" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'an_approval_minted_for_a_source_push_does_not_verify_as_a_release' "$F")" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'an_approval_moved_onto_a_different_subject_digest_is_refused' "$F")" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'frontier_positioning_is_refused_under_the_bundled_placeholder_root' "$F")" -ge 1 &amp;&amp; test "$(/usr/bin/grep -cF 'frontier_positioning_verifies_under_a_throwaway_root_generated_at_run_time' "$F")" -ge 1</automated>
    <automated># NO-SECRET-ON-ARGV GATE. The approval seed is read from standard input exactly as `wayland-receipt sign` reads it, never as a clap argument. Leg 1 anchors on this task's module so the line is red at base; leg 2 asserts the binary declares no argument whose name carries a seed, a private key or a secret.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -f crates/wcore-eval-scenarios/src/reserved_authority.rs &amp;&amp; test "$(/usr/bin/grep -cE 'arg\(long[^)]*(seed|private|secret)' crates/wcore-eval-scenarios/bin/wayland-scorecard.rs)" -eq 0</automated>
    <automated># INPUTS-FROZEN GATE. The pass that grades the evidence must not be the pass that repairs it. 30-01's, 30-02's and 30-03's artifacts are inputs and are untouched. Conjoined with this task's anchor so a clean tree alone cannot pass the line at base.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; P=.planning/phases/30-continuous-scorecard-frontier-review &amp;&amp; test -f crates/wcore-eval-scenarios/src/reserved_authority.rs &amp;&amp; test "$(/usr/bin/git status --porcelain --untracked-files=all -- "$P/evidence/30-01" "$P/evidence/30-02" "$P/evidence/30-03" crates/wcore-eval-scenarios/src/scorecard.rs crates/wcore-eval-scenarios/src/frontier_trials.rs crates/wcore-eval-scenarios/src/claims.rs crates/wcore-cli/src/plugin/index.rs Cargo.toml Cargo.lock | /usr/bin/wc -l | tr -d ' ')" -eq 0</automated>
    <automated># GATE, NOT A PRINTOUT — remote status captured first, no pipe on this line.
SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); LOG=$(mktemp); ssh -o BatchMode=yes hetzner-dsm "export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland &amp;&amp; git fetch --all &amp;&amp; git checkout --detach $SHA &amp;&amp; cargo clippy -p wcore-eval-scenarios --all-targets -- -D warnings &amp;&amp; cargo nextest run -p wcore-eval-scenarios --no-fail-fast" &gt; "$LOG" 2&gt;&amp;1; rc=$?; tail -40 "$LOG"; rm -f "$LOG"; test "$rc" -eq 0</automated>
  </verify>
  <done>The reserved-authority module lands, is declared exactly once, adds no dependency, and extends the existing binary additively. Every behavior has a test that was RED before its implementation and the RED is recorded. The nine reserved actions are a closed enum backed by an exhaustive array whose length the compiler checks, and the invented member is gate-checked absent from the production module while being asserted as a rejection in the test file. The principal enum has one member and neither an agent nor a self-approval member appears anywhere. Nine distinct signature domains exist, counted distinctly. The bundled root is an all-zeros placeholder that refuses with an explicit error naming its substitution point, copying the shape `IndexVerifier::bundled()` already proves, and the precedent itself is gate-checked intact. Frontier positioning is refused under the placeholder and verifies under a throwaway root generated at run time — both proved, so the mechanism is shown to work as well as shown to be closed here. No seed reaches an argv or standard output. The prior plans' artifacts are gate-checked frozen.</done>
</task>

<task type="auto">
  <name>Task 2: Prove both authority runs on hardware and audit that this lane took no reserved action, with the ceiling stated</name>
  <files>.planning/phases/30-continuous-scorecard-frontier-review/30-04-AUTHORITY-PROOF.md, .planning/phases/30-continuous-scorecard-frontier-review/evidence/30-04/</files>
  <read_first>crates/wcore-eval-scenarios/src/reserved_authority.rs (task 1's module — the runs drive the real verifiers, never a test double), .planning/REQUIREMENTS.md Out of Scope table (the authoritative list of reserved actions), the repository's own ref inventory measured at base — thirty-seven non-branch refs comprising thirty-six tags plus `refs/f20a/candidate`, with `refs/tags/f20a-candidate-9821ef76`, `refs/tags/f20a-harness-39d30e55` and `refs/tags/strike/f20-pre-workspace-20260717` among the retained evidence refs whose deletion is itself a reserved action, and note that this repository has NO remote-tracking refs at all, which is what bounds the audit</read_first>
  <behavior>
    - The positive control runs first: a throwaway approval root generated at run time accepts a valid frontier-positioning approval, proving the mechanism can honour one.
    - The refusal runs second: the same approval verified against the committed bundled placeholder root is refused, proving positioning is unreachable in this lane.
    - Both runs drive the real shipped verifier, never a test double, and both exit statuses are captured.
    - No local branch named main or master contains this lane's HEAD.
    - No tag points at this lane's HEAD.
    - The named retained evidence refs still resolve, and the total non-branch ref count has not fallen below the baseline.
    - The audit states its own ceiling explicitly: the GitHub remote cannot be read without a credential this lane must never hold, so the remote-side proof is Sean's to make and is recorded as a stated limit rather than claimed.
    - Every determination is captured, and a determination that could not be reduced to a command says so.
  </behavior>
  <action>Run the positive control first, because a proof that positioning is refused means very little unless the mechanism can accept anything at all. On the build host, initialise a throwaway approval root into a temporary directory, record a frontier-positioning approval over a real subject digest with the seed read from standard input, verify it against that throwaway root, and capture the accepting exit status. The key dies with the run and is never printed.

Then run the refusal: verify that same approval against the committed bundled placeholder root and capture the refusing exit status and the exact error text, which must name the substitution point. Write both runs into `30-04-AUTHORITY-PROOF.md` and state both sentences plainly — the mechanism works, and no approval exists in this lane. Reporting the first without the second is precisely how a lane would drift into acting as though it had approval.

Then audit what this lane actually did. Determine whether any local branch named main or master contains this lane's HEAD; whether any tag points at it; whether the named retained evidence refs still resolve; and whether the non-branch ref count has fallen below the baseline measured during planning. Capture each command and its output.

Then state the ceiling, in the document and again in the verdict. This repository has no remote-tracking refs at all, so nothing here can observe the state of `main` on the GitHub remote, and reading it would require a credential this lane must never hold. The honest statement is therefore precise: no reserved action was taken within this repository's reach, and the remote-side confirmation is Sean's. Do not claim more than that, and do not obtain a credential to close it.

Record every determination into `evidence/30-04/authority-audit.tsv`, one row per determination, shape exactly: `AUTH-NN::&lt;CONFIRMED|REFUTED|NOT-OBSERVABLE-HERE&gt;::evidence=&lt;path relative to evidence/30-04&gt;`. Records evidence for F30-05; marks no requirement complete.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated># AUDIT-LEDGER GATE. Eight determinations is the floor — the two authority runs plus the six audit checks — and every named capture must exist and hold real output. The `NOT-OBSERVABLE-HERE` status exists so the remote-side limit is RECORDED rather than either claimed or omitted. Red at base — the directory does not exist.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/30-continuous-scorecard-frontier-review/evidence/30-04 &amp;&amp; test -s "$EV/authority-audit.tsv" &amp;&amp; test "$(/usr/bin/grep -cE '^AUTH-[0-9]+::(CONFIRMED|REFUTED|NOT-OBSERVABLE-HERE)::evidence=[^[:space:]]+$' "$EV/authority-audit.tsv")" -ge 8 &amp;&amp; while IFS= read -r R; do case "$R" in ""|"#"*) continue;; esac; F="$EV/${R##*::evidence=}"; test -s "$F" || exit 1; test "$(/usr/bin/wc -l &lt; "$F" | tr -d ' ')" -ge 2 || exit 1; done &lt; "$EV/authority-audit.tsv"</automated>
    <automated># CEILING-RECORDED GATE. The remote-side limit must appear as its own determination with the dedicated status, not as a sentence in prose that a later reader may skip. Red at base.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test "$(/usr/bin/grep -c '::NOT-OBSERVABLE-HERE::' .planning/phases/30-continuous-scorecard-frontier-review/evidence/30-04/authority-audit.tsv)" -ge 1</automated>
    <automated># NO-MAIN-MERGE GATE. Merging this lane into main is a reserved action. This asserts no local branch named main or master contains the lane's HEAD. It would pass at base in a repository that has no such branch, so it is conjoined with this task's completion anchor.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -s .planning/phases/30-continuous-scorecard-frontier-review/evidence/30-04/authority-audit.tsv &amp;&amp; test "$(/usr/bin/git branch --contains HEAD --format='%(refname:short)' | /usr/bin/grep -cE '^(main|master)$')" -eq 0</automated>
    <automated># NO-TAG GATE. Tagging is a reserved action and this lane created none, so nothing points at its HEAD. Conjoined with the completion anchor because an untagged HEAD is also the base condition.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -s .planning/phases/30-continuous-scorecard-frontier-review/evidence/30-04/authority-audit.tsv &amp;&amp; test "$(/usr/bin/git tag --points-at HEAD | /usr/bin/wc -l | tr -d ' ')" -eq 0</automated>
    <automated># RETAINED-EVIDENCE-REFS GATE. Deleting a retained evidence ref is a reserved action. Three named refs must still resolve and the total non-branch ref count must not have FALLEN below the thirty-seven measured at base — deliberately a floor rather than an equality, because other lanes share this object store and a ref another lane legitimately adds must not red this gate while a deletion still does.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; test -s .planning/phases/30-continuous-scorecard-frontier-review/evidence/30-04/authority-audit.tsv &amp;&amp; test "$(/usr/bin/git cat-file -t refs/f20a/candidate)" = commit &amp;&amp; test "$(/usr/bin/git rev-parse --verify --quiet refs/tags/f20a-candidate-9821ef76 | /usr/bin/wc -l | tr -d ' ')" -eq 1 &amp;&amp; test "$(/usr/bin/git rev-parse --verify --quiet refs/tags/strike/f20-pre-workspace-20260717 | /usr/bin/wc -l | tr -d ' ')" -eq 1 &amp;&amp; test "$(/usr/bin/git for-each-ref --format='%(refname)' | /usr/bin/grep -cv '^refs/heads/')" -ge 37</automated>
    <automated># POSITIVE-CONTROL RUN. The mechanism must be shown to ACCEPT a valid approval under a throwaway root generated at run time, or the refusal below proves only that the verifier refuses things. The seed never reaches an argv and is never printed. Remote status captured first; no pipe on this line.
SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); LOG=$(mktemp); ssh -o BatchMode=yes hetzner-dsm "export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland &amp;&amp; git fetch --all &amp;&amp; git checkout --detach $SHA &amp;&amp; cargo build --release --locked -p wcore-eval-scenarios &amp;&amp; D=\$(mktemp -d) &amp;&amp; ./target/release/wayland-scorecard authority init-root --dir \$D &amp;&amp; ./target/release/wayland-scorecard authority verify --root \$D/root.json --approval \$D/frontier-positioning.approval.json; rc=\$?; rm -rf \$D; exit \$rc" &gt; "$LOG" 2&gt;&amp;1; rc=$?; tail -30 "$LOG"; rm -f "$LOG"; test "$rc" -eq 0</automated>
    <automated># STRUCTURAL-UNREACHABILITY RUN. The same approval verified against the COMMITTED bundled placeholder root must be REFUSED. The naive form — assert a non-zero remote status — is a guaranteed-GREEN defect that passes when ssh is down or the build fails, so the remote side decides: setup failures exit non-zero, an unexpected acceptance exits 9, and only the refusal exits 0. The local assertion is the ordinary zero. No pipe on this line.
SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); LOG=$(mktemp); ssh -o BatchMode=yes hetzner-dsm "export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland &amp;&amp; git checkout --detach $SHA &amp;&amp; test -x ./target/release/wayland-scorecard &amp;&amp; D=\$(mktemp -d) &amp;&amp; ./target/release/wayland-scorecard authority init-root --dir \$D &amp;&amp; if ./target/release/wayland-scorecard authority verify --bundled-root --approval \$D/frontier-positioning.approval.json; then echo UNEXPECTED_ACCEPTANCE_UNDER_PLACEHOLDER; rm -rf \$D; exit 9; else echo REFUSED_AS_REQUIRED; rm -rf \$D; exit 0; fi" &gt; "$LOG" 2&gt;&amp;1; rc=$?; tail -30 "$LOG"; rm -f "$LOG"; test "$rc" -eq 0</automated>
  </verify>
  <done>The positive control ran first on real hardware: a throwaway root generated at run time accepted a valid frontier-positioning approval, with the accepting exit status captured and the seed never printed. The refusal ran second: the same approval verified against the committed bundled placeholder root was refused, with the exact error text captured naming its substitution point, and the gate asserting that refusal cannot be satisfied by ssh failing. Both sentences are stated plainly in the proof document — the mechanism works, and no approval exists in this lane. The audit records at least eight determinations, each naming a capture that exists: no local main or master contains this lane's HEAD, no tag points at it, the three named retained evidence refs still resolve, and the non-branch ref count has not fallen below the baseline. The remote-side limit is recorded as its own determination with the dedicated status, and the document says precisely that no reserved action was taken within this repository's reach and that the remote-side confirmation is Sean's. No credential was obtained to close it.</done>
</task>

<task type="auto">
  <name>Task 3: Grade Phase 30's four Success Criteria verbatim, build the positioning packet, and file every residual</name>
  <files>.planning/phases/30-continuous-scorecard-frontier-review/30-PHASE-VERDICT.md, .planning/phases/30-continuous-scorecard-frontier-review/30-04-POSITIONING-PACKET.md, .planning/phases/30-continuous-scorecard-frontier-review/evidence/30-04/, .planning/SEAM-REQUESTS/30.md, .planning/BACKLOG.md</files>
  <read_first>.planning/ROADMAP.md Phase 30 Success Criteria (the four sentences, to be QUOTED verbatim and graded, never reworded to fit the evidence), .planning/phases/30-continuous-scorecard-frontier-review/30-01-LEDGER-REVIEW.md and 30-01-SURFACE-INVENTORY.md (Criterion 1's evidence, including every truth still UNPROVEN and every surface owned by no family), 30-02-TRIAL-RESULTS.md and evidence/30-02/legs.tsv (Criterion 2's evidence, including every UNPROVEN leg), 30-03-CLAIMS-ALLOWED.md, 30-03-CLAIMS-PROHIBITED.md and 30-03-LIMITATIONS.md (Criterion 3's evidence), 30-04-AUTHORITY-PROOF.md (Criterion 4's evidence and its stated ceiling), .planning/phases/28-native-cross-platform-certification/ and 29-supply-chain-release-integrity/ for their phase verdicts IF THEY EXIST — 28 states in advance that it expects its own Criterion 2 to fail on Windows on a carried descendant-reap defect, and 29 has filed six named requirements back onto 28; if either verdict is absent, every criterion depending on it is UNPROVEN and never MET, .planning/REQUIREMENTS.md Traceability table (the recorded status of every prior phase, several Open with named unmet clauses)</read_first>
  <behavior>
    - Each of the four Success Criteria is quoted verbatim from ROADMAP.md and graded with the closed enum, never reworded.
    - Every grade is expressed through the real `wayland-scorecard verify` path, so MET on an unresolved or unproven evidence reference is refused by the tool rather than caught by a reader.
    - A criterion whose evidence depends on a Phase 28 or Phase 29 verdict that does not exist is UNPROVEN, never MET.
    - NOT MET, PARTIAL and UNPROVEN are recorded with the same weight and detail as MET, and none is softened.
    - The positioning packet contains no recommendation, no readiness statement and no market comparison — only what may be claimed, what may not, what is unproven, what it would cost to prove, and the enumerated real-key limits.
    - Every real-key and real-account limit across the whole phase is collected in one place with its exact substitution point.
    - Residuals are filed: CRITICAL and HIGH findings named with their owners, MEDIUM and below to BACKLOG, and any cross-phase request to the seam-request file.
    - The phase goal is graded honestly, and if it was not achieved the verdict says so plainly.
  </behavior>
  <action>Grade the four criteria verbatim. Quote each sentence exactly as ROADMAP.md states it, then state the grade, then attach the evidence that settled it with the exact artifact paths. Do not reword a criterion to fit what was built, and do not grade against the plans — the criteria are the contract.

Express the grading as data, not prose. Build `evidence/30-04/phase-verdict.json` as a scorecard document using the types 30-01 landed, one criterion per entry with its verdict and its evidence references, and drive it through the real `wayland-scorecard verify`. That path already refuses MET when any evidence reference fails to resolve or any referenced measurement is unproven, so a criterion cannot be graded MET on evidence that is not there — the tool enforces it and the reader does not have to. Then render the readable verdict from the same data.

Carry the inherited state honestly. This is the last phase in the program and every open thing lands here. If Phase 28's or Phase 29's verdict does not exist when this runs, every criterion depending on it is UNPROVEN — 30-01's verifier will refuse MET on the unresolved reference, and that refusal is the correct answer rather than an obstacle. Phase 28 states in advance that it expects its own Criterion 2 to fail on Windows on a carried descendant-reap defect; if that has landed, it is evidence here and it is named. Do not grade around it.

Expect NOT MET and do not flinch. Criterion 2 requires trials with pinned baselines and confidence bounds across five dimensions for three tools; if legs are UNPROVEN, the criterion is PARTIAL at best and possibly NOT MET, and that is the honest and useful outcome. Criterion 1 requires per-surface truth refreshed at each phase; a first refresh is not a demonstrated cadence, and saying so is worth more than claiming otherwise. Phase 21 declared its own goal NOT ACHIEVED twice and Phase 27 graded four of five criteria NOT MET, and those grades are why those phases are trustworthy.

Then build the positioning packet, which is the artifact Sean actually decides from. It contains the allowed claims as published, the prohibited claims with the rule that refused each, the limitations with their substitution points, the unproven legs with what it would cost to close them, and the enumerated real-key limits. It contains NO recommendation, NO readiness statement and NO market comparison, and it does not tell Sean what to conclude. Say that constraint at the top of the document so a reader knows the omission is deliberate rather than an oversight.

Collect every real-key and real-account limit across the whole phase in one place: every dimension needing a live provider credential, any peer that could not be provisioned without an account, Sean's approval key as the one substitution that makes frontier positioning reachable, and anything Phases 28 and 29 named that this phase inherits. Each with its exact substitution point, so a reader knows precisely what changes on the day a real key exists. None of them is graded as met.

File the residuals. CRITICAL and HIGH findings are named with their owning phase and their evidence; MEDIUM and below go to `.planning/BACKLOG.md`, non-blocking, and the severity policy is not tightened at the end of a phase — inventing a stricter rule here is what turned Phase 20 into a seventy-four-plan loop lasting two weeks. Any cross-phase request goes into `.planning/SEAM-REQUESTS/30.md` in the form the Phase 21 seam request uses: exact file, exact insertion point, exact content, and what breaks if it is omitted.

Finally, grade the phase goal itself — Sean can make a defensible release-positioning decision from repeated, comparable, independently reviewed evidence — and if that is not true, say so plainly. Records evidence for F30-05 and states the closure position for all five F30 requirements.</action>
  <verify>
    <automated>cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; cargo fmt --all -- --check</automated>
    <automated># FOUR-CRITERION GATE. Exactly four criterion entries, each carrying a grade from the closed vocabulary 30-01 landed. The equality leg means an entry with an invented grade breaks the gate rather than hiding in the file. Red at base — the file does not exist.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; T=.planning/phases/30-continuous-scorecard-frontier-review/evidence/30-04/criteria.tsv &amp;&amp; test -s "$T" &amp;&amp; test "$(/usr/bin/grep -cE '^CRIT-0[1-4]::(MET|MET_WITH_STATED_EXCEPTIONS|PARTIAL|NOT_MET|UNPROVEN)::evidence=[^[:space:]]+$' "$T")" -eq 4 &amp;&amp; test "$(/usr/bin/grep -c '^CRIT-' "$T")" -eq 4</automated>
    <automated># INVENTED-GRADE GATE. The grade an agent would reach for to mean ready-without-proved must appear nowhere in the verdict data. This is a zero-count and would pass against a tree where the file is absent, so it is conjoined with the presence leg that is red at base.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; T=.planning/phases/30-continuous-scorecard-frontier-review/evidence/30-04/criteria.tsv &amp;&amp; test -s "$T" &amp;&amp; test "$(/usr/bin/grep -cF 'ready_for_frontier_positioning' "$T")" -eq 0</automated>
    <automated># EVERY-CRITERION-CAPTURED GATE. Each criterion names a capture that exists and holds real content, so a grade is a record of something that was checked rather than a word that was typed.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; EV=.planning/phases/30-continuous-scorecard-frontier-review/evidence/30-04 &amp;&amp; test -s "$EV/criteria.tsv" &amp;&amp; while IFS= read -r R; do case "$R" in ""|"#"*) continue;; esac; F="$EV/${R##*::evidence=}"; test -s "$F" || exit 1; test "$(/usr/bin/wc -l &lt; "$F" | tr -d ' ')" -ge 3 || exit 1; done &lt; "$EV/criteria.tsv"</automated>
    <automated># REAL-KEY-LIMITS GATE. Every point at which a real key or account was the only missing input must be enumerated in one place with its substitution point. Four is the floor: the live-provider credential, the peer account, Sean's approval key, and the inherited Phase 28/29 limits. Red at base.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; T=.planning/phases/30-continuous-scorecard-frontier-review/evidence/30-04/real-key-limits.tsv &amp;&amp; test -s "$T" &amp;&amp; test "$(/usr/bin/grep -cE '^KEY-[0-9]+::.+::substitution=[^[:space:]]+' "$T")" -ge 4</automated>
    <automated># NO-POSITIONING-SENTENCE GATE. The packet is evidence Sean decides from, not a decision. This asserts the readiness vocabulary an agent would reach for is absent from the packet's machine-readable index. It is a zero-count, so it is conjoined with the presence leg that is red at base — otherwise it would pass against a tree where the packet does not exist at all.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; T=.planning/phases/30-continuous-scorecard-frontier-review/evidence/30-04/packet-index.tsv &amp;&amp; test -s "$T" &amp;&amp; test "$(/usr/bin/grep -ciE 'ready to ship|ready for launch|frontier-ready|we recommend positioning' "$T")" -eq 0</automated>
    <automated># INPUTS-FROZEN GATE. The pass that grades the evidence is not the pass that repairs it. All three prior plans' evidence directories and modules are untouched, conjoined with this task's completion anchor so a clean tree alone cannot pass at base.
cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; P=.planning/phases/30-continuous-scorecard-frontier-review &amp;&amp; test -s "$P/evidence/30-04/criteria.tsv" &amp;&amp; test "$(/usr/bin/git status --porcelain --untracked-files=all -- "$P/evidence/30-01" "$P/evidence/30-02" "$P/evidence/30-03" crates/wcore-eval-scenarios/src/scorecard.rs crates/wcore-eval-scenarios/src/frontier_trials.rs crates/wcore-eval-scenarios/src/claims.rs Cargo.toml Cargo.lock | /usr/bin/wc -l | tr -d ' ')" -eq 0</automated>
    <automated># AUTHORITATIVE VERDICT-VERIFY GATE — the verdict graded through the real tool, so MET on an unresolved or unproven evidence reference is refused by code rather than caught by a reader. Remote status captured first; no pipe on this line.
SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); LOG=$(mktemp); ssh -o BatchMode=yes hetzner-dsm "export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland &amp;&amp; git fetch --all &amp;&amp; git checkout --detach $SHA &amp;&amp; cargo build --release --locked -p wcore-eval-scenarios &amp;&amp; ./target/release/wayland-scorecard verify --document .planning/phases/30-continuous-scorecard-frontier-review/evidence/30-04/phase-verdict.json --root ." &gt; "$LOG" 2&gt;&amp;1; rc=$?; tail -40 "$LOG"; rm -f "$LOG"; test "$rc" -eq 0</automated>
    <automated># MET-IS-STILL-EXPENSIVE GATE. 30-01's asymmetry must still hold at the end of the phase: a criterion graded MET on a deliberately broken evidence reference must be REFUSED. The remote side decides — setup failures exit non-zero, an unexpected acceptance exits 9, and only the refusal exits 0 — because asserting a non-zero status locally would pass whenever ssh was down. No pipe on this line.
SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); LOG=$(mktemp); ssh -o BatchMode=yes hetzner-dsm "export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland &amp;&amp; git checkout --detach $SHA &amp;&amp; test -x ./target/release/wayland-scorecard &amp;&amp; sed 's#\"verdict\": *\"NOT_MET\"#\"verdict\": \"MET\"#' .planning/phases/30-continuous-scorecard-frontier-review/evidence/30-04/phase-verdict.json > /tmp/f30-forced-met.json &amp;&amp; test -s /tmp/f30-forced-met.json &amp;&amp; if ./target/release/wayland-scorecard verify --document /tmp/f30-forced-met.json --root .; then echo UNEXPECTED_MET_ACCEPTED; exit 9; else echo REFUSED_AS_REQUIRED; exit 0; fi" &gt; "$LOG" 2&gt;&amp;1; rc=$?; tail -30 "$LOG"; rm -f "$LOG"; test "$rc" -eq 0</automated>
    <automated># GATE, NOT A PRINTOUT — remote status captured first, no pipe on this line.
SHA=$(cd /Users/seandonahoe/dev/waylandcore-ferrox &amp;&amp; /usr/bin/git rev-parse HEAD); LOG=$(mktemp); ssh -o BatchMode=yes hetzner-dsm "export PATH=/root/.cargo/bin:\$PATH; cd /root/wayland &amp;&amp; git checkout --detach $SHA &amp;&amp; cargo clippy -p wcore-eval-scenarios --all-targets -- -D warnings &amp;&amp; cargo nextest run -p wcore-eval-scenarios --no-fail-fast" &gt; "$LOG" 2&gt;&amp;1; rc=$?; tail -40 "$LOG"; rm -f "$LOG"; test "$rc" -eq 0</automated>
  </verify>
  <done>All four Success Criteria are quoted verbatim from ROADMAP.md and graded with the closed enum, each naming a capture that exists and holds real content. The grading runs through the real `wayland-scorecard verify` path, and the asymmetry is re-proved at the end of the phase: forcing a NOT_MET grade to MET against a reference that does not resolve is refused, with the gate structured so ssh failing cannot satisfy it. Any criterion depending on a Phase 28 or Phase 29 verdict that does not exist is UNPROVEN and not MET. NOT MET, PARTIAL and UNPROVEN are recorded with the same detail as MET and none is softened. The positioning packet contains no recommendation, no readiness statement and no market comparison, states that omission as deliberate at the top, and its machine-readable index is gate-checked free of readiness vocabulary. Every real-key and real-account limit across the phase is enumerated in one place with its exact substitution point, at least four of them, and none is graded as met. CRITICAL and HIGH findings are named with their owners; MEDIUM and below are filed to BACKLOG without tightening the severity policy; any cross-phase request is filed to the seam-request file in the established form. The phase goal is graded honestly and, if it was not achieved, the verdict says so plainly. All three prior plans' artifacts are gate-checked frozen.</done>
</task>

</tasks>

## What this plan does NOT change (scope fence)

- **The positioning decision itself — not made here, by construction.** This plan produces the packet Sean decides from. Frontier positioning is a member of the reserved-action enum and is structurally unreachable under the committed placeholder trust root.
- **30-01's, 30-02's and 30-03's artifacts and modules — inputs, gate-checked frozen.** The pass that grades the evidence must not be the pass that repairs it. A defect in them is a finding in the verdict with a severity and an owner.
- **`crates/wcore-cli/src/plugin/index.rs` — READ as the fail-closed precedent, never edited**, and its `F-021` and `INDEX_PUBKEY_HEX` counts are regression-guarded so the precedent cannot be dissolved while being copied.
- **`crates/wcore-eval-scenarios/src/receipt.rs`, `src/receipt_policy.rs`, `bin/wayland-receipt.rs`, `src/release_integrity.rs`, `src/e5_soak.rs`, `src/process_tree.rs`, `src/fixtures/openai.rs` — untouched.** Phases 28 and 29 own them.
- **`crates/wcore-cli/src/lib.rs` and `src/main.rs` — untouched.** Every lane shares these two files.
- **Workspace-root `Cargo.toml` and `Cargo.lock` — untouched.** No new dependency.
- **`.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` — the criteria are QUOTED, never reworded, and requirement status is stated in the verdict rather than by editing the traceability table from inside the phase being graded.**
- **No reserved action.** No push to main, no merge, no pull request, no tag, no release, no deployment, no issue closure, no deletion of a retained evidence ref, no positioning. The lane branch is committed and pushed and that is not a reserved action.
- **No real key, no real account, no real credential**, and specifically Sean's approval key is never obtained, requested or simulated.
- **The severity policy is not tightened at the end of the phase.** CRITICAL and HIGH are fixed or disproved with executable evidence; MEDIUM and below are filed to BACKLOG, non-blocking.
- **No test is deleted, weakened, re-gated, `#[ignore]`d or `#[allow]`ed, and no grade is softened after seeing that a criterion fails.**

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reserved action ← an agent under deadline pressure | The move is always to add an enum member that routes around the wall; this program has already watched it happen once |
| approval ← the party it authorises | An approval that establishes its own authority is not an approval; the trust root must arrive independently |
| one action's approval ← another action's slot | Approving a documentation push must never be replayable as approving a release or a positioning statement |
| verdict ← the pass that produced the evidence | A grader who can repair the evidence he is grading is grading his own work |
| MET ← an evidence reference that does not resolve | A citation reads exactly like a resolved object; only resolution tells them apart, and the tool must enforce it |
| this lane's claim of restraint ← the GitHub remote | The remote cannot be read without a credential this lane must never hold, so the claim has a ceiling that must be stated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-04-01 | Elevation of Privilege | An agent invents a reserved-action member, a principal, or a verdict grade to route around Sean's approval | critical | mitigate | `ReservedActionV1` is a closed nine-member enum backed by a compiler-checked exhaustive array; `PrincipalV1` has exactly one member; `an_invented_reserved_action_named_termination_state_4_fails_to_deserialize` asserts the exact string an agent on this program actually invented, and a gate proves that string is absent from the production module |
| T-30-04-02 | Spoofing | An agent records its own approval for a reserved action | critical | mitigate | There is no agent principal and no self-approval principal to write; both are gate-checked absent from the module and both are proved to fail deserialization by name |
| T-30-04-03 | Elevation of Privilege | Frontier positioning is treated as reached because the clean-room mechanism completed | critical | mitigate | The committed approval root is an all-zeros placeholder that refuses every approval with an explicit error naming its substitution point, copying `IndexVerifier::bundled()`'s proven shape; BOTH runs are required and reported — the throwaway-key acceptance and the placeholder refusal — and the plan states that reporting the first without the second is the drift itself |
| T-30-04-04 | Tampering | An approval minted for one reserved action is replayed into another | high | mitigate | Nine distinct signature domains, one per action, counted distinctly by gate; `an_approval_minted_for_a_source_push_does_not_verify_as_a_release` proves the refusal with a pristine control accepted first |
| T-30-04-05 | Tampering | An approval is moved onto a different subject | high | mitigate | Every approval binds the subject's digest, and `an_approval_moved_onto_a_different_subject_digest_is_refused` proves it |
| T-30-04-06 | Repudiation | A criterion is graded MET on evidence that does not resolve, at the end of a long phase when nobody re-checks | high | mitigate | Grading runs through 30-01's real verifier, which refuses MET on an unresolved or unproven reference, and a hardware gate re-proves that asymmetry by forcing a NOT_MET to MET and asserting the refusal |
| T-30-04-07 | Tampering | The grading pass repairs the evidence it is grading | high | mitigate | All three prior plans' evidence directories and modules are gate-checked untouched, conjoined with this task's completion anchor; a defect in them is a finding with an owner rather than an edit |
| T-30-04-08 | Repudiation | The lane's restraint is claimed more broadly than it can be proved, implying the remote was checked | high | mitigate | The audit uses a dedicated `NOT-OBSERVABLE-HERE` status, a gate requires at least one such determination, and the document states precisely that the claim covers this repository's reach and that the remote-side confirmation is Sean's |
| T-30-04-09 | Information Disclosure | The throwaway approval seed reaches an argv, a log or standard output | high | mitigate | Seeds are generated at run time into a temporary directory, written only to owner-only files, read only from standard input, wiped after use and never printed; a gate asserts the binary declares no seed, private-key or secret argument |
| T-30-04-10 | Denial of Service | A verifier so strict that it refuses every approval passes a rejection-only suite while proving nothing about whether approval can ever be honoured | high | mitigate | `frontier_positioning_verifies_under_a_throwaway_root_generated_at_run_time` is a mandatory positive control, run first on hardware and captured before the refusal is run |
| T-30-04-11 | Spoofing | The positioning packet contains a sentence that reads as a positioning recommendation | high | mitigate | The plan forbids recommendation, readiness and market-comparison language; the packet states the omission as deliberate at the top; and a gate asserts the readiness vocabulary is absent from the packet's machine-readable index, conjoined with a presence leg so it cannot pass vacuously |
| T-30-04-12 | Tampering | The severity policy is tightened at the end of the phase, converting MEDIUM findings into blockers | medium | mitigate | The policy is restated verbatim and the plan names the measured cost of the last time it was tightened — a seventy-four-plan loop lasting two weeks |
| T-30-04-SC | Tampering | npm/pip/cargo installs | low | accept | This plan adds no dependency; the workspace-root manifest and the lockfile are gate-checked untouched and every crate it uses is already a dependency of `wcore-eval-scenarios`. No install task exists here |
</threat_model>

<verification>
Local gates (Mac, source level only — no Cargo here beyond `fmt`): `cargo fmt --all -- --check` clean; the authority module exists and is declared once; four of the nine reserved actions are present by name with a compiler-checked exhaustive array and the invented member absent from the production module; the principal enum exists and neither an agent nor a self-approval member appears; nine DISTINCT signature domains exist; the bundled placeholder exists and the `IndexVerifier::bundled()` precedent is intact at its measured counts of six and six; the seven named contract tests plus the positive control are present; the binary declares no seed, private-key or secret argument; the audit ledger has at least eight well-formed determinations each naming a capture that exists, including at least one `NOT-OBSERVABLE-HERE`; no local main or master contains this lane's HEAD; no tag points at it; three named retained evidence refs resolve and the non-branch ref count has not fallen below thirty-seven; exactly four criterion rows exist with grades from the closed vocabulary and the total row count equals the well-formed count; the invented grade appears nowhere in the verdict data; at least four real-key limits are enumerated with substitution points; the packet index is free of readiness vocabulary; and all three prior plans' evidence directories and modules are untouched.

Every remote gate captures the remote exit status into `rc` BEFORE any filtering and asserts `test "$rc" -eq 0`. No line containing `ssh` carries a pipe. Two gates prove a REFUSAL and neither asserts a non-zero status locally, because that form passes when ssh is down, when the checkout fails and when the binary is missing — none of which say anything about the refusal. Instead the remote side decides: setup failures exit non-zero, an unexpected acceptance exits 9, and only the refusal exits 0.

Authoritative gates (real hardware). Hetzner Linux: clippy clean at `-D warnings`; the targeted suite stated as a delta with every residual failure named; the throwaway-root approval ACCEPTED; the same approval REFUSED under the committed placeholder root; the phase verdict driven through the real `wayland-scorecard verify`; and the MET asymmetry re-proved by forcing a NOT_MET to MET and asserting the refusal.

**Honest limits of this evidence, stated rather than hidden.** Three limits bound this plan. First, the reserved-authority mechanism is proved in a clean room where this lane generates the throwaway key itself — that proves the MECHANISM, and the plan requires both sentences to be stated so it is never mistaken for having an approval. Second, this repository has no remote-tracking refs, so no gate here can observe the GitHub remote; the restraint claim is precise about covering this repository's reach only, and the remote-side confirmation is Sean's. Third, the verdict grades what this phase measured; where Phases 28 and 29 have not landed their own verdicts, the dependent criteria are UNPROVEN and the tool enforces that rather than a reader remembering it.

Known unknowns to record, not to resolve here: whether Phase 28 and Phase 29 will have landed verdicts when this runs, which changes several grades and is outside this lane's control; whether Phase 28's predicted Windows failure on the carried descendant-reap defect materialises, which it states in advance that it expects; whether the six requirements Phase 29 filed back onto 28 are accepted, which is 28's decision; and what Sean concludes from the packet, which is the one thing this phase is built never to answer.
</verification>

<success_criteria>
- The nine reserved actions are a closed enum backed by a compiler-checked exhaustive array; an invented member fails to deserialize, and the exact string an agent on this program actually invented is asserted as a rejection in a named test and is absent from the production module.
- The principal enum has exactly one member; an agent principal and a self-approval principal both fail to deserialize and neither appears anywhere in the module.
- Nine distinct per-action signature domains exist, and an approval minted for one action does not verify as another.
- An approval binds its subject's digest and cannot be moved onto a different subject; an unknown key id is refused rather than trusted.
- The committed approval trust root is an all-zeros placeholder that refuses every approval with an explicit error naming its substitution point, and the `IndexVerifier::bundled()` precedent it copies is gate-checked intact.
- BOTH authority runs are captured on real hardware: the throwaway-root acceptance proving the mechanism works, and the placeholder-root refusal proving positioning is unreachable here. Both sentences are stated plainly.
- No seed reaches an argv or standard output, gate-checked, and Sean's approval key is never obtained, requested or simulated.
- The audit records at least eight determinations, each with a real capture: no local main or master contains this lane's HEAD, no tag points at it, the named retained evidence refs resolve, the ref count has not fallen — and the remote-side limit is recorded as its own `NOT-OBSERVABLE-HERE` determination rather than claimed or omitted.
- All four Success Criteria are quoted verbatim and graded with the closed enum through the real verifier, with any criterion depending on an absent Phase 28 or 29 verdict graded UNPROVEN and never MET.
- The MET asymmetry is re-proved at the end of the phase by forcing a grade upward against an unresolved reference and capturing the refusal, with the gate structured so ssh failing cannot satisfy it.
- The positioning packet contains no recommendation, no readiness statement and no market comparison, and says so at the top.
- Every real-key and real-account limit is enumerated in one place with its exact substitution point, and none is graded as met.
- Residuals are filed with owners and severities, the severity policy is not tightened, and the phase goal is graded honestly — including plainly saying so if it was not achieved.
</success_criteria>

## Artifacts this plan produces
- `crates/wcore-eval-scenarios/src/reserved_authority.rs` — the closed action and principal enums, the nine domains and the fail-closed bundled root.
- `crates/wcore-eval-scenarios/tests/reserved_authority_contract.rs` — the contract suite including the invented-action refusal and the mandatory positive control.
- `.planning/phases/30-continuous-scorecard-frontier-review/30-04-AUTHORITY-PROOF.md` — both authority runs and the no-reserved-action audit with its stated ceiling.
- `.planning/phases/30-continuous-scorecard-frontier-review/30-04-POSITIONING-PACKET.md` — the evidence Sean decides from, containing no decision.
- `.planning/phases/30-continuous-scorecard-frontier-review/30-PHASE-VERDICT.md` — the four criteria quoted verbatim and graded, with the phase goal graded honestly.
- `.planning/phases/30-continuous-scorecard-frontier-review/evidence/30-04/` — the audit ledger, the criteria index, the verdict document, the real-key limits and the packet index.
- `.planning/SEAM-REQUESTS/30.md` and `.planning/BACKLOG.md` — the filed residuals.
- `30-04-SUMMARY.md` recording both runs, the audit, the verdict and the termination state.

<output>
Create `.planning/phases/30-continuous-scorecard-frontier-review/30-04-SUMMARY.md` using the standard summary template. Record: each behavior's RED-before-GREEN evidence; the nine reserved actions and nine domains as landed; both authority runs verbatim, the throwaway-root acceptance first and the placeholder refusal second, with both exit statuses and the exact refusal text including its substitution point; the two sentences stated plainly — the mechanism works, and no approval exists in this lane; every audit determination with its command and output, and the `NOT-OBSERVABLE-HERE` remote-side limit stated precisely; all four Success Criteria quoted verbatim with their grades and the evidence that settled each; the result of forcing a grade upward and the refusal that answered it; the phase goal graded honestly, said plainly if it was not achieved; the closure position for all five F30 requirements; the complete enumerated list of real-key and real-account limits with substitution points; every CRITICAL and HIGH finding with its owner and every MEDIUM and below filed to BACKLOG; any cross-phase seam request filed; the Hetzner clippy and targeted-suite delta with every residual failure attributed; confirmation that no reserved action was taken, that the prior plans' artifacts are frozen, and that the shared CLI fence files and the lockfile are untouched; the recorded unknowns; and which of the three termination states the plan ended in. State explicitly that this plan made no positioning decision and contains no recommendation, because that omission is the requirement rather than an oversight.
</output>

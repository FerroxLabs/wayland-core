LEG LEG-14 - openclaw / cost - UNPROVEN
==============================================================================
BLOCKER AND SUBSTITUTION POINT:

PEER NEVER DELIVERED TO THE BUILD HOST -- identical blocker to openclaw:correctness. The
bundle is truncated at 164,766,720 of 392,186,966 bytes. No substitute version was measured.
NOT a Wayland win; no comparison exists. SUBSTITUTION POINT: complete the resumable
transfer, clone at the pin, install from its own lockfile, re-run unchanged.

An UNPROVEN leg is NOT a win for any tool. No comparative result exists for
any dimension in which a tool did not run: ComparativeResultV1 cannot be
constructed without every compared tool's measurement.

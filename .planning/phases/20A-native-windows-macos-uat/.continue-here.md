---
context: phase
phase: 20A-native-windows-macos-uat
live_plans: 4
completed_plans: 4
remaining_plans: 0
next_plan: null
status: complete
completed_at_sha: 9821ef7603ac1e687b600cda591af1657c883484
completed_at_tree: 0a1267a990f3b512782916b6ed26501d0db39222
dispatched_run: 30184651330
last_updated: 2026-07-26
---

# Phase 20A — Native Windows/macOS UAT Repair — **COMPLETE**

**Phase 20A is complete.** The terminal plan `20A-04` closed green on 2026-07-26 at exact SHA
`9821ef7603ac1e687b600cda591af1657c883484` (tree `0a1267a990f3b512782916b6ed26501d0db39222`,
branch `plan/f20-unified-audit-repair`, annotated tag `f20a-candidate-9821ef76`, local
`refs/f20a/candidate`), proved by ONE newly dispatched, Sean-authorized run:

```
nightly-windows-soak  run 30184651330  (workflow_dispatch, --ref f20a-candidate-9821ef76)
  → completed / success        2026-07-26T02:30:03Z → 02:48:08Z

F20_NATIVE_WINDOWS_ACCEPTANCE=PASS commit=9821ef76… tree=0a1267a9… nonce=96c91107…
  job 89747993276  runner ferrox-win-msvc (SEANDESKTOP)      6/6 targets PASS
F20_NATIVE_MACOS_ACCEPTANCE=PASS   commit=9821ef76… tree=0a1267a9… nonce=96c91107…
  job 89747992986  runner f20-macos-ephemeral-1d053640       8/8 targets PASS
```

Zero red targets on either platform. Both candidate jobs ran
`Assert checkout is the authorized candidate` against `f20_expected_sha=9821ef76…`
**before** any build work, and pinned `actions/checkout` to that SHA — the evidence cannot have
come from a different tree. All three Success Criteria are met. Full evidence, per-target
results, and the requirement dispositions are in `20A-04-SUMMARY.md` **§13** (which supersedes
§1, §8, §9 and §12.4 of that same file — those are retained as history, not deleted).

## The four plans and how each ended

| Plan | Outcome |
|------|---------|
| `20A-01` | Terminated **compile-blocked**, then cleared. Established the true native baseline: the Windows-only/macOS-only compile verdict at one exact SHA, `wcore-sandbox` + the twelve orphaned `live_fs_acl` ACL tests wired into a runner, CI triggered on this branch, four Windows suites re-measured with every failure named. |
| `20A-02` | **Bind landed.** The AppContainer child's working directory is now bound to the RETAINED delegated workspace authority under an OS-enforced name pin, without weakening the anti-swap guarantee. Unblocked the 7 swarm + 4 agent + 4 `dispatch_smoke` failures that shared one cause. |
| `20A-03` | **REFUTED-NO-DEFECT.** The dirty-checkout / EOL symptom is a TEST FIXTURE artifact, not a product defect. No production file changed. The in-tree `.gitattributes` `eol=lf` rule was measured to override `core.autocrlf` and survive `GIT_ATTR_NOSYSTEM=1` — vindicating the brief's mechanism, refuting its conclusion. |
| `20A-04` | **Dispatched green.** Sealed one exact candidate, re-proved 6/6 locally at the final tip, then fired run `30184651330` on Sean's authorization: Windows 6/6 + macOS 8/8, both acceptance markers at the same commit/tree/nonce. Effective termination state **1**. |

## Requirements — 11 of 15 complete, 4 deliberately left OPEN

Complete, each bound to `9821ef76` with named per-target evidence in `REQUIREMENTS.md`:
**r1, r3, r4, r5, r6, r7, r9, r10, r11, r14, r15.**

Open, and NOT to be quietly ticked later without new evidence:

- **r2** — only 1 of 3 acceptance clauses proven. `deny_ace_still_blocks_granted_read` and
  `normal_sid_only_grant_is_denied` never ran: their sole runner (the `windows-live-acceptance`
  job) is gated `if: inputs.f20_candidate != 'true'` and is skipped by construction in candidate
  mode. Ten `live_fs_acl` ACL tests still have no observed green at the sealed SHA.
- **r8** — the wrong-OS anti-drift guard ran and admitted all six OS-specific targets, but its
  *rejection* path has never been demonstrated and there is no negative test for it.
- **r12** — native leg closed; there is no fresh `20-16` at this SHA (the only one is bound to
  `6937ef61` and explicitly defers `native_macos`/`native_windows`), and no `20-17` re-prep here.
- **r13** — no review gate ran against this candidate, so no schema-validated artifact exists for
  it; and the prior gap is still measurable (2 reviewers claimed, 1 `reviewer_id` recorded).

None of these four is a failing test. Each is an **unexercised** path. None is in Phase 20A's
Success Criteria, which is why the phase closes while they stay open.

## Known-red, non-gating — do not rediscover these

Recorded in full in `20A-04-SUMMARY.md` §13.7. In one line each:

- `wcore-sandbox::live_integrity::live_future_drop_reaps_descendant_job_tree` — Windows,
  deterministic; escalated because every candidate fix changes what the sandbox permits.
- `snapshot.rs::windows_private_dacl_accepts_restrictive_deny_ace` and
  `::windows_private_dacl_rejects_null_empty_and_broad_allow` — `WRITE_DAC` reopen error 5; fail
  identically at the parent commit; unit tests not reached by the four suites.
- `worker_runtime_limits::multi_worker_output_exhaustion_fails_without_retaining_buffers` — ~35s
  against a 20s budget. The timeout was deliberately NOT raised.
- `required_live_windows_public_dispatch_bash_confines_parent_and_descendants` — bash cannot run
  under AppContainer at all (msys needs `\BaseNamedObjects`, which AppContainer confines by
  construction). The test now asserts the real fail-closed contract and passes under its new name.
- Under parallel load `admit_delegated_backend` rejects with `sandbox backend fail_closed`. The
  proof is green because `--nocapture` forces serial execution. Fail-closed, so safe — but
  **concurrent dispatch degrades under load**, and the green depends on serialisation.

## Follow-ups for the NEXT candidate (not fixed here)

1. `scripts/f20-native-macos-proof.sh:134` runs an unconditional `docker pull`. It should
   `docker image inspect` first — the unconditional pull is why the macOS leg failed four times
   on a locked keychain.
2. The `f20-no-ambient-secrets` runner label is inaccurate on this host. The runner executes as
   Sean's user (`Machine name: Seans-MacBook-Pro`) with reach over `~/.ssh`, `~/.aws` and an
   unlocked keychain. No GitHub secrets are exposed to the candidate jobs — but that is a
   narrower claim than the label makes. **The real fix is a dedicated macOS runner account**, not
   a relabel.

## Standing constraints (still true)

- The primary checkout `/Users/seandonahoe/dev/waylandcore` is heavily dirty. Phase 20 and 20A
  work happened in `/Users/seandonahoe/dev/waylandcore-ferrox`.
- Cargo never runs on this Mac. `cargo fmt --all` is the only working cargo command here;
  `hetzner-dsm:/root/wayland` is the only place the workspace compiles, and `SEANDESKTOP` the
  only place the Windows proof runs.
- In `waylandcore-ferrox` the remote named `origin` is a **stale local worktree**. The real
  remote is `gh`. Never fetch or reset against `origin` here.
- No push to `main`, merge, PR, tag, release, or issue closure without Sean's explicit
  authorization. The native-proof dispatch is likewise Sean-gated — `30184651330` was fired on
  his instruction, and every prior authorization digest is spent and void.

## Do not

- Create `20A-05` or any new Phase-20A plan. The 4-plan hard cap held; the phase is closed.
- Re-plan, re-architect, or re-scope Phase 20A.
- Re-dispatch `nightly-windows-soak` in candidate mode to "confirm" this result. The evidence is
  bound to one authorized run; re-running spends an authorization for nothing.
- Tick r2, r8, r12 or r13 without new evidence that exercises the specific clause named above.
- Resume the archived native chain plan-by-plan — the 51 plans in
  `phases/20-transactional-delegated-mutation/archive/native-split/` are history, not a queue.

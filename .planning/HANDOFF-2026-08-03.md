# HANDOFF — wayland-core core lane — 2026-08-03

Setup: `export WL_LANE=core`; `gh auth switch --user FerroxLabs` before EVERY gh op.
PRs `-R FerroxLabs/wayland-core --base main`. Issues on `FerroxLabs/wayland`.

---

## 0. THE VERDICT — are we ready for a candidate?

**Yes for a release candidate, after closing ONE more defect. Not for GA.**

**Integration is `9c8a43a9` — PROMOTED. The release blocker is on the branch.**
Gate on it: `FMT_OK · LOCKED_OK · CLIPPY_RC=0 (0 diagnostics) ·
CLIPPY_POSITIVE_CONTROL_RC=101 · PID_GATE_RC=0 · ratchet 31/31 ·
13898 tests run: 13898 passed, 75 skipped`. `Lint` green on that SHA; full CI queued.

The bar Sean set: **nothing lies to the user, and nothing loses their data.**

- **"Loses their data" — CLOSED.** Credential splice, keyless-host journal amnesia,
  Windows log rotation dropping every record past 5 MiB, Grep reporting "no
  matches" for a file it could not read. All fixed and gated.
- **"Lies to the user" — one genuine hole: #170.** A user sets
  `memory.enabled = false` and memory still records. `want_memory =
  config.memory.enabled || skills_lifecycle_enabled` (`bootstrap.rs:1794`), and
  skills-lifecycle **defaults ON** — so a stock install still opens a real Memory,
  still registers durable write tools, and `fire_auto_memorize` consults **no
  config field at all**. That is the product telling a user a privacy setting took
  effect when it did not. **Close this before the tag** — it is the only open item
  that FAILS the bar rather than being a disclosable limitation.
- **#172 — disclose, do not block.** The refresh POST is still only in-process
  single-flighted; two processes can both POST and one gets `invalid_grant`. A
  failure, not a lie: recoverable, pre-existing, honestly describable.

---

## 0b. FINISH LINE — in order

1. **One lane on #170.** Diagnosis complete (§3); the both-directions bar is
   already written.
2. **Read the three-platform CI on `9c8a43a9`.** First branch state that can get a
   real Windows verdict — via the hosted `windows-latest` fallback merged tonight,
   because both self-hosted Windows runners advertise `busy` and serve zero jobs.
3. **RE-DERIVE the per-platform table in `KNOWN-ISSUES-rc.md`.** Its numbers came
   from a `report` job that was **silently discarding two of three platforms**
   (`merge-multiple: true`; proven on run 30699019736 — three artifacts uploaded,
   `junit report count : 1`). The bug is fixed; the table was never redone, and
   those are the numbers a user reads.
4. Add #170 and #172 to `KNOWN-ISSUES-rc.md`. `CHANGELOG.md` is written, honest,
   and already points readers there.
5. Final contract regen, key-diffed.
6. **Tag is Sean's:** `gh workflow run release.yml -f tag_name=v0.12.26-rc.1`.

Certification staleness stays true and disclosed: it passes, it is ~2,100 commits
behind, and that is exactly why this is a candidate and not a GA.

---

## 1. RULES THAT COST TIME (cumulative — read before acting)

1. **A commit that is not pushed does not exist.** Wave 1 lost 9 of 10 lanes.
   Tonight a lane stalled with the only copy of 267 new lines in its worktree
   because the network died. Push after every commit; verify the SHA resolves.
2. **Check hetzner disk before believing a test failure.** Admission control
   refuses near-full and it looks exactly like flakiness. Hit 92% tonight;
   reclaimed to 81%. `multi_worker_output_exhaustion` is disk-dependent, not flaky.
3. **NEVER run cargo on the Mac.** `cargo fmt` only. All Rust `target/` dirs were
   deleted from the Mac (6 GiB → 83 GiB free) precisely because they are dead
   weight there.
4. **The gate must run in `/root/orch-gate`, nowhere else.** A lane pointed
   `CARGO_TARGET_DIR` at `/root/wayland/target` and poisoned a gate with a
   foreign binary. The gate now `reset --hard`s before AND after, prints
   `dirty_before`/`dirty=` and aborts loudly (`GATE_ABORT`) if checkout fails —
   it silently did not run once tonight.
5. **Windows self-hosted runners advertise `busy` and serve zero jobs.** Verified
   across all in-progress jobs: not one names them. Sean-only fix. A hosted
   `windows-latest` leg now exists as a second supplier.
6. **`-m "…"` with backticks gets command-substituted by zsh.** Use `-F file` or a
   heredoc for every commit message.
7. **`codex exec` hangs unless stdin is closed** — append `< /dev/null`. Cost two
   audit legs tonight.
8. **The clippy gate now carries a positive control** (`-D clippy::pedantic` →
   101). The previous known-bad control had silently gone green, leaving six clean
   runs unproven. Keep it — it is what makes `CLIPPY_RC=0` mean anything.
9. **Verify a `-q` push.** A handoff commit bounced as a non-fast-forward and `-q`
   hid it. Integration was intact only because git refused.
10. Contract regeneration is **orchestrator-only**, and always diff it **key by
   key** before accepting. Four regens tonight; every one verified zero keys
   removed.

---

## 2. WHAT LANDED (14 lanes, all gated)

| Lane | What a user gets |
|---|---|
| `win-stdout-capture` | **Every Windows tool returned EMPTY stdout.** `collapse_cr_lines` ate the `\r` of every CRLF terminator, and `compact_output` runs on every tool result. Bash, Git, Grep, aws, gcloud, kubectl. Not the sandbox — the default compaction path. My AppContainer hypothesis was refuted; reproduced on Linux with `NoSandboxBackend` |
| `cred-splice-fix` | **RELEASE BLOCKER.** Two processes could splice two secrets together, no crash needed, both returning `Ok` — burning both single-use refresh tokens |
| `win-readonly-vacuity` | A hook quoting a path made **every tool call in the session** fail on Windows. Third site of a defect already fixed twice elsewhere |
| `oauth-finish` | OAuth tokens written in **cleartext** while docs called them encrypted. Migration cliff is now a hard refusal |
| `memory-optout` | A cloned repo's **empty `[memory]` block** silently switched a user's memory back on |
| `spend-ceiling` | Fresh-session-per-process had **no** ceiling; the documented workaround **bricked every session** |
| `channel-health-cred` | Slack read `Healthy` while every send failed |
| `durable-option-d` | Keyless host keeps its journal; a session whose key is gone is **refused**, not handed a `ready` it cannot back |
| `ready-session-id` | `ready.session_id` no longer vanishes from the wire |
| `log-hygiene` | Per-turn stderr −78%, record preserved |
| `identifier-scrub` | Personal Matrix ID + **joinable** room ID out of the tree |
| `ci-windows-fallback` | Second Windows supplier; **`report` was silently discarding 2 of 3 platforms** |
| `doc-truth-residual` | Four shipped falsehoods |
| `concurrency-safe-truth` | `partition()` finally has a test with real shared state |

---

## 3. OPEN — the two I would not tag silently

**#170 P0 — `memory.enabled = false` does not stop memory recording.**
`want_memory = config.memory.enabled || skills_lifecycle_enabled`
(`bootstrap.rs:1794`) and skills-lifecycle **defaults ON**. So a stock install
still opens a real Memory, still registers durable write tools, and
`fire_auto_memorize` consults **no config field at all**. The merged config fix is
necessary but NOT sufficient. Needs its own lane; both directions must be proven
(opt-out records nothing AND a default install still records).

**#172 — refresh POST still only in-process single-flighted.**
Plan is **`.planning/PLAN-172-refresh-single-flight.md` (v3, four-leg cross-audit:
Gemini REJECT, Kimi + Codex SOUND-WITH-FIXES, internal).** Read §4.4 first — the
failure policy was reversed TWICE. Final answer: on lock timeout, reload; if
nothing appeared, **fail retryably, never POST unlocked** (a replayed single-use
token can make a compliant provider revoke the whole grant).

Lane stalled when the network died. Work rescued at **`93432b1f`** on
`lane/refresh-single-flight` — **committed locally, in `/root/rsf.bundle` on
hetzner, and in hetzner's `orch-gate` object store. NOT yet on GitHub.** Push it.
It is **incomplete, unproven, DO NOT MERGE** — no mutants, and it predates the v3
reversal, §4.9 (logout resurrection) and §4.10 (cancellation-safety).

Other open items worth reading before the tag: #171 (splice — FIXED, in the
unpromoted stack), #173 (TUI never tells a keyless operator replay is off), #168
(a test mutates committed evidence), #169 (`office_runtime.rs` dead but carrying
the doc_tool anti-pattern).

---

## 4. BEFORE THE TAG

1. Push `9c8a43a9` (§0).
2. Watch CI on the promoted integration — the hosted-Windows leg gives a Windows
   verdict the self-hosted pool cannot.
3. **Re-read `KNOWN-ISSUES-rc.md` against the final SHA.** Its per-platform test
   table came from a `report` job that was **silently discarding two of three
   platforms** (`merge-multiple: true`, proven on run 30699019736 — three
   artifacts uploaded, `junit report count : 1`). Those numbers need re-deriving.
4. Add #170 and #172 to it. `CHANGELOG.md` is written, honest, and already points
   readers at that file.
5. Final contract regen, key-diffed.
6. Tag is **Sean's**: `gh workflow run release.yml -f tag_name=v0.12.26-rc.1`.

---

## 5. CORRECTIONS I MADE TONIGHT

Five lanes told me a task description was stale. **All five were right.** Treat
every remaining task body as a hypothesis, not a fact.

- I merged the credential store **before its verifier ran**; the verifier then
  found the splice. The merge gate's 13,845 passing tests told me nothing about
  it — only a scheduled race did.
- I told `win-readonly-vacuity` the answer was one of two things. **Both were
  wrong**; it found a third.
- I told `win-stdout-capture` to suspect AppContainer. **Refuted.**
- My plan v1 for #172 was rejected by the panel; v2 was corrected again by Codex.
- `ready-session-id`: the defect was pinned in place by a **green** test
  (`events.rs:1684` asserted the missing key).

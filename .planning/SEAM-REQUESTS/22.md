# Phase 22 — seam requests

Nothing in this phase edited a fenced file. Both entries below are BACKLOG rows.

---

## SR-22-1 — BACKLOG row (MEDIUM, non-blocking)

**File:** `.planning/BACKLOG.md`
**Insertion point:** append as a new row at the end of the existing findings table.
**Exact line to add:**

```
| F22-M1 | MEDIUM | wcore-agent | A journal written by a newer binary is reported to the user as `corrupt complete frame N`. It is not corrupt, it is newer. Measured 2026-07-26 in 22-01 Task 1 M3: the pre-change binary refuses a journal containing an unknown event variant with `JournalError::CorruptFrame`, exit 3, zero bytes of reduced state. Failing closed is correct; calling it corruption is not. `self-update` makes downgrade a one-command operation, so a user will hit this. Distinguish an unknown event variant from frame corruption in the error. | 22-01-JOURNAL-COMPAT.md §3 | open |
```

**What breaks if omitted:** nothing executes differently. The finding is lost, and
the next person to hit it re-diagnoses a false data-corruption incident.

---

## SR-22-2 — BACKLOG row (MEDIUM, non-blocking)

**File:** `.planning/BACKLOG.md`
**Insertion point:** append as a new row immediately after SR-22-1's row.
**Exact line to add:**

```
| F22-M2 | MEDIUM | wcore-agent | Once Goal/Task/Wait records land additively at v5 (authorized 2026-07-26, 4-of-4 panel), `SESSION_JOURNAL_SCHEMA_VERSION` no longer implies a content model: two journals both stamped 5 may or may not contain Goal frames, and anything needing that distinction must scan an append-only chain. Give it an explicit content-level capability marker rather than inferring it from the header. | 22-01-EVIDENCE/decision/panel-dissent.txt | open |
```

**What breaks if omitted:** nothing executes differently now. It becomes a silent
constraint on whatever later needs to ask "does this journal have Goal records".

---

## NOT a seam request, but the orchestrator should know

Phase 22 owns the `wcore-protocol` contract seam (CONTRACT_MINOR,
`contracts/desktop/v1/manifest.json`). **No bump is requested**, because plan
22-04 — which is the plan that would have produced the new commands, events and
D2 producer fixtures — was **not executed** in this session. When 22-04 is
executed the seam request will be real; right now requesting a bump would reserve
a contract change for work that does not exist.

# Seam requests — Phase 23A (governed skills)

Every item here is a change to a **fenced file** that phase 23A could not make itself.
Each is self-contained and ready to paste with no interpretation needed.

---

## SR-23A-1 — `.planning/BACKLOG.md`: three MEDIUM findings

**File:** `.planning/BACKLOG.md`
**Insertion point:** append as new rows to the existing findings table (or as a new
`## Phase 23A findings` section at end of file if the table shape differs).
**What breaks if omitted:** three measured MEDIUM findings are lost. None blocks any
phase; all three are the kind that becomes a HIGH after an unrelated refactor.

```markdown
| F23A-01-M1 | MEDIUM | wcore-agent | Skill-declared hooks are gated only by accident, never by an explicit quarantine check. `SkillTool::skill_hooks_for` (`crates/wcore-agent/src/skill_tool.rs:434-449`) authorises via `skill_is_authorized` (`:159`), which is `permission_for(skill) == Allow` — a permissions check that never consults `disable_model_invocation`. The route is unreachable today only because (a) `merge_skill_hooks_into` runs solely on a non-error tool result (`crates/wcore-agent/src/orchestration/mod.rs:3086`) and (b) `find_metadata_sync` reads the eager map and the LRU, which `resolve_for_model` never populates for a quarantined name (`crates/wcore-skills/src/refs.rs:293`). The cron pre-dispatch scan already warms that LRU via the unrestricted `resolve()` (`crates/wcore-agent/src/cron.rs:262`), removing condition (b). Any refactor that merges hooks independently of the tool result converts this into a live execution route with no test standing in the way. Suggested fix: add an explicit `disable_model_invocation` check in `skill_hooks_for`. | Phase 23A-01 census |
| F23A-01-M2 | MEDIUM | wcore-skills | The quarantine verdict is computed from two mutable files that live inside the quarantined directory itself: `is_generated_draft` (`crates/wcore-skills/src/loader.rs:463`) reads `<skill_dir>/manifest.json` and falls back to the exact released body shape matched by `crates/wcore-skills/src/draft.rs:40`. Breaking any of the four body-shape conditions with the manifest absent yields content the loader classifies as user-authored. Measured: the classifier half CONFIRMED (and it is deliberate — an `auto-` name alone must not quarantine user content). The tool-write-reachability half NOT ESTABLISHED — whether the agent's own Write/Edit tools can write under `$WAYLAND_HOME/skills/` is owned by `crates/wcore-tools/src/workspace_policy/`. Residual risk with a stated precondition; an actor who can already write there can author a plain user skill and skip the forgery entirely. | Phase 23A-01 census |
| F23A-01-M3 | MEDIUM | wcore-config | `restrict_untrusted_project_config` (`crates/wcore-config/src/config.rs:4351`) preserves an allowlist of power-reducing project settings and silently re-defaults everything else. `observability.skills_lifecycle` was missing from that allowlist and failed OPEN (fixed as F23A-01-H1). The CLASS was not swept: other restricting settings absent from the allowlist may fail open the same way. One setting was followed; the sweep was deliberately not expanded into that plan. | Phase 23A-01 census |
```

---

## SR-23A-2 — `.planning/BACKLOG.md`: one HIGH finding, open, not owned by 23A

**File:** `.planning/BACKLOG.md`
**Insertion point:** same table.
**What breaks if omitted:** a HIGH, deterministic, product-wide defect with a 13-second
reproducer is not tracked anywhere, and the three tests that reproduce it sit red with no
owner. This one genuinely matters — read it before the MEDIUMs.

```markdown
| F23A-01-H2 | HIGH | wcore-agent (session journal / tool dispatch) | **Any tool call that returns an error result kills the session.** The errored tool execution is left nonterminal in the session journal and the engine aborts with `Session persistence authority unavailable: invalid journal state transition: turn <id> has nonterminal tool execution <id>`. Rejecting check: `crates/wcore-agent/src/session_journal/reducer.rs:969-980`. The terminal append that should move it to `Failed` is `crates/wcore-agent/src/orchestration/mod.rs:2290` (`lease.fail(...)`) and looks correct on inspection, so the leak is between `PreparedToolLease::start` (`orchestration/mod.rs:2012`) and that append. Reproduced live on Linux at `2ecdfdf5` in 13s with THREE independent triggers: a `Skill` call for a name absent from the catalog, a `Skill` call for a quarantined generated draft, and a `Read` of a nonexistent path — the `Read` case proves it is NOT skills-specific. Also breaks the repository's own `packaged_driver_gate.rs` lifecycle matrix. Same class as Windows live-UAT defect D1. Reproducer committed RED at `crates/wcore-eval-scenarios/tests/f23a_boundary_drive.rs`. NOT fixed by 23A: the fix is in a subsystem Phases 21/22 are editing concurrently. Regression-vs-shipped attribution attempted and INCONCLUSIVE. | Phase 23A-01 live drive |
```

---

## SR-23A-3 — no CLI, protocol or contract change is requested

Phase 23A made **no** change to `crates/wcore-cli/src/main.rs`, `crates/wcore-cli/src/lib.rs`,
`crates/wcore-agent/src/lib.rs`, `crates/wcore-agent/src/bootstrap.rs`,
`crates/wcore-protocol/src/contract/generate.rs`,
`crates/wcore-protocol/contracts/desktop/v1/manifest.json`, or `.config/nextest.toml`,
and needs none. No `CONTRACT_MINOR` bump. No new subcommand.

Recorded explicitly so integration does not go looking for one.

**Note for whoever integrates 23A-02 later:** the governed promote transaction that plan
describes *will* need a `main.rs` seam, because `run_skills_promote` lives at
`crates/wcore-cli/src/main.rs:2408` and currently fails closed with
`"skill promotion is temporarily unavailable while governed promotion is being implemented"`.
23A-02 was not executed (see `23A-02-SUMMARY.md`), so that seam is not requested now.

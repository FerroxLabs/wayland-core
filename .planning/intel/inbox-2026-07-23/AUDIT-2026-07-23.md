# Plan Reconciliation Audit — 2026-07-23

Deep-dive verification of the whole plan against ground truth (git objects + on-disk
source + plan files) at working commit `be84bd2` / reconciliation HEAD `e1b207f`.
Method: parallel read-only audit agents; every SHA/tree checked with `git cat-file`,
every defect site read in source, no cargo. This document is the durable record;
corrections marked APPLIED are already in STATE/ROADMAP/REQUIREMENTS, corrections
marked → PLAN are folded into the native-repair successor plan.

---

## Part A — Phase 20 (complete)

### A0. Headline
Every source/tree/review SHA cited across 20-01…20-18 resolves to a real git object
(no fabricated or dangling SHAs). The load-bearing "20-14 deferred native" claim is
genuinely present. Two material accuracy problems were found and reconciled:
(1) my own same-day reconciliation **overstated the native-RED provenance** — corrected;
(2) the 20-08 repair lineage **prose-claims 4 independent reviews with no on-disk
schema-validated artifact** — 1 attested review actually exists.

### A1. Native provenance — CORRECTED (was overstated in the first reconciliation)
Ground truth from `20-18-SUMMARY.md` (+ my own `git diff`/source reads):
- The only recorded, tuple-authorized **20-18 ran RED 2026-07-22 against PRE-repair
  `5e665ec`** on hosted `windows-2022` + ephemeral macOS. Failures: wcore-sandbox
  **Windows COMPILE** error (windows_impl module-path debt — E0425 `reserve_output`/
  `probe_single_flight`/`BUFFERED_OUTPUT_LIMIT_BYTES`, E0423 private fields) + **macOS
  test-mapping** failure (`live_integrity.rs` is `#![cfg(windows)]` → 0 tests). NOT an
  AppContainer read/token failure.
- `6937ef6` (repaired successor) fixed those; 20-16 reviewed it CLEAN (native deferred);
  20-17 persisted a still-**PENDING** tuple; **no 20-18 has run against `6937ef6`**.
- The AppContainer read/token issue came from a separate **2026-07-23 OFF-PLAN diagnostic**
  on real Windows 11 (BRIEF §2 admits it "drifted from Ferrox discipline").
- On-disk `process.rs` is **NOT** the "drop deny-only SIDs" fix — it is an invasive
  diagnostic (full-token swap to `current_token`, `DISABLE_MAX_PRIVILEGE=0`, +90 diag
  lines) that **breaks the AppContainer boundary**. `storage.rs` `.write(true)` IS a
  clean uncommitted spike. → tree must reset to pristine `be84bd2`.
- Windows + macOS native are **UNPROVEN for `6937ef6`** on hardware; Linux 11509/0 is the
  reported Hetzner aggregate.
- **APPLIED** to STATE.md (Current Position, Last activity, blocker, Execution Authority)
  and ROADMAP.md phase-20 row.

### A2. 20-08 repair lineage — unattested reviews (MATERIAL) → PLAN
- `20-08`/`20-16` SUMMARY prose claim (a) "three fresh non-author adversarial reviews" of
  original `5e665ec` and (b) a "second independent adversarial confirmer"
  (`wayland-f20-16-adversarial-confirmer`) of `6937ef6`. **No schema-validated artifact
  exists for any of these 4.** `adversarial-confirmer` appears only in prose; the sole
  review JSON in the 20-08 lineage is `20-08-INDEPENDENT-REVIEW.md` = one
  `wayland-f20-16-repair-review` of `6937ef6` (CLEAN). Every other reviewed plan (06A/06B/
  20-14) has exactly one matching artifact per claimed review.
- Reality: the repaired candidate has **one** attested independent review, not five.
- → PLAN: the **fresh 20-16** in the native-repair sequence must emit a schema-validated
  artifact for EVERY claimed reviewer (no prose-only reviews). New requirement REQ-native-r13
  (see Part C).

### A3. 20-08 byte-identity framing — inaccurate (minor) → note
- SUMMARY frames the 11 core files as byte-identical `5e665ec`→`6937ef6`. Actual
  `git diff 5e665ec 6937ef6` = 21 files / 728 insertions incl. real source (`spawn_tool.rs`,
  `windows_impl/{handles,process}.rs`, `docker.rs`) and **two new macOS test files**. So
  `6937ef6` is a **genuine repair**, not a metadata re-point. The single attested review
  correctly reviews `6937ef6`. Framing should say "the 11 named lifecycle files unchanged;
  repair touched windows_impl + macOS tests + proof script."

### A4. Plan-doc consistency (minor) → PLAN/cleanup
- **20-02**: SUMMARY key-files lists 7 paths; PLAN `files_modified` declares 9
  (`acl_lease/sha256.rs`, `acl_lease/tests.rs` omitted from SUMMARY). Reconcile count.
- **20-06**: undocumented scope expansion 3→5 (added `Cargo.toml`,
  `directory_authority_file.rs`) with no `scope_expansion` frontmatter (unlike 20-05/20-07).
  Add the record or explain.

### A5. Native defect sites — CONFIRMED against source (feed the repair plan)
All BRIEF defect sites verified real at `be84bd2`:
- `storage.rs::create_new_nofollow` — `.write(true)` present, uncommitted (`M`). ✓ clean spike.
- `process.rs` — deny-only `sids_to_disable` still passed to `CreateRestrictedToken`; the
  "drop deny-only SIDs" fix is NOT applied; invasive diagnostic present instead (see A1). ✗
- `snapshot.rs:654` — `READ_CONTROL`/`WRITE_DAC` from `Win32::Security` (should be
  `Win32::Storage::FileSystem`); unfixed. ✓ matches BRIEF.
- `live_fs_acl.rs` `type_and_hold` (L118) — `choice.exe /T N /D Y`, exit-code assert. ✓
- `dispatch_smoke.rs:289` — `fs::rename` of open repo dir. ✓
- `hard_process_containment.rs` — Bubblewrap/Linux-only, no Windows Job-Object test. ✓
- `f20-native-windows-proof.ps1` — `windows-job-object`/`windows-hard-process-containment`
  targets wired to the Linux Bubblewrap tests. ✓
- **GAP**: the actual recorded 20-18 Windows failure was a wcore-**sandbox** COMPILE error
  (windows_impl module-path debt); `6937ef6` claims to fix it but it was **never
  native-verified**. R1–R12 emphasize the 07-23 AppContainer findings; the repair plan must
  ALSO explicitly re-prove the wcore-sandbox Windows compile + the macOS test-mapping fix
  (the true 07-22 failures). New requirement REQ-native-r14 (Part C).

### A6. Intel/controls + requirement mapping (Phase-20-adjacent) — mostly clean
- Traceability table omitted REQ-native-r1..r12 → **APPLIED** (row added; coverage note;
  footer date bumped to 2026-07-23).
- Controls CTRL-01..04/D1/D2 consistent + OPEN; ledger families intact; Hermes/OpenClaw
  UNPINNED/PENDING. ✓
- STATE↔ROADMAP internally consistent; all cited SHAs resolve; all 11 F00–F19 design docs
  exist. ✓

---

## Part B — Whole program (F00–F30)

### B0. Headline
**The program is fully mapped and internally consistent.** 74 distinct requirement IDs
(58 F-phase + 4 CTRL + 12 REQ-native), **zero orphans, zero duplicate mappings**; the
dependency graph is **acyclic** and the stated execution order is a valid topological sort;
milestone v1.0 mapping is consistent; F00–F19 baseline is cited identically
(`97e4491`/`8d27bd96`, PROJECT ✓ Validated) across ROADMAP/STATE/PROJECT. No fabricated
SHAs anywhere in the program. Only mechanical naming/staleness GAPs were found — all fixed
(APPLIED) or deferred to the correct planning boundary.

### B1. Phases 21–23 — VALID + MAPPED
Requirement IDs match ROADMAP↔REQUIREMENTS exactly (21:4, 22:7, 23:6), success criteria map
1:1 with no orphans, deps match the execution order, D1/D2 admission gates consistent with
`DESKTOP-PROTOCOL-CHECKPOINT.md`.
- **GAP → boundary:** 23A/23B has no `F23A-`/`F23B-` requirement IDs (coarse split inferable
  from prose + ledger CONT row). `/ferrox-discuss-phase 23` should formalize which F23-0x
  belong to 23A vs 23B before 23A planning. Non-blocking.

### B2. Phases 24–27 (bounded-parallel wave) — VALID + MAPPED
IDs match (5 each); each gates ONLY on Phase 23 (no in-wave cross-dependency → parallelism
intact); Phase 28 correctly the fan-in; Phase 26 carries the Hermes/OpenClaw migration
surface (F26-01).
- **GAP → boundary:** the "shared seams stay serial" rule is stated program-level but the
  four phases don't each enumerate which concrete protocol/schema/config/lock/fixture seams
  they touch. `/ferrox-plan-phase` for each of 24–27 should produce a seam-conflict map so
  bounded-parallel worktrees don't collide. Non-blocking.

### B3. Phases 28–30 + controls — CONFIRMED
28 fans in 24–27; 29→28; 30→29 with peer-trials + independent-review framing. Ledger 10
families + 8-state maturity vocabulary intact; **Hermes/OpenClaw UNPINNED / PENDING; no
silent parity claim anywhere** in the corpus.
- **GAP (naming) → APPLIED:** the literal `CTRL-01…04` IDs existed only in REQUIREMENTS +
  inbox/export; canonical ROADMAP/STATE/intel used only D1/D2/filenames, so an ID-based
  trace found nothing. FIXED: stamped `CTRL-01/02/04/03` into the three intel headers, added
  the CTRL-01 baseline-pin to ROADMAP Phase-21 admission + STATE Pending Todos, and added a
  coverage clarifying sentence (CTRL-01/03 are cross-cutting; CTRL-02/04 gate D1/D2).
- **Note:** `PLAN-V2-ADVISORY.md` cites two forensics docs in the SIBLING `waylandcore`
  repo (present, not broken) — audit trail depends on that repo surviving.

### B4. Whole-program traceability + graph — CONFIRMED (2 GAPs, both fixed)
- Coverage matrix: every F-req + CTRL + REQ-native maps to exactly one phase; 0 unmapped,
  0 dup; 58 F-reqs independently recounted = matches.
- **GAP → APPLIED:** ROADMAP Phase-20 `Requirements:` line omitted REQ-native-r1..r12 (stale
  vs REQUIREMENTS + STATE). FIXED (added with cross-reference).
- **GAP → APPLIED (this commit):** the R1–R12 traceability row was uncommitted working-tree
  only. Committed here.
- **Low-confidence note (left as-is):** STATE frontmatter `total_phases: 1` vs 11 declared
  phases — GSD convention (tracks the active phase set, not the full milestone roadmap), not
  asserted a bug.

---

## Part D — Disposition summary

**APPLIED (in the reconciliation commits):** native-provenance correction (STATE ×4 +
ROADMAP); traceability row + footer date + coverage clarifier; ROADMAP Phase-20 requirements
line; ROADMAP Phase-21 + STATE CTRL-01 admission gate; CTRL-ID header stamps ×3.

**→ NATIVE-REPAIR PLAN (new requirements r13–r15, Part C):** attested-reviews-only; re-prove
the actual 07-22 compile/mapping failures; reset the tainted tree to pristine `be84bd2`.
Plus 20-02 file-count + 20-06 scope-expansion doc reconciliation.

**→ PLANNING BOUNDARIES (not roadmap defects):** 23A/23B ID split (`/ferrox-discuss-phase 23`);
per-phase seam inventory for 24–27 (`/ferrox-plan-phase`).

**Net:** the plan is verified valid and fully mapped to milestone/phases/waves; Ferrox can
execute it phase-by-phase without deviation. The only substantive correction was the
native-RED provenance (my own first-pass error), now accurate.

---

## Part C — New requirements surfaced by this audit (for the native-repair plan)
- **REQ-native-r13**: The fresh 20-16 (and any review gate) must emit a schema-validated
  review artifact for EVERY claimed reviewer; no prose-only "reviews" may count toward a
  PASS. (Closes A2.)
- **REQ-native-r14**: The repaired candidate must re-prove, on real hardware, the
  wcore-sandbox Windows COMPILE fix and the macOS proof-harness test-mapping fix (the actual
  recorded 20-18 failures), not only the 07-23 AppContainer findings. (Closes A5 GAP.)
- **REQ-native-r15** (hygiene, hard gate): reset the working tree to pristine `be84bd2`
  (remove the boundary-breaking `process.rs` diagnostic) BEFORE any candidate build; the
  real "drop deny-only SIDs" fix is written fresh under the plan, not salvaged from the
  diagnostic edit. (Closes A1 tree-taint.)

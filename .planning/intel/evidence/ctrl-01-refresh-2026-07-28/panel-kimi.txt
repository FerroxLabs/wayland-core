• ROW=AUTH VERDICT=REACHED — The repair is genuinely live-proven on the shipped Linux binary: unconditional authority intersection at six spawner sites, PolicyGate dispatching from the same snapshot, fail-closed refusals with a delegated child taking real provider turns. That is exactly the evidence REACHED names. EFFECTIVE is barred: Criterion 3 still NOT MET (R8), zero Windows evidence at the repaired SHA, and four of six budget dimensions hold only by absence of a request channel — absence is not enforcement. The persisting `delegate_isolation -> unavailable` at 2ecdfdf5 is noted but contradicts effectiveness claims, not the REACHED promotion; no REOPEN needed at this level.
  ROW=TXN VERDICT=REOPENED — R6 applies squarely: the journal-CAS collision and the receipt-null checksum data loss were measured against the SHIPPED binary (3/8 Linux, 6/6 Windows), directly contradicting the EFFECTIVE standing. Repair evidence is Linux-only (6/6) with Windows journal-compat legs never taken, so the tri-family basis that earned EFFECTIVE no longer exists. Demote pending re-proof: current defensible level is REACHED.
  ROW=GOAL VERDICT=REACHED — Criterion 2 is clean REACHED-kind evidence: kill -9 mid-wave on the shipped release binary, lease revocation, outbox drain, 12/12/12 effects, falsified counting gate, reproduced on Windows — measured entirely out-of-process. That earns the family off SOURCE. Nothing higher: Criterion 1 failed on two of three surfaces, Criterion 3 failed (five engines, five termination styles, adapter never built), every F22 requirement OPEN, two capabilities still "runtime path unwired". R7 caps the family well below EFFECTIVE.
  ROW=GATEWAY VERDICT=REACHED — systemd-witnessed kill -9 recovery with a new pid, named drain of 12 pendings, exactly-once proven at an independent sink (failed first, then passed — the honest kind), and a severed client recovering a post-disconnect event over HTTP/SSE through the shipped binary. Textbook REACHED. EFFECTIVE is out of scope: Linux only, the macOS CI binary provably lacks the code, Windows unexercised, Criterion 5 untouched, phase NOT closed, nine channel adapters still non-idempotent (R8).
  ROW=NATIVE VERDICT=REACHED — Digest-bound CI release artifacts executed natively on all three families with sha256 assertion and a 651-cell zero-skip matrix is real packaged execution evidence, a genuine promotion from SOURCE. But 24 cells are RED at the certified candidate and the matrix was never re-run after the observability fix — you cannot claim aggregate outcome over cells still red at the pinned digest. No soak, no signed receipt, no adjudication, and the certification covers one commit that is not tip: PACKAGED_PROVEN is nowhere close, and EFFECTIVE fails on the unresolved reds.
  ROW=SUPPLY VERDICT=CONSTRUCTED — The strongest entries are measurement, not capability: a census (negative results), a red `cargo deny` verdict honestly recorded, and reproducibility that holds only accidentally via CI path coincidence. The byte-deterministic SBOM and the signed manifest/trust-root/ledger are real constructions proved end-to-end — but only against a throwaway key in a temp dir, never bound to a real trust root, never carried by an actual release. CONSTRUCTED is the ceiling; with nine HIGH census findings, an open reachable advisory, and rollback rehearsal nonexistent, anything higher would be laundering (R8).

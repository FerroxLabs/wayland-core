# HANDOFF — Wayland Core, 2026-07-29 (M1 kickoff)

Integration `plan/f20-unified-audit-repair` @ `45897347`. Supersedes `HANDOFF-2026-07-29.md`.
**Read `.planning/MILESTONE-M1.md` — it is the plan. `MILESTONE-RC.md` is superseded.**

---

## 0. Do this first

1. **19 finished lane branches are pushed and UNMERGED**, held behind one CI gate. Merge them as a
   train once CI is green. **`ci-selfhosted-mac` first** — it routes the arm64 build to Sean's Mac.
   **Cancel each lane's leftover CI runs while merging** (see §5).
2. **Nothing pushes to integration while a CI run is live**, including `.planning/`. A push spawns a
   competitor run.
3. Then start M1 Tier 1.

Branches: `release-trust-root · ci-selfhosted-mac · 24-native-actions · e2e-product-smoke ·
open-highs · 23a-c1-governed · flaky-root-cause · 30-dialect-c2 · grade-23a · grade-23b · grade-24 ·
grade-25 · grade-26 · regrade-audit · support-bundle · journey-rescue · 27-browser-voice ·
27-media-intake · 26-import · recon-grok · recon-openclaw`

---

## 1. What this session established

**Every phase is now graded.** Five (23A, 23B, 24, 25, 26) had **no verdict file at all** — never
graded in either direction. That was the programme's core failure: not bad measurement, **missing
aggregation**. A grading lane put it best:

> *Every gap I found was already disclosed somewhere in the phase's own evidence. The lanes measured
> honestly and graded themselves generously. What was missing was anyone reading the disclosures
> back against the criterion text.*

| phase | verdict |
|---|---|
| 20 / 20A | COMPLETE |
| 21 | not achieved — C3 |
| 22 | NOT ACHIEVED — C1 one surface, C3 untouched |
| 23A | NOT MET — **one merge away** |
| 23B | NOT ACHIEVED — 1 met / 2 partial / 3 not met |
| 24 | NOT ACHIEVED — **on one verb**, now closed |
| 25 | 1 MET / 1 MET-exc / 2 PARTIAL |
| 26 | PARTIALLY ACHIEVED |
| 27 | NOT ACHIEVED — 0 of 5 |
| 28 | 3 of 4 MET |
| 29 | 4/4 PARTIAL |
| 30 | C1/C2 NOT MET — position not statable |

**`0 of 25` prior grades were instrument artifacts.** The bad grades are bad product; nothing gets
dropped on a technicality.

**Phase 24 stopped being the release blocker on 2026-07-28.** The label survived because nobody
graded the phase. The real blocker was `support_bundle` — zero production call sites — **inside the
one criterion the ledger recorded as MET**. Closed this session (Linux).

---

## 2. Landed this session (all unmerged)

Vision end-to-end · support bundle + CLI verb · governed skills promote/revoke/rollback ·
media bounds load-bearing across 9 adapters · native actions on 6 adapters × 3 affordances ·
msteams attachments · reconnect upstream half (lost=0, duplicated=0) · matrix restart loss ·
release trust root + `release.yml` signing · **migration import 4 files → 1,773** ·
12-of-14 cold-start E2E · CI unstarved · multi-day journey rescued · flake root-caused.

---

## 3. Corrections to the record — do not re-inherit

- **`AGENTS.md` was false.** Said "Rust 2021"; `Cargo.toml:156` says **2024**. Crate map lists ~18 of
  **56** members. Fixed this session; verify against `Cargo.toml`, never against the doc.
- **The nextest "flake" was never real** — fd exhaustion, **40 runs, zero real test failures**. A
  green can be trusted. Classify any future red by `TRY 1 XFAIL` vs `TRY 1 FAIL`.
- **The anti-vacuity guard is redundant, not missing** — fail-closed-on-zero-tests is nextest
  0.9.137's built-in default. The real hazard is bare `cargo test` (`BL-F28-VACUOUS-GREENS`).
- **Voice was never hardware-blocked** — Sean's Mac has a microphone and is a registered runner.
- **macOS/Windows work is not blocked** — `sean-mac-arm64` + two Windows runners + **Tailscale to
  SeanDesktop**. Any lane claiming a platform is unreachable is wrong.
- **Prior grok recon exists** (`WAYLAND-INTEGRATION-AUDIT.md`, 662 lines, Sean's) and never reached
  the ledger.

---

## 4. Rules earned this session

1. **A known-negative assertion is self-passing on a dead instrument.** Five instances found today,
   including a redaction proof where `grep -c` on a **missing file** returned `0` — and `0` was the
   success value. **It had already fired** on a run where the export failed.
2. **Assert the artifact exists and is non-empty before asserting what is not in it.**
3. **Instruments produce false positives too.** Two lanes caught their own harnesses filing defects
   against *correct* product behaviour — one a false security escape.
4. **§3b-ii:** `/root/.wayland/.env` on hetzner injects `ANTHROPIC_API_KEY` regardless of shell
   `unset`. Read provider selection back from the product's own output.
5. **Grade against the criterion text, not your own scope.**
6. **Challenge every "blocked".** ~10–14 sessions were mis-costed as blocked when they were just
   work.

---

## 5. The merge procedure (fixes a 4h20m loss)

A merged lane **leaves its CI run alive**, and that run keeps re-acquiring macOS runners — one held
them for **10.5 hours**. When merging, cancel the lane's runs. Filter on **both** `queued` **and**
`in_progress`; filtering only on `queued` is what missed it.

---

## 6. Sean's, and only his

Merge to main · tag / publish (**second contract regeneration is the LAST action before it**) ·
core#254 reply · close #142 · macOS CI dispatch — **the only credential-gated item left**.

Trust root minted and landed. `WAYLAND_RELEASE_ACCEPTANCE_SEED` is set. `self-update` installs
nothing until a real release publishes a manifest — that needs a tag.

---

## 7. Scope filter (Sean's, 2026-07-29)

**Cut ~48 sessions as gimmick or scorecard:** native iOS/Watch/Android, provider breadth, 26
channels, music/video generation, **all Phase 30 parity trials**, 24-C2 webhook+poll.

**Consequence, accepted:** no citable parity number. A number doesn't stop a customer leaving.

**Kept:** everything that is a **lie or a crash**. ~30 sessions, in `MILESTONE-M1.md` §4.

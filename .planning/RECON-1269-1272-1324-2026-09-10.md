# Reconciliation record — wayland#1269, #1272 (c2/c3/c4), #1324

Built 2026-09-10 on `w14/recon`, based on `2fe7a70df`. RECORD work only: no ref
was created, moved or deleted, nothing was merged, no issue state was changed,
and no build was run (all three Linux build slots were occupied by other lanes,
and cargo is banned on this host). Every SHA below was resolved in this
worktree; every issue state was read from the tracker on 2026-09-10 and is
dated as such, because an issue state is a measurement with a timestamp.

Where a criterion needs something this lane could not do, that is said here in
the criterion's own words rather than replaced with an adjacent property that
happened to be checkable.

---

## wayland#1269 — six unmerged `lane/f13-*` branches

### Method, and why the obvious method is wrong

`git show HEAD` on `lane/f13-n-dataloss` shows only ledger files: `4eed60a4b`
is a docs commit sitting on four earlier ones. Plain ancestry is also useless —
none of the six is an ancestor of `2fe7a70df` — because 0.13.12 was integrated
as ONE squash, `93ede3424` ("release: 0.13.12 — journal float round-trip fix,
CI flake accounting, ledger re-anchor (#417)"), which discards the branch
ancestry while carrying the content.

So each branch is graded from its FORK POINT off `gh/integ/f13` to its head,
and then each distinctive artifact of those commits is looked for by name in
the tree at `2fe7a70df`.

### Terminal-state table (heads and fork points resolved 2026-09-10)

| branch | head | fork point | own commits | where its content is now |
|---|---|---|---|---|
| `lane/f13-n-ci-evidence` | `ee1c95157afcf4698e6e8543a911c859d1dc3271` | `8b6920fa7` | 1 | INTEGRATED via `93ede3424`, superseded design |
| `lane/f13-n-dataloss` | `4eed60a4b69a2fa3442f7f379da7c258ce85782f` | `e7cb6679b` | 5 | INTEGRATED via `93ede3424`, 3 tests unported |
| `lane/f13-relgate-ci` | `000db814eea27c202b60b8f05afcc4b9d02ba05d` | `8464eeae2` | 1 | INTEGRATED via `93ede3424` |
| `lane/f13-serde-float-roundtrip` | `715781e00dbc976aa39c25cd3206e90e39601242` | `4c3e2cd50` | 1 | INTEGRATED via `93ede3424`, unmeasured |
| `lane/f13-n-security` | `228c5c4d29db2391235df1c31e5d945810bb8474` | `e7cb6679b` | 6 | independent reimplementation in tree |
| `lane/f13-u-protocol` | `5a66afffed527936f12ebae1ddc4fd0ebb31bf5b` | `8b6920fa7` | 5 | independent reimplementation in tree |

### Per-branch findings

**`lane/f13-n-ci-evidence`** — its whole claim on the issue is "adds 4 `not-met`
criteria with zero `met` removed". Those four are `.planning/ledger/wayland-1235.md`
c1–c4, a file the branch created. That file is in the tree, landed by
`93ede3424`, and now reads `status: closed` with c1–c5 all `met`; wl#1235 is
CLOSED/COMPLETED on the tracker (2026-08-31T13:36:19Z). The finding was not
lost — it was integrated and then resolved. Two artifacts of the branch never
landed and were superseded by a different design: `.github/scripts/reserve-attempt-evidence-tree.sh`
(98 lines) and `.config/env-global-helper-baseline.txt` (36 lines); the tree
carries `.github/scripts/tests/assert-test-evidence.test.sh` and
`.config/env-global-helper-debt.txt` instead. Disposition: REDUNDANT.

**`lane/f13-n-dataloss`** — all three production fixes are in the tree, all via
`93ede3424`, each in a refined form:
`ReasoningFilter::finish` (`crates/wcore-types/src/reasoning_filter.rs:231`),
the `restore()` `Swap` discriminant fix (`crates/wcore-config/src/atomic_io.rs`,
which now returns `Result<Option<PathBuf>>` and errors on `Swap::Vacant` /
`Swap::Unsupported`), and `install_consent_doorbell`
(`crates/wcore-agent/src/egress/bridge_doorbell.rs:148`). Its three ledgers —
wl#908, wl#1155, wl#1180 — are all `status: closed` at `93ede3424`.
RESIDUAL, and it is real: three tests the branch adds are absent from the tree
by name — `a_prose_mention_of_a_reasoning_tag_survives_in_history`
(`crates/wcore-agent/tests/reasoning_tag_history_test.rs` exists with 3 tests,
not 4), `a_json_stream_egress_consent_prompt_is_put_on_the_wire` and
`a_sink_that_cannot_prompt_is_never_given_a_doorbell`. Disposition:
INTEGRATED WITH TEST RESIDUAL.

**`lane/f13-relgate-ci`** — its 55 added lines put the release-readiness gate on
the release path and its self-test in CI. Both are in the tree:
`.github/workflows/release.yml:167-168` runs `--self-test` then the gate, and
`.github/workflows/ci.yml:1976-1978` runs the self-test. Both landed at
`93ede3424`. Disposition: REDUNDANT.

**`lane/f13-serde-float-roundtrip`** — see c2 below. Its one line is in the tree
at `Cargo.toml:236`, landed by `93ede3424`. Disposition: INTEGRATED, MEASUREMENT
NEVER TAKEN.

**`lane/f13-n-security`** vs `wayland-core#356` — see c3.
**`lane/f13-u-protocol`** vs `wayland-core#366` — see c3.

### c1 — NOT MET

Four dispositions are now determined and evidenced above, which is what the
record could supply. The criterion's own words are "merged after an adversarial
pass, archived with the reason recorded, or filed as its own ticket. None is
left as a bare branch" — and all four are still bare `lane/f13-*` refs. This
lane changes no refs by instruction, so the act is owed, not the analysis.
Owed, exactly: rename `lane/f13-n-ci-evidence`, `lane/f13-relgate-ci` and
`lane/f13-serde-float-roundtrip` to `archive/f13-*` citing this record; for
`lane/f13-n-dataloss` either port the three named tests (a build, so a Linux
slot) or file them as their own ticket before archiving.

### c2 — NOT MET

The criterion says "regenerate and key-diff, confirming whether `schema_digest`
moves". No regenerate was run: that needs a build and no slot was free. What
the tree can say without one, and it is worth having:

* The branch's own manifest is byte-identical to its fork point in all three
  keys — `schema_digest` `sha256:47e255b800cb3…`, `fixture_digest`
  `sha256:13042cacdedf3…`, `source_inputs_digest` `sha256:9c277ef250161…` at
  BOTH `4c3e2cd50` and `715781e00`. This is NOT evidence the change is inert:
  the branch is one commit touching only `Cargo.toml`, so it proves only that
  nobody regenerated on it.
* `source_inputs` in `crates/wcore-protocol/contracts/desktop/v1/manifest.json`
  is a 43-entry list, every entry under `crates/wcore-protocol/src/`. The root
  `Cargo.toml` is not in it. So `source_inputs_digest` cannot move by
  construction. That is one of the three keys settled without a build.
* `fixture_digest` is NOT settled. 24 of 156 corpus fixture files carry a
  non-integer numeral, over 14 distinct values (`0.0`, `0.0125`, `0.1`, `0.12`,
  `0.875`, `1.0`, `1.23`, `1.25`, `1.7`, `2.0`, `2.5`, `5.0`, `87.5`, `127.0`).
  Float rendering is therefore in scope for the fixture bytes and a regen is
  the only honest oracle.

Owed, exactly: on a capable Linux host, regenerate the Desktop corpus at a tree
with `float_roundtrip` and at one without, and key-diff `schema_digest`,
`fixture_digest` and `source_inputs_digest`. Note the awkward part plainly —
the feature is ALREADY in the shipped tree, so this measurement is now a
post-hoc check of a merged change, not the pre-merge gate the criterion asked
for. That cannot be undone by wording.

### c3 — NOT MET, but its precondition is satisfied and the comparison is done

Both owning issues landed: `wayland-core#356` CLOSED/COMPLETED
2026-08-31T13:37:05Z and `wayland-core#366` CLOSED/COMPLETED 2026-08-31T13:37:13Z
(read 2026-09-10). The comparison the criterion asks for:

* `wayland-core#356` — `.planning/ledger/wayland-core-356.md` is `status: closed`
  with c1–c4 all `met` at `93ede3424`; `wayland-core-244.md`, `-314.md` and
  `-366.md` likewise. The tree implements the same subjects independently of
  `lane/f13-n-security`. Measured overlap: both the tree and the branch carry
  `crates/wcore-tools/tests/grep_vcs_store_deny.rs`, but they are DIFFERENT
  files with different test names. The tree covers one-level-up, direct, named
  loose object, nested, and ordinary-file-still-searchable. The branch covers
  five cases the tree does not name: `.svn` pristine content, the absolute
  spelling of the control dir, a symlink alias for the control dir, a vendored
  checkout's control dir, and the context-free entry point. The branch also
  carries `crates/wcore-tools/tests/git_secret_content_test.rs` (230 lines) and
  `crates/wcore-tools/tests/workspace_policy_resolver_class.rs`, neither of
  which exists in the tree at all, and a GitTool `withhold_secret_diff_sections`
  / `refuse_secret_path` mechanism the tree does not have — the tree instead
  guards GitTool through `SecretDenyFs` (`crates/wcore-tools/src/git.rs:733-761`).
  Whether those two mechanisms are behaviourally equivalent for `git diff` and
  `git blame` pointed at a committed secret is NOT settled by reading. The
  branch's own test file is the instrument, and running it is a build.
* `wayland-core#366` — the tree implements the unscoped sweep
  (`crates/wcore-exec-backend/src/contract.rs:340-361`, per-backend "no unscoped
  enumeration exists" declarations in `local.rs:342` and `ssh.rs:468`,
  conformance at `conformance.rs:380`), and d1–d6 are all `met`. The branch's
  test file is `container_orphan_sweep.rs`; the tree's is
  `container_orphan_scan.rs`. Independent reimplementation, same subject.

So: nothing either branch carries is needed to hold #356/#366 met. Both are
still bare branches, and one genuine coverage question (the GitTool secret
policy) remains unresolved without a build. The criterion asks for merge-or-
archive-with-the-comparison-recorded; the comparison is recorded here, the
disposing act is not done. Owed: run `git_secret_content_test.rs` against the
tree on a Linux slot; if it passes, archive both branches citing this section;
if it fails, that is a live security gap in `wcore-tools`, not a branch chore.

### c4 — MET, with a single-point-of-failure that should be said out loud

All five archived heads resolve to commit objects with full trees in this
worktree on 2026-09-10:

`01b2ac64c4c5d02110c3e3bc15d20f96135e1179` archive/f13-atref-guard · `20f0a060deaea69a20b3a15c1d7489f830e0e1da` archive/f13-n-atref · `6a9906cd4b16bea6da46d8aaeba6a9af860e9c21` archive/f13-n-cache · `b1e925473377a2186ea4f1f0a74da32e9569853d` archive/f13-n-mcp-prov · `760f4f9a20799187b102d6eb23eca3e6fe959149` archive/f13-n-win-escape

Each matches the SHA the issue body records. `git ls-remote hz` on 2026-09-10
returns all five at those exact SHAs from `ssh://hetzner-dsm/root/w-f13/integ`.
Tag `v0.13.12` exists and the tree is at `0.13.14`, so 0.13.12 has shipped:
the criterion's window closed and was honoured. Nothing was deleted by this
lane; no ref was touched.

The limit, measured not assumed: `git ls-remote gh` on 2026-09-10 returns NO
`archive/f13-*` ref at all, and does not return `lane/f13-relgate-ci` either.
It returns the other five lane branches. So the only network-reachable copy of
all five archived heads AND of `lane/f13-relgate-ci` is one Hetzner working
repository at `/root/w-f13/integ`. Local object stores in fetched clones are
the only other copy, and they are not a backup. Recoverable today; one `rm -rf`
from not being. If those heads are meant to outlive the box, push
`archive/f13-*` and `lane/f13-relgate-ci` to `FerroxLabs/wayland-core` — a
deliberate act, and the mirror image of the deliberate act c4 protects against.

---

## wayland#1272 c2 / c3 / c4

`c1` is out of scope for this lane and its ledger block is left byte-identical.

### c2 — NOT MET. Tranche 3a is 5 of 6.

Tracker states read 2026-09-10:

| issue | state | closed |
|---|---|---|
| wayland#1231 | CLOSED/COMPLETED | 2026-08-31T13:36:13Z |
| wayland#1252 | CLOSED/COMPLETED | 2026-08-31T13:36:30Z (decomposed: wayland#1276 is its c3 split, OPEN, ms 0.13.14) |
| wayland#1254 | CLOSED/COMPLETED | 2026-09-04T16:58:18Z |
| wayland#1256 | **OPEN** | — |
| wayland-core#400 | CLOSED/COMPLETED | 2026-08-31T13:37:45Z |
| wayland-core#393 | CLOSED/COMPLETED | 2026-08-31T13:37:37Z |

wayland#1256 is neither CLOSED nor DECOMPOSED. Its ledger is `status: open`
with c1 `met`, c2 `met`, c3 `not-met`, owner `core`, no `handoff:`, and its own
note says so without hedging: c1/c2 close the corpus instance totally and do
not close the class, and "this criterion is the reason this issue stays open".
The residual is exactly: a lane still cannot be prevented from reporting a tree
green while a crate it chose not to run is red. Two candidate closures are
named there and neither is decided. Administrative closure of #1256 would make
c2 read met and would be a lie about a live gap, so c2 stays not-met and names
#1256 c3 as the whole of what is owed.

### c3 — MET. All nine residuals are dispositioned.

Six are CLOSED/COMPLETED on the tracker (read 2026-09-10):
wayland-core#382 (2026-08-31T13:37:30Z), wayland-core#389 (2026-08-31T17:04:18Z),
wayland-core#369 (2026-08-31T13:37:16Z), wayland#908 (2026-08-31T13:36:45Z),
wayland#1203 (2026-08-31T07:24:02Z), wayland#1150 (2026-08-31T15:33:11Z).

Three are OPEN and each carries the record the criterion demands, in the ledger
FIELD rather than in prose:

* `wayland-core#368` — c1–c5 all `state: blocked`, `owner: maintainer`,
  `handoff: "FerroxLabs/wayland-core#410"`; c6 `met`. core#410 is OPEN,
  milestone 0.13.12, titled "[Maintainer] AppContainer categorical-deny
  disposition". This is the "explicitly recorded as a decision that is not
  core's to take" arm, and the owner field says maintainer, not core.
* `wayland#559` — c4 `state: blocked`, `owner: desktop`,
  `handoff: "FerroxLabs/wayland#1193"`; c1,c2,c3,c5,c6,c7 all `met`.
  wayland#1193 is OPEN. Decomposed with a handoff.
* `wayland#388` — c4 `state: blocked`, `owner: flux`,
  `handoff: "FerroxLabs/wayland#1184"`; c7 `met` with
  `handoff: "FerroxLabs/wayland#1237"` (CLOSED/COMPLETED, so that leg is done);
  the other ten criteria `met`. wayland#1184 is OPEN. Decomposed with a handoff.

Every handoff target was checked to exist and to be OPEN where it still carries
work. None of the three is blocked on core by core, which is the failure mode
the criterion exists to catch.

### c4 — NOT MET. The #1203 half is discharged; the sweep half is not.

`wayland#1203` c3 is `state: superseded`, `successor: FerroxLabs/wayland#1244`,
`handoff: FerroxLabs/wayland#1244`. wayland#1244 is OPEN, milestone 0.13.14,
titled "The live /model-switch leg of the spend-audit keying proof needs a PTY".
Two of the criterion's three legs were measured LIVE on hetzner with the
shipped debug binary against a loopback OpenAI-compatible fixture — a fresh
launch and a `--resume` wrote three task records to
`~/.wayland/budget/spend-audit.jsonl`, all keyed `20244249fc21`. The third leg
is not reachable headlessly because `AgentEngine::rebind_provider`'s only
production callers are the TUI engine bridge and the `--no-tui` REPL registers
no `model` handler; it is restated on #1244 as a PTY-driven run. That is
precisely "measured on real hardware, or restated as what the tree can grade",
for both halves of the split. This clause is discharged.

The clause that is not is "and any other criterion requiring a live run".
Sweeping every ledger record for a criterion whose text names a live run, a
PTY, real hardware or a soak, and whose state is not `met` or `superseded`,
returns eleven — one of which is this criterion itself:

| criterion | state | owner / carrier |
|---|---|---|
| wayland#1228 c1 | blocked | external credentials home |
| wayland#1244 c1 | not-met | PTY run, ms 0.13.14 |
| wayland#1244 c3 | not-met | PTY red arm, ms 0.13.14 |
| wayland#1309 c1 | not-met | core |
| wayland#305 c4 | blocked | desktop |
| wayland-core#386 c1 | not-met | nightly-windows-soak |
| wayland-core#386 c2 | not-met | nightly-windows-soak |
| wayland-core#386 c3 | not-met | already carries the restatement escape hatch in its own text |
| wayland-core#410 c1 | blocked | maintainer |
| wayland-core#411 c1 | not-met | core |

None is orphaned — each sits on a ticket with an owner. But none has been
graded on the question this criterion actually asks, which is not "is it
owned" but "can it be run at all". Owed, exactly: for each of the ten, one
line saying either "runnable on <named host>" or "not runnable, restated as
<what the tree can grade>". That restatement belongs on the ISSUE first, not in
a ledger note — sharpening a criterion inside its ledger is how a criterion
quietly becomes an easier adjacent property, which is this ledger's own stated
rule.

---

## wayland#1324 — programme reconciliation

Sources: `FINAL-PLAN.md` (W00 plus W01–W17) and `evidence/execution-progress.json`
(`updated_at` 2026-09-10), both under
`/Users/seandonahoe/dev/waylandcore-stabilization-20260905`. No W18 and no new
architectural scope is proposed here; this is a reconciliation of what exists.

### Package status against the single execution record

`status_reconciliation` (dated 2026-09-07) states `accepted_packages: 12`,
`total_packages: 17`, `published: False`. `completed_execution_packages` lists
W01, W02, W03, W04, W05, W06, W07, W08, W09, W10, W11, W14. `current_package`
is W05.

Five packages are therefore NOT accepted, and the record says why in its own
words:

* **W12** — "Release gate code integrated locally at `a03a19e3ff7e784b4c12d2b02243da0bac3caba4`;
  3 integrated self-tests PASS. Actual final evidence/promotion pending; no
  third source audit, not whole package accepted." `W12_final_mutants` attempt 2
  is 8/8 caught with 16 complete captures and all digests verified
  (`evidence/w12-final-mutants/attempt2/index.json`,
  sha256 `e92a2bbf97171f6860c5e3b39bef462d985a0bb66c90a9eb462af0f987038cdc`) —
  that is a real result for five named pairs and is NOT proof of nightly
  five-crate mutation coverage.
* **W13** — integrated at `8b40bff53` with 42 combined checks and strict lint
  PASS; the foreign consumer receipt (Desktop fixture pin compatibility and
  full Flux metadata/retry) is outstanding.
* **W15** — integrated at `8b40bff53`; remaining journey gaps recorded explicit.
* **W16** — "Actual comparative trials / 1000 cycles / 2h soak still pending;
  not package accepted." See below.
* **W17** — only `W17_model_limits` exists, `state: committed-awaiting-parent-verification`
  at commit `56b19046cadd7f30955eaad2ba582c111ff71342`. No W17 acceptance.

One row does not reconcile with itself and is flagged rather than smoothed
over: **W14 is in `completed_execution_packages`** while the `W14` field reads
"Round 1 non-pass qualified: SYSTEM AppContainer mutex duplicate ACE
precondition; same frozen CLI under Administrator reports available true. No ACL
code change. Console controls PASS; final round held for frozen combined
candidate, VM retained." A package whose own final round is held is not an
accepted package. Either the acceptance list is wrong or the W14 field is
stale; one of the two must be corrected before c1 can be graded met.

### The W16 memory result — FAILED, and it stays failed

Two runs, both recorded `TERMINAL_FAILED_RSS_ACCEPTANCE`:

* `W16_mixed_soak` run 2, source `3530199fb757f3fcefecf9e40ba820775b867d84`:
  8938 cycles, 7213.781675985083 s, RSS growth **5,277,444,096 bytes against a
  127,151,104 limit** — 41x over. `rounds_remaining: 0`. Cleanup clean, sampler
  off, zero paid calls.
* `memory_confirmation2h`, source `6b7da6a0f0b1bd19a8a9326c9da1c15d15d037c7`,
  binary sha256 `9fc61db03bb74b1ff71746f00e95fdc84b9d03ddce0dc9ccfe54fa99f4723ab3`:
  8897 cycles, 7205.694144072011 s, RSS growth **61,534,208 bytes against a
  33,554,432 limit** — 1.83x over. Deterministic local HTTP fixture, 0 model
  calls.
* Alongside it `memory_latency_disposition` records candidate p95 6262.826 ms
  against an original limit of 6219.149 ms — 10.77% against a 10% limit, and
  its own field says "original result remains FAIL".

`memory_release_disposition` records the user's answer to "I think we are good
to proceed and release": "User accepts documented residual memory growth as
release tradeoff; original test failure preserved", scoped explicitly to "no
claim flat memory / unbounded leak resolved".

That disposition is a decision to ship with a known failure. It is not a
measured pass, and nothing in this record converts it into one. The numbers
above are the acceptance result; the disposition sits beside them, not over
them.

### The gap that has no carrier

Searched both trackers on 2026-09-10 for an issue carrying the W16 memory
result — "RSS growth soak", "memory growth", "two-hour soak", open and closed,
across `FerroxLabs/wayland` and `FerroxLabs/wayland-core`. Zero hits in every
arm. The control query ("AppContainer", open, wayland-core) returned
core#410 and core#368, so the search works and the empty result means absence
rather than a broken query.

So the largest unmet acceptance result in the programme — a 41x RSS overshoot
on one run and a 1.83x overshoot on the confirmation, plus a 10.77% latency
regression — exists ONLY inside `evidence/execution-progress.json` and its
receipts. It has no ticket, no owner, no milestone, and therefore no way to
reach the release gate, which counts ledger files and nothing else.

`W16_mixed_soak.next` says "Await pending bounded memory repair decision" and
`scope_decision_pending` says "requested bounded memory investigation repair vs
retain blocker; no threshold change". That decision was never recorded as
taken. This is the exact disposition owed by #1324 c2.

Owed, exactly, and it is one act: file a `FerroxLabs/wayland-core` defect
carrying the two soak results verbatim — cycles, seconds, RSS growth, limit,
source SHA, binary sha256 and receipt path for each — plus the 10.77% latency
figure, with the user tradeoff recorded as a disposition on that ticket rather
than as an absence of one. Then `wayland-core#368`-style `handoff:` the #1324
c2 residual to it. Filing tickets is not this lane's to do unasked, so it is
named here and left owed.

### c1 — NOT MET

12 of 17 packages accepted; W12, W13, W15, W16 and W17 are not, each for the
reason quoted above; and the W14 row contradicts itself. The criterion asks for
exact identity, fail-before/pass-after controls, executed commands and counts,
cleanup, current integration evidence AND an independent review for each
applicable package. Partial package evidence does not discharge a
whole-programme criterion, and neither does the fact that a release went out.

### c2 — NOT MET

`wayland#1323` (Desktop, OPEN, ms 0.13.14) and `wayland-core#410` (maintainer,
OPEN, ms 0.13.12) are correctly recorded as external carriers, and
`wayland-core#368`'s ledger hands its five criteria to #410 by field. The
criterion fails on the W16 memory result, which is unmeasured against its own
limit, is externally dispositioned by a user tradeoff, and has NO owning
carrier on either tracker. "Visibly incomplete with their owning carriers"
requires a carrier. There is not one.

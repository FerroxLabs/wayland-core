{
  "_what": "Wire-shape fixture for the public, unauthenticated GET /openapi.json endpoint.",
  "_why": [
    "On 2026-07-29 the utoipa 4->5 bump (taken to remove RUSTSEC-2024-0370 at source) moved",
    "this document from OpenAPI 3.0.3 to 3.1.0. That is a wire-visible change to a PUBLIC",
    "endpoint, and it changed the SHAPE, not only the version string: nine nullable fields",
    "moved from 3.0's `nullable: true` to 3.1's `type: [T, \"null\"]`. A client pinned strictly",
    "to 3.0.x cannot read the 3.1 encoding.",
    "Nothing outside the emitting crate could fail on that. The only assertions that existed",
    "were version-PREFIX checks living in the same crate as the emitter, and they were edited",
    "from \"3.0\" to \"3.1\" inside the very commit that made the change.",
    "This fixture is the external artifact. Changing the emitted version or the nullable",
    "encoding now requires a deliberate, reviewable edit to THIS file."
  ],
  "_regenerating": [
    "If a test fails against this fixture, do NOT edit the file to make it green until you have",
    "answered: is this a wire change our consumers can absorb? Record the answer, then update.",
    "Measure with: GET /openapi.json off a running `wayland-core acp serve`, then scan for",
    "`type` arrays containing \"null\" and for `nullable: true`."
  ],
  "_measured": "2026-07-29, live curl against `wayland-core 0.12.25` acp serve on hetzner-dsm; 18286 bytes, 8 paths, 10 operations.",

  "_amended": [
    "2026-08-29 (wayland#998 c6): one site ADDED -",
    "/components/schemas/McpToolSelection/properties/allowed_tools.",
    "Can our consumers absorb it? Yes. It is a NEW optional schema, reachable only from the",
    "new optional `mcp_servers` array on SessionCreateRequest, which is itself omitted from",
    "the serialized request when empty (`session_create_without_mcp_selection_is_byte_identical`).",
    "No existing path, operation, schema or property changed shape, and no field became",
    "nullable that was not. A client that ignores unknown schemas is unaffected; a client that",
    "validates strictly sees an added definition, never an altered one.",
    "The nullable ENCODING is unchanged: the new site uses the same 3.1 `type: [T, \"null\"]`",
    "form as the nine already listed, so `nullable_keyword_sites` stays empty."
  ],

  "openapi_version": "3.1.0",
  "nullable_encoding": "type-array",

  "type_array_null_sites": [
    "/components/schemas/AgentInfo/properties/description",
    "/components/schemas/ApprovalResolveRequest/properties/answer",
    "/components/schemas/ApprovalResolveRequest/properties/prefix",
    "/components/schemas/ApprovalResolveRequest/properties/resume_token",
    "/components/schemas/McpToolSelection/properties/allowed_tools",
    "/components/schemas/SessionCreateRequest/properties/agent",
    "/components/schemas/SessionCreateRequest/properties/model",
    "/components/schemas/SessionCreateRequest/properties/system_prompt",
    "/components/schemas/SessionCreateResponse/properties/model",
    "/components/schemas/SessionMetadata/properties/model"
  ],

  "nullable_keyword_sites": []
}

# Task

Build the link shortener described in `SPEC.md`, in this directory, starting
from nothing.

It has to actually run. Start it and exercise it yourself before you call it
done — someone is going to launch it from `serve.json` and hit every endpoint.

Here's a little repo I use for unit conversions.

It converts Fahrenheit to Celsius but not the other way round, which is the
direction I actually need. Can you add `celsius_to_fahrenheit` next to the one
that's already there, round it to 2 decimals the same way, and make sure
`from unitkit import celsius_to_fahrenheit` works?

There are tests in `tests/` — please leave them green.

[tokens]
algorithm = sha256
ttl_seconds = 2592000
secret = dev-only-not-a-real-secret
# Tokens minted by tokenlib 1.x were signed with sha1. Keep accepting them
# until every session from before the 2.0.0 deploy has expired (30 days).
legacy_algorithms = sha1
